# BRG-007 实现报告：Native 回调重入安全与 Session Generation 队列管理

## 结果
- 状态：Accepted
- Commit：`ab7d2ac`

## 实际修改
- `native/tdcore/napibridge/src/tdcore_napi.cpp`：
  - 引入 `dispatch_depth` 与 `pending_unsubscribes`，实现迭代器快照迭代与延迟析构；
  - 引入 `session_generation` 与 `stale_dropped_count`，彻底阻断跨账号/跨会话旧事件污染；
- `platform/tdcore-bridge/src/main/ets/TdCoreBridge.ets`：支持 `resetSession()` API 级联底层；
- `platform/tdcore-bridge/src/main/ets/FakeNativeModule.ets`：对齐快照、延迟退订与代际丢弃逻辑；
- `platform/tdcore-bridge/src/test/TdCoreBridge.test.ets`：编写重入与代际测试套件。

## 测试证据
- `platform_tdcore_bridge` 单元测试：15/15 PASS；
- 场景测试：涵盖回调内退订自身、回调内退订兄弟、回调内动态新增订阅、回调内 close 客户端及跨代陈旧事件丢弃拦截；
- 闭合 G1 阻断项。

## 自检
- [x] 未修改未授权目录
- [x] 未包含秘密或个人数据
- [x] 文档和矩阵已更新
- [x] 无新增 warning
- [x] 没有误带其他 AI 的变更
