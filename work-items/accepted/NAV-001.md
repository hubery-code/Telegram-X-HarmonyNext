# NAV-001 实现报告：typed navigation 接管生产路由

## 结果
- 状态：Accepted
- Commit：`9b4bfb0`

## 实际修改
- `entry/oh-package.json5`：引入 `@tgx/core-navigation`；
- `entry/src/main/ets/navigation/EntryNavigationController.ets`：封装强类型 `NavigationStack` 与 `RouteCodec`，暴露单一事实源导航控制接口；
- `entry/src/main/ets/pages/Index.ets`：彻底移除 `showChatList`, `showSettings`, `showSearch`, `currentChatId` 竞争标志，由单一 `@State currentRoute: AppRoute` 驱动页面渲染；
- 生命周期收口：`onRouteChanged` 精确管理协调器；支持 `search → chat → back → search` 返回栈保活与状态不丢；
- `entry/src/test/navigation/EntryNavigation.test.ets`：新增 13 组单元测试；
- `entry/src/test/List.test.ets`：注册导航测试套件。

## 测试证据
- 模块测试：`entry` 单测从 8 增加到 21 全部 PASS；
- 全模块回归：`python3 tools/ci/test_all_modules.py` 19 模块 793/793 项单测 100% PASS；
- CI 门禁：`./tools/ci/ci.sh` 9 步全绿，工作区保持 100% 零污染。

## 自检
- [x] 未修改未授权目录
- [x] 未包含秘密或个人数据
- [x] 文档和矩阵已更新
- [x] 无新增 warning
- [x] 没有误带其他 AI 的变更
