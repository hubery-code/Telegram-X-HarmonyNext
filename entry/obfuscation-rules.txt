# Obfuscation rules for the release build of the entry module.
# Phase 0: obfuscation disabled (ruleOptions.enable=false in build-profile.json5).
# Enable and refine rules before public Beta (see docs/quality/PERFORMANCE_BUDGET.md).
