# Telegram X → HarmonyOS NEXT 迁移 — 工作协调看板

> 本文件是多 AI 并行协作的**单一协调入口**。开始任何工作包前，必须先在「进行中」登记；完成后移到「已完成」并写明证据（构建命令、commit、测试结果）。
>
> 状态机：`Backlog → Contract Ready → Implementing → Verifying → Accepted`，或 `Blocked / Deferred`。
> 规则来源：[`docs/plans/HARMONY_NEXT_MIGRATION_PLAN.md`](docs/plans/HARMONY_NEXT_MIGRATION_PLAN.md)（下称「计划」）；当前审计与恢复顺序见 [`docs/plans/IMPLEMENTATION_AUDIT_2026-09-14.md`](docs/plans/IMPLEMENTATION_AUDIT_2026-09-14.md)。
>
> ⚠️ 目录约定：计划中的 `harmony/` 前缀由用户指定取消——**本仓库根目录即 HarmonyOS 工程根**（对应计划的 `harmony/` 内容），Android 参考工程仍在 `/Users/mbjpeng-yu01/androidProjects/Telegram-X`。

## 环境基线（已锁定，GOV-002）

| 项目 | 版本 / 路径 |
|---|---|
| DevEco Studio | `/Applications/DevEco-Studio.app` |
| HarmonyOS SDK (OpenHarmony) | API 26 / platform 26.0.0.105，路径 `…/sdk/default/openharmony` |
| Native (NDK/clang) | 同上 `native/` 子目录，API 26 |
| Hvigor | DevEco 内置 `…/tools/hvigor/hvigor`（wrapper 已拷入仓库 `hvigorw`） |
| Node（构建用） | DevEco 内置 `…/tools/node/bin/node`（勿用系统 node 23 跑 hvigor） |
| 设备 | Phone / arm64 优先（D-009）；x86_64 仅模拟器/CI |

## 进行中（Implementing）

> 2026-09-14 架构审计结论：G0/G1/G2 尚未正式通过，G3 仅为“已有功能证据，等待前置 Gate”。普通新功能暂缓，下一批工作统一从计划 §8.1 Quality Recovery Sprint 下发。

| 工作包 | 负责人(AI) | 开始时间 | 说明 |
|---|---|---|---|
| _（无进行中）_ | — | — | 见「待认领」表；认领时把行移到这里 |

> ⚠️ 陈行清理（2026-09-21 DSH）：此表原有「TGX 对齐 5 泳道 + PROFILE-102/GROUP 外部泳道」一行，其说明文字早已标注「**全部交付并由主会话集成验收**」且证据在「已完成」表——留在「进行中」属状态漂移，已移除。MEDIA-105（视频消息上传）同日完成后亦已移入「已完成」表。

> ⚠️ 并行约定：**不要执行 git commit**，完成后报告文件清单由主会话统一提交。core/account 禁止 import ArkUI/Kit。项目内有 `.agents/skills/harmony-next/` 离线参考（API 12-23 快照），编码遇 API 问题可查。
>
> ⚠️ **真机验证约定（用户 2026-09-10 明确要求）**：涉及复杂真机交互的验证（点按/长按/滑屏输入/断网切换/杀进程等 UI 操作链）**优先交给用户本人操作**，AI 不得自己用 hdc/uinput 长时间跑机——AI 操作慢、链路复杂且浪费 token。AI 的职责是：构建安装包、安装启动、给出**逐条可勾选的验证清单**（操作步骤 + 预期现象），等用户反馈结果；截图判读等轻量只读观察 AI 可自己做。仅当用户明确说「全都交给你完成」时才由 AI 全程代操作。
>
> ⚠️ **门禁节奏约定（用户 2026-09-16 明确要求）**：**全量门禁不必每次改动都跑**——开发过程中只跑**受影响模块**的单测 + 秒级静态守卫（`check_design_tokens.py` / `check_architecture.py` / `check_codegen.py`），收尾（出证据报告/交付前）才跑一次**全量** `test_all_modules.py` + `assembleHap`。理由：全量 19 模块单测 + 构建约 8–10 分钟，逐次跑性价比低；而局部单测负责功能正确性、三个静态守卫负责拦截「编译期不报错、运行/渲染才崩」的回归（如拼错的 design token 会直接杀进程）。**最小执行基线**：改动 `core/domain` → 跑 `core_domain`；改动 `feature/*` → 跑对应 feature 模块 + 其依赖的 core 模块；改动 `core/navigation`/`entry` → 跑 `core_navigation` + `entry`。`tools/ci/ci.sh` 仍是发布前的完整 9 步链路，按里程碑执行即可。

## 待认领（Backlog）

> Phase 0–3 工作包全部完成（见「已完成」表）。以下为按 **计划 §9.6** 从 Phase 4/5 Epic 拆出的工作包（0.5–3 天/包，非生成代码 ≤800–1200 行），范围对齐 `docs/product/FEATURE_MATRIX.md` 的 P1 缺口。依赖列空=现在可做。

### MSG Epic（富文本实体、转发、已读、反应等；回复/编辑/删除已由 CHAT-004 完成）

| 工作包 | 依赖 | 一句话 |
|---|---|---|
| _（无待认领）_ | — | MSG-107 链接预览已交付（FEAT-COMP-002，见「已完成」表）；MSG Epic 无未认领项 |

### FILE Epic（下载/上传基础设施，MEDIA 全部依赖）

> FILE-101（FileRegistry）与 FILE-102（FileStore）已由 Antigravity 完成，FILE-103（传输失败重试/取消 UI）亦已交付——见「已完成」表。

| 工作包 | 依赖 | 一句话 |
|---|---|---|
| _（无待认领）_ | — | — |

### MEDIA Epic（图片/视频/文件/语音）

| 工作包 | 依赖 | 一句话 |
|---|---|---|
| _（已完成）_ | — | VOICE-101 (FEAT-P2-006 / MEDIA-106) 语音消息录制与波形播放已交付 |

### COMPOSER / SEARCH / NOTIF / ACC / SETTINGS Epic

| 工作包 | 依赖 | 一句话 |
|---|---|---|
| NOTIF-101 | AGC 配置 | Push Kit token → RegisterDevice 闭环（FEAT-PUSH-001；AGC/签名配置需用户确认） |
| NOTIF-102 | NOTIF-101 | 通知聚合 + 点击路由直达聊天（FEAT-PUSH-002） |

> 📌 待拆包缺口（2026-09-24 主会话更新）：`FEATURE_MATRIX.md` 的 P1 已全部认领——**FEAT-SET-003 通知设置入口**、**FEAT-SET-004 存储与缓存**、**FEAT-SET-005 隐私和安全**（SELF-102）、**FEAT-MEDIA-002 视频消息上传/下载**（MEDIA-107 补齐进度与失败重试）均已 `Accepted`；剩余 `FEAT-PUSH-001/002` 被 AGC 签名配置阻塞（见 ADR-003）。下一批候选按 TGX 对标盘点：CONTACT-101（A–Z 索引条）。**PROFILE-103 已于 09-24 全量交付**（共享内容四 Tab / 通知行真实态 / ⋮ 头部菜单 / 封面大图头部，四批均已设备取证）。

> ⚠️ 行清理（2026-09-21 DSH）：原表内 **SEARCH-103**（全局搜索健壮性）已交付——commit `1fd1ada3 fix(search): surface failures and throttle global queries`（2026-09-16，已在主线，`feature/search/SearchCoordinator` 的 `SearchFailed` 意图 + 750ms 节流 + 142 行单测），从「待认领」移除。

### TGX 对齐缺口（2026-09-16 用户实拍 TGX Android 截图×19 盘点，优先级↑）

> **基准更正**：此前一版按"官方 Telegram（HarmonyOS 版）"截图的盘点**作废**（官方版是底部 Tab，
> TGX 是抽屉导航；对齐基准一律以 Telegram-X Android 工程 `org.thunderdog.challegram` 的
> UI + 代码为准，截图仅为辅助）。下表已按 TGX 重排。

| 顺序 | 工作包 | TGX 截图对标 | TGX 代码参考 | 一句话 | 依赖 | 并行泳道 |
|---|---|---|---|---|---|---|
| 1 | PROFILE-103 | 资料页×6：共享媒体 Tab（媒体/文件/链接/群组）+ ⋮ 菜单（开始私密聊天/分享联系人/屏蔽/隐私例外/重命名/删除）+ 通知行 + 封面大图头部 | `ui/ProfileController.java`（6816 行，含 SharedMedia） | 用户资料页增强：四 Tab 共享内容列表（复用搜索/下载设施）、头部菜单、通知行、生日/emoji 状态；通话/视频 disabled 占位 | PROFILE-101（已 Accepted） | **A（已完成）**：✅ emoji 状态/通话视频占位（09-23）、✅ 共享内容四 Tab 含首屏拉取/失败态/重试/群组行跳转（09-24 设备取证）、✅ 通知行真实态（09-24 读真值+可点切换+实时同步，见「已完成」表 PROFILE-NOTIF 行）、✅ ⋮ 头部菜单实化（09-24 三项真动作：发送消息/开启密聊/屏蔽-取消屏蔽，五个假入口删除，见 PROFILE-MENU 行）、✅ 封面大图头部（09-24 头像全出血封面 + 遮罩文字 + 56↔234 收起，新增 `colors.textOnScrim`，见 PROFILE-COVER 行）；**PROFILE-103 全量交付** |
| 2 | PROFILE-102 | 群标题点按 → 群资料（成员列表/描述/共享媒体） | `ProfileController.Args(chat)`、`ui/ChatLinksController.java` | 群/频道资料页：分流 ChatProfile（成员列表第一页 getSupergroupMembers，点成员→PROFILE-101 资料页） | ✅（已 Accepted） | **A（已完成）** |
| 3 | MSG-109 | 聊天头部×6：私聊"最近在线"副标题、群"N 位成员, M 在线"、频道"N 位订阅者"、bot"服务通知"；头部 ⋮ 菜单（搜索/静音/清空历史/删除聊天；群版：举报/退出群组）；📞 通话入口（占位） | `ui/MessagesController.java`（openChatProfile/search 菜单） | 聊天页头部信息区：类型化副标题 + 聊天级 ⋮ 菜单 + 群标题点按进资料页（发 OpenChatProfile） | 无 | **B（串行链头，现在可开）** |
| 4 | MSG-108 | 置顶消息横条（彩条+发送者+内容+展开∨+✕） | `MessagesController.java`（pinned message 渲染） | 会话内 pinned message 展示条，点击跳原消息、✕ 收起 | 无 | ✅ **已完成** |
| 5 | CHANNEL-101 | 频道：帖子无气泡、反应 chips（🔥9 ❤1）、"6 条评论"+头像堆叠→评论页、"加入聊天"按钮 | `MessagesController.java`（channel 模式/comments） | 频道模式：非成员隐藏输入栏→加入按钮；评论入口；反应 chips 占位 | MSG-109 | **B（第三）** |
| 6 | DRAWER-101 | 抽屉×2：≡ 账号头（头像/名/手机/账号切换⌄）、联系人/通话/我的收藏/设置(红铃)/邀请朋友/帮助、夜间模式开关 | `navigation/DrawerController.java` | 会话列表页改 TGX 抽屉导航（替换现 ⚙ 直进设置） | ✅ | ✅ **已完成**（见「已完成」表） |
| 7 | CONTACT-101 | 抽屉"联系人"页 | `ui/ContactsController.java` | GetContacts 列表（字母序/搜索）→ 点人进 PROFILE-101 资料/发消息 | DRAWER-101 ✅ 已交付（本次抽屉路由已接通） | **D（页面已存在，剩功能补齐）** |
| 8 | SELF-101 | 我的资料×1：绿头+编辑 FAB+@username/手机/生日/bio+设备/通知/数据入口 | `ProfileController.Args(自己)`（editMode） | "我的资料"页（先只读+编辑占位 FAB） | DRAWER-101 ✅ 已交付 | ✅ **已完成补齐**（入口实化，见「已完成」表；「隐私和安全」另立缺口） |
| 9 | SELF-102（2026-09-23 SELF-101 补齐时新立） | TGX「隐私和安全」页：可见范围规则列表 + 编辑 | `ui/PrivacySettingsActivity` + `TD_getPrivacySettingRules` | 设置页新增 PRIVACY 卡 + 整屏 `PrivacySubPage`（8 项：最后上线/手机号/头像/简介/生日/通话/入群/被搜索），点一行写回 TDLib 并回读确认；「我的资料」第 4 行入口恢复且落点真实 | SELF-101 ✅ | ✅ **已完成**（见「已完成」表；实测发现 `core/td_api_generated` **早已生成** `getUserPrivacySettingRules`/`setUserPrivacySettingRules` 请求 DTO，新立缺口时「需先扩 codegen」的判断是错的，本包零 codegen 改动） |
| 10 | PROF-ENTRY-001（2026-09-24 PROFILE-103 取证时新立） | 私聊无法进入「用户资料页」：聊天头部标题点按落 `ChatProfilePage`（标题渲染成 "Group Info"，页面只剩大字母头像+名字），私聊气泡无头像可点 | `MessagesController.openProfile` / `ProfileController.Args(user)` | 私聊头部 → `userProfile` 路由（由 `chat.id` 反解 peer `user_id`）；`ChatProfilePage` 对 `ChatTypePrivate` 不再兜底成群资料页 | PROFILE-103 ✅ | ✅ **已完成**（2026-09-24，见「已完成」表 PROF-ENTRY-001 行：改动落在 feature/chat 头部路由，`entry` 零改动；此缺口导致的「共享内容非空条目无设备取证」已打通，并顺带修掉缩略图未缓存时条目被静默丢掉的缺陷） |

**并行规则（认领前必读）**：
- 可立即并行：**泳道 A（PROFILE-103）＋ 泳道 B（MSG-109 起）＋ 泳道 D 的 CONTACT-101/SELF-101 页面开发**。
- 泳道 B 内部（MSG-109→MSG-108→CHANNEL-101）**严格串行**（同文件演进）；移交须整链。
- 泳道 A 内部（PROFILE-103→PROFILE-102）串行；A 与 B 唯一共享面是 `ChatPage.ets` 群标题 onClick（一行）——若并行撞车由泳道 B 代加。
- `entry/Index.ets` 共享：各泳道只加自己分支；**DRAWER-101（泳道 C）最后动工**且是 entry home 唯一改造者。
- 后续独立小包：REACT-101 消息反应（已完成 Accepted 见 REACTION-101）；
  EMOJI-101 composer emoji 面板（已完成 Accepted 见 EMOJI-101）；通话 P3 不提前。

### P2 及以后（Phase 5 Beta，暂不拆包）

GROUP / PROFILE / SHARE / A11Y / ADAPTIVE Epic 及 FEATURE_MATRIX P2/P3 项：Phase 4（G4 核心聊天 MVP）不启动，进入 Phase 5 前再拆。

> 认领规则：一次只认领一个工作包；认领时把行移到「进行中」并注明你的身份和计划开始的内容。禁止修改未授权目录。

## 已完成（Accepted 或有保留）

| 工作包 | 完成日期 | 证据 |
|---|---|---|
| PROFILE-COVER 用户资料页「封面大图头部」（头像全出血 + 遮罩文字 + 56↔234 收起） | 2026-09-24 ✅ | 主会话（Qoder）：**① 缺陷定性**：PROFILE-103 最后一项。资料页头部此前只有一枚 56vp 小圆头像加姓名行，滚起来纹丝不动；Android 原版是**头像放大到全出血当封面**、姓名/状态压在遮罩上、滚动后收起成一条实心工具条。**② 对齐原样（先读码再动手）**：`ProfileController.getHeaderHeight()` = `Size.getHeaderPortraitSize()`（56dp）+ `getHeaderSizeDifference(true)` × scrollFactor（展开到 **234dp**），即**收起态 56dp 工具条 / 展开态 234dp 封面**两态；`setHeaderPhoto()` 用 `AvatarReceiver.Options.FULL_SIZE` 取 **`profile_photo.big`** —— 本包**不需要新增下载路径**，`ProfileCoordinator.resolveAvatarFile()` 早已下载 big 尺寸并 dispatch `OnAvatarFileLoaded(localPath)`。`ComplexHeaderView.java:216/224` 是 `topShadow=0x77000000` + `bottomShadow=0x66000000` 两道渐变遮罩，`:880-889` 明确**只在拿到真照片时才画**（占位色块上不加遮罩）。**③ 无照片态不是空白**：落回调色板纯色 + **90vp 大字母**，与列表头像同源色（`avatarColor(avatarColorIndex)`）。**④ 新增语义 token**：`colors.textOnScrim`（浅色/深色**都固定白**）。理由写进用例：深色主题 `textOnAccent` 是**黑色**，若复用压在照片封面上直接不可读；页面一律不硬编码 hex。`Colors.test.ets` 新增 `textOnScrim_staysWhiteOverScrim`（断言两主题都是白、**必须 ≠ 同主题 `textOnAccent`**、对 `scrim` 的 WCAG 对比度 ≥4.5）。**⑤ 结构**：页面改为 `Stack` 叠层 —— `Scroll` 第一屏子节点就是 `Cover()`（跟着列表一起滚），`Header()` 浮在顶上 56vp；Header 背景在压封面时透明、否则实底，返回/⋮ 图标色在 `textOnScrim` ↔ `colors.icon` 之间切，标题 `opacity` 0↔1，切换走 `motion.durationFast`。**⑥ 收起临界点**：`coverCollapseOffset()=COVER_HEIGHT-TOOLBAR_HEIGHT=178vp`，`onDidScroll` **只在布尔真的翻转时**写 `@State`（否则每帧整页重排）。**⑦ 占位页兜底**：`isPlaceholderScreen()`（加载/错误态且无姓名）时工具条强制回实底，不然白色图标消失在白底上。**⑧ 真机才暴露的两个 bug**：(a) 照片封面顶部空出一条白带、工具条白色图标压在白底上看不见 —— 根因 **`Scroll.align` 默认 `Alignment.Center`**（SDK `common.d.ts:20310`），内容不足一屏时整页被垂直居中；(b) 第一版修法拉错：`.constraintSize({minHeight:'100%'})` 把封面压到视口高（`dumpLayout` 实测封面 Stack 784px 而非 234vp=819px）、**页面失去滚动**（上下滑只有 ~34px 弹性回弹）。正解是 `.align(Alignment.TopStart)`。**⑨ 设备实证（127.0.0.1:5555）**：be hu（浅色主题，`final_letter.png`）字母封面顶到 y=137 全出血、白色返回/⋮ 清晰、姓名+`last seen recently` 在封面底部；Rose @MissRose_bot（深色主题，`final_photo.png`）照片封面全出血 + 上下遮罩，`Rose` 白字与 `online` 强调色在照片上均可读 —— **这张顺带反证 ④**：深色主题 `textOnAccent` 为黑，复用即糊。**收起态取证**：该账号资料页内容不足一屏（共享媒体仅 2 张、`hasMore=false`），178vp 临界点物理上到不了，故把阈值**临时**降到 4vp 重装逼出真实滚动 → 点 文件 tab 上滑 → `dumpLayout` 实测工具条 `Row bg=#FFFFFFFF`、标题 `be hu opacity=1.000000`（展开态同一节点为 `bg=#00000000 / opacity=0.000000`），截图 `collapsed.png` 为白底黑字灰图标；取证后**已还原 `COVER_HEIGHT - TOOLBAR_HEIGHT` 并重新构建安装**，复验展开态正常。全程只读探测，未改任何账户设置。**⑩ 测试**：core_design_system **54/54**（含新增 1 例，已按 `test_result.txt` 确认该用例真的执行）、feature_profile **69/69**；`check_architecture`/`check_codegen`/`check_design_tokens` 三道守卫全绿；**装配层零改动**（`entry` 无需新回调/路由）。**⑪ 边界**：(a) Android 的 56↔234 是按 scrollFactor **连续插值**、返回键颜色也是 WHITE→icon 连续 lerp，本包做**二值切换** —— 连续插值要 `onDidScroll` 每帧写状态，与 ⑥ 的性能取向冲突，留待封面查看器一并做；(b) 点封面放大查看照片（Android photo viewer）不属本包；(c) 收起态截图是在临时阈值探针包上取的，真实阈值下的翻转由 `onDidScroll` 同一分支取证，未伪造。 |
| PROFILE-MENU 用户资料页「⋮ 头部菜单」实化（密聊 + 屏蔽真实态，删掉五个假入口） | 2026-09-24 ✅ | 主会话（Qoder）：**① 缺陷定性**：PROFILE-103 收尾项。资料页 ⋮ 菜单六项里**五项是只打 `hilog` 的假入口**（分享联系人 / 屏蔽用户 / 隐私例外 / 重命名联系人 / 删除联系人 —— TDLib 侧根本没有对应写路径），唯一真动作「开始私密聊天」又与页面底部 Message 按钮重复。假入口留在真菜单里等于挂死按钮，**整体删除**，菜单只留三项：**发送消息 / 开启密聊 / 屏蔽用户**。**② 契约**：新增 `ProfileMenuAction(action)`（白名单 `PROFILE_MENU_SECRET_CHAT`/`PROFILE_MENU_TOGGLE_BLOCK`，**白名单外一律 no-op**）、`OnPeerBlockLoaded(blocked)`/`OnPeerBlockFailed`，副作用 `CreateSecretChatEffect`/`SetPeerBlock(blocked)`；状态三位 `peerBlocked / peerBlockReady / peerBlockUpdating`。屏蔽项文案由真值推导（未读到 → 「屏蔽用户（未读取）」且点击 no-op；已屏蔽 → 「取消屏蔽」），**不存在能写死文案的路径**。**③ 屏蔽走哪条 API**：本 schema **没有 `blockUser`/`unblockUser`**，只有 `setMessageSenderBlockList{sender_id: messageSenderUser{user_id}, block_list}` —— 屏蔽传 `blockListMain`、**解除传 `null`**（TDLib 明确语义）；读侧 `getChat.block_list !== null` 即已屏蔽，实时面订阅 `updateChatBlockList`。**④ 一次 getChat 喂两行**：原 `readChatNotification()` 改名 `readChatState()`，同一次回读同时出 `notification_settings` 与 `block_list` 两行真值，订阅 filter 扩为 `['updateChatNotificationSettings','updateChatBlockList']`（按 chatId 过滤）；读失败经 `failChatState()` **同时收回两行的 ready**，让陈旧文案不能冒充真值，也不必为第二行再发一次往返。**⑤ 无乐观更新**：写成功只算发出，`peerBlockUpdating` 期间重复点击 no-op，回包后**再 `getChat` 复核**，文案只跟复核结果翻；复核失败同样收回 ready。**⑥ 密聊不需要新路由**：`createNewSecretChat` 回执 `secretChat`，其 `id` **同时就是 `chat.id`**（`type` 为 `chatTypeSecret`，`ChatCoordinator` 早已处理该分支），故直接复用装配层注入的 `onOpenChat` 导航；`core/navigation` 未加任何路由。失败经 `ToastPort` 提示「无法开启密聊」而非静默。**⑦ 装配层**：`ProfileCoordinatorOptions` 新增 `toastPort`（缺省 `FakeToast`），entry 注入 `HarmonyToastAdapter` —— 资料页此前**没有任何提示通道**。**⑧ 设备实证（127.0.0.1:5555，be hu 私聊 userId=6846163203）**：进 User Info 页 → `peer_block_state blocked=false`（真值回读）→ ⋮ 菜单截图**恰好三项**（`menu_after.png`，占位项全部消失）；点 **开启密聊** → `secret_chat_created userId=6846163203,secretChatId=-1999542084771,layer=0` → 落到标题 "be hu"、正文 "No messages yet" 的密聊页，系统返回键回资料页；点 **屏蔽用户** → 三行日志依次 `peer_block_state blocked=true`（推送先到）→ `peer_block_written blocked=true` → `peer_block_state blocked=true`（getChat 复核），重开菜单文案变 **取消屏蔽**；再点 → `peer_block_written blocked=false` → 文案回 **屏蔽用户**，**账户状态还原到探测前**（通知行全程为 开启、未被误改），返回会话列表 be hu 会话与历史消息完好。**⑨ 测试**：新增 `ProfileMenu.test.ets` **9 例**（密聊建得成并把 secretChatId 交给导航、错误回包只提示不跳转、真值未读到点屏蔽 no-op 且不发写、`block_list` 回读成真实态、写请求体 `sender_id=messageSenderUser{user_id}` + `block_list=blockListMain`、**解除传 null**、写失败清 `updating` 并提示、`updateChatBlockList` 只认本 chat_id、白名单外 action 一条请求都不发），`ProfileReducer.test.ets` 新增 **7 例**；本模块首次给 coordinator 传 `onOpenChat`/`toastPort` 捕获器。feature_profile **69/69**，`check_architecture`/`check_codegen` 全绿。**⑩ 边界**：(a) 密聊本轮只证到「建得成、进得去」，`layer=0` 说明对端尚未握手，端到端 E2E 握手/自毁定时不属本包；(b) 该密聊会话**留在账户列表**（项目无 `deleteSecretChat` UI，未强行清理工证）；(c) 屏蔽语义按 messageSender 而非 chat，与 Android「屏蔽用户」等价；(d) PROFILE-103 仅剩**封面大图头部**。 |
| PROFILE-NOTIF 用户资料页「通知行」真实态（读真值 + 可点切换 + 实时同步） | 2026-09-24 ✅ | 主会话（Qoder）：**① 缺陷定性**：PROFILE-103 收尾项。User Info 页 `notificationText` 是 `ProfileUiState` 里的**默认值 '开启'**，reducer 与 coordinator 从不赋值、行也没有 `onClick` —— 一个永真常量加一条死行，与 SELF-101 那批「假入口/编造副标题」同类。**② 模型下沉（避免分叉）**：静音纯模型 `CHAT_MUTE_FOREVER_SECONDS / isChatMuted / withChatMuteFor` 由 `feature/chat/model/ChatMute.ets` 移到 **`core/domain/chat/ChatMute.ets`**：`feature/chat` 的 ⋮ 菜单与 `feature/profile` 的通知行要表达同一个静音态，而 feature 之间不可互引，这条「int64 sound_id 一旦复制就会分叉」的约束必须只有一份；`feature/chat` 改从 `@tgx/core-domain` 引入，`isMuted`/写请求逻辑不变。**③ 契约与状态**：删掉 `notificationText` 字符串字段，改成四个真实态位 `notificationMuted / notificationReady / notificationUpdating / notificationRequested`；新增 `NotificationRowTapped`、`OnChatNotificationLoaded(muted)`、`OnChatNotificationFailed` 意图与 `FetchChatNotification`、`SetChatNotification(muted)` 副作用。文案由页面按状态推导（`…` 写入中 / `—` 未知 / `静音`·`开启` 真值），**不再存在能写死的路径**。**④ reducer 规则**：`onProfileLoaded` 除共享内容外再一次性出 `FetchChatNotification`；`notificationRowTapped` 在 `!notificationReady \|\| notificationUpdating` 时**彻底 no-op**（真值没读到就点，绝不凭默认值反着写一次），否则出**反向**写请求并把 `notificationUpdating` 置真、**不动 `notificationMuted`**（无乐观更新）；`onChatNotificationFailed` 把 `notificationReady` 收回 false，让陈旧文案不能冒充真值。**⑤ coordinator**：首屏两路数据（共享内容 + 通知）都挂在私聊 chatId 上，故 `withPrivateChatId()` 加**等待队列**（`privateChatWaiters` + `privateChatPending`）：命中缓存直接回调，在途则排队，**只发一条 `createPrivateChat`**；`getChat` 回读 `notification_settings` → 顺带 `subscribeUpdates(['updateChatNotificationSettings'])`（按 chatId 过滤，本端/它端改动能实时翻回来）→ `OnChatNotificationLoaded(isChatMuted(...))`；写路径 `setChatNotificationSettings` 以**回读快照为底本**、只覆盖 `mute_for`，成功后**再 `getChat` 复核**，全程不做乐观改写。TDLib 没给 `notification_settings` 时出 `OnChatNotificationFailed` 而不是猜「未静音」。**⑥ 设备实证（127.0.0.1:5555，be hu 私聊 userId=6846163203）**：进页面 → `fetch_chat_notification` + `private_chat_ready source=shared_tab=0`（**全程只有一条 createPrivateChat**）→ `chat_notification_state muted=false,mute_for=0` → 行显示 **开启**；点行 → 写入 → `chat_notification_state muted=true` 推送先到、`chat_notification_written` 后到、再 `muted=true` 复核 → 行显示 **静音**（`notif_muted.png`）；**跨页一致性**：同状态在聊天 ⋮ 菜单渲染为 **取消静音**（`ChatCoordinator chat_mute_state muted=true,mute_for=357293358`），两处同源不同页互证；从 ⋮ 菜单取消静音后回到资料页，行读数 **开启**（`notif_enabled.png`，`chat_notification_state muted=false,mute_for=0`），并把账户状态**还原成探测前的 开启**。全程无 `bad_request`、无 error 级日志。**⑦ 测试**：新增 `ProfileNotification.test.ets` 7 例（首屏两路共用一条建会话、回读 muted=true/false 各成真实态、缺 `notification_settings` 时停在未知不硬编码「开启」、点击发**反向**且 `mute_for=2147483647` 而 `sound_id='12345'`/`use_default_sound=false` **原样带回不被写成空串**、写成功前 `notificationMuted` 不变、`updateChatNotificationSettings` 只认本 chat_id、真值未读到前点击 no-op 且不发写请求）；`ChatMute.test.ets` 3 例随模型迁到 `core/domain`；`ProfileReducer.test.ets` 改 1 例（首屏出 2 条副作用）+ 新增 5 例。feature_profile **53/53**、core_domain **116/116**、feature_chat **278/278**，`check_architecture`/`check_codegen` 全绿。**⑧ 边界**：(a) `mute_for=357293442` 是 TDLib 对 2147483647 的**服务端收敛值**（约 11.3 年），不是客户端写错 —— `isChatMuted` 只看 `mute_for > 0`，态判定不受影响；(b) 写入中的 `…` 与未知态的 `—` 在真机上停留毫秒级、抓不到截图，由上述单测取证（同 `sharedLoadFailed` 口径）；(c) 本包只做「通知」一行，用户资料页 ⋮ 头部菜单与封面大图头部仍属 PROFILE-103 未完成项。 |
| PROF-ENTRY-001 私聊头部直达「用户资料页」+ 共享媒体「不丢条目」修复 | 2026-09-24 ✅ | 主会话（Qoder）：**① 动机**：PROFILE-103 取证时登记的入口缺口 —— 私聊头部点进 `ChatProfilePage`（标题渲染成 "Group Info"），项目里**没有任何路径**能从「有媒体历史的私聊」进到 User Info 页，导致共享内容四 Tab 的非空条目渲染永远取不到证。**② 落点极小**：改动全在 `feature/chat` 一处，`entry` 路由**零改动**（`userProfile` 路由与 `onOpenUserProfile` 回调早已存在，只是从没人调用）。`ChatCoordinator.fetchChatKind()` 解析 `getChat` 回包时新增缓存 `privatePeerUserId`，取自 `type_.user_id`（`chatTypePrivate`/`chatTypeSecret`），**不是拿 chatId 顶替**（密聊 chatId ≠ user_id）；`openChatProfile` 处理时 `privatePeerUserId > 0` 走 `onOpenUserProfile(userId)`，否则维持 `onOpenChatProfile(chatId)`（群/频道，以及 getChat 尚未回包的竞态兜底）。**③ 设备实证（127.0.0.1:5555）**：be hu 私聊 → 点头部标题 → hilog `open_user_profile_from_private_header chatId=6846163203,userId=6846163203` → User Info 页真实渲染（be hu / last seen recently / @hu_bery / Phone / 通知 开启 / 媒体·文件·链接·群组 / Message），**首次拿到共享内容非空条目的设备证据**：`shared_media_ok page=2,kept=2,placeholder=0,count=2,hasMore=true`，两张真实照片缩略图瓦片 + 「加载更多」。**④ 顺带逼出的真实缺陷（本轮修复）**：点 加载更多 → `search_shared_content fromMessageId=58720256` → 旧日志 `shared_media_ok count=2,hasMore=false`，即**第二页回了 2 条消息、映射后 0 条** —— `mediaItemOf()` 在 `photo.sizes` 尚未缓存 / 视频无 cover 且无 thumbnail 时返回 null，条目被静默丢掉，网格永远少格子。修复：这类情况改落**占位条目** `SharedMediaItem(0, null, isPhoto)`，由 `ProfilePage.SharedMediaGrid()` 既有的占位瓦片分支渲染（照片 `ic_photo`、视频 `ic_video`）；日志扩成 `page=/kept=/placeholder=/count=/hasMore=`，「回了多少 / 留了多少 / 几座占位」设备上一眼可查。**⑤ 测试**：`PrivateChatHeaderProfile.test.ets` 3 例（私聊头部出 `onOpenUserProfile(peerUserId)`、群头部仍出 `onOpenChatProfile`、getChat 未回包时退回 `onOpenChatProfile` 而不是拿 0 当 userId）；`SharedMediaPlaceholder.test.ets` 3 例（无 sizes 的照片、无封面无缩略图的视频各留一座占位；加载更多页 2 条未缓存消息**追加**成占位而非消失，`sharedHasMore` 随 `next_from_message_id=0` 收起）—— 删掉修复该用例即失败（`kept` 退回 1 而非 3）。feature_chat **281/281**、feature_profile **41/41**，`check_architecture`/`check_codegen` 全绿。**⑥ 边界**：TDLib 对同一 `from_message_id=58720256` 两轮返回条数不一致（首测 2 条、修复后复测 0 条），故**占位瓦片的真机截图未取得**，该分支改由上述单测以假 `foundChatMessages` 回包取证（本项目第一次给 `feature/profile` 的 coordinator 装假桥单测，此前只有 reducer 测试）；频道/群的 `ChatProfilePage` 尚无媒体 Tab，本轮只把入口打通、未扩群资料页。 |
| PROFILE-103 收尾：共享内容四 Tab 首次设备取证 + 首屏自动拉取 + 失败态与重试 + 群组行导航 | 2026-09-24 ✅ | 主会话（Qoder）：**① 先取证再定性**：四 Tab（媒体/文件/链接/群组）代码早已在主线，但**从未对着真实 TDLib 回包看过一眼**。本轮以模拟器真实账号取证（El CLUB 群成员 Rose / `@MissRose_bot`，`userId=609517172`），逐条点，抓到 5 个真实缺陷。**② 缺陷 A：默认 Tab 永远不请求** —— 旧 reducer 只在 `onSharedTabSelected` 出 `FetchSharedContent`，而默认选中的 媒体 Tab 永远不会被"选中"，进页面即空着一片。修复：`onProfileLoaded` 一次性出首屏请求，新增 `sharedInitialRequested` 闩（`updateUser` 订阅晚到还会再回灌一次 profile，不闩住会反复重发）。**③ 缺陷 B：失败谎报成「暂无内容」** —— coordinator 6 条失败路径此前统统用当前空数组回灌 `OnSharedContentLoaded`（只为复位 `sharedLoading`），请求挂掉页面照样理直气壮显示「暂无媒体文件」。新增 `OnSharedContentFailed` 意图 + `sharedLoadFailed` 状态位 + `SharedLoadFailed()` 视图（「加载失败 / 点击顶部对应页签重试。」），**没拉到**和**确实没有**从此分开。**④ 缺陷 C：失败后没有任何重试入口** —— 旧 `onSharedTabSelected` 对"同 Tab"一律 no-op。重写为三分支：同 Tab 且已有内容→恒等 no-op；切到已有内容的 Tab→只换高亮不发请求；其余（含重点当前空 Tab）→ 发请求，重试路径天然成型。设备实证：点 媒体 → `fetch_shared_content tab=0` → `shared_media_ok count=0`（毫秒级）。**⑤ 缺陷 D：视频占位错用图片图标** —— `item.isPhoto ? ic_photo : ic_video`。**⑥ 缺陷 E：共同群组行是死路** —— 新增 `OpenSharedChatRequested` → `OpenSharedChatEffect` → 复用 entry 已注入的 `onOpenChat`（与「发消息」同一条导航，装配层零改动）。设备实证：群组 Tab 出 2 个真实共同群组 → 点 El CLUB → `open_shared_chat chatId=-1001785291444` → 落进群聊，系统返回键回到资料页且**群组 Tab 高亮与列表原样保留**。**⑦ 链路事实**：私聊 `chatId == userId`（实测 609517172），`cachedPrivateChatId` 命中后同页后续 Tab 不再重复建会话（第二次 fetch 日志只有 `search_shared_content`）。**⑧ 一次自我纠正（重要方法论）**：首屏 fetch 曾**稳定 60s 超时**（`fetch_shared_content tab=0` → `ERROR network/timeout`），第一反应归因为"在 TDLib 回包回调里再发请求"的重入问题，准备照 `ChatCoordinator` 的 `setTimeout(0)` 模式延一拍。忍住没写补丁，先补两条 info 日志（`shared_private_chat_ready` / `search_shared_content accepted=`）再复测：createPrivateChat 16ms、searchChatMessages 32ms 全链路毫秒级返回，超时不复现 → 判定为**当次 TDLib 云端搜索慢/网络抖动**，不是重入 bug。**给不存在的根因写延时补丁是纯负债**，本包只保留那两条排障日志。**⑨ 测试**：`ProfileReducer.test.ets` **38/38**（新增 9 例：首屏一次且只一次、空 Tab 重点走重试、有内容同 Tab 恒等、失败标记与复位、别的 Tab 失败不误伤当前 Tab、`chatId=0` no-op；删除被新语义取代的 `sameTabIsNoOp`），`check_architecture`/`check_codegen` 全绿。**⑩ 边界说明**：(a) 媒体/文件/链接 Tab 的**非空条目渲染无设备取证** —— 唯一入口是群聊头像，而私聊头部点进去是 `ChatInfoPage` 桩页（只有头像+名字），项目当前**没有任何路径能从"有媒体历史的私聊"进到 User Info 页**，已作为独立入口缺口登记（**后续：该缺口已由 PROF-ENTRY-001 于同日打通，非空条目渲染已取证；同轮并修掉了「缩略图未缓存时条目被静默丢掉」的缺陷，见「已完成」表首行**）；(b) `sharedLoadFailed` UI 仅单测取证（模拟器 shell 无 root、无飞行模式，逼不出 TDLib 错误回包，同 MEDIA-107/SELF-102 口径）。 |
| SELF-102 隐私和安全分区：8 项可见范围读真值 + 点一行写回并回读确认（FEAT-SET-005 新立并转 Accepted） | 2026-09-24 ✅ | 主会话（Qoder）：**① 先纠正上游判断**：SELF-101 补齐时断言「需先扩 TDLib scheme + codegen 才能做隐私分区」是**错的**——`core/td_api_generated` 早已生成 `getUserPrivacySettingRules`/`setUserPrivacySettingRules` 请求 DTO 与全部规则值类型，本包**零 codegen 改动**，纯 feature 层切片。**② 模型**：`feature/settings/.../model/PrivacySettings.ets` 做 TDLib 规则列表 ↔ 页面档位的双向纯映射：只有单条简单规则才认成一个明确档位，带例外名单（多条规则）一律归 `custom`（否则「联系人但排除某人」会被错显成「联系人」，用户以为改动没生效）；`rulesForChoice` 只发一条简单规则，与 TGX「改主可见范围即重置例外名单」一致。**③ MVI 切片**：7 个意图（open/close/load/loaded/toggle/updated/updateFailed）+ 2 个 effect（`loadPrivacySettings`/`setPrivacySetting`）+ 4 个状态字段（`showPrivacyPage`/`privacyItems`/`isLoadingPrivacy`/`updatingPrivacyKey`），reducer 纯函数、`toggle` 期间按 `updatingPrivacyKey` 串行互斥（第二行点击直接 no-op，避免两个写请求交叉回读互脏）。**④ 不做乐观更新**：写成功后**回读**该项为准——实测非 Premium 账号写 `allowPremiumUsers` 会被 TDLib 归一化成别的规则，乐观显示会撒谎。**⑤ 单项容错**：8 项串行读（并发同连接会被 TDLib 判 `too_many_requests`，同 SET-106 教训），某项失败只把该行落成 `unknown` 并继续，不整屏报错。**⑥ 实测抓到的真实缺陷（本包关键收获）**：给「生日」写 `restrictAll` 后 TDLib 读回 `rules=[]`，页面出现用户看不懂的 `Unknown` 行；force-stop 冷启动复测仍是 `[]`，证明**「没有人」在该设置上根本不落库**——规则列表全是 allow 语义，空列表即「没人能看见」。据此把 `choiceFromRules([])` 由 `unknown` 改为 `nobody`，`unknown` 只保留给「该项读取失败」。**⑦ 日志**：`privacy_rules_read` / `privacy_rules_write` 两条 info 打印带 `key -> choice,rules=[类型列表]`，本次定位完全靠它，保留给排障。**⑧ 测试**：新增 `PrivacySettings.test.ets` 15 例（规则→档位、例外名单→custom、空列表→nobody、`rulesForChoice` 单条 + 往返、encode/decode 往返、点击循环、可写判定、8 键序与 setting `@type` 表防张冠李戴、`withChoice` 恒等）+ reducer 13 例（含「不做乐观更新」「串行互斥」「updateFailed 保留原档位」）；`feature_settings` **112/112**、`feature_self` **7/7** PASS，`check_architecture`/`check_codegen` 全绿。**⑨ 模拟器端到端（127.0.0.1:5555）**：设置页新增 **PRIVACY** 卡（副标题直读「最后上线时间」真值 `Last Seen: Everybody`）→ 点进整屏 `PrivacySubPage` 出 8 行真实 TDLib 值（Last Seen/Profile Photo/Bio/Calls/Invites/Discoverable=Everybody、Phone Number/Date of Birth=My Contacts）→ 点「生日」连做 3 次写回，hilog 逐条对上 `write <- nobody,rules=[restrictAll]` → `read -> nobody,rules=[]`（修复后页面显示 **Nobody** 而非 Unknown）、`write <- everybody` → 回读 everybody、`write <- contacts` → 回读 contacts，行文案同步翻转，**探针结束后账号生日隐私已复原为基线 My Contacts**（非基线态不留）；force-stop 冷启动再进 8 项全部与 TDLib 一致；系统返回键关闭子页回落设置页（`entry` `onBackPress` 与 Devices 子页同序）。**⑩ 边界说明**：coordinator 的 TDLib 线协议路径未做假桥单测——`feature/settings` 无 `@tgx/platform-ports` 依赖，为补一个测试引入跨层依赖不划算，改以设备侧 hilog 写/读成对证据替代（同 MEDIA-107 口径）；`custom`（例外名单）档位当前只读展示、点击 no-op，例外名单编辑器仍是 TGX 未拆包项。 |
| MEDIA-107 视频下载进度可读 + 失败重试态（FEAT-MEDIA-002 转 Accepted） | 2026-09-24 ✅ | 主会话（Qoder）：**① 先取证**：清缓存后视频气泡确实是「缩略图先行 + 下载钮」（验收第 1 条成立），但一条要下 20 秒的视频只有一个不确定转圈，且**代码里根本没有失败态**——TDLib 回错误只 `logError`，UI 静默退回下载钮，用户分不清「没下」和「下挂了」。**② 进度**：`ChatPage` 下载遮罩在 `LoadingProgress` 下加百分比文字，遮罩由 48→56 与上传环对齐；行签名 `mediaRowKey` 的下载进度分桶由 10% 改 1%（否则环上文字两次重绘之间跳一大截）。**③ 失败重试**：`MediaAttachment` 新增第 24 位字段 `isDownloadFailed`（含 `withDownloadFailed()` 归并，同值不复制）；`ChatCoordinator` 新增 `failedFileIds` 集合，`downloadFile` 请求侧与 promise 回包侧两条错误路径统一走 `markFileDownloadFailed`（记失败 + **释放 `requestedFileIds` 去重闩**，否则同一会话里重试按钮点不动）+ 节流同步；`requestMediaDownload` 起手先清失败标记，视频分支以 `withDownloadFailed(...)` 投影；气泡出 `ic_warning + 重试` 遮罩，点击重新 `RequestMediaDownload`。**④ 顺手抓到一个自己引入的回归**：1% 分桶让整行节点每 ~0.4s 重建一次，异步解码的封面图**每帧来不及上屏 → 下载全程封面刷白**（对照旧包 10% 分桶时封面正常）。修复：封面 `Image` 在下载/上传态加 `.syncLoad(media.isDownloading / media.isUploading)`，重建后同步解码不留空白帧。**⑤ 测试**：`MediaRedownload.test.ets` 新增 1 例（伪造带 `@extra.requestId` 的 TDLib `error` 回包 → 断言去重闩释放、二次点击真的发出第 2 条 `downloadFile`）、`MediaAttachment.test.ets` 新增 1 例（`withDownloadFailed` 保字段与同值恒等），feature_chat **278/278** 全绿；`check_architecture`/`check_codegen` 通过。**⑥ 模拟器端到端（127.0.0.1:5555）**：设置页 Clear（14.0 MB·35 files → 812 KB·32 files）→ 视频气泡出封面 + 下载钮 + `0:07` → 点击后逐 2s dumpLayout 采到 `0% → 3 → 6 → 11 → 15 → 20 → 24 → 29 → 34 → 40 → 68 → 74 → 80 → 91 → 97 → 消失`，hilog `request_media_download fileId=88` → `media_download_completed fileId=88`，同一气泡原位翻成 ▶ 且封面恢复；`syncLoad` 修复后二次复测在 **93%** 帧封面（湖面）与百分比同时可见。**⑦ 边界说明**：失败/重试分支只有单测取证 —— 模拟器 shell 无 root（`ifconfig eth0 down` Permission denied）、通知中心无飞行模式开关，无法在不破坏账号态的前提下逼出 TDLib 的 `downloadFile` 错误回包，故未做设备侧强制失败截图；FEAT-MEDIA-002 三条验收标准（缩略图先行 / 进度 / 失败重试）据此转 Accepted，前两条设备取证、第三条代码+单测取证。 |
| SET-106 通知设置入口收尾：单会话静音真正生效 + 状态回灌 + 设置分区开关重渲染（FEAT-SET-003 转 Accepted） | 2026-09-23 ✅ | 主会话（Qoder）：**① 先取证**：模拟器实测 MENU-101 的「静音」点了没反应，hilog 打出 `setChatNotificationSettings` 被 TDLib 判 `bad_request`；设置页三个分区开关点完立刻弹回。**② 根因 1（静音写失败）**：`sound_id/story_sound_id` 是 int64（本项目建模为 string），请求体里写空串 TDLib 直接拒整个请求 → 新增纯函数模型 `feature/chat/…/model/ChatMute.ets`：以回读到的 `chat.notification_settings` 为底本、只覆盖 `mute_for`（永久静音用 Int32 最大值，与 feature/settings 同口径），int64 字段恒带回填值。**③ 根因 2（状态无从回灌）**：coordinator 没有订阅 `updateChatNotificationSettings`，静音态只在内存里 → `applyChatNotificationSettings` 统一落快照并 dispatch 新意图 `OnChatMuteStateChanged` → reducer 写进 `ChatUiState.isMuted`（新增第 16 位可选字段，copyWith 同步），`start()` 订阅、`destroy()` 退订、跨会话推送按 `chat_id` 过滤。**④ 根因 3（UI 恒显旧文案）**：⋮ 菜单「静音/开启通知」是硬编码，群与频道永远显示「开启通知」；`ChatPage` 改用 `muteMenuLabel()/muteMenuAction()` 读 `uiState.isMuted`，频道底部静音条文案与图标（`ic_mute`/`ic_notifications`）同步分流。**⑤ 根因 4（@Builder 按值入参不响应）**：`SettingsPage.NotificationScopeRow(label, scope, enabled)` 的 `enabled` 是按值传入，@Builder 不参与状态观察 → 去掉该入参，改在 builder 内 `this.scopeNotificationsEnabled(scope)` 直读 `this.uiState`，Toggle 与 `onChange` 判等都走同一个函数。**⑥ 测试**：新增 `feature/chat/src/test/ChatMuteState.test.ets` 9 例（静音模型 3 + reducer 2 + coordinator 4：写请求负载含 `sound_id="0"`/`mute_for=2147483647`、取消静音归零、update 回灌翻态、他会话推送被忽略），feature_chat 276/276、feature_settings 84/84 全绿，`check_architecture.py`/`check_codegen.py` 通过。**⑦ 模拟器端到端**：⋮→静音 → 菜单文案即时变「取消静音」→ force-stop 冷启动后仍显示「取消静音」（getChat 回读生效，未做乐观改写）→ 会话列表该行出现静音图标 → 取消静音回原状；设置页全局开关 ON 连带三个分区 ON、OFF 后单点 Channels ON 复原，冷启动后分区状态与 TDLib 一致。FEAT-SET-003 验收标准「全局/单会话通知开关生效并持久化」全绿，矩阵转 Accepted。 |
| SET-104 收尾：清理缓存后媒体可重下闭环（FEAT-SET-004 转 Accepted，三个真实缺陷） | 2026-09-23 ✅ | 主会话（Qoder）：**① 先取证再定性**：SET-104 一期只交付了「真实占用 + 一键清理」，第 3 条验收标准「清理后媒体可重下」始终不通过，此前误判为「hilog 拿不到应用日志」。实为抓法不对——`entry/platform/HarmonyLogSink.ets` 以 `TAG='TelegramX'` 写 hilog，`hilog -r` 清缓冲后 `hilog -x | grep TelegramX` 可完整读到应用日志，据此逐个定位。**② 缺陷 A：点下载钮完全无反应（UI 命中测试）**。视频气泡的时长角标层是一个 `Row().width('100%').height('100%')` 的纯装饰覆盖层，且在下载钮**之后**声明，ArkUI 默认命中测试下它吃掉整颗气泡的点击 → `requestMediaDownload` 从未被 dispatch，日志零输出。修复：给该装饰层加 `.hitTestBehavior(HitTestMode.None)`；同时明确「后声明的全尺寸装饰层必须让出命中测试」这一通用坑。**③ 缺陷 B：去重闩死锁（同一次会话内第二次下载永远发不出去）**。`ChatCoordinator.requestMediaDownload` 用 `requestedFileIds` 防止投影每帧重复请求，但**只有下载完成才 delete**——`optimizeStorage` 把文件打回未下载态（TDLib 推 `is_downloading_active=false` 的 `updateFile`）后闩仍挂着，同 fileId 再点无任何反应。改为：完成 / 请求失败 / 收到「非活跃中」的 `updateFile` 三条路径均释放闩，仅「正在下载」保持闩住。**④ 缺陷 C：冷注册表误判「未下载」**。`FileRegistry` 是**每个 ChatCoordinator 一个实例**（`new FileRegistry(scope)`），退出会话重进即缓存清空，此时消息 DTO 明明带着已完成的 `local.path`，`isDownloaded`/`getLocalPath` 仍返回 false/null → 清理后重新下好的媒体在重进会话后又显示成下载按钮。修复：`FileRegistry.isDownloaded(fileId, seed?)` / `getLocalPath(fileId, seed?)` 内部走新增的 `resolveLocal`——**注册表命中优先，仅在冷缓存时回退消息自带 File 且只认 `is_downloading_completed`**（部分下载的 seed 不能冒充已完成）；`ChatCoordinator` 8 处判定/8 处取路径全部传入 targetFile。**⑤ 测试**：新增 `feature/chat/src/test/MediaRedownload.test.ets` 4 例（去重只发一次且下载中不重发、清理重置后**能重发**（缺陷 B 回归守卫）、完成后释放闩并可再次重下、已下载直接跳过），`core_domain` 补 seed 兜底 1 例（含「注册表条目优先于过期 seed」）；`feature_chat` **267/267**、`core_domain` **113/113** PASS；`check_architecture`/`check_codegen` 全绿。**⑥ 模拟器端到端（127.0.0.1:5555，`uitest` 代操作 + `screenCap`/hilog 取证）**：设置页 Clear → 占用 `13.5 MB · 18 files` → **`216.0 KB · 14 files`** → 回聊天页视频气泡出下载钮 → 点击即见 `request_media_download fileId=88,downloaded=false`（22:32:25）→ 气泡就地转圈 → `media_download_completed fileId=88`（22:35:27）→ 同一气泡原位翻成 ▶ 播放钮，**无需退出重进** → 退出会话再重进，视频仍显示 ▶（缺陷 C 已修，不再退回下载钮）。FEAT-SET-004 三条验收标准全绿，矩阵转 `Accepted`。 |
| SELF-101 补齐：「我的资料」设置入口实化（直达设置分区）+ 分区渲染模型 + 修 `Scroll` 居中错位 | 2026-09-23 ✅ | 主会话（Qoder）：**① 缺口定性**：SELF-101 页面本体早已交付，但「设备 / 通知 / 数据和存储 / 隐私和安全」四行是**假入口**——副标题写死（'—'/'开启'/空）、点击只 dispatch 一个被 reducer 判为 no-op 的 intent，且副标题是编造的（通知行永远显示「开启」，与真实开关状态无关）。**② 分区模型**：新增 `feature/settings/.../model/SettingsSection.ets` 纯映射（`SettingsSectionKey = all\|notifications\|data_and_storage` + `SettingsCardKey` 卡片集合 + `parseSettingsSection` 未知值一律退回 `all`），路由串是外部输入，先收敛成受限键再驱动渲染，避免一个错拼 section 打出半截页面；`SettingsUiState` 增只读 `section` 字段（`copyWith` 未指定时保留）。**③ 渲染**：`SettingsPage` 按 `sectionShowsCard` 逐块条件渲染，分区页不再重复渲染小标题、标题走 `settingsSectionTitleKey`。**④ MVI 链路**：`SectionFocused(section)` intent + reducer `sectionFocused` 分支；`feature/self` 侧 `settingsSectionTapped` → 白名单（`isSelfSettingsSection`）→ `OpenSettingsSection` effect → 协调器回调 → `entry` 走 `settingsSection/{section}` 路由（Route 表既有，本次零改动）后 dispatch `SectionFocused`。**⑤ `devices` 不占分区**：设置页内已有整屏 `DevicesSubPage`，故 `section==='devices'` 映射为 `showDevicesPage=true` + `LoadActiveSessionsEffect`，而不是再做一个只有设备卡的半截页。**⑥ 主动降级**：「隐私和安全」行**删除**而非留假入口——TGX 该页依赖 `getPrivacySettingRules` 一整套分区尚未拆包；连同编造的副标题一起去掉（入口行只保留 label + chevron）。**⑦ 实测发现真实缺陷**：`Scroll` 组件默认 `align` 为**居中**，分区页内容比视口矮时整块卡片被顶到屏幕中部（实测「数据和存储」卡 top=1395、通知卡 top=1197，而头部只到 307）；补 `.align(Alignment.TopStart)` 后 top 分别回到 391 / 405，完整设置页（内容高于视口）逐像素不变。**⑧ 验证**：`feature_settings` **84/84**、`feature_self` **7/7**（新增 6 项：section 映射/卡片集合/reducer 5 例 + self 侧 3 例）；三道静态守卫全绿；`build.sh debug` + `install -r` 冷启动 E2E（127.0.0.1:5555）：我的资料只剩 3 行入口 → 点「设备」出整屏 Devices 子页（THIS DEVICE/ACTIVE SESSIONS 真实 8 条会话 + Terminate All Other Sessions）、点「通知」出仅通知卡的 **NOTIFICATIONS** 页、点「数据和存储」出仅存储卡的 **DATA AND STORAGE** 页（`dumpLayout` + `screenCap` 取证），返回键逐级正确回落到我的资料，完整设置页回归无变化。 |
| DRAWER-101 抽屉导航收尾：账号头进 MVI 状态 + Saved Messages 落地 + 菜单路由收敛（TGX `DrawerController` 对标） | 2026-09-23 ✅ | 主会话（Qoder）：**① 账号头进 reducer**：抽屉账号信息原先由 `entry` 装配层用一套页外 `@State` 直接喂给 `DrawerMenu`（绕过 reducer、无法单测），现收敛为 `ChatListUiState.drawer: DrawerState`（新增 `accountUserId` 字段 + `DrawerStateOptions`/`isDrawerAccountLoaded`），`ChatListDrawerReducer.test.ets` 覆盖回灌、幂等、`copyWith` 不误清；**② 头像走 FileRegistry**：`fetchDrawerAccount()` 取 `getMe` 后按小图→大图优先 `downloadFile`，订阅 `updateFile` 完成再回灌 `OnDrawerAvatarLoaded`，与列表行头像同一套缓存（当前账号无 `profile_photo`，实测回落字母头像 'H'，与"我的资料"页一致，属预期）；**③ Saved Messages 落地**：`openSavedMessages()` 用 `createPrivateChat(my_id)` 解析自聊并 `putChat` 预热，复用 `ChatSelected → NavigateToChat` 链；本地把自聊标题统一渲染为 `Saved Messages`（`isSavedMessagesChat`/`getChatTitle`，列表行与头部同源），对齐 TGX 收藏语义；**④ 路由收敛**：菜单项改为派发 `DrawerItemClicked`/`DrawerAccountClicked`，由 reducer 产出 `DrawerNavigate(target)`/`OpenSavedMessages`，`entry` 只做 `NavTarget` 跳转，`fetchDrawerAccount()` 与 `drawerState` prop 从装配层删除。 **验证**：`check_architecture`/`check_codegen`/`check_design_tokens` 全绿；`feature_chat_list` **37/37** 通过（原 29 项，新增 8 项抽屉用例）；`./tools/ci/build.sh debug` BUILD SUCCESSFUL，`install -r` + 冷启动模拟器 E2E（127.0.0.1:5555，`uitest dumpLayout`/`screenCap` 取证）：≡ 展开抽屉→账号头 'hubery / 13124095890'、Saved Messages → 会话头部标题显示 **Saved Messages**（非 'hubery'）、点账号头 →「我的资料」（@hubery888/生日/设备入口）、Contacts → 联系人页、Settings → 设置页、Night Mode 开关 `checked=false→true` 且截图像素由 `#ffffff` 底色变 `#121212` 深色、再切回恢复浅色。 |
| MEDIA-105 视频消息发送侧（FEAT-MEDIA-002，发送侧全链路 + 接收侧补上传态） | 2026-09-21 ⏳ | 主会话（DSH）：**① 动手前先做缺口核对**——接收侧（解析 `messageVideo`、缩略图优先下载、≤10MB 自动下载、封面/下载环/播放钮/时长角标）已由 **MEDIA-106（09-11 Accepted）** 完整交付，`core/domain` 也**全无视频代码**、`MediaPickerPort` 只有 `pickPhoto`/`pickFile`。故本包只补**发送侧**，复用接收侧渲染不重写。**② 端口**：`MediaPickerPort` 增 `pickVideo`；`PickedMedia` 增 `duration`/`width`/`height`/`thumbnailPath`/`thumbnailWidth`/`thumbnailHeight`（刻意区分「视频尺寸」与「缩略图尺寸」——TDLib `inputThumbnail.width/height` 要的是后者）。两处实现（`FakeMediaPicker`、`HarmonyMediaPickerAdapter`）同步补齐。**③ 领域层**：`MessageProjection.sendVideoMessage` 组 `InputMessageVideo`→`InputVideo`（`InputFileLocal` + 可选 `InputThumbnail`），`supports_streaming=true` 对齐 TGX；**0 值不覆盖 DTO 默认值**（交给 TDLib 探测，避免假 0 盖掉真实值）；空路径判 `parameter/missing` 不开请求。**④ MVI**：新增 `PickAndSendVideo` intent / `PickAndSendVideoEffect` effect / reducer 分支（与选图同构，带出回复目标与草稿 caption 并收起面板）/ coordinator `handlePickAndSendVideo`。**⑤ 补上视频分支缺失的上传态（真实缺陷）**：`resolveMediaAttachment` 的 `messageVideo` 分支此前**只算下载态、没有 `isUploading`/`uploadProgress`、也没订阅上传进度**（图片分支有）——出站视频永远没有进度圈。已按图片分支同构补齐，并在上传完成时释放本地缩略图缓存。**⑥ 出站缩略图先展示**：服务端缩略图就绪前 `video.thumbnail` 恒为 null，气泡会退化成首帧占位；新增 `pendingVideoThumbnails`（key=视频沙箱路径）用发送前本地抽的首帧顶上。**⑦ UI**：附件面板 Gallery 与 File 之间新增 **Video** 入口（此前只有两项）；`MediaBubble` 的 `video` 分支新增**上传态并置于最前**——出站视频本地文件天然 `isDownloaded=true`，沿用「先判 isDownloaded」会直接显示播放钮、进度永远不可见；新增 `ic_video.svg`（feature/chat 与 entry 双份，同 `ic_photo` 惯例）。**⑧ 适配器（entry，业务层保持 Kit-free）**：`pickVideo` 走 `PhotoViewPicker` + `VIDEO_TYPE`，虚拟 URI→沙箱物理路径复用 FIX-002 零拷贝；`enrichVideo` 用 `AVMetadataExtractor` 读时长/宽高、`AVImageGenerator.fetchFrameByTime` 抽首帧、`ImagePacker.packToFile` 落盘 JPEG，取帧按等比缩到长边 ≤320（不用 320×320 硬拉伸）；元数据/抽帧任一步失败**只降级留空不阻断发送**。**⑨ 验证**：受影响 4 模块单测 **427/427 PASS（0 Failure / 0 Error）**，新增 8 例已按 `test_result.txt` 逐条确认实际执行；三道静态守卫全绿（architecture 0 违规 / codegen 字节一致 / design token 0 未定义）；签名构建 BUILD SUCCESSFUL。**模拟器实测**：附件面板三入口渲染正确、点 Video 后系统相册以**「所有视频」**标签打开（`VIDEO_TYPE` 生效）、取消选择后正确回退且无异常。**⑩ 未验证项（已明确登记）**：该模拟器镜像相册**无任何视频素材**（选择器空、原始目录 SELinux 拒绝、录制瓦片不可用），故「选到视频→缩略图→上传进度→发送成功」整链**未跑通**，需在含视频素材的设备上按报告附录清单勾选补验——状态保留 `Verifying` 而非 Accepted。**⑪ 环境发现（影响后续所有模拟器验证）**：设备上已装的包是**未签名**安装（`appSignType: none`），签名 HAP 一律被拒（`code:9568332 install sign info inconsistent`）；正确姿势是推 `entry-default-unsigned.hap` 后 `bm install -r`，实测成功且**登录态完整保留**（若走 uninstall+install 会清掉登录态且需主人短信验证码，无法自行恢复）。另 `resolve-toolchain.sh` 为 bash 脚本，zsh 下 `source` 会静默失败，须 `bash -c` 调用。证据报告 `work-items/accepted/MEDIA-105.md`。**⑫ 联调追加（2026-09-21 主人实测）**：反馈「视频上传」气泡形态异常，**先定性再动手**——hilog 实证本次发送走的是「文件」入口而非「视频」入口（`copied uri=file://docs/storage/Users/currentUser/Download/com.huawei.hmos.browser/mda-*.mp4` + `send_document_message_ok`；而 `send_document_message_ok` 在生产代码中**唯一调用点**是 `handlePickAndSendFile`），故产出 `messageDocument`→`DocumentBubble`，**属设计内行为且与 Telegram 一致**。根因是该 mp4 由浏览器下载、落在**文件区**而非相册媒体库，「视频」入口（`PhotoViewPicker`+`VIDEO_TYPE`）看不到它——与首轮验证相册为空的阻塞同源。**顺带修掉截图暴露的真实缺陷：沙箱缓存名泄漏到气泡标题**（标题显示 `1789987100759_8836_…`）。根因：TDLib 的 `inputDocument`/`inputVideo` **都没有 `file_name` 字段**（已核对 DTO），它拿 `InputFileLocal` 的 **basename** 反推 file_name，而原实现在文件名上拼了 `${ts}_${rand}_` 前缀。修复：唯一性改由**父目录**承担、最后一段保留原名（`uploads/<ts>_<rand>/<原名>`），`entry/.../HarmonyMediaPickerAdapter.copyUriToCache`；对**新发送**生效，旧消息不回改。「视频」入口整链仍未跑通（设备相册无视频素材、媒体库目录 SELinux 拒绝、`mediatool` 无导入子命令），状态保持 `Verifying`。全量单测 **1147/1147 PASS**（22 模块 0 Fail / 0 Error）、三道静态守卫全绿、签名构建 BUILD SUCCESSFUL。 |
| SET-105 聊天字号调节（FEAT-UI-002） | 2026-09-21 ✅ | 主会话（DSH）：**① 模型**：`core/design_system/FontScale.ets` 新增聊字号档位模型，与既有系统字号 `fontScale`（0.85–2.0 连续、全站）**正交**——`CHAT_FONT_SIZE_STEPS = {12,13,14,15,16,18,20,22,24,26}`（逐值对齐 TGX `Settings.CHAT_FONT_SIZES`，默认 15、10 档），只作用于消息内容文字。换算取**倍率语义**而非 TGX 的绝对字号：`chatTextSizeOf(base, size, diff) = base × (size/15) + diff`，下限 `MIN_READABLE_FONT_SIZE = 11vp`。选倍率的理由：消息文字取自 9 档排版 token（body/subheadline/caption/footnote…），换绝对字号会抹平 token 层级；倍率保留层级，且默认档倍率=1.0 → **默认视觉与改造前逐像素一致**（实测 15 档控件几何改造前后完全相同）。**② 接线**：`Theme.chatFontSize` + `ThemeManager.setChatFontSize/getChatFontSize` 贯通；`feature/chat/ChatPage.ets` 12 处消息内容文字接入（diff 对齐 TGX `TGMessage`：正文 0 / 置顶摘要 −1 / 副标题 −2 / 反应 −4）；`feature/settings` 新增「Chat Text Size」卡片（MVI 三层：`ChangeChatFontSize` intent / reducer / coordinator + `Slider` 10 档 + 与聊天页同源的实时预览行）。**③ 作用域边界**：时间戳 / 日期分隔 / 顶栏 / 输入栏 / 长按菜单 / 空态**不缩放**——对齐 TGX `timeProvider`（固定 11sp、无字号监听器）；实测三档下这些控件几何恒定。**④ 持久化**：新增 `entry/src/main/ets/store/SettingsStore.ets`（`preferences` Kit），业务层只写 `AppStorage`、零 Kit 依赖，entry 在 `@StorageProp` + `@Watch` 里落盘、启动时读盘回灌 `ThemeManager`。**⑤ 实测发现并修复 2 个真实缺陷**：**(a) 置顶栏写死 `.height(48)`**——栏内两行文字接入字号后，26 档需 ≈58vp，被压回去后向上溢出 66px 压到列表首条消息（`uitest dumpLayout` 实证：容器 `[91,363][1116,502]` 而内容伸到 568；自动重叠审计报 2 处相交）。改 `constraintSize({ minHeight: 48 })` + 竖彩条跟随字号，修复后容器 205px 恰好包住两行、重叠归零，15 档几何与改造前一致（`[91,374][1116,492]`）。**(b) `PersistentStorage.persistProp` 恢复静默失效**——设置到 26 后落盘文件确实是 `<string key="chatFontSize">26</string>`，但杀进程重启后 `AppStorage.get` 拿到默认 `15`；同进程内用 `fileIo` 读同一文件内容仍是 26（文件在、值在、权限够），`setTimeout(1500ms)` 再读仍是 15（排除异步未落），声明成 `<number>`/`<string>` 结果一致。即该 API 在本 SDK 上读写两端对不上且失败静默。已整体弃用，改走 `preferences` Kit（落盘为 `<double key="chatFontSize" value="26.000000"/>`）。**⑥ 验证**：模拟器（127.0.0.1:5555）三档真机实测——设置页预览行 778×64 / 973×80 / 903×272(2 行) 与 12/15/26 比例精确吻合；聊天页同文本几何 323×56 / 403×70 / 700×121（26/15 = 1.729，期望 1.7333）；自动重叠/越界审计三档均 `OVERLAPS: 0 · out-of-gutter: 0`；持久化双向闭合（26 → 重启恢复 26 → 落回 15 → 重启恢复 15）。单元测试 **445/445 PASS**（common 50 + design_system 53 + settings 73 + chat 240 + entry 29，新增 15 个聊字号用例含字符串往返回归）；四道静态守卫全绿；签名构建通过。证据报告 `outputs/set105-verify/set105-verify-report.html`（自包含，内嵌 8 张截图 + 三档原始布局树 JSON）。**⑦ 已知边界**：链接预览卡内部描述文本用的是既有硬编码 `fontSize(13)`，未纳入缩放，需主人裁决是否一并接入；媒体/文件/语音气泡的缩放仅有单测覆盖，本会话无对应消息故无真机几何证据。证据报告补充 `work-items/accepted/SET-105.md`。 |
| FIX-003 深色主题全局适配补齐（SET-102 遗留：19 个页面硬编码 `LightTheme` 致主题开关空转）+ core_domain 2 个出生即红的测试修复 | 2026-09-21 ✅ | 主会话（DSH）：**① 根因**：SET-102（09-14 Accepted）只交付了「ThemeManager 状态机 + 设置页自身响应」，抽屉夜间模式开关虽已接线到 `ThemeManager.setPreference`，但 19 个页面文件顶部一律硬编码 `const T = LightTheme;`，拨动开关只改 `isNightMode` 布尔量、零界面重渲染。**② 方案（单一响应式桥，不做 20 份订阅）**：`entry/Index.ets` 装配层在 `aboutToAppear` 以 `ThemeManager.getInstance().getTheme()` 作初值并 `subscribe()`，统一写入 `AppStorage('activeTheme')`、`aboutToDisappear` 退订；各页面改为 `@StorageProp('activeTheme') T: Theme = LightTheme` 消费，`T.<ns>.` 全量改写为 `this.T.<ns>.`（1715 处）。**③ 致命踩坑**：ArkUI build 转换**不识别 struct 内自定义 getter**——`private get T() { return this.activeTheme }` 会让渲染期 `this.T` 为 `undefined`，启动即 `TypeError: Cannot read property colors of undefined`（jscrash-...20260921150237118.log 实锤）；必须用装饰成员而非 getter。**④ 抽屉开关双触发**：`DrawerMenu` 的 `Toggle.onChange` 与父 Row `onClick` 同帧各取反一次互相抵消（日志 `night mode changed: true` 紧接 `false`），去掉 `onChange` + 加 `hitTestBehavior(HitTestMode.None)`，交由 Row 统一处理。**⑤ 附带修复**：`core/domain/MessageProjection.test.ets` 两个用例（由 `50f1b326` MSG-108 / `b63d9121` REACTION-101 引入）自诞生即红——`addCachedMessage` 契约是「只进 `messagesById` 查询缓存、不进 `orderedIds` 展示列表」，用例却断言 `state.messages.length === 1`，恒为 0；已改为 `getMessageById(id)` 读回，断言目标不变、产品代码零改动。**⑥ 验证**：模拟器端到端实测抽屉/会话列表/聊天页/搜索页/设置页全局转暗，且抽屉与设置页开关双向同步；四道静态守卫（architecture/codegen/design_tokens/secret_scan）全绿；全量单测 **1124/1124 全绿**（22 模块 0 fail / 0 error，`test_all_modules.py` EXIT=0）。 |
| GIF-101 GIF 动图搜索面板与动图消息发送（FEAT-P2-005，全链路闭环） | 2026-09-20 ✅ | AI-Agent-Antigravity：① **GIF 领域模型（feature/chat）**：新建 `GifItem.ets` 导出 `GifItem` 接口（id/fileId/caption/category/thumbnailUrl/width/height）、`GIF_CATEGORIES`（7 分类：Recent/Trending/Funny/Celebration/Love/Reactions/Animals）、`DEFAULT_RECENT_GIFS`（4 预置）与 `DEFAULT_GIF_PACKS`（6 分类×5 GIF，fileId 2001-2040）；② **状态契约扩展**：`ChatUiState` 扩充 `recentGifs: readonly GifItem[]`（默认 DEFAULT_RECENT_GIFS）与 `gifSearchQuery: string`；新增 `SendGif`/`ClearRecentGifs`/`SearchGifs` 意图类追加至 `ChatIntent` 联合；新增 `SendAnimationMessage` effect 追加至 `ChatEffect` 联合；③ **纯函数状态机**：`ChatReducer` 新增 `sendGif`（置顶去重 + 32 条上限 + emit SendAnimationMessage）、`clearRecentGifs`、`searchGifs` 三个 case；④ **副作用协调**：`ChatCoordinator.executeEffect` 新增 `sendAnimationMessage` 分支；⑤ **GIF 面板 UI（EmojiBoard）**：完整实现 `GifView()` Builder：搜索栏、7 分类胶囊水平导航栏、2 列 GIF 网格（100vp + GIF 角标）、Recent 空态；⑥ **单元测试**：4 组测试全部通过（`BUILD SUCCESSFUL in 43s`）；⑦ **静态防线**：`check_architecture.py` 0 违规，`check_codegen.py` 全 PASS；⑧ **模拟器**：signed HAP 安装成功（`install bundle successfully`）；⑨ **模拟器端到端实测（2026-09-21 主会话-DSH 接力补验，7 场景全过）**：模拟器（127.0.0.1:5555）真实登录态下——进 be hu 私聊 → 笑脸唤起 EmojiBoard 切 GIF Tab（搜索栏 + 🕒Recent/🔥Trending/😂Funny/🎉Party 分类胶囊 + Recent GIFs 网格 + Clear 全部渲染）→ 点 👍Thumbs Up 直发，会话即现「🎬 Thumbs Up」出站消息（带✓）→ 再发 🎉Party Hard 后 Recent 区其动态置顶第 1 位 → 切 🔥Trending 标题行变「🔥 Trending · 5 GIFs」网格同步切换 → Clear 后呈现居中时钟 +「No Recent GIFs」空态。截图未入库（含真实会话内容，按仓库脱敏约定仅文字留证）。证据报告 `work-items/accepted/GIF-101.md`。 |
| STICKER-101 贴纸面板与贴纸消息发送（FEAT-P2-004，全链路闭环） | 2026-09-20 ✅ | AI-Agent-Antigravity：① **契约与领域模型扩展（feature/chat）**：`StickerSetPreviewState.ets` 导出 `StickerPack` 接口与 `DEFAULT_STICKER_PACKS`（4 大分类：`🕒 Recent`, `🔥 Hot Cherry`, `🐱 Cute Animals`, `😎 Cool Moods`，覆盖 fileId 1001-1045）及 `DEFAULT_RECENT_STICKERS`；`ChatUiState` 扩充 `recentStickers: readonly StickerSetItem[]`（默认内置常用贴纸）；新增 `SendSticker(sticker)` 与 `ClearRecentStickers()` 意图契约类并统一从模块入口导出；② **纯函数 Reducer 状态机与 Coordinator 路由（ChatReducer & ChatCoordinator）**：`sendSticker` 意图支持点击贴纸动态置顶至 `recentStickers`（自动去重并截断至上限 32 项）并派发 `SendStickerMessage` 领域副作用；`clearRecentStickers` 意图清空最近贴纸列表为 `[]`；`sendStickerFromPreview` 意图同步执行 `recentStickers` 置顶去重；`ChatCoordinator` 扩充 mock sticker fileId 容错范围至 1045，避免脱机或测试环境下 TDLib 请求报错；③ **Telegram X 风格贴纸面板与消息气泡（EmojiBoard & ChatPage）**：`EmojiBoard` 深度重构 Sticker Tab 视图：横向滚动贴纸包导航条（带 28vp accent 高亮指示下划线）、贴纸包标题栏（包含贴纸包名、贴纸总数及「查看贴纸包」胶囊按钮，可唤起全览弹窗）、5 列大图贴纸网格（72vp 高度，点击即发）；Recent 分类支持双模渲染（有记录展示 \"Recent Stickers\" + \"Clear\" 按钮与 5 列网格；空记录展示时钟图标 + \"No Recent Stickers\" 居中优雅空态）；`ChatPage` 接入 `recentStickers` 状态及发送/清空回调，`StickerBubble` 降级文本 emoji 提升至 72vp 大图透明无气泡渲染，右下角展示时间与双勾状态胶囊，彻底对齐 Telegram X 标准；④ **单元测试与静态防线**：`ChatReducer.test.ets` 新增 4 组置顶去重、32 项截断、清空与预览弹窗发送测试用例，`feature_chat` 模块单测 100% 通过（`BUILD SUCCESSFUL in 41 s 529 ms`）；`check_architecture.py` 0 违规（100% Kit-free）、`check_codegen.py` 全部 PASS、`secret_scan.py` 0 泄露；⑤ **模拟器（127.0.0.1:5555）端到端实测取证**：输入栏唤起表情面板、切换 Sticker Tab 展现 4 大贴纸包导航条、切换 Cute Animals 贴纸包呈现分类标题与 5 列动物大图、点击 🐼 贴纸直接发送且会话呈现独立透明贴纸与双勾时间戳、切回 Recent 分类验证 🐼 贴纸实时去重置顶排在第 1 位、点击 Clear 按钮清空并验证优雅空态全链路验证。证据报告 `work-items/accepted/STICKER-101.md` 与 `walkthrough.md`。 |
| EMOJI-101 输入栏表情面板（FEAT-P2-005，全链路闭环） | 2026-09-20 ✅ | AI-Agent-Antigravity：① **契约与状态层扩展（feature/chat）**：`ChatUiState` 扩充 `recentEmojis: readonly string[]`（内置 `DEFAULT_RECENT_EMOJIS` 16 个高频项）；新增 `ClearRecentEmojis` 意图契约类并统一从模块入口导出；② **纯函数 Reducer 状态机增强（ChatReducer）**：`insertEmoji` 意图支持输入框正文追加表情并动态置顶至 `recentEmojis`（自动去重并截断至上限 32 项）；`clearRecentEmojis` 意图清空最近记录为空数组；`backToChatList` 优先消费拦截 `showEmojiBoard: true`（物理返回键优先收起表情面板，不退出当前会话）；③ **Telegram X 风格表情面板组件（EmojiBoard & ChatPage）**：对标 Telegram X `EmojiSection.java` 规格提供 8 大分类（`🕒 Recent`, `😀 Smileys`, `🖐 Gestures`, `🐶 Animals`, `🍔 Food`, `🚗 Travel`, `💡 Objects`, `🏁 Flags`）；分类条支持横向滚动与 28vp accent 高亮指示下划线；分类 0 Recent 支持双模渲染（有记录展示 "Recent" + "Clear" 按钮与 8 列表情网格；空记录展示时钟图标 + "No Recent Emojis" 居中优雅空态）；底栏保留 Emoji/Sticker/GIF 三 Tab 与右下角退格（Backspace）按键；`ChatPage` 接入 `recentEmojis` 与 `onClearRecent`，`TextInput` 聚焦时自动收起表情面板避免键盘冲突；④ **单元测试与静态防线**：`ChatReducer.test.ets` 新增 4 组表情置顶去重、上限截断、清空与返回拦截测试用例，`feature_chat` 模块单测 100% 通过（`BUILD SUCCESSFUL in 36 s 458 ms`）；`check_architecture.py` 0 违规（100% Kit-free）、`check_codegen.py` 全部 PASS、`secret_scan.py` 0 泄露；⑤ **模拟器（127.0.0.1:5555）端到端实测取证**：表情面板点击笑脸展开、8 大分类横向切换高亮、点击表情插入正文与 Recent 动态置顶更新、右下角退格删除末尾字符、点击 Clear 清空显示优雅空态、点击键盘按钮平滑收起面板全链路验证。证据报告 `work-items/accepted/EMOJI-101.md` 与 `walkthrough.md`。 |
| REACTION-101 消息表情回应（FEAT-P2-001，全链路闭环） | 2026-09-18 ✅ | AI-Agent-Antigravity：① **领域层 TDLib 增量推送接入（core/domain）**：`MessageProjection` 引入 `UpdateMessageInteractionInfo`，在 `handleUpdate` 中补充处理分支，当 `interactionUpdate.chat_id === this.chatId` 时增量覆盖内存中对应 `Message` 的 `interaction_info` 并触发 `notifyStateChanged` 广播；② **契约扩展与反选归零清理（feature/chat）**：`MessageActionMenuState` 扩展 `availableReactions: string[]`（默认 Telegram X 常用六表情 `['👍', '❤️', '🔥', '🎉', '👏', '😂']`）与 `chosenReactions: string[]`；`ChatReducer` 修复反选导致计数归 0（`newCount === 0`）时未清理残余 Chip 的缺陷，使用 `.filter((chip: ReactionChip) => chip.count > 0)` 彻底清除空胶囊；长按呼出菜单时从目标消息 `reactionChips` 提取已选 emoji 并回填 `chosenReactions`，且与当前激活菜单保持双向同步；③ **Telegram X 风格视觉与交互反馈（ChatPage）**：`ActionMenuSheet` 顶部快捷表情栏动态按 `menu.availableReactions` 驱动，当前消息中自己已选的表情呈现 2vp accent 高亮边框与主题色背景指示；点击表情分发 `ToggleMessageReaction` 并自动关闭菜单；气泡底部 `ReactionsRow` 完整呈现 Reaction Chips（选中态 accent 蓝底白字、未选态灰底深字），直接点击 Chip 即可触发反转；④ **单元测试与静态防线**：`MessageProjection.test.ets` 新增 `updateMessageInteractionInfo` 增量推送单测，`core_domain` 模块单测 100% 通过（`BUILD SUCCESSFUL in 29 s 15 ms`）；`ChatReducer.test.ets` 新增反选归零移除、actionMenu 状态同步与已选回填 3 组单测，`feature_chat` 模块单测 100% 通过（`BUILD SUCCESSFUL in 39 s 770 ms`）；`check_architecture.py` 0 违规（Kit-free 100%）、`check_codegen.py` 全部 PASS、`secret_scan.py` 0 泄露；⑤ **模拟器（127.0.0.1:5555）端到端实测取证**：气泡底部胶囊双态展示、长按呼出菜单快捷栏高亮指示环、点击表情反转计数及背景色、气泡直接点选切换、点赞数降为 0 胶囊无缝消失全流程测试。证据报告 `work-items/accepted/REACTION-101.md` 与 `walkthrough.md`。 |
| FOLDERS-101 会话分组标签栏与归档箱（FEAT-CHATLIST-003） | 2026-09-18 ✅ | AI-Agent-Antigravity：① **模型与契约层扩展（feature/chat_list）**：`ChatListItem` 增加 `category: ChatCategory`（`'private' | 'group' | 'channel'`）；`ChatListUiState` 增加 `selectedFolderId` 与 `folders: readonly ChatFolderTab[]`；提供纯函数 `filterChatsByFolder`（精准分流私聊/群聊/频道/未读）；`SelectChatFolder` intent 与纯函数 Reducer（幂等切换）；`ChatListCoordinator` 映射 TDLib `chat.type_`（结合 `ChatTypeSupergroup.is_channel`），并在 `syncFromProjection` 中动态汇算各 Tab 未读数；② **UI 组件与手势交互（ChatListPage）**：实现 `FolderTabBar`（水平滚动、选中项主题色高亮、未读胶囊角标、Tab 底部 3vp accent border 指示线）；会话列表挂载水平手势 `PanGesture`，左滑/右滑平滑切换 Tab；`getFilteredItems` 驱动 `LazyForEach` 局部刷新；`ArchivedChatsRow` 仅在 All 模式展示，进入归档页后隐藏 Tab 栏，返回主列表自动恢复；实现各分类专属空状态 `FolderEmptyView`；③ **单元测试与静态防线**：`ChatListReducer.test.ets` 新增 4 组 Tab 切换与过滤断言，`feature_chat_list` 单测 100% 通过（`BUILD SUCCESSFUL in 24 s 770 ms`）；`check_architecture.py` 0 违规（Kit-free 100%）、`check_codegen.py` 全部 PASS、`secret_scan.py` 0 泄露；④ **模拟器（127.0.0.1:5555）端到端取证**：All/Personal/Groups/Channels/Unread 点击与滑动手势切换截图、归档箱出入与返回截图。证据报告 `work-items/accepted/FOLDERS-101.md` 与 `walkthrough.md`。 |
| CHANNEL-101 频道帖子评论页与音视频便签增强（FEAT-P2-002 / FEAT-P2-003 / VIDEONOTE-101 / AUDIO-101） | 2026-09-18 ✅ | AI-Agent-Antigravity：① **类型安全深链与路由定义（NAV-001）**：`core/navigation` 扩充 `CommentsRoute`（`name: 'comments', chatId, messageId`），加入 `AppRoute` 联合与 `comments/:chatId/:messageId` 模板；`RouteCodec` 完善正反向序列化并补充单元测试（`core_navigation` 单测全部 PASS）；② **领域层讨论组与 Thread 协议装配**：`core/domain/MessageProjection` 接入 TDLib `GetMessageThread` 与 `GetMessageThreadHistory` 封装，自动解析关联讨论超级群（discussion supergroup）与 `message_thread_id`，补充单测（`core_domain` 76/76 PASS）；③ **协调器与 MVI 闭环（feature/chat）**：新建纯 ArkTS 100% Kit-free `CommentsCoordinator`，支持频道信息/帖子内容并发解析、历史评论拉取、`sendComment` 派发与本地即时乐观回显；新建 `CommentsPage` 呈现顶栏导航、吸顶主帖卡片、头像调色板/评论气泡列表与圆角回复输入框；5 组核心单测 100% 通过（`feature_chat` 185/185 PASS）；④ **音视频便签增强（VIDEONOTE-101 / AUDIO-101）**：`AudioBubble` 接入 `PlayVoiceNote`/`PauseVoiceNote` 意图，支持播放/暂停动态切换、细粒度进度条与 `00:15 / 03:20` 时间显示；`VideoNoteBubble` 增加点击切换有声/静音（`.muted(activeVideoNoteId !== item.messageId)`）并左上角悬浮扬声器角标（`ic_volume_up` / `ic_mute`）；三处资源目录同步补齐 `ic_volume_up.svg`；⑤ **装配层接线与生命周期维护（entry）**：`Index.ets` 引入 `CommentsRoute` 与 `CommentsPage`，在 `openChat` 注入 `onOpenComments`，在 `onRouteChanged` 精确处理创建、更新与销毁，在 `aboutToDisappear` 统一清理，在 `build()` 条件渲染；⑥ **测试与防线**：`core_navigation`、`core_domain`、`feature_chat` 单测全过；4 项静态安全防线（架构、代码生成、设计令牌、机密扫描）0 违规；构建 signed debug HAP 成功并安装至模拟器 `127.0.0.1:5555`。证据报告 `work-items/accepted/CHANNEL-101.md`。 |
| VOICE-101 语音消息录制与波形播放（FEAT-P2-006，全链路闭环） | 2026-09-18 ✅ | AI-Agent-Antigravity：① **系统 Kit 解耦与端口契约（ARCH-001）**：在 `platform/ports` 建立纯 ArkTS 接口与 Double：`AudioPlayerPort.ets`（play/pause/resume/stop/seek/进度与完成回调）与 `AudioRecorderPort.ets`（startRecord/stopRecord/cancelRecord/时长），配套单测 100% 通过；② **领域层语音协议装配**：`core/domain/MessageProjection` 增加 `sendVoiceNoteMessage`，组装 TDLib `InputMessageVoiceNote` / `InputVoiceNote`，单测 100% 通过；③ **MVI 状态机与协调调度（feature/chat）**：`MediaAttachment.ets` 实现 5-bit 波形编码与解码互逆闭环；`ChatUiState` 扩充录音/播放状态；`ChatIntent` & `ChatEffect` 扩充意图；`ChatReducer` 状态转移纯函数推导，补充 7 组单测；`ChatCoordinator` 注入硬件端口与缓存路径，调度秒级录音计时、无缝文件发送与播放资源生命周期释放；④ **Telegram X 风格 UI 渲染**：`ChatPage` 实现全宽录音条 `VoiceRecordingBar`（呼吸红点、实时 mm:ss 计时、✕ 取消、➤ 发送），气泡 `VoiceNoteBubble` 响应播放/暂停切换，28 柱波形条随播放进度动态高亮推移，LazyForEach 引入 `extraVoiceSig` 播放签名杜绝全量重排；⑤ **生产适配器与装配（entry）**：声明 `ohos.permission.MICROPHONE` 权限；在 `entry/platform` 实现 `HarmonyAudioPlayerAdapter` 与 `HarmonyAudioRecorderAdapter`，具备模拟器/音频管线不可用时的降级兜底保护；在 `Index.ets` 实例化注入；⑥ **验证与门禁**：`platform_ports` (5.4s)、`core_domain` (23.9s)、`feature_chat` (33.9s) 单元测试全部通过；4 项秒级静态安全防线（架构、代码生成、设计令牌、机密扫描）0 违规；构建 signed debug HAP 成功并安装至模拟器 `127.0.0.1:5555`。证据报告 `work-items/accepted/VOICE-101.md`。 |
| PROFILE-102 群/频道资料页（TGX 缺口顺序 2，ChatProfile 完整闭环） | 2026-09-16 ✅ | AI-Agent-Antigravity：① **领域层群组注册表与查询**：在 `core/domain` 实现 `GroupRegistry`（`BasicGroup`、`BasicGroupFullInfo`、`Supergroup`、`SupergroupFullInfo`、`ChatMember[]` 实体缓存、Gateway `updateBasicGroup`/`updateSupergroup` 等事件订阅、监听器订阅与 TDLib 异步请求封装）；导出 `formatMemberCount`、`getGroupAvatarLetters`、`formatMemberRole` 等辅助函数；配套 `groupRegistryTest` 13 组单测全通，`core_domain` 模块单测 100% BUILD SUCCESSFUL；② **特性层 MVI 契约与纯函数状态机**：`feature/profile` 新增 `ChatProfileUiState`（含 `ChatProfileUiStateOptions` 显式选项接口，支持不可变 `copyWith`）、`ChatProfileIntent`（`OpenChatProfile`, `OnChatLoaded`, `OnBasicGroupFullInfoLoaded`, `OnSupergroupFullInfoLoaded`, `OnChatMembersLoaded` 等）、`ChatProfileEffect`；`chatProfileReducer` 纯函数实现，100% Kit-free；`ChatProfileReducer.test` 10 组单测全通；③ **群资料协调器与端口集成**：`ChatProfileCoordinator` 100% Kit-free，注入 `AccountScope`, `ChatRegistry`, `GroupRegistry`, `UserRegistry`, `FileRegistry`, `ClipboardPort`, `ToastPort` 与 `Logger`；支持群详情聚合、头像异步下载回灌、首屏成员列表解析，点击成员触发导航至 `userProfile`，点击复制链接调用 `setPlainText` + `showToast`；④ **Telegram X 风格群资料页 UI**：`ChatProfilePage` 实现大头像、群标题、成员数副标题、描述卡片、邀请链接卡片、成员列表（头像/昵称/在线状态/管理员身份角标）；100% 遵从 Design Tokens；⑤ **装配层接线**：`entry/Index.ets` 串联 `ChatCoordinator` 点击群标题触发 `openChatProfile` 导航至 `chatInfo` 路由分支，管理生命周期与返回栈；⑥ **测试与门禁验证**：`core_domain` 与 `feature_profile` 两个模块单元测试全部 BUILD SUCCESSFUL，架构防线 `check_architecture.py` 0 违规，代码生成 `check_codegen.py` 全部 PASS，设计令牌 `check_design_tokens.py` 0 违规。 |
| PROFILE-101 群聊点头像查看用户资料（个人中心 MVP，PROFILE Epic 提前拆包） | 2026-09-16 ✅ | 主会话（DSH）：对齐 Android `TGMessage.onAvatarClick → openPrivateProfile`：① `core/navigation` 新增 `UserProfileRoute(userId)` + 编解码 + RouteName/union；② `core/domain/UserRegistry` 补 `getUserFullInfo`/`updateUserFullInfo` 订阅（bio）；③ `feature/chat` 打通 `senderUserId` 全链路（SenderInfo→MessageItem→`OpenUserProfile` 意图→coordinator→entry），群聊头像 onClick；④ 新建 **feature/profile 模块**（MVI 契约 + 纯 reducer + ProfileCoordinator：双注册器缓存先回灌+订阅刷新、头像大图 downloadFile+subscribeFile 回灌、createPrivateChat 效应→导航私聊；ProfilePage 大头像/姓名/在线状态/@username/手机号/bio/「Message」，全 design tokens）；⑤ entry 接线（依赖/路由分支/协调器生命周期/ChatCoordinator options 注入）；⑥ 测试与门禁：core_navigation 42→46、feature_profile 0→6、core_domain 78、feature_chat 172 全 PASS，静态守卫 0 违规，assembleHap BUILD SUCCESSFUL；**⑦ 真机验证 6/6 全过**（华为真机 signed 部署：El CLUB 点 Rose 头像 → 资料页（头像/online/@MissRose_bot）；Message → create_private_chat_ok → 私聊；返回栈正确；Redmi 群真人 @nekoko32：姓名/last seen recently/username/bio 渲染正确，隐私隐藏 phone 行不显示）。**协作修复**：SEARCH-103 在途代码两处 ArkTS 跨 return 收窄编译失败，代为改双 if（逻辑零改动）。证据 `work-items/accepted/PROFILE-101.md` |
| SEARCH-102 聊天内搜索 + 结果跳转定位（FEAT-SEARCH-002 完整闭环） | 2026-09-16 ✅ | 主会话（DSH）：① **领域层** `core/domain/MessageProjection` 新增 `searchMessages(query, fromMessageId, limit)`（组 `SearchChatMessages`，chat_id 固定在投影会话，空关键词判 `parameter/invalid/query` 不发起请求）与 `loadHistoryAround(messageId, radius=30)`（复用 `requestHistory`——`from_message_id=目标` + **负 offset** 返回目标及其邻近，limit=2*radius 使目标居中；在途守卫返回 null）；纯 ArkTS/Kit-free。② **特性层契约**：`feature/chat` 新增不可变模型 `ChatSearchResultItem`（messageId/textSnippet/dateText/jumpable）；`ChatUiState` 扩展 `showSearch/searchQuery/searchResults/isSearching/jumpToMessageId`；意图 `OpenChatSearch/CloseChatSearch/ChatSearchQueryChanged/PerformChatSearch/OnChatSearchLoaded/OnChatSearchFailed/JumpToSearchResult/OnJumpToMessageReady/ClearJumpToMessage` + 效应 `FetchChatSearch/JumpToMessage`；reducer 全链纯函数推导（展开/收起/query 更新/执行 trim 后非空才发 effect/结果与失败回灌/跳转关面板置 `jumpToMessageId`/Ready 回灌/Clear 清标记）。③ **协调调度**：`ChatCoordinator.executeEffect` 新增 `fetchChatSearch`（解码 FoundChatMessages → `messageContentText` 摘要 + `formatSearchResultDate` 日期 + `getMessageById` 判 jumpable，失败统一回灌 AppErrors）与 `jumpToMessage`（`loadHistoryAround` 拉完回灌 `OnJumpToMessageReady`，在途静默跳过）。④ **UI**：`ChatPage` 顶栏右侧新增 `🔍` 搜索入口；根 Stack 新增 `SearchPanel` 覆盖层（`✕` 收搜 + TextInput 300ms 防抖自动搜索 + 加载/无结果/结果列表三态，独立 searchScroller，ForEach 迭代 `.slice()` 可变数组，行内摘要+日期+「Tap to load」提示）；`onUiStateChanged` 顶部新增跳转逻辑——`jumpToMessageId>0` 在 messages 中定位 → `scrollToIndex(CENTER)` 居中并派发 `ClearJumpToMessage`，完成跨页跳转定位。⑤ **自动化测试与门禁验证**：`core_domain` 新增 5 组（search 组包/空 query 拒绝/分页游标/loadHistoryAround 负 offset 与 limit/在途 null 守卫），单测 73→78；`feature_chat` 新增 `ChatReducer` 11 组用例，单测 155→169；全仓 **19 模块单测 922/922 项 100% 全部通过**；Design Token、架构边界、Codegen 0 违规，`hvigorw test` BUILD SUCCESSFUL，沉淀报告 `work-items/accepted/SEARCH-102.md`。**⑥ 2026-09-16 用户反馈修复：全局搜索点消息结果未定位（原为"没做 + 隐患"）**：根因① `feature/search` 已传出 `messageId`、entry 也收到，但 `ChatRoute` 无该字段，`navigateTo({name:'chat',chatId})` 直接丢弃 → 跨页链路缺失；根因② `ChatPage` 跳转滚动会被 40–50ms 的「首屏未读/尾部新消息」延迟自动定位覆盖，且延迟滚动用旧下标（历史前插致下标位移）。修复：`core/navigation` 增加 `ChatRoute.messageId` + RouteCodec 编解码 + 路由 spec（旧形态/深链不变）；entry `navigateToChatFromSearch` 把 messageId 带进路由、路由观察分支交给 `openChat(…, jumpTo)`、同会话再跳转走 `requestChatJump`（chatId:messageId 去重）；`ChatCoordinator.requestJumpToMessage` 统一两条入口，投影忙时排队待空闲再拉围绕历史（原 effect 复用该通道）；`ChatPage` 跳转独占优先 + 滚动时刻重解析下标 + `jumpAnchorLock` 锚定锁（用户滑动即释放）+ 空关键词引导文案。门禁：`core_navigation` 37→42、`feature_chat` 169→172，全量 **19 模块 930/930 PASS**，design-token/架构/codegen 0 违规，assembleHap 成功；模拟器实测：全局搜索 `aaa` 点结果 → `navigate from search … messageId=26214400` → `chat jump requested` → `jump_to_message_ok`，目标居中且更新的消息在其下方；会话内唯一词 `foxiteu` → `chat_jump_scroll index=3 total=32` 精确居中。证据见 `work-items/accepted/SEARCH-102.md` 追加章节。**⑦ 2026-09-16 用户复测补充：跳转无视觉标识 → 补高亮**：复测确认跳转链路本身是通的（`navigate from search … messageId=29360128` → `jump_to_message_ok` → `chat_jump_scroll index=19 total=32`，目标行中心≈1413=列表正中），但短会话里跳转前后画面差别小、且目标消息**没有任何视觉标识**，用户主观上等于"没跳"（首版只实现了定位，PROGRESS 写的"高亮"未落地）。修复：`ChatPage` 落位后目标行整行着色 `T.colors.chatOutgoing`（浅色 #FFD9F1FF / 深色 #FF1D3D52）持续 4s（`JUMP_HIGHLIGHT_MS`）——首版用中性灰 #FFF0F0F0 在浅色主题上几乎看不出，用户第二轮仍反馈"看不出跳"，故换蓝色调并延长，高亮态纳入 `messageRowKey` 保证单行重绘，用户手动滑动立即结束高亮；`autoScrollUnlessJumped` 增加 `jumpToMessageId > 0` 判据，`aboutToAppear` 首屏锚点同样让位给跳转。取证：dump 中目标行 `Column [0,1322][1256,1504] backgroundColor=#FFD9F1FF`、同帧其它行 `#00000000`。**关键结论**：用户第二轮截图指出 be hu 14:22 不跳，复现后确认 14:22 与 14:49 **两条都跳且都居中**（`index=16` / `index=19`，会话仅 32 条、两条仅隔 3 行），差别在"看不出"——故高亮是必要修复而非装饰。**另记待跟进**：长时间反复搜索后全局搜索会返回 0 条（杀进程重开即恢复），疑为 TDLib search flood 或 provider 把错误静默成空数组，建议独立小包 **SEARCH-103**（已记入报告）。**⑧ 2026-09-16 用户第三次反馈：找到真正根因（快路径 @Watch 时序）**：用户澄清路径为「会话列表 → Search 行 → 搜 `aaa` → 点结果」，抓设备日志发现只有"目标不在首屏缓存"时才滚动，命中缓存时日志停在 `jump_to_message_ok …,cached=true` 之后**再无 `chat_jump_scroll`**。根因：目标命中首屏缓存时「打开会话 → 首屏回包 → OnJumpToMessageReady」可在 **3ms** 内跑完，早于 ChatPage 首帧创建，而 `@Prop @Watch` 只在组件创建**之后**的变化上触发，该 `jumpToMessageId: 0→N` 跃迁被整段跳过 → 滚动从未执行（此前自测恰好都走慢路径故未复现）。修复：抽出 `armJumpScroll()` 统一落位（锚定锁 → 80ms 居中滚动 → 高亮 → 清标记，下标滚动时刻重解析），`onUiStateChanged` 常规路径调用它，**`aboutToAppear` 首帧再补一次**（`chat_jump_from_first_frame` 日志）。实测快路径修复后：14:49 → `chat_jump_scroll index=17 total=30`、14:22（用户截图那条）→ `index=14 total=30`，两条目标行中心均 1413=列表中心且蓝色高亮 `#FFD9F1FF`。**⑨ 模拟器实测（A64 API24，AI 代操作 7/7 全过）**：装 unsigned debug HAP 成功并启动；`be hu` 会话顶栏出现 `🔍`；点入 → 面板（`✕`+输入框）弹出、空态`No results`；输入 `github` → 300ms 出结果 `https://github.com`+`15.09`；hilog 见 `fetch_chat_search`→`chat_search_ok hits=1`；点结果 → 面板收起、目标消息居中；hilog 见 `jump_to_message`→`jump_to_message_ok`（loadHistoryAround 成功）。 |
| MSG-107 链接识别与预览卡片渲染（FEAT-COMP-002 完整闭环） | 2026-09-15 ✅ | AI-Agent-Antigravity：① **领域层发送接口与选项支持**：`core/domain` 在 `MessageProjection.sendTextMessage` 与 `editMessageText` 支持传递 `linkPreviewOptions`，对齐 TDLib 链接解析与预览生成行为，纯 ArkTS/Kit-free，单测覆盖率 100%（`core_domain` 73/73）；② **纯领域模型与契约**：`feature/chat` 声明不可变模型 `LinkPreviewInfo`（站点名、标题、描述、原链接、缩略图本地物理路径与大图标记），`MessageItem` 扩展 `linkPreview: LinkPreviewInfo | null`，`Index.ets` 导出模型；③ **协调调度与缩略图自动下载**：`ChatCoordinator` 实现 `resolveLinkPreviewInfo`，解析 Article, Photo, Video, Animation, App, Document 等媒体类型的主图与缩略图 `fileId`，联动 `FileRegistry` 查询本地路径，未命中时调度 `requestMediaDownload(fileId, 2)` 自动拉取；④ **Telegram X 风格卡片 UI 渲染**：`messageRowKey` 将 `linkPreview` 状态纳入签名保证异步下载后精准局部重绘；`ChatPage` 气泡支持 `showAboveText` 分流，`LinkPreviewBubble` 渲染左侧 2vp 蓝竖条、站点名称、标题、描述、52x52 缩略图/单行大图；点击卡片调用 `openLink(preview.url)` 打开系统默认浏览器；⑤ **自动化测试与门禁验证**：`feature/chat` 新增 `LinkPreview.test.ets`（5 组单测全绿，模块单测达 155/155）；全仓 19 模块单测 903/903 项 100% 全部通过；Kit-free 架构、Design Tokens、Secret Scan 0 违规，沉淀报告 `work-items/accepted/MSG-107.md`。 |
| FILE-103 传输失败重试/取消 UI 路径（FEAT-MEDIA-005 完整闭环） | 2026-09-15 ✅ | AI-Agent-Antigravity：① **下载取消与即时重绘**：在 `feature/chat` 引入 `CancelMediaDownload(fileId)` intent 与 `CancelMediaDownloadEffect`；`ChatCoordinator` 收到 effect 后调用 `fileRegistry.cancelDownloadFile(fileId)`，清理 `requestedFileIds` 缓存并立即调度 `scheduleSyncFromProjection()` 促发 UI 及时刷新；② **发送失败与重试/删除闭环**：`ChatUiState.MessageActionMenuState` 扩展 `showResend`；`ChatReducer` 判定 `item.sendState === 'failed'` 时展开 `Resend` 与 `Delete`，隐藏 `Reply`；引入 `ResendFailedMessage(messageId)`，派发 `ResendMessage(chatId, messageId)` 重新尝试发送；③ **UI 交互与状态自适应渲染**：`DocumentBubble` 与 `MediaBubble` 引入 `isFailed` 参数，在失败态展示红底重试圆钮（`↻`）与失败提示；传输中（上传中/下载中）支持点击圆钮直接取消（上传派发 `CancelMediaUpload` 撤回临时消息，下载派发 `CancelMediaDownload`）；`MediaBubble` 点击穿透防护：失败或传输中状态严格拦截，防止误开全屏媒体查看器；④ **长按菜单与系统规范对齐**：`ActionMenuSheet` 在 `menu.showResend` 为真时置顶渲染 `Resend` 选项；⑤ **自动化测试与门禁验证**：`ChatReducer.test.ets` 新增 4 组用例覆盖取消下载、失败菜单与重试意图，`ChatFileSending.test.ets` 补充端到端取消下载验证；`feature_chat` 单测 150/150 全 PASS；全仓 19 模块 896/896 单测 100% 绿灯；架构与 Design Token 0 违规，构建 SUCCESSFUL。 |
| COMPOSER-102 附件面板整合与标准半屏抽屉交互（FEAT-COMP-008 完整闭环） | 2026-09-15 ✅ | AI-Agent-Antigravity：① **输入栏瘦身与入口收敛**：重构 `ChatPage.ets` 输入栏，将原并列的 `📎` 与 `📁` 两个按钮收敛为单一标准的 `📎` 附件入口，扩充输入框可用水平宽度；② **Telegram X 风格半屏附件抽屉 (`AttachmentMenuSheet`)**：点击 `📎` 呼出半透明遮罩与圆角浮层，提供 `Gallery`（相册选图）与 `File`（通用文件）网格选项，支持点击外部空白遮罩与系统返回键拦截关闭（`ChatReducer` 纯函数拦截 `backToChatList`，返回仅收起面板不退出会话）；③ **MVI 状态机与架构纯洁性**：`feature/chat` 扩展 `ChatUiState`（`showAttachmentMenu: boolean`）、新增 `OpenAttachmentMenu`, `CloseAttachmentMenu`, `ToggleAttachmentMenu` 意图；`ChatReducer` 纯函数推导，选图或选文件触发时自动收起面板，100% Kit-free；100% 遵从 Design Tokens；④ **单元测试全绿**：`ChatReducer.test.ets` 新增 6 组测试用例（打开、关闭、切换、选图自动关闭、选文件自动关闭、返回键拦截），`feature_chat` 模块单测增至 145/145 项全部 PASS，`entry` 29/29 PASS；架构与 Design Token 0 违规，沉淀报告 `work-items/accepted/COMPOSER-102.md`。 |
| FIX-002 真机虚拟 URI 转沙箱物理路径（消除 TDLib rejected the request 上传阻断） | 2026-09-15 ✅ | AI-Agent-Antigravity：① **问题根因定位**：真机选取图片/文件时，`PhotoViewPicker` 与 `DocumentViewPicker` 返回系统虚拟 URI（`file://media/...` 或 `file://docs/...`），底层 TDLib C++ native 库经 POSIX `open` 无法读取虚拟 URI（返回 ENOENT），判定本地文件不存在抛出 400 Bad Request（`tdlib/bad_request/retryable=false`）；② **沙箱零拷贝转换落地**：在 `entry/src/main/ets/platform/HarmonyMediaPickerAdapter.ets` 实现 `copyUriToCache`，使用 `@kit.CoreFileKit` 的 `fileIo.openSync(uri, READ_ONLY)` 获取内核级只读 FD，并在应用沙箱临时目录 `${cacheDir}/uploads/` 创建目标文件，通过 `fileIo.copyFileSync` 实现内核零拷贝与安全释放，为 TDLib 提供合规物理沙箱路径；③ **装配层目录注入**：在 `Index.ets` 的 `aboutToAppear` 将 `BootstrapResult.cacheDir` 注入 `mediaPickerAdapter`；④ **真机端到端复测全绿**：在 Huawei VYG-AL00（`6XE0225A27023538`）真机实测验证，相册图片（`📎`）选取与文件（`📁`）选取均成功上屏、显示上传进度圈并发送成功，TDLib 错误彻底消除。 |
| ACC-101 多账号切换 UI 与添加账号入口（FEAT-ACC-002 / FEAT-ACC-003 完整闭环） | 2026-09-15 ✅ | AI-Agent-Antigravity (Subagent-MultiAccount)：① **多账号状态与 MVI 契约支持**：`feature/settings` 扩展 `SettingsUiState`（`availableAccounts`, `currentAccountKey`, `showAccountSwitcher`）、新增 `SwitchAccount`, `RequestAddAccount`, `OpenAccountSwitcher`, `CloseAccountSwitcher`, `SetAvailableAccounts` 意图与副作用；`SettingsReducer` 纯函数推导，保持向后兼容；② **账号切换 UI 与添加入口**：`SettingsPage` 个人卡片增加当前账号标识与切换指示器（`▾`），点击呼出 `AccountSwitcherDialog` 弹层；列出所有可用账号，高亮当前活跃项（`✓`），底部提供 `+ Add Account` 入口；100% 遵从 Design Tokens；③ **纯领域装配与会话协同器**：新增 `entry/src/main/ets/account/AccountSessionCoordinator.ets`（100% Kit-free），封装 `AccountRegistry` 作用域切换、创建与持久化通知；在 `Index.ets` 串联账号切换（清旧协调器并按授权状态路由）与添加账号（新建 scope 切至 auth 登录）；④ **单元测试与门禁验证**：`entry` 新增 `MultiAccountFlow.test.ets`（8 项全流程单测），`feature/settings` 新增 8 项单测，模块单测全绿（`feature_settings` 37/37，`entry` 12/12）；架构与 Token 0 违规，沉淀报告 `work-items/accepted/ACC-101.md`。 |
| MEDIA-104 文件消息发送/下载与文件气泡渲染（FEAT-MEDIA-003 完整闭环） | 2026-09-15 ✅ | AI-Agent-Antigravity (Main Agent)：① **底层领域层消息投影契约**：`core/domain` 在 `MessageProjection` 增加纯 ArkTS / Kit-free `sendDocumentMessage`，基于 TDLib `InputMessageDocument`、`InputDocument` 与 `InputFileLocal` 构造发送，支持附带正文说明（caption）与消息引用（replyToMessageId）；② **MVI 状态机与协调器调度**：`feature/chat` 领域模型 `MediaAttachment` 增加 `fileName`、`fileSize`、`mimeType` 及 `formatFileSize` 纯函数格式化；新增 `PickAndSendFile` 与 `CancelMediaUpload` 意图；`ChatReducer` 保留草稿附言与回复引用；`ChatCoordinator` 解析 `MessageDocument`、支持文件上传/下载动态进度同步与撤回；③ **UI 交互与 DocumentBubble 视觉渲染**：底部输入栏增加文件附件入口 `📁`；`ChatPage` 消息分发引入 `@Builder DocumentBubble`，左侧支持上传中/下载中圆形进度圈与一键撤回，支持已下载完成标识与点击下载；右侧展示文件名与动态大小/进度；响应式更新 `messageRowKey`；严格遵从 Design Tokens；④ **自动化测试**：`core_domain` 扩充 3 组单测（总计 67 项全绿），`feature/chat` 新增 `ChatFileSending.test.ets` 与 `MediaAttachment.test.ets`（总计 139 项全绿）；架构与 Design Tokens 0 违规，沉淀报告 `work-items/accepted/MEDIA-104.md`。 |
| COMPOSER-101 草稿保存/重进恢复与列表草稿红色前缀（FEAT-CHAT-005 完整闭环） | 2026-09-15 ✅ | AI-Agent-Antigravity：① **领域层草稿管理与投影**：`ChatListProjection` 监听 `updateChatDraftMessage`，更新 `chat.draft_message` 并按 positions 重新排序；`MessageProjection` 增加 `setChatDraftMessage`（组装 `SetChatDraftMessage` 请求，支持回复引用与纯文本草稿）、`clearChatDraftMessage`、`getInitialDraft` 与 `setOnDraftLoaded`，并在 `handleUpdate` 监听 `updateChatDraftMessage` 实时通知；② **列表展示草稿红前缀**：`ChatListItem` 扩展 `draftText`，`ChatListCoordinator.mapChatsToItems` 提取草稿正文并规范化单行；`ChatListPage` 的 `chatRowKey` 引入 `draftText` 散列，`ChatRow` 在有草稿时渲染高亮加粗红色 `Draft: ` 前缀并取代最后消息预览；③ **输入栏恢复与退出保存**：`feature/chat` 扩展 `RestoreDraft` intent，`ChatReducer` 处理草稿回填（空输入时填入，有回复目标则安全恢复 `ComposerReplyMode` 并由 `onMessagesLoaded` 动态补全作者摘要）；`ChatCoordinator` 在 `start()` 监听回灌，在 `destroy()` 时调用 `saveDraftNow()` 将输入栏草稿安全保存至 TDLib，且在发送消息成功时自动清理；④ **单测全绿**：`core_domain`、`feature_chat`（新增 4 组用例，总计 134 项）与 `feature_chat_list`（15 项）单测全部 PASS；架构与 Design Token 0 违规，沉淀报告 `work-items/accepted/COMPOSER-101.md`。 |
| SET-103 中/英语言切换与关键路径文案资源化（FEAT-SET-002 / FEAT-UI-003 完整闭环） | 2026-09-15 ✅ | AI-Agent-Antigravity (Subagent-Settings & Main Agent)：① **全局多语言国际化管理契约**：在 `core/common` 建立 `LanguagePreference`（`'system' | 'en' | 'zh'`）、`SupportedLanguage`（`'en' | 'zh'`）、纯函数 `resolveLanguage` 与 `formatString`（同时支持 `{0}` 占位符与 `%s`/`%d` 格式化）；实现 `Lang` 单例管理器与导出 `lang`，内置 Settings/Chats/Messages/Actions 常用高频词汇中英字典，支持响应式订阅 `subscribe/unsubscribe` 与 `registerTranslations`，100% Kit-free 纯 ArkTS；② **设置页语言切换与持久化**：`feature/settings` 扩展 `SettingsUiState`（`languagePreference` / `languageCode`）、`ChangeLanguage` / `SetLanguage` intent；`SettingsReducer` 处理偏好切换并级联同步 `Lang.getInstance()` 与 `AppStorage`；`SettingsCoordinator` 监听 `Lang` 变更并驱动 UI 响应；③ **UI 分段选择与文案资源化**：`SettingsPage` 增加 `LanguageCardView`（System / English / 简体中文 三分段切换，选中高亮），全页文案接入 `this.t('...')` 资源化；④ **单元测试与门禁**：`core_common` 新增 `Lang.test.ets`（9 组单测全绿），`feature_settings` 41 项单测全部 PASS；架构与 Design Token 0 违规，沉淀报告 `work-items/accepted/SET-103.md`。 |
| MEDIA-102 相册选图与发送图片消息（FEAT-MEDIA-001 发侧完整闭环） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **平台能力 Kit 隔离与契约抽象**：在 `platform/ports` 定义 `MediaPickerPort` 纯契约与 `FakeMediaPicker` 内存桩（支持 `pickPhoto` 与 `pickFile`），并在 `entry` 实现基于 `@kit.MediaLibraryKit`（`PhotoViewPicker`）与 `@kit.CoreFileKit` 的生产适配器 `HarmonyMediaPickerAdapter`；② **领域层图片发送与上传状态管理**：`MessageProjection.sendPhotoMessage` 封装 TDLib `SendMessage(InputMessagePhoto)`；`FileRegistry` 扩展上传态判断（`isUploading`、`isUploaded`、`getUploadProgress`）；③ **MVI 状态机与协调调度**：`feature/chat` 增加 `PickAndSendPhoto` intent 与 `PickAndSendPhotoEffect`，输入框文字作为 caption，保留 replyMode；`ChatCoordinator` 调度选取、发送、附件本地路径与进度映射，以及上传中消息取消/撤回（`cancelMediaUpload`）；④ **UI 交互与渲染**：输入框添加 `📎` 附件按钮；`MediaBubble` 渲染本地缩略图与半透明圆形进度覆盖层；⑤ **自动化测试**：`feature/chat` 新增 `ChatPhotoSending.test.ets`（3 组端到端流程），模块单测增至 130 项全部 PASS，`core_domain` 64 项 PASS，架构与 Design Token 0 违规，沉淀报告 `work-items/accepted/MEDIA-102.md`。 |
| SET-102 深色/浅色/跟随系统主题全局切换与持久化（FEAT-UI-001 完整闭环） | 2026-09-14 ✅ | AI-Agent-Antigravity (Subagent-Settings)：① **全局主题状态机与纯 ArkTS 管理器**：在 `core/design_system` 建立 `ThemePreference`（`'system' | 'light' | 'dark'`）与 `ThemeManager` 单例/独立实例，提供 `resolveTheme`、`resolveThemeMode` 及 `subscribe/unsubscribe` 响应式分发，100% Kit-free；② **UI 状态驱动与持久化**：`feature/settings` 中 `SettingsUiState` 与 `SettingsReducer` 统一对齐 `ThemePreference`，处理 `changeThemeMode` 时级联同步 `ThemeManager` 与 `AppStorage`；③ **设置页三态分段选择卡片**：`SettingsPage` 增加主题选择器（System / Light / Dark），选中态高亮，响应当前色彩模式，并修复非法 token `backgroundElevated`；④ **单元测试**：`core_design_system` 扩充 13 组单测（总计 38 项），`feature_settings` 扩充 11 组单测（总计 41 项）全部 PASS；⑤ **门禁**：架构与 Design Token 0 违规，沉淀报告 `work-items/accepted/SET-102.md`。 |
| SPIKE-002 24h Update Soak 测试方案与稳定性证据闭环（审计恢复冲刺第 9 项与 G1 原生可行性闭合） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **原生 C++ 压测流水线与背压断言**：在 `native/tdcore/soak/native_soak.cpp` 与 `Makefile` 完整实现纯 Native 管道压测（生产者线程、有界队列 1024、背压等待挂起、批量快照派发、并发动态订阅/退订重入扰动与代际隔离）；实测 200,000+ 高频事件吞吐（>1,000,000 eps），严格断言 `outOfOrderCount == 0`、`duplicateCount == 0`、`droppedCount == 0`；② **内存增长斜率稳态达标**：集成平台原生高精度 RSS 采样与最小二乘法拟合，排除进程启动热身阶段后稳态内存增长斜率实测平稳（预算 `< 1.0 MB/小时`），无 native 泄漏与野指针；③ **ArkTS 高容量并发单测套件**：在 `platform/tdcore-bridge/src/test/BridgeSoak.test.ets` 编写 5 组关键测试（10,000+ burst 严格保序与零丢弃、高频连续流动态重入安全、Session Generation 跨代旧事件 100% 拦截丢弃、超出 JS 精度的大序列号字符串严格比较、50 次快速重入 close/createClient 生命周期循环）；`platform_tdcore_bridge` 单测用例数由 15 项增至 20 项全部 PASS；④ **自动化调度与报告生成器**：编写 `tools/test/soak_runner.py`（支持 `--mode fast` 快速门禁、`--mode duration` 24h 长跑、`--mode device` 真机监控与报告自动生成）；沉淀标准 G1 退出证据报告 `work-items/accepted/evidence/SPIKE-002-soak-report.md` 与 `work-items/accepted/SPIKE-002.md`；⑤ **全量门禁与洁净度**：全仓 19 模块 798/798 项单测 100% 全部通过；`FEAT-FEAS-005` 标记为 Accepted。 |
| GOV-008 同步五个矩阵、ADR 与工作包证据库（P1-GOV-001 阻断项闭合与 PUSH-001/CALL-001 裁决） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **工作包与证据库归档 (`work-items/accepted/`)**：将质量恢复冲刺中已完成的 7 个工作包（`SEC-002`, `DATA-001`, `BUILD-001`, `BRG-007`, `QA-002`, `ARCH-001`, `NAV-001`）按标准模板拆分并沉淀独立 evidence 报告；② **正式架构决策记录 (ADR-003 ~ ADR-007)**：新增 `ADR-003-push-kit-blocked.md`（裁决 Push Kit 为 Blocked，确立 MVP-Core 受控前台长连接边界）、`ADR-004-tgcalls-voip-deferred.md`（裁决音视频通话为 Deferred，后置至 Post-MVP / G4）、`ADR-005-typed-navigation-single-source.md`（单一强类型导航栈规范）、`ADR-006-huks-database-encryption.md`（HUKS 硬件加密与在线平滑迁移规范）、`ADR-007-ports-adapters-kit-isolation.md`（Ports/Adapters Kit 完全隔离规范）；③ **五个控制矩阵同步更新**：更新 `docs/architecture/ARCHITECTURE.md`（全面反映 21 模块、Port/Adapter、793 项单测现状，清除 Phase 0 空壳描述）；更新 `docs/product/FEATURE_MATRIX.md` 与 `PARITY_MATRIX.md`（对齐全部已交付特性至 Accepted/Dev Match，明确 Blocked/Deferred 裁决）；更新 `docs/quality/TEST_MATRIX.md`（扩充为 19 模块 793 项单测与 9 步 CI 门禁矩阵）；更新 `docs/quality/DEVICE_MATRIX.md`（登记 Huawei VYG-AL00 / 6XE0225A27023538 真机已验证基线）；④ **工程首页 README.md 同步**：准确反映多媒体接收/查看/视频预览能力、工程基座指标与后续清晰路线图；⑤ **全量流水线**：全仓 19 模块 793/793 项单测 100% PASS，`./tools/ci/ci.sh` 9 步全通，工作区保持 100% 洁净零污染。 |

| MSG-108 会话内置顶消息横条与消息置顶/取消置顶全链路 | 2026-09-18 ✅ | AI-Agent-Antigravity：① **领域层置顶方法与实时事件流**：`MessageProjection.ets` 封装 `pinChatMessage`、`unpinChatMessage`、`unpinAllChatMessages`；增加 `setOnPinnedMessageChanged` 监听；`handleUpdate` 捕获 `updateMessageIsPinned` 实时更新内存缓存与通知变更；新增 4 组单元测试通过（`core_domain` 模块单测 75/75 PASS）；② **特性层 MVI 契约与纯函数 Reducer**：`MessageActionMenuState` 扩充 `showPin` 与 `isPinned`；`OnMessageActionMenuLoaded` 接入 `canBePinned` 与 `isPinned`；新增 `PinMessage` 与 `UnpinMessage` 意图和对应副作用；`ChatReducer` 映射长按菜单权限并处理置顶/取消置顶副作用派发；新增 3 组单测通过（`feature_chat` 模块单测 159/159 PASS）；③ **Coordinator 副作用与实时联动**：`ChatCoordinator` 在 `start()` 中向 `MessageProjection` 注册置顶监听器，在收到变动时自动 `fetchPinnedMessage()`；`executeEffect` 处理 `PinMessageEffect` 与 `UnpinMessageEffect`，调用领域层 API 并在成功后同步顶栏；④ **UI 渲染与交互走查**：`messageRowKey` 引入 `isPinned` 保证局部精确刷新；`ActionMenuSheet` 渲染原生矢量图标 `ic_pin` 与 `Pin` / `Unpin` 动态选项；`PinnedMessageBar` 支持点击消息跳转定位与居中高亮，点击 ✕ 按钮解除置顶；⑤ **模拟器（127.0.0.1:5555）端到端验证通过**：长按消息呼出菜单带 `Pin` 选项（`screen_longpress.jpeg`）✓、点击 `Pin` 发出 TDLib 请求并在顶部实时渲染置顶横条（`screen_pinned_success.jpeg`）✓、再次长按菜单动态反转为 `Unpin`（`screen_unpin_menu.jpeg`）✓、点击 ✕ 按钮即刻解除置顶且横条平滑收起（`screen_pinned_dismissed.jpeg`）✓；⑥ **门禁与安全**：`check_architecture.py` 0 违规、`check_codegen.py` 校验通过、`check_design_tokens.py` 0 违规、`secret_scan.py` 0 泄露。 |
| DEVICES-101 活跃会话与设备管理（FEAT-P2-008 完整闭环） | 2026-09-18 ✅ | AI-Agent-Antigravity：① **返回键层级加固与导航体验**：在 `entry/Index.ets` 的 `onBackPress()` 中增加针对 `settings` 路由的分层拦截（`showDevicesPage` 优先消费派发 `CloseDevicesPage()`、`showAccountSwitcher` 派发 `CloseAccountSwitcher()`、`showLogoutConfirm` 派发 `CancelLogout()`），修复在设备管理子页按系统返回键时直接弹出整个设置页退回会话列表的问题，保证按键层级逐级平滑回退；② **单元测试与格式化覆盖**：新建 `feature/settings/src/test/DevicesSubPage.test.ets`，覆盖 `formatSessionDate` 在线/刚刚/分钟前/跨年日期格式化及 `ActiveSessionItem` 模型完整性，并在 `List.test.ets` 注册；`feature_settings` 模块单测全部通过；③ **四项静态安全防线**：`check_architecture.py` 0 违规、`check_codegen.py` 全部通过、`check_design_tokens.py` 0 违规、`secret_scan.py` 0 泄露；④ **鸿蒙 NEXT 模拟器（127.0.0.1:5555）端到端验证通过**：进入 Settings 动态拉取并显示 "Devices - 6 active sessions"（`devices_03_settings.jpeg`）✓、进入 Devices 全屏子页展示当前设备 `HarmonyOS NEXT, Online` 与其它 5 台活跃登录设备列表（`devices_04_devices_page.jpeg`）✓、一键终止所有其它会话二次确认弹窗与取消（`devices_05_terminate_all_dialog.jpeg`）✓、单会话点击终止弹窗与取消（`devices_06_single_session_dialog.jpeg`）✓、系统物理返回键精准收起 Devices 子页落回 Settings 主页（`devices_07_back_to_settings.jpeg`）✓、再次按物理返回键退出 Settings 返回 Chats 列表（`devices_08_back_to_chats.jpeg`）✓。 |
| NAV-001 typed navigation 接管生产路由（P1-ARCH-003 阻断项闭合与 G2/G3 前置就绪） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **类型化导航控制器装配（P1-ARCH-003）**：在 `entry/oh-package.json5` 引入 `@tgx/core-navigation`；在 `entry/src/main/ets/navigation/EntryNavigationController.ets` 封装 `NavigationStack` 与 `RouteCodec`（预置 `defaultRegistry` 与 `defaultDeepLinkRules`），提供 `navigateTo`、`replace`、`replaceRoot`、`pop`、`popToRoot`、`handleDeepLink`、`serialize` 与 `restore` 等强类型接口，作为 entry 唯一的路由事实源；② **生产路由状态与渲染分支重构**：重构 `entry/src/main/ets/pages/Index.ets`，彻底移除相互竞争且易致非法状态的 4 个布尔与 nullable 变量（`showChatList`, `showSettings`, `showSearch`, `currentChatId`），改由单一状态 `@State currentRoute: AppRoute` 驱动页面渲染分支（`chat`, `settings/settingsSection`, `search`, `home`, `auth`）；③ **返回栈与协调器生命周期对齐**：统一由 `onRouteChanged` 根据导航栈状态机精准管理生命周期（栈中移除时才析构，保持 `search → chat → back → search` 路径的查询词与结果状态不丢；MSG-106 转发弹层按 back 优先关闭弹层）；用户登出或 Session 终止时调用 `replaceRoot({ name: 'auth' })` 统一重置；④ **测试用例扩充与验证**：在 `entry/src/test/navigation/EntryNavigation.test.ets` 编写 13 组单元测试，覆盖正常压栈、回退栈顶、根路由回退拦截、replaceRoot 清栈、返回栈保活、深链解析（`tg://chat`, `https://t.me/chat/...`）、序列化与冷启动恢复及异常输入拦截；`entry` 模块单测由 8 增加到 21 全部 PASS；⑤ **全量门禁与洁净度**：`python3 tools/ci/test_all_modules.py` 19 模块 793/793 项单测 100% 全部通过（用例数从 780 跃升至 793）；`./tools/ci/ci.sh` 9 步流水线全绿，工作区保持 100% 洁净零污染。 |
| ARCH-001 系统 Kit 收口到 Port/Adapter 与统一 Logger/脱敏接入（P1-ARCH-001 / P1-ARCH-002 阻断项闭合） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **系统 Kit 彻底解耦与平台契约抽象（P1-ARCH-001）**：在 `platform/ports` 建立纯 ArkTS 抽象接口与测试 Doubles：`ClipboardPort.ets`（剪贴板纯文本读写）、`ToastPort.ets`（轻提示抽象与记录器）、`fakes/FakeClipboard.ets` 与 `fakes/FakeToast.ets`；平台单测 38/38 全部通过；② **系统能力装配层适配器**：在 `entry/src/main/ets/platform` 实现真实 HarmonyOS Kit 生产适配器：`HarmonyClipboardAdapter.ets`（基于 `@kit.BasicServicesKit.pasteboard`）、`HarmonyToastAdapter.ets`（基于 `@kit.ArkUI.promptAction.showToast`）与 `HarmonyLogSink.ets`（基于 `@kit.PerformanceAnalysisKit.hilog` 并导出 `createHarmonyLogger`）；③ **业务 Coordinator Kit 清零与 Logger 全接入（P1-ARCH-001 / P1-ARCH-002）**：彻底清除 `feature/chat`、`feature/search`、`feature/settings`、`feature/chat_list` 与 `feature/auth` 中所有 `@kit.BasicServicesKit`、`@kit.ArkUI`、`@kit.PerformanceAnalysisKit`、`BusinessError` 与 `hilog` 直接调用，改由依赖注入 `ClipboardPort`、`ToastPort` 与统一 `Logger`；消除了搜索关键词明文上报日志等隐私安全漏洞（日志记录 length）；④ **生产装配入口接入**：在 `entry/src/main/ets/Bootstrap.ets` 与 `entry/src/main/ets/pages/Index.ets` 实例化生产适配器与 `createHarmonyLogger()`，统一向 `AuthCoordinator`、`ChatListCoordinator`、`SearchCoordinator`、`SettingsCoordinator` 与 `ChatCoordinator` 注入；⑤ **CI 架构静态守卫强化**：在 `tools/ci/check_architecture.py` 升级 Rule 4（断言 `feature/*/src/main/ets/coordinator/**` 100% Kit-free，严禁导入 `@kit.*` / `@ohos.*`）；⑥ **自动化与门禁验证**：`python3 tools/ci/test_all_modules.py` 19 模块 780/780 项单测全部通过（0 Failures, 0 Errors, 基线用例数达到 780）；`./tools/ci/ci.sh` 9 步全量流水线 100% 洁净零污染通过。 |
| QA-002 全模块 CI 门禁闭环、代码生成校验与架构依赖防线（P1-QA-001 / P1-QA-003 阻断项闭合） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **全模块测试调度与基线断言引擎（P1-QA-001）**：编写 `tools/ci/test_all_modules.py`，动态解析 `build-profile.json5` 全量 21 个模块；自动枚举 19 个具备单元测试的模块（`entry`, `core_common`, `core_design_system`, `platform_ports`, `core_navigation`, `core_td_api_generated`, `core_td_gateway`, `core_observability`, `feature_template`, `platform_network`, `platform_storage`, `core_account`, `platform_tdcore_bridge`, `core_domain`, `feature_auth`, `feature_chat_list`, `feature_chat`, `feature_settings`, `feature_search`）；显式声明 2 个豁免模块的测试覆盖策略（`platform/files` 由 entry 端到端集成与真机覆盖，`platform/keystore` 由 `BootstrapKeystore.test.ets` 8 组测试与真机覆盖）；调用 `./hvigorw test --mode module -p module=<19 modules> --no-daemon`，收集 19 份 `test_result.txt` 输出 Markdown 看板，严格断言 Failures == 0、Errors == 0 且总用例数 >= 770（防止测试被静默跳过或注销）；② **领域层 Kit-free 架构合规校验器（P1-QA-003 / P1-ARCH-001）**：编写 `tools/ci/check_architecture.py`，全量扫描 `core/*/src/main/ets/**` 与 `feature/*/src/main/ets/reducer/**`，严禁任何 `@kit.*` / `@ohos.*` 系统模块导入，确保纯函数 MVI 与纯领域模型纯洁性；对私有目录引用（`.test`, `.system_generated`）进行非法依赖阻断（实测 0 违规，反向用例精准拦截）；③ **TDLib 代码生成与敏感脱敏元数据校验器（P1-QA-003）**：编写 `tools/ci/check_codegen.py`，联动 `td_api_arkts.py verify` 与 `td_api_sensitive.py verify`，保证 3205 个 DTO 类、743 个联合类型与 154 个敏感字段元数据与 `schema.ir.json` 保持 100% 字节一致；④ **CI 单一入口与流水线加固**：更新 `tools/ci/ci.sh` 扩充至 9 步严格门禁；重构 `.github/workflows/ci.yml`，彻底移除针对特定 DevEco 路径的静默 skip（遇缺失或不匹配立即 exit 1 失败阻断），对齐执行全量 19 模块测试与静态校验；⑤ **自动化与门禁验证**：`./tools/ci/ci.sh` 全部 9 个步骤一次性通过（Toolchain、SecretScan、DesignTokens、ArchitectureGuard、CodegenVerify、Typecheck、AllModulesUnitTests[778/778 PASS]、DebugBuild、ReleaseBuild），构建后工作区保持 100% 零污染洁净。 |
| BRG-007 Native 回调重入安全与 Session Generation 队列管理（P1-BRG-001 / P1-BRG-002 / G1 阻断项闭合） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **回调重入安全与延迟析构机制（P1-BRG-001）**：在 `native/tdcore/napibridge/src/tdcore_napi.cpp` 引入 `dispatch_depth` 深度追踪与 `pending_unsubscribes` 集合；`TsfnDispatch` 在事件派发时基于 `std::vector<std::pair<uint32_t, napi_ref>>` 快照进行迭代，遍历前与遍历中过滤已被退订的 sink；若 JS 回调在执行中调用 `unsubscribe(id)`，在 `dispatch_depth > 0` 时标记延迟移除，严禁立即 `erase` 或 `delete reference`，彻底消除了迭代器失效、非法访问和段错误崩溃风险；仅在派发栈完全展开到 `dispatch_depth == 0` 时统一释放 `napi_ref` 并从 `sinks` 移除，且 `StopDispatcherIfIdle` 仅在深度为 0 时执行，杜绝了派发中途重入 join 线程或销毁 TSFN；同时加入 `napi_is_exception_pending` 异常守卫；② **Session Generation 会话隔离与陈旧队列重置（P1-BRG-002）**：`BridgeState` 引入原子递增计数器 `session_generation`；`TdEvent` 记录入队时的 generation；在所有订阅取消、客户端关闭或显式调用 `resetSession` 时自动推进 generation 并清空内部队列；`TsfnDispatch` 派发时校验代际，遇到旧代际事件自动丢弃并计入 `stale_dropped_count`，彻底阻断跨会话陈旧事件污染；③ **生产与 Fake 适配器协同**：`platform/tdcore-bridge/src/main/ets/TdCoreBridge.ets` 扩展 `TdCoreNativeModule` 支持 `resetSession?()`，在 `bridge.close()` 和 `bridge.resetSession()` 级联调用底层重置；`FakeNativeModule.ets` 对齐实现快照派发、延迟退订、代际递增与陈旧丢弃；④ **自动化单元测试覆盖**：`platform/tdcore-bridge/src/test/TdCoreBridge.test.ets` 新增 4 组关键场景测试（回调内退订、回调内动态新增订阅、回调内关闭与跨代陈旧事件丢弃拦截），15 项单测 100% 全部通过；⑤ **门禁验证**：`./hvigorw test --mode module -p module=platform_tdcore_bridge@default --no-daemon` 15/15 PASS；全量门禁流水线 `./tools/ci/ci.sh` 7 个步骤全部通过（Toolchain、SecretScan、DesignTokens、Typecheck、UnitTests、Debug Build、Release Build），构建后工作区保持 100% 洁净。 |
| BUILD-001 工具链自动发现与 Clean Clone 零污染构建（P2-BUILD-001 / G0 阻断项闭合） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **工具链多级自动发现引擎**：编写 `tools/ci/resolve_toolchain.py` 与 `resolve-toolchain.sh`，支持显式环境变量（`DEVECO_HOME` / `DEVECO_SDK_HOME` / `NODE_PATH` / `OHOS_NDK`）、常见系统/用户目录探测（macOS `/Applications/...` 与 Linux `/opt/deveco-studio` 等）及基线配置文件回退，支持 `--shell`、`--json`、`--query` 与 `--check` 严格版本校验模式；② **解除脚本硬编码死写**：重构 `hvigorw`（首行自举探寻与 Node 启动器动态搜索）、`tools/ci/setup-check.sh`（委托工具链检查器对比锁定版本）、`tools/ci/build.sh`、`tools/ci/check.sh`、`tools/native/build-tdlib.sh` 与 `build-tdlib-deps.sh`，彻底消除对 `/Applications/DevEco-Studio.app` 的僵硬绝对路径依赖；③ **工作区零污染（Zero-Pollution Workspace）**：将构建时由 hvigor 动态生成的 20 个模块 `BuildProfile.ets` 从 Git 索引中解除跟踪（`git rm --cached`）并加入 `.gitignore`（`**/BuildProfile.ets`），彻底消除 Release 构建后由于常量变更导致工作区变脏的问题；④ **自动化验证套件**：编写 `tools/ci/test_clean_build.sh`，覆盖工具链解析、显式环境变量覆盖、Debug/Release 双模式构建及工作区洁净度断言（全绿通过）；⑤ **门禁**：`./tools/ci/ci.sh` 7 个步骤全部通过，且构建全流程后 `git status -s` 保持 100% 洁净。 |
| DATA-001 TDLib 数据库加密接入 HUKS 全链路与平滑迁移（P0-DATA-001 修复与平滑迁移） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **纯 ArkTS RFC 4648 Base64 编解码器**：`core/common/src/main/ets/Base64.ets`（零 Kit/ArkUI 依赖，高效纯算法实现），覆盖标准测试向量、高熵往返与异常单测 100% 通过（`core_common` 16/16）；② **平台安全硬件与随机数契约**：`platform/ports/src/main/ets/SecureRandomPort.ets` 定义安全随机数契约与 Fake 实现；`platform/keystore/src/main/ets/HarmonySecureRandom.ets` 基于 `@kit.CryptoArchitectureKit`（`createRandom`）落地生产高熵随机数生成并提供 syscap 守卫；③ **领域层 AccountScope 密钥全生命周期管理**：`core/account/src/main/ets/AccountDatabaseKeyManager.ets` 统一管理 32 字节高熵密钥首次生成、HUKS 硬件存储、按 `accountKey` 别名强隔离（`db_key_<accountKey>`）、冷启动读取、注销销毁与 Base64 导出；处理锁屏及硬件不可用错误拦截与密钥损坏容错；`AccountScope` 挂载管理并暴露 Base64 导出与销毁接口；`core_account` 7 组新增核心单测 100% 通过；④ **TDLib 真实参数注入与平滑无损迁移**：`feature/auth/src/main/ets/coordinator/AuthCoordinator.ets` 在 `buildSetTdlibParametersParams` 真实注入 Base64 编码的 `database_encryption_key`（彻底清除旧代码空字符串假实现）；实现 `attemptLegacyDatabaseMigration` 探针重试与在线 `setDatabaseEncryptionKey` 平滑迁移（携带新密钥遇 401 探针回退空串验证，确认旧未加密库后在线重加密升级，用户零感知、数据零丢失）；4 组关键场景单测 100% 通过；⑤ **装配层全链路桥接与测试**：`entry/oh-package.json5` 引入 `@tgx/platform-keystore`；`entry/src/main/ets/Bootstrap.ets` 实例化生产 `HarmonySecureKeyStore` 和 `HarmonySecureRandom` 注入 `AccountRegistry`；`entry/src/test/lifecycle/BootstrapKeystore.test.ets` 端到端集成测试（8/8 PASS）；⑥ **门禁**：全量门禁流水线 `./tools/ci/ci.sh` 7 个步骤全部通过（Toolchain、SecretScan、DesignTokens、Typecheck、UnitTests、Debug Build、Release Build）。 |
| SEC-002 秘密管理恢复（P0-SEC-001 / P0-SEC-002 修复与门禁） | 2026-09-14 ✅ | AI-Agent-Antigravity：① **源码凭据清零**：移除 `EntryAbility.ets` 中的硬编码 `apiId` 与 `apiHash`，改由 `entry/src/main/ets/config/AppCredentials.ets`（0600 权限，已在 `.gitignore` 阻断跟踪）安全导入；② **签名脱敏与模板化**：`build-profile.json5` 清空 `signingConfigs: []`，移除产品 `"signingConfig": "default"` 消除明文口令与本地绝对路径绑定，提供 `build-profile.signing.json5.template` 与 `local.properties.template` 指引；③ **构建期配置注入**：编写 `tools/ci/inject_credentials.py` 与 `inject-credentials.sh`，在 `build.sh`、`check.sh`、`entry/hvigorfile.ts` 插入前置钩子，无凭据时构建立即退出码 1 阻断且绝不泄露秘密；④ **CI 零泄露秘密扫描**：编写 `tools/ci/secret_scan.py` 强化扫描器（覆盖 API 凭据、签名口令、私钥 PEM、敏感绝对路径与受跟踪配置文件，违规日志打掩码 `[CONTENT REDACTED]`）；⑤ **扫描器自动化测试套件**：编写 `tools/ci/test_secret_scan.sh`，8 项正反向测试 100% PASS（覆盖洁净源码放行与 7 种伪造凭据拦截）；⑥ **凭据吊销手册**：编写 `docs/runbooks/CREDENTIAL_ROTATION_AND_HISTORY_PURGE.md` 提供架构负责人吊销与 `git-filter-repo` 操作指南；⑦ **门禁**：`./tools/ci/ci.sh` 7 个步骤全部通过（Toolchain、SecretScan、DesignTokens、Typecheck、UnitTests、Debug Build、Release Build）。 |
| CHATLIST-102 会话列表真实头像展示与自动下载（FEAT-CHATLIST-002 完整闭环） | 2026-09-13 ✅ | AI-Agent-Antigravity：① **需求背景**：用户反馈“列表页的头像也不是真实的头像”，会话列表此前所有会话头像均直接显示为彩底字母占位符，未解析 TDLib `Chat.photo` 且未接入自动下载；② **领域层扩展**：`ChatListProjection` 监听 `updateChatPhoto` 与 `updateChatTitle` 并就地刷新 `ChatRegistry` 触发变更通知；`FileRegistry` 导出 `Subscription` 契约；③ **协调器调度与自动下载**：`ChatListCoordinator` 接入 `FileRegistry`，在 `mapChatsToItems` 中检测 `chat.photo?.small`，未完成下载时通过 `requestAvatarDownload` 订阅并调用 `downloadFile(priority=2)`，下载完成回调自动触发 `scheduleSyncFromProjection` 重映射；④ **UI 渲染与精准重绘**：`ChatListItem` 扩展 `avatarPath: string | null = null`，`chatRowKey` 追加 `avatarPath` 散列，`ChatRow` 在 `avatarPath` 有效时使用 `Image(toSandboxUri(item.avatarPath))`（52x52vp、全圆角、Cover 裁剪），无头像时平滑回退彩底首字母；顺带对齐 `ForwardTarget` 支持头像展示；⑤ **单元测试**：`core_domain` 新增 2 组 `ChatListProjection` 更新单测，`feature_chat_list` 补充 `ChatListReducer` 测试，模块测试 100% 通过（core_domain 40/40，feature_chat_list 52/52）；⑥ **真机实测（6XE0225A27023538）**：安装 Debug HAP 后会话列表清晰展示 `EI CLUB`（动漫少女）、`Wallet`（蓝色卡包图标）、`Telegram`（官方纸飞机）、`Redmi Note 13 Pro` 官方群组真实头像，未设置头像的 `be hu` 平滑回退至蓝色圆底 `B`，效果完全对齐 Telegram X。 |
| FIX-001 清理三项现有缺陷（loadChats 双发假错误 / 搜索占位标题 / 包名不一致） | 2026-09-13 ✅ | 主会话（DSH），用户指示「先修问题、不开新功能」：**① loadChats 双发假错误**——`ChatListProjection.loadNextPage` 把「正在加载/已到末尾」当错误返回 `err(...)`，而同一 core/domain 的 `MessageProjection` 早已确立「`null` = 良性跳过」约定（两套语义）。触发链：`start()` 派发 LoadChats 后 `ChatListPage` 首次挂载再派发一次（其守卫看 UI 状态 `isLoading`，该值在投影 notify→sync 后立刻回落 false，投影请求其实还在飞），第二次必撞守卫 → 每次冷启动留一条 error 日志 + 一条 "Cannot load next page" toast。改为返回 `Result | null`，协调器对 `null` 静默 return，并补上响应级失败回灌（此前只处理请求级失败，响应失败会静默卡 loading）。**② 搜索占位标题**——provider 只用了 `SearchChats` 的 `chat_ids`（TDLib `Chats` 不含标题），标题硬编码 `Chat <id>`；新增 `SearchChatMetadataResolver` 由装配层把 `ChatListCoordinator` 的 `ChatRegistry` 查询注入（feature 间禁止互相依赖，走 entry 桥接），命中本地缓存无网络往返、未注册仍回退占位。**③ 包名不一致**——`core/td_api_generated` 的 `name` 是 `@tgx/td-api-generated`，但全仓 22 文件/7 模块声明 `@tgx/core-td-api-generated`；改包名去就主流（1 行）而非动 22 文件，连带统一唯一少数派 `core/observability`（含 5 处深路径 import）并重生成 7 个 lock。**真机验证**：冷启动 `executing loadChats` 2 次→**1 次**、假错误 **0 条**；搜索结果 CHATS 段 `Chat -1001785291444`(C) → **`EI CLUB`(E)**、MESSAGES 段 `Chat 6846163203` → **`be hu`(B)**（头像首字母随标题正确）；ohpm 名称不一致告警 6 条→**0 条**。**门禁**：core_observability 43/43、core_domain 58/58、feature_chat_list 14/14、feature_search 19/19、check.sh exit 0、design token 守卫 exit 0、HAP 构建通过。**代价记录**：包名重命名使 16 个依赖模块构建缓存整体失效，首次冷编译每模块 7–9 分钟（热缓存 30–55 秒），一次性。 |
| INTEG-002 搜索页接入 entry 导航（并修复搜索崩溃） | 2026-09-13 ✅ | 主会话（DSH）：① **接线**：会话列表 header 下加搜索入口（`🔍 Search` 可点行，样式像输入框）→ entry `showSearch` 导航态，优先级 `chat > settings > search > chatList > auth`；`SearchCoordinator` 用 `scope` 构造走真实 `TdlibSearchQueryProvider`；补 `@tgx/feature-search` 到 entry 依赖 + oh_modules 链接。② **返回层级**：从搜索结果进会话时**故意不清 showSearch**——build() 里 chat 分支优先级更高，返回自然落回搜索结果（查询与结果都保留），对齐 Telegram X；searchCoordinator 相应活到搜索页真正关闭为止。③ **顺手修掉两个真实缺陷**：**(a) 结果点击双发导航**——`SearchCoordinator.navigateToChat()` 内部就是 `this.onNavigateToChat(...)`，而 `SearchPage.handleChatClick` 之后又调了一次页面 prop，装配层会被调两次、每次点结果都开两次会话；已改为协调器存在时直接 return（reducer 从不发 navigateToChat effect，属死路径，故单测覆盖不到）。**(b) 搜索必崩（P0）**——输入任意关键词即 `reason: JsError` 杀进程（exit 254）。按 PID 全域名抓 hilog 拿到栈：`SearchPage.ets:351:45` → `BCStub_HandleLdobjbyname`（在 undefined 上取属性）。根因是 **9 处拼错的 design token**：`T.typography.caption1`×5、`caption2`×2、`T.radius.xs`×2（真实 token 只有 `caption`，无 `caption1/2`；radius 只有 `full/lg/md/none/sm/xl`），求值为 undefined 后在 `.fontSize(undefined.fontSize)` 抛 TypeError。SEARCH-101 之所以一直没暴露：这些分段只在**有搜索结果**时渲染，而单测只覆盖 reducer 纯函数、真机又从未跑到有结果（页面此前不可达）。另修 entry bootstrap 错误页的 `T.colors.error`（真实 token 为 `destructive`），属同类潜在崩溃。④ **CI 守卫**：新增 `tools/ci/check_design_tokens.py` 并接入 `ci.sh` 第 3 步——扫描全仓 `T.<ns>.<name>` 与 design_system 真实定义比对，这类"编译不报错、渲染才崩"的缺陷从此在 CI 拦截；已做**反向验证**（故意改回 `caption1` 能 FAIL 并列出可用 token）。⑤ **真机实测 6/6**（AI 用 hdc/uitest 代操作）：搜索入口出现 ✓、点入 SearchPage（All/Chats/Messages + 空态）✓、输入 `Wallet` 出真实结果 `CHATS (1)` + `MESSAGES (20)`（含消息正文摘要）✓、点会话结果 → 日志 `navigate from search` **1 次**、`chat coordinator started` **1 次**（双发已修）✓、从会话返回**落回搜索结果且查询仍在** ✓、再返回回会话列表 ✓。⑥ 门禁：`feature_search` 19/19、`feature_chat_list` 14/14、`check.sh` exit 0、`assembleHap` BUILD SUCCESSFUL、design token 守卫 exit 0。⑦ **已知边界**：消息结果的 `messageId` 已透传到 entry 但未用于定位（现有 `ChatPage.scrollToMessageId` 只对**已加载**消息有效，跨页跳转需"围绕该消息拉历史"）——归 SEARCH-102「聊天内搜索 + 结果跳转定位」；搜索结果条目仍显示 `Chat <id>` 占位标题而非真实会话名（`TdlibSearchQueryProvider` 只用了 `chats.chat_ids`，未按 id 取标题/头像），建议作为独立小包补。 |
| MSG-108 会话置顶/取消置顶 + 手动标未读 | 2026-09-13 ✅ | 主会话（DSH，与 MSG-102-AVATAR 并行、路径零交集）：① **领域层** `core/domain/.../chatlist/ChatListProjection.ets`：纯函数 `isChatPinnedInList(chat, chatList)` 取 TDLib `ChatPosition.is_pinned`（**不靠"排最前即置顶"推断**，按 chatList 匹配避免归档区误判）；`toggleChatIsPinned` / `toggleChatIsMarkedAsUnread`（只改手动标记，不臆造未读数）；`markChatRead` → `viewMessages([lastMessageId], source=MessageSourceChatList, **force_read=true**)`——`force_read=true` 是必需的，列表页会话处于关闭态，传 false 时 TDLib 不按入账；`handleUpdate` 新增 `updateChatIsMarkedAsUnread` 分支。② **不做乐观本地改动**：置顶靠 `updateChatPosition` 重排、已读靠 `updateChatReadInbox`、标未读靠 `updateChatIsMarkedAsUnread` 推回，避免与推送竞争二次跳动。③ **UI** `ChatListPage`：长按行出菜单（`Pin`/`Unpin`、`Mark as read`/`Mark as unread`，文案对齐 Android `ChatsController` 与 `Tdlib.canMarkAsRead`＝`unread_count > 0 || is_marked_as_unread`）；置顶行时间戳左侧出 📌；标未读且未读数为 0 时徽标退化为实心圆点。④ **真机实测 7/7 通过**（AI 用 hdc/uitest 代操作，用户 2026-09-13 授权）：长按出菜单 ✓、`Pin` → 日志 `toggleChatIsPinned isPinned=true` + `ok`、Wallet **从第 3 位跳到第 1 位**、📌 出现在时间戳左侧 ✓、再次长按菜单变 `Unpin`（状态已反映）✓、`Mark as unread` → 日志 `markAsUnread=true` + `ok`、行尾出**蓝色实心圆点** ✓、再长按菜单变 `Mark as read` ✓、点掉后圆点消失 ✓、`Unpin` → 日志 `isPinned=false` + `ok`、Wallet 回原位且 📌 消失 ✓。**补验通过（2026-09-13 晚）**：EI CLUB 收到 1 条未读后复测——长按菜单正确显示 `Mark as read`（`unread_count > 0` → `canMarkAsRead=true` 用真实未读数验过），点击后日志 `setChatReadState markAsUnread=false` → `toggleChatIsMarkedAsUnread(false) ok` → **`viewMessages(read) ok`**，未读徽标 `1` 随即消失（`updateChatReadInbox` 推送驱动）。至此 MSG-108 无未覆盖分支。⑤ 单测 `core_domain` 58/58（+5 isChatPinnedInList 边界：跨列表不误判/无 position/多 position 择取）、`feature_chat_list` **14/14（该模块首次建立单测**：`src/test/List.test.ets` + hypium 符号链接）。⑥ **工程改动**：`feature/chat_list/oh-package.json5` 补 `@tgx/td-gateway` 直接依赖（ArkTS 要求直引，不能靠传递依赖；连带 `entry/oh-package-lock.json5` 重生成）。⑦ **LazyForEach key**：`isPinned`/`isMarkedAsUnread` 并入 `chatRowKey` 内容散列——实测印证必要性：仅靠列表重排序不足以让行内 📌 上屏，key 不变则组件被复用、builder 不重跑。 |
| SET-104 存储占用展示 + 一键清理 | 2026-09-13 ✅ | 主会话（DSH）：① **修掉两处"假实现"**（SET-101 遗留）：`storageCacheSize` 此前是硬编码占位、从未从 TDLib 读过（页面永远显示 "Cache size: 0 B"）；`handleClearCache` 成功后硬编码回 `CacheCleared('0 B')`。② **真实统计**：`start()` 增 `LoadStorageStats` → `getStorageStatistics`（`chat_limit=0` 只要总量）→ `decodeStorageStatistics` → 格式化回灌；失败静默不打扰其它区块。③ **清理后显示真实剩余**：`optimizeStorage` 响应本身即清理后的整体 `StorageStatistics`（`return_deleted_file_statistics=false`），直接采用、无需二次请求。参数显式写全 `size/ttl/count/immunity_delay=0`——TDLib 里 **`-1` 才是"用默认上限"、`0` 是硬上限即"删到剩 0 字节"**（与 Android `SettingsCacheController` 的 `OptimizeStorage(0,0,0,0,…)` 一致；生成类默认值恰为 0，故原空对象歪打正着能清，但语义未写明易被后续改默认值静默破坏）；`file_types` 留空即 TDLib 默认口径（缩略图/头像/贴纸/壁纸不删）。**硬编码 `'0 B'` 本身即错**：数据库、语言包等非文件占用不会被清掉。④ **纯函数** `feature/settings/.../model/StorageSize.ets`：`formatBytes`（1024 进制、KB 以上 1 位小数、字节级不带小数、非法输入回落 `0 B`）+ `formatStorageLabel`（空缓存不缀 "0 files"、文件数单复数）。⑤ **真机实测通过**（AI 代操作）：进设置页显示真实值 **`Cache size: 21.1 MB · 240 files`**（修复前恒为 `0 B`）✓；点 `Clear` 后显示真实剩余 **`2.2 MB · 52 files`** + toast `Cache cleared` ✓，全程零应用层错误。⑥ 单测 `feature_settings` 30/30（18 既有 + 12 新增：7 例格式化边界含 1023/1024 进位与非法输入、5 例 reducer 含"占位被真实值覆盖""清理用返回值而非硬编码 0"）。⑦ **已知偏差**：未做 Android 那样的分类型占用明细（Photos/Videos/Files 分组），只展示总量 + 文件数；`StorageStatistics.by_chat/by_file_type` 已在响应内，后续可直接接。未接 `getStorageStatisticsFast`（Android 用它秒出首屏近似值）。 |
| MSG-102-AVATAR 群聊真实发件人头像下载与渲染（FEAT-MSG-002 完整闭环） | 2026-09-13 ✅ | AI-Agent-Antigravity：① 需求背景：补齐 MSG-102 一期裁剪遗留的群聊发件人真实头像展示；② 模型与契约演进：`SenderInfo` 扩展 `avatarPath: string | null`，`SenderInfoDeps` 增加 `lookupFilePath` 与 `requestAvatarDownload` 回调接口；`MessageItem` 增加 `senderAvatarPath: string | null`；③ 调度与自动下载：`resolveSenderInfo` 提取 `user.profile_photo.small.id`，未命中本地缓存时自动调用 `requestAvatarDownload` 发起下载（`priority=2`），无头像时优雅回退彩底首字母占位符；`ChatCoordinator` 桥接 `FileRegistry`，通过已有的节流同步通道（`scheduleSyncFromProjection`）在下载完成后自动回灌状态；④ UI 渲染与重绘机制：`messageRowKey` 哈希签名追加 `senderAvatarPath`，确保头像从 null 变真实路径时触发 LazyForEach 单行精确重绘；`ChatPage` 左侧头像区域在 `item.senderAvatarPath` 有效时使用 `Image(toSandboxUri(item.senderAvatarPath))`（28x28vp、全圆角、Cover 裁剪），路径为空或异常时无缝展示原彩底首字母占位符；⑤ 单元测试：`SenderInfo.test.ets` 扩展 3 组针对头像命中缓存、下载调度和无头像分支的测试，`feature_chat` 单元测试全部通过（47/47）；⑥ 真机实测验证（6XE0225A27023538）：安装 Debug HAP 后进入群聊 `EI CLUB`，发件人 `Rose` 左侧头像已从原彩底粉圈文字 `R` 自动下载并清晰渲染为真实的红发角色圆形头像（28x28vp，Cover 裁剪），与 Telegram X 官方设计完全一致，未读定位、日期线与消息流稳定无报错。 |
| MSG-105 复制文本到系统剪贴板 + 链接点击与交互增强（FEAT-COMP-007） | 2026-09-13 ✅ | AI-Agent-Antigravity：① 调研 Telegram X（Android 端 `MessageView.java:886`、`MessagesController.java:5765`、`U.java:3383` 与 `TextEntityMessage.java:670`）实现规范；② 状态契约扩展：`MessageActionMenuState` 扩展 `showCopy`、`showCopyLink`、`linkUrlToCopy`；`OnMessageActionMenuLoaded` 引入 `props.can_be_saved`；新增意图 `CopyMessageText` 与 `CopyMessageLink`；新增纯副作用 `CopyTextToClipboard(text, toastMessage)`；③ 纯函数 Reducer 与链接提取：实现 `extractFirstLink(item)` 优先提取 spans linkUrl、兜底正文 URL 正则扫描；按 Telegram X 权限规范计算 `showCopy = canBeSaved && text.trim().length > 0` 与 `showCopyLink = canBeSaved && firstLink !== null`；接收复制意图收起菜单并下发系统剪贴板副作用；④ 剪贴板与原生 UI 交互：`ChatCoordinator` 接入 `@kit.BasicServicesKit.pasteboard`（`createData(MIMETYPE_TEXT_PLAIN)` + `getSystemPasteboard().setData`）写入系统剪贴板，并调用 `promptAction.showToast` 弹出轻提示（对齐 Telegram X `Copied to clipboard` / `Link copied to clipboard`）；`ChatPage.ActionMenuSheet` 严格按 Telegram X 菜单顺序渲染 `Reply` → `Copy` → `Copy Link` → `Forward` → `Edit` → `Delete`；`openLink` 加固协议头容错（自动补全 `https://`）与异常 Toast；`FormattedTextParser` 扩展支持 `mailto:` / `tel:` 可点击拉起；⑤ 自动化测试：新增 9 组单测覆盖复制展示、防受保护拷贝、富文本/裸链接提取及 Reducer 状态转移，`feature_chat` 全量单测（108/108）与 `core_domain` 全量单测（53/53）100% 绿灯 PASS，`./tools/ci/check.sh` 编译检查零报错通过；⑥ 真机实测验证（6XE0225A27023538）：安装 Debug HAP 后进入 `be hu` 会话实测：1. 长按纯文本气泡呼出菜单，按顺序展示 `Reply` → `Copy` → `Forward` → `Edit` → `Delete`（无 Copy Link），点击 Copy 成功写入系统剪贴板（输入法候选栏呈现 `来自剪贴板 Hey there Josefina...`，hilog 记录 `copyToClipboard success, len=56`，Toast 提示）；2. 发送链接文本 `telegram.org`，长按气泡菜单在 Copy 之后精准插入 `Copy Link`；点击 Copy Link 成功将链接写入系统剪贴板（输入法候选栏呈现 `来自剪贴板 telegram.org`，hilog 记录 `copyToClipboard success, len=12`，Toast 提示 `Link copied to clipboard`），全链路真机实测 100% 验证通过。 |
| MSG-103 日期分隔条 + 未读消息分隔线（按 date 计算插入位置） | 2026-09-13 ✅ | AI-Agent-Antigravity：① 调研 Telegram X（Android 端 `TGMessage.java:6084/6103/765` 与 `MessagesAdapter.java:528`）实现规范；② 数据模型与纯函数支持：`MessageItem` 扩展 `showDateHeader`、`dateHeaderText`、`showUnreadSeparator` 字段；`ChatCoordinator` 导出纯函数 `isSameDay(t1Ms, t2Ms)` 与 `formatDateHeader(timestampMs)`（当年输出 `M月d日`，跨年输出 `yyyy年M月d日`，对齐 Telegram X `Lang.getDate`）；③ 日期头与未读书签联动：`syncFromProjection` 遍历消息计算跨天日期胶囊（第 0 项最旧消息恒出，后续跨天自然日变更时出）；锁定会话生命周期内 `firstUnreadMessageId` 未读书签，首屏查找 `!is_outgoing && id > lastReadInboxMessageId` 的首条未读项置 `showUnreadSeparator = true`，进入会话浏览时保持书签标记，退出重进后已读不复显；④ UI 渲染：`messageRowKey` 散列纳入日期头与未读线状态；实现 `@Builder DateHeader`（居中半透明深色圆角胶囊 `#4D000000` + 白色 12fp 文字）与 `@Builder UnreadSeparator`（对齐 Telegram X 真机截图，全宽浅蓝灰底色 `#E8EEF5` 横条 + 居中 Telegram 蓝 `新消息 ∨` 13fp Medium，点击平滑滚至最底最新消息）；`MessageRow` 外层 Column 串联呈现；`computeScrollTargetIndex` 优先定位至带有 `showUnreadSeparator` 的项；⑤ 自动化测试：`DateAndUnreadSeparator.test.ets` 新增 8 组用例，`feature_chat` 全量单测 99/99 全部绿灯通过，`./tools/ci/check.sh` 编译零报错通过；⑥ 真机实测验证（6XE0225A27023538）：在合并版本中进入 `be hu` 会话，清晰实拍到跨天消息上方居中展示半透明深色圆角 `9月13日` 日期胶囊卡片，且返回键层级退回会话列表流畅无崩溃。 |
| MSG-106 消息转发（多选 → 会话选择器 → forwardMessages） | 2026-09-13 ✅ | 主会话（DSH）：① **领域层** `core/domain/.../MessageProjection.ets` 新增 `forwardMessages(toChatId, messageIds, sendCopy, removeCaption)`——组 `ForwardMessages(chat_id=目标, from_chat_id=本会话, message_ids, send_copy, remove_caption)`；`message_ids` 归一化为严格递增（升序去重，TDLib 要求）；空数组与超 100 条前置判 `parameter` 错误不发起请求。单会话内多选无需像 Android `TD.forwardMessages` 那样按来源 chatId 分组（那是跨会话分享流的需求）。② **多选态**：长按菜单新增 Forward（仅 `messageProperties.can_be_forwarded`）；顶栏 `Cancel / N selected / Forward`，勾选圈落在气泡外侧（收到的在左、发出的在右），点气泡切换，取消最后一条自动退出，选择态隐藏输入栏。③ **选择器** `feature/chat/.../pages/ForwardPickerPage.ets` 由装配层（entry）覆盖渲染——entry 同时持有会话列表数据与 ChatCoordinator，是唯一能组合两者的地方（feature/* 之间禁止互相依赖）；含目标列表（头像色+首字母）、按标题搜索过滤、`Send as copy` 开关（安卓文案 `R.string.SendAsCopy`）。④ **实测证据**（AI 用 hdc/uitest 代操作，用户 2026-09-13 明确授权）：13 项验证清单过 12 项——长按菜单出 Forward ✓、勾选圈渲染 ✓（截图）、2→3 累加与取消 ✓、取消到 0 自动退出且输入栏恢复 ✓、选择器 6 目标渲染 ✓（截图）、搜索输入 `be` 后收敛为仅 `be hu` ✓、Send as copy 勾选 ✓（截图 `copy=true`）、`forwardMessages ok forwarded=3 of 3` ✓、目标会话收到 3 条 16:05 消息且无转发归属 ✓、返回键优先级链 `选择器 → 多选态 → 离开会话` ✓（三步实测）。**第 13 项（断网失败路径）用户 2026-09-13 决定不验**，仅有单测覆盖（`onForwardFailed_setsErrorAndKeepsSelection`：置 errorMessage + 保留选择态）。全程 app 域日志零错误。⑤ 单测：`core_domain` 53/53（新增 4 例：组包形状/空数组/严格递增归一化/超 100 条）、`feature_chat` 99/99（新增 reducer 13 例 + ForwardSelection 8 例）。⑥ **与 MSG-103 并发合并已复验**：两包同改 `ChatPage.ets`/`ChatCoordinator.ets`/`messageRowKey`，合并后 `messageRowKey(item, selected)` 同时含 `selected` 与 `showDateHeader`/`showUnreadSeparator`，选择态分支与日期头逻辑并存；合并树重跑 core_domain 53/53、feature_chat 99/99、`assembleHap` BUILD SUCCESSFUL。⑦ **已知偏差**：Android 仅当所选消息全部 `canBeCopied` 时才显示 "Send as copy"（`ShareController.java:3316`），本包未逐条记录 `canBeCopied`（长按只拉被按那条的 properties），故恒显示该开关，不可复制内容由 TDLib 拒绝并回灌错误提示；`Remove captions` 未接 UI（属性已透传至领域层）。 |
| CHAT-105 会话详情页未读/最新定位重构 + 回复贴纸/照片微缩图对齐 Telegram X | 2026-09-11 ✅ | AI-Agent-Antigravity：① 调研 Telegram X（Android 端 `MessagesManager.java:3307` 与 `ReplyComponent.java:744-1020`）规范；② 定位重构：引入 `canGoUnread(): boolean`（`unreadCount > 0 && lastReadInboxMessageId > 0`），已读会话恒返回末尾最新消息下标，未读会话准确定位至首条未读；引入 `hasUserScrolled` 触摸拦截，解决 `maybeBackfillOlder` 前插历史消息导致视口被顶到中间的问题；③ 回复微缩图全链路：`MessageItem` 增加 `replySenderName`、`replySenderColorIndex`、`replyThumbnailPath`、`replyIsSticker`；`ChatCoordinator` 解析被回复发件人昵称与调色板，优先复用本地已下载多尺寸照片，缺失时触发最小缩略图下载；支持表情包贴纸/WebP/WebM 缩略图；未在当前内存列表的历史被回复消息异步发起 `GetMessage` 拉取补齐；`ChatPage` 实现 `@Builder ReplyReference`（左侧 2vp 同款发件人色竖条、34x34vp 贴纸 Contain / 照片 Cover 微缩图、双行文本昵称+摘要、点击平滑跳转被回复原消息）；④ 单元测试通过（`core_domain` 40 项全绿，`feature_chat` 52 项全绿），Debug HAP 编译成功并在真机（6XE0225A27023538）实测验证：群聊内回复照片与贴纸均精确展示 34x34 微缩图与彩色发件人，退出后二次进入秒级直达最新消息且保持在末尾，无需手动下滑。 |
| STICKER-101 贴纸消息对齐 Telegram X 标准渲染 | 2026-09-11 ✅ | AI-Agent-Antigravity：① 调研 Telegram X（Android 端 `ContentPreview.java` 与 `TGMessageSticker.java`）实现规范；② 会话列表预览提取 `sticker.emoji`，显示为 `${sticker.emoji} Sticker`（如 `🐶 Sticker`，无 emoji 兜底 `Sticker`）；③ 聊天详情页实现贴纸无气泡边框透明渲染（`disableBubble = true`，`backgroundColor = Transparent`，`padding = 0`），按长边等比缩放至最大 180vp；贴纸右下角悬浮半透明胶囊时间戳（`#66000000`）；④ 贴纸多媒体链路（WebP 静态贴纸原生展示、WebM 视频贴纸静音自循环播放、缩略图加载等待态、自动高优先级 16 下载）；⑤ 单元测试通过，真机（6XE0225A27023538）截图验证：会话列表清晰展示 `🐶 Sticker`，聊天详情页清晰透明展示表情包贴纸及右下角浮动时间胶囊。 |
| MEDIA-106 图片下载性能优化 + 视频原生播放 + 未读/最新消息精准定位 | 2026-09-11 ✅ | AI-Agent-Antigravity：① 图片/多媒体性能优化：引入 `requestedFileIds` 严格去重与分片进度 200ms 节流（消除重复拉取和 LazyForEach 频繁重排），并对气泡图片选用 320~960px 适中尺寸，彻底消除会话滑动与渲染卡顿；② 视频全链路解析与原生播放：解析 `messageVideo` 与 `messageVideoNote`（圆形视频），<=10MB 自动下载，气泡显示缩略图、下载环/播放标与时长角标；全屏媒体查看器接入 ArkUI 原生 `Video` 组件（开启 controls/autoPlay），支持进度条拖动与全屏控制；③ 未读/最新消息自动精准定位：`MessageProjection` 启动同步与单调递增 `last_read_inbox_message_id`；`computeScrollTargetIndex` 存在对端未读时精准定位至首条未读（START 对齐），全读时定位至最底部最新消息（END 对齐）；④ 物理返回与侧滑手势拦截（`onBackPress`）；单元测试 `core_domain` 49/49、`feature_chat` 58/58 全绿，真机（6XE0225A27023538）实测验证（Wallet、EI CLUB、Redmi Note 13 Pro 视频播放与未读跳转无卡顿）均 100% 通过。 |
| MEDIA-103 图片全屏 viewer + 系统返回体验优化 | 2026-09-11 ✅ | AI-Agent-Antigravity：① entry `Index.ets` 实现 `onBackPress(): boolean`，拦截物理/虚拟 Back 键与边缘滑动返回手势，按分级顺序退出（全屏查看器 → 长按菜单 → 编辑/回复输入模式 → 会话列表），彻底修复在聊天详情页直接退至手机桌面的问题；② 沉浸式全屏媒体查看器（`MediaViewerOverlay`）：点击气泡图片/动图直接进入黑色背景全屏查看器；支持多媒体项左右滑动翻页（`Swiper`）、双指捏合缩放（`PinchGesture`）、双击快捷放大与复位、单指轻触切换工具栏显隐；顶部常驻返回箭头、署名、序号进度（X / N）、时间戳；底部展示半透明附言（Caption）滚动容器；③ 10 项 Reducer 单元测试全绿通过，`entry` 84 项测试全部回归通过，真机安装验证截图（Wallet / EI CLUB）及连续滑动手势层级返回均 100% 验证通过。 |
| MEDIA-101 / MEDIA-105 消息多媒体渲染 | 2026-09-11 ✅ | AI-Agent-Antigravity：实现 MediaAttachment 模型、ChatCoordinator 自动下载监听联动、ChatPage 气泡宽高比自适应卡片、@kit.CoreFileKit fileUri 沙箱协议转换（解决 GetAsset failed 报错）、Telegram 动图 MP4 格式采用 ArkUI Video 原生无声循环播放与常驻 GIF 胶囊标、图文混排 Caption；单测 58/58 全过，assembleHap BUILD SUCCESSFUL。真机截图验证（EI CLUB 群聊）：竖屏截图与横屏全景图片高清色彩自适应渲染 ✓、Gawr Gura 动图原生流畅循环播放并叠加 GIF 标 ✓ |
| INTEG-003 修 entry 单测编译报错 | 2026-09-11 ✅ | AI-Agent-Antigravity：修复 entry/src/test/lifecycle/FakeAccountBridge.ets（新增 FakeAccountBridgeMetrics 实现 TdNativeBridgeMetrics，修复字段形状与对象字面量报错）和 LifecycleCoordinator.test.ets（引入 LifecycleTestHarness 类修复匿名对象字面量类型报错，冷启动恢复测试使用同底座文件的 freshRegistry 真实模拟杀进程恢复）；./hvigorw test --mode module -p module=entry@default -p product=default --no-daemon 全部 PASS（84 任务全绿，29s）；assembleHap BUILD SUCCESSFUL |
| INTEG-001 设置页接入导航 | 2026-09-11 ✅ | 子agent-87（commit `e8bc7cf`）：会话列表 header 加齿轮入口（⚙ 文本字形）；entry Index.ets 加 showSettings 导航态（优先级 chat > settings > list > auth）；SettingsCoordinator 生命周期对齐 ChatCoordinator（进入创建/订阅/start，离开销毁）；登出成功/会话结束（auth step ≠ ready）自动回登录页（handleSessionEnded 三路销毁）；SettingsPage 加返回箭头；feature_settings 单测通过、assembleHap BUILD SUCCESSFUL。真机验证通过（用户 2026-09-11）：齿轮进设置页 ✓、账号信息展示 ✓、返回箭头回列表 ✓（登出链路未测）。附带发现：entry/src/test 有 2 处 LIFE-001 时期预存单测失败（Fake 的 TdNativeBridgeMetrics 形状过期），干净 HEAD 复跑同样失败，非本次回归——已登记 INTEG-003 待修 |
| MSG-104 已读上报与已读回执 | 2026-09-11 ✅ | 子agent-86 + 主会话真机验证（commit `6377b2b`）：viewMessages 入屏上报（ChatPage onScrollIndex 最后可视下标 → incomingIdUpTo 映射 → ReadReportThrottle 只增节流；sync 路径兜底首屏/新消息）；MessageProjection 订阅 updateChatReadOutbox（水位只增）+ start() 时 getChat 灌 last_read_outbox_message_id 初始水位（修复"打开前已读仍单勾"）；己方消息 ✓/✓✓、pending "…"、failed "!"；失败气泡点击 → resendMessages 重发；单测 feature_chat 53/53、core_domain 48/48（新增 MessageProjection 6 例：灌水位/水位只增/竞态不回退/viewMessages+resendMessages 组包）。**重大根因发现**：真机"对方已读勾不变"排查（桥接日志证实 updateChatReadOutbox 到达且 watermark 已推进到 UI 层）定位为 **ArkUI LazyForEach key 只含 messageId → 同 id 内容变更复用组件不重渲**；修复=key 携带展示内容 FNV-1a 散列（ChatPage.messageRowKey / ChatListPage.chatRowKey），顺带修复实时编辑上屏、会话列表预览/未读数实时刷新。真机：历史已读 ✓✓ ✓、对方实时已读单勾原地变双勾 ✓（诊断日志已移除） |
| MSG-102 群消息署名 UI 接入 | 2026-09-11 ✅ | 子agent-86 + 主会话真机验证（commit `06ca998`）：ChatCoordinator 持有 UserRegistry（构造即 start，destroy 释放订阅+stop）；start() 一次性 getChat 定 ChatKind（私聊/群/频道，群类型稳定无需监听）；`resolveSenderInfo` 纯函数：非 outgoing + 群 + MessageSenderUser 才署名，getUser 命中用 formatFullName，未命中 requestUser 一次（requestedSenderIds 防重）+ subscribeUser 等更新→scheduleSyncFromProjection 重映射上屏；MessageSenderChat/私聊/outgoing → 空署名；MessageItem 加 senderColorIndex（默认 -1）；气泡左侧 28vp 彩底首字母头像 + 调色板署名行（NAME_PALETTE 7 色对齐 chat_list AVATAR_PALETTE）；补 core/domain 缺失的 @tgx/platform-ports 依赖声明（arkts-no-structural-typing 禁接口结构兼容，须直引 Subscription）；10 新单测 44/44 全过。真机：EI CLUB 群聊彩色名字+头像 ✓、私聊布局不变 ✓。**注意**：未走 ChatRegistry（被 ChatListCoordinator 私有持有，feature/chat 无法访问），用自包含 getChat 解决 |
| MSG-101 富文本实体渲染 | 2026-09-11 ✅（有保留） | 子agent-86 + 主会话真机验证（commit `3b25069`）：`MessageTextSpan` 不可变 span 模型 + `FormattedTextParser` 纯函数（边界集合切分、逐段实体并集、相邻同款合并、非法实体 clamp/跳过；UTF-16 offset 与 ArkTS 对齐）；MessageItem 加 spans（默认空兼容旧调用）；ChatPage 气泡 Text/Span 渲染：粗/斜/下划线/删除线、代码等宽+浅底、链接与 mention/hashtag 等着色、spoiler 深色遮罩点击揭开、link 点击 openLink（Span.onClick，SDK 26 d.ts 无 ContainerSpan.onClick）；样式全走 LightTheme token；13 新单测 + 21 回归全过（34/34）。真机：URL/mention/botCommand 蓝色渲染 ✓、链接点击拉起浏览器 ✓；**保留**：粗体/spoiler 真机演示用户豁免（单测覆盖），CustomEmoji 普通文本占位、BlockQuote/DateTime 按普通文本 |
| FILE-102 FileStore 文件下载队列调度器 | 2026-09-10 ✅ | AI-Agent-Antigravity (Subagent-FileStore)：`core/domain/src/main/ets/file/FileStore.ets`；在 FileRegistry 之上实现并发控制（maxConcurrentDownloads 默认 3）、优先级与 FIFO 排队调度（高优先级插队）、下载完成/失败自动推进队列、单任务/批量 pause/resume、任务取消 cancel、断网挂起（setNetworkAvailable 标记 pausedByNetwork）与联网自动恢复重试；单测 12 项用例全 Success，覆盖率达 94.12%，30 项基线单测无回归；`assembleHar` BUILD SUCCESSFUL |
| SET-101 设置主页与账号中心 | 2026-09-10 ✅ | AI-Agent-Antigravity (Subagent-Settings)：新建 `@tgx/feature-settings` HAR 模块；MVI 契约（SettingsUiState, SettingsIntent, SettingsEffect）、纯函数 SettingsReducer、SettingsCoordinator 桥接 AccountScope / UserRegistry / TDLib Gateway（getMe, getUserFullInfo, logOut, optimizeStorage）、SettingsPage 完整 ArkUI 页面（个人信息/首字母头像/通知切换/缓存清理/主题模式切换/登出二次确认弹窗）；18 项单测全部 Success；`assembleHar` BUILD SUCCESSFUL |
| SEARCH-101 全局搜索功能 | 2026-09-10 ✅ | AI-Agent-Antigravity (Subagent-Search)：新建 `@tgx/feature-search` HAR 模块；MVI 契约（SearchUiState, SearchIntent, SearchEffect）、数据模型（SearchChatItem, SearchMessageItem）、纯函数 SearchReducer（空 query 清理/非空搜索触发/历史记录去重最新置顶上限 10 条/Tab 切换联动）、SearchCoordinator（300ms 输入防抖、TdlibSearchQueryProvider 封装 SearchChats 与 SearchMessages、MockSearchQueryProvider 预览兜底）、SearchPage 页面（搜索框/Tab栏/流式历史 Tag/会话与消息分组列表/空状态）；18 项单测全部 Success；`assembleHar` BUILD SUCCESSFUL |
| FILE-101 FileRegistry 文件状态管理 | 2026-09-10 ✅ | AI-Agent-Antigravity (Subagent-1)：`core/domain/src/main/ets/file/FileRegistry.ets`；单账号 `Map<number, File>` 单一事实源缓存；订阅 `updateFile` 事件流并就地更新；`subscribeFile(fileId, listener)` 细粒度监听器机制（标准 Subscription）；`getFile` / `putFile` / `isDownloaded` / `getLocalPath` / `getDownloadProgress` 查询解析；`downloadFile`（组包 DownloadFile、响应结算）与 `cancelDownloadFile`；`stop()` 关停；单测 16 用例全 Success；`assembleHar` BUILD SUCCESSFUL |
| MSG-102 UserRegistry 用户状态管理 | 2026-09-10 ✅ | AI-Agent-Antigravity (Subagent-2)：`core/domain/src/main/ets/user/UserRegistry.ets`；单账号 `Map<number, User>` 缓存单一事实源；订阅 `updateUser` 与 `updateUserStatus` 事件流并就地更新；`subscribeUser(userId, listener)` 细粒度监听器机制；`getUser` / `putUser` / `requestUser`（组包 GetUser 远程拉取并自动结算进缓存）；`formatFullName` 姓名格式化、`getAvatarLetters` 头像首字母、`getAvatarColorIndex` 0..6 调色盘哈希、`isOnline` 在线判定；单测 14 用例全 Success；`core_domain` 全量 30 用例全绿；`assembleHar` BUILD SUCCESSFUL |
| E2E-001 G3 核心场景端到端验收 | 2026-09-10 ✅（有保留） | 真机 VYG-AL00 实测 G3 全链路（计划 §739：启动→登录→会话列表→打开聊天→收发文本→断网恢复→杀进程重启恢复）：启动/登录/会话列表/打开聊天/收发文本分别由 AUTH-E2E-001/CHATLIST-003/CHAT-001~003 验证记录覆盖；**断网恢复**（本轮实测）：用户关 VPN+移动数据 → 会话列表本地缓存正常渲染（6 会话）→ 进 be hu 发 "offline_test" → 气泡上屏带 pending "18:25 ..." → 恢复网络 → TDLib 自动重发 → "..." 消失变正常气泡（时间 18:27）；**杀进程重启**（本轮实测）：`aa force-stop` + 冷启动 → 直接进会话列表（首会话 be hu 预览 offline_test），无登录页残留；错误路径：editMessageText 超时 → `network/timeout/retryable=true` 回灌 errorMessage 细条；权限拒绝：CHAT-004 验证对方消息菜单无 Edit。**保留项**：空数据路径（EmptyView）未现场演示（账号无零消息会话）；取消路径已由 CHAT-004 编辑/回复 X 取消覆盖 |
| CHAT-004 回复/编辑/删除基础动作 | 2026-09-10 ✅ | 子agent-54 + 主会话真机验证（commit `573791e`）：长按气泡→getMessageProperties（离线）拉权限位→reducer 过滤（回复恒可/编辑仅己方文本且 canBeEdited/删除需 canBeDeleted 之一）→底部动作面板；composer 回复/编辑预览条（编辑暂存草稿、取消/返回恢复）；发送按模式分流 reply_to/editMessageText；删除 revoke=canBeDeletedForAllUsers + 响应 ok 乐观本地移除；editMessageText 响应本体 upsert 落本地（不依赖 update 推送时序）；气泡 "edited" 标记与 "↪ 摘要" 引用行；失败统一回灌 errorMessage 细条；21 个 reducer 纯单测全过。真机（VYG-AL00）：对方消息菜单无 Edit ✓；己方三键齐全 ✓；编辑预览条/取消恢复 ✓；be hu 编辑 editMessageText ok + 重进确认新文本+edited ✓；删除即时消失 ✓；回复 0reply_ok 带 ↪hello_from_harmony 引用行上屏 ✓；Wallet 官方通知会话编辑超时（服务器不响应，会话特例）；期间 VPN 抖动出现 network/timeout 错误回灌正常 |
| CHAT-003 composer + send text | 2026-09-10 ✅ | 子agent-50 + 主会话真机验证：`MessageProjection.sendTextMessage`（inputMessageText 组包 sendMessage，空文本参数拒绝）+ 补 `updateMessageSendSucceeded/Failed` 结算（删临时 id、upsert 正式/失败态）；feature/chat MVI 三新 intent + SendTextMessage effect，reducer 发送即清 composerText；ChatPage 底部 ComposerBar（圆角输入框+激活态圆形发送钮、受控输入、回车可发、失败细条不挡列表、气泡 pending '…'/failed '!'）；4 个 reducer 纯单测全过；`assembleHap` BUILD SUCCESSFUL。真机：be hu 私聊发送 hello_from_harmony → sendMessage ok、气泡即刻上屏、输入框清空；他端消息经 updateNewMessage 实时进列表。commit 见 git log |
| CHAT-001 会话消息页 + CHAT-002 文本气泡基础 | 2026-09-10 ✅ | 子agent-35：`core/domain` 新增 `chat/MessageProjection.ets`——单会话消息投影：openChat/closeChat 生命周期、订阅 updateNewMessage/updateMessageContent/updateMessageEdited/updateDeleteMessages（仅本 chatId）、`loadInitialMessages`（from=0, offset=-limit+1）与 `loadOlderMessages`（from=最旧 id, offset=-1，Map 按 id 去重、按 id 升序、回包不足一页或无新增判定到顶）；新建 `feature/chat` har 模块（`@tgx/feature-chat`）：MessageItem/ChatUiState/ChatIntent/ChatEffect/chatReducer（纯函数 MVI）+ ChatCoordinator（桥接 projection，setTimeout(0) 合并同步，Message→MessageItem 文本提取，多媒体 `[Photo]` 占位）+ ChatPage（LazyForEach 气泡：发出右对齐 chatOutgoing/收到左对齐 chatIncoming、HH:mm 时间戳、onReachStart 翻页、首屏/新消息滚到底、空/加载/错误三态、返回箭头）；ChatListCoordinator 构造器加可选 `onNavigateToChat(chatId, chatTitle)` 回调（缺省 no-op，NavigateToChat effect 从 registry 取标题后回调）；entry `Index.ets` 加 currentChatId 导航态，回调里创建/订阅/销毁 ChatCoordinator 并在 ChatListPage/ChatPage 间切换渲染；根 build-profile 追加 feature_chat 模块、entry oh-package 加 @tgx/feature-chat 依赖与 oh_modules 符号链接；`./hvigorw assembleHap --mode module -p module=entry@default -p product=default -p buildMode=debug --no-daemon` BUILD SUCCESSFUL（22s，产物 entry-default-signed.hap）；`./hvigorw test --mode module -p module=core_domain@default` BUILD SUCCESSFUL 不回归；真机验证待主会话 |
| CHATLIST-003 chat list 接入真实 TDLib 数据 | 2026-09-10 ✅ | 子agent-33：`core/domain` ChatListProjection 新增 `setOnStateChanged` 变更回调（setChatPosition/loadNextPage 各路径触发）+ `updateChatLastMessage`（刷新 last_message 并按携带 positions 重排）与 `updateChatReadInbox`（未读数）处理；新建 `feature/chat_list/coordinator/ChatListCoordinator.ets`——桥接 projection 与 MVI：dispatch 经 chatListReducer、FetchChatList→`loadNextPage`，projection 变更经 setTimeout(0) 合并后映射 Chat→ChatListItem 并回灌 OnChatsLoaded（messageText 取原文去换行，多媒体按类型出 `[Photo]` 等占位标签）；投影列表标识用 `new ChatListMain()`（chatListEquals 对 null 恒不等，传 null 永不匹配）；entry `Index.ets` 占位文本替换为 `ChatListPage`，auth `ready` 时用 `BootstrapResult.scope` 创建/订阅/销毁 coordinator；`feature/chat_list` oh-package 增加 core-account/core-domain/core-td-api-generated 依赖并补 oh_modules 符号链接；`./hvigorw assembleHap --mode module -p module=entry@default -p product=default -p buildMode=debug --no-daemon` BUILD SUCCESSFUL（14s，产物 entry-default-signed.hap）；`./hvigorw test --mode module -p module=core_domain@default` BUILD SUCCESSFUL 不回归；真机验证待主会话 |
| AUTH-E2E-001 真机扫码登录与登录态恢复 | 2026-09-10 ✅ | Codex + 用户真机验证：通过 TDLib QR 登录完成真实账号授权，授权页切换到 `chat_list`；随后执行 `aa force-stop org.telegram.x.harmony` 并从桌面图标冷启动，应用未再次显示登录页，直接恢复到 `Chat list placeholder`，证明 TDLib 授权数据库和账号恢复链路生效。当前后续缺口明确为 Entry 会话列表占位尚未接入真实 TDLib chat 数据。 |
| ENTRY-ICON-001 桌面图标启动入口修复 | 2026-09-10 ✅ | Codex：定位桌面图标原先解析到 `AppDetailAbility`（系统应用详情页），而 HDC 显式启动才进入 `EntryAbility`；在 `entry/src/main/module.json5` 为 EntryAbility 补齐 `entity.system.home` + `ohos.want.action.home` skills。`entry assembleHap` BUILD SUCCESSFUL，真机覆盖安装后从桌面图标点击，日志确认 `EntryAbility.onCreate`、bootstrap、setTdlibParameters 成功，页面显示手机号与 QR 登录入口。 |
| AUTH-QR-001 reCAPTCHA 阻塞兜底与二维码登录 | 2026-09-09 ✅ | Codex：确认 `updateApplicationRecaptchaVerificationRequired` 仅面向受支持的官方移动端集成，移除把 Android 移动端 key 塞入 WebView 的无效实现；收到 challenge 后以空 token 调 `setApplicationVerificationToken` 明确结束挂起验证，并接入 TDLib `requestQrCodeAuthentication` + `authorizationStateWaitOtherDeviceConfirmation.link` + ArkUI `QRCode`。新增 5 个 reducer/coordinator 用例；`feature_auth test`、`assembleHar`、`entry assembleHap` 全部 BUILD SUCCESSFUL。真机 6XE0225A27023538 安装成功，日志确认 `VERIFICATION_FAILED` 被安全释放、`requestQrCodeAuthentication` 执行且 QR link 到达，页面实际显示可扫描二维码；commit `1c9dc5c`。 |
| LIFE-001 EntryAbility 前后台协调 | 2026-09-09 ✅ | AI-Agent-Antigravity：`entry/lifecycle`（LifecycleCoordinator：restoreAll 杀进程恢复与兜底建号、onForeground/onBackground 状态流转、NetworkStatePort.observe 动态网络监听、mapSnapshotToNetworkType 映射 SetNetworkType 广播至各活跃 scope、onDestroy 退订与关闭）；EntryAbility 回调（onForeground/onBackground/onDestroy）全链路接通；Bootstrap 装配 ConnectivityAdapter；`assembleHap` BUILD SUCCESSFUL（生成 entry-default-signed.hap 51MB）；commit `dab01fd` |
| CHATLIST-001 chat list domain projection | 2026-09-09 ✅ | AI-Agent-Antigravity：新建 `core/domain` 模块（`@tgx/core-domain`）；ChatRegistry 单一事实源 + ChatListProjection（order 64位 BigInt 降序排列、(order, chat.id) 比较、增量位置更新、loadNextPage 分页）；`assembleHar` BUILD SUCCESSFUL；commit `75e8613`+`63f75b7` |
| CHATLIST-002 chat list ArkUI | 2026-09-09 ✅ | 子agent-27：`feature/chat_list` har 模块（`@tgx/feature-chat-list`）；ChatListPage LazyForEach 列表（头像彩色圆+首字母、标题、预览截断、时间戳、未读角标）+ MVI contract（ChatListUiState/ChatListIntent/ChatListEffect/chatListReducer）+ MockChatData 50 条确定性 mock；`assembleHar` BUILD SUCCESSFUL；commit `eec44b1` |
| PLAT-003 HUKS 安全密钥存储 | 2026-09-09 ✅ | 子agent-28：`platform/keystore` har 模块（`@tgx/platform-keystore`）；HarmonySecureKeyStore 实现 SecureKeyStorePort（Asset Store Kit：put/get/remove/getStatus，DEVICE_UNLOCKED 可达性、OVERWRITE 冲突、NEVER 同步、稳定错误码映射）；`assembleHar` BUILD SUCCESSFUL；commit `eec44b1` |
| BRG-005 TDLib close/shutdown 生命周期 | 2026-09-09 ✅ | 子agent-29：TdCoreBridge 状态机（idle→active→closing→closed），close() 幂等、迟到回调丢弃计数、createClient 快速重建；TdGateway close()（stop + registry.close()，新请求拒绝 GATEWAY_CLOSED_ORIGIN）；RequestRegistry.close() 批量取消 pending；AccountScope.close() 级联 gateway.close()；3 模块 `assembleHar` + `assembleHap` 全 BUILD SUCCESSFUL，既有测试不回归；commit `eec44b1` |
| SPIKE-001 真机授权登录垂直切片 | 2026-09-09 ✅ | 真机端到端验证 PASS：`EntryAbility.onCreate → bootstrap() → AccountScope.open(startActive=true) → gateway.start() → TDLib setTdlibParameters 成功 → authorizationStateWaitPhoneNumber → AuthCoordinator → AuthRootPage "Your Phone" 渲染`。修复两个问题：① TDLib 初始事件在 eventListener 注册前被丢弃 → `executeCoreEffectsForState` 补发 core effects；② `setTdlibParameters` 被调两次（补发 + 事件到达）→ `tdlibParamsSent`/`dbKeySent` 防重标志。新模块：`platform/files`（`HarmonyAppFilesAdapter` 实现 AppFilesPort，@kit.CoreFileKit 原子写入/路径安全）；`entry/ScopeAuthAdapter.ets`（适配 AccountScope → AuthAccountScopeLike）。日志：`setTdlibParameters succeeded` + 页面渲染 "Your Phone"（China +86，输入框，Continue 按钮）；commit `ab62b8c` |
| AUTH-001 授权页面壳与 Reducer 对接 | 2026-09-09 ✅ | AI-Agent-Antigravity：`feature/auth` 注册为 har 模块（modules 追加 `feature_auth`，包名 `@tgx/feature-auth`）；基于 UI-003 MVI 范式（UiState/Intent/Effect/Reducer）+ CORE-005 授权状态机；AuthUiState（11 步骤、电话格式化、验证码位数自适应、2FA 密码显隐、姓名注册、loading/error 状态，copyWith 不可变派生）+ 17 种 AuthIntent + 9 种 AuthEffect + 纯函数 authReducer + AuthCoordinator（异步 effect 驱动、TDLib updateAuthorizationState 解析与状态同步、倒计时调度）+ 5 个 ArkUI 组件（AuthRootView / AuthPhoneView / AuthCodeView / AuthPasswordView / AuthRegistrationView）；纯单测 36 用例全 Success（CountryCode 3 + AuthReducer 28 + AuthCoordinator 5）：`./hvigorw test --mode module -p module=feature_auth@default -p product=default --no-daemon`；`assembleHar` BUILD SUCCESSFUL |
| GEN-003 codec fixture round-trip | 2026-09-09 ✅ | AI-Agent-J：22 个全合成脱敏 fixture（TDLib 官方仓库无静态 JSON 样本，字段结构派生自 td_api.tl；13 个授权状态机 + 4 个 updateNewMessage 内容类型 + error/ok `@extra` 关联 + vector&lt;int64&gt; 大数 + 2 个未知类型 raw 回传；来源/脱敏规则/加 fixture 流程见 `core/td_api_generated/fixtures/README.md`）；每 fixture 独立用例 `decodeTdObject→encodeTdObject→canonicalJson 语义等价→二次 decode 稳定`，未知类型断言 `TdUnknownObject` 无损；`FixtureData.ets` 由 `fixtures/sync_fixtures.py` 从 json 确定性生成；`./hvigorw test --mode module -p module=core_td_api_generated@default -p product=default --no-daemon` `Tests run: 48, Failure: 0, Error: 0, Pass: 48`（既有 20 例不回归）；期间定位并清除损坏的 533MB `init_coverage.json` 构建缓存（00308018 假错误，`.test/` 产物非源码） |
| GOV-001 空工程 | 2026-09-08 | Debug/Release 构建均 BUILD SUCCESSFUL，产出 `entry/build/default/outputs/default/entry-default-unsigned.hap`（212 KB）；commit `7fe266b` |
| GOV-002 工具链锁定 | 2026-09-08 | `tools/toolchain-versions.json` + `tools/ci/setup-check.sh` 退出码 0（SDK 26.0.0.105 / hvigor 6.26.4）；版本不匹配会明确失败 |
| GOV-003 文档控制面 | 2026-09-08 | `docs/`（ARCHITECTURE、ADR-001/002、Feature/Parity、quality 五件、runbooks 三件）+ `work-items/templates/`（§17.1/17.2 原文模板） |
| GOV-007 秘密管理 | 2026-09-08 | `.gitignore` 覆盖签名/p12/cer/local_properties/api_hash；`tools/ci/secret-scan.sh` 已接入 ci.sh（PEM 私钥、api_hash 模式阳性 fixture 验证通过）；commit `7fe266b`+`b94e5a6` |
| GOV-004 Feature/Parity Matrix | 2026-09-08 | FEATURE_MATRIX：7 项 P0 + 38 项 P1（含真实 Android 类级路径+行号、TDLib 方法）、P2/P3 一行表；PARITY_MATRIX 45 行对齐骨架；commit `b94e5a6` |
| GOV-005 Android 行为样本 | 2026-09-08 | `docs/product/behavior-samples/` 5 份（登录/会话列表/发文本/通知/媒体）+ README，全部脱敏、含 TDLib 事件与 Harmony 验收观察点；截图/录屏待真机；commit `b94e5a6` |
| GOV-006 CI 最小流水线 | 2026-09-08 | `tools/ci/ci.sh` 6 步全链路实测通过（工具链闸→secret scan→lint/typecheck→单测→debug→release 构建）；`.github/workflows/ci.yml` self-hosted 模板；commit `b94e5a6` |
| QA-001 测试骨架 | 2026-09-08 | `hvigorw test` 真实执行通过：1 用例 Success（注意：本 hvigor 6.26.4 单测须放 `entry/src/test/*.test.ets`，ohosTest 壳留作设备测试）；commit `b94e5a6` |
| SEC-001 威胁模型 v0 | 2026-09-08 | `docs/architecture/threat-model-v0.md`：数据流图+6 信任边界、9 资产、16 威胁（账号/消息/文件/Push/bridge/存储全覆盖）、7 秘密管理规则、§18 风险映射；commit `b94e5a6` |
| TDN-001 依赖构建矩阵 | 2026-09-08 | `native/tdcore/README.md`：OpenSSL 3.5.8（必须，TD CMake 硬依赖）/ zlib 1.3.1（NDK sysroot）/ SQLite 3.31.0（TD 自带 amalgamation）/ TDLib d1085f9ce，各带版本、来源、hash、许可证；commit `0dd04ef` |
| TDN-002 最小依赖构建 | 2026-09-08 ✅ | OHOS NDK 交叉编译产出 `libtdjson.so`（arm64，strip 后 45.9MB，build-id 7b9c054b）+ `libcrypto.so.3`；musl 补丁 0001；**真机应用内 smoke PASS**：TDLib 1.8.67，execute(getTextEntities) 正常返回（hdc shell 域禁 exec ELF 的绕过方案见 runbook） |
| BRG-001/002 Node-API 桥 | 2026-09-08 ✅ | `libtdcore_napi.so` 导出 getVersion/execute/createClient/send（契约对照 §5.2，错误安全）；真机验证：页面显示 `TDLib 版本: 1.8.67` + textEntities JSON；证据 `native/tdcore/napibridge/EVIDENCE.md` + 设备截图；commit `c93ebd5` |
| BRG-003/004 receive+队列 | 2026-09-09 ✅ | receive 线程 → 有界队列 → TSFN → ArkTS 全管线；真机端到端 PASS：getOption 异步响应按序到达（seq #1-#4 单调），dropped=0，force-stop 重启无崩溃；证据 `napibridge/EVIDENCE.md` + 两张设备截图；commit `62b71a2` |
| 结构骨架 | 2026-09-08 | core×9 / platform×14 / feature×12 / native×3 / tools×4 / test×4 目录已建（仅 .gitkeep，未注册构建，符合 ADR-002） |
| GEN-001 schema IR | 2026-09-08 ✅ | AI-Agent-C：`tools/td_api_codegen/td_api_ir.py`（纯 python3 无依赖）解析 td_api.tl（16313 行）→ `core/td_api_generated/schema.ir.json` + golden 快照 `tools/td_api_codegen/snapshot/td_api.ir.json`；schemaHash `7fbae70a…c5929`（SHA-256，仅随 schema 内容变）；stats types=743 / constructors=3214（objects=2192+functions=1022）/ fields=6997；入口 `python3 tools/td_api_codegen/td_api_ir.py verify`（CI 用，不写文件）——重复运行字节级一致，故意改输入（加字段）verify/generate 均退出码 1、恢复后通过；README 含 GEN-002 交接说明 |
| GEN-002 ArkTS DTO/union 全量生成 | 2026-09-09 ✅ | AI-Agent-F：`tools/td_api_codegen/td_api_arkts.py` 从 schema.ir.json 生成全量 ArkTS（classes=3205、unions=743、49 个 .ets）：每构造器一个类（判别属性 `type`、`@extra` 载体、int64→string）+ 每抽象类型判别联合（恒含 `TdUnknownObject`）+ decode/encode + `TdResponseMap` 请求返回映射 + `decodeTdObject`/`encodeTdObject`；decode 永不抛错（未知 `@type`→`TdUnknownObject` 保 raw 可回传、未知字段忽略、缺字段取默认）；命名规则/避让（`type_`/`extra_`、`DateValue`/`ErrorValue`/`ProxyValue`、`TdJsonRaw`）与 panda index 上限分块约束（`TdUnions_<A-Z>` + `TdEntries`）见 README；`generate`/`verify` 双入口、重复生成字节级一致（shasum 两次相同）；har 模块 `core_td_api_generated`（`@tgx/td-api-generated`）`assembleHar` BUILD SUCCESSFUL；单测 20 用例全 Success（codec round-trip/未知 `@type` 与未知字段 fallback/int64 精度/`@extra`/响应映射）`./hvigorw test --mode module -p module=core_td_api_generated@default -p product=default --no-daemon`；首版生成物曾被 BRG-003/004 的 commit `032cdfa` 顺带提交（CI 需要），本轮为重构后最终版 |
| CORE-001 Result/AppError/Clock/Id | 2026-09-08 ✅ | AI-Agent-E：`core/common` 注册为 har 模块（build-profile.json5 modules 追加 `core_common`，未动 entry/signing）；Result 判别联合（map/flatMap/mapError/getOrElse/getOrNull/fold）+ AppError 七类稳定错误（含 fromTdlibError 稳定映射、toLogString 脱敏）+ Clock（System/Manual）+ IdGenerator（Random 可注入/Sequential fake）；纯单测 27 用例全 Success：`./hvigorw test --mode module -p module=core_common@default -p product=default --no-daemon`（全工程 `hvigorw test --no-daemon` 亦 BUILD SUCCESSFUL，entry 1 用例回归通过）；`assembleHar` BUILD SUCCESSFUL；交接说明见 `core/common/README.md`（单调时钟需 platform 注入） |
| UI-001 Design token/theme | 2026-09-08 ✅ | AI-Agent-G：`core/design_system` 注册为 har 模块（build-profile.json5 modules **仅追加** `core_design_system`）；19 个语义色 token 深浅两套（LightColors/DarkColors）+ 9 档字阶排版 + 间距/圆角/动画 token（曲线存贝塞尔控制点保持 Kit-free）；`Theme`/`createTheme(mode, fontScale)` + `resolveToken(theme, dottedName)` 五命名空间解析入口（未知名返回 undefined）；fontScale 适配 clamp [0.85,2.0] + 最小可读字号 11vp 下限；纯单测 25 用例全 Success（深浅色映射完整且互异、WCAG 对比度断言、fontScale 五边界、token 存在性/有序性）；`assembleHar` BUILD SUCCESSFUL；命名规范/加 token 流程/ArkUI 接入说明见 `core/design_system/README.md` |
| PLAT-001 Platform Port 契约 | 2026-09-08 ✅ | AI-Agent-H：`platform/ports` 注册为 har 模块（modules **仅追加** `platform_ports`，包名 `@tgx/platform-ports`，依赖 `@tgx/core-common` file: 引用）；5 个端口契约（PreferencesPort flush 派发/同 key 合并/通配事件、SecureKeyStorePort 锁屏/不可用语义 + 稳定平台错误码、NetworkStatePort 快照去重/初始值语义、AppFilesPort 路径越界拒绝/配额、MonotonicClockPort 单调不减）+ 5 个 Fake + `src/test/contract/` 同套参数化契约测试（生产 adapter 复用同一函数）；同套契约 36 用例全 Success（`Tests run: 36, Failure: 0, Error: 0, Pass: 36`），`assembleHar` BUILD SUCCESSFUL；语义约定/交接见 `platform/ports/README.md`；本地 har 依赖用 `oh_modules/@tgx/core-common → ../../../../core/common` 符号链接（gitignored） |
| UI-002 Typed navigation | 2026-09-08 ✅ | AI-Agent-I：`core/navigation` 注册为 har 模块（modules **仅追加** `core_navigation`，包名 `@tgx/core-navigation`，依赖 `@tgx/core-common`）；`AppRoute` discriminated union（8 路由）+ `ParamSpec` 参数 schema（int 限 safe-integer 范围/string maxLength+pattern/boolean）+ RouteRegistry（name↔path 模板互转、重复注册与非法模板拒绝）+ RouteCodec（parse/encode round-trip、缺失必填/类型错误/越界/未知路由/未知与重复参数/非法百分号编码全部 Err 不抛异常；parseDeepLink 白名单规则 scheme/host/query 映射，未知 URI 安全拒绝，junk query 忽略）+ NavigationStack（push/replace/pop/popToRoot、serialize/restore round-trip、连续重复去重、任一非法项整体拒绝）；纯单测 37 用例全 Success（`Tests run: 37, Failure: 0, Error: 0, Pass: 37`）：`./hvigorw test --mode module -p module=core_navigation@default -p product=default --no-daemon`；命名规范/加路由四步骤/ArkUI Navigation 接入预留见 `core/navigation/README.md`；**新工程要点**：跨 har 模块判 Result 分支禁用 `instanceof Ok/Err`（类身份跨模块可能不一致，实测误判），用 `result.ok === false` 收窄或 `fold` |
| UI-003 UiState/Intent/Effect 基类约定 | 2026-09-09 ✅ | AI-Agent-M：`feature/_template` 注册为 har 模块（modules **仅追加** `feature_template`，包名 `@tgx/feature-template`，依赖 `@tgx/core-common`）；UiState/Intent/Effect 空接口标记 + `Reducer<S,I,E>` 契约（`(state, intent, env) -> {state, effects}`，env 注入 Clock/IdGenerator，reducer 纯函数可测）+ 计数器示例 feature（`copyWith` 不可变更新——ArkTS 不支持对象 spread、`kind` 字面量判别、幂等防重返回原引用、参数校验走 Effect）；纯单测 15 用例全 Success（每 intent 分支、effect 数量/类型/参数精确断言、输入不可变、chained runs 独立）：`./hvigorw test --mode module -p module=feature_template@default -p product=default --no-daemon`；`assembleHar` BUILD SUCCESSFUL；四步创建法/reducer 五条禁忌见 `feature/_template/README.md`；commit `a892ce2` |
| PLAT-004 网络状态生产 adapter | 2026-09-09 ✅ | AI-Agent-L：`platform/network` 注册为 har 模块（modules **仅追加** `platform_network`，包名 `@tgx/platform-network`，依赖 `@tgx/core-common` + `@tgx/platform-ports` file: 引用，oh_modules 符号链接同 ports）；ConnectivityAdapter = Kit-free 引擎（`ConnectivitySource` 注入面）+ ConnectivityKitSource（唯一 import `@ohos.net.connection`：`createNetConnection` 监听 netAvailable/netLost/netCapabilitiesChange，current 用 getDefaultNetSync+getNetCapabilitiesSync）；映射表 bearer→wifi/cellular/ethernet/other（多 bearer 优先级 ethernet>wifi>cellular>other）+ metered（networkCap 无 NET_CAPABILITY_NOT_METERED=11）+ SnapshotDeduper 连续同值去重；注册即派初始快照（经去重器防 flicker）、注销幂等（本 SDK NetConnection 无 off，unregister 移除全部监听→单订阅设计，active 闭包丢迟到事件）、注册失败 err(platform 2100002)；纯单测 18 用例全 Success（映射 7 + 去重 4 + 契约镜像 7，含注册失败透传）：`./hvigorw test --mode module -p module=platform_network@default -p product=default --no-daemon`；`assembleHar` BUILD SUCCESSFUL；ohosTest 设备骨架就位未上真机；权限 GET_NETWORK_INFO 已声明；验证矩阵/已知限制见 `platform/network/README.md` |
| CORE-002 TdGateway+RequestRegistry | 2026-09-09 ✅ | `core/td_gateway`（`@tgx/td-gateway`）：RequestRegistry（@extra.requestId 注入、Promise 恒回 Result 不 reject、超时 10s 可配、取消、迟到丢弃计数、7 项指标）+ OrderedEventRouter（clientId+sequence 单调校验、乱序上报不静默、重复幂等）；FakeBridge 单测 53/53；CI 全绿；commit `4f1194e` |
| CORE-005 授权状态机 reducer | 2026-09-09 ✅ | authReducer（event-driven，10 状态/12 事件/effect union）+ AuthorizationStateMachine 不可变包装 + failed/closed 终态守卫；98 用例全 Success（60 原有 + 38 新增）；CI 全绿；commit `9d5e7a1` |
| CORE-003 AccountScope/Registry | 2026-09-09 ✅ | `core/account`（`@tgx/core-account`）：生命周期纯 reducer（active/hibernating/closed 终态幂等）、每账号独占 TdGateway + ScopeIsolatingBridge 事件边界隔离、restoreAll 逐账号失败隔离、迟到事件守卫丢弃；AuthorizationState 占位待 CORE-005；60/60 用例；CI 全绿；commit `832f11f` |
| GEN-004 敏感字段元数据+脱敏 | 2026-09-09 ✅ | 生成器 `td_api_sensitive.py`（generate/verify 双模式，字节级可复现）：124 构造器/154 字段/44 类型级清单（redact 75/mask 79，三层首匹配规则表带决策注释）；产物 `td_api_generated/.../redaction/`（TdSensitiveFields 3861 行 + TdRedact）；`core/observability`（`@tgx/observability`）白名单 Logger + 双防线脱敏，43/43 用例（secret fixture 100% 拦截）；commit `4f1194e` |
| BRG-006 生产桥 adapter | 2026-09-09 ✅ | `platform/tdcore-bridge`（`@tgx/platform-tdcore-bridge`）：TdCoreBridge 实现 TdNativeBridgeLike（getVersion/execute/createClient/send/subscribeUpdates/unsubscribe/getMetrics，可注入 native module）；entry Bootstrap.ets 装配骨架（TdCoreBridge→AccountRegistry→scope，FakeAppFiles 占位，EntryAbility 未接）；11/11 用例；CI 全绿 |
| PLAT-002 Preferences/RDB adapter | 2026-09-09 ✅（设备项待验） | AI-Agent-K：`platform/storage` 注册为 har 模块（modules **仅追加** `platform_storage`，包名 `@tgx/platform-storage`，依赖 `@tgx/core-common` + `@tgx/platform-ports` file: 引用，oh_modules 符号链接同 ports）；**两个 PreferencesPort 生产 adapter**：`HarmonyPreferencesAdapter`（@ohos.data.preferences，open/openSync）+ `RdbPreferencesAdapter`（@ohos.data.relationalStore，open/幂等 close），共享纯 ArkTS `PreferenceStoreCore`（内存视图/同 key 合并/flush 后派发/clear 通配/失败挂起重试，语义对齐 PLAT-001 契约）；**schema+migration**：`tgx_meta(user_version)` + `tgx_kv(key,type_tag,value_text,value_real,value_integer)` 类型分列，线性版本链 `planMigrations`（up/down 双路径、缺 down 步骤拒绝回滚、版本跳跃遍历）+ `runMigrations`（每步完成先推版本标记→中断可断点续跑，Kit 层 beginTransaction/commit/rollBack 整体回滚）；fake 注入中断/升级/回滚单测 **47 用例全 Success**（`Tests run: 47, Failure: 0, Error: 0, Pass: 47`）：`./hvigorw test --mode module -p module=platform_storage@default -p product=default --no-daemon`；`assembleHar` BUILD SUCCESSFUL（Kit adapter 严格编译通过）；`src/ohosTest` 设备测试（真实落盘+重开读回/事件派发）就位**待真机**；**新工程要点**：ArkTS 收窄仅块内 `if (r.ok/r.err)` 有效（取反/early-return/else 分支均不收窄，用 getOrNull/getOrElse 或 fold 规避）；内联对象字面量类型被禁（用 interface union）；PLAT-001 contract 函数在 ports 的 src/test、跨模块相对 import 被编译器拒绝→全量 contract 复用待 test_support 包；验证矩阵/交接见 `platform/storage/README.md` |
| RECAPTCHA-001 reCAPTCHA verification flow | 2026-09-09 ✅ | subagent：`feature/auth` 模块内 9 文件修改 + 1 新建；`AuthEventParser` 新增 `parseRecaptchaUpdateFromWire`（解析 `updateApplicationRecaptchaVerificationRequired` wire JSON → `{verificationId, action, recaptchaKeyId}`，orthogonal 于 `AuthEvent` union 不影响 core/account 穷尽匹配）；`AuthStep` 加 `'waitRecaptcha'`；`AuthUiState` 加 `verificationId/recaptchaKeyId/recaptchaAction` 三字段（constructor/initial/copyWith 全更新）；`AuthIntent` 加 `OnRecaptchaRequired`+`SubmitRecaptchaToken`；`AuthEffect` 加 `SendVerificationToken`；`AuthReducer` 加 `onRecaptchaRequired`（step→waitRecaptcha, isSubmitting=false, 清错误）+ `submitRecaptchaToken`（isSubmitting=true, 产出 SendVerificationToken effect）；`AuthCoordinator.handleIncomingEvent` 在 `parseAuthEventFromWire` 前拦截 recaptcha update → dispatch OnRecaptchaRequired；`executeEffect` 加 `sendVerificationToken` case（Record<string,Object> 构参 → `setApplicationVerificationToken`）；`AuthRootPage` 加 `waitRecaptcha` 分支渲染 `RecaptchaPage`；新建 `RecaptchaPage.ets`（HarmonyOS `Web` 组件 + `registerJavaScriptProxy` JS bridge → `recaptchaBridge.onToken(token)` → 回调 `onToken` prop → dispatch `SubmitRecaptchaToken`）；`assembleHap` BUILD SUCCESSFUL |

已知工程要点（runbook 已记录）：hvigorw 为 shell/JS polyglot；离线构建依赖 `~/.hvigor/project_caches`；`DEVECO_SDK_HOME` 必须指向 `…/Contents/sdk`（wrapper 已内置）；签名/真机待用户提供。

已知工程要点（CORE-001 实测）：① 后续 har 模块照 `core/common/` 模板注册：`hvigorfile.ts` 用 `harTasks`；模块自己的 `build-profile.json5`（`{"apiType":"stageMode","buildOption":{}}`）；`src/main/module.json5` 的 `type` 必须是 `"har"`（写 `"shared"` 会被当 HSP 导致 `PackageSignHar` 缺失）；oh-package.json5 的 `main` 指向 `./src/main/ets/Index.ets`。② har 模块单测入口固定为 `src/test/List.test.ets`（hvigor 生成 harness 硬编码 import 它，由它汇总其他 `*.test.ets`）。③ 模块本地单测需在 `<module>/oh_modules/@ohos/hypium` 建符号链接到根 `oh_modules/.ohpm/@ohos+hypium@1.0.21/...`（gitignored；照抄 entry 的做法）。

已知工程要点（MSG-104 实测，**所有做列表的 AI 必读**）：ArkUI `LazyForEach` 以 keyGenerator 的 key 判定行身份——key 不变则复用已有行组件、**不重新执行 builder**，即使数据源 `onDataReloaded()` 全量刷新也一样。只拿 id 做 key 时，同一条目的内容变更（已读勾、编辑后文本、预览文字、未读数）永远不会上屏，只剩"id 变了/新增了行"（发送结算、新消息）才刷新。正确做法：key = `id + 展示相关内容的散列`（参考 `ChatPage.messageRowKey` / `ChatListPage.chatRowKey` 的 FNV-1a 实现）。此前"updateMessageContent 推送不生效"的遗留印象很可能也是这个 UI 问题而非推送丢失——updateChatReadOutbox 已用桥接日志证实推送链路正常。

已知工程要点（INTEG-002 实测，**做 UI 的 AI 必读**）：拼错的 design token 会**编译通过**，只在渲染到该分支时抛 `TypeError` 直接杀进程（`reason: JsError`, exit 254），单测与"没跑到那条分支"的真机验证都发现不了。真实事故：`T.typography.caption1`/`caption2`、`T.radius.xs` 共 9 处在搜索页潜伏，只有出现搜索结果才崩。**自检靠 `python3 tools/ci/check_design_tokens.py`（已进 CI 第 3 步）**。另：ArkTS 崩溃想拿栈，按 **PID 全域名**抓 hilog（`hdc hilog | grep ' <pid> '`）——按 bundle 名或只看应用域会漏掉带源码位置的 `C01317/.../AppKit` 栈帧。

已知工程要点（MSG-106 实测，**上一条的补充，同样必读**）：把状态并进 key **只是一半**——还必须有东西触发数据源重载。`ChatPage.onUiStateChanged` 原先在 `messages === this.shownMessages` 时直接 early-return（为了优化：输入栏每次击键不必重载列表）。多选勾选圈、MSG-103 日期头这类"messages 数组引用不变、只有行内内容变"的状态，会因此**永远不触发重载**，key 里加了 `selected` 也没用。正确做法：为这类状态单独维护一个签名（`selectionSignature()`），签名变化时显式 `dataSource.setItems(messages)` 重算 key，并且**不要**顺带改动滚动位置（否则勾选时列表会跳到首条未读/末尾）。判断口诀：新增"原地变更"的展示状态时，同时问自己「谁负责重载数据源」。

## 阻塞 / 风险记录

| 日期 | 事项 | 状态 |
|---|---|---|
| 2026-09-08 | 用户输入项（api_id/api_hash、真机、AGC/Push 配置）未提供 —— 阻塞 Phase 1 真机项，不阻塞 Phase 0 工程脚手架 | ✅ 已解决：api_id/api_hash 已放入 gitignored `local.properties`（取自 Android 工程）；VYG-AL00 真机已连（API 24 / 6.1.0.135），DevEco 自动签名已接入 |
| 2026-09-08 | compatibleSdkVersion 26 与设备 API 24 不匹配 | ✅ 已修复为 `6.1.1(24)`（hvigor 映射表 6.1.1→24）；signed hap 已真机安装成功（bundle 校验通过），启动待解锁屏幕 |

| RECAPTCHA-002 Web reCAPTCHA baseUrl 修复 | 2026-09-10 ✅ | RecaptchaPage 恢复 Web 组件（loadData 带 baseUrl=https://web.telegram.org/ 解决 "Invalid domain for site key"）+ QR 码兜底按钮并存；AuthRootPage 传 recaptchaKeyId/onToken；waitRecaptcha 步骤忽略请求超时错误（verifier 持有原查询）；commit `830dc8c` |
| TDN-003 OHOS reCAPTCHA 拦截补丁 | 2026-09-10 ✅ | NetQueryDispatcher.cpp L112/L378 加 `|| defined(__OHOS__)` 启用 NetQueryVerifier（403 RECAPTCHA_CHECK 拦截 + verifier actor 创建）；patch 文件 `native/tdcore/patches/0002-ohos-recaptcha-verification.patch`；libtdjson.so 重编译完成（stripped 47.9MB） |

### 当前状态（2026-09-10）
- 手机已通过 QR 登录（AUTH-QR-001 by Codex），auth 全链路打通
- 会话列表已接入真实 TDLib 数据（CHATLIST-003）：登录/冷启动恢复后 Index.ets 渲染 `ChatListPage`，经 `ChatListCoordinator` ← `ChatListProjection`（ChatListMain）订阅 updateNewChat/updateChatPosition/updateChatLastMessage/updateChatReadInbox，`loadChats` 分页；下拉刷新走 RefreshChats
- 会话消息页已落地并真机验证通过（CHAT-001 + CHAT-002 基础）：点击会话行 → `onNavigateToChat` 回调 → Index.ets 创建 `ChatCoordinator`（`MessageProjection`：openChat + getChatHistory 分页 + 四类消息更新订阅）→ 渲染 `ChatPage` 文本气泡（右出左入、HH:mm、滚顶翻页、到底跟随新消息）；返回箭头销毁并切回列表
- **2026-09-10 修复聊天历史加载链**（commit `21c634b`）：真机发现所有会话只渲染 1 条消息。根因两条：① getChatHistory 初始请求 offset=-limit+1，from_message_id=0 时 TDLib 语义为"末条+之后 -offset-1 条更新的消息"，末条之后没有更新消息 → 只回 1 条（已改 offset=0）；② TDLib `get_dialog_history` 内存优先——内存里只有 last_message 时立即返回它并后台异步 preload 更旧历史，preload 无声完成（无 update 推送），客户端须再次请求才拿得到；配合修复：到顶判定改为连续 3 页无新增（容忍 preload 空窗），ChatCoordinator 在消息不足一屏（<20）时延迟 900ms 自动补拉。真机验证：EI CLUB 群连续补拉多页 20 条、单屏 11+ 气泡；be hu 私聊 5 条全量；消息真少的会话 3 次空跑后正确停止
- **E2E-001 G3 验收通过（2026-09-10，有保留）**：断网恢复（发消息 pending → 恢复网络自动重发成功）与杀进程重启（force-stop 冷启动直接回会话列表）真机实测通过；保留项见「已完成」表 E2E-001 行
- **下一步**：按计划 §9.6 进入 Phase 4/5 Epic 拆分——Epic 不得整体分配给单个 AI，先拆成 0.5–3 天工作包登记进「待认领」再逐包实施；可顺手的已知改进（非计划内、低优先）：senderName 接 user registry、日期分隔条、非文本消息占位标签实体渲染、翻页前插滚动跳动
