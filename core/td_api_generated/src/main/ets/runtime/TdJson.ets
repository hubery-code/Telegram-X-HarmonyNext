/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

// Schema: native/tdcore/third_party/td/td/generate/scheme/td_api.tl
// schemaHash: 7fbae70abe4def1a2576f9b89fe364b52501c68ac074a14ba67be87c729c5929

/** JSON value as produced/consumed by the TDLib JSON interface. */
export type TdJsonRaw = boolean | number | string | TdJsonRaw[] | TdJsonObject | null;
export interface TdJsonObject extends Record<string, TdJsonRaw> {
}

/** SHA-256 over the canonicalized schema (GEN-001). */
export const TD_SCHEMA_HASH: string = '7fbae70abe4def1a2576f9b89fe364b52501c68ac074a14ba67be87c729c5929';
export const TD_CODEGEN: string = 'tools/td_api_codegen/td_api_arkts.py';
export const TD_CODEGEN_FORMAT_VERSION: number = 1;

/** Forward-compatibility fallback for an unknown TDLib constructor (`@type` not present in this schema). Never throws; preserves the raw record so it can be re-encoded losslessly. */
export class TdUnknownObject {
  type: string = '';
  raw: Record<string, TdJsonRaw> = {};

  constructor(type: string = '', raw: Record<string, TdJsonRaw> = {}) {
    this.type = type;
    this.raw = raw;
  }
}

/** Re-encode an unknown object preserving its raw JSON fields. */
export function encodeUnknownObject(value: TdUnknownObject): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  for (const key of Object.keys(value.raw)) {
    out[key] = value.raw[key];
  }
  return out;
}

export function asRecord(value: TdJsonRaw): Record<string, TdJsonRaw> {
  if (typeof value === 'object' && value !== null && !Array.isArray(value)) {
    return value as Record<string, TdJsonRaw>;
  }
  return {};
}

export function asString(value: TdJsonRaw, defaultValue: string = ''): string {
  if (typeof value === 'string') {
    return value;
  }
  if (typeof value === 'number') {
    return value.toString();
  }
  return defaultValue;
}

export function asNumber(value: TdJsonRaw, defaultValue: number = 0): number {
  if (typeof value === 'number') {
    return value;
  }
  if (typeof value === 'string') {
    const parsed: number = Number(value);
    if (!Number.isNaN(parsed)) {
      return parsed;
    }
  }
  return defaultValue;
}

export function asInt64(value: TdJsonRaw): string {
  if (typeof value === 'string') {
    return value;
  }
  if (typeof value === 'number') {
    return value.toString();
  }
  return '0';
}

export function asBool(value: TdJsonRaw): boolean {
  return value === true;
}

export function decodeVector<T>(value: TdJsonRaw, decoder: (item: TdJsonRaw) => T): T[] {
  if (!Array.isArray(value)) {
    return [];
  }
  const out: T[] = [];
  for (const item of value) {
    out.push(decoder(item));
  }
  return out;
}

export function encodeVector<T>(value: T[], encoder: (item: T) => TdJsonRaw): TdJsonRaw[] {
  const out: TdJsonRaw[] = [];
  for (const item of value) {
    out.push(encoder(item));
  }
  return out;
}
