/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { File, decodeFile, encodeFile } from './TdTypes_F';
import { MaskPosition, Message, decodeMaskPosition, decodeMessage, encodeMaskPosition, encodeMessage } from './TdTypes_M';
import { TdFileType, decodeTdFileType, encodeTdFileType } from './TdUnions_F';
import { TdInputFile, decodeTdInputFile, encodeTdInputFile } from './TdUnions_I';
import { TdMessageSender, decodeTdMessageSender, encodeTdMessageSender } from './TdUnions_M';
import { TdNetworkStatisticsEntry, TdNetworkType, TdNotificationGroupType, TdNotificationType, decodeTdNetworkStatisticsEntry, decodeTdNetworkType, decodeTdNotificationGroupType, decodeTdNotificationType, encodeTdNetworkStatisticsEntry, encodeTdNetworkType, encodeTdNotificationGroupType, encodeTdNotificationType } from './TdUnions_N';
import { TdPushMessageContent, decodeTdPushMessageContent, encodeTdPushMessageContent } from './TdUnions_P';
import { TdStickerFormat, decodeTdStickerFormat, encodeTdStickerFormat } from './TdUnions_S';

/** networkStatistics = NetworkStatistics — A full list of available network statistic entries */
export class NetworkStatistics {
  type: 'networkStatistics' = 'networkStatistics';
  extra?: string;
  /** since_date: int32 — Point in time (Unix timestamp) from which the statistics are collected */
  since_date: number = 0;
  /** entries: vector<NetworkStatisticsEntry> — Network statistics entries */
  entries: TdNetworkStatisticsEntry[] = [];
}

/** Decode a TDLib `networkStatistics` object; null input -> null. */
export function decodeNetworkStatistics(json: TdJsonRaw): NetworkStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NetworkStatistics = new NetworkStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.since_date = asNumber(record['since_date']);
  out.entries = decodeVector(record['entries'], (item: TdJsonRaw): TdNetworkStatisticsEntry => decodeTdNetworkStatisticsEntry(item) as TdNetworkStatisticsEntry);
  return out;
}

/** Encode a `networkStatistics` into TDLib JSON wire shape. */
export function encodeNetworkStatistics(value: NetworkStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'networkStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['since_date'] = value.since_date;
  out['entries'] = encodeVector(value.entries, (item: TdNetworkStatisticsEntry | null): TdJsonRaw => item === null ? null : encodeTdNetworkStatisticsEntry(item));
  return out;
}

/** networkStatisticsEntryCall = NetworkStatisticsEntry — Contains information about the total amount of data that was used for calls */
export class NetworkStatisticsEntryCall {
  type: 'networkStatisticsEntryCall' = 'networkStatisticsEntryCall';
  extra?: string;
  /** network_type: NetworkType — Type of the network the data was sent through. Call setNetworkType to maintain the actual network type */
  network_type: TdNetworkType | null = null;
  /** sent_bytes: int53 — Total number of bytes sent */
  sent_bytes: number = 0;
  /** received_bytes: int53 — Total number of bytes received */
  received_bytes: number = 0;
  /** duration: double — Total call duration, in seconds */
  duration: number = 0;
}

/** Decode a TDLib `networkStatisticsEntryCall` object; null input -> null. */
export function decodeNetworkStatisticsEntryCall(json: TdJsonRaw): NetworkStatisticsEntryCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NetworkStatisticsEntryCall = new NetworkStatisticsEntryCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.network_type = decodeTdNetworkType(record['network_type']);
  out.sent_bytes = asNumber(record['sent_bytes']);
  out.received_bytes = asNumber(record['received_bytes']);
  out.duration = asNumber(record['duration']);
  return out;
}

/** Encode a `networkStatisticsEntryCall` into TDLib JSON wire shape. */
export function encodeNetworkStatisticsEntryCall(value: NetworkStatisticsEntryCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'networkStatisticsEntryCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['network_type'] = value.network_type === null ? null : encodeTdNetworkType(value.network_type);
  out['sent_bytes'] = value.sent_bytes;
  out['received_bytes'] = value.received_bytes;
  out['duration'] = value.duration;
  return out;
}

/** networkStatisticsEntryFile = NetworkStatisticsEntry — Contains information about the total amount of data that was used to send and receive files */
export class NetworkStatisticsEntryFile {
  type: 'networkStatisticsEntryFile' = 'networkStatisticsEntryFile';
  extra?: string;
  /** file_type: FileType — Type of the file the data is part of; pass null if the data isn't related to files */
  file_type: TdFileType | null = null;
  /** network_type: NetworkType — Type of the network the data was sent through. Call setNetworkType to maintain the actual network type */
  network_type: TdNetworkType | null = null;
  /** sent_bytes: int53 — Total number of bytes sent */
  sent_bytes: number = 0;
  /** received_bytes: int53 — Total number of bytes received */
  received_bytes: number = 0;
}

/** Decode a TDLib `networkStatisticsEntryFile` object; null input -> null. */
export function decodeNetworkStatisticsEntryFile(json: TdJsonRaw): NetworkStatisticsEntryFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NetworkStatisticsEntryFile = new NetworkStatisticsEntryFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_type = decodeTdFileType(record['file_type']);
  out.network_type = decodeTdNetworkType(record['network_type']);
  out.sent_bytes = asNumber(record['sent_bytes']);
  out.received_bytes = asNumber(record['received_bytes']);
  return out;
}

/** Encode a `networkStatisticsEntryFile` into TDLib JSON wire shape. */
export function encodeNetworkStatisticsEntryFile(value: NetworkStatisticsEntryFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'networkStatisticsEntryFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_type'] = value.file_type === null ? null : encodeTdFileType(value.file_type);
  out['network_type'] = value.network_type === null ? null : encodeTdNetworkType(value.network_type);
  out['sent_bytes'] = value.sent_bytes;
  out['received_bytes'] = value.received_bytes;
  return out;
}

/** networkTypeMobile = NetworkType — A mobile network */
export class NetworkTypeMobile {
  type: 'networkTypeMobile' = 'networkTypeMobile';
  extra?: string;
}

/** Decode a TDLib `networkTypeMobile` object; null input -> null. */
export function decodeNetworkTypeMobile(json: TdJsonRaw): NetworkTypeMobile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NetworkTypeMobile = new NetworkTypeMobile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `networkTypeMobile` into TDLib JSON wire shape. */
export function encodeNetworkTypeMobile(value: NetworkTypeMobile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'networkTypeMobile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** networkTypeMobileRoaming = NetworkType — A mobile roaming network */
export class NetworkTypeMobileRoaming {
  type: 'networkTypeMobileRoaming' = 'networkTypeMobileRoaming';
  extra?: string;
}

/** Decode a TDLib `networkTypeMobileRoaming` object; null input -> null. */
export function decodeNetworkTypeMobileRoaming(json: TdJsonRaw): NetworkTypeMobileRoaming | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NetworkTypeMobileRoaming = new NetworkTypeMobileRoaming();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `networkTypeMobileRoaming` into TDLib JSON wire shape. */
export function encodeNetworkTypeMobileRoaming(value: NetworkTypeMobileRoaming): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'networkTypeMobileRoaming';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** networkTypeNone = NetworkType — The network is not available */
export class NetworkTypeNone {
  type: 'networkTypeNone' = 'networkTypeNone';
  extra?: string;
}

/** Decode a TDLib `networkTypeNone` object; null input -> null. */
export function decodeNetworkTypeNone(json: TdJsonRaw): NetworkTypeNone | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NetworkTypeNone = new NetworkTypeNone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `networkTypeNone` into TDLib JSON wire shape. */
export function encodeNetworkTypeNone(value: NetworkTypeNone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'networkTypeNone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** networkTypeOther = NetworkType — A different network type (e.g., Ethernet network) */
export class NetworkTypeOther {
  type: 'networkTypeOther' = 'networkTypeOther';
  extra?: string;
}

/** Decode a TDLib `networkTypeOther` object; null input -> null. */
export function decodeNetworkTypeOther(json: TdJsonRaw): NetworkTypeOther | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NetworkTypeOther = new NetworkTypeOther();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `networkTypeOther` into TDLib JSON wire shape. */
export function encodeNetworkTypeOther(value: NetworkTypeOther): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'networkTypeOther';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** networkTypeWiFi = NetworkType — A Wi-Fi network */
export class NetworkTypeWiFi {
  type: 'networkTypeWiFi' = 'networkTypeWiFi';
  extra?: string;
}

/** Decode a TDLib `networkTypeWiFi` object; null input -> null. */
export function decodeNetworkTypeWiFi(json: TdJsonRaw): NetworkTypeWiFi | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NetworkTypeWiFi = new NetworkTypeWiFi();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `networkTypeWiFi` into TDLib JSON wire shape. */
export function encodeNetworkTypeWiFi(value: NetworkTypeWiFi): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'networkTypeWiFi';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** newChatPrivacySettings = NewChatPrivacySettings — Contains privacy settings for chats with non-contacts If positive, then allow_new_chats_from_unknown_users must be true. The current user will receive getOption("paid_message_earnings_per... */
export class NewChatPrivacySettings {
  type: 'newChatPrivacySettings' = 'newChatPrivacySettings';
  extra?: string;
  /** allow_new_chats_from_unknown_users: Bool — True, if non-contacts users are able to write first to the current user. Telegram Premium subscribers are able to write first regardless of this setting */
  allow_new_chats_from_unknown_users: boolean = false;
  /** incoming_paid_message_star_count: int53 — Number of Telegram Stars that must be paid for every incoming private message by non-contacts; 0-getOption("paid_message_star_count_max"). */
  incoming_paid_message_star_count: number = 0;
}

/** Decode a TDLib `newChatPrivacySettings` object; null input -> null. */
export function decodeNewChatPrivacySettings(json: TdJsonRaw): NewChatPrivacySettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NewChatPrivacySettings = new NewChatPrivacySettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.allow_new_chats_from_unknown_users = asBool(record['allow_new_chats_from_unknown_users']);
  out.incoming_paid_message_star_count = asNumber(record['incoming_paid_message_star_count']);
  return out;
}

/** Encode a `newChatPrivacySettings` into TDLib JSON wire shape. */
export function encodeNewChatPrivacySettings(value: NewChatPrivacySettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'newChatPrivacySettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['allow_new_chats_from_unknown_users'] = value.allow_new_chats_from_unknown_users;
  out['incoming_paid_message_star_count'] = value.incoming_paid_message_star_count;
  return out;
}

/** newSticker = NewSticker — A sticker to be added to a sticker set See https://core.telegram.org/animated_stickers#technical-requirements for technical requirements */
export class NewSticker {
  type: 'newSticker' = 'newSticker';
  extra?: string;
  /** sticker: InputFile — File with the sticker; must fit in a 512x512 square. For WEBP stickers the file must be in WEBP or PNG format, which will be converted to WEBP server-side. */
  sticker: TdInputFile | null = null;
  /** format: StickerFormat — Format of the sticker */
  format: TdStickerFormat | null = null;
  /** emojis: string — String with 1-20 emoji corresponding to the sticker */
  emojis: string = '';
  /** mask_position: maskPosition — Position where the mask is placed; pass null if not specified */
  mask_position: MaskPosition | null = null;
  /** keywords: vector<string> — List of up to 20 keywords with total length up to 64 characters, which can be used to find the sticker */
  keywords: string[] = [];
}

/** Decode a TDLib `newSticker` object; null input -> null. */
export function decodeNewSticker(json: TdJsonRaw): NewSticker | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NewSticker = new NewSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  out.format = decodeTdStickerFormat(record['format']);
  out.emojis = asString(record['emojis']);
  out.mask_position = decodeMaskPosition(record['mask_position']);
  out.keywords = decodeVector(record['keywords'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `newSticker` into TDLib JSON wire shape. */
export function encodeNewSticker(value: NewSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'newSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  out['format'] = value.format === null ? null : encodeTdStickerFormat(value.format);
  out['emojis'] = value.emojis;
  out['mask_position'] = value.mask_position === null ? null : encodeMaskPosition(value.mask_position);
  out['keywords'] = encodeVector(value.keywords, (item: string): TdJsonRaw => item);
  return out;
}

/** notification = Notification — Contains information about a notification */
export class Notification {
  type: 'notification' = 'notification';
  extra?: string;
  /** id: int32 — Unique persistent identifier of this notification */
  id: number = 0;
  /** date: int32 — Notification date */
  date: number = 0;
  /** is_silent: Bool — True, if the notification was explicitly sent without sound */
  is_silent: boolean = false;
  /** type: NotificationType — Notification type */
  type_: TdNotificationType | null = null;
}

/** Decode a TDLib `notification` object; null input -> null. */
export function decodeNotification(json: TdJsonRaw): Notification | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Notification = new Notification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.date = asNumber(record['date']);
  out.is_silent = asBool(record['is_silent']);
  out.type_ = decodeTdNotificationType(record['type']);
  return out;
}

/** Encode a `notification` into TDLib JSON wire shape. */
export function encodeNotification(value: Notification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['date'] = value.date;
  out['is_silent'] = value.is_silent;
  out['type'] = value.type_ === null ? null : encodeTdNotificationType(value.type_);
  return out;
}

/** notificationGroup = NotificationGroup — Describes a group of notifications */
export class NotificationGroup {
  type: 'notificationGroup' = 'notificationGroup';
  extra?: string;
  /** id: int32 — Unique persistent auto-incremented from 1 identifier of the notification group */
  id: number = 0;
  /** type: NotificationGroupType — Type of the group */
  type_: TdNotificationGroupType | null = null;
  /** chat_id: int53 — Identifier of a chat to which all notifications in the group belong */
  chat_id: number = 0;
  /** total_count: int32 — Total number of active notifications in the group */
  total_count: number = 0;
  /** notifications: vector<notification> — The list of active notifications */
  notifications: Notification[] = [];
}

/** Decode a TDLib `notificationGroup` object; null input -> null. */
export function decodeNotificationGroup(json: TdJsonRaw): NotificationGroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationGroup = new NotificationGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.type_ = decodeTdNotificationGroupType(record['type']);
  out.chat_id = asNumber(record['chat_id']);
  out.total_count = asNumber(record['total_count']);
  out.notifications = decodeVector(record['notifications'], (item: TdJsonRaw): Notification => decodeNotification(item) as Notification);
  return out;
}

/** Encode a `notificationGroup` into TDLib JSON wire shape. */
export function encodeNotificationGroup(value: NotificationGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['type'] = value.type_ === null ? null : encodeTdNotificationGroupType(value.type_);
  out['chat_id'] = value.chat_id;
  out['total_count'] = value.total_count;
  out['notifications'] = encodeVector(value.notifications, (item: Notification | null): TdJsonRaw => item === null ? null : encodeNotification(item));
  return out;
}

/** notificationGroupTypeCalls = NotificationGroupType — A group containing notifications of type notificationTypeNewCall */
export class NotificationGroupTypeCalls {
  type: 'notificationGroupTypeCalls' = 'notificationGroupTypeCalls';
  extra?: string;
}

/** Decode a TDLib `notificationGroupTypeCalls` object; null input -> null. */
export function decodeNotificationGroupTypeCalls(json: TdJsonRaw): NotificationGroupTypeCalls | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationGroupTypeCalls = new NotificationGroupTypeCalls();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `notificationGroupTypeCalls` into TDLib JSON wire shape. */
export function encodeNotificationGroupTypeCalls(value: NotificationGroupTypeCalls): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationGroupTypeCalls';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** notificationGroupTypeMentions = NotificationGroupType — A group containing notifications of type notificationTypeNewMessage and notificationTypeNewPushMessage with unread mentions of the current user, replies to their messages, or a pinn... */
export class NotificationGroupTypeMentions {
  type: 'notificationGroupTypeMentions' = 'notificationGroupTypeMentions';
  extra?: string;
}

/** Decode a TDLib `notificationGroupTypeMentions` object; null input -> null. */
export function decodeNotificationGroupTypeMentions(json: TdJsonRaw): NotificationGroupTypeMentions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationGroupTypeMentions = new NotificationGroupTypeMentions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `notificationGroupTypeMentions` into TDLib JSON wire shape. */
export function encodeNotificationGroupTypeMentions(value: NotificationGroupTypeMentions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationGroupTypeMentions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** notificationGroupTypeMessages = NotificationGroupType — A group containing notifications of type notificationTypeNewMessage and notificationTypeNewPushMessage with ordinary unread messages */
export class NotificationGroupTypeMessages {
  type: 'notificationGroupTypeMessages' = 'notificationGroupTypeMessages';
  extra?: string;
}

/** Decode a TDLib `notificationGroupTypeMessages` object; null input -> null. */
export function decodeNotificationGroupTypeMessages(json: TdJsonRaw): NotificationGroupTypeMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationGroupTypeMessages = new NotificationGroupTypeMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `notificationGroupTypeMessages` into TDLib JSON wire shape. */
export function encodeNotificationGroupTypeMessages(value: NotificationGroupTypeMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationGroupTypeMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** notificationGroupTypeSecretChat = NotificationGroupType — A group containing a notification of type notificationTypeNewSecretChat */
export class NotificationGroupTypeSecretChat {
  type: 'notificationGroupTypeSecretChat' = 'notificationGroupTypeSecretChat';
  extra?: string;
}

/** Decode a TDLib `notificationGroupTypeSecretChat` object; null input -> null. */
export function decodeNotificationGroupTypeSecretChat(json: TdJsonRaw): NotificationGroupTypeSecretChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationGroupTypeSecretChat = new NotificationGroupTypeSecretChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `notificationGroupTypeSecretChat` into TDLib JSON wire shape. */
export function encodeNotificationGroupTypeSecretChat(value: NotificationGroupTypeSecretChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationGroupTypeSecretChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** notificationSettingsScopeChannelChats = NotificationSettingsScope — Notification settings applied to all channel chats when the corresponding chat setting has a default value */
export class NotificationSettingsScopeChannelChats {
  type: 'notificationSettingsScopeChannelChats' = 'notificationSettingsScopeChannelChats';
  extra?: string;
}

/** Decode a TDLib `notificationSettingsScopeChannelChats` object; null input -> null. */
export function decodeNotificationSettingsScopeChannelChats(json: TdJsonRaw): NotificationSettingsScopeChannelChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationSettingsScopeChannelChats = new NotificationSettingsScopeChannelChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `notificationSettingsScopeChannelChats` into TDLib JSON wire shape. */
export function encodeNotificationSettingsScopeChannelChats(value: NotificationSettingsScopeChannelChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationSettingsScopeChannelChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** notificationSettingsScopeGroupChats = NotificationSettingsScope — Notification settings applied to all basic group and supergroup chats when the corresponding chat setting has a default value */
export class NotificationSettingsScopeGroupChats {
  type: 'notificationSettingsScopeGroupChats' = 'notificationSettingsScopeGroupChats';
  extra?: string;
}

/** Decode a TDLib `notificationSettingsScopeGroupChats` object; null input -> null. */
export function decodeNotificationSettingsScopeGroupChats(json: TdJsonRaw): NotificationSettingsScopeGroupChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationSettingsScopeGroupChats = new NotificationSettingsScopeGroupChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `notificationSettingsScopeGroupChats` into TDLib JSON wire shape. */
export function encodeNotificationSettingsScopeGroupChats(value: NotificationSettingsScopeGroupChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationSettingsScopeGroupChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** notificationSettingsScopePrivateChats = NotificationSettingsScope — Notification settings applied to all private and secret chats when the corresponding chat setting has a default value */
export class NotificationSettingsScopePrivateChats {
  type: 'notificationSettingsScopePrivateChats' = 'notificationSettingsScopePrivateChats';
  extra?: string;
}

/** Decode a TDLib `notificationSettingsScopePrivateChats` object; null input -> null. */
export function decodeNotificationSettingsScopePrivateChats(json: TdJsonRaw): NotificationSettingsScopePrivateChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationSettingsScopePrivateChats = new NotificationSettingsScopePrivateChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `notificationSettingsScopePrivateChats` into TDLib JSON wire shape. */
export function encodeNotificationSettingsScopePrivateChats(value: NotificationSettingsScopePrivateChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationSettingsScopePrivateChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** notificationSound = NotificationSound — Describes a notification sound in MP3 format */
export class NotificationSound {
  type: 'notificationSound' = 'notificationSound';
  extra?: string;
  /** id: int64 — Unique identifier of the notification sound */
  id: string = '';
  /** duration: int32 — Duration of the sound, in seconds */
  duration: number = 0;
  /** date: int32 — Point in time (Unix timestamp) when the sound was created */
  date: number = 0;
  /** title: string — Title of the notification sound */
  title: string = '';
  /** data: string — Arbitrary data, defined while the sound was uploaded */
  data: string = '';
  /** sound: file — File containing the sound */
  sound: File | null = null;
}

/** Decode a TDLib `notificationSound` object; null input -> null. */
export function decodeNotificationSound(json: TdJsonRaw): NotificationSound | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationSound = new NotificationSound();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.duration = asNumber(record['duration']);
  out.date = asNumber(record['date']);
  out.title = asString(record['title']);
  out.data = asString(record['data']);
  out.sound = decodeFile(record['sound']);
  return out;
}

/** Encode a `notificationSound` into TDLib JSON wire shape. */
export function encodeNotificationSound(value: NotificationSound): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationSound';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['duration'] = value.duration;
  out['date'] = value.date;
  out['title'] = value.title;
  out['data'] = value.data;
  out['sound'] = value.sound === null ? null : encodeFile(value.sound);
  return out;
}

/** notificationSounds = NotificationSounds — Contains a list of notification sounds */
export class NotificationSounds {
  type: 'notificationSounds' = 'notificationSounds';
  extra?: string;
  /** notification_sounds: vector<notificationSound> — A list of notification sounds */
  notification_sounds: NotificationSound[] = [];
}

/** Decode a TDLib `notificationSounds` object; null input -> null. */
export function decodeNotificationSounds(json: TdJsonRaw): NotificationSounds | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationSounds = new NotificationSounds();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_sounds = decodeVector(record['notification_sounds'], (item: TdJsonRaw): NotificationSound => decodeNotificationSound(item) as NotificationSound);
  return out;
}

/** Encode a `notificationSounds` into TDLib JSON wire shape. */
export function encodeNotificationSounds(value: NotificationSounds): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationSounds';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_sounds'] = encodeVector(value.notification_sounds, (item: NotificationSound | null): TdJsonRaw => item === null ? null : encodeNotificationSound(item));
  return out;
}

/** notificationTypeNewCall = NotificationType — New call was received */
export class NotificationTypeNewCall {
  type: 'notificationTypeNewCall' = 'notificationTypeNewCall';
  extra?: string;
  /** call_id: int32 — Call identifier */
  call_id: number = 0;
}

/** Decode a TDLib `notificationTypeNewCall` object; null input -> null. */
export function decodeNotificationTypeNewCall(json: TdJsonRaw): NotificationTypeNewCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationTypeNewCall = new NotificationTypeNewCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call_id = asNumber(record['call_id']);
  return out;
}

/** Encode a `notificationTypeNewCall` into TDLib JSON wire shape. */
export function encodeNotificationTypeNewCall(value: NotificationTypeNewCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationTypeNewCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call_id'] = value.call_id;
  return out;
}

/** notificationTypeNewMessage = NotificationType — New message was received */
export class NotificationTypeNewMessage {
  type: 'notificationTypeNewMessage' = 'notificationTypeNewMessage';
  extra?: string;
  /** message: message — The message */
  message: Message | null = null;
  /** show_preview: Bool — True, if message content must be displayed in notifications */
  show_preview: boolean = false;
}

/** Decode a TDLib `notificationTypeNewMessage` object; null input -> null. */
export function decodeNotificationTypeNewMessage(json: TdJsonRaw): NotificationTypeNewMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationTypeNewMessage = new NotificationTypeNewMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  out.show_preview = asBool(record['show_preview']);
  return out;
}

/** Encode a `notificationTypeNewMessage` into TDLib JSON wire shape. */
export function encodeNotificationTypeNewMessage(value: NotificationTypeNewMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationTypeNewMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  out['show_preview'] = value.show_preview;
  return out;
}

/** notificationTypeNewPushMessage = NotificationType — New message was received through a push notification */
export class NotificationTypeNewPushMessage {
  type: 'notificationTypeNewPushMessage' = 'notificationTypeNewPushMessage';
  extra?: string;
  /** message_id: int53 — The message identifier. The message will not be available in the chat history, but the identifier can be used in viewMessages, or as a message to be replied in the same chat */
  message_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of the message. Corresponding user or chat may be inaccessible */
  sender_id: TdMessageSender | null = null;
  /** sender_name: string — Name of the sender */
  sender_name: string = '';
  /** is_outgoing: Bool — True, if the message is outgoing */
  is_outgoing: boolean = false;
  /** content: PushMessageContent — Push message content */
  content: TdPushMessageContent | null = null;
}

/** Decode a TDLib `notificationTypeNewPushMessage` object; null input -> null. */
export function decodeNotificationTypeNewPushMessage(json: TdJsonRaw): NotificationTypeNewPushMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationTypeNewPushMessage = new NotificationTypeNewPushMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_id = asNumber(record['message_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.sender_name = asString(record['sender_name']);
  out.is_outgoing = asBool(record['is_outgoing']);
  out.content = decodeTdPushMessageContent(record['content']);
  return out;
}

/** Encode a `notificationTypeNewPushMessage` into TDLib JSON wire shape. */
export function encodeNotificationTypeNewPushMessage(value: NotificationTypeNewPushMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationTypeNewPushMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_id'] = value.message_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['sender_name'] = value.sender_name;
  out['is_outgoing'] = value.is_outgoing;
  out['content'] = value.content === null ? null : encodeTdPushMessageContent(value.content);
  return out;
}

/** notificationTypeNewSecretChat = NotificationType — New secret chat was created */
export class NotificationTypeNewSecretChat {
  type: 'notificationTypeNewSecretChat' = 'notificationTypeNewSecretChat';
  extra?: string;
}

/** Decode a TDLib `notificationTypeNewSecretChat` object; null input -> null. */
export function decodeNotificationTypeNewSecretChat(json: TdJsonRaw): NotificationTypeNewSecretChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: NotificationTypeNewSecretChat = new NotificationTypeNewSecretChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `notificationTypeNewSecretChat` into TDLib JSON wire shape. */
export function encodeNotificationTypeNewSecretChat(value: NotificationTypeNewSecretChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'notificationTypeNewSecretChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}
