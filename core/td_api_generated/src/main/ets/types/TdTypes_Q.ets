/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { TdMessageContent, TdMessageSendingState, TdReplyMarkup, decodeTdMessageContent, decodeTdMessageSendingState, decodeTdReplyMarkup, encodeTdMessageContent, encodeTdMessageSendingState, encodeTdReplyMarkup } from './TdUnions';

/** quickReplyMessage = QuickReplyMessage — Describes a message that can be used for quick reply */
export class QuickReplyMessage {
  type: 'quickReplyMessage' = 'quickReplyMessage';
  extra?: string;
  /** id: int53 — Unique message identifier among all quick replies */
  id: number = 0;
  /** sending_state: MessageSendingState — The sending state of the message; may be null if the message isn't being sent and didn't fail to be sent */
  sending_state: TdMessageSendingState | null = null;
  /** can_be_edited: Bool — True, if the message can be edited */
  can_be_edited: boolean = false;
  /** reply_to_message_id: int53 — The identifier of the quick reply message to which the message replies; 0 if none */
  reply_to_message_id: number = 0;
  /** via_bot_user_id: int53 — If non-zero, the user identifier of the bot through which this message was sent */
  via_bot_user_id: number = 0;
  /** media_album_id: int64 — Unique identifier of an album this message belongs to; 0 if none. Only audios, documents, photos and videos can be grouped together in albums */
  media_album_id: string = '';
  /** content: MessageContent — Content of the message */
  content: TdMessageContent | null = null;
  /** reply_markup: ReplyMarkup — Inline keyboard reply markup for the message; may be null if none */
  reply_markup: TdReplyMarkup | null = null;
}

/** Decode a TDLib `quickReplyMessage` object; null input -> null. */
export function decodeQuickReplyMessage(json: TdJsonRaw): QuickReplyMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: QuickReplyMessage = new QuickReplyMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.sending_state = decodeTdMessageSendingState(record['sending_state']);
  out.can_be_edited = asBool(record['can_be_edited']);
  out.reply_to_message_id = asNumber(record['reply_to_message_id']);
  out.via_bot_user_id = asNumber(record['via_bot_user_id']);
  out.media_album_id = asInt64(record['media_album_id']);
  out.content = decodeTdMessageContent(record['content']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  return out;
}

/** Encode a `quickReplyMessage` into TDLib JSON wire shape. */
export function encodeQuickReplyMessage(value: QuickReplyMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'quickReplyMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['sending_state'] = value.sending_state === null ? null : encodeTdMessageSendingState(value.sending_state);
  out['can_be_edited'] = value.can_be_edited;
  out['reply_to_message_id'] = value.reply_to_message_id;
  out['via_bot_user_id'] = value.via_bot_user_id;
  out['media_album_id'] = value.media_album_id;
  out['content'] = value.content === null ? null : encodeTdMessageContent(value.content);
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  return out;
}

/** quickReplyMessages = QuickReplyMessages — Contains a list of quick reply messages */
export class QuickReplyMessages {
  type: 'quickReplyMessages' = 'quickReplyMessages';
  extra?: string;
  /** messages: vector<quickReplyMessage> — List of quick reply messages; messages may be null */
  messages: QuickReplyMessage[] = [];
}

/** Decode a TDLib `quickReplyMessages` object; null input -> null. */
export function decodeQuickReplyMessages(json: TdJsonRaw): QuickReplyMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: QuickReplyMessages = new QuickReplyMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): QuickReplyMessage => decodeQuickReplyMessage(item) as QuickReplyMessage);
  return out;
}

/** Encode a `quickReplyMessages` into TDLib JSON wire shape. */
export function encodeQuickReplyMessages(value: QuickReplyMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'quickReplyMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['messages'] = encodeVector(value.messages, (item: QuickReplyMessage | null): TdJsonRaw => item === null ? null : encodeQuickReplyMessage(item));
  return out;
}

/** quickReplyShortcut = QuickReplyShortcut — Describes a shortcut that can be used for a quick reply */
export class QuickReplyShortcut {
  type: 'quickReplyShortcut' = 'quickReplyShortcut';
  extra?: string;
  /** id: int32 — Unique shortcut identifier */
  id: number = 0;
  /** name: string — The name of the shortcut that can be used to use the shortcut */
  name: string = '';
  /** first_message: quickReplyMessage — The first shortcut message */
  first_message: QuickReplyMessage | null = null;
  /** message_count: int32 — The total number of messages in the shortcut */
  message_count: number = 0;
}

/** Decode a TDLib `quickReplyShortcut` object; null input -> null. */
export function decodeQuickReplyShortcut(json: TdJsonRaw): QuickReplyShortcut | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: QuickReplyShortcut = new QuickReplyShortcut();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.name = asString(record['name']);
  out.first_message = decodeQuickReplyMessage(record['first_message']);
  out.message_count = asNumber(record['message_count']);
  return out;
}

/** Encode a `quickReplyShortcut` into TDLib JSON wire shape. */
export function encodeQuickReplyShortcut(value: QuickReplyShortcut): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'quickReplyShortcut';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['name'] = value.name;
  out['first_message'] = value.first_message === null ? null : encodeQuickReplyMessage(value.first_message);
  out['message_count'] = value.message_count;
  return out;
}
