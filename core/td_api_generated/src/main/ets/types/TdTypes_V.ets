/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { AdvertisementSponsor, decodeAdvertisementSponsor, encodeAdvertisementSponsor } from './TdTypes_A';
import { File, decodeFile, encodeFile } from './TdTypes_F';
import { Location, decodeLocation, encodeLocation } from './TdTypes_L';
import { Minithumbnail, decodeMinithumbnail, encodeMinithumbnail } from './TdTypes_M';
import { OrderInfo, decodeOrderInfo, encodeOrderInfo } from './TdTypes_O';
import { Point, decodePoint, encodePoint } from './TdTypes_P';
import { ShippingOption, decodeShippingOption, encodeShippingOption } from './TdTypes_S';
import { Thumbnail, decodeThumbnail, encodeThumbnail } from './TdTypes_T';
import { TdInputInvoice, TdMessageSender, TdMessageSource, TdPremiumFeature, TdSpeechRecognitionResult, decodeTdInputInvoice, decodeTdMessageSender, decodeTdMessageSource, decodeTdPremiumFeature, decodeTdSpeechRecognitionResult, encodeTdInputInvoice, encodeTdMessageSender, encodeTdMessageSource, encodeTdPremiumFeature, encodeTdSpeechRecognitionResult } from './TdUnions';

/** validateOrderInfo = ValidatedOrderInfo — Validates the order information provided by a user and returns the available shipping options for a flexible invoice */
export class ValidateOrderInfo {
  type: 'validateOrderInfo' = 'validateOrderInfo';
  extra?: string;
  /** input_invoice: InputInvoice — The invoice */
  input_invoice: TdInputInvoice | null = null;
  /** order_info: orderInfo — The order information, provided by the user; pass null if empty */
  order_info: OrderInfo | null = null;
  /** allow_save: Bool — Pass true to save the order information */
  allow_save: boolean = false;
}

/** Decode a TDLib `validateOrderInfo` object; null input -> null. */
export function decodeValidateOrderInfo(json: TdJsonRaw): ValidateOrderInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ValidateOrderInfo = new ValidateOrderInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.input_invoice = decodeTdInputInvoice(record['input_invoice']);
  out.order_info = decodeOrderInfo(record['order_info']);
  out.allow_save = asBool(record['allow_save']);
  return out;
}

/** Encode a `validateOrderInfo` into TDLib JSON wire shape. */
export function encodeValidateOrderInfo(value: ValidateOrderInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'validateOrderInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['input_invoice'] = value.input_invoice === null ? null : encodeTdInputInvoice(value.input_invoice);
  out['order_info'] = value.order_info === null ? null : encodeOrderInfo(value.order_info);
  out['allow_save'] = value.allow_save;
  return out;
}

/** validatedOrderInfo = ValidatedOrderInfo — Contains a temporary identifier of validated order information, which is stored for one hour, and the available shipping options */
export class ValidatedOrderInfo {
  type: 'validatedOrderInfo' = 'validatedOrderInfo';
  extra?: string;
  /** order_info_id: string — Temporary identifier of the order information */
  order_info_id: string = '';
  /** shipping_options: vector<shippingOption> — Available shipping options */
  shipping_options: ShippingOption[] = [];
}

/** Decode a TDLib `validatedOrderInfo` object; null input -> null. */
export function decodeValidatedOrderInfo(json: TdJsonRaw): ValidatedOrderInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ValidatedOrderInfo = new ValidatedOrderInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.order_info_id = asString(record['order_info_id']);
  out.shipping_options = decodeVector(record['shipping_options'], (item: TdJsonRaw): ShippingOption => decodeShippingOption(item) as ShippingOption);
  return out;
}

/** Encode a `validatedOrderInfo` into TDLib JSON wire shape. */
export function encodeValidatedOrderInfo(value: ValidatedOrderInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'validatedOrderInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['order_info_id'] = value.order_info_id;
  out['shipping_options'] = encodeVector(value.shipping_options, (item: ShippingOption | null): TdJsonRaw => item === null ? null : encodeShippingOption(item));
  return out;
}

/** vectorPathCommandCubicBezierCurve = VectorPathCommand — A cubic Bézier curve to a given point */
export class VectorPathCommandCubicBezierCurve {
  type: 'vectorPathCommandCubicBezierCurve' = 'vectorPathCommandCubicBezierCurve';
  extra?: string;
  /** start_control_point: point — The start control point of the curve */
  start_control_point: Point | null = null;
  /** end_control_point: point — The end control point of the curve */
  end_control_point: Point | null = null;
  /** end_point: point — The end point of the curve */
  end_point: Point | null = null;
}

/** Decode a TDLib `vectorPathCommandCubicBezierCurve` object; null input -> null. */
export function decodeVectorPathCommandCubicBezierCurve(json: TdJsonRaw): VectorPathCommandCubicBezierCurve | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: VectorPathCommandCubicBezierCurve = new VectorPathCommandCubicBezierCurve();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.start_control_point = decodePoint(record['start_control_point']);
  out.end_control_point = decodePoint(record['end_control_point']);
  out.end_point = decodePoint(record['end_point']);
  return out;
}

/** Encode a `vectorPathCommandCubicBezierCurve` into TDLib JSON wire shape. */
export function encodeVectorPathCommandCubicBezierCurve(value: VectorPathCommandCubicBezierCurve): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'vectorPathCommandCubicBezierCurve';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['start_control_point'] = value.start_control_point === null ? null : encodePoint(value.start_control_point);
  out['end_control_point'] = value.end_control_point === null ? null : encodePoint(value.end_control_point);
  out['end_point'] = value.end_point === null ? null : encodePoint(value.end_point);
  return out;
}

/** vectorPathCommandLine = VectorPathCommand — A straight line to a given point */
export class VectorPathCommandLine {
  type: 'vectorPathCommandLine' = 'vectorPathCommandLine';
  extra?: string;
  /** end_point: point — The end point of the straight line */
  end_point: Point | null = null;
}

/** Decode a TDLib `vectorPathCommandLine` object; null input -> null. */
export function decodeVectorPathCommandLine(json: TdJsonRaw): VectorPathCommandLine | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: VectorPathCommandLine = new VectorPathCommandLine();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.end_point = decodePoint(record['end_point']);
  return out;
}

/** Encode a `vectorPathCommandLine` into TDLib JSON wire shape. */
export function encodeVectorPathCommandLine(value: VectorPathCommandLine): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'vectorPathCommandLine';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['end_point'] = value.end_point === null ? null : encodePoint(value.end_point);
  return out;
}

/** venue = Venue — Describes a venue */
export class Venue {
  type: 'venue' = 'venue';
  extra?: string;
  /** location: location — Venue location; as defined by the sender */
  location: Location | null = null;
  /** title: string — Venue name; as defined by the sender */
  title: string = '';
  /** address: string — Venue address; as defined by the sender */
  address: string = '';
  /** provider: string — Provider of the venue database; as defined by the sender. Currently, only "foursquare" and "gplaces" (Google Places) need to be supported */
  provider: string = '';
  /** id: string — Identifier of the venue in the provider database; as defined by the sender */
  id: string = '';
  /** type: string — Type of the venue in the provider database; as defined by the sender */
  type_: string = '';
}

/** Decode a TDLib `venue` object; null input -> null. */
export function decodeVenue(json: TdJsonRaw): Venue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Venue = new Venue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  out.title = asString(record['title']);
  out.address = asString(record['address']);
  out.provider = asString(record['provider']);
  out.id = asString(record['id']);
  out.type_ = asString(record['type']);
  return out;
}

/** Encode a `venue` into TDLib JSON wire shape. */
export function encodeVenue(value: Venue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'venue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['title'] = value.title;
  out['address'] = value.address;
  out['provider'] = value.provider;
  out['id'] = value.id;
  out['type'] = value.type_;
  return out;
}

/** verificationStatus = VerificationStatus — Contains information about verification status of a chat or a user */
export class VerificationStatus {
  type: 'verificationStatus' = 'verificationStatus';
  extra?: string;
  /** is_verified: Bool — True, if the chat or the user is verified by Telegram */
  is_verified: boolean = false;
  /** is_scam: Bool — True, if the chat or the user is marked as scam by Telegram */
  is_scam: boolean = false;
  /** is_fake: Bool — True, if the chat or the user is marked as fake by Telegram */
  is_fake: boolean = false;
  /** bot_verification_icon_custom_emoji_id: int64 — Identifier of the custom emoji to be shown as verification sign provided by a bot for the user; 0 if none */
  bot_verification_icon_custom_emoji_id: string = '';
}

/** Decode a TDLib `verificationStatus` object; null input -> null. */
export function decodeVerificationStatus(json: TdJsonRaw): VerificationStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: VerificationStatus = new VerificationStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_verified = asBool(record['is_verified']);
  out.is_scam = asBool(record['is_scam']);
  out.is_fake = asBool(record['is_fake']);
  out.bot_verification_icon_custom_emoji_id = asInt64(record['bot_verification_icon_custom_emoji_id']);
  return out;
}

/** Encode a `verificationStatus` into TDLib JSON wire shape. */
export function encodeVerificationStatus(value: VerificationStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'verificationStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_verified'] = value.is_verified;
  out['is_scam'] = value.is_scam;
  out['is_fake'] = value.is_fake;
  out['bot_verification_icon_custom_emoji_id'] = value.bot_verification_icon_custom_emoji_id;
  return out;
}

/** video = Video — Describes a video file */
export class Video {
  type: 'video' = 'video';
  extra?: string;
  /** duration: int32 — Duration of the video, in seconds; as defined by the sender */
  duration: number = 0;
  /** width: int32 — Video width; as defined by the sender */
  width: number = 0;
  /** height: int32 — Video height; as defined by the sender */
  height: number = 0;
  /** file_name: string — Original name of the file; as defined by the sender */
  file_name: string = '';
  /** mime_type: string — MIME type of the file; as defined by the sender */
  mime_type: string = '';
  /** has_stickers: Bool — True, if stickers were added to the video. The list of corresponding sticker sets can be received using getAttachedStickerSets */
  has_stickers: boolean = false;
  /** supports_streaming: Bool — True, if the video is expected to be streamed */
  supports_streaming: boolean = false;
  /** minithumbnail: minithumbnail — Video minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
  /** thumbnail: thumbnail — Video thumbnail in JPEG or MPEG4 format; as defined by the sender; may be null */
  thumbnail: Thumbnail | null = null;
  /** video: file — File containing the video */
  video: File | null = null;
}

/** Decode a TDLib `video` object; null input -> null. */
export function decodeVideo(json: TdJsonRaw): Video | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Video = new Video();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.duration = asNumber(record['duration']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.file_name = asString(record['file_name']);
  out.mime_type = asString(record['mime_type']);
  out.has_stickers = asBool(record['has_stickers']);
  out.supports_streaming = asBool(record['supports_streaming']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  out.video = decodeFile(record['video']);
  return out;
}

/** Encode a `video` into TDLib JSON wire shape. */
export function encodeVideo(value: Video): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'video';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['duration'] = value.duration;
  out['width'] = value.width;
  out['height'] = value.height;
  out['file_name'] = value.file_name;
  out['mime_type'] = value.mime_type;
  out['has_stickers'] = value.has_stickers;
  out['supports_streaming'] = value.supports_streaming;
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  out['video'] = value.video === null ? null : encodeFile(value.video);
  return out;
}

/** videoChat = VideoChat — Describes a video chat, i.e. a group call bound to a chat */
export class VideoChat {
  type: 'videoChat' = 'videoChat';
  extra?: string;
  /** group_call_id: int32 — Group call identifier of an active video chat; 0 if none. Full information about the video chat can be received through the method getGroupCall */
  group_call_id: number = 0;
  /** has_participants: Bool — True, if the video chat has participants */
  has_participants: boolean = false;
  /** default_participant_id: MessageSender — Default group call participant identifier to join the video chat; may be null */
  default_participant_id: TdMessageSender | null = null;
}

/** Decode a TDLib `videoChat` object; null input -> null. */
export function decodeVideoChat(json: TdJsonRaw): VideoChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: VideoChat = new VideoChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.has_participants = asBool(record['has_participants']);
  out.default_participant_id = decodeTdMessageSender(record['default_participant_id']);
  return out;
}

/** Encode a `videoChat` into TDLib JSON wire shape. */
export function encodeVideoChat(value: VideoChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'videoChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['has_participants'] = value.has_participants;
  out['default_participant_id'] = value.default_participant_id === null ? null : encodeTdMessageSender(value.default_participant_id);
  return out;
}

/** videoMessageAdvertisement = VideoMessageAdvertisement — Describes an advertisement to be shown while a video from a message is watched */
export class VideoMessageAdvertisement {
  type: 'videoMessageAdvertisement' = 'videoMessageAdvertisement';
  extra?: string;
  /** unique_id: int53 — Unique identifier of this result */
  unique_id: number = 0;
  /** text: string — Text of the advertisement */
  text: string = '';
  /** min_display_duration: int32 — The minimum amount of time the advertisement must be displayed before it can be hidden by the user, in seconds */
  min_display_duration: number = 0;
  /** max_display_duration: int32 — The maximum amount of time the advertisement must be displayed before it must be automatically hidden, in seconds */
  max_display_duration: number = 0;
  /** can_be_reported: Bool — True, if the advertisement can be reported to Telegram moderators through reportVideoMessageAdvertisement */
  can_be_reported: boolean = false;
  /** sponsor: advertisementSponsor — Information about the sponsor of the advertisement */
  sponsor: AdvertisementSponsor | null = null;
  /** title: string — Title of the sponsored message */
  title: string = '';
  /** additional_info: string — If non-empty, additional information about the sponsored message to be shown along with the message */
  additional_info: string = '';
}

/** Decode a TDLib `videoMessageAdvertisement` object; null input -> null. */
export function decodeVideoMessageAdvertisement(json: TdJsonRaw): VideoMessageAdvertisement | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: VideoMessageAdvertisement = new VideoMessageAdvertisement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.unique_id = asNumber(record['unique_id']);
  out.text = asString(record['text']);
  out.min_display_duration = asNumber(record['min_display_duration']);
  out.max_display_duration = asNumber(record['max_display_duration']);
  out.can_be_reported = asBool(record['can_be_reported']);
  out.sponsor = decodeAdvertisementSponsor(record['sponsor']);
  out.title = asString(record['title']);
  out.additional_info = asString(record['additional_info']);
  return out;
}

/** Encode a `videoMessageAdvertisement` into TDLib JSON wire shape. */
export function encodeVideoMessageAdvertisement(value: VideoMessageAdvertisement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'videoMessageAdvertisement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['unique_id'] = value.unique_id;
  out['text'] = value.text;
  out['min_display_duration'] = value.min_display_duration;
  out['max_display_duration'] = value.max_display_duration;
  out['can_be_reported'] = value.can_be_reported;
  out['sponsor'] = value.sponsor === null ? null : encodeAdvertisementSponsor(value.sponsor);
  out['title'] = value.title;
  out['additional_info'] = value.additional_info;
  return out;
}

/** videoMessageAdvertisements = VideoMessageAdvertisements — Contains a list of advertisements to be shown while a video from a message is watched */
export class VideoMessageAdvertisements {
  type: 'videoMessageAdvertisements' = 'videoMessageAdvertisements';
  extra?: string;
  /** advertisements: vector<videoMessageAdvertisement> — List of advertisements */
  advertisements: VideoMessageAdvertisement[] = [];
  /** start_delay: int32 — Delay before the first advertisement is shown, in seconds */
  start_delay: number = 0;
  /** between_delay: int32 — Delay between consecutive advertisements, in seconds */
  between_delay: number = 0;
}

/** Decode a TDLib `videoMessageAdvertisements` object; null input -> null. */
export function decodeVideoMessageAdvertisements(json: TdJsonRaw): VideoMessageAdvertisements | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: VideoMessageAdvertisements = new VideoMessageAdvertisements();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.advertisements = decodeVector(record['advertisements'], (item: TdJsonRaw): VideoMessageAdvertisement => decodeVideoMessageAdvertisement(item) as VideoMessageAdvertisement);
  out.start_delay = asNumber(record['start_delay']);
  out.between_delay = asNumber(record['between_delay']);
  return out;
}

/** Encode a `videoMessageAdvertisements` into TDLib JSON wire shape. */
export function encodeVideoMessageAdvertisements(value: VideoMessageAdvertisements): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'videoMessageAdvertisements';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['advertisements'] = encodeVector(value.advertisements, (item: VideoMessageAdvertisement | null): TdJsonRaw => item === null ? null : encodeVideoMessageAdvertisement(item));
  out['start_delay'] = value.start_delay;
  out['between_delay'] = value.between_delay;
  return out;
}

/** videoNote = VideoNote — Describes a video note. The video must be equal in width and height, cropped to a circle, and stored in MPEG4 format */
export class VideoNote {
  type: 'videoNote' = 'videoNote';
  extra?: string;
  /** duration: int32 — Duration of the video, in seconds; as defined by the sender */
  duration: number = 0;
  /** waveform: bytes — A waveform representation of the video note's audio in 5-bit format; may be empty if unknown */
  waveform: string = '';
  /** length: int32 — Video width and height; as defined by the sender */
  length: number = 0;
  /** minithumbnail: minithumbnail — Video minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
  /** thumbnail: thumbnail — Video thumbnail in JPEG format; as defined by the sender; may be null */
  thumbnail: Thumbnail | null = null;
  /** speech_recognition_result: SpeechRecognitionResult — Result of speech recognition in the video note; may be null */
  speech_recognition_result: TdSpeechRecognitionResult | null = null;
  /** video: file — File containing the video */
  video: File | null = null;
}

/** Decode a TDLib `videoNote` object; null input -> null. */
export function decodeVideoNote(json: TdJsonRaw): VideoNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: VideoNote = new VideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.duration = asNumber(record['duration']);
  out.waveform = asString(record['waveform']);
  out.length = asNumber(record['length']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  out.speech_recognition_result = decodeTdSpeechRecognitionResult(record['speech_recognition_result']);
  out.video = decodeFile(record['video']);
  return out;
}

/** Encode a `videoNote` into TDLib JSON wire shape. */
export function encodeVideoNote(value: VideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'videoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['duration'] = value.duration;
  out['waveform'] = value.waveform;
  out['length'] = value.length;
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  out['speech_recognition_result'] = value.speech_recognition_result === null ? null : encodeTdSpeechRecognitionResult(value.speech_recognition_result);
  out['video'] = value.video === null ? null : encodeFile(value.video);
  return out;
}

/** videoStoryboard = VideoStoryboard — Describes a storyboard for a video */
export class VideoStoryboard {
  type: 'videoStoryboard' = 'videoStoryboard';
  extra?: string;
  /** storyboard_file: file — A JPEG file that contains tiled previews of video */
  storyboard_file: File | null = null;
  /** width: int32 — Width of a tile */
  width: number = 0;
  /** height: int32 — Height of a tile */
  height: number = 0;
  /** map_file: file — File that describes mapping of position in the video to a tile in the JPEG file */
  map_file: File | null = null;
}

/** Decode a TDLib `videoStoryboard` object; null input -> null. */
export function decodeVideoStoryboard(json: TdJsonRaw): VideoStoryboard | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: VideoStoryboard = new VideoStoryboard();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.storyboard_file = decodeFile(record['storyboard_file']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.map_file = decodeFile(record['map_file']);
  return out;
}

/** Encode a `videoStoryboard` into TDLib JSON wire shape. */
export function encodeVideoStoryboard(value: VideoStoryboard): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'videoStoryboard';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['storyboard_file'] = value.storyboard_file === null ? null : encodeFile(value.storyboard_file);
  out['width'] = value.width;
  out['height'] = value.height;
  out['map_file'] = value.map_file === null ? null : encodeFile(value.map_file);
  return out;
}

/** viewMessages = Ok — Informs TDLib that messages are being viewed by the user. Sponsored messages must be marked as viewed only when the entire text of the message is shown on the screen (excluding the button). Many useful activities depe... */
export class ViewMessages {
  type: 'viewMessages' = 'viewMessages';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_ids: vector<int53> — The identifiers of the messages being viewed */
  message_ids: number[] = [];
  /** source: MessageSource — Source of the message view; pass null to guess the source based on chat open state */
  source: TdMessageSource | null = null;
  /** force_read: Bool — Pass true to mark as read the specified messages even if the chat is closed */
  force_read: boolean = false;
}

/** Decode a TDLib `viewMessages` object; null input -> null. */
export function decodeViewMessages(json: TdJsonRaw): ViewMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ViewMessages = new ViewMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.source = decodeTdMessageSource(record['source']);
  out.force_read = asBool(record['force_read']);
  return out;
}

/** Encode a `viewMessages` into TDLib JSON wire shape. */
export function encodeViewMessages(value: ViewMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'viewMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  out['source'] = value.source === null ? null : encodeTdMessageSource(value.source);
  out['force_read'] = value.force_read;
  return out;
}

/** viewPremiumFeature = Ok — Informs TDLib that the user viewed detailed information about a Premium feature on the Premium features screen */
export class ViewPremiumFeature {
  type: 'viewPremiumFeature' = 'viewPremiumFeature';
  extra?: string;
  /** feature: PremiumFeature — The viewed premium feature */
  feature: TdPremiumFeature | null = null;
}

/** Decode a TDLib `viewPremiumFeature` object; null input -> null. */
export function decodeViewPremiumFeature(json: TdJsonRaw): ViewPremiumFeature | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ViewPremiumFeature = new ViewPremiumFeature();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.feature = decodeTdPremiumFeature(record['feature']);
  return out;
}

/** Encode a `viewPremiumFeature` into TDLib JSON wire shape. */
export function encodeViewPremiumFeature(value: ViewPremiumFeature): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'viewPremiumFeature';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['feature'] = value.feature === null ? null : encodeTdPremiumFeature(value.feature);
  return out;
}

/** viewSponsoredChat = Ok — Informs TDLib that the user fully viewed a sponsored chat */
export class ViewSponsoredChat {
  type: 'viewSponsoredChat' = 'viewSponsoredChat';
  extra?: string;
  /** sponsored_chat_unique_id: int53 — Unique identifier of the sponsored chat */
  sponsored_chat_unique_id: number = 0;
}

/** Decode a TDLib `viewSponsoredChat` object; null input -> null. */
export function decodeViewSponsoredChat(json: TdJsonRaw): ViewSponsoredChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ViewSponsoredChat = new ViewSponsoredChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sponsored_chat_unique_id = asNumber(record['sponsored_chat_unique_id']);
  return out;
}

/** Encode a `viewSponsoredChat` into TDLib JSON wire shape. */
export function encodeViewSponsoredChat(value: ViewSponsoredChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'viewSponsoredChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sponsored_chat_unique_id'] = value.sponsored_chat_unique_id;
  return out;
}

/** viewTrendingStickerSets = Ok — Informs the server that some trending sticker sets have been viewed by the user */
export class ViewTrendingStickerSets {
  type: 'viewTrendingStickerSets' = 'viewTrendingStickerSets';
  extra?: string;
  /** sticker_set_ids: vector<int64> — Identifiers of viewed trending sticker sets */
  sticker_set_ids: string[] = [];
}

/** Decode a TDLib `viewTrendingStickerSets` object; null input -> null. */
export function decodeViewTrendingStickerSets(json: TdJsonRaw): ViewTrendingStickerSets | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ViewTrendingStickerSets = new ViewTrendingStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_set_ids = decodeVector(record['sticker_set_ids'], (item: TdJsonRaw): string => asInt64(item));
  return out;
}

/** Encode a `viewTrendingStickerSets` into TDLib JSON wire shape. */
export function encodeViewTrendingStickerSets(value: ViewTrendingStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'viewTrendingStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_set_ids'] = encodeVector(value.sticker_set_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** viewVideoMessageAdvertisement = Ok — Informs TDLib that the user viewed a video message advertisement */
export class ViewVideoMessageAdvertisement {
  type: 'viewVideoMessageAdvertisement' = 'viewVideoMessageAdvertisement';
  extra?: string;
  /** advertisement_unique_id: int53 — Unique identifier of the advertisement */
  advertisement_unique_id: number = 0;
}

/** Decode a TDLib `viewVideoMessageAdvertisement` object; null input -> null. */
export function decodeViewVideoMessageAdvertisement(json: TdJsonRaw): ViewVideoMessageAdvertisement | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ViewVideoMessageAdvertisement = new ViewVideoMessageAdvertisement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.advertisement_unique_id = asNumber(record['advertisement_unique_id']);
  return out;
}

/** Encode a `viewVideoMessageAdvertisement` into TDLib JSON wire shape. */
export function encodeViewVideoMessageAdvertisement(value: ViewVideoMessageAdvertisement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'viewVideoMessageAdvertisement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['advertisement_unique_id'] = value.advertisement_unique_id;
  return out;
}

/** voiceNote = VoiceNote — Describes a voice note */
export class VoiceNote {
  type: 'voiceNote' = 'voiceNote';
  extra?: string;
  /** duration: int32 — Duration of the voice note, in seconds; as defined by the sender */
  duration: number = 0;
  /** waveform: bytes — A waveform representation of the voice note in 5-bit format */
  waveform: string = '';
  /** mime_type: string — MIME type of the file; as defined by the sender. Usually, one of "audio/ogg" for Opus in an OGG container, "audio/mpeg" for an MP3 audio, or "audio/mp4" for an M4A audio */
  mime_type: string = '';
  /** speech_recognition_result: SpeechRecognitionResult — Result of speech recognition in the voice note; may be null */
  speech_recognition_result: TdSpeechRecognitionResult | null = null;
  /** voice: file — File containing the voice note */
  voice: File | null = null;
}

/** Decode a TDLib `voiceNote` object; null input -> null. */
export function decodeVoiceNote(json: TdJsonRaw): VoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: VoiceNote = new VoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.duration = asNumber(record['duration']);
  out.waveform = asString(record['waveform']);
  out.mime_type = asString(record['mime_type']);
  out.speech_recognition_result = decodeTdSpeechRecognitionResult(record['speech_recognition_result']);
  out.voice = decodeFile(record['voice']);
  return out;
}

/** Encode a `voiceNote` into TDLib JSON wire shape. */
export function encodeVoiceNote(value: VoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'voiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['duration'] = value.duration;
  out['waveform'] = value.waveform;
  out['mime_type'] = value.mime_type;
  out['speech_recognition_result'] = value.speech_recognition_result === null ? null : encodeTdSpeechRecognitionResult(value.speech_recognition_result);
  out['voice'] = value.voice === null ? null : encodeFile(value.voice);
  return out;
}
