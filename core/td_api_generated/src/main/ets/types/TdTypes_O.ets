/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { Address, decodeAddress, encodeAddress } from './TdTypes_A';
import { ClosedVectorPath, decodeClosedVectorPath, encodeClosedVectorPath } from './TdTypes_C';
import { WebAppOpenParameters, decodeWebAppOpenParameters, encodeWebAppOpenParameters } from './TdTypes_W';
import { TdFileType, decodeTdFileType, encodeTdFileType } from './TdUnions_F';
import { TdInputMessageReplyTo, decodeTdInputMessageReplyTo, encodeTdInputMessageReplyTo } from './TdUnions_I';
import { TdMessageTopic, decodeTdMessageTopic, encodeTdMessageTopic } from './TdUnions_M';

/** oauthLinkInfo = OauthLinkInfo — Information about the OAuth authorization If the user is logged in the application, then they must be chosen for authorization by default */
export class OauthLinkInfo {
  type: 'oauthLinkInfo' = 'oauthLinkInfo';
  extra?: string;
  /** user_id: int53 — Identifier of the user for which the link was generated; may be 0 if unknown. The corresponding user may be unknown. */
  user_id: number = 0;
  /** url: string — An HTTP URL where the user authorizes */
  url: string = '';
  /** domain: string — A domain of the URL */
  domain: string = '';
  /** from_app: Bool — True, if the authorization originates from an application */
  from_app: boolean = false;
  /** verified_app_name: string — Verified name of the application; if empty, then "Unverified App" must be shown instead */
  verified_app_name: string = '';
  /** bot_user_id: int53 — User identifier of a bot linked with the website */
  bot_user_id: number = 0;
  /** request_write_access: Bool — True, if the user must be asked for the permission to the bot to send them messages */
  request_write_access: boolean = false;
  /** request_phone_number_access: Bool — True, if the user must be asked for the permission to share their phone number */
  request_phone_number_access: boolean = false;
  /** browser: string — The version of a browser used for the authorization */
  browser: string = '';
  /** platform: string — Operating system the browser is running on */
  platform: string = '';
  /** ip_address: string — IP address from which the authorization is performed, in human-readable format */
  ip_address: string = '';
  /** location: string — Human-readable description of a country and a region from which the authorization is performed, based on the IP address */
  location: string = '';
  /** match_code_first: Bool — True, if code matching dialog must be shown first and checkOauthRequestMatchCode must be called before acceptOauthRequest. Otherwise, checkOauthRequestMatchCode must not be called */
  match_code_first: boolean = false;
  /** match_codes: vector<string> — The list of codes to match; may be empty if irrelevant */
  match_codes: string[] = [];
}

/** Decode a TDLib `oauthLinkInfo` object; null input -> null. */
export function decodeOauthLinkInfo(json: TdJsonRaw): OauthLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OauthLinkInfo = new OauthLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.url = asString(record['url']);
  out.domain = asString(record['domain']);
  out.from_app = asBool(record['from_app']);
  out.verified_app_name = asString(record['verified_app_name']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.request_write_access = asBool(record['request_write_access']);
  out.request_phone_number_access = asBool(record['request_phone_number_access']);
  out.browser = asString(record['browser']);
  out.platform = asString(record['platform']);
  out.ip_address = asString(record['ip_address']);
  out.location = asString(record['location']);
  out.match_code_first = asBool(record['match_code_first']);
  out.match_codes = decodeVector(record['match_codes'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `oauthLinkInfo` into TDLib JSON wire shape. */
export function encodeOauthLinkInfo(value: OauthLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'oauthLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['url'] = value.url;
  out['domain'] = value.domain;
  out['from_app'] = value.from_app;
  out['verified_app_name'] = value.verified_app_name;
  out['bot_user_id'] = value.bot_user_id;
  out['request_write_access'] = value.request_write_access;
  out['request_phone_number_access'] = value.request_phone_number_access;
  out['browser'] = value.browser;
  out['platform'] = value.platform;
  out['ip_address'] = value.ip_address;
  out['location'] = value.location;
  out['match_code_first'] = value.match_code_first;
  out['match_codes'] = encodeVector(value.match_codes, (item: string): TdJsonRaw => item);
  return out;
}

/** ok = Ok — An object of this type is returned on a successful function call for certain functions */
export class Ok {
  type: 'ok' = 'ok';
  extra?: string;
}

/** Decode a TDLib `ok` object; null input -> null. */
export function decodeOk(json: TdJsonRaw): Ok | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Ok = new Ok();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `ok` into TDLib JSON wire shape. */
export function encodeOk(value: Ok): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'ok';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** openBotSimilarBot = Ok — Informs TDLib that a bot was opened from the list of similar bots */
export class OpenBotSimilarBot {
  type: 'openBotSimilarBot' = 'openBotSimilarBot';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the original bot, which similar bots were requested */
  bot_user_id: number = 0;
  /** opened_bot_user_id: int53 — Identifier of the opened bot */
  opened_bot_user_id: number = 0;
}

/** Decode a TDLib `openBotSimilarBot` object; null input -> null. */
export function decodeOpenBotSimilarBot(json: TdJsonRaw): OpenBotSimilarBot | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OpenBotSimilarBot = new OpenBotSimilarBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.opened_bot_user_id = asNumber(record['opened_bot_user_id']);
  return out;
}

/** Encode a `openBotSimilarBot` into TDLib JSON wire shape. */
export function encodeOpenBotSimilarBot(value: OpenBotSimilarBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'openBotSimilarBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['opened_bot_user_id'] = value.opened_bot_user_id;
  return out;
}

/** openChat = Ok — Informs TDLib that the chat is opened by the user. Many useful activities depend on the chat being opened or closed (e.g., in supergroups and channels all updates are received only for opened chats) */
export class OpenChat {
  type: 'openChat' = 'openChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `openChat` object; null input -> null. */
export function decodeOpenChat(json: TdJsonRaw): OpenChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OpenChat = new OpenChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `openChat` into TDLib JSON wire shape. */
export function encodeOpenChat(value: OpenChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'openChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** openChatSimilarChat = Ok — Informs TDLib that a chat was opened from the list of similar chats. The method is independent of openChat and closeChat methods */
export class OpenChatSimilarChat {
  type: 'openChatSimilarChat' = 'openChatSimilarChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the original chat, which similar chats were requested */
  chat_id: number = 0;
  /** opened_chat_id: int53 — Identifier of the opened chat */
  opened_chat_id: number = 0;
}

/** Decode a TDLib `openChatSimilarChat` object; null input -> null. */
export function decodeOpenChatSimilarChat(json: TdJsonRaw): OpenChatSimilarChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OpenChatSimilarChat = new OpenChatSimilarChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.opened_chat_id = asNumber(record['opened_chat_id']);
  return out;
}

/** Encode a `openChatSimilarChat` into TDLib JSON wire shape. */
export function encodeOpenChatSimilarChat(value: OpenChatSimilarChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'openChatSimilarChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['opened_chat_id'] = value.opened_chat_id;
  return out;
}

/** openGiftAuction = Ok — Informs TDLib that a gift auction was opened by the user */
export class OpenGiftAuction {
  type: 'openGiftAuction' = 'openGiftAuction';
  extra?: string;
  /** gift_id: int64 — Identifier of the gift, which auction was opened */
  gift_id: string = '';
}

/** Decode a TDLib `openGiftAuction` object; null input -> null. */
export function decodeOpenGiftAuction(json: TdJsonRaw): OpenGiftAuction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OpenGiftAuction = new OpenGiftAuction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_id = asInt64(record['gift_id']);
  return out;
}

/** Encode a `openGiftAuction` into TDLib JSON wire shape. */
export function encodeOpenGiftAuction(value: OpenGiftAuction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'openGiftAuction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_id'] = value.gift_id;
  return out;
}

/** openMessageContent = Ok — Informs TDLib that the message content has been opened (e.g., the user has opened a photo, video, document, location or venue, or has listened to an audio file or voice note message). An updateMessageContentOpen... */
export class OpenMessageContent {
  type: 'openMessageContent' = 'openMessageContent';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message with the opened content */
  message_id: number = 0;
}

/** Decode a TDLib `openMessageContent` object; null input -> null. */
export function decodeOpenMessageContent(json: TdJsonRaw): OpenMessageContent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OpenMessageContent = new OpenMessageContent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `openMessageContent` into TDLib JSON wire shape. */
export function encodeOpenMessageContent(value: OpenMessageContent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'openMessageContent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** openSponsoredChat = Ok — Informs TDLib that the user opened a sponsored chat */
export class OpenSponsoredChat {
  type: 'openSponsoredChat' = 'openSponsoredChat';
  extra?: string;
  /** sponsored_chat_unique_id: int53 — Unique identifier of the sponsored chat */
  sponsored_chat_unique_id: number = 0;
}

/** Decode a TDLib `openSponsoredChat` object; null input -> null. */
export function decodeOpenSponsoredChat(json: TdJsonRaw): OpenSponsoredChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OpenSponsoredChat = new OpenSponsoredChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sponsored_chat_unique_id = asNumber(record['sponsored_chat_unique_id']);
  return out;
}

/** Encode a `openSponsoredChat` into TDLib JSON wire shape. */
export function encodeOpenSponsoredChat(value: OpenSponsoredChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'openSponsoredChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sponsored_chat_unique_id'] = value.sponsored_chat_unique_id;
  return out;
}

/** openStory = Ok — Informs TDLib that a story is opened and is being viewed by the user */
export class OpenStory {
  type: 'openStory' = 'openStory';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the chat that posted the opened story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — The identifier of the story */
  story_id: number = 0;
}

/** Decode a TDLib `openStory` object; null input -> null. */
export function decodeOpenStory(json: TdJsonRaw): OpenStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OpenStory = new OpenStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `openStory` into TDLib JSON wire shape. */
export function encodeOpenStory(value: OpenStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'openStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** openWebApp = WebAppInfo — Informs TDLib that a Web App is being opened from the attachment menu, a botMenuButton button, an internalLinkTypeAttachmentMenuBot link, or an inlineKeyboardButtonTypeWebApp button. For each bot, a confirmation... */
export class OpenWebApp {
  type: 'openWebApp' = 'openWebApp';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in which the Web App is opened. The Web App can't be opened in secret chats */
  chat_id: number = 0;
  /** bot_user_id: int53 — Identifier of the bot, providing the Web App. If the bot is restricted for the current user, then show an error instead of calling the method */
  bot_user_id: number = 0;
  /** url: string — The URL from an inlineKeyboardButtonTypeWebApp button, a botMenuButton button, an internalLinkTypeAttachmentMenuBot link, or an empty string otherwise */
  url: string = '';
  /** topic_id: MessageTopic — Topic in which the message will be sent; pass null if none */
  topic_id: TdMessageTopic | null = null;
  /** reply_to: InputMessageReplyTo — Information about the message or story to be replied in the message sent by the Web App; pass null if none */
  reply_to: TdInputMessageReplyTo | null = null;
  /** parameters: webAppOpenParameters — Parameters to use to open the Web App */
  parameters: WebAppOpenParameters | null = null;
}

/** Decode a TDLib `openWebApp` object; null input -> null. */
export function decodeOpenWebApp(json: TdJsonRaw): OpenWebApp | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OpenWebApp = new OpenWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.url = asString(record['url']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.reply_to = decodeTdInputMessageReplyTo(record['reply_to']);
  out.parameters = decodeWebAppOpenParameters(record['parameters']);
  return out;
}

/** Encode a `openWebApp` into TDLib JSON wire shape. */
export function encodeOpenWebApp(value: OpenWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'openWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['bot_user_id'] = value.bot_user_id;
  out['url'] = value.url;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['reply_to'] = value.reply_to === null ? null : encodeTdInputMessageReplyTo(value.reply_to);
  out['parameters'] = value.parameters === null ? null : encodeWebAppOpenParameters(value.parameters);
  return out;
}

/** optimizeStorage = StorageStatistics — Optimizes storage usage, i.e. deletes some files and returns new storage usage statistics. Secret thumbnails can't be deleted */
export class OptimizeStorage {
  type: 'optimizeStorage' = 'optimizeStorage';
  extra?: string;
  /** size: int53 — Limit on the total size of files after deletion, in bytes. Pass -1 to use the default limit */
  size: number = 0;
  /** ttl: int32 — Limit on the time that has passed since the last time a file was accessed (or creation time for some filesystems). Pass -1 to use the default limit */
  ttl: number = 0;
  /** count: int32 — Limit on the total number of files after deletion. Pass -1 to use the default limit */
  count: number = 0;
  /** immunity_delay: int32 — The amount of time after the creation of a file during which it can't be deleted, in seconds. Pass -1 to use the default value */
  immunity_delay: number = 0;
  /** file_types: vector<FileType> — If non-empty, only files with the given types are considered. By default, all types except thumbnails, profile photos, stickers and wallpapers are deleted */
  file_types: TdFileType[] = [];
  /** chat_ids: vector<int53> — If non-empty, only files from the given chats are considered. Use 0 as chat identifier to delete files not belonging to any chat (e.g., profile photos) */
  chat_ids: number[] = [];
  /** exclude_chat_ids: vector<int53> — If non-empty, files from the given chats are excluded. Use 0 as chat identifier to exclude all files not belonging to any chat (e.g., profile photos) */
  exclude_chat_ids: number[] = [];
  /** return_deleted_file_statistics: Bool — Pass true if statistics about the files that were deleted must be returned instead of the whole storage usage statistics. Affects only returned statistics */
  return_deleted_file_statistics: boolean = false;
  /** chat_limit: int32 — Same as in getStorageStatistics. Affects only returned statistics */
  chat_limit: number = 0;
}

/** Decode a TDLib `optimizeStorage` object; null input -> null. */
export function decodeOptimizeStorage(json: TdJsonRaw): OptimizeStorage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OptimizeStorage = new OptimizeStorage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.size = asNumber(record['size']);
  out.ttl = asNumber(record['ttl']);
  out.count = asNumber(record['count']);
  out.immunity_delay = asNumber(record['immunity_delay']);
  out.file_types = decodeVector(record['file_types'], (item: TdJsonRaw): TdFileType => decodeTdFileType(item) as TdFileType);
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.exclude_chat_ids = decodeVector(record['exclude_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.return_deleted_file_statistics = asBool(record['return_deleted_file_statistics']);
  out.chat_limit = asNumber(record['chat_limit']);
  return out;
}

/** Encode a `optimizeStorage` into TDLib JSON wire shape. */
export function encodeOptimizeStorage(value: OptimizeStorage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'optimizeStorage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['size'] = value.size;
  out['ttl'] = value.ttl;
  out['count'] = value.count;
  out['immunity_delay'] = value.immunity_delay;
  out['file_types'] = encodeVector(value.file_types, (item: TdFileType | null): TdJsonRaw => item === null ? null : encodeTdFileType(item));
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  out['exclude_chat_ids'] = encodeVector(value.exclude_chat_ids, (item: number): TdJsonRaw => item);
  out['return_deleted_file_statistics'] = value.return_deleted_file_statistics;
  out['chat_limit'] = value.chat_limit;
  return out;
}

/** optionValueBoolean = OptionValue — Represents a boolean option */
export class OptionValueBoolean {
  type: 'optionValueBoolean' = 'optionValueBoolean';
  extra?: string;
  /** value: Bool — The value of the option */
  value: boolean = false;
}

/** Decode a TDLib `optionValueBoolean` object; null input -> null. */
export function decodeOptionValueBoolean(json: TdJsonRaw): OptionValueBoolean | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OptionValueBoolean = new OptionValueBoolean();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asBool(record['value']);
  return out;
}

/** Encode a `optionValueBoolean` into TDLib JSON wire shape. */
export function encodeOptionValueBoolean(value: OptionValueBoolean): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'optionValueBoolean';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}

/** optionValueEmpty = OptionValue — Represents an unknown option or an option which has a default value */
export class OptionValueEmpty {
  type: 'optionValueEmpty' = 'optionValueEmpty';
  extra?: string;
}

/** Decode a TDLib `optionValueEmpty` object; null input -> null. */
export function decodeOptionValueEmpty(json: TdJsonRaw): OptionValueEmpty | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OptionValueEmpty = new OptionValueEmpty();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `optionValueEmpty` into TDLib JSON wire shape. */
export function encodeOptionValueEmpty(value: OptionValueEmpty): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'optionValueEmpty';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** optionValueInteger = OptionValue — Represents an integer option */
export class OptionValueInteger {
  type: 'optionValueInteger' = 'optionValueInteger';
  extra?: string;
  /** value: int64 — The value of the option */
  value: string = '';
}

/** Decode a TDLib `optionValueInteger` object; null input -> null. */
export function decodeOptionValueInteger(json: TdJsonRaw): OptionValueInteger | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OptionValueInteger = new OptionValueInteger();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asInt64(record['value']);
  return out;
}

/** Encode a `optionValueInteger` into TDLib JSON wire shape. */
export function encodeOptionValueInteger(value: OptionValueInteger): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'optionValueInteger';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}

/** optionValueString = OptionValue — Represents a string option */
export class OptionValueString {
  type: 'optionValueString' = 'optionValueString';
  extra?: string;
  /** value: string — The value of the option */
  value: string = '';
}

/** Decode a TDLib `optionValueString` object; null input -> null. */
export function decodeOptionValueString(json: TdJsonRaw): OptionValueString | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OptionValueString = new OptionValueString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asString(record['value']);
  return out;
}

/** Encode a `optionValueString` into TDLib JSON wire shape. */
export function encodeOptionValueString(value: OptionValueString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'optionValueString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}

/** orderInfo = OrderInfo — Order information */
export class OrderInfo {
  type: 'orderInfo' = 'orderInfo';
  extra?: string;
  /** name: string — Name of the user */
  name: string = '';
  /** phone_number: string — Phone number of the user */
  phone_number: string = '';
  /** email_address: string — Email address of the user */
  email_address: string = '';
  /** shipping_address: address — Shipping address for this order; may be null */
  shipping_address: Address | null = null;
}

/** Decode a TDLib `orderInfo` object; null input -> null. */
export function decodeOrderInfo(json: TdJsonRaw): OrderInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: OrderInfo = new OrderInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.phone_number = asString(record['phone_number']);
  out.email_address = asString(record['email_address']);
  out.shipping_address = decodeAddress(record['shipping_address']);
  return out;
}

/** Encode a `orderInfo` into TDLib JSON wire shape. */
export function encodeOrderInfo(value: OrderInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'orderInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['phone_number'] = value.phone_number;
  out['email_address'] = value.email_address;
  out['shipping_address'] = value.shipping_address === null ? null : encodeAddress(value.shipping_address);
  return out;
}

/** outline = Outline — Represents outline of an image */
export class Outline {
  type: 'outline' = 'outline';
  extra?: string;
  /** paths: vector<closedVectorPath> — The list of closed vector paths */
  paths: ClosedVectorPath[] = [];
}

/** Decode a TDLib `outline` object; null input -> null. */
export function decodeOutline(json: TdJsonRaw): Outline | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Outline = new Outline();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.paths = decodeVector(record['paths'], (item: TdJsonRaw): ClosedVectorPath => decodeClosedVectorPath(item) as ClosedVectorPath);
  return out;
}

/** Encode a `outline` into TDLib JSON wire shape. */
export function encodeOutline(value: Outline): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'outline';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['paths'] = encodeVector(value.paths, (item: ClosedVectorPath | null): TdJsonRaw => item === null ? null : encodeClosedVectorPath(item));
  return out;
}
