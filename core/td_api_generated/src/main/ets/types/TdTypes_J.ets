/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { GroupCallJoinParameters, decodeGroupCallJoinParameters, encodeGroupCallJoinParameters } from './TdTypes_G';
import { TdInputGroupCall, decodeTdInputGroupCall, encodeTdInputGroupCall } from './TdUnions_I';
import { TdJsonValue, decodeTdJsonValue, encodeTdJsonValue } from './TdUnions_J';
import { TdMessageSender, decodeTdMessageSender, encodeTdMessageSender } from './TdUnions_M';

/** joinChat = ChatJoinResult — Adds the current user as a new member to a chat. Private and secret chats can't be joined using this method */
export class JoinChat {
  type: 'joinChat' = 'joinChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `joinChat` object; null input -> null. */
export function decodeJoinChat(json: TdJsonRaw): JoinChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JoinChat = new JoinChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `joinChat` into TDLib JSON wire shape. */
export function encodeJoinChat(value: JoinChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'joinChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** joinChatByInviteLink = ChatJoinResult — Uses an invite link to add the current user to the chat if possible */
export class JoinChatByInviteLink {
  type: 'joinChatByInviteLink' = 'joinChatByInviteLink';
  extra?: string;
  /** invite_link: string — Invite link to use */
  invite_link: string = '';
}

/** Decode a TDLib `joinChatByInviteLink` object; null input -> null. */
export function decodeJoinChatByInviteLink(json: TdJsonRaw): JoinChatByInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JoinChatByInviteLink = new JoinChatByInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `joinChatByInviteLink` into TDLib JSON wire shape. */
export function encodeJoinChatByInviteLink(value: JoinChatByInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'joinChatByInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  return out;
}

/** joinGroupCall = GroupCallInfo — Joins a regular group call that is not bound to a chat */
export class JoinGroupCall {
  type: 'joinGroupCall' = 'joinGroupCall';
  extra?: string;
  /** input_group_call: InputGroupCall — The group call to join */
  input_group_call: TdInputGroupCall | null = null;
  /** join_parameters: groupCallJoinParameters — Parameters to join the call */
  join_parameters: GroupCallJoinParameters | null = null;
}

/** Decode a TDLib `joinGroupCall` object; null input -> null. */
export function decodeJoinGroupCall(json: TdJsonRaw): JoinGroupCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JoinGroupCall = new JoinGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.input_group_call = decodeTdInputGroupCall(record['input_group_call']);
  out.join_parameters = decodeGroupCallJoinParameters(record['join_parameters']);
  return out;
}

/** Encode a `joinGroupCall` into TDLib JSON wire shape. */
export function encodeJoinGroupCall(value: JoinGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'joinGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['input_group_call'] = value.input_group_call === null ? null : encodeTdInputGroupCall(value.input_group_call);
  out['join_parameters'] = value.join_parameters === null ? null : encodeGroupCallJoinParameters(value.join_parameters);
  return out;
}

/** joinLiveStory = Text — Joins a group call of an active live story. Returns join response payload for tgcalls */
export class JoinLiveStory {
  type: 'joinLiveStory' = 'joinLiveStory';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** join_parameters: groupCallJoinParameters — Parameters to join the call */
  join_parameters: GroupCallJoinParameters | null = null;
}

/** Decode a TDLib `joinLiveStory` object; null input -> null. */
export function decodeJoinLiveStory(json: TdJsonRaw): JoinLiveStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JoinLiveStory = new JoinLiveStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.join_parameters = decodeGroupCallJoinParameters(record['join_parameters']);
  return out;
}

/** Encode a `joinLiveStory` into TDLib JSON wire shape. */
export function encodeJoinLiveStory(value: JoinLiveStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'joinLiveStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['join_parameters'] = value.join_parameters === null ? null : encodeGroupCallJoinParameters(value.join_parameters);
  return out;
}

/** joinVideoChat = Text — Joins an active video chat. Returns join response payload for tgcalls */
export class JoinVideoChat {
  type: 'joinVideoChat' = 'joinVideoChat';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** participant_id: MessageSender — Identifier of a group call participant, which will be used to join the call; pass null to join as self */
  participant_id: TdMessageSender | null = null;
  /** join_parameters: groupCallJoinParameters — Parameters to join the call */
  join_parameters: GroupCallJoinParameters | null = null;
  /** invite_hash: string — Invite hash as received from internalLinkTypeVideoChat */
  invite_hash: string = '';
}

/** Decode a TDLib `joinVideoChat` object; null input -> null. */
export function decodeJoinVideoChat(json: TdJsonRaw): JoinVideoChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JoinVideoChat = new JoinVideoChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.participant_id = decodeTdMessageSender(record['participant_id']);
  out.join_parameters = decodeGroupCallJoinParameters(record['join_parameters']);
  out.invite_hash = asString(record['invite_hash']);
  return out;
}

/** Encode a `joinVideoChat` into TDLib JSON wire shape. */
export function encodeJoinVideoChat(value: JoinVideoChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'joinVideoChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['participant_id'] = value.participant_id === null ? null : encodeTdMessageSender(value.participant_id);
  out['join_parameters'] = value.join_parameters === null ? null : encodeGroupCallJoinParameters(value.join_parameters);
  out['invite_hash'] = value.invite_hash;
  return out;
}

/** jsonObjectMember = JsonObjectMember — Represents one member of a JSON object */
export class JsonObjectMember {
  type: 'jsonObjectMember' = 'jsonObjectMember';
  extra?: string;
  /** key: string — Member's key */
  key: string = '';
  /** value: JsonValue — Member's value */
  value: TdJsonValue | null = null;
}

/** Decode a TDLib `jsonObjectMember` object; null input -> null. */
export function decodeJsonObjectMember(json: TdJsonRaw): JsonObjectMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JsonObjectMember = new JsonObjectMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.key = asString(record['key']);
  out.value = decodeTdJsonValue(record['value']);
  return out;
}

/** Encode a `jsonObjectMember` into TDLib JSON wire shape. */
export function encodeJsonObjectMember(value: JsonObjectMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'jsonObjectMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['key'] = value.key;
  out['value'] = value.value === null ? null : encodeTdJsonValue(value.value);
  return out;
}

/** jsonValueArray = JsonValue — Represents a JSON array */
export class JsonValueArray {
  type: 'jsonValueArray' = 'jsonValueArray';
  extra?: string;
  /** values: vector<JsonValue> — The list of array elements */
  values: TdJsonValue[] = [];
}

/** Decode a TDLib `jsonValueArray` object; null input -> null. */
export function decodeJsonValueArray(json: TdJsonRaw): JsonValueArray | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JsonValueArray = new JsonValueArray();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.values = decodeVector(record['values'], (item: TdJsonRaw): TdJsonValue => decodeTdJsonValue(item) as TdJsonValue);
  return out;
}

/** Encode a `jsonValueArray` into TDLib JSON wire shape. */
export function encodeJsonValueArray(value: JsonValueArray): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'jsonValueArray';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['values'] = encodeVector(value.values, (item: TdJsonValue | null): TdJsonRaw => item === null ? null : encodeTdJsonValue(item));
  return out;
}

/** jsonValueBoolean = JsonValue — Represents a boolean JSON value */
export class JsonValueBoolean {
  type: 'jsonValueBoolean' = 'jsonValueBoolean';
  extra?: string;
  /** value: Bool — The value */
  value: boolean = false;
}

/** Decode a TDLib `jsonValueBoolean` object; null input -> null. */
export function decodeJsonValueBoolean(json: TdJsonRaw): JsonValueBoolean | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JsonValueBoolean = new JsonValueBoolean();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asBool(record['value']);
  return out;
}

/** Encode a `jsonValueBoolean` into TDLib JSON wire shape. */
export function encodeJsonValueBoolean(value: JsonValueBoolean): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'jsonValueBoolean';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}

/** jsonValueNull = JsonValue — Represents a null JSON value */
export class JsonValueNull {
  type: 'jsonValueNull' = 'jsonValueNull';
  extra?: string;
}

/** Decode a TDLib `jsonValueNull` object; null input -> null. */
export function decodeJsonValueNull(json: TdJsonRaw): JsonValueNull | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JsonValueNull = new JsonValueNull();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `jsonValueNull` into TDLib JSON wire shape. */
export function encodeJsonValueNull(value: JsonValueNull): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'jsonValueNull';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** jsonValueNumber = JsonValue — Represents a numeric JSON value */
export class JsonValueNumber {
  type: 'jsonValueNumber' = 'jsonValueNumber';
  extra?: string;
  /** value: double — The value */
  value: number = 0;
}

/** Decode a TDLib `jsonValueNumber` object; null input -> null. */
export function decodeJsonValueNumber(json: TdJsonRaw): JsonValueNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JsonValueNumber = new JsonValueNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asNumber(record['value']);
  return out;
}

/** Encode a `jsonValueNumber` into TDLib JSON wire shape. */
export function encodeJsonValueNumber(value: JsonValueNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'jsonValueNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}

/** jsonValueObject = JsonValue — Represents a JSON object */
export class JsonValueObject {
  type: 'jsonValueObject' = 'jsonValueObject';
  extra?: string;
  /** members: vector<jsonObjectMember> — The list of object members */
  members: JsonObjectMember[] = [];
}

/** Decode a TDLib `jsonValueObject` object; null input -> null. */
export function decodeJsonValueObject(json: TdJsonRaw): JsonValueObject | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JsonValueObject = new JsonValueObject();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.members = decodeVector(record['members'], (item: TdJsonRaw): JsonObjectMember => decodeJsonObjectMember(item) as JsonObjectMember);
  return out;
}

/** Encode a `jsonValueObject` into TDLib JSON wire shape. */
export function encodeJsonValueObject(value: JsonValueObject): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'jsonValueObject';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['members'] = encodeVector(value.members, (item: JsonObjectMember | null): TdJsonRaw => item === null ? null : encodeJsonObjectMember(item));
  return out;
}

/** jsonValueString = JsonValue — Represents a string JSON value */
export class JsonValueString {
  type: 'jsonValueString' = 'jsonValueString';
  extra?: string;
  /** value: string — The value */
  value: string = '';
}

/** Decode a TDLib `jsonValueString` object; null input -> null. */
export function decodeJsonValueString(json: TdJsonRaw): JsonValueString | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: JsonValueString = new JsonValueString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asString(record['value']);
  return out;
}

/** Encode a `jsonValueString` into TDLib JSON wire shape. */
export function encodeJsonValueString(value: JsonValueString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'jsonValueString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}
