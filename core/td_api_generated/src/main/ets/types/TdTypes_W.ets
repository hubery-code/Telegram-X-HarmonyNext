/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { Animation, decodeAnimation, encodeAnimation } from './TdTypes_A';
import { Photo, decodePhoto, encodePhoto } from './TdTypes_P';
import { ThemeParameters, decodeThemeParameters, encodeThemeParameters } from './TdTypes_T';
import { TdInternalLinkType, TdMessageContent, TdPageBlock, TdWebAppOpenMode, decodeTdInternalLinkType, decodeTdMessageContent, decodeTdPageBlock, decodeTdWebAppOpenMode, encodeTdInternalLinkType, encodeTdMessageContent, encodeTdPageBlock, encodeTdWebAppOpenMode } from './TdUnions';

/** webApp = WebApp — Describes a Web App. Use getInternalLink with internalLinkTypeWebApp to share the Web App */
export class WebApp {
  type: 'webApp' = 'webApp';
  extra?: string;
  /** short_name: string — Web App short name */
  short_name: string = '';
  /** title: string — Web App title */
  title: string = '';
  /** description: string */
  description: string = '';
  /** photo: photo — Web App photo */
  photo: Photo | null = null;
  /** animation: animation — Web App animation; may be null */
  animation: Animation | null = null;
}

/** Decode a TDLib `webApp` object; null input -> null. */
export function decodeWebApp(json: TdJsonRaw): WebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebApp = new WebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.short_name = asString(record['short_name']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  out.photo = decodePhoto(record['photo']);
  out.animation = decodeAnimation(record['animation']);
  return out;
}

/** Encode a `webApp` into TDLib JSON wire shape. */
export function encodeWebApp(value: WebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['short_name'] = value.short_name;
  out['title'] = value.title;
  out['description'] = value.description;
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  return out;
}

/** webAppInfo = WebAppInfo — Contains information about a Web App */
export class WebAppInfo {
  type: 'webAppInfo' = 'webAppInfo';
  extra?: string;
  /** launch_id: int64 — Unique identifier for the Web App launch */
  launch_id: string = '';
  /** url: webAppUrl — The Web App URL to open in a web view */
  url: WebAppUrl | null = null;
}

/** Decode a TDLib `webAppInfo` object; null input -> null. */
export function decodeWebAppInfo(json: TdJsonRaw): WebAppInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebAppInfo = new WebAppInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.launch_id = asInt64(record['launch_id']);
  out.url = decodeWebAppUrl(record['url']);
  return out;
}

/** Encode a `webAppInfo` into TDLib JSON wire shape. */
export function encodeWebAppInfo(value: WebAppInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webAppInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['launch_id'] = value.launch_id;
  out['url'] = value.url === null ? null : encodeWebAppUrl(value.url);
  return out;
}

/** webAppOpenModeCompact = WebAppOpenMode — The Web App is opened in the compact mode */
export class WebAppOpenModeCompact {
  type: 'webAppOpenModeCompact' = 'webAppOpenModeCompact';
  extra?: string;
}

/** Decode a TDLib `webAppOpenModeCompact` object; null input -> null. */
export function decodeWebAppOpenModeCompact(json: TdJsonRaw): WebAppOpenModeCompact | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebAppOpenModeCompact = new WebAppOpenModeCompact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `webAppOpenModeCompact` into TDLib JSON wire shape. */
export function encodeWebAppOpenModeCompact(value: WebAppOpenModeCompact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webAppOpenModeCompact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** webAppOpenModeFullScreen = WebAppOpenMode — The Web App is opened in the full-screen mode */
export class WebAppOpenModeFullScreen {
  type: 'webAppOpenModeFullScreen' = 'webAppOpenModeFullScreen';
  extra?: string;
}

/** Decode a TDLib `webAppOpenModeFullScreen` object; null input -> null. */
export function decodeWebAppOpenModeFullScreen(json: TdJsonRaw): WebAppOpenModeFullScreen | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebAppOpenModeFullScreen = new WebAppOpenModeFullScreen();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `webAppOpenModeFullScreen` into TDLib JSON wire shape. */
export function encodeWebAppOpenModeFullScreen(value: WebAppOpenModeFullScreen): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webAppOpenModeFullScreen';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** webAppOpenModeFullSize = WebAppOpenMode — The Web App is opened in the full-size mode */
export class WebAppOpenModeFullSize {
  type: 'webAppOpenModeFullSize' = 'webAppOpenModeFullSize';
  extra?: string;
}

/** Decode a TDLib `webAppOpenModeFullSize` object; null input -> null. */
export function decodeWebAppOpenModeFullSize(json: TdJsonRaw): WebAppOpenModeFullSize | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebAppOpenModeFullSize = new WebAppOpenModeFullSize();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `webAppOpenModeFullSize` into TDLib JSON wire shape. */
export function encodeWebAppOpenModeFullSize(value: WebAppOpenModeFullSize): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webAppOpenModeFullSize';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** webAppOpenParameters = WebAppOpenParameters — Options to be used when a Web App is opened */
export class WebAppOpenParameters {
  type: 'webAppOpenParameters' = 'webAppOpenParameters';
  extra?: string;
  /** theme: themeParameters — Preferred Web App theme; pass null to use the default theme */
  theme: ThemeParameters | null = null;
  /** application_name: string — Short name of the current application; 0-64 English letters, digits, and underscores */
  application_name: string = '';
  /** mode: WebAppOpenMode — The mode in which the Web App is opened; pass null to open in webAppOpenModeFullSize */
  mode: TdWebAppOpenMode | null = null;
}

/** Decode a TDLib `webAppOpenParameters` object; null input -> null. */
export function decodeWebAppOpenParameters(json: TdJsonRaw): WebAppOpenParameters | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebAppOpenParameters = new WebAppOpenParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.theme = decodeThemeParameters(record['theme']);
  out.application_name = asString(record['application_name']);
  out.mode = decodeTdWebAppOpenMode(record['mode']);
  return out;
}

/** Encode a `webAppOpenParameters` into TDLib JSON wire shape. */
export function encodeWebAppOpenParameters(value: WebAppOpenParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webAppOpenParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['theme'] = value.theme === null ? null : encodeThemeParameters(value.theme);
  out['application_name'] = value.application_name;
  out['mode'] = value.mode === null ? null : encodeTdWebAppOpenMode(value.mode);
  return out;
}

/** webAppUrl = WebAppUrl — Contains information about a Web App URL */
export class WebAppUrl {
  type: 'webAppUrl' = 'webAppUrl';
  extra?: string;
  /** url: string — The Web App URL to open in a web view */
  url: string = '';
  /** require_same_origin: Bool — True, if events from the Web App must be accepted only from the same origin as the URL */
  require_same_origin: boolean = false;
}

/** Decode a TDLib `webAppUrl` object; null input -> null. */
export function decodeWebAppUrl(json: TdJsonRaw): WebAppUrl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebAppUrl = new WebAppUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.require_same_origin = asBool(record['require_same_origin']);
  return out;
}

/** Encode a `webAppUrl` into TDLib JSON wire shape. */
export function encodeWebAppUrl(value: WebAppUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webAppUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['require_same_origin'] = value.require_same_origin;
  return out;
}

/** webBrowserSettings = WebBrowserSettings — Describes web browser settings */
export class WebBrowserSettings {
  type: 'webBrowserSettings' = 'webBrowserSettings';
  extra?: string;
  /** open_external_browser: Bool — True, if links are opened in an external browser by default */
  open_external_browser: boolean = false;
  /** external_exceptions: vector<webDomainException> — The list of websites which must always be opened in an external browser */
  external_exceptions: WebDomainException[] = [];
  /** in_app_exceptions: vector<webDomainException> — The list of websites which must always be opened in the in-app browser */
  in_app_exceptions: WebDomainException[] = [];
  /** display_close_button: Bool — True, if a close button must be shown in the in-app browser; for Android app only */
  display_close_button: boolean = false;
}

/** Decode a TDLib `webBrowserSettings` object; null input -> null. */
export function decodeWebBrowserSettings(json: TdJsonRaw): WebBrowserSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebBrowserSettings = new WebBrowserSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.open_external_browser = asBool(record['open_external_browser']);
  out.external_exceptions = decodeVector(record['external_exceptions'], (item: TdJsonRaw): WebDomainException => decodeWebDomainException(item) as WebDomainException);
  out.in_app_exceptions = decodeVector(record['in_app_exceptions'], (item: TdJsonRaw): WebDomainException => decodeWebDomainException(item) as WebDomainException);
  out.display_close_button = asBool(record['display_close_button']);
  return out;
}

/** Encode a `webBrowserSettings` into TDLib JSON wire shape. */
export function encodeWebBrowserSettings(value: WebBrowserSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webBrowserSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['open_external_browser'] = value.open_external_browser;
  out['external_exceptions'] = encodeVector(value.external_exceptions, (item: WebDomainException | null): TdJsonRaw => item === null ? null : encodeWebDomainException(item));
  out['in_app_exceptions'] = encodeVector(value.in_app_exceptions, (item: WebDomainException | null): TdJsonRaw => item === null ? null : encodeWebDomainException(item));
  out['display_close_button'] = value.display_close_button;
  return out;
}

/** webBrowserTypeExternal = WebBrowserType — An external web browser */
export class WebBrowserTypeExternal {
  type: 'webBrowserTypeExternal' = 'webBrowserTypeExternal';
  extra?: string;
}

/** Decode a TDLib `webBrowserTypeExternal` object; null input -> null. */
export function decodeWebBrowserTypeExternal(json: TdJsonRaw): WebBrowserTypeExternal | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebBrowserTypeExternal = new WebBrowserTypeExternal();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `webBrowserTypeExternal` into TDLib JSON wire shape. */
export function encodeWebBrowserTypeExternal(value: WebBrowserTypeExternal): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webBrowserTypeExternal';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** webBrowserTypeInApp = WebBrowserType — The in-app browser */
export class WebBrowserTypeInApp {
  type: 'webBrowserTypeInApp' = 'webBrowserTypeInApp';
  extra?: string;
}

/** Decode a TDLib `webBrowserTypeInApp` object; null input -> null. */
export function decodeWebBrowserTypeInApp(json: TdJsonRaw): WebBrowserTypeInApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebBrowserTypeInApp = new WebBrowserTypeInApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `webBrowserTypeInApp` into TDLib JSON wire shape. */
export function encodeWebBrowserTypeInApp(value: WebBrowserTypeInApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webBrowserTypeInApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** webDomainException = WebDomainException — Describes an exception for built-in browser usage */
export class WebDomainException {
  type: 'webDomainException' = 'webDomainException';
  extra?: string;
  /** url: string — URL for which the exception is done */
  url: string = '';
  /** domain: string — Domain of the URL. All URLs on the domain and subdomains of the domain are subject to the exception */
  domain: string = '';
  /** title: string — Title of the website */
  title: string = '';
  /** favicon_custom_emoji_id: int64 — Identifier of the custom emoji with favicon of the website; may be 0 if unknown, in which case the first letter of the domain must be used */
  favicon_custom_emoji_id: string = '';
}

/** Decode a TDLib `webDomainException` object; null input -> null. */
export function decodeWebDomainException(json: TdJsonRaw): WebDomainException | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebDomainException = new WebDomainException();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.domain = asString(record['domain']);
  out.title = asString(record['title']);
  out.favicon_custom_emoji_id = asInt64(record['favicon_custom_emoji_id']);
  return out;
}

/** Encode a `webDomainException` into TDLib JSON wire shape. */
export function encodeWebDomainException(value: WebDomainException): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webDomainException';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['domain'] = value.domain;
  out['title'] = value.title;
  out['favicon_custom_emoji_id'] = value.favicon_custom_emoji_id;
  return out;
}

/** webPageInstantView = WebPageInstantView — Describes an instant view page for a web page */
export class WebPageInstantView {
  type: 'webPageInstantView' = 'webPageInstantView';
  extra?: string;
  /** blocks: vector<PageBlock> — Content of the instant view page */
  blocks: TdPageBlock[] = [];
  /** view_count: int32 — Number of the instant view views; 0 if unknown */
  view_count: number = 0;
  /** version: int32 — Version of the instant view; currently, can be 1 or 2 */
  version: number = 0;
  /** is_rtl: Bool — True, if the instant view must be shown from right to left */
  is_rtl: boolean = false;
  /** is_full: Bool — True, if the instant view contains the full page. A network request might be needed to get the full instant view */
  is_full: boolean = false;
  /** feedback_link: InternalLinkType — An internal link to be opened to leave feedback about the instant view */
  feedback_link: TdInternalLinkType | null = null;
}

/** Decode a TDLib `webPageInstantView` object; null input -> null. */
export function decodeWebPageInstantView(json: TdJsonRaw): WebPageInstantView | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WebPageInstantView = new WebPageInstantView();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdPageBlock => decodeTdPageBlock(item) as TdPageBlock);
  out.view_count = asNumber(record['view_count']);
  out.version = asNumber(record['version']);
  out.is_rtl = asBool(record['is_rtl']);
  out.is_full = asBool(record['is_full']);
  out.feedback_link = decodeTdInternalLinkType(record['feedback_link']);
  return out;
}

/** Encode a `webPageInstantView` into TDLib JSON wire shape. */
export function encodeWebPageInstantView(value: WebPageInstantView): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'webPageInstantView';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdPageBlock | null): TdJsonRaw => item === null ? null : encodeTdPageBlock(item));
  out['view_count'] = value.view_count;
  out['version'] = value.version;
  out['is_rtl'] = value.is_rtl;
  out['is_full'] = value.is_full;
  out['feedback_link'] = value.feedback_link === null ? null : encodeTdInternalLinkType(value.feedback_link);
  return out;
}

/** welcomeMessage = WelcomeMessage — Describes a set up welcome message */
export class WelcomeMessage {
  type: 'welcomeMessage' = 'welcomeMessage';
  extra?: string;
  /** id: int32 — Welcome message identifier; unique for the chat to which the welcome message belongs */
  id: number = 0;
  /** content: MessageContent — Content of the welcome message */
  content: TdMessageContent | null = null;
}

/** Decode a TDLib `welcomeMessage` object; null input -> null. */
export function decodeWelcomeMessage(json: TdJsonRaw): WelcomeMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WelcomeMessage = new WelcomeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.content = decodeTdMessageContent(record['content']);
  return out;
}

/** Encode a `welcomeMessage` into TDLib JSON wire shape. */
export function encodeWelcomeMessage(value: WelcomeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'welcomeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['content'] = value.content === null ? null : encodeTdMessageContent(value.content);
  return out;
}

/** writeGeneratedFilePart = Ok — Writes a part of a generated file. This method is intended to be used only if the application has no direct access to TDLib's file system, because it is usually slower than a direct write to the destination ... */
export class WriteGeneratedFilePart {
  type: 'writeGeneratedFilePart' = 'writeGeneratedFilePart';
  extra?: string;
  /** generation_id: int64 — The identifier of the generation process */
  generation_id: string = '';
  /** offset: int53 — The offset from which to write the data to the file */
  offset: number = 0;
  /** data: bytes — The data to write */
  data: string = '';
}

/** Decode a TDLib `writeGeneratedFilePart` object; null input -> null. */
export function decodeWriteGeneratedFilePart(json: TdJsonRaw): WriteGeneratedFilePart | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: WriteGeneratedFilePart = new WriteGeneratedFilePart();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.generation_id = asInt64(record['generation_id']);
  out.offset = asNumber(record['offset']);
  out.data = asString(record['data']);
  return out;
}

/** Encode a `writeGeneratedFilePart` into TDLib JSON wire shape. */
export function encodeWriteGeneratedFilePart(value: WriteGeneratedFilePart): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'writeGeneratedFilePart';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['generation_id'] = value.generation_id;
  out['offset'] = value.offset;
  out['data'] = value.data;
  return out;
}
