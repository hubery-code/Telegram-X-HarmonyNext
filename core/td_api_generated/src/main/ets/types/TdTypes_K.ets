/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString } from '../runtime/TdJson';
import { ChatAdministratorRights, decodeChatAdministratorRights, encodeChatAdministratorRights } from './TdTypes_C';
import { TdButtonStyle, TdKeyboardButtonType, decodeTdButtonStyle, decodeTdKeyboardButtonType, encodeTdButtonStyle, encodeTdKeyboardButtonType } from './TdUnions';

/** keyboardButton = KeyboardButton — Represents a single button in a bot keyboard */
export class KeyboardButton {
  type: 'keyboardButton' = 'keyboardButton';
  extra?: string;
  /** text: string — Text of the button */
  text: string = '';
  /** icon_custom_emoji_id: int64 — Identifier of the custom emoji that must be shown on the button; 0 if none */
  icon_custom_emoji_id: string = '';
  /** style: ButtonStyle — Style of the button */
  style: TdButtonStyle | null = null;
  /** type: KeyboardButtonType — Type of the button */
  type_: TdKeyboardButtonType | null = null;
}

/** Decode a TDLib `keyboardButton` object; null input -> null. */
export function decodeKeyboardButton(json: TdJsonRaw): KeyboardButton | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButton = new KeyboardButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.icon_custom_emoji_id = asInt64(record['icon_custom_emoji_id']);
  out.style = decodeTdButtonStyle(record['style']);
  out.type_ = decodeTdKeyboardButtonType(record['type']);
  return out;
}

/** Encode a `keyboardButton` into TDLib JSON wire shape. */
export function encodeKeyboardButton(value: KeyboardButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['icon_custom_emoji_id'] = value.icon_custom_emoji_id;
  out['style'] = value.style === null ? null : encodeTdButtonStyle(value.style);
  out['type'] = value.type_ === null ? null : encodeTdKeyboardButtonType(value.type_);
  return out;
}

/** keyboardButtonSourceMessage = KeyboardButtonSource — The button is from a bot's message */
export class KeyboardButtonSourceMessage {
  type: 'keyboardButtonSourceMessage' = 'keyboardButtonSourceMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message with the button */
  message_id: number = 0;
}

/** Decode a TDLib `keyboardButtonSourceMessage` object; null input -> null. */
export function decodeKeyboardButtonSourceMessage(json: TdJsonRaw): KeyboardButtonSourceMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonSourceMessage = new KeyboardButtonSourceMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `keyboardButtonSourceMessage` into TDLib JSON wire shape. */
export function encodeKeyboardButtonSourceMessage(value: KeyboardButtonSourceMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonSourceMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** keyboardButtonSourceWebApp = KeyboardButtonSource — The button is a prepared keyboard button from a Mini App received via getPreparedKeyboardButton */
export class KeyboardButtonSourceWebApp {
  type: 'keyboardButtonSourceWebApp' = 'keyboardButtonSourceWebApp';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the bot that created the button */
  bot_user_id: number = 0;
  /** prepared_button_id: string — Identifier of the prepared button */
  prepared_button_id: string = '';
}

/** Decode a TDLib `keyboardButtonSourceWebApp` object; null input -> null. */
export function decodeKeyboardButtonSourceWebApp(json: TdJsonRaw): KeyboardButtonSourceWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonSourceWebApp = new KeyboardButtonSourceWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.prepared_button_id = asString(record['prepared_button_id']);
  return out;
}

/** Encode a `keyboardButtonSourceWebApp` into TDLib JSON wire shape. */
export function encodeKeyboardButtonSourceWebApp(value: KeyboardButtonSourceWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonSourceWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['prepared_button_id'] = value.prepared_button_id;
  return out;
}

/** keyboardButtonTypeRequestChat = KeyboardButtonType — A button that requests a chat to be shared by the current user; available only in private chats. Use the method shareChatWithBot to complete the request */
export class KeyboardButtonTypeRequestChat {
  type: 'keyboardButtonTypeRequestChat' = 'keyboardButtonTypeRequestChat';
  extra?: string;
  /** id: int32 — Unique button identifier */
  id: number = 0;
  /** chat_is_channel: Bool — True, if the chat must be a channel; otherwise, a basic group or a supergroup chat is shared */
  chat_is_channel: boolean = false;
  /** restrict_chat_is_forum: Bool — True, if the chat must or must not be a forum supergroup */
  restrict_chat_is_forum: boolean = false;
  /** chat_is_forum: Bool — True, if the chat must be a forum supergroup; otherwise, the chat must not be a forum supergroup. Ignored if restrict_chat_is_forum is false */
  chat_is_forum: boolean = false;
  /** restrict_chat_has_username: Bool — True, if the chat must or must not have a username */
  restrict_chat_has_username: boolean = false;
  /** chat_has_username: Bool — True, if the chat must have a username; otherwise, the chat must not have a username. Ignored if restrict_chat_has_username is false */
  chat_has_username: boolean = false;
  /** chat_is_created: Bool — True, if the chat must be created by the current user */
  chat_is_created: boolean = false;
  /** user_administrator_rights: chatAdministratorRights — Expected user administrator rights in the chat; may be null if they aren't restricted */
  user_administrator_rights: ChatAdministratorRights | null = null;
  /** bot_administrator_rights: chatAdministratorRights — Expected bot administrator rights in the chat; may be null if they aren't restricted */
  bot_administrator_rights: ChatAdministratorRights | null = null;
  /** bot_is_member: Bool — True, if the bot must be a member of the chat; for basic group and supergroup chats only */
  bot_is_member: boolean = false;
  /** request_title: Bool — Pass true to request title of the chat; bots only */
  request_title: boolean = false;
  /** request_username: Bool — Pass true to request username of the chat; bots only */
  request_username: boolean = false;
  /** request_photo: Bool — Pass true to request photo of the chat; bots only */
  request_photo: boolean = false;
}

/** Decode a TDLib `keyboardButtonTypeRequestChat` object; null input -> null. */
export function decodeKeyboardButtonTypeRequestChat(json: TdJsonRaw): KeyboardButtonTypeRequestChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonTypeRequestChat = new KeyboardButtonTypeRequestChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.chat_is_channel = asBool(record['chat_is_channel']);
  out.restrict_chat_is_forum = asBool(record['restrict_chat_is_forum']);
  out.chat_is_forum = asBool(record['chat_is_forum']);
  out.restrict_chat_has_username = asBool(record['restrict_chat_has_username']);
  out.chat_has_username = asBool(record['chat_has_username']);
  out.chat_is_created = asBool(record['chat_is_created']);
  out.user_administrator_rights = decodeChatAdministratorRights(record['user_administrator_rights']);
  out.bot_administrator_rights = decodeChatAdministratorRights(record['bot_administrator_rights']);
  out.bot_is_member = asBool(record['bot_is_member']);
  out.request_title = asBool(record['request_title']);
  out.request_username = asBool(record['request_username']);
  out.request_photo = asBool(record['request_photo']);
  return out;
}

/** Encode a `keyboardButtonTypeRequestChat` into TDLib JSON wire shape. */
export function encodeKeyboardButtonTypeRequestChat(value: KeyboardButtonTypeRequestChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonTypeRequestChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['chat_is_channel'] = value.chat_is_channel;
  out['restrict_chat_is_forum'] = value.restrict_chat_is_forum;
  out['chat_is_forum'] = value.chat_is_forum;
  out['restrict_chat_has_username'] = value.restrict_chat_has_username;
  out['chat_has_username'] = value.chat_has_username;
  out['chat_is_created'] = value.chat_is_created;
  out['user_administrator_rights'] = value.user_administrator_rights === null ? null : encodeChatAdministratorRights(value.user_administrator_rights);
  out['bot_administrator_rights'] = value.bot_administrator_rights === null ? null : encodeChatAdministratorRights(value.bot_administrator_rights);
  out['bot_is_member'] = value.bot_is_member;
  out['request_title'] = value.request_title;
  out['request_username'] = value.request_username;
  out['request_photo'] = value.request_photo;
  return out;
}

/** keyboardButtonTypeRequestLocation = KeyboardButtonType — A button that sends the user's location when pressed; available only in private chats */
export class KeyboardButtonTypeRequestLocation {
  type: 'keyboardButtonTypeRequestLocation' = 'keyboardButtonTypeRequestLocation';
  extra?: string;
}

/** Decode a TDLib `keyboardButtonTypeRequestLocation` object; null input -> null. */
export function decodeKeyboardButtonTypeRequestLocation(json: TdJsonRaw): KeyboardButtonTypeRequestLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonTypeRequestLocation = new KeyboardButtonTypeRequestLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `keyboardButtonTypeRequestLocation` into TDLib JSON wire shape. */
export function encodeKeyboardButtonTypeRequestLocation(value: KeyboardButtonTypeRequestLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonTypeRequestLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** keyboardButtonTypeRequestManagedBot = KeyboardButtonType — A button that requests creation of a managed bot by the current user; available only in private chats. Use the method createBot to complete the request */
export class KeyboardButtonTypeRequestManagedBot {
  type: 'keyboardButtonTypeRequestManagedBot' = 'keyboardButtonTypeRequestManagedBot';
  extra?: string;
  /** id: int32 — Unique button identifier */
  id: number = 0;
  /** suggested_name: string — Suggested name for the bot; may be empty if not specified */
  suggested_name: string = '';
  /** suggested_username: string — Suggested username for the bot; may be empty if not specified */
  suggested_username: string = '';
}

/** Decode a TDLib `keyboardButtonTypeRequestManagedBot` object; null input -> null. */
export function decodeKeyboardButtonTypeRequestManagedBot(json: TdJsonRaw): KeyboardButtonTypeRequestManagedBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonTypeRequestManagedBot = new KeyboardButtonTypeRequestManagedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.suggested_name = asString(record['suggested_name']);
  out.suggested_username = asString(record['suggested_username']);
  return out;
}

/** Encode a `keyboardButtonTypeRequestManagedBot` into TDLib JSON wire shape. */
export function encodeKeyboardButtonTypeRequestManagedBot(value: KeyboardButtonTypeRequestManagedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonTypeRequestManagedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['suggested_name'] = value.suggested_name;
  out['suggested_username'] = value.suggested_username;
  return out;
}

/** keyboardButtonTypeRequestPhoneNumber = KeyboardButtonType — A button that sends the user's phone number when pressed; available only in private chats */
export class KeyboardButtonTypeRequestPhoneNumber {
  type: 'keyboardButtonTypeRequestPhoneNumber' = 'keyboardButtonTypeRequestPhoneNumber';
  extra?: string;
}

/** Decode a TDLib `keyboardButtonTypeRequestPhoneNumber` object; null input -> null. */
export function decodeKeyboardButtonTypeRequestPhoneNumber(json: TdJsonRaw): KeyboardButtonTypeRequestPhoneNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonTypeRequestPhoneNumber = new KeyboardButtonTypeRequestPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `keyboardButtonTypeRequestPhoneNumber` into TDLib JSON wire shape. */
export function encodeKeyboardButtonTypeRequestPhoneNumber(value: KeyboardButtonTypeRequestPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonTypeRequestPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** keyboardButtonTypeRequestPoll = KeyboardButtonType — A button that allows the user to create and send a poll when pressed; available only in private chats */
export class KeyboardButtonTypeRequestPoll {
  type: 'keyboardButtonTypeRequestPoll' = 'keyboardButtonTypeRequestPoll';
  extra?: string;
  /** force_regular: Bool — If true, only regular polls must be allowed to create */
  force_regular: boolean = false;
  /** force_quiz: Bool — If true, only polls in quiz mode must be allowed to create */
  force_quiz: boolean = false;
}

/** Decode a TDLib `keyboardButtonTypeRequestPoll` object; null input -> null. */
export function decodeKeyboardButtonTypeRequestPoll(json: TdJsonRaw): KeyboardButtonTypeRequestPoll | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonTypeRequestPoll = new KeyboardButtonTypeRequestPoll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.force_regular = asBool(record['force_regular']);
  out.force_quiz = asBool(record['force_quiz']);
  return out;
}

/** Encode a `keyboardButtonTypeRequestPoll` into TDLib JSON wire shape. */
export function encodeKeyboardButtonTypeRequestPoll(value: KeyboardButtonTypeRequestPoll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonTypeRequestPoll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['force_regular'] = value.force_regular;
  out['force_quiz'] = value.force_quiz;
  return out;
}

/** keyboardButtonTypeRequestUsers = KeyboardButtonType — A button that requests users to be shared by the current user; available only in private chats. Use the method shareUsersWithBot to complete the request */
export class KeyboardButtonTypeRequestUsers {
  type: 'keyboardButtonTypeRequestUsers' = 'keyboardButtonTypeRequestUsers';
  extra?: string;
  /** id: int32 — Unique button identifier */
  id: number = 0;
  /** restrict_user_is_bot: Bool — True, if the shared users must or must not be bots */
  restrict_user_is_bot: boolean = false;
  /** user_is_bot: Bool — True, if the shared users must be bots; otherwise, the shared users must not be bots. Ignored if restrict_user_is_bot is false */
  user_is_bot: boolean = false;
  /** restrict_user_is_premium: Bool — True, if the shared users must or must not be Telegram Premium users */
  restrict_user_is_premium: boolean = false;
  /** user_is_premium: Bool — True, if the shared users must be Telegram Premium users; otherwise, the shared users must not be Telegram Premium users. Ignored if restrict_user_is_premium is false */
  user_is_premium: boolean = false;
  /** max_quantity: int32 — The maximum number of users to share */
  max_quantity: number = 0;
  /** request_name: Bool — Pass true to request name of the users; bots only */
  request_name: boolean = false;
  /** request_username: Bool — Pass true to request username of the users; bots only */
  request_username: boolean = false;
  /** request_photo: Bool — Pass true to request photo of the users; bots only */
  request_photo: boolean = false;
}

/** Decode a TDLib `keyboardButtonTypeRequestUsers` object; null input -> null. */
export function decodeKeyboardButtonTypeRequestUsers(json: TdJsonRaw): KeyboardButtonTypeRequestUsers | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonTypeRequestUsers = new KeyboardButtonTypeRequestUsers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.restrict_user_is_bot = asBool(record['restrict_user_is_bot']);
  out.user_is_bot = asBool(record['user_is_bot']);
  out.restrict_user_is_premium = asBool(record['restrict_user_is_premium']);
  out.user_is_premium = asBool(record['user_is_premium']);
  out.max_quantity = asNumber(record['max_quantity']);
  out.request_name = asBool(record['request_name']);
  out.request_username = asBool(record['request_username']);
  out.request_photo = asBool(record['request_photo']);
  return out;
}

/** Encode a `keyboardButtonTypeRequestUsers` into TDLib JSON wire shape. */
export function encodeKeyboardButtonTypeRequestUsers(value: KeyboardButtonTypeRequestUsers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonTypeRequestUsers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['restrict_user_is_bot'] = value.restrict_user_is_bot;
  out['user_is_bot'] = value.user_is_bot;
  out['restrict_user_is_premium'] = value.restrict_user_is_premium;
  out['user_is_premium'] = value.user_is_premium;
  out['max_quantity'] = value.max_quantity;
  out['request_name'] = value.request_name;
  out['request_username'] = value.request_username;
  out['request_photo'] = value.request_photo;
  return out;
}

/** keyboardButtonTypeText = KeyboardButtonType — A simple button, with text that must be sent when the button is pressed */
export class KeyboardButtonTypeText {
  type: 'keyboardButtonTypeText' = 'keyboardButtonTypeText';
  extra?: string;
}

/** Decode a TDLib `keyboardButtonTypeText` object; null input -> null. */
export function decodeKeyboardButtonTypeText(json: TdJsonRaw): KeyboardButtonTypeText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonTypeText = new KeyboardButtonTypeText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `keyboardButtonTypeText` into TDLib JSON wire shape. */
export function encodeKeyboardButtonTypeText(value: KeyboardButtonTypeText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonTypeText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** keyboardButtonTypeWebApp = KeyboardButtonType — A button that opens a Web App by calling getWebAppUrl */
export class KeyboardButtonTypeWebApp {
  type: 'keyboardButtonTypeWebApp' = 'keyboardButtonTypeWebApp';
  extra?: string;
  /** url: string — An HTTP URL to pass to getWebAppUrl */
  url: string = '';
}

/** Decode a TDLib `keyboardButtonTypeWebApp` object; null input -> null. */
export function decodeKeyboardButtonTypeWebApp(json: TdJsonRaw): KeyboardButtonTypeWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: KeyboardButtonTypeWebApp = new KeyboardButtonTypeWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `keyboardButtonTypeWebApp` into TDLib JSON wire shape. */
export function encodeKeyboardButtonTypeWebApp(value: KeyboardButtonTypeWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'keyboardButtonTypeWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}
