/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { AccountTtl, AdvertisementSponsor, AffiliateInfo, AffiliateProgramParameters, ArchiveChatListSettings, AutoDownloadSettings, decodeAccountTtl, decodeAdvertisementSponsor, decodeAffiliateInfo, decodeAffiliateProgramParameters, decodeArchiveChatListSettings, decodeAutoDownloadSettings, encodeAccountTtl, encodeAdvertisementSponsor, encodeAffiliateInfo, encodeAffiliateProgramParameters, encodeArchiveChatListSettings, encodeAutoDownloadSettings } from './TdTypes_A';
import { Birthdate, BotAccessSettings, BotCommand, BotCommands, BotMenuButton, BotVerification, BusinessAwayMessageSettings, BusinessConnectedBot, BusinessGreetingMessageSettings, BusinessLocation, BusinessOpeningHours, decodeBirthdate, decodeBotAccessSettings, decodeBotCommand, decodeBotCommands, decodeBotMenuButton, decodeBotVerification, decodeBusinessAwayMessageSettings, decodeBusinessConnectedBot, decodeBusinessGreetingMessageSettings, decodeBusinessLocation, decodeBusinessOpeningHours, encodeBirthdate, encodeBotAccessSettings, encodeBotCommand, encodeBotCommands, encodeBotMenuButton, encodeBotVerification, encodeBusinessAwayMessageSettings, encodeBusinessConnectedBot, encodeBusinessGreetingMessageSettings, encodeBusinessLocation, encodeBusinessOpeningHours } from './TdTypes_B';
import { ChatAdministratorRights, ChatInviteLink, ChatLocation, ChatNotificationSettings, ChatPermissions, ChatPhoto, decodeChatAdministratorRights, decodeChatInviteLink, decodeChatLocation, decodeChatNotificationSettings, decodeChatPermissions, decodeChatPhoto, encodeChatAdministratorRights, encodeChatInviteLink, encodeChatLocation, encodeChatNotificationSettings, encodeChatPermissions, encodeChatPhoto } from './TdTypes_C';
import { DraftMessage, decodeDraftMessage, encodeDraftMessage } from './TdTypes_D';
import { EmojiStatus, Emojis, ErrorValue, decodeEmojiStatus, decodeEmojis, decodeErrorValue, encodeEmojiStatus, encodeEmojis, encodeErrorValue } from './TdTypes_E';
import { File, FormattedText, decodeFile, decodeFormattedText, encodeFile, encodeFormattedText } from './TdTypes_F';
import { Gift, GiftSettings, GiveawayParameters, decodeGift, decodeGiftSettings, decodeGiveawayParameters, encodeGift, encodeGiftSettings, encodeGiveawayParameters } from './TdTypes_G';
import { InputBusinessStartPage, InputPassportElementError, InputRichMessage, decodeInputBusinessStartPage, decodeInputPassportElementError, decodeInputRichMessage, encodeInputBusinessStartPage, encodeInputPassportElementError, encodeInputRichMessage } from './TdTypes_I';
import { KeyboardButton, decodeKeyboardButton, encodeKeyboardButton } from './TdTypes_K';
import { LabeledPricePart, LanguagePackInfo, LanguagePackString, Location, LocationAddress, decodeLabeledPricePart, decodeLanguagePackInfo, decodeLanguagePackString, decodeLocation, decodeLocationAddress, encodeLabeledPricePart, encodeLanguagePackInfo, encodeLanguagePackString, encodeLocation, encodeLocationAddress } from './TdTypes_L';
import { MaskPosition, Message, MessageAutoDeleteTime, MessageSendOptions, Minithumbnail, decodeMaskPosition, decodeMessage, decodeMessageAutoDeleteTime, decodeMessageSendOptions, decodeMinithumbnail, encodeMaskPosition, encodeMessage, encodeMessageAutoDeleteTime, encodeMessageSendOptions, encodeMinithumbnail } from './TdTypes_M';
import { NewChatPrivacySettings, decodeNewChatPrivacySettings, encodeNewChatPrivacySettings } from './TdTypes_N';
import { Outline, decodeOutline, encodeOutline } from './TdTypes_O';
import { PhoneNumberAuthenticationSettings, Photo, ProductInfo, decodePhoneNumberAuthenticationSettings, decodePhoto, decodeProductInfo, encodePhoneNumberAuthenticationSettings, encodePhoto, encodeProductInfo } from './TdTypes_P';
import { ReactionNotificationSettings, ReadDatePrivacySettings, RestrictionInfo, decodeReactionNotificationSettings, decodeReadDatePrivacySettings, decodeRestrictionInfo, encodeReactionNotificationSettings, encodeReadDatePrivacySettings, encodeRestrictionInfo } from './TdTypes_R';
import { TargetChatTypes, Thumbnail, decodeTargetChatTypes, decodeThumbnail, encodeTargetChatTypes, encodeThumbnail } from './TdTypes_T';
import { UpgradedGift, UserPrivacySettingRules, Usernames, decodeUpgradedGift, decodeUserPrivacySettingRules, decodeUsernames, encodeUpgradedGift, encodeUserPrivacySettingRules, encodeUsernames } from './TdTypes_U';
import { Venue, VerificationStatus, Video, decodeVenue, decodeVerificationStatus, decodeVideo, encodeVenue, encodeVerificationStatus, encodeVideo } from './TdTypes_V';
import { TdActiveStoryState, TdAffiliateProgramSortOrder, TdAffiliateType, TdAutosaveSettingsScope, TdBackgroundType, TdBlockList, TdBotCommandScope, TdCallProblem, TdCanPostStoryResult, TdChatAction, TdChatAvailableReactions, TdChatList, TdChatMemberStatus, TdChatMembersFilter, TdFileType, TdGiftForResaleOrder, TdGiftResalePrice, TdInputBackground, TdInputCall, TdInputChatPhoto, TdInputChatTheme, TdInputCredentials, TdInputFile, TdInputInlineQueryResult, TdInputInvoice, TdInputMessageContent, TdInputMessageReplyTo, TdInputPassportElement, TdJsonValue, TdKeyboardButtonSource, TdLogStream, TdMessageContent, TdMessageSender, TdMessageTopic, TdNetworkType, TdNotificationSettingsScope, TdOptionValue, TdPaidMedia, TdPaidReactionType, TdPassportElementType, TdPhoneNumberCodeType, TdProfileTab, TdReactionType, TdReplyMarkup, TdRevenueWithdrawalState, TdSavedMessagesTopicType, TdSearchChatTypeFilter, TdSearchMessagesChatTypeFilter, TdSearchMessagesFilter, TdSecretChatState, TdSessionDeviceType, TdStarSubscriptionType, TdStarTransactionType, TdStatisticalGraph, TdStickerFormat, TdStickerFullType, TdStickerType, TdStoreTransaction, TdStoryAreaType, TdStoryContent, TdStoryInteractionType, TdStoryList, TdStoryOrigin, TdStoryPrivacySettings, TdSuggestedPostPrice, TdSuggestedPostState, TdUpgradedGiftAttributeId, TdUserPrivacySetting, decodeTdActiveStoryState, decodeTdAffiliateProgramSortOrder, decodeTdAffiliateType, decodeTdAutosaveSettingsScope, decodeTdBackgroundType, decodeTdBlockList, decodeTdBotCommandScope, decodeTdCallProblem, decodeTdCanPostStoryResult, decodeTdChatAction, decodeTdChatAvailableReactions, decodeTdChatList, decodeTdChatMemberStatus, decodeTdChatMembersFilter, decodeTdFileType, decodeTdGiftForResaleOrder, decodeTdGiftResalePrice, decodeTdInputBackground, decodeTdInputCall, decodeTdInputChatPhoto, decodeTdInputChatTheme, decodeTdInputCredentials, decodeTdInputFile, decodeTdInputInlineQueryResult, decodeTdInputInvoice, decodeTdInputMessageContent, decodeTdInputMessageReplyTo, decodeTdInputPassportElement, decodeTdJsonValue, decodeTdKeyboardButtonSource, decodeTdLogStream, decodeTdMessageContent, decodeTdMessageSender, decodeTdMessageTopic, decodeTdNetworkType, decodeTdNotificationSettingsScope, decodeTdOptionValue, decodeTdPaidMedia, decodeTdPaidReactionType, decodeTdPassportElementType, decodeTdPhoneNumberCodeType, decodeTdProfileTab, decodeTdReactionType, decodeTdReplyMarkup, decodeTdRevenueWithdrawalState, decodeTdSavedMessagesTopicType, decodeTdSearchChatTypeFilter, decodeTdSearchMessagesChatTypeFilter, decodeTdSearchMessagesFilter, decodeTdSecretChatState, decodeTdSessionDeviceType, decodeTdStarSubscriptionType, decodeTdStarTransactionType, decodeTdStatisticalGraph, decodeTdStickerFormat, decodeTdStickerFullType, decodeTdStickerType, decodeTdStoreTransaction, decodeTdStoryAreaType, decodeTdStoryContent, decodeTdStoryInteractionType, decodeTdStoryList, decodeTdStoryOrigin, decodeTdStoryPrivacySettings, decodeTdSuggestedPostPrice, decodeTdSuggestedPostState, decodeTdUpgradedGiftAttributeId, decodeTdUserPrivacySetting, encodeTdActiveStoryState, encodeTdAffiliateProgramSortOrder, encodeTdAffiliateType, encodeTdAutosaveSettingsScope, encodeTdBackgroundType, encodeTdBlockList, encodeTdBotCommandScope, encodeTdCallProblem, encodeTdCanPostStoryResult, encodeTdChatAction, encodeTdChatAvailableReactions, encodeTdChatList, encodeTdChatMemberStatus, encodeTdChatMembersFilter, encodeTdFileType, encodeTdGiftForResaleOrder, encodeTdGiftResalePrice, encodeTdInputBackground, encodeTdInputCall, encodeTdInputChatPhoto, encodeTdInputChatTheme, encodeTdInputCredentials, encodeTdInputFile, encodeTdInputInlineQueryResult, encodeTdInputInvoice, encodeTdInputMessageContent, encodeTdInputMessageReplyTo, encodeTdInputPassportElement, encodeTdJsonValue, encodeTdKeyboardButtonSource, encodeTdLogStream, encodeTdMessageContent, encodeTdMessageSender, encodeTdMessageTopic, encodeTdNetworkType, encodeTdNotificationSettingsScope, encodeTdOptionValue, encodeTdPaidMedia, encodeTdPaidReactionType, encodeTdPassportElementType, encodeTdPhoneNumberCodeType, encodeTdProfileTab, encodeTdReactionType, encodeTdReplyMarkup, encodeTdRevenueWithdrawalState, encodeTdSavedMessagesTopicType, encodeTdSearchChatTypeFilter, encodeTdSearchMessagesChatTypeFilter, encodeTdSearchMessagesFilter, encodeTdSecretChatState, encodeTdSessionDeviceType, encodeTdStarSubscriptionType, encodeTdStarTransactionType, encodeTdStatisticalGraph, encodeTdStickerFormat, encodeTdStickerFullType, encodeTdStickerType, encodeTdStoreTransaction, encodeTdStoryAreaType, encodeTdStoryContent, encodeTdStoryInteractionType, encodeTdStoryList, encodeTdStoryOrigin, encodeTdStoryPrivacySettings, encodeTdSuggestedPostPrice, encodeTdSuggestedPostState, encodeTdUpgradedGiftAttributeId, encodeTdUserPrivacySetting } from './TdUnions';

/** saveApplicationLogEvent = Ok — Saves application log event on the server. Can be called before authorization */
export class SaveApplicationLogEvent {
  type: 'saveApplicationLogEvent' = 'saveApplicationLogEvent';
  extra?: string;
  /** type: string — Event type */
  type_: string = '';
  /** chat_id: int53 — Optional chat identifier, associated with the event */
  chat_id: number = 0;
  /** data: JsonValue — The log event data */
  data: TdJsonValue | null = null;
}

/** Decode a TDLib `saveApplicationLogEvent` object; null input -> null. */
export function decodeSaveApplicationLogEvent(json: TdJsonRaw): SaveApplicationLogEvent | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SaveApplicationLogEvent = new SaveApplicationLogEvent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = asString(record['type']);
  out.chat_id = asNumber(record['chat_id']);
  out.data = decodeTdJsonValue(record['data']);
  return out;
}

/** Encode a `saveApplicationLogEvent` into TDLib JSON wire shape. */
export function encodeSaveApplicationLogEvent(value: SaveApplicationLogEvent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'saveApplicationLogEvent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_;
  out['chat_id'] = value.chat_id;
  out['data'] = value.data === null ? null : encodeTdJsonValue(value.data);
  return out;
}

/** savePreparedInlineMessage = PreparedInlineMessageId — Saves an inline message to be sent by the given user; for bots only */
export class SavePreparedInlineMessage {
  type: 'savePreparedInlineMessage' = 'savePreparedInlineMessage';
  extra?: string;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
  /** result: InputInlineQueryResult — The description of the message */
  result: TdInputInlineQueryResult | null = null;
  /** chat_types: targetChatTypes — Types of the chats to which the message can be sent */
  chat_types: TargetChatTypes | null = null;
}

/** Decode a TDLib `savePreparedInlineMessage` object; null input -> null. */
export function decodeSavePreparedInlineMessage(json: TdJsonRaw): SavePreparedInlineMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SavePreparedInlineMessage = new SavePreparedInlineMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.result = decodeTdInputInlineQueryResult(record['result']);
  out.chat_types = decodeTargetChatTypes(record['chat_types']);
  return out;
}

/** Encode a `savePreparedInlineMessage` into TDLib JSON wire shape. */
export function encodeSavePreparedInlineMessage(value: SavePreparedInlineMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'savePreparedInlineMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['result'] = value.result === null ? null : encodeTdInputInlineQueryResult(value.result);
  out['chat_types'] = value.chat_types === null ? null : encodeTargetChatTypes(value.chat_types);
  return out;
}

/** savePreparedKeyboardButton = Text — Saves a keyboard button to be shown to the given user; for bots only */
export class SavePreparedKeyboardButton {
  type: 'savePreparedKeyboardButton' = 'savePreparedKeyboardButton';
  extra?: string;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
  /** button: keyboardButton — The button; must be of the type keyboardButtonTypeRequestUsers, keyboardButtonTypeRequestChat, or keyboardButtonTypeRequestManagedBot */
  button: KeyboardButton | null = null;
}

/** Decode a TDLib `savePreparedKeyboardButton` object; null input -> null. */
export function decodeSavePreparedKeyboardButton(json: TdJsonRaw): SavePreparedKeyboardButton | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SavePreparedKeyboardButton = new SavePreparedKeyboardButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.button = decodeKeyboardButton(record['button']);
  return out;
}

/** Encode a `savePreparedKeyboardButton` into TDLib JSON wire shape. */
export function encodeSavePreparedKeyboardButton(value: SavePreparedKeyboardButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'savePreparedKeyboardButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['button'] = value.button === null ? null : encodeKeyboardButton(value.button);
  return out;
}

/** savedCredentials = SavedCredentials — Contains information about saved payment credentials */
export class SavedCredentials {
  type: 'savedCredentials' = 'savedCredentials';
  extra?: string;
  /** id: string — Unique identifier of the saved credentials */
  id: string = '';
  /** title: string — Title of the saved credentials */
  title: string = '';
}

/** Decode a TDLib `savedCredentials` object; null input -> null. */
export function decodeSavedCredentials(json: TdJsonRaw): SavedCredentials | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SavedCredentials = new SavedCredentials();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.title = asString(record['title']);
  return out;
}

/** Encode a `savedCredentials` into TDLib JSON wire shape. */
export function encodeSavedCredentials(value: SavedCredentials): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'savedCredentials';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  return out;
}

/** savedMessagesTag = SavedMessagesTag — Represents a tag used in Saved Messages or a Saved Messages topic */
export class SavedMessagesTag {
  type: 'savedMessagesTag' = 'savedMessagesTag';
  extra?: string;
  /** tag: ReactionType — The tag */
  tag: TdReactionType | null = null;
  /** label: string — Label of the tag; 0-12 characters. Always empty if the tag is returned for a Saved Messages topic */
  label: string = '';
  /** count: int32 — Number of times the tag was used; may be 0 if the tag has non-empty label */
  count: number = 0;
}

/** Decode a TDLib `savedMessagesTag` object; null input -> null. */
export function decodeSavedMessagesTag(json: TdJsonRaw): SavedMessagesTag | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SavedMessagesTag = new SavedMessagesTag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.tag = decodeTdReactionType(record['tag']);
  out.label = asString(record['label']);
  out.count = asNumber(record['count']);
  return out;
}

/** Encode a `savedMessagesTag` into TDLib JSON wire shape. */
export function encodeSavedMessagesTag(value: SavedMessagesTag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'savedMessagesTag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['tag'] = value.tag === null ? null : encodeTdReactionType(value.tag);
  out['label'] = value.label;
  out['count'] = value.count;
  return out;
}

/** savedMessagesTags = SavedMessagesTags — Contains a list of tags used in Saved Messages */
export class SavedMessagesTags {
  type: 'savedMessagesTags' = 'savedMessagesTags';
  extra?: string;
  /** tags: vector<savedMessagesTag> — List of tags */
  tags: SavedMessagesTag[] = [];
}

/** Decode a TDLib `savedMessagesTags` object; null input -> null. */
export function decodeSavedMessagesTags(json: TdJsonRaw): SavedMessagesTags | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SavedMessagesTags = new SavedMessagesTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.tags = decodeVector(record['tags'], (item: TdJsonRaw): SavedMessagesTag => decodeSavedMessagesTag(item) as SavedMessagesTag);
  return out;
}

/** Encode a `savedMessagesTags` into TDLib JSON wire shape. */
export function encodeSavedMessagesTags(value: SavedMessagesTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'savedMessagesTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['tags'] = encodeVector(value.tags, (item: SavedMessagesTag | null): TdJsonRaw => item === null ? null : encodeSavedMessagesTag(item));
  return out;
}

/** savedMessagesTopic = SavedMessagesTopic — Contains information about a Saved Messages topic */
export class SavedMessagesTopic {
  type: 'savedMessagesTopic' = 'savedMessagesTopic';
  extra?: string;
  /** id: int53 — Unique topic identifier */
  id: number = 0;
  /** type: SavedMessagesTopicType — Type of the topic */
  type_: TdSavedMessagesTopicType | null = null;
  /** is_pinned: Bool — True, if the topic is pinned */
  is_pinned: boolean = false;
  /** order: int64 — A parameter used to determine order of the topic in the topic list. Topics must be sorted by the order in descending order */
  order: string = '';
  /** last_message: message — Last message in the topic; may be null if none or unknown */
  last_message: Message | null = null;
  /** draft_message: draftMessage — A draft of a message in the topic; may be null if none */
  draft_message: DraftMessage | null = null;
}

/** Decode a TDLib `savedMessagesTopic` object; null input -> null. */
export function decodeSavedMessagesTopic(json: TdJsonRaw): SavedMessagesTopic | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SavedMessagesTopic = new SavedMessagesTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.type_ = decodeTdSavedMessagesTopicType(record['type']);
  out.is_pinned = asBool(record['is_pinned']);
  out.order = asInt64(record['order']);
  out.last_message = decodeMessage(record['last_message']);
  out.draft_message = decodeDraftMessage(record['draft_message']);
  return out;
}

/** Encode a `savedMessagesTopic` into TDLib JSON wire shape. */
export function encodeSavedMessagesTopic(value: SavedMessagesTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'savedMessagesTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['type'] = value.type_ === null ? null : encodeTdSavedMessagesTopicType(value.type_);
  out['is_pinned'] = value.is_pinned;
  out['order'] = value.order;
  out['last_message'] = value.last_message === null ? null : encodeMessage(value.last_message);
  out['draft_message'] = value.draft_message === null ? null : encodeDraftMessage(value.draft_message);
  return out;
}

/** savedMessagesTopicTypeAuthorHidden = SavedMessagesTopicType — Topic containing messages forwarded from a user with hidden privacy */
export class SavedMessagesTopicTypeAuthorHidden {
  type: 'savedMessagesTopicTypeAuthorHidden' = 'savedMessagesTopicTypeAuthorHidden';
  extra?: string;
}

/** Decode a TDLib `savedMessagesTopicTypeAuthorHidden` object; null input -> null. */
export function decodeSavedMessagesTopicTypeAuthorHidden(json: TdJsonRaw): SavedMessagesTopicTypeAuthorHidden | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SavedMessagesTopicTypeAuthorHidden = new SavedMessagesTopicTypeAuthorHidden();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `savedMessagesTopicTypeAuthorHidden` into TDLib JSON wire shape. */
export function encodeSavedMessagesTopicTypeAuthorHidden(value: SavedMessagesTopicTypeAuthorHidden): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'savedMessagesTopicTypeAuthorHidden';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** savedMessagesTopicTypeMyNotes = SavedMessagesTopicType — Topic containing messages sent by the current user of forwarded from an unknown chat */
export class SavedMessagesTopicTypeMyNotes {
  type: 'savedMessagesTopicTypeMyNotes' = 'savedMessagesTopicTypeMyNotes';
  extra?: string;
}

/** Decode a TDLib `savedMessagesTopicTypeMyNotes` object; null input -> null. */
export function decodeSavedMessagesTopicTypeMyNotes(json: TdJsonRaw): SavedMessagesTopicTypeMyNotes | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SavedMessagesTopicTypeMyNotes = new SavedMessagesTopicTypeMyNotes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `savedMessagesTopicTypeMyNotes` into TDLib JSON wire shape. */
export function encodeSavedMessagesTopicTypeMyNotes(value: SavedMessagesTopicTypeMyNotes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'savedMessagesTopicTypeMyNotes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** savedMessagesTopicTypeSavedFromChat = SavedMessagesTopicType — Topic containing messages forwarded from a specific chat */
export class SavedMessagesTopicTypeSavedFromChat {
  type: 'savedMessagesTopicTypeSavedFromChat' = 'savedMessagesTopicTypeSavedFromChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
}

/** Decode a TDLib `savedMessagesTopicTypeSavedFromChat` object; null input -> null. */
export function decodeSavedMessagesTopicTypeSavedFromChat(json: TdJsonRaw): SavedMessagesTopicTypeSavedFromChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SavedMessagesTopicTypeSavedFromChat = new SavedMessagesTopicTypeSavedFromChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `savedMessagesTopicTypeSavedFromChat` into TDLib JSON wire shape. */
export function encodeSavedMessagesTopicTypeSavedFromChat(value: SavedMessagesTopicTypeSavedFromChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'savedMessagesTopicTypeSavedFromChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** scopeAutosaveSettings = ScopeAutosaveSettings — Contains autosave settings for an autosave settings scope */
export class ScopeAutosaveSettings {
  type: 'scopeAutosaveSettings' = 'scopeAutosaveSettings';
  extra?: string;
  /** autosave_photos: Bool — True, if photo autosave is enabled */
  autosave_photos: boolean = false;
  /** autosave_videos: Bool — True, if video autosave is enabled */
  autosave_videos: boolean = false;
  /** max_video_file_size: int53 — The maximum size of a video file to be autosaved, in bytes; 512 KB - 4000 MB */
  max_video_file_size: number = 0;
}

/** Decode a TDLib `scopeAutosaveSettings` object; null input -> null. */
export function decodeScopeAutosaveSettings(json: TdJsonRaw): ScopeAutosaveSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ScopeAutosaveSettings = new ScopeAutosaveSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.autosave_photos = asBool(record['autosave_photos']);
  out.autosave_videos = asBool(record['autosave_videos']);
  out.max_video_file_size = asNumber(record['max_video_file_size']);
  return out;
}

/** Encode a `scopeAutosaveSettings` into TDLib JSON wire shape. */
export function encodeScopeAutosaveSettings(value: ScopeAutosaveSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'scopeAutosaveSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['autosave_photos'] = value.autosave_photos;
  out['autosave_videos'] = value.autosave_videos;
  out['max_video_file_size'] = value.max_video_file_size;
  return out;
}

/** scopeNotificationSettings = ScopeNotificationSettings — Contains information about notification settings for several chats */
export class ScopeNotificationSettings {
  type: 'scopeNotificationSettings' = 'scopeNotificationSettings';
  extra?: string;
  /** mute_for: int32 — Time left before notifications will be unmuted, in seconds */
  mute_for: number = 0;
  /** sound_id: int64 — Identifier of the notification sound to be played; 0 if sound is disabled; pass -1 to use the app-dependent default sound */
  sound_id: string = '';
  /** show_preview: Bool — True, if message content must be displayed in notifications */
  show_preview: boolean = false;
  /** use_default_mute_stories: Bool — If true, story notifications are received only for the first 5 chats from topChatCategoryUsers regardless of the value of mute_stories */
  use_default_mute_stories: boolean = false;
  /** mute_stories: Bool — True, if story notifications are disabled */
  mute_stories: boolean = false;
  /** story_sound_id: int64 — Identifier of the notification sound to be played for stories; 0 if sound is disabled; pass -1 to use the app-dependent default sound */
  story_sound_id: string = '';
  /** show_story_poster: Bool — True, if the chat that posted a story must be displayed in notifications */
  show_story_poster: boolean = false;
  /** disable_pinned_message_notifications: Bool — True, if notifications for incoming pinned messages will be created as for an ordinary unread message */
  disable_pinned_message_notifications: boolean = false;
  /** disable_mention_notifications: Bool — True, if notifications for messages with mentions will be created as for an ordinary unread message */
  disable_mention_notifications: boolean = false;
}

/** Decode a TDLib `scopeNotificationSettings` object; null input -> null. */
export function decodeScopeNotificationSettings(json: TdJsonRaw): ScopeNotificationSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ScopeNotificationSettings = new ScopeNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.mute_for = asNumber(record['mute_for']);
  out.sound_id = asInt64(record['sound_id']);
  out.show_preview = asBool(record['show_preview']);
  out.use_default_mute_stories = asBool(record['use_default_mute_stories']);
  out.mute_stories = asBool(record['mute_stories']);
  out.story_sound_id = asInt64(record['story_sound_id']);
  out.show_story_poster = asBool(record['show_story_poster']);
  out.disable_pinned_message_notifications = asBool(record['disable_pinned_message_notifications']);
  out.disable_mention_notifications = asBool(record['disable_mention_notifications']);
  return out;
}

/** Encode a `scopeNotificationSettings` into TDLib JSON wire shape. */
export function encodeScopeNotificationSettings(value: ScopeNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'scopeNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['mute_for'] = value.mute_for;
  out['sound_id'] = value.sound_id;
  out['show_preview'] = value.show_preview;
  out['use_default_mute_stories'] = value.use_default_mute_stories;
  out['mute_stories'] = value.mute_stories;
  out['story_sound_id'] = value.story_sound_id;
  out['show_story_poster'] = value.show_story_poster;
  out['disable_pinned_message_notifications'] = value.disable_pinned_message_notifications;
  out['disable_mention_notifications'] = value.disable_mention_notifications;
  return out;
}

/** searchAffiliatePrograms = FoundAffiliatePrograms — Searches affiliate programs that can be connected to the given affiliate */
export class SearchAffiliatePrograms {
  type: 'searchAffiliatePrograms' = 'searchAffiliatePrograms';
  extra?: string;
  /** affiliate: AffiliateType — The affiliate for which affiliate programs are searched for */
  affiliate: TdAffiliateType | null = null;
  /** sort_order: AffiliateProgramSortOrder — Sort order for the results */
  sort_order: TdAffiliateProgramSortOrder | null = null;
  /** offset: string — Offset of the first affiliate program to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of affiliate programs to return */
  limit: number = 0;
}

/** Decode a TDLib `searchAffiliatePrograms` object; null input -> null. */
export function decodeSearchAffiliatePrograms(json: TdJsonRaw): SearchAffiliatePrograms | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchAffiliatePrograms = new SearchAffiliatePrograms();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.affiliate = decodeTdAffiliateType(record['affiliate']);
  out.sort_order = decodeTdAffiliateProgramSortOrder(record['sort_order']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchAffiliatePrograms` into TDLib JSON wire shape. */
export function encodeSearchAffiliatePrograms(value: SearchAffiliatePrograms): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchAffiliatePrograms';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['affiliate'] = value.affiliate === null ? null : encodeTdAffiliateType(value.affiliate);
  out['sort_order'] = value.sort_order === null ? null : encodeTdAffiliateProgramSortOrder(value.sort_order);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** searchBackground = Background — Searches for a background by its name */
export class SearchBackground {
  type: 'searchBackground' = 'searchBackground';
  extra?: string;
  /** name: string — The name of the background */
  name: string = '';
}

/** Decode a TDLib `searchBackground` object; null input -> null. */
export function decodeSearchBackground(json: TdJsonRaw): SearchBackground | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchBackground = new SearchBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `searchBackground` into TDLib JSON wire shape. */
export function encodeSearchBackground(value: SearchBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** searchCallMessages = FoundMessages — Searches for call and group call messages. Returns the results in reverse chronological order (i.e., in order of decreasing message_id). For optimal performance, the number of returned messages is cho... */
export class SearchCallMessages {
  type: 'searchCallMessages' = 'searchCallMessages';
  extra?: string;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of messages to be returned; up to 100. For optimal performance, the number of returned messages is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
  /** only_missed: Bool — Pass true to search only for messages with missed/declined calls */
  only_missed: boolean = false;
}

/** Decode a TDLib `searchCallMessages` object; null input -> null. */
export function decodeSearchCallMessages(json: TdJsonRaw): SearchCallMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchCallMessages = new SearchCallMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  out.only_missed = asBool(record['only_missed']);
  return out;
}

/** Encode a `searchCallMessages` into TDLib JSON wire shape. */
export function encodeSearchCallMessages(value: SearchCallMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchCallMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  out['only_missed'] = value.only_missed;
  return out;
}

/** searchChatAffiliateProgram = Chat — Searches a chat with an affiliate program. Returns the chat if found and the program is active */
export class SearchChatAffiliateProgram {
  type: 'searchChatAffiliateProgram' = 'searchChatAffiliateProgram';
  extra?: string;
  /** username: string — Username of the chat */
  username: string = '';
  /** referrer: string — The referrer from an internalLinkTypeChatAffiliateProgram link */
  referrer: string = '';
}

/** Decode a TDLib `searchChatAffiliateProgram` object; null input -> null. */
export function decodeSearchChatAffiliateProgram(json: TdJsonRaw): SearchChatAffiliateProgram | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchChatAffiliateProgram = new SearchChatAffiliateProgram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  out.referrer = asString(record['referrer']);
  return out;
}

/** Encode a `searchChatAffiliateProgram` into TDLib JSON wire shape. */
export function encodeSearchChatAffiliateProgram(value: SearchChatAffiliateProgram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchChatAffiliateProgram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  out['referrer'] = value.referrer;
  return out;
}

/** searchChatMembers = ChatMembers — Searches for a specified query in the first name, last name and usernames of the members of a specified chat. Requires administrator rights if the chat is a channel */
export class SearchChatMembers {
  type: 'searchChatMembers' = 'searchChatMembers';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** query: string — Query to search for */
  query: string = '';
  /** limit: int32 — The maximum number of users to be returned; up to 200 */
  limit: number = 0;
  /** filter: ChatMembersFilter — The type of users to search for; pass null to search among all chat members */
  filter: TdChatMembersFilter | null = null;
}

/** Decode a TDLib `searchChatMembers` object; null input -> null. */
export function decodeSearchChatMembers(json: TdJsonRaw): SearchChatMembers | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchChatMembers = new SearchChatMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.query = asString(record['query']);
  out.limit = asNumber(record['limit']);
  out.filter = decodeTdChatMembersFilter(record['filter']);
  return out;
}

/** Encode a `searchChatMembers` into TDLib JSON wire shape. */
export function encodeSearchChatMembers(value: SearchChatMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchChatMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['query'] = value.query;
  out['limit'] = value.limit;
  out['filter'] = value.filter === null ? null : encodeTdChatMembersFilter(value.filter);
  return out;
}

/** searchChatMessages = FoundChatMessages — Searches for messages with given words in the chat. Returns the results in reverse chronological order, i.e. in order of decreasing message_id. Cannot be used in secret chats with a non-empty quer... */
export class SearchChatMessages {
  type: 'searchChatMessages' = 'searchChatMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in which to search messages */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Pass topic identifier to search messages only in specific topic; pass null to search for messages in all topics */
  topic_id: TdMessageTopic | null = null;
  /** query: string — Query to search for */
  query: string = '';
  /** sender_id: MessageSender — Identifier of the sender of messages to search for; pass null to search for messages from any sender. Not supported in secret chats */
  sender_id: TdMessageSender | null = null;
  /** from_message_id: int53 — Identifier of the message starting from which history must be fetched; use 0 to get results from the last message */
  from_message_id: number = 0;
  /** offset: int32 — Specify 0 to get results from exactly the message from_message_id or a negative number to get the specified message and some newer messages */
  offset: number = 0;
  /** limit: int32 — The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, then the limit must be greater than -offset. */
  limit: number = 0;
  /** filter: SearchMessagesFilter — Additional filter for messages to search; pass null to search for all messages */
  filter: TdSearchMessagesFilter | null = null;
}

/** Decode a TDLib `searchChatMessages` object; null input -> null. */
export function decodeSearchChatMessages(json: TdJsonRaw): SearchChatMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchChatMessages = new SearchChatMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.query = asString(record['query']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.from_message_id = asNumber(record['from_message_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  out.filter = decodeTdSearchMessagesFilter(record['filter']);
  return out;
}

/** Encode a `searchChatMessages` into TDLib JSON wire shape. */
export function encodeSearchChatMessages(value: SearchChatMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchChatMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['query'] = value.query;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['from_message_id'] = value.from_message_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  out['filter'] = value.filter === null ? null : encodeTdSearchMessagesFilter(value.filter);
  return out;
}

/** searchChatRecentLocationMessages = Messages — Returns information about the recent live locations of chat members that were sent to the chat. Returns at most one live location message per user */
export class SearchChatRecentLocationMessages {
  type: 'searchChatRecentLocationMessages' = 'searchChatRecentLocationMessages';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** limit: int32 — The maximum number of messages to be returned */
  limit: number = 0;
}

/** Decode a TDLib `searchChatRecentLocationMessages` object; null input -> null. */
export function decodeSearchChatRecentLocationMessages(json: TdJsonRaw): SearchChatRecentLocationMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchChatRecentLocationMessages = new SearchChatRecentLocationMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchChatRecentLocationMessages` into TDLib JSON wire shape. */
export function encodeSearchChatRecentLocationMessages(value: SearchChatRecentLocationMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchChatRecentLocationMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['limit'] = value.limit;
  return out;
}

/** searchChatTypeFilterBot = SearchChatTypeFilter — Returns only private chats with bots */
export class SearchChatTypeFilterBot {
  type: 'searchChatTypeFilterBot' = 'searchChatTypeFilterBot';
  extra?: string;
}

/** Decode a TDLib `searchChatTypeFilterBot` object; null input -> null. */
export function decodeSearchChatTypeFilterBot(json: TdJsonRaw): SearchChatTypeFilterBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchChatTypeFilterBot = new SearchChatTypeFilterBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchChatTypeFilterBot` into TDLib JSON wire shape. */
export function encodeSearchChatTypeFilterBot(value: SearchChatTypeFilterBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchChatTypeFilterBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchChatTypeFilterChannel = SearchChatTypeFilter — Returns only channel chats */
export class SearchChatTypeFilterChannel {
  type: 'searchChatTypeFilterChannel' = 'searchChatTypeFilterChannel';
  extra?: string;
}

/** Decode a TDLib `searchChatTypeFilterChannel` object; null input -> null. */
export function decodeSearchChatTypeFilterChannel(json: TdJsonRaw): SearchChatTypeFilterChannel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchChatTypeFilterChannel = new SearchChatTypeFilterChannel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchChatTypeFilterChannel` into TDLib JSON wire shape. */
export function encodeSearchChatTypeFilterChannel(value: SearchChatTypeFilterChannel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchChatTypeFilterChannel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchChats = Chats — Searches for the specified query in the title and username of already known chats. This is an offline method. Returns chats in the order seen in the main chat list */
export class SearchChats {
  type: 'searchChats' = 'searchChats';
  extra?: string;
  /** query: string — Query to search for. If the query is empty, returns up to 50 recently found chats */
  query: string = '';
  /** type_filter: SearchChatTypeFilter — Additional filter for type of the chats to be returned; pass null to search for chats of all types */
  type_filter: TdSearchChatTypeFilter | null = null;
  /** limit: int32 — The maximum number of chats to be returned */
  limit: number = 0;
}

/** Decode a TDLib `searchChats` object; null input -> null. */
export function decodeSearchChats(json: TdJsonRaw): SearchChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchChats = new SearchChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.type_filter = decodeTdSearchChatTypeFilter(record['type_filter']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchChats` into TDLib JSON wire shape. */
export function encodeSearchChats(value: SearchChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['type_filter'] = value.type_filter === null ? null : encodeTdSearchChatTypeFilter(value.type_filter);
  out['limit'] = value.limit;
  return out;
}

/** searchChatsOnServer = Chats — Searches for the specified query in the title and username of already known chats via request to the server. Returns chats in the order seen in the main chat list */
export class SearchChatsOnServer {
  type: 'searchChatsOnServer' = 'searchChatsOnServer';
  extra?: string;
  /** query: string — Query to search for */
  query: string = '';
  /** type_filter: SearchChatTypeFilter — Additional filter for type of the chats to be returned; pass null to search for chats of all types */
  type_filter: TdSearchChatTypeFilter | null = null;
  /** limit: int32 — The maximum number of chats to be returned */
  limit: number = 0;
}

/** Decode a TDLib `searchChatsOnServer` object; null input -> null. */
export function decodeSearchChatsOnServer(json: TdJsonRaw): SearchChatsOnServer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchChatsOnServer = new SearchChatsOnServer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.type_filter = decodeTdSearchChatTypeFilter(record['type_filter']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchChatsOnServer` into TDLib JSON wire shape. */
export function encodeSearchChatsOnServer(value: SearchChatsOnServer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchChatsOnServer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['type_filter'] = value.type_filter === null ? null : encodeTdSearchChatTypeFilter(value.type_filter);
  out['limit'] = value.limit;
  return out;
}

/** searchContacts = Users — Searches for the specified query in the first names, last names and usernames of the known user contacts */
export class SearchContacts {
  type: 'searchContacts' = 'searchContacts';
  extra?: string;
  /** query: string — Query to search for; may be empty to return all contacts */
  query: string = '';
  /** limit: int32 — The maximum number of users to be returned */
  limit: number = 0;
}

/** Decode a TDLib `searchContacts` object; null input -> null. */
export function decodeSearchContacts(json: TdJsonRaw): SearchContacts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchContacts = new SearchContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchContacts` into TDLib JSON wire shape. */
export function encodeSearchContacts(value: SearchContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['limit'] = value.limit;
  return out;
}

/** searchEmojis = EmojiKeywords — Searches for emojis by keywords. Supported only if the file database is enabled. Order of results is unspecified */
export class SearchEmojis {
  type: 'searchEmojis' = 'searchEmojis';
  extra?: string;
  /** text: string — Text to search for */
  text: string = '';
  /** input_language_codes: vector<string> — List of possible IETF language tags of the user's input language; may be empty if unknown */
  input_language_codes: string[] = [];
}

/** Decode a TDLib `searchEmojis` object; null input -> null. */
export function decodeSearchEmojis(json: TdJsonRaw): SearchEmojis | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchEmojis = new SearchEmojis();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.input_language_codes = decodeVector(record['input_language_codes'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `searchEmojis` into TDLib JSON wire shape. */
export function encodeSearchEmojis(value: SearchEmojis): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchEmojis';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['input_language_codes'] = encodeVector(value.input_language_codes, (item: string): TdJsonRaw => item);
  return out;
}

/** searchFileDownloads = FoundFileDownloads — Searches for files in the file download list or recently downloaded files from the list */
export class SearchFileDownloads {
  type: 'searchFileDownloads' = 'searchFileDownloads';
  extra?: string;
  /** query: string — Query to search for; may be empty to return all downloaded files */
  query: string = '';
  /** only_active: Bool — Pass true to search only for active downloads, including paused */
  only_active: boolean = false;
  /** only_completed: Bool — Pass true to search only for completed downloads */
  only_completed: boolean = false;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of files to be returned */
  limit: number = 0;
}

/** Decode a TDLib `searchFileDownloads` object; null input -> null. */
export function decodeSearchFileDownloads(json: TdJsonRaw): SearchFileDownloads | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchFileDownloads = new SearchFileDownloads();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.only_active = asBool(record['only_active']);
  out.only_completed = asBool(record['only_completed']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchFileDownloads` into TDLib JSON wire shape. */
export function encodeSearchFileDownloads(value: SearchFileDownloads): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchFileDownloads';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['only_active'] = value.only_active;
  out['only_completed'] = value.only_completed;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** searchGiftsForResale = GiftsForResale — Returns upgraded gifts that can be bought from other owners using sendResoldGift If none attributes of specific type are specified, then all values for this attribute type are allowed */
export class SearchGiftsForResale {
  type: 'searchGiftsForResale' = 'searchGiftsForResale';
  extra?: string;
  /** gift_id: int64 — Identifier of the regular gift that was upgraded to a unique gift */
  gift_id: string = '';
  /** order: GiftForResaleOrder — Order in which the results will be sorted */
  order: TdGiftForResaleOrder | null = null;
  /** for_crafting: Bool — Pass true to get only gifts suitable for crafting */
  for_crafting: boolean = false;
  /** for_stars: Bool — Pass true to get only gifts that can be bought using Telegram Stars */
  for_stars: boolean = false;
  /** attributes: vector<UpgradedGiftAttributeId> — Attributes used to filter received gifts. If multiple attributes of the same type are specified, then all of them are allowed. */
  attributes: TdUpgradedGiftAttributeId[] = [];
  /** offset: string — Offset of the first entry to return as received from the previous request with the same order and attributes; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of gifts to return */
  limit: number = 0;
}

/** Decode a TDLib `searchGiftsForResale` object; null input -> null. */
export function decodeSearchGiftsForResale(json: TdJsonRaw): SearchGiftsForResale | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchGiftsForResale = new SearchGiftsForResale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_id = asInt64(record['gift_id']);
  out.order = decodeTdGiftForResaleOrder(record['order']);
  out.for_crafting = asBool(record['for_crafting']);
  out.for_stars = asBool(record['for_stars']);
  out.attributes = decodeVector(record['attributes'], (item: TdJsonRaw): TdUpgradedGiftAttributeId => decodeTdUpgradedGiftAttributeId(item) as TdUpgradedGiftAttributeId);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchGiftsForResale` into TDLib JSON wire shape. */
export function encodeSearchGiftsForResale(value: SearchGiftsForResale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchGiftsForResale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_id'] = value.gift_id;
  out['order'] = value.order === null ? null : encodeTdGiftForResaleOrder(value.order);
  out['for_crafting'] = value.for_crafting;
  out['for_stars'] = value.for_stars;
  out['attributes'] = encodeVector(value.attributes, (item: TdUpgradedGiftAttributeId | null): TdJsonRaw => item === null ? null : encodeTdUpgradedGiftAttributeId(item));
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** searchHashtags = Hashtags — Searches for recently used hashtags by their prefix */
export class SearchHashtags {
  type: 'searchHashtags' = 'searchHashtags';
  extra?: string;
  /** prefix: string — Hashtag prefix to search for */
  prefix: string = '';
  /** limit: int32 — The maximum number of hashtags to be returned */
  limit: number = 0;
}

/** Decode a TDLib `searchHashtags` object; null input -> null. */
export function decodeSearchHashtags(json: TdJsonRaw): SearchHashtags | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchHashtags = new SearchHashtags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.prefix = asString(record['prefix']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchHashtags` into TDLib JSON wire shape. */
export function encodeSearchHashtags(value: SearchHashtags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchHashtags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['prefix'] = value.prefix;
  out['limit'] = value.limit;
  return out;
}

/** searchInstalledStickerSets = StickerSets — Searches for installed sticker sets by looking for specified query in their title and name */
export class SearchInstalledStickerSets {
  type: 'searchInstalledStickerSets' = 'searchInstalledStickerSets';
  extra?: string;
  /** sticker_type: StickerType — Type of the sticker sets to search for */
  sticker_type: TdStickerType | null = null;
  /** query: string — Query to search for */
  query: string = '';
  /** limit: int32 — The maximum number of sticker sets to return */
  limit: number = 0;
}

/** Decode a TDLib `searchInstalledStickerSets` object; null input -> null. */
export function decodeSearchInstalledStickerSets(json: TdJsonRaw): SearchInstalledStickerSets | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchInstalledStickerSets = new SearchInstalledStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.query = asString(record['query']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchInstalledStickerSets` into TDLib JSON wire shape. */
export function encodeSearchInstalledStickerSets(value: SearchInstalledStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchInstalledStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['query'] = value.query;
  out['limit'] = value.limit;
  return out;
}

/** searchMessages = FoundMessages — Searches for messages in all chats except secret chats. Returns the results in reverse chronological order (i.e., in order of decreasing (date, chat_id, message_id)). For optimal performance, the number o... */
export class SearchMessages {
  type: 'searchMessages' = 'searchMessages';
  extra?: string;
  /** chat_list: ChatList — Chat list in which to search messages; pass null to search in all chats regardless of their chat list. Only Main and Archive chat lists are supported */
  chat_list: TdChatList | null = null;
  /** query: string — Query to search for */
  query: string = '';
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of messages to be returned; up to 100. For optimal performance, the number of returned messages is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
  /** filter: SearchMessagesFilter — Additional filter for messages to search; pass null to search for all messages. Filters searchMessagesFilterMention, searchMessagesFilterUnreadMention, searchMessagesFilterUnreadReaction, */
  filter: TdSearchMessagesFilter | null = null;
  /** chat_type_filter: SearchMessagesChatTypeFilter — Additional filter for type of the chat of the searched messages; pass null to search for messages in all chats */
  chat_type_filter: TdSearchMessagesChatTypeFilter | null = null;
  /** min_date: int32 — If not 0, the minimum date of the messages to return */
  min_date: number = 0;
  /** max_date: int32 — If not 0, the maximum date of the messages to return */
  max_date: number = 0;
}

/** Decode a TDLib `searchMessages` object; null input -> null. */
export function decodeSearchMessages(json: TdJsonRaw): SearchMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessages = new SearchMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_list = decodeTdChatList(record['chat_list']);
  out.query = asString(record['query']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  out.filter = decodeTdSearchMessagesFilter(record['filter']);
  out.chat_type_filter = decodeTdSearchMessagesChatTypeFilter(record['chat_type_filter']);
  out.min_date = asNumber(record['min_date']);
  out.max_date = asNumber(record['max_date']);
  return out;
}

/** Encode a `searchMessages` into TDLib JSON wire shape. */
export function encodeSearchMessages(value: SearchMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  out['query'] = value.query;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  out['filter'] = value.filter === null ? null : encodeTdSearchMessagesFilter(value.filter);
  out['chat_type_filter'] = value.chat_type_filter === null ? null : encodeTdSearchMessagesChatTypeFilter(value.chat_type_filter);
  out['min_date'] = value.min_date;
  out['max_date'] = value.max_date;
  return out;
}

/** searchMessagesChatTypeFilterChannel = SearchMessagesChatTypeFilter — Returns only messages in channel chats */
export class SearchMessagesChatTypeFilterChannel {
  type: 'searchMessagesChatTypeFilterChannel' = 'searchMessagesChatTypeFilterChannel';
  extra?: string;
}

/** Decode a TDLib `searchMessagesChatTypeFilterChannel` object; null input -> null. */
export function decodeSearchMessagesChatTypeFilterChannel(json: TdJsonRaw): SearchMessagesChatTypeFilterChannel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesChatTypeFilterChannel = new SearchMessagesChatTypeFilterChannel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesChatTypeFilterChannel` into TDLib JSON wire shape. */
export function encodeSearchMessagesChatTypeFilterChannel(value: SearchMessagesChatTypeFilterChannel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesChatTypeFilterChannel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesChatTypeFilterCommunity = SearchMessagesChatTypeFilter — Returns only messages in the specified community */
export class SearchMessagesChatTypeFilterCommunity {
  type: 'searchMessagesChatTypeFilterCommunity' = 'searchMessagesChatTypeFilterCommunity';
  extra?: string;
  /** community_id: int53 — Identifier of the community to search in */
  community_id: number = 0;
}

/** Decode a TDLib `searchMessagesChatTypeFilterCommunity` object; null input -> null. */
export function decodeSearchMessagesChatTypeFilterCommunity(json: TdJsonRaw): SearchMessagesChatTypeFilterCommunity | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesChatTypeFilterCommunity = new SearchMessagesChatTypeFilterCommunity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.community_id = asNumber(record['community_id']);
  return out;
}

/** Encode a `searchMessagesChatTypeFilterCommunity` into TDLib JSON wire shape. */
export function encodeSearchMessagesChatTypeFilterCommunity(value: SearchMessagesChatTypeFilterCommunity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesChatTypeFilterCommunity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['community_id'] = value.community_id;
  return out;
}

/** searchMessagesChatTypeFilterGroup = SearchMessagesChatTypeFilter — Returns only messages in basic group and supergroup chats */
export class SearchMessagesChatTypeFilterGroup {
  type: 'searchMessagesChatTypeFilterGroup' = 'searchMessagesChatTypeFilterGroup';
  extra?: string;
}

/** Decode a TDLib `searchMessagesChatTypeFilterGroup` object; null input -> null. */
export function decodeSearchMessagesChatTypeFilterGroup(json: TdJsonRaw): SearchMessagesChatTypeFilterGroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesChatTypeFilterGroup = new SearchMessagesChatTypeFilterGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesChatTypeFilterGroup` into TDLib JSON wire shape. */
export function encodeSearchMessagesChatTypeFilterGroup(value: SearchMessagesChatTypeFilterGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesChatTypeFilterGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesChatTypeFilterPrivate = SearchMessagesChatTypeFilter — Returns only messages in private chats */
export class SearchMessagesChatTypeFilterPrivate {
  type: 'searchMessagesChatTypeFilterPrivate' = 'searchMessagesChatTypeFilterPrivate';
  extra?: string;
}

/** Decode a TDLib `searchMessagesChatTypeFilterPrivate` object; null input -> null. */
export function decodeSearchMessagesChatTypeFilterPrivate(json: TdJsonRaw): SearchMessagesChatTypeFilterPrivate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesChatTypeFilterPrivate = new SearchMessagesChatTypeFilterPrivate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesChatTypeFilterPrivate` into TDLib JSON wire shape. */
export function encodeSearchMessagesChatTypeFilterPrivate(value: SearchMessagesChatTypeFilterPrivate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesChatTypeFilterPrivate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterAnimation = SearchMessagesFilter — Returns only animation messages */
export class SearchMessagesFilterAnimation {
  type: 'searchMessagesFilterAnimation' = 'searchMessagesFilterAnimation';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterAnimation` object; null input -> null. */
export function decodeSearchMessagesFilterAnimation(json: TdJsonRaw): SearchMessagesFilterAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterAnimation = new SearchMessagesFilterAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterAnimation` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterAnimation(value: SearchMessagesFilterAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterAudio = SearchMessagesFilter — Returns only audio messages */
export class SearchMessagesFilterAudio {
  type: 'searchMessagesFilterAudio' = 'searchMessagesFilterAudio';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterAudio` object; null input -> null. */
export function decodeSearchMessagesFilterAudio(json: TdJsonRaw): SearchMessagesFilterAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterAudio = new SearchMessagesFilterAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterAudio` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterAudio(value: SearchMessagesFilterAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterChatPhoto = SearchMessagesFilter — Returns only messages containing chat photos */
export class SearchMessagesFilterChatPhoto {
  type: 'searchMessagesFilterChatPhoto' = 'searchMessagesFilterChatPhoto';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterChatPhoto` object; null input -> null. */
export function decodeSearchMessagesFilterChatPhoto(json: TdJsonRaw): SearchMessagesFilterChatPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterChatPhoto = new SearchMessagesFilterChatPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterChatPhoto` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterChatPhoto(value: SearchMessagesFilterChatPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterChatPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterDocument = SearchMessagesFilter — Returns only document messages */
export class SearchMessagesFilterDocument {
  type: 'searchMessagesFilterDocument' = 'searchMessagesFilterDocument';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterDocument` object; null input -> null. */
export function decodeSearchMessagesFilterDocument(json: TdJsonRaw): SearchMessagesFilterDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterDocument = new SearchMessagesFilterDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterDocument` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterDocument(value: SearchMessagesFilterDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterEmpty = SearchMessagesFilter — Returns all found messages, no filter is applied */
export class SearchMessagesFilterEmpty {
  type: 'searchMessagesFilterEmpty' = 'searchMessagesFilterEmpty';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterEmpty` object; null input -> null. */
export function decodeSearchMessagesFilterEmpty(json: TdJsonRaw): SearchMessagesFilterEmpty | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterEmpty = new SearchMessagesFilterEmpty();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterEmpty` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterEmpty(value: SearchMessagesFilterEmpty): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterEmpty';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterFailedToSend = SearchMessagesFilter — Returns only failed to send messages. This filter can be used only if the message database is used */
export class SearchMessagesFilterFailedToSend {
  type: 'searchMessagesFilterFailedToSend' = 'searchMessagesFilterFailedToSend';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterFailedToSend` object; null input -> null. */
export function decodeSearchMessagesFilterFailedToSend(json: TdJsonRaw): SearchMessagesFilterFailedToSend | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterFailedToSend = new SearchMessagesFilterFailedToSend();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterFailedToSend` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterFailedToSend(value: SearchMessagesFilterFailedToSend): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterFailedToSend';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterMention = SearchMessagesFilter — Returns only messages with mentions of the current user, or messages that are replies to their messages */
export class SearchMessagesFilterMention {
  type: 'searchMessagesFilterMention' = 'searchMessagesFilterMention';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterMention` object; null input -> null. */
export function decodeSearchMessagesFilterMention(json: TdJsonRaw): SearchMessagesFilterMention | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterMention = new SearchMessagesFilterMention();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterMention` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterMention(value: SearchMessagesFilterMention): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterMention';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterPhoto = SearchMessagesFilter — Returns only photo messages */
export class SearchMessagesFilterPhoto {
  type: 'searchMessagesFilterPhoto' = 'searchMessagesFilterPhoto';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterPhoto` object; null input -> null. */
export function decodeSearchMessagesFilterPhoto(json: TdJsonRaw): SearchMessagesFilterPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterPhoto = new SearchMessagesFilterPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterPhoto` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterPhoto(value: SearchMessagesFilterPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterPhotoAndVideo = SearchMessagesFilter — Returns only photo and video messages */
export class SearchMessagesFilterPhotoAndVideo {
  type: 'searchMessagesFilterPhotoAndVideo' = 'searchMessagesFilterPhotoAndVideo';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterPhotoAndVideo` object; null input -> null. */
export function decodeSearchMessagesFilterPhotoAndVideo(json: TdJsonRaw): SearchMessagesFilterPhotoAndVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterPhotoAndVideo = new SearchMessagesFilterPhotoAndVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterPhotoAndVideo` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterPhotoAndVideo(value: SearchMessagesFilterPhotoAndVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterPhotoAndVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterPinned = SearchMessagesFilter — Returns only pinned messages */
export class SearchMessagesFilterPinned {
  type: 'searchMessagesFilterPinned' = 'searchMessagesFilterPinned';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterPinned` object; null input -> null. */
export function decodeSearchMessagesFilterPinned(json: TdJsonRaw): SearchMessagesFilterPinned | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterPinned = new SearchMessagesFilterPinned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterPinned` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterPinned(value: SearchMessagesFilterPinned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterPinned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterPoll = SearchMessagesFilter — Returns only poll messages */
export class SearchMessagesFilterPoll {
  type: 'searchMessagesFilterPoll' = 'searchMessagesFilterPoll';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterPoll` object; null input -> null. */
export function decodeSearchMessagesFilterPoll(json: TdJsonRaw): SearchMessagesFilterPoll | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterPoll = new SearchMessagesFilterPoll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterPoll` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterPoll(value: SearchMessagesFilterPoll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterPoll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterUnreadMention = SearchMessagesFilter — Returns only messages with unread mentions of the current user, or messages that are replies to their messages. When using this filter the results can't be additionally filtered ... */
export class SearchMessagesFilterUnreadMention {
  type: 'searchMessagesFilterUnreadMention' = 'searchMessagesFilterUnreadMention';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterUnreadMention` object; null input -> null. */
export function decodeSearchMessagesFilterUnreadMention(json: TdJsonRaw): SearchMessagesFilterUnreadMention | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterUnreadMention = new SearchMessagesFilterUnreadMention();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterUnreadMention` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterUnreadMention(value: SearchMessagesFilterUnreadMention): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterUnreadMention';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterUnreadPollVote = SearchMessagesFilter — Returns only messages with unread poll votes for the current user. When using this filter the results can't be additionally filtered by a query or by the sending user */
export class SearchMessagesFilterUnreadPollVote {
  type: 'searchMessagesFilterUnreadPollVote' = 'searchMessagesFilterUnreadPollVote';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterUnreadPollVote` object; null input -> null. */
export function decodeSearchMessagesFilterUnreadPollVote(json: TdJsonRaw): SearchMessagesFilterUnreadPollVote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterUnreadPollVote = new SearchMessagesFilterUnreadPollVote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterUnreadPollVote` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterUnreadPollVote(value: SearchMessagesFilterUnreadPollVote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterUnreadPollVote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterUnreadReaction = SearchMessagesFilter — Returns only messages with unread reactions for the current user. When using this filter the results can't be additionally filtered by a query or by the sending user */
export class SearchMessagesFilterUnreadReaction {
  type: 'searchMessagesFilterUnreadReaction' = 'searchMessagesFilterUnreadReaction';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterUnreadReaction` object; null input -> null. */
export function decodeSearchMessagesFilterUnreadReaction(json: TdJsonRaw): SearchMessagesFilterUnreadReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterUnreadReaction = new SearchMessagesFilterUnreadReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterUnreadReaction` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterUnreadReaction(value: SearchMessagesFilterUnreadReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterUnreadReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterUrl = SearchMessagesFilter — Returns only messages containing URLs */
export class SearchMessagesFilterUrl {
  type: 'searchMessagesFilterUrl' = 'searchMessagesFilterUrl';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterUrl` object; null input -> null. */
export function decodeSearchMessagesFilterUrl(json: TdJsonRaw): SearchMessagesFilterUrl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterUrl = new SearchMessagesFilterUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterUrl` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterUrl(value: SearchMessagesFilterUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterVideo = SearchMessagesFilter — Returns only video messages */
export class SearchMessagesFilterVideo {
  type: 'searchMessagesFilterVideo' = 'searchMessagesFilterVideo';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterVideo` object; null input -> null. */
export function decodeSearchMessagesFilterVideo(json: TdJsonRaw): SearchMessagesFilterVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterVideo = new SearchMessagesFilterVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterVideo` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterVideo(value: SearchMessagesFilterVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterVideoNote = SearchMessagesFilter — Returns only video note messages */
export class SearchMessagesFilterVideoNote {
  type: 'searchMessagesFilterVideoNote' = 'searchMessagesFilterVideoNote';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterVideoNote` object; null input -> null. */
export function decodeSearchMessagesFilterVideoNote(json: TdJsonRaw): SearchMessagesFilterVideoNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterVideoNote = new SearchMessagesFilterVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterVideoNote` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterVideoNote(value: SearchMessagesFilterVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterVoiceAndVideoNote = SearchMessagesFilter — Returns only voice and video note messages */
export class SearchMessagesFilterVoiceAndVideoNote {
  type: 'searchMessagesFilterVoiceAndVideoNote' = 'searchMessagesFilterVoiceAndVideoNote';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterVoiceAndVideoNote` object; null input -> null. */
export function decodeSearchMessagesFilterVoiceAndVideoNote(json: TdJsonRaw): SearchMessagesFilterVoiceAndVideoNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterVoiceAndVideoNote = new SearchMessagesFilterVoiceAndVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterVoiceAndVideoNote` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterVoiceAndVideoNote(value: SearchMessagesFilterVoiceAndVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterVoiceAndVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchMessagesFilterVoiceNote = SearchMessagesFilter — Returns only voice note messages */
export class SearchMessagesFilterVoiceNote {
  type: 'searchMessagesFilterVoiceNote' = 'searchMessagesFilterVoiceNote';
  extra?: string;
}

/** Decode a TDLib `searchMessagesFilterVoiceNote` object; null input -> null. */
export function decodeSearchMessagesFilterVoiceNote(json: TdJsonRaw): SearchMessagesFilterVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchMessagesFilterVoiceNote = new SearchMessagesFilterVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `searchMessagesFilterVoiceNote` into TDLib JSON wire shape. */
export function encodeSearchMessagesFilterVoiceNote(value: SearchMessagesFilterVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchMessagesFilterVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** searchOutgoingDocumentMessages = FoundMessages — Searches for outgoing messages with content of the type messageDocument in all chats except secret chats. Returns the results in reverse chronological order */
export class SearchOutgoingDocumentMessages {
  type: 'searchOutgoingDocumentMessages' = 'searchOutgoingDocumentMessages';
  extra?: string;
  /** query: string — Query to search for in document file name and message caption */
  query: string = '';
  /** limit: int32 — The maximum number of messages to be returned; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `searchOutgoingDocumentMessages` object; null input -> null. */
export function decodeSearchOutgoingDocumentMessages(json: TdJsonRaw): SearchOutgoingDocumentMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchOutgoingDocumentMessages = new SearchOutgoingDocumentMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchOutgoingDocumentMessages` into TDLib JSON wire shape. */
export function encodeSearchOutgoingDocumentMessages(value: SearchOutgoingDocumentMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchOutgoingDocumentMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['limit'] = value.limit;
  return out;
}

/** searchPublicChat = Chat — Searches a public chat by its username. Currently, only private chats, supergroups and channels can be public. Returns the chat if found; otherwise, an error is returned */
export class SearchPublicChat {
  type: 'searchPublicChat' = 'searchPublicChat';
  extra?: string;
  /** username: string — Username to be resolved */
  username: string = '';
}

/** Decode a TDLib `searchPublicChat` object; null input -> null. */
export function decodeSearchPublicChat(json: TdJsonRaw): SearchPublicChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchPublicChat = new SearchPublicChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  return out;
}

/** Encode a `searchPublicChat` into TDLib JSON wire shape. */
export function encodeSearchPublicChat(value: SearchPublicChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchPublicChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  return out;
}

/** searchPublicChats = Chats — Searches public chats by looking for specified query in their username and title. Currently, only private chats, supergroups and channels can be public. Returns a meaningful number of results. Excludes private... */
export class SearchPublicChats {
  type: 'searchPublicChats' = 'searchPublicChats';
  extra?: string;
  /** query: string — Query to search for */
  query: string = '';
  /** type_filter: SearchChatTypeFilter — Additional filter for type of the chats to be returned; pass null to search for chats of all types */
  type_filter: TdSearchChatTypeFilter | null = null;
}

/** Decode a TDLib `searchPublicChats` object; null input -> null. */
export function decodeSearchPublicChats(json: TdJsonRaw): SearchPublicChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchPublicChats = new SearchPublicChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.type_filter = decodeTdSearchChatTypeFilter(record['type_filter']);
  return out;
}

/** Encode a `searchPublicChats` into TDLib JSON wire shape. */
export function encodeSearchPublicChats(value: SearchPublicChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchPublicChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['type_filter'] = value.type_filter === null ? null : encodeTdSearchChatTypeFilter(value.type_filter);
  return out;
}

/** searchPublicMessagesByTag = FoundMessages — Searches for public channel posts containing the given hashtag or cashtag. For optimal performance, the number of returned messages is chosen by TDLib and can be smaller than the specified limit */
export class SearchPublicMessagesByTag {
  type: 'searchPublicMessagesByTag' = 'searchPublicMessagesByTag';
  extra?: string;
  /** tag: string — Hashtag or cashtag to search for */
  tag: string = '';
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of messages to be returned; up to 100. For optimal performance, the number of returned messages is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `searchPublicMessagesByTag` object; null input -> null. */
export function decodeSearchPublicMessagesByTag(json: TdJsonRaw): SearchPublicMessagesByTag | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchPublicMessagesByTag = new SearchPublicMessagesByTag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.tag = asString(record['tag']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchPublicMessagesByTag` into TDLib JSON wire shape. */
export function encodeSearchPublicMessagesByTag(value: SearchPublicMessagesByTag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchPublicMessagesByTag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['tag'] = value.tag;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** searchPublicPosts = FoundPublicPosts — Searches for public channel posts using the given query. For optimal performance, the number of returned messages is chosen by TDLib and can be smaller than the specified limit */
export class SearchPublicPosts {
  type: 'searchPublicPosts' = 'searchPublicPosts';
  extra?: string;
  /** query: string — Query to search for */
  query: string = '';
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of messages to be returned; up to 100. For optimal performance, the number of returned messages is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
  /** star_count: int53 — The Telegram Star amount the user agreed to pay for the search; pass 0 for free searches */
  star_count: number = 0;
}

/** Decode a TDLib `searchPublicPosts` object; null input -> null. */
export function decodeSearchPublicPosts(json: TdJsonRaw): SearchPublicPosts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchPublicPosts = new SearchPublicPosts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `searchPublicPosts` into TDLib JSON wire shape. */
export function encodeSearchPublicPosts(value: SearchPublicPosts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchPublicPosts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  out['star_count'] = value.star_count;
  return out;
}

/** searchPublicStoriesByLocation = FoundStories — Searches for public stories by the given address location. For optimal performance, the number of returned stories is chosen by TDLib and can be smaller than the specified limit */
export class SearchPublicStoriesByLocation {
  type: 'searchPublicStoriesByLocation' = 'searchPublicStoriesByLocation';
  extra?: string;
  /** address: locationAddress — Address of the location */
  address: LocationAddress | null = null;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of stories to be returned; up to 100. For optimal performance, the number of returned stories is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `searchPublicStoriesByLocation` object; null input -> null. */
export function decodeSearchPublicStoriesByLocation(json: TdJsonRaw): SearchPublicStoriesByLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchPublicStoriesByLocation = new SearchPublicStoriesByLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.address = decodeLocationAddress(record['address']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchPublicStoriesByLocation` into TDLib JSON wire shape. */
export function encodeSearchPublicStoriesByLocation(value: SearchPublicStoriesByLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchPublicStoriesByLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['address'] = value.address === null ? null : encodeLocationAddress(value.address);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** searchPublicStoriesByTag = FoundStories — Searches for public stories containing the given hashtag or cashtag. For optimal performance, the number of returned stories is chosen by TDLib and can be smaller than the specified limit */
export class SearchPublicStoriesByTag {
  type: 'searchPublicStoriesByTag' = 'searchPublicStoriesByTag';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the stories to search for; pass 0 to search stories in all chats */
  story_poster_chat_id: number = 0;
  /** tag: string — Hashtag or cashtag to search for */
  tag: string = '';
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of stories to be returned; up to 100. For optimal performance, the number of returned stories is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `searchPublicStoriesByTag` object; null input -> null. */
export function decodeSearchPublicStoriesByTag(json: TdJsonRaw): SearchPublicStoriesByTag | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchPublicStoriesByTag = new SearchPublicStoriesByTag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.tag = asString(record['tag']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchPublicStoriesByTag` into TDLib JSON wire shape. */
export function encodeSearchPublicStoriesByTag(value: SearchPublicStoriesByTag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchPublicStoriesByTag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['tag'] = value.tag;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** searchPublicStoriesByVenue = FoundStories — Searches for public stories from the given venue. For optimal performance, the number of returned stories is chosen by TDLib and can be smaller than the specified limit */
export class SearchPublicStoriesByVenue {
  type: 'searchPublicStoriesByVenue' = 'searchPublicStoriesByVenue';
  extra?: string;
  /** venue_provider: string — Provider of the venue */
  venue_provider: string = '';
  /** venue_id: string — Identifier of the venue in the provider database */
  venue_id: string = '';
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of stories to be returned; up to 100. For optimal performance, the number of returned stories is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `searchPublicStoriesByVenue` object; null input -> null. */
export function decodeSearchPublicStoriesByVenue(json: TdJsonRaw): SearchPublicStoriesByVenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchPublicStoriesByVenue = new SearchPublicStoriesByVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.venue_provider = asString(record['venue_provider']);
  out.venue_id = asString(record['venue_id']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchPublicStoriesByVenue` into TDLib JSON wire shape. */
export function encodeSearchPublicStoriesByVenue(value: SearchPublicStoriesByVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchPublicStoriesByVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['venue_provider'] = value.venue_provider;
  out['venue_id'] = value.venue_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** searchQuote = FoundPosition — Searches for a given quote in a text. Returns found quote start position in UTF-16 code units. Returns a 404 error if the quote is not found. Can be called synchronously */
export class SearchQuote {
  type: 'searchQuote' = 'searchQuote';
  extra?: string;
  /** text: formattedText — Text in which to search for the quote */
  text: FormattedText | null = null;
  /** quote: formattedText — Quote to search for */
  quote: FormattedText | null = null;
  /** quote_position: int32 — Approximate quote position in UTF-16 code units */
  quote_position: number = 0;
}

/** Decode a TDLib `searchQuote` object; null input -> null. */
export function decodeSearchQuote(json: TdJsonRaw): SearchQuote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchQuote = new SearchQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.quote = decodeFormattedText(record['quote']);
  out.quote_position = asNumber(record['quote_position']);
  return out;
}

/** Encode a `searchQuote` into TDLib JSON wire shape. */
export function encodeSearchQuote(value: SearchQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['quote'] = value.quote === null ? null : encodeFormattedText(value.quote);
  out['quote_position'] = value.quote_position;
  return out;
}

/** searchRecentlyFoundChats = Chats — Searches for the specified query in the title and username of up to 50 recently found chats. This is an offline method */
export class SearchRecentlyFoundChats {
  type: 'searchRecentlyFoundChats' = 'searchRecentlyFoundChats';
  extra?: string;
  /** query: string — Query to search for */
  query: string = '';
  /** type_filter: SearchChatTypeFilter — Additional filter for type of the chats to be returned; pass null to search for chats of all types */
  type_filter: TdSearchChatTypeFilter | null = null;
  /** limit: int32 — The maximum number of chats to be returned */
  limit: number = 0;
}

/** Decode a TDLib `searchRecentlyFoundChats` object; null input -> null. */
export function decodeSearchRecentlyFoundChats(json: TdJsonRaw): SearchRecentlyFoundChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchRecentlyFoundChats = new SearchRecentlyFoundChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.type_filter = decodeTdSearchChatTypeFilter(record['type_filter']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchRecentlyFoundChats` into TDLib JSON wire shape. */
export function encodeSearchRecentlyFoundChats(value: SearchRecentlyFoundChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchRecentlyFoundChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['type_filter'] = value.type_filter === null ? null : encodeTdSearchChatTypeFilter(value.type_filter);
  out['limit'] = value.limit;
  return out;
}

/** searchSavedMessages = FoundChatMessages — Searches for messages tagged by the given reaction and with the given words in the Saved Messages chat; for Telegram Premium users only. Returns the results in reverse chronological order, i.e. i... */
export class SearchSavedMessages {
  type: 'searchSavedMessages' = 'searchSavedMessages';
  extra?: string;
  /** saved_messages_topic_id: int53 — If not 0, only messages in the specified Saved Messages topic will be considered; pass 0 to consider all messages */
  saved_messages_topic_id: number = 0;
  /** tag: ReactionType — Tag to search for; pass null to return all suitable messages */
  tag: TdReactionType | null = null;
  /** query: string — Query to search for */
  query: string = '';
  /** from_message_id: int53 — Identifier of the message starting from which messages must be fetched; use 0 to get results from the last message */
  from_message_id: number = 0;
  /** offset: int32 — Specify 0 to get results from exactly the message from_message_id or a negative number to get the specified message and some newer messages */
  offset: number = 0;
  /** limit: int32 — The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, then the limit must be greater than -offset. */
  limit: number = 0;
}

/** Decode a TDLib `searchSavedMessages` object; null input -> null. */
export function decodeSearchSavedMessages(json: TdJsonRaw): SearchSavedMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchSavedMessages = new SearchSavedMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  out.tag = decodeTdReactionType(record['tag']);
  out.query = asString(record['query']);
  out.from_message_id = asNumber(record['from_message_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchSavedMessages` into TDLib JSON wire shape. */
export function encodeSearchSavedMessages(value: SearchSavedMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchSavedMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  out['tag'] = value.tag === null ? null : encodeTdReactionType(value.tag);
  out['query'] = value.query;
  out['from_message_id'] = value.from_message_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** searchSecretMessages = FoundMessages — Searches for messages in secret chats. Returns the results in reverse chronological order. For optimal performance, the number of returned messages is chosen by TDLib */
export class SearchSecretMessages {
  type: 'searchSecretMessages' = 'searchSecretMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in which to search. Specify 0 to search in all secret chats */
  chat_id: number = 0;
  /** query: string — Query to search for. If empty, searchChatMessages must be used instead */
  query: string = '';
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of messages to be returned; up to 100. For optimal performance, the number of returned messages is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
  /** filter: SearchMessagesFilter — Additional filter for messages to search; pass null to search for all messages */
  filter: TdSearchMessagesFilter | null = null;
}

/** Decode a TDLib `searchSecretMessages` object; null input -> null. */
export function decodeSearchSecretMessages(json: TdJsonRaw): SearchSecretMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchSecretMessages = new SearchSecretMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.query = asString(record['query']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  out.filter = decodeTdSearchMessagesFilter(record['filter']);
  return out;
}

/** Encode a `searchSecretMessages` into TDLib JSON wire shape. */
export function encodeSearchSecretMessages(value: SearchSecretMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchSecretMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['query'] = value.query;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  out['filter'] = value.filter === null ? null : encodeTdSearchMessagesFilter(value.filter);
  return out;
}

/** searchStickerSet = StickerSet — Searches for a sticker set by its name */
export class SearchStickerSet {
  type: 'searchStickerSet' = 'searchStickerSet';
  extra?: string;
  /** name: string — Name of the sticker set */
  name: string = '';
  /** ignore_cache: Bool — Pass true to ignore local cache of sticker sets and always send a network request */
  ignore_cache: boolean = false;
}

/** Decode a TDLib `searchStickerSet` object; null input -> null. */
export function decodeSearchStickerSet(json: TdJsonRaw): SearchStickerSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchStickerSet = new SearchStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.ignore_cache = asBool(record['ignore_cache']);
  return out;
}

/** Encode a `searchStickerSet` into TDLib JSON wire shape. */
export function encodeSearchStickerSet(value: SearchStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['ignore_cache'] = value.ignore_cache;
  return out;
}

/** searchStickerSets = StickerSets — Searches for sticker sets by looking for specified query in their title and name. Excludes installed sticker sets from the results */
export class SearchStickerSets {
  type: 'searchStickerSets' = 'searchStickerSets';
  extra?: string;
  /** sticker_type: StickerType — Type of the sticker sets to return */
  sticker_type: TdStickerType | null = null;
  /** query: string — Query to search for */
  query: string = '';
}

/** Decode a TDLib `searchStickerSets` object; null input -> null. */
export function decodeSearchStickerSets(json: TdJsonRaw): SearchStickerSets | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchStickerSets = new SearchStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.query = asString(record['query']);
  return out;
}

/** Encode a `searchStickerSets` into TDLib JSON wire shape. */
export function encodeSearchStickerSets(value: SearchStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['query'] = value.query;
  return out;
}

/** searchStickers = Stickers — Searches for stickers from public sticker sets that correspond to any of the given emoji */
export class SearchStickers {
  type: 'searchStickers' = 'searchStickers';
  extra?: string;
  /** sticker_type: StickerType — Type of the stickers to return */
  sticker_type: TdStickerType | null = null;
  /** emojis: string — Space-separated list of emojis to search for */
  emojis: string = '';
  /** query: string — Query to search for; may be empty to search for emoji only */
  query: string = '';
  /** input_language_codes: vector<string> — List of possible IETF language tags of the user's input language; may be empty if unknown */
  input_language_codes: string[] = [];
  /** offset: int32 — The offset from which to return the stickers; must be non-negative */
  offset: number = 0;
  /** limit: int32 — The maximum number of stickers to be returned; 0-100 */
  limit: number = 0;
}

/** Decode a TDLib `searchStickers` object; null input -> null. */
export function decodeSearchStickers(json: TdJsonRaw): SearchStickers | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchStickers = new SearchStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.emojis = asString(record['emojis']);
  out.query = asString(record['query']);
  out.input_language_codes = decodeVector(record['input_language_codes'], (item: TdJsonRaw): string => asString(item));
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `searchStickers` into TDLib JSON wire shape. */
export function encodeSearchStickers(value: SearchStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['emojis'] = value.emojis;
  out['query'] = value.query;
  out['input_language_codes'] = encodeVector(value.input_language_codes, (item: string): TdJsonRaw => item);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** searchStringsByPrefix = FoundPositions — Searches specified query by word prefixes in the provided strings. Returns 0-based positions of strings that matched. Can be called synchronously */
export class SearchStringsByPrefix {
  type: 'searchStringsByPrefix' = 'searchStringsByPrefix';
  extra?: string;
  /** strings: vector<string> — The strings to search in for the query */
  strings: string[] = [];
  /** query: string — Query to search for */
  query: string = '';
  /** limit: int32 — The maximum number of objects to return */
  limit: number = 0;
  /** return_none_for_empty_query: Bool — Pass true to receive no results for an empty query */
  return_none_for_empty_query: boolean = false;
}

/** Decode a TDLib `searchStringsByPrefix` object; null input -> null. */
export function decodeSearchStringsByPrefix(json: TdJsonRaw): SearchStringsByPrefix | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchStringsByPrefix = new SearchStringsByPrefix();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.strings = decodeVector(record['strings'], (item: TdJsonRaw): string => asString(item));
  out.query = asString(record['query']);
  out.limit = asNumber(record['limit']);
  out.return_none_for_empty_query = asBool(record['return_none_for_empty_query']);
  return out;
}

/** Encode a `searchStringsByPrefix` into TDLib JSON wire shape. */
export function encodeSearchStringsByPrefix(value: SearchStringsByPrefix): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchStringsByPrefix';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['strings'] = encodeVector(value.strings, (item: string): TdJsonRaw => item);
  out['query'] = value.query;
  out['limit'] = value.limit;
  out['return_none_for_empty_query'] = value.return_none_for_empty_query;
  return out;
}

/** searchTextCompositionStyle = TextCompositionStyle — Searches a custom text composition style by its name */
export class SearchTextCompositionStyle {
  type: 'searchTextCompositionStyle' = 'searchTextCompositionStyle';
  extra?: string;
  /** name: string — Name of the style */
  name: string = '';
}

/** Decode a TDLib `searchTextCompositionStyle` object; null input -> null. */
export function decodeSearchTextCompositionStyle(json: TdJsonRaw): SearchTextCompositionStyle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchTextCompositionStyle = new SearchTextCompositionStyle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `searchTextCompositionStyle` into TDLib JSON wire shape. */
export function encodeSearchTextCompositionStyle(value: SearchTextCompositionStyle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchTextCompositionStyle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** searchUserByPhoneNumber = User — Searches a user by their phone number. Returns a 404 error if the user can't be found */
export class SearchUserByPhoneNumber {
  type: 'searchUserByPhoneNumber' = 'searchUserByPhoneNumber';
  extra?: string;
  /** phone_number: string — Phone number to search for */
  phone_number: string = '';
  /** only_local: Bool — Pass true to get only locally available information without sending network requests */
  only_local: boolean = false;
}

/** Decode a TDLib `searchUserByPhoneNumber` object; null input -> null. */
export function decodeSearchUserByPhoneNumber(json: TdJsonRaw): SearchUserByPhoneNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchUserByPhoneNumber = new SearchUserByPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  out.only_local = asBool(record['only_local']);
  return out;
}

/** Encode a `searchUserByPhoneNumber` into TDLib JSON wire shape. */
export function encodeSearchUserByPhoneNumber(value: SearchUserByPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchUserByPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  out['only_local'] = value.only_local;
  return out;
}

/** searchUserByToken = User — Searches a user by a token from the user's link */
export class SearchUserByToken {
  type: 'searchUserByToken' = 'searchUserByToken';
  extra?: string;
  /** token: string — Token to search for */
  token: string = '';
}

/** Decode a TDLib `searchUserByToken` object; null input -> null. */
export function decodeSearchUserByToken(json: TdJsonRaw): SearchUserByToken | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchUserByToken = new SearchUserByToken();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `searchUserByToken` into TDLib JSON wire shape. */
export function encodeSearchUserByToken(value: SearchUserByToken): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchUserByToken';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** searchWebApp = FoundWebApp — Returns information about a Web App by its short name. Returns a 404 error if the Web App is not found */
export class SearchWebApp {
  type: 'searchWebApp' = 'searchWebApp';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** web_app_short_name: string — Short name of the Web App */
  web_app_short_name: string = '';
}

/** Decode a TDLib `searchWebApp` object; null input -> null. */
export function decodeSearchWebApp(json: TdJsonRaw): SearchWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SearchWebApp = new SearchWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.web_app_short_name = asString(record['web_app_short_name']);
  return out;
}

/** Encode a `searchWebApp` into TDLib JSON wire shape. */
export function encodeSearchWebApp(value: SearchWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'searchWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['web_app_short_name'] = value.web_app_short_name;
  return out;
}

/** seconds = Seconds — Contains a value representing a number of seconds */
export class Seconds {
  type: 'seconds' = 'seconds';
  extra?: string;
  /** seconds: double — Number of seconds */
  seconds: number = 0;
}

/** Decode a TDLib `seconds` object; null input -> null. */
export function decodeSeconds(json: TdJsonRaw): Seconds | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Seconds = new Seconds();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.seconds = asNumber(record['seconds']);
  return out;
}

/** Encode a `seconds` into TDLib JSON wire shape. */
export function encodeSeconds(value: Seconds): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'seconds';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['seconds'] = value.seconds;
  return out;
}

/** secretChat = SecretChat — Represents a secret chat The pixels must be used to make a 12x12 square image filled from left to right, top to bottom. Alternatively, the first 32 bytes of the hash can be converted to the hexadecimal format an... */
export class SecretChat {
  type: 'secretChat' = 'secretChat';
  extra?: string;
  /** id: int32 — Secret chat identifier */
  id: number = 0;
  /** user_id: int53 — Identifier of the chat partner */
  user_id: number = 0;
  /** state: SecretChatState — State of the secret chat */
  state: TdSecretChatState | null = null;
  /** is_outbound: Bool — True, if the chat was created by the current user; false otherwise */
  is_outbound: boolean = false;
  /** key_hash: bytes — Hash of the currently used key for comparison with the hash of the chat partner's key. This is a string of 36 little-endian bytes, which must be split into groups of 2 bits, each denoting a pixel of one of 4 colors FFFF... */
  key_hash: string = '';
  /** layer: int32 — Secret chat layer; determines features supported by the chat partner's application. Nested text entities and underline and strikethrough entities are supported if the layer >= 101, */
  layer: number = 0;
}

/** Decode a TDLib `secretChat` object; null input -> null. */
export function decodeSecretChat(json: TdJsonRaw): SecretChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SecretChat = new SecretChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.user_id = asNumber(record['user_id']);
  out.state = decodeTdSecretChatState(record['state']);
  out.is_outbound = asBool(record['is_outbound']);
  out.key_hash = asString(record['key_hash']);
  out.layer = asNumber(record['layer']);
  return out;
}

/** Encode a `secretChat` into TDLib JSON wire shape. */
export function encodeSecretChat(value: SecretChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'secretChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['user_id'] = value.user_id;
  out['state'] = value.state === null ? null : encodeTdSecretChatState(value.state);
  out['is_outbound'] = value.is_outbound;
  out['key_hash'] = value.key_hash;
  out['layer'] = value.layer;
  return out;
}

/** secretChatStateClosed = SecretChatState — The secret chat is closed */
export class SecretChatStateClosed {
  type: 'secretChatStateClosed' = 'secretChatStateClosed';
  extra?: string;
}

/** Decode a TDLib `secretChatStateClosed` object; null input -> null. */
export function decodeSecretChatStateClosed(json: TdJsonRaw): SecretChatStateClosed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SecretChatStateClosed = new SecretChatStateClosed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `secretChatStateClosed` into TDLib JSON wire shape. */
export function encodeSecretChatStateClosed(value: SecretChatStateClosed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'secretChatStateClosed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** secretChatStatePending = SecretChatState — The secret chat is not yet created; waiting for the other user to get online */
export class SecretChatStatePending {
  type: 'secretChatStatePending' = 'secretChatStatePending';
  extra?: string;
}

/** Decode a TDLib `secretChatStatePending` object; null input -> null. */
export function decodeSecretChatStatePending(json: TdJsonRaw): SecretChatStatePending | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SecretChatStatePending = new SecretChatStatePending();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `secretChatStatePending` into TDLib JSON wire shape. */
export function encodeSecretChatStatePending(value: SecretChatStatePending): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'secretChatStatePending';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** secretChatStateReady = SecretChatState — The secret chat is ready to use */
export class SecretChatStateReady {
  type: 'secretChatStateReady' = 'secretChatStateReady';
  extra?: string;
}

/** Decode a TDLib `secretChatStateReady` object; null input -> null. */
export function decodeSecretChatStateReady(json: TdJsonRaw): SecretChatStateReady | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SecretChatStateReady = new SecretChatStateReady();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `secretChatStateReady` into TDLib JSON wire shape. */
export function encodeSecretChatStateReady(value: SecretChatStateReady): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'secretChatStateReady';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sellGift = Ok — Sells a gift for Telegram Stars; requires owner privileges for gifts owned by a chat */
export class SellGift {
  type: 'sellGift' = 'sellGift';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which to send the request; for bots only */
  business_connection_id: string = '';
  /** received_gift_id: string — Identifier of the gift */
  received_gift_id: string = '';
}

/** Decode a TDLib `sellGift` object; null input -> null. */
export function decodeSellGift(json: TdJsonRaw): SellGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SellGift = new SellGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.received_gift_id = asString(record['received_gift_id']);
  return out;
}

/** Encode a `sellGift` into TDLib JSON wire shape. */
export function encodeSellGift(value: SellGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sellGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['received_gift_id'] = value.received_gift_id;
  return out;
}

/** sendAuthenticationFirebaseSms = Ok — Sends Firebase Authentication SMS to the phone number of the user. Works only when the current authorization state is authorizationStateWaitCode and the server returned code of the type authentication... */
export class SendAuthenticationFirebaseSms {
  type: 'sendAuthenticationFirebaseSms' = 'sendAuthenticationFirebaseSms';
  extra?: string;
  /** token: string — Play Integrity API or SafetyNet Attestation API token for the Android application, or secret from push notification for the iOS application */
  token: string = '';
}

/** Decode a TDLib `sendAuthenticationFirebaseSms` object; null input -> null. */
export function decodeSendAuthenticationFirebaseSms(json: TdJsonRaw): SendAuthenticationFirebaseSms | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendAuthenticationFirebaseSms = new SendAuthenticationFirebaseSms();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `sendAuthenticationFirebaseSms` into TDLib JSON wire shape. */
export function encodeSendAuthenticationFirebaseSms(value: SendAuthenticationFirebaseSms): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendAuthenticationFirebaseSms';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** sendBotStartMessage = Message — Invites a bot to a chat (if it is not yet a member) and sends it the /start command; requires can_invite_users member right. Bots can't be invited to a private chat other than the chat with the bot. Bots c... */
export class SendBotStartMessage {
  type: 'sendBotStartMessage' = 'sendBotStartMessage';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the bot */
  bot_user_id: number = 0;
  /** chat_id: int53 — Identifier of the target chat */
  chat_id: number = 0;
  /** parameter: string — A hidden parameter sent to the bot for deep linking purposes (https://core.telegram.org/bots#deep-linking) */
  parameter: string = '';
}

/** Decode a TDLib `sendBotStartMessage` object; null input -> null. */
export function decodeSendBotStartMessage(json: TdJsonRaw): SendBotStartMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendBotStartMessage = new SendBotStartMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.parameter = asString(record['parameter']);
  return out;
}

/** Encode a `sendBotStartMessage` into TDLib JSON wire shape. */
export function encodeSendBotStartMessage(value: SendBotStartMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendBotStartMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['chat_id'] = value.chat_id;
  out['parameter'] = value.parameter;
  return out;
}

/** sendBusinessMessage = BusinessMessage — Sends a message on behalf of a business account; for bots only. Returns the message after it was sent */
export class SendBusinessMessage {
  type: 'sendBusinessMessage' = 'sendBusinessMessage';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which to send the request */
  business_connection_id: string = '';
  /** chat_id: int53 — Target chat */
  chat_id: number = 0;
  /** reply_to: InputMessageReplyTo — Information about the message to be replied; pass null if none */
  reply_to: TdInputMessageReplyTo | null = null;
  /** disable_notification: Bool — Pass true to disable notification for the message */
  disable_notification: boolean = false;
  /** protect_content: Bool — Pass true if the content of the message must be protected from forwarding and saving */
  protect_content: boolean = false;
  /** effect_id: int64 — Identifier of the effect to apply to the message */
  effect_id: string = '';
  /** reply_markup: ReplyMarkup — Markup for replying to the message; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `sendBusinessMessage` object; null input -> null. */
export function decodeSendBusinessMessage(json: TdJsonRaw): SendBusinessMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendBusinessMessage = new SendBusinessMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.reply_to = decodeTdInputMessageReplyTo(record['reply_to']);
  out.disable_notification = asBool(record['disable_notification']);
  out.protect_content = asBool(record['protect_content']);
  out.effect_id = asInt64(record['effect_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `sendBusinessMessage` into TDLib JSON wire shape. */
export function encodeSendBusinessMessage(value: SendBusinessMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendBusinessMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['reply_to'] = value.reply_to === null ? null : encodeTdInputMessageReplyTo(value.reply_to);
  out['disable_notification'] = value.disable_notification;
  out['protect_content'] = value.protect_content;
  out['effect_id'] = value.effect_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** sendBusinessMessageAlbum = BusinessMessages — Sends 2-10 messages grouped together into an album on behalf of a business account; for bots only. Currently, only audio, document, photo and video messages can be grouped into an album. Docu... */
export class SendBusinessMessageAlbum {
  type: 'sendBusinessMessageAlbum' = 'sendBusinessMessageAlbum';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which to send the request */
  business_connection_id: string = '';
  /** chat_id: int53 — Target chat */
  chat_id: number = 0;
  /** reply_to: InputMessageReplyTo — Information about the message to be replied; pass null if none */
  reply_to: TdInputMessageReplyTo | null = null;
  /** disable_notification: Bool — Pass true to disable notification for the message */
  disable_notification: boolean = false;
  /** protect_content: Bool — Pass true if the content of the message must be protected from forwarding and saving */
  protect_content: boolean = false;
  /** effect_id: int64 — Identifier of the effect to apply to the message */
  effect_id: string = '';
  /** input_message_contents: vector<InputMessageContent> — Contents of messages to be sent. At most 10 messages can be added to an album. All messages must have the same value of show_caption_above_media */
  input_message_contents: TdInputMessageContent[] = [];
}

/** Decode a TDLib `sendBusinessMessageAlbum` object; null input -> null. */
export function decodeSendBusinessMessageAlbum(json: TdJsonRaw): SendBusinessMessageAlbum | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendBusinessMessageAlbum = new SendBusinessMessageAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.reply_to = decodeTdInputMessageReplyTo(record['reply_to']);
  out.disable_notification = asBool(record['disable_notification']);
  out.protect_content = asBool(record['protect_content']);
  out.effect_id = asInt64(record['effect_id']);
  out.input_message_contents = decodeVector(record['input_message_contents'], (item: TdJsonRaw): TdInputMessageContent => decodeTdInputMessageContent(item) as TdInputMessageContent);
  return out;
}

/** Encode a `sendBusinessMessageAlbum` into TDLib JSON wire shape. */
export function encodeSendBusinessMessageAlbum(value: SendBusinessMessageAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendBusinessMessageAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['reply_to'] = value.reply_to === null ? null : encodeTdInputMessageReplyTo(value.reply_to);
  out['disable_notification'] = value.disable_notification;
  out['protect_content'] = value.protect_content;
  out['effect_id'] = value.effect_id;
  out['input_message_contents'] = encodeVector(value.input_message_contents, (item: TdInputMessageContent | null): TdJsonRaw => item === null ? null : encodeTdInputMessageContent(item));
  return out;
}

/** sendCallDebugInformation = Ok — Sends debug information for a call to Telegram servers */
export class SendCallDebugInformation {
  type: 'sendCallDebugInformation' = 'sendCallDebugInformation';
  extra?: string;
  /** call_id: InputCall — Call identifier */
  call_id: TdInputCall | null = null;
  /** debug_information: string — Debug information in application-specific format */
  debug_information: string = '';
}

/** Decode a TDLib `sendCallDebugInformation` object; null input -> null. */
export function decodeSendCallDebugInformation(json: TdJsonRaw): SendCallDebugInformation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendCallDebugInformation = new SendCallDebugInformation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call_id = decodeTdInputCall(record['call_id']);
  out.debug_information = asString(record['debug_information']);
  return out;
}

/** Encode a `sendCallDebugInformation` into TDLib JSON wire shape. */
export function encodeSendCallDebugInformation(value: SendCallDebugInformation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendCallDebugInformation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call_id'] = value.call_id === null ? null : encodeTdInputCall(value.call_id);
  out['debug_information'] = value.debug_information;
  return out;
}

/** sendCallLog = Ok — Sends log file for a call to Telegram servers */
export class SendCallLog {
  type: 'sendCallLog' = 'sendCallLog';
  extra?: string;
  /** call_id: InputCall — Call identifier */
  call_id: TdInputCall | null = null;
  /** log_file: InputFile — Call log file. Only inputFileLocal and inputFileGenerated are supported */
  log_file: TdInputFile | null = null;
}

/** Decode a TDLib `sendCallLog` object; null input -> null. */
export function decodeSendCallLog(json: TdJsonRaw): SendCallLog | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendCallLog = new SendCallLog();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call_id = decodeTdInputCall(record['call_id']);
  out.log_file = decodeTdInputFile(record['log_file']);
  return out;
}

/** Encode a `sendCallLog` into TDLib JSON wire shape. */
export function encodeSendCallLog(value: SendCallLog): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendCallLog';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call_id'] = value.call_id === null ? null : encodeTdInputCall(value.call_id);
  out['log_file'] = value.log_file === null ? null : encodeTdInputFile(value.log_file);
  return out;
}

/** sendCallRating = Ok — Sends a call rating */
export class SendCallRating {
  type: 'sendCallRating' = 'sendCallRating';
  extra?: string;
  /** call_id: InputCall — Call identifier */
  call_id: TdInputCall | null = null;
  /** rating: int32 — Call rating; 1-5 */
  rating: number = 0;
  /** comment: string — An optional user comment if the rating is less than 5 */
  comment: string = '';
  /** problems: vector<CallProblem> — List of the exact types of problems with the call, specified by the user */
  problems: TdCallProblem[] = [];
}

/** Decode a TDLib `sendCallRating` object; null input -> null. */
export function decodeSendCallRating(json: TdJsonRaw): SendCallRating | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendCallRating = new SendCallRating();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call_id = decodeTdInputCall(record['call_id']);
  out.rating = asNumber(record['rating']);
  out.comment = asString(record['comment']);
  out.problems = decodeVector(record['problems'], (item: TdJsonRaw): TdCallProblem => decodeTdCallProblem(item) as TdCallProblem);
  return out;
}

/** Encode a `sendCallRating` into TDLib JSON wire shape. */
export function encodeSendCallRating(value: SendCallRating): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendCallRating';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call_id'] = value.call_id === null ? null : encodeTdInputCall(value.call_id);
  out['rating'] = value.rating;
  out['comment'] = value.comment;
  out['problems'] = encodeVector(value.problems, (item: TdCallProblem | null): TdJsonRaw => item === null ? null : encodeTdCallProblem(item));
  return out;
}

/** sendCallSignalingData = Ok — Sends call signaling data */
export class SendCallSignalingData {
  type: 'sendCallSignalingData' = 'sendCallSignalingData';
  extra?: string;
  /** call_id: int32 — Call identifier */
  call_id: number = 0;
  /** data: bytes — The data */
  data: string = '';
}

/** Decode a TDLib `sendCallSignalingData` object; null input -> null. */
export function decodeSendCallSignalingData(json: TdJsonRaw): SendCallSignalingData | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendCallSignalingData = new SendCallSignalingData();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call_id = asNumber(record['call_id']);
  out.data = asString(record['data']);
  return out;
}

/** Encode a `sendCallSignalingData` into TDLib JSON wire shape. */
export function encodeSendCallSignalingData(value: SendCallSignalingData): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendCallSignalingData';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call_id'] = value.call_id;
  out['data'] = value.data;
  return out;
}

/** sendChatAction = Ok — Sends a notification about user activity in a chat */
export class SendChatAction {
  type: 'sendChatAction' = 'sendChatAction';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Identifier of the topic in which the action is performed; pass null if none */
  topic_id: TdMessageTopic | null = null;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which to send the request; for bots only */
  business_connection_id: string = '';
  /** action: ChatAction — The action description; pass null to cancel the currently active action */
  action: TdChatAction | null = null;
}

/** Decode a TDLib `sendChatAction` object; null input -> null. */
export function decodeSendChatAction(json: TdJsonRaw): SendChatAction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendChatAction = new SendChatAction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.business_connection_id = asString(record['business_connection_id']);
  out.action = decodeTdChatAction(record['action']);
  return out;
}

/** Encode a `sendChatAction` into TDLib JSON wire shape. */
export function encodeSendChatAction(value: SendChatAction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendChatAction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['business_connection_id'] = value.business_connection_id;
  out['action'] = value.action === null ? null : encodeTdChatAction(value.action);
  return out;
}

/** sendCustomRequest = CustomRequestResult — Sends a custom request; for bots only */
export class SendCustomRequest {
  type: 'sendCustomRequest' = 'sendCustomRequest';
  extra?: string;
  /** method: string — The method name */
  method: string = '';
  /** parameters: string — JSON-serialized method parameters */
  parameters: string = '';
}

/** Decode a TDLib `sendCustomRequest` object; null input -> null. */
export function decodeSendCustomRequest(json: TdJsonRaw): SendCustomRequest | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendCustomRequest = new SendCustomRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.method = asString(record['method']);
  out.parameters = asString(record['parameters']);
  return out;
}

/** Encode a `sendCustomRequest` into TDLib JSON wire shape. */
export function encodeSendCustomRequest(value: SendCustomRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendCustomRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['method'] = value.method;
  out['parameters'] = value.parameters;
  return out;
}

/** sendEmailAddressVerificationCode = EmailAddressAuthenticationCodeInfo — Sends a code to verify an email address to be added to a user's Telegram Passport */
export class SendEmailAddressVerificationCode {
  type: 'sendEmailAddressVerificationCode' = 'sendEmailAddressVerificationCode';
  extra?: string;
  /** email_address: string — Email address */
  email_address: string = '';
}

/** Decode a TDLib `sendEmailAddressVerificationCode` object; null input -> null. */
export function decodeSendEmailAddressVerificationCode(json: TdJsonRaw): SendEmailAddressVerificationCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendEmailAddressVerificationCode = new SendEmailAddressVerificationCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.email_address = asString(record['email_address']);
  return out;
}

/** Encode a `sendEmailAddressVerificationCode` into TDLib JSON wire shape. */
export function encodeSendEmailAddressVerificationCode(value: SendEmailAddressVerificationCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendEmailAddressVerificationCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['email_address'] = value.email_address;
  return out;
}

/** sendEphemeralMessage = Message — Sends an ephemeral message which will be received only by one bot in a chat. Currently, only ephemeral bot commands and replies to bot ephemeral messages can be sent using the method. The message is persi... */
export class SendEphemeralMessage {
  type: 'sendEphemeralMessage' = 'sendEphemeralMessage';
  extra?: string;
  /** chat_id: int53 — Target chat */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Topic in which the message will be sent; pass null if none */
  topic_id: TdMessageTopic | null = null;
  /** receiver_user_id: int53 — Identifier of the user who will receive the message */
  receiver_user_id: number = 0;
  /** callback_query_id: int64 — Identifier of the callback query which triggered the message; for bots only */
  callback_query_id: string = '';
  /** replace_callback_query_message: Bool — Pass true if the ephemeral message must replace the message from which the callback query originated; for bots only */
  replace_callback_query_message: boolean = false;
  /** reply_to: InputMessageReplyTo — Information about the message to be replied; pass null if none. The message can be an incoming ephemeral message */
  reply_to: TdInputMessageReplyTo | null = null;
  /** protect_content: Bool — Pass true if the content of the message must be protected from forwarding and saving; for bots only */
  protect_content: boolean = false;
  /** sending_id: int32 — Non-persistent identifier, which will be returned back in messageSendingStatePending object and can be used to match sent messages and corresponding updateNewMessage updates */
  sending_id: number = 0;
  /** only_preview: Bool — Pass true to get a fake message instead of actually sending them */
  only_preview: boolean = false;
  /** reply_markup: ReplyMarkup — Markup for replying to the message; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageAnimation, */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `sendEphemeralMessage` object; null input -> null. */
export function decodeSendEphemeralMessage(json: TdJsonRaw): SendEphemeralMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendEphemeralMessage = new SendEphemeralMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.receiver_user_id = asNumber(record['receiver_user_id']);
  out.callback_query_id = asInt64(record['callback_query_id']);
  out.replace_callback_query_message = asBool(record['replace_callback_query_message']);
  out.reply_to = decodeTdInputMessageReplyTo(record['reply_to']);
  out.protect_content = asBool(record['protect_content']);
  out.sending_id = asNumber(record['sending_id']);
  out.only_preview = asBool(record['only_preview']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `sendEphemeralMessage` into TDLib JSON wire shape. */
export function encodeSendEphemeralMessage(value: SendEphemeralMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendEphemeralMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['receiver_user_id'] = value.receiver_user_id;
  out['callback_query_id'] = value.callback_query_id;
  out['replace_callback_query_message'] = value.replace_callback_query_message;
  out['reply_to'] = value.reply_to === null ? null : encodeTdInputMessageReplyTo(value.reply_to);
  out['protect_content'] = value.protect_content;
  out['sending_id'] = value.sending_id;
  out['only_preview'] = value.only_preview;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** sendGift = Ok — Sends a gift to another user or channel chat. May return an error with a message "STARGIFT_USAGE_LIMITED" if the gift was sold out Must be empty if the receiver enabled paid messages */
export class SendGift {
  type: 'sendGift' = 'sendGift';
  extra?: string;
  /** gift_id: int64 — Identifier of the gift to send */
  gift_id: string = '';
  /** owner_id: MessageSender — Identifier of the user or the channel chat that will receive the gift; limited gifts can't be sent to channel chats */
  owner_id: TdMessageSender | null = null;
  /** text: formattedText — Text to show along with the gift; 0-getOption("gift_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed. */
  text: FormattedText | null = null;
  /** is_private: Bool — Pass true to show gift text and sender only to the gift receiver; otherwise, everyone will be able to see them */
  is_private: boolean = false;
  /** pay_for_upgrade: Bool — Pass true to additionally pay for the gift upgrade and allow the receiver to upgrade it for free */
  pay_for_upgrade: boolean = false;
}

/** Decode a TDLib `sendGift` object; null input -> null. */
export function decodeSendGift(json: TdJsonRaw): SendGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendGift = new SendGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_id = asInt64(record['gift_id']);
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.text = decodeFormattedText(record['text']);
  out.is_private = asBool(record['is_private']);
  out.pay_for_upgrade = asBool(record['pay_for_upgrade']);
  return out;
}

/** Encode a `sendGift` into TDLib JSON wire shape. */
export function encodeSendGift(value: SendGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_id'] = value.gift_id;
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['is_private'] = value.is_private;
  out['pay_for_upgrade'] = value.pay_for_upgrade;
  return out;
}

/** sendGiftPurchaseOffer = Ok — Sends an offer to purchase an upgraded gift */
export class SendGiftPurchaseOffer {
  type: 'sendGiftPurchaseOffer' = 'sendGiftPurchaseOffer';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that currently owns the gift and will receive the offer */
  owner_id: TdMessageSender | null = null;
  /** gift_name: string — Name of the upgraded gift */
  gift_name: string = '';
  /** price: GiftResalePrice — The price that the user agreed to pay for the gift */
  price: TdGiftResalePrice | null = null;
  /** duration: int32 — Duration of the offer, in seconds; must be one of 21600, 43200, 86400, 129600, 172800, or 259200. Can also be 120 if Telegram test environment is used */
  duration: number = 0;
  /** paid_message_star_count: int53 — The number of Telegram Stars the user agreed to pay additionally for sending of the offer message to the current gift owner; pass userFullInfo.outgoing_paid_message_star_count for users and 0 otherwise */
  paid_message_star_count: number = 0;
}

/** Decode a TDLib `sendGiftPurchaseOffer` object; null input -> null. */
export function decodeSendGiftPurchaseOffer(json: TdJsonRaw): SendGiftPurchaseOffer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendGiftPurchaseOffer = new SendGiftPurchaseOffer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.gift_name = asString(record['gift_name']);
  out.price = decodeTdGiftResalePrice(record['price']);
  out.duration = asNumber(record['duration']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  return out;
}

/** Encode a `sendGiftPurchaseOffer` into TDLib JSON wire shape. */
export function encodeSendGiftPurchaseOffer(value: SendGiftPurchaseOffer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendGiftPurchaseOffer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['gift_name'] = value.gift_name;
  out['price'] = value.price === null ? null : encodeTdGiftResalePrice(value.price);
  out['duration'] = value.duration;
  out['paid_message_star_count'] = value.paid_message_star_count;
  return out;
}

/** sendGroupCallMessage = Ok — Sends a message to other participants of a group call. Requires groupCall.can_send_messages right which depends on paid_message_star_count. Can't contain line feeds for live stories. Can contain only Bold, Ita... */
export class SendGroupCallMessage {
  type: 'sendGroupCallMessage' = 'sendGroupCallMessage';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** text: formattedText — Text of the message to send; 1-getOption("group_call_message_text_length_max") characters for non-live-stories; see updateGroupCallMessageLevels for live story restrictions, */
  text: FormattedText | null = null;
  /** paid_message_star_count: int53 — The number of Telegram Stars the user agreed to pay to send the message; for live stories only; 0-getOption("paid_group_call_message_star_count_max"). */
  paid_message_star_count: number = 0;
}

/** Decode a TDLib `sendGroupCallMessage` object; null input -> null. */
export function decodeSendGroupCallMessage(json: TdJsonRaw): SendGroupCallMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendGroupCallMessage = new SendGroupCallMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.text = decodeFormattedText(record['text']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  return out;
}

/** Encode a `sendGroupCallMessage` into TDLib JSON wire shape. */
export function encodeSendGroupCallMessage(value: SendGroupCallMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendGroupCallMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['paid_message_star_count'] = value.paid_message_star_count;
  return out;
}

/** sendInlineQueryResultMessage = Message — Sends the result of an inline query as a message. Returns the sent message. Always clears a chat draft message */
export class SendInlineQueryResultMessage {
  type: 'sendInlineQueryResultMessage' = 'sendInlineQueryResultMessage';
  extra?: string;
  /** chat_id: int53 — Target chat */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Topic in which the message will be sent; pass null if none */
  topic_id: TdMessageTopic | null = null;
  /** reply_to: InputMessageReplyTo — Information about the message or story to be replied; pass null if none */
  reply_to: TdInputMessageReplyTo | null = null;
  /** options: messageSendOptions — Options to be used to send the message; pass null to use default options */
  options: MessageSendOptions | null = null;
  /** query_id: int64 — Identifier of the inline query */
  query_id: string = '';
  /** result_id: string — Identifier of the inline query result */
  result_id: string = '';
  /** hide_via_bot: Bool — Pass true to hide the bot, via which the message is sent. Can be used only for bots getOption("animation_search_bot_username"), getOption("photo_search_bot_username"), and getOption("venue_search_bot_username") */
  hide_via_bot: boolean = false;
}

/** Decode a TDLib `sendInlineQueryResultMessage` object; null input -> null. */
export function decodeSendInlineQueryResultMessage(json: TdJsonRaw): SendInlineQueryResultMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendInlineQueryResultMessage = new SendInlineQueryResultMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.reply_to = decodeTdInputMessageReplyTo(record['reply_to']);
  out.options = decodeMessageSendOptions(record['options']);
  out.query_id = asInt64(record['query_id']);
  out.result_id = asString(record['result_id']);
  out.hide_via_bot = asBool(record['hide_via_bot']);
  return out;
}

/** Encode a `sendInlineQueryResultMessage` into TDLib JSON wire shape. */
export function encodeSendInlineQueryResultMessage(value: SendInlineQueryResultMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendInlineQueryResultMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['reply_to'] = value.reply_to === null ? null : encodeTdInputMessageReplyTo(value.reply_to);
  out['options'] = value.options === null ? null : encodeMessageSendOptions(value.options);
  out['query_id'] = value.query_id;
  out['result_id'] = value.result_id;
  out['hide_via_bot'] = value.hide_via_bot;
  return out;
}

/** sendMessage = Message — Sends a message. Returns the sent message */
export class SendMessage {
  type: 'sendMessage' = 'sendMessage';
  extra?: string;
  /** chat_id: int53 — Target chat */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Topic in which the message will be sent; pass null if none */
  topic_id: TdMessageTopic | null = null;
  /** reply_to: InputMessageReplyTo — Information about the message or story to be replied; pass null if none */
  reply_to: TdInputMessageReplyTo | null = null;
  /** options: messageSendOptions — Options to be used to send the message; pass null to use default options */
  options: MessageSendOptions | null = null;
  /** reply_markup: ReplyMarkup — Markup for replying to the message; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `sendMessage` object; null input -> null. */
export function decodeSendMessage(json: TdJsonRaw): SendMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendMessage = new SendMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.reply_to = decodeTdInputMessageReplyTo(record['reply_to']);
  out.options = decodeMessageSendOptions(record['options']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `sendMessage` into TDLib JSON wire shape. */
export function encodeSendMessage(value: SendMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['reply_to'] = value.reply_to === null ? null : encodeTdInputMessageReplyTo(value.reply_to);
  out['options'] = value.options === null ? null : encodeMessageSendOptions(value.options);
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** sendMessageAlbum = Messages — Sends 2-10 messages grouped together into an album. Currently, only audio, document, photo and video messages can be grouped into an album. Documents and audio files can be only grouped in an album with mess... */
export class SendMessageAlbum {
  type: 'sendMessageAlbum' = 'sendMessageAlbum';
  extra?: string;
  /** chat_id: int53 — Target chat */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Topic in which the messages will be sent; pass null if none */
  topic_id: TdMessageTopic | null = null;
  /** reply_to: InputMessageReplyTo — Information about the message or story to be replied; pass null if none */
  reply_to: TdInputMessageReplyTo | null = null;
  /** options: messageSendOptions — Options to be used to send the messages; pass null to use default options */
  options: MessageSendOptions | null = null;
  /** input_message_contents: vector<InputMessageContent> — Contents of messages to be sent. At most 10 messages can be added to an album. All messages must have the same value of show_caption_above_media */
  input_message_contents: TdInputMessageContent[] = [];
}

/** Decode a TDLib `sendMessageAlbum` object; null input -> null. */
export function decodeSendMessageAlbum(json: TdJsonRaw): SendMessageAlbum | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendMessageAlbum = new SendMessageAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.reply_to = decodeTdInputMessageReplyTo(record['reply_to']);
  out.options = decodeMessageSendOptions(record['options']);
  out.input_message_contents = decodeVector(record['input_message_contents'], (item: TdJsonRaw): TdInputMessageContent => decodeTdInputMessageContent(item) as TdInputMessageContent);
  return out;
}

/** Encode a `sendMessageAlbum` into TDLib JSON wire shape. */
export function encodeSendMessageAlbum(value: SendMessageAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendMessageAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['reply_to'] = value.reply_to === null ? null : encodeTdInputMessageReplyTo(value.reply_to);
  out['options'] = value.options === null ? null : encodeMessageSendOptions(value.options);
  out['input_message_contents'] = encodeVector(value.input_message_contents, (item: TdInputMessageContent | null): TdJsonRaw => item === null ? null : encodeTdInputMessageContent(item));
  return out;
}

/** sendMessageViewMetrics = Ok — Informs TDLib about details of a message view by the user from a chat, a message thread or a forum topic history. The method must be called if the message wasn't seen for more than 300 milliseconds, the view... */
export class SendMessageViewMetrics {
  type: 'sendMessageViewMetrics' = 'sendMessageViewMetrics';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — The identifier of the message being viewed */
  message_id: number = 0;
  /** time_in_view_ms: int32 — The amount of time the message was seen by at least 1 pixel; in milliseconds */
  time_in_view_ms: number = 0;
  /** active_time_in_view_ms: int32 — The amount of time the message was seen by at least 1 pixel within 15 seconds after any action from the user; in milliseconds */
  active_time_in_view_ms: number = 0;
  /** height_to_viewport_ratio_per_mille: int32 — The ratio of the post height to the viewport height in 1/1000 fractions */
  height_to_viewport_ratio_per_mille: number = 0;
  /** seen_range_ratio_per_mille: int32 — The ratio of the viewed post height to the full post height in 1/1000 fractions; 0-1000 */
  seen_range_ratio_per_mille: number = 0;
}

/** Decode a TDLib `sendMessageViewMetrics` object; null input -> null. */
export function decodeSendMessageViewMetrics(json: TdJsonRaw): SendMessageViewMetrics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendMessageViewMetrics = new SendMessageViewMetrics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.time_in_view_ms = asNumber(record['time_in_view_ms']);
  out.active_time_in_view_ms = asNumber(record['active_time_in_view_ms']);
  out.height_to_viewport_ratio_per_mille = asNumber(record['height_to_viewport_ratio_per_mille']);
  out.seen_range_ratio_per_mille = asNumber(record['seen_range_ratio_per_mille']);
  return out;
}

/** Encode a `sendMessageViewMetrics` into TDLib JSON wire shape. */
export function encodeSendMessageViewMetrics(value: SendMessageViewMetrics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendMessageViewMetrics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['time_in_view_ms'] = value.time_in_view_ms;
  out['active_time_in_view_ms'] = value.active_time_in_view_ms;
  out['height_to_viewport_ratio_per_mille'] = value.height_to_viewport_ratio_per_mille;
  out['seen_range_ratio_per_mille'] = value.seen_range_ratio_per_mille;
  return out;
}

/** sendPassportAuthorizationForm = Ok — Sends a Telegram Passport authorization form, effectively sharing data with the service. This method must be called after getPassportAuthorizationFormAvailableElements if some previously available ele... */
export class SendPassportAuthorizationForm {
  type: 'sendPassportAuthorizationForm' = 'sendPassportAuthorizationForm';
  extra?: string;
  /** authorization_form_id: int32 — Authorization form identifier */
  authorization_form_id: number = 0;
  /** types: vector<PassportElementType> — Types of Telegram Passport elements chosen by user to complete the authorization form */
  types: TdPassportElementType[] = [];
}

/** Decode a TDLib `sendPassportAuthorizationForm` object; null input -> null. */
export function decodeSendPassportAuthorizationForm(json: TdJsonRaw): SendPassportAuthorizationForm | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendPassportAuthorizationForm = new SendPassportAuthorizationForm();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.authorization_form_id = asNumber(record['authorization_form_id']);
  out.types = decodeVector(record['types'], (item: TdJsonRaw): TdPassportElementType => decodeTdPassportElementType(item) as TdPassportElementType);
  return out;
}

/** Encode a `sendPassportAuthorizationForm` into TDLib JSON wire shape. */
export function encodeSendPassportAuthorizationForm(value: SendPassportAuthorizationForm): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendPassportAuthorizationForm';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['authorization_form_id'] = value.authorization_form_id;
  out['types'] = encodeVector(value.types, (item: TdPassportElementType | null): TdJsonRaw => item === null ? null : encodeTdPassportElementType(item));
  return out;
}

/** sendPaymentForm = PaymentResult — Sends a filled-out payment form to the bot for final verification */
export class SendPaymentForm {
  type: 'sendPaymentForm' = 'sendPaymentForm';
  extra?: string;
  /** input_invoice: InputInvoice — The invoice */
  input_invoice: TdInputInvoice | null = null;
  /** payment_form_id: int64 — Payment form identifier returned by getPaymentForm */
  payment_form_id: string = '';
  /** order_info_id: string — Identifier returned by validateOrderInfo, or an empty string */
  order_info_id: string = '';
  /** shipping_option_id: string — Identifier of a chosen shipping option, if applicable */
  shipping_option_id: string = '';
  /** credentials: InputCredentials — The credentials chosen by user for payment; pass null for a payment in Telegram Stars */
  credentials: TdInputCredentials | null = null;
  /** tip_amount: int53 — Chosen by the user amount of tip in the smallest units of the currency */
  tip_amount: number = 0;
}

/** Decode a TDLib `sendPaymentForm` object; null input -> null. */
export function decodeSendPaymentForm(json: TdJsonRaw): SendPaymentForm | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendPaymentForm = new SendPaymentForm();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.input_invoice = decodeTdInputInvoice(record['input_invoice']);
  out.payment_form_id = asInt64(record['payment_form_id']);
  out.order_info_id = asString(record['order_info_id']);
  out.shipping_option_id = asString(record['shipping_option_id']);
  out.credentials = decodeTdInputCredentials(record['credentials']);
  out.tip_amount = asNumber(record['tip_amount']);
  return out;
}

/** Encode a `sendPaymentForm` into TDLib JSON wire shape. */
export function encodeSendPaymentForm(value: SendPaymentForm): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendPaymentForm';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['input_invoice'] = value.input_invoice === null ? null : encodeTdInputInvoice(value.input_invoice);
  out['payment_form_id'] = value.payment_form_id;
  out['order_info_id'] = value.order_info_id;
  out['shipping_option_id'] = value.shipping_option_id;
  out['credentials'] = value.credentials === null ? null : encodeTdInputCredentials(value.credentials);
  out['tip_amount'] = value.tip_amount;
  return out;
}

/** sendPhoneNumberCode = AuthenticationCodeInfo — Sends a code to the specified phone number. Aborts previous phone number verification if there was one. On success, returns information about the sent code */
export class SendPhoneNumberCode {
  type: 'sendPhoneNumberCode' = 'sendPhoneNumberCode';
  extra?: string;
  /** phone_number: string — The phone number, in international format */
  phone_number: string = '';
  /** settings: phoneNumberAuthenticationSettings — Settings for the authentication of the user's phone number; pass null to use default settings */
  settings: PhoneNumberAuthenticationSettings | null = null;
  /** type: PhoneNumberCodeType — Type of the request for which the code is sent */
  type_: TdPhoneNumberCodeType | null = null;
}

/** Decode a TDLib `sendPhoneNumberCode` object; null input -> null. */
export function decodeSendPhoneNumberCode(json: TdJsonRaw): SendPhoneNumberCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendPhoneNumberCode = new SendPhoneNumberCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  out.settings = decodePhoneNumberAuthenticationSettings(record['settings']);
  out.type_ = decodeTdPhoneNumberCodeType(record['type']);
  return out;
}

/** Encode a `sendPhoneNumberCode` into TDLib JSON wire shape. */
export function encodeSendPhoneNumberCode(value: SendPhoneNumberCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendPhoneNumberCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  out['settings'] = value.settings === null ? null : encodePhoneNumberAuthenticationSettings(value.settings);
  out['type'] = value.type_ === null ? null : encodeTdPhoneNumberCodeType(value.type_);
  return out;
}

/** sendPhoneNumberFirebaseSms = Ok — Sends Firebase Authentication SMS to the specified phone number. Works only when received a code of the type authenticationCodeTypeFirebaseAndroid or authenticationCodeTypeFirebaseIos */
export class SendPhoneNumberFirebaseSms {
  type: 'sendPhoneNumberFirebaseSms' = 'sendPhoneNumberFirebaseSms';
  extra?: string;
  /** token: string — Play Integrity API or SafetyNet Attestation API token for the Android application, or secret from push notification for the iOS application */
  token: string = '';
}

/** Decode a TDLib `sendPhoneNumberFirebaseSms` object; null input -> null. */
export function decodeSendPhoneNumberFirebaseSms(json: TdJsonRaw): SendPhoneNumberFirebaseSms | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendPhoneNumberFirebaseSms = new SendPhoneNumberFirebaseSms();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `sendPhoneNumberFirebaseSms` into TDLib JSON wire shape. */
export function encodeSendPhoneNumberFirebaseSms(value: SendPhoneNumberFirebaseSms): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendPhoneNumberFirebaseSms';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** sendQuickReplyShortcutMessages = Messages — Sends messages from a quick reply shortcut. Requires Telegram Business subscription. Can't be used to send paid messages */
export class SendQuickReplyShortcutMessages {
  type: 'sendQuickReplyShortcutMessages' = 'sendQuickReplyShortcutMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which to send messages. The chat must be a private chat with a regular user */
  chat_id: number = 0;
  /** shortcut_id: int32 — Unique identifier of the quick reply shortcut */
  shortcut_id: number = 0;
  /** sending_id: int32 — Non-persistent identifier, which will be returned back in messageSendingStatePending object and can be used to match sent messages and corresponding updateNewMessage updates */
  sending_id: number = 0;
}

/** Decode a TDLib `sendQuickReplyShortcutMessages` object; null input -> null. */
export function decodeSendQuickReplyShortcutMessages(json: TdJsonRaw): SendQuickReplyShortcutMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendQuickReplyShortcutMessages = new SendQuickReplyShortcutMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.shortcut_id = asNumber(record['shortcut_id']);
  out.sending_id = asNumber(record['sending_id']);
  return out;
}

/** Encode a `sendQuickReplyShortcutMessages` into TDLib JSON wire shape. */
export function encodeSendQuickReplyShortcutMessages(value: SendQuickReplyShortcutMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendQuickReplyShortcutMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['shortcut_id'] = value.shortcut_id;
  out['sending_id'] = value.sending_id;
  return out;
}

/** sendResoldGift = GiftResaleResult — Sends an upgraded gift that is available for resale to another user or channel chat; gifts already owned by the current user must be transferred using transferGift and can't be passed to the method Mus... */
export class SendResoldGift {
  type: 'sendResoldGift' = 'sendResoldGift';
  extra?: string;
  /** gift_name: string — Name of the upgraded gift to send */
  gift_name: string = '';
  /** owner_id: MessageSender — Identifier of the user or the channel chat that will receive the gift */
  owner_id: TdMessageSender | null = null;
  /** price: GiftResalePrice — The price that the user agreed to pay for the gift */
  price: TdGiftResalePrice | null = null;
  /** text: formattedText — Text to show along with the gift; 0-getOption("gift_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed. */
  text: FormattedText | null = null;
  /** is_private: Bool — Pass true to show gift text and sender only to the gift receiver; otherwise, everyone will be able to see them */
  is_private: boolean = false;
}

/** Decode a TDLib `sendResoldGift` object; null input -> null. */
export function decodeSendResoldGift(json: TdJsonRaw): SendResoldGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendResoldGift = new SendResoldGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_name = asString(record['gift_name']);
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.price = decodeTdGiftResalePrice(record['price']);
  out.text = decodeFormattedText(record['text']);
  out.is_private = asBool(record['is_private']);
  return out;
}

/** Encode a `sendResoldGift` into TDLib JSON wire shape. */
export function encodeSendResoldGift(value: SendResoldGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendResoldGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_name'] = value.gift_name;
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['price'] = value.price === null ? null : encodeTdGiftResalePrice(value.price);
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['is_private'] = value.is_private;
  return out;
}

/** sendRichMessageDraft = Ok — Sends a draft for a being generated rich message; for bots only */
export class SendRichMessageDraft {
  type: 'sendRichMessageDraft' = 'sendRichMessageDraft';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — The forum topic identifier in which the message will be sent; pass 0 if none */
  forum_topic_id: number = 0;
  /** draft_id: int64 — Unique identifier of the draft */
  draft_id: string = '';
  /** can_stop: Bool — Pass true to show the user a button to stop further drafts */
  can_stop: boolean = false;
  /** keep_on_stop: Bool — Pass true to keep the current draft when the user stops further generation */
  keep_on_stop: boolean = false;
  /** message: inputRichMessage — Draft of the message; file upload isn't supported */
  message: InputRichMessage | null = null;
}

/** Decode a TDLib `sendRichMessageDraft` object; null input -> null. */
export function decodeSendRichMessageDraft(json: TdJsonRaw): SendRichMessageDraft | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendRichMessageDraft = new SendRichMessageDraft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.draft_id = asInt64(record['draft_id']);
  out.can_stop = asBool(record['can_stop']);
  out.keep_on_stop = asBool(record['keep_on_stop']);
  out.message = decodeInputRichMessage(record['message']);
  return out;
}

/** Encode a `sendRichMessageDraft` into TDLib JSON wire shape. */
export function encodeSendRichMessageDraft(value: SendRichMessageDraft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendRichMessageDraft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['draft_id'] = value.draft_id;
  out['can_stop'] = value.can_stop;
  out['keep_on_stop'] = value.keep_on_stop;
  out['message'] = value.message === null ? null : encodeInputRichMessage(value.message);
  return out;
}

/** sendTextMessageDraft = Ok — Sends a draft for a being generated text message; for bots only */
export class SendTextMessageDraft {
  type: 'sendTextMessageDraft' = 'sendTextMessageDraft';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — The forum topic identifier in which the message will be sent; pass 0 if none */
  forum_topic_id: number = 0;
  /** draft_id: int64 — Unique identifier of the draft */
  draft_id: string = '';
  /** can_stop: Bool — Pass true to show the user a button to stop further drafts */
  can_stop: boolean = false;
  /** keep_on_stop: Bool — Pass true to keep the current draft when the user stops further generation */
  keep_on_stop: boolean = false;
  /** text: formattedText — Draft text of the message; pass null to show a "Thinking..." placeholder */
  text: FormattedText | null = null;
}

/** Decode a TDLib `sendTextMessageDraft` object; null input -> null. */
export function decodeSendTextMessageDraft(json: TdJsonRaw): SendTextMessageDraft | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendTextMessageDraft = new SendTextMessageDraft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.draft_id = asInt64(record['draft_id']);
  out.can_stop = asBool(record['can_stop']);
  out.keep_on_stop = asBool(record['keep_on_stop']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `sendTextMessageDraft` into TDLib JSON wire shape. */
export function encodeSendTextMessageDraft(value: SendTextMessageDraft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendTextMessageDraft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['draft_id'] = value.draft_id;
  out['can_stop'] = value.can_stop;
  out['keep_on_stop'] = value.keep_on_stop;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** sendWebAppCustomRequest = CustomRequestResult — Sends a custom request from a Web App */
export class SendWebAppCustomRequest {
  type: 'sendWebAppCustomRequest' = 'sendWebAppCustomRequest';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the bot */
  bot_user_id: number = 0;
  /** method: string — The method name */
  method: string = '';
  /** parameters: string — JSON-serialized method parameters */
  parameters: string = '';
}

/** Decode a TDLib `sendWebAppCustomRequest` object; null input -> null. */
export function decodeSendWebAppCustomRequest(json: TdJsonRaw): SendWebAppCustomRequest | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendWebAppCustomRequest = new SendWebAppCustomRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.method = asString(record['method']);
  out.parameters = asString(record['parameters']);
  return out;
}

/** Encode a `sendWebAppCustomRequest` into TDLib JSON wire shape. */
export function encodeSendWebAppCustomRequest(value: SendWebAppCustomRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendWebAppCustomRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['method'] = value.method;
  out['parameters'] = value.parameters;
  return out;
}

/** sendWebAppData = Ok — Sends data received from a keyboardButtonTypeWebApp Web App to a bot */
export class SendWebAppData {
  type: 'sendWebAppData' = 'sendWebAppData';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** button_text: string — Text of the keyboardButtonTypeWebApp button, which opened the Web App */
  button_text: string = '';
  /** data: string — The data */
  data: string = '';
}

/** Decode a TDLib `sendWebAppData` object; null input -> null. */
export function decodeSendWebAppData(json: TdJsonRaw): SendWebAppData | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SendWebAppData = new SendWebAppData();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.button_text = asString(record['button_text']);
  out.data = asString(record['data']);
  return out;
}

/** Encode a `sendWebAppData` into TDLib JSON wire shape. */
export function encodeSendWebAppData(value: SendWebAppData): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sendWebAppData';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['button_text'] = value.button_text;
  out['data'] = value.data;
  return out;
}

/** sentGiftRegular = SentGift — Regular gift */
export class SentGiftRegular {
  type: 'sentGiftRegular' = 'sentGiftRegular';
  extra?: string;
  /** gift: gift — The gift */
  gift: Gift | null = null;
}

/** Decode a TDLib `sentGiftRegular` object; null input -> null. */
export function decodeSentGiftRegular(json: TdJsonRaw): SentGiftRegular | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SentGiftRegular = new SentGiftRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeGift(record['gift']);
  return out;
}

/** Encode a `sentGiftRegular` into TDLib JSON wire shape. */
export function encodeSentGiftRegular(value: SentGiftRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sentGiftRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  return out;
}

/** sentGiftUpgraded = SentGift — Upgraded gift */
export class SentGiftUpgraded {
  type: 'sentGiftUpgraded' = 'sentGiftUpgraded';
  extra?: string;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
}

/** Decode a TDLib `sentGiftUpgraded` object; null input -> null. */
export function decodeSentGiftUpgraded(json: TdJsonRaw): SentGiftUpgraded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SentGiftUpgraded = new SentGiftUpgraded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  return out;
}

/** Encode a `sentGiftUpgraded` into TDLib JSON wire shape. */
export function encodeSentGiftUpgraded(value: SentGiftUpgraded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sentGiftUpgraded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  return out;
}

/** session = Session — Contains information about one session in a Telegram application used by the current user. Sessions must be shown to the user in the returned order */
export class Session {
  type: 'session' = 'session';
  extra?: string;
  /** id: int64 — Session identifier */
  id: string = '';
  /** is_current: Bool — True, if this session is the current session */
  is_current: boolean = false;
  /** is_password_pending: Bool — True, if a 2-step verification password is needed to complete authorization of the session */
  is_password_pending: boolean = false;
  /** is_unconfirmed: Bool — True, if the session wasn't confirmed from another session */
  is_unconfirmed: boolean = false;
  /** can_accept_secret_chats: Bool — True, if incoming secret chats can be accepted by the session */
  can_accept_secret_chats: boolean = false;
  /** can_accept_calls: Bool — True, if incoming calls can be accepted by the session */
  can_accept_calls: boolean = false;
  /** device_type: SessionDeviceType — Session device type based on the system and application version, which can be used to display a corresponding icon */
  device_type: TdSessionDeviceType | null = null;
  /** api_id: int32 — Telegram API identifier, as provided by the application */
  api_id: number = 0;
  /** application_name: string — Name of the application, as provided by the application */
  application_name: string = '';
  /** application_version: string — The version of the application, as provided by the application */
  application_version: string = '';
  /** is_official_application: Bool — True, if the application is an official application or uses the api_id of an official application */
  is_official_application: boolean = false;
  /** device_model: string — Model of the device the application has been run or is running on, as provided by the application */
  device_model: string = '';
  /** platform: string — Operating system the application has been run or is running on, as provided by the application */
  platform: string = '';
  /** system_version: string — Version of the operating system the application has been run or is running on, as provided by the application */
  system_version: string = '';
  /** log_in_date: int32 — Point in time (Unix timestamp) when the user has logged in */
  log_in_date: number = 0;
  /** last_active_date: int32 — Point in time (Unix timestamp) when the session was last used */
  last_active_date: number = 0;
  /** ip_address: string — IP address from which the session was created, in human-readable format */
  ip_address: string = '';
  /** location: string — A human-readable description of the location from which the session was created, based on the IP address */
  location: string = '';
}

/** Decode a TDLib `session` object; null input -> null. */
export function decodeSession(json: TdJsonRaw): Session | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Session = new Session();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.is_current = asBool(record['is_current']);
  out.is_password_pending = asBool(record['is_password_pending']);
  out.is_unconfirmed = asBool(record['is_unconfirmed']);
  out.can_accept_secret_chats = asBool(record['can_accept_secret_chats']);
  out.can_accept_calls = asBool(record['can_accept_calls']);
  out.device_type = decodeTdSessionDeviceType(record['device_type']);
  out.api_id = asNumber(record['api_id']);
  out.application_name = asString(record['application_name']);
  out.application_version = asString(record['application_version']);
  out.is_official_application = asBool(record['is_official_application']);
  out.device_model = asString(record['device_model']);
  out.platform = asString(record['platform']);
  out.system_version = asString(record['system_version']);
  out.log_in_date = asNumber(record['log_in_date']);
  out.last_active_date = asNumber(record['last_active_date']);
  out.ip_address = asString(record['ip_address']);
  out.location = asString(record['location']);
  return out;
}

/** Encode a `session` into TDLib JSON wire shape. */
export function encodeSession(value: Session): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'session';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['is_current'] = value.is_current;
  out['is_password_pending'] = value.is_password_pending;
  out['is_unconfirmed'] = value.is_unconfirmed;
  out['can_accept_secret_chats'] = value.can_accept_secret_chats;
  out['can_accept_calls'] = value.can_accept_calls;
  out['device_type'] = value.device_type === null ? null : encodeTdSessionDeviceType(value.device_type);
  out['api_id'] = value.api_id;
  out['application_name'] = value.application_name;
  out['application_version'] = value.application_version;
  out['is_official_application'] = value.is_official_application;
  out['device_model'] = value.device_model;
  out['platform'] = value.platform;
  out['system_version'] = value.system_version;
  out['log_in_date'] = value.log_in_date;
  out['last_active_date'] = value.last_active_date;
  out['ip_address'] = value.ip_address;
  out['location'] = value.location;
  return out;
}

/** sessionDeviceTypeAndroid = SessionDeviceType — The session is running on an Android device */
export class SessionDeviceTypeAndroid {
  type: 'sessionDeviceTypeAndroid' = 'sessionDeviceTypeAndroid';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeAndroid` object; null input -> null. */
export function decodeSessionDeviceTypeAndroid(json: TdJsonRaw): SessionDeviceTypeAndroid | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeAndroid = new SessionDeviceTypeAndroid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeAndroid` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeAndroid(value: SessionDeviceTypeAndroid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeAndroid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeApple = SessionDeviceType — The session is running on a generic Apple device */
export class SessionDeviceTypeApple {
  type: 'sessionDeviceTypeApple' = 'sessionDeviceTypeApple';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeApple` object; null input -> null. */
export function decodeSessionDeviceTypeApple(json: TdJsonRaw): SessionDeviceTypeApple | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeApple = new SessionDeviceTypeApple();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeApple` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeApple(value: SessionDeviceTypeApple): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeApple';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeBrave = SessionDeviceType — The session is running on the Brave browser */
export class SessionDeviceTypeBrave {
  type: 'sessionDeviceTypeBrave' = 'sessionDeviceTypeBrave';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeBrave` object; null input -> null. */
export function decodeSessionDeviceTypeBrave(json: TdJsonRaw): SessionDeviceTypeBrave | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeBrave = new SessionDeviceTypeBrave();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeBrave` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeBrave(value: SessionDeviceTypeBrave): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeBrave';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeChrome = SessionDeviceType — The session is running on the Chrome browser */
export class SessionDeviceTypeChrome {
  type: 'sessionDeviceTypeChrome' = 'sessionDeviceTypeChrome';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeChrome` object; null input -> null. */
export function decodeSessionDeviceTypeChrome(json: TdJsonRaw): SessionDeviceTypeChrome | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeChrome = new SessionDeviceTypeChrome();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeChrome` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeChrome(value: SessionDeviceTypeChrome): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeChrome';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeEdge = SessionDeviceType — The session is running on the Edge browser */
export class SessionDeviceTypeEdge {
  type: 'sessionDeviceTypeEdge' = 'sessionDeviceTypeEdge';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeEdge` object; null input -> null. */
export function decodeSessionDeviceTypeEdge(json: TdJsonRaw): SessionDeviceTypeEdge | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeEdge = new SessionDeviceTypeEdge();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeEdge` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeEdge(value: SessionDeviceTypeEdge): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeEdge';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeFirefox = SessionDeviceType — The session is running on the Firefox browser */
export class SessionDeviceTypeFirefox {
  type: 'sessionDeviceTypeFirefox' = 'sessionDeviceTypeFirefox';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeFirefox` object; null input -> null. */
export function decodeSessionDeviceTypeFirefox(json: TdJsonRaw): SessionDeviceTypeFirefox | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeFirefox = new SessionDeviceTypeFirefox();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeFirefox` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeFirefox(value: SessionDeviceTypeFirefox): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeFirefox';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeIpad = SessionDeviceType — The session is running on an iPad device */
export class SessionDeviceTypeIpad {
  type: 'sessionDeviceTypeIpad' = 'sessionDeviceTypeIpad';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeIpad` object; null input -> null. */
export function decodeSessionDeviceTypeIpad(json: TdJsonRaw): SessionDeviceTypeIpad | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeIpad = new SessionDeviceTypeIpad();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeIpad` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeIpad(value: SessionDeviceTypeIpad): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeIpad';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeIphone = SessionDeviceType — The session is running on an iPhone device */
export class SessionDeviceTypeIphone {
  type: 'sessionDeviceTypeIphone' = 'sessionDeviceTypeIphone';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeIphone` object; null input -> null. */
export function decodeSessionDeviceTypeIphone(json: TdJsonRaw): SessionDeviceTypeIphone | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeIphone = new SessionDeviceTypeIphone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeIphone` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeIphone(value: SessionDeviceTypeIphone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeIphone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeLinux = SessionDeviceType — The session is running on a Linux device */
export class SessionDeviceTypeLinux {
  type: 'sessionDeviceTypeLinux' = 'sessionDeviceTypeLinux';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeLinux` object; null input -> null. */
export function decodeSessionDeviceTypeLinux(json: TdJsonRaw): SessionDeviceTypeLinux | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeLinux = new SessionDeviceTypeLinux();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeLinux` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeLinux(value: SessionDeviceTypeLinux): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeLinux';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeMac = SessionDeviceType — The session is running on a Mac device */
export class SessionDeviceTypeMac {
  type: 'sessionDeviceTypeMac' = 'sessionDeviceTypeMac';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeMac` object; null input -> null. */
export function decodeSessionDeviceTypeMac(json: TdJsonRaw): SessionDeviceTypeMac | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeMac = new SessionDeviceTypeMac();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeMac` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeMac(value: SessionDeviceTypeMac): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeMac';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeOpera = SessionDeviceType — The session is running on the Opera browser */
export class SessionDeviceTypeOpera {
  type: 'sessionDeviceTypeOpera' = 'sessionDeviceTypeOpera';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeOpera` object; null input -> null. */
export function decodeSessionDeviceTypeOpera(json: TdJsonRaw): SessionDeviceTypeOpera | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeOpera = new SessionDeviceTypeOpera();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeOpera` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeOpera(value: SessionDeviceTypeOpera): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeOpera';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeSafari = SessionDeviceType — The session is running on the Safari browser */
export class SessionDeviceTypeSafari {
  type: 'sessionDeviceTypeSafari' = 'sessionDeviceTypeSafari';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeSafari` object; null input -> null. */
export function decodeSessionDeviceTypeSafari(json: TdJsonRaw): SessionDeviceTypeSafari | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeSafari = new SessionDeviceTypeSafari();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeSafari` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeSafari(value: SessionDeviceTypeSafari): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeSafari';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeUbuntu = SessionDeviceType — The session is running on an Ubuntu device */
export class SessionDeviceTypeUbuntu {
  type: 'sessionDeviceTypeUbuntu' = 'sessionDeviceTypeUbuntu';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeUbuntu` object; null input -> null. */
export function decodeSessionDeviceTypeUbuntu(json: TdJsonRaw): SessionDeviceTypeUbuntu | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeUbuntu = new SessionDeviceTypeUbuntu();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeUbuntu` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeUbuntu(value: SessionDeviceTypeUbuntu): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeUbuntu';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeUnknown = SessionDeviceType — The session is running on an unknown type of device */
export class SessionDeviceTypeUnknown {
  type: 'sessionDeviceTypeUnknown' = 'sessionDeviceTypeUnknown';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeUnknown` object; null input -> null. */
export function decodeSessionDeviceTypeUnknown(json: TdJsonRaw): SessionDeviceTypeUnknown | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeUnknown = new SessionDeviceTypeUnknown();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeUnknown` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeUnknown(value: SessionDeviceTypeUnknown): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeUnknown';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeVivaldi = SessionDeviceType — The session is running on the Vivaldi browser */
export class SessionDeviceTypeVivaldi {
  type: 'sessionDeviceTypeVivaldi' = 'sessionDeviceTypeVivaldi';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeVivaldi` object; null input -> null. */
export function decodeSessionDeviceTypeVivaldi(json: TdJsonRaw): SessionDeviceTypeVivaldi | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeVivaldi = new SessionDeviceTypeVivaldi();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeVivaldi` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeVivaldi(value: SessionDeviceTypeVivaldi): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeVivaldi';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeWindows = SessionDeviceType — The session is running on a Windows device */
export class SessionDeviceTypeWindows {
  type: 'sessionDeviceTypeWindows' = 'sessionDeviceTypeWindows';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeWindows` object; null input -> null. */
export function decodeSessionDeviceTypeWindows(json: TdJsonRaw): SessionDeviceTypeWindows | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeWindows = new SessionDeviceTypeWindows();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeWindows` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeWindows(value: SessionDeviceTypeWindows): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeWindows';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionDeviceTypeXbox = SessionDeviceType — The session is running on an Xbox console */
export class SessionDeviceTypeXbox {
  type: 'sessionDeviceTypeXbox' = 'sessionDeviceTypeXbox';
  extra?: string;
}

/** Decode a TDLib `sessionDeviceTypeXbox` object; null input -> null. */
export function decodeSessionDeviceTypeXbox(json: TdJsonRaw): SessionDeviceTypeXbox | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionDeviceTypeXbox = new SessionDeviceTypeXbox();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `sessionDeviceTypeXbox` into TDLib JSON wire shape. */
export function encodeSessionDeviceTypeXbox(value: SessionDeviceTypeXbox): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionDeviceTypeXbox';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** sessionTypeConnectedBot = SessionType — A business bot connected to the current user's account */
export class SessionTypeConnectedBot {
  type: 'sessionTypeConnectedBot' = 'sessionTypeConnectedBot';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the bot. Use deleteBusinessConnectedBot to remove it or confirmBusinessConnectedBot to confirm it if it isn't confirmed yet */
  bot_user_id: number = 0;
}

/** Decode a TDLib `sessionTypeConnectedBot` object; null input -> null. */
export function decodeSessionTypeConnectedBot(json: TdJsonRaw): SessionTypeConnectedBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionTypeConnectedBot = new SessionTypeConnectedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `sessionTypeConnectedBot` into TDLib JSON wire shape. */
export function encodeSessionTypeConnectedBot(value: SessionTypeConnectedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionTypeConnectedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** sessionTypeDevice = SessionType — A regular session from a device */
export class SessionTypeDevice {
  type: 'sessionTypeDevice' = 'sessionTypeDevice';
  extra?: string;
  /** session_id: int64 — Unique identifier of the session. Use terminateSession to terminate it or confirmSession to confirm it if it isn't confirmed yet */
  session_id: string = '';
}

/** Decode a TDLib `sessionTypeDevice` object; null input -> null. */
export function decodeSessionTypeDevice(json: TdJsonRaw): SessionTypeDevice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SessionTypeDevice = new SessionTypeDevice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.session_id = asInt64(record['session_id']);
  return out;
}

/** Encode a `sessionTypeDevice` into TDLib JSON wire shape. */
export function encodeSessionTypeDevice(value: SessionTypeDevice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessionTypeDevice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['session_id'] = value.session_id;
  return out;
}

/** sessions = Sessions — Contains a list of sessions */
export class Sessions {
  type: 'sessions' = 'sessions';
  extra?: string;
  /** sessions: vector<session> — List of sessions */
  sessions: Session[] = [];
  /** inactive_session_ttl_days: int32 — Number of days of inactivity before sessions will automatically be terminated; 1-366 days */
  inactive_session_ttl_days: number = 0;
}

/** Decode a TDLib `sessions` object; null input -> null. */
export function decodeSessions(json: TdJsonRaw): Sessions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Sessions = new Sessions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sessions = decodeVector(record['sessions'], (item: TdJsonRaw): Session => decodeSession(item) as Session);
  out.inactive_session_ttl_days = asNumber(record['inactive_session_ttl_days']);
  return out;
}

/** Encode a `sessions` into TDLib JSON wire shape. */
export function encodeSessions(value: Sessions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sessions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sessions'] = encodeVector(value.sessions, (item: Session | null): TdJsonRaw => item === null ? null : encodeSession(item));
  out['inactive_session_ttl_days'] = value.inactive_session_ttl_days;
  return out;
}

/** setAccentColor = Ok — Changes accent color and background custom emoji for the current user; for Telegram Premium users only */
export class SetAccentColor {
  type: 'setAccentColor' = 'setAccentColor';
  extra?: string;
  /** accent_color_id: int32 — Identifier of the accent color to use */
  accent_color_id: number = 0;
  /** background_custom_emoji_id: int64 — Identifier of a custom emoji to be shown on the reply header and link preview background; 0 if none */
  background_custom_emoji_id: string = '';
}

/** Decode a TDLib `setAccentColor` object; null input -> null. */
export function decodeSetAccentColor(json: TdJsonRaw): SetAccentColor | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetAccentColor = new SetAccentColor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.accent_color_id = asNumber(record['accent_color_id']);
  out.background_custom_emoji_id = asInt64(record['background_custom_emoji_id']);
  return out;
}

/** Encode a `setAccentColor` into TDLib JSON wire shape. */
export function encodeSetAccentColor(value: SetAccentColor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setAccentColor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['accent_color_id'] = value.accent_color_id;
  out['background_custom_emoji_id'] = value.background_custom_emoji_id;
  return out;
}

/** setAccountTtl = Ok — Changes the period of inactivity after which the account of the current user will automatically be deleted */
export class SetAccountTtl {
  type: 'setAccountTtl' = 'setAccountTtl';
  extra?: string;
  /** ttl: accountTtl — New account TTL */
  ttl: AccountTtl | null = null;
}

/** Decode a TDLib `setAccountTtl` object; null input -> null. */
export function decodeSetAccountTtl(json: TdJsonRaw): SetAccountTtl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetAccountTtl = new SetAccountTtl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.ttl = decodeAccountTtl(record['ttl']);
  return out;
}

/** Encode a `setAccountTtl` into TDLib JSON wire shape. */
export function encodeSetAccountTtl(value: SetAccountTtl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setAccountTtl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['ttl'] = value.ttl === null ? null : encodeAccountTtl(value.ttl);
  return out;
}

/** setAlarm = Ok — Succeeds after a specified amount of time has passed. Can be called before initialization */
export class SetAlarm {
  type: 'setAlarm' = 'setAlarm';
  extra?: string;
  /** seconds: double — Number of seconds before the function returns */
  seconds: number = 0;
}

/** Decode a TDLib `setAlarm` object; null input -> null. */
export function decodeSetAlarm(json: TdJsonRaw): SetAlarm | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetAlarm = new SetAlarm();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.seconds = asNumber(record['seconds']);
  return out;
}

/** Encode a `setAlarm` into TDLib JSON wire shape. */
export function encodeSetAlarm(value: SetAlarm): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setAlarm';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['seconds'] = value.seconds;
  return out;
}

/** setApplicationVerificationToken = Ok — Informs TDLib that application or reCAPTCHA verification has been completed. Can be called before authorization pass an empty string to abort verification and receive the error "VERIFICATION_FAILED"... */
export class SetApplicationVerificationToken {
  type: 'setApplicationVerificationToken' = 'setApplicationVerificationToken';
  extra?: string;
  /** verification_id: int53 — Unique identifier for the verification process as received from updateApplicationVerificationRequired or updateApplicationRecaptchaVerificationRequired */
  verification_id: number = 0;
  /** token: string — Play Integrity API token for the Android application, or secret from push notification for the iOS application for application verification, or reCAPTCHA token for reCAPTCHA verifications; */
  token: string = '';
}

/** Decode a TDLib `setApplicationVerificationToken` object; null input -> null. */
export function decodeSetApplicationVerificationToken(json: TdJsonRaw): SetApplicationVerificationToken | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetApplicationVerificationToken = new SetApplicationVerificationToken();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.verification_id = asNumber(record['verification_id']);
  out.token = asString(record['token']);
  return out;
}

/** Encode a `setApplicationVerificationToken` into TDLib JSON wire shape. */
export function encodeSetApplicationVerificationToken(value: SetApplicationVerificationToken): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setApplicationVerificationToken';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['verification_id'] = value.verification_id;
  out['token'] = value.token;
  return out;
}

/** setArchiveChatListSettings = Ok — Changes settings for automatic moving of chats to and from the Archive chat lists */
export class SetArchiveChatListSettings {
  type: 'setArchiveChatListSettings' = 'setArchiveChatListSettings';
  extra?: string;
  /** settings: archiveChatListSettings — New settings */
  settings: ArchiveChatListSettings | null = null;
}

/** Decode a TDLib `setArchiveChatListSettings` object; null input -> null. */
export function decodeSetArchiveChatListSettings(json: TdJsonRaw): SetArchiveChatListSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetArchiveChatListSettings = new SetArchiveChatListSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.settings = decodeArchiveChatListSettings(record['settings']);
  return out;
}

/** Encode a `setArchiveChatListSettings` into TDLib JSON wire shape. */
export function encodeSetArchiveChatListSettings(value: SetArchiveChatListSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setArchiveChatListSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['settings'] = value.settings === null ? null : encodeArchiveChatListSettings(value.settings);
  return out;
}

/** setAuthenticationEmailAddress = Ok — Sets the email address of the user and sends an authentication code to the email address. Works only when the current authorization state is authorizationStateWaitEmailAddress */
export class SetAuthenticationEmailAddress {
  type: 'setAuthenticationEmailAddress' = 'setAuthenticationEmailAddress';
  extra?: string;
  /** email_address: string — The email address of the user */
  email_address: string = '';
}

/** Decode a TDLib `setAuthenticationEmailAddress` object; null input -> null. */
export function decodeSetAuthenticationEmailAddress(json: TdJsonRaw): SetAuthenticationEmailAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetAuthenticationEmailAddress = new SetAuthenticationEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.email_address = asString(record['email_address']);
  return out;
}

/** Encode a `setAuthenticationEmailAddress` into TDLib JSON wire shape. */
export function encodeSetAuthenticationEmailAddress(value: SetAuthenticationEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setAuthenticationEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['email_address'] = value.email_address;
  return out;
}

/** setAuthenticationPhoneNumber = Ok — Sets the phone number of the user and sends an authentication code to the user. Works only when the current authorization state is authorizationStateWaitPhoneNumber, or if there is no pending authentic... */
export class SetAuthenticationPhoneNumber {
  type: 'setAuthenticationPhoneNumber' = 'setAuthenticationPhoneNumber';
  extra?: string;
  /** phone_number: string — The phone number of the user, in international format */
  phone_number: string = '';
  /** settings: phoneNumberAuthenticationSettings — Settings for the authentication of the user's phone number; pass null to use default settings */
  settings: PhoneNumberAuthenticationSettings | null = null;
}

/** Decode a TDLib `setAuthenticationPhoneNumber` object; null input -> null. */
export function decodeSetAuthenticationPhoneNumber(json: TdJsonRaw): SetAuthenticationPhoneNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetAuthenticationPhoneNumber = new SetAuthenticationPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  out.settings = decodePhoneNumberAuthenticationSettings(record['settings']);
  return out;
}

/** Encode a `setAuthenticationPhoneNumber` into TDLib JSON wire shape. */
export function encodeSetAuthenticationPhoneNumber(value: SetAuthenticationPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setAuthenticationPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  out['settings'] = value.settings === null ? null : encodePhoneNumberAuthenticationSettings(value.settings);
  return out;
}

/** setAuthenticationPremiumPurchaseTransaction = Ok — Informs server about an in-store purchase of Telegram Premium before authorization. Works only when the current authorization state is authorizationStateWaitPremiumPurchase */
export class SetAuthenticationPremiumPurchaseTransaction {
  type: 'setAuthenticationPremiumPurchaseTransaction' = 'setAuthenticationPremiumPurchaseTransaction';
  extra?: string;
  /** transaction: StoreTransaction — Information about the transaction */
  transaction: TdStoreTransaction | null = null;
  /** is_restore: Bool — Pass true if this is a restore of a Telegram Premium purchase; only for App Store */
  is_restore: boolean = false;
  /** premium_day_count: int32 — The number of days for which the Telegram Premium subscription will be granted */
  premium_day_count: number = 0;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
}

/** Decode a TDLib `setAuthenticationPremiumPurchaseTransaction` object; null input -> null. */
export function decodeSetAuthenticationPremiumPurchaseTransaction(json: TdJsonRaw): SetAuthenticationPremiumPurchaseTransaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetAuthenticationPremiumPurchaseTransaction = new SetAuthenticationPremiumPurchaseTransaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.transaction = decodeTdStoreTransaction(record['transaction']);
  out.is_restore = asBool(record['is_restore']);
  out.premium_day_count = asNumber(record['premium_day_count']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  return out;
}

/** Encode a `setAuthenticationPremiumPurchaseTransaction` into TDLib JSON wire shape. */
export function encodeSetAuthenticationPremiumPurchaseTransaction(value: SetAuthenticationPremiumPurchaseTransaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setAuthenticationPremiumPurchaseTransaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['transaction'] = value.transaction === null ? null : encodeTdStoreTransaction(value.transaction);
  out['is_restore'] = value.is_restore;
  out['premium_day_count'] = value.premium_day_count;
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  return out;
}

/** setAutoDownloadSettings = Ok — Sets auto-download settings */
export class SetAutoDownloadSettings {
  type: 'setAutoDownloadSettings' = 'setAutoDownloadSettings';
  extra?: string;
  /** settings: autoDownloadSettings — New user auto-download settings */
  settings: AutoDownloadSettings | null = null;
  /** type: NetworkType — Type of the network for which the new settings are relevant */
  type_: TdNetworkType | null = null;
}

/** Decode a TDLib `setAutoDownloadSettings` object; null input -> null. */
export function decodeSetAutoDownloadSettings(json: TdJsonRaw): SetAutoDownloadSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetAutoDownloadSettings = new SetAutoDownloadSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.settings = decodeAutoDownloadSettings(record['settings']);
  out.type_ = decodeTdNetworkType(record['type']);
  return out;
}

/** Encode a `setAutoDownloadSettings` into TDLib JSON wire shape. */
export function encodeSetAutoDownloadSettings(value: SetAutoDownloadSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setAutoDownloadSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['settings'] = value.settings === null ? null : encodeAutoDownloadSettings(value.settings);
  out['type'] = value.type_ === null ? null : encodeTdNetworkType(value.type_);
  return out;
}

/** setAutosaveSettings = Ok — Sets autosave settings for the given scope. The method is guaranteed to work only after at least one call to getAutosaveSettings */
export class SetAutosaveSettings {
  type: 'setAutosaveSettings' = 'setAutosaveSettings';
  extra?: string;
  /** scope: AutosaveSettingsScope — Autosave settings scope */
  scope: TdAutosaveSettingsScope | null = null;
  /** settings: scopeAutosaveSettings — New autosave settings for the scope; pass null to set autosave settings to default */
  settings: ScopeAutosaveSettings | null = null;
}

/** Decode a TDLib `setAutosaveSettings` object; null input -> null. */
export function decodeSetAutosaveSettings(json: TdJsonRaw): SetAutosaveSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetAutosaveSettings = new SetAutosaveSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scope = decodeTdAutosaveSettingsScope(record['scope']);
  out.settings = decodeScopeAutosaveSettings(record['settings']);
  return out;
}

/** Encode a `setAutosaveSettings` into TDLib JSON wire shape. */
export function encodeSetAutosaveSettings(value: SetAutosaveSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setAutosaveSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scope'] = value.scope === null ? null : encodeTdAutosaveSettingsScope(value.scope);
  out['settings'] = value.settings === null ? null : encodeScopeAutosaveSettings(value.settings);
  return out;
}

/** setBio = Ok — Changes the bio of the current user */
export class SetBio {
  type: 'setBio' = 'setBio';
  extra?: string;
  /** bio: string — The new value of the user bio; 0-getOption("bio_length_max") characters without line feeds */
  bio: string = '';
}

/** Decode a TDLib `setBio` object; null input -> null. */
export function decodeSetBio(json: TdJsonRaw): SetBio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBio = new SetBio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bio = asString(record['bio']);
  return out;
}

/** Encode a `setBio` into TDLib JSON wire shape. */
export function encodeSetBio(value: SetBio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bio'] = value.bio;
  return out;
}

/** setBirthdate = Ok — Changes the birthdate of the current user */
export class SetBirthdate {
  type: 'setBirthdate' = 'setBirthdate';
  extra?: string;
  /** birthdate: birthdate — The new value of the current user's birthdate; pass null to remove the birthdate */
  birthdate: Birthdate | null = null;
}

/** Decode a TDLib `setBirthdate` object; null input -> null. */
export function decodeSetBirthdate(json: TdJsonRaw): SetBirthdate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBirthdate = new SetBirthdate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.birthdate = decodeBirthdate(record['birthdate']);
  return out;
}

/** Encode a `setBirthdate` into TDLib JSON wire shape. */
export function encodeSetBirthdate(value: SetBirthdate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBirthdate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['birthdate'] = value.birthdate === null ? null : encodeBirthdate(value.birthdate);
  return out;
}

/** setBotInfoDescription = Ok — Sets the text shown in the chat with a bot if the chat is empty. Can be called only if userTypeBot.can_be_edited == true */
export class SetBotInfoDescription {
  type: 'setBotInfoDescription' = 'setBotInfoDescription';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** language_code: string — A two-letter ISO 639-1 language code. If empty, the description will be shown to all users for whose languages there is no dedicated description */
  language_code: string = '';
  /** description: string */
  description: string = '';
}

/** Decode a TDLib `setBotInfoDescription` object; null input -> null. */
export function decodeSetBotInfoDescription(json: TdJsonRaw): SetBotInfoDescription | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBotInfoDescription = new SetBotInfoDescription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  out.description = asString(record['description']);
  return out;
}

/** Encode a `setBotInfoDescription` into TDLib JSON wire shape. */
export function encodeSetBotInfoDescription(value: SetBotInfoDescription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBotInfoDescription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  out['description'] = value.description;
  return out;
}

/** setBotInfoShortDescription = Ok — Sets the text shown on a bot's profile page and sent together with the link when users share the bot. Can be called only if userTypeBot.can_be_edited == true */
export class SetBotInfoShortDescription {
  type: 'setBotInfoShortDescription' = 'setBotInfoShortDescription';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** language_code: string — A two-letter ISO 639-1 language code. If empty, the short description will be shown to all users for whose languages there is no dedicated description */
  language_code: string = '';
  /** short_description: string — New bot's short description on the specified language */
  short_description: string = '';
}

/** Decode a TDLib `setBotInfoShortDescription` object; null input -> null. */
export function decodeSetBotInfoShortDescription(json: TdJsonRaw): SetBotInfoShortDescription | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBotInfoShortDescription = new SetBotInfoShortDescription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  out.short_description = asString(record['short_description']);
  return out;
}

/** Encode a `setBotInfoShortDescription` into TDLib JSON wire shape. */
export function encodeSetBotInfoShortDescription(value: SetBotInfoShortDescription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBotInfoShortDescription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  out['short_description'] = value.short_description;
  return out;
}

/** setBotName = Ok — Sets the name of a bot. Can be called only if userTypeBot.can_be_edited == true */
export class SetBotName {
  type: 'setBotName' = 'setBotName';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** language_code: string — A two-letter ISO 639-1 language code. If empty, the name will be shown to all users for whose languages there is no dedicated name */
  language_code: string = '';
  /** name: string — New bot's name on the specified language; 0-64 characters; must be non-empty if language code is empty */
  name: string = '';
}

/** Decode a TDLib `setBotName` object; null input -> null. */
export function decodeSetBotName(json: TdJsonRaw): SetBotName | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBotName = new SetBotName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  out.name = asString(record['name']);
  return out;
}

/** Encode a `setBotName` into TDLib JSON wire shape. */
export function encodeSetBotName(value: SetBotName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBotName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  out['name'] = value.name;
  return out;
}

/** setBotProfilePhoto = Ok — Changes a profile photo for a bot */
export class SetBotProfilePhoto {
  type: 'setBotProfilePhoto' = 'setBotProfilePhoto';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** photo: InputChatPhoto — Profile photo to set; pass null to delete the chat photo */
  photo: TdInputChatPhoto | null = null;
}

/** Decode a TDLib `setBotProfilePhoto` object; null input -> null. */
export function decodeSetBotProfilePhoto(json: TdJsonRaw): SetBotProfilePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBotProfilePhoto = new SetBotProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.photo = decodeTdInputChatPhoto(record['photo']);
  return out;
}

/** Encode a `setBotProfilePhoto` into TDLib JSON wire shape. */
export function encodeSetBotProfilePhoto(value: SetBotProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBotProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['photo'] = value.photo === null ? null : encodeTdInputChatPhoto(value.photo);
  return out;
}

/** setBotUpdatesStatus = Ok — Informs the server about the number of pending bot updates if they haven't been processed for a long time; for bots only */
export class SetBotUpdatesStatus {
  type: 'setBotUpdatesStatus' = 'setBotUpdatesStatus';
  extra?: string;
  /** pending_update_count: int32 — The number of pending updates */
  pending_update_count: number = 0;
  /** error_message: string — The last error message */
  error_message: string = '';
}

/** Decode a TDLib `setBotUpdatesStatus` object; null input -> null. */
export function decodeSetBotUpdatesStatus(json: TdJsonRaw): SetBotUpdatesStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBotUpdatesStatus = new SetBotUpdatesStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.pending_update_count = asNumber(record['pending_update_count']);
  out.error_message = asString(record['error_message']);
  return out;
}

/** Encode a `setBotUpdatesStatus` into TDLib JSON wire shape. */
export function encodeSetBotUpdatesStatus(value: SetBotUpdatesStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBotUpdatesStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['pending_update_count'] = value.pending_update_count;
  out['error_message'] = value.error_message;
  return out;
}

/** setBusinessAccountBio = Ok — Changes the bio of a business account; for bots only */
export class SetBusinessAccountBio {
  type: 'setBusinessAccountBio' = 'setBusinessAccountBio';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection */
  business_connection_id: string = '';
  /** bio: string — The new value of the bio; 0-getOption("bio_length_max") characters without line feeds */
  bio: string = '';
}

/** Decode a TDLib `setBusinessAccountBio` object; null input -> null. */
export function decodeSetBusinessAccountBio(json: TdJsonRaw): SetBusinessAccountBio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessAccountBio = new SetBusinessAccountBio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.bio = asString(record['bio']);
  return out;
}

/** Encode a `setBusinessAccountBio` into TDLib JSON wire shape. */
export function encodeSetBusinessAccountBio(value: SetBusinessAccountBio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessAccountBio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['bio'] = value.bio;
  return out;
}

/** setBusinessAccountGiftSettings = Ok — Changes settings for gift receiving of a business account; for bots only */
export class SetBusinessAccountGiftSettings {
  type: 'setBusinessAccountGiftSettings' = 'setBusinessAccountGiftSettings';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection */
  business_connection_id: string = '';
  /** settings: giftSettings — The new settings */
  settings: GiftSettings | null = null;
}

/** Decode a TDLib `setBusinessAccountGiftSettings` object; null input -> null. */
export function decodeSetBusinessAccountGiftSettings(json: TdJsonRaw): SetBusinessAccountGiftSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessAccountGiftSettings = new SetBusinessAccountGiftSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.settings = decodeGiftSettings(record['settings']);
  return out;
}

/** Encode a `setBusinessAccountGiftSettings` into TDLib JSON wire shape. */
export function encodeSetBusinessAccountGiftSettings(value: SetBusinessAccountGiftSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessAccountGiftSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['settings'] = value.settings === null ? null : encodeGiftSettings(value.settings);
  return out;
}

/** setBusinessAccountName = Ok — Changes the first and last name of a business account; for bots only */
export class SetBusinessAccountName {
  type: 'setBusinessAccountName' = 'setBusinessAccountName';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection */
  business_connection_id: string = '';
  /** first_name: string — The new value of the first name for the business account; 1-64 characters */
  first_name: string = '';
  /** last_name: string — The new value of the optional last name for the business account; 0-64 characters */
  last_name: string = '';
}

/** Decode a TDLib `setBusinessAccountName` object; null input -> null. */
export function decodeSetBusinessAccountName(json: TdJsonRaw): SetBusinessAccountName | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessAccountName = new SetBusinessAccountName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.first_name = asString(record['first_name']);
  out.last_name = asString(record['last_name']);
  return out;
}

/** Encode a `setBusinessAccountName` into TDLib JSON wire shape. */
export function encodeSetBusinessAccountName(value: SetBusinessAccountName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessAccountName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['first_name'] = value.first_name;
  out['last_name'] = value.last_name;
  return out;
}

/** setBusinessAccountProfilePhoto = Ok — Changes a profile photo of a business account; for bots only */
export class SetBusinessAccountProfilePhoto {
  type: 'setBusinessAccountProfilePhoto' = 'setBusinessAccountProfilePhoto';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection */
  business_connection_id: string = '';
  /** photo: InputChatPhoto — Profile photo to set; pass null to remove the photo */
  photo: TdInputChatPhoto | null = null;
  /** is_public: Bool — Pass true to set the public photo, which will be visible even if the main photo is hidden by privacy settings */
  is_public: boolean = false;
}

/** Decode a TDLib `setBusinessAccountProfilePhoto` object; null input -> null. */
export function decodeSetBusinessAccountProfilePhoto(json: TdJsonRaw): SetBusinessAccountProfilePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessAccountProfilePhoto = new SetBusinessAccountProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.photo = decodeTdInputChatPhoto(record['photo']);
  out.is_public = asBool(record['is_public']);
  return out;
}

/** Encode a `setBusinessAccountProfilePhoto` into TDLib JSON wire shape. */
export function encodeSetBusinessAccountProfilePhoto(value: SetBusinessAccountProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessAccountProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['photo'] = value.photo === null ? null : encodeTdInputChatPhoto(value.photo);
  out['is_public'] = value.is_public;
  return out;
}

/** setBusinessAccountUsername = Ok — Changes the editable username of a business account; for bots only */
export class SetBusinessAccountUsername {
  type: 'setBusinessAccountUsername' = 'setBusinessAccountUsername';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection */
  business_connection_id: string = '';
  /** username: string — The new value of the username */
  username: string = '';
}

/** Decode a TDLib `setBusinessAccountUsername` object; null input -> null. */
export function decodeSetBusinessAccountUsername(json: TdJsonRaw): SetBusinessAccountUsername | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessAccountUsername = new SetBusinessAccountUsername();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.username = asString(record['username']);
  return out;
}

/** Encode a `setBusinessAccountUsername` into TDLib JSON wire shape. */
export function encodeSetBusinessAccountUsername(value: SetBusinessAccountUsername): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessAccountUsername';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['username'] = value.username;
  return out;
}

/** setBusinessAwayMessageSettings = Ok — Changes the business away message settings of the current user. Requires Telegram Business subscription */
export class SetBusinessAwayMessageSettings {
  type: 'setBusinessAwayMessageSettings' = 'setBusinessAwayMessageSettings';
  extra?: string;
  /** away_message_settings: businessAwayMessageSettings — The new settings for the away message of the business; pass null to disable the away message */
  away_message_settings: BusinessAwayMessageSettings | null = null;
}

/** Decode a TDLib `setBusinessAwayMessageSettings` object; null input -> null. */
export function decodeSetBusinessAwayMessageSettings(json: TdJsonRaw): SetBusinessAwayMessageSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessAwayMessageSettings = new SetBusinessAwayMessageSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.away_message_settings = decodeBusinessAwayMessageSettings(record['away_message_settings']);
  return out;
}

/** Encode a `setBusinessAwayMessageSettings` into TDLib JSON wire shape. */
export function encodeSetBusinessAwayMessageSettings(value: SetBusinessAwayMessageSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessAwayMessageSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['away_message_settings'] = value.away_message_settings === null ? null : encodeBusinessAwayMessageSettings(value.away_message_settings);
  return out;
}

/** setBusinessConnectedBot = Ok — Adds or changes business bot that is connected to the current user account */
export class SetBusinessConnectedBot {
  type: 'setBusinessConnectedBot' = 'setBusinessConnectedBot';
  extra?: string;
  /** bot: businessConnectedBot — Connection settings for the bot */
  bot: BusinessConnectedBot | null = null;
}

/** Decode a TDLib `setBusinessConnectedBot` object; null input -> null. */
export function decodeSetBusinessConnectedBot(json: TdJsonRaw): SetBusinessConnectedBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessConnectedBot = new SetBusinessConnectedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot = decodeBusinessConnectedBot(record['bot']);
  return out;
}

/** Encode a `setBusinessConnectedBot` into TDLib JSON wire shape. */
export function encodeSetBusinessConnectedBot(value: SetBusinessConnectedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessConnectedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot'] = value.bot === null ? null : encodeBusinessConnectedBot(value.bot);
  return out;
}

/** setBusinessGreetingMessageSettings = Ok — Changes the business greeting message settings of the current user. Requires Telegram Business subscription */
export class SetBusinessGreetingMessageSettings {
  type: 'setBusinessGreetingMessageSettings' = 'setBusinessGreetingMessageSettings';
  extra?: string;
  /** greeting_message_settings: businessGreetingMessageSettings — The new settings for the greeting message of the business; pass null to disable the greeting message */
  greeting_message_settings: BusinessGreetingMessageSettings | null = null;
}

/** Decode a TDLib `setBusinessGreetingMessageSettings` object; null input -> null. */
export function decodeSetBusinessGreetingMessageSettings(json: TdJsonRaw): SetBusinessGreetingMessageSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessGreetingMessageSettings = new SetBusinessGreetingMessageSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.greeting_message_settings = decodeBusinessGreetingMessageSettings(record['greeting_message_settings']);
  return out;
}

/** Encode a `setBusinessGreetingMessageSettings` into TDLib JSON wire shape. */
export function encodeSetBusinessGreetingMessageSettings(value: SetBusinessGreetingMessageSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessGreetingMessageSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['greeting_message_settings'] = value.greeting_message_settings === null ? null : encodeBusinessGreetingMessageSettings(value.greeting_message_settings);
  return out;
}

/** setBusinessLocation = Ok — Changes the business location of the current user. Requires Telegram Business subscription */
export class SetBusinessLocation {
  type: 'setBusinessLocation' = 'setBusinessLocation';
  extra?: string;
  /** location: businessLocation — The new location of the business; pass null to remove the location */
  location: BusinessLocation | null = null;
}

/** Decode a TDLib `setBusinessLocation` object; null input -> null. */
export function decodeSetBusinessLocation(json: TdJsonRaw): SetBusinessLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessLocation = new SetBusinessLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeBusinessLocation(record['location']);
  return out;
}

/** Encode a `setBusinessLocation` into TDLib JSON wire shape. */
export function encodeSetBusinessLocation(value: SetBusinessLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeBusinessLocation(value.location);
  return out;
}

/** setBusinessMessageIsPinned = Ok — Pins or unpins a message sent on behalf of a business account; for bots only */
export class SetBusinessMessageIsPinned {
  type: 'setBusinessMessageIsPinned' = 'setBusinessMessageIsPinned';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which the message was sent */
  business_connection_id: string = '';
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** is_pinned: Bool — Pass true to pin the message, pass false to unpin it */
  is_pinned: boolean = false;
}

/** Decode a TDLib `setBusinessMessageIsPinned` object; null input -> null. */
export function decodeSetBusinessMessageIsPinned(json: TdJsonRaw): SetBusinessMessageIsPinned | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessMessageIsPinned = new SetBusinessMessageIsPinned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `setBusinessMessageIsPinned` into TDLib JSON wire shape. */
export function encodeSetBusinessMessageIsPinned(value: SetBusinessMessageIsPinned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessMessageIsPinned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** setBusinessOpeningHours = Ok — Changes the business opening hours of the current user. Requires Telegram Business subscription */
export class SetBusinessOpeningHours {
  type: 'setBusinessOpeningHours' = 'setBusinessOpeningHours';
  extra?: string;
  /** opening_hours: businessOpeningHours — The new opening hours of the business; pass null to remove the opening hours; up to 28 time intervals can be specified */
  opening_hours: BusinessOpeningHours | null = null;
}

/** Decode a TDLib `setBusinessOpeningHours` object; null input -> null. */
export function decodeSetBusinessOpeningHours(json: TdJsonRaw): SetBusinessOpeningHours | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessOpeningHours = new SetBusinessOpeningHours();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.opening_hours = decodeBusinessOpeningHours(record['opening_hours']);
  return out;
}

/** Encode a `setBusinessOpeningHours` into TDLib JSON wire shape. */
export function encodeSetBusinessOpeningHours(value: SetBusinessOpeningHours): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessOpeningHours';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['opening_hours'] = value.opening_hours === null ? null : encodeBusinessOpeningHours(value.opening_hours);
  return out;
}

/** setBusinessStartPage = Ok — Changes the business start page of the current user. Requires Telegram Business subscription */
export class SetBusinessStartPage {
  type: 'setBusinessStartPage' = 'setBusinessStartPage';
  extra?: string;
  /** start_page: inputBusinessStartPage — The new start page of the business; pass null to remove custom start page */
  start_page: InputBusinessStartPage | null = null;
}

/** Decode a TDLib `setBusinessStartPage` object; null input -> null. */
export function decodeSetBusinessStartPage(json: TdJsonRaw): SetBusinessStartPage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetBusinessStartPage = new SetBusinessStartPage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.start_page = decodeInputBusinessStartPage(record['start_page']);
  return out;
}

/** Encode a `setBusinessStartPage` into TDLib JSON wire shape. */
export function encodeSetBusinessStartPage(value: SetBusinessStartPage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setBusinessStartPage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['start_page'] = value.start_page === null ? null : encodeInputBusinessStartPage(value.start_page);
  return out;
}

/** setChatAccentColor = Ok — Changes accent color and background custom emoji of a channel chat. Requires can_change_info administrator right */
export class SetChatAccentColor {
  type: 'setChatAccentColor' = 'setChatAccentColor';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** accent_color_id: int32 — Identifier of the accent color to use. The chat must have at least accentColor.min_channel_chat_boost_level boost level to pass the corresponding color */
  accent_color_id: number = 0;
  /** background_custom_emoji_id: int64 — Identifier of a custom emoji to be shown on the reply header and link preview background; 0 if none. Use chatBoostLevelFeatures.can_set_background_custom_emoji to check whether a custom emoji can be set */
  background_custom_emoji_id: string = '';
}

/** Decode a TDLib `setChatAccentColor` object; null input -> null. */
export function decodeSetChatAccentColor(json: TdJsonRaw): SetChatAccentColor | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatAccentColor = new SetChatAccentColor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.accent_color_id = asNumber(record['accent_color_id']);
  out.background_custom_emoji_id = asInt64(record['background_custom_emoji_id']);
  return out;
}

/** Encode a `setChatAccentColor` into TDLib JSON wire shape. */
export function encodeSetChatAccentColor(value: SetChatAccentColor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatAccentColor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['accent_color_id'] = value.accent_color_id;
  out['background_custom_emoji_id'] = value.background_custom_emoji_id;
  return out;
}

/** setChatActiveStoriesList = Ok — Changes story list in which stories from the chat are shown */
export class SetChatActiveStoriesList {
  type: 'setChatActiveStoriesList' = 'setChatActiveStoriesList';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that posted stories */
  chat_id: number = 0;
  /** story_list: StoryList — New list for active stories posted by the chat */
  story_list: TdStoryList | null = null;
}

/** Decode a TDLib `setChatActiveStoriesList` object; null input -> null. */
export function decodeSetChatActiveStoriesList(json: TdJsonRaw): SetChatActiveStoriesList | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatActiveStoriesList = new SetChatActiveStoriesList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_list = decodeTdStoryList(record['story_list']);
  return out;
}

/** Encode a `setChatActiveStoriesList` into TDLib JSON wire shape. */
export function encodeSetChatActiveStoriesList(value: SetChatActiveStoriesList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatActiveStoriesList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_list'] = value.story_list === null ? null : encodeTdStoryList(value.story_list);
  return out;
}

/** setChatAffiliateProgram = Ok — Changes affiliate program for a bot If the active program is scheduled to be closed, then it can't be changed anymore */
export class SetChatAffiliateProgram {
  type: 'setChatAffiliateProgram' = 'setChatAffiliateProgram';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with an owned bot for which affiliate program is changed */
  chat_id: number = 0;
  /** parameters: affiliateProgramParameters — Parameters of the affiliate program; pass null to close the currently active program. If there is an active program, then commission and program duration can only be increased. */
  parameters: AffiliateProgramParameters | null = null;
}

/** Decode a TDLib `setChatAffiliateProgram` object; null input -> null. */
export function decodeSetChatAffiliateProgram(json: TdJsonRaw): SetChatAffiliateProgram | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatAffiliateProgram = new SetChatAffiliateProgram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.parameters = decodeAffiliateProgramParameters(record['parameters']);
  return out;
}

/** Encode a `setChatAffiliateProgram` into TDLib JSON wire shape. */
export function encodeSetChatAffiliateProgram(value: SetChatAffiliateProgram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatAffiliateProgram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['parameters'] = value.parameters === null ? null : encodeAffiliateProgramParameters(value.parameters);
  return out;
}

/** setChatAvailableReactions = Ok — Changes reactions, available in a chat. Available for basic groups, supergroups, and channels. Requires can_change_info member right */
export class SetChatAvailableReactions {
  type: 'setChatAvailableReactions' = 'setChatAvailableReactions';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** available_reactions: ChatAvailableReactions — Reactions available in the chat. All explicitly specified emoji reactions must be active. In channel chats up to the chat's boost level custom emoji reactions can be explicitly specified */
  available_reactions: TdChatAvailableReactions | null = null;
}

/** Decode a TDLib `setChatAvailableReactions` object; null input -> null. */
export function decodeSetChatAvailableReactions(json: TdJsonRaw): SetChatAvailableReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatAvailableReactions = new SetChatAvailableReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.available_reactions = decodeTdChatAvailableReactions(record['available_reactions']);
  return out;
}

/** Encode a `setChatAvailableReactions` into TDLib JSON wire shape. */
export function encodeSetChatAvailableReactions(value: SetChatAvailableReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatAvailableReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['available_reactions'] = value.available_reactions === null ? null : encodeTdChatAvailableReactions(value.available_reactions);
  return out;
}

/** setChatBackground = Ok — Sets the background in a specific chat. Supported only in private and secret chats with non-deleted users, and in chats with sufficient boost level and can_change_info administrator right Use chatBoostLevelFeatur... */
export class SetChatBackground {
  type: 'setChatBackground' = 'setChatBackground';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** background: InputBackground — The input background to use; pass null to create a new filled or chat theme background */
  background: TdInputBackground | null = null;
  /** type: BackgroundType — Background type; pass null to use default background type for the chosen background; backgroundTypeChatTheme isn't supported for private and secret chats. */
  type_: TdBackgroundType | null = null;
  /** dark_theme_dimming: int32 — Dimming of the background in dark themes, as a percentage; 0-100. Applied only to Wallpaper and Fill types of background */
  dark_theme_dimming: number = 0;
  /** only_for_self: Bool — Pass true to set background only for self; pass false to set background for all chat users. Always false for backgrounds set in boosted chats. Background can be set for both users only by Telegram Premium users and ... */
  only_for_self: boolean = false;
}

/** Decode a TDLib `setChatBackground` object; null input -> null. */
export function decodeSetChatBackground(json: TdJsonRaw): SetChatBackground | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatBackground = new SetChatBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.background = decodeTdInputBackground(record['background']);
  out.type_ = decodeTdBackgroundType(record['type']);
  out.dark_theme_dimming = asNumber(record['dark_theme_dimming']);
  out.only_for_self = asBool(record['only_for_self']);
  return out;
}

/** Encode a `setChatBackground` into TDLib JSON wire shape. */
export function encodeSetChatBackground(value: SetChatBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['background'] = value.background === null ? null : encodeTdInputBackground(value.background);
  out['type'] = value.type_ === null ? null : encodeTdBackgroundType(value.type_);
  out['dark_theme_dimming'] = value.dark_theme_dimming;
  out['only_for_self'] = value.only_for_self;
  return out;
}

/** setChatClientData = Ok — Changes application-specific data associated with a chat */
export class SetChatClientData {
  type: 'setChatClientData' = 'setChatClientData';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** client_data: string — New value of client_data */
  client_data: string = '';
}

/** Decode a TDLib `setChatClientData` object; null input -> null. */
export function decodeSetChatClientData(json: TdJsonRaw): SetChatClientData | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatClientData = new SetChatClientData();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.client_data = asString(record['client_data']);
  return out;
}

/** Encode a `setChatClientData` into TDLib JSON wire shape. */
export function encodeSetChatClientData(value: SetChatClientData): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatClientData';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['client_data'] = value.client_data;
  return out;
}

/** setChatDescription = Ok — Changes information about a chat. Available for basic groups, supergroups, and channels. Requires can_change_info member right */
export class SetChatDescription {
  type: 'setChatDescription' = 'setChatDescription';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** description: string */
  description: string = '';
}

/** Decode a TDLib `setChatDescription` object; null input -> null. */
export function decodeSetChatDescription(json: TdJsonRaw): SetChatDescription | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatDescription = new SetChatDescription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.description = asString(record['description']);
  return out;
}

/** Encode a `setChatDescription` into TDLib JSON wire shape. */
export function encodeSetChatDescription(value: SetChatDescription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatDescription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['description'] = value.description;
  return out;
}

/** setChatDirectMessagesGroup = Ok — Changes direct messages group settings for a channel chat; requires owner privileges in the chat The channel will receive getOption("paid_message_earnings_per_mille") Telegram Stars for each 1000 Telegra... */
export class SetChatDirectMessagesGroup {
  type: 'setChatDirectMessagesGroup' = 'setChatDirectMessagesGroup';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat */
  chat_id: number = 0;
  /** is_enabled: Bool — Pass true if the direct messages group is enabled for the channel chat; pass false otherwise */
  is_enabled: boolean = false;
  /** paid_message_star_count: int53 — The new number of Telegram Stars that must be paid for each message that is sent to the direct messages chat unless the sender is an administrator of the channel chat; 0-getOption("paid_message_star_count... */
  paid_message_star_count: number = 0;
}

/** Decode a TDLib `setChatDirectMessagesGroup` object; null input -> null. */
export function decodeSetChatDirectMessagesGroup(json: TdJsonRaw): SetChatDirectMessagesGroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatDirectMessagesGroup = new SetChatDirectMessagesGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.is_enabled = asBool(record['is_enabled']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  return out;
}

/** Encode a `setChatDirectMessagesGroup` into TDLib JSON wire shape. */
export function encodeSetChatDirectMessagesGroup(value: SetChatDirectMessagesGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatDirectMessagesGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['is_enabled'] = value.is_enabled;
  out['paid_message_star_count'] = value.paid_message_star_count;
  return out;
}

/** setChatDiscussionGroup = Ok — Changes the discussion group of a channel chat; requires can_change_info administrator right in the channel if it is specified Basic group chats must be first upgraded to supergroup chats. If new chat member... */
export class SetChatDiscussionGroup {
  type: 'setChatDiscussionGroup' = 'setChatDiscussionGroup';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat. Pass 0 to remove a link from the supergroup passed in the second argument to a linked channel chat (requires can_pin_messages member right in the supergroup) */
  chat_id: number = 0;
  /** discussion_chat_id: int53 — Identifier of a new channel's discussion group. Use 0 to remove the discussion group. Use the method getSuitableDiscussionChats to find all suitable groups. */
  discussion_chat_id: number = 0;
}

/** Decode a TDLib `setChatDiscussionGroup` object; null input -> null. */
export function decodeSetChatDiscussionGroup(json: TdJsonRaw): SetChatDiscussionGroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatDiscussionGroup = new SetChatDiscussionGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.discussion_chat_id = asNumber(record['discussion_chat_id']);
  return out;
}

/** Encode a `setChatDiscussionGroup` into TDLib JSON wire shape. */
export function encodeSetChatDiscussionGroup(value: SetChatDiscussionGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatDiscussionGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['discussion_chat_id'] = value.discussion_chat_id;
  return out;
}

/** setChatDraftMessage = Ok — Changes the draft message in a chat or a topic */
export class SetChatDraftMessage {
  type: 'setChatDraftMessage' = 'setChatDraftMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Topic in which the draft will be changed; pass null to change the draft for the chat itself */
  topic_id: TdMessageTopic | null = null;
  /** draft_message: draftMessage — New draft message; pass null to remove the draft */
  draft_message: DraftMessage | null = null;
}

/** Decode a TDLib `setChatDraftMessage` object; null input -> null. */
export function decodeSetChatDraftMessage(json: TdJsonRaw): SetChatDraftMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatDraftMessage = new SetChatDraftMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.draft_message = decodeDraftMessage(record['draft_message']);
  return out;
}

/** Encode a `setChatDraftMessage` into TDLib JSON wire shape. */
export function encodeSetChatDraftMessage(value: SetChatDraftMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatDraftMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['draft_message'] = value.draft_message === null ? null : encodeDraftMessage(value.draft_message);
  return out;
}

/** setChatEmojiStatus = Ok — Changes the emoji status of a chat. Use chatBoostLevelFeatures.can_set_emoji_status to check whether an emoji status can be set. Requires can_change_info administrator right */
export class SetChatEmojiStatus {
  type: 'setChatEmojiStatus' = 'setChatEmojiStatus';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** emoji_status: emojiStatus — New emoji status; pass null to remove emoji status */
  emoji_status: EmojiStatus | null = null;
}

/** Decode a TDLib `setChatEmojiStatus` object; null input -> null. */
export function decodeSetChatEmojiStatus(json: TdJsonRaw): SetChatEmojiStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatEmojiStatus = new SetChatEmojiStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.emoji_status = decodeEmojiStatus(record['emoji_status']);
  return out;
}

/** Encode a `setChatEmojiStatus` into TDLib JSON wire shape. */
export function encodeSetChatEmojiStatus(value: SetChatEmojiStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatEmojiStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['emoji_status'] = value.emoji_status === null ? null : encodeEmojiStatus(value.emoji_status);
  return out;
}

/** setChatLocation = Ok — Changes the location of a chat. Available only for some location-based supergroups, use supergroupFullInfo.can_set_location to check whether the method is allowed to use */
export class SetChatLocation {
  type: 'setChatLocation' = 'setChatLocation';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** location: chatLocation — New location for the chat; must be valid and not null */
  location: ChatLocation | null = null;
}

/** Decode a TDLib `setChatLocation` object; null input -> null. */
export function decodeSetChatLocation(json: TdJsonRaw): SetChatLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatLocation = new SetChatLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.location = decodeChatLocation(record['location']);
  return out;
}

/** Encode a `setChatLocation` into TDLib JSON wire shape. */
export function encodeSetChatLocation(value: SetChatLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['location'] = value.location === null ? null : encodeChatLocation(value.location);
  return out;
}

/** setChatMemberStatus = Ok — Changes the status of a chat member; requires can_invite_users member right to add a chat member, can_promote_members administrator right to change administrator rights of the member, and can_restrict_members a... */
export class SetChatMemberStatus {
  type: 'setChatMemberStatus' = 'setChatMemberStatus';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** member_id: MessageSender — Member identifier. Chats can be only banned and unbanned in supergroups and channels */
  member_id: TdMessageSender | null = null;
  /** status: ChatMemberStatus — The new status of the member in the chat */
  status: TdChatMemberStatus | null = null;
}

/** Decode a TDLib `setChatMemberStatus` object; null input -> null. */
export function decodeSetChatMemberStatus(json: TdJsonRaw): SetChatMemberStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatMemberStatus = new SetChatMemberStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.member_id = decodeTdMessageSender(record['member_id']);
  out.status = decodeTdChatMemberStatus(record['status']);
  return out;
}

/** Encode a `setChatMemberStatus` into TDLib JSON wire shape. */
export function encodeSetChatMemberStatus(value: SetChatMemberStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatMemberStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['member_id'] = value.member_id === null ? null : encodeTdMessageSender(value.member_id);
  out['status'] = value.status === null ? null : encodeTdChatMemberStatus(value.status);
  return out;
}

/** setChatMemberTag = Ok — Changes the tag or custom title of a chat member; requires can_manage_tags administrator right to change tag of other users; for basic groups and supergroups only */
export class SetChatMemberTag {
  type: 'setChatMemberTag' = 'setChatMemberTag';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** user_id: int53 — Identifier of the user whose tag is changed. Chats can't have member tags */
  user_id: number = 0;
  /** tag: string — The new tag of the member in the chat; 0-16 characters without emoji */
  tag: string = '';
}

/** Decode a TDLib `setChatMemberTag` object; null input -> null. */
export function decodeSetChatMemberTag(json: TdJsonRaw): SetChatMemberTag | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatMemberTag = new SetChatMemberTag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.user_id = asNumber(record['user_id']);
  out.tag = asString(record['tag']);
  return out;
}

/** Encode a `setChatMemberTag` into TDLib JSON wire shape. */
export function encodeSetChatMemberTag(value: SetChatMemberTag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatMemberTag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['user_id'] = value.user_id;
  out['tag'] = value.tag;
  return out;
}

/** setChatMessageAutoDeleteTime = Ok — Changes the message auto-delete or self-destruct (for secret chats) time in a chat. Requires change_info administrator right in basic groups, supergroups and channels. Message auto-delete time can't be... */
export class SetChatMessageAutoDeleteTime {
  type: 'setChatMessageAutoDeleteTime' = 'setChatMessageAutoDeleteTime';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_auto_delete_time: int32 — New time value, in seconds; unless the chat is secret, it must be from 0 up to 365 * 86400 and be divisible by 86400. If 0, then messages aren't deleted automatically */
  message_auto_delete_time: number = 0;
}

/** Decode a TDLib `setChatMessageAutoDeleteTime` object; null input -> null. */
export function decodeSetChatMessageAutoDeleteTime(json: TdJsonRaw): SetChatMessageAutoDeleteTime | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatMessageAutoDeleteTime = new SetChatMessageAutoDeleteTime();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_auto_delete_time = asNumber(record['message_auto_delete_time']);
  return out;
}

/** Encode a `setChatMessageAutoDeleteTime` into TDLib JSON wire shape. */
export function encodeSetChatMessageAutoDeleteTime(value: SetChatMessageAutoDeleteTime): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatMessageAutoDeleteTime';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_auto_delete_time'] = value.message_auto_delete_time;
  return out;
}

/** setChatMessageSender = Ok — Selects a message sender to send messages in a chat */
export class SetChatMessageSender {
  type: 'setChatMessageSender' = 'setChatMessageSender';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_sender_id: MessageSender — New message sender for the chat */
  message_sender_id: TdMessageSender | null = null;
}

/** Decode a TDLib `setChatMessageSender` object; null input -> null. */
export function decodeSetChatMessageSender(json: TdJsonRaw): SetChatMessageSender | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatMessageSender = new SetChatMessageSender();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_sender_id = decodeTdMessageSender(record['message_sender_id']);
  return out;
}

/** Encode a `setChatMessageSender` into TDLib JSON wire shape. */
export function encodeSetChatMessageSender(value: SetChatMessageSender): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatMessageSender';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_sender_id'] = value.message_sender_id === null ? null : encodeTdMessageSender(value.message_sender_id);
  return out;
}

/** setChatNotificationSettings = Ok — Changes the notification settings of a chat. Notification settings of a chat with the current user (Saved Messages) can't be changed */
export class SetChatNotificationSettings {
  type: 'setChatNotificationSettings' = 'setChatNotificationSettings';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** notification_settings: chatNotificationSettings — New notification settings for the chat. If the chat is muted for more than 366 days, it is considered to be muted forever */
  notification_settings: ChatNotificationSettings | null = null;
}

/** Decode a TDLib `setChatNotificationSettings` object; null input -> null. */
export function decodeSetChatNotificationSettings(json: TdJsonRaw): SetChatNotificationSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatNotificationSettings = new SetChatNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.notification_settings = decodeChatNotificationSettings(record['notification_settings']);
  return out;
}

/** Encode a `setChatNotificationSettings` into TDLib JSON wire shape. */
export function encodeSetChatNotificationSettings(value: SetChatNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['notification_settings'] = value.notification_settings === null ? null : encodeChatNotificationSettings(value.notification_settings);
  return out;
}

/** setChatPaidMessageStarCount = Ok — Changes the Telegram Star amount that must be paid to send a message to a supergroup chat; requires can_restrict_members administrator right and supergroupFullInfo.can_enable_paid_messages The supergrou... */
export class SetChatPaidMessageStarCount {
  type: 'setChatPaidMessageStarCount' = 'setChatPaidMessageStarCount';
  extra?: string;
  /** chat_id: int53 — Identifier of the supergroup chat */
  chat_id: number = 0;
  /** paid_message_star_count: int53 — The new number of Telegram Stars that must be paid for each message that is sent to the supergroup chat unless the sender is an administrator of the chat; 0-getOption("paid_message_star_count_max"). */
  paid_message_star_count: number = 0;
}

/** Decode a TDLib `setChatPaidMessageStarCount` object; null input -> null. */
export function decodeSetChatPaidMessageStarCount(json: TdJsonRaw): SetChatPaidMessageStarCount | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatPaidMessageStarCount = new SetChatPaidMessageStarCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  return out;
}

/** Encode a `setChatPaidMessageStarCount` into TDLib JSON wire shape. */
export function encodeSetChatPaidMessageStarCount(value: SetChatPaidMessageStarCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatPaidMessageStarCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['paid_message_star_count'] = value.paid_message_star_count;
  return out;
}

/** setChatPermissions = Ok — Changes the chat members permissions. Supported only for basic groups and supergroups. Requires can_restrict_members administrator right */
export class SetChatPermissions {
  type: 'setChatPermissions' = 'setChatPermissions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** permissions: chatPermissions — New non-administrator members permissions in the chat */
  permissions: ChatPermissions | null = null;
}

/** Decode a TDLib `setChatPermissions` object; null input -> null. */
export function decodeSetChatPermissions(json: TdJsonRaw): SetChatPermissions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatPermissions = new SetChatPermissions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.permissions = decodeChatPermissions(record['permissions']);
  return out;
}

/** Encode a `setChatPermissions` into TDLib JSON wire shape. */
export function encodeSetChatPermissions(value: SetChatPermissions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatPermissions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['permissions'] = value.permissions === null ? null : encodeChatPermissions(value.permissions);
  return out;
}

/** setChatPhoto = Ok — Changes the photo of a chat. Supported only for basic groups, supergroups and channels. Requires can_change_info member right */
export class SetChatPhoto {
  type: 'setChatPhoto' = 'setChatPhoto';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** photo: InputChatPhoto — New chat photo; pass null to delete the chat photo */
  photo: TdInputChatPhoto | null = null;
}

/** Decode a TDLib `setChatPhoto` object; null input -> null. */
export function decodeSetChatPhoto(json: TdJsonRaw): SetChatPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatPhoto = new SetChatPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.photo = decodeTdInputChatPhoto(record['photo']);
  return out;
}

/** Encode a `setChatPhoto` into TDLib JSON wire shape. */
export function encodeSetChatPhoto(value: SetChatPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['photo'] = value.photo === null ? null : encodeTdInputChatPhoto(value.photo);
  return out;
}

/** setChatPinnedStories = Ok — Changes the list of pinned stories on a chat page; requires can_edit_stories administrator right in the chat */
export class SetChatPinnedStories {
  type: 'setChatPinnedStories' = 'setChatPinnedStories';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that posted the stories */
  chat_id: number = 0;
  /** story_ids: vector<int32> — New list of pinned stories. All stories must be posted to the chat page first. There can be up to getOption("pinned_story_count_max") pinned stories on a chat page */
  story_ids: number[] = [];
}

/** Decode a TDLib `setChatPinnedStories` object; null input -> null. */
export function decodeSetChatPinnedStories(json: TdJsonRaw): SetChatPinnedStories | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatPinnedStories = new SetChatPinnedStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_ids = decodeVector(record['story_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `setChatPinnedStories` into TDLib JSON wire shape. */
export function encodeSetChatPinnedStories(value: SetChatPinnedStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatPinnedStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_ids'] = encodeVector(value.story_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** setChatProfileAccentColor = Ok — Changes accent color and background custom emoji for profile of a supergroup or channel chat. Requires can_change_info administrator right or profileAccentColor.min_channel_chat_boost_level for channels b... */
export class SetChatProfileAccentColor {
  type: 'setChatProfileAccentColor' = 'setChatProfileAccentColor';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** profile_accent_color_id: int32 — Identifier of the accent color to use for profile; pass -1 if none. The chat must have at least profileAccentColor.min_supergroup_chat_boost_level for supergroups */
  profile_accent_color_id: number = 0;
  /** profile_background_custom_emoji_id: int64 — Identifier of a custom emoji to be shown on the chat's profile photo background; 0 if none. Use chatBoostLevelFeatures.can_set_profile_background_custom_emoji to check whether a custom emoji ca... */
  profile_background_custom_emoji_id: string = '';
}

/** Decode a TDLib `setChatProfileAccentColor` object; null input -> null. */
export function decodeSetChatProfileAccentColor(json: TdJsonRaw): SetChatProfileAccentColor | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatProfileAccentColor = new SetChatProfileAccentColor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.profile_accent_color_id = asNumber(record['profile_accent_color_id']);
  out.profile_background_custom_emoji_id = asInt64(record['profile_background_custom_emoji_id']);
  return out;
}

/** Encode a `setChatProfileAccentColor` into TDLib JSON wire shape. */
export function encodeSetChatProfileAccentColor(value: SetChatProfileAccentColor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatProfileAccentColor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['profile_accent_color_id'] = value.profile_accent_color_id;
  out['profile_background_custom_emoji_id'] = value.profile_background_custom_emoji_id;
  return out;
}

/** setChatSlowModeDelay = Ok — Changes the slow mode delay of a chat. Available only for supergroups; requires can_restrict_members administrator right */
export class SetChatSlowModeDelay {
  type: 'setChatSlowModeDelay' = 'setChatSlowModeDelay';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** slow_mode_delay: int32 — New slow mode delay for the chat, in seconds; must be one of 0, 5, 10, 30, 60, 300, 900, 3600 */
  slow_mode_delay: number = 0;
}

/** Decode a TDLib `setChatSlowModeDelay` object; null input -> null. */
export function decodeSetChatSlowModeDelay(json: TdJsonRaw): SetChatSlowModeDelay | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatSlowModeDelay = new SetChatSlowModeDelay();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.slow_mode_delay = asNumber(record['slow_mode_delay']);
  return out;
}

/** Encode a `setChatSlowModeDelay` into TDLib JSON wire shape. */
export function encodeSetChatSlowModeDelay(value: SetChatSlowModeDelay): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatSlowModeDelay';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['slow_mode_delay'] = value.slow_mode_delay;
  return out;
}

/** setChatTheme = Ok — Changes the chat theme. Supported only in private and secret chats */
export class SetChatTheme {
  type: 'setChatTheme' = 'setChatTheme';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** theme: InputChatTheme — New chat theme; pass null to return the default theme */
  theme: TdInputChatTheme | null = null;
}

/** Decode a TDLib `setChatTheme` object; null input -> null. */
export function decodeSetChatTheme(json: TdJsonRaw): SetChatTheme | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatTheme = new SetChatTheme();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.theme = decodeTdInputChatTheme(record['theme']);
  return out;
}

/** Encode a `setChatTheme` into TDLib JSON wire shape. */
export function encodeSetChatTheme(value: SetChatTheme): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatTheme';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['theme'] = value.theme === null ? null : encodeTdInputChatTheme(value.theme);
  return out;
}

/** setChatTitle = Ok — Changes the chat title. Supported only for basic groups, supergroups and channels. Requires can_change_info member right */
export class SetChatTitle {
  type: 'setChatTitle' = 'setChatTitle';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** title: string — New title of the chat; 1-128 characters */
  title: string = '';
}

/** Decode a TDLib `setChatTitle` object; null input -> null. */
export function decodeSetChatTitle(json: TdJsonRaw): SetChatTitle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetChatTitle = new SetChatTitle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.title = asString(record['title']);
  return out;
}

/** Encode a `setChatTitle` into TDLib JSON wire shape. */
export function encodeSetChatTitle(value: SetChatTitle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setChatTitle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['title'] = value.title;
  return out;
}

/** setCloseFriends = Ok — Changes the list of close friends of the current user */
export class SetCloseFriends {
  type: 'setCloseFriends' = 'setCloseFriends';
  extra?: string;
  /** user_ids: vector<int53> — User identifiers of close friends; the users must be contacts of the current user */
  user_ids: number[] = [];
}

/** Decode a TDLib `setCloseFriends` object; null input -> null. */
export function decodeSetCloseFriends(json: TdJsonRaw): SetCloseFriends | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetCloseFriends = new SetCloseFriends();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `setCloseFriends` into TDLib JSON wire shape. */
export function encodeSetCloseFriends(value: SetCloseFriends): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setCloseFriends';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** setCommands = Ok — Sets the list of commands supported by the bot for the given user scope and language; for bots only */
export class SetCommands {
  type: 'setCommands' = 'setCommands';
  extra?: string;
  /** scope: BotCommandScope — The scope to which the commands are relevant; pass null to change commands in the default bot command scope */
  scope: TdBotCommandScope | null = null;
  /** language_code: string — A two-letter ISO 639-1 language code. If empty, the commands will be applied to all users from the given scope, for which language there are no dedicated commands */
  language_code: string = '';
  /** commands: vector<botCommand> — List of the bot's commands */
  commands: BotCommand[] = [];
}

/** Decode a TDLib `setCommands` object; null input -> null. */
export function decodeSetCommands(json: TdJsonRaw): SetCommands | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetCommands = new SetCommands();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scope = decodeTdBotCommandScope(record['scope']);
  out.language_code = asString(record['language_code']);
  out.commands = decodeVector(record['commands'], (item: TdJsonRaw): BotCommand => decodeBotCommand(item) as BotCommand);
  return out;
}

/** Encode a `setCommands` into TDLib JSON wire shape. */
export function encodeSetCommands(value: SetCommands): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setCommands';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scope'] = value.scope === null ? null : encodeTdBotCommandScope(value.scope);
  out['language_code'] = value.language_code;
  out['commands'] = encodeVector(value.commands, (item: BotCommand | null): TdJsonRaw => item === null ? null : encodeBotCommand(item));
  return out;
}

/** setCommunityName = Ok — Changes name of the given community; requires can_change_info administrator right in the community */
export class SetCommunityName {
  type: 'setCommunityName' = 'setCommunityName';
  extra?: string;
  /** community_id: int53 — Identifier of the community */
  community_id: number = 0;
  /** name: string — New name of the community */
  name: string = '';
}

/** Decode a TDLib `setCommunityName` object; null input -> null. */
export function decodeSetCommunityName(json: TdJsonRaw): SetCommunityName | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetCommunityName = new SetCommunityName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.community_id = asNumber(record['community_id']);
  out.name = asString(record['name']);
  return out;
}

/** Encode a `setCommunityName` into TDLib JSON wire shape. */
export function encodeSetCommunityName(value: SetCommunityName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setCommunityName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['community_id'] = value.community_id;
  out['name'] = value.name;
  return out;
}

/** setCustomEmojiStickerSetThumbnail = Ok — Sets a custom emoji sticker set thumbnail */
export class SetCustomEmojiStickerSetThumbnail {
  type: 'setCustomEmojiStickerSetThumbnail' = 'setCustomEmojiStickerSetThumbnail';
  extra?: string;
  /** name: string — Sticker set name. The sticker set must be owned by the current user */
  name: string = '';
  /** custom_emoji_id: int64 — Identifier of the custom emoji from the sticker set, which will be set as sticker set thumbnail; pass 0 to remove the sticker set thumbnail */
  custom_emoji_id: string = '';
}

/** Decode a TDLib `setCustomEmojiStickerSetThumbnail` object; null input -> null. */
export function decodeSetCustomEmojiStickerSetThumbnail(json: TdJsonRaw): SetCustomEmojiStickerSetThumbnail | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetCustomEmojiStickerSetThumbnail = new SetCustomEmojiStickerSetThumbnail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  return out;
}

/** Encode a `setCustomEmojiStickerSetThumbnail` into TDLib JSON wire shape. */
export function encodeSetCustomEmojiStickerSetThumbnail(value: SetCustomEmojiStickerSetThumbnail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setCustomEmojiStickerSetThumbnail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['custom_emoji_id'] = value.custom_emoji_id;
  return out;
}

/** setCustomLanguagePack = Ok — Adds or changes a custom local language pack to the current localization target */
export class SetCustomLanguagePack {
  type: 'setCustomLanguagePack' = 'setCustomLanguagePack';
  extra?: string;
  /** info: languagePackInfo — Information about the language pack. Language pack identifier must start with 'X', consist only of English letters, digits and hyphens, and must not exceed 64 characters. Can be called before authorization */
  info: LanguagePackInfo | null = null;
  /** strings: vector<languagePackString> — Strings of the new language pack */
  strings: LanguagePackString[] = [];
}

/** Decode a TDLib `setCustomLanguagePack` object; null input -> null. */
export function decodeSetCustomLanguagePack(json: TdJsonRaw): SetCustomLanguagePack | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetCustomLanguagePack = new SetCustomLanguagePack();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.info = decodeLanguagePackInfo(record['info']);
  out.strings = decodeVector(record['strings'], (item: TdJsonRaw): LanguagePackString => decodeLanguagePackString(item) as LanguagePackString);
  return out;
}

/** Encode a `setCustomLanguagePack` into TDLib JSON wire shape. */
export function encodeSetCustomLanguagePack(value: SetCustomLanguagePack): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setCustomLanguagePack';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['info'] = value.info === null ? null : encodeLanguagePackInfo(value.info);
  out['strings'] = encodeVector(value.strings, (item: LanguagePackString | null): TdJsonRaw => item === null ? null : encodeLanguagePackString(item));
  return out;
}

/** setCustomLanguagePackString = Ok — Adds, edits or deletes a string in a custom local language pack. Can be called before authorization */
export class SetCustomLanguagePackString {
  type: 'setCustomLanguagePackString' = 'setCustomLanguagePackString';
  extra?: string;
  /** language_pack_id: string — Identifier of a previously added custom local language pack in the current localization target */
  language_pack_id: string = '';
  /** new_string: languagePackString — New language pack string */
  new_string: LanguagePackString | null = null;
}

/** Decode a TDLib `setCustomLanguagePackString` object; null input -> null. */
export function decodeSetCustomLanguagePackString(json: TdJsonRaw): SetCustomLanguagePackString | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetCustomLanguagePackString = new SetCustomLanguagePackString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_pack_id = asString(record['language_pack_id']);
  out.new_string = decodeLanguagePackString(record['new_string']);
  return out;
}

/** Encode a `setCustomLanguagePackString` into TDLib JSON wire shape. */
export function encodeSetCustomLanguagePackString(value: SetCustomLanguagePackString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setCustomLanguagePackString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_pack_id'] = value.language_pack_id;
  out['new_string'] = value.new_string === null ? null : encodeLanguagePackString(value.new_string);
  return out;
}

/** setDatabaseEncryptionKey = Ok — Changes the database encryption key. Usually the encryption key is never changed and is stored in some OS keychain */
export class SetDatabaseEncryptionKey {
  type: 'setDatabaseEncryptionKey' = 'setDatabaseEncryptionKey';
  extra?: string;
  /** new_encryption_key: bytes — New encryption key */
  new_encryption_key: string = '';
}

/** Decode a TDLib `setDatabaseEncryptionKey` object; null input -> null. */
export function decodeSetDatabaseEncryptionKey(json: TdJsonRaw): SetDatabaseEncryptionKey | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetDatabaseEncryptionKey = new SetDatabaseEncryptionKey();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.new_encryption_key = asString(record['new_encryption_key']);
  return out;
}

/** Encode a `setDatabaseEncryptionKey` into TDLib JSON wire shape. */
export function encodeSetDatabaseEncryptionKey(value: SetDatabaseEncryptionKey): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setDatabaseEncryptionKey';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['new_encryption_key'] = value.new_encryption_key;
  return out;
}

/** setDefaultBackground = Background — Sets default background for chats; adds the background to the list of installed backgrounds */
export class SetDefaultBackground {
  type: 'setDefaultBackground' = 'setDefaultBackground';
  extra?: string;
  /** background: InputBackground — The input background to use; pass null to create a new filled background */
  background: TdInputBackground | null = null;
  /** type: BackgroundType — Background type; pass null to use the default type of the remote background; backgroundTypeChatTheme isn't supported */
  type_: TdBackgroundType | null = null;
  /** for_dark_theme: Bool — Pass true if the background is set for a dark theme */
  for_dark_theme: boolean = false;
}

/** Decode a TDLib `setDefaultBackground` object; null input -> null. */
export function decodeSetDefaultBackground(json: TdJsonRaw): SetDefaultBackground | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetDefaultBackground = new SetDefaultBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.background = decodeTdInputBackground(record['background']);
  out.type_ = decodeTdBackgroundType(record['type']);
  out.for_dark_theme = asBool(record['for_dark_theme']);
  return out;
}

/** Encode a `setDefaultBackground` into TDLib JSON wire shape. */
export function encodeSetDefaultBackground(value: SetDefaultBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setDefaultBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['background'] = value.background === null ? null : encodeTdInputBackground(value.background);
  out['type'] = value.type_ === null ? null : encodeTdBackgroundType(value.type_);
  out['for_dark_theme'] = value.for_dark_theme;
  return out;
}

/** setDefaultChannelAdministratorRights = Ok — Sets default administrator rights for adding the bot to channel chats; for bots only */
export class SetDefaultChannelAdministratorRights {
  type: 'setDefaultChannelAdministratorRights' = 'setDefaultChannelAdministratorRights';
  extra?: string;
  /** default_channel_administrator_rights: chatAdministratorRights — Default administrator rights for adding the bot to channels; pass null to remove default rights */
  default_channel_administrator_rights: ChatAdministratorRights | null = null;
}

/** Decode a TDLib `setDefaultChannelAdministratorRights` object; null input -> null. */
export function decodeSetDefaultChannelAdministratorRights(json: TdJsonRaw): SetDefaultChannelAdministratorRights | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetDefaultChannelAdministratorRights = new SetDefaultChannelAdministratorRights();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.default_channel_administrator_rights = decodeChatAdministratorRights(record['default_channel_administrator_rights']);
  return out;
}

/** Encode a `setDefaultChannelAdministratorRights` into TDLib JSON wire shape. */
export function encodeSetDefaultChannelAdministratorRights(value: SetDefaultChannelAdministratorRights): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setDefaultChannelAdministratorRights';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['default_channel_administrator_rights'] = value.default_channel_administrator_rights === null ? null : encodeChatAdministratorRights(value.default_channel_administrator_rights);
  return out;
}

/** setDefaultGroupAdministratorRights = Ok — Sets default administrator rights for adding the bot to basic group and supergroup chats; for bots only */
export class SetDefaultGroupAdministratorRights {
  type: 'setDefaultGroupAdministratorRights' = 'setDefaultGroupAdministratorRights';
  extra?: string;
  /** default_group_administrator_rights: chatAdministratorRights — Default administrator rights for adding the bot to basic group and supergroup chats; pass null to remove default rights */
  default_group_administrator_rights: ChatAdministratorRights | null = null;
}

/** Decode a TDLib `setDefaultGroupAdministratorRights` object; null input -> null. */
export function decodeSetDefaultGroupAdministratorRights(json: TdJsonRaw): SetDefaultGroupAdministratorRights | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetDefaultGroupAdministratorRights = new SetDefaultGroupAdministratorRights();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.default_group_administrator_rights = decodeChatAdministratorRights(record['default_group_administrator_rights']);
  return out;
}

/** Encode a `setDefaultGroupAdministratorRights` into TDLib JSON wire shape. */
export function encodeSetDefaultGroupAdministratorRights(value: SetDefaultGroupAdministratorRights): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setDefaultGroupAdministratorRights';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['default_group_administrator_rights'] = value.default_group_administrator_rights === null ? null : encodeChatAdministratorRights(value.default_group_administrator_rights);
  return out;
}

/** setDefaultMessageAutoDeleteTime = Ok — Changes the default message auto-delete time for new chats */
export class SetDefaultMessageAutoDeleteTime {
  type: 'setDefaultMessageAutoDeleteTime' = 'setDefaultMessageAutoDeleteTime';
  extra?: string;
  /** message_auto_delete_time: messageAutoDeleteTime — New default message auto-delete time; must be from 0 up to 365 * 86400 and be divisible by 86400. If 0, then messages aren't deleted automatically */
  message_auto_delete_time: MessageAutoDeleteTime | null = null;
}

/** Decode a TDLib `setDefaultMessageAutoDeleteTime` object; null input -> null. */
export function decodeSetDefaultMessageAutoDeleteTime(json: TdJsonRaw): SetDefaultMessageAutoDeleteTime | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetDefaultMessageAutoDeleteTime = new SetDefaultMessageAutoDeleteTime();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_auto_delete_time = decodeMessageAutoDeleteTime(record['message_auto_delete_time']);
  return out;
}

/** Encode a `setDefaultMessageAutoDeleteTime` into TDLib JSON wire shape. */
export function encodeSetDefaultMessageAutoDeleteTime(value: SetDefaultMessageAutoDeleteTime): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setDefaultMessageAutoDeleteTime';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_auto_delete_time'] = value.message_auto_delete_time === null ? null : encodeMessageAutoDeleteTime(value.message_auto_delete_time);
  return out;
}

/** setDefaultReactionType = Ok — Changes type of default reaction for the current user */
export class SetDefaultReactionType {
  type: 'setDefaultReactionType' = 'setDefaultReactionType';
  extra?: string;
  /** reaction_type: ReactionType — New type of the default reaction. The paid reaction can't be set as default */
  reaction_type: TdReactionType | null = null;
}

/** Decode a TDLib `setDefaultReactionType` object; null input -> null. */
export function decodeSetDefaultReactionType(json: TdJsonRaw): SetDefaultReactionType | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetDefaultReactionType = new SetDefaultReactionType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reaction_type = decodeTdReactionType(record['reaction_type']);
  return out;
}

/** Encode a `setDefaultReactionType` into TDLib JSON wire shape. */
export function encodeSetDefaultReactionType(value: SetDefaultReactionType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setDefaultReactionType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reaction_type'] = value.reaction_type === null ? null : encodeTdReactionType(value.reaction_type);
  return out;
}

/** setDirectMessagesChatTopicIsMarkedAsUnread = Ok — Changes the marked as unread state of the topic in a channel direct messages chat administered by the current user */
export class SetDirectMessagesChatTopicIsMarkedAsUnread {
  type: 'setDirectMessagesChatTopicIsMarkedAsUnread' = 'setDirectMessagesChatTopicIsMarkedAsUnread';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** topic_id: int53 — Topic identifier */
  topic_id: number = 0;
  /** is_marked_as_unread: Bool — New value of is_marked_as_unread */
  is_marked_as_unread: boolean = false;
}

/** Decode a TDLib `setDirectMessagesChatTopicIsMarkedAsUnread` object; null input -> null. */
export function decodeSetDirectMessagesChatTopicIsMarkedAsUnread(json: TdJsonRaw): SetDirectMessagesChatTopicIsMarkedAsUnread | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetDirectMessagesChatTopicIsMarkedAsUnread = new SetDirectMessagesChatTopicIsMarkedAsUnread();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  out.is_marked_as_unread = asBool(record['is_marked_as_unread']);
  return out;
}

/** Encode a `setDirectMessagesChatTopicIsMarkedAsUnread` into TDLib JSON wire shape. */
export function encodeSetDirectMessagesChatTopicIsMarkedAsUnread(value: SetDirectMessagesChatTopicIsMarkedAsUnread): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setDirectMessagesChatTopicIsMarkedAsUnread';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  out['is_marked_as_unread'] = value.is_marked_as_unread;
  return out;
}

/** setEmojiStatus = Ok — Changes the emoji status of the current user; for Telegram Premium users only */
export class SetEmojiStatus {
  type: 'setEmojiStatus' = 'setEmojiStatus';
  extra?: string;
  /** emoji_status: emojiStatus — New emoji status; pass null to switch to the default badge */
  emoji_status: EmojiStatus | null = null;
}

/** Decode a TDLib `setEmojiStatus` object; null input -> null. */
export function decodeSetEmojiStatus(json: TdJsonRaw): SetEmojiStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetEmojiStatus = new SetEmojiStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji_status = decodeEmojiStatus(record['emoji_status']);
  return out;
}

/** Encode a `setEmojiStatus` into TDLib JSON wire shape. */
export function encodeSetEmojiStatus(value: SetEmojiStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setEmojiStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji_status'] = value.emoji_status === null ? null : encodeEmojiStatus(value.emoji_status);
  return out;
}

/** setFileGenerationProgress = Ok — Informs TDLib on a file generation progress */
export class SetFileGenerationProgress {
  type: 'setFileGenerationProgress' = 'setFileGenerationProgress';
  extra?: string;
  /** generation_id: int64 — The identifier of the generation process */
  generation_id: string = '';
  /** expected_size: int53 — Expected size of the generated file, in bytes; 0 if unknown */
  expected_size: number = 0;
  /** local_prefix_size: int53 — The number of bytes already generated */
  local_prefix_size: number = 0;
}

/** Decode a TDLib `setFileGenerationProgress` object; null input -> null. */
export function decodeSetFileGenerationProgress(json: TdJsonRaw): SetFileGenerationProgress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetFileGenerationProgress = new SetFileGenerationProgress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.generation_id = asInt64(record['generation_id']);
  out.expected_size = asNumber(record['expected_size']);
  out.local_prefix_size = asNumber(record['local_prefix_size']);
  return out;
}

/** Encode a `setFileGenerationProgress` into TDLib JSON wire shape. */
export function encodeSetFileGenerationProgress(value: SetFileGenerationProgress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setFileGenerationProgress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['generation_id'] = value.generation_id;
  out['expected_size'] = value.expected_size;
  out['local_prefix_size'] = value.local_prefix_size;
  return out;
}

/** setForumTopicNotificationSettings = Ok — Changes the notification settings of a forum topic in a forum supergroup chat or a chat with a bot with topics */
export class SetForumTopicNotificationSettings {
  type: 'setForumTopicNotificationSettings' = 'setForumTopicNotificationSettings';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier */
  forum_topic_id: number = 0;
  /** notification_settings: chatNotificationSettings — New notification settings for the forum topic. If the topic is muted for more than 366 days, it is considered to be muted forever */
  notification_settings: ChatNotificationSettings | null = null;
}

/** Decode a TDLib `setForumTopicNotificationSettings` object; null input -> null. */
export function decodeSetForumTopicNotificationSettings(json: TdJsonRaw): SetForumTopicNotificationSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetForumTopicNotificationSettings = new SetForumTopicNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.notification_settings = decodeChatNotificationSettings(record['notification_settings']);
  return out;
}

/** Encode a `setForumTopicNotificationSettings` into TDLib JSON wire shape. */
export function encodeSetForumTopicNotificationSettings(value: SetForumTopicNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setForumTopicNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['notification_settings'] = value.notification_settings === null ? null : encodeChatNotificationSettings(value.notification_settings);
  return out;
}

/** setGameScore = Message — Updates the game score of the specified user in the game; for bots only */
export class SetGameScore {
  type: 'setGameScore' = 'setGameScore';
  extra?: string;
  /** chat_id: int53 — The chat to which the message with the game belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** edit_message: Bool — Pass true to edit the game message to include the current scoreboard */
  edit_message: boolean = false;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** score: int32 — The new score */
  score: number = 0;
  /** force: Bool — Pass true to update the score even if it decreases. If the score is 0, the user will be deleted from the high score table */
  force: boolean = false;
}

/** Decode a TDLib `setGameScore` object; null input -> null. */
export function decodeSetGameScore(json: TdJsonRaw): SetGameScore | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetGameScore = new SetGameScore();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.edit_message = asBool(record['edit_message']);
  out.user_id = asNumber(record['user_id']);
  out.score = asNumber(record['score']);
  out.force = asBool(record['force']);
  return out;
}

/** Encode a `setGameScore` into TDLib JSON wire shape. */
export function encodeSetGameScore(value: SetGameScore): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setGameScore';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['edit_message'] = value.edit_message;
  out['user_id'] = value.user_id;
  out['score'] = value.score;
  out['force'] = value.force;
  return out;
}

/** setGiftCollectionName = GiftCollection — Changes name of a gift collection. If the collection is owned by a channel chat, then requires can_post_messages administrator right in the channel chat. Returns the changed collection */
export class SetGiftCollectionName {
  type: 'setGiftCollectionName' = 'setGiftCollectionName';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that owns the collection */
  owner_id: TdMessageSender | null = null;
  /** collection_id: int32 — Identifier of the gift collection */
  collection_id: number = 0;
  /** name: string — New name of the collection; 1-12 characters */
  name: string = '';
}

/** Decode a TDLib `setGiftCollectionName` object; null input -> null. */
export function decodeSetGiftCollectionName(json: TdJsonRaw): SetGiftCollectionName | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetGiftCollectionName = new SetGiftCollectionName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.collection_id = asNumber(record['collection_id']);
  out.name = asString(record['name']);
  return out;
}

/** Encode a `setGiftCollectionName` into TDLib JSON wire shape. */
export function encodeSetGiftCollectionName(value: SetGiftCollectionName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setGiftCollectionName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['collection_id'] = value.collection_id;
  out['name'] = value.name;
  return out;
}

/** setGiftResalePrice = Ok — Changes resale price of a unique gift owned by the current user getOption("gift_resale_star_earnings_per_mille") Telegram Stars for each 1000 Telegram Stars paid for the gift if the gift price is in Telegram Sta... */
export class SetGiftResalePrice {
  type: 'setGiftResalePrice' = 'setGiftResalePrice';
  extra?: string;
  /** received_gift_id: string — Identifier of the unique gift */
  received_gift_id: string = '';
  /** price: GiftResalePrice — The new price for the unique gift; pass null to disallow gift resale. The current user will receive */
  price: TdGiftResalePrice | null = null;
}

/** Decode a TDLib `setGiftResalePrice` object; null input -> null. */
export function decodeSetGiftResalePrice(json: TdJsonRaw): SetGiftResalePrice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetGiftResalePrice = new SetGiftResalePrice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.received_gift_id = asString(record['received_gift_id']);
  out.price = decodeTdGiftResalePrice(record['price']);
  return out;
}

/** Encode a `setGiftResalePrice` into TDLib JSON wire shape. */
export function encodeSetGiftResalePrice(value: SetGiftResalePrice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setGiftResalePrice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['received_gift_id'] = value.received_gift_id;
  out['price'] = value.price === null ? null : encodeTdGiftResalePrice(value.price);
  return out;
}

/** setGiftSettings = Ok — Changes settings for gift receiving for the current user */
export class SetGiftSettings {
  type: 'setGiftSettings' = 'setGiftSettings';
  extra?: string;
  /** settings: giftSettings — The new settings */
  settings: GiftSettings | null = null;
}

/** Decode a TDLib `setGiftSettings` object; null input -> null. */
export function decodeSetGiftSettings(json: TdJsonRaw): SetGiftSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetGiftSettings = new SetGiftSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.settings = decodeGiftSettings(record['settings']);
  return out;
}

/** Encode a `setGiftSettings` into TDLib JSON wire shape. */
export function encodeSetGiftSettings(value: SetGiftSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setGiftSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['settings'] = value.settings === null ? null : encodeGiftSettings(value.settings);
  return out;
}

/** setGroupCallPaidMessageStarCount = Ok — Changes the minimum number of Telegram Stars that must be paid by general participant for each sent message to a live story call. Requires groupCall.can_be_managed right */
export class SetGroupCallPaidMessageStarCount {
  type: 'setGroupCallPaidMessageStarCount' = 'setGroupCallPaidMessageStarCount';
  extra?: string;
  /** group_call_id: int32 — Group call identifier; must be an identifier of a live story call */
  group_call_id: number = 0;
  /** paid_message_star_count: int53 — The new minimum number of Telegram Stars; 0-getOption("paid_group_call_message_star_count_max") */
  paid_message_star_count: number = 0;
}

/** Decode a TDLib `setGroupCallPaidMessageStarCount` object; null input -> null. */
export function decodeSetGroupCallPaidMessageStarCount(json: TdJsonRaw): SetGroupCallPaidMessageStarCount | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetGroupCallPaidMessageStarCount = new SetGroupCallPaidMessageStarCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  return out;
}

/** Encode a `setGroupCallPaidMessageStarCount` into TDLib JSON wire shape. */
export function encodeSetGroupCallPaidMessageStarCount(value: SetGroupCallPaidMessageStarCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setGroupCallPaidMessageStarCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['paid_message_star_count'] = value.paid_message_star_count;
  return out;
}

/** setGroupCallParticipantIsSpeaking = MessageSender — Informs TDLib that speaking state of a participant of an active group call has changed. Returns identifier of the participant if it is found */
export class SetGroupCallParticipantIsSpeaking {
  type: 'setGroupCallParticipantIsSpeaking' = 'setGroupCallParticipantIsSpeaking';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** audio_source: int32 — Group call participant's synchronization audio source identifier, or 0 for the current user */
  audio_source: number = 0;
  /** is_speaking: Bool — Pass true if the user is speaking */
  is_speaking: boolean = false;
}

/** Decode a TDLib `setGroupCallParticipantIsSpeaking` object; null input -> null. */
export function decodeSetGroupCallParticipantIsSpeaking(json: TdJsonRaw): SetGroupCallParticipantIsSpeaking | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetGroupCallParticipantIsSpeaking = new SetGroupCallParticipantIsSpeaking();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.audio_source = asNumber(record['audio_source']);
  out.is_speaking = asBool(record['is_speaking']);
  return out;
}

/** Encode a `setGroupCallParticipantIsSpeaking` into TDLib JSON wire shape. */
export function encodeSetGroupCallParticipantIsSpeaking(value: SetGroupCallParticipantIsSpeaking): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setGroupCallParticipantIsSpeaking';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['audio_source'] = value.audio_source;
  out['is_speaking'] = value.is_speaking;
  return out;
}

/** setGroupCallParticipantVolumeLevel = Ok — Changes volume level of a participant of an active group call; not supported for live stories. If the current user can manage the group call or is the owner of the group call, then the participan... */
export class SetGroupCallParticipantVolumeLevel {
  type: 'setGroupCallParticipantVolumeLevel' = 'setGroupCallParticipantVolumeLevel';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** participant_id: MessageSender — Participant identifier */
  participant_id: TdMessageSender | null = null;
  /** volume_level: int32 — New participant's volume level; 1-20000 in hundreds of percents */
  volume_level: number = 0;
}

/** Decode a TDLib `setGroupCallParticipantVolumeLevel` object; null input -> null. */
export function decodeSetGroupCallParticipantVolumeLevel(json: TdJsonRaw): SetGroupCallParticipantVolumeLevel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetGroupCallParticipantVolumeLevel = new SetGroupCallParticipantVolumeLevel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.participant_id = decodeTdMessageSender(record['participant_id']);
  out.volume_level = asNumber(record['volume_level']);
  return out;
}

/** Encode a `setGroupCallParticipantVolumeLevel` into TDLib JSON wire shape. */
export function encodeSetGroupCallParticipantVolumeLevel(value: SetGroupCallParticipantVolumeLevel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setGroupCallParticipantVolumeLevel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['participant_id'] = value.participant_id === null ? null : encodeTdMessageSender(value.participant_id);
  out['volume_level'] = value.volume_level;
  return out;
}

/** setInactiveSessionTtl = Ok — Changes the period of inactivity after which sessions will automatically be terminated */
export class SetInactiveSessionTtl {
  type: 'setInactiveSessionTtl' = 'setInactiveSessionTtl';
  extra?: string;
  /** inactive_session_ttl_days: int32 — New number of days of inactivity before sessions will be automatically terminated; 1-366 days */
  inactive_session_ttl_days: number = 0;
}

/** Decode a TDLib `setInactiveSessionTtl` object; null input -> null. */
export function decodeSetInactiveSessionTtl(json: TdJsonRaw): SetInactiveSessionTtl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetInactiveSessionTtl = new SetInactiveSessionTtl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inactive_session_ttl_days = asNumber(record['inactive_session_ttl_days']);
  return out;
}

/** Encode a `setInactiveSessionTtl` into TDLib JSON wire shape. */
export function encodeSetInactiveSessionTtl(value: SetInactiveSessionTtl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setInactiveSessionTtl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inactive_session_ttl_days'] = value.inactive_session_ttl_days;
  return out;
}

/** setInlineGameScore = Ok — Updates the game score of the specified user in a game; for bots only */
export class SetInlineGameScore {
  type: 'setInlineGameScore' = 'setInlineGameScore';
  extra?: string;
  /** inline_message_id: string — Inline message identifier */
  inline_message_id: string = '';
  /** edit_message: Bool — Pass true to edit the game message to include the current scoreboard */
  edit_message: boolean = false;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** score: int32 — The new score */
  score: number = 0;
  /** force: Bool — Pass true to update the score even if it decreases. If the score is 0, the user will be deleted from the high score table */
  force: boolean = false;
}

/** Decode a TDLib `setInlineGameScore` object; null input -> null. */
export function decodeSetInlineGameScore(json: TdJsonRaw): SetInlineGameScore | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetInlineGameScore = new SetInlineGameScore();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_message_id = asString(record['inline_message_id']);
  out.edit_message = asBool(record['edit_message']);
  out.user_id = asNumber(record['user_id']);
  out.score = asNumber(record['score']);
  out.force = asBool(record['force']);
  return out;
}

/** Encode a `setInlineGameScore` into TDLib JSON wire shape. */
export function encodeSetInlineGameScore(value: SetInlineGameScore): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setInlineGameScore';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_message_id'] = value.inline_message_id;
  out['edit_message'] = value.edit_message;
  out['user_id'] = value.user_id;
  out['score'] = value.score;
  out['force'] = value.force;
  return out;
}

/** setLiveStoryMessageSender = Ok — Selects a message sender to send messages in a live story call */
export class SetLiveStoryMessageSender {
  type: 'setLiveStoryMessageSender' = 'setLiveStoryMessageSender';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** message_sender_id: MessageSender — New message sender for the group call */
  message_sender_id: TdMessageSender | null = null;
}

/** Decode a TDLib `setLiveStoryMessageSender` object; null input -> null. */
export function decodeSetLiveStoryMessageSender(json: TdJsonRaw): SetLiveStoryMessageSender | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetLiveStoryMessageSender = new SetLiveStoryMessageSender();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.message_sender_id = decodeTdMessageSender(record['message_sender_id']);
  return out;
}

/** Encode a `setLiveStoryMessageSender` into TDLib JSON wire shape. */
export function encodeSetLiveStoryMessageSender(value: SetLiveStoryMessageSender): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setLiveStoryMessageSender';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['message_sender_id'] = value.message_sender_id === null ? null : encodeTdMessageSender(value.message_sender_id);
  return out;
}

/** setLogStream = Ok — Sets new log stream for internal logging of TDLib. Can be called synchronously */
export class SetLogStream {
  type: 'setLogStream' = 'setLogStream';
  extra?: string;
  /** log_stream: LogStream — New log stream */
  log_stream: TdLogStream | null = null;
}

/** Decode a TDLib `setLogStream` object; null input -> null. */
export function decodeSetLogStream(json: TdJsonRaw): SetLogStream | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetLogStream = new SetLogStream();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.log_stream = decodeTdLogStream(record['log_stream']);
  return out;
}

/** Encode a `setLogStream` into TDLib JSON wire shape. */
export function encodeSetLogStream(value: SetLogStream): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setLogStream';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['log_stream'] = value.log_stream === null ? null : encodeTdLogStream(value.log_stream);
  return out;
}

/** setLogTagVerbosityLevel = Ok — Sets the verbosity level for a specified TDLib internal log tag. Can be called synchronously */
export class SetLogTagVerbosityLevel {
  type: 'setLogTagVerbosityLevel' = 'setLogTagVerbosityLevel';
  extra?: string;
  /** tag: string — Logging tag to change verbosity level */
  tag: string = '';
  /** new_verbosity_level: int32 — New verbosity level; 1-1024 */
  new_verbosity_level: number = 0;
}

/** Decode a TDLib `setLogTagVerbosityLevel` object; null input -> null. */
export function decodeSetLogTagVerbosityLevel(json: TdJsonRaw): SetLogTagVerbosityLevel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetLogTagVerbosityLevel = new SetLogTagVerbosityLevel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.tag = asString(record['tag']);
  out.new_verbosity_level = asNumber(record['new_verbosity_level']);
  return out;
}

/** Encode a `setLogTagVerbosityLevel` into TDLib JSON wire shape. */
export function encodeSetLogTagVerbosityLevel(value: SetLogTagVerbosityLevel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setLogTagVerbosityLevel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['tag'] = value.tag;
  out['new_verbosity_level'] = value.new_verbosity_level;
  return out;
}

/** setLogVerbosityLevel = Ok — Sets the verbosity level of the internal logging of TDLib. Can be called synchronously value 3 corresponds to informational, value 4 corresponds to debug, value 5 corresponds to verbose debug, value greater th... */
export class SetLogVerbosityLevel {
  type: 'setLogVerbosityLevel' = 'setLogVerbosityLevel';
  extra?: string;
  /** new_verbosity_level: int32 — New value of the verbosity level for logging. Value 0 corresponds to fatal errors, value 1 corresponds to errors, value 2 corresponds to warnings and debug warnings, */
  new_verbosity_level: number = 0;
}

/** Decode a TDLib `setLogVerbosityLevel` object; null input -> null. */
export function decodeSetLogVerbosityLevel(json: TdJsonRaw): SetLogVerbosityLevel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetLogVerbosityLevel = new SetLogVerbosityLevel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.new_verbosity_level = asNumber(record['new_verbosity_level']);
  return out;
}

/** Encode a `setLogVerbosityLevel` into TDLib JSON wire shape. */
export function encodeSetLogVerbosityLevel(value: SetLogVerbosityLevel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setLogVerbosityLevel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['new_verbosity_level'] = value.new_verbosity_level;
  return out;
}

/** setLoginEmailAddress = EmailAddressAuthenticationCodeInfo — Changes the login email address of the user. The email address can be changed only if the current user already has login email and passwordState.login_email_address_pattern is n... */
export class SetLoginEmailAddress {
  type: 'setLoginEmailAddress' = 'setLoginEmailAddress';
  extra?: string;
  /** new_login_email_address: string — New login email address */
  new_login_email_address: string = '';
}

/** Decode a TDLib `setLoginEmailAddress` object; null input -> null. */
export function decodeSetLoginEmailAddress(json: TdJsonRaw): SetLoginEmailAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetLoginEmailAddress = new SetLoginEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.new_login_email_address = asString(record['new_login_email_address']);
  return out;
}

/** Encode a `setLoginEmailAddress` into TDLib JSON wire shape. */
export function encodeSetLoginEmailAddress(value: SetLoginEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setLoginEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['new_login_email_address'] = value.new_login_email_address;
  return out;
}

/** setMainProfileTab = Ok — Changes the main profile tab of the current user */
export class SetMainProfileTab {
  type: 'setMainProfileTab' = 'setMainProfileTab';
  extra?: string;
  /** main_profile_tab: ProfileTab — The new value of the main profile tab */
  main_profile_tab: TdProfileTab | null = null;
}

/** Decode a TDLib `setMainProfileTab` object; null input -> null. */
export function decodeSetMainProfileTab(json: TdJsonRaw): SetMainProfileTab | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetMainProfileTab = new SetMainProfileTab();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.main_profile_tab = decodeTdProfileTab(record['main_profile_tab']);
  return out;
}

/** Encode a `setMainProfileTab` into TDLib JSON wire shape. */
export function encodeSetMainProfileTab(value: SetMainProfileTab): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setMainProfileTab';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['main_profile_tab'] = value.main_profile_tab === null ? null : encodeTdProfileTab(value.main_profile_tab);
  return out;
}

/** setManagedBotAccessSettings = Ok — Sets access settings of a managed bot; for bots only */
export class SetManagedBotAccessSettings {
  type: 'setManagedBotAccessSettings' = 'setManagedBotAccessSettings';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the managed bot */
  bot_user_id: number = 0;
  /** settings: botAccessSettings — New access settings */
  settings: BotAccessSettings | null = null;
}

/** Decode a TDLib `setManagedBotAccessSettings` object; null input -> null. */
export function decodeSetManagedBotAccessSettings(json: TdJsonRaw): SetManagedBotAccessSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetManagedBotAccessSettings = new SetManagedBotAccessSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.settings = decodeBotAccessSettings(record['settings']);
  return out;
}

/** Encode a `setManagedBotAccessSettings` into TDLib JSON wire shape. */
export function encodeSetManagedBotAccessSettings(value: SetManagedBotAccessSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setManagedBotAccessSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['settings'] = value.settings === null ? null : encodeBotAccessSettings(value.settings);
  return out;
}

/** setMenuButton = Ok — Sets menu button for the given user or for all users; for bots only */
export class SetMenuButton {
  type: 'setMenuButton' = 'setMenuButton';
  extra?: string;
  /** user_id: int53 — Identifier of the user or 0 to set menu button for all users */
  user_id: number = 0;
  /** menu_button: botMenuButton — New menu button */
  menu_button: BotMenuButton | null = null;
}

/** Decode a TDLib `setMenuButton` object; null input -> null. */
export function decodeSetMenuButton(json: TdJsonRaw): SetMenuButton | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetMenuButton = new SetMenuButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.menu_button = decodeBotMenuButton(record['menu_button']);
  return out;
}

/** Encode a `setMenuButton` into TDLib JSON wire shape. */
export function encodeSetMenuButton(value: SetMenuButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setMenuButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['menu_button'] = value.menu_button === null ? null : encodeBotMenuButton(value.menu_button);
  return out;
}

/** setMessageFactCheck = Ok — Changes the fact-check of a message. Can be only used if messageProperties.can_set_fact_check == true */
export class SetMessageFactCheck {
  type: 'setMessageFactCheck' = 'setMessageFactCheck';
  extra?: string;
  /** chat_id: int53 — The channel chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** text: formattedText — New text of the fact-check; 0-getOption("fact_check_length_max") characters; pass null to remove it. Only Bold, Italic, and TextUrl entities with https://t.me/ links are supported */
  text: FormattedText | null = null;
}

/** Decode a TDLib `setMessageFactCheck` object; null input -> null. */
export function decodeSetMessageFactCheck(json: TdJsonRaw): SetMessageFactCheck | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetMessageFactCheck = new SetMessageFactCheck();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `setMessageFactCheck` into TDLib JSON wire shape. */
export function encodeSetMessageFactCheck(value: SetMessageFactCheck): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setMessageFactCheck';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** setMessageReactions = Ok — Sets reactions on a message; for bots only */
export class SetMessageReactions {
  type: 'setMessageReactions' = 'setMessageReactions';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** reaction_types: vector<ReactionType> — Types of the reaction to set; pass an empty list to remove the reactions */
  reaction_types: TdReactionType[] = [];
  /** is_big: Bool — Pass true if the reactions are added with a big animation */
  is_big: boolean = false;
}

/** Decode a TDLib `setMessageReactions` object; null input -> null. */
export function decodeSetMessageReactions(json: TdJsonRaw): SetMessageReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetMessageReactions = new SetMessageReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reaction_types = decodeVector(record['reaction_types'], (item: TdJsonRaw): TdReactionType => decodeTdReactionType(item) as TdReactionType);
  out.is_big = asBool(record['is_big']);
  return out;
}

/** Encode a `setMessageReactions` into TDLib JSON wire shape. */
export function encodeSetMessageReactions(value: SetMessageReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setMessageReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reaction_types'] = encodeVector(value.reaction_types, (item: TdReactionType | null): TdJsonRaw => item === null ? null : encodeTdReactionType(item));
  out['is_big'] = value.is_big;
  return out;
}

/** setMessageSenderBlockList = Ok — Changes the block list of a message sender. Currently, only users and supergroup chats can be blocked */
export class SetMessageSenderBlockList {
  type: 'setMessageSenderBlockList' = 'setMessageSenderBlockList';
  extra?: string;
  /** sender_id: MessageSender — Identifier of a message sender to block/unblock */
  sender_id: TdMessageSender | null = null;
  /** block_list: BlockList — New block list for the message sender; pass null to unblock the message sender */
  block_list: TdBlockList | null = null;
}

/** Decode a TDLib `setMessageSenderBlockList` object; null input -> null. */
export function decodeSetMessageSenderBlockList(json: TdJsonRaw): SetMessageSenderBlockList | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetMessageSenderBlockList = new SetMessageSenderBlockList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.block_list = decodeTdBlockList(record['block_list']);
  return out;
}

/** Encode a `setMessageSenderBlockList` into TDLib JSON wire shape. */
export function encodeSetMessageSenderBlockList(value: SetMessageSenderBlockList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setMessageSenderBlockList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['block_list'] = value.block_list === null ? null : encodeTdBlockList(value.block_list);
  return out;
}

/** setMessageSenderBotVerification = Ok — Changes the verification status of a user or a chat by an owned bot If empty, then "was verified by organization "organization_name"" will be used as description. Can be specified only if the bot is... */
export class SetMessageSenderBotVerification {
  type: 'setMessageSenderBotVerification' = 'setMessageSenderBotVerification';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the owned bot, which will verify the user or the chat */
  bot_user_id: number = 0;
  /** verified_id: MessageSender — Identifier of the user or the supergroup or channel chat, which will be verified by the bot */
  verified_id: TdMessageSender | null = null;
  /** custom_description: string — Custom description of verification reason; 0-getOption("bot_verification_custom_description_length_max"). */
  custom_description: string = '';
}

/** Decode a TDLib `setMessageSenderBotVerification` object; null input -> null. */
export function decodeSetMessageSenderBotVerification(json: TdJsonRaw): SetMessageSenderBotVerification | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetMessageSenderBotVerification = new SetMessageSenderBotVerification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.verified_id = decodeTdMessageSender(record['verified_id']);
  out.custom_description = asString(record['custom_description']);
  return out;
}

/** Encode a `setMessageSenderBotVerification` into TDLib JSON wire shape. */
export function encodeSetMessageSenderBotVerification(value: SetMessageSenderBotVerification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setMessageSenderBotVerification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['verified_id'] = value.verified_id === null ? null : encodeTdMessageSender(value.verified_id);
  out['custom_description'] = value.custom_description;
  return out;
}

/** setName = Ok — Changes the first and last name of the current user */
export class SetName {
  type: 'setName' = 'setName';
  extra?: string;
  /** first_name: string — The new value of the first name for the current user; 1-64 characters */
  first_name: string = '';
  /** last_name: string — The new value of the optional last name for the current user; 0-64 characters */
  last_name: string = '';
}

/** Decode a TDLib `setName` object; null input -> null. */
export function decodeSetName(json: TdJsonRaw): SetName | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetName = new SetName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.first_name = asString(record['first_name']);
  out.last_name = asString(record['last_name']);
  return out;
}

/** Encode a `setName` into TDLib JSON wire shape. */
export function encodeSetName(value: SetName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['first_name'] = value.first_name;
  out['last_name'] = value.last_name;
  return out;
}

/** setNetworkType = Ok — Sets the current network type. Can be called before authorization. Calling this method forces all network connections to reopen, mitigating the delay in switching between different networks, so it must be called whe... */
export class SetNetworkType {
  type: 'setNetworkType' = 'setNetworkType';
  extra?: string;
  /** type: NetworkType — The new network type; pass null to set network type to networkTypeOther */
  type_: TdNetworkType | null = null;
}

/** Decode a TDLib `setNetworkType` object; null input -> null. */
export function decodeSetNetworkType(json: TdJsonRaw): SetNetworkType | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetNetworkType = new SetNetworkType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdNetworkType(record['type']);
  return out;
}

/** Encode a `setNetworkType` into TDLib JSON wire shape. */
export function encodeSetNetworkType(value: SetNetworkType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setNetworkType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdNetworkType(value.type_);
  return out;
}

/** setNewChatPrivacySettings = Ok — Changes privacy settings for new chat creation; can be used only if getOption("can_set_new_chat_privacy_settings") */
export class SetNewChatPrivacySettings {
  type: 'setNewChatPrivacySettings' = 'setNewChatPrivacySettings';
  extra?: string;
  /** settings: newChatPrivacySettings — New settings */
  settings: NewChatPrivacySettings | null = null;
}

/** Decode a TDLib `setNewChatPrivacySettings` object; null input -> null. */
export function decodeSetNewChatPrivacySettings(json: TdJsonRaw): SetNewChatPrivacySettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetNewChatPrivacySettings = new SetNewChatPrivacySettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.settings = decodeNewChatPrivacySettings(record['settings']);
  return out;
}

/** Encode a `setNewChatPrivacySettings` into TDLib JSON wire shape. */
export function encodeSetNewChatPrivacySettings(value: SetNewChatPrivacySettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setNewChatPrivacySettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['settings'] = value.settings === null ? null : encodeNewChatPrivacySettings(value.settings);
  return out;
}

/** setOption = Ok — Sets the value of an option. (Check the list of available options on https://core.telegram.org/tdlib/options.) Only writable options can be set. Can be called before authorization */
export class SetOption {
  type: 'setOption' = 'setOption';
  extra?: string;
  /** name: string — The name of the option */
  name: string = '';
  /** value: OptionValue — The new value of the option; pass null to reset option value to a default value */
  value: TdOptionValue | null = null;
}

/** Decode a TDLib `setOption` object; null input -> null. */
export function decodeSetOption(json: TdJsonRaw): SetOption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetOption = new SetOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.value = decodeTdOptionValue(record['value']);
  return out;
}

/** Encode a `setOption` into TDLib JSON wire shape. */
export function encodeSetOption(value: SetOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['value'] = value.value === null ? null : encodeTdOptionValue(value.value);
  return out;
}

/** setPaidMessageReactionType = Ok — Changes type of paid message reaction of the current user on a message. The message must have paid reaction added by the current user */
export class SetPaidMessageReactionType {
  type: 'setPaidMessageReactionType' = 'setPaidMessageReactionType';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** type: PaidReactionType — New type of the paid reaction */
  type_: TdPaidReactionType | null = null;
}

/** Decode a TDLib `setPaidMessageReactionType` object; null input -> null. */
export function decodeSetPaidMessageReactionType(json: TdJsonRaw): SetPaidMessageReactionType | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPaidMessageReactionType = new SetPaidMessageReactionType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.type_ = decodeTdPaidReactionType(record['type']);
  return out;
}

/** Encode a `setPaidMessageReactionType` into TDLib JSON wire shape. */
export function encodeSetPaidMessageReactionType(value: SetPaidMessageReactionType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPaidMessageReactionType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['type'] = value.type_ === null ? null : encodeTdPaidReactionType(value.type_);
  return out;
}

/** setPassportElement = PassportElement — Adds an element to the user's Telegram Passport. May return an error with a message "PHONE_VERIFICATION_NEEDED" or "EMAIL_VERIFICATION_NEEDED" if the chosen phone number or the chosen email address ... */
export class SetPassportElement {
  type: 'setPassportElement' = 'setPassportElement';
  extra?: string;
  /** element: InputPassportElement — Input Telegram Passport element */
  element: TdInputPassportElement | null = null;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
}

/** Decode a TDLib `setPassportElement` object; null input -> null. */
export function decodeSetPassportElement(json: TdJsonRaw): SetPassportElement | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPassportElement = new SetPassportElement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.element = decodeTdInputPassportElement(record['element']);
  out.password = asString(record['password']);
  return out;
}

/** Encode a `setPassportElement` into TDLib JSON wire shape. */
export function encodeSetPassportElement(value: SetPassportElement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPassportElement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['element'] = value.element === null ? null : encodeTdInputPassportElement(value.element);
  out['password'] = value.password;
  return out;
}

/** setPassportElementErrors = Ok — Informs the user that some of the elements in their Telegram Passport contain errors; for bots only. The user will not be able to resend the elements, until the errors are fixed */
export class SetPassportElementErrors {
  type: 'setPassportElementErrors' = 'setPassportElementErrors';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** errors: vector<inputPassportElementError> — The errors */
  errors: InputPassportElementError[] = [];
}

/** Decode a TDLib `setPassportElementErrors` object; null input -> null. */
export function decodeSetPassportElementErrors(json: TdJsonRaw): SetPassportElementErrors | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPassportElementErrors = new SetPassportElementErrors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.errors = decodeVector(record['errors'], (item: TdJsonRaw): InputPassportElementError => decodeInputPassportElementError(item) as InputPassportElementError);
  return out;
}

/** Encode a `setPassportElementErrors` into TDLib JSON wire shape. */
export function encodeSetPassportElementErrors(value: SetPassportElementErrors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPassportElementErrors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['errors'] = encodeVector(value.errors, (item: InputPassportElementError | null): TdJsonRaw => item === null ? null : encodeInputPassportElementError(item));
  return out;
}

/** setPassword = PasswordState — Changes the 2-step verification password for the current user. If a new recovery email address is specified, then the change will not be applied until the new recovery email address is confirmed */
export class SetPassword {
  type: 'setPassword' = 'setPassword';
  extra?: string;
  /** old_password: string — Previous 2-step verification password of the user */
  old_password: string = '';
  /** new_password: string — New 2-step verification password of the user; may be empty to remove the password */
  new_password: string = '';
  /** new_hint: string — New password hint; may be empty */
  new_hint: string = '';
  /** set_recovery_email_address: Bool — Pass true to change also the recovery email address */
  set_recovery_email_address: boolean = false;
  /** new_recovery_email_address: string — New recovery email address; may be empty */
  new_recovery_email_address: string = '';
}

/** Decode a TDLib `setPassword` object; null input -> null. */
export function decodeSetPassword(json: TdJsonRaw): SetPassword | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPassword = new SetPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_password = asString(record['old_password']);
  out.new_password = asString(record['new_password']);
  out.new_hint = asString(record['new_hint']);
  out.set_recovery_email_address = asBool(record['set_recovery_email_address']);
  out.new_recovery_email_address = asString(record['new_recovery_email_address']);
  return out;
}

/** Encode a `setPassword` into TDLib JSON wire shape. */
export function encodeSetPassword(value: SetPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_password'] = value.old_password;
  out['new_password'] = value.new_password;
  out['new_hint'] = value.new_hint;
  out['set_recovery_email_address'] = value.set_recovery_email_address;
  out['new_recovery_email_address'] = value.new_recovery_email_address;
  return out;
}

/** setPersonalChat = Ok — Changes the personal chat of the current user */
export class SetPersonalChat {
  type: 'setPersonalChat' = 'setPersonalChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the new personal chat; pass 0 to remove the chat. Use getSuitablePersonalChats to get suitable chats */
  chat_id: number = 0;
}

/** Decode a TDLib `setPersonalChat` object; null input -> null. */
export function decodeSetPersonalChat(json: TdJsonRaw): SetPersonalChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPersonalChat = new SetPersonalChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `setPersonalChat` into TDLib JSON wire shape. */
export function encodeSetPersonalChat(value: SetPersonalChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPersonalChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** setPinnedChats = Ok — Changes the order of pinned chats */
export class SetPinnedChats {
  type: 'setPinnedChats' = 'setPinnedChats';
  extra?: string;
  /** chat_list: ChatList — Chat list in which to change the order of pinned chats */
  chat_list: TdChatList | null = null;
  /** chat_ids: vector<int53> — The new list of pinned chats */
  chat_ids: number[] = [];
}

/** Decode a TDLib `setPinnedChats` object; null input -> null. */
export function decodeSetPinnedChats(json: TdJsonRaw): SetPinnedChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPinnedChats = new SetPinnedChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_list = decodeTdChatList(record['chat_list']);
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `setPinnedChats` into TDLib JSON wire shape. */
export function encodeSetPinnedChats(value: SetPinnedChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPinnedChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** setPinnedForumTopics = Ok — Changes the order of pinned topics in a forum supergroup chat or a chat with a bot with topics; requires can_manage_topics administrator right in the supergroup */
export class SetPinnedForumTopics {
  type: 'setPinnedForumTopics' = 'setPinnedForumTopics';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_ids: vector<int32> — The new list of identifiers of the pinned forum topics */
  forum_topic_ids: number[] = [];
}

/** Decode a TDLib `setPinnedForumTopics` object; null input -> null. */
export function decodeSetPinnedForumTopics(json: TdJsonRaw): SetPinnedForumTopics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPinnedForumTopics = new SetPinnedForumTopics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_ids = decodeVector(record['forum_topic_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `setPinnedForumTopics` into TDLib JSON wire shape. */
export function encodeSetPinnedForumTopics(value: SetPinnedForumTopics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPinnedForumTopics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_ids'] = encodeVector(value.forum_topic_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** setPinnedGifts = Ok — Changes the list of pinned gifts on the current user's or the channel's profile page; requires can_post_messages administrator right in the channel chat */
export class SetPinnedGifts {
  type: 'setPinnedGifts' = 'setPinnedGifts';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that received the gifts */
  owner_id: TdMessageSender | null = null;
  /** received_gift_ids: vector<string> — New list of pinned gifts. All gifts must be upgraded and saved on the profile page first. There can be up to getOption("pinned_gift_count_max") pinned gifts */
  received_gift_ids: string[] = [];
}

/** Decode a TDLib `setPinnedGifts` object; null input -> null. */
export function decodeSetPinnedGifts(json: TdJsonRaw): SetPinnedGifts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPinnedGifts = new SetPinnedGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.received_gift_ids = decodeVector(record['received_gift_ids'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `setPinnedGifts` into TDLib JSON wire shape. */
export function encodeSetPinnedGifts(value: SetPinnedGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPinnedGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['received_gift_ids'] = encodeVector(value.received_gift_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** setPinnedSavedMessagesTopics = Ok — Changes the order of pinned Saved Messages topics */
export class SetPinnedSavedMessagesTopics {
  type: 'setPinnedSavedMessagesTopics' = 'setPinnedSavedMessagesTopics';
  extra?: string;
  /** saved_messages_topic_ids: vector<int53> — Identifiers of the new pinned Saved Messages topics */
  saved_messages_topic_ids: number[] = [];
}

/** Decode a TDLib `setPinnedSavedMessagesTopics` object; null input -> null. */
export function decodeSetPinnedSavedMessagesTopics(json: TdJsonRaw): SetPinnedSavedMessagesTopics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPinnedSavedMessagesTopics = new SetPinnedSavedMessagesTopics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_ids = decodeVector(record['saved_messages_topic_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `setPinnedSavedMessagesTopics` into TDLib JSON wire shape. */
export function encodeSetPinnedSavedMessagesTopics(value: SetPinnedSavedMessagesTopics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPinnedSavedMessagesTopics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_ids'] = encodeVector(value.saved_messages_topic_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** setPollAnswer = Ok — Changes the user answer to a poll */
export class SetPollAnswer {
  type: 'setPollAnswer' = 'setPollAnswer';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the poll belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the poll */
  message_id: number = 0;
  /** option_ids: vector<int32> — 0-based identifiers of answer options, chosen by the user. User can choose more than 1 answer option only is the poll allows multiple answers */
  option_ids: number[] = [];
}

/** Decode a TDLib `setPollAnswer` object; null input -> null. */
export function decodeSetPollAnswer(json: TdJsonRaw): SetPollAnswer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetPollAnswer = new SetPollAnswer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.option_ids = decodeVector(record['option_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `setPollAnswer` into TDLib JSON wire shape. */
export function encodeSetPollAnswer(value: SetPollAnswer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setPollAnswer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['option_ids'] = encodeVector(value.option_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** setProfileAccentColor = Ok — Changes accent color and background custom emoji for profile of the current user; for Telegram Premium users only */
export class SetProfileAccentColor {
  type: 'setProfileAccentColor' = 'setProfileAccentColor';
  extra?: string;
  /** profile_accent_color_id: int32 — Identifier of the accent color to use for profile; pass -1 if none */
  profile_accent_color_id: number = 0;
  /** profile_background_custom_emoji_id: int64 — Identifier of a custom emoji to be shown on the user's profile photo background; 0 if none */
  profile_background_custom_emoji_id: string = '';
}

/** Decode a TDLib `setProfileAccentColor` object; null input -> null. */
export function decodeSetProfileAccentColor(json: TdJsonRaw): SetProfileAccentColor | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetProfileAccentColor = new SetProfileAccentColor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.profile_accent_color_id = asNumber(record['profile_accent_color_id']);
  out.profile_background_custom_emoji_id = asInt64(record['profile_background_custom_emoji_id']);
  return out;
}

/** Encode a `setProfileAccentColor` into TDLib JSON wire shape. */
export function encodeSetProfileAccentColor(value: SetProfileAccentColor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setProfileAccentColor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['profile_accent_color_id'] = value.profile_accent_color_id;
  out['profile_background_custom_emoji_id'] = value.profile_background_custom_emoji_id;
  return out;
}

/** setProfileAudioPosition = Ok — Changes position of an audio file in the profile audio files of the current user */
export class SetProfileAudioPosition {
  type: 'setProfileAudioPosition' = 'setProfileAudioPosition';
  extra?: string;
  /** file_id: int32 — Identifier of the file from profile audio files, which position will be changed */
  file_id: number = 0;
  /** after_file_id: int32 — Identifier of the file from profile audio files after which the file will be positioned; pass 0 to move the file to the beginning of the list */
  after_file_id: number = 0;
}

/** Decode a TDLib `setProfileAudioPosition` object; null input -> null. */
export function decodeSetProfileAudioPosition(json: TdJsonRaw): SetProfileAudioPosition | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetProfileAudioPosition = new SetProfileAudioPosition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.after_file_id = asNumber(record['after_file_id']);
  return out;
}

/** Encode a `setProfileAudioPosition` into TDLib JSON wire shape. */
export function encodeSetProfileAudioPosition(value: SetProfileAudioPosition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setProfileAudioPosition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['after_file_id'] = value.after_file_id;
  return out;
}

/** setProfilePhoto = Ok — Changes a profile photo for the current user */
export class SetProfilePhoto {
  type: 'setProfilePhoto' = 'setProfilePhoto';
  extra?: string;
  /** photo: InputChatPhoto — Profile photo to set */
  photo: TdInputChatPhoto | null = null;
  /** is_public: Bool — Pass true to set the public photo, which will be visible even if the main photo is hidden by privacy settings */
  is_public: boolean = false;
}

/** Decode a TDLib `setProfilePhoto` object; null input -> null. */
export function decodeSetProfilePhoto(json: TdJsonRaw): SetProfilePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetProfilePhoto = new SetProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeTdInputChatPhoto(record['photo']);
  out.is_public = asBool(record['is_public']);
  return out;
}

/** Encode a `setProfilePhoto` into TDLib JSON wire shape. */
export function encodeSetProfilePhoto(value: SetProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeTdInputChatPhoto(value.photo);
  out['is_public'] = value.is_public;
  return out;
}

/** setQuickReplyShortcutName = Ok — Changes name of a quick reply shortcut */
export class SetQuickReplyShortcutName {
  type: 'setQuickReplyShortcutName' = 'setQuickReplyShortcutName';
  extra?: string;
  /** shortcut_id: int32 — Unique identifier of the quick reply shortcut */
  shortcut_id: number = 0;
  /** name: string — New name for the shortcut. Use checkQuickReplyShortcutName to check its validness */
  name: string = '';
}

/** Decode a TDLib `setQuickReplyShortcutName` object; null input -> null. */
export function decodeSetQuickReplyShortcutName(json: TdJsonRaw): SetQuickReplyShortcutName | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetQuickReplyShortcutName = new SetQuickReplyShortcutName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_id = asNumber(record['shortcut_id']);
  out.name = asString(record['name']);
  return out;
}

/** Encode a `setQuickReplyShortcutName` into TDLib JSON wire shape. */
export function encodeSetQuickReplyShortcutName(value: SetQuickReplyShortcutName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setQuickReplyShortcutName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_id'] = value.shortcut_id;
  out['name'] = value.name;
  return out;
}

/** setReactionNotificationSettings = Ok — Changes notification settings for reactions */
export class SetReactionNotificationSettings {
  type: 'setReactionNotificationSettings' = 'setReactionNotificationSettings';
  extra?: string;
  /** notification_settings: reactionNotificationSettings — The new notification settings for reactions */
  notification_settings: ReactionNotificationSettings | null = null;
}

/** Decode a TDLib `setReactionNotificationSettings` object; null input -> null. */
export function decodeSetReactionNotificationSettings(json: TdJsonRaw): SetReactionNotificationSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetReactionNotificationSettings = new SetReactionNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_settings = decodeReactionNotificationSettings(record['notification_settings']);
  return out;
}

/** Encode a `setReactionNotificationSettings` into TDLib JSON wire shape. */
export function encodeSetReactionNotificationSettings(value: SetReactionNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setReactionNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_settings'] = value.notification_settings === null ? null : encodeReactionNotificationSettings(value.notification_settings);
  return out;
}

/** setReadDatePrivacySettings = Ok — Changes privacy settings for message read date */
export class SetReadDatePrivacySettings {
  type: 'setReadDatePrivacySettings' = 'setReadDatePrivacySettings';
  extra?: string;
  /** settings: readDatePrivacySettings — New settings */
  settings: ReadDatePrivacySettings | null = null;
}

/** Decode a TDLib `setReadDatePrivacySettings` object; null input -> null. */
export function decodeSetReadDatePrivacySettings(json: TdJsonRaw): SetReadDatePrivacySettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetReadDatePrivacySettings = new SetReadDatePrivacySettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.settings = decodeReadDatePrivacySettings(record['settings']);
  return out;
}

/** Encode a `setReadDatePrivacySettings` into TDLib JSON wire shape. */
export function encodeSetReadDatePrivacySettings(value: SetReadDatePrivacySettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setReadDatePrivacySettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['settings'] = value.settings === null ? null : encodeReadDatePrivacySettings(value.settings);
  return out;
}

/** setRecoveryEmailAddress = PasswordState — Changes the 2-step verification recovery email address of the user. If a new recovery email address is specified, then the change will not be applied until the new recovery email address is confi... */
export class SetRecoveryEmailAddress {
  type: 'setRecoveryEmailAddress' = 'setRecoveryEmailAddress';
  extra?: string;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
  /** new_recovery_email_address: string — New recovery email address */
  new_recovery_email_address: string = '';
}

/** Decode a TDLib `setRecoveryEmailAddress` object; null input -> null. */
export function decodeSetRecoveryEmailAddress(json: TdJsonRaw): SetRecoveryEmailAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetRecoveryEmailAddress = new SetRecoveryEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.password = asString(record['password']);
  out.new_recovery_email_address = asString(record['new_recovery_email_address']);
  return out;
}

/** Encode a `setRecoveryEmailAddress` into TDLib JSON wire shape. */
export function encodeSetRecoveryEmailAddress(value: SetRecoveryEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setRecoveryEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['password'] = value.password;
  out['new_recovery_email_address'] = value.new_recovery_email_address;
  return out;
}

/** setSavedMessagesTagLabel = Ok — Changes label of a Saved Messages tag; for Telegram Premium users only */
export class SetSavedMessagesTagLabel {
  type: 'setSavedMessagesTagLabel' = 'setSavedMessagesTagLabel';
  extra?: string;
  /** tag: ReactionType — The tag which label will be changed */
  tag: TdReactionType | null = null;
  /** label: string — New label for the tag; 0-12 characters */
  label: string = '';
}

/** Decode a TDLib `setSavedMessagesTagLabel` object; null input -> null. */
export function decodeSetSavedMessagesTagLabel(json: TdJsonRaw): SetSavedMessagesTagLabel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetSavedMessagesTagLabel = new SetSavedMessagesTagLabel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.tag = decodeTdReactionType(record['tag']);
  out.label = asString(record['label']);
  return out;
}

/** Encode a `setSavedMessagesTagLabel` into TDLib JSON wire shape. */
export function encodeSetSavedMessagesTagLabel(value: SetSavedMessagesTagLabel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setSavedMessagesTagLabel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['tag'] = value.tag === null ? null : encodeTdReactionType(value.tag);
  out['label'] = value.label;
  return out;
}

/** setScopeNotificationSettings = Ok — Changes notification settings for chats of a given type */
export class SetScopeNotificationSettings {
  type: 'setScopeNotificationSettings' = 'setScopeNotificationSettings';
  extra?: string;
  /** scope: NotificationSettingsScope — Types of chats for which to change the notification settings */
  scope: TdNotificationSettingsScope | null = null;
  /** notification_settings: scopeNotificationSettings — The new notification settings for the given scope */
  notification_settings: ScopeNotificationSettings | null = null;
}

/** Decode a TDLib `setScopeNotificationSettings` object; null input -> null. */
export function decodeSetScopeNotificationSettings(json: TdJsonRaw): SetScopeNotificationSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetScopeNotificationSettings = new SetScopeNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scope = decodeTdNotificationSettingsScope(record['scope']);
  out.notification_settings = decodeScopeNotificationSettings(record['notification_settings']);
  return out;
}

/** Encode a `setScopeNotificationSettings` into TDLib JSON wire shape. */
export function encodeSetScopeNotificationSettings(value: SetScopeNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setScopeNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scope'] = value.scope === null ? null : encodeTdNotificationSettingsScope(value.scope);
  out['notification_settings'] = value.notification_settings === null ? null : encodeScopeNotificationSettings(value.notification_settings);
  return out;
}

/** setStickerEmojis = Ok — Changes the list of emojis corresponding to a sticker. The sticker must belong to a regular or custom emoji sticker set that is owned by the current user */
export class SetStickerEmojis {
  type: 'setStickerEmojis' = 'setStickerEmojis';
  extra?: string;
  /** sticker: InputFile — Sticker */
  sticker: TdInputFile | null = null;
  /** emojis: string — New string with 1-20 emoji corresponding to the sticker */
  emojis: string = '';
}

/** Decode a TDLib `setStickerEmojis` object; null input -> null. */
export function decodeSetStickerEmojis(json: TdJsonRaw): SetStickerEmojis | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetStickerEmojis = new SetStickerEmojis();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  out.emojis = asString(record['emojis']);
  return out;
}

/** Encode a `setStickerEmojis` into TDLib JSON wire shape. */
export function encodeSetStickerEmojis(value: SetStickerEmojis): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setStickerEmojis';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  out['emojis'] = value.emojis;
  return out;
}

/** setStickerKeywords = Ok — Changes the list of keywords of a sticker. The sticker must belong to a regular or custom emoji sticker set that is owned by the current user */
export class SetStickerKeywords {
  type: 'setStickerKeywords' = 'setStickerKeywords';
  extra?: string;
  /** sticker: InputFile — Sticker */
  sticker: TdInputFile | null = null;
  /** keywords: vector<string> — List of up to 20 keywords with total length up to 64 characters, which can be used to find the sticker */
  keywords: string[] = [];
}

/** Decode a TDLib `setStickerKeywords` object; null input -> null. */
export function decodeSetStickerKeywords(json: TdJsonRaw): SetStickerKeywords | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetStickerKeywords = new SetStickerKeywords();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  out.keywords = decodeVector(record['keywords'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `setStickerKeywords` into TDLib JSON wire shape. */
export function encodeSetStickerKeywords(value: SetStickerKeywords): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setStickerKeywords';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  out['keywords'] = encodeVector(value.keywords, (item: string): TdJsonRaw => item);
  return out;
}

/** setStickerMaskPosition = Ok — Changes the mask position of a mask sticker. The sticker must belong to a mask sticker set that is owned by the current user */
export class SetStickerMaskPosition {
  type: 'setStickerMaskPosition' = 'setStickerMaskPosition';
  extra?: string;
  /** sticker: InputFile — Sticker */
  sticker: TdInputFile | null = null;
  /** mask_position: maskPosition — Position where the mask is placed; pass null to remove mask position */
  mask_position: MaskPosition | null = null;
}

/** Decode a TDLib `setStickerMaskPosition` object; null input -> null. */
export function decodeSetStickerMaskPosition(json: TdJsonRaw): SetStickerMaskPosition | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetStickerMaskPosition = new SetStickerMaskPosition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  out.mask_position = decodeMaskPosition(record['mask_position']);
  return out;
}

/** Encode a `setStickerMaskPosition` into TDLib JSON wire shape. */
export function encodeSetStickerMaskPosition(value: SetStickerMaskPosition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setStickerMaskPosition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  out['mask_position'] = value.mask_position === null ? null : encodeMaskPosition(value.mask_position);
  return out;
}

/** setStickerPositionInSet = Ok — Changes the position of a sticker in the set to which it belongs. The sticker set must be owned by the current user */
export class SetStickerPositionInSet {
  type: 'setStickerPositionInSet' = 'setStickerPositionInSet';
  extra?: string;
  /** sticker: InputFile — Sticker */
  sticker: TdInputFile | null = null;
  /** position: int32 — New position of the sticker in the set, 0-based */
  position: number = 0;
}

/** Decode a TDLib `setStickerPositionInSet` object; null input -> null. */
export function decodeSetStickerPositionInSet(json: TdJsonRaw): SetStickerPositionInSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetStickerPositionInSet = new SetStickerPositionInSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  out.position = asNumber(record['position']);
  return out;
}

/** Encode a `setStickerPositionInSet` into TDLib JSON wire shape. */
export function encodeSetStickerPositionInSet(value: SetStickerPositionInSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setStickerPositionInSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  out['position'] = value.position;
  return out;
}

/** setStickerSetThumbnail = Ok — Sets a sticker set thumbnail */
export class SetStickerSetThumbnail {
  type: 'setStickerSetThumbnail' = 'setStickerSetThumbnail';
  extra?: string;
  /** user_id: int53 — Sticker set owner; ignored for regular users */
  user_id: number = 0;
  /** name: string — Sticker set name. The sticker set must be owned by the current user */
  name: string = '';
  /** thumbnail: InputFile — Thumbnail to set; pass null to remove the sticker set thumbnail */
  thumbnail: TdInputFile | null = null;
  /** format: StickerFormat — Format of the thumbnail; pass null if thumbnail is removed */
  format: TdStickerFormat | null = null;
}

/** Decode a TDLib `setStickerSetThumbnail` object; null input -> null. */
export function decodeSetStickerSetThumbnail(json: TdJsonRaw): SetStickerSetThumbnail | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetStickerSetThumbnail = new SetStickerSetThumbnail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.name = asString(record['name']);
  out.thumbnail = decodeTdInputFile(record['thumbnail']);
  out.format = decodeTdStickerFormat(record['format']);
  return out;
}

/** Encode a `setStickerSetThumbnail` into TDLib JSON wire shape. */
export function encodeSetStickerSetThumbnail(value: SetStickerSetThumbnail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setStickerSetThumbnail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['name'] = value.name;
  out['thumbnail'] = value.thumbnail === null ? null : encodeTdInputFile(value.thumbnail);
  out['format'] = value.format === null ? null : encodeTdStickerFormat(value.format);
  return out;
}

/** setStickerSetTitle = Ok — Sets a sticker set title */
export class SetStickerSetTitle {
  type: 'setStickerSetTitle' = 'setStickerSetTitle';
  extra?: string;
  /** name: string — Sticker set name. The sticker set must be owned by the current user */
  name: string = '';
  /** title: string — New sticker set title */
  title: string = '';
}

/** Decode a TDLib `setStickerSetTitle` object; null input -> null. */
export function decodeSetStickerSetTitle(json: TdJsonRaw): SetStickerSetTitle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetStickerSetTitle = new SetStickerSetTitle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.title = asString(record['title']);
  return out;
}

/** Encode a `setStickerSetTitle` into TDLib JSON wire shape. */
export function encodeSetStickerSetTitle(value: SetStickerSetTitle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setStickerSetTitle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['title'] = value.title;
  return out;
}

/** setStoryAlbumName = StoryAlbum — Changes name of an album of stories. If the album is owned by a supergroup or a channel chat, then requires can_edit_stories administrator right in the chat. Returns the changed album */
export class SetStoryAlbumName {
  type: 'setStoryAlbumName' = 'setStoryAlbumName';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that owns the stories */
  chat_id: number = 0;
  /** story_album_id: int32 — Identifier of the story album */
  story_album_id: number = 0;
  /** name: string — New name of the album; 1-12 characters */
  name: string = '';
}

/** Decode a TDLib `setStoryAlbumName` object; null input -> null. */
export function decodeSetStoryAlbumName(json: TdJsonRaw): SetStoryAlbumName | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetStoryAlbumName = new SetStoryAlbumName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_album_id = asNumber(record['story_album_id']);
  out.name = asString(record['name']);
  return out;
}

/** Encode a `setStoryAlbumName` into TDLib JSON wire shape. */
export function encodeSetStoryAlbumName(value: SetStoryAlbumName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setStoryAlbumName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_album_id'] = value.story_album_id;
  out['name'] = value.name;
  return out;
}

/** setStoryPrivacySettings = Ok — Changes privacy settings of a story. The method can be called only for stories posted on behalf of the current user and if story.can_set_privacy_settings == true */
export class SetStoryPrivacySettings {
  type: 'setStoryPrivacySettings' = 'setStoryPrivacySettings';
  extra?: string;
  /** story_id: int32 — Identifier of the story */
  story_id: number = 0;
  /** privacy_settings: StoryPrivacySettings — The new privacy settings for the story */
  privacy_settings: TdStoryPrivacySettings | null = null;
}

/** Decode a TDLib `setStoryPrivacySettings` object; null input -> null. */
export function decodeSetStoryPrivacySettings(json: TdJsonRaw): SetStoryPrivacySettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetStoryPrivacySettings = new SetStoryPrivacySettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_id = asNumber(record['story_id']);
  out.privacy_settings = decodeTdStoryPrivacySettings(record['privacy_settings']);
  return out;
}

/** Encode a `setStoryPrivacySettings` into TDLib JSON wire shape. */
export function encodeSetStoryPrivacySettings(value: SetStoryPrivacySettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setStoryPrivacySettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_id'] = value.story_id;
  out['privacy_settings'] = value.privacy_settings === null ? null : encodeTdStoryPrivacySettings(value.privacy_settings);
  return out;
}

/** setStoryReaction = Ok — Changes chosen reaction on a story that has already been sent; not supported for live stories */
export class SetStoryReaction {
  type: 'setStoryReaction' = 'setStoryReaction';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the poster of the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — The identifier of the story */
  story_id: number = 0;
  /** reaction_type: ReactionType — Type of the reaction to set; pass null to remove the reaction. Custom emoji reactions can be used only by Telegram Premium users. Paid reactions can't be set */
  reaction_type: TdReactionType | null = null;
  /** update_recent_reactions: Bool — Pass true if the reaction needs to be added to recent reactions */
  update_recent_reactions: boolean = false;
}

/** Decode a TDLib `setStoryReaction` object; null input -> null. */
export function decodeSetStoryReaction(json: TdJsonRaw): SetStoryReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetStoryReaction = new SetStoryReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.reaction_type = decodeTdReactionType(record['reaction_type']);
  out.update_recent_reactions = asBool(record['update_recent_reactions']);
  return out;
}

/** Encode a `setStoryReaction` into TDLib JSON wire shape. */
export function encodeSetStoryReaction(value: SetStoryReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setStoryReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['reaction_type'] = value.reaction_type === null ? null : encodeTdReactionType(value.reaction_type);
  out['update_recent_reactions'] = value.update_recent_reactions;
  return out;
}

/** setSupergroupCustomEmojiStickerSet = Ok — Changes the custom emoji sticker set of a supergroup; requires can_change_info administrator right. The chat must have at least chatBoostFeatures.min_custom_emoji_sticker_set_boost_level boost le... */
export class SetSupergroupCustomEmojiStickerSet {
  type: 'setSupergroupCustomEmojiStickerSet' = 'setSupergroupCustomEmojiStickerSet';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup */
  supergroup_id: number = 0;
  /** custom_emoji_sticker_set_id: int64 — New value of the custom emoji sticker set identifier for the supergroup. Use 0 to remove the custom emoji sticker set in the supergroup */
  custom_emoji_sticker_set_id: string = '';
}

/** Decode a TDLib `setSupergroupCustomEmojiStickerSet` object; null input -> null. */
export function decodeSetSupergroupCustomEmojiStickerSet(json: TdJsonRaw): SetSupergroupCustomEmojiStickerSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetSupergroupCustomEmojiStickerSet = new SetSupergroupCustomEmojiStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.custom_emoji_sticker_set_id = asInt64(record['custom_emoji_sticker_set_id']);
  return out;
}

/** Encode a `setSupergroupCustomEmojiStickerSet` into TDLib JSON wire shape. */
export function encodeSetSupergroupCustomEmojiStickerSet(value: SetSupergroupCustomEmojiStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setSupergroupCustomEmojiStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['custom_emoji_sticker_set_id'] = value.custom_emoji_sticker_set_id;
  return out;
}

/** setSupergroupMainProfileTab = Ok — Changes the main profile tab of the channel; requires can_change_info administrator right */
export class SetSupergroupMainProfileTab {
  type: 'setSupergroupMainProfileTab' = 'setSupergroupMainProfileTab';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the channel */
  supergroup_id: number = 0;
  /** main_profile_tab: ProfileTab — The new value of the main profile tab */
  main_profile_tab: TdProfileTab | null = null;
}

/** Decode a TDLib `setSupergroupMainProfileTab` object; null input -> null. */
export function decodeSetSupergroupMainProfileTab(json: TdJsonRaw): SetSupergroupMainProfileTab | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetSupergroupMainProfileTab = new SetSupergroupMainProfileTab();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.main_profile_tab = decodeTdProfileTab(record['main_profile_tab']);
  return out;
}

/** Encode a `setSupergroupMainProfileTab` into TDLib JSON wire shape. */
export function encodeSetSupergroupMainProfileTab(value: SetSupergroupMainProfileTab): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setSupergroupMainProfileTab';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['main_profile_tab'] = value.main_profile_tab === null ? null : encodeTdProfileTab(value.main_profile_tab);
  return out;
}

/** setSupergroupStickerSet = Ok — Changes the sticker set of a supergroup; requires can_change_info administrator right */
export class SetSupergroupStickerSet {
  type: 'setSupergroupStickerSet' = 'setSupergroupStickerSet';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup */
  supergroup_id: number = 0;
  /** sticker_set_id: int64 — New value of the supergroup sticker set identifier. Use 0 to remove the supergroup sticker set */
  sticker_set_id: string = '';
}

/** Decode a TDLib `setSupergroupStickerSet` object; null input -> null. */
export function decodeSetSupergroupStickerSet(json: TdJsonRaw): SetSupergroupStickerSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetSupergroupStickerSet = new SetSupergroupStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.sticker_set_id = asInt64(record['sticker_set_id']);
  return out;
}

/** Encode a `setSupergroupStickerSet` into TDLib JSON wire shape. */
export function encodeSetSupergroupStickerSet(value: SetSupergroupStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setSupergroupStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['sticker_set_id'] = value.sticker_set_id;
  return out;
}

/** setSupergroupUnrestrictBoostCount = Ok — Changes the number of times the supergroup must be boosted by a user to ignore slow mode and chat permission restrictions; requires can_restrict_members administrator right */
export class SetSupergroupUnrestrictBoostCount {
  type: 'setSupergroupUnrestrictBoostCount' = 'setSupergroupUnrestrictBoostCount';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup */
  supergroup_id: number = 0;
  /** unrestrict_boost_count: int32 — New value of the unrestrict_boost_count supergroup setting; 0-8. Use 0 to remove the setting */
  unrestrict_boost_count: number = 0;
}

/** Decode a TDLib `setSupergroupUnrestrictBoostCount` object; null input -> null. */
export function decodeSetSupergroupUnrestrictBoostCount(json: TdJsonRaw): SetSupergroupUnrestrictBoostCount | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetSupergroupUnrestrictBoostCount = new SetSupergroupUnrestrictBoostCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.unrestrict_boost_count = asNumber(record['unrestrict_boost_count']);
  return out;
}

/** Encode a `setSupergroupUnrestrictBoostCount` into TDLib JSON wire shape. */
export function encodeSetSupergroupUnrestrictBoostCount(value: SetSupergroupUnrestrictBoostCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setSupergroupUnrestrictBoostCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['unrestrict_boost_count'] = value.unrestrict_boost_count;
  return out;
}

/** setSupergroupUsername = Ok — Changes the editable username of a supergroup or channel, requires owner privileges in the supergroup or channel */
export class SetSupergroupUsername {
  type: 'setSupergroupUsername' = 'setSupergroupUsername';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup or channel */
  supergroup_id: number = 0;
  /** username: string — New value of the username. Use an empty string to remove the username. The username can't be completely removed if there is another active or disabled username */
  username: string = '';
}

/** Decode a TDLib `setSupergroupUsername` object; null input -> null. */
export function decodeSetSupergroupUsername(json: TdJsonRaw): SetSupergroupUsername | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetSupergroupUsername = new SetSupergroupUsername();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.username = asString(record['username']);
  return out;
}

/** Encode a `setSupergroupUsername` into TDLib JSON wire shape. */
export function encodeSetSupergroupUsername(value: SetSupergroupUsername): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setSupergroupUsername';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['username'] = value.username;
  return out;
}

/** setTdlibParameters = Ok — Sets the parameters for TDLib initialization. Works only when the current authorization state is authorizationStateWaitTdlibParameters */
export class SetTdlibParameters {
  type: 'setTdlibParameters' = 'setTdlibParameters';
  extra?: string;
  /** use_test_dc: Bool — Pass true to use Telegram test environment instead of the production environment */
  use_test_dc: boolean = false;
  /** database_directory: string — The path to the directory for the persistent database; if empty, the current working directory will be used */
  database_directory: string = '';
  /** files_directory: string — The path to the directory for storing files; if empty, database_directory will be used */
  files_directory: string = '';
  /** database_encryption_key: bytes — Encryption key for the database. If the encryption key is invalid, then an error with code 401 will be returned */
  database_encryption_key: string = '';
  /** use_file_database: Bool — Pass true to keep information about downloaded and uploaded files between application restarts */
  use_file_database: boolean = false;
  /** use_chat_info_database: Bool — Pass true to keep cache of users, basic groups, supergroups, channels and secret chats between restarts. Implies use_file_database */
  use_chat_info_database: boolean = false;
  /** use_message_database: Bool — Pass true to keep cache of chats and messages between restarts. Implies use_chat_info_database */
  use_message_database: boolean = false;
  /** use_secret_chats: Bool — Pass true to enable support for secret chats */
  use_secret_chats: boolean = false;
  /** api_id: int32 — Application identifier for Telegram API access, which can be obtained at https://my.telegram.org */
  api_id: number = 0;
  /** api_hash: string — Application identifier hash for Telegram API access, which can be obtained at https://my.telegram.org */
  api_hash: string = '';
  /** system_language_code: string — IETF language tag of the user's operating system language; must be non-empty */
  system_language_code: string = '';
  /** device_model: string — Model of the device the application is being run on; must be non-empty */
  device_model: string = '';
  /** system_version: string — Version of the operating system the application is being run on. If empty, the version is automatically detected by TDLib */
  system_version: string = '';
  /** application_version: string — Application version; must be non-empty */
  application_version: string = '';
}

/** Decode a TDLib `setTdlibParameters` object; null input -> null. */
export function decodeSetTdlibParameters(json: TdJsonRaw): SetTdlibParameters | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetTdlibParameters = new SetTdlibParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.use_test_dc = asBool(record['use_test_dc']);
  out.database_directory = asString(record['database_directory']);
  out.files_directory = asString(record['files_directory']);
  out.database_encryption_key = asString(record['database_encryption_key']);
  out.use_file_database = asBool(record['use_file_database']);
  out.use_chat_info_database = asBool(record['use_chat_info_database']);
  out.use_message_database = asBool(record['use_message_database']);
  out.use_secret_chats = asBool(record['use_secret_chats']);
  out.api_id = asNumber(record['api_id']);
  out.api_hash = asString(record['api_hash']);
  out.system_language_code = asString(record['system_language_code']);
  out.device_model = asString(record['device_model']);
  out.system_version = asString(record['system_version']);
  out.application_version = asString(record['application_version']);
  return out;
}

/** Encode a `setTdlibParameters` into TDLib JSON wire shape. */
export function encodeSetTdlibParameters(value: SetTdlibParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setTdlibParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['use_test_dc'] = value.use_test_dc;
  out['database_directory'] = value.database_directory;
  out['files_directory'] = value.files_directory;
  out['database_encryption_key'] = value.database_encryption_key;
  out['use_file_database'] = value.use_file_database;
  out['use_chat_info_database'] = value.use_chat_info_database;
  out['use_message_database'] = value.use_message_database;
  out['use_secret_chats'] = value.use_secret_chats;
  out['api_id'] = value.api_id;
  out['api_hash'] = value.api_hash;
  out['system_language_code'] = value.system_language_code;
  out['device_model'] = value.device_model;
  out['system_version'] = value.system_version;
  out['application_version'] = value.application_version;
  return out;
}

/** setUpgradedGiftColors = Ok — Changes color scheme for the current user based on an owned or a hosted upgraded gift; for Telegram Premium users only */
export class SetUpgradedGiftColors {
  type: 'setUpgradedGiftColors' = 'setUpgradedGiftColors';
  extra?: string;
  /** upgraded_gift_colors_id: int64 — Identifier of the upgradedGiftColors scheme to use */
  upgraded_gift_colors_id: string = '';
}

/** Decode a TDLib `setUpgradedGiftColors` object; null input -> null. */
export function decodeSetUpgradedGiftColors(json: TdJsonRaw): SetUpgradedGiftColors | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetUpgradedGiftColors = new SetUpgradedGiftColors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.upgraded_gift_colors_id = asInt64(record['upgraded_gift_colors_id']);
  return out;
}

/** Encode a `setUpgradedGiftColors` into TDLib JSON wire shape. */
export function encodeSetUpgradedGiftColors(value: SetUpgradedGiftColors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setUpgradedGiftColors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['upgraded_gift_colors_id'] = value.upgraded_gift_colors_id;
  return out;
}

/** setUserEmojiStatus = Ok — Changes the emoji status of a user; for bots only */
export class SetUserEmojiStatus {
  type: 'setUserEmojiStatus' = 'setUserEmojiStatus';
  extra?: string;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
  /** emoji_status: emojiStatus — New emoji status; pass null to switch to the default badge */
  emoji_status: EmojiStatus | null = null;
}

/** Decode a TDLib `setUserEmojiStatus` object; null input -> null. */
export function decodeSetUserEmojiStatus(json: TdJsonRaw): SetUserEmojiStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetUserEmojiStatus = new SetUserEmojiStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.emoji_status = decodeEmojiStatus(record['emoji_status']);
  return out;
}

/** Encode a `setUserEmojiStatus` into TDLib JSON wire shape. */
export function encodeSetUserEmojiStatus(value: SetUserEmojiStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setUserEmojiStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['emoji_status'] = value.emoji_status === null ? null : encodeEmojiStatus(value.emoji_status);
  return out;
}

/** setUserNote = Ok — Changes a note of a contact user */
export class SetUserNote {
  type: 'setUserNote' = 'setUserNote';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** note: formattedText — Note to set for the user; 0-getOption("user_note_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed */
  note: FormattedText | null = null;
}

/** Decode a TDLib `setUserNote` object; null input -> null. */
export function decodeSetUserNote(json: TdJsonRaw): SetUserNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetUserNote = new SetUserNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.note = decodeFormattedText(record['note']);
  return out;
}

/** Encode a `setUserNote` into TDLib JSON wire shape. */
export function encodeSetUserNote(value: SetUserNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setUserNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['note'] = value.note === null ? null : encodeFormattedText(value.note);
  return out;
}

/** setUserPersonalProfilePhoto = Ok — Changes a personal profile photo of a contact user */
export class SetUserPersonalProfilePhoto {
  type: 'setUserPersonalProfilePhoto' = 'setUserPersonalProfilePhoto';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** photo: InputChatPhoto — Profile photo to set; pass null to delete the photo; inputChatPhotoPrevious isn't supported in this function */
  photo: TdInputChatPhoto | null = null;
}

/** Decode a TDLib `setUserPersonalProfilePhoto` object; null input -> null. */
export function decodeSetUserPersonalProfilePhoto(json: TdJsonRaw): SetUserPersonalProfilePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetUserPersonalProfilePhoto = new SetUserPersonalProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.photo = decodeTdInputChatPhoto(record['photo']);
  return out;
}

/** Encode a `setUserPersonalProfilePhoto` into TDLib JSON wire shape. */
export function encodeSetUserPersonalProfilePhoto(value: SetUserPersonalProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setUserPersonalProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['photo'] = value.photo === null ? null : encodeTdInputChatPhoto(value.photo);
  return out;
}

/** setUserPrivacySettingRules = Ok — Changes user privacy settings */
export class SetUserPrivacySettingRules {
  type: 'setUserPrivacySettingRules' = 'setUserPrivacySettingRules';
  extra?: string;
  /** setting: UserPrivacySetting — The privacy setting */
  setting: TdUserPrivacySetting | null = null;
  /** rules: userPrivacySettingRules — The new privacy rules */
  rules: UserPrivacySettingRules | null = null;
}

/** Decode a TDLib `setUserPrivacySettingRules` object; null input -> null. */
export function decodeSetUserPrivacySettingRules(json: TdJsonRaw): SetUserPrivacySettingRules | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetUserPrivacySettingRules = new SetUserPrivacySettingRules();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.setting = decodeTdUserPrivacySetting(record['setting']);
  out.rules = decodeUserPrivacySettingRules(record['rules']);
  return out;
}

/** Encode a `setUserPrivacySettingRules` into TDLib JSON wire shape. */
export function encodeSetUserPrivacySettingRules(value: SetUserPrivacySettingRules): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setUserPrivacySettingRules';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['setting'] = value.setting === null ? null : encodeTdUserPrivacySetting(value.setting);
  out['rules'] = value.rules === null ? null : encodeUserPrivacySettingRules(value.rules);
  return out;
}

/** setUserSupportInfo = UserSupportInfo — Sets support information for the given user; for Telegram support only */
export class SetUserSupportInfo {
  type: 'setUserSupportInfo' = 'setUserSupportInfo';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** message: formattedText — New information message */
  message: FormattedText | null = null;
}

/** Decode a TDLib `setUserSupportInfo` object; null input -> null. */
export function decodeSetUserSupportInfo(json: TdJsonRaw): SetUserSupportInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetUserSupportInfo = new SetUserSupportInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.message = decodeFormattedText(record['message']);
  return out;
}

/** Encode a `setUserSupportInfo` into TDLib JSON wire shape. */
export function encodeSetUserSupportInfo(value: SetUserSupportInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setUserSupportInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['message'] = value.message === null ? null : encodeFormattedText(value.message);
  return out;
}

/** setUsername = Ok — Changes the editable username of the current user */
export class SetUsername {
  type: 'setUsername' = 'setUsername';
  extra?: string;
  /** username: string — The new value of the username. Use an empty string to remove the username. The username can't be completely removed if there is another active or disabled username */
  username: string = '';
}

/** Decode a TDLib `setUsername` object; null input -> null. */
export function decodeSetUsername(json: TdJsonRaw): SetUsername | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetUsername = new SetUsername();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  return out;
}

/** Encode a `setUsername` into TDLib JSON wire shape. */
export function encodeSetUsername(value: SetUsername): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setUsername';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  return out;
}

/** setVideoChatDefaultParticipant = Ok — Changes default participant identifier, on whose behalf a video chat in the chat will be joined */
export class SetVideoChatDefaultParticipant {
  type: 'setVideoChatDefaultParticipant' = 'setVideoChatDefaultParticipant';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** default_participant_id: MessageSender — Default group call participant identifier to join the video chats in the chat */
  default_participant_id: TdMessageSender | null = null;
}

/** Decode a TDLib `setVideoChatDefaultParticipant` object; null input -> null. */
export function decodeSetVideoChatDefaultParticipant(json: TdJsonRaw): SetVideoChatDefaultParticipant | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetVideoChatDefaultParticipant = new SetVideoChatDefaultParticipant();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.default_participant_id = decodeTdMessageSender(record['default_participant_id']);
  return out;
}

/** Encode a `setVideoChatDefaultParticipant` into TDLib JSON wire shape. */
export function encodeSetVideoChatDefaultParticipant(value: SetVideoChatDefaultParticipant): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setVideoChatDefaultParticipant';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['default_participant_id'] = value.default_participant_id === null ? null : encodeTdMessageSender(value.default_participant_id);
  return out;
}

/** setVideoChatTitle = Ok — Sets title of a video chat; requires groupCall.can_be_managed right */
export class SetVideoChatTitle {
  type: 'setVideoChatTitle' = 'setVideoChatTitle';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** title: string — New group call title; 1-64 characters */
  title: string = '';
}

/** Decode a TDLib `setVideoChatTitle` object; null input -> null. */
export function decodeSetVideoChatTitle(json: TdJsonRaw): SetVideoChatTitle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SetVideoChatTitle = new SetVideoChatTitle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.title = asString(record['title']);
  return out;
}

/** Encode a `setVideoChatTitle` into TDLib JSON wire shape. */
export function encodeSetVideoChatTitle(value: SetVideoChatTitle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'setVideoChatTitle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['title'] = value.title;
  return out;
}

/** settingsSectionAppearance = SettingsSection — The appearance section "", "themes", "themes/edit", "themes/create", "wallpapers", "wallpapers/edit", "wallpapers/set", "wallpapers/choose-photo", "your-color/profile", "your-color/profile/ad... */
export class SettingsSectionAppearance {
  type: 'settingsSectionAppearance' = 'settingsSectionAppearance';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionAppearance` object; null input -> null. */
export function decodeSettingsSectionAppearance(json: TdJsonRaw): SettingsSectionAppearance | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionAppearance = new SettingsSectionAppearance();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionAppearance` into TDLib JSON wire shape. */
export function encodeSettingsSectionAppearance(value: SettingsSectionAppearance): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionAppearance';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionAskQuestion = SettingsSection — The "Ask a question" section */
export class SettingsSectionAskQuestion {
  type: 'settingsSectionAskQuestion' = 'settingsSectionAskQuestion';
  extra?: string;
}

/** Decode a TDLib `settingsSectionAskQuestion` object; null input -> null. */
export function decodeSettingsSectionAskQuestion(json: TdJsonRaw): SettingsSectionAskQuestion | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionAskQuestion = new SettingsSectionAskQuestion();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `settingsSectionAskQuestion` into TDLib JSON wire shape. */
export function encodeSettingsSectionAskQuestion(value: SettingsSectionAskQuestion): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionAskQuestion';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** settingsSectionBusiness = SettingsSection — The "Telegram Business" section "", "do-not-hide-ads" */
export class SettingsSectionBusiness {
  type: 'settingsSectionBusiness' = 'settingsSectionBusiness';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionBusiness` object; null input -> null. */
export function decodeSettingsSectionBusiness(json: TdJsonRaw): SettingsSectionBusiness | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionBusiness = new SettingsSectionBusiness();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionBusiness` into TDLib JSON wire shape. */
export function encodeSettingsSectionBusiness(value: SettingsSectionBusiness): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionBusiness';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionChatFolders = SettingsSection — The chat folder settings section "", "edit", "create", "add-recommended", "show-tags", "tab-view" */
export class SettingsSectionChatFolders {
  type: 'settingsSectionChatFolders' = 'settingsSectionChatFolders';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionChatFolders` object; null input -> null. */
export function decodeSettingsSectionChatFolders(json: TdJsonRaw): SettingsSectionChatFolders | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionChatFolders = new SettingsSectionChatFolders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionChatFolders` into TDLib JSON wire shape. */
export function encodeSettingsSectionChatFolders(value: SettingsSectionChatFolders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionChatFolders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionDataAndStorage = SettingsSection — The data and storage settings section "", "storage", "storage/edit", "storage/auto-remove", "storage/clear-cache", "storage/max-cache", "usage", "usage/mobile", "usage/wifi", "usage/reset... */
export class SettingsSectionDataAndStorage {
  type: 'settingsSectionDataAndStorage' = 'settingsSectionDataAndStorage';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionDataAndStorage` object; null input -> null. */
export function decodeSettingsSectionDataAndStorage(json: TdJsonRaw): SettingsSectionDataAndStorage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionDataAndStorage = new SettingsSectionDataAndStorage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionDataAndStorage` into TDLib JSON wire shape. */
export function encodeSettingsSectionDataAndStorage(value: SettingsSectionDataAndStorage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionDataAndStorage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionDevices = SettingsSection — The Devices section "", "edit", "link-desktop", "terminate-sessions", "auto-terminate" */
export class SettingsSectionDevices {
  type: 'settingsSectionDevices' = 'settingsSectionDevices';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionDevices` object; null input -> null. */
export function decodeSettingsSectionDevices(json: TdJsonRaw): SettingsSectionDevices | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionDevices = new SettingsSectionDevices();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionDevices` into TDLib JSON wire shape. */
export function encodeSettingsSectionDevices(value: SettingsSectionDevices): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionDevices';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionEditProfile = SettingsSection — The profile edit section "", "set-photo", "first-name", "last-name", "emoji-status", "bio", "birthday", "change-number", "username", "your-color", "channel", "add-account", "log-out", "profi... */
export class SettingsSectionEditProfile {
  type: 'settingsSectionEditProfile' = 'settingsSectionEditProfile';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionEditProfile` object; null input -> null. */
export function decodeSettingsSectionEditProfile(json: TdJsonRaw): SettingsSectionEditProfile | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionEditProfile = new SettingsSectionEditProfile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionEditProfile` into TDLib JSON wire shape. */
export function encodeSettingsSectionEditProfile(value: SettingsSectionEditProfile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionEditProfile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionFaq = SettingsSection — The FAQ section */
export class SettingsSectionFaq {
  type: 'settingsSectionFaq' = 'settingsSectionFaq';
  extra?: string;
}

/** Decode a TDLib `settingsSectionFaq` object; null input -> null. */
export function decodeSettingsSectionFaq(json: TdJsonRaw): SettingsSectionFaq | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionFaq = new SettingsSectionFaq();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `settingsSectionFaq` into TDLib JSON wire shape. */
export function encodeSettingsSectionFaq(value: SettingsSectionFaq): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionFaq';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** settingsSectionFeatures = SettingsSection — The "Telegram Features" section */
export class SettingsSectionFeatures {
  type: 'settingsSectionFeatures' = 'settingsSectionFeatures';
  extra?: string;
}

/** Decode a TDLib `settingsSectionFeatures` object; null input -> null. */
export function decodeSettingsSectionFeatures(json: TdJsonRaw): SettingsSectionFeatures | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionFeatures = new SettingsSectionFeatures();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `settingsSectionFeatures` into TDLib JSON wire shape. */
export function encodeSettingsSectionFeatures(value: SettingsSectionFeatures): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionFeatures';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** settingsSectionInAppBrowser = SettingsSection — The in-app browser settings section "", "enable-browser", "clear-cookies", "clear-cache", "history", "clear-history", "never-open", "clear-list", "search" */
export class SettingsSectionInAppBrowser {
  type: 'settingsSectionInAppBrowser' = 'settingsSectionInAppBrowser';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionInAppBrowser` object; null input -> null. */
export function decodeSettingsSectionInAppBrowser(json: TdJsonRaw): SettingsSectionInAppBrowser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionInAppBrowser = new SettingsSectionInAppBrowser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionInAppBrowser` into TDLib JSON wire shape. */
export function encodeSettingsSectionInAppBrowser(value: SettingsSectionInAppBrowser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionInAppBrowser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionLanguage = SettingsSection — The application language section "translate-chats" for Translate Entire Chats toggle, "do-not-translate" - for Do Not Translate language list */
export class SettingsSectionLanguage {
  type: 'settingsSectionLanguage' = 'settingsSectionLanguage';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of "", "show-button" for Show Translate Button toggle, */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionLanguage` object; null input -> null. */
export function decodeSettingsSectionLanguage(json: TdJsonRaw): SettingsSectionLanguage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionLanguage = new SettingsSectionLanguage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionLanguage` into TDLib JSON wire shape. */
export function encodeSettingsSectionLanguage(value: SettingsSectionLanguage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionLanguage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionMyGrams = SettingsSection — The TON Gram balance and transaction section */
export class SettingsSectionMyGrams {
  type: 'settingsSectionMyGrams' = 'settingsSectionMyGrams';
  extra?: string;
}

/** Decode a TDLib `settingsSectionMyGrams` object; null input -> null. */
export function decodeSettingsSectionMyGrams(json: TdJsonRaw): SettingsSectionMyGrams | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionMyGrams = new SettingsSectionMyGrams();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `settingsSectionMyGrams` into TDLib JSON wire shape. */
export function encodeSettingsSectionMyGrams(value: SettingsSectionMyGrams): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionMyGrams';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** settingsSectionMyStars = SettingsSection — The Telegram Star balance and transaction section "", "top-up", "stats", "gift", "earn" */
export class SettingsSectionMyStars {
  type: 'settingsSectionMyStars' = 'settingsSectionMyStars';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionMyStars` object; null input -> null. */
export function decodeSettingsSectionMyStars(json: TdJsonRaw): SettingsSectionMyStars | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionMyStars = new SettingsSectionMyStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionMyStars` into TDLib JSON wire shape. */
export function encodeSettingsSectionMyStars(value: SettingsSectionMyStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionMyStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionNotifications = SettingsSection — The notification settings section "", "accounts", "private-chats", "private-chats/edit", "private-chats/show", "private-chats/preview", "private-chats/sound", "private-chats/add-exception"... */
export class SettingsSectionNotifications {
  type: 'settingsSectionNotifications' = 'settingsSectionNotifications';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionNotifications` object; null input -> null. */
export function decodeSettingsSectionNotifications(json: TdJsonRaw): SettingsSectionNotifications | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionNotifications = new SettingsSectionNotifications();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionNotifications` into TDLib JSON wire shape. */
export function encodeSettingsSectionNotifications(value: SettingsSectionNotifications): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionNotifications';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionPowerSaving = SettingsSection — The power saving settings section "", "videos", "gifs", "stickers", "emoji", "effects", "preload", "background", "call-animations", "particles", "transitions" */
export class SettingsSectionPowerSaving {
  type: 'settingsSectionPowerSaving' = 'settingsSectionPowerSaving';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionPowerSaving` object; null input -> null. */
export function decodeSettingsSectionPowerSaving(json: TdJsonRaw): SettingsSectionPowerSaving | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionPowerSaving = new SettingsSectionPowerSaving();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionPowerSaving` into TDLib JSON wire shape. */
export function encodeSettingsSectionPowerSaving(value: SettingsSectionPowerSaving): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionPowerSaving';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionPremium = SettingsSection — The "Telegram Premium" section */
export class SettingsSectionPremium {
  type: 'settingsSectionPremium' = 'settingsSectionPremium';
  extra?: string;
}

/** Decode a TDLib `settingsSectionPremium` object; null input -> null. */
export function decodeSettingsSectionPremium(json: TdJsonRaw): SettingsSectionPremium | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionPremium = new SettingsSectionPremium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `settingsSectionPremium` into TDLib JSON wire shape. */
export function encodeSettingsSectionPremium(value: SettingsSectionPremium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionPremium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** settingsSectionPrivacyAndSecurity = SettingsSection — The privacy and security section "", "blocked", "blocked/edit", "blocked/block-user", "blocked/block-user/chats", "blocked/block-user/contacts", "active-websites", "active-websites/ed... */
export class SettingsSectionPrivacyAndSecurity {
  type: 'settingsSectionPrivacyAndSecurity' = 'settingsSectionPrivacyAndSecurity';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionPrivacyAndSecurity` object; null input -> null. */
export function decodeSettingsSectionPrivacyAndSecurity(json: TdJsonRaw): SettingsSectionPrivacyAndSecurity | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionPrivacyAndSecurity = new SettingsSectionPrivacyAndSecurity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionPrivacyAndSecurity` into TDLib JSON wire shape. */
export function encodeSettingsSectionPrivacyAndSecurity(value: SettingsSectionPrivacyAndSecurity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionPrivacyAndSecurity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionPrivacyPolicy = SettingsSection — The "Privacy Policy" section */
export class SettingsSectionPrivacyPolicy {
  type: 'settingsSectionPrivacyPolicy' = 'settingsSectionPrivacyPolicy';
  extra?: string;
}

/** Decode a TDLib `settingsSectionPrivacyPolicy` object; null input -> null. */
export function decodeSettingsSectionPrivacyPolicy(json: TdJsonRaw): SettingsSectionPrivacyPolicy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionPrivacyPolicy = new SettingsSectionPrivacyPolicy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `settingsSectionPrivacyPolicy` into TDLib JSON wire shape. */
export function encodeSettingsSectionPrivacyPolicy(value: SettingsSectionPrivacyPolicy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionPrivacyPolicy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** settingsSectionQrCode = SettingsSection — The current user's QR code section "", "share", "scan" */
export class SettingsSectionQrCode {
  type: 'settingsSectionQrCode' = 'settingsSectionQrCode';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionQrCode` object; null input -> null. */
export function decodeSettingsSectionQrCode(json: TdJsonRaw): SettingsSectionQrCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionQrCode = new SettingsSectionQrCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionQrCode` into TDLib JSON wire shape. */
export function encodeSettingsSectionQrCode(value: SettingsSectionQrCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionQrCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** settingsSectionSearch = SettingsSection — Search in Settings */
export class SettingsSectionSearch {
  type: 'settingsSectionSearch' = 'settingsSectionSearch';
  extra?: string;
}

/** Decode a TDLib `settingsSectionSearch` object; null input -> null. */
export function decodeSettingsSectionSearch(json: TdJsonRaw): SettingsSectionSearch | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionSearch = new SettingsSectionSearch();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `settingsSectionSearch` into TDLib JSON wire shape. */
export function encodeSettingsSectionSearch(value: SettingsSectionSearch): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionSearch';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** settingsSectionSendGift = SettingsSection — The "Send a gift" section "", "self" */
export class SettingsSectionSendGift {
  type: 'settingsSectionSendGift' = 'settingsSectionSendGift';
  extra?: string;
  /** subsection: string — Subsection of the section; may be one of */
  subsection: string = '';
}

/** Decode a TDLib `settingsSectionSendGift` object; null input -> null. */
export function decodeSettingsSectionSendGift(json: TdJsonRaw): SettingsSectionSendGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SettingsSectionSendGift = new SettingsSectionSendGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subsection = asString(record['subsection']);
  return out;
}

/** Encode a `settingsSectionSendGift` into TDLib JSON wire shape. */
export function encodeSettingsSectionSendGift(value: SettingsSectionSendGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'settingsSectionSendGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subsection'] = value.subsection;
  return out;
}

/** shareChatWithBot = Ok — Shares a chat after pressing a keyboardButtonTypeRequestChat button with the bot If the bot must be a member, then all chats from getGroupsInCommon and all chats, where the user can add the bot, are suitable. In t... */
export class ShareChatWithBot {
  type: 'shareChatWithBot' = 'shareChatWithBot';
  extra?: string;
  /** source: KeyboardButtonSource — Source of the button */
  source: TdKeyboardButtonSource | null = null;
  /** button_id: int32 — Identifier of the button */
  button_id: number = 0;
  /** shared_chat_id: int53 — Identifier of the shared chat */
  shared_chat_id: number = 0;
  /** only_check: Bool — Pass true to check that the chat can be shared by the button instead of actually sharing it. Doesn't check bot_is_member and bot_administrator_rights restrictions. */
  only_check: boolean = false;
}

/** Decode a TDLib `shareChatWithBot` object; null input -> null. */
export function decodeShareChatWithBot(json: TdJsonRaw): ShareChatWithBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ShareChatWithBot = new ShareChatWithBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.source = decodeTdKeyboardButtonSource(record['source']);
  out.button_id = asNumber(record['button_id']);
  out.shared_chat_id = asNumber(record['shared_chat_id']);
  out.only_check = asBool(record['only_check']);
  return out;
}

/** Encode a `shareChatWithBot` into TDLib JSON wire shape. */
export function encodeShareChatWithBot(value: ShareChatWithBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'shareChatWithBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['source'] = value.source === null ? null : encodeTdKeyboardButtonSource(value.source);
  out['button_id'] = value.button_id;
  out['shared_chat_id'] = value.shared_chat_id;
  out['only_check'] = value.only_check;
  return out;
}

/** sharePhoneNumber = Ok — Shares the phone number of the current user with a mutual contact. Supposed to be called when the user clicks on chatActionBarSharePhoneNumber */
export class SharePhoneNumber {
  type: 'sharePhoneNumber' = 'sharePhoneNumber';
  extra?: string;
  /** user_id: int53 — Identifier of the user with whom to share the phone number. The user must be a mutual contact */
  user_id: number = 0;
}

/** Decode a TDLib `sharePhoneNumber` object; null input -> null. */
export function decodeSharePhoneNumber(json: TdJsonRaw): SharePhoneNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SharePhoneNumber = new SharePhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `sharePhoneNumber` into TDLib JSON wire shape. */
export function encodeSharePhoneNumber(value: SharePhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sharePhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** shareUsersWithBot = Ok — Shares users after pressing a keyboardButtonTypeRequestUsers button with the bot */
export class ShareUsersWithBot {
  type: 'shareUsersWithBot' = 'shareUsersWithBot';
  extra?: string;
  /** source: KeyboardButtonSource — Source of the button */
  source: TdKeyboardButtonSource | null = null;
  /** button_id: int32 — Identifier of the button */
  button_id: number = 0;
  /** shared_user_ids: vector<int53> — Identifiers of the shared users */
  shared_user_ids: number[] = [];
  /** only_check: Bool — Pass true to check that the users can be shared by the button instead of actually sharing them */
  only_check: boolean = false;
}

/** Decode a TDLib `shareUsersWithBot` object; null input -> null. */
export function decodeShareUsersWithBot(json: TdJsonRaw): ShareUsersWithBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ShareUsersWithBot = new ShareUsersWithBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.source = decodeTdKeyboardButtonSource(record['source']);
  out.button_id = asNumber(record['button_id']);
  out.shared_user_ids = decodeVector(record['shared_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.only_check = asBool(record['only_check']);
  return out;
}

/** Encode a `shareUsersWithBot` into TDLib JSON wire shape. */
export function encodeShareUsersWithBot(value: ShareUsersWithBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'shareUsersWithBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['source'] = value.source === null ? null : encodeTdKeyboardButtonSource(value.source);
  out['button_id'] = value.button_id;
  out['shared_user_ids'] = encodeVector(value.shared_user_ids, (item: number): TdJsonRaw => item);
  out['only_check'] = value.only_check;
  return out;
}

/** sharedChat = SharedChat — Contains information about a chat shared with a bot */
export class SharedChat {
  type: 'sharedChat' = 'sharedChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** title: string — Title of the chat; for bots only */
  title: string = '';
  /** username: string — Username of the chat; for bots only */
  username: string = '';
  /** photo: photo — Photo of the chat; for bots only; may be null */
  photo: Photo | null = null;
}

/** Decode a TDLib `sharedChat` object; null input -> null. */
export function decodeSharedChat(json: TdJsonRaw): SharedChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SharedChat = new SharedChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.title = asString(record['title']);
  out.username = asString(record['username']);
  out.photo = decodePhoto(record['photo']);
  return out;
}

/** Encode a `sharedChat` into TDLib JSON wire shape. */
export function encodeSharedChat(value: SharedChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sharedChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['title'] = value.title;
  out['username'] = value.username;
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  return out;
}

/** sharedUser = SharedUser — Contains information about a user shared with a bot */
export class SharedUser {
  type: 'sharedUser' = 'sharedUser';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** first_name: string — First name of the user; for bots only */
  first_name: string = '';
  /** last_name: string — Last name of the user; for bots only */
  last_name: string = '';
  /** username: string — Username of the user; for bots only */
  username: string = '';
  /** photo: photo — Profile photo of the user; for bots only; may be null */
  photo: Photo | null = null;
}

/** Decode a TDLib `sharedUser` object; null input -> null. */
export function decodeSharedUser(json: TdJsonRaw): SharedUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SharedUser = new SharedUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.first_name = asString(record['first_name']);
  out.last_name = asString(record['last_name']);
  out.username = asString(record['username']);
  out.photo = decodePhoto(record['photo']);
  return out;
}

/** Encode a `sharedUser` into TDLib JSON wire shape. */
export function encodeSharedUser(value: SharedUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sharedUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['first_name'] = value.first_name;
  out['last_name'] = value.last_name;
  out['username'] = value.username;
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  return out;
}

/** shippingOption = ShippingOption — One shipping option */
export class ShippingOption {
  type: 'shippingOption' = 'shippingOption';
  extra?: string;
  /** id: string — Shipping option identifier */
  id: string = '';
  /** title: string — Option title */
  title: string = '';
  /** price_parts: vector<labeledPricePart> — A list of objects used to calculate the total shipping costs */
  price_parts: LabeledPricePart[] = [];
}

/** Decode a TDLib `shippingOption` object; null input -> null. */
export function decodeShippingOption(json: TdJsonRaw): ShippingOption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ShippingOption = new ShippingOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.title = asString(record['title']);
  out.price_parts = decodeVector(record['price_parts'], (item: TdJsonRaw): LabeledPricePart => decodeLabeledPricePart(item) as LabeledPricePart);
  return out;
}

/** Encode a `shippingOption` into TDLib JSON wire shape. */
export function encodeShippingOption(value: ShippingOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'shippingOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  out['price_parts'] = encodeVector(value.price_parts, (item: LabeledPricePart | null): TdJsonRaw => item === null ? null : encodeLabeledPricePart(item));
  return out;
}

/** speechRecognitionResultError = SpeechRecognitionResult — The speech recognition failed */
export class SpeechRecognitionResultError {
  type: 'speechRecognitionResultError' = 'speechRecognitionResultError';
  extra?: string;
  /** error: error — Recognition error. An error with a message "MSG_VOICE_TOO_LONG" is returned when media duration is too big to be recognized */
  error: ErrorValue | null = null;
}

/** Decode a TDLib `speechRecognitionResultError` object; null input -> null. */
export function decodeSpeechRecognitionResultError(json: TdJsonRaw): SpeechRecognitionResultError | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SpeechRecognitionResultError = new SpeechRecognitionResultError();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.error = decodeErrorValue(record['error']);
  return out;
}

/** Encode a `speechRecognitionResultError` into TDLib JSON wire shape. */
export function encodeSpeechRecognitionResultError(value: SpeechRecognitionResultError): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'speechRecognitionResultError';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['error'] = value.error === null ? null : encodeErrorValue(value.error);
  return out;
}

/** speechRecognitionResultPending = SpeechRecognitionResult — The speech recognition is ongoing */
export class SpeechRecognitionResultPending {
  type: 'speechRecognitionResultPending' = 'speechRecognitionResultPending';
  extra?: string;
  /** partial_text: string — Partially recognized text */
  partial_text: string = '';
}

/** Decode a TDLib `speechRecognitionResultPending` object; null input -> null. */
export function decodeSpeechRecognitionResultPending(json: TdJsonRaw): SpeechRecognitionResultPending | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SpeechRecognitionResultPending = new SpeechRecognitionResultPending();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.partial_text = asString(record['partial_text']);
  return out;
}

/** Encode a `speechRecognitionResultPending` into TDLib JSON wire shape. */
export function encodeSpeechRecognitionResultPending(value: SpeechRecognitionResultPending): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'speechRecognitionResultPending';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['partial_text'] = value.partial_text;
  return out;
}

/** speechRecognitionResultText = SpeechRecognitionResult — The speech recognition successfully finished */
export class SpeechRecognitionResultText {
  type: 'speechRecognitionResultText' = 'speechRecognitionResultText';
  extra?: string;
  /** text: string — Recognized text */
  text: string = '';
}

/** Decode a TDLib `speechRecognitionResultText` object; null input -> null. */
export function decodeSpeechRecognitionResultText(json: TdJsonRaw): SpeechRecognitionResultText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SpeechRecognitionResultText = new SpeechRecognitionResultText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  return out;
}

/** Encode a `speechRecognitionResultText` into TDLib JSON wire shape. */
export function encodeSpeechRecognitionResultText(value: SpeechRecognitionResultText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'speechRecognitionResultText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  return out;
}

/** sponsoredChat = SponsoredChat — Describes a sponsored chat */
export class SponsoredChat {
  type: 'sponsoredChat' = 'sponsoredChat';
  extra?: string;
  /** unique_id: int53 — Unique identifier of this result */
  unique_id: number = 0;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** sponsor_info: string — Additional optional information about the sponsor to be shown along with the chat */
  sponsor_info: string = '';
  /** additional_info: string — If non-empty, additional information about the sponsored chat to be shown along with the chat */
  additional_info: string = '';
}

/** Decode a TDLib `sponsoredChat` object; null input -> null. */
export function decodeSponsoredChat(json: TdJsonRaw): SponsoredChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SponsoredChat = new SponsoredChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.unique_id = asNumber(record['unique_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.sponsor_info = asString(record['sponsor_info']);
  out.additional_info = asString(record['additional_info']);
  return out;
}

/** Encode a `sponsoredChat` into TDLib JSON wire shape. */
export function encodeSponsoredChat(value: SponsoredChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sponsoredChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['unique_id'] = value.unique_id;
  out['chat_id'] = value.chat_id;
  out['sponsor_info'] = value.sponsor_info;
  out['additional_info'] = value.additional_info;
  return out;
}

/** sponsoredChats = SponsoredChats — Contains a list of sponsored chats */
export class SponsoredChats {
  type: 'sponsoredChats' = 'sponsoredChats';
  extra?: string;
  /** chats: vector<sponsoredChat> — List of sponsored chats */
  chats: SponsoredChat[] = [];
}

/** Decode a TDLib `sponsoredChats` object; null input -> null. */
export function decodeSponsoredChats(json: TdJsonRaw): SponsoredChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SponsoredChats = new SponsoredChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chats = decodeVector(record['chats'], (item: TdJsonRaw): SponsoredChat => decodeSponsoredChat(item) as SponsoredChat);
  return out;
}

/** Encode a `sponsoredChats` into TDLib JSON wire shape. */
export function encodeSponsoredChats(value: SponsoredChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sponsoredChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chats'] = encodeVector(value.chats, (item: SponsoredChat | null): TdJsonRaw => item === null ? null : encodeSponsoredChat(item));
  return out;
}

/** sponsoredMessage = SponsoredMessage — Describes a sponsored message The content must be fully downloaded before the message is shown */
export class SponsoredMessage {
  type: 'sponsoredMessage' = 'sponsoredMessage';
  extra?: string;
  /** message_id: int53 — Message identifier; unique for the chat to which the sponsored message belongs among both ordinary and sponsored messages */
  message_id: number = 0;
  /** is_recommended: Bool — True, if the message needs to be labeled as "recommended" instead of "sponsored" */
  is_recommended: boolean = false;
  /** can_be_reported: Bool — True, if the message can be reported to Telegram moderators through reportChatSponsoredMessage */
  can_be_reported: boolean = false;
  /** content: MessageContent — Content of the message. Currently, can be only of the types messageText, messageAnimation, messagePhoto, or messageVideo. Video messages can be viewed fullscreen. */
  content: TdMessageContent | null = null;
  /** sponsor: advertisementSponsor — Information about the sponsor of the message */
  sponsor: AdvertisementSponsor | null = null;
  /** title: string — Title of the sponsored message */
  title: string = '';
  /** button_text: string — Text for the message action button */
  button_text: string = '';
  /** accent_color_id: int32 — Identifier of the accent color for title, button text and message background */
  accent_color_id: number = 0;
  /** background_custom_emoji_id: int64 — Identifier of a custom emoji to be shown on the message background; 0 if none */
  background_custom_emoji_id: string = '';
  /** additional_info: string — If non-empty, additional information about the sponsored message to be shown along with the message */
  additional_info: string = '';
}

/** Decode a TDLib `sponsoredMessage` object; null input -> null. */
export function decodeSponsoredMessage(json: TdJsonRaw): SponsoredMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SponsoredMessage = new SponsoredMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_id = asNumber(record['message_id']);
  out.is_recommended = asBool(record['is_recommended']);
  out.can_be_reported = asBool(record['can_be_reported']);
  out.content = decodeTdMessageContent(record['content']);
  out.sponsor = decodeAdvertisementSponsor(record['sponsor']);
  out.title = asString(record['title']);
  out.button_text = asString(record['button_text']);
  out.accent_color_id = asNumber(record['accent_color_id']);
  out.background_custom_emoji_id = asInt64(record['background_custom_emoji_id']);
  out.additional_info = asString(record['additional_info']);
  return out;
}

/** Encode a `sponsoredMessage` into TDLib JSON wire shape. */
export function encodeSponsoredMessage(value: SponsoredMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sponsoredMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_id'] = value.message_id;
  out['is_recommended'] = value.is_recommended;
  out['can_be_reported'] = value.can_be_reported;
  out['content'] = value.content === null ? null : encodeTdMessageContent(value.content);
  out['sponsor'] = value.sponsor === null ? null : encodeAdvertisementSponsor(value.sponsor);
  out['title'] = value.title;
  out['button_text'] = value.button_text;
  out['accent_color_id'] = value.accent_color_id;
  out['background_custom_emoji_id'] = value.background_custom_emoji_id;
  out['additional_info'] = value.additional_info;
  return out;
}

/** sponsoredMessages = SponsoredMessages — Contains a list of sponsored messages */
export class SponsoredMessages {
  type: 'sponsoredMessages' = 'sponsoredMessages';
  extra?: string;
  /** messages: vector<sponsoredMessage> — List of sponsored messages */
  messages: SponsoredMessage[] = [];
  /** messages_between: int32 — The minimum number of messages between shown sponsored messages, or 0 if only one sponsored message must be shown after all ordinary messages */
  messages_between: number = 0;
}

/** Decode a TDLib `sponsoredMessages` object; null input -> null. */
export function decodeSponsoredMessages(json: TdJsonRaw): SponsoredMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SponsoredMessages = new SponsoredMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): SponsoredMessage => decodeSponsoredMessage(item) as SponsoredMessage);
  out.messages_between = asNumber(record['messages_between']);
  return out;
}

/** Encode a `sponsoredMessages` into TDLib JSON wire shape. */
export function encodeSponsoredMessages(value: SponsoredMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sponsoredMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['messages'] = encodeVector(value.messages, (item: SponsoredMessage | null): TdJsonRaw => item === null ? null : encodeSponsoredMessage(item));
  out['messages_between'] = value.messages_between;
  return out;
}

/** stakeDiceState = StakeDiceState — Describes state of the stake dice */
export class StakeDiceState {
  type: 'stakeDiceState' = 'stakeDiceState';
  extra?: string;
  /** state_hash: string — Hash of the state to use for sending the next dice; may be empty if the stake dice can't be sent by the current user */
  state_hash: string = '';
  /** stake_gram_amount: int53 — The amount of TON Grams staked in the previous roll; in the smallest units of the currency */
  stake_gram_amount: number = 0;
  /** suggested_stake_gram_amounts: vector<int53> — The amounts of Grams that are suggested to be staked; in the smallest units of the currency */
  suggested_stake_gram_amounts: number[] = [];
  /** current_streak: int32 — The number of rolled sixes towards the streak; 0-2 */
  current_streak: number = 0;
  /** prize_per_mille: vector<int32> — The number of Grams received by the user for each 1000 Grams staked if the dice outcome is 1-6 correspondingly; may be empty if the stake dice can't be sent by the current user */
  prize_per_mille: number[] = [];
  /** streak_prize_per_mille: int32 — The number of Grams received by the user for each 1000 Grams staked if the dice outcome is 6 three times in a row with the same stake */
  streak_prize_per_mille: number = 0;
}

/** Decode a TDLib `stakeDiceState` object; null input -> null. */
export function decodeStakeDiceState(json: TdJsonRaw): StakeDiceState | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StakeDiceState = new StakeDiceState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.state_hash = asString(record['state_hash']);
  out.stake_gram_amount = asNumber(record['stake_gram_amount']);
  out.suggested_stake_gram_amounts = decodeVector(record['suggested_stake_gram_amounts'], (item: TdJsonRaw): number => asNumber(item));
  out.current_streak = asNumber(record['current_streak']);
  out.prize_per_mille = decodeVector(record['prize_per_mille'], (item: TdJsonRaw): number => asNumber(item));
  out.streak_prize_per_mille = asNumber(record['streak_prize_per_mille']);
  return out;
}

/** Encode a `stakeDiceState` into TDLib JSON wire shape. */
export function encodeStakeDiceState(value: StakeDiceState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stakeDiceState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['state_hash'] = value.state_hash;
  out['stake_gram_amount'] = value.stake_gram_amount;
  out['suggested_stake_gram_amounts'] = encodeVector(value.suggested_stake_gram_amounts, (item: number): TdJsonRaw => item);
  out['current_streak'] = value.current_streak;
  out['prize_per_mille'] = encodeVector(value.prize_per_mille, (item: number): TdJsonRaw => item);
  out['streak_prize_per_mille'] = value.streak_prize_per_mille;
  return out;
}

/** starAmount = StarAmount — Describes a possibly non-integer Telegram Star amount */
export class StarAmount {
  type: 'starAmount' = 'starAmount';
  extra?: string;
  /** star_count: int53 — The integer Telegram Star amount rounded to 0 */
  star_count: number = 0;
  /** nanostar_count: int32 — The number of 1/1000000000 shares of Telegram Stars; from -999999999 to 999999999 */
  nanostar_count: number = 0;
}

/** Decode a TDLib `starAmount` object; null input -> null. */
export function decodeStarAmount(json: TdJsonRaw): StarAmount | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarAmount = new StarAmount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.nanostar_count = asNumber(record['nanostar_count']);
  return out;
}

/** Encode a `starAmount` into TDLib JSON wire shape. */
export function encodeStarAmount(value: StarAmount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starAmount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['nanostar_count'] = value.nanostar_count;
  return out;
}

/** starCount = StarCount — Contains a number of Telegram Stars */
export class StarCount {
  type: 'starCount' = 'starCount';
  extra?: string;
  /** star_count: int53 — Number of Telegram Stars */
  star_count: number = 0;
}

/** Decode a TDLib `starCount` object; null input -> null. */
export function decodeStarCount(json: TdJsonRaw): StarCount | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarCount = new StarCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `starCount` into TDLib JSON wire shape. */
export function encodeStarCount(value: StarCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  return out;
}

/** starGiveawayPaymentOption = StarGiveawayPaymentOption — Describes an option for creating of Telegram Star giveaway. Use telegramPaymentPurposeStarGiveaway for out-of-store payments */
export class StarGiveawayPaymentOption {
  type: 'starGiveawayPaymentOption' = 'starGiveawayPaymentOption';
  extra?: string;
  /** currency: string — ISO 4217 currency code for the payment */
  currency: string = '';
  /** amount: int53 — The amount to pay, in the smallest units of the currency */
  amount: number = 0;
  /** star_count: int53 — Number of Telegram Stars that will be distributed among winners */
  star_count: number = 0;
  /** store_product_id: string — Identifier of the store product associated with the option; may be empty if none */
  store_product_id: string = '';
  /** yearly_boost_count: int32 — Number of times the chat will be boosted for one year if the option is chosen */
  yearly_boost_count: number = 0;
  /** winner_options: vector<starGiveawayWinnerOption> — Allowed options for the number of giveaway winners */
  winner_options: StarGiveawayWinnerOption[] = [];
  /** is_default: Bool — True, if the option must be chosen by default */
  is_default: boolean = false;
  /** is_additional: Bool — True, if the option must be shown only in the full list of payment options */
  is_additional: boolean = false;
}

/** Decode a TDLib `starGiveawayPaymentOption` object; null input -> null. */
export function decodeStarGiveawayPaymentOption(json: TdJsonRaw): StarGiveawayPaymentOption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarGiveawayPaymentOption = new StarGiveawayPaymentOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.star_count = asNumber(record['star_count']);
  out.store_product_id = asString(record['store_product_id']);
  out.yearly_boost_count = asNumber(record['yearly_boost_count']);
  out.winner_options = decodeVector(record['winner_options'], (item: TdJsonRaw): StarGiveawayWinnerOption => decodeStarGiveawayWinnerOption(item) as StarGiveawayWinnerOption);
  out.is_default = asBool(record['is_default']);
  out.is_additional = asBool(record['is_additional']);
  return out;
}

/** Encode a `starGiveawayPaymentOption` into TDLib JSON wire shape. */
export function encodeStarGiveawayPaymentOption(value: StarGiveawayPaymentOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starGiveawayPaymentOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['star_count'] = value.star_count;
  out['store_product_id'] = value.store_product_id;
  out['yearly_boost_count'] = value.yearly_boost_count;
  out['winner_options'] = encodeVector(value.winner_options, (item: StarGiveawayWinnerOption | null): TdJsonRaw => item === null ? null : encodeStarGiveawayWinnerOption(item));
  out['is_default'] = value.is_default;
  out['is_additional'] = value.is_additional;
  return out;
}

/** starGiveawayPaymentOptions = StarGiveawayPaymentOptions — Contains a list of options for creating of Telegram Star giveaway */
export class StarGiveawayPaymentOptions {
  type: 'starGiveawayPaymentOptions' = 'starGiveawayPaymentOptions';
  extra?: string;
  /** options: vector<starGiveawayPaymentOption> — The list of options */
  options: StarGiveawayPaymentOption[] = [];
}

/** Decode a TDLib `starGiveawayPaymentOptions` object; null input -> null. */
export function decodeStarGiveawayPaymentOptions(json: TdJsonRaw): StarGiveawayPaymentOptions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarGiveawayPaymentOptions = new StarGiveawayPaymentOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.options = decodeVector(record['options'], (item: TdJsonRaw): StarGiveawayPaymentOption => decodeStarGiveawayPaymentOption(item) as StarGiveawayPaymentOption);
  return out;
}

/** Encode a `starGiveawayPaymentOptions` into TDLib JSON wire shape. */
export function encodeStarGiveawayPaymentOptions(value: StarGiveawayPaymentOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starGiveawayPaymentOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['options'] = encodeVector(value.options, (item: StarGiveawayPaymentOption | null): TdJsonRaw => item === null ? null : encodeStarGiveawayPaymentOption(item));
  return out;
}

/** starGiveawayWinnerOption = StarGiveawayWinnerOption — Describes an option for the number of winners of a Telegram Star giveaway */
export class StarGiveawayWinnerOption {
  type: 'starGiveawayWinnerOption' = 'starGiveawayWinnerOption';
  extra?: string;
  /** winner_count: int32 — The number of users that will be chosen as winners */
  winner_count: number = 0;
  /** won_star_count: int53 — The number of Telegram Stars that will be won by the winners of the giveaway */
  won_star_count: number = 0;
  /** is_default: Bool — True, if the option must be chosen by default */
  is_default: boolean = false;
}

/** Decode a TDLib `starGiveawayWinnerOption` object; null input -> null. */
export function decodeStarGiveawayWinnerOption(json: TdJsonRaw): StarGiveawayWinnerOption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarGiveawayWinnerOption = new StarGiveawayWinnerOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.winner_count = asNumber(record['winner_count']);
  out.won_star_count = asNumber(record['won_star_count']);
  out.is_default = asBool(record['is_default']);
  return out;
}

/** Encode a `starGiveawayWinnerOption` into TDLib JSON wire shape. */
export function encodeStarGiveawayWinnerOption(value: StarGiveawayWinnerOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starGiveawayWinnerOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['winner_count'] = value.winner_count;
  out['won_star_count'] = value.won_star_count;
  out['is_default'] = value.is_default;
  return out;
}

/** starPaymentOption = StarPaymentOption — Describes an option for buying Telegram Stars. Use telegramPaymentPurposeStars for out-of-store payments */
export class StarPaymentOption {
  type: 'starPaymentOption' = 'starPaymentOption';
  extra?: string;
  /** currency: string — ISO 4217 currency code for the payment */
  currency: string = '';
  /** amount: int53 — The amount to pay, in the smallest units of the currency */
  amount: number = 0;
  /** star_count: int53 — Number of Telegram Stars that will be purchased */
  star_count: number = 0;
  /** store_product_id: string — Identifier of the store product associated with the option; may be empty if none */
  store_product_id: string = '';
  /** is_additional: Bool — True, if the option must be shown only in the full list of payment options */
  is_additional: boolean = false;
}

/** Decode a TDLib `starPaymentOption` object; null input -> null. */
export function decodeStarPaymentOption(json: TdJsonRaw): StarPaymentOption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarPaymentOption = new StarPaymentOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.star_count = asNumber(record['star_count']);
  out.store_product_id = asString(record['store_product_id']);
  out.is_additional = asBool(record['is_additional']);
  return out;
}

/** Encode a `starPaymentOption` into TDLib JSON wire shape. */
export function encodeStarPaymentOption(value: StarPaymentOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starPaymentOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['star_count'] = value.star_count;
  out['store_product_id'] = value.store_product_id;
  out['is_additional'] = value.is_additional;
  return out;
}

/** starPaymentOptions = StarPaymentOptions — Contains a list of options for buying Telegram Stars */
export class StarPaymentOptions {
  type: 'starPaymentOptions' = 'starPaymentOptions';
  extra?: string;
  /** options: vector<starPaymentOption> — The list of options */
  options: StarPaymentOption[] = [];
}

/** Decode a TDLib `starPaymentOptions` object; null input -> null. */
export function decodeStarPaymentOptions(json: TdJsonRaw): StarPaymentOptions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarPaymentOptions = new StarPaymentOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.options = decodeVector(record['options'], (item: TdJsonRaw): StarPaymentOption => decodeStarPaymentOption(item) as StarPaymentOption);
  return out;
}

/** Encode a `starPaymentOptions` into TDLib JSON wire shape. */
export function encodeStarPaymentOptions(value: StarPaymentOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starPaymentOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['options'] = encodeVector(value.options, (item: StarPaymentOption | null): TdJsonRaw => item === null ? null : encodeStarPaymentOption(item));
  return out;
}

/** starRevenueStatistics = StarRevenueStatistics — A detailed statistics about Telegram Stars earned by a user or a chat */
export class StarRevenueStatistics {
  type: 'starRevenueStatistics' = 'starRevenueStatistics';
  extra?: string;
  /** revenue_by_day_graph: StatisticalGraph — A graph containing amount of revenue in a given day */
  revenue_by_day_graph: TdStatisticalGraph | null = null;
  /** status: starRevenueStatus — Telegram Star revenue status */
  status: StarRevenueStatus | null = null;
  /** usd_rate: double — Current conversion rate of a Telegram Star to USD */
  usd_rate: number = 0;
}

/** Decode a TDLib `starRevenueStatistics` object; null input -> null. */
export function decodeStarRevenueStatistics(json: TdJsonRaw): StarRevenueStatistics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarRevenueStatistics = new StarRevenueStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.revenue_by_day_graph = decodeTdStatisticalGraph(record['revenue_by_day_graph']);
  out.status = decodeStarRevenueStatus(record['status']);
  out.usd_rate = asNumber(record['usd_rate']);
  return out;
}

/** Encode a `starRevenueStatistics` into TDLib JSON wire shape. */
export function encodeStarRevenueStatistics(value: StarRevenueStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starRevenueStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['revenue_by_day_graph'] = value.revenue_by_day_graph === null ? null : encodeTdStatisticalGraph(value.revenue_by_day_graph);
  out['status'] = value.status === null ? null : encodeStarRevenueStatus(value.status);
  out['usd_rate'] = value.usd_rate;
  return out;
}

/** starRevenueStatus = StarRevenueStatus — Contains information about Telegram Stars earned by a user or a chat */
export class StarRevenueStatus {
  type: 'starRevenueStatus' = 'starRevenueStatus';
  extra?: string;
  /** total_amount: starAmount — Total Telegram Star amount earned */
  total_amount: StarAmount | null = null;
  /** current_amount: starAmount — The Telegram Star amount that isn't withdrawn yet */
  current_amount: StarAmount | null = null;
  /** available_amount: starAmount — The Telegram Star amount that is available for withdrawal */
  available_amount: StarAmount | null = null;
  /** withdrawal_enabled: Bool — True, if Telegram Stars can be withdrawn now or later */
  withdrawal_enabled: boolean = false;
  /** next_withdrawal_in: int32 — Time left before the next withdrawal can be started, in seconds; 0 if withdrawal can be started now */
  next_withdrawal_in: number = 0;
}

/** Decode a TDLib `starRevenueStatus` object; null input -> null. */
export function decodeStarRevenueStatus(json: TdJsonRaw): StarRevenueStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarRevenueStatus = new StarRevenueStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_amount = decodeStarAmount(record['total_amount']);
  out.current_amount = decodeStarAmount(record['current_amount']);
  out.available_amount = decodeStarAmount(record['available_amount']);
  out.withdrawal_enabled = asBool(record['withdrawal_enabled']);
  out.next_withdrawal_in = asNumber(record['next_withdrawal_in']);
  return out;
}

/** Encode a `starRevenueStatus` into TDLib JSON wire shape. */
export function encodeStarRevenueStatus(value: StarRevenueStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starRevenueStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_amount'] = value.total_amount === null ? null : encodeStarAmount(value.total_amount);
  out['current_amount'] = value.current_amount === null ? null : encodeStarAmount(value.current_amount);
  out['available_amount'] = value.available_amount === null ? null : encodeStarAmount(value.available_amount);
  out['withdrawal_enabled'] = value.withdrawal_enabled;
  out['next_withdrawal_in'] = value.next_withdrawal_in;
  return out;
}

/** starSubscription = StarSubscription — Contains information about subscription to a channel chat, a bot, or a business account that was paid in Telegram Stars */
export class StarSubscription {
  type: 'starSubscription' = 'starSubscription';
  extra?: string;
  /** id: string — Unique identifier of the subscription */
  id: string = '';
  /** chat_id: int53 — Identifier of the chat that is subscribed */
  chat_id: number = 0;
  /** expiration_date: int32 — Point in time (Unix timestamp) when the subscription will expire or expired */
  expiration_date: number = 0;
  /** is_canceled: Bool — True, if the subscription was canceled */
  is_canceled: boolean = false;
  /** is_expiring: Bool — True, if the subscription expires soon and there aren't enough Telegram Stars on the user's balance to extend it */
  is_expiring: boolean = false;
  /** pricing: starSubscriptionPricing — The subscription plan */
  pricing: StarSubscriptionPricing | null = null;
  /** type: StarSubscriptionType — Type of the subscription */
  type_: TdStarSubscriptionType | null = null;
}

/** Decode a TDLib `starSubscription` object; null input -> null. */
export function decodeStarSubscription(json: TdJsonRaw): StarSubscription | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarSubscription = new StarSubscription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.chat_id = asNumber(record['chat_id']);
  out.expiration_date = asNumber(record['expiration_date']);
  out.is_canceled = asBool(record['is_canceled']);
  out.is_expiring = asBool(record['is_expiring']);
  out.pricing = decodeStarSubscriptionPricing(record['pricing']);
  out.type_ = decodeTdStarSubscriptionType(record['type']);
  return out;
}

/** Encode a `starSubscription` into TDLib JSON wire shape. */
export function encodeStarSubscription(value: StarSubscription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starSubscription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['chat_id'] = value.chat_id;
  out['expiration_date'] = value.expiration_date;
  out['is_canceled'] = value.is_canceled;
  out['is_expiring'] = value.is_expiring;
  out['pricing'] = value.pricing === null ? null : encodeStarSubscriptionPricing(value.pricing);
  out['type'] = value.type_ === null ? null : encodeTdStarSubscriptionType(value.type_);
  return out;
}

/** starSubscriptionPricing = StarSubscriptionPricing — Describes subscription plan paid in Telegram Stars */
export class StarSubscriptionPricing {
  type: 'starSubscriptionPricing' = 'starSubscriptionPricing';
  extra?: string;
  /** period: int32 — The number of seconds between consecutive Telegram Star debiting */
  period: number = 0;
  /** star_count: int53 — The Telegram Star amount that must be paid for each period */
  star_count: number = 0;
}

/** Decode a TDLib `starSubscriptionPricing` object; null input -> null. */
export function decodeStarSubscriptionPricing(json: TdJsonRaw): StarSubscriptionPricing | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarSubscriptionPricing = new StarSubscriptionPricing();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.period = asNumber(record['period']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `starSubscriptionPricing` into TDLib JSON wire shape. */
export function encodeStarSubscriptionPricing(value: StarSubscriptionPricing): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starSubscriptionPricing';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['period'] = value.period;
  out['star_count'] = value.star_count;
  return out;
}

/** starSubscriptionTypeBot = StarSubscriptionType — Describes a subscription in a bot or a business account */
export class StarSubscriptionTypeBot {
  type: 'starSubscriptionTypeBot' = 'starSubscriptionTypeBot';
  extra?: string;
  /** is_canceled_by_bot: Bool — True, if the subscription was canceled by the bot and can't be extended */
  is_canceled_by_bot: boolean = false;
  /** title: string — Subscription invoice title */
  title: string = '';
  /** photo: photo — Subscription invoice photo */
  photo: Photo | null = null;
  /** invoice_link: string — The link to the subscription invoice */
  invoice_link: string = '';
}

/** Decode a TDLib `starSubscriptionTypeBot` object; null input -> null. */
export function decodeStarSubscriptionTypeBot(json: TdJsonRaw): StarSubscriptionTypeBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarSubscriptionTypeBot = new StarSubscriptionTypeBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_canceled_by_bot = asBool(record['is_canceled_by_bot']);
  out.title = asString(record['title']);
  out.photo = decodePhoto(record['photo']);
  out.invoice_link = asString(record['invoice_link']);
  return out;
}

/** Encode a `starSubscriptionTypeBot` into TDLib JSON wire shape. */
export function encodeStarSubscriptionTypeBot(value: StarSubscriptionTypeBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starSubscriptionTypeBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_canceled_by_bot'] = value.is_canceled_by_bot;
  out['title'] = value.title;
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['invoice_link'] = value.invoice_link;
  return out;
}

/** starSubscriptionTypeChannel = StarSubscriptionType — Describes a subscription to a channel chat */
export class StarSubscriptionTypeChannel {
  type: 'starSubscriptionTypeChannel' = 'starSubscriptionTypeChannel';
  extra?: string;
  /** can_reuse: Bool — True, if the subscription is active and the user can use the method reuseStarSubscription to join the subscribed chat again */
  can_reuse: boolean = false;
  /** invite_link: string — The invite link that can be used to renew the subscription if it has expired; may be empty if the link isn't available anymore */
  invite_link: string = '';
}

/** Decode a TDLib `starSubscriptionTypeChannel` object; null input -> null. */
export function decodeStarSubscriptionTypeChannel(json: TdJsonRaw): StarSubscriptionTypeChannel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarSubscriptionTypeChannel = new StarSubscriptionTypeChannel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_reuse = asBool(record['can_reuse']);
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `starSubscriptionTypeChannel` into TDLib JSON wire shape. */
export function encodeStarSubscriptionTypeChannel(value: StarSubscriptionTypeChannel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starSubscriptionTypeChannel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_reuse'] = value.can_reuse;
  out['invite_link'] = value.invite_link;
  return out;
}

/** starSubscriptions = StarSubscriptions — Represents a list of Telegram Star subscriptions */
export class StarSubscriptions {
  type: 'starSubscriptions' = 'starSubscriptions';
  extra?: string;
  /** star_amount: starAmount — The amount of owned Telegram Stars */
  star_amount: StarAmount | null = null;
  /** subscriptions: vector<starSubscription> — List of subscriptions for Telegram Stars */
  subscriptions: StarSubscription[] = [];
  /** required_star_count: int53 — The number of Telegram Stars required to buy to extend subscriptions expiring soon */
  required_star_count: number = 0;
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `starSubscriptions` object; null input -> null. */
export function decodeStarSubscriptions(json: TdJsonRaw): StarSubscriptions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarSubscriptions = new StarSubscriptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_amount = decodeStarAmount(record['star_amount']);
  out.subscriptions = decodeVector(record['subscriptions'], (item: TdJsonRaw): StarSubscription => decodeStarSubscription(item) as StarSubscription);
  out.required_star_count = asNumber(record['required_star_count']);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `starSubscriptions` into TDLib JSON wire shape. */
export function encodeStarSubscriptions(value: StarSubscriptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starSubscriptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_amount'] = value.star_amount === null ? null : encodeStarAmount(value.star_amount);
  out['subscriptions'] = encodeVector(value.subscriptions, (item: StarSubscription | null): TdJsonRaw => item === null ? null : encodeStarSubscription(item));
  out['required_star_count'] = value.required_star_count;
  out['next_offset'] = value.next_offset;
  return out;
}

/** starTransaction = StarTransaction — Represents a transaction changing the amount of owned Telegram Stars */
export class StarTransaction {
  type: 'starTransaction' = 'starTransaction';
  extra?: string;
  /** id: string — Unique identifier of the transaction */
  id: string = '';
  /** star_amount: starAmount — The amount of added owned Telegram Stars; negative for outgoing transactions */
  star_amount: StarAmount | null = null;
  /** is_refund: Bool — True, if the transaction is a refund of a previous transaction */
  is_refund: boolean = false;
  /** date: int32 — Point in time (Unix timestamp) when the transaction was completed */
  date: number = 0;
  /** type: StarTransactionType — Type of the transaction */
  type_: TdStarTransactionType | null = null;
}

/** Decode a TDLib `starTransaction` object; null input -> null. */
export function decodeStarTransaction(json: TdJsonRaw): StarTransaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransaction = new StarTransaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.star_amount = decodeStarAmount(record['star_amount']);
  out.is_refund = asBool(record['is_refund']);
  out.date = asNumber(record['date']);
  out.type_ = decodeTdStarTransactionType(record['type']);
  return out;
}

/** Encode a `starTransaction` into TDLib JSON wire shape. */
export function encodeStarTransaction(value: StarTransaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['star_amount'] = value.star_amount === null ? null : encodeStarAmount(value.star_amount);
  out['is_refund'] = value.is_refund;
  out['date'] = value.date;
  out['type'] = value.type_ === null ? null : encodeTdStarTransactionType(value.type_);
  return out;
}

/** starTransactionTypeAffiliateProgramCommission = StarTransactionType — The transaction is a receiving of a commission from an affiliate program; relevant for regular users, bots and channel chats only */
export class StarTransactionTypeAffiliateProgramCommission {
  type: 'starTransactionTypeAffiliateProgramCommission' = 'starTransactionTypeAffiliateProgramCommission';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that created the affiliate program */
  chat_id: number = 0;
  /** commission_per_mille: int32 — The number of Telegram Stars received by the affiliate for each 1000 Telegram Stars received by the program owner */
  commission_per_mille: number = 0;
}

/** Decode a TDLib `starTransactionTypeAffiliateProgramCommission` object; null input -> null. */
export function decodeStarTransactionTypeAffiliateProgramCommission(json: TdJsonRaw): StarTransactionTypeAffiliateProgramCommission | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeAffiliateProgramCommission = new StarTransactionTypeAffiliateProgramCommission();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.commission_per_mille = asNumber(record['commission_per_mille']);
  return out;
}

/** Encode a `starTransactionTypeAffiliateProgramCommission` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeAffiliateProgramCommission(value: StarTransactionTypeAffiliateProgramCommission): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeAffiliateProgramCommission';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['commission_per_mille'] = value.commission_per_mille;
  return out;
}

/** starTransactionTypeAppStoreDeposit = StarTransactionType — The transaction is a deposit of Telegram Stars from App Store; relevant for regular users only */
export class StarTransactionTypeAppStoreDeposit {
  type: 'starTransactionTypeAppStoreDeposit' = 'starTransactionTypeAppStoreDeposit';
  extra?: string;
}

/** Decode a TDLib `starTransactionTypeAppStoreDeposit` object; null input -> null. */
export function decodeStarTransactionTypeAppStoreDeposit(json: TdJsonRaw): StarTransactionTypeAppStoreDeposit | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeAppStoreDeposit = new StarTransactionTypeAppStoreDeposit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `starTransactionTypeAppStoreDeposit` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeAppStoreDeposit(value: StarTransactionTypeAppStoreDeposit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeAppStoreDeposit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** starTransactionTypeBotInvoicePurchase = StarTransactionType — The transaction is a purchase of a product from a bot or a business account by the current user; relevant for regular users only */
export class StarTransactionTypeBotInvoicePurchase {
  type: 'starTransactionTypeBotInvoicePurchase' = 'starTransactionTypeBotInvoicePurchase';
  extra?: string;
  /** user_id: int53 — Identifier of the bot or the business account user who created the invoice */
  user_id: number = 0;
  /** product_info: productInfo — Information about the bought product */
  product_info: ProductInfo | null = null;
}

/** Decode a TDLib `starTransactionTypeBotInvoicePurchase` object; null input -> null. */
export function decodeStarTransactionTypeBotInvoicePurchase(json: TdJsonRaw): StarTransactionTypeBotInvoicePurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeBotInvoicePurchase = new StarTransactionTypeBotInvoicePurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.product_info = decodeProductInfo(record['product_info']);
  return out;
}

/** Encode a `starTransactionTypeBotInvoicePurchase` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeBotInvoicePurchase(value: StarTransactionTypeBotInvoicePurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeBotInvoicePurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['product_info'] = value.product_info === null ? null : encodeProductInfo(value.product_info);
  return out;
}

/** starTransactionTypeBotInvoiceSale = StarTransactionType — The transaction is a sale of a product by the bot; relevant for bots only */
export class StarTransactionTypeBotInvoiceSale {
  type: 'starTransactionTypeBotInvoiceSale' = 'starTransactionTypeBotInvoiceSale';
  extra?: string;
  /** user_id: int53 — Identifier of the user who bought the product */
  user_id: number = 0;
  /** product_info: productInfo — Information about the bought product */
  product_info: ProductInfo | null = null;
  /** invoice_payload: bytes — Invoice payload */
  invoice_payload: string = '';
  /** affiliate: affiliateInfo — Information about the affiliate which received commission from the transaction; may be null if none */
  affiliate: AffiliateInfo | null = null;
}

/** Decode a TDLib `starTransactionTypeBotInvoiceSale` object; null input -> null. */
export function decodeStarTransactionTypeBotInvoiceSale(json: TdJsonRaw): StarTransactionTypeBotInvoiceSale | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeBotInvoiceSale = new StarTransactionTypeBotInvoiceSale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.product_info = decodeProductInfo(record['product_info']);
  out.invoice_payload = asString(record['invoice_payload']);
  out.affiliate = decodeAffiliateInfo(record['affiliate']);
  return out;
}

/** Encode a `starTransactionTypeBotInvoiceSale` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeBotInvoiceSale(value: StarTransactionTypeBotInvoiceSale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeBotInvoiceSale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['product_info'] = value.product_info === null ? null : encodeProductInfo(value.product_info);
  out['invoice_payload'] = value.invoice_payload;
  out['affiliate'] = value.affiliate === null ? null : encodeAffiliateInfo(value.affiliate);
  return out;
}

/** starTransactionTypeBotPaidMediaPurchase = StarTransactionType — The transaction is a purchase of paid media from a bot or a business account by the current user; relevant for regular users only */
export class StarTransactionTypeBotPaidMediaPurchase {
  type: 'starTransactionTypeBotPaidMediaPurchase' = 'starTransactionTypeBotPaidMediaPurchase';
  extra?: string;
  /** user_id: int53 — Identifier of the bot or the business account user who sent the paid media */
  user_id: number = 0;
  /** media: vector<PaidMedia> — The bought media if the transaction wasn't refunded */
  media: TdPaidMedia[] = [];
}

/** Decode a TDLib `starTransactionTypeBotPaidMediaPurchase` object; null input -> null. */
export function decodeStarTransactionTypeBotPaidMediaPurchase(json: TdJsonRaw): StarTransactionTypeBotPaidMediaPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeBotPaidMediaPurchase = new StarTransactionTypeBotPaidMediaPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.media = decodeVector(record['media'], (item: TdJsonRaw): TdPaidMedia => decodeTdPaidMedia(item) as TdPaidMedia);
  return out;
}

/** Encode a `starTransactionTypeBotPaidMediaPurchase` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeBotPaidMediaPurchase(value: StarTransactionTypeBotPaidMediaPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeBotPaidMediaPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['media'] = encodeVector(value.media, (item: TdPaidMedia | null): TdJsonRaw => item === null ? null : encodeTdPaidMedia(item));
  return out;
}

/** starTransactionTypeBotPaidMediaSale = StarTransactionType — The transaction is a sale of paid media by the bot or a business account managed by the bot; relevant for bots only */
export class StarTransactionTypeBotPaidMediaSale {
  type: 'starTransactionTypeBotPaidMediaSale' = 'starTransactionTypeBotPaidMediaSale';
  extra?: string;
  /** user_id: int53 — Identifier of the user who bought the media */
  user_id: number = 0;
  /** media: vector<PaidMedia> — The bought media */
  media: TdPaidMedia[] = [];
  /** payload: string — Bot-provided payload */
  payload: string = '';
  /** affiliate: affiliateInfo — Information about the affiliate which received commission from the transaction; may be null if none */
  affiliate: AffiliateInfo | null = null;
}

/** Decode a TDLib `starTransactionTypeBotPaidMediaSale` object; null input -> null. */
export function decodeStarTransactionTypeBotPaidMediaSale(json: TdJsonRaw): StarTransactionTypeBotPaidMediaSale | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeBotPaidMediaSale = new StarTransactionTypeBotPaidMediaSale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.media = decodeVector(record['media'], (item: TdJsonRaw): TdPaidMedia => decodeTdPaidMedia(item) as TdPaidMedia);
  out.payload = asString(record['payload']);
  out.affiliate = decodeAffiliateInfo(record['affiliate']);
  return out;
}

/** Encode a `starTransactionTypeBotPaidMediaSale` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeBotPaidMediaSale(value: StarTransactionTypeBotPaidMediaSale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeBotPaidMediaSale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['media'] = encodeVector(value.media, (item: TdPaidMedia | null): TdJsonRaw => item === null ? null : encodeTdPaidMedia(item));
  out['payload'] = value.payload;
  out['affiliate'] = value.affiliate === null ? null : encodeAffiliateInfo(value.affiliate);
  return out;
}

/** starTransactionTypeBotSubscriptionPurchase = StarTransactionType — The transaction is a purchase of a subscription from a bot or a business account by the current user; relevant for regular users only */
export class StarTransactionTypeBotSubscriptionPurchase {
  type: 'starTransactionTypeBotSubscriptionPurchase' = 'starTransactionTypeBotSubscriptionPurchase';
  extra?: string;
  /** user_id: int53 — Identifier of the bot or the business account user who created the subscription link */
  user_id: number = 0;
  /** subscription_period: int32 — The number of seconds between consecutive Telegram Star debitings */
  subscription_period: number = 0;
  /** product_info: productInfo — Information about the bought subscription */
  product_info: ProductInfo | null = null;
}

/** Decode a TDLib `starTransactionTypeBotSubscriptionPurchase` object; null input -> null. */
export function decodeStarTransactionTypeBotSubscriptionPurchase(json: TdJsonRaw): StarTransactionTypeBotSubscriptionPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeBotSubscriptionPurchase = new StarTransactionTypeBotSubscriptionPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.subscription_period = asNumber(record['subscription_period']);
  out.product_info = decodeProductInfo(record['product_info']);
  return out;
}

/** Encode a `starTransactionTypeBotSubscriptionPurchase` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeBotSubscriptionPurchase(value: StarTransactionTypeBotSubscriptionPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeBotSubscriptionPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['subscription_period'] = value.subscription_period;
  out['product_info'] = value.product_info === null ? null : encodeProductInfo(value.product_info);
  return out;
}

/** starTransactionTypeBotSubscriptionSale = StarTransactionType — The transaction is a sale of a subscription by the bot; relevant for bots only */
export class StarTransactionTypeBotSubscriptionSale {
  type: 'starTransactionTypeBotSubscriptionSale' = 'starTransactionTypeBotSubscriptionSale';
  extra?: string;
  /** user_id: int53 — Identifier of the user who bought the subscription */
  user_id: number = 0;
  /** subscription_period: int32 — The number of seconds between consecutive Telegram Star debitings */
  subscription_period: number = 0;
  /** product_info: productInfo — Information about the bought subscription */
  product_info: ProductInfo | null = null;
  /** invoice_payload: bytes — Invoice payload */
  invoice_payload: string = '';
  /** affiliate: affiliateInfo — Information about the affiliate which received commission from the transaction; may be null if none */
  affiliate: AffiliateInfo | null = null;
}

/** Decode a TDLib `starTransactionTypeBotSubscriptionSale` object; null input -> null. */
export function decodeStarTransactionTypeBotSubscriptionSale(json: TdJsonRaw): StarTransactionTypeBotSubscriptionSale | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeBotSubscriptionSale = new StarTransactionTypeBotSubscriptionSale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.subscription_period = asNumber(record['subscription_period']);
  out.product_info = decodeProductInfo(record['product_info']);
  out.invoice_payload = asString(record['invoice_payload']);
  out.affiliate = decodeAffiliateInfo(record['affiliate']);
  return out;
}

/** Encode a `starTransactionTypeBotSubscriptionSale` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeBotSubscriptionSale(value: StarTransactionTypeBotSubscriptionSale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeBotSubscriptionSale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['subscription_period'] = value.subscription_period;
  out['product_info'] = value.product_info === null ? null : encodeProductInfo(value.product_info);
  out['invoice_payload'] = value.invoice_payload;
  out['affiliate'] = value.affiliate === null ? null : encodeAffiliateInfo(value.affiliate);
  return out;
}

/** starTransactionTypeBusinessBotTransferReceive = StarTransactionType — The transaction is a transfer of Telegram Stars from a business account; relevant for bots only */
export class StarTransactionTypeBusinessBotTransferReceive {
  type: 'starTransactionTypeBusinessBotTransferReceive' = 'starTransactionTypeBusinessBotTransferReceive';
  extra?: string;
  /** user_id: int53 — Identifier of the user who sent Telegram Stars */
  user_id: number = 0;
}

/** Decode a TDLib `starTransactionTypeBusinessBotTransferReceive` object; null input -> null. */
export function decodeStarTransactionTypeBusinessBotTransferReceive(json: TdJsonRaw): StarTransactionTypeBusinessBotTransferReceive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeBusinessBotTransferReceive = new StarTransactionTypeBusinessBotTransferReceive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `starTransactionTypeBusinessBotTransferReceive` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeBusinessBotTransferReceive(value: StarTransactionTypeBusinessBotTransferReceive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeBusinessBotTransferReceive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** starTransactionTypeBusinessBotTransferSend = StarTransactionType — The transaction is a transfer of Telegram Stars to a business bot; relevant for regular users only */
export class StarTransactionTypeBusinessBotTransferSend {
  type: 'starTransactionTypeBusinessBotTransferSend' = 'starTransactionTypeBusinessBotTransferSend';
  extra?: string;
  /** user_id: int53 — Identifier of the bot that received Telegram Stars */
  user_id: number = 0;
}

/** Decode a TDLib `starTransactionTypeBusinessBotTransferSend` object; null input -> null. */
export function decodeStarTransactionTypeBusinessBotTransferSend(json: TdJsonRaw): StarTransactionTypeBusinessBotTransferSend | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeBusinessBotTransferSend = new StarTransactionTypeBusinessBotTransferSend();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `starTransactionTypeBusinessBotTransferSend` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeBusinessBotTransferSend(value: StarTransactionTypeBusinessBotTransferSend): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeBusinessBotTransferSend';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** starTransactionTypeChannelPaidMediaPurchase = StarTransactionType — The transaction is a purchase of paid media from a channel by the current user; relevant for regular users only */
export class StarTransactionTypeChannelPaidMediaPurchase {
  type: 'starTransactionTypeChannelPaidMediaPurchase' = 'starTransactionTypeChannelPaidMediaPurchase';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat that sent the paid media */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the corresponding message with paid media; may be 0 or an identifier of a deleted message */
  message_id: number = 0;
  /** media: vector<PaidMedia> — The bought media if the transaction wasn't refunded */
  media: TdPaidMedia[] = [];
}

/** Decode a TDLib `starTransactionTypeChannelPaidMediaPurchase` object; null input -> null. */
export function decodeStarTransactionTypeChannelPaidMediaPurchase(json: TdJsonRaw): StarTransactionTypeChannelPaidMediaPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeChannelPaidMediaPurchase = new StarTransactionTypeChannelPaidMediaPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.media = decodeVector(record['media'], (item: TdJsonRaw): TdPaidMedia => decodeTdPaidMedia(item) as TdPaidMedia);
  return out;
}

/** Encode a `starTransactionTypeChannelPaidMediaPurchase` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeChannelPaidMediaPurchase(value: StarTransactionTypeChannelPaidMediaPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeChannelPaidMediaPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['media'] = encodeVector(value.media, (item: TdPaidMedia | null): TdJsonRaw => item === null ? null : encodeTdPaidMedia(item));
  return out;
}

/** starTransactionTypeChannelPaidMediaSale = StarTransactionType — The transaction is a sale of paid media by the channel chat; relevant for channel chats only */
export class StarTransactionTypeChannelPaidMediaSale {
  type: 'starTransactionTypeChannelPaidMediaSale' = 'starTransactionTypeChannelPaidMediaSale';
  extra?: string;
  /** user_id: int53 — Identifier of the user who bought the media */
  user_id: number = 0;
  /** message_id: int53 — Identifier of the corresponding message with paid media; may be 0 or an identifier of a deleted message */
  message_id: number = 0;
  /** media: vector<PaidMedia> — The bought media */
  media: TdPaidMedia[] = [];
}

/** Decode a TDLib `starTransactionTypeChannelPaidMediaSale` object; null input -> null. */
export function decodeStarTransactionTypeChannelPaidMediaSale(json: TdJsonRaw): StarTransactionTypeChannelPaidMediaSale | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeChannelPaidMediaSale = new StarTransactionTypeChannelPaidMediaSale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.message_id = asNumber(record['message_id']);
  out.media = decodeVector(record['media'], (item: TdJsonRaw): TdPaidMedia => decodeTdPaidMedia(item) as TdPaidMedia);
  return out;
}

/** Encode a `starTransactionTypeChannelPaidMediaSale` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeChannelPaidMediaSale(value: StarTransactionTypeChannelPaidMediaSale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeChannelPaidMediaSale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['message_id'] = value.message_id;
  out['media'] = encodeVector(value.media, (item: TdPaidMedia | null): TdJsonRaw => item === null ? null : encodeTdPaidMedia(item));
  return out;
}

/** starTransactionTypeChannelPaidReactionReceive = StarTransactionType — The transaction is a receiving of a paid reaction to a message by the channel chat; relevant for channel chats only */
export class StarTransactionTypeChannelPaidReactionReceive {
  type: 'starTransactionTypeChannelPaidReactionReceive' = 'starTransactionTypeChannelPaidReactionReceive';
  extra?: string;
  /** user_id: int53 — Identifier of the user who added the paid reaction */
  user_id: number = 0;
  /** message_id: int53 — Identifier of the reacted message; may be 0 or an identifier of a deleted message */
  message_id: number = 0;
}

/** Decode a TDLib `starTransactionTypeChannelPaidReactionReceive` object; null input -> null. */
export function decodeStarTransactionTypeChannelPaidReactionReceive(json: TdJsonRaw): StarTransactionTypeChannelPaidReactionReceive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeChannelPaidReactionReceive = new StarTransactionTypeChannelPaidReactionReceive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `starTransactionTypeChannelPaidReactionReceive` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeChannelPaidReactionReceive(value: StarTransactionTypeChannelPaidReactionReceive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeChannelPaidReactionReceive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['message_id'] = value.message_id;
  return out;
}

/** starTransactionTypeChannelPaidReactionSend = StarTransactionType — The transaction is a sending of a paid reaction to a message in a channel chat by the current user; relevant for regular users only */
export class StarTransactionTypeChannelPaidReactionSend {
  type: 'starTransactionTypeChannelPaidReactionSend' = 'starTransactionTypeChannelPaidReactionSend';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the reacted message; may be 0 or an identifier of a deleted message */
  message_id: number = 0;
}

/** Decode a TDLib `starTransactionTypeChannelPaidReactionSend` object; null input -> null. */
export function decodeStarTransactionTypeChannelPaidReactionSend(json: TdJsonRaw): StarTransactionTypeChannelPaidReactionSend | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeChannelPaidReactionSend = new StarTransactionTypeChannelPaidReactionSend();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `starTransactionTypeChannelPaidReactionSend` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeChannelPaidReactionSend(value: StarTransactionTypeChannelPaidReactionSend): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeChannelPaidReactionSend';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** starTransactionTypeChannelSubscriptionPurchase = StarTransactionType — The transaction is a purchase of a subscription to a channel chat by the current user; relevant for regular users only */
export class StarTransactionTypeChannelSubscriptionPurchase {
  type: 'starTransactionTypeChannelSubscriptionPurchase' = 'starTransactionTypeChannelSubscriptionPurchase';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat that created the subscription */
  chat_id: number = 0;
  /** subscription_period: int32 — The number of seconds between consecutive Telegram Star debitings */
  subscription_period: number = 0;
}

/** Decode a TDLib `starTransactionTypeChannelSubscriptionPurchase` object; null input -> null. */
export function decodeStarTransactionTypeChannelSubscriptionPurchase(json: TdJsonRaw): StarTransactionTypeChannelSubscriptionPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeChannelSubscriptionPurchase = new StarTransactionTypeChannelSubscriptionPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.subscription_period = asNumber(record['subscription_period']);
  return out;
}

/** Encode a `starTransactionTypeChannelSubscriptionPurchase` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeChannelSubscriptionPurchase(value: StarTransactionTypeChannelSubscriptionPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeChannelSubscriptionPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['subscription_period'] = value.subscription_period;
  return out;
}

/** starTransactionTypeChannelSubscriptionSale = StarTransactionType — The transaction is a sale of a subscription by the channel chat; relevant for channel chats only */
export class StarTransactionTypeChannelSubscriptionSale {
  type: 'starTransactionTypeChannelSubscriptionSale' = 'starTransactionTypeChannelSubscriptionSale';
  extra?: string;
  /** user_id: int53 — Identifier of the user who bought the subscription */
  user_id: number = 0;
  /** subscription_period: int32 — The number of seconds between consecutive Telegram Star debitings */
  subscription_period: number = 0;
}

/** Decode a TDLib `starTransactionTypeChannelSubscriptionSale` object; null input -> null. */
export function decodeStarTransactionTypeChannelSubscriptionSale(json: TdJsonRaw): StarTransactionTypeChannelSubscriptionSale | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeChannelSubscriptionSale = new StarTransactionTypeChannelSubscriptionSale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.subscription_period = asNumber(record['subscription_period']);
  return out;
}

/** Encode a `starTransactionTypeChannelSubscriptionSale` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeChannelSubscriptionSale(value: StarTransactionTypeChannelSubscriptionSale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeChannelSubscriptionSale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['subscription_period'] = value.subscription_period;
  return out;
}

/** starTransactionTypeFragmentDeposit = StarTransactionType — The transaction is a deposit of Telegram Stars from Fragment; relevant for regular users and bots only */
export class StarTransactionTypeFragmentDeposit {
  type: 'starTransactionTypeFragmentDeposit' = 'starTransactionTypeFragmentDeposit';
  extra?: string;
}

/** Decode a TDLib `starTransactionTypeFragmentDeposit` object; null input -> null. */
export function decodeStarTransactionTypeFragmentDeposit(json: TdJsonRaw): StarTransactionTypeFragmentDeposit | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeFragmentDeposit = new StarTransactionTypeFragmentDeposit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `starTransactionTypeFragmentDeposit` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeFragmentDeposit(value: StarTransactionTypeFragmentDeposit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeFragmentDeposit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** starTransactionTypeFragmentWithdrawal = StarTransactionType — The transaction is a withdrawal of earned Telegram Stars to Fragment; relevant for regular users, bots, supergroup and channel chats only */
export class StarTransactionTypeFragmentWithdrawal {
  type: 'starTransactionTypeFragmentWithdrawal' = 'starTransactionTypeFragmentWithdrawal';
  extra?: string;
  /** withdrawal_state: RevenueWithdrawalState — State of the withdrawal; may be null for refunds from Fragment */
  withdrawal_state: TdRevenueWithdrawalState | null = null;
}

/** Decode a TDLib `starTransactionTypeFragmentWithdrawal` object; null input -> null. */
export function decodeStarTransactionTypeFragmentWithdrawal(json: TdJsonRaw): StarTransactionTypeFragmentWithdrawal | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeFragmentWithdrawal = new StarTransactionTypeFragmentWithdrawal();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.withdrawal_state = decodeTdRevenueWithdrawalState(record['withdrawal_state']);
  return out;
}

/** Encode a `starTransactionTypeFragmentWithdrawal` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeFragmentWithdrawal(value: StarTransactionTypeFragmentWithdrawal): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeFragmentWithdrawal';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['withdrawal_state'] = value.withdrawal_state === null ? null : encodeTdRevenueWithdrawalState(value.withdrawal_state);
  return out;
}

/** starTransactionTypeGiftAuctionBid = StarTransactionType — The transaction is a bid on a gift auction; relevant for regular users only */
export class StarTransactionTypeGiftAuctionBid {
  type: 'starTransactionTypeGiftAuctionBid' = 'starTransactionTypeGiftAuctionBid';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user who will receive the gift */
  owner_id: TdMessageSender | null = null;
  /** gift: gift — The gift */
  gift: Gift | null = null;
}

/** Decode a TDLib `starTransactionTypeGiftAuctionBid` object; null input -> null. */
export function decodeStarTransactionTypeGiftAuctionBid(json: TdJsonRaw): StarTransactionTypeGiftAuctionBid | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGiftAuctionBid = new StarTransactionTypeGiftAuctionBid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.gift = decodeGift(record['gift']);
  return out;
}

/** Encode a `starTransactionTypeGiftAuctionBid` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGiftAuctionBid(value: StarTransactionTypeGiftAuctionBid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGiftAuctionBid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  return out;
}

/** starTransactionTypeGiftOriginalDetailsDrop = StarTransactionType — The transaction is a drop of original details of an upgraded gift; relevant for regular users only */
export class StarTransactionTypeGiftOriginalDetailsDrop {
  type: 'starTransactionTypeGiftOriginalDetailsDrop' = 'starTransactionTypeGiftOriginalDetailsDrop';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel that owns the gift */
  owner_id: TdMessageSender | null = null;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
}

/** Decode a TDLib `starTransactionTypeGiftOriginalDetailsDrop` object; null input -> null. */
export function decodeStarTransactionTypeGiftOriginalDetailsDrop(json: TdJsonRaw): StarTransactionTypeGiftOriginalDetailsDrop | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGiftOriginalDetailsDrop = new StarTransactionTypeGiftOriginalDetailsDrop();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.gift = decodeUpgradedGift(record['gift']);
  return out;
}

/** Encode a `starTransactionTypeGiftOriginalDetailsDrop` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGiftOriginalDetailsDrop(value: StarTransactionTypeGiftOriginalDetailsDrop): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGiftOriginalDetailsDrop';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  return out;
}

/** starTransactionTypeGiftPurchase = StarTransactionType — The transaction is a purchase of a regular gift; relevant for regular users and bots only */
export class StarTransactionTypeGiftPurchase {
  type: 'starTransactionTypeGiftPurchase' = 'starTransactionTypeGiftPurchase';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel that received the gift */
  owner_id: TdMessageSender | null = null;
  /** gift: gift — The gift */
  gift: Gift | null = null;
}

/** Decode a TDLib `starTransactionTypeGiftPurchase` object; null input -> null. */
export function decodeStarTransactionTypeGiftPurchase(json: TdJsonRaw): StarTransactionTypeGiftPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGiftPurchase = new StarTransactionTypeGiftPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.gift = decodeGift(record['gift']);
  return out;
}

/** Encode a `starTransactionTypeGiftPurchase` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGiftPurchase(value: StarTransactionTypeGiftPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGiftPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  return out;
}

/** starTransactionTypeGiftPurchaseOffer = StarTransactionType — The transaction is an offer of gift purchase; relevant for regular users only */
export class StarTransactionTypeGiftPurchaseOffer {
  type: 'starTransactionTypeGiftPurchaseOffer' = 'starTransactionTypeGiftPurchaseOffer';
  extra?: string;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
}

/** Decode a TDLib `starTransactionTypeGiftPurchaseOffer` object; null input -> null. */
export function decodeStarTransactionTypeGiftPurchaseOffer(json: TdJsonRaw): StarTransactionTypeGiftPurchaseOffer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGiftPurchaseOffer = new StarTransactionTypeGiftPurchaseOffer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  return out;
}

/** Encode a `starTransactionTypeGiftPurchaseOffer` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGiftPurchaseOffer(value: StarTransactionTypeGiftPurchaseOffer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGiftPurchaseOffer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  return out;
}

/** starTransactionTypeGiftSale = StarTransactionType — The transaction is a sale of a received gift; relevant for regular users and channel chats only */
export class StarTransactionTypeGiftSale {
  type: 'starTransactionTypeGiftSale' = 'starTransactionTypeGiftSale';
  extra?: string;
  /** user_id: int53 — Identifier of the user who sent the gift */
  user_id: number = 0;
  /** gift: gift — The gift */
  gift: Gift | null = null;
}

/** Decode a TDLib `starTransactionTypeGiftSale` object; null input -> null. */
export function decodeStarTransactionTypeGiftSale(json: TdJsonRaw): StarTransactionTypeGiftSale | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGiftSale = new StarTransactionTypeGiftSale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.gift = decodeGift(record['gift']);
  return out;
}

/** Encode a `starTransactionTypeGiftSale` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGiftSale(value: StarTransactionTypeGiftSale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGiftSale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  return out;
}

/** starTransactionTypeGiftTransfer = StarTransactionType — The transaction is a transfer of an upgraded gift; relevant for regular users only */
export class StarTransactionTypeGiftTransfer {
  type: 'starTransactionTypeGiftTransfer' = 'starTransactionTypeGiftTransfer';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel that received the gift */
  owner_id: TdMessageSender | null = null;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
}

/** Decode a TDLib `starTransactionTypeGiftTransfer` object; null input -> null. */
export function decodeStarTransactionTypeGiftTransfer(json: TdJsonRaw): StarTransactionTypeGiftTransfer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGiftTransfer = new StarTransactionTypeGiftTransfer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.gift = decodeUpgradedGift(record['gift']);
  return out;
}

/** Encode a `starTransactionTypeGiftTransfer` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGiftTransfer(value: StarTransactionTypeGiftTransfer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGiftTransfer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  return out;
}

/** starTransactionTypeGiftUpgrade = StarTransactionType — The transaction is an upgrade of a gift; relevant for regular users only */
export class StarTransactionTypeGiftUpgrade {
  type: 'starTransactionTypeGiftUpgrade' = 'starTransactionTypeGiftUpgrade';
  extra?: string;
  /** user_id: int53 — Identifier of the user who initially sent the gift */
  user_id: number = 0;
  /** gift: upgradedGift — The upgraded gift */
  gift: UpgradedGift | null = null;
}

/** Decode a TDLib `starTransactionTypeGiftUpgrade` object; null input -> null. */
export function decodeStarTransactionTypeGiftUpgrade(json: TdJsonRaw): StarTransactionTypeGiftUpgrade | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGiftUpgrade = new StarTransactionTypeGiftUpgrade();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.gift = decodeUpgradedGift(record['gift']);
  return out;
}

/** Encode a `starTransactionTypeGiftUpgrade` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGiftUpgrade(value: StarTransactionTypeGiftUpgrade): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGiftUpgrade';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  return out;
}

/** starTransactionTypeGiftUpgradePurchase = StarTransactionType — The transaction is a purchase of an upgrade of a gift owned by another user or channel; relevant for regular users only */
export class StarTransactionTypeGiftUpgradePurchase {
  type: 'starTransactionTypeGiftUpgradePurchase' = 'starTransactionTypeGiftUpgradePurchase';
  extra?: string;
  /** owner_id: MessageSender — Owner of the upgraded gift */
  owner_id: TdMessageSender | null = null;
  /** gift: gift — The gift */
  gift: Gift | null = null;
}

/** Decode a TDLib `starTransactionTypeGiftUpgradePurchase` object; null input -> null. */
export function decodeStarTransactionTypeGiftUpgradePurchase(json: TdJsonRaw): StarTransactionTypeGiftUpgradePurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGiftUpgradePurchase = new StarTransactionTypeGiftUpgradePurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.gift = decodeGift(record['gift']);
  return out;
}

/** Encode a `starTransactionTypeGiftUpgradePurchase` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGiftUpgradePurchase(value: StarTransactionTypeGiftUpgradePurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGiftUpgradePurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  return out;
}

/** starTransactionTypeGiveawayDeposit = StarTransactionType — The transaction is a deposit of Telegram Stars from a giveaway; relevant for regular users only */
export class StarTransactionTypeGiveawayDeposit {
  type: 'starTransactionTypeGiveawayDeposit' = 'starTransactionTypeGiveawayDeposit';
  extra?: string;
  /** chat_id: int53 — Identifier of a supergroup or a channel chat that created the giveaway */
  chat_id: number = 0;
  /** giveaway_message_id: int53 — Identifier of the message with the giveaway; may be 0 or an identifier of a deleted message */
  giveaway_message_id: number = 0;
}

/** Decode a TDLib `starTransactionTypeGiveawayDeposit` object; null input -> null. */
export function decodeStarTransactionTypeGiveawayDeposit(json: TdJsonRaw): StarTransactionTypeGiveawayDeposit | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGiveawayDeposit = new StarTransactionTypeGiveawayDeposit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.giveaway_message_id = asNumber(record['giveaway_message_id']);
  return out;
}

/** Encode a `starTransactionTypeGiveawayDeposit` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGiveawayDeposit(value: StarTransactionTypeGiveawayDeposit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGiveawayDeposit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['giveaway_message_id'] = value.giveaway_message_id;
  return out;
}

/** starTransactionTypeGooglePlayDeposit = StarTransactionType — The transaction is a deposit of Telegram Stars from Google Play; relevant for regular users only */
export class StarTransactionTypeGooglePlayDeposit {
  type: 'starTransactionTypeGooglePlayDeposit' = 'starTransactionTypeGooglePlayDeposit';
  extra?: string;
}

/** Decode a TDLib `starTransactionTypeGooglePlayDeposit` object; null input -> null. */
export function decodeStarTransactionTypeGooglePlayDeposit(json: TdJsonRaw): StarTransactionTypeGooglePlayDeposit | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeGooglePlayDeposit = new StarTransactionTypeGooglePlayDeposit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `starTransactionTypeGooglePlayDeposit` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeGooglePlayDeposit(value: StarTransactionTypeGooglePlayDeposit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeGooglePlayDeposit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** starTransactionTypePaidGroupCallMessageReceive = StarTransactionType — The transaction is a receiving of a paid group call message; relevant for regular users and channel chats only */
export class StarTransactionTypePaidGroupCallMessageReceive {
  type: 'starTransactionTypePaidGroupCallMessageReceive' = 'starTransactionTypePaidGroupCallMessageReceive';
  extra?: string;
  /** sender_id: MessageSender — Identifier of the sender of the message */
  sender_id: TdMessageSender | null = null;
  /** commission_per_mille: int32 — The number of Telegram Stars received by the Telegram for each 1000 Telegram Stars paid for message sending */
  commission_per_mille: number = 0;
  /** commission_star_amount: starAmount — The Telegram Star amount that was received by Telegram; can be negative for refunds */
  commission_star_amount: StarAmount | null = null;
}

/** Decode a TDLib `starTransactionTypePaidGroupCallMessageReceive` object; null input -> null. */
export function decodeStarTransactionTypePaidGroupCallMessageReceive(json: TdJsonRaw): StarTransactionTypePaidGroupCallMessageReceive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypePaidGroupCallMessageReceive = new StarTransactionTypePaidGroupCallMessageReceive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.commission_per_mille = asNumber(record['commission_per_mille']);
  out.commission_star_amount = decodeStarAmount(record['commission_star_amount']);
  return out;
}

/** Encode a `starTransactionTypePaidGroupCallMessageReceive` into TDLib JSON wire shape. */
export function encodeStarTransactionTypePaidGroupCallMessageReceive(value: StarTransactionTypePaidGroupCallMessageReceive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypePaidGroupCallMessageReceive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['commission_per_mille'] = value.commission_per_mille;
  out['commission_star_amount'] = value.commission_star_amount === null ? null : encodeStarAmount(value.commission_star_amount);
  return out;
}

/** starTransactionTypePaidGroupCallMessageSend = StarTransactionType — The transaction is a sending of a paid group call message; relevant for regular users only */
export class StarTransactionTypePaidGroupCallMessageSend {
  type: 'starTransactionTypePaidGroupCallMessageSend' = 'starTransactionTypePaidGroupCallMessageSend';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that received the payment */
  chat_id: number = 0;
}

/** Decode a TDLib `starTransactionTypePaidGroupCallMessageSend` object; null input -> null. */
export function decodeStarTransactionTypePaidGroupCallMessageSend(json: TdJsonRaw): StarTransactionTypePaidGroupCallMessageSend | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypePaidGroupCallMessageSend = new StarTransactionTypePaidGroupCallMessageSend();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `starTransactionTypePaidGroupCallMessageSend` into TDLib JSON wire shape. */
export function encodeStarTransactionTypePaidGroupCallMessageSend(value: StarTransactionTypePaidGroupCallMessageSend): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypePaidGroupCallMessageSend';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** starTransactionTypePaidGroupCallReactionReceive = StarTransactionType — The transaction is a receiving of a paid group call reaction; relevant for regular users and channel chats only */
export class StarTransactionTypePaidGroupCallReactionReceive {
  type: 'starTransactionTypePaidGroupCallReactionReceive' = 'starTransactionTypePaidGroupCallReactionReceive';
  extra?: string;
  /** sender_id: MessageSender — Identifier of the sender of the reaction */
  sender_id: TdMessageSender | null = null;
  /** commission_per_mille: int32 — The number of Telegram Stars received by the Telegram for each 1000 Telegram Stars paid for reaction sending */
  commission_per_mille: number = 0;
  /** commission_star_amount: starAmount — The Telegram Star amount that was received by Telegram; can be negative for refunds */
  commission_star_amount: StarAmount | null = null;
}

/** Decode a TDLib `starTransactionTypePaidGroupCallReactionReceive` object; null input -> null. */
export function decodeStarTransactionTypePaidGroupCallReactionReceive(json: TdJsonRaw): StarTransactionTypePaidGroupCallReactionReceive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypePaidGroupCallReactionReceive = new StarTransactionTypePaidGroupCallReactionReceive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.commission_per_mille = asNumber(record['commission_per_mille']);
  out.commission_star_amount = decodeStarAmount(record['commission_star_amount']);
  return out;
}

/** Encode a `starTransactionTypePaidGroupCallReactionReceive` into TDLib JSON wire shape. */
export function encodeStarTransactionTypePaidGroupCallReactionReceive(value: StarTransactionTypePaidGroupCallReactionReceive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypePaidGroupCallReactionReceive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['commission_per_mille'] = value.commission_per_mille;
  out['commission_star_amount'] = value.commission_star_amount === null ? null : encodeStarAmount(value.commission_star_amount);
  return out;
}

/** starTransactionTypePaidGroupCallReactionSend = StarTransactionType — The transaction is a sending of a paid group reaction; relevant for regular users only */
export class StarTransactionTypePaidGroupCallReactionSend {
  type: 'starTransactionTypePaidGroupCallReactionSend' = 'starTransactionTypePaidGroupCallReactionSend';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that received the payment */
  chat_id: number = 0;
}

/** Decode a TDLib `starTransactionTypePaidGroupCallReactionSend` object; null input -> null. */
export function decodeStarTransactionTypePaidGroupCallReactionSend(json: TdJsonRaw): StarTransactionTypePaidGroupCallReactionSend | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypePaidGroupCallReactionSend = new StarTransactionTypePaidGroupCallReactionSend();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `starTransactionTypePaidGroupCallReactionSend` into TDLib JSON wire shape. */
export function encodeStarTransactionTypePaidGroupCallReactionSend(value: StarTransactionTypePaidGroupCallReactionSend): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypePaidGroupCallReactionSend';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** starTransactionTypePaidMessageReceive = StarTransactionType — The transaction is a receiving of a paid message; relevant for regular users, supergroup and channel chats only */
export class StarTransactionTypePaidMessageReceive {
  type: 'starTransactionTypePaidMessageReceive' = 'starTransactionTypePaidMessageReceive';
  extra?: string;
  /** sender_id: MessageSender — Identifier of the sender of the message */
  sender_id: TdMessageSender | null = null;
  /** message_count: int32 — Number of received paid messages */
  message_count: number = 0;
  /** commission_per_mille: int32 — The number of Telegram Stars received by the Telegram for each 1000 Telegram Stars paid for message sending */
  commission_per_mille: number = 0;
  /** commission_star_amount: starAmount — The Telegram Star amount that was received by Telegram; can be negative for refunds */
  commission_star_amount: StarAmount | null = null;
}

/** Decode a TDLib `starTransactionTypePaidMessageReceive` object; null input -> null. */
export function decodeStarTransactionTypePaidMessageReceive(json: TdJsonRaw): StarTransactionTypePaidMessageReceive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypePaidMessageReceive = new StarTransactionTypePaidMessageReceive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.message_count = asNumber(record['message_count']);
  out.commission_per_mille = asNumber(record['commission_per_mille']);
  out.commission_star_amount = decodeStarAmount(record['commission_star_amount']);
  return out;
}

/** Encode a `starTransactionTypePaidMessageReceive` into TDLib JSON wire shape. */
export function encodeStarTransactionTypePaidMessageReceive(value: StarTransactionTypePaidMessageReceive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypePaidMessageReceive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['message_count'] = value.message_count;
  out['commission_per_mille'] = value.commission_per_mille;
  out['commission_star_amount'] = value.commission_star_amount === null ? null : encodeStarAmount(value.commission_star_amount);
  return out;
}

/** starTransactionTypePaidMessageSend = StarTransactionType — The transaction is a sending of a paid message; relevant for regular users only */
export class StarTransactionTypePaidMessageSend {
  type: 'starTransactionTypePaidMessageSend' = 'starTransactionTypePaidMessageSend';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that received the payment */
  chat_id: number = 0;
  /** message_count: int32 — Number of sent paid messages */
  message_count: number = 0;
}

/** Decode a TDLib `starTransactionTypePaidMessageSend` object; null input -> null. */
export function decodeStarTransactionTypePaidMessageSend(json: TdJsonRaw): StarTransactionTypePaidMessageSend | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypePaidMessageSend = new StarTransactionTypePaidMessageSend();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_count = asNumber(record['message_count']);
  return out;
}

/** Encode a `starTransactionTypePaidMessageSend` into TDLib JSON wire shape. */
export function encodeStarTransactionTypePaidMessageSend(value: StarTransactionTypePaidMessageSend): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypePaidMessageSend';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_count'] = value.message_count;
  return out;
}

/** starTransactionTypePremiumBotDeposit = StarTransactionType — The transaction is a deposit of Telegram Stars from the Premium bot; relevant for regular users only */
export class StarTransactionTypePremiumBotDeposit {
  type: 'starTransactionTypePremiumBotDeposit' = 'starTransactionTypePremiumBotDeposit';
  extra?: string;
}

/** Decode a TDLib `starTransactionTypePremiumBotDeposit` object; null input -> null. */
export function decodeStarTransactionTypePremiumBotDeposit(json: TdJsonRaw): StarTransactionTypePremiumBotDeposit | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypePremiumBotDeposit = new StarTransactionTypePremiumBotDeposit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `starTransactionTypePremiumBotDeposit` into TDLib JSON wire shape. */
export function encodeStarTransactionTypePremiumBotDeposit(value: StarTransactionTypePremiumBotDeposit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypePremiumBotDeposit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** starTransactionTypePremiumPurchase = StarTransactionType — The transaction is a purchase of Telegram Premium subscription; relevant for regular users and bots only */
export class StarTransactionTypePremiumPurchase {
  type: 'starTransactionTypePremiumPurchase' = 'starTransactionTypePremiumPurchase';
  extra?: string;
  /** user_id: int53 — Identifier of the user who received the Telegram Premium subscription */
  user_id: number = 0;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active */
  month_count: number = 0;
  /** sticker: sticker — A sticker to be shown in the transaction information; may be null if unknown */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `starTransactionTypePremiumPurchase` object; null input -> null. */
export function decodeStarTransactionTypePremiumPurchase(json: TdJsonRaw): StarTransactionTypePremiumPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypePremiumPurchase = new StarTransactionTypePremiumPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.month_count = asNumber(record['month_count']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `starTransactionTypePremiumPurchase` into TDLib JSON wire shape. */
export function encodeStarTransactionTypePremiumPurchase(value: StarTransactionTypePremiumPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypePremiumPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['month_count'] = value.month_count;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** starTransactionTypePublicPostSearch = StarTransactionType — The transaction is a payment for search of posts in public Telegram channels; relevant for regular users only */
export class StarTransactionTypePublicPostSearch {
  type: 'starTransactionTypePublicPostSearch' = 'starTransactionTypePublicPostSearch';
  extra?: string;
}

/** Decode a TDLib `starTransactionTypePublicPostSearch` object; null input -> null. */
export function decodeStarTransactionTypePublicPostSearch(json: TdJsonRaw): StarTransactionTypePublicPostSearch | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypePublicPostSearch = new StarTransactionTypePublicPostSearch();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `starTransactionTypePublicPostSearch` into TDLib JSON wire shape. */
export function encodeStarTransactionTypePublicPostSearch(value: StarTransactionTypePublicPostSearch): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypePublicPostSearch';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** starTransactionTypeSuggestedPostPaymentReceive = StarTransactionType — The transaction is a receiving of a payment for a suggested post by the channel chat; relevant for channel chats only */
export class StarTransactionTypeSuggestedPostPaymentReceive {
  type: 'starTransactionTypeSuggestedPostPaymentReceive' = 'starTransactionTypeSuggestedPostPaymentReceive';
  extra?: string;
  /** user_id: int53 — Identifier of the user who paid for the suggested post */
  user_id: number = 0;
}

/** Decode a TDLib `starTransactionTypeSuggestedPostPaymentReceive` object; null input -> null. */
export function decodeStarTransactionTypeSuggestedPostPaymentReceive(json: TdJsonRaw): StarTransactionTypeSuggestedPostPaymentReceive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeSuggestedPostPaymentReceive = new StarTransactionTypeSuggestedPostPaymentReceive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `starTransactionTypeSuggestedPostPaymentReceive` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeSuggestedPostPaymentReceive(value: StarTransactionTypeSuggestedPostPaymentReceive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeSuggestedPostPaymentReceive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** starTransactionTypeSuggestedPostPaymentSend = StarTransactionType — The transaction is a payment for a suggested post; relevant for regular users only */
export class StarTransactionTypeSuggestedPostPaymentSend {
  type: 'starTransactionTypeSuggestedPostPaymentSend' = 'starTransactionTypeSuggestedPostPaymentSend';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat that posted the post */
  chat_id: number = 0;
}

/** Decode a TDLib `starTransactionTypeSuggestedPostPaymentSend` object; null input -> null. */
export function decodeStarTransactionTypeSuggestedPostPaymentSend(json: TdJsonRaw): StarTransactionTypeSuggestedPostPaymentSend | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeSuggestedPostPaymentSend = new StarTransactionTypeSuggestedPostPaymentSend();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `starTransactionTypeSuggestedPostPaymentSend` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeSuggestedPostPaymentSend(value: StarTransactionTypeSuggestedPostPaymentSend): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeSuggestedPostPaymentSend';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** starTransactionTypeTelegramAdsWithdrawal = StarTransactionType — The transaction is a withdrawal of earned Telegram Stars to Telegram Ad platform; relevant for bots and channel chats only */
export class StarTransactionTypeTelegramAdsWithdrawal {
  type: 'starTransactionTypeTelegramAdsWithdrawal' = 'starTransactionTypeTelegramAdsWithdrawal';
  extra?: string;
}

/** Decode a TDLib `starTransactionTypeTelegramAdsWithdrawal` object; null input -> null. */
export function decodeStarTransactionTypeTelegramAdsWithdrawal(json: TdJsonRaw): StarTransactionTypeTelegramAdsWithdrawal | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeTelegramAdsWithdrawal = new StarTransactionTypeTelegramAdsWithdrawal();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `starTransactionTypeTelegramAdsWithdrawal` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeTelegramAdsWithdrawal(value: StarTransactionTypeTelegramAdsWithdrawal): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeTelegramAdsWithdrawal';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** starTransactionTypeTelegramApiUsage = StarTransactionType — The transaction is a payment for Telegram API usage; relevant for bots only */
export class StarTransactionTypeTelegramApiUsage {
  type: 'starTransactionTypeTelegramApiUsage' = 'starTransactionTypeTelegramApiUsage';
  extra?: string;
  /** request_count: int32 — The number of billed requests */
  request_count: number = 0;
}

/** Decode a TDLib `starTransactionTypeTelegramApiUsage` object; null input -> null. */
export function decodeStarTransactionTypeTelegramApiUsage(json: TdJsonRaw): StarTransactionTypeTelegramApiUsage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeTelegramApiUsage = new StarTransactionTypeTelegramApiUsage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.request_count = asNumber(record['request_count']);
  return out;
}

/** Encode a `starTransactionTypeTelegramApiUsage` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeTelegramApiUsage(value: StarTransactionTypeTelegramApiUsage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeTelegramApiUsage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['request_count'] = value.request_count;
  return out;
}

/** starTransactionTypeUnsupported = StarTransactionType — The transaction is a transaction of an unsupported type */
export class StarTransactionTypeUnsupported {
  type: 'starTransactionTypeUnsupported' = 'starTransactionTypeUnsupported';
  extra?: string;
}

/** Decode a TDLib `starTransactionTypeUnsupported` object; null input -> null. */
export function decodeStarTransactionTypeUnsupported(json: TdJsonRaw): StarTransactionTypeUnsupported | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeUnsupported = new StarTransactionTypeUnsupported();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `starTransactionTypeUnsupported` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeUnsupported(value: StarTransactionTypeUnsupported): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeUnsupported';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** starTransactionTypeUpgradedGiftPurchase = StarTransactionType — The transaction is a purchase of an upgraded gift for some user or channel; relevant for regular users only */
export class StarTransactionTypeUpgradedGiftPurchase {
  type: 'starTransactionTypeUpgradedGiftPurchase' = 'starTransactionTypeUpgradedGiftPurchase';
  extra?: string;
  /** user_id: int53 — Identifier of the user who sold the gift */
  user_id: number = 0;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
}

/** Decode a TDLib `starTransactionTypeUpgradedGiftPurchase` object; null input -> null. */
export function decodeStarTransactionTypeUpgradedGiftPurchase(json: TdJsonRaw): StarTransactionTypeUpgradedGiftPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeUpgradedGiftPurchase = new StarTransactionTypeUpgradedGiftPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.gift = decodeUpgradedGift(record['gift']);
  return out;
}

/** Encode a `starTransactionTypeUpgradedGiftPurchase` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeUpgradedGiftPurchase(value: StarTransactionTypeUpgradedGiftPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeUpgradedGiftPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  return out;
}

/** starTransactionTypeUpgradedGiftSale = StarTransactionType — The transaction is a sale of an upgraded gift; relevant for regular users only */
export class StarTransactionTypeUpgradedGiftSale {
  type: 'starTransactionTypeUpgradedGiftSale' = 'starTransactionTypeUpgradedGiftSale';
  extra?: string;
  /** user_id: int53 — Identifier of the user who bought the gift */
  user_id: number = 0;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
  /** commission_per_mille: int32 — The number of Telegram Stars received by the Telegram for each 1000 Telegram Stars received by the seller of the gift */
  commission_per_mille: number = 0;
  /** commission_star_amount: starAmount — The Telegram Star amount that was received by Telegram; can be negative for refunds */
  commission_star_amount: StarAmount | null = null;
  /** via_offer: Bool — True, if the gift was sold through a purchase offer */
  via_offer: boolean = false;
}

/** Decode a TDLib `starTransactionTypeUpgradedGiftSale` object; null input -> null. */
export function decodeStarTransactionTypeUpgradedGiftSale(json: TdJsonRaw): StarTransactionTypeUpgradedGiftSale | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeUpgradedGiftSale = new StarTransactionTypeUpgradedGiftSale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.gift = decodeUpgradedGift(record['gift']);
  out.commission_per_mille = asNumber(record['commission_per_mille']);
  out.commission_star_amount = decodeStarAmount(record['commission_star_amount']);
  out.via_offer = asBool(record['via_offer']);
  return out;
}

/** Encode a `starTransactionTypeUpgradedGiftSale` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeUpgradedGiftSale(value: StarTransactionTypeUpgradedGiftSale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeUpgradedGiftSale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  out['commission_per_mille'] = value.commission_per_mille;
  out['commission_star_amount'] = value.commission_star_amount === null ? null : encodeStarAmount(value.commission_star_amount);
  out['via_offer'] = value.via_offer;
  return out;
}

/** starTransactionTypeUserDeposit = StarTransactionType — The transaction is a deposit of Telegram Stars by another user; relevant for regular users only */
export class StarTransactionTypeUserDeposit {
  type: 'starTransactionTypeUserDeposit' = 'starTransactionTypeUserDeposit';
  extra?: string;
  /** user_id: int53 — Identifier of the user who gifted Telegram Stars; 0 if the user was anonymous */
  user_id: number = 0;
  /** sticker: sticker — The sticker to be shown in the transaction information; may be null if unknown */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `starTransactionTypeUserDeposit` object; null input -> null. */
export function decodeStarTransactionTypeUserDeposit(json: TdJsonRaw): StarTransactionTypeUserDeposit | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactionTypeUserDeposit = new StarTransactionTypeUserDeposit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `starTransactionTypeUserDeposit` into TDLib JSON wire shape. */
export function encodeStarTransactionTypeUserDeposit(value: StarTransactionTypeUserDeposit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactionTypeUserDeposit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** starTransactions = StarTransactions — Represents a list of Telegram Star transactions */
export class StarTransactions {
  type: 'starTransactions' = 'starTransactions';
  extra?: string;
  /** star_amount: starAmount — The amount of owned Telegram Stars */
  star_amount: StarAmount | null = null;
  /** transactions: vector<starTransaction> — List of transactions with Telegram Stars */
  transactions: StarTransaction[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `starTransactions` object; null input -> null. */
export function decodeStarTransactions(json: TdJsonRaw): StarTransactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StarTransactions = new StarTransactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_amount = decodeStarAmount(record['star_amount']);
  out.transactions = decodeVector(record['transactions'], (item: TdJsonRaw): StarTransaction => decodeStarTransaction(item) as StarTransaction);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `starTransactions` into TDLib JSON wire shape. */
export function encodeStarTransactions(value: StarTransactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'starTransactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_amount'] = value.star_amount === null ? null : encodeStarAmount(value.star_amount);
  out['transactions'] = encodeVector(value.transactions, (item: StarTransaction | null): TdJsonRaw => item === null ? null : encodeStarTransaction(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** startGroupCallRecording = Ok — Starts recording of an active group call; for video chats only. Requires groupCall.can_be_managed right */
export class StartGroupCallRecording {
  type: 'startGroupCallRecording' = 'startGroupCallRecording';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** title: string — Group call recording title; 0-64 characters */
  title: string = '';
  /** record_video: Bool — Pass true to record a video file instead of an audio file */
  record_video: boolean = false;
  /** use_portrait_orientation: Bool — Pass true to use portrait orientation for video instead of landscape one */
  use_portrait_orientation: boolean = false;
}

/** Decode a TDLib `startGroupCallRecording` object; null input -> null. */
export function decodeStartGroupCallRecording(json: TdJsonRaw): StartGroupCallRecording | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StartGroupCallRecording = new StartGroupCallRecording();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.title = asString(record['title']);
  out.record_video = asBool(record['record_video']);
  out.use_portrait_orientation = asBool(record['use_portrait_orientation']);
  return out;
}

/** Encode a `startGroupCallRecording` into TDLib JSON wire shape. */
export function encodeStartGroupCallRecording(value: StartGroupCallRecording): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'startGroupCallRecording';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['title'] = value.title;
  out['record_video'] = value.record_video;
  out['use_portrait_orientation'] = value.use_portrait_orientation;
  return out;
}

/** startGroupCallScreenSharing = Text — Starts screen sharing in a joined group call; not supported in live stories. Returns join response payload for tgcalls */
export class StartGroupCallScreenSharing {
  type: 'startGroupCallScreenSharing' = 'startGroupCallScreenSharing';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** audio_source_id: int32 — Screen sharing audio channel synchronization source identifier; received from tgcalls */
  audio_source_id: number = 0;
  /** payload: string — Group call join payload; received from tgcalls */
  payload: string = '';
}

/** Decode a TDLib `startGroupCallScreenSharing` object; null input -> null. */
export function decodeStartGroupCallScreenSharing(json: TdJsonRaw): StartGroupCallScreenSharing | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StartGroupCallScreenSharing = new StartGroupCallScreenSharing();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.audio_source_id = asNumber(record['audio_source_id']);
  out.payload = asString(record['payload']);
  return out;
}

/** Encode a `startGroupCallScreenSharing` into TDLib JSON wire shape. */
export function encodeStartGroupCallScreenSharing(value: StartGroupCallScreenSharing): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'startGroupCallScreenSharing';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['audio_source_id'] = value.audio_source_id;
  out['payload'] = value.payload;
  return out;
}

/** startLiveStory = StartLiveStoryResult — Starts a new live story on behalf of a chat; requires can_post_stories administrator right for channel chats */
export class StartLiveStory {
  type: 'startLiveStory' = 'startLiveStory';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that will start the live story. Pass Saved Messages chat identifier when starting a live story on behalf of the current user, or a channel chat identifier */
  chat_id: number = 0;
  /** privacy_settings: StoryPrivacySettings — The privacy settings for the story; ignored for stories posted on behalf of channel chats */
  privacy_settings: TdStoryPrivacySettings | null = null;
  /** protect_content: Bool — Pass true if the content of the story must be protected from screenshotting */
  protect_content: boolean = false;
  /** is_rtmp_stream: Bool — Pass true to create an RTMP stream instead of an ordinary group call */
  is_rtmp_stream: boolean = false;
  /** enable_messages: Bool — Pass true to allow viewers of the story to send messages */
  enable_messages: boolean = false;
  /** paid_message_star_count: int53 — The minimum number of Telegram Stars that must be paid by viewers for each sent message to the call; 0-getOption("paid_group_call_message_star_count_max") */
  paid_message_star_count: number = 0;
}

/** Decode a TDLib `startLiveStory` object; null input -> null. */
export function decodeStartLiveStory(json: TdJsonRaw): StartLiveStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StartLiveStory = new StartLiveStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.privacy_settings = decodeTdStoryPrivacySettings(record['privacy_settings']);
  out.protect_content = asBool(record['protect_content']);
  out.is_rtmp_stream = asBool(record['is_rtmp_stream']);
  out.enable_messages = asBool(record['enable_messages']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  return out;
}

/** Encode a `startLiveStory` into TDLib JSON wire shape. */
export function encodeStartLiveStory(value: StartLiveStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'startLiveStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['privacy_settings'] = value.privacy_settings === null ? null : encodeTdStoryPrivacySettings(value.privacy_settings);
  out['protect_content'] = value.protect_content;
  out['is_rtmp_stream'] = value.is_rtmp_stream;
  out['enable_messages'] = value.enable_messages;
  out['paid_message_star_count'] = value.paid_message_star_count;
  return out;
}

/** startLiveStoryResultFail = StartLiveStoryResult — The live story failed to post with an error to be handled */
export class StartLiveStoryResultFail {
  type: 'startLiveStoryResultFail' = 'startLiveStoryResultFail';
  extra?: string;
  /** error_type: CanPostStoryResult — Type of the error; other error types may be returned as regular errors */
  error_type: TdCanPostStoryResult | null = null;
}

/** Decode a TDLib `startLiveStoryResultFail` object; null input -> null. */
export function decodeStartLiveStoryResultFail(json: TdJsonRaw): StartLiveStoryResultFail | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StartLiveStoryResultFail = new StartLiveStoryResultFail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.error_type = decodeTdCanPostStoryResult(record['error_type']);
  return out;
}

/** Encode a `startLiveStoryResultFail` into TDLib JSON wire shape. */
export function encodeStartLiveStoryResultFail(value: StartLiveStoryResultFail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'startLiveStoryResultFail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['error_type'] = value.error_type === null ? null : encodeTdCanPostStoryResult(value.error_type);
  return out;
}

/** startLiveStoryResultOk = StartLiveStoryResult — The live story was successfully posted */
export class StartLiveStoryResultOk {
  type: 'startLiveStoryResultOk' = 'startLiveStoryResultOk';
  extra?: string;
  /** story: story — The live story */
  story: Story | null = null;
}

/** Decode a TDLib `startLiveStoryResultOk` object; null input -> null. */
export function decodeStartLiveStoryResultOk(json: TdJsonRaw): StartLiveStoryResultOk | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StartLiveStoryResultOk = new StartLiveStoryResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story = decodeStory(record['story']);
  return out;
}

/** Encode a `startLiveStoryResultOk` into TDLib JSON wire shape. */
export function encodeStartLiveStoryResultOk(value: StartLiveStoryResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'startLiveStoryResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story'] = value.story === null ? null : encodeStory(value.story);
  return out;
}

/** startScheduledVideoChat = Ok — Starts a scheduled video chat */
export class StartScheduledVideoChat {
  type: 'startScheduledVideoChat' = 'startScheduledVideoChat';
  extra?: string;
  /** group_call_id: int32 — Group call identifier of the video chat */
  group_call_id: number = 0;
}

/** Decode a TDLib `startScheduledVideoChat` object; null input -> null. */
export function decodeStartScheduledVideoChat(json: TdJsonRaw): StartScheduledVideoChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StartScheduledVideoChat = new StartScheduledVideoChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `startScheduledVideoChat` into TDLib JSON wire shape. */
export function encodeStartScheduledVideoChat(value: StartScheduledVideoChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'startScheduledVideoChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** statisticalGraphAsync = StatisticalGraph — The graph data to be asynchronously loaded through getStatisticalGraph */
export class StatisticalGraphAsync {
  type: 'statisticalGraphAsync' = 'statisticalGraphAsync';
  extra?: string;
  /** token: string — The token to use for data loading */
  token: string = '';
}

/** Decode a TDLib `statisticalGraphAsync` object; null input -> null. */
export function decodeStatisticalGraphAsync(json: TdJsonRaw): StatisticalGraphAsync | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StatisticalGraphAsync = new StatisticalGraphAsync();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `statisticalGraphAsync` into TDLib JSON wire shape. */
export function encodeStatisticalGraphAsync(value: StatisticalGraphAsync): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'statisticalGraphAsync';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** statisticalGraphData = StatisticalGraph — A graph data */
export class StatisticalGraphData {
  type: 'statisticalGraphData' = 'statisticalGraphData';
  extra?: string;
  /** json_data: string — Graph data in JSON format */
  json_data: string = '';
  /** zoom_token: string — If non-empty, a token which can be used to receive a zoomed in graph */
  zoom_token: string = '';
}

/** Decode a TDLib `statisticalGraphData` object; null input -> null. */
export function decodeStatisticalGraphData(json: TdJsonRaw): StatisticalGraphData | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StatisticalGraphData = new StatisticalGraphData();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.json_data = asString(record['json_data']);
  out.zoom_token = asString(record['zoom_token']);
  return out;
}

/** Encode a `statisticalGraphData` into TDLib JSON wire shape. */
export function encodeStatisticalGraphData(value: StatisticalGraphData): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'statisticalGraphData';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['json_data'] = value.json_data;
  out['zoom_token'] = value.zoom_token;
  return out;
}

/** statisticalGraphError = StatisticalGraph — An error message to be shown to the user instead of the graph */
export class StatisticalGraphError {
  type: 'statisticalGraphError' = 'statisticalGraphError';
  extra?: string;
  /** error_message: string — The error message */
  error_message: string = '';
}

/** Decode a TDLib `statisticalGraphError` object; null input -> null. */
export function decodeStatisticalGraphError(json: TdJsonRaw): StatisticalGraphError | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StatisticalGraphError = new StatisticalGraphError();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.error_message = asString(record['error_message']);
  return out;
}

/** Encode a `statisticalGraphError` into TDLib JSON wire shape. */
export function encodeStatisticalGraphError(value: StatisticalGraphError): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'statisticalGraphError';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['error_message'] = value.error_message;
  return out;
}

/** statisticalValue = StatisticalValue — A value with information about its recent changes */
export class StatisticalValue {
  type: 'statisticalValue' = 'statisticalValue';
  extra?: string;
  /** value: double — The current value */
  value: number = 0;
  /** previous_value: double — The value for the previous day */
  previous_value: number = 0;
  /** growth_rate_percentage: double — The growth rate of the value, as a percentage */
  growth_rate_percentage: number = 0;
}

/** Decode a TDLib `statisticalValue` object; null input -> null. */
export function decodeStatisticalValue(json: TdJsonRaw): StatisticalValue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StatisticalValue = new StatisticalValue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asNumber(record['value']);
  out.previous_value = asNumber(record['previous_value']);
  out.growth_rate_percentage = asNumber(record['growth_rate_percentage']);
  return out;
}

/** Encode a `statisticalValue` into TDLib JSON wire shape. */
export function encodeStatisticalValue(value: StatisticalValue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'statisticalValue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  out['previous_value'] = value.previous_value;
  out['growth_rate_percentage'] = value.growth_rate_percentage;
  return out;
}

/** sticker = Sticker — Describes a sticker */
export class Sticker {
  type: 'sticker' = 'sticker';
  extra?: string;
  /** id: int64 — Unique sticker identifier within the set; 0 if none */
  id: string = '';
  /** set_id: int64 — Identifier of the sticker set to which the sticker belongs; 0 if none */
  set_id: string = '';
  /** width: int32 — Sticker width; as defined by the sender */
  width: number = 0;
  /** height: int32 — Sticker height; as defined by the sender */
  height: number = 0;
  /** emoji: string — Emoji corresponding to the sticker; may be empty if unknown */
  emoji: string = '';
  /** format: StickerFormat — Sticker format */
  format: TdStickerFormat | null = null;
  /** full_type: StickerFullType — Sticker's full type */
  full_type: TdStickerFullType | null = null;
  /** thumbnail: thumbnail — Sticker thumbnail in WEBP or JPEG format; may be null */
  thumbnail: Thumbnail | null = null;
  /** sticker: file — File containing the sticker */
  sticker: File | null = null;
}

/** Decode a TDLib `sticker` object; null input -> null. */
export function decodeSticker(json: TdJsonRaw): Sticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Sticker = new Sticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.set_id = asInt64(record['set_id']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.emoji = asString(record['emoji']);
  out.format = decodeTdStickerFormat(record['format']);
  out.full_type = decodeTdStickerFullType(record['full_type']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  out.sticker = decodeFile(record['sticker']);
  return out;
}

/** Encode a `sticker` into TDLib JSON wire shape. */
export function encodeSticker(value: Sticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'sticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['set_id'] = value.set_id;
  out['width'] = value.width;
  out['height'] = value.height;
  out['emoji'] = value.emoji;
  out['format'] = value.format === null ? null : encodeTdStickerFormat(value.format);
  out['full_type'] = value.full_type === null ? null : encodeTdStickerFullType(value.full_type);
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  out['sticker'] = value.sticker === null ? null : encodeFile(value.sticker);
  return out;
}

/** stickerFormatTgs = StickerFormat — The sticker is an animation in TGS format */
export class StickerFormatTgs {
  type: 'stickerFormatTgs' = 'stickerFormatTgs';
  extra?: string;
}

/** Decode a TDLib `stickerFormatTgs` object; null input -> null. */
export function decodeStickerFormatTgs(json: TdJsonRaw): StickerFormatTgs | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerFormatTgs = new StickerFormatTgs();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `stickerFormatTgs` into TDLib JSON wire shape. */
export function encodeStickerFormatTgs(value: StickerFormatTgs): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerFormatTgs';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** stickerFormatWebm = StickerFormat — The sticker is a video in WEBM format */
export class StickerFormatWebm {
  type: 'stickerFormatWebm' = 'stickerFormatWebm';
  extra?: string;
}

/** Decode a TDLib `stickerFormatWebm` object; null input -> null. */
export function decodeStickerFormatWebm(json: TdJsonRaw): StickerFormatWebm | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerFormatWebm = new StickerFormatWebm();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `stickerFormatWebm` into TDLib JSON wire shape. */
export function encodeStickerFormatWebm(value: StickerFormatWebm): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerFormatWebm';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** stickerFormatWebp = StickerFormat — The sticker is an image in WEBP format */
export class StickerFormatWebp {
  type: 'stickerFormatWebp' = 'stickerFormatWebp';
  extra?: string;
}

/** Decode a TDLib `stickerFormatWebp` object; null input -> null. */
export function decodeStickerFormatWebp(json: TdJsonRaw): StickerFormatWebp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerFormatWebp = new StickerFormatWebp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `stickerFormatWebp` into TDLib JSON wire shape. */
export function encodeStickerFormatWebp(value: StickerFormatWebp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerFormatWebp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** stickerFullTypeCustomEmoji = StickerFullType — The sticker is a custom emoji to be used inside message text and caption. Currently, only Telegram Premium users can use custom emoji */
export class StickerFullTypeCustomEmoji {
  type: 'stickerFullTypeCustomEmoji' = 'stickerFullTypeCustomEmoji';
  extra?: string;
  /** custom_emoji_id: int64 — Identifier of the custom emoji */
  custom_emoji_id: string = '';
  /** needs_repainting: Bool — True, if the sticker must be repainted to a text color in messages, the color of the Telegram Premium badge in emoji status, white color on chat photos, or another appropriate color in other places */
  needs_repainting: boolean = false;
}

/** Decode a TDLib `stickerFullTypeCustomEmoji` object; null input -> null. */
export function decodeStickerFullTypeCustomEmoji(json: TdJsonRaw): StickerFullTypeCustomEmoji | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerFullTypeCustomEmoji = new StickerFullTypeCustomEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  out.needs_repainting = asBool(record['needs_repainting']);
  return out;
}

/** Encode a `stickerFullTypeCustomEmoji` into TDLib JSON wire shape. */
export function encodeStickerFullTypeCustomEmoji(value: StickerFullTypeCustomEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerFullTypeCustomEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_emoji_id'] = value.custom_emoji_id;
  out['needs_repainting'] = value.needs_repainting;
  return out;
}

/** stickerFullTypeMask = StickerFullType — The sticker is a mask in WEBP format to be placed on photos or videos */
export class StickerFullTypeMask {
  type: 'stickerFullTypeMask' = 'stickerFullTypeMask';
  extra?: string;
  /** mask_position: maskPosition — Position where the mask is placed; may be null */
  mask_position: MaskPosition | null = null;
}

/** Decode a TDLib `stickerFullTypeMask` object; null input -> null. */
export function decodeStickerFullTypeMask(json: TdJsonRaw): StickerFullTypeMask | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerFullTypeMask = new StickerFullTypeMask();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.mask_position = decodeMaskPosition(record['mask_position']);
  return out;
}

/** Encode a `stickerFullTypeMask` into TDLib JSON wire shape. */
export function encodeStickerFullTypeMask(value: StickerFullTypeMask): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerFullTypeMask';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['mask_position'] = value.mask_position === null ? null : encodeMaskPosition(value.mask_position);
  return out;
}

/** stickerFullTypeRegular = StickerFullType — The sticker is a regular sticker */
export class StickerFullTypeRegular {
  type: 'stickerFullTypeRegular' = 'stickerFullTypeRegular';
  extra?: string;
  /** premium_animation: file — Premium animation of the sticker; may be null. If present, only Telegram Premium users can use the sticker */
  premium_animation: File | null = null;
}

/** Decode a TDLib `stickerFullTypeRegular` object; null input -> null. */
export function decodeStickerFullTypeRegular(json: TdJsonRaw): StickerFullTypeRegular | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerFullTypeRegular = new StickerFullTypeRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.premium_animation = decodeFile(record['premium_animation']);
  return out;
}

/** Encode a `stickerFullTypeRegular` into TDLib JSON wire shape. */
export function encodeStickerFullTypeRegular(value: StickerFullTypeRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerFullTypeRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['premium_animation'] = value.premium_animation === null ? null : encodeFile(value.premium_animation);
  return out;
}

/** stickerSet = StickerSet — Represents a sticker set */
export class StickerSet {
  type: 'stickerSet' = 'stickerSet';
  extra?: string;
  /** id: int64 — Identifier of the sticker set */
  id: string = '';
  /** title: string — Title of the sticker set */
  title: string = '';
  /** name: string — Name of the sticker set */
  name: string = '';
  /** thumbnail: thumbnail — Sticker set thumbnail in WEBP, TGS, or WEBM format with width and height 100; may be null. The file can be downloaded only before the thumbnail is changed */
  thumbnail: Thumbnail | null = null;
  /** thumbnail_outline: outline — Sticker set thumbnail's outline; may be null if unknown */
  thumbnail_outline: Outline | null = null;
  /** is_owned: Bool — True, if the sticker set is owned by the current user */
  is_owned: boolean = false;
  /** is_installed: Bool — True, if the sticker set has been installed by the current user */
  is_installed: boolean = false;
  /** is_archived: Bool — True, if the sticker set has been archived. A sticker set can't be installed and archived simultaneously */
  is_archived: boolean = false;
  /** is_official: Bool — True, if the sticker set is official */
  is_official: boolean = false;
  /** sticker_type: StickerType — Type of the stickers in the set */
  sticker_type: TdStickerType | null = null;
  /** needs_repainting: Bool — True, if stickers in the sticker set are custom emoji that must be repainted; for custom emoji sticker sets only */
  needs_repainting: boolean = false;
  /** is_allowed_as_chat_emoji_status: Bool — True, if stickers in the sticker set are custom emoji that can be used as chat emoji status; for custom emoji sticker sets only */
  is_allowed_as_chat_emoji_status: boolean = false;
  /** is_viewed: Bool — True for already viewed trending sticker sets */
  is_viewed: boolean = false;
  /** stickers: vector<sticker> — List of stickers in this set */
  stickers: Sticker[] = [];
  /** emojis: vector<emojis> — A list of emojis corresponding to the stickers in the same order. The list is only for informational purposes, because a sticker is always sent with a fixed emoji from the corresponding Sticker object */
  emojis: Emojis[] = [];
}

/** Decode a TDLib `stickerSet` object; null input -> null. */
export function decodeStickerSet(json: TdJsonRaw): StickerSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerSet = new StickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.title = asString(record['title']);
  out.name = asString(record['name']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  out.thumbnail_outline = decodeOutline(record['thumbnail_outline']);
  out.is_owned = asBool(record['is_owned']);
  out.is_installed = asBool(record['is_installed']);
  out.is_archived = asBool(record['is_archived']);
  out.is_official = asBool(record['is_official']);
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.needs_repainting = asBool(record['needs_repainting']);
  out.is_allowed_as_chat_emoji_status = asBool(record['is_allowed_as_chat_emoji_status']);
  out.is_viewed = asBool(record['is_viewed']);
  out.stickers = decodeVector(record['stickers'], (item: TdJsonRaw): Sticker => decodeSticker(item) as Sticker);
  out.emojis = decodeVector(record['emojis'], (item: TdJsonRaw): Emojis => decodeEmojis(item) as Emojis);
  return out;
}

/** Encode a `stickerSet` into TDLib JSON wire shape. */
export function encodeStickerSet(value: StickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  out['name'] = value.name;
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  out['thumbnail_outline'] = value.thumbnail_outline === null ? null : encodeOutline(value.thumbnail_outline);
  out['is_owned'] = value.is_owned;
  out['is_installed'] = value.is_installed;
  out['is_archived'] = value.is_archived;
  out['is_official'] = value.is_official;
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['needs_repainting'] = value.needs_repainting;
  out['is_allowed_as_chat_emoji_status'] = value.is_allowed_as_chat_emoji_status;
  out['is_viewed'] = value.is_viewed;
  out['stickers'] = encodeVector(value.stickers, (item: Sticker | null): TdJsonRaw => item === null ? null : encodeSticker(item));
  out['emojis'] = encodeVector(value.emojis, (item: Emojis | null): TdJsonRaw => item === null ? null : encodeEmojis(item));
  return out;
}

/** stickerSetInfo = StickerSetInfo — Represents short information about a sticker set */
export class StickerSetInfo {
  type: 'stickerSetInfo' = 'stickerSetInfo';
  extra?: string;
  /** id: int64 — Identifier of the sticker set */
  id: string = '';
  /** title: string — Title of the sticker set */
  title: string = '';
  /** name: string — Name of the sticker set */
  name: string = '';
  /** thumbnail: thumbnail — Sticker set thumbnail in WEBP, TGS, or WEBM format with width and height 100; may be null. The file can be downloaded only before the thumbnail is changed */
  thumbnail: Thumbnail | null = null;
  /** thumbnail_outline: outline — Sticker set thumbnail's outline; may be null if unknown */
  thumbnail_outline: Outline | null = null;
  /** is_owned: Bool — True, if the sticker set is owned by the current user */
  is_owned: boolean = false;
  /** is_installed: Bool — True, if the sticker set has been installed by the current user */
  is_installed: boolean = false;
  /** is_archived: Bool — True, if the sticker set has been archived. A sticker set can't be installed and archived simultaneously */
  is_archived: boolean = false;
  /** is_official: Bool — True, if the sticker set is official */
  is_official: boolean = false;
  /** sticker_type: StickerType — Type of the stickers in the set */
  sticker_type: TdStickerType | null = null;
  /** needs_repainting: Bool — True, if stickers in the sticker set are custom emoji that must be repainted; for custom emoji sticker sets only */
  needs_repainting: boolean = false;
  /** is_allowed_as_chat_emoji_status: Bool — True, if stickers in the sticker set are custom emoji that can be used as chat emoji status; for custom emoji sticker sets only */
  is_allowed_as_chat_emoji_status: boolean = false;
  /** is_viewed: Bool — True for already viewed trending sticker sets */
  is_viewed: boolean = false;
  /** size: int32 — Total number of stickers in the set */
  size: number = 0;
  /** covers: vector<sticker> — Up to the first 5 stickers from the set, depending on the context. If the application needs more stickers the full sticker set needs to be requested */
  covers: Sticker[] = [];
}

/** Decode a TDLib `stickerSetInfo` object; null input -> null. */
export function decodeStickerSetInfo(json: TdJsonRaw): StickerSetInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerSetInfo = new StickerSetInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.title = asString(record['title']);
  out.name = asString(record['name']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  out.thumbnail_outline = decodeOutline(record['thumbnail_outline']);
  out.is_owned = asBool(record['is_owned']);
  out.is_installed = asBool(record['is_installed']);
  out.is_archived = asBool(record['is_archived']);
  out.is_official = asBool(record['is_official']);
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.needs_repainting = asBool(record['needs_repainting']);
  out.is_allowed_as_chat_emoji_status = asBool(record['is_allowed_as_chat_emoji_status']);
  out.is_viewed = asBool(record['is_viewed']);
  out.size = asNumber(record['size']);
  out.covers = decodeVector(record['covers'], (item: TdJsonRaw): Sticker => decodeSticker(item) as Sticker);
  return out;
}

/** Encode a `stickerSetInfo` into TDLib JSON wire shape. */
export function encodeStickerSetInfo(value: StickerSetInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerSetInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  out['name'] = value.name;
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  out['thumbnail_outline'] = value.thumbnail_outline === null ? null : encodeOutline(value.thumbnail_outline);
  out['is_owned'] = value.is_owned;
  out['is_installed'] = value.is_installed;
  out['is_archived'] = value.is_archived;
  out['is_official'] = value.is_official;
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['needs_repainting'] = value.needs_repainting;
  out['is_allowed_as_chat_emoji_status'] = value.is_allowed_as_chat_emoji_status;
  out['is_viewed'] = value.is_viewed;
  out['size'] = value.size;
  out['covers'] = encodeVector(value.covers, (item: Sticker | null): TdJsonRaw => item === null ? null : encodeSticker(item));
  return out;
}

/** stickerSets = StickerSets — Represents a list of sticker sets */
export class StickerSets {
  type: 'stickerSets' = 'stickerSets';
  extra?: string;
  /** total_count: int32 — Approximate total number of sticker sets found */
  total_count: number = 0;
  /** sets: vector<stickerSetInfo> — List of sticker sets */
  sets: StickerSetInfo[] = [];
}

/** Decode a TDLib `stickerSets` object; null input -> null. */
export function decodeStickerSets(json: TdJsonRaw): StickerSets | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerSets = new StickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.sets = decodeVector(record['sets'], (item: TdJsonRaw): StickerSetInfo => decodeStickerSetInfo(item) as StickerSetInfo);
  return out;
}

/** Encode a `stickerSets` into TDLib JSON wire shape. */
export function encodeStickerSets(value: StickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['sets'] = encodeVector(value.sets, (item: StickerSetInfo | null): TdJsonRaw => item === null ? null : encodeStickerSetInfo(item));
  return out;
}

/** stickerTypeCustomEmoji = StickerType — The sticker is a custom emoji to be used inside message text and caption */
export class StickerTypeCustomEmoji {
  type: 'stickerTypeCustomEmoji' = 'stickerTypeCustomEmoji';
  extra?: string;
}

/** Decode a TDLib `stickerTypeCustomEmoji` object; null input -> null. */
export function decodeStickerTypeCustomEmoji(json: TdJsonRaw): StickerTypeCustomEmoji | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerTypeCustomEmoji = new StickerTypeCustomEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `stickerTypeCustomEmoji` into TDLib JSON wire shape. */
export function encodeStickerTypeCustomEmoji(value: StickerTypeCustomEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerTypeCustomEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** stickerTypeMask = StickerType — The sticker is a mask in WEBP format to be placed on photos or videos */
export class StickerTypeMask {
  type: 'stickerTypeMask' = 'stickerTypeMask';
  extra?: string;
}

/** Decode a TDLib `stickerTypeMask` object; null input -> null. */
export function decodeStickerTypeMask(json: TdJsonRaw): StickerTypeMask | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerTypeMask = new StickerTypeMask();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `stickerTypeMask` into TDLib JSON wire shape. */
export function encodeStickerTypeMask(value: StickerTypeMask): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerTypeMask';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** stickerTypeRegular = StickerType — The sticker is a regular sticker */
export class StickerTypeRegular {
  type: 'stickerTypeRegular' = 'stickerTypeRegular';
  extra?: string;
}

/** Decode a TDLib `stickerTypeRegular` object; null input -> null. */
export function decodeStickerTypeRegular(json: TdJsonRaw): StickerTypeRegular | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StickerTypeRegular = new StickerTypeRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `stickerTypeRegular` into TDLib JSON wire shape. */
export function encodeStickerTypeRegular(value: StickerTypeRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickerTypeRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** stickers = Stickers — Represents a list of stickers */
export class Stickers {
  type: 'stickers' = 'stickers';
  extra?: string;
  /** stickers: vector<sticker> — List of stickers */
  stickers: Sticker[] = [];
}

/** Decode a TDLib `stickers` object; null input -> null. */
export function decodeStickers(json: TdJsonRaw): Stickers | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Stickers = new Stickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.stickers = decodeVector(record['stickers'], (item: TdJsonRaw): Sticker => decodeSticker(item) as Sticker);
  return out;
}

/** Encode a `stickers` into TDLib JSON wire shape. */
export function encodeStickers(value: Stickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['stickers'] = encodeVector(value.stickers, (item: Sticker | null): TdJsonRaw => item === null ? null : encodeSticker(item));
  return out;
}

/** stopBusinessPoll = BusinessMessage — Stops a poll sent on behalf of a business account; for bots only */
export class StopBusinessPoll {
  type: 'stopBusinessPoll' = 'stopBusinessPoll';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which the message with the poll was sent */
  business_connection_id: string = '';
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the poll */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
}

/** Decode a TDLib `stopBusinessPoll` object; null input -> null. */
export function decodeStopBusinessPoll(json: TdJsonRaw): StopBusinessPoll | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StopBusinessPoll = new StopBusinessPoll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  return out;
}

/** Encode a `stopBusinessPoll` into TDLib JSON wire shape. */
export function encodeStopBusinessPoll(value: StopBusinessPoll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stopBusinessPoll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  return out;
}

/** stopPendingMessage = Ok — Stops a pending message generation by a bot */
export class StopPendingMessage {
  type: 'stopPendingMessage' = 'stopPendingMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the bot */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Identifier of the topic in which the action is performed; pass null if none */
  topic_id: TdMessageTopic | null = null;
  /** draft_id: int64 — Unique identifier of the message draft within the message thread */
  draft_id: string = '';
}

/** Decode a TDLib `stopPendingMessage` object; null input -> null. */
export function decodeStopPendingMessage(json: TdJsonRaw): StopPendingMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StopPendingMessage = new StopPendingMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.draft_id = asInt64(record['draft_id']);
  return out;
}

/** Encode a `stopPendingMessage` into TDLib JSON wire shape. */
export function encodeStopPendingMessage(value: StopPendingMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stopPendingMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['draft_id'] = value.draft_id;
  return out;
}

/** stopPoll = Ok — Stops a poll */
export class StopPoll {
  type: 'stopPoll' = 'stopPoll';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the poll belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the poll. Use messageProperties.can_be_edited to check whether the poll can be stopped */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
}

/** Decode a TDLib `stopPoll` object; null input -> null. */
export function decodeStopPoll(json: TdJsonRaw): StopPoll | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StopPoll = new StopPoll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  return out;
}

/** Encode a `stopPoll` into TDLib JSON wire shape. */
export function encodeStopPoll(value: StopPoll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stopPoll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  return out;
}

/** storageStatistics = StorageStatistics — Contains the exact storage usage statistics split by chats and file type */
export class StorageStatistics {
  type: 'storageStatistics' = 'storageStatistics';
  extra?: string;
  /** size: int53 — Total size of files, in bytes */
  size: number = 0;
  /** count: int32 — Total number of files */
  count: number = 0;
  /** by_chat: vector<storageStatisticsByChat> — Statistics split by chats */
  by_chat: StorageStatisticsByChat[] = [];
}

/** Decode a TDLib `storageStatistics` object; null input -> null. */
export function decodeStorageStatistics(json: TdJsonRaw): StorageStatistics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorageStatistics = new StorageStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.size = asNumber(record['size']);
  out.count = asNumber(record['count']);
  out.by_chat = decodeVector(record['by_chat'], (item: TdJsonRaw): StorageStatisticsByChat => decodeStorageStatisticsByChat(item) as StorageStatisticsByChat);
  return out;
}

/** Encode a `storageStatistics` into TDLib JSON wire shape. */
export function encodeStorageStatistics(value: StorageStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storageStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['size'] = value.size;
  out['count'] = value.count;
  out['by_chat'] = encodeVector(value.by_chat, (item: StorageStatisticsByChat | null): TdJsonRaw => item === null ? null : encodeStorageStatisticsByChat(item));
  return out;
}

/** storageStatisticsByChat = StorageStatisticsByChat — Contains the storage usage statistics for a specific chat */
export class StorageStatisticsByChat {
  type: 'storageStatisticsByChat' = 'storageStatisticsByChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier; 0 if none */
  chat_id: number = 0;
  /** size: int53 — Total size of the files in the chat, in bytes */
  size: number = 0;
  /** count: int32 — Total number of files in the chat */
  count: number = 0;
  /** by_file_type: vector<storageStatisticsByFileType> — Statistics split by file types */
  by_file_type: StorageStatisticsByFileType[] = [];
}

/** Decode a TDLib `storageStatisticsByChat` object; null input -> null. */
export function decodeStorageStatisticsByChat(json: TdJsonRaw): StorageStatisticsByChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorageStatisticsByChat = new StorageStatisticsByChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.size = asNumber(record['size']);
  out.count = asNumber(record['count']);
  out.by_file_type = decodeVector(record['by_file_type'], (item: TdJsonRaw): StorageStatisticsByFileType => decodeStorageStatisticsByFileType(item) as StorageStatisticsByFileType);
  return out;
}

/** Encode a `storageStatisticsByChat` into TDLib JSON wire shape. */
export function encodeStorageStatisticsByChat(value: StorageStatisticsByChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storageStatisticsByChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['size'] = value.size;
  out['count'] = value.count;
  out['by_file_type'] = encodeVector(value.by_file_type, (item: StorageStatisticsByFileType | null): TdJsonRaw => item === null ? null : encodeStorageStatisticsByFileType(item));
  return out;
}

/** storageStatisticsByFileType = StorageStatisticsByFileType — Contains the storage usage statistics for a specific file type */
export class StorageStatisticsByFileType {
  type: 'storageStatisticsByFileType' = 'storageStatisticsByFileType';
  extra?: string;
  /** file_type: FileType — File type */
  file_type: TdFileType | null = null;
  /** size: int53 — Total size of the files, in bytes */
  size: number = 0;
  /** count: int32 — Total number of files */
  count: number = 0;
}

/** Decode a TDLib `storageStatisticsByFileType` object; null input -> null. */
export function decodeStorageStatisticsByFileType(json: TdJsonRaw): StorageStatisticsByFileType | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorageStatisticsByFileType = new StorageStatisticsByFileType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_type = decodeTdFileType(record['file_type']);
  out.size = asNumber(record['size']);
  out.count = asNumber(record['count']);
  return out;
}

/** Encode a `storageStatisticsByFileType` into TDLib JSON wire shape. */
export function encodeStorageStatisticsByFileType(value: StorageStatisticsByFileType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storageStatisticsByFileType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_type'] = value.file_type === null ? null : encodeTdFileType(value.file_type);
  out['size'] = value.size;
  out['count'] = value.count;
  return out;
}

/** storageStatisticsFast = StorageStatisticsFast — Contains approximate storage usage statistics, excluding files of unknown file type */
export class StorageStatisticsFast {
  type: 'storageStatisticsFast' = 'storageStatisticsFast';
  extra?: string;
  /** files_size: int53 — Approximate total size of files, in bytes */
  files_size: number = 0;
  /** file_count: int32 — Approximate number of files */
  file_count: number = 0;
  /** database_size: int53 — Size of the database */
  database_size: number = 0;
  /** language_pack_database_size: int53 — Size of the language pack database */
  language_pack_database_size: number = 0;
  /** log_size: int53 — Size of the TDLib internal log */
  log_size: number = 0;
}

/** Decode a TDLib `storageStatisticsFast` object; null input -> null. */
export function decodeStorageStatisticsFast(json: TdJsonRaw): StorageStatisticsFast | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorageStatisticsFast = new StorageStatisticsFast();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.files_size = asNumber(record['files_size']);
  out.file_count = asNumber(record['file_count']);
  out.database_size = asNumber(record['database_size']);
  out.language_pack_database_size = asNumber(record['language_pack_database_size']);
  out.log_size = asNumber(record['log_size']);
  return out;
}

/** Encode a `storageStatisticsFast` into TDLib JSON wire shape. */
export function encodeStorageStatisticsFast(value: StorageStatisticsFast): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storageStatisticsFast';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['files_size'] = value.files_size;
  out['file_count'] = value.file_count;
  out['database_size'] = value.database_size;
  out['language_pack_database_size'] = value.language_pack_database_size;
  out['log_size'] = value.log_size;
  return out;
}

/** storePaymentPurposeGiftedStars = StorePaymentPurpose — The user buying Telegram Stars for other users */
export class StorePaymentPurposeGiftedStars {
  type: 'storePaymentPurposeGiftedStars' = 'storePaymentPurposeGiftedStars';
  extra?: string;
  /** user_id: int53 — Identifier of the user to which Telegram Stars are gifted */
  user_id: number = 0;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** star_count: int53 — Number of bought Telegram Stars */
  star_count: number = 0;
}

/** Decode a TDLib `storePaymentPurposeGiftedStars` object; null input -> null. */
export function decodeStorePaymentPurposeGiftedStars(json: TdJsonRaw): StorePaymentPurposeGiftedStars | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorePaymentPurposeGiftedStars = new StorePaymentPurposeGiftedStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `storePaymentPurposeGiftedStars` into TDLib JSON wire shape. */
export function encodeStorePaymentPurposeGiftedStars(value: StorePaymentPurposeGiftedStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storePaymentPurposeGiftedStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['star_count'] = value.star_count;
  return out;
}

/** storePaymentPurposePremiumGift = StorePaymentPurpose — The user gifting Telegram Premium to another user */
export class StorePaymentPurposePremiumGift {
  type: 'storePaymentPurposePremiumGift' = 'storePaymentPurposePremiumGift';
  extra?: string;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** user_id: int53 — Identifier of the user who will receive Telegram Premium */
  user_id: number = 0;
  /** text: formattedText — Text to show along with the gift codes; 0-getOption("gift_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed */
  text: FormattedText | null = null;
}

/** Decode a TDLib `storePaymentPurposePremiumGift` object; null input -> null. */
export function decodeStorePaymentPurposePremiumGift(json: TdJsonRaw): StorePaymentPurposePremiumGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorePaymentPurposePremiumGift = new StorePaymentPurposePremiumGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.user_id = asNumber(record['user_id']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `storePaymentPurposePremiumGift` into TDLib JSON wire shape. */
export function encodeStorePaymentPurposePremiumGift(value: StorePaymentPurposePremiumGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storePaymentPurposePremiumGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['user_id'] = value.user_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** storePaymentPurposePremiumGiftCodes = StorePaymentPurpose — The user boosting a chat by creating Telegram Premium gift codes for other users */
export class StorePaymentPurposePremiumGiftCodes {
  type: 'storePaymentPurposePremiumGiftCodes' = 'storePaymentPurposePremiumGiftCodes';
  extra?: string;
  /** boosted_chat_id: int53 — Identifier of the supergroup or channel chat, which will be automatically boosted by the users for duration of the Premium subscription and which is administered by the user */
  boosted_chat_id: number = 0;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** user_ids: vector<int53> — Identifiers of the users who can activate the gift codes */
  user_ids: number[] = [];
  /** text: formattedText — Text to show along with the gift codes; 0-getOption("gift_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed */
  text: FormattedText | null = null;
}

/** Decode a TDLib `storePaymentPurposePremiumGiftCodes` object; null input -> null. */
export function decodeStorePaymentPurposePremiumGiftCodes(json: TdJsonRaw): StorePaymentPurposePremiumGiftCodes | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorePaymentPurposePremiumGiftCodes = new StorePaymentPurposePremiumGiftCodes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.boosted_chat_id = asNumber(record['boosted_chat_id']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `storePaymentPurposePremiumGiftCodes` into TDLib JSON wire shape. */
export function encodeStorePaymentPurposePremiumGiftCodes(value: StorePaymentPurposePremiumGiftCodes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storePaymentPurposePremiumGiftCodes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['boosted_chat_id'] = value.boosted_chat_id;
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** storePaymentPurposePremiumGiveaway = StorePaymentPurpose — The user creating a Telegram Premium giveaway */
export class StorePaymentPurposePremiumGiveaway {
  type: 'storePaymentPurposePremiumGiveaway' = 'storePaymentPurposePremiumGiveaway';
  extra?: string;
  /** parameters: giveawayParameters — Giveaway parameters */
  parameters: GiveawayParameters | null = null;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
}

/** Decode a TDLib `storePaymentPurposePremiumGiveaway` object; null input -> null. */
export function decodeStorePaymentPurposePremiumGiveaway(json: TdJsonRaw): StorePaymentPurposePremiumGiveaway | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorePaymentPurposePremiumGiveaway = new StorePaymentPurposePremiumGiveaway();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.parameters = decodeGiveawayParameters(record['parameters']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  return out;
}

/** Encode a `storePaymentPurposePremiumGiveaway` into TDLib JSON wire shape. */
export function encodeStorePaymentPurposePremiumGiveaway(value: StorePaymentPurposePremiumGiveaway): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storePaymentPurposePremiumGiveaway';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['parameters'] = value.parameters === null ? null : encodeGiveawayParameters(value.parameters);
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  return out;
}

/** storePaymentPurposePremiumSubscription = StorePaymentPurpose — The user subscribing to Telegram Premium */
export class StorePaymentPurposePremiumSubscription {
  type: 'storePaymentPurposePremiumSubscription' = 'storePaymentPurposePremiumSubscription';
  extra?: string;
  /** is_restore: Bool — Pass true if this is a restore of a Telegram Premium purchase; only for App Store */
  is_restore: boolean = false;
  /** is_upgrade: Bool — Pass true if this is an upgrade from a monthly subscription to early subscription; only for App Store */
  is_upgrade: boolean = false;
}

/** Decode a TDLib `storePaymentPurposePremiumSubscription` object; null input -> null. */
export function decodeStorePaymentPurposePremiumSubscription(json: TdJsonRaw): StorePaymentPurposePremiumSubscription | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorePaymentPurposePremiumSubscription = new StorePaymentPurposePremiumSubscription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_restore = asBool(record['is_restore']);
  out.is_upgrade = asBool(record['is_upgrade']);
  return out;
}

/** Encode a `storePaymentPurposePremiumSubscription` into TDLib JSON wire shape. */
export function encodeStorePaymentPurposePremiumSubscription(value: StorePaymentPurposePremiumSubscription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storePaymentPurposePremiumSubscription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_restore'] = value.is_restore;
  out['is_upgrade'] = value.is_upgrade;
  return out;
}

/** storePaymentPurposeStarGiveaway = StorePaymentPurpose — The user creating a Telegram Star giveaway */
export class StorePaymentPurposeStarGiveaway {
  type: 'storePaymentPurposeStarGiveaway' = 'storePaymentPurposeStarGiveaway';
  extra?: string;
  /** parameters: giveawayParameters — Giveaway parameters */
  parameters: GiveawayParameters | null = null;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** winner_count: int32 — The number of users to receive Telegram Stars */
  winner_count: number = 0;
  /** star_count: int53 — The number of Telegram Stars to be distributed through the giveaway */
  star_count: number = 0;
}

/** Decode a TDLib `storePaymentPurposeStarGiveaway` object; null input -> null. */
export function decodeStorePaymentPurposeStarGiveaway(json: TdJsonRaw): StorePaymentPurposeStarGiveaway | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorePaymentPurposeStarGiveaway = new StorePaymentPurposeStarGiveaway();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.parameters = decodeGiveawayParameters(record['parameters']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.winner_count = asNumber(record['winner_count']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `storePaymentPurposeStarGiveaway` into TDLib JSON wire shape. */
export function encodeStorePaymentPurposeStarGiveaway(value: StorePaymentPurposeStarGiveaway): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storePaymentPurposeStarGiveaway';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['parameters'] = value.parameters === null ? null : encodeGiveawayParameters(value.parameters);
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['winner_count'] = value.winner_count;
  out['star_count'] = value.star_count;
  return out;
}

/** storePaymentPurposeStars = StorePaymentPurpose — The user buying Telegram Stars */
export class StorePaymentPurposeStars {
  type: 'storePaymentPurposeStars' = 'storePaymentPurposeStars';
  extra?: string;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** star_count: int53 — Number of bought Telegram Stars */
  star_count: number = 0;
  /** chat_id: int53 — Identifier of the chat that is supposed to receive the Telegram Stars; pass 0 if none */
  chat_id: number = 0;
}

/** Decode a TDLib `storePaymentPurposeStars` object; null input -> null. */
export function decodeStorePaymentPurposeStars(json: TdJsonRaw): StorePaymentPurposeStars | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StorePaymentPurposeStars = new StorePaymentPurposeStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.star_count = asNumber(record['star_count']);
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `storePaymentPurposeStars` into TDLib JSON wire shape. */
export function encodeStorePaymentPurposeStars(value: StorePaymentPurposeStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storePaymentPurposeStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['star_count'] = value.star_count;
  out['chat_id'] = value.chat_id;
  return out;
}

/** storeTransactionAppStore = StoreTransaction — A purchase through App Store */
export class StoreTransactionAppStore {
  type: 'storeTransactionAppStore' = 'storeTransactionAppStore';
  extra?: string;
  /** receipt: bytes — App Store receipt */
  receipt: string = '';
}

/** Decode a TDLib `storeTransactionAppStore` object; null input -> null. */
export function decodeStoreTransactionAppStore(json: TdJsonRaw): StoreTransactionAppStore | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoreTransactionAppStore = new StoreTransactionAppStore();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.receipt = asString(record['receipt']);
  return out;
}

/** Encode a `storeTransactionAppStore` into TDLib JSON wire shape. */
export function encodeStoreTransactionAppStore(value: StoreTransactionAppStore): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storeTransactionAppStore';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['receipt'] = value.receipt;
  return out;
}

/** storeTransactionGooglePlay = StoreTransaction — A purchase through Google Play */
export class StoreTransactionGooglePlay {
  type: 'storeTransactionGooglePlay' = 'storeTransactionGooglePlay';
  extra?: string;
  /** package_name: string — Application package name */
  package_name: string = '';
  /** store_product_id: string — Identifier of the purchased store product */
  store_product_id: string = '';
  /** purchase_token: string — Google Play purchase token */
  purchase_token: string = '';
}

/** Decode a TDLib `storeTransactionGooglePlay` object; null input -> null. */
export function decodeStoreTransactionGooglePlay(json: TdJsonRaw): StoreTransactionGooglePlay | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoreTransactionGooglePlay = new StoreTransactionGooglePlay();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.package_name = asString(record['package_name']);
  out.store_product_id = asString(record['store_product_id']);
  out.purchase_token = asString(record['purchase_token']);
  return out;
}

/** Encode a `storeTransactionGooglePlay` into TDLib JSON wire shape. */
export function encodeStoreTransactionGooglePlay(value: StoreTransactionGooglePlay): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storeTransactionGooglePlay';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['package_name'] = value.package_name;
  out['store_product_id'] = value.store_product_id;
  out['purchase_token'] = value.purchase_token;
  return out;
}

/** stories = Stories — Represents a list of stories */
export class Stories {
  type: 'stories' = 'stories';
  extra?: string;
  /** total_count: int32 — Approximate total number of stories found */
  total_count: number = 0;
  /** stories: vector<story> — The list of stories */
  stories: Story[] = [];
  /** pinned_story_ids: vector<int32> — Identifiers of the pinned stories; returned only in getChatPostedToChatPageStories with from_story_id == 0 */
  pinned_story_ids: number[] = [];
}

/** Decode a TDLib `stories` object; null input -> null. */
export function decodeStories(json: TdJsonRaw): Stories | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Stories = new Stories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.stories = decodeVector(record['stories'], (item: TdJsonRaw): Story => decodeStory(item) as Story);
  out.pinned_story_ids = decodeVector(record['pinned_story_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `stories` into TDLib JSON wire shape. */
export function encodeStories(value: Stories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'stories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['stories'] = encodeVector(value.stories, (item: Story | null): TdJsonRaw => item === null ? null : encodeStory(item));
  out['pinned_story_ids'] = encodeVector(value.pinned_story_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** story = Story — Represents a story */
export class Story {
  type: 'story' = 'story';
  extra?: string;
  /** id: int32 — Unique story identifier among stories posted by the given chat */
  id: number = 0;
  /** poster_chat_id: int53 — Identifier of the chat that posted the story */
  poster_chat_id: number = 0;
  /** poster_id: MessageSender — Identifier of the user or chat that posted the story; may be null if the story is posted on behalf of the poster_chat_id */
  poster_id: TdMessageSender | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the story was published */
  date: number = 0;
  /** is_being_posted: Bool — True, if the story is being posted by the current user */
  is_being_posted: boolean = false;
  /** is_being_edited: Bool — True, if the story is being edited by the current user */
  is_being_edited: boolean = false;
  /** is_edited: Bool — True, if the story was edited */
  is_edited: boolean = false;
  /** is_posted_to_chat_page: Bool — True, if the story is saved in the profile of the chat that posted it and will be available there after expiration */
  is_posted_to_chat_page: boolean = false;
  /** is_visible_only_for_self: Bool — True, if the story is visible only for the current user */
  is_visible_only_for_self: boolean = false;
  /** can_be_added_to_album: Bool — True, if the story can be added to an album using createStoryAlbum and addStoryAlbumStories */
  can_be_added_to_album: boolean = false;
  /** can_be_deleted: Bool — True, if the story can be deleted */
  can_be_deleted: boolean = false;
  /** can_be_edited: Bool — True, if the story can be edited */
  can_be_edited: boolean = false;
  /** can_be_forwarded: Bool — True, if the story can be forwarded as a message or reposted as a story. Otherwise, screenshotting and saving of the story content must be also forbidden */
  can_be_forwarded: boolean = false;
  /** can_be_replied: Bool — True, if the story can be replied in the chat with the user who posted the story */
  can_be_replied: boolean = false;
  /** can_set_privacy_settings: Bool — True, if the story privacy settings can be changed */
  can_set_privacy_settings: boolean = false;
  /** can_toggle_is_posted_to_chat_page: Bool — True, if the story's is_posted_to_chat_page value can be changed */
  can_toggle_is_posted_to_chat_page: boolean = false;
  /** can_get_statistics: Bool — True, if the story statistics are available through getStoryStatistics */
  can_get_statistics: boolean = false;
  /** can_get_interactions: Bool — True, if interactions with the story can be received through getStoryInteractions */
  can_get_interactions: boolean = false;
  /** has_expired_viewers: Bool — True, if users who viewed the story can't be received, because the story has expired more than getOption("story_viewers_expiration_delay") seconds ago */
  has_expired_viewers: boolean = false;
  /** repost_info: storyRepostInfo — Information about the original story; may be null if the story wasn't reposted */
  repost_info: StoryRepostInfo | null = null;
  /** interaction_info: storyInteractionInfo — Information about interactions with the story; may be null if the story isn't owned or there were no interactions */
  interaction_info: StoryInteractionInfo | null = null;
  /** chosen_reaction_type: ReactionType — Type of the chosen reaction; may be null if none */
  chosen_reaction_type: TdReactionType | null = null;
  /** privacy_settings: StoryPrivacySettings — Privacy rules affecting story visibility; may be approximate for non-owned stories */
  privacy_settings: TdStoryPrivacySettings | null = null;
  /** content: StoryContent — Content of the story */
  content: TdStoryContent | null = null;
  /** areas: vector<storyArea> — Clickable areas to be shown on the story content */
  areas: StoryArea[] = [];
  /** caption: formattedText — Caption of the story */
  caption: FormattedText | null = null;
  /** album_ids: vector<int32> — Identifiers of story albums to which the story is added; only for manageable stories */
  album_ids: number[] = [];
}

/** Decode a TDLib `story` object; null input -> null. */
export function decodeStory(json: TdJsonRaw): Story | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Story = new Story();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.poster_chat_id = asNumber(record['poster_chat_id']);
  out.poster_id = decodeTdMessageSender(record['poster_id']);
  out.date = asNumber(record['date']);
  out.is_being_posted = asBool(record['is_being_posted']);
  out.is_being_edited = asBool(record['is_being_edited']);
  out.is_edited = asBool(record['is_edited']);
  out.is_posted_to_chat_page = asBool(record['is_posted_to_chat_page']);
  out.is_visible_only_for_self = asBool(record['is_visible_only_for_self']);
  out.can_be_added_to_album = asBool(record['can_be_added_to_album']);
  out.can_be_deleted = asBool(record['can_be_deleted']);
  out.can_be_edited = asBool(record['can_be_edited']);
  out.can_be_forwarded = asBool(record['can_be_forwarded']);
  out.can_be_replied = asBool(record['can_be_replied']);
  out.can_set_privacy_settings = asBool(record['can_set_privacy_settings']);
  out.can_toggle_is_posted_to_chat_page = asBool(record['can_toggle_is_posted_to_chat_page']);
  out.can_get_statistics = asBool(record['can_get_statistics']);
  out.can_get_interactions = asBool(record['can_get_interactions']);
  out.has_expired_viewers = asBool(record['has_expired_viewers']);
  out.repost_info = decodeStoryRepostInfo(record['repost_info']);
  out.interaction_info = decodeStoryInteractionInfo(record['interaction_info']);
  out.chosen_reaction_type = decodeTdReactionType(record['chosen_reaction_type']);
  out.privacy_settings = decodeTdStoryPrivacySettings(record['privacy_settings']);
  out.content = decodeTdStoryContent(record['content']);
  out.areas = decodeVector(record['areas'], (item: TdJsonRaw): StoryArea => decodeStoryArea(item) as StoryArea);
  out.caption = decodeFormattedText(record['caption']);
  out.album_ids = decodeVector(record['album_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `story` into TDLib JSON wire shape. */
export function encodeStory(value: Story): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'story';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['poster_chat_id'] = value.poster_chat_id;
  out['poster_id'] = value.poster_id === null ? null : encodeTdMessageSender(value.poster_id);
  out['date'] = value.date;
  out['is_being_posted'] = value.is_being_posted;
  out['is_being_edited'] = value.is_being_edited;
  out['is_edited'] = value.is_edited;
  out['is_posted_to_chat_page'] = value.is_posted_to_chat_page;
  out['is_visible_only_for_self'] = value.is_visible_only_for_self;
  out['can_be_added_to_album'] = value.can_be_added_to_album;
  out['can_be_deleted'] = value.can_be_deleted;
  out['can_be_edited'] = value.can_be_edited;
  out['can_be_forwarded'] = value.can_be_forwarded;
  out['can_be_replied'] = value.can_be_replied;
  out['can_set_privacy_settings'] = value.can_set_privacy_settings;
  out['can_toggle_is_posted_to_chat_page'] = value.can_toggle_is_posted_to_chat_page;
  out['can_get_statistics'] = value.can_get_statistics;
  out['can_get_interactions'] = value.can_get_interactions;
  out['has_expired_viewers'] = value.has_expired_viewers;
  out['repost_info'] = value.repost_info === null ? null : encodeStoryRepostInfo(value.repost_info);
  out['interaction_info'] = value.interaction_info === null ? null : encodeStoryInteractionInfo(value.interaction_info);
  out['chosen_reaction_type'] = value.chosen_reaction_type === null ? null : encodeTdReactionType(value.chosen_reaction_type);
  out['privacy_settings'] = value.privacy_settings === null ? null : encodeTdStoryPrivacySettings(value.privacy_settings);
  out['content'] = value.content === null ? null : encodeTdStoryContent(value.content);
  out['areas'] = encodeVector(value.areas, (item: StoryArea | null): TdJsonRaw => item === null ? null : encodeStoryArea(item));
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['album_ids'] = encodeVector(value.album_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** storyAlbum = StoryAlbum — Describes album of stories */
export class StoryAlbum {
  type: 'storyAlbum' = 'storyAlbum';
  extra?: string;
  /** id: int32 — Unique identifier of the album */
  id: number = 0;
  /** name: string — Name of the album */
  name: string = '';
  /** photo_icon: photo — Icon of the album; may be null if none */
  photo_icon: Photo | null = null;
  /** video_icon: video — Video icon of the album; may be null if none */
  video_icon: Video | null = null;
}

/** Decode a TDLib `storyAlbum` object; null input -> null. */
export function decodeStoryAlbum(json: TdJsonRaw): StoryAlbum | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAlbum = new StoryAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.name = asString(record['name']);
  out.photo_icon = decodePhoto(record['photo_icon']);
  out.video_icon = decodeVideo(record['video_icon']);
  return out;
}

/** Encode a `storyAlbum` into TDLib JSON wire shape. */
export function encodeStoryAlbum(value: StoryAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['name'] = value.name;
  out['photo_icon'] = value.photo_icon === null ? null : encodePhoto(value.photo_icon);
  out['video_icon'] = value.video_icon === null ? null : encodeVideo(value.video_icon);
  return out;
}

/** storyAlbums = StoryAlbums — Represents a list of story albums */
export class StoryAlbums {
  type: 'storyAlbums' = 'storyAlbums';
  extra?: string;
  /** albums: vector<storyAlbum> — List of story albums */
  albums: StoryAlbum[] = [];
}

/** Decode a TDLib `storyAlbums` object; null input -> null. */
export function decodeStoryAlbums(json: TdJsonRaw): StoryAlbums | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAlbums = new StoryAlbums();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.albums = decodeVector(record['albums'], (item: TdJsonRaw): StoryAlbum => decodeStoryAlbum(item) as StoryAlbum);
  return out;
}

/** Encode a `storyAlbums` into TDLib JSON wire shape. */
export function encodeStoryAlbums(value: StoryAlbums): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAlbums';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['albums'] = encodeVector(value.albums, (item: StoryAlbum | null): TdJsonRaw => item === null ? null : encodeStoryAlbum(item));
  return out;
}

/** storyArea = StoryArea — Describes a clickable rectangle area on a story media */
export class StoryArea {
  type: 'storyArea' = 'storyArea';
  extra?: string;
  /** position: storyAreaPosition — Position of the area */
  position: StoryAreaPosition | null = null;
  /** type: StoryAreaType — Type of the area */
  type_: TdStoryAreaType | null = null;
}

/** Decode a TDLib `storyArea` object; null input -> null. */
export function decodeStoryArea(json: TdJsonRaw): StoryArea | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryArea = new StoryArea();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.position = decodeStoryAreaPosition(record['position']);
  out.type_ = decodeTdStoryAreaType(record['type']);
  return out;
}

/** Encode a `storyArea` into TDLib JSON wire shape. */
export function encodeStoryArea(value: StoryArea): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyArea';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['position'] = value.position === null ? null : encodeStoryAreaPosition(value.position);
  out['type'] = value.type_ === null ? null : encodeTdStoryAreaType(value.type_);
  return out;
}

/** storyAreaPosition = StoryAreaPosition — Describes position of a clickable rectangle area on a story media */
export class StoryAreaPosition {
  type: 'storyAreaPosition' = 'storyAreaPosition';
  extra?: string;
  /** x_percentage: double — The abscissa of the rectangle's center, as a percentage of the media width */
  x_percentage: number = 0;
  /** y_percentage: double — The ordinate of the rectangle's center, as a percentage of the media height */
  y_percentage: number = 0;
  /** width_percentage: double — The width of the rectangle, as a percentage of the media width */
  width_percentage: number = 0;
  /** height_percentage: double — The height of the rectangle, as a percentage of the media height */
  height_percentage: number = 0;
  /** rotation_angle: double — Clockwise rotation angle of the rectangle, in degrees; 0-360 */
  rotation_angle: number = 0;
  /** corner_radius_percentage: double — The radius of the rectangle corner rounding, as a percentage of the media width */
  corner_radius_percentage: number = 0;
}

/** Decode a TDLib `storyAreaPosition` object; null input -> null. */
export function decodeStoryAreaPosition(json: TdJsonRaw): StoryAreaPosition | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAreaPosition = new StoryAreaPosition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.x_percentage = asNumber(record['x_percentage']);
  out.y_percentage = asNumber(record['y_percentage']);
  out.width_percentage = asNumber(record['width_percentage']);
  out.height_percentage = asNumber(record['height_percentage']);
  out.rotation_angle = asNumber(record['rotation_angle']);
  out.corner_radius_percentage = asNumber(record['corner_radius_percentage']);
  return out;
}

/** Encode a `storyAreaPosition` into TDLib JSON wire shape. */
export function encodeStoryAreaPosition(value: StoryAreaPosition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAreaPosition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['x_percentage'] = value.x_percentage;
  out['y_percentage'] = value.y_percentage;
  out['width_percentage'] = value.width_percentage;
  out['height_percentage'] = value.height_percentage;
  out['rotation_angle'] = value.rotation_angle;
  out['corner_radius_percentage'] = value.corner_radius_percentage;
  return out;
}

/** storyAreaTypeLink = StoryAreaType — An area pointing to a HTTP or tg:// link */
export class StoryAreaTypeLink {
  type: 'storyAreaTypeLink' = 'storyAreaTypeLink';
  extra?: string;
  /** url: string — HTTP or tg:// URL to be opened when the area is clicked */
  url: string = '';
}

/** Decode a TDLib `storyAreaTypeLink` object; null input -> null. */
export function decodeStoryAreaTypeLink(json: TdJsonRaw): StoryAreaTypeLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAreaTypeLink = new StoryAreaTypeLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `storyAreaTypeLink` into TDLib JSON wire shape. */
export function encodeStoryAreaTypeLink(value: StoryAreaTypeLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAreaTypeLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** storyAreaTypeLocation = StoryAreaType — An area pointing to a location */
export class StoryAreaTypeLocation {
  type: 'storyAreaTypeLocation' = 'storyAreaTypeLocation';
  extra?: string;
  /** location: location — The location */
  location: Location | null = null;
  /** address: locationAddress — Address of the location; may be null if unknown */
  address: LocationAddress | null = null;
}

/** Decode a TDLib `storyAreaTypeLocation` object; null input -> null. */
export function decodeStoryAreaTypeLocation(json: TdJsonRaw): StoryAreaTypeLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAreaTypeLocation = new StoryAreaTypeLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  out.address = decodeLocationAddress(record['address']);
  return out;
}

/** Encode a `storyAreaTypeLocation` into TDLib JSON wire shape. */
export function encodeStoryAreaTypeLocation(value: StoryAreaTypeLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAreaTypeLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['address'] = value.address === null ? null : encodeLocationAddress(value.address);
  return out;
}

/** storyAreaTypeMessage = StoryAreaType — An area pointing to a message */
export class StoryAreaTypeMessage {
  type: 'storyAreaTypeMessage' = 'storyAreaTypeMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `storyAreaTypeMessage` object; null input -> null. */
export function decodeStoryAreaTypeMessage(json: TdJsonRaw): StoryAreaTypeMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAreaTypeMessage = new StoryAreaTypeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `storyAreaTypeMessage` into TDLib JSON wire shape. */
export function encodeStoryAreaTypeMessage(value: StoryAreaTypeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAreaTypeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** storyAreaTypeSuggestedReaction = StoryAreaType — An area pointing to a suggested reaction. App needs to show a clickable reaction on the area and call setStoryReaction when the area is clicked */
export class StoryAreaTypeSuggestedReaction {
  type: 'storyAreaTypeSuggestedReaction' = 'storyAreaTypeSuggestedReaction';
  extra?: string;
  /** reaction_type: ReactionType — Type of the reaction */
  reaction_type: TdReactionType | null = null;
  /** total_count: int32 — Number of times the reaction was added */
  total_count: number = 0;
  /** is_dark: Bool — True, if reaction has a dark background */
  is_dark: boolean = false;
  /** is_flipped: Bool — True, if reaction corner is flipped */
  is_flipped: boolean = false;
}

/** Decode a TDLib `storyAreaTypeSuggestedReaction` object; null input -> null. */
export function decodeStoryAreaTypeSuggestedReaction(json: TdJsonRaw): StoryAreaTypeSuggestedReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAreaTypeSuggestedReaction = new StoryAreaTypeSuggestedReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reaction_type = decodeTdReactionType(record['reaction_type']);
  out.total_count = asNumber(record['total_count']);
  out.is_dark = asBool(record['is_dark']);
  out.is_flipped = asBool(record['is_flipped']);
  return out;
}

/** Encode a `storyAreaTypeSuggestedReaction` into TDLib JSON wire shape. */
export function encodeStoryAreaTypeSuggestedReaction(value: StoryAreaTypeSuggestedReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAreaTypeSuggestedReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reaction_type'] = value.reaction_type === null ? null : encodeTdReactionType(value.reaction_type);
  out['total_count'] = value.total_count;
  out['is_dark'] = value.is_dark;
  out['is_flipped'] = value.is_flipped;
  return out;
}

/** storyAreaTypeUpgradedGift = StoryAreaType — An area with an upgraded gift */
export class StoryAreaTypeUpgradedGift {
  type: 'storyAreaTypeUpgradedGift' = 'storyAreaTypeUpgradedGift';
  extra?: string;
  /** gift_name: string — Unique name of the upgraded gift */
  gift_name: string = '';
}

/** Decode a TDLib `storyAreaTypeUpgradedGift` object; null input -> null. */
export function decodeStoryAreaTypeUpgradedGift(json: TdJsonRaw): StoryAreaTypeUpgradedGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAreaTypeUpgradedGift = new StoryAreaTypeUpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_name = asString(record['gift_name']);
  return out;
}

/** Encode a `storyAreaTypeUpgradedGift` into TDLib JSON wire shape. */
export function encodeStoryAreaTypeUpgradedGift(value: StoryAreaTypeUpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAreaTypeUpgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_name'] = value.gift_name;
  return out;
}

/** storyAreaTypeVenue = StoryAreaType — An area pointing to a venue */
export class StoryAreaTypeVenue {
  type: 'storyAreaTypeVenue' = 'storyAreaTypeVenue';
  extra?: string;
  /** venue: venue — Information about the venue */
  venue: Venue | null = null;
}

/** Decode a TDLib `storyAreaTypeVenue` object; null input -> null. */
export function decodeStoryAreaTypeVenue(json: TdJsonRaw): StoryAreaTypeVenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAreaTypeVenue = new StoryAreaTypeVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.venue = decodeVenue(record['venue']);
  return out;
}

/** Encode a `storyAreaTypeVenue` into TDLib JSON wire shape. */
export function encodeStoryAreaTypeVenue(value: StoryAreaTypeVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAreaTypeVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['venue'] = value.venue === null ? null : encodeVenue(value.venue);
  return out;
}

/** storyAreaTypeWeather = StoryAreaType — An area with information about weather */
export class StoryAreaTypeWeather {
  type: 'storyAreaTypeWeather' = 'storyAreaTypeWeather';
  extra?: string;
  /** temperature: double — Temperature, in degree Celsius */
  temperature: number = 0;
  /** emoji: string — Emoji representing the weather */
  emoji: string = '';
  /** background_color: int32 — A color of the area background in the ARGB format */
  background_color: number = 0;
}

/** Decode a TDLib `storyAreaTypeWeather` object; null input -> null. */
export function decodeStoryAreaTypeWeather(json: TdJsonRaw): StoryAreaTypeWeather | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryAreaTypeWeather = new StoryAreaTypeWeather();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.temperature = asNumber(record['temperature']);
  out.emoji = asString(record['emoji']);
  out.background_color = asNumber(record['background_color']);
  return out;
}

/** Encode a `storyAreaTypeWeather` into TDLib JSON wire shape. */
export function encodeStoryAreaTypeWeather(value: StoryAreaTypeWeather): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyAreaTypeWeather';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['temperature'] = value.temperature;
  out['emoji'] = value.emoji;
  out['background_color'] = value.background_color;
  return out;
}

/** storyContentLive = StoryContent — A live story */
export class StoryContentLive {
  type: 'storyContentLive' = 'storyContentLive';
  extra?: string;
  /** group_call_id: int32 — Identifier of the corresponding group call. The group call can be received through the method getGroupCall */
  group_call_id: number = 0;
  /** is_rtmp_stream: Bool — True, if the call is an RTMP stream instead of an ordinary group call */
  is_rtmp_stream: boolean = false;
}

/** Decode a TDLib `storyContentLive` object; null input -> null. */
export function decodeStoryContentLive(json: TdJsonRaw): StoryContentLive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryContentLive = new StoryContentLive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.is_rtmp_stream = asBool(record['is_rtmp_stream']);
  return out;
}

/** Encode a `storyContentLive` into TDLib JSON wire shape. */
export function encodeStoryContentLive(value: StoryContentLive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyContentLive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['is_rtmp_stream'] = value.is_rtmp_stream;
  return out;
}

/** storyContentPhoto = StoryContent — A photo story */
export class StoryContentPhoto {
  type: 'storyContentPhoto' = 'storyContentPhoto';
  extra?: string;
  /** photo: photo — The photo */
  photo: Photo | null = null;
}

/** Decode a TDLib `storyContentPhoto` object; null input -> null. */
export function decodeStoryContentPhoto(json: TdJsonRaw): StoryContentPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryContentPhoto = new StoryContentPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  return out;
}

/** Encode a `storyContentPhoto` into TDLib JSON wire shape. */
export function encodeStoryContentPhoto(value: StoryContentPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyContentPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  return out;
}

/** storyContentTypeLive = StoryContentType — A live story */
export class StoryContentTypeLive {
  type: 'storyContentTypeLive' = 'storyContentTypeLive';
  extra?: string;
}

/** Decode a TDLib `storyContentTypeLive` object; null input -> null. */
export function decodeStoryContentTypeLive(json: TdJsonRaw): StoryContentTypeLive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryContentTypeLive = new StoryContentTypeLive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `storyContentTypeLive` into TDLib JSON wire shape. */
export function encodeStoryContentTypeLive(value: StoryContentTypeLive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyContentTypeLive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** storyContentTypePhoto = StoryContentType — A photo story */
export class StoryContentTypePhoto {
  type: 'storyContentTypePhoto' = 'storyContentTypePhoto';
  extra?: string;
}

/** Decode a TDLib `storyContentTypePhoto` object; null input -> null. */
export function decodeStoryContentTypePhoto(json: TdJsonRaw): StoryContentTypePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryContentTypePhoto = new StoryContentTypePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `storyContentTypePhoto` into TDLib JSON wire shape. */
export function encodeStoryContentTypePhoto(value: StoryContentTypePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyContentTypePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** storyContentTypeUnsupported = StoryContentType — A story of unknown content type */
export class StoryContentTypeUnsupported {
  type: 'storyContentTypeUnsupported' = 'storyContentTypeUnsupported';
  extra?: string;
}

/** Decode a TDLib `storyContentTypeUnsupported` object; null input -> null. */
export function decodeStoryContentTypeUnsupported(json: TdJsonRaw): StoryContentTypeUnsupported | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryContentTypeUnsupported = new StoryContentTypeUnsupported();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `storyContentTypeUnsupported` into TDLib JSON wire shape. */
export function encodeStoryContentTypeUnsupported(value: StoryContentTypeUnsupported): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyContentTypeUnsupported';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** storyContentTypeVideo = StoryContentType — A video story */
export class StoryContentTypeVideo {
  type: 'storyContentTypeVideo' = 'storyContentTypeVideo';
  extra?: string;
}

/** Decode a TDLib `storyContentTypeVideo` object; null input -> null. */
export function decodeStoryContentTypeVideo(json: TdJsonRaw): StoryContentTypeVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryContentTypeVideo = new StoryContentTypeVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `storyContentTypeVideo` into TDLib JSON wire shape. */
export function encodeStoryContentTypeVideo(value: StoryContentTypeVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyContentTypeVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** storyContentUnsupported = StoryContent — A story content that is not supported in the current TDLib version */
export class StoryContentUnsupported {
  type: 'storyContentUnsupported' = 'storyContentUnsupported';
  extra?: string;
}

/** Decode a TDLib `storyContentUnsupported` object; null input -> null. */
export function decodeStoryContentUnsupported(json: TdJsonRaw): StoryContentUnsupported | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryContentUnsupported = new StoryContentUnsupported();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `storyContentUnsupported` into TDLib JSON wire shape. */
export function encodeStoryContentUnsupported(value: StoryContentUnsupported): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyContentUnsupported';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** storyContentVideo = StoryContent — A video story */
export class StoryContentVideo {
  type: 'storyContentVideo' = 'storyContentVideo';
  extra?: string;
  /** video: storyVideo — The video in MPEG4 format */
  video: StoryVideo | null = null;
  /** alternative_video: storyVideo — Alternative version of the video in MPEG4 format, encoded with H.264 codec; may be null */
  alternative_video: StoryVideo | null = null;
}

/** Decode a TDLib `storyContentVideo` object; null input -> null. */
export function decodeStoryContentVideo(json: TdJsonRaw): StoryContentVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryContentVideo = new StoryContentVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeStoryVideo(record['video']);
  out.alternative_video = decodeStoryVideo(record['alternative_video']);
  return out;
}

/** Encode a `storyContentVideo` into TDLib JSON wire shape. */
export function encodeStoryContentVideo(value: StoryContentVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyContentVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeStoryVideo(value.video);
  out['alternative_video'] = value.alternative_video === null ? null : encodeStoryVideo(value.alternative_video);
  return out;
}

/** storyFullId = StoryFullId — Contains identifier of a story along with identifier of the chat that posted it */
export class StoryFullId {
  type: 'storyFullId' = 'storyFullId';
  extra?: string;
  /** poster_chat_id: int53 — Identifier of the chat that posted the story */
  poster_chat_id: number = 0;
  /** story_id: int32 — Unique story identifier among stories of the chat */
  story_id: number = 0;
}

/** Decode a TDLib `storyFullId` object; null input -> null. */
export function decodeStoryFullId(json: TdJsonRaw): StoryFullId | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryFullId = new StoryFullId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.poster_chat_id = asNumber(record['poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `storyFullId` into TDLib JSON wire shape. */
export function encodeStoryFullId(value: StoryFullId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyFullId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['poster_chat_id'] = value.poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** storyInfo = StoryInfo — Contains basic information about a story */
export class StoryInfo {
  type: 'storyInfo' = 'storyInfo';
  extra?: string;
  /** story_id: int32 — Unique story identifier among stories of the chat */
  story_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) when the story was published */
  date: number = 0;
  /** is_for_close_friends: Bool — True, if the story is available only to close friends */
  is_for_close_friends: boolean = false;
  /** is_live: Bool — True, if the story is a live story */
  is_live: boolean = false;
}

/** Decode a TDLib `storyInfo` object; null input -> null. */
export function decodeStoryInfo(json: TdJsonRaw): StoryInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryInfo = new StoryInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_id = asNumber(record['story_id']);
  out.date = asNumber(record['date']);
  out.is_for_close_friends = asBool(record['is_for_close_friends']);
  out.is_live = asBool(record['is_live']);
  return out;
}

/** Encode a `storyInfo` into TDLib JSON wire shape. */
export function encodeStoryInfo(value: StoryInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_id'] = value.story_id;
  out['date'] = value.date;
  out['is_for_close_friends'] = value.is_for_close_friends;
  out['is_live'] = value.is_live;
  return out;
}

/** storyInteraction = StoryInteraction — Represents interaction with a story */
export class StoryInteraction {
  type: 'storyInteraction' = 'storyInteraction';
  extra?: string;
  /** actor_id: MessageSender — Identifier of the user or chat that made the interaction */
  actor_id: TdMessageSender | null = null;
  /** interaction_date: int32 — Approximate point in time (Unix timestamp) when the interaction happened */
  interaction_date: number = 0;
  /** block_list: BlockList — Block list to which the actor is added; may be null if none or for chat stories */
  block_list: TdBlockList | null = null;
  /** type: StoryInteractionType — Type of the interaction */
  type_: TdStoryInteractionType | null = null;
}

/** Decode a TDLib `storyInteraction` object; null input -> null. */
export function decodeStoryInteraction(json: TdJsonRaw): StoryInteraction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryInteraction = new StoryInteraction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.actor_id = decodeTdMessageSender(record['actor_id']);
  out.interaction_date = asNumber(record['interaction_date']);
  out.block_list = decodeTdBlockList(record['block_list']);
  out.type_ = decodeTdStoryInteractionType(record['type']);
  return out;
}

/** Encode a `storyInteraction` into TDLib JSON wire shape. */
export function encodeStoryInteraction(value: StoryInteraction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyInteraction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['actor_id'] = value.actor_id === null ? null : encodeTdMessageSender(value.actor_id);
  out['interaction_date'] = value.interaction_date;
  out['block_list'] = value.block_list === null ? null : encodeTdBlockList(value.block_list);
  out['type'] = value.type_ === null ? null : encodeTdStoryInteractionType(value.type_);
  return out;
}

/** storyInteractionInfo = StoryInteractionInfo — Contains information about interactions with a story */
export class StoryInteractionInfo {
  type: 'storyInteractionInfo' = 'storyInteractionInfo';
  extra?: string;
  /** view_count: int32 — Number of times the story was viewed */
  view_count: number = 0;
  /** forward_count: int32 — Number of times the story was forwarded; 0 if none or unknown */
  forward_count: number = 0;
  /** reaction_count: int32 — Number of reactions added to the story; 0 if none or unknown */
  reaction_count: number = 0;
  /** recent_viewer_user_ids: vector<int53> — Identifiers of at most 3 recent viewers of the story */
  recent_viewer_user_ids: number[] = [];
}

/** Decode a TDLib `storyInteractionInfo` object; null input -> null. */
export function decodeStoryInteractionInfo(json: TdJsonRaw): StoryInteractionInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryInteractionInfo = new StoryInteractionInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.view_count = asNumber(record['view_count']);
  out.forward_count = asNumber(record['forward_count']);
  out.reaction_count = asNumber(record['reaction_count']);
  out.recent_viewer_user_ids = decodeVector(record['recent_viewer_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `storyInteractionInfo` into TDLib JSON wire shape. */
export function encodeStoryInteractionInfo(value: StoryInteractionInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyInteractionInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['view_count'] = value.view_count;
  out['forward_count'] = value.forward_count;
  out['reaction_count'] = value.reaction_count;
  out['recent_viewer_user_ids'] = encodeVector(value.recent_viewer_user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** storyInteractionTypeForward = StoryInteractionType — A forward of the story as a message */
export class StoryInteractionTypeForward {
  type: 'storyInteractionTypeForward' = 'storyInteractionTypeForward';
  extra?: string;
  /** message: message — The message with story forward */
  message: Message | null = null;
}

/** Decode a TDLib `storyInteractionTypeForward` object; null input -> null. */
export function decodeStoryInteractionTypeForward(json: TdJsonRaw): StoryInteractionTypeForward | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryInteractionTypeForward = new StoryInteractionTypeForward();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  return out;
}

/** Encode a `storyInteractionTypeForward` into TDLib JSON wire shape. */
export function encodeStoryInteractionTypeForward(value: StoryInteractionTypeForward): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyInteractionTypeForward';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  return out;
}

/** storyInteractionTypeRepost = StoryInteractionType — A repost of the story as a story */
export class StoryInteractionTypeRepost {
  type: 'storyInteractionTypeRepost' = 'storyInteractionTypeRepost';
  extra?: string;
  /** story: story — The reposted story */
  story: Story | null = null;
}

/** Decode a TDLib `storyInteractionTypeRepost` object; null input -> null. */
export function decodeStoryInteractionTypeRepost(json: TdJsonRaw): StoryInteractionTypeRepost | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryInteractionTypeRepost = new StoryInteractionTypeRepost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story = decodeStory(record['story']);
  return out;
}

/** Encode a `storyInteractionTypeRepost` into TDLib JSON wire shape. */
export function encodeStoryInteractionTypeRepost(value: StoryInteractionTypeRepost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyInteractionTypeRepost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story'] = value.story === null ? null : encodeStory(value.story);
  return out;
}

/** storyInteractionTypeView = StoryInteractionType — A view of the story */
export class StoryInteractionTypeView {
  type: 'storyInteractionTypeView' = 'storyInteractionTypeView';
  extra?: string;
  /** chosen_reaction_type: ReactionType — Type of the reaction that was chosen by the viewer; may be null if none */
  chosen_reaction_type: TdReactionType | null = null;
}

/** Decode a TDLib `storyInteractionTypeView` object; null input -> null. */
export function decodeStoryInteractionTypeView(json: TdJsonRaw): StoryInteractionTypeView | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryInteractionTypeView = new StoryInteractionTypeView();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chosen_reaction_type = decodeTdReactionType(record['chosen_reaction_type']);
  return out;
}

/** Encode a `storyInteractionTypeView` into TDLib JSON wire shape. */
export function encodeStoryInteractionTypeView(value: StoryInteractionTypeView): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyInteractionTypeView';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chosen_reaction_type'] = value.chosen_reaction_type === null ? null : encodeTdReactionType(value.chosen_reaction_type);
  return out;
}

/** storyInteractions = StoryInteractions — Represents a list of interactions with a story */
export class StoryInteractions {
  type: 'storyInteractions' = 'storyInteractions';
  extra?: string;
  /** total_count: int32 — Approximate total number of interactions found */
  total_count: number = 0;
  /** total_forward_count: int32 — Approximate total number of found forwards and reposts; always 0 for chat stories */
  total_forward_count: number = 0;
  /** total_reaction_count: int32 — Approximate total number of found reactions; always 0 for chat stories */
  total_reaction_count: number = 0;
  /** interactions: vector<storyInteraction> — List of story interactions */
  interactions: StoryInteraction[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `storyInteractions` object; null input -> null. */
export function decodeStoryInteractions(json: TdJsonRaw): StoryInteractions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryInteractions = new StoryInteractions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.total_forward_count = asNumber(record['total_forward_count']);
  out.total_reaction_count = asNumber(record['total_reaction_count']);
  out.interactions = decodeVector(record['interactions'], (item: TdJsonRaw): StoryInteraction => decodeStoryInteraction(item) as StoryInteraction);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `storyInteractions` into TDLib JSON wire shape. */
export function encodeStoryInteractions(value: StoryInteractions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyInteractions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['total_forward_count'] = value.total_forward_count;
  out['total_reaction_count'] = value.total_reaction_count;
  out['interactions'] = encodeVector(value.interactions, (item: StoryInteraction | null): TdJsonRaw => item === null ? null : encodeStoryInteraction(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** storyListArchive = StoryList — The list of stories, shown in the Archive chat list */
export class StoryListArchive {
  type: 'storyListArchive' = 'storyListArchive';
  extra?: string;
}

/** Decode a TDLib `storyListArchive` object; null input -> null. */
export function decodeStoryListArchive(json: TdJsonRaw): StoryListArchive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryListArchive = new StoryListArchive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `storyListArchive` into TDLib JSON wire shape. */
export function encodeStoryListArchive(value: StoryListArchive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyListArchive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** storyListMain = StoryList — The list of stories, shown in the main chat list and folder chat lists */
export class StoryListMain {
  type: 'storyListMain' = 'storyListMain';
  extra?: string;
}

/** Decode a TDLib `storyListMain` object; null input -> null. */
export function decodeStoryListMain(json: TdJsonRaw): StoryListMain | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryListMain = new StoryListMain();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `storyListMain` into TDLib JSON wire shape. */
export function encodeStoryListMain(value: StoryListMain): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyListMain';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** storyOriginHiddenUser = StoryOrigin — The original story was posted by an unknown user */
export class StoryOriginHiddenUser {
  type: 'storyOriginHiddenUser' = 'storyOriginHiddenUser';
  extra?: string;
  /** poster_name: string — Name of the user or the chat that posted the story */
  poster_name: string = '';
}

/** Decode a TDLib `storyOriginHiddenUser` object; null input -> null. */
export function decodeStoryOriginHiddenUser(json: TdJsonRaw): StoryOriginHiddenUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryOriginHiddenUser = new StoryOriginHiddenUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.poster_name = asString(record['poster_name']);
  return out;
}

/** Encode a `storyOriginHiddenUser` into TDLib JSON wire shape. */
export function encodeStoryOriginHiddenUser(value: StoryOriginHiddenUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyOriginHiddenUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['poster_name'] = value.poster_name;
  return out;
}

/** storyOriginPublicStory = StoryOrigin — The original story was a public story that was posted by a known chat */
export class StoryOriginPublicStory {
  type: 'storyOriginPublicStory' = 'storyOriginPublicStory';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that posted original story */
  chat_id: number = 0;
  /** story_id: int32 — Story identifier of the original story */
  story_id: number = 0;
}

/** Decode a TDLib `storyOriginPublicStory` object; null input -> null. */
export function decodeStoryOriginPublicStory(json: TdJsonRaw): StoryOriginPublicStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryOriginPublicStory = new StoryOriginPublicStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `storyOriginPublicStory` into TDLib JSON wire shape. */
export function encodeStoryOriginPublicStory(value: StoryOriginPublicStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyOriginPublicStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** storyPrivacySettingsCloseFriends = StoryPrivacySettings — The story can be viewed by all close friends */
export class StoryPrivacySettingsCloseFriends {
  type: 'storyPrivacySettingsCloseFriends' = 'storyPrivacySettingsCloseFriends';
  extra?: string;
}

/** Decode a TDLib `storyPrivacySettingsCloseFriends` object; null input -> null. */
export function decodeStoryPrivacySettingsCloseFriends(json: TdJsonRaw): StoryPrivacySettingsCloseFriends | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryPrivacySettingsCloseFriends = new StoryPrivacySettingsCloseFriends();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `storyPrivacySettingsCloseFriends` into TDLib JSON wire shape. */
export function encodeStoryPrivacySettingsCloseFriends(value: StoryPrivacySettingsCloseFriends): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyPrivacySettingsCloseFriends';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** storyPrivacySettingsContacts = StoryPrivacySettings — The story can be viewed by all contacts except chosen users */
export class StoryPrivacySettingsContacts {
  type: 'storyPrivacySettingsContacts' = 'storyPrivacySettingsContacts';
  extra?: string;
  /** except_user_ids: vector<int53> — User identifiers of the contacts that can't see the story; always unknown and empty for non-owned stories */
  except_user_ids: number[] = [];
}

/** Decode a TDLib `storyPrivacySettingsContacts` object; null input -> null. */
export function decodeStoryPrivacySettingsContacts(json: TdJsonRaw): StoryPrivacySettingsContacts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryPrivacySettingsContacts = new StoryPrivacySettingsContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.except_user_ids = decodeVector(record['except_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `storyPrivacySettingsContacts` into TDLib JSON wire shape. */
export function encodeStoryPrivacySettingsContacts(value: StoryPrivacySettingsContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyPrivacySettingsContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['except_user_ids'] = encodeVector(value.except_user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** storyPrivacySettingsEveryone = StoryPrivacySettings — The story can be viewed by everyone */
export class StoryPrivacySettingsEveryone {
  type: 'storyPrivacySettingsEveryone' = 'storyPrivacySettingsEveryone';
  extra?: string;
  /** except_user_ids: vector<int53> — Identifiers of the users that can't see the story; always unknown and empty for non-owned stories */
  except_user_ids: number[] = [];
}

/** Decode a TDLib `storyPrivacySettingsEveryone` object; null input -> null. */
export function decodeStoryPrivacySettingsEveryone(json: TdJsonRaw): StoryPrivacySettingsEveryone | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryPrivacySettingsEveryone = new StoryPrivacySettingsEveryone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.except_user_ids = decodeVector(record['except_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `storyPrivacySettingsEveryone` into TDLib JSON wire shape. */
export function encodeStoryPrivacySettingsEveryone(value: StoryPrivacySettingsEveryone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyPrivacySettingsEveryone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['except_user_ids'] = encodeVector(value.except_user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** storyPrivacySettingsSelectedUsers = StoryPrivacySettings — The story can be viewed by certain specified users */
export class StoryPrivacySettingsSelectedUsers {
  type: 'storyPrivacySettingsSelectedUsers' = 'storyPrivacySettingsSelectedUsers';
  extra?: string;
  /** user_ids: vector<int53> — Identifiers of the users; always unknown and empty for non-owned stories */
  user_ids: number[] = [];
}

/** Decode a TDLib `storyPrivacySettingsSelectedUsers` object; null input -> null. */
export function decodeStoryPrivacySettingsSelectedUsers(json: TdJsonRaw): StoryPrivacySettingsSelectedUsers | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryPrivacySettingsSelectedUsers = new StoryPrivacySettingsSelectedUsers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `storyPrivacySettingsSelectedUsers` into TDLib JSON wire shape. */
export function encodeStoryPrivacySettingsSelectedUsers(value: StoryPrivacySettingsSelectedUsers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyPrivacySettingsSelectedUsers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** storyRepostInfo = StoryRepostInfo — Contains information about original story that was reposted */
export class StoryRepostInfo {
  type: 'storyRepostInfo' = 'storyRepostInfo';
  extra?: string;
  /** origin: StoryOrigin — Origin of the story that was reposted */
  origin: TdStoryOrigin | null = null;
  /** is_content_modified: Bool — True, if story content was modified during reposting; otherwise, story wasn't modified */
  is_content_modified: boolean = false;
}

/** Decode a TDLib `storyRepostInfo` object; null input -> null. */
export function decodeStoryRepostInfo(json: TdJsonRaw): StoryRepostInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryRepostInfo = new StoryRepostInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.origin = decodeTdStoryOrigin(record['origin']);
  out.is_content_modified = asBool(record['is_content_modified']);
  return out;
}

/** Encode a `storyRepostInfo` into TDLib JSON wire shape. */
export function encodeStoryRepostInfo(value: StoryRepostInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyRepostInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['origin'] = value.origin === null ? null : encodeTdStoryOrigin(value.origin);
  out['is_content_modified'] = value.is_content_modified;
  return out;
}

/** storyStatistics = StoryStatistics — A detailed statistics about a story */
export class StoryStatistics {
  type: 'storyStatistics' = 'storyStatistics';
  extra?: string;
  /** story_interaction_graph: StatisticalGraph — A graph containing number of story views and shares */
  story_interaction_graph: TdStatisticalGraph | null = null;
  /** story_reaction_graph: StatisticalGraph — A graph containing number of story reactions */
  story_reaction_graph: TdStatisticalGraph | null = null;
}

/** Decode a TDLib `storyStatistics` object; null input -> null. */
export function decodeStoryStatistics(json: TdJsonRaw): StoryStatistics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryStatistics = new StoryStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_interaction_graph = decodeTdStatisticalGraph(record['story_interaction_graph']);
  out.story_reaction_graph = decodeTdStatisticalGraph(record['story_reaction_graph']);
  return out;
}

/** Encode a `storyStatistics` into TDLib JSON wire shape. */
export function encodeStoryStatistics(value: StoryStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_interaction_graph'] = value.story_interaction_graph === null ? null : encodeTdStatisticalGraph(value.story_interaction_graph);
  out['story_reaction_graph'] = value.story_reaction_graph === null ? null : encodeTdStatisticalGraph(value.story_reaction_graph);
  return out;
}

/** storyVideo = StoryVideo — Describes a video file posted as a story */
export class StoryVideo {
  type: 'storyVideo' = 'storyVideo';
  extra?: string;
  /** duration: double — Duration of the video, in seconds */
  duration: number = 0;
  /** width: int32 — Video width */
  width: number = 0;
  /** height: int32 — Video height */
  height: number = 0;
  /** has_stickers: Bool — True, if stickers were added to the video. The list of corresponding sticker sets can be received using getAttachedStickerSets */
  has_stickers: boolean = false;
  /** is_animation: Bool — True, if the video has no sound */
  is_animation: boolean = false;
  /** minithumbnail: minithumbnail — Video minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
  /** thumbnail: thumbnail — Video thumbnail in JPEG or MPEG4 format; may be null */
  thumbnail: Thumbnail | null = null;
  /** preload_prefix_size: int32 — Size of file prefix, which is expected to be preloaded, in bytes */
  preload_prefix_size: number = 0;
  /** cover_frame_timestamp: double — Timestamp of the frame used as video thumbnail */
  cover_frame_timestamp: number = 0;
  /** video: file — File containing the video */
  video: File | null = null;
}

/** Decode a TDLib `storyVideo` object; null input -> null. */
export function decodeStoryVideo(json: TdJsonRaw): StoryVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: StoryVideo = new StoryVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.duration = asNumber(record['duration']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.has_stickers = asBool(record['has_stickers']);
  out.is_animation = asBool(record['is_animation']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  out.preload_prefix_size = asNumber(record['preload_prefix_size']);
  out.cover_frame_timestamp = asNumber(record['cover_frame_timestamp']);
  out.video = decodeFile(record['video']);
  return out;
}

/** Encode a `storyVideo` into TDLib JSON wire shape. */
export function encodeStoryVideo(value: StoryVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'storyVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['duration'] = value.duration;
  out['width'] = value.width;
  out['height'] = value.height;
  out['has_stickers'] = value.has_stickers;
  out['is_animation'] = value.is_animation;
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  out['preload_prefix_size'] = value.preload_prefix_size;
  out['cover_frame_timestamp'] = value.cover_frame_timestamp;
  out['video'] = value.video === null ? null : encodeFile(value.video);
  return out;
}

/** suggestUserBirthdate = Ok — Suggests a birthdate to another regular user with common messages and allowing non-paid messages */
export class SuggestUserBirthdate {
  type: 'suggestUserBirthdate' = 'suggestUserBirthdate';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** birthdate: birthdate — Birthdate to suggest */
  birthdate: Birthdate | null = null;
}

/** Decode a TDLib `suggestUserBirthdate` object; null input -> null. */
export function decodeSuggestUserBirthdate(json: TdJsonRaw): SuggestUserBirthdate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestUserBirthdate = new SuggestUserBirthdate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.birthdate = decodeBirthdate(record['birthdate']);
  return out;
}

/** Encode a `suggestUserBirthdate` into TDLib JSON wire shape. */
export function encodeSuggestUserBirthdate(value: SuggestUserBirthdate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestUserBirthdate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['birthdate'] = value.birthdate === null ? null : encodeBirthdate(value.birthdate);
  return out;
}

/** suggestUserProfilePhoto = Ok — Suggests a profile photo to another regular user with common messages and allowing non-paid messages */
export class SuggestUserProfilePhoto {
  type: 'suggestUserProfilePhoto' = 'suggestUserProfilePhoto';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** photo: InputChatPhoto — Profile photo to suggest; inputChatPhotoPrevious isn't supported in this function */
  photo: TdInputChatPhoto | null = null;
}

/** Decode a TDLib `suggestUserProfilePhoto` object; null input -> null. */
export function decodeSuggestUserProfilePhoto(json: TdJsonRaw): SuggestUserProfilePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestUserProfilePhoto = new SuggestUserProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.photo = decodeTdInputChatPhoto(record['photo']);
  return out;
}

/** Encode a `suggestUserProfilePhoto` into TDLib JSON wire shape. */
export function encodeSuggestUserProfilePhoto(value: SuggestUserProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestUserProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['photo'] = value.photo === null ? null : encodeTdInputChatPhoto(value.photo);
  return out;
}

/** suggestedActionAddLoginPasskey = SuggestedAction — Suggests the user to add a passkey for login using addLoginPasskey */
export class SuggestedActionAddLoginPasskey {
  type: 'suggestedActionAddLoginPasskey' = 'suggestedActionAddLoginPasskey';
  extra?: string;
}

/** Decode a TDLib `suggestedActionAddLoginPasskey` object; null input -> null. */
export function decodeSuggestedActionAddLoginPasskey(json: TdJsonRaw): SuggestedActionAddLoginPasskey | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionAddLoginPasskey = new SuggestedActionAddLoginPasskey();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionAddLoginPasskey` into TDLib JSON wire shape. */
export function encodeSuggestedActionAddLoginPasskey(value: SuggestedActionAddLoginPasskey): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionAddLoginPasskey';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionCheckPassword = SuggestedAction — Suggests the user to check whether they still remember their 2-step verification password */
export class SuggestedActionCheckPassword {
  type: 'suggestedActionCheckPassword' = 'suggestedActionCheckPassword';
  extra?: string;
}

/** Decode a TDLib `suggestedActionCheckPassword` object; null input -> null. */
export function decodeSuggestedActionCheckPassword(json: TdJsonRaw): SuggestedActionCheckPassword | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionCheckPassword = new SuggestedActionCheckPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionCheckPassword` into TDLib JSON wire shape. */
export function encodeSuggestedActionCheckPassword(value: SuggestedActionCheckPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionCheckPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionCheckPhoneNumber = SuggestedAction — Suggests the user to check whether authorization phone number is correct and change the phone number if it is inaccessible */
export class SuggestedActionCheckPhoneNumber {
  type: 'suggestedActionCheckPhoneNumber' = 'suggestedActionCheckPhoneNumber';
  extra?: string;
}

/** Decode a TDLib `suggestedActionCheckPhoneNumber` object; null input -> null. */
export function decodeSuggestedActionCheckPhoneNumber(json: TdJsonRaw): SuggestedActionCheckPhoneNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionCheckPhoneNumber = new SuggestedActionCheckPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionCheckPhoneNumber` into TDLib JSON wire shape. */
export function encodeSuggestedActionCheckPhoneNumber(value: SuggestedActionCheckPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionCheckPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionConvertToBroadcastGroup = SuggestedAction — Suggests the user to convert specified supergroup to a broadcast group */
export class SuggestedActionConvertToBroadcastGroup {
  type: 'suggestedActionConvertToBroadcastGroup' = 'suggestedActionConvertToBroadcastGroup';
  extra?: string;
  /** supergroup_id: int53 — Supergroup identifier */
  supergroup_id: number = 0;
}

/** Decode a TDLib `suggestedActionConvertToBroadcastGroup` object; null input -> null. */
export function decodeSuggestedActionConvertToBroadcastGroup(json: TdJsonRaw): SuggestedActionConvertToBroadcastGroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionConvertToBroadcastGroup = new SuggestedActionConvertToBroadcastGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  return out;
}

/** Encode a `suggestedActionConvertToBroadcastGroup` into TDLib JSON wire shape. */
export function encodeSuggestedActionConvertToBroadcastGroup(value: SuggestedActionConvertToBroadcastGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionConvertToBroadcastGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  return out;
}

/** suggestedActionCustom = SuggestedAction — A custom suggestion to be shown at the top of the chat list */
export class SuggestedActionCustom {
  type: 'suggestedActionCustom' = 'suggestedActionCustom';
  extra?: string;
  /** name: string — Unique name of the suggestion */
  name: string = '';
  /** title: formattedText — Title of the suggestion */
  title: FormattedText | null = null;
  /** description: formattedText */
  description: FormattedText | null = null;
  /** url: string — The link to open when the suggestion is clicked */
  url: string = '';
}

/** Decode a TDLib `suggestedActionCustom` object; null input -> null. */
export function decodeSuggestedActionCustom(json: TdJsonRaw): SuggestedActionCustom | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionCustom = new SuggestedActionCustom();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.title = decodeFormattedText(record['title']);
  out.description = decodeFormattedText(record['description']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `suggestedActionCustom` into TDLib JSON wire shape. */
export function encodeSuggestedActionCustom(value: SuggestedActionCustom): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionCustom';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['title'] = value.title === null ? null : encodeFormattedText(value.title);
  out['description'] = value.description === null ? null : encodeFormattedText(value.description);
  out['url'] = value.url;
  return out;
}

/** suggestedActionEnableArchiveAndMuteNewChats = SuggestedAction — Suggests the user to enable archive_and_mute_new_chats_from_unknown_users setting in archiveChatListSettings */
export class SuggestedActionEnableArchiveAndMuteNewChats {
  type: 'suggestedActionEnableArchiveAndMuteNewChats' = 'suggestedActionEnableArchiveAndMuteNewChats';
  extra?: string;
}

/** Decode a TDLib `suggestedActionEnableArchiveAndMuteNewChats` object; null input -> null. */
export function decodeSuggestedActionEnableArchiveAndMuteNewChats(json: TdJsonRaw): SuggestedActionEnableArchiveAndMuteNewChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionEnableArchiveAndMuteNewChats = new SuggestedActionEnableArchiveAndMuteNewChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionEnableArchiveAndMuteNewChats` into TDLib JSON wire shape. */
export function encodeSuggestedActionEnableArchiveAndMuteNewChats(value: SuggestedActionEnableArchiveAndMuteNewChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionEnableArchiveAndMuteNewChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionExtendPremium = SuggestedAction — Suggests the user to extend their expiring Telegram Premium subscription */
export class SuggestedActionExtendPremium {
  type: 'suggestedActionExtendPremium' = 'suggestedActionExtendPremium';
  extra?: string;
  /** manage_premium_subscription_url: string — A URL for managing Telegram Premium subscription */
  manage_premium_subscription_url: string = '';
}

/** Decode a TDLib `suggestedActionExtendPremium` object; null input -> null. */
export function decodeSuggestedActionExtendPremium(json: TdJsonRaw): SuggestedActionExtendPremium | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionExtendPremium = new SuggestedActionExtendPremium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.manage_premium_subscription_url = asString(record['manage_premium_subscription_url']);
  return out;
}

/** Encode a `suggestedActionExtendPremium` into TDLib JSON wire shape. */
export function encodeSuggestedActionExtendPremium(value: SuggestedActionExtendPremium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionExtendPremium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['manage_premium_subscription_url'] = value.manage_premium_subscription_url;
  return out;
}

/** suggestedActionExtendStarSubscriptions = SuggestedAction — Suggests the user to extend their expiring Telegram Star subscriptions. Call getStarSubscriptions with only_expiring == true to get the number of expiring subscriptions and the n... */
export class SuggestedActionExtendStarSubscriptions {
  type: 'suggestedActionExtendStarSubscriptions' = 'suggestedActionExtendStarSubscriptions';
  extra?: string;
}

/** Decode a TDLib `suggestedActionExtendStarSubscriptions` object; null input -> null. */
export function decodeSuggestedActionExtendStarSubscriptions(json: TdJsonRaw): SuggestedActionExtendStarSubscriptions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionExtendStarSubscriptions = new SuggestedActionExtendStarSubscriptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionExtendStarSubscriptions` into TDLib JSON wire shape. */
export function encodeSuggestedActionExtendStarSubscriptions(value: SuggestedActionExtendStarSubscriptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionExtendStarSubscriptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionGiftPremiumForChristmas = SuggestedAction — Suggests the user to gift Telegram Premium to friends for Christmas */
export class SuggestedActionGiftPremiumForChristmas {
  type: 'suggestedActionGiftPremiumForChristmas' = 'suggestedActionGiftPremiumForChristmas';
  extra?: string;
}

/** Decode a TDLib `suggestedActionGiftPremiumForChristmas` object; null input -> null. */
export function decodeSuggestedActionGiftPremiumForChristmas(json: TdJsonRaw): SuggestedActionGiftPremiumForChristmas | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionGiftPremiumForChristmas = new SuggestedActionGiftPremiumForChristmas();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionGiftPremiumForChristmas` into TDLib JSON wire shape. */
export function encodeSuggestedActionGiftPremiumForChristmas(value: SuggestedActionGiftPremiumForChristmas): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionGiftPremiumForChristmas';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionRestorePremium = SuggestedAction — Suggests the user to restore a recently expired Premium subscription */
export class SuggestedActionRestorePremium {
  type: 'suggestedActionRestorePremium' = 'suggestedActionRestorePremium';
  extra?: string;
}

/** Decode a TDLib `suggestedActionRestorePremium` object; null input -> null. */
export function decodeSuggestedActionRestorePremium(json: TdJsonRaw): SuggestedActionRestorePremium | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionRestorePremium = new SuggestedActionRestorePremium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionRestorePremium` into TDLib JSON wire shape. */
export function encodeSuggestedActionRestorePremium(value: SuggestedActionRestorePremium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionRestorePremium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionSetBirthdate = SuggestedAction — Suggests the user to set birthdate */
export class SuggestedActionSetBirthdate {
  type: 'suggestedActionSetBirthdate' = 'suggestedActionSetBirthdate';
  extra?: string;
}

/** Decode a TDLib `suggestedActionSetBirthdate` object; null input -> null. */
export function decodeSuggestedActionSetBirthdate(json: TdJsonRaw): SuggestedActionSetBirthdate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionSetBirthdate = new SuggestedActionSetBirthdate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionSetBirthdate` into TDLib JSON wire shape. */
export function encodeSuggestedActionSetBirthdate(value: SuggestedActionSetBirthdate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionSetBirthdate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionSetLoginEmailAddress = SuggestedAction — Suggests the user to add login email address. Call isLoginEmailAddressRequired, and then setLoginEmailAddress or checkLoginEmailAddressCode to change the login email address */
export class SuggestedActionSetLoginEmailAddress {
  type: 'suggestedActionSetLoginEmailAddress' = 'suggestedActionSetLoginEmailAddress';
  extra?: string;
  /** can_be_hidden: Bool — True, if the suggested action can be hidden using hideSuggestedAction. Otherwise, the user must not be able to use the application without setting up the email address */
  can_be_hidden: boolean = false;
}

/** Decode a TDLib `suggestedActionSetLoginEmailAddress` object; null input -> null. */
export function decodeSuggestedActionSetLoginEmailAddress(json: TdJsonRaw): SuggestedActionSetLoginEmailAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionSetLoginEmailAddress = new SuggestedActionSetLoginEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_be_hidden = asBool(record['can_be_hidden']);
  return out;
}

/** Encode a `suggestedActionSetLoginEmailAddress` into TDLib JSON wire shape. */
export function encodeSuggestedActionSetLoginEmailAddress(value: SuggestedActionSetLoginEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionSetLoginEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_be_hidden'] = value.can_be_hidden;
  return out;
}

/** suggestedActionSetPassword = SuggestedAction — Suggests the user to set a 2-step verification password to be able to log in again */
export class SuggestedActionSetPassword {
  type: 'suggestedActionSetPassword' = 'suggestedActionSetPassword';
  extra?: string;
  /** authorization_delay: int32 — The number of days to pass between consecutive authorizations if the user declines to set password; if 0, then the user is advised to set the password for security reasons */
  authorization_delay: number = 0;
}

/** Decode a TDLib `suggestedActionSetPassword` object; null input -> null. */
export function decodeSuggestedActionSetPassword(json: TdJsonRaw): SuggestedActionSetPassword | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionSetPassword = new SuggestedActionSetPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.authorization_delay = asNumber(record['authorization_delay']);
  return out;
}

/** Encode a `suggestedActionSetPassword` into TDLib JSON wire shape. */
export function encodeSuggestedActionSetPassword(value: SuggestedActionSetPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionSetPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['authorization_delay'] = value.authorization_delay;
  return out;
}

/** suggestedActionSetProfilePhoto = SuggestedAction — Suggests the user to set profile photo */
export class SuggestedActionSetProfilePhoto {
  type: 'suggestedActionSetProfilePhoto' = 'suggestedActionSetProfilePhoto';
  extra?: string;
}

/** Decode a TDLib `suggestedActionSetProfilePhoto` object; null input -> null. */
export function decodeSuggestedActionSetProfilePhoto(json: TdJsonRaw): SuggestedActionSetProfilePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionSetProfilePhoto = new SuggestedActionSetProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionSetProfilePhoto` into TDLib JSON wire shape. */
export function encodeSuggestedActionSetProfilePhoto(value: SuggestedActionSetProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionSetProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionSubscribeToAnnualPremium = SuggestedAction — Suggests the user to subscribe to the Premium subscription with annual payments */
export class SuggestedActionSubscribeToAnnualPremium {
  type: 'suggestedActionSubscribeToAnnualPremium' = 'suggestedActionSubscribeToAnnualPremium';
  extra?: string;
}

/** Decode a TDLib `suggestedActionSubscribeToAnnualPremium` object; null input -> null. */
export function decodeSuggestedActionSubscribeToAnnualPremium(json: TdJsonRaw): SuggestedActionSubscribeToAnnualPremium | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionSubscribeToAnnualPremium = new SuggestedActionSubscribeToAnnualPremium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionSubscribeToAnnualPremium` into TDLib JSON wire shape. */
export function encodeSuggestedActionSubscribeToAnnualPremium(value: SuggestedActionSubscribeToAnnualPremium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionSubscribeToAnnualPremium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionUpgradePremium = SuggestedAction — Suggests the user to upgrade the Premium subscription from monthly payments to annual payments */
export class SuggestedActionUpgradePremium {
  type: 'suggestedActionUpgradePremium' = 'suggestedActionUpgradePremium';
  extra?: string;
}

/** Decode a TDLib `suggestedActionUpgradePremium` object; null input -> null. */
export function decodeSuggestedActionUpgradePremium(json: TdJsonRaw): SuggestedActionUpgradePremium | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionUpgradePremium = new SuggestedActionUpgradePremium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionUpgradePremium` into TDLib JSON wire shape. */
export function encodeSuggestedActionUpgradePremium(value: SuggestedActionUpgradePremium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionUpgradePremium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedActionViewChecksHint = SuggestedAction — Suggests the user to view a hint about the meaning of one and two check marks on sent messages */
export class SuggestedActionViewChecksHint {
  type: 'suggestedActionViewChecksHint' = 'suggestedActionViewChecksHint';
  extra?: string;
}

/** Decode a TDLib `suggestedActionViewChecksHint` object; null input -> null. */
export function decodeSuggestedActionViewChecksHint(json: TdJsonRaw): SuggestedActionViewChecksHint | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedActionViewChecksHint = new SuggestedActionViewChecksHint();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedActionViewChecksHint` into TDLib JSON wire shape. */
export function encodeSuggestedActionViewChecksHint(value: SuggestedActionViewChecksHint): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedActionViewChecksHint';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedPostInfo = SuggestedPostInfo — Contains information about a suggested post. If the post can be approved or declined, then changes to the post can be also suggested. Use sendMessage with reply to the message and suggested post in... */
export class SuggestedPostInfo {
  type: 'suggestedPostInfo' = 'suggestedPostInfo';
  extra?: string;
  /** price: SuggestedPostPrice — Price of the suggested post; may be null if the post is non-paid */
  price: TdSuggestedPostPrice | null = null;
  /** send_date: int32 — Point in time (Unix timestamp) when the post is expected to be published; 0 if the specific date isn't set yet */
  send_date: number = 0;
  /** state: SuggestedPostState — State of the post */
  state: TdSuggestedPostState | null = null;
  /** can_be_approved: Bool — True, if the suggested post can be approved by the current user using approveSuggestedPost; updates aren't sent when value of this field changes */
  can_be_approved: boolean = false;
  /** can_be_declined: Bool — True, if the suggested post can be declined by the current user using declineSuggestedPost; updates aren't sent when value of this field changes */
  can_be_declined: boolean = false;
}

/** Decode a TDLib `suggestedPostInfo` object; null input -> null. */
export function decodeSuggestedPostInfo(json: TdJsonRaw): SuggestedPostInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedPostInfo = new SuggestedPostInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.price = decodeTdSuggestedPostPrice(record['price']);
  out.send_date = asNumber(record['send_date']);
  out.state = decodeTdSuggestedPostState(record['state']);
  out.can_be_approved = asBool(record['can_be_approved']);
  out.can_be_declined = asBool(record['can_be_declined']);
  return out;
}

/** Encode a `suggestedPostInfo` into TDLib JSON wire shape. */
export function encodeSuggestedPostInfo(value: SuggestedPostInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedPostInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['price'] = value.price === null ? null : encodeTdSuggestedPostPrice(value.price);
  out['send_date'] = value.send_date;
  out['state'] = value.state === null ? null : encodeTdSuggestedPostState(value.state);
  out['can_be_approved'] = value.can_be_approved;
  out['can_be_declined'] = value.can_be_declined;
  return out;
}

/** suggestedPostPriceGram = SuggestedPostPrice — Describes price of a suggested post in TON Grams */
export class SuggestedPostPriceGram {
  type: 'suggestedPostPriceGram' = 'suggestedPostPriceGram';
  extra?: string;
  /** gram_cent_count: int53 — The amount of 1/100 of Gram expected to be paid for the post; getOption("suggested_post_gram_cent_count_min")-getOption("suggested_post_gram_cent_count_max") */
  gram_cent_count: number = 0;
}

/** Decode a TDLib `suggestedPostPriceGram` object; null input -> null. */
export function decodeSuggestedPostPriceGram(json: TdJsonRaw): SuggestedPostPriceGram | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedPostPriceGram = new SuggestedPostPriceGram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gram_cent_count = asNumber(record['gram_cent_count']);
  return out;
}

/** Encode a `suggestedPostPriceGram` into TDLib JSON wire shape. */
export function encodeSuggestedPostPriceGram(value: SuggestedPostPriceGram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedPostPriceGram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gram_cent_count'] = value.gram_cent_count;
  return out;
}

/** suggestedPostPriceStar = SuggestedPostPrice — Describes price of a suggested post in Telegram Stars */
export class SuggestedPostPriceStar {
  type: 'suggestedPostPriceStar' = 'suggestedPostPriceStar';
  extra?: string;
  /** star_count: int53 — The Telegram Star amount expected to be paid for the post; getOption("suggested_post_star_count_min")-getOption("suggested_post_star_count_max") */
  star_count: number = 0;
}

/** Decode a TDLib `suggestedPostPriceStar` object; null input -> null. */
export function decodeSuggestedPostPriceStar(json: TdJsonRaw): SuggestedPostPriceStar | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedPostPriceStar = new SuggestedPostPriceStar();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `suggestedPostPriceStar` into TDLib JSON wire shape. */
export function encodeSuggestedPostPriceStar(value: SuggestedPostPriceStar): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedPostPriceStar';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  return out;
}

/** suggestedPostRefundReasonPaymentRefunded = SuggestedPostRefundReason — The post was refunded, because the payment for the post was refunded */
export class SuggestedPostRefundReasonPaymentRefunded {
  type: 'suggestedPostRefundReasonPaymentRefunded' = 'suggestedPostRefundReasonPaymentRefunded';
  extra?: string;
}

/** Decode a TDLib `suggestedPostRefundReasonPaymentRefunded` object; null input -> null. */
export function decodeSuggestedPostRefundReasonPaymentRefunded(json: TdJsonRaw): SuggestedPostRefundReasonPaymentRefunded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedPostRefundReasonPaymentRefunded = new SuggestedPostRefundReasonPaymentRefunded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedPostRefundReasonPaymentRefunded` into TDLib JSON wire shape. */
export function encodeSuggestedPostRefundReasonPaymentRefunded(value: SuggestedPostRefundReasonPaymentRefunded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedPostRefundReasonPaymentRefunded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedPostRefundReasonPostDeleted = SuggestedPostRefundReason — The post was refunded, because it was deleted by channel administrators in less than getOption("suggested_post_lifetime_min") seconds */
export class SuggestedPostRefundReasonPostDeleted {
  type: 'suggestedPostRefundReasonPostDeleted' = 'suggestedPostRefundReasonPostDeleted';
  extra?: string;
}

/** Decode a TDLib `suggestedPostRefundReasonPostDeleted` object; null input -> null. */
export function decodeSuggestedPostRefundReasonPostDeleted(json: TdJsonRaw): SuggestedPostRefundReasonPostDeleted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedPostRefundReasonPostDeleted = new SuggestedPostRefundReasonPostDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedPostRefundReasonPostDeleted` into TDLib JSON wire shape. */
export function encodeSuggestedPostRefundReasonPostDeleted(value: SuggestedPostRefundReasonPostDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedPostRefundReasonPostDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedPostStateApproved = SuggestedPostState — The post was approved */
export class SuggestedPostStateApproved {
  type: 'suggestedPostStateApproved' = 'suggestedPostStateApproved';
  extra?: string;
}

/** Decode a TDLib `suggestedPostStateApproved` object; null input -> null. */
export function decodeSuggestedPostStateApproved(json: TdJsonRaw): SuggestedPostStateApproved | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedPostStateApproved = new SuggestedPostStateApproved();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedPostStateApproved` into TDLib JSON wire shape. */
export function encodeSuggestedPostStateApproved(value: SuggestedPostStateApproved): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedPostStateApproved';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedPostStateDeclined = SuggestedPostState — The post was declined */
export class SuggestedPostStateDeclined {
  type: 'suggestedPostStateDeclined' = 'suggestedPostStateDeclined';
  extra?: string;
}

/** Decode a TDLib `suggestedPostStateDeclined` object; null input -> null. */
export function decodeSuggestedPostStateDeclined(json: TdJsonRaw): SuggestedPostStateDeclined | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedPostStateDeclined = new SuggestedPostStateDeclined();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedPostStateDeclined` into TDLib JSON wire shape. */
export function encodeSuggestedPostStateDeclined(value: SuggestedPostStateDeclined): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedPostStateDeclined';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** suggestedPostStatePending = SuggestedPostState — The post must be approved or declined */
export class SuggestedPostStatePending {
  type: 'suggestedPostStatePending' = 'suggestedPostStatePending';
  extra?: string;
}

/** Decode a TDLib `suggestedPostStatePending` object; null input -> null. */
export function decodeSuggestedPostStatePending(json: TdJsonRaw): SuggestedPostStatePending | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SuggestedPostStatePending = new SuggestedPostStatePending();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `suggestedPostStatePending` into TDLib JSON wire shape. */
export function encodeSuggestedPostStatePending(value: SuggestedPostStatePending): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'suggestedPostStatePending';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** summarizeMessage = FormattedText — Summarizes content of the message with non-empty summary_language_code */
export class SummarizeMessage {
  type: 'summarizeMessage' = 'summarizeMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** translate_to_language_code: string — Pass a language code to which the summary will be translated; pass an empty string if translation isn't needed. See translateText.to_language_code for the list of supported values */
  translate_to_language_code: string = '';
  /** tone: string — Tone of the summarization; see translateText.tone for the list of supported values */
  tone: string = '';
}

/** Decode a TDLib `summarizeMessage` object; null input -> null. */
export function decodeSummarizeMessage(json: TdJsonRaw): SummarizeMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SummarizeMessage = new SummarizeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.translate_to_language_code = asString(record['translate_to_language_code']);
  out.tone = asString(record['tone']);
  return out;
}

/** Encode a `summarizeMessage` into TDLib JSON wire shape. */
export function encodeSummarizeMessage(value: SummarizeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'summarizeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['translate_to_language_code'] = value.translate_to_language_code;
  out['tone'] = value.tone;
  return out;
}

/** supergroup = Supergroup — Represents a supergroup or channel with zero or more members (subscribers in the case of channels) getChatSimilarChats, getChatsToPostStories, getCreatedPublicChats, getGroupsInCommon, getInactiveSupergroupChats... */
export class Supergroup {
  type: 'supergroup' = 'supergroup';
  extra?: string;
  /** id: int53 — Supergroup or channel identifier */
  id: number = 0;
  /** usernames: usernames — Usernames of the supergroup or channel; may be null */
  usernames: Usernames | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the current user joined, or the point in time when the supergroup or channel was created, in case the user is not a member */
  date: number = 0;
  /** status: ChatMemberStatus — Status of the current user in the supergroup or channel */
  status: TdChatMemberStatus | null = null;
  /** member_count: int32 — Number of members in the supergroup or channel; 0 if unknown. Currently, it is guaranteed to be known only if the supergroup or channel was received through */
  member_count: number = 0;
  /** boost_level: int32 — Approximate boost level for the chat */
  boost_level: number = 0;
  /** has_automatic_translation: Bool — True, if automatic translation of messages is enabled in the channel */
  has_automatic_translation: boolean = false;
  /** has_linked_chat: Bool — True, if the channel has a discussion group, or the supergroup is the designated discussion group for a channel */
  has_linked_chat: boolean = false;
  /** has_location: Bool — True, if the supergroup is connected to a location, i.e. the supergroup is a location-based supergroup */
  has_location: boolean = false;
  /** sign_messages: Bool — True, if messages sent to the channel contains name of the sender. This field is only applicable to channels */
  sign_messages: boolean = false;
  /** show_message_sender: Bool — True, if messages sent to the channel have information about the sender user. This field is only applicable to channels */
  show_message_sender: boolean = false;
  /** join_to_send_messages: Bool — True, if users need to join the supergroup before they can send messages. May be false only for discussion supergroups and channel direct messages groups */
  join_to_send_messages: boolean = false;
  /** join_by_request: Bool — True, if all users directly joining the supergroup need to be approved by supergroup administrators */
  join_by_request: boolean = false;
  /** is_slow_mode_enabled: Bool — True, if the slow mode is enabled in the supergroup */
  is_slow_mode_enabled: boolean = false;
  /** is_channel: Bool — True, if the supergroup is a channel, which can have an unlimited number of subscribers, but only administrators can post there and see the list of subscribers */
  is_channel: boolean = false;
  /** is_broadcast_group: Bool — True, if the supergroup is a broadcast group, i.e. only administrators can send messages and there is no limit on the number of members */
  is_broadcast_group: boolean = false;
  /** is_forum: Bool — True, if the supergroup is a forum with topics */
  is_forum: boolean = false;
  /** is_direct_messages_group: Bool — True, if the supergroup is a direct message group for a channel chat */
  is_direct_messages_group: boolean = false;
  /** is_administered_direct_messages_group: Bool — True, if the supergroup is a direct messages group for a channel chat that is administered by the current user */
  is_administered_direct_messages_group: boolean = false;
  /** verification_status: verificationStatus — Information about verification status of the supergroup or channel; may be null if none */
  verification_status: VerificationStatus | null = null;
  /** has_direct_messages_group: Bool — True, if the channel has direct messages group */
  has_direct_messages_group: boolean = false;
  /** has_forum_tabs: Bool — True, if the supergroup is a forum, which topics are shown in the same way as in channel direct messages groups */
  has_forum_tabs: boolean = false;
  /** restriction_info: restrictionInfo — Information about the restrictions that must be applied to the corresponding supergroup or channel chat; may be null if none */
  restriction_info: RestrictionInfo | null = null;
  /** paid_message_star_count: int53 — Number of Telegram Stars that must be paid by non-administrator users of the supergroup chat for each sent message */
  paid_message_star_count: number = 0;
  /** active_story_state: ActiveStoryState — State of active stories of the supergroup or channel; may be null if there are no active stories */
  active_story_state: TdActiveStoryState | null = null;
}

/** Decode a TDLib `supergroup` object; null input -> null. */
export function decodeSupergroup(json: TdJsonRaw): Supergroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Supergroup = new Supergroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.usernames = decodeUsernames(record['usernames']);
  out.date = asNumber(record['date']);
  out.status = decodeTdChatMemberStatus(record['status']);
  out.member_count = asNumber(record['member_count']);
  out.boost_level = asNumber(record['boost_level']);
  out.has_automatic_translation = asBool(record['has_automatic_translation']);
  out.has_linked_chat = asBool(record['has_linked_chat']);
  out.has_location = asBool(record['has_location']);
  out.sign_messages = asBool(record['sign_messages']);
  out.show_message_sender = asBool(record['show_message_sender']);
  out.join_to_send_messages = asBool(record['join_to_send_messages']);
  out.join_by_request = asBool(record['join_by_request']);
  out.is_slow_mode_enabled = asBool(record['is_slow_mode_enabled']);
  out.is_channel = asBool(record['is_channel']);
  out.is_broadcast_group = asBool(record['is_broadcast_group']);
  out.is_forum = asBool(record['is_forum']);
  out.is_direct_messages_group = asBool(record['is_direct_messages_group']);
  out.is_administered_direct_messages_group = asBool(record['is_administered_direct_messages_group']);
  out.verification_status = decodeVerificationStatus(record['verification_status']);
  out.has_direct_messages_group = asBool(record['has_direct_messages_group']);
  out.has_forum_tabs = asBool(record['has_forum_tabs']);
  out.restriction_info = decodeRestrictionInfo(record['restriction_info']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  out.active_story_state = decodeTdActiveStoryState(record['active_story_state']);
  return out;
}

/** Encode a `supergroup` into TDLib JSON wire shape. */
export function encodeSupergroup(value: Supergroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['usernames'] = value.usernames === null ? null : encodeUsernames(value.usernames);
  out['date'] = value.date;
  out['status'] = value.status === null ? null : encodeTdChatMemberStatus(value.status);
  out['member_count'] = value.member_count;
  out['boost_level'] = value.boost_level;
  out['has_automatic_translation'] = value.has_automatic_translation;
  out['has_linked_chat'] = value.has_linked_chat;
  out['has_location'] = value.has_location;
  out['sign_messages'] = value.sign_messages;
  out['show_message_sender'] = value.show_message_sender;
  out['join_to_send_messages'] = value.join_to_send_messages;
  out['join_by_request'] = value.join_by_request;
  out['is_slow_mode_enabled'] = value.is_slow_mode_enabled;
  out['is_channel'] = value.is_channel;
  out['is_broadcast_group'] = value.is_broadcast_group;
  out['is_forum'] = value.is_forum;
  out['is_direct_messages_group'] = value.is_direct_messages_group;
  out['is_administered_direct_messages_group'] = value.is_administered_direct_messages_group;
  out['verification_status'] = value.verification_status === null ? null : encodeVerificationStatus(value.verification_status);
  out['has_direct_messages_group'] = value.has_direct_messages_group;
  out['has_forum_tabs'] = value.has_forum_tabs;
  out['restriction_info'] = value.restriction_info === null ? null : encodeRestrictionInfo(value.restriction_info);
  out['paid_message_star_count'] = value.paid_message_star_count;
  out['active_story_state'] = value.active_story_state === null ? null : encodeTdActiveStoryState(value.active_story_state);
  return out;
}

/** supergroupFullInfo = SupergroupFullInfo — Contains full information about a supergroup or channel so this option affects only private non-forum supergroups without a linked chat. The value of this field is only available to chat administ... */
export class SupergroupFullInfo {
  type: 'supergroupFullInfo' = 'supergroupFullInfo';
  extra?: string;
  /** photo: chatPhoto — Chat photo; may be null if empty or unknown. If non-null, then it is the same photo as in chat.photo */
  photo: ChatPhoto | null = null;
  /** community_id: int53 — Identifier of the community to which the corresponding chat was added */
  community_id: number = 0;
  /** description: string */
  description: string = '';
  /** member_count: int32 — Number of members in the supergroup or channel; 0 if unknown */
  member_count: number = 0;
  /** administrator_count: int32 — Number of privileged users in the supergroup or channel; 0 if unknown */
  administrator_count: number = 0;
  /** restricted_count: int32 — Number of restricted users in the supergroup; 0 if unknown */
  restricted_count: number = 0;
  /** banned_count: int32 — Number of users banned from chat; 0 if unknown */
  banned_count: number = 0;
  /** linked_chat_id: int53 — Chat identifier of a discussion group for the channel, or a channel, for which the supergroup is the designated discussion group; 0 if none or unknown */
  linked_chat_id: number = 0;
  /** direct_messages_chat_id: int53 — Chat identifier of a direct messages group for the channel, or a channel, for which the supergroup is the designated direct messages group; 0 if none */
  direct_messages_chat_id: number = 0;
  /** slow_mode_delay: int32 — Delay between consecutive sent messages for non-administrator supergroup members, in seconds */
  slow_mode_delay: number = 0;
  /** slow_mode_delay_expires_in: double — Time left before next message can be sent in the supergroup, in seconds. An updateSupergroupFullInfo update is not triggered when value of this field changes, but both new and old values are non-zero */
  slow_mode_delay_expires_in: number = 0;
  /** can_enable_paid_messages: Bool — True, if paid messages can be enabled in the supergroup chat; for supergroup only */
  can_enable_paid_messages: boolean = false;
  /** can_enable_paid_reaction: Bool — True, if paid reaction can be enabled in the channel chat; for channels only */
  can_enable_paid_reaction: boolean = false;
  /** can_get_members: Bool — True, if members of the chat can be retrieved via getSupergroupMembers or searchChatMembers */
  can_get_members: boolean = false;
  /** has_hidden_members: Bool — True, if non-administrators can receive only administrators and bots using getSupergroupMembers or searchChatMembers */
  has_hidden_members: boolean = false;
  /** can_hide_members: Bool — True, if non-administrators and non-bots can be hidden in responses to getSupergroupMembers and searchChatMembers for non-administrators */
  can_hide_members: boolean = false;
  /** can_set_sticker_set: Bool — True, if the supergroup sticker set can be changed */
  can_set_sticker_set: boolean = false;
  /** can_set_location: Bool — True, if the supergroup location can be changed */
  can_set_location: boolean = false;
  /** can_get_statistics: Bool — True, if the supergroup or channel statistics are available */
  can_get_statistics: boolean = false;
  /** can_get_revenue_statistics: Bool — True, if the supergroup or channel revenue statistics are available */
  can_get_revenue_statistics: boolean = false;
  /** can_get_star_revenue_statistics: Bool — True, if the supergroup or channel Telegram Star revenue statistics are available */
  can_get_star_revenue_statistics: boolean = false;
  /** can_send_gift: Bool — True, if the user can send a gift to the supergroup or channel using sendGift or transferGift */
  can_send_gift: boolean = false;
  /** can_toggle_aggressive_anti_spam: Bool — True, if aggressive anti-spam checks can be enabled or disabled in the supergroup */
  can_toggle_aggressive_anti_spam: boolean = false;
  /** is_all_history_available: Bool — True, if new chat members will have access to old messages. In public, discussion, of forum groups and all channels, old messages are always available, */
  is_all_history_available: boolean = false;
  /** can_have_sponsored_messages: Bool — True, if the chat can have sponsored messages. The value of this field is only available to the owner of the chat */
  can_have_sponsored_messages: boolean = false;
  /** has_aggressive_anti_spam_enabled: Bool — True, if aggressive anti-spam checks are enabled in the supergroup. The value of this field is only available to chat administrators */
  has_aggressive_anti_spam_enabled: boolean = false;
  /** has_paid_media_allowed: Bool — True, if paid media can be sent and forwarded to the channel chat; for channels only */
  has_paid_media_allowed: boolean = false;
  /** has_pinned_stories: Bool — True, if the supergroup or channel has pinned stories */
  has_pinned_stories: boolean = false;
  /** gift_count: int32 — Number of saved to profile gifts for channels without can_post_messages administrator right, otherwise, the total number of received gifts */
  gift_count: number = 0;
  /** my_boost_count: int32 — Number of times the current user boosted the supergroup or channel */
  my_boost_count: number = 0;
  /** unrestrict_boost_count: int32 — Number of times the supergroup must be boosted by a user to ignore slow mode and chat permission restrictions; 0 if unspecified */
  unrestrict_boost_count: number = 0;
  /** outgoing_paid_message_star_count: int53 — Number of Telegram Stars that must be paid by the current user for each sent message to the supergroup */
  outgoing_paid_message_star_count: number = 0;
  /** sticker_set_id: int64 — Identifier of the supergroup sticker set that must be shown before user sticker sets; 0 if none */
  sticker_set_id: string = '';
  /** custom_emoji_sticker_set_id: int64 — Identifier of the custom emoji sticker set that can be used in the supergroup without Telegram Premium subscription; 0 if none */
  custom_emoji_sticker_set_id: string = '';
  /** location: chatLocation — Location to which the supergroup is connected; may be null if none */
  location: ChatLocation | null = null;
  /** invite_link: chatInviteLink — Primary invite link for the chat; may be null. For chat administrators with can_invite_users right only */
  invite_link: ChatInviteLink | null = null;
  /** guard_bot_user_id: int53 — User identifier of the guard bot in the group; for chat administrators only */
  guard_bot_user_id: number = 0;
  /** bot_commands: vector<botCommands> — List of commands of bots in the group */
  bot_commands: BotCommands[] = [];
  /** bot_verification: botVerification — Information about verification status of the supergroup or the channel provided by a bot; may be null if none or unknown */
  bot_verification: BotVerification | null = null;
  /** main_profile_tab: ProfileTab — The main tab chosen by the administrators of the channel; may be null if not chosen manually */
  main_profile_tab: TdProfileTab | null = null;
  /** upgraded_from_basic_group_id: int53 — Identifier of the basic group from which supergroup was upgraded; 0 if none */
  upgraded_from_basic_group_id: number = 0;
  /** upgraded_from_max_message_id: int53 — Identifier of the last message in the basic group from which supergroup was upgraded; 0 if none */
  upgraded_from_max_message_id: number = 0;
}

/** Decode a TDLib `supergroupFullInfo` object; null input -> null. */
export function decodeSupergroupFullInfo(json: TdJsonRaw): SupergroupFullInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SupergroupFullInfo = new SupergroupFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  out.community_id = asNumber(record['community_id']);
  out.description = asString(record['description']);
  out.member_count = asNumber(record['member_count']);
  out.administrator_count = asNumber(record['administrator_count']);
  out.restricted_count = asNumber(record['restricted_count']);
  out.banned_count = asNumber(record['banned_count']);
  out.linked_chat_id = asNumber(record['linked_chat_id']);
  out.direct_messages_chat_id = asNumber(record['direct_messages_chat_id']);
  out.slow_mode_delay = asNumber(record['slow_mode_delay']);
  out.slow_mode_delay_expires_in = asNumber(record['slow_mode_delay_expires_in']);
  out.can_enable_paid_messages = asBool(record['can_enable_paid_messages']);
  out.can_enable_paid_reaction = asBool(record['can_enable_paid_reaction']);
  out.can_get_members = asBool(record['can_get_members']);
  out.has_hidden_members = asBool(record['has_hidden_members']);
  out.can_hide_members = asBool(record['can_hide_members']);
  out.can_set_sticker_set = asBool(record['can_set_sticker_set']);
  out.can_set_location = asBool(record['can_set_location']);
  out.can_get_statistics = asBool(record['can_get_statistics']);
  out.can_get_revenue_statistics = asBool(record['can_get_revenue_statistics']);
  out.can_get_star_revenue_statistics = asBool(record['can_get_star_revenue_statistics']);
  out.can_send_gift = asBool(record['can_send_gift']);
  out.can_toggle_aggressive_anti_spam = asBool(record['can_toggle_aggressive_anti_spam']);
  out.is_all_history_available = asBool(record['is_all_history_available']);
  out.can_have_sponsored_messages = asBool(record['can_have_sponsored_messages']);
  out.has_aggressive_anti_spam_enabled = asBool(record['has_aggressive_anti_spam_enabled']);
  out.has_paid_media_allowed = asBool(record['has_paid_media_allowed']);
  out.has_pinned_stories = asBool(record['has_pinned_stories']);
  out.gift_count = asNumber(record['gift_count']);
  out.my_boost_count = asNumber(record['my_boost_count']);
  out.unrestrict_boost_count = asNumber(record['unrestrict_boost_count']);
  out.outgoing_paid_message_star_count = asNumber(record['outgoing_paid_message_star_count']);
  out.sticker_set_id = asInt64(record['sticker_set_id']);
  out.custom_emoji_sticker_set_id = asInt64(record['custom_emoji_sticker_set_id']);
  out.location = decodeChatLocation(record['location']);
  out.invite_link = decodeChatInviteLink(record['invite_link']);
  out.guard_bot_user_id = asNumber(record['guard_bot_user_id']);
  out.bot_commands = decodeVector(record['bot_commands'], (item: TdJsonRaw): BotCommands => decodeBotCommands(item) as BotCommands);
  out.bot_verification = decodeBotVerification(record['bot_verification']);
  out.main_profile_tab = decodeTdProfileTab(record['main_profile_tab']);
  out.upgraded_from_basic_group_id = asNumber(record['upgraded_from_basic_group_id']);
  out.upgraded_from_max_message_id = asNumber(record['upgraded_from_max_message_id']);
  return out;
}

/** Encode a `supergroupFullInfo` into TDLib JSON wire shape. */
export function encodeSupergroupFullInfo(value: SupergroupFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroupFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  out['community_id'] = value.community_id;
  out['description'] = value.description;
  out['member_count'] = value.member_count;
  out['administrator_count'] = value.administrator_count;
  out['restricted_count'] = value.restricted_count;
  out['banned_count'] = value.banned_count;
  out['linked_chat_id'] = value.linked_chat_id;
  out['direct_messages_chat_id'] = value.direct_messages_chat_id;
  out['slow_mode_delay'] = value.slow_mode_delay;
  out['slow_mode_delay_expires_in'] = value.slow_mode_delay_expires_in;
  out['can_enable_paid_messages'] = value.can_enable_paid_messages;
  out['can_enable_paid_reaction'] = value.can_enable_paid_reaction;
  out['can_get_members'] = value.can_get_members;
  out['has_hidden_members'] = value.has_hidden_members;
  out['can_hide_members'] = value.can_hide_members;
  out['can_set_sticker_set'] = value.can_set_sticker_set;
  out['can_set_location'] = value.can_set_location;
  out['can_get_statistics'] = value.can_get_statistics;
  out['can_get_revenue_statistics'] = value.can_get_revenue_statistics;
  out['can_get_star_revenue_statistics'] = value.can_get_star_revenue_statistics;
  out['can_send_gift'] = value.can_send_gift;
  out['can_toggle_aggressive_anti_spam'] = value.can_toggle_aggressive_anti_spam;
  out['is_all_history_available'] = value.is_all_history_available;
  out['can_have_sponsored_messages'] = value.can_have_sponsored_messages;
  out['has_aggressive_anti_spam_enabled'] = value.has_aggressive_anti_spam_enabled;
  out['has_paid_media_allowed'] = value.has_paid_media_allowed;
  out['has_pinned_stories'] = value.has_pinned_stories;
  out['gift_count'] = value.gift_count;
  out['my_boost_count'] = value.my_boost_count;
  out['unrestrict_boost_count'] = value.unrestrict_boost_count;
  out['outgoing_paid_message_star_count'] = value.outgoing_paid_message_star_count;
  out['sticker_set_id'] = value.sticker_set_id;
  out['custom_emoji_sticker_set_id'] = value.custom_emoji_sticker_set_id;
  out['location'] = value.location === null ? null : encodeChatLocation(value.location);
  out['invite_link'] = value.invite_link === null ? null : encodeChatInviteLink(value.invite_link);
  out['guard_bot_user_id'] = value.guard_bot_user_id;
  out['bot_commands'] = encodeVector(value.bot_commands, (item: BotCommands | null): TdJsonRaw => item === null ? null : encodeBotCommands(item));
  out['bot_verification'] = value.bot_verification === null ? null : encodeBotVerification(value.bot_verification);
  out['main_profile_tab'] = value.main_profile_tab === null ? null : encodeTdProfileTab(value.main_profile_tab);
  out['upgraded_from_basic_group_id'] = value.upgraded_from_basic_group_id;
  out['upgraded_from_max_message_id'] = value.upgraded_from_max_message_id;
  return out;
}

/** supergroupMembersFilterAdministrators = SupergroupMembersFilter — Returns the owner and administrators */
export class SupergroupMembersFilterAdministrators {
  type: 'supergroupMembersFilterAdministrators' = 'supergroupMembersFilterAdministrators';
  extra?: string;
}

/** Decode a TDLib `supergroupMembersFilterAdministrators` object; null input -> null. */
export function decodeSupergroupMembersFilterAdministrators(json: TdJsonRaw): SupergroupMembersFilterAdministrators | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SupergroupMembersFilterAdministrators = new SupergroupMembersFilterAdministrators();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `supergroupMembersFilterAdministrators` into TDLib JSON wire shape. */
export function encodeSupergroupMembersFilterAdministrators(value: SupergroupMembersFilterAdministrators): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroupMembersFilterAdministrators';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** supergroupMembersFilterBanned = SupergroupMembersFilter — Returns users banned from the supergroup or channel; can be used only by administrators */
export class SupergroupMembersFilterBanned {
  type: 'supergroupMembersFilterBanned' = 'supergroupMembersFilterBanned';
  extra?: string;
  /** query: string — Query to search for */
  query: string = '';
}

/** Decode a TDLib `supergroupMembersFilterBanned` object; null input -> null. */
export function decodeSupergroupMembersFilterBanned(json: TdJsonRaw): SupergroupMembersFilterBanned | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SupergroupMembersFilterBanned = new SupergroupMembersFilterBanned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  return out;
}

/** Encode a `supergroupMembersFilterBanned` into TDLib JSON wire shape. */
export function encodeSupergroupMembersFilterBanned(value: SupergroupMembersFilterBanned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroupMembersFilterBanned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  return out;
}

/** supergroupMembersFilterBots = SupergroupMembersFilter — Returns bot members of the supergroup or channel */
export class SupergroupMembersFilterBots {
  type: 'supergroupMembersFilterBots' = 'supergroupMembersFilterBots';
  extra?: string;
}

/** Decode a TDLib `supergroupMembersFilterBots` object; null input -> null. */
export function decodeSupergroupMembersFilterBots(json: TdJsonRaw): SupergroupMembersFilterBots | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SupergroupMembersFilterBots = new SupergroupMembersFilterBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `supergroupMembersFilterBots` into TDLib JSON wire shape. */
export function encodeSupergroupMembersFilterBots(value: SupergroupMembersFilterBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroupMembersFilterBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** supergroupMembersFilterContacts = SupergroupMembersFilter — Returns contacts of the current user who are members of the supergroup or channel */
export class SupergroupMembersFilterContacts {
  type: 'supergroupMembersFilterContacts' = 'supergroupMembersFilterContacts';
  extra?: string;
  /** query: string — Query to search for */
  query: string = '';
}

/** Decode a TDLib `supergroupMembersFilterContacts` object; null input -> null. */
export function decodeSupergroupMembersFilterContacts(json: TdJsonRaw): SupergroupMembersFilterContacts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SupergroupMembersFilterContacts = new SupergroupMembersFilterContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  return out;
}

/** Encode a `supergroupMembersFilterContacts` into TDLib JSON wire shape. */
export function encodeSupergroupMembersFilterContacts(value: SupergroupMembersFilterContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroupMembersFilterContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  return out;
}

/** supergroupMembersFilterMention = SupergroupMembersFilter — Returns users who can be mentioned in the supergroup */
export class SupergroupMembersFilterMention {
  type: 'supergroupMembersFilterMention' = 'supergroupMembersFilterMention';
  extra?: string;
  /** query: string — Query to search for */
  query: string = '';
  /** topic_id: MessageTopic — Identifier of the topic in which the users will be mentioned; pass null if none */
  topic_id: TdMessageTopic | null = null;
}

/** Decode a TDLib `supergroupMembersFilterMention` object; null input -> null. */
export function decodeSupergroupMembersFilterMention(json: TdJsonRaw): SupergroupMembersFilterMention | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SupergroupMembersFilterMention = new SupergroupMembersFilterMention();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  return out;
}

/** Encode a `supergroupMembersFilterMention` into TDLib JSON wire shape. */
export function encodeSupergroupMembersFilterMention(value: SupergroupMembersFilterMention): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroupMembersFilterMention';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  return out;
}

/** supergroupMembersFilterRecent = SupergroupMembersFilter — Returns recently active users in reverse chronological order */
export class SupergroupMembersFilterRecent {
  type: 'supergroupMembersFilterRecent' = 'supergroupMembersFilterRecent';
  extra?: string;
}

/** Decode a TDLib `supergroupMembersFilterRecent` object; null input -> null. */
export function decodeSupergroupMembersFilterRecent(json: TdJsonRaw): SupergroupMembersFilterRecent | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SupergroupMembersFilterRecent = new SupergroupMembersFilterRecent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `supergroupMembersFilterRecent` into TDLib JSON wire shape. */
export function encodeSupergroupMembersFilterRecent(value: SupergroupMembersFilterRecent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroupMembersFilterRecent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** supergroupMembersFilterRestricted = SupergroupMembersFilter — Returns restricted supergroup members; can be used only by administrators */
export class SupergroupMembersFilterRestricted {
  type: 'supergroupMembersFilterRestricted' = 'supergroupMembersFilterRestricted';
  extra?: string;
  /** query: string — Query to search for */
  query: string = '';
}

/** Decode a TDLib `supergroupMembersFilterRestricted` object; null input -> null. */
export function decodeSupergroupMembersFilterRestricted(json: TdJsonRaw): SupergroupMembersFilterRestricted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SupergroupMembersFilterRestricted = new SupergroupMembersFilterRestricted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  return out;
}

/** Encode a `supergroupMembersFilterRestricted` into TDLib JSON wire shape. */
export function encodeSupergroupMembersFilterRestricted(value: SupergroupMembersFilterRestricted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroupMembersFilterRestricted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  return out;
}

/** supergroupMembersFilterSearch = SupergroupMembersFilter — Used to search for supergroup or channel members via a (string) query */
export class SupergroupMembersFilterSearch {
  type: 'supergroupMembersFilterSearch' = 'supergroupMembersFilterSearch';
  extra?: string;
  /** query: string — Query to search for */
  query: string = '';
}

/** Decode a TDLib `supergroupMembersFilterSearch` object; null input -> null. */
export function decodeSupergroupMembersFilterSearch(json: TdJsonRaw): SupergroupMembersFilterSearch | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SupergroupMembersFilterSearch = new SupergroupMembersFilterSearch();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  return out;
}

/** Encode a `supergroupMembersFilterSearch` into TDLib JSON wire shape. */
export function encodeSupergroupMembersFilterSearch(value: SupergroupMembersFilterSearch): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'supergroupMembersFilterSearch';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  return out;
}

/** synchronizeLanguagePack = Ok — Fetches the latest versions of all strings from a language pack in the current localization target from the server. This method doesn't need to be called explicitly for the current used/base language packs.... */
export class SynchronizeLanguagePack {
  type: 'synchronizeLanguagePack' = 'synchronizeLanguagePack';
  extra?: string;
  /** language_pack_id: string — Language pack identifier */
  language_pack_id: string = '';
}

/** Decode a TDLib `synchronizeLanguagePack` object; null input -> null. */
export function decodeSynchronizeLanguagePack(json: TdJsonRaw): SynchronizeLanguagePack | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: SynchronizeLanguagePack = new SynchronizeLanguagePack();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_pack_id = asString(record['language_pack_id']);
  return out;
}

/** Encode a `synchronizeLanguagePack` into TDLib JSON wire shape. */
export function encodeSynchronizeLanguagePack(value: SynchronizeLanguagePack): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'synchronizeLanguagePack';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_pack_id'] = value.language_pack_id;
  return out;
}
