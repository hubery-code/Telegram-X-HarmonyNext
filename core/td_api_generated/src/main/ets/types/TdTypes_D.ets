/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { File, FormattedText, decodeFile, decodeFormattedText, encodeFile, encodeFormattedText } from './TdTypes_F';
import { InputRichMessage, InputSuggestedPostInfo, decodeInputRichMessage, decodeInputSuggestedPostInfo, encodeInputRichMessage, encodeInputSuggestedPostInfo } from './TdTypes_I';
import { LinkPreviewOptions, decodeLinkPreviewOptions, encodeLinkPreviewOptions } from './TdTypes_L';
import { Message, Minithumbnail, decodeMessage, decodeMinithumbnail, encodeMessage, encodeMinithumbnail } from './TdTypes_M';
import { RichMessage, decodeRichMessage, encodeRichMessage } from './TdTypes_R';
import { Sticker, decodeSticker, encodeSticker } from './TdTypes_S';
import { Thumbnail, decodeThumbnail, encodeThumbnail } from './TdTypes_T';
import { TdAffiliateType, TdBotCommandScope, TdDateTimePartPrecision, TdDiffEntityType, TdDraftMessageContent, TdGroupCallDataChannel, TdInputMessageReplyTo, TdMessageSelfDestructType, TdMessageSender, TdPassportElementType, decodeTdAffiliateType, decodeTdBotCommandScope, decodeTdDateTimePartPrecision, decodeTdDiffEntityType, decodeTdDraftMessageContent, decodeTdGroupCallDataChannel, decodeTdInputMessageReplyTo, decodeTdMessageSelfDestructType, decodeTdMessageSender, decodeTdPassportElementType, encodeTdAffiliateType, encodeTdBotCommandScope, encodeTdDateTimePartPrecision, encodeTdDiffEntityType, encodeTdDraftMessageContent, encodeTdGroupCallDataChannel, encodeTdInputMessageReplyTo, encodeTdMessageSelfDestructType, encodeTdMessageSender, encodeTdPassportElementType } from './TdUnions';

/** data = Data — Contains some binary data */
export class Data {
  type: 'data' = 'data';
  extra?: string;
  /** data: bytes — Data */
  data: string = '';
}

/** Decode a TDLib `data` object; null input -> null. */
export function decodeData(json: TdJsonRaw): Data | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Data = new Data();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.data = asString(record['data']);
  return out;
}

/** Encode a `data` into TDLib JSON wire shape. */
export function encodeData(value: Data): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'data';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['data'] = value.data;
  return out;
}

/** databaseStatistics = DatabaseStatistics — Contains database statistics */
export class DatabaseStatistics {
  type: 'databaseStatistics' = 'databaseStatistics';
  extra?: string;
  /** statistics: string — Database statistics in an unspecified human-readable format */
  statistics: string = '';
}

/** Decode a TDLib `databaseStatistics` object; null input -> null. */
export function decodeDatabaseStatistics(json: TdJsonRaw): DatabaseStatistics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DatabaseStatistics = new DatabaseStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.statistics = asString(record['statistics']);
  return out;
}

/** Encode a `databaseStatistics` into TDLib JSON wire shape. */
export function encodeDatabaseStatistics(value: DatabaseStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'databaseStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['statistics'] = value.statistics;
  return out;
}

/** date = Date — Represents a date according to the Gregorian calendar */
export class DateValue {
  type: 'date' = 'date';
  extra?: string;
  /** day: int32 — Day of the month; 1-31 */
  day: number = 0;
  /** month: int32 — Month; 1-12 */
  month: number = 0;
  /** year: int32 — Year; 1-9999 */
  year: number = 0;
}

/** Decode a TDLib `date` object; null input -> null. */
export function decodeDateValue(json: TdJsonRaw): DateValue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DateValue = new DateValue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.day = asNumber(record['day']);
  out.month = asNumber(record['month']);
  out.year = asNumber(record['year']);
  return out;
}

/** Encode a `date` into TDLib JSON wire shape. */
export function encodeDateValue(value: DateValue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'date';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['day'] = value.day;
  out['month'] = value.month;
  out['year'] = value.year;
  return out;
}

/** dateRange = DateRange — Represents a date range */
export class DateRange {
  type: 'dateRange' = 'dateRange';
  extra?: string;
  /** start_date: int32 — Point in time (Unix timestamp) at which the date range begins */
  start_date: number = 0;
  /** end_date: int32 — Point in time (Unix timestamp) at which the date range ends */
  end_date: number = 0;
}

/** Decode a TDLib `dateRange` object; null input -> null. */
export function decodeDateRange(json: TdJsonRaw): DateRange | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DateRange = new DateRange();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.start_date = asNumber(record['start_date']);
  out.end_date = asNumber(record['end_date']);
  return out;
}

/** Encode a `dateRange` into TDLib JSON wire shape. */
export function encodeDateRange(value: DateRange): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'dateRange';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['start_date'] = value.start_date;
  out['end_date'] = value.end_date;
  return out;
}

/** dateTimeFormattingTypeAbsolute = DateTimeFormattingType — The date and time must be shown as absolute timestamps */
export class DateTimeFormattingTypeAbsolute {
  type: 'dateTimeFormattingTypeAbsolute' = 'dateTimeFormattingTypeAbsolute';
  extra?: string;
  /** time_precision: DateTimePartPrecision — The precision with which hours, minutes and seconds are shown */
  time_precision: TdDateTimePartPrecision | null = null;
  /** date_precision: DateTimePartPrecision — The precision with which the date is shown */
  date_precision: TdDateTimePartPrecision | null = null;
  /** show_day_of_week: Bool — True, if the day of week must be shown */
  show_day_of_week: boolean = false;
}

/** Decode a TDLib `dateTimeFormattingTypeAbsolute` object; null input -> null. */
export function decodeDateTimeFormattingTypeAbsolute(json: TdJsonRaw): DateTimeFormattingTypeAbsolute | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DateTimeFormattingTypeAbsolute = new DateTimeFormattingTypeAbsolute();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.time_precision = decodeTdDateTimePartPrecision(record['time_precision']);
  out.date_precision = decodeTdDateTimePartPrecision(record['date_precision']);
  out.show_day_of_week = asBool(record['show_day_of_week']);
  return out;
}

/** Encode a `dateTimeFormattingTypeAbsolute` into TDLib JSON wire shape. */
export function encodeDateTimeFormattingTypeAbsolute(value: DateTimeFormattingTypeAbsolute): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'dateTimeFormattingTypeAbsolute';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['time_precision'] = value.time_precision === null ? null : encodeTdDateTimePartPrecision(value.time_precision);
  out['date_precision'] = value.date_precision === null ? null : encodeTdDateTimePartPrecision(value.date_precision);
  out['show_day_of_week'] = value.show_day_of_week;
  return out;
}

/** dateTimeFormattingTypeRelative = DateTimeFormattingType — The time must be shown relative to the current time ([in ] X seconds, minutes, hours, days, months, years [ago]) */
export class DateTimeFormattingTypeRelative {
  type: 'dateTimeFormattingTypeRelative' = 'dateTimeFormattingTypeRelative';
  extra?: string;
}

/** Decode a TDLib `dateTimeFormattingTypeRelative` object; null input -> null. */
export function decodeDateTimeFormattingTypeRelative(json: TdJsonRaw): DateTimeFormattingTypeRelative | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DateTimeFormattingTypeRelative = new DateTimeFormattingTypeRelative();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `dateTimeFormattingTypeRelative` into TDLib JSON wire shape. */
export function encodeDateTimeFormattingTypeRelative(value: DateTimeFormattingTypeRelative): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'dateTimeFormattingTypeRelative';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** dateTimePartPrecisionLong = DateTimePartPrecision — Show the date or time in a long way (March 17, 2022 or 22:45:00) */
export class DateTimePartPrecisionLong {
  type: 'dateTimePartPrecisionLong' = 'dateTimePartPrecisionLong';
  extra?: string;
}

/** Decode a TDLib `dateTimePartPrecisionLong` object; null input -> null. */
export function decodeDateTimePartPrecisionLong(json: TdJsonRaw): DateTimePartPrecisionLong | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DateTimePartPrecisionLong = new DateTimePartPrecisionLong();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `dateTimePartPrecisionLong` into TDLib JSON wire shape. */
export function encodeDateTimePartPrecisionLong(value: DateTimePartPrecisionLong): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'dateTimePartPrecisionLong';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** dateTimePartPrecisionNone = DateTimePartPrecision — Don't show the date or time */
export class DateTimePartPrecisionNone {
  type: 'dateTimePartPrecisionNone' = 'dateTimePartPrecisionNone';
  extra?: string;
}

/** Decode a TDLib `dateTimePartPrecisionNone` object; null input -> null. */
export function decodeDateTimePartPrecisionNone(json: TdJsonRaw): DateTimePartPrecisionNone | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DateTimePartPrecisionNone = new DateTimePartPrecisionNone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `dateTimePartPrecisionNone` into TDLib JSON wire shape. */
export function encodeDateTimePartPrecisionNone(value: DateTimePartPrecisionNone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'dateTimePartPrecisionNone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** dateTimePartPrecisionShort = DateTimePartPrecision — Show the date or time in a short way (17.03.22 or 22:45) */
export class DateTimePartPrecisionShort {
  type: 'dateTimePartPrecisionShort' = 'dateTimePartPrecisionShort';
  extra?: string;
}

/** Decode a TDLib `dateTimePartPrecisionShort` object; null input -> null. */
export function decodeDateTimePartPrecisionShort(json: TdJsonRaw): DateTimePartPrecisionShort | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DateTimePartPrecisionShort = new DateTimePartPrecisionShort();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `dateTimePartPrecisionShort` into TDLib JSON wire shape. */
export function encodeDateTimePartPrecisionShort(value: DateTimePartPrecisionShort): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'dateTimePartPrecisionShort';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** datedFile = DatedFile — File with the date it was uploaded */
export class DatedFile {
  type: 'datedFile' = 'datedFile';
  extra?: string;
  /** file: file — The file */
  file: File | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the file was uploaded */
  date: number = 0;
}

/** Decode a TDLib `datedFile` object; null input -> null. */
export function decodeDatedFile(json: TdJsonRaw): DatedFile | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DatedFile = new DatedFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file = decodeFile(record['file']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `datedFile` into TDLib JSON wire shape. */
export function encodeDatedFile(value: DatedFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'datedFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file'] = value.file === null ? null : encodeFile(value.file);
  out['date'] = value.date;
  return out;
}

/** declineGroupCallInvitation = Ok — Declines an invitation to an active group call via messageGroupCall. Can be called both by the sender and the receiver of the invitation */
export class DeclineGroupCallInvitation {
  type: 'declineGroupCallInvitation' = 'declineGroupCallInvitation';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message of the type messageGroupCall */
  message_id: number = 0;
}

/** Decode a TDLib `declineGroupCallInvitation` object; null input -> null. */
export function decodeDeclineGroupCallInvitation(json: TdJsonRaw): DeclineGroupCallInvitation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeclineGroupCallInvitation = new DeclineGroupCallInvitation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `declineGroupCallInvitation` into TDLib JSON wire shape. */
export function encodeDeclineGroupCallInvitation(value: DeclineGroupCallInvitation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'declineGroupCallInvitation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** declineOauthRequest = Ok — Declines an OAuth authorization request */
export class DeclineOauthRequest {
  type: 'declineOauthRequest' = 'declineOauthRequest';
  extra?: string;
  /** url: string — URL of the OAuth deep link */
  url: string = '';
}

/** Decode a TDLib `declineOauthRequest` object; null input -> null. */
export function decodeDeclineOauthRequest(json: TdJsonRaw): DeclineOauthRequest | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeclineOauthRequest = new DeclineOauthRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `declineOauthRequest` into TDLib JSON wire shape. */
export function encodeDeclineOauthRequest(value: DeclineOauthRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'declineOauthRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** declineSuggestedPost = Ok — Declines a suggested post in a channel direct messages chat */
export class DeclineSuggestedPost {
  type: 'declineSuggestedPost' = 'declineSuggestedPost';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message with the suggested post. Use messageProperties.can_be_declined to check whether the suggested post can be declined */
  message_id: number = 0;
  /** comment: string — Comment for the creator of the suggested post; 0-128 characters */
  comment: string = '';
}

/** Decode a TDLib `declineSuggestedPost` object; null input -> null. */
export function decodeDeclineSuggestedPost(json: TdJsonRaw): DeclineSuggestedPost | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeclineSuggestedPost = new DeclineSuggestedPost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.comment = asString(record['comment']);
  return out;
}

/** Encode a `declineSuggestedPost` into TDLib JSON wire shape. */
export function encodeDeclineSuggestedPost(value: DeclineSuggestedPost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'declineSuggestedPost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['comment'] = value.comment;
  return out;
}

/** decryptGroupCallData = Data — Decrypts group call data received by tgcalls */
export class DecryptGroupCallData {
  type: 'decryptGroupCallData' = 'decryptGroupCallData';
  extra?: string;
  /** group_call_id: int32 — Group call identifier. The call must not be a video chat */
  group_call_id: number = 0;
  /** participant_id: MessageSender — Identifier of the group call participant, which sent the data */
  participant_id: TdMessageSender | null = null;
  /** data_channel: GroupCallDataChannel — Data channel for which data was encrypted; pass null if unknown */
  data_channel: TdGroupCallDataChannel | null = null;
  /** data: bytes — Data to decrypt */
  data: string = '';
}

/** Decode a TDLib `decryptGroupCallData` object; null input -> null. */
export function decodeDecryptGroupCallData(json: TdJsonRaw): DecryptGroupCallData | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DecryptGroupCallData = new DecryptGroupCallData();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.participant_id = decodeTdMessageSender(record['participant_id']);
  out.data_channel = decodeTdGroupCallDataChannel(record['data_channel']);
  out.data = asString(record['data']);
  return out;
}

/** Encode a `decryptGroupCallData` into TDLib JSON wire shape. */
export function encodeDecryptGroupCallData(value: DecryptGroupCallData): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'decryptGroupCallData';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['participant_id'] = value.participant_id === null ? null : encodeTdMessageSender(value.participant_id);
  out['data_channel'] = value.data_channel === null ? null : encodeTdGroupCallDataChannel(value.data_channel);
  out['data'] = value.data;
  return out;
}

/** deepLinkInfo = DeepLinkInfo — Contains information about a tg: deep link */
export class DeepLinkInfo {
  type: 'deepLinkInfo' = 'deepLinkInfo';
  extra?: string;
  /** text: formattedText — Text to be shown to the user */
  text: FormattedText | null = null;
  /** need_update_application: Bool — True, if the user must be asked to update the application */
  need_update_application: boolean = false;
}

/** Decode a TDLib `deepLinkInfo` object; null input -> null. */
export function decodeDeepLinkInfo(json: TdJsonRaw): DeepLinkInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeepLinkInfo = new DeepLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.need_update_application = asBool(record['need_update_application']);
  return out;
}

/** Encode a `deepLinkInfo` into TDLib JSON wire shape. */
export function encodeDeepLinkInfo(value: DeepLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deepLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['need_update_application'] = value.need_update_application;
  return out;
}

/** deleteAccount = Ok — Deletes the account of the current user, deleting all information associated with the user from the server. The phone number of the account can be used to create a new account. Can be called before authorization when... */
export class DeleteAccount {
  type: 'deleteAccount' = 'deleteAccount';
  extra?: string;
  /** reason: string — The reason why the account was deleted; optional */
  reason: string = '';
  /** password: string — The 2-step verification password of the current user. If the current user isn't authorized, then an empty string can be passed and account deletion can be canceled within one week */
  password: string = '';
}

/** Decode a TDLib `deleteAccount` object; null input -> null. */
export function decodeDeleteAccount(json: TdJsonRaw): DeleteAccount | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteAccount = new DeleteAccount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reason = asString(record['reason']);
  out.password = asString(record['password']);
  return out;
}

/** Encode a `deleteAccount` into TDLib JSON wire shape. */
export function encodeDeleteAccount(value: DeleteAccount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteAccount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reason'] = value.reason;
  out['password'] = value.password;
  return out;
}

/** deleteAllCallMessages = Ok — Deletes all call messages */
export class DeleteAllCallMessages {
  type: 'deleteAllCallMessages' = 'deleteAllCallMessages';
  extra?: string;
  /** revoke: Bool — Pass true to delete the messages for all users */
  revoke: boolean = false;
}

/** Decode a TDLib `deleteAllCallMessages` object; null input -> null. */
export function decodeDeleteAllCallMessages(json: TdJsonRaw): DeleteAllCallMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteAllCallMessages = new DeleteAllCallMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.revoke = asBool(record['revoke']);
  return out;
}

/** Encode a `deleteAllCallMessages` into TDLib JSON wire shape. */
export function encodeDeleteAllCallMessages(value: DeleteAllCallMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteAllCallMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['revoke'] = value.revoke;
  return out;
}

/** deleteAllChatWelcomeMessages = Ok — Deletes all welcome messages of a chat; requires can_send_welcome_messages administrator right in the chat */
export class DeleteAllChatWelcomeMessages {
  type: 'deleteAllChatWelcomeMessages' = 'deleteAllChatWelcomeMessages';
  extra?: string;
  /** chat_id: int53 — The identifier of the chat */
  chat_id: number = 0;
}

/** Decode a TDLib `deleteAllChatWelcomeMessages` object; null input -> null. */
export function decodeDeleteAllChatWelcomeMessages(json: TdJsonRaw): DeleteAllChatWelcomeMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteAllChatWelcomeMessages = new DeleteAllChatWelcomeMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `deleteAllChatWelcomeMessages` into TDLib JSON wire shape. */
export function encodeDeleteAllChatWelcomeMessages(value: DeleteAllChatWelcomeMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteAllChatWelcomeMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** deleteAllRecentMessageReactionsFromSender = Ok — Deletes all recent reactions added by the specified sender in a chat. Supported only for basic groups and supergroups; requires can_delete_messages administrator right */
export class DeleteAllRecentMessageReactionsFromSender {
  type: 'deleteAllRecentMessageReactionsFromSender' = 'deleteAllRecentMessageReactionsFromSender';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of reactions to delete */
  sender_id: TdMessageSender | null = null;
}

/** Decode a TDLib `deleteAllRecentMessageReactionsFromSender` object; null input -> null. */
export function decodeDeleteAllRecentMessageReactionsFromSender(json: TdJsonRaw): DeleteAllRecentMessageReactionsFromSender | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteAllRecentMessageReactionsFromSender = new DeleteAllRecentMessageReactionsFromSender();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  return out;
}

/** Encode a `deleteAllRecentMessageReactionsFromSender` into TDLib JSON wire shape. */
export function encodeDeleteAllRecentMessageReactionsFromSender(value: DeleteAllRecentMessageReactionsFromSender): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteAllRecentMessageReactionsFromSender';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  return out;
}

/** deleteAllRevokedChatInviteLinks = Ok — Deletes all revoked chat invite links created by a given chat administrator. Requires administrator privileges and can_invite_users right in the chat for own links and owner privileges for other links */
export class DeleteAllRevokedChatInviteLinks {
  type: 'deleteAllRevokedChatInviteLinks' = 'deleteAllRevokedChatInviteLinks';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** creator_user_id: int53 — User identifier of a chat administrator, which links will be deleted. Must be an identifier of the current user for non-owner */
  creator_user_id: number = 0;
}

/** Decode a TDLib `deleteAllRevokedChatInviteLinks` object; null input -> null. */
export function decodeDeleteAllRevokedChatInviteLinks(json: TdJsonRaw): DeleteAllRevokedChatInviteLinks | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteAllRevokedChatInviteLinks = new DeleteAllRevokedChatInviteLinks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.creator_user_id = asNumber(record['creator_user_id']);
  return out;
}

/** Encode a `deleteAllRevokedChatInviteLinks` into TDLib JSON wire shape. */
export function encodeDeleteAllRevokedChatInviteLinks(value: DeleteAllRevokedChatInviteLinks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteAllRevokedChatInviteLinks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['creator_user_id'] = value.creator_user_id;
  return out;
}

/** deleteBotMediaPreviews = Ok — Deletes media previews from the list of media previews of a bot */
export class DeleteBotMediaPreviews {
  type: 'deleteBotMediaPreviews' = 'deleteBotMediaPreviews';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot. The bot must be owned and must have the main Web App */
  bot_user_id: number = 0;
  /** language_code: string — Language code of the media previews to delete */
  language_code: string = '';
  /** file_ids: vector<int32> — File identifiers of the media to delete */
  file_ids: number[] = [];
}

/** Decode a TDLib `deleteBotMediaPreviews` object; null input -> null. */
export function decodeDeleteBotMediaPreviews(json: TdJsonRaw): DeleteBotMediaPreviews | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteBotMediaPreviews = new DeleteBotMediaPreviews();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  out.file_ids = decodeVector(record['file_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `deleteBotMediaPreviews` into TDLib JSON wire shape. */
export function encodeDeleteBotMediaPreviews(value: DeleteBotMediaPreviews): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteBotMediaPreviews';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  out['file_ids'] = encodeVector(value.file_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** deleteBusinessChatLink = Ok — Deletes a business chat link of the current account */
export class DeleteBusinessChatLink {
  type: 'deleteBusinessChatLink' = 'deleteBusinessChatLink';
  extra?: string;
  /** link: string — The link to delete */
  link: string = '';
}

/** Decode a TDLib `deleteBusinessChatLink` object; null input -> null. */
export function decodeDeleteBusinessChatLink(json: TdJsonRaw): DeleteBusinessChatLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteBusinessChatLink = new DeleteBusinessChatLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  return out;
}

/** Encode a `deleteBusinessChatLink` into TDLib JSON wire shape. */
export function encodeDeleteBusinessChatLink(value: DeleteBusinessChatLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteBusinessChatLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  return out;
}

/** deleteBusinessConnectedBot = Ok — Deletes the business bot that is connected to the current user account */
export class DeleteBusinessConnectedBot {
  type: 'deleteBusinessConnectedBot' = 'deleteBusinessConnectedBot';
  extra?: string;
  /** bot_user_id: int53 — Unique user identifier for the bot */
  bot_user_id: number = 0;
}

/** Decode a TDLib `deleteBusinessConnectedBot` object; null input -> null. */
export function decodeDeleteBusinessConnectedBot(json: TdJsonRaw): DeleteBusinessConnectedBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteBusinessConnectedBot = new DeleteBusinessConnectedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `deleteBusinessConnectedBot` into TDLib JSON wire shape. */
export function encodeDeleteBusinessConnectedBot(value: DeleteBusinessConnectedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteBusinessConnectedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** deleteBusinessMessages = Ok — Deletes messages on behalf of a business account; for bots only */
export class DeleteBusinessMessages {
  type: 'deleteBusinessMessages' = 'deleteBusinessMessages';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection through which the messages were received */
  business_connection_id: string = '';
  /** message_ids: vector<int53> — Identifier of the messages */
  message_ids: number[] = [];
}

/** Decode a TDLib `deleteBusinessMessages` object; null input -> null. */
export function decodeDeleteBusinessMessages(json: TdJsonRaw): DeleteBusinessMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteBusinessMessages = new DeleteBusinessMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `deleteBusinessMessages` into TDLib JSON wire shape. */
export function encodeDeleteBusinessMessages(value: DeleteBusinessMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteBusinessMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** deleteBusinessStory = Ok — Deletes a story posted by the bot on behalf of a business account; for bots only */
export class DeleteBusinessStory {
  type: 'deleteBusinessStory' = 'deleteBusinessStory';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection */
  business_connection_id: string = '';
  /** story_id: int32 — Identifier of the story to delete */
  story_id: number = 0;
}

/** Decode a TDLib `deleteBusinessStory` object; null input -> null. */
export function decodeDeleteBusinessStory(json: TdJsonRaw): DeleteBusinessStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteBusinessStory = new DeleteBusinessStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `deleteBusinessStory` into TDLib JSON wire shape. */
export function encodeDeleteBusinessStory(value: DeleteBusinessStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteBusinessStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['story_id'] = value.story_id;
  return out;
}

/** deleteChat = Ok — Deletes a chat along with all messages in the corresponding chat for all chat members. For group chats this will release the usernames and remove all members. Use the field chat.can_be_deleted_for_all_users to find whet... */
export class DeleteChat {
  type: 'deleteChat' = 'deleteChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `deleteChat` object; null input -> null. */
export function decodeDeleteChat(json: TdJsonRaw): DeleteChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteChat = new DeleteChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `deleteChat` into TDLib JSON wire shape. */
export function encodeDeleteChat(value: DeleteChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** deleteChatBackground = Ok — Deletes background in a specific chat Supposed to be used from messageChatSetBackground messages with the currently set background that was set for both sides by the other user */
export class DeleteChatBackground {
  type: 'deleteChatBackground' = 'deleteChatBackground';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** restore_previous: Bool — Pass true to restore previously set background. Can be used only in private and secret chats with non-deleted users if userFullInfo.set_chat_background == true. */
  restore_previous: boolean = false;
}

/** Decode a TDLib `deleteChatBackground` object; null input -> null. */
export function decodeDeleteChatBackground(json: TdJsonRaw): DeleteChatBackground | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteChatBackground = new DeleteChatBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.restore_previous = asBool(record['restore_previous']);
  return out;
}

/** Encode a `deleteChatBackground` into TDLib JSON wire shape. */
export function encodeDeleteChatBackground(value: DeleteChatBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteChatBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['restore_previous'] = value.restore_previous;
  return out;
}

/** deleteChatFolder = Ok — Deletes existing chat folder */
export class DeleteChatFolder {
  type: 'deleteChatFolder' = 'deleteChatFolder';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
  /** leave_chat_ids: vector<int53> — Identifiers of the chats to leave. The chats must be pinned or always included in the folder */
  leave_chat_ids: number[] = [];
}

/** Decode a TDLib `deleteChatFolder` object; null input -> null. */
export function decodeDeleteChatFolder(json: TdJsonRaw): DeleteChatFolder | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteChatFolder = new DeleteChatFolder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  out.leave_chat_ids = decodeVector(record['leave_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `deleteChatFolder` into TDLib JSON wire shape. */
export function encodeDeleteChatFolder(value: DeleteChatFolder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteChatFolder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  out['leave_chat_ids'] = encodeVector(value.leave_chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** deleteChatFolderInviteLink = Ok — Deletes an invite link for a chat folder */
export class DeleteChatFolderInviteLink {
  type: 'deleteChatFolderInviteLink' = 'deleteChatFolderInviteLink';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
  /** invite_link: string — Invite link to be deleted */
  invite_link: string = '';
}

/** Decode a TDLib `deleteChatFolderInviteLink` object; null input -> null. */
export function decodeDeleteChatFolderInviteLink(json: TdJsonRaw): DeleteChatFolderInviteLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteChatFolderInviteLink = new DeleteChatFolderInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `deleteChatFolderInviteLink` into TDLib JSON wire shape. */
export function encodeDeleteChatFolderInviteLink(value: DeleteChatFolderInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteChatFolderInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  out['invite_link'] = value.invite_link;
  return out;
}

/** deleteChatHistory = Ok — Deletes all messages in the chat. Use chat.can_be_deleted_only_for_self and chat.can_be_deleted_for_all_users fields to find whether and how the method can be applied to the chat */
export class DeleteChatHistory {
  type: 'deleteChatHistory' = 'deleteChatHistory';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** remove_from_chat_list: Bool — Pass true to remove the chat from all chat lists */
  remove_from_chat_list: boolean = false;
  /** revoke: Bool — Pass true to delete chat history for all users */
  revoke: boolean = false;
}

/** Decode a TDLib `deleteChatHistory` object; null input -> null. */
export function decodeDeleteChatHistory(json: TdJsonRaw): DeleteChatHistory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteChatHistory = new DeleteChatHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.remove_from_chat_list = asBool(record['remove_from_chat_list']);
  out.revoke = asBool(record['revoke']);
  return out;
}

/** Encode a `deleteChatHistory` into TDLib JSON wire shape. */
export function encodeDeleteChatHistory(value: DeleteChatHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteChatHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['remove_from_chat_list'] = value.remove_from_chat_list;
  out['revoke'] = value.revoke;
  return out;
}

/** deleteChatMessagesByDate = Ok — Deletes all messages between the specified dates in a chat. Supported only for private chats and basic groups. Messages sent in the last 30 seconds will not be deleted */
export class DeleteChatMessagesByDate {
  type: 'deleteChatMessagesByDate' = 'deleteChatMessagesByDate';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** min_date: int32 — The minimum date of the messages to delete */
  min_date: number = 0;
  /** max_date: int32 — The maximum date of the messages to delete */
  max_date: number = 0;
  /** revoke: Bool — Pass true to delete chat messages for all users; private chats only */
  revoke: boolean = false;
}

/** Decode a TDLib `deleteChatMessagesByDate` object; null input -> null. */
export function decodeDeleteChatMessagesByDate(json: TdJsonRaw): DeleteChatMessagesByDate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteChatMessagesByDate = new DeleteChatMessagesByDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.min_date = asNumber(record['min_date']);
  out.max_date = asNumber(record['max_date']);
  out.revoke = asBool(record['revoke']);
  return out;
}

/** Encode a `deleteChatMessagesByDate` into TDLib JSON wire shape. */
export function encodeDeleteChatMessagesByDate(value: DeleteChatMessagesByDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteChatMessagesByDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['min_date'] = value.min_date;
  out['max_date'] = value.max_date;
  out['revoke'] = value.revoke;
  return out;
}

/** deleteChatMessagesBySender = Ok — Deletes all messages sent by the specified message sender in a chat. Supported only for supergroups; requires can_delete_messages administrator right */
export class DeleteChatMessagesBySender {
  type: 'deleteChatMessagesBySender' = 'deleteChatMessagesBySender';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of messages to delete */
  sender_id: TdMessageSender | null = null;
}

/** Decode a TDLib `deleteChatMessagesBySender` object; null input -> null. */
export function decodeDeleteChatMessagesBySender(json: TdJsonRaw): DeleteChatMessagesBySender | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteChatMessagesBySender = new DeleteChatMessagesBySender();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  return out;
}

/** Encode a `deleteChatMessagesBySender` into TDLib JSON wire shape. */
export function encodeDeleteChatMessagesBySender(value: DeleteChatMessagesBySender): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteChatMessagesBySender';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  return out;
}

/** deleteChatReplyMarkup = Ok — Deletes the default reply markup from a chat. Must be called after a one-time keyboard or a replyMarkupForceReply reply markup has been used or dismissed */
export class DeleteChatReplyMarkup {
  type: 'deleteChatReplyMarkup' = 'deleteChatReplyMarkup';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — The message identifier of the used keyboard */
  message_id: number = 0;
}

/** Decode a TDLib `deleteChatReplyMarkup` object; null input -> null. */
export function decodeDeleteChatReplyMarkup(json: TdJsonRaw): DeleteChatReplyMarkup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteChatReplyMarkup = new DeleteChatReplyMarkup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `deleteChatReplyMarkup` into TDLib JSON wire shape. */
export function encodeDeleteChatReplyMarkup(value: DeleteChatReplyMarkup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteChatReplyMarkup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** deleteChatWelcomeMessage = Ok — Deletes a welcome message of a chat; requires can_send_welcome_messages administrator right in the chat */
export class DeleteChatWelcomeMessage {
  type: 'deleteChatWelcomeMessage' = 'deleteChatWelcomeMessage';
  extra?: string;
  /** chat_id: int53 — The identifier of the chat */
  chat_id: number = 0;
  /** welcome_message_id: int32 — The identifier of the welcome message */
  welcome_message_id: number = 0;
}

/** Decode a TDLib `deleteChatWelcomeMessage` object; null input -> null. */
export function decodeDeleteChatWelcomeMessage(json: TdJsonRaw): DeleteChatWelcomeMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteChatWelcomeMessage = new DeleteChatWelcomeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.welcome_message_id = asNumber(record['welcome_message_id']);
  return out;
}

/** Encode a `deleteChatWelcomeMessage` into TDLib JSON wire shape. */
export function encodeDeleteChatWelcomeMessage(value: DeleteChatWelcomeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteChatWelcomeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['welcome_message_id'] = value.welcome_message_id;
  return out;
}

/** deleteCommands = Ok — Deletes commands supported by the bot for the given user scope and language; for bots only */
export class DeleteCommands {
  type: 'deleteCommands' = 'deleteCommands';
  extra?: string;
  /** scope: BotCommandScope — The scope to which the commands are relevant; pass null to delete commands in the default bot command scope */
  scope: TdBotCommandScope | null = null;
  /** language_code: string — A two-letter ISO 639-1 language code or an empty string */
  language_code: string = '';
}

/** Decode a TDLib `deleteCommands` object; null input -> null. */
export function decodeDeleteCommands(json: TdJsonRaw): DeleteCommands | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteCommands = new DeleteCommands();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scope = decodeTdBotCommandScope(record['scope']);
  out.language_code = asString(record['language_code']);
  return out;
}

/** Encode a `deleteCommands` into TDLib JSON wire shape. */
export function encodeDeleteCommands(value: DeleteCommands): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteCommands';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scope'] = value.scope === null ? null : encodeTdBotCommandScope(value.scope);
  out['language_code'] = value.language_code;
  return out;
}

/** deleteDefaultBackground = Ok — Deletes default background for chats */
export class DeleteDefaultBackground {
  type: 'deleteDefaultBackground' = 'deleteDefaultBackground';
  extra?: string;
  /** for_dark_theme: Bool — Pass true if the background is deleted for a dark theme */
  for_dark_theme: boolean = false;
}

/** Decode a TDLib `deleteDefaultBackground` object; null input -> null. */
export function decodeDeleteDefaultBackground(json: TdJsonRaw): DeleteDefaultBackground | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteDefaultBackground = new DeleteDefaultBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.for_dark_theme = asBool(record['for_dark_theme']);
  return out;
}

/** Encode a `deleteDefaultBackground` into TDLib JSON wire shape. */
export function encodeDeleteDefaultBackground(value: DeleteDefaultBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteDefaultBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['for_dark_theme'] = value.for_dark_theme;
  return out;
}

/** deleteDirectMessagesChatTopicHistory = Ok — Deletes all messages in the topic in a channel direct messages chat administered by the current user */
export class DeleteDirectMessagesChatTopicHistory {
  type: 'deleteDirectMessagesChatTopicHistory' = 'deleteDirectMessagesChatTopicHistory';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** topic_id: int53 — Identifier of the topic which messages will be deleted */
  topic_id: number = 0;
}

/** Decode a TDLib `deleteDirectMessagesChatTopicHistory` object; null input -> null. */
export function decodeDeleteDirectMessagesChatTopicHistory(json: TdJsonRaw): DeleteDirectMessagesChatTopicHistory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteDirectMessagesChatTopicHistory = new DeleteDirectMessagesChatTopicHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  return out;
}

/** Encode a `deleteDirectMessagesChatTopicHistory` into TDLib JSON wire shape. */
export function encodeDeleteDirectMessagesChatTopicHistory(value: DeleteDirectMessagesChatTopicHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteDirectMessagesChatTopicHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  return out;
}

/** deleteDirectMessagesChatTopicMessagesByDate = Ok — Deletes all messages between the specified dates in the topic in a channel direct messages chat administered by the current user. Messages sent in the last 30 seconds will not be deleted */
export class DeleteDirectMessagesChatTopicMessagesByDate {
  type: 'deleteDirectMessagesChatTopicMessagesByDate' = 'deleteDirectMessagesChatTopicMessagesByDate';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** topic_id: int53 — Identifier of the topic which messages will be deleted */
  topic_id: number = 0;
  /** min_date: int32 — The minimum date of the messages to delete */
  min_date: number = 0;
  /** max_date: int32 — The maximum date of the messages to delete */
  max_date: number = 0;
}

/** Decode a TDLib `deleteDirectMessagesChatTopicMessagesByDate` object; null input -> null. */
export function decodeDeleteDirectMessagesChatTopicMessagesByDate(json: TdJsonRaw): DeleteDirectMessagesChatTopicMessagesByDate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteDirectMessagesChatTopicMessagesByDate = new DeleteDirectMessagesChatTopicMessagesByDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  out.min_date = asNumber(record['min_date']);
  out.max_date = asNumber(record['max_date']);
  return out;
}

/** Encode a `deleteDirectMessagesChatTopicMessagesByDate` into TDLib JSON wire shape. */
export function encodeDeleteDirectMessagesChatTopicMessagesByDate(value: DeleteDirectMessagesChatTopicMessagesByDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteDirectMessagesChatTopicMessagesByDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  out['min_date'] = value.min_date;
  out['max_date'] = value.max_date;
  return out;
}

/** deleteEphemeralMessage = Ok — Deletes an ephemeral message; for bots only */
export class DeleteEphemeralMessage {
  type: 'deleteEphemeralMessage' = 'deleteEphemeralMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** receiver_user_id: int53 — Identifier of the user who received the message */
  receiver_user_id: number = 0;
  /** ephemeral_message_id: int32 — Identifier of the message to be deleted */
  ephemeral_message_id: number = 0;
}

/** Decode a TDLib `deleteEphemeralMessage` object; null input -> null. */
export function decodeDeleteEphemeralMessage(json: TdJsonRaw): DeleteEphemeralMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteEphemeralMessage = new DeleteEphemeralMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.receiver_user_id = asNumber(record['receiver_user_id']);
  out.ephemeral_message_id = asNumber(record['ephemeral_message_id']);
  return out;
}

/** Encode a `deleteEphemeralMessage` into TDLib JSON wire shape. */
export function encodeDeleteEphemeralMessage(value: DeleteEphemeralMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteEphemeralMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['receiver_user_id'] = value.receiver_user_id;
  out['ephemeral_message_id'] = value.ephemeral_message_id;
  return out;
}

/** deleteFile = Ok — Deletes a file from the TDLib file cache */
export class DeleteFile {
  type: 'deleteFile' = 'deleteFile';
  extra?: string;
  /** file_id: int32 — Identifier of the file to delete */
  file_id: number = 0;
}

/** Decode a TDLib `deleteFile` object; null input -> null. */
export function decodeDeleteFile(json: TdJsonRaw): DeleteFile | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteFile = new DeleteFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  return out;
}

/** Encode a `deleteFile` into TDLib JSON wire shape. */
export function encodeDeleteFile(value: DeleteFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  return out;
}

/** deleteForumTopic = Ok — Deletes all messages from a topic in a forum supergroup chat or a chat with a bot with topics; requires can_delete_messages administrator right in the supergroup unless the user is creator of the topic, the topic ... */
export class DeleteForumTopic {
  type: 'deleteForumTopic' = 'deleteForumTopic';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier */
  forum_topic_id: number = 0;
}

/** Decode a TDLib `deleteForumTopic` object; null input -> null. */
export function decodeDeleteForumTopic(json: TdJsonRaw): DeleteForumTopic | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteForumTopic = new DeleteForumTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  return out;
}

/** Encode a `deleteForumTopic` into TDLib JSON wire shape. */
export function encodeDeleteForumTopic(value: DeleteForumTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteForumTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  return out;
}

/** deleteGiftCollection = Ok — Deletes a gift collection. If the collection is owned by a channel chat, then requires can_post_messages administrator right in the channel chat */
export class DeleteGiftCollection {
  type: 'deleteGiftCollection' = 'deleteGiftCollection';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that owns the collection */
  owner_id: TdMessageSender | null = null;
  /** collection_id: int32 — Identifier of the gift collection */
  collection_id: number = 0;
}

/** Decode a TDLib `deleteGiftCollection` object; null input -> null. */
export function decodeDeleteGiftCollection(json: TdJsonRaw): DeleteGiftCollection | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteGiftCollection = new DeleteGiftCollection();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.collection_id = asNumber(record['collection_id']);
  return out;
}

/** Encode a `deleteGiftCollection` into TDLib JSON wire shape. */
export function encodeDeleteGiftCollection(value: DeleteGiftCollection): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteGiftCollection';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['collection_id'] = value.collection_id;
  return out;
}

/** deleteGroupCallMessages = Ok — Deletes messages in a group call; for live story calls only. Requires groupCallMessage.can_be_deleted right */
export class DeleteGroupCallMessages {
  type: 'deleteGroupCallMessages' = 'deleteGroupCallMessages';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** message_ids: vector<int32> — Identifiers of the messages to be deleted */
  message_ids: number[] = [];
  /** report_spam: Bool — Pass true to report the messages as spam */
  report_spam: boolean = false;
}

/** Decode a TDLib `deleteGroupCallMessages` object; null input -> null. */
export function decodeDeleteGroupCallMessages(json: TdJsonRaw): DeleteGroupCallMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteGroupCallMessages = new DeleteGroupCallMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.report_spam = asBool(record['report_spam']);
  return out;
}

/** Encode a `deleteGroupCallMessages` into TDLib JSON wire shape. */
export function encodeDeleteGroupCallMessages(value: DeleteGroupCallMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteGroupCallMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  out['report_spam'] = value.report_spam;
  return out;
}

/** deleteGroupCallMessagesBySender = Ok — Deletes all messages sent by the specified message sender in a group call; for live story calls only. Requires groupCall.can_delete_messages right */
export class DeleteGroupCallMessagesBySender {
  type: 'deleteGroupCallMessagesBySender' = 'deleteGroupCallMessagesBySender';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of messages to delete */
  sender_id: TdMessageSender | null = null;
  /** report_spam: Bool — Pass true to report the messages as spam */
  report_spam: boolean = false;
}

/** Decode a TDLib `deleteGroupCallMessagesBySender` object; null input -> null. */
export function decodeDeleteGroupCallMessagesBySender(json: TdJsonRaw): DeleteGroupCallMessagesBySender | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteGroupCallMessagesBySender = new DeleteGroupCallMessagesBySender();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.report_spam = asBool(record['report_spam']);
  return out;
}

/** Encode a `deleteGroupCallMessagesBySender` into TDLib JSON wire shape. */
export function encodeDeleteGroupCallMessagesBySender(value: DeleteGroupCallMessagesBySender): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteGroupCallMessagesBySender';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['report_spam'] = value.report_spam;
  return out;
}

/** deleteLanguagePack = Ok — Deletes all information about a language pack in the current localization target. The language pack which is currently in use (including base language pack) or is being synchronized can't be deleted. Can be call... */
export class DeleteLanguagePack {
  type: 'deleteLanguagePack' = 'deleteLanguagePack';
  extra?: string;
  /** language_pack_id: string — Identifier of the language pack to delete */
  language_pack_id: string = '';
}

/** Decode a TDLib `deleteLanguagePack` object; null input -> null. */
export function decodeDeleteLanguagePack(json: TdJsonRaw): DeleteLanguagePack | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteLanguagePack = new DeleteLanguagePack();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_pack_id = asString(record['language_pack_id']);
  return out;
}

/** Encode a `deleteLanguagePack` into TDLib JSON wire shape. */
export function encodeDeleteLanguagePack(value: DeleteLanguagePack): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteLanguagePack';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_pack_id'] = value.language_pack_id;
  return out;
}

/** deleteMessageEphemeralContent = Ok — Removes message ephemeral content and reverts message state to the original */
export class DeleteMessageEphemeralContent {
  type: 'deleteMessageEphemeralContent' = 'deleteMessageEphemeralContent';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `deleteMessageEphemeralContent` object; null input -> null. */
export function decodeDeleteMessageEphemeralContent(json: TdJsonRaw): DeleteMessageEphemeralContent | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteMessageEphemeralContent = new DeleteMessageEphemeralContent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `deleteMessageEphemeralContent` into TDLib JSON wire shape. */
export function encodeDeleteMessageEphemeralContent(value: DeleteMessageEphemeralContent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteMessageEphemeralContent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** deleteMessageReactionsFromSender = Ok — Deletes all reactions added by the specified sender on a message */
export class DeleteMessageReactionsFromSender {
  type: 'deleteMessageReactionsFromSender' = 'deleteMessageReactionsFromSender';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the reactions. Use messageProperties.can_delete_reactions to check whether the method can be used for a message */
  message_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of reactions to delete */
  sender_id: TdMessageSender | null = null;
}

/** Decode a TDLib `deleteMessageReactionsFromSender` object; null input -> null. */
export function decodeDeleteMessageReactionsFromSender(json: TdJsonRaw): DeleteMessageReactionsFromSender | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteMessageReactionsFromSender = new DeleteMessageReactionsFromSender();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  return out;
}

/** Encode a `deleteMessageReactionsFromSender` into TDLib JSON wire shape. */
export function encodeDeleteMessageReactionsFromSender(value: DeleteMessageReactionsFromSender): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteMessageReactionsFromSender';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  return out;
}

/** deleteMessages = Ok — Deletes messages */
export class DeleteMessages {
  type: 'deleteMessages' = 'deleteMessages';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_ids: vector<int53> — Identifiers of the messages to be deleted. Use messageProperties.can_be_deleted_only_for_self and messageProperties.can_be_deleted_for_all_users to get suitable messages */
  message_ids: number[] = [];
  /** revoke: Bool — Pass true to delete messages for all chat members. Always true for supergroups, channels and secret chats */
  revoke: boolean = false;
}

/** Decode a TDLib `deleteMessages` object; null input -> null. */
export function decodeDeleteMessages(json: TdJsonRaw): DeleteMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteMessages = new DeleteMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.revoke = asBool(record['revoke']);
  return out;
}

/** Encode a `deleteMessages` into TDLib JSON wire shape. */
export function encodeDeleteMessages(value: DeleteMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  out['revoke'] = value.revoke;
  return out;
}

/** deletePassportElement = Ok — Deletes a Telegram Passport element */
export class DeletePassportElement {
  type: 'deletePassportElement' = 'deletePassportElement';
  extra?: string;
  /** type: PassportElementType — Element type */
  type_: TdPassportElementType | null = null;
}

/** Decode a TDLib `deletePassportElement` object; null input -> null. */
export function decodeDeletePassportElement(json: TdJsonRaw): DeletePassportElement | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeletePassportElement = new DeletePassportElement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPassportElementType(record['type']);
  return out;
}

/** Encode a `deletePassportElement` into TDLib JSON wire shape. */
export function encodeDeletePassportElement(value: DeletePassportElement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deletePassportElement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPassportElementType(value.type_);
  return out;
}

/** deletePollOption = Ok — Deletes an option from a poll */
export class DeletePollOption {
  type: 'deletePollOption' = 'deletePollOption';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the poll belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the poll */
  message_id: number = 0;
  /** option_id: string — Unique identifier of the option. Use pollOptionProperties.can_be_deleted to check whether the option can be deleted by the user */
  option_id: string = '';
}

/** Decode a TDLib `deletePollOption` object; null input -> null. */
export function decodeDeletePollOption(json: TdJsonRaw): DeletePollOption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeletePollOption = new DeletePollOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.option_id = asString(record['option_id']);
  return out;
}

/** Encode a `deletePollOption` into TDLib JSON wire shape. */
export function encodeDeletePollOption(value: DeletePollOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deletePollOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['option_id'] = value.option_id;
  return out;
}

/** deleteProfilePhoto = Ok — Deletes a profile photo */
export class DeleteProfilePhoto {
  type: 'deleteProfilePhoto' = 'deleteProfilePhoto';
  extra?: string;
  /** profile_photo_id: int64 — Identifier of the profile photo to delete */
  profile_photo_id: string = '';
}

/** Decode a TDLib `deleteProfilePhoto` object; null input -> null. */
export function decodeDeleteProfilePhoto(json: TdJsonRaw): DeleteProfilePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteProfilePhoto = new DeleteProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.profile_photo_id = asInt64(record['profile_photo_id']);
  return out;
}

/** Encode a `deleteProfilePhoto` into TDLib JSON wire shape. */
export function encodeDeleteProfilePhoto(value: DeleteProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['profile_photo_id'] = value.profile_photo_id;
  return out;
}

/** deleteQuickReplyShortcut = Ok — Deletes a quick reply shortcut */
export class DeleteQuickReplyShortcut {
  type: 'deleteQuickReplyShortcut' = 'deleteQuickReplyShortcut';
  extra?: string;
  /** shortcut_id: int32 — Unique identifier of the quick reply shortcut */
  shortcut_id: number = 0;
}

/** Decode a TDLib `deleteQuickReplyShortcut` object; null input -> null. */
export function decodeDeleteQuickReplyShortcut(json: TdJsonRaw): DeleteQuickReplyShortcut | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteQuickReplyShortcut = new DeleteQuickReplyShortcut();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_id = asNumber(record['shortcut_id']);
  return out;
}

/** Encode a `deleteQuickReplyShortcut` into TDLib JSON wire shape. */
export function encodeDeleteQuickReplyShortcut(value: DeleteQuickReplyShortcut): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteQuickReplyShortcut';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_id'] = value.shortcut_id;
  return out;
}

/** deleteQuickReplyShortcutMessages = Ok — Deletes specified quick reply messages */
export class DeleteQuickReplyShortcutMessages {
  type: 'deleteQuickReplyShortcutMessages' = 'deleteQuickReplyShortcutMessages';
  extra?: string;
  /** shortcut_id: int32 — Unique identifier of the quick reply shortcut to which the messages belong */
  shortcut_id: number = 0;
  /** message_ids: vector<int53> — Unique identifiers of the messages */
  message_ids: number[] = [];
}

/** Decode a TDLib `deleteQuickReplyShortcutMessages` object; null input -> null. */
export function decodeDeleteQuickReplyShortcutMessages(json: TdJsonRaw): DeleteQuickReplyShortcutMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteQuickReplyShortcutMessages = new DeleteQuickReplyShortcutMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_id = asNumber(record['shortcut_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `deleteQuickReplyShortcutMessages` into TDLib JSON wire shape. */
export function encodeDeleteQuickReplyShortcutMessages(value: DeleteQuickReplyShortcutMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteQuickReplyShortcutMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_id'] = value.shortcut_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** deleteRevokedChatInviteLink = Ok — Deletes revoked chat invite links. Requires administrator privileges and can_invite_users right in the chat for own links and owner privileges for other links */
export class DeleteRevokedChatInviteLink {
  type: 'deleteRevokedChatInviteLink' = 'deleteRevokedChatInviteLink';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** invite_link: string — Invite link to revoke */
  invite_link: string = '';
}

/** Decode a TDLib `deleteRevokedChatInviteLink` object; null input -> null. */
export function decodeDeleteRevokedChatInviteLink(json: TdJsonRaw): DeleteRevokedChatInviteLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteRevokedChatInviteLink = new DeleteRevokedChatInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `deleteRevokedChatInviteLink` into TDLib JSON wire shape. */
export function encodeDeleteRevokedChatInviteLink(value: DeleteRevokedChatInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteRevokedChatInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['invite_link'] = value.invite_link;
  return out;
}

/** deleteSavedCredentials = Ok — Deletes saved credentials for all payment provider bots */
export class DeleteSavedCredentials {
  type: 'deleteSavedCredentials' = 'deleteSavedCredentials';
  extra?: string;
}

/** Decode a TDLib `deleteSavedCredentials` object; null input -> null. */
export function decodeDeleteSavedCredentials(json: TdJsonRaw): DeleteSavedCredentials | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteSavedCredentials = new DeleteSavedCredentials();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `deleteSavedCredentials` into TDLib JSON wire shape. */
export function encodeDeleteSavedCredentials(value: DeleteSavedCredentials): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteSavedCredentials';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** deleteSavedMessagesTopicHistory = Ok — Deletes all messages in a Saved Messages topic */
export class DeleteSavedMessagesTopicHistory {
  type: 'deleteSavedMessagesTopicHistory' = 'deleteSavedMessagesTopicHistory';
  extra?: string;
  /** saved_messages_topic_id: int53 — Identifier of Saved Messages topic which messages will be deleted */
  saved_messages_topic_id: number = 0;
}

/** Decode a TDLib `deleteSavedMessagesTopicHistory` object; null input -> null. */
export function decodeDeleteSavedMessagesTopicHistory(json: TdJsonRaw): DeleteSavedMessagesTopicHistory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteSavedMessagesTopicHistory = new DeleteSavedMessagesTopicHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  return out;
}

/** Encode a `deleteSavedMessagesTopicHistory` into TDLib JSON wire shape. */
export function encodeDeleteSavedMessagesTopicHistory(value: DeleteSavedMessagesTopicHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteSavedMessagesTopicHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  return out;
}

/** deleteSavedMessagesTopicMessagesByDate = Ok — Deletes all messages between the specified dates in a Saved Messages topic. Messages sent in the last 30 seconds will not be deleted */
export class DeleteSavedMessagesTopicMessagesByDate {
  type: 'deleteSavedMessagesTopicMessagesByDate' = 'deleteSavedMessagesTopicMessagesByDate';
  extra?: string;
  /** saved_messages_topic_id: int53 — Identifier of Saved Messages topic which messages will be deleted */
  saved_messages_topic_id: number = 0;
  /** min_date: int32 — The minimum date of the messages to delete */
  min_date: number = 0;
  /** max_date: int32 — The maximum date of the messages to delete */
  max_date: number = 0;
}

/** Decode a TDLib `deleteSavedMessagesTopicMessagesByDate` object; null input -> null. */
export function decodeDeleteSavedMessagesTopicMessagesByDate(json: TdJsonRaw): DeleteSavedMessagesTopicMessagesByDate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteSavedMessagesTopicMessagesByDate = new DeleteSavedMessagesTopicMessagesByDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  out.min_date = asNumber(record['min_date']);
  out.max_date = asNumber(record['max_date']);
  return out;
}

/** Encode a `deleteSavedMessagesTopicMessagesByDate` into TDLib JSON wire shape. */
export function encodeDeleteSavedMessagesTopicMessagesByDate(value: DeleteSavedMessagesTopicMessagesByDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteSavedMessagesTopicMessagesByDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  out['min_date'] = value.min_date;
  out['max_date'] = value.max_date;
  return out;
}

/** deleteSavedOrderInfo = Ok — Deletes saved order information */
export class DeleteSavedOrderInfo {
  type: 'deleteSavedOrderInfo' = 'deleteSavedOrderInfo';
  extra?: string;
}

/** Decode a TDLib `deleteSavedOrderInfo` object; null input -> null. */
export function decodeDeleteSavedOrderInfo(json: TdJsonRaw): DeleteSavedOrderInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteSavedOrderInfo = new DeleteSavedOrderInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `deleteSavedOrderInfo` into TDLib JSON wire shape. */
export function encodeDeleteSavedOrderInfo(value: DeleteSavedOrderInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteSavedOrderInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** deleteStickerSet = Ok — Completely deletes a sticker set */
export class DeleteStickerSet {
  type: 'deleteStickerSet' = 'deleteStickerSet';
  extra?: string;
  /** name: string — Sticker set name. The sticker set must be owned by the current user */
  name: string = '';
}

/** Decode a TDLib `deleteStickerSet` object; null input -> null. */
export function decodeDeleteStickerSet(json: TdJsonRaw): DeleteStickerSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteStickerSet = new DeleteStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `deleteStickerSet` into TDLib JSON wire shape. */
export function encodeDeleteStickerSet(value: DeleteStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** deleteStory = Ok — Deletes a previously posted story. Can be called only if story.can_be_deleted == true */
export class DeleteStory {
  type: 'deleteStory' = 'deleteStory';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Identifier of the story to delete */
  story_id: number = 0;
}

/** Decode a TDLib `deleteStory` object; null input -> null. */
export function decodeDeleteStory(json: TdJsonRaw): DeleteStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteStory = new DeleteStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `deleteStory` into TDLib JSON wire shape. */
export function encodeDeleteStory(value: DeleteStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** deleteStoryAlbum = Ok — Deletes a story album. If the album is owned by a supergroup or a channel chat, then requires can_edit_stories administrator right in the chat */
export class DeleteStoryAlbum {
  type: 'deleteStoryAlbum' = 'deleteStoryAlbum';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that owns the stories */
  chat_id: number = 0;
  /** story_album_id: int32 — Identifier of the story album */
  story_album_id: number = 0;
}

/** Decode a TDLib `deleteStoryAlbum` object; null input -> null. */
export function decodeDeleteStoryAlbum(json: TdJsonRaw): DeleteStoryAlbum | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteStoryAlbum = new DeleteStoryAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_album_id = asNumber(record['story_album_id']);
  return out;
}

/** Encode a `deleteStoryAlbum` into TDLib JSON wire shape. */
export function encodeDeleteStoryAlbum(value: DeleteStoryAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteStoryAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_album_id'] = value.story_album_id;
  return out;
}

/** deleteTextCompositionStyle = Ok — Deletes a custom text composition style that was created by the current user */
export class DeleteTextCompositionStyle {
  type: 'deleteTextCompositionStyle' = 'deleteTextCompositionStyle';
  extra?: string;
  /** name: string — Name of the style */
  name: string = '';
}

/** Decode a TDLib `deleteTextCompositionStyle` object; null input -> null. */
export function decodeDeleteTextCompositionStyle(json: TdJsonRaw): DeleteTextCompositionStyle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeleteTextCompositionStyle = new DeleteTextCompositionStyle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `deleteTextCompositionStyle` into TDLib JSON wire shape. */
export function encodeDeleteTextCompositionStyle(value: DeleteTextCompositionStyle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deleteTextCompositionStyle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** destroy = Ok — Closes the TDLib instance, destroying all local data without a proper logout. The current user session will remain in the list of all active sessions. All local data will be destroyed. After the destruction completes updat... */
export class Destroy {
  type: 'destroy' = 'destroy';
  extra?: string;
}

/** Decode a TDLib `destroy` object; null input -> null. */
export function decodeDestroy(json: TdJsonRaw): Destroy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Destroy = new Destroy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `destroy` into TDLib JSON wire shape. */
export function encodeDestroy(value: Destroy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'destroy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** deviceTokenApplePush = DeviceToken — A token for Apple Push Notification service */
export class DeviceTokenApplePush {
  type: 'deviceTokenApplePush' = 'deviceTokenApplePush';
  extra?: string;
  /** device_token: string — Device token; may be empty to deregister a device */
  device_token: string = '';
  /** is_app_sandbox: Bool — True, if App Sandbox is enabled */
  is_app_sandbox: boolean = false;
}

/** Decode a TDLib `deviceTokenApplePush` object; null input -> null. */
export function decodeDeviceTokenApplePush(json: TdJsonRaw): DeviceTokenApplePush | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenApplePush = new DeviceTokenApplePush();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.device_token = asString(record['device_token']);
  out.is_app_sandbox = asBool(record['is_app_sandbox']);
  return out;
}

/** Encode a `deviceTokenApplePush` into TDLib JSON wire shape. */
export function encodeDeviceTokenApplePush(value: DeviceTokenApplePush): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenApplePush';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['device_token'] = value.device_token;
  out['is_app_sandbox'] = value.is_app_sandbox;
  return out;
}

/** deviceTokenApplePushVoIP = DeviceToken — A token for Apple Push Notification service VoIP notifications */
export class DeviceTokenApplePushVoIP {
  type: 'deviceTokenApplePushVoIP' = 'deviceTokenApplePushVoIP';
  extra?: string;
  /** device_token: string — Device token; may be empty to deregister a device */
  device_token: string = '';
  /** is_app_sandbox: Bool — True, if App Sandbox is enabled */
  is_app_sandbox: boolean = false;
  /** encrypt: Bool — True, if push notifications must be additionally encrypted */
  encrypt: boolean = false;
}

/** Decode a TDLib `deviceTokenApplePushVoIP` object; null input -> null. */
export function decodeDeviceTokenApplePushVoIP(json: TdJsonRaw): DeviceTokenApplePushVoIP | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenApplePushVoIP = new DeviceTokenApplePushVoIP();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.device_token = asString(record['device_token']);
  out.is_app_sandbox = asBool(record['is_app_sandbox']);
  out.encrypt = asBool(record['encrypt']);
  return out;
}

/** Encode a `deviceTokenApplePushVoIP` into TDLib JSON wire shape. */
export function encodeDeviceTokenApplePushVoIP(value: DeviceTokenApplePushVoIP): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenApplePushVoIP';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['device_token'] = value.device_token;
  out['is_app_sandbox'] = value.is_app_sandbox;
  out['encrypt'] = value.encrypt;
  return out;
}

/** deviceTokenBlackBerryPush = DeviceToken — A token for BlackBerry Push Service */
export class DeviceTokenBlackBerryPush {
  type: 'deviceTokenBlackBerryPush' = 'deviceTokenBlackBerryPush';
  extra?: string;
  /** token: string — Token; may be empty to deregister a device */
  token: string = '';
}

/** Decode a TDLib `deviceTokenBlackBerryPush` object; null input -> null. */
export function decodeDeviceTokenBlackBerryPush(json: TdJsonRaw): DeviceTokenBlackBerryPush | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenBlackBerryPush = new DeviceTokenBlackBerryPush();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `deviceTokenBlackBerryPush` into TDLib JSON wire shape. */
export function encodeDeviceTokenBlackBerryPush(value: DeviceTokenBlackBerryPush): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenBlackBerryPush';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** deviceTokenFirebaseCloudMessaging = DeviceToken — A token for Firebase Cloud Messaging */
export class DeviceTokenFirebaseCloudMessaging {
  type: 'deviceTokenFirebaseCloudMessaging' = 'deviceTokenFirebaseCloudMessaging';
  extra?: string;
  /** token: string — Device registration token; may be empty to deregister a device */
  token: string = '';
  /** encrypt: Bool — True, if push notifications must be additionally encrypted */
  encrypt: boolean = false;
}

/** Decode a TDLib `deviceTokenFirebaseCloudMessaging` object; null input -> null. */
export function decodeDeviceTokenFirebaseCloudMessaging(json: TdJsonRaw): DeviceTokenFirebaseCloudMessaging | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenFirebaseCloudMessaging = new DeviceTokenFirebaseCloudMessaging();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  out.encrypt = asBool(record['encrypt']);
  return out;
}

/** Encode a `deviceTokenFirebaseCloudMessaging` into TDLib JSON wire shape. */
export function encodeDeviceTokenFirebaseCloudMessaging(value: DeviceTokenFirebaseCloudMessaging): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenFirebaseCloudMessaging';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  out['encrypt'] = value.encrypt;
  return out;
}

/** deviceTokenHuaweiPush = DeviceToken — A token for HUAWEI Push Service */
export class DeviceTokenHuaweiPush {
  type: 'deviceTokenHuaweiPush' = 'deviceTokenHuaweiPush';
  extra?: string;
  /** token: string — Device registration token; may be empty to deregister a device */
  token: string = '';
  /** encrypt: Bool — True, if push notifications must be additionally encrypted */
  encrypt: boolean = false;
}

/** Decode a TDLib `deviceTokenHuaweiPush` object; null input -> null. */
export function decodeDeviceTokenHuaweiPush(json: TdJsonRaw): DeviceTokenHuaweiPush | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenHuaweiPush = new DeviceTokenHuaweiPush();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  out.encrypt = asBool(record['encrypt']);
  return out;
}

/** Encode a `deviceTokenHuaweiPush` into TDLib JSON wire shape. */
export function encodeDeviceTokenHuaweiPush(value: DeviceTokenHuaweiPush): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenHuaweiPush';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  out['encrypt'] = value.encrypt;
  return out;
}

/** deviceTokenMicrosoftPush = DeviceToken — A token for Microsoft Push Notification Service */
export class DeviceTokenMicrosoftPush {
  type: 'deviceTokenMicrosoftPush' = 'deviceTokenMicrosoftPush';
  extra?: string;
  /** channel_uri: string — Push notification channel URI; may be empty to deregister a device */
  channel_uri: string = '';
}

/** Decode a TDLib `deviceTokenMicrosoftPush` object; null input -> null. */
export function decodeDeviceTokenMicrosoftPush(json: TdJsonRaw): DeviceTokenMicrosoftPush | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenMicrosoftPush = new DeviceTokenMicrosoftPush();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.channel_uri = asString(record['channel_uri']);
  return out;
}

/** Encode a `deviceTokenMicrosoftPush` into TDLib JSON wire shape. */
export function encodeDeviceTokenMicrosoftPush(value: DeviceTokenMicrosoftPush): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenMicrosoftPush';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['channel_uri'] = value.channel_uri;
  return out;
}

/** deviceTokenMicrosoftPushVoIP = DeviceToken — A token for Microsoft Push Notification Service VoIP channel */
export class DeviceTokenMicrosoftPushVoIP {
  type: 'deviceTokenMicrosoftPushVoIP' = 'deviceTokenMicrosoftPushVoIP';
  extra?: string;
  /** channel_uri: string — Push notification channel URI; may be empty to deregister a device */
  channel_uri: string = '';
}

/** Decode a TDLib `deviceTokenMicrosoftPushVoIP` object; null input -> null. */
export function decodeDeviceTokenMicrosoftPushVoIP(json: TdJsonRaw): DeviceTokenMicrosoftPushVoIP | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenMicrosoftPushVoIP = new DeviceTokenMicrosoftPushVoIP();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.channel_uri = asString(record['channel_uri']);
  return out;
}

/** Encode a `deviceTokenMicrosoftPushVoIP` into TDLib JSON wire shape. */
export function encodeDeviceTokenMicrosoftPushVoIP(value: DeviceTokenMicrosoftPushVoIP): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenMicrosoftPushVoIP';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['channel_uri'] = value.channel_uri;
  return out;
}

/** deviceTokenSimplePush = DeviceToken — A token for Simple Push API for Firefox OS */
export class DeviceTokenSimplePush {
  type: 'deviceTokenSimplePush' = 'deviceTokenSimplePush';
  extra?: string;
  /** endpoint: string — Absolute URL exposed by the push service where the application server can send push messages; may be empty to deregister a device */
  endpoint: string = '';
}

/** Decode a TDLib `deviceTokenSimplePush` object; null input -> null. */
export function decodeDeviceTokenSimplePush(json: TdJsonRaw): DeviceTokenSimplePush | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenSimplePush = new DeviceTokenSimplePush();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.endpoint = asString(record['endpoint']);
  return out;
}

/** Encode a `deviceTokenSimplePush` into TDLib JSON wire shape. */
export function encodeDeviceTokenSimplePush(value: DeviceTokenSimplePush): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenSimplePush';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['endpoint'] = value.endpoint;
  return out;
}

/** deviceTokenTizenPush = DeviceToken — A token for Tizen Push Service */
export class DeviceTokenTizenPush {
  type: 'deviceTokenTizenPush' = 'deviceTokenTizenPush';
  extra?: string;
  /** reg_id: string — Push service registration identifier; may be empty to deregister a device */
  reg_id: string = '';
}

/** Decode a TDLib `deviceTokenTizenPush` object; null input -> null. */
export function decodeDeviceTokenTizenPush(json: TdJsonRaw): DeviceTokenTizenPush | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenTizenPush = new DeviceTokenTizenPush();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reg_id = asString(record['reg_id']);
  return out;
}

/** Encode a `deviceTokenTizenPush` into TDLib JSON wire shape. */
export function encodeDeviceTokenTizenPush(value: DeviceTokenTizenPush): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenTizenPush';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reg_id'] = value.reg_id;
  return out;
}

/** deviceTokenUbuntuPush = DeviceToken — A token for Ubuntu Push Client service */
export class DeviceTokenUbuntuPush {
  type: 'deviceTokenUbuntuPush' = 'deviceTokenUbuntuPush';
  extra?: string;
  /** token: string — Token; may be empty to deregister a device */
  token: string = '';
}

/** Decode a TDLib `deviceTokenUbuntuPush` object; null input -> null. */
export function decodeDeviceTokenUbuntuPush(json: TdJsonRaw): DeviceTokenUbuntuPush | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenUbuntuPush = new DeviceTokenUbuntuPush();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `deviceTokenUbuntuPush` into TDLib JSON wire shape. */
export function encodeDeviceTokenUbuntuPush(value: DeviceTokenUbuntuPush): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenUbuntuPush';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** deviceTokenWebPush = DeviceToken — A token for web Push API */
export class DeviceTokenWebPush {
  type: 'deviceTokenWebPush' = 'deviceTokenWebPush';
  extra?: string;
  /** endpoint: string — Absolute URL exposed by the push service where the application server can send push messages; may be empty to deregister a device */
  endpoint: string = '';
  /** p256dh_base64url: string — Base64url-encoded P-256 elliptic curve Diffie-Hellman public key */
  p256dh_base64url: string = '';
  /** auth_base64url: string — Base64url-encoded authentication secret */
  auth_base64url: string = '';
}

/** Decode a TDLib `deviceTokenWebPush` object; null input -> null. */
export function decodeDeviceTokenWebPush(json: TdJsonRaw): DeviceTokenWebPush | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenWebPush = new DeviceTokenWebPush();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.endpoint = asString(record['endpoint']);
  out.p256dh_base64url = asString(record['p256dh_base64url']);
  out.auth_base64url = asString(record['auth_base64url']);
  return out;
}

/** Encode a `deviceTokenWebPush` into TDLib JSON wire shape. */
export function encodeDeviceTokenWebPush(value: DeviceTokenWebPush): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenWebPush';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['endpoint'] = value.endpoint;
  out['p256dh_base64url'] = value.p256dh_base64url;
  out['auth_base64url'] = value.auth_base64url;
  return out;
}

/** deviceTokenWindowsPush = DeviceToken — A token for Windows Push Notification Services */
export class DeviceTokenWindowsPush {
  type: 'deviceTokenWindowsPush' = 'deviceTokenWindowsPush';
  extra?: string;
  /** access_token: string — The access token that will be used to send notifications; may be empty to deregister a device */
  access_token: string = '';
}

/** Decode a TDLib `deviceTokenWindowsPush` object; null input -> null. */
export function decodeDeviceTokenWindowsPush(json: TdJsonRaw): DeviceTokenWindowsPush | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DeviceTokenWindowsPush = new DeviceTokenWindowsPush();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.access_token = asString(record['access_token']);
  return out;
}

/** Encode a `deviceTokenWindowsPush` into TDLib JSON wire shape. */
export function encodeDeviceTokenWindowsPush(value: DeviceTokenWindowsPush): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'deviceTokenWindowsPush';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['access_token'] = value.access_token;
  return out;
}

/** diceStickersRegular = DiceStickers — A regular animated sticker */
export class DiceStickersRegular {
  type: 'diceStickersRegular' = 'diceStickersRegular';
  extra?: string;
  /** sticker: sticker — The animated sticker with the dice animation */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `diceStickersRegular` object; null input -> null. */
export function decodeDiceStickersRegular(json: TdJsonRaw): DiceStickersRegular | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DiceStickersRegular = new DiceStickersRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `diceStickersRegular` into TDLib JSON wire shape. */
export function encodeDiceStickersRegular(value: DiceStickersRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'diceStickersRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** diceStickersSlotMachine = DiceStickers — Animated stickers to be combined into a slot machine */
export class DiceStickersSlotMachine {
  type: 'diceStickersSlotMachine' = 'diceStickersSlotMachine';
  extra?: string;
  /** background: sticker — The animated sticker with the slot machine background. The background animation must start playing after all reel animations finish */
  background: Sticker | null = null;
  /** lever: sticker — The animated sticker with the lever animation. The lever animation must play once in the initial dice state */
  lever: Sticker | null = null;
  /** left_reel: sticker — The animated sticker with the left reel */
  left_reel: Sticker | null = null;
  /** center_reel: sticker — The animated sticker with the center reel */
  center_reel: Sticker | null = null;
  /** right_reel: sticker — The animated sticker with the right reel */
  right_reel: Sticker | null = null;
}

/** Decode a TDLib `diceStickersSlotMachine` object; null input -> null. */
export function decodeDiceStickersSlotMachine(json: TdJsonRaw): DiceStickersSlotMachine | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DiceStickersSlotMachine = new DiceStickersSlotMachine();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.background = decodeSticker(record['background']);
  out.lever = decodeSticker(record['lever']);
  out.left_reel = decodeSticker(record['left_reel']);
  out.center_reel = decodeSticker(record['center_reel']);
  out.right_reel = decodeSticker(record['right_reel']);
  return out;
}

/** Encode a `diceStickersSlotMachine` into TDLib JSON wire shape. */
export function encodeDiceStickersSlotMachine(value: DiceStickersSlotMachine): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'diceStickersSlotMachine';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['background'] = value.background === null ? null : encodeSticker(value.background);
  out['lever'] = value.lever === null ? null : encodeSticker(value.lever);
  out['left_reel'] = value.left_reel === null ? null : encodeSticker(value.left_reel);
  out['center_reel'] = value.center_reel === null ? null : encodeSticker(value.center_reel);
  out['right_reel'] = value.right_reel === null ? null : encodeSticker(value.right_reel);
  return out;
}

/** diffEntity = DiffEntity — Represents a change of a text */
export class DiffEntity {
  type: 'diffEntity' = 'diffEntity';
  extra?: string;
  /** offset: int32 — Offset of the entity, in UTF-16 code units */
  offset: number = 0;
  /** length: int32 — Length of the entity, in UTF-16 code units */
  length: number = 0;
  /** type: DiffEntityType — Type of the entity */
  type_: TdDiffEntityType | null = null;
}

/** Decode a TDLib `diffEntity` object; null input -> null. */
export function decodeDiffEntity(json: TdJsonRaw): DiffEntity | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DiffEntity = new DiffEntity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.offset = asNumber(record['offset']);
  out.length = asNumber(record['length']);
  out.type_ = decodeTdDiffEntityType(record['type']);
  return out;
}

/** Encode a `diffEntity` into TDLib JSON wire shape. */
export function encodeDiffEntity(value: DiffEntity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'diffEntity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['offset'] = value.offset;
  out['length'] = value.length;
  out['type'] = value.type_ === null ? null : encodeTdDiffEntityType(value.type_);
  return out;
}

/** diffEntityTypeDelete = DiffEntityType — Removal of some text */
export class DiffEntityTypeDelete {
  type: 'diffEntityTypeDelete' = 'diffEntityTypeDelete';
  extra?: string;
}

/** Decode a TDLib `diffEntityTypeDelete` object; null input -> null. */
export function decodeDiffEntityTypeDelete(json: TdJsonRaw): DiffEntityTypeDelete | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DiffEntityTypeDelete = new DiffEntityTypeDelete();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `diffEntityTypeDelete` into TDLib JSON wire shape. */
export function encodeDiffEntityTypeDelete(value: DiffEntityTypeDelete): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'diffEntityTypeDelete';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** diffEntityTypeInsert = DiffEntityType — Addition of some text */
export class DiffEntityTypeInsert {
  type: 'diffEntityTypeInsert' = 'diffEntityTypeInsert';
  extra?: string;
}

/** Decode a TDLib `diffEntityTypeInsert` object; null input -> null. */
export function decodeDiffEntityTypeInsert(json: TdJsonRaw): DiffEntityTypeInsert | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DiffEntityTypeInsert = new DiffEntityTypeInsert();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `diffEntityTypeInsert` into TDLib JSON wire shape. */
export function encodeDiffEntityTypeInsert(value: DiffEntityTypeInsert): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'diffEntityTypeInsert';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** diffEntityTypeReplace = DiffEntityType — Change of some text */
export class DiffEntityTypeReplace {
  type: 'diffEntityTypeReplace' = 'diffEntityTypeReplace';
  extra?: string;
  /** old_text: string — The old text */
  old_text: string = '';
}

/** Decode a TDLib `diffEntityTypeReplace` object; null input -> null. */
export function decodeDiffEntityTypeReplace(json: TdJsonRaw): DiffEntityTypeReplace | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DiffEntityTypeReplace = new DiffEntityTypeReplace();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_text = asString(record['old_text']);
  return out;
}

/** Encode a `diffEntityTypeReplace` into TDLib JSON wire shape. */
export function encodeDiffEntityTypeReplace(value: DiffEntityTypeReplace): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'diffEntityTypeReplace';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_text'] = value.old_text;
  return out;
}

/** diffText = DiffText — A text with some changes highlighted */
export class DiffText {
  type: 'diffText' = 'diffText';
  extra?: string;
  /** text: string — The text */
  text: string = '';
  /** entities: vector<diffEntity> — Entities describing changes in the text. Entities don't mutually intersect with each other */
  entities: DiffEntity[] = [];
}

/** Decode a TDLib `diffText` object; null input -> null. */
export function decodeDiffText(json: TdJsonRaw): DiffText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DiffText = new DiffText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.entities = decodeVector(record['entities'], (item: TdJsonRaw): DiffEntity => decodeDiffEntity(item) as DiffEntity);
  return out;
}

/** Encode a `diffText` into TDLib JSON wire shape. */
export function encodeDiffText(value: DiffText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'diffText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['entities'] = encodeVector(value.entities, (item: DiffEntity | null): TdJsonRaw => item === null ? null : encodeDiffEntity(item));
  return out;
}

/** directMessagesChatTopic = DirectMessagesChatTopic — Contains information about a topic in a channel direct messages chat administered by the current user */
export class DirectMessagesChatTopic {
  type: 'directMessagesChatTopic' = 'directMessagesChatTopic';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the topic belongs */
  chat_id: number = 0;
  /** id: int53 — Unique topic identifier */
  id: number = 0;
  /** sender_id: MessageSender — Identifier of the user or chat that sends the messages to the topic */
  sender_id: TdMessageSender | null = null;
  /** order: int64 — A parameter used to determine order of the topic in the topic list. Topics must be sorted by the order in descending order */
  order: string = '';
  /** can_send_unpaid_messages: Bool — True, if the other party can send unpaid messages even if the chat has paid messages enabled */
  can_send_unpaid_messages: boolean = false;
  /** is_marked_as_unread: Bool — True, if the topic is marked as unread */
  is_marked_as_unread: boolean = false;
  /** unread_count: int53 — Number of unread messages in the chat */
  unread_count: number = 0;
  /** last_read_inbox_message_id: int53 — Identifier of the last read incoming message */
  last_read_inbox_message_id: number = 0;
  /** last_read_outbox_message_id: int53 — Identifier of the last read outgoing message */
  last_read_outbox_message_id: number = 0;
  /** unread_reaction_count: int53 — Number of messages with unread reactions in the chat */
  unread_reaction_count: number = 0;
  /** last_message: message — Last message in the topic; may be null if none or unknown */
  last_message: Message | null = null;
  /** draft_message: draftMessage — A draft of a message in the topic; may be null if none */
  draft_message: DraftMessage | null = null;
}

/** Decode a TDLib `directMessagesChatTopic` object; null input -> null. */
export function decodeDirectMessagesChatTopic(json: TdJsonRaw): DirectMessagesChatTopic | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DirectMessagesChatTopic = new DirectMessagesChatTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.id = asNumber(record['id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.order = asInt64(record['order']);
  out.can_send_unpaid_messages = asBool(record['can_send_unpaid_messages']);
  out.is_marked_as_unread = asBool(record['is_marked_as_unread']);
  out.unread_count = asNumber(record['unread_count']);
  out.last_read_inbox_message_id = asNumber(record['last_read_inbox_message_id']);
  out.last_read_outbox_message_id = asNumber(record['last_read_outbox_message_id']);
  out.unread_reaction_count = asNumber(record['unread_reaction_count']);
  out.last_message = decodeMessage(record['last_message']);
  out.draft_message = decodeDraftMessage(record['draft_message']);
  return out;
}

/** Encode a `directMessagesChatTopic` into TDLib JSON wire shape. */
export function encodeDirectMessagesChatTopic(value: DirectMessagesChatTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'directMessagesChatTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['id'] = value.id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['order'] = value.order;
  out['can_send_unpaid_messages'] = value.can_send_unpaid_messages;
  out['is_marked_as_unread'] = value.is_marked_as_unread;
  out['unread_count'] = value.unread_count;
  out['last_read_inbox_message_id'] = value.last_read_inbox_message_id;
  out['last_read_outbox_message_id'] = value.last_read_outbox_message_id;
  out['unread_reaction_count'] = value.unread_reaction_count;
  out['last_message'] = value.last_message === null ? null : encodeMessage(value.last_message);
  out['draft_message'] = value.draft_message === null ? null : encodeDraftMessage(value.draft_message);
  return out;
}

/** disableAllSupergroupUsernames = Ok — Disables all active non-editable usernames of a supergroup or channel, requires owner privileges in the supergroup or channel */
export class DisableAllSupergroupUsernames {
  type: 'disableAllSupergroupUsernames' = 'disableAllSupergroupUsernames';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup or channel */
  supergroup_id: number = 0;
}

/** Decode a TDLib `disableAllSupergroupUsernames` object; null input -> null. */
export function decodeDisableAllSupergroupUsernames(json: TdJsonRaw): DisableAllSupergroupUsernames | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DisableAllSupergroupUsernames = new DisableAllSupergroupUsernames();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  return out;
}

/** Encode a `disableAllSupergroupUsernames` into TDLib JSON wire shape. */
export function encodeDisableAllSupergroupUsernames(value: DisableAllSupergroupUsernames): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'disableAllSupergroupUsernames';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  return out;
}

/** disableProxy = Ok — Disables the currently enabled proxy. Can be called before authorization */
export class DisableProxy {
  type: 'disableProxy' = 'disableProxy';
  extra?: string;
}

/** Decode a TDLib `disableProxy` object; null input -> null. */
export function decodeDisableProxy(json: TdJsonRaw): DisableProxy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DisableProxy = new DisableProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `disableProxy` into TDLib JSON wire shape. */
export function encodeDisableProxy(value: DisableProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'disableProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** discardCall = Ok — Discards a call */
export class DiscardCall {
  type: 'discardCall' = 'discardCall';
  extra?: string;
  /** call_id: int32 — Call identifier */
  call_id: number = 0;
  /** is_disconnected: Bool — Pass true if the user was disconnected */
  is_disconnected: boolean = false;
  /** invite_link: string — If the call was upgraded to a group call, pass invite link to the group call */
  invite_link: string = '';
  /** duration: int32 — The call duration, in seconds */
  duration: number = 0;
  /** is_video: Bool — Pass true if the call was a video call */
  is_video: boolean = false;
  /** connection_id: int64 — Identifier of the connection used during the call */
  connection_id: string = '';
}

/** Decode a TDLib `discardCall` object; null input -> null. */
export function decodeDiscardCall(json: TdJsonRaw): DiscardCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DiscardCall = new DiscardCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call_id = asNumber(record['call_id']);
  out.is_disconnected = asBool(record['is_disconnected']);
  out.invite_link = asString(record['invite_link']);
  out.duration = asNumber(record['duration']);
  out.is_video = asBool(record['is_video']);
  out.connection_id = asInt64(record['connection_id']);
  return out;
}

/** Encode a `discardCall` into TDLib JSON wire shape. */
export function encodeDiscardCall(value: DiscardCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'discardCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call_id'] = value.call_id;
  out['is_disconnected'] = value.is_disconnected;
  out['invite_link'] = value.invite_link;
  out['duration'] = value.duration;
  out['is_video'] = value.is_video;
  out['connection_id'] = value.connection_id;
  return out;
}

/** disconnectAffiliateProgram = ConnectedAffiliateProgram — Disconnects an affiliate program from the given affiliate and immediately deactivates its referral link. Returns updated information about the disconnected affiliate program */
export class DisconnectAffiliateProgram {
  type: 'disconnectAffiliateProgram' = 'disconnectAffiliateProgram';
  extra?: string;
  /** affiliate: AffiliateType — The affiliate to which the affiliate program is connected */
  affiliate: TdAffiliateType | null = null;
  /** url: string — The referral link of the affiliate program */
  url: string = '';
}

/** Decode a TDLib `disconnectAffiliateProgram` object; null input -> null. */
export function decodeDisconnectAffiliateProgram(json: TdJsonRaw): DisconnectAffiliateProgram | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DisconnectAffiliateProgram = new DisconnectAffiliateProgram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.affiliate = decodeTdAffiliateType(record['affiliate']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `disconnectAffiliateProgram` into TDLib JSON wire shape. */
export function encodeDisconnectAffiliateProgram(value: DisconnectAffiliateProgram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'disconnectAffiliateProgram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['affiliate'] = value.affiliate === null ? null : encodeTdAffiliateType(value.affiliate);
  out['url'] = value.url;
  return out;
}

/** disconnectAllWebsites = Ok — Disconnects all websites from the current user's Telegram account */
export class DisconnectAllWebsites {
  type: 'disconnectAllWebsites' = 'disconnectAllWebsites';
  extra?: string;
}

/** Decode a TDLib `disconnectAllWebsites` object; null input -> null. */
export function decodeDisconnectAllWebsites(json: TdJsonRaw): DisconnectAllWebsites | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DisconnectAllWebsites = new DisconnectAllWebsites();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `disconnectAllWebsites` into TDLib JSON wire shape. */
export function encodeDisconnectAllWebsites(value: DisconnectAllWebsites): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'disconnectAllWebsites';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** disconnectWebsite = Ok — Disconnects website from the current user's Telegram account */
export class DisconnectWebsite {
  type: 'disconnectWebsite' = 'disconnectWebsite';
  extra?: string;
  /** website_id: int64 — Website identifier */
  website_id: string = '';
}

/** Decode a TDLib `disconnectWebsite` object; null input -> null. */
export function decodeDisconnectWebsite(json: TdJsonRaw): DisconnectWebsite | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DisconnectWebsite = new DisconnectWebsite();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.website_id = asInt64(record['website_id']);
  return out;
}

/** Encode a `disconnectWebsite` into TDLib JSON wire shape. */
export function encodeDisconnectWebsite(value: DisconnectWebsite): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'disconnectWebsite';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['website_id'] = value.website_id;
  return out;
}

/** document = Document — Describes a document of any type */
export class Document {
  type: 'document' = 'document';
  extra?: string;
  /** file_name: string — Original name of the file; as defined by the sender */
  file_name: string = '';
  /** mime_type: string — MIME type of the file; as defined by the sender */
  mime_type: string = '';
  /** minithumbnail: minithumbnail — Document minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
  /** thumbnail: thumbnail — Document thumbnail in JPEG or PNG format (PNG will be used only for background patterns); as defined by the sender; may be null */
  thumbnail: Thumbnail | null = null;
  /** document: file — File containing the document */
  document: File | null = null;
}

/** Decode a TDLib `document` object; null input -> null. */
export function decodeDocument(json: TdJsonRaw): Document | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Document = new Document();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_name = asString(record['file_name']);
  out.mime_type = asString(record['mime_type']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  out.document = decodeFile(record['document']);
  return out;
}

/** Encode a `document` into TDLib JSON wire shape. */
export function encodeDocument(value: Document): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'document';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_name'] = value.file_name;
  out['mime_type'] = value.mime_type;
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  out['document'] = value.document === null ? null : encodeFile(value.document);
  return out;
}

/** downloadFile = File — Downloads a file from the cloud. Download progress and completion of the download will be notified through updateFile updates */
export class DownloadFile {
  type: 'downloadFile' = 'downloadFile';
  extra?: string;
  /** file_id: int32 — Identifier of the file to download */
  file_id: number = 0;
  /** priority: int32 — Priority of the download (1-32). The higher the priority, the earlier the file will be downloaded. If the priorities of two files are equal, then the last one for which downloadFile/addFileToDownloads was called will be... */
  priority: number = 0;
  /** offset: int53 — The starting position from which the file needs to be downloaded */
  offset: number = 0;
  /** limit: int53 — Number of bytes which need to be downloaded starting from the "offset" position before the download will automatically be canceled; use 0 to download without a limit */
  limit: number = 0;
  /** synchronous: Bool — Pass true to return response only after the file download has succeeded, has failed, has been canceled, or a new downloadFile request with different offset/limit parameters was sent; pass false to return file state im... */
  synchronous: boolean = false;
}

/** Decode a TDLib `downloadFile` object; null input -> null. */
export function decodeDownloadFile(json: TdJsonRaw): DownloadFile | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DownloadFile = new DownloadFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.priority = asNumber(record['priority']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  out.synchronous = asBool(record['synchronous']);
  return out;
}

/** Encode a `downloadFile` into TDLib JSON wire shape. */
export function encodeDownloadFile(value: DownloadFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'downloadFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['priority'] = value.priority;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  out['synchronous'] = value.synchronous;
  return out;
}

/** downloadedFileCounts = DownloadedFileCounts — Contains number of being downloaded and recently downloaded files found */
export class DownloadedFileCounts {
  type: 'downloadedFileCounts' = 'downloadedFileCounts';
  extra?: string;
  /** active_count: int32 — Number of active file downloads found, including paused */
  active_count: number = 0;
  /** paused_count: int32 — Number of paused file downloads found */
  paused_count: number = 0;
  /** completed_count: int32 — Number of completed file downloads found */
  completed_count: number = 0;
}

/** Decode a TDLib `downloadedFileCounts` object; null input -> null. */
export function decodeDownloadedFileCounts(json: TdJsonRaw): DownloadedFileCounts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DownloadedFileCounts = new DownloadedFileCounts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.active_count = asNumber(record['active_count']);
  out.paused_count = asNumber(record['paused_count']);
  out.completed_count = asNumber(record['completed_count']);
  return out;
}

/** Encode a `downloadedFileCounts` into TDLib JSON wire shape. */
export function encodeDownloadedFileCounts(value: DownloadedFileCounts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'downloadedFileCounts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['active_count'] = value.active_count;
  out['paused_count'] = value.paused_count;
  out['completed_count'] = value.completed_count;
  return out;
}

/** draftMessage = DraftMessage — Contains information about a message draft */
export class DraftMessage {
  type: 'draftMessage' = 'draftMessage';
  extra?: string;
  /** reply_to: InputMessageReplyTo — Information about the message to be replied; inputMessageReplyToStory is unsupported; may be null if none */
  reply_to: TdInputMessageReplyTo | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the draft was created */
  date: number = 0;
  /** content: DraftMessageContent — Content of the message draft */
  content: TdDraftMessageContent | null = null;
  /** effect_id: int64 — Identifier of the effect to apply to the message when it is sent; 0 if none */
  effect_id: string = '';
  /** suggested_post_info: inputSuggestedPostInfo — Information about the suggested post; may be null if none */
  suggested_post_info: InputSuggestedPostInfo | null = null;
}

/** Decode a TDLib `draftMessage` object; null input -> null. */
export function decodeDraftMessage(json: TdJsonRaw): DraftMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DraftMessage = new DraftMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reply_to = decodeTdInputMessageReplyTo(record['reply_to']);
  out.date = asNumber(record['date']);
  out.content = decodeTdDraftMessageContent(record['content']);
  out.effect_id = asInt64(record['effect_id']);
  out.suggested_post_info = decodeInputSuggestedPostInfo(record['suggested_post_info']);
  return out;
}

/** Encode a `draftMessage` into TDLib JSON wire shape. */
export function encodeDraftMessage(value: DraftMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'draftMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reply_to'] = value.reply_to === null ? null : encodeTdInputMessageReplyTo(value.reply_to);
  out['date'] = value.date;
  out['content'] = value.content === null ? null : encodeTdDraftMessageContent(value.content);
  out['effect_id'] = value.effect_id;
  out['suggested_post_info'] = value.suggested_post_info === null ? null : encodeInputSuggestedPostInfo(value.suggested_post_info);
  return out;
}

/** draftMessageContentInputRichMessage = DraftMessageContent — A rich message draft; only for setChatDraftMessage */
export class DraftMessageContentInputRichMessage {
  type: 'draftMessageContentInputRichMessage' = 'draftMessageContentInputRichMessage';
  extra?: string;
  /** message: inputRichMessage — The rich message */
  message: InputRichMessage | null = null;
}

/** Decode a TDLib `draftMessageContentInputRichMessage` object; null input -> null. */
export function decodeDraftMessageContentInputRichMessage(json: TdJsonRaw): DraftMessageContentInputRichMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DraftMessageContentInputRichMessage = new DraftMessageContentInputRichMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeInputRichMessage(record['message']);
  return out;
}

/** Encode a `draftMessageContentInputRichMessage` into TDLib JSON wire shape. */
export function encodeDraftMessageContentInputRichMessage(value: DraftMessageContentInputRichMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'draftMessageContentInputRichMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeInputRichMessage(value.message);
  return out;
}

/** draftMessageContentRichMessage = DraftMessageContent — A rich message draft; not supported in setChatDraftMessage */
export class DraftMessageContentRichMessage {
  type: 'draftMessageContentRichMessage' = 'draftMessageContentRichMessage';
  extra?: string;
  /** message: richMessage — The rich message */
  message: RichMessage | null = null;
}

/** Decode a TDLib `draftMessageContentRichMessage` object; null input -> null. */
export function decodeDraftMessageContentRichMessage(json: TdJsonRaw): DraftMessageContentRichMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DraftMessageContentRichMessage = new DraftMessageContentRichMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeRichMessage(record['message']);
  return out;
}

/** Encode a `draftMessageContentRichMessage` into TDLib JSON wire shape. */
export function encodeDraftMessageContentRichMessage(value: DraftMessageContentRichMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'draftMessageContentRichMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeRichMessage(value.message);
  return out;
}

/** draftMessageContentText = DraftMessageContent — A text message draft */
export class DraftMessageContentText {
  type: 'draftMessageContentText' = 'draftMessageContentText';
  extra?: string;
  /** text: formattedText — Formatted text to be saved as a draft; 0-getOption("message_text_length_max") characters */
  text: FormattedText | null = null;
  /** link_preview_options: linkPreviewOptions — Options to be used for generation of a link preview; may be null if none; pass null to use default link preview options */
  link_preview_options: LinkPreviewOptions | null = null;
}

/** Decode a TDLib `draftMessageContentText` object; null input -> null. */
export function decodeDraftMessageContentText(json: TdJsonRaw): DraftMessageContentText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DraftMessageContentText = new DraftMessageContentText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.link_preview_options = decodeLinkPreviewOptions(record['link_preview_options']);
  return out;
}

/** Encode a `draftMessageContentText` into TDLib JSON wire shape. */
export function encodeDraftMessageContentText(value: DraftMessageContentText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'draftMessageContentText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['link_preview_options'] = value.link_preview_options === null ? null : encodeLinkPreviewOptions(value.link_preview_options);
  return out;
}

/** draftMessageContentVideoNote = DraftMessageContent — A video note message draft */
export class DraftMessageContentVideoNote {
  type: 'draftMessageContentVideoNote' = 'draftMessageContentVideoNote';
  extra?: string;
  /** file_path: string — Path to the file with the video note */
  file_path: string = '';
  /** duration: int32 — Duration of the video, in seconds; 0-60 */
  duration: number = 0;
  /** length: int32 — Video width and height; must be positive and not greater than 640 */
  length: number = 0;
  /** self_destruct_type: MessageSelfDestructType — Video note self-destruct type; may be null if none; pass null if none; private chats only */
  self_destruct_type: TdMessageSelfDestructType | null = null;
}

/** Decode a TDLib `draftMessageContentVideoNote` object; null input -> null. */
export function decodeDraftMessageContentVideoNote(json: TdJsonRaw): DraftMessageContentVideoNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DraftMessageContentVideoNote = new DraftMessageContentVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_path = asString(record['file_path']);
  out.duration = asNumber(record['duration']);
  out.length = asNumber(record['length']);
  out.self_destruct_type = decodeTdMessageSelfDestructType(record['self_destruct_type']);
  return out;
}

/** Encode a `draftMessageContentVideoNote` into TDLib JSON wire shape. */
export function encodeDraftMessageContentVideoNote(value: DraftMessageContentVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'draftMessageContentVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_path'] = value.file_path;
  out['duration'] = value.duration;
  out['length'] = value.length;
  out['self_destruct_type'] = value.self_destruct_type === null ? null : encodeTdMessageSelfDestructType(value.self_destruct_type);
  return out;
}

/** draftMessageContentVoiceNote = DraftMessageContent — A voice note message draft */
export class DraftMessageContentVoiceNote {
  type: 'draftMessageContentVoiceNote' = 'draftMessageContentVoiceNote';
  extra?: string;
  /** file_path: string — Path to the file with the voice note */
  file_path: string = '';
  /** duration: int32 — Duration of the voice note, in seconds */
  duration: number = 0;
  /** waveform: bytes — Waveform representation of the voice note in 5-bit format */
  waveform: string = '';
  /** self_destruct_type: MessageSelfDestructType — Voice note self-destruct type; may be null if none; pass null if none; private chats only */
  self_destruct_type: TdMessageSelfDestructType | null = null;
}

/** Decode a TDLib `draftMessageContentVoiceNote` object; null input -> null. */
export function decodeDraftMessageContentVoiceNote(json: TdJsonRaw): DraftMessageContentVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DraftMessageContentVoiceNote = new DraftMessageContentVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_path = asString(record['file_path']);
  out.duration = asNumber(record['duration']);
  out.waveform = asString(record['waveform']);
  out.self_destruct_type = decodeTdMessageSelfDestructType(record['self_destruct_type']);
  return out;
}

/** Encode a `draftMessageContentVoiceNote` into TDLib JSON wire shape. */
export function encodeDraftMessageContentVoiceNote(value: DraftMessageContentVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'draftMessageContentVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_path'] = value.file_path;
  out['duration'] = value.duration;
  out['waveform'] = value.waveform;
  out['self_destruct_type'] = value.self_destruct_type === null ? null : encodeTdMessageSelfDestructType(value.self_destruct_type);
  return out;
}

/** dropGiftOriginalDetails = Ok — Drops original details for an upgraded gift */
export class DropGiftOriginalDetails {
  type: 'dropGiftOriginalDetails' = 'dropGiftOriginalDetails';
  extra?: string;
  /** received_gift_id: string — Identifier of the gift */
  received_gift_id: string = '';
  /** star_count: int53 — The Telegram Star amount required to pay for the operation */
  star_count: number = 0;
}

/** Decode a TDLib `dropGiftOriginalDetails` object; null input -> null. */
export function decodeDropGiftOriginalDetails(json: TdJsonRaw): DropGiftOriginalDetails | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: DropGiftOriginalDetails = new DropGiftOriginalDetails();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.received_gift_id = asString(record['received_gift_id']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `dropGiftOriginalDetails` into TDLib JSON wire shape. */
export function encodeDropGiftOriginalDetails(value: DropGiftOriginalDetails): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'dropGiftOriginalDetails';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['received_gift_id'] = value.received_gift_id;
  out['star_count'] = value.star_count;
  return out;
}
