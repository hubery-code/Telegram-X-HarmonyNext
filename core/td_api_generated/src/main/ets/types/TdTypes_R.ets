/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { ChatFolder, decodeChatFolder, encodeChatFolder } from './TdTypes_C';
import { Document, decodeDocument, encodeDocument } from './TdTypes_D';
import { FormattedText, decodeFormattedText, encodeFormattedText } from './TdTypes_F';
import { InlineButton, InlineKeyboardButton, InputRichMessageMedia, InputTextQuote, decodeInlineButton, decodeInlineKeyboardButton, decodeInputRichMessageMedia, decodeInputTextQuote, encodeInlineButton, encodeInlineKeyboardButton, encodeInputRichMessageMedia, encodeInputTextQuote } from './TdTypes_I';
import { KeyboardButton, decodeKeyboardButton, encodeKeyboardButton } from './TdTypes_K';
import { NewSticker, decodeNewSticker, encodeNewSticker } from './TdTypes_N';
import { TdChatList, TdDateTimeFormattingType, TdDeviceToken, TdInputFile, TdInputPageBlock, TdMessageSender, TdPageBlock, TdReactionNotificationSource, TdReactionType, TdReportReason, TdResendCodeReason, TdRichText, TdSentGift, TdStickerType, TdTopChatCategory, decodeTdChatList, decodeTdDateTimeFormattingType, decodeTdDeviceToken, decodeTdInputFile, decodeTdInputPageBlock, decodeTdMessageSender, decodeTdPageBlock, decodeTdReactionNotificationSource, decodeTdReactionType, decodeTdReportReason, decodeTdResendCodeReason, decodeTdRichText, decodeTdSentGift, decodeTdStickerType, decodeTdTopChatCategory, encodeTdChatList, encodeTdDateTimeFormattingType, encodeTdDeviceToken, encodeTdInputFile, encodeTdInputPageBlock, encodeTdMessageSender, encodeTdPageBlock, encodeTdReactionNotificationSource, encodeTdReactionType, encodeTdReportReason, encodeTdResendCodeReason, encodeTdRichText, encodeTdSentGift, encodeTdStickerType, encodeTdTopChatCategory } from './TdUnions';

/** rateSpeechRecognition = Ok — Rates recognized speech in a video note or a voice note message */
export class RateSpeechRecognition {
  type: 'rateSpeechRecognition' = 'rateSpeechRecognition';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** is_good: Bool — Pass true if the speech recognition is good */
  is_good: boolean = false;
}

/** Decode a TDLib `rateSpeechRecognition` object; null input -> null. */
export function decodeRateSpeechRecognition(json: TdJsonRaw): RateSpeechRecognition | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RateSpeechRecognition = new RateSpeechRecognition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.is_good = asBool(record['is_good']);
  return out;
}

/** Encode a `rateSpeechRecognition` into TDLib JSON wire shape. */
export function encodeRateSpeechRecognition(value: RateSpeechRecognition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'rateSpeechRecognition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['is_good'] = value.is_good;
  return out;
}

/** reactionNotificationSettings = ReactionNotificationSettings — Contains information about notification settings for reactions and poll votes */
export class ReactionNotificationSettings {
  type: 'reactionNotificationSettings' = 'reactionNotificationSettings';
  extra?: string;
  /** message_reaction_source: ReactionNotificationSource — Source of message reactions for which notifications are shown */
  message_reaction_source: TdReactionNotificationSource | null = null;
  /** story_reaction_source: ReactionNotificationSource — Source of story reactions for which notifications are shown */
  story_reaction_source: TdReactionNotificationSource | null = null;
  /** poll_vote_source: ReactionNotificationSource — Source of poll votes for which notifications are shown */
  poll_vote_source: TdReactionNotificationSource | null = null;
  /** sound_id: int64 — Identifier of the notification sound to be played; 0 if sound is disabled; pass -1 to use the app-dependent default sound */
  sound_id: string = '';
  /** show_preview: Bool — True, if reaction sender and emoji must be displayed in notifications */
  show_preview: boolean = false;
}

/** Decode a TDLib `reactionNotificationSettings` object; null input -> null. */
export function decodeReactionNotificationSettings(json: TdJsonRaw): ReactionNotificationSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionNotificationSettings = new ReactionNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_reaction_source = decodeTdReactionNotificationSource(record['message_reaction_source']);
  out.story_reaction_source = decodeTdReactionNotificationSource(record['story_reaction_source']);
  out.poll_vote_source = decodeTdReactionNotificationSource(record['poll_vote_source']);
  out.sound_id = asInt64(record['sound_id']);
  out.show_preview = asBool(record['show_preview']);
  return out;
}

/** Encode a `reactionNotificationSettings` into TDLib JSON wire shape. */
export function encodeReactionNotificationSettings(value: ReactionNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_reaction_source'] = value.message_reaction_source === null ? null : encodeTdReactionNotificationSource(value.message_reaction_source);
  out['story_reaction_source'] = value.story_reaction_source === null ? null : encodeTdReactionNotificationSource(value.story_reaction_source);
  out['poll_vote_source'] = value.poll_vote_source === null ? null : encodeTdReactionNotificationSource(value.poll_vote_source);
  out['sound_id'] = value.sound_id;
  out['show_preview'] = value.show_preview;
  return out;
}

/** reactionNotificationSourceAll = ReactionNotificationSource — Notifications for reactions are shown for all reactions */
export class ReactionNotificationSourceAll {
  type: 'reactionNotificationSourceAll' = 'reactionNotificationSourceAll';
  extra?: string;
}

/** Decode a TDLib `reactionNotificationSourceAll` object; null input -> null. */
export function decodeReactionNotificationSourceAll(json: TdJsonRaw): ReactionNotificationSourceAll | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionNotificationSourceAll = new ReactionNotificationSourceAll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reactionNotificationSourceAll` into TDLib JSON wire shape. */
export function encodeReactionNotificationSourceAll(value: ReactionNotificationSourceAll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionNotificationSourceAll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reactionNotificationSourceContacts = ReactionNotificationSource — Notifications for reactions are shown only for reactions from contacts */
export class ReactionNotificationSourceContacts {
  type: 'reactionNotificationSourceContacts' = 'reactionNotificationSourceContacts';
  extra?: string;
}

/** Decode a TDLib `reactionNotificationSourceContacts` object; null input -> null. */
export function decodeReactionNotificationSourceContacts(json: TdJsonRaw): ReactionNotificationSourceContacts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionNotificationSourceContacts = new ReactionNotificationSourceContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reactionNotificationSourceContacts` into TDLib JSON wire shape. */
export function encodeReactionNotificationSourceContacts(value: ReactionNotificationSourceContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionNotificationSourceContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reactionNotificationSourceNone = ReactionNotificationSource — Notifications for reactions are disabled */
export class ReactionNotificationSourceNone {
  type: 'reactionNotificationSourceNone' = 'reactionNotificationSourceNone';
  extra?: string;
}

/** Decode a TDLib `reactionNotificationSourceNone` object; null input -> null. */
export function decodeReactionNotificationSourceNone(json: TdJsonRaw): ReactionNotificationSourceNone | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionNotificationSourceNone = new ReactionNotificationSourceNone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reactionNotificationSourceNone` into TDLib JSON wire shape. */
export function encodeReactionNotificationSourceNone(value: ReactionNotificationSourceNone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionNotificationSourceNone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reactionTypeCustomEmoji = ReactionType — A reaction with a custom emoji */
export class ReactionTypeCustomEmoji {
  type: 'reactionTypeCustomEmoji' = 'reactionTypeCustomEmoji';
  extra?: string;
  /** custom_emoji_id: int64 — Unique identifier of the custom emoji */
  custom_emoji_id: string = '';
}

/** Decode a TDLib `reactionTypeCustomEmoji` object; null input -> null. */
export function decodeReactionTypeCustomEmoji(json: TdJsonRaw): ReactionTypeCustomEmoji | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionTypeCustomEmoji = new ReactionTypeCustomEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  return out;
}

/** Encode a `reactionTypeCustomEmoji` into TDLib JSON wire shape. */
export function encodeReactionTypeCustomEmoji(value: ReactionTypeCustomEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionTypeCustomEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_emoji_id'] = value.custom_emoji_id;
  return out;
}

/** reactionTypeEmoji = ReactionType — A reaction with an emoji */
export class ReactionTypeEmoji {
  type: 'reactionTypeEmoji' = 'reactionTypeEmoji';
  extra?: string;
  /** emoji: string — Text representation of the reaction */
  emoji: string = '';
}

/** Decode a TDLib `reactionTypeEmoji` object; null input -> null. */
export function decodeReactionTypeEmoji(json: TdJsonRaw): ReactionTypeEmoji | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionTypeEmoji = new ReactionTypeEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji = asString(record['emoji']);
  return out;
}

/** Encode a `reactionTypeEmoji` into TDLib JSON wire shape. */
export function encodeReactionTypeEmoji(value: ReactionTypeEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionTypeEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji'] = value.emoji;
  return out;
}

/** reactionTypePaid = ReactionType — The paid reaction in a channel chat */
export class ReactionTypePaid {
  type: 'reactionTypePaid' = 'reactionTypePaid';
  extra?: string;
}

/** Decode a TDLib `reactionTypePaid` object; null input -> null. */
export function decodeReactionTypePaid(json: TdJsonRaw): ReactionTypePaid | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionTypePaid = new ReactionTypePaid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reactionTypePaid` into TDLib JSON wire shape. */
export function encodeReactionTypePaid(value: ReactionTypePaid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionTypePaid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reactionUnavailabilityReasonAnonymousAdministrator = ReactionUnavailabilityReason — The user is an anonymous administrator in the supergroup, but isn't a creator of it, so they can't vote on behalf of the supergroup */
export class ReactionUnavailabilityReasonAnonymousAdministrator {
  type: 'reactionUnavailabilityReasonAnonymousAdministrator' = 'reactionUnavailabilityReasonAnonymousAdministrator';
  extra?: string;
}

/** Decode a TDLib `reactionUnavailabilityReasonAnonymousAdministrator` object; null input -> null. */
export function decodeReactionUnavailabilityReasonAnonymousAdministrator(json: TdJsonRaw): ReactionUnavailabilityReasonAnonymousAdministrator | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionUnavailabilityReasonAnonymousAdministrator = new ReactionUnavailabilityReasonAnonymousAdministrator();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reactionUnavailabilityReasonAnonymousAdministrator` into TDLib JSON wire shape. */
export function encodeReactionUnavailabilityReasonAnonymousAdministrator(value: ReactionUnavailabilityReasonAnonymousAdministrator): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionUnavailabilityReasonAnonymousAdministrator';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reactionUnavailabilityReasonGuest = ReactionUnavailabilityReason — The user isn't a member of the supergroup and can't send messages and reactions there without joining */
export class ReactionUnavailabilityReasonGuest {
  type: 'reactionUnavailabilityReasonGuest' = 'reactionUnavailabilityReasonGuest';
  extra?: string;
}

/** Decode a TDLib `reactionUnavailabilityReasonGuest` object; null input -> null. */
export function decodeReactionUnavailabilityReasonGuest(json: TdJsonRaw): ReactionUnavailabilityReasonGuest | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionUnavailabilityReasonGuest = new ReactionUnavailabilityReasonGuest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reactionUnavailabilityReasonGuest` into TDLib JSON wire shape. */
export function encodeReactionUnavailabilityReasonGuest(value: ReactionUnavailabilityReasonGuest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionUnavailabilityReasonGuest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reactionUnavailabilityReasonRestricted = ReactionUnavailabilityReason — The user is restricted in the chat */
export class ReactionUnavailabilityReasonRestricted {
  type: 'reactionUnavailabilityReasonRestricted' = 'reactionUnavailabilityReasonRestricted';
  extra?: string;
}

/** Decode a TDLib `reactionUnavailabilityReasonRestricted` object; null input -> null. */
export function decodeReactionUnavailabilityReasonRestricted(json: TdJsonRaw): ReactionUnavailabilityReasonRestricted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReactionUnavailabilityReasonRestricted = new ReactionUnavailabilityReasonRestricted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reactionUnavailabilityReasonRestricted` into TDLib JSON wire shape. */
export function encodeReactionUnavailabilityReasonRestricted(value: ReactionUnavailabilityReasonRestricted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reactionUnavailabilityReasonRestricted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** readAllChatMentions = Ok — Marks all mentions in a chat as read */
export class ReadAllChatMentions {
  type: 'readAllChatMentions' = 'readAllChatMentions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `readAllChatMentions` object; null input -> null. */
export function decodeReadAllChatMentions(json: TdJsonRaw): ReadAllChatMentions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadAllChatMentions = new ReadAllChatMentions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `readAllChatMentions` into TDLib JSON wire shape. */
export function encodeReadAllChatMentions(value: ReadAllChatMentions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readAllChatMentions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** readAllChatPollVotes = Ok — Marks all poll votes in a chat as read */
export class ReadAllChatPollVotes {
  type: 'readAllChatPollVotes' = 'readAllChatPollVotes';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `readAllChatPollVotes` object; null input -> null. */
export function decodeReadAllChatPollVotes(json: TdJsonRaw): ReadAllChatPollVotes | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadAllChatPollVotes = new ReadAllChatPollVotes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `readAllChatPollVotes` into TDLib JSON wire shape. */
export function encodeReadAllChatPollVotes(value: ReadAllChatPollVotes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readAllChatPollVotes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** readAllChatReactions = Ok — Marks all reactions in a chat as read */
export class ReadAllChatReactions {
  type: 'readAllChatReactions' = 'readAllChatReactions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `readAllChatReactions` object; null input -> null. */
export function decodeReadAllChatReactions(json: TdJsonRaw): ReadAllChatReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadAllChatReactions = new ReadAllChatReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `readAllChatReactions` into TDLib JSON wire shape. */
export function encodeReadAllChatReactions(value: ReadAllChatReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readAllChatReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** readAllDirectMessagesChatTopicReactions = Ok — Removes all unread reactions in the topic in a channel direct messages chat administered by the current user */
export class ReadAllDirectMessagesChatTopicReactions {
  type: 'readAllDirectMessagesChatTopicReactions' = 'readAllDirectMessagesChatTopicReactions';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** topic_id: int53 — Topic identifier */
  topic_id: number = 0;
}

/** Decode a TDLib `readAllDirectMessagesChatTopicReactions` object; null input -> null. */
export function decodeReadAllDirectMessagesChatTopicReactions(json: TdJsonRaw): ReadAllDirectMessagesChatTopicReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadAllDirectMessagesChatTopicReactions = new ReadAllDirectMessagesChatTopicReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  return out;
}

/** Encode a `readAllDirectMessagesChatTopicReactions` into TDLib JSON wire shape. */
export function encodeReadAllDirectMessagesChatTopicReactions(value: ReadAllDirectMessagesChatTopicReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readAllDirectMessagesChatTopicReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  return out;
}

/** readAllForumTopicMentions = Ok — Marks all mentions in a topic in a forum supergroup chat as read */
export class ReadAllForumTopicMentions {
  type: 'readAllForumTopicMentions' = 'readAllForumTopicMentions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier in which mentions are marked as read */
  forum_topic_id: number = 0;
}

/** Decode a TDLib `readAllForumTopicMentions` object; null input -> null. */
export function decodeReadAllForumTopicMentions(json: TdJsonRaw): ReadAllForumTopicMentions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadAllForumTopicMentions = new ReadAllForumTopicMentions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  return out;
}

/** Encode a `readAllForumTopicMentions` into TDLib JSON wire shape. */
export function encodeReadAllForumTopicMentions(value: ReadAllForumTopicMentions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readAllForumTopicMentions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  return out;
}

/** readAllForumTopicPollVotes = Ok — Marks all poll votes in a topic in a forum supergroup chat as read */
export class ReadAllForumTopicPollVotes {
  type: 'readAllForumTopicPollVotes' = 'readAllForumTopicPollVotes';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier in which poll votes are marked as read */
  forum_topic_id: number = 0;
}

/** Decode a TDLib `readAllForumTopicPollVotes` object; null input -> null. */
export function decodeReadAllForumTopicPollVotes(json: TdJsonRaw): ReadAllForumTopicPollVotes | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadAllForumTopicPollVotes = new ReadAllForumTopicPollVotes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  return out;
}

/** Encode a `readAllForumTopicPollVotes` into TDLib JSON wire shape. */
export function encodeReadAllForumTopicPollVotes(value: ReadAllForumTopicPollVotes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readAllForumTopicPollVotes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  return out;
}

/** readAllForumTopicReactions = Ok — Marks all reactions in a topic in a forum supergroup chat or a chat with a bot with topics as read */
export class ReadAllForumTopicReactions {
  type: 'readAllForumTopicReactions' = 'readAllForumTopicReactions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier in which reactions are marked as read */
  forum_topic_id: number = 0;
}

/** Decode a TDLib `readAllForumTopicReactions` object; null input -> null. */
export function decodeReadAllForumTopicReactions(json: TdJsonRaw): ReadAllForumTopicReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadAllForumTopicReactions = new ReadAllForumTopicReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  return out;
}

/** Encode a `readAllForumTopicReactions` into TDLib JSON wire shape. */
export function encodeReadAllForumTopicReactions(value: ReadAllForumTopicReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readAllForumTopicReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  return out;
}

/** readBusinessMessage = Ok — Reads a message on behalf of a business account; for bots only */
export class ReadBusinessMessage {
  type: 'readBusinessMessage' = 'readBusinessMessage';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection through which the message was received */
  business_connection_id: string = '';
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `readBusinessMessage` object; null input -> null. */
export function decodeReadBusinessMessage(json: TdJsonRaw): ReadBusinessMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadBusinessMessage = new ReadBusinessMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `readBusinessMessage` into TDLib JSON wire shape. */
export function encodeReadBusinessMessage(value: ReadBusinessMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readBusinessMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** readChatList = Ok — Traverses all chats in a chat list and marks all messages in the chats as read */
export class ReadChatList {
  type: 'readChatList' = 'readChatList';
  extra?: string;
  /** chat_list: ChatList — Chat list in which to mark all chats as read */
  chat_list: TdChatList | null = null;
}

/** Decode a TDLib `readChatList` object; null input -> null. */
export function decodeReadChatList(json: TdJsonRaw): ReadChatList | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadChatList = new ReadChatList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_list = decodeTdChatList(record['chat_list']);
  return out;
}

/** Encode a `readChatList` into TDLib JSON wire shape. */
export function encodeReadChatList(value: ReadChatList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readChatList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  return out;
}

/** readDatePrivacySettings = ReadDatePrivacySettings — Contains privacy settings for message read date in private chats. Read dates are always shown to the users that can see online status of the current user regardless of this setting */
export class ReadDatePrivacySettings {
  type: 'readDatePrivacySettings' = 'readDatePrivacySettings';
  extra?: string;
  /** show_read_date: Bool — True, if message read date is shown to other users in private chats. If false and the current user isn't a Telegram Premium user, then they will not be able to see other's message read date */
  show_read_date: boolean = false;
}

/** Decode a TDLib `readDatePrivacySettings` object; null input -> null. */
export function decodeReadDatePrivacySettings(json: TdJsonRaw): ReadDatePrivacySettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadDatePrivacySettings = new ReadDatePrivacySettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.show_read_date = asBool(record['show_read_date']);
  return out;
}

/** Encode a `readDatePrivacySettings` into TDLib JSON wire shape. */
export function encodeReadDatePrivacySettings(value: ReadDatePrivacySettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readDatePrivacySettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['show_read_date'] = value.show_read_date;
  return out;
}

/** readFilePart = Data — Reads a part of a file from the TDLib file cache and returns read bytes. This method is intended to be used only if the application has no direct access to TDLib's file system, because it is usually slower than a di... */
export class ReadFilePart {
  type: 'readFilePart' = 'readFilePart';
  extra?: string;
  /** file_id: int32 — Identifier of the file. The file must be located in the TDLib file cache */
  file_id: number = 0;
  /** offset: int53 — The offset from which to read the file */
  offset: number = 0;
  /** count: int53 — Number of bytes to read. An error will be returned if there are not enough bytes available in the file from the specified position. Pass 0 to read all available data from the specified position */
  count: number = 0;
}

/** Decode a TDLib `readFilePart` object; null input -> null. */
export function decodeReadFilePart(json: TdJsonRaw): ReadFilePart | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReadFilePart = new ReadFilePart();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.offset = asNumber(record['offset']);
  out.count = asNumber(record['count']);
  return out;
}

/** Encode a `readFilePart` into TDLib JSON wire shape. */
export function encodeReadFilePart(value: ReadFilePart): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readFilePart';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['offset'] = value.offset;
  out['count'] = value.count;
  return out;
}

/** readdQuickReplyShortcutMessages = QuickReplyMessages — Re-adds quick reply messages which failed to add. Can be called only for messages for which messageSendingStateFailed.can_retry is true and after specified in messageSendingStateFail... */
export class ReaddQuickReplyShortcutMessages {
  type: 'readdQuickReplyShortcutMessages' = 'readdQuickReplyShortcutMessages';
  extra?: string;
  /** shortcut_name: string — Name of the target shortcut */
  shortcut_name: string = '';
  /** message_ids: vector<int53> — Identifiers of the quick reply messages to re-add. Message identifiers must be in a strictly increasing order */
  message_ids: number[] = [];
}

/** Decode a TDLib `readdQuickReplyShortcutMessages` object; null input -> null. */
export function decodeReaddQuickReplyShortcutMessages(json: TdJsonRaw): ReaddQuickReplyShortcutMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReaddQuickReplyShortcutMessages = new ReaddQuickReplyShortcutMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_name = asString(record['shortcut_name']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `readdQuickReplyShortcutMessages` into TDLib JSON wire shape. */
export function encodeReaddQuickReplyShortcutMessages(value: ReaddQuickReplyShortcutMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'readdQuickReplyShortcutMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_name'] = value.shortcut_name;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** receivedGift = ReceivedGift — Represents a gift received by a user or a chat */
export class ReceivedGift {
  type: 'receivedGift' = 'receivedGift';
  extra?: string;
  /** received_gift_id: string — Unique identifier of the received gift for the current user; only for the receiver of the gift */
  received_gift_id: string = '';
  /** sender_id: MessageSender — Identifier of a user or a chat that sent the gift; may be null if unknown */
  sender_id: TdMessageSender | null = null;
  /** text: formattedText — Message added to the gift */
  text: FormattedText | null = null;
  /** unique_gift_number: int32 — Unique number of the gift among gifts upgraded from the same gift after upgrade; 0 if yet unassigned */
  unique_gift_number: number = 0;
  /** is_private: Bool — True, if the sender and gift text are shown only to the gift receiver; otherwise, everyone are able to see them */
  is_private: boolean = false;
  /** is_saved: Bool — True, if the gift is displayed on the chat's profile page; only for the receiver of the gift */
  is_saved: boolean = false;
  /** is_pinned: Bool — True, if the gift is pinned to the top of the chat's profile page */
  is_pinned: boolean = false;
  /** can_be_upgraded: Bool — True, if the gift is a regular gift that can be upgraded to a unique gift; only for the receiver of the gift */
  can_be_upgraded: boolean = false;
  /** can_be_transferred: Bool — True, if the gift is an upgraded gift that can be transferred to another owner; only for the receiver of the gift */
  can_be_transferred: boolean = false;
  /** was_refunded: Bool — True, if the gift was refunded and isn't available anymore */
  was_refunded: boolean = false;
  /** date: int32 — Point in time (Unix timestamp) when the gift was sent */
  date: number = 0;
  /** gift: SentGift — The gift */
  gift: TdSentGift | null = null;
  /** collection_ids: vector<int32> — Identifiers of collections to which the gift is added; only for the receiver of the gift */
  collection_ids: number[] = [];
  /** sell_star_count: int53 — Number of Telegram Stars that can be claimed by the receiver instead of the regular gift; 0 if the gift can't be sold by the current user */
  sell_star_count: number = 0;
  /** prepaid_upgrade_star_count: int53 — Number of Telegram Stars that were paid by the sender for the ability to upgrade the gift */
  prepaid_upgrade_star_count: number = 0;
  /** is_upgrade_separate: Bool — True, if the upgrade was bought after the gift was sent. In this case, prepaid upgrade cost must not be added to the gift cost */
  is_upgrade_separate: boolean = false;
  /** transfer_star_count: int53 — Number of Telegram Stars that must be paid to transfer the upgraded gift; only for the receiver of the gift */
  transfer_star_count: number = 0;
  /** drop_original_details_star_count: int53 — Number of Telegram Stars that must be paid to drop original details of the upgraded gift; 0 if not available; only for the receiver of the gift */
  drop_original_details_star_count: number = 0;
  /** next_transfer_date: int32 — Point in time (Unix timestamp) when the gift can be transferred to another owner; can be in the past; 0 if the gift can be transferred immediately or transfer isn't possible; only for the receiver of the gift */
  next_transfer_date: number = 0;
  /** next_resale_date: int32 — Point in time (Unix timestamp) when the gift can be resold to another user; can be in the past; 0 if the gift can't be resold; only for the receiver of the gift */
  next_resale_date: number = 0;
  /** export_date: int32 — Point in time (Unix timestamp) when the upgraded gift can be transferred to the TON blockchain as an NFT; can be in the past; 0 if NFT export isn't possible; only for the receiver of the gift */
  export_date: number = 0;
  /** prepaid_upgrade_hash: string — If non-empty, then the user can pay for an upgrade of the gift using buyGiftUpgrade */
  prepaid_upgrade_hash: string = '';
  /** craft_date: int32 — Point in time (Unix timestamp) when the gift can be used to craft another gift; can be in the past; only for the receiver of the gift */
  craft_date: number = 0;
}

/** Decode a TDLib `receivedGift` object; null input -> null. */
export function decodeReceivedGift(json: TdJsonRaw): ReceivedGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReceivedGift = new ReceivedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.received_gift_id = asString(record['received_gift_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.text = decodeFormattedText(record['text']);
  out.unique_gift_number = asNumber(record['unique_gift_number']);
  out.is_private = asBool(record['is_private']);
  out.is_saved = asBool(record['is_saved']);
  out.is_pinned = asBool(record['is_pinned']);
  out.can_be_upgraded = asBool(record['can_be_upgraded']);
  out.can_be_transferred = asBool(record['can_be_transferred']);
  out.was_refunded = asBool(record['was_refunded']);
  out.date = asNumber(record['date']);
  out.gift = decodeTdSentGift(record['gift']);
  out.collection_ids = decodeVector(record['collection_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.sell_star_count = asNumber(record['sell_star_count']);
  out.prepaid_upgrade_star_count = asNumber(record['prepaid_upgrade_star_count']);
  out.is_upgrade_separate = asBool(record['is_upgrade_separate']);
  out.transfer_star_count = asNumber(record['transfer_star_count']);
  out.drop_original_details_star_count = asNumber(record['drop_original_details_star_count']);
  out.next_transfer_date = asNumber(record['next_transfer_date']);
  out.next_resale_date = asNumber(record['next_resale_date']);
  out.export_date = asNumber(record['export_date']);
  out.prepaid_upgrade_hash = asString(record['prepaid_upgrade_hash']);
  out.craft_date = asNumber(record['craft_date']);
  return out;
}

/** Encode a `receivedGift` into TDLib JSON wire shape. */
export function encodeReceivedGift(value: ReceivedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'receivedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['received_gift_id'] = value.received_gift_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['unique_gift_number'] = value.unique_gift_number;
  out['is_private'] = value.is_private;
  out['is_saved'] = value.is_saved;
  out['is_pinned'] = value.is_pinned;
  out['can_be_upgraded'] = value.can_be_upgraded;
  out['can_be_transferred'] = value.can_be_transferred;
  out['was_refunded'] = value.was_refunded;
  out['date'] = value.date;
  out['gift'] = value.gift === null ? null : encodeTdSentGift(value.gift);
  out['collection_ids'] = encodeVector(value.collection_ids, (item: number): TdJsonRaw => item);
  out['sell_star_count'] = value.sell_star_count;
  out['prepaid_upgrade_star_count'] = value.prepaid_upgrade_star_count;
  out['is_upgrade_separate'] = value.is_upgrade_separate;
  out['transfer_star_count'] = value.transfer_star_count;
  out['drop_original_details_star_count'] = value.drop_original_details_star_count;
  out['next_transfer_date'] = value.next_transfer_date;
  out['next_resale_date'] = value.next_resale_date;
  out['export_date'] = value.export_date;
  out['prepaid_upgrade_hash'] = value.prepaid_upgrade_hash;
  out['craft_date'] = value.craft_date;
  return out;
}

/** receivedGifts = ReceivedGifts — Represents a list of gifts received by a user or a chat */
export class ReceivedGifts {
  type: 'receivedGifts' = 'receivedGifts';
  extra?: string;
  /** total_count: int32 — The total number of received gifts */
  total_count: number = 0;
  /** gifts: vector<receivedGift> — The list of gifts */
  gifts: ReceivedGift[] = [];
  /** are_notifications_enabled: Bool — True, if notifications about new gifts of the owner are enabled */
  are_notifications_enabled: boolean = false;
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `receivedGifts` object; null input -> null. */
export function decodeReceivedGifts(json: TdJsonRaw): ReceivedGifts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReceivedGifts = new ReceivedGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.gifts = decodeVector(record['gifts'], (item: TdJsonRaw): ReceivedGift => decodeReceivedGift(item) as ReceivedGift);
  out.are_notifications_enabled = asBool(record['are_notifications_enabled']);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `receivedGifts` into TDLib JSON wire shape. */
export function encodeReceivedGifts(value: ReceivedGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'receivedGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['gifts'] = encodeVector(value.gifts, (item: ReceivedGift | null): TdJsonRaw => item === null ? null : encodeReceivedGift(item));
  out['are_notifications_enabled'] = value.are_notifications_enabled;
  out['next_offset'] = value.next_offset;
  return out;
}

/** recognizeSpeech = Ok — Recognizes speech in a video note or a voice note message */
export class RecognizeSpeech {
  type: 'recognizeSpeech' = 'recognizeSpeech';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use messageProperties.can_recognize_speech to check whether the message is suitable */
  message_id: number = 0;
}

/** Decode a TDLib `recognizeSpeech` object; null input -> null. */
export function decodeRecognizeSpeech(json: TdJsonRaw): RecognizeSpeech | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RecognizeSpeech = new RecognizeSpeech();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `recognizeSpeech` into TDLib JSON wire shape. */
export function encodeRecognizeSpeech(value: RecognizeSpeech): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'recognizeSpeech';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** recommendedChatFolder = RecommendedChatFolder — Describes a recommended chat folder */
export class RecommendedChatFolder {
  type: 'recommendedChatFolder' = 'recommendedChatFolder';
  extra?: string;
  /** folder: chatFolder — The chat folder */
  folder: ChatFolder | null = null;
  /** description: string */
  description: string = '';
}

/** Decode a TDLib `recommendedChatFolder` object; null input -> null. */
export function decodeRecommendedChatFolder(json: TdJsonRaw): RecommendedChatFolder | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RecommendedChatFolder = new RecommendedChatFolder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.folder = decodeChatFolder(record['folder']);
  out.description = asString(record['description']);
  return out;
}

/** Encode a `recommendedChatFolder` into TDLib JSON wire shape. */
export function encodeRecommendedChatFolder(value: RecommendedChatFolder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'recommendedChatFolder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['folder'] = value.folder === null ? null : encodeChatFolder(value.folder);
  out['description'] = value.description;
  return out;
}

/** recommendedChatFolders = RecommendedChatFolders — Contains a list of recommended chat folders */
export class RecommendedChatFolders {
  type: 'recommendedChatFolders' = 'recommendedChatFolders';
  extra?: string;
  /** chat_folders: vector<recommendedChatFolder> — List of recommended chat folders */
  chat_folders: RecommendedChatFolder[] = [];
}

/** Decode a TDLib `recommendedChatFolders` object; null input -> null. */
export function decodeRecommendedChatFolders(json: TdJsonRaw): RecommendedChatFolders | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RecommendedChatFolders = new RecommendedChatFolders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folders = decodeVector(record['chat_folders'], (item: TdJsonRaw): RecommendedChatFolder => decodeRecommendedChatFolder(item) as RecommendedChatFolder);
  return out;
}

/** Encode a `recommendedChatFolders` into TDLib JSON wire shape. */
export function encodeRecommendedChatFolders(value: RecommendedChatFolders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'recommendedChatFolders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folders'] = encodeVector(value.chat_folders, (item: RecommendedChatFolder | null): TdJsonRaw => item === null ? null : encodeRecommendedChatFolder(item));
  return out;
}

/** recoverAuthenticationPassword = Ok — Recovers the 2-step verification password with a password recovery code sent to an email address that was previously set up. Works only when the current authorization state is authorizationStateWaitPa... */
export class RecoverAuthenticationPassword {
  type: 'recoverAuthenticationPassword' = 'recoverAuthenticationPassword';
  extra?: string;
  /** recovery_code: string — Recovery code to check */
  recovery_code: string = '';
  /** new_password: string — New 2-step verification password of the user; may be empty to remove the password */
  new_password: string = '';
  /** new_hint: string — New password hint; may be empty */
  new_hint: string = '';
}

/** Decode a TDLib `recoverAuthenticationPassword` object; null input -> null. */
export function decodeRecoverAuthenticationPassword(json: TdJsonRaw): RecoverAuthenticationPassword | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RecoverAuthenticationPassword = new RecoverAuthenticationPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.recovery_code = asString(record['recovery_code']);
  out.new_password = asString(record['new_password']);
  out.new_hint = asString(record['new_hint']);
  return out;
}

/** Encode a `recoverAuthenticationPassword` into TDLib JSON wire shape. */
export function encodeRecoverAuthenticationPassword(value: RecoverAuthenticationPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'recoverAuthenticationPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['recovery_code'] = value.recovery_code;
  out['new_password'] = value.new_password;
  out['new_hint'] = value.new_hint;
  return out;
}

/** recoverPassword = PasswordState — Recovers the 2-step verification password using a recovery code sent to an email address that was previously set up */
export class RecoverPassword {
  type: 'recoverPassword' = 'recoverPassword';
  extra?: string;
  /** recovery_code: string — Recovery code to check */
  recovery_code: string = '';
  /** new_password: string — New 2-step verification password of the user; may be empty to remove the password */
  new_password: string = '';
  /** new_hint: string — New password hint; may be empty */
  new_hint: string = '';
}

/** Decode a TDLib `recoverPassword` object; null input -> null. */
export function decodeRecoverPassword(json: TdJsonRaw): RecoverPassword | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RecoverPassword = new RecoverPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.recovery_code = asString(record['recovery_code']);
  out.new_password = asString(record['new_password']);
  out.new_hint = asString(record['new_hint']);
  return out;
}

/** Encode a `recoverPassword` into TDLib JSON wire shape. */
export function encodeRecoverPassword(value: RecoverPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'recoverPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['recovery_code'] = value.recovery_code;
  out['new_password'] = value.new_password;
  out['new_hint'] = value.new_hint;
  return out;
}

/** recoveryEmailAddress = RecoveryEmailAddress — Contains information about the current recovery email address */
export class RecoveryEmailAddress {
  type: 'recoveryEmailAddress' = 'recoveryEmailAddress';
  extra?: string;
  /** recovery_email_address: string — Recovery email address */
  recovery_email_address: string = '';
}

/** Decode a TDLib `recoveryEmailAddress` object; null input -> null. */
export function decodeRecoveryEmailAddress(json: TdJsonRaw): RecoveryEmailAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RecoveryEmailAddress = new RecoveryEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.recovery_email_address = asString(record['recovery_email_address']);
  return out;
}

/** Encode a `recoveryEmailAddress` into TDLib JSON wire shape. */
export function encodeRecoveryEmailAddress(value: RecoveryEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'recoveryEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['recovery_email_address'] = value.recovery_email_address;
  return out;
}

/** refundStarPayment = Ok — Refunds a previously done payment in Telegram Stars; for bots only */
export class RefundStarPayment {
  type: 'refundStarPayment' = 'refundStarPayment';
  extra?: string;
  /** user_id: int53 — Identifier of the user who did the payment */
  user_id: number = 0;
  /** telegram_payment_charge_id: string — Telegram payment identifier */
  telegram_payment_charge_id: string = '';
}

/** Decode a TDLib `refundStarPayment` object; null input -> null. */
export function decodeRefundStarPayment(json: TdJsonRaw): RefundStarPayment | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RefundStarPayment = new RefundStarPayment();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.telegram_payment_charge_id = asString(record['telegram_payment_charge_id']);
  return out;
}

/** Encode a `refundStarPayment` into TDLib JSON wire shape. */
export function encodeRefundStarPayment(value: RefundStarPayment): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'refundStarPayment';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['telegram_payment_charge_id'] = value.telegram_payment_charge_id;
  return out;
}

/** registerDevice = PushReceiverId — Registers the currently used device for receiving push notifications. Returns a globally unique identifier of the push notification subscription */
export class RegisterDevice {
  type: 'registerDevice' = 'registerDevice';
  extra?: string;
  /** device_token: DeviceToken — Device token */
  device_token: TdDeviceToken | null = null;
  /** other_user_ids: vector<int53> — List of user identifiers of other users currently using the application */
  other_user_ids: number[] = [];
}

/** Decode a TDLib `registerDevice` object; null input -> null. */
export function decodeRegisterDevice(json: TdJsonRaw): RegisterDevice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RegisterDevice = new RegisterDevice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.device_token = decodeTdDeviceToken(record['device_token']);
  out.other_user_ids = decodeVector(record['other_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `registerDevice` into TDLib JSON wire shape. */
export function encodeRegisterDevice(value: RegisterDevice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'registerDevice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['device_token'] = value.device_token === null ? null : encodeTdDeviceToken(value.device_token);
  out['other_user_ids'] = encodeVector(value.other_user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** registerUser = Ok — Finishes user registration. Works only when the current authorization state is authorizationStateWaitRegistration */
export class RegisterUser {
  type: 'registerUser' = 'registerUser';
  extra?: string;
  /** first_name: string — The first name of the user; 1-64 characters */
  first_name: string = '';
  /** last_name: string — The last name of the user; 0-64 characters */
  last_name: string = '';
  /** disable_notification: Bool — Pass true to disable notification about the current user joining Telegram for other users that added them to contact list */
  disable_notification: boolean = false;
}

/** Decode a TDLib `registerUser` object; null input -> null. */
export function decodeRegisterUser(json: TdJsonRaw): RegisterUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RegisterUser = new RegisterUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.first_name = asString(record['first_name']);
  out.last_name = asString(record['last_name']);
  out.disable_notification = asBool(record['disable_notification']);
  return out;
}

/** Encode a `registerUser` into TDLib JSON wire shape. */
export function encodeRegisterUser(value: RegisterUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'registerUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['first_name'] = value.first_name;
  out['last_name'] = value.last_name;
  out['disable_notification'] = value.disable_notification;
  return out;
}

/** remoteFile = RemoteFile — Represents a remote file If the identifier starts with "http://" or "https://", it represents the HTTP URL of the file. TDLib is currently unable to download files if only their URL is known. If downloadFile/add... */
export class RemoteFile {
  type: 'remoteFile' = 'remoteFile';
  extra?: string;
  /** id: string — Remote file identifier; may be empty. Can be used by the current user across application restarts or even from other devices. Uniquely identifies a file, but a file can have a lot of different valid identifiers. */
  id: string = '';
  /** unique_id: string — Unique file identifier; may be empty if unknown. The unique file identifier which is the same for the same file even for different users and is persistent over time */
  unique_id: string = '';
  /** is_uploading_active: Bool — True, if the file is currently being uploaded (or a remote copy is being generated by some other means) */
  is_uploading_active: boolean = false;
  /** is_uploading_completed: Bool — True, if a remote copy is fully available */
  is_uploading_completed: boolean = false;
  /** uploaded_size: int53 — Size of the remote available part of the file, in bytes; 0 if unknown */
  uploaded_size: number = 0;
}

/** Decode a TDLib `remoteFile` object; null input -> null. */
export function decodeRemoteFile(json: TdJsonRaw): RemoteFile | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoteFile = new RemoteFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.unique_id = asString(record['unique_id']);
  out.is_uploading_active = asBool(record['is_uploading_active']);
  out.is_uploading_completed = asBool(record['is_uploading_completed']);
  out.uploaded_size = asNumber(record['uploaded_size']);
  return out;
}

/** Encode a `remoteFile` into TDLib JSON wire shape. */
export function encodeRemoteFile(value: RemoteFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'remoteFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['unique_id'] = value.unique_id;
  out['is_uploading_active'] = value.is_uploading_active;
  out['is_uploading_completed'] = value.is_uploading_completed;
  out['uploaded_size'] = value.uploaded_size;
  return out;
}

/** removeAllFilesFromDownloads = Ok — Removes all files from the file download list */
export class RemoveAllFilesFromDownloads {
  type: 'removeAllFilesFromDownloads' = 'removeAllFilesFromDownloads';
  extra?: string;
  /** only_active: Bool — Pass true to remove only active downloads, including paused */
  only_active: boolean = false;
  /** only_completed: Bool — Pass true to remove only completed downloads */
  only_completed: boolean = false;
  /** delete_from_cache: Bool — Pass true to delete the file from the TDLib file cache */
  delete_from_cache: boolean = false;
}

/** Decode a TDLib `removeAllFilesFromDownloads` object; null input -> null. */
export function decodeRemoveAllFilesFromDownloads(json: TdJsonRaw): RemoveAllFilesFromDownloads | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveAllFilesFromDownloads = new RemoveAllFilesFromDownloads();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.only_active = asBool(record['only_active']);
  out.only_completed = asBool(record['only_completed']);
  out.delete_from_cache = asBool(record['delete_from_cache']);
  return out;
}

/** Encode a `removeAllFilesFromDownloads` into TDLib JSON wire shape. */
export function encodeRemoveAllFilesFromDownloads(value: RemoveAllFilesFromDownloads): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeAllFilesFromDownloads';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['only_active'] = value.only_active;
  out['only_completed'] = value.only_completed;
  out['delete_from_cache'] = value.delete_from_cache;
  return out;
}

/** removeAllWebBrowserSettingsExceptions = Ok — Removes special handling for the opening of all links */
export class RemoveAllWebBrowserSettingsExceptions {
  type: 'removeAllWebBrowserSettingsExceptions' = 'removeAllWebBrowserSettingsExceptions';
  extra?: string;
}

/** Decode a TDLib `removeAllWebBrowserSettingsExceptions` object; null input -> null. */
export function decodeRemoveAllWebBrowserSettingsExceptions(json: TdJsonRaw): RemoveAllWebBrowserSettingsExceptions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveAllWebBrowserSettingsExceptions = new RemoveAllWebBrowserSettingsExceptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `removeAllWebBrowserSettingsExceptions` into TDLib JSON wire shape. */
export function encodeRemoveAllWebBrowserSettingsExceptions(value: RemoveAllWebBrowserSettingsExceptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeAllWebBrowserSettingsExceptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** removeBusinessConnectedBotFromChat = Ok — Removes the connected business bot from a specific chat by adding the chat to businessRecipients.excluded_chat_ids */
export class RemoveBusinessConnectedBotFromChat {
  type: 'removeBusinessConnectedBotFromChat' = 'removeBusinessConnectedBotFromChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `removeBusinessConnectedBotFromChat` object; null input -> null. */
export function decodeRemoveBusinessConnectedBotFromChat(json: TdJsonRaw): RemoveBusinessConnectedBotFromChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveBusinessConnectedBotFromChat = new RemoveBusinessConnectedBotFromChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `removeBusinessConnectedBotFromChat` into TDLib JSON wire shape. */
export function encodeRemoveBusinessConnectedBotFromChat(value: RemoveBusinessConnectedBotFromChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeBusinessConnectedBotFromChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** removeChatActionBar = Ok — Removes a chat action bar without any other action */
export class RemoveChatActionBar {
  type: 'removeChatActionBar' = 'removeChatActionBar';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `removeChatActionBar` object; null input -> null. */
export function decodeRemoveChatActionBar(json: TdJsonRaw): RemoveChatActionBar | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveChatActionBar = new RemoveChatActionBar();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `removeChatActionBar` into TDLib JSON wire shape. */
export function encodeRemoveChatActionBar(value: RemoveChatActionBar): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeChatActionBar';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** removeContacts = Ok — Removes users from the contact list */
export class RemoveContacts {
  type: 'removeContacts' = 'removeContacts';
  extra?: string;
  /** user_ids: vector<int53> — Identifiers of users to be deleted */
  user_ids: number[] = [];
}

/** Decode a TDLib `removeContacts` object; null input -> null. */
export function decodeRemoveContacts(json: TdJsonRaw): RemoveContacts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveContacts = new RemoveContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `removeContacts` into TDLib JSON wire shape. */
export function encodeRemoveContacts(value: RemoveContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** removeFavoriteSticker = Ok — Removes a sticker from the list of favorite stickers */
export class RemoveFavoriteSticker {
  type: 'removeFavoriteSticker' = 'removeFavoriteSticker';
  extra?: string;
  /** sticker: InputFile — Sticker file to delete from the list */
  sticker: TdInputFile | null = null;
}

/** Decode a TDLib `removeFavoriteSticker` object; null input -> null. */
export function decodeRemoveFavoriteSticker(json: TdJsonRaw): RemoveFavoriteSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveFavoriteSticker = new RemoveFavoriteSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  return out;
}

/** Encode a `removeFavoriteSticker` into TDLib JSON wire shape. */
export function encodeRemoveFavoriteSticker(value: RemoveFavoriteSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeFavoriteSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  return out;
}

/** removeFileFromDownloads = Ok — Removes a file from the file download list */
export class RemoveFileFromDownloads {
  type: 'removeFileFromDownloads' = 'removeFileFromDownloads';
  extra?: string;
  /** file_id: int32 — Identifier of the downloaded file */
  file_id: number = 0;
  /** delete_from_cache: Bool — Pass true to delete the file from the TDLib file cache */
  delete_from_cache: boolean = false;
}

/** Decode a TDLib `removeFileFromDownloads` object; null input -> null. */
export function decodeRemoveFileFromDownloads(json: TdJsonRaw): RemoveFileFromDownloads | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveFileFromDownloads = new RemoveFileFromDownloads();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.delete_from_cache = asBool(record['delete_from_cache']);
  return out;
}

/** Encode a `removeFileFromDownloads` into TDLib JSON wire shape. */
export function encodeRemoveFileFromDownloads(value: RemoveFileFromDownloads): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeFileFromDownloads';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['delete_from_cache'] = value.delete_from_cache;
  return out;
}

/** removeGiftCollectionGifts = GiftCollection — Removes gifts from a collection. If the collection is owned by a channel chat, then requires can_post_messages administrator right in the channel chat. Returns the changed collection */
export class RemoveGiftCollectionGifts {
  type: 'removeGiftCollectionGifts' = 'removeGiftCollectionGifts';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that owns the collection */
  owner_id: TdMessageSender | null = null;
  /** collection_id: int32 — Identifier of the gift collection */
  collection_id: number = 0;
  /** received_gift_ids: vector<string> — Identifier of the gifts to remove from the collection */
  received_gift_ids: string[] = [];
}

/** Decode a TDLib `removeGiftCollectionGifts` object; null input -> null. */
export function decodeRemoveGiftCollectionGifts(json: TdJsonRaw): RemoveGiftCollectionGifts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveGiftCollectionGifts = new RemoveGiftCollectionGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.collection_id = asNumber(record['collection_id']);
  out.received_gift_ids = decodeVector(record['received_gift_ids'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `removeGiftCollectionGifts` into TDLib JSON wire shape. */
export function encodeRemoveGiftCollectionGifts(value: RemoveGiftCollectionGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeGiftCollectionGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['collection_id'] = value.collection_id;
  out['received_gift_ids'] = encodeVector(value.received_gift_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** removeInstalledBackground = Ok — Removes background from the list of installed backgrounds */
export class RemoveInstalledBackground {
  type: 'removeInstalledBackground' = 'removeInstalledBackground';
  extra?: string;
  /** background_id: int64 — The background identifier */
  background_id: string = '';
}

/** Decode a TDLib `removeInstalledBackground` object; null input -> null. */
export function decodeRemoveInstalledBackground(json: TdJsonRaw): RemoveInstalledBackground | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveInstalledBackground = new RemoveInstalledBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.background_id = asInt64(record['background_id']);
  return out;
}

/** Encode a `removeInstalledBackground` into TDLib JSON wire shape. */
export function encodeRemoveInstalledBackground(value: RemoveInstalledBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeInstalledBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['background_id'] = value.background_id;
  return out;
}

/** removeLoginPasskey = Ok — Removes a passkey from the list of passkeys allowed to be used for the login by the current user */
export class RemoveLoginPasskey {
  type: 'removeLoginPasskey' = 'removeLoginPasskey';
  extra?: string;
  /** passkey_id: string — Unique identifier of the passkey to remove */
  passkey_id: string = '';
}

/** Decode a TDLib `removeLoginPasskey` object; null input -> null. */
export function decodeRemoveLoginPasskey(json: TdJsonRaw): RemoveLoginPasskey | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveLoginPasskey = new RemoveLoginPasskey();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.passkey_id = asString(record['passkey_id']);
  return out;
}

/** Encode a `removeLoginPasskey` into TDLib JSON wire shape. */
export function encodeRemoveLoginPasskey(value: RemoveLoginPasskey): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeLoginPasskey';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['passkey_id'] = value.passkey_id;
  return out;
}

/** removeMessageReaction = Ok — Removes a reaction from a message. A chosen reaction can always be removed */
export class RemoveMessageReaction {
  type: 'removeMessageReaction' = 'removeMessageReaction';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** reaction_type: ReactionType — Type of the reaction to remove. The paid reaction can't be removed */
  reaction_type: TdReactionType | null = null;
}

/** Decode a TDLib `removeMessageReaction` object; null input -> null. */
export function decodeRemoveMessageReaction(json: TdJsonRaw): RemoveMessageReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveMessageReaction = new RemoveMessageReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reaction_type = decodeTdReactionType(record['reaction_type']);
  return out;
}

/** Encode a `removeMessageReaction` into TDLib JSON wire shape. */
export function encodeRemoveMessageReaction(value: RemoveMessageReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeMessageReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reaction_type'] = value.reaction_type === null ? null : encodeTdReactionType(value.reaction_type);
  return out;
}

/** removeMessageSenderBotVerification = Ok — Removes the verification status of a user or a chat by an owned bot */
export class RemoveMessageSenderBotVerification {
  type: 'removeMessageSenderBotVerification' = 'removeMessageSenderBotVerification';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the owned bot, which verified the user or the chat */
  bot_user_id: number = 0;
  /** verified_id: MessageSender — Identifier of the user or the supergroup or channel chat, which verification is removed */
  verified_id: TdMessageSender | null = null;
}

/** Decode a TDLib `removeMessageSenderBotVerification` object; null input -> null. */
export function decodeRemoveMessageSenderBotVerification(json: TdJsonRaw): RemoveMessageSenderBotVerification | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveMessageSenderBotVerification = new RemoveMessageSenderBotVerification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.verified_id = decodeTdMessageSender(record['verified_id']);
  return out;
}

/** Encode a `removeMessageSenderBotVerification` into TDLib JSON wire shape. */
export function encodeRemoveMessageSenderBotVerification(value: RemoveMessageSenderBotVerification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeMessageSenderBotVerification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['verified_id'] = value.verified_id === null ? null : encodeTdMessageSender(value.verified_id);
  return out;
}

/** removeNotification = Ok — Removes an active notification from notification list. Needs to be called only if the notification is removed by the current user */
export class RemoveNotification {
  type: 'removeNotification' = 'removeNotification';
  extra?: string;
  /** notification_group_id: int32 — Identifier of notification group to which the notification belongs */
  notification_group_id: number = 0;
  /** notification_id: int32 — Identifier of removed notification */
  notification_id: number = 0;
}

/** Decode a TDLib `removeNotification` object; null input -> null. */
export function decodeRemoveNotification(json: TdJsonRaw): RemoveNotification | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveNotification = new RemoveNotification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_group_id = asNumber(record['notification_group_id']);
  out.notification_id = asNumber(record['notification_id']);
  return out;
}

/** Encode a `removeNotification` into TDLib JSON wire shape. */
export function encodeRemoveNotification(value: RemoveNotification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeNotification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_group_id'] = value.notification_group_id;
  out['notification_id'] = value.notification_id;
  return out;
}

/** removeNotificationGroup = Ok — Removes a group of active notifications. Needs to be called only if the notification group is removed by the current user */
export class RemoveNotificationGroup {
  type: 'removeNotificationGroup' = 'removeNotificationGroup';
  extra?: string;
  /** notification_group_id: int32 — Notification group identifier */
  notification_group_id: number = 0;
  /** max_notification_id: int32 — The maximum identifier of removed notifications */
  max_notification_id: number = 0;
}

/** Decode a TDLib `removeNotificationGroup` object; null input -> null. */
export function decodeRemoveNotificationGroup(json: TdJsonRaw): RemoveNotificationGroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveNotificationGroup = new RemoveNotificationGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_group_id = asNumber(record['notification_group_id']);
  out.max_notification_id = asNumber(record['max_notification_id']);
  return out;
}

/** Encode a `removeNotificationGroup` into TDLib JSON wire shape. */
export function encodeRemoveNotificationGroup(value: RemoveNotificationGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeNotificationGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_group_id'] = value.notification_group_id;
  out['max_notification_id'] = value.max_notification_id;
  return out;
}

/** removePendingLiveStoryReactions = Ok — Removes all pending paid reactions in a live story group call */
export class RemovePendingLiveStoryReactions {
  type: 'removePendingLiveStoryReactions' = 'removePendingLiveStoryReactions';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `removePendingLiveStoryReactions` object; null input -> null. */
export function decodeRemovePendingLiveStoryReactions(json: TdJsonRaw): RemovePendingLiveStoryReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemovePendingLiveStoryReactions = new RemovePendingLiveStoryReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `removePendingLiveStoryReactions` into TDLib JSON wire shape. */
export function encodeRemovePendingLiveStoryReactions(value: RemovePendingLiveStoryReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removePendingLiveStoryReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** removePendingPaidMessageReactions = Ok — Removes all pending paid reactions on a message */
export class RemovePendingPaidMessageReactions {
  type: 'removePendingPaidMessageReactions' = 'removePendingPaidMessageReactions';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `removePendingPaidMessageReactions` object; null input -> null. */
export function decodeRemovePendingPaidMessageReactions(json: TdJsonRaw): RemovePendingPaidMessageReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemovePendingPaidMessageReactions = new RemovePendingPaidMessageReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `removePendingPaidMessageReactions` into TDLib JSON wire shape. */
export function encodeRemovePendingPaidMessageReactions(value: RemovePendingPaidMessageReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removePendingPaidMessageReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** removeProfileAudio = Ok — Removes an audio file from the profile audio files of the current user */
export class RemoveProfileAudio {
  type: 'removeProfileAudio' = 'removeProfileAudio';
  extra?: string;
  /** file_id: int32 — Identifier of the audio file to be removed */
  file_id: number = 0;
}

/** Decode a TDLib `removeProfileAudio` object; null input -> null. */
export function decodeRemoveProfileAudio(json: TdJsonRaw): RemoveProfileAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveProfileAudio = new RemoveProfileAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  return out;
}

/** Encode a `removeProfileAudio` into TDLib JSON wire shape. */
export function encodeRemoveProfileAudio(value: RemoveProfileAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeProfileAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  return out;
}

/** removeProxy = Ok — Removes a proxy server. Can be called before authorization */
export class RemoveProxy {
  type: 'removeProxy' = 'removeProxy';
  extra?: string;
  /** proxy_id: int32 — Proxy identifier */
  proxy_id: number = 0;
}

/** Decode a TDLib `removeProxy` object; null input -> null. */
export function decodeRemoveProxy(json: TdJsonRaw): RemoveProxy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveProxy = new RemoveProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.proxy_id = asNumber(record['proxy_id']);
  return out;
}

/** Encode a `removeProxy` into TDLib JSON wire shape. */
export function encodeRemoveProxy(value: RemoveProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['proxy_id'] = value.proxy_id;
  return out;
}

/** removeRecentHashtag = Ok — Removes a hashtag from the list of recently used hashtags */
export class RemoveRecentHashtag {
  type: 'removeRecentHashtag' = 'removeRecentHashtag';
  extra?: string;
  /** hashtag: string — Hashtag to delete */
  hashtag: string = '';
}

/** Decode a TDLib `removeRecentHashtag` object; null input -> null. */
export function decodeRemoveRecentHashtag(json: TdJsonRaw): RemoveRecentHashtag | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveRecentHashtag = new RemoveRecentHashtag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.hashtag = asString(record['hashtag']);
  return out;
}

/** Encode a `removeRecentHashtag` into TDLib JSON wire shape. */
export function encodeRemoveRecentHashtag(value: RemoveRecentHashtag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeRecentHashtag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['hashtag'] = value.hashtag;
  return out;
}

/** removeRecentSticker = Ok — Removes a sticker from the list of recently used stickers */
export class RemoveRecentSticker {
  type: 'removeRecentSticker' = 'removeRecentSticker';
  extra?: string;
  /** is_attached: Bool — Pass true to remove the sticker from the list of stickers recently attached to photo or video files; pass false to remove the sticker from the list of recently sent stickers */
  is_attached: boolean = false;
  /** sticker: InputFile — Sticker file to delete */
  sticker: TdInputFile | null = null;
}

/** Decode a TDLib `removeRecentSticker` object; null input -> null. */
export function decodeRemoveRecentSticker(json: TdJsonRaw): RemoveRecentSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveRecentSticker = new RemoveRecentSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_attached = asBool(record['is_attached']);
  out.sticker = decodeTdInputFile(record['sticker']);
  return out;
}

/** Encode a `removeRecentSticker` into TDLib JSON wire shape. */
export function encodeRemoveRecentSticker(value: RemoveRecentSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeRecentSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_attached'] = value.is_attached;
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  return out;
}

/** removeRecentlyFoundChat = Ok — Removes a chat from the list of recently found chats */
export class RemoveRecentlyFoundChat {
  type: 'removeRecentlyFoundChat' = 'removeRecentlyFoundChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to be removed */
  chat_id: number = 0;
}

/** Decode a TDLib `removeRecentlyFoundChat` object; null input -> null. */
export function decodeRemoveRecentlyFoundChat(json: TdJsonRaw): RemoveRecentlyFoundChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveRecentlyFoundChat = new RemoveRecentlyFoundChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `removeRecentlyFoundChat` into TDLib JSON wire shape. */
export function encodeRemoveRecentlyFoundChat(value: RemoveRecentlyFoundChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeRecentlyFoundChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** removeSavedAnimation = Ok — Removes an animation from the list of saved animations */
export class RemoveSavedAnimation {
  type: 'removeSavedAnimation' = 'removeSavedAnimation';
  extra?: string;
  /** animation: InputFile — Animation file to be removed */
  animation: TdInputFile | null = null;
}

/** Decode a TDLib `removeSavedAnimation` object; null input -> null. */
export function decodeRemoveSavedAnimation(json: TdJsonRaw): RemoveSavedAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveSavedAnimation = new RemoveSavedAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeTdInputFile(record['animation']);
  return out;
}

/** Encode a `removeSavedAnimation` into TDLib JSON wire shape. */
export function encodeRemoveSavedAnimation(value: RemoveSavedAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeSavedAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeTdInputFile(value.animation);
  return out;
}

/** removeSavedNotificationSound = Ok — Removes a notification sound from the list of saved notification sounds */
export class RemoveSavedNotificationSound {
  type: 'removeSavedNotificationSound' = 'removeSavedNotificationSound';
  extra?: string;
  /** notification_sound_id: int64 — Identifier of the notification sound */
  notification_sound_id: string = '';
}

/** Decode a TDLib `removeSavedNotificationSound` object; null input -> null. */
export function decodeRemoveSavedNotificationSound(json: TdJsonRaw): RemoveSavedNotificationSound | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveSavedNotificationSound = new RemoveSavedNotificationSound();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_sound_id = asInt64(record['notification_sound_id']);
  return out;
}

/** Encode a `removeSavedNotificationSound` into TDLib JSON wire shape. */
export function encodeRemoveSavedNotificationSound(value: RemoveSavedNotificationSound): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeSavedNotificationSound';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_sound_id'] = value.notification_sound_id;
  return out;
}

/** removeSearchedForTag = Ok — Removes a hashtag or a cashtag from the list of recently searched for hashtags or cashtags */
export class RemoveSearchedForTag {
  type: 'removeSearchedForTag' = 'removeSearchedForTag';
  extra?: string;
  /** tag: string — Hashtag or cashtag to delete */
  tag: string = '';
}

/** Decode a TDLib `removeSearchedForTag` object; null input -> null. */
export function decodeRemoveSearchedForTag(json: TdJsonRaw): RemoveSearchedForTag | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveSearchedForTag = new RemoveSearchedForTag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.tag = asString(record['tag']);
  return out;
}

/** Encode a `removeSearchedForTag` into TDLib JSON wire shape. */
export function encodeRemoveSearchedForTag(value: RemoveSearchedForTag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeSearchedForTag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['tag'] = value.tag;
  return out;
}

/** removeStickerFromSet = Ok — Removes a sticker from the set to which it belongs. The sticker set must be owned by the current user */
export class RemoveStickerFromSet {
  type: 'removeStickerFromSet' = 'removeStickerFromSet';
  extra?: string;
  /** sticker: InputFile — Sticker to remove from the set */
  sticker: TdInputFile | null = null;
}

/** Decode a TDLib `removeStickerFromSet` object; null input -> null. */
export function decodeRemoveStickerFromSet(json: TdJsonRaw): RemoveStickerFromSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveStickerFromSet = new RemoveStickerFromSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  return out;
}

/** Encode a `removeStickerFromSet` into TDLib JSON wire shape. */
export function encodeRemoveStickerFromSet(value: RemoveStickerFromSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeStickerFromSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  return out;
}

/** removeStoryAlbumStories = StoryAlbum — Removes stories from an album. If the album is owned by a supergroup or a channel chat, then requires can_edit_stories administrator right in the chat. Returns the changed album */
export class RemoveStoryAlbumStories {
  type: 'removeStoryAlbumStories' = 'removeStoryAlbumStories';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that owns the stories */
  chat_id: number = 0;
  /** story_album_id: int32 — Identifier of the story album */
  story_album_id: number = 0;
  /** story_ids: vector<int32> — Identifier of the stories to remove from the album */
  story_ids: number[] = [];
}

/** Decode a TDLib `removeStoryAlbumStories` object; null input -> null. */
export function decodeRemoveStoryAlbumStories(json: TdJsonRaw): RemoveStoryAlbumStories | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveStoryAlbumStories = new RemoveStoryAlbumStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_album_id = asNumber(record['story_album_id']);
  out.story_ids = decodeVector(record['story_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `removeStoryAlbumStories` into TDLib JSON wire shape. */
export function encodeRemoveStoryAlbumStories(value: RemoveStoryAlbumStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeStoryAlbumStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_album_id'] = value.story_album_id;
  out['story_ids'] = encodeVector(value.story_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** removeTextCompositionStyle = Ok — Removes a custom text composition style from the list of used by the user styles. If the style was created by the current user, then it can only be deleted */
export class RemoveTextCompositionStyle {
  type: 'removeTextCompositionStyle' = 'removeTextCompositionStyle';
  extra?: string;
  /** name: string — Name of the style */
  name: string = '';
}

/** Decode a TDLib `removeTextCompositionStyle` object; null input -> null. */
export function decodeRemoveTextCompositionStyle(json: TdJsonRaw): RemoveTextCompositionStyle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveTextCompositionStyle = new RemoveTextCompositionStyle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `removeTextCompositionStyle` into TDLib JSON wire shape. */
export function encodeRemoveTextCompositionStyle(value: RemoveTextCompositionStyle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeTextCompositionStyle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** removeTopChat = Ok — Removes a chat from the list of frequently used chats. Supported only if the chat info database is enabled */
export class RemoveTopChat {
  type: 'removeTopChat' = 'removeTopChat';
  extra?: string;
  /** category: TopChatCategory — Category of frequently used chats */
  category: TdTopChatCategory | null = null;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `removeTopChat` object; null input -> null. */
export function decodeRemoveTopChat(json: TdJsonRaw): RemoveTopChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveTopChat = new RemoveTopChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.category = decodeTdTopChatCategory(record['category']);
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `removeTopChat` into TDLib JSON wire shape. */
export function encodeRemoveTopChat(value: RemoveTopChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeTopChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['category'] = value.category === null ? null : encodeTdTopChatCategory(value.category);
  out['chat_id'] = value.chat_id;
  return out;
}

/** removeWebBrowserSettingsException = Ok — Removes a special handling for the opening of the specified URL */
export class RemoveWebBrowserSettingsException {
  type: 'removeWebBrowserSettingsException' = 'removeWebBrowserSettingsException';
  extra?: string;
  /** url: string — URL of the website */
  url: string = '';
}

/** Decode a TDLib `removeWebBrowserSettingsException` object; null input -> null. */
export function decodeRemoveWebBrowserSettingsException(json: TdJsonRaw): RemoveWebBrowserSettingsException | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RemoveWebBrowserSettingsException = new RemoveWebBrowserSettingsException();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `removeWebBrowserSettingsException` into TDLib JSON wire shape. */
export function encodeRemoveWebBrowserSettingsException(value: RemoveWebBrowserSettingsException): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'removeWebBrowserSettingsException';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** reorderActiveUsernames = Ok — Changes order of active usernames of the current user */
export class ReorderActiveUsernames {
  type: 'reorderActiveUsernames' = 'reorderActiveUsernames';
  extra?: string;
  /** usernames: vector<string> — The new order of active usernames. All currently active usernames must be specified */
  usernames: string[] = [];
}

/** Decode a TDLib `reorderActiveUsernames` object; null input -> null. */
export function decodeReorderActiveUsernames(json: TdJsonRaw): ReorderActiveUsernames | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderActiveUsernames = new ReorderActiveUsernames();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.usernames = decodeVector(record['usernames'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `reorderActiveUsernames` into TDLib JSON wire shape. */
export function encodeReorderActiveUsernames(value: ReorderActiveUsernames): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderActiveUsernames';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['usernames'] = encodeVector(value.usernames, (item: string): TdJsonRaw => item);
  return out;
}

/** reorderBotActiveUsernames = Ok — Changes order of active usernames of a bot. Can be called only if userTypeBot.can_be_edited == true */
export class ReorderBotActiveUsernames {
  type: 'reorderBotActiveUsernames' = 'reorderBotActiveUsernames';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** usernames: vector<string> — The new order of active usernames. All currently active usernames must be specified */
  usernames: string[] = [];
}

/** Decode a TDLib `reorderBotActiveUsernames` object; null input -> null. */
export function decodeReorderBotActiveUsernames(json: TdJsonRaw): ReorderBotActiveUsernames | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderBotActiveUsernames = new ReorderBotActiveUsernames();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.usernames = decodeVector(record['usernames'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `reorderBotActiveUsernames` into TDLib JSON wire shape. */
export function encodeReorderBotActiveUsernames(value: ReorderBotActiveUsernames): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderBotActiveUsernames';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['usernames'] = encodeVector(value.usernames, (item: string): TdJsonRaw => item);
  return out;
}

/** reorderBotMediaPreviews = Ok — Changes order of media previews in the list of media previews of a bot */
export class ReorderBotMediaPreviews {
  type: 'reorderBotMediaPreviews' = 'reorderBotMediaPreviews';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot. The bot must be owned and must have the main Web App */
  bot_user_id: number = 0;
  /** language_code: string — Language code of the media previews to reorder */
  language_code: string = '';
  /** file_ids: vector<int32> — File identifiers of the media in the new order */
  file_ids: number[] = [];
}

/** Decode a TDLib `reorderBotMediaPreviews` object; null input -> null. */
export function decodeReorderBotMediaPreviews(json: TdJsonRaw): ReorderBotMediaPreviews | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderBotMediaPreviews = new ReorderBotMediaPreviews();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  out.file_ids = decodeVector(record['file_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `reorderBotMediaPreviews` into TDLib JSON wire shape. */
export function encodeReorderBotMediaPreviews(value: ReorderBotMediaPreviews): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderBotMediaPreviews';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  out['file_ids'] = encodeVector(value.file_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** reorderChatFolders = Ok — Changes the order of chat folders */
export class ReorderChatFolders {
  type: 'reorderChatFolders' = 'reorderChatFolders';
  extra?: string;
  /** chat_folder_ids: vector<int32> — Identifiers of chat folders in the new correct order */
  chat_folder_ids: number[] = [];
  /** main_chat_list_position: int32 — Position of the main chat list among chat folders, 0-based. Can be non-zero only for Premium users */
  main_chat_list_position: number = 0;
}

/** Decode a TDLib `reorderChatFolders` object; null input -> null. */
export function decodeReorderChatFolders(json: TdJsonRaw): ReorderChatFolders | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderChatFolders = new ReorderChatFolders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_ids = decodeVector(record['chat_folder_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.main_chat_list_position = asNumber(record['main_chat_list_position']);
  return out;
}

/** Encode a `reorderChatFolders` into TDLib JSON wire shape. */
export function encodeReorderChatFolders(value: ReorderChatFolders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderChatFolders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_ids'] = encodeVector(value.chat_folder_ids, (item: number): TdJsonRaw => item);
  out['main_chat_list_position'] = value.main_chat_list_position;
  return out;
}

/** reorderGiftCollectionGifts = GiftCollection — Changes order of gifts in a collection. If the collection is owned by a channel chat, then requires can_post_messages administrator right in the channel chat. Returns the changed collection */
export class ReorderGiftCollectionGifts {
  type: 'reorderGiftCollectionGifts' = 'reorderGiftCollectionGifts';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that owns the collection */
  owner_id: TdMessageSender | null = null;
  /** collection_id: int32 — Identifier of the gift collection */
  collection_id: number = 0;
  /** received_gift_ids: vector<string> — Identifier of the gifts to move to the beginning of the collection. All other gifts are placed in the current order after the specified gifts */
  received_gift_ids: string[] = [];
}

/** Decode a TDLib `reorderGiftCollectionGifts` object; null input -> null. */
export function decodeReorderGiftCollectionGifts(json: TdJsonRaw): ReorderGiftCollectionGifts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderGiftCollectionGifts = new ReorderGiftCollectionGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.collection_id = asNumber(record['collection_id']);
  out.received_gift_ids = decodeVector(record['received_gift_ids'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `reorderGiftCollectionGifts` into TDLib JSON wire shape. */
export function encodeReorderGiftCollectionGifts(value: ReorderGiftCollectionGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderGiftCollectionGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['collection_id'] = value.collection_id;
  out['received_gift_ids'] = encodeVector(value.received_gift_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** reorderGiftCollections = Ok — Changes order of gift collections. If the collections are owned by a channel chat, then requires can_post_messages administrator right in the channel chat */
export class ReorderGiftCollections {
  type: 'reorderGiftCollections' = 'reorderGiftCollections';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that owns the collection */
  owner_id: TdMessageSender | null = null;
  /** collection_ids: vector<int32> — New order of gift collections */
  collection_ids: number[] = [];
}

/** Decode a TDLib `reorderGiftCollections` object; null input -> null. */
export function decodeReorderGiftCollections(json: TdJsonRaw): ReorderGiftCollections | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderGiftCollections = new ReorderGiftCollections();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.collection_ids = decodeVector(record['collection_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `reorderGiftCollections` into TDLib JSON wire shape. */
export function encodeReorderGiftCollections(value: ReorderGiftCollections): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderGiftCollections';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['collection_ids'] = encodeVector(value.collection_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** reorderInstalledStickerSets = Ok — Changes the order of installed sticker sets */
export class ReorderInstalledStickerSets {
  type: 'reorderInstalledStickerSets' = 'reorderInstalledStickerSets';
  extra?: string;
  /** sticker_type: StickerType — Type of the sticker sets to reorder */
  sticker_type: TdStickerType | null = null;
  /** sticker_set_ids: vector<int64> — Identifiers of installed sticker sets in the new correct order */
  sticker_set_ids: string[] = [];
}

/** Decode a TDLib `reorderInstalledStickerSets` object; null input -> null. */
export function decodeReorderInstalledStickerSets(json: TdJsonRaw): ReorderInstalledStickerSets | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderInstalledStickerSets = new ReorderInstalledStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.sticker_set_ids = decodeVector(record['sticker_set_ids'], (item: TdJsonRaw): string => asInt64(item));
  return out;
}

/** Encode a `reorderInstalledStickerSets` into TDLib JSON wire shape. */
export function encodeReorderInstalledStickerSets(value: ReorderInstalledStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderInstalledStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['sticker_set_ids'] = encodeVector(value.sticker_set_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** reorderQuickReplyShortcuts = Ok — Changes the order of quick reply shortcuts */
export class ReorderQuickReplyShortcuts {
  type: 'reorderQuickReplyShortcuts' = 'reorderQuickReplyShortcuts';
  extra?: string;
  /** shortcut_ids: vector<int32> — The new order of quick reply shortcuts */
  shortcut_ids: number[] = [];
}

/** Decode a TDLib `reorderQuickReplyShortcuts` object; null input -> null. */
export function decodeReorderQuickReplyShortcuts(json: TdJsonRaw): ReorderQuickReplyShortcuts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderQuickReplyShortcuts = new ReorderQuickReplyShortcuts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_ids = decodeVector(record['shortcut_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `reorderQuickReplyShortcuts` into TDLib JSON wire shape. */
export function encodeReorderQuickReplyShortcuts(value: ReorderQuickReplyShortcuts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderQuickReplyShortcuts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_ids'] = encodeVector(value.shortcut_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** reorderStoryAlbumStories = StoryAlbum — Changes order of stories in an album. If the album is owned by a supergroup or a channel chat, then requires can_edit_stories administrator right in the chat. Returns the changed album */
export class ReorderStoryAlbumStories {
  type: 'reorderStoryAlbumStories' = 'reorderStoryAlbumStories';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that owns the stories */
  chat_id: number = 0;
  /** story_album_id: int32 — Identifier of the story album */
  story_album_id: number = 0;
  /** story_ids: vector<int32> — Identifier of the stories to move to the beginning of the album. All other stories are placed in the current order after the specified stories */
  story_ids: number[] = [];
}

/** Decode a TDLib `reorderStoryAlbumStories` object; null input -> null. */
export function decodeReorderStoryAlbumStories(json: TdJsonRaw): ReorderStoryAlbumStories | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderStoryAlbumStories = new ReorderStoryAlbumStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_album_id = asNumber(record['story_album_id']);
  out.story_ids = decodeVector(record['story_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `reorderStoryAlbumStories` into TDLib JSON wire shape. */
export function encodeReorderStoryAlbumStories(value: ReorderStoryAlbumStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderStoryAlbumStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_album_id'] = value.story_album_id;
  out['story_ids'] = encodeVector(value.story_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** reorderStoryAlbums = Ok — Changes order of story albums. If the albums are owned by a supergroup or a channel chat, then requires can_edit_stories administrator right in the chat */
export class ReorderStoryAlbums {
  type: 'reorderStoryAlbums' = 'reorderStoryAlbums';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that owns the stories */
  chat_id: number = 0;
  /** story_album_ids: vector<int32> — New order of story albums */
  story_album_ids: number[] = [];
}

/** Decode a TDLib `reorderStoryAlbums` object; null input -> null. */
export function decodeReorderStoryAlbums(json: TdJsonRaw): ReorderStoryAlbums | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderStoryAlbums = new ReorderStoryAlbums();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_album_ids = decodeVector(record['story_album_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `reorderStoryAlbums` into TDLib JSON wire shape. */
export function encodeReorderStoryAlbums(value: ReorderStoryAlbums): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderStoryAlbums';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_album_ids'] = encodeVector(value.story_album_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** reorderSupergroupActiveUsernames = Ok — Changes order of active usernames of a supergroup or channel, requires owner privileges in the supergroup or channel */
export class ReorderSupergroupActiveUsernames {
  type: 'reorderSupergroupActiveUsernames' = 'reorderSupergroupActiveUsernames';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup or channel */
  supergroup_id: number = 0;
  /** usernames: vector<string> — The new order of active usernames. All currently active usernames must be specified */
  usernames: string[] = [];
}

/** Decode a TDLib `reorderSupergroupActiveUsernames` object; null input -> null. */
export function decodeReorderSupergroupActiveUsernames(json: TdJsonRaw): ReorderSupergroupActiveUsernames | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReorderSupergroupActiveUsernames = new ReorderSupergroupActiveUsernames();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.usernames = decodeVector(record['usernames'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `reorderSupergroupActiveUsernames` into TDLib JSON wire shape. */
export function encodeReorderSupergroupActiveUsernames(value: ReorderSupergroupActiveUsernames): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reorderSupergroupActiveUsernames';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['usernames'] = encodeVector(value.usernames, (item: string): TdJsonRaw => item);
  return out;
}

/** replaceLiveStoryRtmpUrl = RtmpUrl — Replaces the current RTMP URL for streaming to a live story; requires owner privileges for channel chats */
export class ReplaceLiveStoryRtmpUrl {
  type: 'replaceLiveStoryRtmpUrl' = 'replaceLiveStoryRtmpUrl';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `replaceLiveStoryRtmpUrl` object; null input -> null. */
export function decodeReplaceLiveStoryRtmpUrl(json: TdJsonRaw): ReplaceLiveStoryRtmpUrl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReplaceLiveStoryRtmpUrl = new ReplaceLiveStoryRtmpUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `replaceLiveStoryRtmpUrl` into TDLib JSON wire shape. */
export function encodeReplaceLiveStoryRtmpUrl(value: ReplaceLiveStoryRtmpUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'replaceLiveStoryRtmpUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** replacePrimaryChatInviteLink = ChatInviteLink — Replaces current primary invite link for a chat with a new primary invite link. Available for basic groups, supergroups, and channels. Requires administrator privileges and can_invite_users... */
export class ReplacePrimaryChatInviteLink {
  type: 'replacePrimaryChatInviteLink' = 'replacePrimaryChatInviteLink';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `replacePrimaryChatInviteLink` object; null input -> null. */
export function decodeReplacePrimaryChatInviteLink(json: TdJsonRaw): ReplacePrimaryChatInviteLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReplacePrimaryChatInviteLink = new ReplacePrimaryChatInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `replacePrimaryChatInviteLink` into TDLib JSON wire shape. */
export function encodeReplacePrimaryChatInviteLink(value: ReplacePrimaryChatInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'replacePrimaryChatInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** replaceStickerInSet = Ok — Replaces existing sticker in a set. The function is equivalent to removeStickerFromSet, then addStickerToSet, then setStickerPositionInSet */
export class ReplaceStickerInSet {
  type: 'replaceStickerInSet' = 'replaceStickerInSet';
  extra?: string;
  /** user_id: int53 — Sticker set owner; ignored for regular users */
  user_id: number = 0;
  /** name: string — Sticker set name. The sticker set must be owned by the current user */
  name: string = '';
  /** old_sticker: InputFile — Sticker to remove from the set */
  old_sticker: TdInputFile | null = null;
  /** new_sticker: newSticker — Sticker to add to the set */
  new_sticker: NewSticker | null = null;
}

/** Decode a TDLib `replaceStickerInSet` object; null input -> null. */
export function decodeReplaceStickerInSet(json: TdJsonRaw): ReplaceStickerInSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReplaceStickerInSet = new ReplaceStickerInSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.name = asString(record['name']);
  out.old_sticker = decodeTdInputFile(record['old_sticker']);
  out.new_sticker = decodeNewSticker(record['new_sticker']);
  return out;
}

/** Encode a `replaceStickerInSet` into TDLib JSON wire shape. */
export function encodeReplaceStickerInSet(value: ReplaceStickerInSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'replaceStickerInSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['name'] = value.name;
  out['old_sticker'] = value.old_sticker === null ? null : encodeTdInputFile(value.old_sticker);
  out['new_sticker'] = value.new_sticker === null ? null : encodeNewSticker(value.new_sticker);
  return out;
}

/** replaceVideoChatRtmpUrl = RtmpUrl — Replaces the current RTMP URL for streaming to the video chat of a chat; requires owner privileges in the chat */
export class ReplaceVideoChatRtmpUrl {
  type: 'replaceVideoChatRtmpUrl' = 'replaceVideoChatRtmpUrl';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `replaceVideoChatRtmpUrl` object; null input -> null. */
export function decodeReplaceVideoChatRtmpUrl(json: TdJsonRaw): ReplaceVideoChatRtmpUrl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReplaceVideoChatRtmpUrl = new ReplaceVideoChatRtmpUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `replaceVideoChatRtmpUrl` into TDLib JSON wire shape. */
export function encodeReplaceVideoChatRtmpUrl(value: ReplaceVideoChatRtmpUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'replaceVideoChatRtmpUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** replyMarkupForceReply = ReplyMarkup — Instructs application to force a reply to this message */
export class ReplyMarkupForceReply {
  type: 'replyMarkupForceReply' = 'replyMarkupForceReply';
  extra?: string;
  /** is_personal: Bool — True, if a forced reply must automatically be shown to the current user. For outgoing messages, specify true to show the forced reply only for the mentioned users and for the target user of a reply */
  is_personal: boolean = false;
  /** input_field_placeholder: string — If non-empty, the placeholder to be shown in the input field when the reply is active; 0-64 characters */
  input_field_placeholder: string = '';
}

/** Decode a TDLib `replyMarkupForceReply` object; null input -> null. */
export function decodeReplyMarkupForceReply(json: TdJsonRaw): ReplyMarkupForceReply | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReplyMarkupForceReply = new ReplyMarkupForceReply();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_personal = asBool(record['is_personal']);
  out.input_field_placeholder = asString(record['input_field_placeholder']);
  return out;
}

/** Encode a `replyMarkupForceReply` into TDLib JSON wire shape. */
export function encodeReplyMarkupForceReply(value: ReplyMarkupForceReply): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'replyMarkupForceReply';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_personal'] = value.is_personal;
  out['input_field_placeholder'] = value.input_field_placeholder;
  return out;
}

/** replyMarkupInlineKeyboard = ReplyMarkup — Contains an inline keyboard layout */
export class ReplyMarkupInlineKeyboard {
  type: 'replyMarkupInlineKeyboard' = 'replyMarkupInlineKeyboard';
  extra?: string;
  /** rows: vector<vector<inlineKeyboardButton>> — A list of rows of inline keyboard buttons */
  rows: InlineKeyboardButton[][] = [];
  /** force_reply: Bool — True, if a reply to the message must be forced when the message is received */
  force_reply: boolean = false;
}

/** Decode a TDLib `replyMarkupInlineKeyboard` object; null input -> null. */
export function decodeReplyMarkupInlineKeyboard(json: TdJsonRaw): ReplyMarkupInlineKeyboard | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReplyMarkupInlineKeyboard = new ReplyMarkupInlineKeyboard();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.rows = decodeVector(record['rows'], (items: TdJsonRaw): (InlineKeyboardButton)[] => decodeVector(items, (item: TdJsonRaw): InlineKeyboardButton => decodeInlineKeyboardButton(item) as InlineKeyboardButton));
  out.force_reply = asBool(record['force_reply']);
  return out;
}

/** Encode a `replyMarkupInlineKeyboard` into TDLib JSON wire shape. */
export function encodeReplyMarkupInlineKeyboard(value: ReplyMarkupInlineKeyboard): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'replyMarkupInlineKeyboard';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['rows'] = encodeVector(value.rows, (items: (InlineKeyboardButton | null)[]): TdJsonRaw[] => encodeVector(items, (item: InlineKeyboardButton | null): TdJsonRaw => item === null ? null : encodeInlineKeyboardButton(item)));
  out['force_reply'] = value.force_reply;
  return out;
}

/** replyMarkupRemoveKeyboard = ReplyMarkup — Instructs application to remove the keyboard once this message has been received. This kind of keyboard can't be received in an incoming message; instead, updateChatReplyMarkup with reply_markup_... */
export class ReplyMarkupRemoveKeyboard {
  type: 'replyMarkupRemoveKeyboard' = 'replyMarkupRemoveKeyboard';
  extra?: string;
  /** is_personal: Bool — True, if the keyboard is removed only for the mentioned users or the target user of a reply */
  is_personal: boolean = false;
}

/** Decode a TDLib `replyMarkupRemoveKeyboard` object; null input -> null. */
export function decodeReplyMarkupRemoveKeyboard(json: TdJsonRaw): ReplyMarkupRemoveKeyboard | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReplyMarkupRemoveKeyboard = new ReplyMarkupRemoveKeyboard();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_personal = asBool(record['is_personal']);
  return out;
}

/** Encode a `replyMarkupRemoveKeyboard` into TDLib JSON wire shape. */
export function encodeReplyMarkupRemoveKeyboard(value: ReplyMarkupRemoveKeyboard): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'replyMarkupRemoveKeyboard';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_personal'] = value.is_personal;
  return out;
}

/** replyMarkupShowKeyboard = ReplyMarkup — Contains a custom keyboard layout to quickly reply to bots */
export class ReplyMarkupShowKeyboard {
  type: 'replyMarkupShowKeyboard' = 'replyMarkupShowKeyboard';
  extra?: string;
  /** rows: vector<vector<keyboardButton>> — A list of rows of bot keyboard buttons */
  rows: KeyboardButton[][] = [];
  /** is_persistent: Bool — True, if the keyboard is expected to always be shown when the ordinary keyboard is hidden */
  is_persistent: boolean = false;
  /** resize_keyboard: Bool — True, if the application needs to resize the keyboard vertically */
  resize_keyboard: boolean = false;
  /** one_time: Bool — True, if the application needs to hide the keyboard after use */
  one_time: boolean = false;
  /** is_personal: Bool — True, if the keyboard must automatically be shown to the current user. For outgoing messages, specify true to show the keyboard only for the mentioned users and for the target user of a reply */
  is_personal: boolean = false;
  /** force_reply: Bool — True, if the keyboard must force reply to the message with the keyboard */
  force_reply: boolean = false;
  /** input_field_placeholder: string — If non-empty, the placeholder to be shown in the input field when the keyboard is active; 0-64 characters */
  input_field_placeholder: string = '';
}

/** Decode a TDLib `replyMarkupShowKeyboard` object; null input -> null. */
export function decodeReplyMarkupShowKeyboard(json: TdJsonRaw): ReplyMarkupShowKeyboard | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReplyMarkupShowKeyboard = new ReplyMarkupShowKeyboard();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.rows = decodeVector(record['rows'], (items: TdJsonRaw): (KeyboardButton)[] => decodeVector(items, (item: TdJsonRaw): KeyboardButton => decodeKeyboardButton(item) as KeyboardButton));
  out.is_persistent = asBool(record['is_persistent']);
  out.resize_keyboard = asBool(record['resize_keyboard']);
  out.one_time = asBool(record['one_time']);
  out.is_personal = asBool(record['is_personal']);
  out.force_reply = asBool(record['force_reply']);
  out.input_field_placeholder = asString(record['input_field_placeholder']);
  return out;
}

/** Encode a `replyMarkupShowKeyboard` into TDLib JSON wire shape. */
export function encodeReplyMarkupShowKeyboard(value: ReplyMarkupShowKeyboard): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'replyMarkupShowKeyboard';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['rows'] = encodeVector(value.rows, (items: (KeyboardButton | null)[]): TdJsonRaw[] => encodeVector(items, (item: KeyboardButton | null): TdJsonRaw => item === null ? null : encodeKeyboardButton(item)));
  out['is_persistent'] = value.is_persistent;
  out['resize_keyboard'] = value.resize_keyboard;
  out['one_time'] = value.one_time;
  out['is_personal'] = value.is_personal;
  out['force_reply'] = value.force_reply;
  out['input_field_placeholder'] = value.input_field_placeholder;
  return out;
}

/** reportAuthenticationCodeMissing = Ok — Reports that authentication code wasn't delivered via SMS; for official mobile applications only. Works only when the current authorization state is authorizationStateWaitCode */
export class ReportAuthenticationCodeMissing {
  type: 'reportAuthenticationCodeMissing' = 'reportAuthenticationCodeMissing';
  extra?: string;
  /** mobile_network_code: string — Current mobile network code */
  mobile_network_code: string = '';
}

/** Decode a TDLib `reportAuthenticationCodeMissing` object; null input -> null. */
export function decodeReportAuthenticationCodeMissing(json: TdJsonRaw): ReportAuthenticationCodeMissing | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportAuthenticationCodeMissing = new ReportAuthenticationCodeMissing();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.mobile_network_code = asString(record['mobile_network_code']);
  return out;
}

/** Encode a `reportAuthenticationCodeMissing` into TDLib JSON wire shape. */
export function encodeReportAuthenticationCodeMissing(value: ReportAuthenticationCodeMissing): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportAuthenticationCodeMissing';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['mobile_network_code'] = value.mobile_network_code;
  return out;
}

/** reportChat = ReportChatResult — Reports a chat to the Telegram moderators. A chat can be reported only from the chat action bar, or if chat.can_be_reported */
export class ReportChat {
  type: 'reportChat' = 'reportChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** option_id: bytes — Option identifier chosen by the user; leave empty for the initial request */
  option_id: string = '';
  /** message_ids: vector<int53> — Identifiers of reported messages. Use messageProperties.can_report_chat to check whether the message can be reported */
  message_ids: number[] = [];
  /** text: string — Additional report details if asked by the server; 0-1024 characters; leave empty for the initial request */
  text: string = '';
}

/** Decode a TDLib `reportChat` object; null input -> null. */
export function decodeReportChat(json: TdJsonRaw): ReportChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportChat = new ReportChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.option_id = asString(record['option_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.text = asString(record['text']);
  return out;
}

/** Encode a `reportChat` into TDLib JSON wire shape. */
export function encodeReportChat(value: ReportChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['option_id'] = value.option_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  out['text'] = value.text;
  return out;
}

/** reportChatPhoto = Ok — Reports a chat photo to the Telegram moderators. A chat photo can be reported only if chat.can_be_reported */
export class ReportChatPhoto {
  type: 'reportChatPhoto' = 'reportChatPhoto';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** file_id: int32 — Identifier of the photo to report. Only full photos from chatPhoto can be reported */
  file_id: number = 0;
  /** reason: ReportReason — The reason for reporting the chat photo */
  reason: TdReportReason | null = null;
  /** text: string — Additional report details; 0-1024 characters */
  text: string = '';
}

/** Decode a TDLib `reportChatPhoto` object; null input -> null. */
export function decodeReportChatPhoto(json: TdJsonRaw): ReportChatPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportChatPhoto = new ReportChatPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.file_id = asNumber(record['file_id']);
  out.reason = decodeTdReportReason(record['reason']);
  out.text = asString(record['text']);
  return out;
}

/** Encode a `reportChatPhoto` into TDLib JSON wire shape. */
export function encodeReportChatPhoto(value: ReportChatPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportChatPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['file_id'] = value.file_id;
  out['reason'] = value.reason === null ? null : encodeTdReportReason(value.reason);
  out['text'] = value.text;
  return out;
}

/** reportChatResultMessagesRequired = ReportChatResult — The user must choose messages to report and repeat the reportChat request with the chosen messages */
export class ReportChatResultMessagesRequired {
  type: 'reportChatResultMessagesRequired' = 'reportChatResultMessagesRequired';
  extra?: string;
}

/** Decode a TDLib `reportChatResultMessagesRequired` object; null input -> null. */
export function decodeReportChatResultMessagesRequired(json: TdJsonRaw): ReportChatResultMessagesRequired | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportChatResultMessagesRequired = new ReportChatResultMessagesRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportChatResultMessagesRequired` into TDLib JSON wire shape. */
export function encodeReportChatResultMessagesRequired(value: ReportChatResultMessagesRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportChatResultMessagesRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportChatResultOk = ReportChatResult — The chat was reported successfully */
export class ReportChatResultOk {
  type: 'reportChatResultOk' = 'reportChatResultOk';
  extra?: string;
}

/** Decode a TDLib `reportChatResultOk` object; null input -> null. */
export function decodeReportChatResultOk(json: TdJsonRaw): ReportChatResultOk | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportChatResultOk = new ReportChatResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportChatResultOk` into TDLib JSON wire shape. */
export function encodeReportChatResultOk(value: ReportChatResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportChatResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportChatResultOptionRequired = ReportChatResult — The user must choose an option to report the chat and repeat request with the chosen option */
export class ReportChatResultOptionRequired {
  type: 'reportChatResultOptionRequired' = 'reportChatResultOptionRequired';
  extra?: string;
  /** title: string — Title for the option choice */
  title: string = '';
  /** options: vector<reportOption> — List of available options */
  options: ReportOption[] = [];
}

/** Decode a TDLib `reportChatResultOptionRequired` object; null input -> null. */
export function decodeReportChatResultOptionRequired(json: TdJsonRaw): ReportChatResultOptionRequired | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportChatResultOptionRequired = new ReportChatResultOptionRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.options = decodeVector(record['options'], (item: TdJsonRaw): ReportOption => decodeReportOption(item) as ReportOption);
  return out;
}

/** Encode a `reportChatResultOptionRequired` into TDLib JSON wire shape. */
export function encodeReportChatResultOptionRequired(value: ReportChatResultOptionRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportChatResultOptionRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['options'] = encodeVector(value.options, (item: ReportOption | null): TdJsonRaw => item === null ? null : encodeReportOption(item));
  return out;
}

/** reportChatResultTextRequired = ReportChatResult — The user must add additional text details to the report */
export class ReportChatResultTextRequired {
  type: 'reportChatResultTextRequired' = 'reportChatResultTextRequired';
  extra?: string;
  /** option_id: bytes — Option identifier for the next reportChat request */
  option_id: string = '';
  /** is_optional: Bool — True, if the user can skip text adding */
  is_optional: boolean = false;
}

/** Decode a TDLib `reportChatResultTextRequired` object; null input -> null. */
export function decodeReportChatResultTextRequired(json: TdJsonRaw): ReportChatResultTextRequired | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportChatResultTextRequired = new ReportChatResultTextRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.option_id = asString(record['option_id']);
  out.is_optional = asBool(record['is_optional']);
  return out;
}

/** Encode a `reportChatResultTextRequired` into TDLib JSON wire shape. */
export function encodeReportChatResultTextRequired(value: ReportChatResultTextRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportChatResultTextRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['option_id'] = value.option_id;
  out['is_optional'] = value.is_optional;
  return out;
}

/** reportChatSponsoredMessage = ReportSponsoredResult — Reports a sponsored message to Telegram moderators */
export class ReportChatSponsoredMessage {
  type: 'reportChatSponsoredMessage' = 'reportChatSponsoredMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the sponsored message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the sponsored message */
  message_id: number = 0;
  /** option_id: bytes — Option identifier chosen by the user; leave empty for the initial request */
  option_id: string = '';
}

/** Decode a TDLib `reportChatSponsoredMessage` object; null input -> null. */
export function decodeReportChatSponsoredMessage(json: TdJsonRaw): ReportChatSponsoredMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportChatSponsoredMessage = new ReportChatSponsoredMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.option_id = asString(record['option_id']);
  return out;
}

/** Encode a `reportChatSponsoredMessage` into TDLib JSON wire shape. */
export function encodeReportChatSponsoredMessage(value: ReportChatSponsoredMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportChatSponsoredMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['option_id'] = value.option_id;
  return out;
}

/** reportMessageReactions = Ok — Reports reactions set on a message to the Telegram moderators. Reactions on a message can be reported only if messageProperties.can_report_reactions */
export class ReportMessageReactions {
  type: 'reportMessageReactions' = 'reportMessageReactions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender, which added the reaction */
  sender_id: TdMessageSender | null = null;
}

/** Decode a TDLib `reportMessageReactions` object; null input -> null. */
export function decodeReportMessageReactions(json: TdJsonRaw): ReportMessageReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportMessageReactions = new ReportMessageReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  return out;
}

/** Encode a `reportMessageReactions` into TDLib JSON wire shape. */
export function encodeReportMessageReactions(value: ReportMessageReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportMessageReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  return out;
}

/** reportOption = ReportOption — Describes an option to report an entity to Telegram */
export class ReportOption {
  type: 'reportOption' = 'reportOption';
  extra?: string;
  /** id: bytes — Unique identifier of the option */
  id: string = '';
  /** text: string — Text of the option */
  text: string = '';
}

/** Decode a TDLib `reportOption` object; null input -> null. */
export function decodeReportOption(json: TdJsonRaw): ReportOption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportOption = new ReportOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.text = asString(record['text']);
  return out;
}

/** Encode a `reportOption` into TDLib JSON wire shape. */
export function encodeReportOption(value: ReportOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['text'] = value.text;
  return out;
}

/** reportPhoneNumberCodeMissing = Ok — Reports that authentication code wasn't delivered via SMS to the specified phone number; for official mobile applications only */
export class ReportPhoneNumberCodeMissing {
  type: 'reportPhoneNumberCodeMissing' = 'reportPhoneNumberCodeMissing';
  extra?: string;
  /** mobile_network_code: string — Current mobile network code */
  mobile_network_code: string = '';
}

/** Decode a TDLib `reportPhoneNumberCodeMissing` object; null input -> null. */
export function decodeReportPhoneNumberCodeMissing(json: TdJsonRaw): ReportPhoneNumberCodeMissing | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportPhoneNumberCodeMissing = new ReportPhoneNumberCodeMissing();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.mobile_network_code = asString(record['mobile_network_code']);
  return out;
}

/** Encode a `reportPhoneNumberCodeMissing` into TDLib JSON wire shape. */
export function encodeReportPhoneNumberCodeMissing(value: ReportPhoneNumberCodeMissing): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportPhoneNumberCodeMissing';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['mobile_network_code'] = value.mobile_network_code;
  return out;
}

/** reportReasonChildAbuse = ReportReason — The chat has child abuse related content */
export class ReportReasonChildAbuse {
  type: 'reportReasonChildAbuse' = 'reportReasonChildAbuse';
  extra?: string;
}

/** Decode a TDLib `reportReasonChildAbuse` object; null input -> null. */
export function decodeReportReasonChildAbuse(json: TdJsonRaw): ReportReasonChildAbuse | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonChildAbuse = new ReportReasonChildAbuse();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonChildAbuse` into TDLib JSON wire shape. */
export function encodeReportReasonChildAbuse(value: ReportReasonChildAbuse): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonChildAbuse';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportReasonCopyright = ReportReason — The chat contains copyrighted content */
export class ReportReasonCopyright {
  type: 'reportReasonCopyright' = 'reportReasonCopyright';
  extra?: string;
}

/** Decode a TDLib `reportReasonCopyright` object; null input -> null. */
export function decodeReportReasonCopyright(json: TdJsonRaw): ReportReasonCopyright | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonCopyright = new ReportReasonCopyright();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonCopyright` into TDLib JSON wire shape. */
export function encodeReportReasonCopyright(value: ReportReasonCopyright): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonCopyright';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportReasonCustom = ReportReason — A custom reason provided by the user */
export class ReportReasonCustom {
  type: 'reportReasonCustom' = 'reportReasonCustom';
  extra?: string;
}

/** Decode a TDLib `reportReasonCustom` object; null input -> null. */
export function decodeReportReasonCustom(json: TdJsonRaw): ReportReasonCustom | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonCustom = new ReportReasonCustom();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonCustom` into TDLib JSON wire shape. */
export function encodeReportReasonCustom(value: ReportReasonCustom): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonCustom';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportReasonFake = ReportReason — The chat represents a fake account */
export class ReportReasonFake {
  type: 'reportReasonFake' = 'reportReasonFake';
  extra?: string;
}

/** Decode a TDLib `reportReasonFake` object; null input -> null. */
export function decodeReportReasonFake(json: TdJsonRaw): ReportReasonFake | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonFake = new ReportReasonFake();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonFake` into TDLib JSON wire shape. */
export function encodeReportReasonFake(value: ReportReasonFake): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonFake';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportReasonIllegalDrugs = ReportReason — The chat has illegal drugs related content */
export class ReportReasonIllegalDrugs {
  type: 'reportReasonIllegalDrugs' = 'reportReasonIllegalDrugs';
  extra?: string;
}

/** Decode a TDLib `reportReasonIllegalDrugs` object; null input -> null. */
export function decodeReportReasonIllegalDrugs(json: TdJsonRaw): ReportReasonIllegalDrugs | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonIllegalDrugs = new ReportReasonIllegalDrugs();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonIllegalDrugs` into TDLib JSON wire shape. */
export function encodeReportReasonIllegalDrugs(value: ReportReasonIllegalDrugs): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonIllegalDrugs';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportReasonPersonalDetails = ReportReason — The chat contains messages with personal details */
export class ReportReasonPersonalDetails {
  type: 'reportReasonPersonalDetails' = 'reportReasonPersonalDetails';
  extra?: string;
}

/** Decode a TDLib `reportReasonPersonalDetails` object; null input -> null. */
export function decodeReportReasonPersonalDetails(json: TdJsonRaw): ReportReasonPersonalDetails | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonPersonalDetails = new ReportReasonPersonalDetails();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonPersonalDetails` into TDLib JSON wire shape. */
export function encodeReportReasonPersonalDetails(value: ReportReasonPersonalDetails): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonPersonalDetails';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportReasonPornography = ReportReason — The chat contains pornographic messages */
export class ReportReasonPornography {
  type: 'reportReasonPornography' = 'reportReasonPornography';
  extra?: string;
}

/** Decode a TDLib `reportReasonPornography` object; null input -> null. */
export function decodeReportReasonPornography(json: TdJsonRaw): ReportReasonPornography | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonPornography = new ReportReasonPornography();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonPornography` into TDLib JSON wire shape. */
export function encodeReportReasonPornography(value: ReportReasonPornography): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonPornography';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportReasonSpam = ReportReason — The chat contains spam messages */
export class ReportReasonSpam {
  type: 'reportReasonSpam' = 'reportReasonSpam';
  extra?: string;
}

/** Decode a TDLib `reportReasonSpam` object; null input -> null. */
export function decodeReportReasonSpam(json: TdJsonRaw): ReportReasonSpam | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonSpam = new ReportReasonSpam();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonSpam` into TDLib JSON wire shape. */
export function encodeReportReasonSpam(value: ReportReasonSpam): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonSpam';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportReasonUnrelatedLocation = ReportReason — The location-based chat is unrelated to its stated location */
export class ReportReasonUnrelatedLocation {
  type: 'reportReasonUnrelatedLocation' = 'reportReasonUnrelatedLocation';
  extra?: string;
}

/** Decode a TDLib `reportReasonUnrelatedLocation` object; null input -> null. */
export function decodeReportReasonUnrelatedLocation(json: TdJsonRaw): ReportReasonUnrelatedLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonUnrelatedLocation = new ReportReasonUnrelatedLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonUnrelatedLocation` into TDLib JSON wire shape. */
export function encodeReportReasonUnrelatedLocation(value: ReportReasonUnrelatedLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonUnrelatedLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportReasonViolence = ReportReason — The chat promotes violence */
export class ReportReasonViolence {
  type: 'reportReasonViolence' = 'reportReasonViolence';
  extra?: string;
}

/** Decode a TDLib `reportReasonViolence` object; null input -> null. */
export function decodeReportReasonViolence(json: TdJsonRaw): ReportReasonViolence | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportReasonViolence = new ReportReasonViolence();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportReasonViolence` into TDLib JSON wire shape. */
export function encodeReportReasonViolence(value: ReportReasonViolence): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportReasonViolence';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportSponsoredChat = ReportSponsoredResult — Reports a sponsored chat to Telegram moderators */
export class ReportSponsoredChat {
  type: 'reportSponsoredChat' = 'reportSponsoredChat';
  extra?: string;
  /** sponsored_chat_unique_id: int53 — Unique identifier of the sponsored chat */
  sponsored_chat_unique_id: number = 0;
  /** option_id: bytes — Option identifier chosen by the user; leave empty for the initial request */
  option_id: string = '';
}

/** Decode a TDLib `reportSponsoredChat` object; null input -> null. */
export function decodeReportSponsoredChat(json: TdJsonRaw): ReportSponsoredChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportSponsoredChat = new ReportSponsoredChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sponsored_chat_unique_id = asNumber(record['sponsored_chat_unique_id']);
  out.option_id = asString(record['option_id']);
  return out;
}

/** Encode a `reportSponsoredChat` into TDLib JSON wire shape. */
export function encodeReportSponsoredChat(value: ReportSponsoredChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportSponsoredChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sponsored_chat_unique_id'] = value.sponsored_chat_unique_id;
  out['option_id'] = value.option_id;
  return out;
}

/** reportSponsoredResultAdsHidden = ReportSponsoredResult — Sponsored messages were hidden for the user in all chats */
export class ReportSponsoredResultAdsHidden {
  type: 'reportSponsoredResultAdsHidden' = 'reportSponsoredResultAdsHidden';
  extra?: string;
}

/** Decode a TDLib `reportSponsoredResultAdsHidden` object; null input -> null. */
export function decodeReportSponsoredResultAdsHidden(json: TdJsonRaw): ReportSponsoredResultAdsHidden | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportSponsoredResultAdsHidden = new ReportSponsoredResultAdsHidden();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportSponsoredResultAdsHidden` into TDLib JSON wire shape. */
export function encodeReportSponsoredResultAdsHidden(value: ReportSponsoredResultAdsHidden): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportSponsoredResultAdsHidden';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportSponsoredResultFailed = ReportSponsoredResult — The sponsored message is too old or not found */
export class ReportSponsoredResultFailed {
  type: 'reportSponsoredResultFailed' = 'reportSponsoredResultFailed';
  extra?: string;
}

/** Decode a TDLib `reportSponsoredResultFailed` object; null input -> null. */
export function decodeReportSponsoredResultFailed(json: TdJsonRaw): ReportSponsoredResultFailed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportSponsoredResultFailed = new ReportSponsoredResultFailed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportSponsoredResultFailed` into TDLib JSON wire shape. */
export function encodeReportSponsoredResultFailed(value: ReportSponsoredResultFailed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportSponsoredResultFailed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportSponsoredResultOk = ReportSponsoredResult — The message was reported successfully */
export class ReportSponsoredResultOk {
  type: 'reportSponsoredResultOk' = 'reportSponsoredResultOk';
  extra?: string;
}

/** Decode a TDLib `reportSponsoredResultOk` object; null input -> null. */
export function decodeReportSponsoredResultOk(json: TdJsonRaw): ReportSponsoredResultOk | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportSponsoredResultOk = new ReportSponsoredResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportSponsoredResultOk` into TDLib JSON wire shape. */
export function encodeReportSponsoredResultOk(value: ReportSponsoredResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportSponsoredResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportSponsoredResultOptionRequired = ReportSponsoredResult — The user must choose an option to report the message and repeat request with the chosen option */
export class ReportSponsoredResultOptionRequired {
  type: 'reportSponsoredResultOptionRequired' = 'reportSponsoredResultOptionRequired';
  extra?: string;
  /** title: string — Title for the option choice */
  title: string = '';
  /** options: vector<reportOption> — List of available options */
  options: ReportOption[] = [];
}

/** Decode a TDLib `reportSponsoredResultOptionRequired` object; null input -> null. */
export function decodeReportSponsoredResultOptionRequired(json: TdJsonRaw): ReportSponsoredResultOptionRequired | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportSponsoredResultOptionRequired = new ReportSponsoredResultOptionRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.options = decodeVector(record['options'], (item: TdJsonRaw): ReportOption => decodeReportOption(item) as ReportOption);
  return out;
}

/** Encode a `reportSponsoredResultOptionRequired` into TDLib JSON wire shape. */
export function encodeReportSponsoredResultOptionRequired(value: ReportSponsoredResultOptionRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportSponsoredResultOptionRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['options'] = encodeVector(value.options, (item: ReportOption | null): TdJsonRaw => item === null ? null : encodeReportOption(item));
  return out;
}

/** reportSponsoredResultPremiumRequired = ReportSponsoredResult — The user asked to hide sponsored messages, but Telegram Premium is required for this */
export class ReportSponsoredResultPremiumRequired {
  type: 'reportSponsoredResultPremiumRequired' = 'reportSponsoredResultPremiumRequired';
  extra?: string;
}

/** Decode a TDLib `reportSponsoredResultPremiumRequired` object; null input -> null. */
export function decodeReportSponsoredResultPremiumRequired(json: TdJsonRaw): ReportSponsoredResultPremiumRequired | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportSponsoredResultPremiumRequired = new ReportSponsoredResultPremiumRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportSponsoredResultPremiumRequired` into TDLib JSON wire shape. */
export function encodeReportSponsoredResultPremiumRequired(value: ReportSponsoredResultPremiumRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportSponsoredResultPremiumRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportStory = ReportStoryResult — Reports a story to the Telegram moderators */
export class ReportStory {
  type: 'reportStory' = 'reportStory';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the poster of the story to report */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — The identifier of the story to report */
  story_id: number = 0;
  /** option_id: bytes — Option identifier chosen by the user; leave empty for the initial request */
  option_id: string = '';
  /** text: string — Additional report details; 0-1024 characters; leave empty for the initial request */
  text: string = '';
}

/** Decode a TDLib `reportStory` object; null input -> null. */
export function decodeReportStory(json: TdJsonRaw): ReportStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportStory = new ReportStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.option_id = asString(record['option_id']);
  out.text = asString(record['text']);
  return out;
}

/** Encode a `reportStory` into TDLib JSON wire shape. */
export function encodeReportStory(value: ReportStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['option_id'] = value.option_id;
  out['text'] = value.text;
  return out;
}

/** reportStoryResultOk = ReportStoryResult — The story was reported successfully */
export class ReportStoryResultOk {
  type: 'reportStoryResultOk' = 'reportStoryResultOk';
  extra?: string;
}

/** Decode a TDLib `reportStoryResultOk` object; null input -> null. */
export function decodeReportStoryResultOk(json: TdJsonRaw): ReportStoryResultOk | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportStoryResultOk = new ReportStoryResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `reportStoryResultOk` into TDLib JSON wire shape. */
export function encodeReportStoryResultOk(value: ReportStoryResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportStoryResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** reportStoryResultOptionRequired = ReportStoryResult — The user must choose an option to report the story and repeat request with the chosen option */
export class ReportStoryResultOptionRequired {
  type: 'reportStoryResultOptionRequired' = 'reportStoryResultOptionRequired';
  extra?: string;
  /** title: string — Title for the option choice */
  title: string = '';
  /** options: vector<reportOption> — List of available options */
  options: ReportOption[] = [];
}

/** Decode a TDLib `reportStoryResultOptionRequired` object; null input -> null. */
export function decodeReportStoryResultOptionRequired(json: TdJsonRaw): ReportStoryResultOptionRequired | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportStoryResultOptionRequired = new ReportStoryResultOptionRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.options = decodeVector(record['options'], (item: TdJsonRaw): ReportOption => decodeReportOption(item) as ReportOption);
  return out;
}

/** Encode a `reportStoryResultOptionRequired` into TDLib JSON wire shape. */
export function encodeReportStoryResultOptionRequired(value: ReportStoryResultOptionRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportStoryResultOptionRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['options'] = encodeVector(value.options, (item: ReportOption | null): TdJsonRaw => item === null ? null : encodeReportOption(item));
  return out;
}

/** reportStoryResultTextRequired = ReportStoryResult — The user must add additional text details to the report */
export class ReportStoryResultTextRequired {
  type: 'reportStoryResultTextRequired' = 'reportStoryResultTextRequired';
  extra?: string;
  /** option_id: bytes — Option identifier for the next reportStory request */
  option_id: string = '';
  /** is_optional: Bool — True, if the user can skip text adding */
  is_optional: boolean = false;
}

/** Decode a TDLib `reportStoryResultTextRequired` object; null input -> null. */
export function decodeReportStoryResultTextRequired(json: TdJsonRaw): ReportStoryResultTextRequired | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportStoryResultTextRequired = new ReportStoryResultTextRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.option_id = asString(record['option_id']);
  out.is_optional = asBool(record['is_optional']);
  return out;
}

/** Encode a `reportStoryResultTextRequired` into TDLib JSON wire shape. */
export function encodeReportStoryResultTextRequired(value: ReportStoryResultTextRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportStoryResultTextRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['option_id'] = value.option_id;
  out['is_optional'] = value.is_optional;
  return out;
}

/** reportSupergroupAntiSpamFalsePositive = Ok — Reports a false deletion of a message by aggressive anti-spam checks; requires administrator rights in the supergroup. Can be called only for messages from chatEventMessageDeleted with can_rep... */
export class ReportSupergroupAntiSpamFalsePositive {
  type: 'reportSupergroupAntiSpamFalsePositive' = 'reportSupergroupAntiSpamFalsePositive';
  extra?: string;
  /** supergroup_id: int53 — Supergroup identifier */
  supergroup_id: number = 0;
  /** message_id: int53 — Identifier of the erroneously deleted message from chatEventMessageDeleted */
  message_id: number = 0;
}

/** Decode a TDLib `reportSupergroupAntiSpamFalsePositive` object; null input -> null. */
export function decodeReportSupergroupAntiSpamFalsePositive(json: TdJsonRaw): ReportSupergroupAntiSpamFalsePositive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportSupergroupAntiSpamFalsePositive = new ReportSupergroupAntiSpamFalsePositive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `reportSupergroupAntiSpamFalsePositive` into TDLib JSON wire shape. */
export function encodeReportSupergroupAntiSpamFalsePositive(value: ReportSupergroupAntiSpamFalsePositive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportSupergroupAntiSpamFalsePositive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['message_id'] = value.message_id;
  return out;
}

/** reportSupergroupSpam = Ok — Reports messages in a supergroup as spam; requires administrator rights in the supergroup */
export class ReportSupergroupSpam {
  type: 'reportSupergroupSpam' = 'reportSupergroupSpam';
  extra?: string;
  /** supergroup_id: int53 — Supergroup identifier */
  supergroup_id: number = 0;
  /** message_ids: vector<int53> — Identifiers of messages to report. Use messageProperties.can_report_supergroup_spam to check whether the message can be reported */
  message_ids: number[] = [];
}

/** Decode a TDLib `reportSupergroupSpam` object; null input -> null. */
export function decodeReportSupergroupSpam(json: TdJsonRaw): ReportSupergroupSpam | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportSupergroupSpam = new ReportSupergroupSpam();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `reportSupergroupSpam` into TDLib JSON wire shape. */
export function encodeReportSupergroupSpam(value: ReportSupergroupSpam): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportSupergroupSpam';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** reportVideoMessageAdvertisement = ReportSponsoredResult — Reports a video message advertisement to Telegram moderators */
export class ReportVideoMessageAdvertisement {
  type: 'reportVideoMessageAdvertisement' = 'reportVideoMessageAdvertisement';
  extra?: string;
  /** advertisement_unique_id: int53 — Unique identifier of the advertisement */
  advertisement_unique_id: number = 0;
  /** option_id: bytes — Option identifier chosen by the user; leave empty for the initial request */
  option_id: string = '';
}

/** Decode a TDLib `reportVideoMessageAdvertisement` object; null input -> null. */
export function decodeReportVideoMessageAdvertisement(json: TdJsonRaw): ReportVideoMessageAdvertisement | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReportVideoMessageAdvertisement = new ReportVideoMessageAdvertisement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.advertisement_unique_id = asNumber(record['advertisement_unique_id']);
  out.option_id = asString(record['option_id']);
  return out;
}

/** Encode a `reportVideoMessageAdvertisement` into TDLib JSON wire shape. */
export function encodeReportVideoMessageAdvertisement(value: ReportVideoMessageAdvertisement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reportVideoMessageAdvertisement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['advertisement_unique_id'] = value.advertisement_unique_id;
  out['option_id'] = value.option_id;
  return out;
}

/** requestAuthenticationPasswordRecovery = Ok — Requests to send a 2-step verification password recovery code to an email address that was previously set up. Works only when the current authorization state is authorizationStateWaitPassword */
export class RequestAuthenticationPasswordRecovery {
  type: 'requestAuthenticationPasswordRecovery' = 'requestAuthenticationPasswordRecovery';
  extra?: string;
}

/** Decode a TDLib `requestAuthenticationPasswordRecovery` object; null input -> null. */
export function decodeRequestAuthenticationPasswordRecovery(json: TdJsonRaw): RequestAuthenticationPasswordRecovery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RequestAuthenticationPasswordRecovery = new RequestAuthenticationPasswordRecovery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `requestAuthenticationPasswordRecovery` into TDLib JSON wire shape. */
export function encodeRequestAuthenticationPasswordRecovery(value: RequestAuthenticationPasswordRecovery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'requestAuthenticationPasswordRecovery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** requestPasswordRecovery = EmailAddressAuthenticationCodeInfo — Requests to send a 2-step verification password recovery code to an email address that was previously set up */
export class RequestPasswordRecovery {
  type: 'requestPasswordRecovery' = 'requestPasswordRecovery';
  extra?: string;
}

/** Decode a TDLib `requestPasswordRecovery` object; null input -> null. */
export function decodeRequestPasswordRecovery(json: TdJsonRaw): RequestPasswordRecovery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RequestPasswordRecovery = new RequestPasswordRecovery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `requestPasswordRecovery` into TDLib JSON wire shape. */
export function encodeRequestPasswordRecovery(value: RequestPasswordRecovery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'requestPasswordRecovery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** requestQrCodeAuthentication = Ok — Requests QR code authentication by scanning a QR code on another logged in device. Works only when the current authorization state is authorizationStateWaitPhoneNumber, or if there is no pending authent... */
export class RequestQrCodeAuthentication {
  type: 'requestQrCodeAuthentication' = 'requestQrCodeAuthentication';
  extra?: string;
  /** other_user_ids: vector<int53> — List of user identifiers of other users currently using the application */
  other_user_ids: number[] = [];
}

/** Decode a TDLib `requestQrCodeAuthentication` object; null input -> null. */
export function decodeRequestQrCodeAuthentication(json: TdJsonRaw): RequestQrCodeAuthentication | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RequestQrCodeAuthentication = new RequestQrCodeAuthentication();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.other_user_ids = decodeVector(record['other_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `requestQrCodeAuthentication` into TDLib JSON wire shape. */
export function encodeRequestQrCodeAuthentication(value: RequestQrCodeAuthentication): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'requestQrCodeAuthentication';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['other_user_ids'] = encodeVector(value.other_user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** resendAuthenticationCode = Ok — Resends an authentication code to the user. Works only when the current authorization state is authorizationStateWaitCode, the next_code_type of the result is not null and the server-specified timeout has ... */
export class ResendAuthenticationCode {
  type: 'resendAuthenticationCode' = 'resendAuthenticationCode';
  extra?: string;
  /** reason: ResendCodeReason — Reason of code resending; pass null if unknown */
  reason: TdResendCodeReason | null = null;
}

/** Decode a TDLib `resendAuthenticationCode` object; null input -> null. */
export function decodeResendAuthenticationCode(json: TdJsonRaw): ResendAuthenticationCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResendAuthenticationCode = new ResendAuthenticationCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reason = decodeTdResendCodeReason(record['reason']);
  return out;
}

/** Encode a `resendAuthenticationCode` into TDLib JSON wire shape. */
export function encodeResendAuthenticationCode(value: ResendAuthenticationCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resendAuthenticationCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reason'] = value.reason === null ? null : encodeTdResendCodeReason(value.reason);
  return out;
}

/** resendCodeReasonUserRequest = ResendCodeReason — The user requested to resend the code */
export class ResendCodeReasonUserRequest {
  type: 'resendCodeReasonUserRequest' = 'resendCodeReasonUserRequest';
  extra?: string;
}

/** Decode a TDLib `resendCodeReasonUserRequest` object; null input -> null. */
export function decodeResendCodeReasonUserRequest(json: TdJsonRaw): ResendCodeReasonUserRequest | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResendCodeReasonUserRequest = new ResendCodeReasonUserRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resendCodeReasonUserRequest` into TDLib JSON wire shape. */
export function encodeResendCodeReasonUserRequest(value: ResendCodeReasonUserRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resendCodeReasonUserRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resendCodeReasonVerificationFailed = ResendCodeReason — The code is re-sent, because device verification has failed */
export class ResendCodeReasonVerificationFailed {
  type: 'resendCodeReasonVerificationFailed' = 'resendCodeReasonVerificationFailed';
  extra?: string;
  /** error_message: string — Cause of the verification failure, for example, "PLAY_SERVICES_NOT_AVAILABLE", "APNS_RECEIVE_TIMEOUT", or "APNS_INIT_FAILED" */
  error_message: string = '';
}

/** Decode a TDLib `resendCodeReasonVerificationFailed` object; null input -> null. */
export function decodeResendCodeReasonVerificationFailed(json: TdJsonRaw): ResendCodeReasonVerificationFailed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResendCodeReasonVerificationFailed = new ResendCodeReasonVerificationFailed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.error_message = asString(record['error_message']);
  return out;
}

/** Encode a `resendCodeReasonVerificationFailed` into TDLib JSON wire shape. */
export function encodeResendCodeReasonVerificationFailed(value: ResendCodeReasonVerificationFailed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resendCodeReasonVerificationFailed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['error_message'] = value.error_message;
  return out;
}

/** resendEmailAddressVerificationCode = EmailAddressAuthenticationCodeInfo — Resends the code to verify an email address to be added to a user's Telegram Passport */
export class ResendEmailAddressVerificationCode {
  type: 'resendEmailAddressVerificationCode' = 'resendEmailAddressVerificationCode';
  extra?: string;
}

/** Decode a TDLib `resendEmailAddressVerificationCode` object; null input -> null. */
export function decodeResendEmailAddressVerificationCode(json: TdJsonRaw): ResendEmailAddressVerificationCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResendEmailAddressVerificationCode = new ResendEmailAddressVerificationCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resendEmailAddressVerificationCode` into TDLib JSON wire shape. */
export function encodeResendEmailAddressVerificationCode(value: ResendEmailAddressVerificationCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resendEmailAddressVerificationCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resendLoginEmailAddressCode = EmailAddressAuthenticationCodeInfo — Resends the login email address verification code */
export class ResendLoginEmailAddressCode {
  type: 'resendLoginEmailAddressCode' = 'resendLoginEmailAddressCode';
  extra?: string;
}

/** Decode a TDLib `resendLoginEmailAddressCode` object; null input -> null. */
export function decodeResendLoginEmailAddressCode(json: TdJsonRaw): ResendLoginEmailAddressCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResendLoginEmailAddressCode = new ResendLoginEmailAddressCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resendLoginEmailAddressCode` into TDLib JSON wire shape. */
export function encodeResendLoginEmailAddressCode(value: ResendLoginEmailAddressCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resendLoginEmailAddressCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resendMessages = Messages — Resends messages which failed to send. Can be called only for messages for which messageSendingStateFailed.can_retry is true and after specified in messageSendingStateFailed.retry_after time passed. If a messa... */
export class ResendMessages {
  type: 'resendMessages' = 'resendMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to send messages */
  chat_id: number = 0;
  /** message_ids: vector<int53> — Identifiers of the messages to resend. Message identifiers must be in a strictly increasing order */
  message_ids: number[] = [];
  /** quote: inputTextQuote — New manually chosen quote from the message to be replied; pass null if none. Ignored if more than one message is re-sent, or if messageSendingStateFailed.need_another_reply_quote == false */
  quote: InputTextQuote | null = null;
  /** paid_message_star_count: int53 — The number of Telegram Stars the user agreed to pay to send the messages. Ignored if messageSendingStateFailed.required_paid_message_star_count == 0 */
  paid_message_star_count: number = 0;
}

/** Decode a TDLib `resendMessages` object; null input -> null. */
export function decodeResendMessages(json: TdJsonRaw): ResendMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResendMessages = new ResendMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.quote = decodeInputTextQuote(record['quote']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  return out;
}

/** Encode a `resendMessages` into TDLib JSON wire shape. */
export function encodeResendMessages(value: ResendMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resendMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  out['quote'] = value.quote === null ? null : encodeInputTextQuote(value.quote);
  out['paid_message_star_count'] = value.paid_message_star_count;
  return out;
}

/** resendPhoneNumberCode = AuthenticationCodeInfo — Resends the authentication code sent to a phone number. Works only if the previously received authenticationCodeInfo next_code_type was not null and the server-specified timeout has passed */
export class ResendPhoneNumberCode {
  type: 'resendPhoneNumberCode' = 'resendPhoneNumberCode';
  extra?: string;
  /** reason: ResendCodeReason — Reason of code resending; pass null if unknown */
  reason: TdResendCodeReason | null = null;
}

/** Decode a TDLib `resendPhoneNumberCode` object; null input -> null. */
export function decodeResendPhoneNumberCode(json: TdJsonRaw): ResendPhoneNumberCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResendPhoneNumberCode = new ResendPhoneNumberCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reason = decodeTdResendCodeReason(record['reason']);
  return out;
}

/** Encode a `resendPhoneNumberCode` into TDLib JSON wire shape. */
export function encodeResendPhoneNumberCode(value: ResendPhoneNumberCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resendPhoneNumberCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reason'] = value.reason === null ? null : encodeTdResendCodeReason(value.reason);
  return out;
}

/** resendRecoveryEmailAddressCode = PasswordState — Resends the 2-step verification recovery email address verification code */
export class ResendRecoveryEmailAddressCode {
  type: 'resendRecoveryEmailAddressCode' = 'resendRecoveryEmailAddressCode';
  extra?: string;
}

/** Decode a TDLib `resendRecoveryEmailAddressCode` object; null input -> null. */
export function decodeResendRecoveryEmailAddressCode(json: TdJsonRaw): ResendRecoveryEmailAddressCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResendRecoveryEmailAddressCode = new ResendRecoveryEmailAddressCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resendRecoveryEmailAddressCode` into TDLib JSON wire shape. */
export function encodeResendRecoveryEmailAddressCode(value: ResendRecoveryEmailAddressCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resendRecoveryEmailAddressCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resetAllNotificationSettings = Ok — Resets all chat and scope notification settings to their default values. By default, all chats are unmuted and message previews are shown */
export class ResetAllNotificationSettings {
  type: 'resetAllNotificationSettings' = 'resetAllNotificationSettings';
  extra?: string;
}

/** Decode a TDLib `resetAllNotificationSettings` object; null input -> null. */
export function decodeResetAllNotificationSettings(json: TdJsonRaw): ResetAllNotificationSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResetAllNotificationSettings = new ResetAllNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resetAllNotificationSettings` into TDLib JSON wire shape. */
export function encodeResetAllNotificationSettings(value: ResetAllNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resetAllNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resetAuthenticationEmailAddress = Ok — Resets the login email address. May return an error with a message "TASK_ALREADY_EXISTS" if reset is still pending. Works only when the current authorization state is authorizationStateWaitEmailCode... */
export class ResetAuthenticationEmailAddress {
  type: 'resetAuthenticationEmailAddress' = 'resetAuthenticationEmailAddress';
  extra?: string;
}

/** Decode a TDLib `resetAuthenticationEmailAddress` object; null input -> null. */
export function decodeResetAuthenticationEmailAddress(json: TdJsonRaw): ResetAuthenticationEmailAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResetAuthenticationEmailAddress = new ResetAuthenticationEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resetAuthenticationEmailAddress` into TDLib JSON wire shape. */
export function encodeResetAuthenticationEmailAddress(value: ResetAuthenticationEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resetAuthenticationEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resetInstalledBackgrounds = Ok — Resets list of installed backgrounds to its default value */
export class ResetInstalledBackgrounds {
  type: 'resetInstalledBackgrounds' = 'resetInstalledBackgrounds';
  extra?: string;
}

/** Decode a TDLib `resetInstalledBackgrounds` object; null input -> null. */
export function decodeResetInstalledBackgrounds(json: TdJsonRaw): ResetInstalledBackgrounds | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResetInstalledBackgrounds = new ResetInstalledBackgrounds();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resetInstalledBackgrounds` into TDLib JSON wire shape. */
export function encodeResetInstalledBackgrounds(value: ResetInstalledBackgrounds): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resetInstalledBackgrounds';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resetNetworkStatistics = Ok — Resets all network data usage statistics to zero. Can be called before authorization */
export class ResetNetworkStatistics {
  type: 'resetNetworkStatistics' = 'resetNetworkStatistics';
  extra?: string;
}

/** Decode a TDLib `resetNetworkStatistics` object; null input -> null. */
export function decodeResetNetworkStatistics(json: TdJsonRaw): ResetNetworkStatistics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResetNetworkStatistics = new ResetNetworkStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resetNetworkStatistics` into TDLib JSON wire shape. */
export function encodeResetNetworkStatistics(value: ResetNetworkStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resetNetworkStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resetPassword = ResetPasswordResult — Removes 2-step verification password without previous password and access to recovery email address. The password can't be reset immediately and the request needs to be repeated after the specified time */
export class ResetPassword {
  type: 'resetPassword' = 'resetPassword';
  extra?: string;
}

/** Decode a TDLib `resetPassword` object; null input -> null. */
export function decodeResetPassword(json: TdJsonRaw): ResetPassword | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResetPassword = new ResetPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resetPassword` into TDLib JSON wire shape. */
export function encodeResetPassword(value: ResetPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resetPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resetPasswordResultDeclined = ResetPasswordResult — The password reset request was declined */
export class ResetPasswordResultDeclined {
  type: 'resetPasswordResultDeclined' = 'resetPasswordResultDeclined';
  extra?: string;
  /** retry_date: int32 — Point in time (Unix timestamp) when the password reset can be retried */
  retry_date: number = 0;
}

/** Decode a TDLib `resetPasswordResultDeclined` object; null input -> null. */
export function decodeResetPasswordResultDeclined(json: TdJsonRaw): ResetPasswordResultDeclined | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResetPasswordResultDeclined = new ResetPasswordResultDeclined();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.retry_date = asNumber(record['retry_date']);
  return out;
}

/** Encode a `resetPasswordResultDeclined` into TDLib JSON wire shape. */
export function encodeResetPasswordResultDeclined(value: ResetPasswordResultDeclined): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resetPasswordResultDeclined';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['retry_date'] = value.retry_date;
  return out;
}

/** resetPasswordResultOk = ResetPasswordResult — The password was reset */
export class ResetPasswordResultOk {
  type: 'resetPasswordResultOk' = 'resetPasswordResultOk';
  extra?: string;
}

/** Decode a TDLib `resetPasswordResultOk` object; null input -> null. */
export function decodeResetPasswordResultOk(json: TdJsonRaw): ResetPasswordResultOk | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResetPasswordResultOk = new ResetPasswordResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `resetPasswordResultOk` into TDLib JSON wire shape. */
export function encodeResetPasswordResultOk(value: ResetPasswordResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resetPasswordResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** resetPasswordResultPending = ResetPasswordResult — The password reset request is pending */
export class ResetPasswordResultPending {
  type: 'resetPasswordResultPending' = 'resetPasswordResultPending';
  extra?: string;
  /** pending_reset_date: int32 — Point in time (Unix timestamp) after which the password can be reset immediately using resetPassword */
  pending_reset_date: number = 0;
}

/** Decode a TDLib `resetPasswordResultPending` object; null input -> null. */
export function decodeResetPasswordResultPending(json: TdJsonRaw): ResetPasswordResultPending | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ResetPasswordResultPending = new ResetPasswordResultPending();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.pending_reset_date = asNumber(record['pending_reset_date']);
  return out;
}

/** Encode a `resetPasswordResultPending` into TDLib JSON wire shape. */
export function encodeResetPasswordResultPending(value: ResetPasswordResultPending): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'resetPasswordResultPending';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['pending_reset_date'] = value.pending_reset_date;
  return out;
}

/** restrictionInfo = RestrictionInfo — Contains information about restrictions that must be applied to a chat or a message but may be covered by hidden with 18+ spoiler anyway Use value of the option "can_ignore_sensitive_content_restrictio... */
export class RestrictionInfo {
  type: 'restrictionInfo' = 'restrictionInfo';
  extra?: string;
  /** restriction_reason: string — A human-readable description of the reason why access to the content must be restricted. If empty, then the content can be accessed, */
  restriction_reason: string = '';
  /** has_sensitive_content: Bool — True, if media content of the messages must be hidden with 18+ spoiler. */
  has_sensitive_content: boolean = false;
}

/** Decode a TDLib `restrictionInfo` object; null input -> null. */
export function decodeRestrictionInfo(json: TdJsonRaw): RestrictionInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RestrictionInfo = new RestrictionInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.restriction_reason = asString(record['restriction_reason']);
  out.has_sensitive_content = asBool(record['has_sensitive_content']);
  return out;
}

/** Encode a `restrictionInfo` into TDLib JSON wire shape. */
export function encodeRestrictionInfo(value: RestrictionInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'restrictionInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['restriction_reason'] = value.restriction_reason;
  out['has_sensitive_content'] = value.has_sensitive_content;
  return out;
}

/** reuseStarSubscription = Ok — Reuses an active Telegram Star subscription to a channel chat and joins the chat again */
export class ReuseStarSubscription {
  type: 'reuseStarSubscription' = 'reuseStarSubscription';
  extra?: string;
  /** subscription_id: string — Identifier of the subscription */
  subscription_id: string = '';
}

/** Decode a TDLib `reuseStarSubscription` object; null input -> null. */
export function decodeReuseStarSubscription(json: TdJsonRaw): ReuseStarSubscription | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ReuseStarSubscription = new ReuseStarSubscription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subscription_id = asString(record['subscription_id']);
  return out;
}

/** Encode a `reuseStarSubscription` into TDLib JSON wire shape. */
export function encodeReuseStarSubscription(value: ReuseStarSubscription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'reuseStarSubscription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subscription_id'] = value.subscription_id;
  return out;
}

/** revenueWithdrawalStateFailed = RevenueWithdrawalState — Withdrawal failed */
export class RevenueWithdrawalStateFailed {
  type: 'revenueWithdrawalStateFailed' = 'revenueWithdrawalStateFailed';
  extra?: string;
}

/** Decode a TDLib `revenueWithdrawalStateFailed` object; null input -> null. */
export function decodeRevenueWithdrawalStateFailed(json: TdJsonRaw): RevenueWithdrawalStateFailed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RevenueWithdrawalStateFailed = new RevenueWithdrawalStateFailed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `revenueWithdrawalStateFailed` into TDLib JSON wire shape. */
export function encodeRevenueWithdrawalStateFailed(value: RevenueWithdrawalStateFailed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'revenueWithdrawalStateFailed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** revenueWithdrawalStatePending = RevenueWithdrawalState — Withdrawal is pending */
export class RevenueWithdrawalStatePending {
  type: 'revenueWithdrawalStatePending' = 'revenueWithdrawalStatePending';
  extra?: string;
}

/** Decode a TDLib `revenueWithdrawalStatePending` object; null input -> null. */
export function decodeRevenueWithdrawalStatePending(json: TdJsonRaw): RevenueWithdrawalStatePending | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RevenueWithdrawalStatePending = new RevenueWithdrawalStatePending();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `revenueWithdrawalStatePending` into TDLib JSON wire shape. */
export function encodeRevenueWithdrawalStatePending(value: RevenueWithdrawalStatePending): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'revenueWithdrawalStatePending';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** revenueWithdrawalStateSucceeded = RevenueWithdrawalState — Withdrawal succeeded */
export class RevenueWithdrawalStateSucceeded {
  type: 'revenueWithdrawalStateSucceeded' = 'revenueWithdrawalStateSucceeded';
  extra?: string;
  /** date: int32 — Point in time (Unix timestamp) when the withdrawal was completed */
  date: number = 0;
  /** url: string — The URL where the withdrawal transaction can be viewed */
  url: string = '';
}

/** Decode a TDLib `revenueWithdrawalStateSucceeded` object; null input -> null. */
export function decodeRevenueWithdrawalStateSucceeded(json: TdJsonRaw): RevenueWithdrawalStateSucceeded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RevenueWithdrawalStateSucceeded = new RevenueWithdrawalStateSucceeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.date = asNumber(record['date']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `revenueWithdrawalStateSucceeded` into TDLib JSON wire shape. */
export function encodeRevenueWithdrawalStateSucceeded(value: RevenueWithdrawalStateSucceeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'revenueWithdrawalStateSucceeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['date'] = value.date;
  out['url'] = value.url;
  return out;
}

/** revokeChatInviteLink = ChatInviteLinks — Revokes invite link for a chat. Available in basic groups, supergroups, and channels. Requires administrator privileges and can_invite_users right in the chat for own links and owner privileges fo... */
export class RevokeChatInviteLink {
  type: 'revokeChatInviteLink' = 'revokeChatInviteLink';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** invite_link: string — Invite link to be revoked */
  invite_link: string = '';
}

/** Decode a TDLib `revokeChatInviteLink` object; null input -> null. */
export function decodeRevokeChatInviteLink(json: TdJsonRaw): RevokeChatInviteLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RevokeChatInviteLink = new RevokeChatInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `revokeChatInviteLink` into TDLib JSON wire shape. */
export function encodeRevokeChatInviteLink(value: RevokeChatInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'revokeChatInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['invite_link'] = value.invite_link;
  return out;
}

/** revokeGroupCallInviteLink = Ok — Revokes invite link for a group call. Requires groupCall.can_be_managed right for video chats or groupCall.is_owned otherwise */
export class RevokeGroupCallInviteLink {
  type: 'revokeGroupCallInviteLink' = 'revokeGroupCallInviteLink';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `revokeGroupCallInviteLink` object; null input -> null. */
export function decodeRevokeGroupCallInviteLink(json: TdJsonRaw): RevokeGroupCallInviteLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RevokeGroupCallInviteLink = new RevokeGroupCallInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `revokeGroupCallInviteLink` into TDLib JSON wire shape. */
export function encodeRevokeGroupCallInviteLink(value: RevokeGroupCallInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'revokeGroupCallInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** richMessage = RichMessage — Describes a message with rich formatting */
export class RichMessage {
  type: 'richMessage' = 'richMessage';
  extra?: string;
  /** blocks: vector<PageBlock> — Content of the message */
  blocks: TdPageBlock[] = [];
  /** is_rtl: Bool — True, if the message must be shown from right to left */
  is_rtl: boolean = false;
  /** is_full: Bool — True, if the object contains the full message. Otherwise, getFullRichMessage must be used to get the full message */
  is_full: boolean = false;
}

/** Decode a TDLib `richMessage` object; null input -> null. */
export function decodeRichMessage(json: TdJsonRaw): RichMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichMessage = new RichMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdPageBlock => decodeTdPageBlock(item) as TdPageBlock);
  out.is_rtl = asBool(record['is_rtl']);
  out.is_full = asBool(record['is_full']);
  return out;
}

/** Encode a `richMessage` into TDLib JSON wire shape. */
export function encodeRichMessage(value: RichMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdPageBlock | null): TdJsonRaw => item === null ? null : encodeTdPageBlock(item));
  out['is_rtl'] = value.is_rtl;
  out['is_full'] = value.is_full;
  return out;
}

/** richMessageSourceBlocks = RichMessageSource — A rich message defined by blocks */
export class RichMessageSourceBlocks {
  type: 'richMessageSourceBlocks' = 'richMessageSourceBlocks';
  extra?: string;
  /** blocks: vector<InputPageBlock> — Content of the message */
  blocks: TdInputPageBlock[] = [];
}

/** Decode a TDLib `richMessageSourceBlocks` object; null input -> null. */
export function decodeRichMessageSourceBlocks(json: TdJsonRaw): RichMessageSourceBlocks | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichMessageSourceBlocks = new RichMessageSourceBlocks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdInputPageBlock => decodeTdInputPageBlock(item) as TdInputPageBlock);
  return out;
}

/** Encode a `richMessageSourceBlocks` into TDLib JSON wire shape. */
export function encodeRichMessageSourceBlocks(value: RichMessageSourceBlocks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richMessageSourceBlocks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdInputPageBlock | null): TdJsonRaw => item === null ? null : encodeTdInputPageBlock(item));
  return out;
}

/** richMessageSourceHtml = RichMessageSource — An HTML-formatted rich message; for bots only */
export class RichMessageSourceHtml {
  type: 'richMessageSourceHtml' = 'richMessageSourceHtml';
  extra?: string;
  /** text: string — HTML-formatted text of the message */
  text: string = '';
  /** media: vector<inputRichMessageMedia> — Media used in the message */
  media: InputRichMessageMedia[] = [];
}

/** Decode a TDLib `richMessageSourceHtml` object; null input -> null. */
export function decodeRichMessageSourceHtml(json: TdJsonRaw): RichMessageSourceHtml | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichMessageSourceHtml = new RichMessageSourceHtml();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.media = decodeVector(record['media'], (item: TdJsonRaw): InputRichMessageMedia => decodeInputRichMessageMedia(item) as InputRichMessageMedia);
  return out;
}

/** Encode a `richMessageSourceHtml` into TDLib JSON wire shape. */
export function encodeRichMessageSourceHtml(value: RichMessageSourceHtml): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richMessageSourceHtml';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['media'] = encodeVector(value.media, (item: InputRichMessageMedia | null): TdJsonRaw => item === null ? null : encodeInputRichMessageMedia(item));
  return out;
}

/** richMessageSourceMarkdown = RichMessageSource — A Markdown-formatted rich message; for bots only */
export class RichMessageSourceMarkdown {
  type: 'richMessageSourceMarkdown' = 'richMessageSourceMarkdown';
  extra?: string;
  /** text: string — Markdown-formatted text of the message */
  text: string = '';
  /** media: vector<inputRichMessageMedia> — Media used in the message */
  media: InputRichMessageMedia[] = [];
}

/** Decode a TDLib `richMessageSourceMarkdown` object; null input -> null. */
export function decodeRichMessageSourceMarkdown(json: TdJsonRaw): RichMessageSourceMarkdown | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichMessageSourceMarkdown = new RichMessageSourceMarkdown();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.media = decodeVector(record['media'], (item: TdJsonRaw): InputRichMessageMedia => decodeInputRichMessageMedia(item) as InputRichMessageMedia);
  return out;
}

/** Encode a `richMessageSourceMarkdown` into TDLib JSON wire shape. */
export function encodeRichMessageSourceMarkdown(value: RichMessageSourceMarkdown): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richMessageSourceMarkdown';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['media'] = encodeVector(value.media, (item: InputRichMessageMedia | null): TdJsonRaw => item === null ? null : encodeInputRichMessageMedia(item));
  return out;
}

/** richTextAnchor = RichText — An anchor */
export class RichTextAnchor {
  type: 'richTextAnchor' = 'richTextAnchor';
  extra?: string;
  /** name: string — Anchor name */
  name: string = '';
}

/** Decode a TDLib `richTextAnchor` object; null input -> null. */
export function decodeRichTextAnchor(json: TdJsonRaw): RichTextAnchor | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextAnchor = new RichTextAnchor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `richTextAnchor` into TDLib JSON wire shape. */
export function encodeRichTextAnchor(value: RichTextAnchor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextAnchor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** richTextAnchorLink = RichText — A link to an anchor on the same page */
export class RichTextAnchorLink {
  type: 'richTextAnchorLink' = 'richTextAnchorLink';
  extra?: string;
  /** text: RichText — The link text */
  text: TdRichText | null = null;
  /** anchor_name: string — The anchor name. If the name is empty, the link must bring back to top */
  anchor_name: string = '';
  /** url: string — An HTTP URL that opens the anchor */
  url: string = '';
}

/** Decode a TDLib `richTextAnchorLink` object; null input -> null. */
export function decodeRichTextAnchorLink(json: TdJsonRaw): RichTextAnchorLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextAnchorLink = new RichTextAnchorLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.anchor_name = asString(record['anchor_name']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `richTextAnchorLink` into TDLib JSON wire shape. */
export function encodeRichTextAnchorLink(value: RichTextAnchorLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextAnchorLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['anchor_name'] = value.anchor_name;
  out['url'] = value.url;
  return out;
}

/** richTextBankCardNumber = RichText — A bank card number */
export class RichTextBankCardNumber {
  type: 'richTextBankCardNumber' = 'richTextBankCardNumber';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** bank_card_number: string — The number of the bank card */
  bank_card_number: string = '';
}

/** Decode a TDLib `richTextBankCardNumber` object; null input -> null. */
export function decodeRichTextBankCardNumber(json: TdJsonRaw): RichTextBankCardNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextBankCardNumber = new RichTextBankCardNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.bank_card_number = asString(record['bank_card_number']);
  return out;
}

/** Encode a `richTextBankCardNumber` into TDLib JSON wire shape. */
export function encodeRichTextBankCardNumber(value: RichTextBankCardNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextBankCardNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['bank_card_number'] = value.bank_card_number;
  return out;
}

/** richTextBold = RichText — A bold rich text */
export class RichTextBold {
  type: 'richTextBold' = 'richTextBold';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextBold` object; null input -> null. */
export function decodeRichTextBold(json: TdJsonRaw): RichTextBold | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextBold = new RichTextBold();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextBold` into TDLib JSON wire shape. */
export function encodeRichTextBold(value: RichTextBold): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextBold';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextBotCommand = RichText — A bot command */
export class RichTextBotCommand {
  type: 'richTextBotCommand' = 'richTextBotCommand';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** bot_command: string — The bot command */
  bot_command: string = '';
}

/** Decode a TDLib `richTextBotCommand` object; null input -> null. */
export function decodeRichTextBotCommand(json: TdJsonRaw): RichTextBotCommand | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextBotCommand = new RichTextBotCommand();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.bot_command = asString(record['bot_command']);
  return out;
}

/** Encode a `richTextBotCommand` into TDLib JSON wire shape. */
export function encodeRichTextBotCommand(value: RichTextBotCommand): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextBotCommand';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['bot_command'] = value.bot_command;
  return out;
}

/** richTextButton = RichText — A button */
export class RichTextButton {
  type: 'richTextButton' = 'richTextButton';
  extra?: string;
  /** button: inlineButton — The button */
  button: InlineButton | null = null;
}

/** Decode a TDLib `richTextButton` object; null input -> null. */
export function decodeRichTextButton(json: TdJsonRaw): RichTextButton | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextButton = new RichTextButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.button = decodeInlineButton(record['button']);
  return out;
}

/** Encode a `richTextButton` into TDLib JSON wire shape. */
export function encodeRichTextButton(value: RichTextButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['button'] = value.button === null ? null : encodeInlineButton(value.button);
  return out;
}

/** richTextCashtag = RichText — A cashtag */
export class RichTextCashtag {
  type: 'richTextCashtag' = 'richTextCashtag';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** cashtag: string — The cashtag */
  cashtag: string = '';
}

/** Decode a TDLib `richTextCashtag` object; null input -> null. */
export function decodeRichTextCashtag(json: TdJsonRaw): RichTextCashtag | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextCashtag = new RichTextCashtag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.cashtag = asString(record['cashtag']);
  return out;
}

/** Encode a `richTextCashtag` into TDLib JSON wire shape. */
export function encodeRichTextCashtag(value: RichTextCashtag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextCashtag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['cashtag'] = value.cashtag;
  return out;
}

/** richTextCustomEmoji = RichText — A custom emoji */
export class RichTextCustomEmoji {
  type: 'richTextCustomEmoji' = 'richTextCustomEmoji';
  extra?: string;
  /** custom_emoji_id: int64 — Unique identifier of the custom emoji */
  custom_emoji_id: string = '';
  /** alternative_text: string — Alternative text for the custom emoji */
  alternative_text: string = '';
}

/** Decode a TDLib `richTextCustomEmoji` object; null input -> null. */
export function decodeRichTextCustomEmoji(json: TdJsonRaw): RichTextCustomEmoji | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextCustomEmoji = new RichTextCustomEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  out.alternative_text = asString(record['alternative_text']);
  return out;
}

/** Encode a `richTextCustomEmoji` into TDLib JSON wire shape. */
export function encodeRichTextCustomEmoji(value: RichTextCustomEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextCustomEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_emoji_id'] = value.custom_emoji_id;
  out['alternative_text'] = value.alternative_text;
  return out;
}

/** richTextDateTime = RichText — A date and time */
export class RichTextDateTime {
  type: 'richTextDateTime' = 'richTextDateTime';
  extra?: string;
  /** text: RichText — Original text */
  text: TdRichText | null = null;
  /** unix_time: int32 — Point in time (Unix timestamp) representing the date and time */
  unix_time: number = 0;
  /** formatting_type: DateTimeFormattingType — Date and time formatting type; may be null if none and the original text must not be changed */
  formatting_type: TdDateTimeFormattingType | null = null;
}

/** Decode a TDLib `richTextDateTime` object; null input -> null. */
export function decodeRichTextDateTime(json: TdJsonRaw): RichTextDateTime | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextDateTime = new RichTextDateTime();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.unix_time = asNumber(record['unix_time']);
  out.formatting_type = decodeTdDateTimeFormattingType(record['formatting_type']);
  return out;
}

/** Encode a `richTextDateTime` into TDLib JSON wire shape. */
export function encodeRichTextDateTime(value: RichTextDateTime): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextDateTime';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['unix_time'] = value.unix_time;
  out['formatting_type'] = value.formatting_type === null ? null : encodeTdDateTimeFormattingType(value.formatting_type);
  return out;
}

/** richTextDiff = RichText — A rich text replacing another rich text; not supported in inputRichMessage */
export class RichTextDiff {
  type: 'richTextDiff' = 'richTextDiff';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** old_text: RichText — The old text */
  old_text: TdRichText | null = null;
}

/** Decode a TDLib `richTextDiff` object; null input -> null. */
export function decodeRichTextDiff(json: TdJsonRaw): RichTextDiff | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextDiff = new RichTextDiff();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.old_text = decodeTdRichText(record['old_text']);
  return out;
}

/** Encode a `richTextDiff` into TDLib JSON wire shape. */
export function encodeRichTextDiff(value: RichTextDiff): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextDiff';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['old_text'] = value.old_text === null ? null : encodeTdRichText(value.old_text);
  return out;
}

/** richTextEmailAddress = RichText — A rich text email address */
export class RichTextEmailAddress {
  type: 'richTextEmailAddress' = 'richTextEmailAddress';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** email_address: string — Email address */
  email_address: string = '';
}

/** Decode a TDLib `richTextEmailAddress` object; null input -> null. */
export function decodeRichTextEmailAddress(json: TdJsonRaw): RichTextEmailAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextEmailAddress = new RichTextEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.email_address = asString(record['email_address']);
  return out;
}

/** Encode a `richTextEmailAddress` into TDLib JSON wire shape. */
export function encodeRichTextEmailAddress(value: RichTextEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['email_address'] = value.email_address;
  return out;
}

/** richTextFixed = RichText — A fixed-width rich text */
export class RichTextFixed {
  type: 'richTextFixed' = 'richTextFixed';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextFixed` object; null input -> null. */
export function decodeRichTextFixed(json: TdJsonRaw): RichTextFixed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextFixed = new RichTextFixed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextFixed` into TDLib JSON wire shape. */
export function encodeRichTextFixed(value: RichTextFixed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextFixed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextHashtag = RichText — A hashtag */
export class RichTextHashtag {
  type: 'richTextHashtag' = 'richTextHashtag';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** hashtag: string — The hashtag */
  hashtag: string = '';
}

/** Decode a TDLib `richTextHashtag` object; null input -> null. */
export function decodeRichTextHashtag(json: TdJsonRaw): RichTextHashtag | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextHashtag = new RichTextHashtag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.hashtag = asString(record['hashtag']);
  return out;
}

/** Encode a `richTextHashtag` into TDLib JSON wire shape. */
export function encodeRichTextHashtag(value: RichTextHashtag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextHashtag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['hashtag'] = value.hashtag;
  return out;
}

/** richTextIcon = RichText — A small image inside the text; instant view only */
export class RichTextIcon {
  type: 'richTextIcon' = 'richTextIcon';
  extra?: string;
  /** document: document — The image represented as a document. The image can be in GIF, JPEG or PNG format */
  document: Document | null = null;
  /** width: int32 — Width of a bounding box in which the image must be shown; 0 if unknown */
  width: number = 0;
  /** height: int32 — Height of a bounding box in which the image must be shown; 0 if unknown */
  height: number = 0;
}

/** Decode a TDLib `richTextIcon` object; null input -> null. */
export function decodeRichTextIcon(json: TdJsonRaw): RichTextIcon | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextIcon = new RichTextIcon();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeDocument(record['document']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  return out;
}

/** Encode a `richTextIcon` into TDLib JSON wire shape. */
export function encodeRichTextIcon(value: RichTextIcon): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextIcon';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeDocument(value.document);
  out['width'] = value.width;
  out['height'] = value.height;
  return out;
}

/** richTextItalic = RichText — An italicized rich text */
export class RichTextItalic {
  type: 'richTextItalic' = 'richTextItalic';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextItalic` object; null input -> null. */
export function decodeRichTextItalic(json: TdJsonRaw): RichTextItalic | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextItalic = new RichTextItalic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextItalic` into TDLib JSON wire shape. */
export function encodeRichTextItalic(value: RichTextItalic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextItalic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextMarked = RichText — A marked rich text */
export class RichTextMarked {
  type: 'richTextMarked' = 'richTextMarked';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextMarked` object; null input -> null. */
export function decodeRichTextMarked(json: TdJsonRaw): RichTextMarked | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextMarked = new RichTextMarked();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextMarked` into TDLib JSON wire shape. */
export function encodeRichTextMarked(value: RichTextMarked): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextMarked';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextMathematicalExpression = RichText — A mathematical expression */
export class RichTextMathematicalExpression {
  type: 'richTextMathematicalExpression' = 'richTextMathematicalExpression';
  extra?: string;
  /** expression: string — The expression in LaTeX format */
  expression: string = '';
}

/** Decode a TDLib `richTextMathematicalExpression` object; null input -> null. */
export function decodeRichTextMathematicalExpression(json: TdJsonRaw): RichTextMathematicalExpression | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextMathematicalExpression = new RichTextMathematicalExpression();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.expression = asString(record['expression']);
  return out;
}

/** Encode a `richTextMathematicalExpression` into TDLib JSON wire shape. */
export function encodeRichTextMathematicalExpression(value: RichTextMathematicalExpression): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextMathematicalExpression';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['expression'] = value.expression;
  return out;
}

/** richTextMention = RichText — A mention of a Telegram user or chat by a username */
export class RichTextMention {
  type: 'richTextMention' = 'richTextMention';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** username: string — The username */
  username: string = '';
}

/** Decode a TDLib `richTextMention` object; null input -> null. */
export function decodeRichTextMention(json: TdJsonRaw): RichTextMention | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextMention = new RichTextMention();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.username = asString(record['username']);
  return out;
}

/** Encode a `richTextMention` into TDLib JSON wire shape. */
export function encodeRichTextMention(value: RichTextMention): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextMention';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['username'] = value.username;
  return out;
}

/** richTextMentionName = RichText — A rich text that serves as a mention of a user */
export class RichTextMentionName {
  type: 'richTextMentionName' = 'richTextMentionName';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** user_id: int53 — Identifier of the mentioned user */
  user_id: number = 0;
}

/** Decode a TDLib `richTextMentionName` object; null input -> null. */
export function decodeRichTextMentionName(json: TdJsonRaw): RichTextMentionName | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextMentionName = new RichTextMentionName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `richTextMentionName` into TDLib JSON wire shape. */
export function encodeRichTextMentionName(value: RichTextMentionName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextMentionName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['user_id'] = value.user_id;
  return out;
}

/** richTextPhoneNumber = RichText — A rich text phone number */
export class RichTextPhoneNumber {
  type: 'richTextPhoneNumber' = 'richTextPhoneNumber';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** phone_number: string — Phone number */
  phone_number: string = '';
}

/** Decode a TDLib `richTextPhoneNumber` object; null input -> null. */
export function decodeRichTextPhoneNumber(json: TdJsonRaw): RichTextPhoneNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextPhoneNumber = new RichTextPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.phone_number = asString(record['phone_number']);
  return out;
}

/** Encode a `richTextPhoneNumber` into TDLib JSON wire shape. */
export function encodeRichTextPhoneNumber(value: RichTextPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['phone_number'] = value.phone_number;
  return out;
}

/** richTextPlain = RichText — A plain text */
export class RichTextPlain {
  type: 'richTextPlain' = 'richTextPlain';
  extra?: string;
  /** text: string — Text */
  text: string = '';
}

/** Decode a TDLib `richTextPlain` object; null input -> null. */
export function decodeRichTextPlain(json: TdJsonRaw): RichTextPlain | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextPlain = new RichTextPlain();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  return out;
}

/** Encode a `richTextPlain` into TDLib JSON wire shape. */
export function encodeRichTextPlain(value: RichTextPlain): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextPlain';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  return out;
}

/** richTextReference = RichText — A reference */
export class RichTextReference {
  type: 'richTextReference' = 'richTextReference';
  extra?: string;
  /** name: string — Reference name */
  name: string = '';
  /** text: RichText — Text of the reference */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextReference` object; null input -> null. */
export function decodeRichTextReference(json: TdJsonRaw): RichTextReference | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextReference = new RichTextReference();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextReference` into TDLib JSON wire shape. */
export function encodeRichTextReference(value: RichTextReference): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextReference';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextReferenceLink = RichText — A link to a reference on the same page */
export class RichTextReferenceLink {
  type: 'richTextReferenceLink' = 'richTextReferenceLink';
  extra?: string;
  /** text: RichText — The link text */
  text: TdRichText | null = null;
  /** reference_name: string — The reference name */
  reference_name: string = '';
  /** url: string — An HTTP URL that opens the reference */
  url: string = '';
}

/** Decode a TDLib `richTextReferenceLink` object; null input -> null. */
export function decodeRichTextReferenceLink(json: TdJsonRaw): RichTextReferenceLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextReferenceLink = new RichTextReferenceLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.reference_name = asString(record['reference_name']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `richTextReferenceLink` into TDLib JSON wire shape. */
export function encodeRichTextReferenceLink(value: RichTextReferenceLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextReferenceLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['reference_name'] = value.reference_name;
  out['url'] = value.url;
  return out;
}

/** richTextSpoiler = RichText — A spoilered rich text */
export class RichTextSpoiler {
  type: 'richTextSpoiler' = 'richTextSpoiler';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextSpoiler` object; null input -> null. */
export function decodeRichTextSpoiler(json: TdJsonRaw): RichTextSpoiler | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextSpoiler = new RichTextSpoiler();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextSpoiler` into TDLib JSON wire shape. */
export function encodeRichTextSpoiler(value: RichTextSpoiler): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextSpoiler';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextStrikethrough = RichText — A strikethrough rich text */
export class RichTextStrikethrough {
  type: 'richTextStrikethrough' = 'richTextStrikethrough';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextStrikethrough` object; null input -> null. */
export function decodeRichTextStrikethrough(json: TdJsonRaw): RichTextStrikethrough | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextStrikethrough = new RichTextStrikethrough();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextStrikethrough` into TDLib JSON wire shape. */
export function encodeRichTextStrikethrough(value: RichTextStrikethrough): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextStrikethrough';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextSubscript = RichText — A subscript rich text */
export class RichTextSubscript {
  type: 'richTextSubscript' = 'richTextSubscript';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextSubscript` object; null input -> null. */
export function decodeRichTextSubscript(json: TdJsonRaw): RichTextSubscript | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextSubscript = new RichTextSubscript();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextSubscript` into TDLib JSON wire shape. */
export function encodeRichTextSubscript(value: RichTextSubscript): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextSubscript';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextSuperscript = RichText — A superscript rich text */
export class RichTextSuperscript {
  type: 'richTextSuperscript' = 'richTextSuperscript';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextSuperscript` object; null input -> null. */
export function decodeRichTextSuperscript(json: TdJsonRaw): RichTextSuperscript | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextSuperscript = new RichTextSuperscript();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextSuperscript` into TDLib JSON wire shape. */
export function encodeRichTextSuperscript(value: RichTextSuperscript): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextSuperscript';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextUnderline = RichText — An underlined rich text */
export class RichTextUnderline {
  type: 'richTextUnderline' = 'richTextUnderline';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `richTextUnderline` object; null input -> null. */
export function decodeRichTextUnderline(json: TdJsonRaw): RichTextUnderline | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextUnderline = new RichTextUnderline();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `richTextUnderline` into TDLib JSON wire shape. */
export function encodeRichTextUnderline(value: RichTextUnderline): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextUnderline';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** richTextUrl = RichText — A rich text URL link */
export class RichTextUrl {
  type: 'richTextUrl' = 'richTextUrl';
  extra?: string;
  /** text: RichText — Text */
  text: TdRichText | null = null;
  /** url: string — URL */
  url: string = '';
  /** is_cached: Bool — True, if the URL has cached instant view server-side; instant view only */
  is_cached: boolean = false;
}

/** Decode a TDLib `richTextUrl` object; null input -> null. */
export function decodeRichTextUrl(json: TdJsonRaw): RichTextUrl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTextUrl = new RichTextUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.url = asString(record['url']);
  out.is_cached = asBool(record['is_cached']);
  return out;
}

/** Encode a `richTextUrl` into TDLib JSON wire shape. */
export function encodeRichTextUrl(value: RichTextUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTextUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['url'] = value.url;
  out['is_cached'] = value.is_cached;
  return out;
}

/** richTexts = RichText — A concatenation of rich texts */
export class RichTexts {
  type: 'richTexts' = 'richTexts';
  extra?: string;
  /** texts: vector<RichText> — Texts */
  texts: TdRichText[] = [];
}

/** Decode a TDLib `richTexts` object; null input -> null. */
export function decodeRichTexts(json: TdJsonRaw): RichTexts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RichTexts = new RichTexts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.texts = decodeVector(record['texts'], (item: TdJsonRaw): TdRichText => decodeTdRichText(item) as TdRichText);
  return out;
}

/** Encode a `richTexts` into TDLib JSON wire shape. */
export function encodeRichTexts(value: RichTexts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'richTexts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['texts'] = encodeVector(value.texts, (item: TdRichText | null): TdJsonRaw => item === null ? null : encodeTdRichText(item));
  return out;
}

/** rtmpUrl = RtmpUrl — Represents an RTMP URL */
export class RtmpUrl {
  type: 'rtmpUrl' = 'rtmpUrl';
  extra?: string;
  /** url: string — The URL */
  url: string = '';
  /** stream_key: string — Stream key */
  stream_key: string = '';
}

/** Decode a TDLib `rtmpUrl` object; null input -> null. */
export function decodeRtmpUrl(json: TdJsonRaw): RtmpUrl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: RtmpUrl = new RtmpUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.stream_key = asString(record['stream_key']);
  return out;
}

/** Encode a `rtmpUrl` into TDLib JSON wire shape. */
export function encodeRtmpUrl(value: RtmpUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'rtmpUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['stream_key'] = value.stream_key;
  return out;
}
