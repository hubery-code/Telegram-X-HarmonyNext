/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { Address, Animation, Audio, decodeAddress, decodeAnimation, decodeAudio, encodeAddress, encodeAnimation, encodeAudio } from './TdTypes_A';
import { ChatAdministratorRights, ChatPhotoSticker, Contact, decodeChatAdministratorRights, decodeChatPhotoSticker, decodeContact, encodeChatAdministratorRights, encodeChatPhotoSticker, encodeContact } from './TdTypes_C';
import { DateValue, DatedFile, Document, decodeDateValue, decodeDatedFile, decodeDocument, encodeDateValue, encodeDatedFile, encodeDocument } from './TdTypes_D';
import { FormattedText, decodeFormattedText, encodeFormattedText } from './TdTypes_F';
import { Game, decodeGame, encodeGame } from './TdTypes_G';
import { LabeledPricePart, LinkPreviewOptions, LiveLocation, Location, LocationAddress, decodeLabeledPricePart, decodeLinkPreviewOptions, decodeLiveLocation, decodeLocation, decodeLocationAddress, encodeLabeledPricePart, encodeLinkPreviewOptions, encodeLiveLocation, encodeLocation, encodeLocationAddress } from './TdTypes_L';
import { MessageCopyOptions, decodeMessageCopyOptions, encodeMessageCopyOptions } from './TdTypes_M';
import { PageBlockCaption, PageBlockTableCell, PersonalDetails, Photo, ProxyValue, decodePageBlockCaption, decodePageBlockTableCell, decodePersonalDetails, decodePhoto, decodeProxyValue, encodePageBlockCaption, encodePageBlockTableCell, encodePersonalDetails, encodePhoto, encodeProxyValue } from './TdTypes_P';
import { Sticker, StoryAreaPosition, decodeSticker, decodeStoryAreaPosition, encodeSticker, encodeStoryAreaPosition } from './TdTypes_S';
import { Thumbnail, decodeThumbnail, encodeThumbnail } from './TdTypes_T';
import { Venue, Video, VoiceNote, decodeVenue, decodeVideo, decodeVoiceNote, encodeVenue, encodeVideo, encodeVoiceNote } from './TdTypes_V';
import { TdButtonStyle, TdInlineKeyboardButtonType, TdInlineQueryResult, TdInlineQueryResultsButtonType, TdInputFile, TdInputMessageContent, TdInputPageBlock, TdInputPaidMediaType, TdInputPassportElementErrorSource, TdInputPollMedia, TdInputPollType, TdInputStoryAreaType, TdMessageSelfDestructType, TdPageBlockHorizontalAlignment, TdPassportElementType, TdReactionType, TdReplyMarkup, TdRichMessageSource, TdRichText, TdSettingsSection, TdStoryContentType, TdSuggestedPostPrice, TdTargetChat, TdTelegramPaymentPurpose, TdWebAppOpenMode, decodeTdButtonStyle, decodeTdInlineKeyboardButtonType, decodeTdInlineQueryResult, decodeTdInlineQueryResultsButtonType, decodeTdInputFile, decodeTdInputMessageContent, decodeTdInputPageBlock, decodeTdInputPaidMediaType, decodeTdInputPassportElementErrorSource, decodeTdInputPollMedia, decodeTdInputPollType, decodeTdInputStoryAreaType, decodeTdMessageSelfDestructType, decodeTdPageBlockHorizontalAlignment, decodeTdPassportElementType, decodeTdReactionType, decodeTdReplyMarkup, decodeTdRichMessageSource, decodeTdRichText, decodeTdSettingsSection, decodeTdStoryContentType, decodeTdSuggestedPostPrice, decodeTdTargetChat, decodeTdTelegramPaymentPurpose, decodeTdWebAppOpenMode, encodeTdButtonStyle, encodeTdInlineKeyboardButtonType, encodeTdInlineQueryResult, encodeTdInlineQueryResultsButtonType, encodeTdInputFile, encodeTdInputMessageContent, encodeTdInputPageBlock, encodeTdInputPaidMediaType, encodeTdInputPassportElementErrorSource, encodeTdInputPollMedia, encodeTdInputPollType, encodeTdInputStoryAreaType, encodeTdMessageSelfDestructType, encodeTdPageBlockHorizontalAlignment, encodeTdPassportElementType, encodeTdReactionType, encodeTdReplyMarkup, encodeTdRichMessageSource, encodeTdRichText, encodeTdSettingsSection, encodeTdStoryContentType, encodeTdSuggestedPostPrice, encodeTdTargetChat, encodeTdTelegramPaymentPurpose, encodeTdWebAppOpenMode } from './TdUnions';

/** identityDocument = IdentityDocument — An identity document */
export class IdentityDocument {
  type: 'identityDocument' = 'identityDocument';
  extra?: string;
  /** number: string — Document number; 1-24 characters */
  number: string = '';
  /** expiration_date: date — Document expiration date; may be null if not applicable */
  expiration_date: DateValue | null = null;
  /** front_side: datedFile — Front side of the document */
  front_side: DatedFile | null = null;
  /** reverse_side: datedFile — Reverse side of the document; only for driver license and identity card; may be null */
  reverse_side: DatedFile | null = null;
  /** selfie: datedFile — Selfie with the document; may be null */
  selfie: DatedFile | null = null;
  /** translation: vector<datedFile> — List of files containing a certified English translation of the document */
  translation: DatedFile[] = [];
}

/** Decode a TDLib `identityDocument` object; null input -> null. */
export function decodeIdentityDocument(json: TdJsonRaw): IdentityDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: IdentityDocument = new IdentityDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.number = asString(record['number']);
  out.expiration_date = decodeDateValue(record['expiration_date']);
  out.front_side = decodeDatedFile(record['front_side']);
  out.reverse_side = decodeDatedFile(record['reverse_side']);
  out.selfie = decodeDatedFile(record['selfie']);
  out.translation = decodeVector(record['translation'], (item: TdJsonRaw): DatedFile => decodeDatedFile(item) as DatedFile);
  return out;
}

/** Encode a `identityDocument` into TDLib JSON wire shape. */
export function encodeIdentityDocument(value: IdentityDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'identityDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['number'] = value.number;
  out['expiration_date'] = value.expiration_date === null ? null : encodeDateValue(value.expiration_date);
  out['front_side'] = value.front_side === null ? null : encodeDatedFile(value.front_side);
  out['reverse_side'] = value.reverse_side === null ? null : encodeDatedFile(value.reverse_side);
  out['selfie'] = value.selfie === null ? null : encodeDatedFile(value.selfie);
  out['translation'] = encodeVector(value.translation, (item: DatedFile | null): TdJsonRaw => item === null ? null : encodeDatedFile(item));
  return out;
}

/** importContacts = ImportedContacts — Adds new contacts or edits existing contacts by their phone numbers; contacts' user identifiers are ignored */
export class ImportContacts {
  type: 'importContacts' = 'importContacts';
  extra?: string;
  /** contacts: vector<importedContact> — The list of contacts to import or edit */
  contacts: ImportedContact[] = [];
}

/** Decode a TDLib `importContacts` object; null input -> null. */
export function decodeImportContacts(json: TdJsonRaw): ImportContacts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ImportContacts = new ImportContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.contacts = decodeVector(record['contacts'], (item: TdJsonRaw): ImportedContact => decodeImportedContact(item) as ImportedContact);
  return out;
}

/** Encode a `importContacts` into TDLib JSON wire shape. */
export function encodeImportContacts(value: ImportContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'importContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['contacts'] = encodeVector(value.contacts, (item: ImportedContact | null): TdJsonRaw => item === null ? null : encodeImportedContact(item));
  return out;
}

/** importMessages = Ok — Imports messages exported from another application */
export class ImportMessages {
  type: 'importMessages' = 'importMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of a chat to which the messages will be imported. It must be an identifier of a private chat with a mutual contact or an identifier of a supergroup chat with can_change_info member right */
  chat_id: number = 0;
  /** message_file: InputFile — File with messages to import. Only inputFileLocal and inputFileGenerated are supported. The file must not be previously uploaded */
  message_file: TdInputFile | null = null;
  /** attached_files: vector<InputFile> — Files used in the imported messages. Only inputFileLocal and inputFileGenerated are supported. The files must not be previously uploaded */
  attached_files: TdInputFile[] = [];
}

/** Decode a TDLib `importMessages` object; null input -> null. */
export function decodeImportMessages(json: TdJsonRaw): ImportMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ImportMessages = new ImportMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_file = decodeTdInputFile(record['message_file']);
  out.attached_files = decodeVector(record['attached_files'], (item: TdJsonRaw): TdInputFile => decodeTdInputFile(item) as TdInputFile);
  return out;
}

/** Encode a `importMessages` into TDLib JSON wire shape. */
export function encodeImportMessages(value: ImportMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'importMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_file'] = value.message_file === null ? null : encodeTdInputFile(value.message_file);
  out['attached_files'] = encodeVector(value.attached_files, (item: TdInputFile | null): TdJsonRaw => item === null ? null : encodeTdInputFile(item));
  return out;
}

/** importedContact = ImportedContact — Describes a contact to import pass null to keep the current user's note */
export class ImportedContact {
  type: 'importedContact' = 'importedContact';
  extra?: string;
  /** phone_number: string — Phone number of the user */
  phone_number: string = '';
  /** first_name: string — First name of the user; 1-64 characters */
  first_name: string = '';
  /** last_name: string — Last name of the user; 0-64 characters */
  last_name: string = '';
  /** note: formattedText — Note to add about the user; 0-getOption("user_note_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed; */
  note: FormattedText | null = null;
}

/** Decode a TDLib `importedContact` object; null input -> null. */
export function decodeImportedContact(json: TdJsonRaw): ImportedContact | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ImportedContact = new ImportedContact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  out.first_name = asString(record['first_name']);
  out.last_name = asString(record['last_name']);
  out.note = decodeFormattedText(record['note']);
  return out;
}

/** Encode a `importedContact` into TDLib JSON wire shape. */
export function encodeImportedContact(value: ImportedContact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'importedContact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  out['first_name'] = value.first_name;
  out['last_name'] = value.last_name;
  out['note'] = value.note === null ? null : encodeFormattedText(value.note);
  return out;
}

/** importedContacts = ImportedContacts — Represents the result of an importContacts request */
export class ImportedContacts {
  type: 'importedContacts' = 'importedContacts';
  extra?: string;
  /** user_ids: vector<int53> — User identifiers of the imported contacts in the same order as they were specified in the request; 0 if the contact is not yet a registered user */
  user_ids: number[] = [];
  /** importer_count: vector<int32> — The number of users that imported the corresponding contact; 0 for already registered users or if unavailable */
  importer_count: number[] = [];
}

/** Decode a TDLib `importedContacts` object; null input -> null. */
export function decodeImportedContacts(json: TdJsonRaw): ImportedContacts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ImportedContacts = new ImportedContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.importer_count = decodeVector(record['importer_count'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `importedContacts` into TDLib JSON wire shape. */
export function encodeImportedContacts(value: ImportedContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'importedContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  out['importer_count'] = encodeVector(value.importer_count, (item: number): TdJsonRaw => item);
  return out;
}

/** increaseGiftAuctionBid = Ok — Increases a bid for an auction gift without changing gift text and receiver */
export class IncreaseGiftAuctionBid {
  type: 'increaseGiftAuctionBid' = 'increaseGiftAuctionBid';
  extra?: string;
  /** gift_id: int64 — Identifier of the gift to put the bid on */
  gift_id: string = '';
  /** star_count: int53 — The number of Telegram Stars to put in the bid */
  star_count: number = 0;
}

/** Decode a TDLib `increaseGiftAuctionBid` object; null input -> null. */
export function decodeIncreaseGiftAuctionBid(json: TdJsonRaw): IncreaseGiftAuctionBid | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: IncreaseGiftAuctionBid = new IncreaseGiftAuctionBid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_id = asInt64(record['gift_id']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `increaseGiftAuctionBid` into TDLib JSON wire shape. */
export function encodeIncreaseGiftAuctionBid(value: IncreaseGiftAuctionBid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'increaseGiftAuctionBid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_id'] = value.gift_id;
  out['star_count'] = value.star_count;
  return out;
}

/** inlineButton = InlineButton — Represents a button inside a rich message inlineKeyboardButtonTypeSwitchInline, inlineKeyboardButtonTypeUser, inlineKeyboardButtonTypeCopyText. Additionally, inlineKeyboardButtonTypeCallbackWithPassword and ... */
export class InlineButton {
  type: 'inlineButton' = 'inlineButton';
  extra?: string;
  /** text: RichText — Text of the button; only richTexts, richTextPlain, and richTextCustomEmoji are allowed */
  text: TdRichText | null = null;
  /** style: ButtonStyle — Style of the button */
  style: TdButtonStyle | null = null;
  /** type: InlineKeyboardButtonType — Type of the button; must be one of inlineKeyboardButtonTypeUrl, inlineKeyboardButtonTypeLoginUrl, inlineKeyboardButtonTypeWebApp, inlineKeyboardButtonTypeCallback, */
  type_: TdInlineKeyboardButtonType | null = null;
}

/** Decode a TDLib `inlineButton` object; null input -> null. */
export function decodeInlineButton(json: TdJsonRaw): InlineButton | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineButton = new InlineButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.style = decodeTdButtonStyle(record['style']);
  out.type_ = decodeTdInlineKeyboardButtonType(record['type']);
  return out;
}

/** Encode a `inlineButton` into TDLib JSON wire shape. */
export function encodeInlineButton(value: InlineButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['style'] = value.style === null ? null : encodeTdButtonStyle(value.style);
  out['type'] = value.type_ === null ? null : encodeTdInlineKeyboardButtonType(value.type_);
  return out;
}

/** inlineKeyboardButton = InlineKeyboardButton — Represents a single button in an inline keyboard */
export class InlineKeyboardButton {
  type: 'inlineKeyboardButton' = 'inlineKeyboardButton';
  extra?: string;
  /** text: string — Text of the button */
  text: string = '';
  /** icon_custom_emoji_id: int64 — Identifier of the custom emoji that must be shown on the button; 0 if none */
  icon_custom_emoji_id: string = '';
  /** style: ButtonStyle — Style of the button */
  style: TdButtonStyle | null = null;
  /** type: InlineKeyboardButtonType — Type of the button */
  type_: TdInlineKeyboardButtonType | null = null;
}

/** Decode a TDLib `inlineKeyboardButton` object; null input -> null. */
export function decodeInlineKeyboardButton(json: TdJsonRaw): InlineKeyboardButton | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButton = new InlineKeyboardButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.icon_custom_emoji_id = asInt64(record['icon_custom_emoji_id']);
  out.style = decodeTdButtonStyle(record['style']);
  out.type_ = decodeTdInlineKeyboardButtonType(record['type']);
  return out;
}

/** Encode a `inlineKeyboardButton` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButton(value: InlineKeyboardButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['icon_custom_emoji_id'] = value.icon_custom_emoji_id;
  out['style'] = value.style === null ? null : encodeTdButtonStyle(value.style);
  out['type'] = value.type_ === null ? null : encodeTdInlineKeyboardButtonType(value.type_);
  return out;
}

/** inlineKeyboardButtonTypeBuy = InlineKeyboardButtonType — A button to buy something. This button must be in the first column and row of the keyboard and can be attached only to a message with content of the type messageInvoice */
export class InlineKeyboardButtonTypeBuy {
  type: 'inlineKeyboardButtonTypeBuy' = 'inlineKeyboardButtonTypeBuy';
  extra?: string;
}

/** Decode a TDLib `inlineKeyboardButtonTypeBuy` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeBuy(json: TdJsonRaw): InlineKeyboardButtonTypeBuy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeBuy = new InlineKeyboardButtonTypeBuy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inlineKeyboardButtonTypeBuy` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeBuy(value: InlineKeyboardButtonTypeBuy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeBuy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inlineKeyboardButtonTypeCallback = InlineKeyboardButtonType — A button that sends a callback query to a bot */
export class InlineKeyboardButtonTypeCallback {
  type: 'inlineKeyboardButtonTypeCallback' = 'inlineKeyboardButtonTypeCallback';
  extra?: string;
  /** data: bytes — Data to be sent to the bot via a callback query */
  data: string = '';
}

/** Decode a TDLib `inlineKeyboardButtonTypeCallback` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeCallback(json: TdJsonRaw): InlineKeyboardButtonTypeCallback | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeCallback = new InlineKeyboardButtonTypeCallback();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.data = asString(record['data']);
  return out;
}

/** Encode a `inlineKeyboardButtonTypeCallback` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeCallback(value: InlineKeyboardButtonTypeCallback): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeCallback';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['data'] = value.data;
  return out;
}

/** inlineKeyboardButtonTypeCallbackGame = InlineKeyboardButtonType — A button with a game that sends a callback query to a bot. This button must be in the first column and row of the keyboard and can be attached only to a message with conte... */
export class InlineKeyboardButtonTypeCallbackGame {
  type: 'inlineKeyboardButtonTypeCallbackGame' = 'inlineKeyboardButtonTypeCallbackGame';
  extra?: string;
}

/** Decode a TDLib `inlineKeyboardButtonTypeCallbackGame` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeCallbackGame(json: TdJsonRaw): InlineKeyboardButtonTypeCallbackGame | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeCallbackGame = new InlineKeyboardButtonTypeCallbackGame();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inlineKeyboardButtonTypeCallbackGame` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeCallbackGame(value: InlineKeyboardButtonTypeCallbackGame): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeCallbackGame';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inlineKeyboardButtonTypeCallbackWithPassword = InlineKeyboardButtonType — A button that asks for the 2-step verification password of the current user and then sends a callback query to a bot */
export class InlineKeyboardButtonTypeCallbackWithPassword {
  type: 'inlineKeyboardButtonTypeCallbackWithPassword' = 'inlineKeyboardButtonTypeCallbackWithPassword';
  extra?: string;
  /** data: bytes — Data to be sent to the bot via a callback query */
  data: string = '';
}

/** Decode a TDLib `inlineKeyboardButtonTypeCallbackWithPassword` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeCallbackWithPassword(json: TdJsonRaw): InlineKeyboardButtonTypeCallbackWithPassword | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeCallbackWithPassword = new InlineKeyboardButtonTypeCallbackWithPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.data = asString(record['data']);
  return out;
}

/** Encode a `inlineKeyboardButtonTypeCallbackWithPassword` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeCallbackWithPassword(value: InlineKeyboardButtonTypeCallbackWithPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeCallbackWithPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['data'] = value.data;
  return out;
}

/** inlineKeyboardButtonTypeCopyText = InlineKeyboardButtonType — A button that copies specified text to clipboard */
export class InlineKeyboardButtonTypeCopyText {
  type: 'inlineKeyboardButtonTypeCopyText' = 'inlineKeyboardButtonTypeCopyText';
  extra?: string;
  /** text: string — The text to copy to clipboard */
  text: string = '';
}

/** Decode a TDLib `inlineKeyboardButtonTypeCopyText` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeCopyText(json: TdJsonRaw): InlineKeyboardButtonTypeCopyText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeCopyText = new InlineKeyboardButtonTypeCopyText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  return out;
}

/** Encode a `inlineKeyboardButtonTypeCopyText` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeCopyText(value: InlineKeyboardButtonTypeCopyText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeCopyText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  return out;
}

/** inlineKeyboardButtonTypeDisabled = InlineKeyboardButtonType — A disabled button */
export class InlineKeyboardButtonTypeDisabled {
  type: 'inlineKeyboardButtonTypeDisabled' = 'inlineKeyboardButtonTypeDisabled';
  extra?: string;
}

/** Decode a TDLib `inlineKeyboardButtonTypeDisabled` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeDisabled(json: TdJsonRaw): InlineKeyboardButtonTypeDisabled | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeDisabled = new InlineKeyboardButtonTypeDisabled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inlineKeyboardButtonTypeDisabled` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeDisabled(value: InlineKeyboardButtonTypeDisabled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeDisabled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inlineKeyboardButtonTypeLoginUrl = InlineKeyboardButtonType — A button that opens a specified URL and automatically authorize the current user by calling getLoginUrlInfo; not supported in ephemeral messages */
export class InlineKeyboardButtonTypeLoginUrl {
  type: 'inlineKeyboardButtonTypeLoginUrl' = 'inlineKeyboardButtonTypeLoginUrl';
  extra?: string;
  /** url: string — An HTTP URL to pass to getLoginUrlInfo */
  url: string = '';
  /** id: int53 — Unique button identifier */
  id: number = 0;
  /** forward_text: string — If non-empty, new text of the button in forwarded messages */
  forward_text: string = '';
}

/** Decode a TDLib `inlineKeyboardButtonTypeLoginUrl` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeLoginUrl(json: TdJsonRaw): InlineKeyboardButtonTypeLoginUrl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeLoginUrl = new InlineKeyboardButtonTypeLoginUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.id = asNumber(record['id']);
  out.forward_text = asString(record['forward_text']);
  return out;
}

/** Encode a `inlineKeyboardButtonTypeLoginUrl` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeLoginUrl(value: InlineKeyboardButtonTypeLoginUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeLoginUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['id'] = value.id;
  out['forward_text'] = value.forward_text;
  return out;
}

/** inlineKeyboardButtonTypeSwitchInline = InlineKeyboardButtonType — A button that forces an inline query to the bot to be inserted in the input field */
export class InlineKeyboardButtonTypeSwitchInline {
  type: 'inlineKeyboardButtonTypeSwitchInline' = 'inlineKeyboardButtonTypeSwitchInline';
  extra?: string;
  /** query: string — Inline query to be sent to the bot */
  query: string = '';
  /** target_chat: TargetChat — Target chat from which to send the inline query */
  target_chat: TdTargetChat | null = null;
}

/** Decode a TDLib `inlineKeyboardButtonTypeSwitchInline` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeSwitchInline(json: TdJsonRaw): InlineKeyboardButtonTypeSwitchInline | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeSwitchInline = new InlineKeyboardButtonTypeSwitchInline();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  out.target_chat = decodeTdTargetChat(record['target_chat']);
  return out;
}

/** Encode a `inlineKeyboardButtonTypeSwitchInline` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeSwitchInline(value: InlineKeyboardButtonTypeSwitchInline): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeSwitchInline';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  out['target_chat'] = value.target_chat === null ? null : encodeTdTargetChat(value.target_chat);
  return out;
}

/** inlineKeyboardButtonTypeUrl = InlineKeyboardButtonType — A button that opens a specified URL */
export class InlineKeyboardButtonTypeUrl {
  type: 'inlineKeyboardButtonTypeUrl' = 'inlineKeyboardButtonTypeUrl';
  extra?: string;
  /** url: string — HTTP or tg:// URL to open. If the link is of the type internalLinkTypeWebApp, then the button must be marked as a Web App button */
  url: string = '';
}

/** Decode a TDLib `inlineKeyboardButtonTypeUrl` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeUrl(json: TdJsonRaw): InlineKeyboardButtonTypeUrl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeUrl = new InlineKeyboardButtonTypeUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `inlineKeyboardButtonTypeUrl` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeUrl(value: InlineKeyboardButtonTypeUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** inlineKeyboardButtonTypeUser = InlineKeyboardButtonType — A button with a user reference to be handled in the same way as textEntityTypeMentionName entities */
export class InlineKeyboardButtonTypeUser {
  type: 'inlineKeyboardButtonTypeUser' = 'inlineKeyboardButtonTypeUser';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
}

/** Decode a TDLib `inlineKeyboardButtonTypeUser` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeUser(json: TdJsonRaw): InlineKeyboardButtonTypeUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeUser = new InlineKeyboardButtonTypeUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `inlineKeyboardButtonTypeUser` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeUser(value: InlineKeyboardButtonTypeUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** inlineKeyboardButtonTypeWebApp = InlineKeyboardButtonType — A button that opens a Web App by calling openWebApp */
export class InlineKeyboardButtonTypeWebApp {
  type: 'inlineKeyboardButtonTypeWebApp' = 'inlineKeyboardButtonTypeWebApp';
  extra?: string;
  /** url: string — An HTTP URL to pass to openWebApp */
  url: string = '';
}

/** Decode a TDLib `inlineKeyboardButtonTypeWebApp` object; null input -> null. */
export function decodeInlineKeyboardButtonTypeWebApp(json: TdJsonRaw): InlineKeyboardButtonTypeWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineKeyboardButtonTypeWebApp = new InlineKeyboardButtonTypeWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `inlineKeyboardButtonTypeWebApp` into TDLib JSON wire shape. */
export function encodeInlineKeyboardButtonTypeWebApp(value: InlineKeyboardButtonTypeWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineKeyboardButtonTypeWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** inlineMessageId = InlineMessageId — Contains identifier of a sent guest message */
export class InlineMessageId {
  type: 'inlineMessageId' = 'inlineMessageId';
  extra?: string;
  /** id: string — Unique identifier for the message */
  id: string = '';
}

/** Decode a TDLib `inlineMessageId` object; null input -> null. */
export function decodeInlineMessageId(json: TdJsonRaw): InlineMessageId | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineMessageId = new InlineMessageId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  return out;
}

/** Encode a `inlineMessageId` into TDLib JSON wire shape. */
export function encodeInlineMessageId(value: InlineMessageId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineMessageId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  return out;
}

/** inlineQueryResultAnimation = InlineQueryResult — Represents an animation file */
export class InlineQueryResultAnimation {
  type: 'inlineQueryResultAnimation' = 'inlineQueryResultAnimation';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** animation: animation — Animation file */
  animation: Animation | null = null;
  /** title: string — Animation title */
  title: string = '';
}

/** Decode a TDLib `inlineQueryResultAnimation` object; null input -> null. */
export function decodeInlineQueryResultAnimation(json: TdJsonRaw): InlineQueryResultAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultAnimation = new InlineQueryResultAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.animation = decodeAnimation(record['animation']);
  out.title = asString(record['title']);
  return out;
}

/** Encode a `inlineQueryResultAnimation` into TDLib JSON wire shape. */
export function encodeInlineQueryResultAnimation(value: InlineQueryResultAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  out['title'] = value.title;
  return out;
}

/** inlineQueryResultArticle = InlineQueryResult — Represents a link to an article or web page */
export class InlineQueryResultArticle {
  type: 'inlineQueryResultArticle' = 'inlineQueryResultArticle';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** url: string — URL of the result, if it exists */
  url: string = '';
  /** title: string — Title of the result */
  title: string = '';
  /** description: string */
  description: string = '';
  /** thumbnail: thumbnail — Result thumbnail in JPEG format; may be null */
  thumbnail: Thumbnail | null = null;
}

/** Decode a TDLib `inlineQueryResultArticle` object; null input -> null. */
export function decodeInlineQueryResultArticle(json: TdJsonRaw): InlineQueryResultArticle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultArticle = new InlineQueryResultArticle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.url = asString(record['url']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  return out;
}

/** Encode a `inlineQueryResultArticle` into TDLib JSON wire shape. */
export function encodeInlineQueryResultArticle(value: InlineQueryResultArticle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultArticle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['url'] = value.url;
  out['title'] = value.title;
  out['description'] = value.description;
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  return out;
}

/** inlineQueryResultAudio = InlineQueryResult — Represents an audio file */
export class InlineQueryResultAudio {
  type: 'inlineQueryResultAudio' = 'inlineQueryResultAudio';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** audio: audio — Audio file */
  audio: Audio | null = null;
}

/** Decode a TDLib `inlineQueryResultAudio` object; null input -> null. */
export function decodeInlineQueryResultAudio(json: TdJsonRaw): InlineQueryResultAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultAudio = new InlineQueryResultAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.audio = decodeAudio(record['audio']);
  return out;
}

/** Encode a `inlineQueryResultAudio` into TDLib JSON wire shape. */
export function encodeInlineQueryResultAudio(value: InlineQueryResultAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['audio'] = value.audio === null ? null : encodeAudio(value.audio);
  return out;
}

/** inlineQueryResultContact = InlineQueryResult — Represents a user contact */
export class InlineQueryResultContact {
  type: 'inlineQueryResultContact' = 'inlineQueryResultContact';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** contact: contact — A user contact */
  contact: Contact | null = null;
  /** thumbnail: thumbnail — Result thumbnail in JPEG format; may be null */
  thumbnail: Thumbnail | null = null;
}

/** Decode a TDLib `inlineQueryResultContact` object; null input -> null. */
export function decodeInlineQueryResultContact(json: TdJsonRaw): InlineQueryResultContact | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultContact = new InlineQueryResultContact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.contact = decodeContact(record['contact']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  return out;
}

/** Encode a `inlineQueryResultContact` into TDLib JSON wire shape. */
export function encodeInlineQueryResultContact(value: InlineQueryResultContact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultContact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['contact'] = value.contact === null ? null : encodeContact(value.contact);
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  return out;
}

/** inlineQueryResultDocument = InlineQueryResult — Represents a document */
export class InlineQueryResultDocument {
  type: 'inlineQueryResultDocument' = 'inlineQueryResultDocument';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** document: document — Document */
  document: Document | null = null;
  /** title: string — Document title */
  title: string = '';
  /** description: string */
  description: string = '';
}

/** Decode a TDLib `inlineQueryResultDocument` object; null input -> null. */
export function decodeInlineQueryResultDocument(json: TdJsonRaw): InlineQueryResultDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultDocument = new InlineQueryResultDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.document = decodeDocument(record['document']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  return out;
}

/** Encode a `inlineQueryResultDocument` into TDLib JSON wire shape. */
export function encodeInlineQueryResultDocument(value: InlineQueryResultDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['document'] = value.document === null ? null : encodeDocument(value.document);
  out['title'] = value.title;
  out['description'] = value.description;
  return out;
}

/** inlineQueryResultGame = InlineQueryResult — Represents information about a game */
export class InlineQueryResultGame {
  type: 'inlineQueryResultGame' = 'inlineQueryResultGame';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** game: game — Game result */
  game: Game | null = null;
}

/** Decode a TDLib `inlineQueryResultGame` object; null input -> null. */
export function decodeInlineQueryResultGame(json: TdJsonRaw): InlineQueryResultGame | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultGame = new InlineQueryResultGame();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.game = decodeGame(record['game']);
  return out;
}

/** Encode a `inlineQueryResultGame` into TDLib JSON wire shape. */
export function encodeInlineQueryResultGame(value: InlineQueryResultGame): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultGame';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['game'] = value.game === null ? null : encodeGame(value.game);
  return out;
}

/** inlineQueryResultLocation = InlineQueryResult — Represents a point on the map */
export class InlineQueryResultLocation {
  type: 'inlineQueryResultLocation' = 'inlineQueryResultLocation';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** location: location — Location result */
  location: Location | null = null;
  /** title: string — Title of the result */
  title: string = '';
  /** thumbnail: thumbnail — Result thumbnail in JPEG format; may be null */
  thumbnail: Thumbnail | null = null;
}

/** Decode a TDLib `inlineQueryResultLocation` object; null input -> null. */
export function decodeInlineQueryResultLocation(json: TdJsonRaw): InlineQueryResultLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultLocation = new InlineQueryResultLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.location = decodeLocation(record['location']);
  out.title = asString(record['title']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  return out;
}

/** Encode a `inlineQueryResultLocation` into TDLib JSON wire shape. */
export function encodeInlineQueryResultLocation(value: InlineQueryResultLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['title'] = value.title;
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  return out;
}

/** inlineQueryResultPhoto = InlineQueryResult — Represents a photo */
export class InlineQueryResultPhoto {
  type: 'inlineQueryResultPhoto' = 'inlineQueryResultPhoto';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** photo: photo — Photo */
  photo: Photo | null = null;
  /** title: string — Title of the result, if known */
  title: string = '';
  /** description: string */
  description: string = '';
}

/** Decode a TDLib `inlineQueryResultPhoto` object; null input -> null. */
export function decodeInlineQueryResultPhoto(json: TdJsonRaw): InlineQueryResultPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultPhoto = new InlineQueryResultPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.photo = decodePhoto(record['photo']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  return out;
}

/** Encode a `inlineQueryResultPhoto` into TDLib JSON wire shape. */
export function encodeInlineQueryResultPhoto(value: InlineQueryResultPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['title'] = value.title;
  out['description'] = value.description;
  return out;
}

/** inlineQueryResultSticker = InlineQueryResult — Represents a sticker */
export class InlineQueryResultSticker {
  type: 'inlineQueryResultSticker' = 'inlineQueryResultSticker';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** sticker: sticker — Sticker */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `inlineQueryResultSticker` object; null input -> null. */
export function decodeInlineQueryResultSticker(json: TdJsonRaw): InlineQueryResultSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultSticker = new InlineQueryResultSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `inlineQueryResultSticker` into TDLib JSON wire shape. */
export function encodeInlineQueryResultSticker(value: InlineQueryResultSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** inlineQueryResultVenue = InlineQueryResult — Represents information about a venue */
export class InlineQueryResultVenue {
  type: 'inlineQueryResultVenue' = 'inlineQueryResultVenue';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** venue: venue — Venue result */
  venue: Venue | null = null;
  /** thumbnail: thumbnail — Result thumbnail in JPEG format; may be null */
  thumbnail: Thumbnail | null = null;
}

/** Decode a TDLib `inlineQueryResultVenue` object; null input -> null. */
export function decodeInlineQueryResultVenue(json: TdJsonRaw): InlineQueryResultVenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultVenue = new InlineQueryResultVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.venue = decodeVenue(record['venue']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  return out;
}

/** Encode a `inlineQueryResultVenue` into TDLib JSON wire shape. */
export function encodeInlineQueryResultVenue(value: InlineQueryResultVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['venue'] = value.venue === null ? null : encodeVenue(value.venue);
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  return out;
}

/** inlineQueryResultVideo = InlineQueryResult — Represents a video */
export class InlineQueryResultVideo {
  type: 'inlineQueryResultVideo' = 'inlineQueryResultVideo';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** video: video — Video */
  video: Video | null = null;
  /** title: string — Title of the video */
  title: string = '';
  /** description: string */
  description: string = '';
}

/** Decode a TDLib `inlineQueryResultVideo` object; null input -> null. */
export function decodeInlineQueryResultVideo(json: TdJsonRaw): InlineQueryResultVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultVideo = new InlineQueryResultVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.video = decodeVideo(record['video']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  return out;
}

/** Encode a `inlineQueryResultVideo` into TDLib JSON wire shape. */
export function encodeInlineQueryResultVideo(value: InlineQueryResultVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  out['title'] = value.title;
  out['description'] = value.description;
  return out;
}

/** inlineQueryResultVoiceNote = InlineQueryResult — Represents a voice note */
export class InlineQueryResultVoiceNote {
  type: 'inlineQueryResultVoiceNote' = 'inlineQueryResultVoiceNote';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** voice_note: voiceNote — Voice note */
  voice_note: VoiceNote | null = null;
  /** title: string — Title of the voice note */
  title: string = '';
}

/** Decode a TDLib `inlineQueryResultVoiceNote` object; null input -> null. */
export function decodeInlineQueryResultVoiceNote(json: TdJsonRaw): InlineQueryResultVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultVoiceNote = new InlineQueryResultVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.voice_note = decodeVoiceNote(record['voice_note']);
  out.title = asString(record['title']);
  return out;
}

/** Encode a `inlineQueryResultVoiceNote` into TDLib JSON wire shape. */
export function encodeInlineQueryResultVoiceNote(value: InlineQueryResultVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['voice_note'] = value.voice_note === null ? null : encodeVoiceNote(value.voice_note);
  out['title'] = value.title;
  return out;
}

/** inlineQueryResults = InlineQueryResults — Represents the results of the inline query. Use sendInlineQueryResultMessage to send the result of the query */
export class InlineQueryResults {
  type: 'inlineQueryResults' = 'inlineQueryResults';
  extra?: string;
  /** inline_query_id: int64 — Unique identifier of the inline query */
  inline_query_id: string = '';
  /** button: inlineQueryResultsButton — Button to be shown above inline query results; may be null */
  button: InlineQueryResultsButton | null = null;
  /** results: vector<InlineQueryResult> — Results of the query */
  results: TdInlineQueryResult[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `inlineQueryResults` object; null input -> null. */
export function decodeInlineQueryResults(json: TdJsonRaw): InlineQueryResults | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResults = new InlineQueryResults();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_query_id = asInt64(record['inline_query_id']);
  out.button = decodeInlineQueryResultsButton(record['button']);
  out.results = decodeVector(record['results'], (item: TdJsonRaw): TdInlineQueryResult => decodeTdInlineQueryResult(item) as TdInlineQueryResult);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `inlineQueryResults` into TDLib JSON wire shape. */
export function encodeInlineQueryResults(value: InlineQueryResults): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResults';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_query_id'] = value.inline_query_id;
  out['button'] = value.button === null ? null : encodeInlineQueryResultsButton(value.button);
  out['results'] = encodeVector(value.results, (item: TdInlineQueryResult | null): TdJsonRaw => item === null ? null : encodeTdInlineQueryResult(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** inlineQueryResultsButton = InlineQueryResultsButton — Represents a button to be shown above inline query results */
export class InlineQueryResultsButton {
  type: 'inlineQueryResultsButton' = 'inlineQueryResultsButton';
  extra?: string;
  /** text: string — The text of the button */
  text: string = '';
  /** type: InlineQueryResultsButtonType — Type of the button */
  type_: TdInlineQueryResultsButtonType | null = null;
}

/** Decode a TDLib `inlineQueryResultsButton` object; null input -> null. */
export function decodeInlineQueryResultsButton(json: TdJsonRaw): InlineQueryResultsButton | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultsButton = new InlineQueryResultsButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.type_ = decodeTdInlineQueryResultsButtonType(record['type']);
  return out;
}

/** Encode a `inlineQueryResultsButton` into TDLib JSON wire shape. */
export function encodeInlineQueryResultsButton(value: InlineQueryResultsButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultsButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['type'] = value.type_ === null ? null : encodeTdInlineQueryResultsButtonType(value.type_);
  return out;
}

/** inlineQueryResultsButtonTypeStartBot = InlineQueryResultsButtonType — Describes the button that opens a private chat with the bot and sends a start message to the bot with the given parameter */
export class InlineQueryResultsButtonTypeStartBot {
  type: 'inlineQueryResultsButtonTypeStartBot' = 'inlineQueryResultsButtonTypeStartBot';
  extra?: string;
  /** parameter: string — The parameter for the bot start message */
  parameter: string = '';
}

/** Decode a TDLib `inlineQueryResultsButtonTypeStartBot` object; null input -> null. */
export function decodeInlineQueryResultsButtonTypeStartBot(json: TdJsonRaw): InlineQueryResultsButtonTypeStartBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultsButtonTypeStartBot = new InlineQueryResultsButtonTypeStartBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.parameter = asString(record['parameter']);
  return out;
}

/** Encode a `inlineQueryResultsButtonTypeStartBot` into TDLib JSON wire shape. */
export function encodeInlineQueryResultsButtonTypeStartBot(value: InlineQueryResultsButtonTypeStartBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultsButtonTypeStartBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['parameter'] = value.parameter;
  return out;
}

/** inlineQueryResultsButtonTypeWebApp = InlineQueryResultsButtonType — Describes the button that opens a Web App by calling getWebAppUrl */
export class InlineQueryResultsButtonTypeWebApp {
  type: 'inlineQueryResultsButtonTypeWebApp' = 'inlineQueryResultsButtonTypeWebApp';
  extra?: string;
  /** url: string — An HTTP URL to pass to getWebAppUrl */
  url: string = '';
}

/** Decode a TDLib `inlineQueryResultsButtonTypeWebApp` object; null input -> null. */
export function decodeInlineQueryResultsButtonTypeWebApp(json: TdJsonRaw): InlineQueryResultsButtonTypeWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InlineQueryResultsButtonTypeWebApp = new InlineQueryResultsButtonTypeWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `inlineQueryResultsButtonTypeWebApp` into TDLib JSON wire shape. */
export function encodeInlineQueryResultsButtonTypeWebApp(value: InlineQueryResultsButtonTypeWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inlineQueryResultsButtonTypeWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** inputAnimation = InputAnimation — An animation to be sent */
export class InputAnimation {
  type: 'inputAnimation' = 'inputAnimation';
  extra?: string;
  /** animation: InputFile — Animation file to be sent */
  animation: TdInputFile | null = null;
  /** thumbnail: inputThumbnail — Animation thumbnail; pass null to skip thumbnail uploading */
  thumbnail: InputThumbnail | null = null;
  /** added_sticker_file_ids: vector<int32> — File identifiers of the stickers added to the animation, if applicable */
  added_sticker_file_ids: number[] = [];
  /** duration: int32 — Duration of the animation, in seconds; may be replaced by the server */
  duration: number = 0;
  /** width: int32 — Width of the animation; may be replaced by the server */
  width: number = 0;
  /** height: int32 — Height of the animation; may be replaced by the server */
  height: number = 0;
}

/** Decode a TDLib `inputAnimation` object; null input -> null. */
export function decodeInputAnimation(json: TdJsonRaw): InputAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputAnimation = new InputAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeTdInputFile(record['animation']);
  out.thumbnail = decodeInputThumbnail(record['thumbnail']);
  out.added_sticker_file_ids = decodeVector(record['added_sticker_file_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.duration = asNumber(record['duration']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  return out;
}

/** Encode a `inputAnimation` into TDLib JSON wire shape. */
export function encodeInputAnimation(value: InputAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeTdInputFile(value.animation);
  out['thumbnail'] = value.thumbnail === null ? null : encodeInputThumbnail(value.thumbnail);
  out['added_sticker_file_ids'] = encodeVector(value.added_sticker_file_ids, (item: number): TdJsonRaw => item);
  out['duration'] = value.duration;
  out['width'] = value.width;
  out['height'] = value.height;
  return out;
}

/** inputAudio = InputAudio — An audio to be sent */
export class InputAudio {
  type: 'inputAudio' = 'inputAudio';
  extra?: string;
  /** audio: InputFile — Audio file to be sent */
  audio: TdInputFile | null = null;
  /** album_cover_thumbnail: inputThumbnail — Thumbnail of the cover for the album; pass null to skip thumbnail uploading */
  album_cover_thumbnail: InputThumbnail | null = null;
  /** duration: int32 — Duration of the audio, in seconds; may be replaced by the server */
  duration: number = 0;
  /** title: string — Title of the audio; 0-64 characters; may be replaced by the server */
  title: string = '';
  /** performer: string — Performer of the audio; 0-64 characters, may be replaced by the server */
  performer: string = '';
}

/** Decode a TDLib `inputAudio` object; null input -> null. */
export function decodeInputAudio(json: TdJsonRaw): InputAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputAudio = new InputAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeTdInputFile(record['audio']);
  out.album_cover_thumbnail = decodeInputThumbnail(record['album_cover_thumbnail']);
  out.duration = asNumber(record['duration']);
  out.title = asString(record['title']);
  out.performer = asString(record['performer']);
  return out;
}

/** Encode a `inputAudio` into TDLib JSON wire shape. */
export function encodeInputAudio(value: InputAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeTdInputFile(value.audio);
  out['album_cover_thumbnail'] = value.album_cover_thumbnail === null ? null : encodeInputThumbnail(value.album_cover_thumbnail);
  out['duration'] = value.duration;
  out['title'] = value.title;
  out['performer'] = value.performer;
  return out;
}

/** inputBackgroundLocal = InputBackground — A background from a local file */
export class InputBackgroundLocal {
  type: 'inputBackgroundLocal' = 'inputBackgroundLocal';
  extra?: string;
  /** background: InputFile — Background file to use. Only inputFileLocal and inputFileGenerated are supported. The file must be in JPEG format for wallpapers and in PNG format for patterns */
  background: TdInputFile | null = null;
}

/** Decode a TDLib `inputBackgroundLocal` object; null input -> null. */
export function decodeInputBackgroundLocal(json: TdJsonRaw): InputBackgroundLocal | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputBackgroundLocal = new InputBackgroundLocal();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.background = decodeTdInputFile(record['background']);
  return out;
}

/** Encode a `inputBackgroundLocal` into TDLib JSON wire shape. */
export function encodeInputBackgroundLocal(value: InputBackgroundLocal): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputBackgroundLocal';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['background'] = value.background === null ? null : encodeTdInputFile(value.background);
  return out;
}

/** inputBackgroundPrevious = InputBackground — A background previously set in the chat; for chat backgrounds only */
export class InputBackgroundPrevious {
  type: 'inputBackgroundPrevious' = 'inputBackgroundPrevious';
  extra?: string;
  /** message_id: int53 — Identifier of the message with the background */
  message_id: number = 0;
}

/** Decode a TDLib `inputBackgroundPrevious` object; null input -> null. */
export function decodeInputBackgroundPrevious(json: TdJsonRaw): InputBackgroundPrevious | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputBackgroundPrevious = new InputBackgroundPrevious();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `inputBackgroundPrevious` into TDLib JSON wire shape. */
export function encodeInputBackgroundPrevious(value: InputBackgroundPrevious): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputBackgroundPrevious';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_id'] = value.message_id;
  return out;
}

/** inputBackgroundRemote = InputBackground — A background from the server */
export class InputBackgroundRemote {
  type: 'inputBackgroundRemote' = 'inputBackgroundRemote';
  extra?: string;
  /** background_id: int64 — The background identifier */
  background_id: string = '';
}

/** Decode a TDLib `inputBackgroundRemote` object; null input -> null. */
export function decodeInputBackgroundRemote(json: TdJsonRaw): InputBackgroundRemote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputBackgroundRemote = new InputBackgroundRemote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.background_id = asInt64(record['background_id']);
  return out;
}

/** Encode a `inputBackgroundRemote` into TDLib JSON wire shape. */
export function encodeInputBackgroundRemote(value: InputBackgroundRemote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputBackgroundRemote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['background_id'] = value.background_id;
  return out;
}

/** inputBusinessChatLink = InputBusinessChatLink — Describes a business chat link to create or edit */
export class InputBusinessChatLink {
  type: 'inputBusinessChatLink' = 'inputBusinessChatLink';
  extra?: string;
  /** text: formattedText — Message draft text that will be added to the input field */
  text: FormattedText | null = null;
  /** title: string — Link title */
  title: string = '';
}

/** Decode a TDLib `inputBusinessChatLink` object; null input -> null. */
export function decodeInputBusinessChatLink(json: TdJsonRaw): InputBusinessChatLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputBusinessChatLink = new InputBusinessChatLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.title = asString(record['title']);
  return out;
}

/** Encode a `inputBusinessChatLink` into TDLib JSON wire shape. */
export function encodeInputBusinessChatLink(value: InputBusinessChatLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputBusinessChatLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['title'] = value.title;
  return out;
}

/** inputBusinessStartPage = InputBusinessStartPage — Describes settings for a business account start page to set */
export class InputBusinessStartPage {
  type: 'inputBusinessStartPage' = 'inputBusinessStartPage';
  extra?: string;
  /** title: string — Title text of the start page; 0-getOption("business_start_page_title_length_max") characters */
  title: string = '';
  /** message: string — Message text of the start page; 0-getOption("business_start_page_message_length_max") characters */
  message: string = '';
  /** sticker: InputFile — Greeting sticker of the start page; pass null if none. The sticker must belong to a sticker set and must not be a custom emoji */
  sticker: TdInputFile | null = null;
}

/** Decode a TDLib `inputBusinessStartPage` object; null input -> null. */
export function decodeInputBusinessStartPage(json: TdJsonRaw): InputBusinessStartPage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputBusinessStartPage = new InputBusinessStartPage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.message = asString(record['message']);
  out.sticker = decodeTdInputFile(record['sticker']);
  return out;
}

/** Encode a `inputBusinessStartPage` into TDLib JSON wire shape. */
export function encodeInputBusinessStartPage(value: InputBusinessStartPage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputBusinessStartPage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['message'] = value.message;
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  return out;
}

/** inputCallDiscarded = InputCall — A just ended call */
export class InputCallDiscarded {
  type: 'inputCallDiscarded' = 'inputCallDiscarded';
  extra?: string;
  /** call_id: int32 — Identifier of the call */
  call_id: number = 0;
}

/** Decode a TDLib `inputCallDiscarded` object; null input -> null. */
export function decodeInputCallDiscarded(json: TdJsonRaw): InputCallDiscarded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputCallDiscarded = new InputCallDiscarded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call_id = asNumber(record['call_id']);
  return out;
}

/** Encode a `inputCallDiscarded` into TDLib JSON wire shape. */
export function encodeInputCallDiscarded(value: InputCallDiscarded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputCallDiscarded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call_id'] = value.call_id;
  return out;
}

/** inputCallFromMessage = InputCall — A call from a message of the type messageCall with non-zero messageCall.unique_id */
export class InputCallFromMessage {
  type: 'inputCallFromMessage' = 'inputCallFromMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the message */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
}

/** Decode a TDLib `inputCallFromMessage` object; null input -> null. */
export function decodeInputCallFromMessage(json: TdJsonRaw): InputCallFromMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputCallFromMessage = new InputCallFromMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `inputCallFromMessage` into TDLib JSON wire shape. */
export function encodeInputCallFromMessage(value: InputCallFromMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputCallFromMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** inputChatPhotoAnimation = InputChatPhoto — An animation in MPEG4 format; must be square, at most 10 seconds long, have width between 160 and 1280 and be at most 2MB in size */
export class InputChatPhotoAnimation {
  type: 'inputChatPhotoAnimation' = 'inputChatPhotoAnimation';
  extra?: string;
  /** animation: InputFile — Animation to be set as profile photo. Only inputFileLocal and inputFileGenerated are allowed */
  animation: TdInputFile | null = null;
  /** main_frame_timestamp: double — Timestamp of the frame, which will be used as static chat photo */
  main_frame_timestamp: number = 0;
}

/** Decode a TDLib `inputChatPhotoAnimation` object; null input -> null. */
export function decodeInputChatPhotoAnimation(json: TdJsonRaw): InputChatPhotoAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputChatPhotoAnimation = new InputChatPhotoAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeTdInputFile(record['animation']);
  out.main_frame_timestamp = asNumber(record['main_frame_timestamp']);
  return out;
}

/** Encode a `inputChatPhotoAnimation` into TDLib JSON wire shape. */
export function encodeInputChatPhotoAnimation(value: InputChatPhotoAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputChatPhotoAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeTdInputFile(value.animation);
  out['main_frame_timestamp'] = value.main_frame_timestamp;
  return out;
}

/** inputChatPhotoPrevious = InputChatPhoto — A previously used profile photo of the current user */
export class InputChatPhotoPrevious {
  type: 'inputChatPhotoPrevious' = 'inputChatPhotoPrevious';
  extra?: string;
  /** chat_photo_id: int64 — Identifier of the current user's profile photo to reuse */
  chat_photo_id: string = '';
}

/** Decode a TDLib `inputChatPhotoPrevious` object; null input -> null. */
export function decodeInputChatPhotoPrevious(json: TdJsonRaw): InputChatPhotoPrevious | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputChatPhotoPrevious = new InputChatPhotoPrevious();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_photo_id = asInt64(record['chat_photo_id']);
  return out;
}

/** Encode a `inputChatPhotoPrevious` into TDLib JSON wire shape. */
export function encodeInputChatPhotoPrevious(value: InputChatPhotoPrevious): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputChatPhotoPrevious';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_photo_id'] = value.chat_photo_id;
  return out;
}

/** inputChatPhotoStatic = InputChatPhoto — A static photo in JPEG format */
export class InputChatPhotoStatic {
  type: 'inputChatPhotoStatic' = 'inputChatPhotoStatic';
  extra?: string;
  /** photo: InputFile — Photo to be set as profile photo. Only inputFileLocal and inputFileGenerated are allowed */
  photo: TdInputFile | null = null;
}

/** Decode a TDLib `inputChatPhotoStatic` object; null input -> null. */
export function decodeInputChatPhotoStatic(json: TdJsonRaw): InputChatPhotoStatic | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputChatPhotoStatic = new InputChatPhotoStatic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeTdInputFile(record['photo']);
  return out;
}

/** Encode a `inputChatPhotoStatic` into TDLib JSON wire shape. */
export function encodeInputChatPhotoStatic(value: InputChatPhotoStatic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputChatPhotoStatic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeTdInputFile(value.photo);
  return out;
}

/** inputChatPhotoSticker = InputChatPhoto — A sticker on a custom background */
export class InputChatPhotoSticker {
  type: 'inputChatPhotoSticker' = 'inputChatPhotoSticker';
  extra?: string;
  /** sticker: chatPhotoSticker — Information about the sticker */
  sticker: ChatPhotoSticker | null = null;
}

/** Decode a TDLib `inputChatPhotoSticker` object; null input -> null. */
export function decodeInputChatPhotoSticker(json: TdJsonRaw): InputChatPhotoSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputChatPhotoSticker = new InputChatPhotoSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeChatPhotoSticker(record['sticker']);
  return out;
}

/** Encode a `inputChatPhotoSticker` into TDLib JSON wire shape. */
export function encodeInputChatPhotoSticker(value: InputChatPhotoSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputChatPhotoSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeChatPhotoSticker(value.sticker);
  return out;
}

/** inputChatThemeEmoji = InputChatTheme — A theme based on an emoji */
export class InputChatThemeEmoji {
  type: 'inputChatThemeEmoji' = 'inputChatThemeEmoji';
  extra?: string;
  /** name: string — Name of the theme */
  name: string = '';
}

/** Decode a TDLib `inputChatThemeEmoji` object; null input -> null. */
export function decodeInputChatThemeEmoji(json: TdJsonRaw): InputChatThemeEmoji | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputChatThemeEmoji = new InputChatThemeEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `inputChatThemeEmoji` into TDLib JSON wire shape. */
export function encodeInputChatThemeEmoji(value: InputChatThemeEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputChatThemeEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** inputChatThemeGift = InputChatTheme — A theme based on an upgraded gift When the same gift is used in another chat, theme in the previous chat is reset to default */
export class InputChatThemeGift {
  type: 'inputChatThemeGift' = 'inputChatThemeGift';
  extra?: string;
  /** name: string — Name of the upgraded gift. A gift can be used only in one chat in a time. */
  name: string = '';
}

/** Decode a TDLib `inputChatThemeGift` object; null input -> null. */
export function decodeInputChatThemeGift(json: TdJsonRaw): InputChatThemeGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputChatThemeGift = new InputChatThemeGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `inputChatThemeGift` into TDLib JSON wire shape. */
export function encodeInputChatThemeGift(value: InputChatThemeGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputChatThemeGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** inputChecklist = InputChecklist — Describes a checklist to be sent */
export class InputChecklist {
  type: 'inputChecklist' = 'inputChecklist';
  extra?: string;
  /** title: formattedText — Title of the checklist; 1-getOption("checklist_title_length_max") characters. May contain only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities */
  title: FormattedText | null = null;
  /** tasks: vector<inputChecklistTask> — List of tasks in the checklist; 1-getOption("checklist_task_count_max") tasks */
  tasks: InputChecklistTask[] = [];
  /** others_can_add_tasks: Bool — True, if other users can add tasks to the list */
  others_can_add_tasks: boolean = false;
  /** others_can_mark_tasks_as_done: Bool — True, if other users can mark tasks as done or not done */
  others_can_mark_tasks_as_done: boolean = false;
}

/** Decode a TDLib `inputChecklist` object; null input -> null. */
export function decodeInputChecklist(json: TdJsonRaw): InputChecklist | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputChecklist = new InputChecklist();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = decodeFormattedText(record['title']);
  out.tasks = decodeVector(record['tasks'], (item: TdJsonRaw): InputChecklistTask => decodeInputChecklistTask(item) as InputChecklistTask);
  out.others_can_add_tasks = asBool(record['others_can_add_tasks']);
  out.others_can_mark_tasks_as_done = asBool(record['others_can_mark_tasks_as_done']);
  return out;
}

/** Encode a `inputChecklist` into TDLib JSON wire shape. */
export function encodeInputChecklist(value: InputChecklist): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputChecklist';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title === null ? null : encodeFormattedText(value.title);
  out['tasks'] = encodeVector(value.tasks, (item: InputChecklistTask | null): TdJsonRaw => item === null ? null : encodeInputChecklistTask(item));
  out['others_can_add_tasks'] = value.others_can_add_tasks;
  out['others_can_mark_tasks_as_done'] = value.others_can_mark_tasks_as_done;
  return out;
}

/** inputChecklistTask = InputChecklistTask — Describes a task in a checklist to be sent */
export class InputChecklistTask {
  type: 'inputChecklistTask' = 'inputChecklistTask';
  extra?: string;
  /** id: int32 — Unique identifier of the task; must be positive */
  id: number = 0;
  /** text: formattedText — Text of the task; 1-getOption("checklist_task_text_length_max") characters without line feeds. May contain only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities */
  text: FormattedText | null = null;
}

/** Decode a TDLib `inputChecklistTask` object; null input -> null. */
export function decodeInputChecklistTask(json: TdJsonRaw): InputChecklistTask | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputChecklistTask = new InputChecklistTask();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `inputChecklistTask` into TDLib JSON wire shape. */
export function encodeInputChecklistTask(value: InputChecklistTask): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputChecklistTask';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** inputCredentialsApplePay = InputCredentials — Applies if a user enters new credentials using Apple Pay */
export class InputCredentialsApplePay {
  type: 'inputCredentialsApplePay' = 'inputCredentialsApplePay';
  extra?: string;
  /** data: string — JSON-encoded data with the credential identifier */
  data: string = '';
}

/** Decode a TDLib `inputCredentialsApplePay` object; null input -> null. */
export function decodeInputCredentialsApplePay(json: TdJsonRaw): InputCredentialsApplePay | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputCredentialsApplePay = new InputCredentialsApplePay();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.data = asString(record['data']);
  return out;
}

/** Encode a `inputCredentialsApplePay` into TDLib JSON wire shape. */
export function encodeInputCredentialsApplePay(value: InputCredentialsApplePay): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputCredentialsApplePay';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['data'] = value.data;
  return out;
}

/** inputCredentialsGooglePay = InputCredentials — Applies if a user enters new credentials using Google Pay */
export class InputCredentialsGooglePay {
  type: 'inputCredentialsGooglePay' = 'inputCredentialsGooglePay';
  extra?: string;
  /** data: string — JSON-encoded data with the credential identifier */
  data: string = '';
}

/** Decode a TDLib `inputCredentialsGooglePay` object; null input -> null. */
export function decodeInputCredentialsGooglePay(json: TdJsonRaw): InputCredentialsGooglePay | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputCredentialsGooglePay = new InputCredentialsGooglePay();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.data = asString(record['data']);
  return out;
}

/** Encode a `inputCredentialsGooglePay` into TDLib JSON wire shape. */
export function encodeInputCredentialsGooglePay(value: InputCredentialsGooglePay): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputCredentialsGooglePay';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['data'] = value.data;
  return out;
}

/** inputCredentialsNew = InputCredentials — Applies if a user enters new credentials on a payment provider website */
export class InputCredentialsNew {
  type: 'inputCredentialsNew' = 'inputCredentialsNew';
  extra?: string;
  /** data: string — JSON-encoded data with the credential identifier from the payment provider */
  data: string = '';
  /** allow_save: Bool — True, if the credential identifier can be saved on the server side */
  allow_save: boolean = false;
}

/** Decode a TDLib `inputCredentialsNew` object; null input -> null. */
export function decodeInputCredentialsNew(json: TdJsonRaw): InputCredentialsNew | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputCredentialsNew = new InputCredentialsNew();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.data = asString(record['data']);
  out.allow_save = asBool(record['allow_save']);
  return out;
}

/** Encode a `inputCredentialsNew` into TDLib JSON wire shape. */
export function encodeInputCredentialsNew(value: InputCredentialsNew): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputCredentialsNew';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['data'] = value.data;
  out['allow_save'] = value.allow_save;
  return out;
}

/** inputCredentialsSaved = InputCredentials — Applies if a user chooses some previously saved payment credentials. To use their previously saved credentials, the user must have a valid temporary password */
export class InputCredentialsSaved {
  type: 'inputCredentialsSaved' = 'inputCredentialsSaved';
  extra?: string;
  /** saved_credentials_id: string — Identifier of the saved credentials */
  saved_credentials_id: string = '';
}

/** Decode a TDLib `inputCredentialsSaved` object; null input -> null. */
export function decodeInputCredentialsSaved(json: TdJsonRaw): InputCredentialsSaved | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputCredentialsSaved = new InputCredentialsSaved();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_credentials_id = asString(record['saved_credentials_id']);
  return out;
}

/** Encode a `inputCredentialsSaved` into TDLib JSON wire shape. */
export function encodeInputCredentialsSaved(value: InputCredentialsSaved): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputCredentialsSaved';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_credentials_id'] = value.saved_credentials_id;
  return out;
}

/** inputDocument = InputDocument — A document (general file) to be sent */
export class InputDocument {
  type: 'inputDocument' = 'inputDocument';
  extra?: string;
  /** document: InputFile — File to be sent */
  document: TdInputFile | null = null;
  /** thumbnail: inputThumbnail — Document thumbnail; pass null to skip thumbnail uploading */
  thumbnail: InputThumbnail | null = null;
  /** disable_content_type_detection: Bool — Pass true to disable automatic file type detection and send the document as a file. Always true for files sent to secret chats */
  disable_content_type_detection: boolean = false;
}

/** Decode a TDLib `inputDocument` object; null input -> null. */
export function decodeInputDocument(json: TdJsonRaw): InputDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputDocument = new InputDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeTdInputFile(record['document']);
  out.thumbnail = decodeInputThumbnail(record['thumbnail']);
  out.disable_content_type_detection = asBool(record['disable_content_type_detection']);
  return out;
}

/** Encode a `inputDocument` into TDLib JSON wire shape. */
export function encodeInputDocument(value: InputDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeTdInputFile(value.document);
  out['thumbnail'] = value.thumbnail === null ? null : encodeInputThumbnail(value.thumbnail);
  out['disable_content_type_detection'] = value.disable_content_type_detection;
  return out;
}

/** inputFileGenerated = InputFile — A file generated by the application. The application must handle updates updateFileGenerationStart and updateFileGenerationStop to generate the file when asked by TDLib */
export class InputFileGenerated {
  type: 'inputFileGenerated' = 'inputFileGenerated';
  extra?: string;
  /** original_path: string — Local path to a file from which the file is generated. The path doesn't have to be a valid path and is used by TDLib only to detect name and MIME type of the generated file */
  original_path: string = '';
  /** conversion: string — String specifying the conversion applied to the original file; must be persistent across application restarts. Conversions beginning with '#' are reserved for internal TDLib usage */
  conversion: string = '';
  /** expected_size: int53 — Expected size of the generated file, in bytes; pass 0 if unknown */
  expected_size: number = 0;
}

/** Decode a TDLib `inputFileGenerated` object; null input -> null. */
export function decodeInputFileGenerated(json: TdJsonRaw): InputFileGenerated | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputFileGenerated = new InputFileGenerated();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.original_path = asString(record['original_path']);
  out.conversion = asString(record['conversion']);
  out.expected_size = asNumber(record['expected_size']);
  return out;
}

/** Encode a `inputFileGenerated` into TDLib JSON wire shape. */
export function encodeInputFileGenerated(value: InputFileGenerated): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputFileGenerated';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['original_path'] = value.original_path;
  out['conversion'] = value.conversion;
  out['expected_size'] = value.expected_size;
  return out;
}

/** inputFileId = InputFile — A file defined by its unique identifier */
export class InputFileId {
  type: 'inputFileId' = 'inputFileId';
  extra?: string;
  /** id: int32 — Unique file identifier */
  id: number = 0;
}

/** Decode a TDLib `inputFileId` object; null input -> null. */
export function decodeInputFileId(json: TdJsonRaw): InputFileId | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputFileId = new InputFileId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  return out;
}

/** Encode a `inputFileId` into TDLib JSON wire shape. */
export function encodeInputFileId(value: InputFileId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputFileId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  return out;
}

/** inputFileLocal = InputFile — A file defined by a local path */
export class InputFileLocal {
  type: 'inputFileLocal' = 'inputFileLocal';
  extra?: string;
  /** path: string — Local path to the file */
  path: string = '';
}

/** Decode a TDLib `inputFileLocal` object; null input -> null. */
export function decodeInputFileLocal(json: TdJsonRaw): InputFileLocal | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputFileLocal = new InputFileLocal();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.path = asString(record['path']);
  return out;
}

/** Encode a `inputFileLocal` into TDLib JSON wire shape. */
export function encodeInputFileLocal(value: InputFileLocal): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputFileLocal';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['path'] = value.path;
  return out;
}

/** inputFileRemote = InputFile — A file defined by its remote identifier. The remote identifier is guaranteed to be usable only if the corresponding file is still accessible to the user and known to TDLib. For example, if the file is from a... */
export class InputFileRemote {
  type: 'inputFileRemote' = 'inputFileRemote';
  extra?: string;
  /** id: string — Remote file identifier */
  id: string = '';
}

/** Decode a TDLib `inputFileRemote` object; null input -> null. */
export function decodeInputFileRemote(json: TdJsonRaw): InputFileRemote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputFileRemote = new InputFileRemote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  return out;
}

/** Encode a `inputFileRemote` into TDLib JSON wire shape. */
export function encodeInputFileRemote(value: InputFileRemote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputFileRemote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  return out;
}

/** inputGroupCallLink = InputGroupCall — The group call is accessible through a link */
export class InputGroupCallLink {
  type: 'inputGroupCallLink' = 'inputGroupCallLink';
  extra?: string;
  /** link: string — The link for the group call */
  link: string = '';
}

/** Decode a TDLib `inputGroupCallLink` object; null input -> null. */
export function decodeInputGroupCallLink(json: TdJsonRaw): InputGroupCallLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputGroupCallLink = new InputGroupCallLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  return out;
}

/** Encode a `inputGroupCallLink` into TDLib JSON wire shape. */
export function encodeInputGroupCallLink(value: InputGroupCallLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputGroupCallLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  return out;
}

/** inputGroupCallMessage = InputGroupCall — The group call is accessible through a message of the type messageGroupCall */
export class InputGroupCallMessage {
  type: 'inputGroupCallMessage' = 'inputGroupCallMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message of the type messageGroupCall */
  message_id: number = 0;
}

/** Decode a TDLib `inputGroupCallMessage` object; null input -> null. */
export function decodeInputGroupCallMessage(json: TdJsonRaw): InputGroupCallMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputGroupCallMessage = new InputGroupCallMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `inputGroupCallMessage` into TDLib JSON wire shape. */
export function encodeInputGroupCallMessage(value: InputGroupCallMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputGroupCallMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** inputIdentityDocument = InputIdentityDocument — An identity document to be saved to Telegram Passport */
export class InputIdentityDocument {
  type: 'inputIdentityDocument' = 'inputIdentityDocument';
  extra?: string;
  /** number: string — Document number; 1-24 characters */
  number: string = '';
  /** expiration_date: date — Document expiration date; pass null if not applicable */
  expiration_date: DateValue | null = null;
  /** front_side: InputFile — Front side of the document */
  front_side: TdInputFile | null = null;
  /** reverse_side: InputFile — Reverse side of the document; only for driver license and identity card; pass null otherwise */
  reverse_side: TdInputFile | null = null;
  /** selfie: InputFile — Selfie with the document; pass null if unavailable */
  selfie: TdInputFile | null = null;
  /** translation: vector<InputFile> — List of files containing a certified English translation of the document */
  translation: TdInputFile[] = [];
}

/** Decode a TDLib `inputIdentityDocument` object; null input -> null. */
export function decodeInputIdentityDocument(json: TdJsonRaw): InputIdentityDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputIdentityDocument = new InputIdentityDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.number = asString(record['number']);
  out.expiration_date = decodeDateValue(record['expiration_date']);
  out.front_side = decodeTdInputFile(record['front_side']);
  out.reverse_side = decodeTdInputFile(record['reverse_side']);
  out.selfie = decodeTdInputFile(record['selfie']);
  out.translation = decodeVector(record['translation'], (item: TdJsonRaw): TdInputFile => decodeTdInputFile(item) as TdInputFile);
  return out;
}

/** Encode a `inputIdentityDocument` into TDLib JSON wire shape. */
export function encodeInputIdentityDocument(value: InputIdentityDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputIdentityDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['number'] = value.number;
  out['expiration_date'] = value.expiration_date === null ? null : encodeDateValue(value.expiration_date);
  out['front_side'] = value.front_side === null ? null : encodeTdInputFile(value.front_side);
  out['reverse_side'] = value.reverse_side === null ? null : encodeTdInputFile(value.reverse_side);
  out['selfie'] = value.selfie === null ? null : encodeTdInputFile(value.selfie);
  out['translation'] = encodeVector(value.translation, (item: TdInputFile | null): TdJsonRaw => item === null ? null : encodeTdInputFile(item));
  return out;
}

/** inputInlineQueryResultAnimation = InputInlineQueryResult — Represents a link to an animated GIF or an animated (i.e., without sound) H.264/MPEG-4 AVC video */
export class InputInlineQueryResultAnimation {
  type: 'inputInlineQueryResultAnimation' = 'inputInlineQueryResultAnimation';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** title: string — Title of the query result */
  title: string = '';
  /** thumbnail_url: string — URL of the result thumbnail (JPEG, GIF, or MPEG4), if it exists */
  thumbnail_url: string = '';
  /** thumbnail_mime_type: string — MIME type of the video thumbnail. If non-empty, must be one of "image/jpeg", "image/gif" and "video/mp4" */
  thumbnail_mime_type: string = '';
  /** video_url: string — The URL of the video file (file size must not exceed 1MB) */
  video_url: string = '';
  /** video_mime_type: string — MIME type of the video file. Must be one of "image/gif" and "video/mp4" */
  video_mime_type: string = '';
  /** video_duration: int32 — Duration of the video, in seconds */
  video_duration: number = 0;
  /** video_width: int32 — Width of the video */
  video_width: number = 0;
  /** video_height: int32 — Height of the video */
  video_height: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageAnimation, inputMessageInvoice, inputMessageLiveLocation, inpu... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultAnimation` object; null input -> null. */
export function decodeInputInlineQueryResultAnimation(json: TdJsonRaw): InputInlineQueryResultAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultAnimation = new InputInlineQueryResultAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.title = asString(record['title']);
  out.thumbnail_url = asString(record['thumbnail_url']);
  out.thumbnail_mime_type = asString(record['thumbnail_mime_type']);
  out.video_url = asString(record['video_url']);
  out.video_mime_type = asString(record['video_mime_type']);
  out.video_duration = asNumber(record['video_duration']);
  out.video_width = asNumber(record['video_width']);
  out.video_height = asNumber(record['video_height']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultAnimation` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultAnimation(value: InputInlineQueryResultAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  out['thumbnail_url'] = value.thumbnail_url;
  out['thumbnail_mime_type'] = value.thumbnail_mime_type;
  out['video_url'] = value.video_url;
  out['video_mime_type'] = value.video_mime_type;
  out['video_duration'] = value.video_duration;
  out['video_width'] = value.video_width;
  out['video_height'] = value.video_height;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultArticle = InputInlineQueryResult — Represents a link to an article or web page */
export class InputInlineQueryResultArticle {
  type: 'inputInlineQueryResultArticle' = 'inputInlineQueryResultArticle';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** url: string — URL of the result, if it exists */
  url: string = '';
  /** title: string — Title of the result */
  title: string = '';
  /** description: string */
  description: string = '';
  /** thumbnail_url: string — URL of the result thumbnail, if it exists */
  thumbnail_url: string = '';
  /** thumbnail_width: int32 — Thumbnail width, if known */
  thumbnail_width: number = 0;
  /** thumbnail_height: int32 — Thumbnail height, if known */
  thumbnail_height: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageInvoice, inputMessageLiveLocation, inputMessageLocation, input... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultArticle` object; null input -> null. */
export function decodeInputInlineQueryResultArticle(json: TdJsonRaw): InputInlineQueryResultArticle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultArticle = new InputInlineQueryResultArticle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.url = asString(record['url']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  out.thumbnail_url = asString(record['thumbnail_url']);
  out.thumbnail_width = asNumber(record['thumbnail_width']);
  out.thumbnail_height = asNumber(record['thumbnail_height']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultArticle` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultArticle(value: InputInlineQueryResultArticle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultArticle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['url'] = value.url;
  out['title'] = value.title;
  out['description'] = value.description;
  out['thumbnail_url'] = value.thumbnail_url;
  out['thumbnail_width'] = value.thumbnail_width;
  out['thumbnail_height'] = value.thumbnail_height;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultAudio = InputInlineQueryResult — Represents a link to an MP3 audio file */
export class InputInlineQueryResultAudio {
  type: 'inputInlineQueryResultAudio' = 'inputInlineQueryResultAudio';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** title: string — Title of the audio file */
  title: string = '';
  /** performer: string — Performer of the audio file */
  performer: string = '';
  /** audio_url: string — The URL of the audio file */
  audio_url: string = '';
  /** audio_duration: int32 — Audio file duration, in seconds */
  audio_duration: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageAudio, inputMessageInvoice, inputMessageLiveLocation, inputMes... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultAudio` object; null input -> null. */
export function decodeInputInlineQueryResultAudio(json: TdJsonRaw): InputInlineQueryResultAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultAudio = new InputInlineQueryResultAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.title = asString(record['title']);
  out.performer = asString(record['performer']);
  out.audio_url = asString(record['audio_url']);
  out.audio_duration = asNumber(record['audio_duration']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultAudio` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultAudio(value: InputInlineQueryResultAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  out['performer'] = value.performer;
  out['audio_url'] = value.audio_url;
  out['audio_duration'] = value.audio_duration;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultContact = InputInlineQueryResult — Represents a user contact */
export class InputInlineQueryResultContact {
  type: 'inputInlineQueryResultContact' = 'inputInlineQueryResultContact';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** contact: contact — User contact */
  contact: Contact | null = null;
  /** thumbnail_url: string — URL of the result thumbnail, if it exists */
  thumbnail_url: string = '';
  /** thumbnail_width: int32 — Thumbnail width, if known */
  thumbnail_width: number = 0;
  /** thumbnail_height: int32 — Thumbnail height, if known */
  thumbnail_height: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageInvoice, inputMessageLiveLocation, inputMessageLocation, input... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultContact` object; null input -> null. */
export function decodeInputInlineQueryResultContact(json: TdJsonRaw): InputInlineQueryResultContact | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultContact = new InputInlineQueryResultContact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.contact = decodeContact(record['contact']);
  out.thumbnail_url = asString(record['thumbnail_url']);
  out.thumbnail_width = asNumber(record['thumbnail_width']);
  out.thumbnail_height = asNumber(record['thumbnail_height']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultContact` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultContact(value: InputInlineQueryResultContact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultContact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['contact'] = value.contact === null ? null : encodeContact(value.contact);
  out['thumbnail_url'] = value.thumbnail_url;
  out['thumbnail_width'] = value.thumbnail_width;
  out['thumbnail_height'] = value.thumbnail_height;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultDocument = InputInlineQueryResult — Represents a link to a file */
export class InputInlineQueryResultDocument {
  type: 'inputInlineQueryResultDocument' = 'inputInlineQueryResultDocument';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** title: string — Title of the resulting file */
  title: string = '';
  /** description: string */
  description: string = '';
  /** document_url: string — URL of the file */
  document_url: string = '';
  /** mime_type: string — MIME type of the file content; only "application/pdf" and "application/zip" are currently allowed */
  mime_type: string = '';
  /** thumbnail_url: string — The URL of the file thumbnail, if it exists */
  thumbnail_url: string = '';
  /** thumbnail_width: int32 — Width of the thumbnail */
  thumbnail_width: number = 0;
  /** thumbnail_height: int32 — Height of the thumbnail */
  thumbnail_height: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageDocument, inputMessageInvoice, inputMessageLiveLocation, input... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultDocument` object; null input -> null. */
export function decodeInputInlineQueryResultDocument(json: TdJsonRaw): InputInlineQueryResultDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultDocument = new InputInlineQueryResultDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  out.document_url = asString(record['document_url']);
  out.mime_type = asString(record['mime_type']);
  out.thumbnail_url = asString(record['thumbnail_url']);
  out.thumbnail_width = asNumber(record['thumbnail_width']);
  out.thumbnail_height = asNumber(record['thumbnail_height']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultDocument` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultDocument(value: InputInlineQueryResultDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  out['description'] = value.description;
  out['document_url'] = value.document_url;
  out['mime_type'] = value.mime_type;
  out['thumbnail_url'] = value.thumbnail_url;
  out['thumbnail_width'] = value.thumbnail_width;
  out['thumbnail_height'] = value.thumbnail_height;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultGame = InputInlineQueryResult — Represents a game */
export class InputInlineQueryResultGame {
  type: 'inputInlineQueryResultGame' = 'inputInlineQueryResultGame';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** game_short_name: string — Short name of the game */
  game_short_name: string = '';
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
}

/** Decode a TDLib `inputInlineQueryResultGame` object; null input -> null. */
export function decodeInputInlineQueryResultGame(json: TdJsonRaw): InputInlineQueryResultGame | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultGame = new InputInlineQueryResultGame();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.game_short_name = asString(record['game_short_name']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  return out;
}

/** Encode a `inputInlineQueryResultGame` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultGame(value: InputInlineQueryResultGame): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultGame';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['game_short_name'] = value.game_short_name;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  return out;
}

/** inputInlineQueryResultLocation = InputInlineQueryResult — Represents a point on the map */
export class InputInlineQueryResultLocation {
  type: 'inputInlineQueryResultLocation' = 'inputInlineQueryResultLocation';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** location: location — Location result */
  location: Location | null = null;
  /** live_period: int32 — Amount of time relative to the message sent time until the location can be updated, in seconds */
  live_period: number = 0;
  /** title: string — Title of the result */
  title: string = '';
  /** thumbnail_url: string — URL of the result thumbnail, if it exists */
  thumbnail_url: string = '';
  /** thumbnail_width: int32 — Thumbnail width, if known */
  thumbnail_width: number = 0;
  /** thumbnail_height: int32 — Thumbnail height, if known */
  thumbnail_height: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageInvoice, inputMessageLiveLocation, inputMessageLocation, input... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultLocation` object; null input -> null. */
export function decodeInputInlineQueryResultLocation(json: TdJsonRaw): InputInlineQueryResultLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultLocation = new InputInlineQueryResultLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.location = decodeLocation(record['location']);
  out.live_period = asNumber(record['live_period']);
  out.title = asString(record['title']);
  out.thumbnail_url = asString(record['thumbnail_url']);
  out.thumbnail_width = asNumber(record['thumbnail_width']);
  out.thumbnail_height = asNumber(record['thumbnail_height']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultLocation` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultLocation(value: InputInlineQueryResultLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['live_period'] = value.live_period;
  out['title'] = value.title;
  out['thumbnail_url'] = value.thumbnail_url;
  out['thumbnail_width'] = value.thumbnail_width;
  out['thumbnail_height'] = value.thumbnail_height;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultPhoto = InputInlineQueryResult — Represents link to a JPEG image */
export class InputInlineQueryResultPhoto {
  type: 'inputInlineQueryResultPhoto' = 'inputInlineQueryResultPhoto';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** title: string — Title of the result, if known */
  title: string = '';
  /** description: string */
  description: string = '';
  /** thumbnail_url: string — URL of the photo thumbnail, if it exists */
  thumbnail_url: string = '';
  /** photo_url: string — The URL of the JPEG photo (photo size must not exceed 5MB) */
  photo_url: string = '';
  /** photo_width: int32 — Width of the photo */
  photo_width: number = 0;
  /** photo_height: int32 — Height of the photo */
  photo_height: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessagePhoto, inputMessageInvoice, inputMessageLiveLocation, inputMes... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultPhoto` object; null input -> null. */
export function decodeInputInlineQueryResultPhoto(json: TdJsonRaw): InputInlineQueryResultPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultPhoto = new InputInlineQueryResultPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  out.thumbnail_url = asString(record['thumbnail_url']);
  out.photo_url = asString(record['photo_url']);
  out.photo_width = asNumber(record['photo_width']);
  out.photo_height = asNumber(record['photo_height']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultPhoto` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultPhoto(value: InputInlineQueryResultPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  out['description'] = value.description;
  out['thumbnail_url'] = value.thumbnail_url;
  out['photo_url'] = value.photo_url;
  out['photo_width'] = value.photo_width;
  out['photo_height'] = value.photo_height;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultSticker = InputInlineQueryResult — Represents a link to a WEBP, TGS, or WEBM sticker */
export class InputInlineQueryResultSticker {
  type: 'inputInlineQueryResultSticker' = 'inputInlineQueryResultSticker';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** thumbnail_url: string — URL of the sticker thumbnail, if it exists */
  thumbnail_url: string = '';
  /** sticker_url: string — The URL of the WEBP, TGS, or WEBM sticker (sticker file size must not exceed 5MB) */
  sticker_url: string = '';
  /** sticker_width: int32 — Width of the sticker */
  sticker_width: number = 0;
  /** sticker_height: int32 — Height of the sticker */
  sticker_height: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageSticker, inputMessageInvoice, inputMessageLiveLocation, inputM... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultSticker` object; null input -> null. */
export function decodeInputInlineQueryResultSticker(json: TdJsonRaw): InputInlineQueryResultSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultSticker = new InputInlineQueryResultSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.thumbnail_url = asString(record['thumbnail_url']);
  out.sticker_url = asString(record['sticker_url']);
  out.sticker_width = asNumber(record['sticker_width']);
  out.sticker_height = asNumber(record['sticker_height']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultSticker` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultSticker(value: InputInlineQueryResultSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['thumbnail_url'] = value.thumbnail_url;
  out['sticker_url'] = value.sticker_url;
  out['sticker_width'] = value.sticker_width;
  out['sticker_height'] = value.sticker_height;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultVenue = InputInlineQueryResult — Represents information about a venue */
export class InputInlineQueryResultVenue {
  type: 'inputInlineQueryResultVenue' = 'inputInlineQueryResultVenue';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** venue: venue — Venue result */
  venue: Venue | null = null;
  /** thumbnail_url: string — URL of the result thumbnail, if it exists */
  thumbnail_url: string = '';
  /** thumbnail_width: int32 — Thumbnail width, if known */
  thumbnail_width: number = 0;
  /** thumbnail_height: int32 — Thumbnail height, if known */
  thumbnail_height: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageInvoice, inputMessageLiveLocation, inputMessageLocation, input... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultVenue` object; null input -> null. */
export function decodeInputInlineQueryResultVenue(json: TdJsonRaw): InputInlineQueryResultVenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultVenue = new InputInlineQueryResultVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.venue = decodeVenue(record['venue']);
  out.thumbnail_url = asString(record['thumbnail_url']);
  out.thumbnail_width = asNumber(record['thumbnail_width']);
  out.thumbnail_height = asNumber(record['thumbnail_height']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultVenue` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultVenue(value: InputInlineQueryResultVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['venue'] = value.venue === null ? null : encodeVenue(value.venue);
  out['thumbnail_url'] = value.thumbnail_url;
  out['thumbnail_width'] = value.thumbnail_width;
  out['thumbnail_height'] = value.thumbnail_height;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultVideo = InputInlineQueryResult — Represents a link to a page containing an embedded video player or a video file */
export class InputInlineQueryResultVideo {
  type: 'inputInlineQueryResultVideo' = 'inputInlineQueryResultVideo';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** title: string — Title of the result */
  title: string = '';
  /** description: string */
  description: string = '';
  /** thumbnail_url: string — The URL of the video thumbnail (JPEG), if it exists */
  thumbnail_url: string = '';
  /** video_url: string — URL of the embedded video player or video file */
  video_url: string = '';
  /** mime_type: string — MIME type of the content of the video URL, only "text/html" or "video/mp4" are currently supported */
  mime_type: string = '';
  /** video_width: int32 — Width of the video */
  video_width: number = 0;
  /** video_height: int32 — Height of the video */
  video_height: number = 0;
  /** video_duration: int32 — Video duration, in seconds */
  video_duration: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageVideo, inputMessageInvoice, inputMessageLiveLocation, inputMes... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultVideo` object; null input -> null. */
export function decodeInputInlineQueryResultVideo(json: TdJsonRaw): InputInlineQueryResultVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultVideo = new InputInlineQueryResultVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  out.thumbnail_url = asString(record['thumbnail_url']);
  out.video_url = asString(record['video_url']);
  out.mime_type = asString(record['mime_type']);
  out.video_width = asNumber(record['video_width']);
  out.video_height = asNumber(record['video_height']);
  out.video_duration = asNumber(record['video_duration']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultVideo` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultVideo(value: InputInlineQueryResultVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  out['description'] = value.description;
  out['thumbnail_url'] = value.thumbnail_url;
  out['video_url'] = value.video_url;
  out['mime_type'] = value.mime_type;
  out['video_width'] = value.video_width;
  out['video_height'] = value.video_height;
  out['video_duration'] = value.video_duration;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInlineQueryResultVoiceNote = InputInlineQueryResult — Represents a link to an opus-encoded audio file within an OGG container, single channel audio */
export class InputInlineQueryResultVoiceNote {
  type: 'inputInlineQueryResultVoiceNote' = 'inputInlineQueryResultVoiceNote';
  extra?: string;
  /** id: string — Unique identifier of the query result */
  id: string = '';
  /** title: string — Title of the voice note */
  title: string = '';
  /** voice_note_url: string — The URL of the voice note file */
  voice_note_url: string = '';
  /** voice_note_duration: int32 — Duration of the voice note, in seconds */
  voice_note_duration: number = 0;
  /** reply_markup: ReplyMarkup — The message reply markup; pass null if none. Must be of type replyMarkupInlineKeyboard or null */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageRichMessage, inputMessageVoiceNote, inputMessageInvoice, inputMessageLiveLocation, inpu... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputInlineQueryResultVoiceNote` object; null input -> null. */
export function decodeInputInlineQueryResultVoiceNote(json: TdJsonRaw): InputInlineQueryResultVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInlineQueryResultVoiceNote = new InputInlineQueryResultVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.title = asString(record['title']);
  out.voice_note_url = asString(record['voice_note_url']);
  out.voice_note_duration = asNumber(record['voice_note_duration']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `inputInlineQueryResultVoiceNote` into TDLib JSON wire shape. */
export function encodeInputInlineQueryResultVoiceNote(value: InputInlineQueryResultVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInlineQueryResultVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['title'] = value.title;
  out['voice_note_url'] = value.voice_note_url;
  out['voice_note_duration'] = value.voice_note_duration;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** inputInvoiceMessage = InputInvoice — An invoice from a message of the type messageInvoice or paid media purchase from messagePaidMedia */
export class InputInvoiceMessage {
  type: 'inputInvoiceMessage' = 'inputInvoiceMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the message */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier. Use messageProperties.can_be_paid to check whether the message can be used in the method */
  message_id: number = 0;
}

/** Decode a TDLib `inputInvoiceMessage` object; null input -> null. */
export function decodeInputInvoiceMessage(json: TdJsonRaw): InputInvoiceMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInvoiceMessage = new InputInvoiceMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `inputInvoiceMessage` into TDLib JSON wire shape. */
export function encodeInputInvoiceMessage(value: InputInvoiceMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInvoiceMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** inputInvoiceName = InputInvoice — An invoice from a link of the type internalLinkTypeInvoice */
export class InputInvoiceName {
  type: 'inputInvoiceName' = 'inputInvoiceName';
  extra?: string;
  /** name: string — Name of the invoice */
  name: string = '';
}

/** Decode a TDLib `inputInvoiceName` object; null input -> null. */
export function decodeInputInvoiceName(json: TdJsonRaw): InputInvoiceName | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInvoiceName = new InputInvoiceName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `inputInvoiceName` into TDLib JSON wire shape. */
export function encodeInputInvoiceName(value: InputInvoiceName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInvoiceName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** inputInvoiceTelegram = InputInvoice — An invoice for a payment toward Telegram; must not be used in the in-store apps */
export class InputInvoiceTelegram {
  type: 'inputInvoiceTelegram' = 'inputInvoiceTelegram';
  extra?: string;
  /** purpose: TelegramPaymentPurpose — Transaction purpose */
  purpose: TdTelegramPaymentPurpose | null = null;
}

/** Decode a TDLib `inputInvoiceTelegram` object; null input -> null. */
export function decodeInputInvoiceTelegram(json: TdJsonRaw): InputInvoiceTelegram | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputInvoiceTelegram = new InputInvoiceTelegram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.purpose = decodeTdTelegramPaymentPurpose(record['purpose']);
  return out;
}

/** Encode a `inputInvoiceTelegram` into TDLib JSON wire shape. */
export function encodeInputInvoiceTelegram(value: InputInvoiceTelegram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputInvoiceTelegram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['purpose'] = value.purpose === null ? null : encodeTdTelegramPaymentPurpose(value.purpose);
  return out;
}

/** inputMessageAnimation = InputMessageContent — An animation message (GIF-style). */
export class InputMessageAnimation {
  type: 'inputMessageAnimation' = 'inputMessageAnimation';
  extra?: string;
  /** animation: inputAnimation — The animation to be sent */
  animation: InputAnimation | null = null;
  /** caption: formattedText — Animation caption; pass null to use an empty caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — True, if the caption must be shown above the animation; otherwise, the caption must be shown below the animation; not supported in secret chats */
  show_caption_above_media: boolean = false;
  /** has_spoiler: Bool — True, if the animation preview must be covered by a spoiler animation; not supported in secret chats */
  has_spoiler: boolean = false;
}

/** Decode a TDLib `inputMessageAnimation` object; null input -> null. */
export function decodeInputMessageAnimation(json: TdJsonRaw): InputMessageAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageAnimation = new InputMessageAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeInputAnimation(record['animation']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  out.has_spoiler = asBool(record['has_spoiler']);
  return out;
}

/** Encode a `inputMessageAnimation` into TDLib JSON wire shape. */
export function encodeInputMessageAnimation(value: InputMessageAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeInputAnimation(value.animation);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  out['has_spoiler'] = value.has_spoiler;
  return out;
}

/** inputMessageAudio = InputMessageContent — An audio message */
export class InputMessageAudio {
  type: 'inputMessageAudio' = 'inputMessageAudio';
  extra?: string;
  /** audio: inputAudio — Audio to be sent */
  audio: InputAudio | null = null;
  /** caption: formattedText — Audio caption; pass null to use an empty caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
}

/** Decode a TDLib `inputMessageAudio` object; null input -> null. */
export function decodeInputMessageAudio(json: TdJsonRaw): InputMessageAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageAudio = new InputMessageAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeInputAudio(record['audio']);
  out.caption = decodeFormattedText(record['caption']);
  return out;
}

/** Encode a `inputMessageAudio` into TDLib JSON wire shape. */
export function encodeInputMessageAudio(value: InputMessageAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeInputAudio(value.audio);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  return out;
}

/** inputMessageChecklist = InputMessageContent — A message with a checklist. Checklists can't be sent to secret chats, channel chats and channel direct messages chats; for Telegram Premium users only */
export class InputMessageChecklist {
  type: 'inputMessageChecklist' = 'inputMessageChecklist';
  extra?: string;
  /** checklist: inputChecklist — The checklist to send */
  checklist: InputChecklist | null = null;
}

/** Decode a TDLib `inputMessageChecklist` object; null input -> null. */
export function decodeInputMessageChecklist(json: TdJsonRaw): InputMessageChecklist | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageChecklist = new InputMessageChecklist();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.checklist = decodeInputChecklist(record['checklist']);
  return out;
}

/** Encode a `inputMessageChecklist` into TDLib JSON wire shape. */
export function encodeInputMessageChecklist(value: InputMessageChecklist): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageChecklist';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['checklist'] = value.checklist === null ? null : encodeInputChecklist(value.checklist);
  return out;
}

/** inputMessageContact = InputMessageContent — A message containing a user contact */
export class InputMessageContact {
  type: 'inputMessageContact' = 'inputMessageContact';
  extra?: string;
  /** contact: contact — Contact to send */
  contact: Contact | null = null;
}

/** Decode a TDLib `inputMessageContact` object; null input -> null. */
export function decodeInputMessageContact(json: TdJsonRaw): InputMessageContact | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageContact = new InputMessageContact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.contact = decodeContact(record['contact']);
  return out;
}

/** Encode a `inputMessageContact` into TDLib JSON wire shape. */
export function encodeInputMessageContact(value: InputMessageContact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageContact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['contact'] = value.contact === null ? null : encodeContact(value.contact);
  return out;
}

/** inputMessageDice = InputMessageContent — A dice message */
export class InputMessageDice {
  type: 'inputMessageDice' = 'inputMessageDice';
  extra?: string;
  /** emoji: string — Emoji on which the dice throw animation is based */
  emoji: string = '';
  /** clear_draft: Bool — Pass true to delete message draft in the chat */
  clear_draft: boolean = false;
}

/** Decode a TDLib `inputMessageDice` object; null input -> null. */
export function decodeInputMessageDice(json: TdJsonRaw): InputMessageDice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageDice = new InputMessageDice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji = asString(record['emoji']);
  out.clear_draft = asBool(record['clear_draft']);
  return out;
}

/** Encode a `inputMessageDice` into TDLib JSON wire shape. */
export function encodeInputMessageDice(value: InputMessageDice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageDice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji'] = value.emoji;
  out['clear_draft'] = value.clear_draft;
  return out;
}

/** inputMessageDocument = InputMessageContent — A document message (general file) */
export class InputMessageDocument {
  type: 'inputMessageDocument' = 'inputMessageDocument';
  extra?: string;
  /** document: inputDocument — Document to be sent */
  document: InputDocument | null = null;
  /** caption: formattedText — Document caption; pass null to use an empty caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
}

/** Decode a TDLib `inputMessageDocument` object; null input -> null. */
export function decodeInputMessageDocument(json: TdJsonRaw): InputMessageDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageDocument = new InputMessageDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeInputDocument(record['document']);
  out.caption = decodeFormattedText(record['caption']);
  return out;
}

/** Encode a `inputMessageDocument` into TDLib JSON wire shape. */
export function encodeInputMessageDocument(value: InputMessageDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeInputDocument(value.document);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  return out;
}

/** inputMessageForwarded = InputMessageContent — A forwarded message */
export class InputMessageForwarded {
  type: 'inputMessageForwarded' = 'inputMessageForwarded';
  extra?: string;
  /** from_chat_id: int53 — Identifier for the chat this forwarded message came from */
  from_chat_id: number = 0;
  /** message_id: int53 — Identifier of the message to forward. A message can be forwarded only if messageProperties.can_be_forwarded */
  message_id: number = 0;
  /** in_game_share: Bool — Pass true if a game message is being shared from a launched game; applies only to game messages */
  in_game_share: boolean = false;
  /** replace_video_start_timestamp: Bool — Pass true to replace video start timestamp in the forwarded message */
  replace_video_start_timestamp: boolean = false;
  /** new_video_start_timestamp: int32 — The new video start timestamp; ignored if replace_video_start_timestamp == false */
  new_video_start_timestamp: number = 0;
  /** copy_options: messageCopyOptions — Options to be used to copy content of the message without reference to the original sender; pass null to forward the message as usual */
  copy_options: MessageCopyOptions | null = null;
}

/** Decode a TDLib `inputMessageForwarded` object; null input -> null. */
export function decodeInputMessageForwarded(json: TdJsonRaw): InputMessageForwarded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageForwarded = new InputMessageForwarded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.from_chat_id = asNumber(record['from_chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.in_game_share = asBool(record['in_game_share']);
  out.replace_video_start_timestamp = asBool(record['replace_video_start_timestamp']);
  out.new_video_start_timestamp = asNumber(record['new_video_start_timestamp']);
  out.copy_options = decodeMessageCopyOptions(record['copy_options']);
  return out;
}

/** Encode a `inputMessageForwarded` into TDLib JSON wire shape. */
export function encodeInputMessageForwarded(value: InputMessageForwarded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageForwarded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['from_chat_id'] = value.from_chat_id;
  out['message_id'] = value.message_id;
  out['in_game_share'] = value.in_game_share;
  out['replace_video_start_timestamp'] = value.replace_video_start_timestamp;
  out['new_video_start_timestamp'] = value.new_video_start_timestamp;
  out['copy_options'] = value.copy_options === null ? null : encodeMessageCopyOptions(value.copy_options);
  return out;
}

/** inputMessageGame = InputMessageContent — A message with a game; not supported for channels or secret chats */
export class InputMessageGame {
  type: 'inputMessageGame' = 'inputMessageGame';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the bot that owns the game */
  bot_user_id: number = 0;
  /** game_short_name: string — Short name of the game */
  game_short_name: string = '';
}

/** Decode a TDLib `inputMessageGame` object; null input -> null. */
export function decodeInputMessageGame(json: TdJsonRaw): InputMessageGame | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageGame = new InputMessageGame();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.game_short_name = asString(record['game_short_name']);
  return out;
}

/** Encode a `inputMessageGame` into TDLib JSON wire shape. */
export function encodeInputMessageGame(value: InputMessageGame): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageGame';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['game_short_name'] = value.game_short_name;
  return out;
}

/** inputMessageInvoice = InputMessageContent — A message with an invoice; can be used only by bots */
export class InputMessageInvoice {
  type: 'inputMessageInvoice' = 'inputMessageInvoice';
  extra?: string;
  /** invoice: invoice — Invoice */
  invoice: Invoice | null = null;
  /** title: string — Product title; 1-32 characters */
  title: string = '';
  /** description: string */
  description: string = '';
  /** photo_url: string — Product photo URL; optional */
  photo_url: string = '';
  /** photo_size: int32 — Product photo size */
  photo_size: number = 0;
  /** photo_width: int32 — Product photo width */
  photo_width: number = 0;
  /** photo_height: int32 — Product photo height */
  photo_height: number = 0;
  /** payload: bytes — The invoice payload */
  payload: string = '';
  /** provider_token: string — Payment provider token; may be empty for payments in Telegram Stars */
  provider_token: string = '';
  /** provider_data: string — JSON-encoded data about the invoice, which will be shared with the payment provider */
  provider_data: string = '';
  /** start_parameter: string — Unique invoice bot deep link parameter for the generation of this invoice. If empty, it would be possible to pay directly from forwards of the invoice message */
  start_parameter: string = '';
  /** paid_media: inputPaidMedia — The content of paid media attached to the invoice; pass null if none */
  paid_media: InputPaidMedia | null = null;
  /** paid_media_caption: formattedText — Paid media caption; pass null to use an empty caption; 0-getOption("message_caption_length_max") characters */
  paid_media_caption: FormattedText | null = null;
}

/** Decode a TDLib `inputMessageInvoice` object; null input -> null. */
export function decodeInputMessageInvoice(json: TdJsonRaw): InputMessageInvoice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageInvoice = new InputMessageInvoice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invoice = decodeInvoice(record['invoice']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  out.photo_url = asString(record['photo_url']);
  out.photo_size = asNumber(record['photo_size']);
  out.photo_width = asNumber(record['photo_width']);
  out.photo_height = asNumber(record['photo_height']);
  out.payload = asString(record['payload']);
  out.provider_token = asString(record['provider_token']);
  out.provider_data = asString(record['provider_data']);
  out.start_parameter = asString(record['start_parameter']);
  out.paid_media = decodeInputPaidMedia(record['paid_media']);
  out.paid_media_caption = decodeFormattedText(record['paid_media_caption']);
  return out;
}

/** Encode a `inputMessageInvoice` into TDLib JSON wire shape. */
export function encodeInputMessageInvoice(value: InputMessageInvoice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageInvoice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invoice'] = value.invoice === null ? null : encodeInvoice(value.invoice);
  out['title'] = value.title;
  out['description'] = value.description;
  out['photo_url'] = value.photo_url;
  out['photo_size'] = value.photo_size;
  out['photo_width'] = value.photo_width;
  out['photo_height'] = value.photo_height;
  out['payload'] = value.payload;
  out['provider_token'] = value.provider_token;
  out['provider_data'] = value.provider_data;
  out['start_parameter'] = value.start_parameter;
  out['paid_media'] = value.paid_media === null ? null : encodeInputPaidMedia(value.paid_media);
  out['paid_media_caption'] = value.paid_media_caption === null ? null : encodeFormattedText(value.paid_media_caption);
  return out;
}

/** inputMessageLiveLocation = InputMessageContent — A message with a live location */
export class InputMessageLiveLocation {
  type: 'inputMessageLiveLocation' = 'inputMessageLiveLocation';
  extra?: string;
  /** location: liveLocation — Initial state of the live location to be sent. Live period must be equal to 0x7FFFFFFF for permanent live locations, or between 60 and 86400 */
  location: LiveLocation | null = null;
}

/** Decode a TDLib `inputMessageLiveLocation` object; null input -> null. */
export function decodeInputMessageLiveLocation(json: TdJsonRaw): InputMessageLiveLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageLiveLocation = new InputMessageLiveLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLiveLocation(record['location']);
  return out;
}

/** Encode a `inputMessageLiveLocation` into TDLib JSON wire shape. */
export function encodeInputMessageLiveLocation(value: InputMessageLiveLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageLiveLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLiveLocation(value.location);
  return out;
}

/** inputMessageLocation = InputMessageContent — A message with a location */
export class InputMessageLocation {
  type: 'inputMessageLocation' = 'inputMessageLocation';
  extra?: string;
  /** location: location — Location to be sent */
  location: Location | null = null;
}

/** Decode a TDLib `inputMessageLocation` object; null input -> null. */
export function decodeInputMessageLocation(json: TdJsonRaw): InputMessageLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageLocation = new InputMessageLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  return out;
}

/** Encode a `inputMessageLocation` into TDLib JSON wire shape. */
export function encodeInputMessageLocation(value: InputMessageLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  return out;
}

/** inputMessagePaidMedia = InputMessageContent — A message with paid media; can be used only in channel chats with supergroupFullInfo.has_paid_media_allowed */
export class InputMessagePaidMedia {
  type: 'inputMessagePaidMedia' = 'inputMessagePaidMedia';
  extra?: string;
  /** star_count: int53 — The number of Telegram Stars that must be paid to see the media; 1-getOption("paid_media_message_star_count_max") */
  star_count: number = 0;
  /** paid_media: vector<inputPaidMedia> — The content of the paid media */
  paid_media: InputPaidMedia[] = [];
  /** caption: formattedText — Message caption; pass null to use an empty caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — True, if the caption must be shown above the media; otherwise, the caption must be shown below the media; not supported in secret chats */
  show_caption_above_media: boolean = false;
  /** payload: string — Bot-provided data for the paid media; bots only */
  payload: string = '';
}

/** Decode a TDLib `inputMessagePaidMedia` object; null input -> null. */
export function decodeInputMessagePaidMedia(json: TdJsonRaw): InputMessagePaidMedia | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessagePaidMedia = new InputMessagePaidMedia();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.paid_media = decodeVector(record['paid_media'], (item: TdJsonRaw): InputPaidMedia => decodeInputPaidMedia(item) as InputPaidMedia);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  out.payload = asString(record['payload']);
  return out;
}

/** Encode a `inputMessagePaidMedia` into TDLib JSON wire shape. */
export function encodeInputMessagePaidMedia(value: InputMessagePaidMedia): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessagePaidMedia';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['paid_media'] = encodeVector(value.paid_media, (item: InputPaidMedia | null): TdJsonRaw => item === null ? null : encodeInputPaidMedia(item));
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  out['payload'] = value.payload;
  return out;
}

/** inputMessagePhoto = InputMessageContent — A photo message */
export class InputMessagePhoto {
  type: 'inputMessagePhoto' = 'inputMessagePhoto';
  extra?: string;
  /** photo: inputPhoto — Photo to be sent */
  photo: InputPhoto | null = null;
  /** caption: formattedText — Photo caption; pass null to use an empty caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — True, if the caption must be shown above the photo; otherwise, the caption must be shown below the photo; not supported in secret chats */
  show_caption_above_media: boolean = false;
  /** self_destruct_type: MessageSelfDestructType — Photo self-destruct type; pass null if none; private chats only */
  self_destruct_type: TdMessageSelfDestructType | null = null;
  /** has_spoiler: Bool — True, if the photo preview must be covered by a spoiler animation; not supported in secret chats */
  has_spoiler: boolean = false;
}

/** Decode a TDLib `inputMessagePhoto` object; null input -> null. */
export function decodeInputMessagePhoto(json: TdJsonRaw): InputMessagePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessagePhoto = new InputMessagePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeInputPhoto(record['photo']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  out.self_destruct_type = decodeTdMessageSelfDestructType(record['self_destruct_type']);
  out.has_spoiler = asBool(record['has_spoiler']);
  return out;
}

/** Encode a `inputMessagePhoto` into TDLib JSON wire shape. */
export function encodeInputMessagePhoto(value: InputMessagePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessagePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeInputPhoto(value.photo);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  out['self_destruct_type'] = value.self_destruct_type === null ? null : encodeTdMessageSelfDestructType(value.self_destruct_type);
  out['has_spoiler'] = value.has_spoiler;
  return out;
}

/** inputMessagePoll = InputMessageContent — A message with a poll. Polls can't be sent to secret chats and channel direct messages chats. Polls can be sent to a private chat only if the chat is a chat with a bot or the Saved Messages chat i... */
export class InputMessagePoll {
  type: 'inputMessagePoll' = 'inputMessagePoll';
  extra?: string;
  /** question: formattedText — Poll question; 1-255 characters (up to 300 characters for bots). Only custom emoji entities are allowed to be added and only by Premium users */
  question: FormattedText | null = null;
  /** options: vector<inputPollOption> — List of poll answer options; 1-getOption("poll_answer_count_max") options */
  options: InputPollOption[] = [];
  /** description: formattedText */
  description: FormattedText | null = null;
  /** media: InputPollMedia — Media attached to the poll; pass null if none. Must be one of the following types: inputPollMediaAnimation, inputPollMediaAudio, inputPollMediaDocument, inputPollMediaLocation, */
  media: TdInputPollMedia | null = null;
  /** is_anonymous: Bool — True, if the poll voters are anonymous. Non-anonymous polls can't be sent or forwarded to channels */
  is_anonymous: boolean = false;
  /** allows_multiple_answers: Bool — True, if multiple answer options can be chosen simultaneously */
  allows_multiple_answers: boolean = false;
  /** allows_revoting: Bool — True, if the poll can be answered multiple times */
  allows_revoting: boolean = false;
  /** members_only: Bool — True, if only the users that are members of the chat for more than a day will be able to vote; for channel chats only */
  members_only: boolean = false;
  /** country_codes: vector<string> — The list of two-letter ISO 3166-1 alpha-2 codes of countries, users from which will be able to vote; for channel chats only. If empty, then all users can participate in the poll. */
  country_codes: string[] = [];
  /** shuffle_options: Bool — True, if poll options must be shown in a fixed random order */
  shuffle_options: boolean = false;
  /** hide_results_until_closes: Bool — True, if the poll results will appear only after the poll closes */
  hide_results_until_closes: boolean = false;
  /** type: InputPollType — Type of the poll */
  type_: TdInputPollType | null = null;
  /** open_period: int32 — Amount of time the poll will be active after creation, in seconds; 0-getOption("poll_open_period_max"); pass 0 if not specified */
  open_period: number = 0;
  /** close_date: int32 — Point in time (Unix timestamp) when the poll will automatically be closed; must be 0-getOption("poll_open_period_max") seconds in the future; pass 0 if not specified */
  close_date: number = 0;
  /** is_closed: Bool — True, if the poll needs to be sent already closed; for bots only */
  is_closed: boolean = false;
}

/** Decode a TDLib `inputMessagePoll` object; null input -> null. */
export function decodeInputMessagePoll(json: TdJsonRaw): InputMessagePoll | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessagePoll = new InputMessagePoll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.question = decodeFormattedText(record['question']);
  out.options = decodeVector(record['options'], (item: TdJsonRaw): InputPollOption => decodeInputPollOption(item) as InputPollOption);
  out.description = decodeFormattedText(record['description']);
  out.media = decodeTdInputPollMedia(record['media']);
  out.is_anonymous = asBool(record['is_anonymous']);
  out.allows_multiple_answers = asBool(record['allows_multiple_answers']);
  out.allows_revoting = asBool(record['allows_revoting']);
  out.members_only = asBool(record['members_only']);
  out.country_codes = decodeVector(record['country_codes'], (item: TdJsonRaw): string => asString(item));
  out.shuffle_options = asBool(record['shuffle_options']);
  out.hide_results_until_closes = asBool(record['hide_results_until_closes']);
  out.type_ = decodeTdInputPollType(record['type']);
  out.open_period = asNumber(record['open_period']);
  out.close_date = asNumber(record['close_date']);
  out.is_closed = asBool(record['is_closed']);
  return out;
}

/** Encode a `inputMessagePoll` into TDLib JSON wire shape. */
export function encodeInputMessagePoll(value: InputMessagePoll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessagePoll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['question'] = value.question === null ? null : encodeFormattedText(value.question);
  out['options'] = encodeVector(value.options, (item: InputPollOption | null): TdJsonRaw => item === null ? null : encodeInputPollOption(item));
  out['description'] = value.description === null ? null : encodeFormattedText(value.description);
  out['media'] = value.media === null ? null : encodeTdInputPollMedia(value.media);
  out['is_anonymous'] = value.is_anonymous;
  out['allows_multiple_answers'] = value.allows_multiple_answers;
  out['allows_revoting'] = value.allows_revoting;
  out['members_only'] = value.members_only;
  out['country_codes'] = encodeVector(value.country_codes, (item: string): TdJsonRaw => item);
  out['shuffle_options'] = value.shuffle_options;
  out['hide_results_until_closes'] = value.hide_results_until_closes;
  out['type'] = value.type_ === null ? null : encodeTdInputPollType(value.type_);
  out['open_period'] = value.open_period;
  out['close_date'] = value.close_date;
  out['is_closed'] = value.is_closed;
  return out;
}

/** inputMessageReplyToEphemeralMessage = InputMessageReplyTo — Describes an ephemeral message to be replied; for bots only */
export class InputMessageReplyToEphemeralMessage {
  type: 'inputMessageReplyToEphemeralMessage' = 'inputMessageReplyToEphemeralMessage';
  extra?: string;
  /** ephemeral_message_id: int32 — The identifier of the ephemeral message to be replied */
  ephemeral_message_id: number = 0;
}

/** Decode a TDLib `inputMessageReplyToEphemeralMessage` object; null input -> null. */
export function decodeInputMessageReplyToEphemeralMessage(json: TdJsonRaw): InputMessageReplyToEphemeralMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageReplyToEphemeralMessage = new InputMessageReplyToEphemeralMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.ephemeral_message_id = asNumber(record['ephemeral_message_id']);
  return out;
}

/** Encode a `inputMessageReplyToEphemeralMessage` into TDLib JSON wire shape. */
export function encodeInputMessageReplyToEphemeralMessage(value: InputMessageReplyToEphemeralMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageReplyToEphemeralMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['ephemeral_message_id'] = value.ephemeral_message_id;
  return out;
}

/** inputMessageReplyToExternalMessage = InputMessageReplyTo — Describes a message to be replied that is from a different chat or a forum topic; not supported in secret chats */
export class InputMessageReplyToExternalMessage {
  type: 'inputMessageReplyToExternalMessage' = 'inputMessageReplyToExternalMessage';
  extra?: string;
  /** chat_id: int53 — The identifier of the chat to which the message to be replied belongs */
  chat_id: number = 0;
  /** message_id: int53 — The identifier of the message to be replied in the specified chat. A message can be replied in another chat or forum topic only if messageProperties.can_be_replied_in_another_chat */
  message_id: number = 0;
  /** quote: inputTextQuote — Quote from the message to be replied; pass null if none */
  quote: InputTextQuote | null = null;
  /** checklist_task_id: int32 — Identifier of the checklist task in the message to be replied; pass 0 to reply to the whole message */
  checklist_task_id: number = 0;
  /** poll_option_id: string — Identifier of the poll option in the message to be replied; pass an empty string if none */
  poll_option_id: string = '';
}

/** Decode a TDLib `inputMessageReplyToExternalMessage` object; null input -> null. */
export function decodeInputMessageReplyToExternalMessage(json: TdJsonRaw): InputMessageReplyToExternalMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageReplyToExternalMessage = new InputMessageReplyToExternalMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.quote = decodeInputTextQuote(record['quote']);
  out.checklist_task_id = asNumber(record['checklist_task_id']);
  out.poll_option_id = asString(record['poll_option_id']);
  return out;
}

/** Encode a `inputMessageReplyToExternalMessage` into TDLib JSON wire shape. */
export function encodeInputMessageReplyToExternalMessage(value: InputMessageReplyToExternalMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageReplyToExternalMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['quote'] = value.quote === null ? null : encodeInputTextQuote(value.quote);
  out['checklist_task_id'] = value.checklist_task_id;
  out['poll_option_id'] = value.poll_option_id;
  return out;
}

/** inputMessageReplyToMessage = InputMessageReplyTo — Describes a message to be replied in the same chat and forum topic */
export class InputMessageReplyToMessage {
  type: 'inputMessageReplyToMessage' = 'inputMessageReplyToMessage';
  extra?: string;
  /** message_id: int53 — The identifier of the message to be replied in the same chat and forum topic. A message can be replied in the same chat and forum topic only if messageProperties.can_be_replied */
  message_id: number = 0;
  /** quote: inputTextQuote — Quote from the message to be replied; pass null if none. Must always be null for replies in secret chats */
  quote: InputTextQuote | null = null;
  /** checklist_task_id: int32 — Identifier of the checklist task in the message to be replied; pass 0 to reply to the whole message */
  checklist_task_id: number = 0;
  /** poll_option_id: string — Identifier of the poll option in the message to be replied; pass an empty string if none */
  poll_option_id: string = '';
}

/** Decode a TDLib `inputMessageReplyToMessage` object; null input -> null. */
export function decodeInputMessageReplyToMessage(json: TdJsonRaw): InputMessageReplyToMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageReplyToMessage = new InputMessageReplyToMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_id = asNumber(record['message_id']);
  out.quote = decodeInputTextQuote(record['quote']);
  out.checklist_task_id = asNumber(record['checklist_task_id']);
  out.poll_option_id = asString(record['poll_option_id']);
  return out;
}

/** Encode a `inputMessageReplyToMessage` into TDLib JSON wire shape. */
export function encodeInputMessageReplyToMessage(value: InputMessageReplyToMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageReplyToMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_id'] = value.message_id;
  out['quote'] = value.quote === null ? null : encodeInputTextQuote(value.quote);
  out['checklist_task_id'] = value.checklist_task_id;
  out['poll_option_id'] = value.poll_option_id;
  return out;
}

/** inputMessageReplyToStory = InputMessageReplyTo — Describes a story to be replied */
export class InputMessageReplyToStory {
  type: 'inputMessageReplyToStory' = 'inputMessageReplyToStory';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the poster of the story. Currently, stories can be replied only in the chat that posted the story; channel stories can't be replied */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — The identifier of the story */
  story_id: number = 0;
}

/** Decode a TDLib `inputMessageReplyToStory` object; null input -> null. */
export function decodeInputMessageReplyToStory(json: TdJsonRaw): InputMessageReplyToStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageReplyToStory = new InputMessageReplyToStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `inputMessageReplyToStory` into TDLib JSON wire shape. */
export function encodeInputMessageReplyToStory(value: InputMessageReplyToStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageReplyToStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** inputMessageRichMessage = InputMessageContent — A rich message */
export class InputMessageRichMessage {
  type: 'inputMessageRichMessage' = 'inputMessageRichMessage';
  extra?: string;
  /** message: inputRichMessage — The rich message to send */
  message: InputRichMessage | null = null;
  /** clear_draft: Bool — Pass true to delete message draft in the chat */
  clear_draft: boolean = false;
}

/** Decode a TDLib `inputMessageRichMessage` object; null input -> null. */
export function decodeInputMessageRichMessage(json: TdJsonRaw): InputMessageRichMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageRichMessage = new InputMessageRichMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeInputRichMessage(record['message']);
  out.clear_draft = asBool(record['clear_draft']);
  return out;
}

/** Encode a `inputMessageRichMessage` into TDLib JSON wire shape. */
export function encodeInputMessageRichMessage(value: InputMessageRichMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageRichMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeInputRichMessage(value.message);
  out['clear_draft'] = value.clear_draft;
  return out;
}

/** inputMessageStakeDice = InputMessageContent — A stake dice message getOption("stake_dice_stake_amount_min")-getOption("stake_dice_stake_amount_max") */
export class InputMessageStakeDice {
  type: 'inputMessageStakeDice' = 'inputMessageStakeDice';
  extra?: string;
  /** state_hash: string — Hash of the stake dice state. The state hash can be used only if it was received recently enough. Otherwise, a new state must be requested using getStakeDiceState */
  state_hash: string = '';
  /** stake_gram_amount: int53 — The TON Gram amount that will be staked; in the smallest units of the currency. Must be in the range */
  stake_gram_amount: number = 0;
  /** clear_draft: Bool — Pass true to delete message draft in the chat */
  clear_draft: boolean = false;
}

/** Decode a TDLib `inputMessageStakeDice` object; null input -> null. */
export function decodeInputMessageStakeDice(json: TdJsonRaw): InputMessageStakeDice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageStakeDice = new InputMessageStakeDice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.state_hash = asString(record['state_hash']);
  out.stake_gram_amount = asNumber(record['stake_gram_amount']);
  out.clear_draft = asBool(record['clear_draft']);
  return out;
}

/** Encode a `inputMessageStakeDice` into TDLib JSON wire shape. */
export function encodeInputMessageStakeDice(value: InputMessageStakeDice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageStakeDice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['state_hash'] = value.state_hash;
  out['stake_gram_amount'] = value.stake_gram_amount;
  out['clear_draft'] = value.clear_draft;
  return out;
}

/** inputMessageSticker = InputMessageContent — A sticker message */
export class InputMessageSticker {
  type: 'inputMessageSticker' = 'inputMessageSticker';
  extra?: string;
  /** sticker: inputSticker — Sticker to be sent */
  sticker: InputSticker | null = null;
  /** emoji: string — Emoji used to choose the sticker */
  emoji: string = '';
}

/** Decode a TDLib `inputMessageSticker` object; null input -> null. */
export function decodeInputMessageSticker(json: TdJsonRaw): InputMessageSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageSticker = new InputMessageSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeInputSticker(record['sticker']);
  out.emoji = asString(record['emoji']);
  return out;
}

/** Encode a `inputMessageSticker` into TDLib JSON wire shape. */
export function encodeInputMessageSticker(value: InputMessageSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeInputSticker(value.sticker);
  out['emoji'] = value.emoji;
  return out;
}

/** inputMessageStory = InputMessageContent — A message with a forwarded story. Stories can't be forwarded to secret chats. A story can be forwarded only if story.can_be_forwarded */
export class InputMessageStory {
  type: 'inputMessageStory' = 'inputMessageStory';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
}

/** Decode a TDLib `inputMessageStory` object; null input -> null. */
export function decodeInputMessageStory(json: TdJsonRaw): InputMessageStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageStory = new InputMessageStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `inputMessageStory` into TDLib JSON wire shape. */
export function encodeInputMessageStory(value: InputMessageStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** inputMessageText = InputMessageContent — A text message Code, Pre, PreCode, TextUrl, MentionName, and DateTime entities are allowed to be specified manually */
export class InputMessageText {
  type: 'inputMessageText' = 'inputMessageText';
  extra?: string;
  /** text: formattedText — Formatted text to be sent; 0-getOption("message_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, BlockQuote, ExpandableBlockQuote, */
  text: FormattedText | null = null;
  /** link_preview_options: linkPreviewOptions — Options to be used for generation of a link preview; may be null if none; pass null to use default link preview options */
  link_preview_options: LinkPreviewOptions | null = null;
  /** clear_draft: Bool — Pass true to delete message draft in the chat */
  clear_draft: boolean = false;
}

/** Decode a TDLib `inputMessageText` object; null input -> null. */
export function decodeInputMessageText(json: TdJsonRaw): InputMessageText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageText = new InputMessageText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.link_preview_options = decodeLinkPreviewOptions(record['link_preview_options']);
  out.clear_draft = asBool(record['clear_draft']);
  return out;
}

/** Encode a `inputMessageText` into TDLib JSON wire shape. */
export function encodeInputMessageText(value: InputMessageText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['link_preview_options'] = value.link_preview_options === null ? null : encodeLinkPreviewOptions(value.link_preview_options);
  out['clear_draft'] = value.clear_draft;
  return out;
}

/** inputMessageVenue = InputMessageContent — A message with information about a venue */
export class InputMessageVenue {
  type: 'inputMessageVenue' = 'inputMessageVenue';
  extra?: string;
  /** venue: venue — Venue to send */
  venue: Venue | null = null;
}

/** Decode a TDLib `inputMessageVenue` object; null input -> null. */
export function decodeInputMessageVenue(json: TdJsonRaw): InputMessageVenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageVenue = new InputMessageVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.venue = decodeVenue(record['venue']);
  return out;
}

/** Encode a `inputMessageVenue` into TDLib JSON wire shape. */
export function encodeInputMessageVenue(value: InputMessageVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['venue'] = value.venue === null ? null : encodeVenue(value.venue);
  return out;
}

/** inputMessageVideo = InputMessageContent — A video message */
export class InputMessageVideo {
  type: 'inputMessageVideo' = 'inputMessageVideo';
  extra?: string;
  /** video: inputVideo — Video to be sent */
  video: InputVideo | null = null;
  /** caption: formattedText — Video caption; pass null to use an empty caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — True, if the caption must be shown above the video; otherwise, the caption must be shown below the video; not supported in secret chats */
  show_caption_above_media: boolean = false;
  /** self_destruct_type: MessageSelfDestructType — Video self-destruct type; pass null if none; private chats only */
  self_destruct_type: TdMessageSelfDestructType | null = null;
  /** has_spoiler: Bool — True, if the video preview must be covered by a spoiler animation; not supported in secret chats */
  has_spoiler: boolean = false;
}

/** Decode a TDLib `inputMessageVideo` object; null input -> null. */
export function decodeInputMessageVideo(json: TdJsonRaw): InputMessageVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageVideo = new InputMessageVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeInputVideo(record['video']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  out.self_destruct_type = decodeTdMessageSelfDestructType(record['self_destruct_type']);
  out.has_spoiler = asBool(record['has_spoiler']);
  return out;
}

/** Encode a `inputMessageVideo` into TDLib JSON wire shape. */
export function encodeInputMessageVideo(value: InputMessageVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeInputVideo(value.video);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  out['self_destruct_type'] = value.self_destruct_type === null ? null : encodeTdMessageSelfDestructType(value.self_destruct_type);
  out['has_spoiler'] = value.has_spoiler;
  return out;
}

/** inputMessageVideoNote = InputMessageContent — A video note message */
export class InputMessageVideoNote {
  type: 'inputMessageVideoNote' = 'inputMessageVideoNote';
  extra?: string;
  /** video_note: inputVideoNote — Video note to be sent */
  video_note: InputVideoNote | null = null;
  /** self_destruct_type: MessageSelfDestructType — Video note self-destruct type; may be null if none; pass null if none; private chats only */
  self_destruct_type: TdMessageSelfDestructType | null = null;
}

/** Decode a TDLib `inputMessageVideoNote` object; null input -> null. */
export function decodeInputMessageVideoNote(json: TdJsonRaw): InputMessageVideoNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageVideoNote = new InputMessageVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video_note = decodeInputVideoNote(record['video_note']);
  out.self_destruct_type = decodeTdMessageSelfDestructType(record['self_destruct_type']);
  return out;
}

/** Encode a `inputMessageVideoNote` into TDLib JSON wire shape. */
export function encodeInputMessageVideoNote(value: InputMessageVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video_note'] = value.video_note === null ? null : encodeInputVideoNote(value.video_note);
  out['self_destruct_type'] = value.self_destruct_type === null ? null : encodeTdMessageSelfDestructType(value.self_destruct_type);
  return out;
}

/** inputMessageVoiceNote = InputMessageContent — A voice note message */
export class InputMessageVoiceNote {
  type: 'inputMessageVoiceNote' = 'inputMessageVoiceNote';
  extra?: string;
  /** voice_note: inputVoiceNote — Voice note to be sent */
  voice_note: InputVoiceNote | null = null;
  /** caption: formattedText — Voice note caption; pass null to use an empty caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
  /** self_destruct_type: MessageSelfDestructType — Voice note self-destruct type; may be null if none; pass null if none; private chats only */
  self_destruct_type: TdMessageSelfDestructType | null = null;
}

/** Decode a TDLib `inputMessageVoiceNote` object; null input -> null. */
export function decodeInputMessageVoiceNote(json: TdJsonRaw): InputMessageVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputMessageVoiceNote = new InputMessageVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.voice_note = decodeInputVoiceNote(record['voice_note']);
  out.caption = decodeFormattedText(record['caption']);
  out.self_destruct_type = decodeTdMessageSelfDestructType(record['self_destruct_type']);
  return out;
}

/** Encode a `inputMessageVoiceNote` into TDLib JSON wire shape. */
export function encodeInputMessageVoiceNote(value: InputMessageVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputMessageVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['voice_note'] = value.voice_note === null ? null : encodeInputVoiceNote(value.voice_note);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['self_destruct_type'] = value.self_destruct_type === null ? null : encodeTdMessageSelfDestructType(value.self_destruct_type);
  return out;
}

/** inputPageBlockAnchor = InputPageBlock — An invisible anchor */
export class InputPageBlockAnchor {
  type: 'inputPageBlockAnchor' = 'inputPageBlockAnchor';
  extra?: string;
  /** name: string — Name of the anchor */
  name: string = '';
}

/** Decode a TDLib `inputPageBlockAnchor` object; null input -> null. */
export function decodeInputPageBlockAnchor(json: TdJsonRaw): InputPageBlockAnchor | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockAnchor = new InputPageBlockAnchor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `inputPageBlockAnchor` into TDLib JSON wire shape. */
export function encodeInputPageBlockAnchor(value: InputPageBlockAnchor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockAnchor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** inputPageBlockAnimation = InputPageBlock — An animation */
export class InputPageBlockAnimation {
  type: 'inputPageBlockAnimation' = 'inputPageBlockAnimation';
  extra?: string;
  /** animation: inputAnimation — The animation to be sent */
  animation: InputAnimation | null = null;
  /** caption: pageBlockCaption — Animation caption; pass null if none */
  caption: PageBlockCaption | null = null;
  /** has_spoiler: Bool — True, if the animation preview must be covered by a spoiler animation */
  has_spoiler: boolean = false;
}

/** Decode a TDLib `inputPageBlockAnimation` object; null input -> null. */
export function decodeInputPageBlockAnimation(json: TdJsonRaw): InputPageBlockAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockAnimation = new InputPageBlockAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeInputAnimation(record['animation']);
  out.caption = decodePageBlockCaption(record['caption']);
  out.has_spoiler = asBool(record['has_spoiler']);
  return out;
}

/** Encode a `inputPageBlockAnimation` into TDLib JSON wire shape. */
export function encodeInputPageBlockAnimation(value: InputPageBlockAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeInputAnimation(value.animation);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  out['has_spoiler'] = value.has_spoiler;
  return out;
}

/** inputPageBlockAudio = InputPageBlock — An audio file */
export class InputPageBlockAudio {
  type: 'inputPageBlockAudio' = 'inputPageBlockAudio';
  extra?: string;
  /** audio: inputAudio — The audio to be sent */
  audio: InputAudio | null = null;
  /** caption: pageBlockCaption — Audio file caption; pass null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `inputPageBlockAudio` object; null input -> null. */
export function decodeInputPageBlockAudio(json: TdJsonRaw): InputPageBlockAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockAudio = new InputPageBlockAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeInputAudio(record['audio']);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `inputPageBlockAudio` into TDLib JSON wire shape. */
export function encodeInputPageBlockAudio(value: InputPageBlockAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeInputAudio(value.audio);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** inputPageBlockBlockQuote = InputPageBlock — A block quote */
export class InputPageBlockBlockQuote {
  type: 'inputPageBlockBlockQuote' = 'inputPageBlockBlockQuote';
  extra?: string;
  /** blocks: vector<InputPageBlock> — Quote blocks */
  blocks: TdInputPageBlock[] = [];
  /** credit: RichText — Quote credit; pass null if none */
  credit: TdRichText | null = null;
}

/** Decode a TDLib `inputPageBlockBlockQuote` object; null input -> null. */
export function decodeInputPageBlockBlockQuote(json: TdJsonRaw): InputPageBlockBlockQuote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockBlockQuote = new InputPageBlockBlockQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdInputPageBlock => decodeTdInputPageBlock(item) as TdInputPageBlock);
  out.credit = decodeTdRichText(record['credit']);
  return out;
}

/** Encode a `inputPageBlockBlockQuote` into TDLib JSON wire shape. */
export function encodeInputPageBlockBlockQuote(value: InputPageBlockBlockQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockBlockQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdInputPageBlock | null): TdJsonRaw => item === null ? null : encodeTdInputPageBlock(item));
  out['credit'] = value.credit === null ? null : encodeTdRichText(value.credit);
  return out;
}

/** inputPageBlockButtonRow = InputPageBlock — A list of buttons shown in a row */
export class InputPageBlockButtonRow {
  type: 'inputPageBlockButtonRow' = 'inputPageBlockButtonRow';
  extra?: string;
  /** buttons: vector<inlineButton> — The buttons */
  buttons: InlineButton[] = [];
  /** align: PageBlockHorizontalAlignment — Horizontal alignment of the buttons; pass null if the buttons must be shown full-width */
  align: TdPageBlockHorizontalAlignment | null = null;
}

/** Decode a TDLib `inputPageBlockButtonRow` object; null input -> null. */
export function decodeInputPageBlockButtonRow(json: TdJsonRaw): InputPageBlockButtonRow | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockButtonRow = new InputPageBlockButtonRow();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.buttons = decodeVector(record['buttons'], (item: TdJsonRaw): InlineButton => decodeInlineButton(item) as InlineButton);
  out.align = decodeTdPageBlockHorizontalAlignment(record['align']);
  return out;
}

/** Encode a `inputPageBlockButtonRow` into TDLib JSON wire shape. */
export function encodeInputPageBlockButtonRow(value: InputPageBlockButtonRow): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockButtonRow';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['buttons'] = encodeVector(value.buttons, (item: InlineButton | null): TdJsonRaw => item === null ? null : encodeInlineButton(item));
  out['align'] = value.align === null ? null : encodeTdPageBlockHorizontalAlignment(value.align);
  return out;
}

/** inputPageBlockCollage = InputPageBlock — A collage */
export class InputPageBlockCollage {
  type: 'inputPageBlockCollage' = 'inputPageBlockCollage';
  extra?: string;
  /** blocks: vector<InputPageBlock> — Collage item contents */
  blocks: TdInputPageBlock[] = [];
  /** caption: pageBlockCaption — Block caption; pass null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `inputPageBlockCollage` object; null input -> null. */
export function decodeInputPageBlockCollage(json: TdJsonRaw): InputPageBlockCollage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockCollage = new InputPageBlockCollage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdInputPageBlock => decodeTdInputPageBlock(item) as TdInputPageBlock);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `inputPageBlockCollage` into TDLib JSON wire shape. */
export function encodeInputPageBlockCollage(value: InputPageBlockCollage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockCollage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdInputPageBlock | null): TdJsonRaw => item === null ? null : encodeTdInputPageBlock(item));
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** inputPageBlockDetails = InputPageBlock — A collapsible block */
export class InputPageBlockDetails {
  type: 'inputPageBlockDetails' = 'inputPageBlockDetails';
  extra?: string;
  /** header: RichText — Always visible heading for the block */
  header: TdRichText | null = null;
  /** blocks: vector<InputPageBlock> — Block contents */
  blocks: TdInputPageBlock[] = [];
  /** is_open: Bool — True, if the block is open by default */
  is_open: boolean = false;
}

/** Decode a TDLib `inputPageBlockDetails` object; null input -> null. */
export function decodeInputPageBlockDetails(json: TdJsonRaw): InputPageBlockDetails | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockDetails = new InputPageBlockDetails();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.header = decodeTdRichText(record['header']);
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdInputPageBlock => decodeTdInputPageBlock(item) as TdInputPageBlock);
  out.is_open = asBool(record['is_open']);
  return out;
}

/** Encode a `inputPageBlockDetails` into TDLib JSON wire shape. */
export function encodeInputPageBlockDetails(value: InputPageBlockDetails): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockDetails';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['header'] = value.header === null ? null : encodeTdRichText(value.header);
  out['blocks'] = encodeVector(value.blocks, (item: TdInputPageBlock | null): TdJsonRaw => item === null ? null : encodeTdInputPageBlock(item));
  out['is_open'] = value.is_open;
  return out;
}

/** inputPageBlockDivider = InputPageBlock — An empty block separating the page */
export class InputPageBlockDivider {
  type: 'inputPageBlockDivider' = 'inputPageBlockDivider';
  extra?: string;
}

/** Decode a TDLib `inputPageBlockDivider` object; null input -> null. */
export function decodeInputPageBlockDivider(json: TdJsonRaw): InputPageBlockDivider | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockDivider = new InputPageBlockDivider();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inputPageBlockDivider` into TDLib JSON wire shape. */
export function encodeInputPageBlockDivider(value: InputPageBlockDivider): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockDivider';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inputPageBlockDocument = InputPageBlock — A general file */
export class InputPageBlockDocument {
  type: 'inputPageBlockDocument' = 'inputPageBlockDocument';
  extra?: string;
  /** document: inputDocument — The file to be sent */
  document: InputDocument | null = null;
  /** caption: pageBlockCaption — File caption; pass null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `inputPageBlockDocument` object; null input -> null. */
export function decodeInputPageBlockDocument(json: TdJsonRaw): InputPageBlockDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockDocument = new InputPageBlockDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeInputDocument(record['document']);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `inputPageBlockDocument` into TDLib JSON wire shape. */
export function encodeInputPageBlockDocument(value: InputPageBlockDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeInputDocument(value.document);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** inputPageBlockExpandableBlockQuote = InputPageBlock — An expandable block quote */
export class InputPageBlockExpandableBlockQuote {
  type: 'inputPageBlockExpandableBlockQuote' = 'inputPageBlockExpandableBlockQuote';
  extra?: string;
  /** text: RichText — Quote text */
  text: TdRichText | null = null;
  /** credit: RichText — Quote credit; pass null if none */
  credit: TdRichText | null = null;
}

/** Decode a TDLib `inputPageBlockExpandableBlockQuote` object; null input -> null. */
export function decodeInputPageBlockExpandableBlockQuote(json: TdJsonRaw): InputPageBlockExpandableBlockQuote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockExpandableBlockQuote = new InputPageBlockExpandableBlockQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.credit = decodeTdRichText(record['credit']);
  return out;
}

/** Encode a `inputPageBlockExpandableBlockQuote` into TDLib JSON wire shape. */
export function encodeInputPageBlockExpandableBlockQuote(value: InputPageBlockExpandableBlockQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockExpandableBlockQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['credit'] = value.credit === null ? null : encodeTdRichText(value.credit);
  return out;
}

/** inputPageBlockFooter = InputPageBlock — The footer of the page */
export class InputPageBlockFooter {
  type: 'inputPageBlockFooter' = 'inputPageBlockFooter';
  extra?: string;
  /** footer: RichText — Footer */
  footer: TdRichText | null = null;
}

/** Decode a TDLib `inputPageBlockFooter` object; null input -> null. */
export function decodeInputPageBlockFooter(json: TdJsonRaw): InputPageBlockFooter | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockFooter = new InputPageBlockFooter();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.footer = decodeTdRichText(record['footer']);
  return out;
}

/** Encode a `inputPageBlockFooter` into TDLib JSON wire shape. */
export function encodeInputPageBlockFooter(value: InputPageBlockFooter): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockFooter';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['footer'] = value.footer === null ? null : encodeTdRichText(value.footer);
  return out;
}

/** inputPageBlockList = InputPageBlock — A list of data blocks */
export class InputPageBlockList {
  type: 'inputPageBlockList' = 'inputPageBlockList';
  extra?: string;
  /** items: vector<inputPageBlockListItem> — The items of the list */
  items: InputPageBlockListItem[] = [];
}

/** Decode a TDLib `inputPageBlockList` object; null input -> null. */
export function decodeInputPageBlockList(json: TdJsonRaw): InputPageBlockList | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockList = new InputPageBlockList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.items = decodeVector(record['items'], (item: TdJsonRaw): InputPageBlockListItem => decodeInputPageBlockListItem(item) as InputPageBlockListItem);
  return out;
}

/** Encode a `inputPageBlockList` into TDLib JSON wire shape. */
export function encodeInputPageBlockList(value: InputPageBlockList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['items'] = encodeVector(value.items, (item: InputPageBlockListItem | null): TdJsonRaw => item === null ? null : encodeInputPageBlockListItem(item));
  return out;
}

/** inputPageBlockListItem = InputPageBlockListItem — Describes an item of a list page block to be sent "1" for decimal numbers, or empty for unordered lists */
export class InputPageBlockListItem {
  type: 'inputPageBlockListItem' = 'inputPageBlockListItem';
  extra?: string;
  /** blocks: vector<InputPageBlock> — Item blocks */
  blocks: TdInputPageBlock[] = [];
  /** has_checkbox: Bool — True, if the item has a checkbox */
  has_checkbox: boolean = false;
  /** is_checked: Bool — True, if the item is checked */
  is_checked: boolean = false;
  /** value: int32 — Value of the item; pass 0 for unordered lists */
  value: number = 0;
  /** type: string — Type of the item numbering type; must be one of "a" for a lowercase letter, "A" for an uppercase letter, "i" for lowercase Roman numerals, "I" for uppercase Roman numerals, */
  type_: string = '';
}

/** Decode a TDLib `inputPageBlockListItem` object; null input -> null. */
export function decodeInputPageBlockListItem(json: TdJsonRaw): InputPageBlockListItem | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockListItem = new InputPageBlockListItem();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdInputPageBlock => decodeTdInputPageBlock(item) as TdInputPageBlock);
  out.has_checkbox = asBool(record['has_checkbox']);
  out.is_checked = asBool(record['is_checked']);
  out.value = asNumber(record['value']);
  out.type_ = asString(record['type']);
  return out;
}

/** Encode a `inputPageBlockListItem` into TDLib JSON wire shape. */
export function encodeInputPageBlockListItem(value: InputPageBlockListItem): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockListItem';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdInputPageBlock | null): TdJsonRaw => item === null ? null : encodeTdInputPageBlock(item));
  out['has_checkbox'] = value.has_checkbox;
  out['is_checked'] = value.is_checked;
  out['value'] = value.value;
  out['type'] = value.type_;
  return out;
}

/** inputPageBlockMap = InputPageBlock — A map. The map's width and height must not exceed 10000 in total. Width and height ratio must be at most 20 */
export class InputPageBlockMap {
  type: 'inputPageBlockMap' = 'inputPageBlockMap';
  extra?: string;
  /** location: location — Location of the map center */
  location: Location | null = null;
  /** zoom: int32 — Map zoom level; 0-24 */
  zoom: number = 0;
  /** width: int32 — Map width; 0-10000 */
  width: number = 0;
  /** height: int32 — Map height; 0-10000 */
  height: number = 0;
  /** caption: pageBlockCaption — Block caption; pass null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `inputPageBlockMap` object; null input -> null. */
export function decodeInputPageBlockMap(json: TdJsonRaw): InputPageBlockMap | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockMap = new InputPageBlockMap();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  out.zoom = asNumber(record['zoom']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `inputPageBlockMap` into TDLib JSON wire shape. */
export function encodeInputPageBlockMap(value: InputPageBlockMap): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockMap';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['zoom'] = value.zoom;
  out['width'] = value.width;
  out['height'] = value.height;
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** inputPageBlockMathematicalExpression = InputPageBlock — A mathematical expression */
export class InputPageBlockMathematicalExpression {
  type: 'inputPageBlockMathematicalExpression' = 'inputPageBlockMathematicalExpression';
  extra?: string;
  /** expression: string — The expression in LaTeX format */
  expression: string = '';
}

/** Decode a TDLib `inputPageBlockMathematicalExpression` object; null input -> null. */
export function decodeInputPageBlockMathematicalExpression(json: TdJsonRaw): InputPageBlockMathematicalExpression | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockMathematicalExpression = new InputPageBlockMathematicalExpression();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.expression = asString(record['expression']);
  return out;
}

/** Encode a `inputPageBlockMathematicalExpression` into TDLib JSON wire shape. */
export function encodeInputPageBlockMathematicalExpression(value: InputPageBlockMathematicalExpression): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockMathematicalExpression';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['expression'] = value.expression;
  return out;
}

/** inputPageBlockParagraph = InputPageBlock — A text paragraph */
export class InputPageBlockParagraph {
  type: 'inputPageBlockParagraph' = 'inputPageBlockParagraph';
  extra?: string;
  /** text: RichText — Paragraph text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `inputPageBlockParagraph` object; null input -> null. */
export function decodeInputPageBlockParagraph(json: TdJsonRaw): InputPageBlockParagraph | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockParagraph = new InputPageBlockParagraph();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `inputPageBlockParagraph` into TDLib JSON wire shape. */
export function encodeInputPageBlockParagraph(value: InputPageBlockParagraph): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockParagraph';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** inputPageBlockPhoto = InputPageBlock — A photo */
export class InputPageBlockPhoto {
  type: 'inputPageBlockPhoto' = 'inputPageBlockPhoto';
  extra?: string;
  /** photo: inputPhoto — The photo to be sent */
  photo: InputPhoto | null = null;
  /** caption: pageBlockCaption — Photo caption; pass null if none */
  caption: PageBlockCaption | null = null;
  /** has_spoiler: Bool — True, if the photo preview must be covered by a spoiler animation */
  has_spoiler: boolean = false;
}

/** Decode a TDLib `inputPageBlockPhoto` object; null input -> null. */
export function decodeInputPageBlockPhoto(json: TdJsonRaw): InputPageBlockPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockPhoto = new InputPageBlockPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeInputPhoto(record['photo']);
  out.caption = decodePageBlockCaption(record['caption']);
  out.has_spoiler = asBool(record['has_spoiler']);
  return out;
}

/** Encode a `inputPageBlockPhoto` into TDLib JSON wire shape. */
export function encodeInputPageBlockPhoto(value: InputPageBlockPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeInputPhoto(value.photo);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  out['has_spoiler'] = value.has_spoiler;
  return out;
}

/** inputPageBlockPreformatted = InputPageBlock — A preformatted text paragraph */
export class InputPageBlockPreformatted {
  type: 'inputPageBlockPreformatted' = 'inputPageBlockPreformatted';
  extra?: string;
  /** text: RichText — Paragraph text */
  text: TdRichText | null = null;
  /** language: string — Programming language for which the text needs to be formatted */
  language: string = '';
}

/** Decode a TDLib `inputPageBlockPreformatted` object; null input -> null. */
export function decodeInputPageBlockPreformatted(json: TdJsonRaw): InputPageBlockPreformatted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockPreformatted = new InputPageBlockPreformatted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.language = asString(record['language']);
  return out;
}

/** Encode a `inputPageBlockPreformatted` into TDLib JSON wire shape. */
export function encodeInputPageBlockPreformatted(value: InputPageBlockPreformatted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockPreformatted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['language'] = value.language;
  return out;
}

/** inputPageBlockPullQuote = InputPageBlock — A pull quote */
export class InputPageBlockPullQuote {
  type: 'inputPageBlockPullQuote' = 'inputPageBlockPullQuote';
  extra?: string;
  /** text: RichText — Quote text */
  text: TdRichText | null = null;
  /** credit: RichText — Quote credit; pass null if none */
  credit: TdRichText | null = null;
}

/** Decode a TDLib `inputPageBlockPullQuote` object; null input -> null. */
export function decodeInputPageBlockPullQuote(json: TdJsonRaw): InputPageBlockPullQuote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockPullQuote = new InputPageBlockPullQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.credit = decodeTdRichText(record['credit']);
  return out;
}

/** Encode a `inputPageBlockPullQuote` into TDLib JSON wire shape. */
export function encodeInputPageBlockPullQuote(value: InputPageBlockPullQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockPullQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['credit'] = value.credit === null ? null : encodeTdRichText(value.credit);
  return out;
}

/** inputPageBlockSectionHeading = InputPageBlock — A section heading */
export class InputPageBlockSectionHeading {
  type: 'inputPageBlockSectionHeading' = 'inputPageBlockSectionHeading';
  extra?: string;
  /** text: RichText — Text of the section heading */
  text: TdRichText | null = null;
  /** size: int32 — Relative size of the text font; 1-6, 1 is the largest, 6 is the smallest */
  size: number = 0;
}

/** Decode a TDLib `inputPageBlockSectionHeading` object; null input -> null. */
export function decodeInputPageBlockSectionHeading(json: TdJsonRaw): InputPageBlockSectionHeading | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockSectionHeading = new InputPageBlockSectionHeading();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.size = asNumber(record['size']);
  return out;
}

/** Encode a `inputPageBlockSectionHeading` into TDLib JSON wire shape. */
export function encodeInputPageBlockSectionHeading(value: InputPageBlockSectionHeading): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockSectionHeading';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['size'] = value.size;
  return out;
}

/** inputPageBlockSlideshow = InputPageBlock — A slideshow */
export class InputPageBlockSlideshow {
  type: 'inputPageBlockSlideshow' = 'inputPageBlockSlideshow';
  extra?: string;
  /** blocks: vector<InputPageBlock> — Slideshow item contents */
  blocks: TdInputPageBlock[] = [];
  /** caption: pageBlockCaption — Block caption; pass null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `inputPageBlockSlideshow` object; null input -> null. */
export function decodeInputPageBlockSlideshow(json: TdJsonRaw): InputPageBlockSlideshow | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockSlideshow = new InputPageBlockSlideshow();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdInputPageBlock => decodeTdInputPageBlock(item) as TdInputPageBlock);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `inputPageBlockSlideshow` into TDLib JSON wire shape. */
export function encodeInputPageBlockSlideshow(value: InputPageBlockSlideshow): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockSlideshow';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdInputPageBlock | null): TdJsonRaw => item === null ? null : encodeTdInputPageBlock(item));
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** inputPageBlockTable = InputPageBlock — A table */
export class InputPageBlockTable {
  type: 'inputPageBlockTable' = 'inputPageBlockTable';
  extra?: string;
  /** caption: RichText — Table caption */
  caption: TdRichText | null = null;
  /** cells: vector<vector<pageBlockTableCell>> — Table cells */
  cells: PageBlockTableCell[][] = [];
  /** is_bordered: Bool — Pass true if the table is bordered */
  is_bordered: boolean = false;
  /** is_striped: Bool — Pass true if the table is striped */
  is_striped: boolean = false;
  /** is_compact: Bool — Pass true if table cells must have smaller indents */
  is_compact: boolean = false;
}

/** Decode a TDLib `inputPageBlockTable` object; null input -> null. */
export function decodeInputPageBlockTable(json: TdJsonRaw): InputPageBlockTable | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockTable = new InputPageBlockTable();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.caption = decodeTdRichText(record['caption']);
  out.cells = decodeVector(record['cells'], (items: TdJsonRaw): (PageBlockTableCell)[] => decodeVector(items, (item: TdJsonRaw): PageBlockTableCell => decodePageBlockTableCell(item) as PageBlockTableCell));
  out.is_bordered = asBool(record['is_bordered']);
  out.is_striped = asBool(record['is_striped']);
  out.is_compact = asBool(record['is_compact']);
  return out;
}

/** Encode a `inputPageBlockTable` into TDLib JSON wire shape. */
export function encodeInputPageBlockTable(value: InputPageBlockTable): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockTable';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['caption'] = value.caption === null ? null : encodeTdRichText(value.caption);
  out['cells'] = encodeVector(value.cells, (items: (PageBlockTableCell | null)[]): TdJsonRaw[] => encodeVector(items, (item: PageBlockTableCell | null): TdJsonRaw => item === null ? null : encodePageBlockTableCell(item)));
  out['is_bordered'] = value.is_bordered;
  out['is_striped'] = value.is_striped;
  out['is_compact'] = value.is_compact;
  return out;
}

/** inputPageBlockThinking = InputPageBlock — A "Thinking..." placeholder; for pending rich messages only; for bots only */
export class InputPageBlockThinking {
  type: 'inputPageBlockThinking' = 'inputPageBlockThinking';
  extra?: string;
  /** text: RichText — Text of the placeholder */
  text: TdRichText | null = null;
}

/** Decode a TDLib `inputPageBlockThinking` object; null input -> null. */
export function decodeInputPageBlockThinking(json: TdJsonRaw): InputPageBlockThinking | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockThinking = new InputPageBlockThinking();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `inputPageBlockThinking` into TDLib JSON wire shape. */
export function encodeInputPageBlockThinking(value: InputPageBlockThinking): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockThinking';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** inputPageBlockVideo = InputPageBlock — A video */
export class InputPageBlockVideo {
  type: 'inputPageBlockVideo' = 'inputPageBlockVideo';
  extra?: string;
  /** video: inputVideo — The video to be sent */
  video: InputVideo | null = null;
  /** caption: pageBlockCaption — Video caption; pass null if none */
  caption: PageBlockCaption | null = null;
  /** has_spoiler: Bool — True, if the video preview must be covered by a spoiler animation */
  has_spoiler: boolean = false;
}

/** Decode a TDLib `inputPageBlockVideo` object; null input -> null. */
export function decodeInputPageBlockVideo(json: TdJsonRaw): InputPageBlockVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockVideo = new InputPageBlockVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeInputVideo(record['video']);
  out.caption = decodePageBlockCaption(record['caption']);
  out.has_spoiler = asBool(record['has_spoiler']);
  return out;
}

/** Encode a `inputPageBlockVideo` into TDLib JSON wire shape. */
export function encodeInputPageBlockVideo(value: InputPageBlockVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeInputVideo(value.video);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  out['has_spoiler'] = value.has_spoiler;
  return out;
}

/** inputPageBlockVoiceNote = InputPageBlock — A voice note */
export class InputPageBlockVoiceNote {
  type: 'inputPageBlockVoiceNote' = 'inputPageBlockVoiceNote';
  extra?: string;
  /** voice_note: inputVoiceNote — The voice note to be sent */
  voice_note: InputVoiceNote | null = null;
  /** caption: pageBlockCaption — Voice note caption; pass null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `inputPageBlockVoiceNote` object; null input -> null. */
export function decodeInputPageBlockVoiceNote(json: TdJsonRaw): InputPageBlockVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPageBlockVoiceNote = new InputPageBlockVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.voice_note = decodeInputVoiceNote(record['voice_note']);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `inputPageBlockVoiceNote` into TDLib JSON wire shape. */
export function encodeInputPageBlockVoiceNote(value: InputPageBlockVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPageBlockVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['voice_note'] = value.voice_note === null ? null : encodeInputVoiceNote(value.voice_note);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** inputPaidMedia = InputPaidMedia — Describes a paid media to be sent */
export class InputPaidMedia {
  type: 'inputPaidMedia' = 'inputPaidMedia';
  extra?: string;
  /** type: InputPaidMediaType — Type of the media */
  type_: TdInputPaidMediaType | null = null;
  /** media: InputFile — Photo or video to be sent */
  media: TdInputFile | null = null;
  /** thumbnail: inputThumbnail — Media thumbnail; pass null to skip thumbnail uploading */
  thumbnail: InputThumbnail | null = null;
  /** added_sticker_file_ids: vector<int32> — File identifiers of the stickers added to the media, if applicable */
  added_sticker_file_ids: number[] = [];
  /** width: int32 — Media width */
  width: number = 0;
  /** height: int32 — Media height */
  height: number = 0;
}

/** Decode a TDLib `inputPaidMedia` object; null input -> null. */
export function decodeInputPaidMedia(json: TdJsonRaw): InputPaidMedia | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPaidMedia = new InputPaidMedia();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdInputPaidMediaType(record['type']);
  out.media = decodeTdInputFile(record['media']);
  out.thumbnail = decodeInputThumbnail(record['thumbnail']);
  out.added_sticker_file_ids = decodeVector(record['added_sticker_file_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  return out;
}

/** Encode a `inputPaidMedia` into TDLib JSON wire shape. */
export function encodeInputPaidMedia(value: InputPaidMedia): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPaidMedia';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdInputPaidMediaType(value.type_);
  out['media'] = value.media === null ? null : encodeTdInputFile(value.media);
  out['thumbnail'] = value.thumbnail === null ? null : encodeInputThumbnail(value.thumbnail);
  out['added_sticker_file_ids'] = encodeVector(value.added_sticker_file_ids, (item: number): TdJsonRaw => item);
  out['width'] = value.width;
  out['height'] = value.height;
  return out;
}

/** inputPaidMediaTypePhoto = InputPaidMediaType — The media is a photo. The photo must be at most 10 MB in size. The photo's width and height must not exceed 10000 in total. Width and height ratio must be at most 20 */
export class InputPaidMediaTypePhoto {
  type: 'inputPaidMediaTypePhoto' = 'inputPaidMediaTypePhoto';
  extra?: string;
  /** video: InputFile — Video of the live photo; pass null if the photo isn't a live photo */
  video: TdInputFile | null = null;
}

/** Decode a TDLib `inputPaidMediaTypePhoto` object; null input -> null. */
export function decodeInputPaidMediaTypePhoto(json: TdJsonRaw): InputPaidMediaTypePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPaidMediaTypePhoto = new InputPaidMediaTypePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeTdInputFile(record['video']);
  return out;
}

/** Encode a `inputPaidMediaTypePhoto` into TDLib JSON wire shape. */
export function encodeInputPaidMediaTypePhoto(value: InputPaidMediaTypePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPaidMediaTypePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeTdInputFile(value.video);
  return out;
}

/** inputPaidMediaTypeVideo = InputPaidMediaType — The media is a video */
export class InputPaidMediaTypeVideo {
  type: 'inputPaidMediaTypeVideo' = 'inputPaidMediaTypeVideo';
  extra?: string;
  /** cover: InputFile — Cover of the video; pass null to skip cover uploading */
  cover: TdInputFile | null = null;
  /** start_timestamp: int32 — Timestamp from which the video playing must start, in seconds */
  start_timestamp: number = 0;
  /** duration: int32 — Duration of the video, in seconds */
  duration: number = 0;
  /** supports_streaming: Bool — True, if the video is expected to be streamed */
  supports_streaming: boolean = false;
}

/** Decode a TDLib `inputPaidMediaTypeVideo` object; null input -> null. */
export function decodeInputPaidMediaTypeVideo(json: TdJsonRaw): InputPaidMediaTypeVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPaidMediaTypeVideo = new InputPaidMediaTypeVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.cover = decodeTdInputFile(record['cover']);
  out.start_timestamp = asNumber(record['start_timestamp']);
  out.duration = asNumber(record['duration']);
  out.supports_streaming = asBool(record['supports_streaming']);
  return out;
}

/** Encode a `inputPaidMediaTypeVideo` into TDLib JSON wire shape. */
export function encodeInputPaidMediaTypeVideo(value: InputPaidMediaTypeVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPaidMediaTypeVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['cover'] = value.cover === null ? null : encodeTdInputFile(value.cover);
  out['start_timestamp'] = value.start_timestamp;
  out['duration'] = value.duration;
  out['supports_streaming'] = value.supports_streaming;
  return out;
}

/** inputPassportElementAddress = InputPassportElement — A Telegram Passport element to be saved containing the user's address */
export class InputPassportElementAddress {
  type: 'inputPassportElementAddress' = 'inputPassportElementAddress';
  extra?: string;
  /** address: address — The address to be saved */
  address: Address | null = null;
}

/** Decode a TDLib `inputPassportElementAddress` object; null input -> null. */
export function decodeInputPassportElementAddress(json: TdJsonRaw): InputPassportElementAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementAddress = new InputPassportElementAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.address = decodeAddress(record['address']);
  return out;
}

/** Encode a `inputPassportElementAddress` into TDLib JSON wire shape. */
export function encodeInputPassportElementAddress(value: InputPassportElementAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['address'] = value.address === null ? null : encodeAddress(value.address);
  return out;
}

/** inputPassportElementBankStatement = InputPassportElement — A Telegram Passport element to be saved containing the user's bank statement */
export class InputPassportElementBankStatement {
  type: 'inputPassportElementBankStatement' = 'inputPassportElementBankStatement';
  extra?: string;
  /** bank_statement: inputPersonalDocument — The bank statement to be saved */
  bank_statement: InputPersonalDocument | null = null;
}

/** Decode a TDLib `inputPassportElementBankStatement` object; null input -> null. */
export function decodeInputPassportElementBankStatement(json: TdJsonRaw): InputPassportElementBankStatement | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementBankStatement = new InputPassportElementBankStatement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bank_statement = decodeInputPersonalDocument(record['bank_statement']);
  return out;
}

/** Encode a `inputPassportElementBankStatement` into TDLib JSON wire shape. */
export function encodeInputPassportElementBankStatement(value: InputPassportElementBankStatement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementBankStatement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bank_statement'] = value.bank_statement === null ? null : encodeInputPersonalDocument(value.bank_statement);
  return out;
}

/** inputPassportElementDriverLicense = InputPassportElement — A Telegram Passport element to be saved containing the user's driver license */
export class InputPassportElementDriverLicense {
  type: 'inputPassportElementDriverLicense' = 'inputPassportElementDriverLicense';
  extra?: string;
  /** driver_license: inputIdentityDocument — The driver license to be saved */
  driver_license: InputIdentityDocument | null = null;
}

/** Decode a TDLib `inputPassportElementDriverLicense` object; null input -> null. */
export function decodeInputPassportElementDriverLicense(json: TdJsonRaw): InputPassportElementDriverLicense | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementDriverLicense = new InputPassportElementDriverLicense();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.driver_license = decodeInputIdentityDocument(record['driver_license']);
  return out;
}

/** Encode a `inputPassportElementDriverLicense` into TDLib JSON wire shape. */
export function encodeInputPassportElementDriverLicense(value: InputPassportElementDriverLicense): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementDriverLicense';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['driver_license'] = value.driver_license === null ? null : encodeInputIdentityDocument(value.driver_license);
  return out;
}

/** inputPassportElementEmailAddress = InputPassportElement — A Telegram Passport element to be saved containing the user's email address */
export class InputPassportElementEmailAddress {
  type: 'inputPassportElementEmailAddress' = 'inputPassportElementEmailAddress';
  extra?: string;
  /** email_address: string — The email address to be saved */
  email_address: string = '';
}

/** Decode a TDLib `inputPassportElementEmailAddress` object; null input -> null. */
export function decodeInputPassportElementEmailAddress(json: TdJsonRaw): InputPassportElementEmailAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementEmailAddress = new InputPassportElementEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.email_address = asString(record['email_address']);
  return out;
}

/** Encode a `inputPassportElementEmailAddress` into TDLib JSON wire shape. */
export function encodeInputPassportElementEmailAddress(value: InputPassportElementEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['email_address'] = value.email_address;
  return out;
}

/** inputPassportElementError = InputPassportElementError — Contains the description of an error in a Telegram Passport element; for bots only */
export class InputPassportElementError {
  type: 'inputPassportElementError' = 'inputPassportElementError';
  extra?: string;
  /** type: PassportElementType — Type of Telegram Passport element that has the error */
  type_: TdPassportElementType | null = null;
  /** message: string — Error message */
  message: string = '';
  /** source: InputPassportElementErrorSource — Error source */
  source: TdInputPassportElementErrorSource | null = null;
}

/** Decode a TDLib `inputPassportElementError` object; null input -> null. */
export function decodeInputPassportElementError(json: TdJsonRaw): InputPassportElementError | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementError = new InputPassportElementError();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPassportElementType(record['type']);
  out.message = asString(record['message']);
  out.source = decodeTdInputPassportElementErrorSource(record['source']);
  return out;
}

/** Encode a `inputPassportElementError` into TDLib JSON wire shape. */
export function encodeInputPassportElementError(value: InputPassportElementError): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementError';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPassportElementType(value.type_);
  out['message'] = value.message;
  out['source'] = value.source === null ? null : encodeTdInputPassportElementErrorSource(value.source);
  return out;
}

/** inputPassportElementErrorSourceDataField = InputPassportElementErrorSource — A data field contains an error. The error is considered resolved when the field's value changes */
export class InputPassportElementErrorSourceDataField {
  type: 'inputPassportElementErrorSourceDataField' = 'inputPassportElementErrorSourceDataField';
  extra?: string;
  /** field_name: string — Field name */
  field_name: string = '';
  /** data_hash: bytes — Current data hash */
  data_hash: string = '';
}

/** Decode a TDLib `inputPassportElementErrorSourceDataField` object; null input -> null. */
export function decodeInputPassportElementErrorSourceDataField(json: TdJsonRaw): InputPassportElementErrorSourceDataField | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementErrorSourceDataField = new InputPassportElementErrorSourceDataField();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.field_name = asString(record['field_name']);
  out.data_hash = asString(record['data_hash']);
  return out;
}

/** Encode a `inputPassportElementErrorSourceDataField` into TDLib JSON wire shape. */
export function encodeInputPassportElementErrorSourceDataField(value: InputPassportElementErrorSourceDataField): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementErrorSourceDataField';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['field_name'] = value.field_name;
  out['data_hash'] = value.data_hash;
  return out;
}

/** inputPassportElementErrorSourceFile = InputPassportElementErrorSource — The file contains an error. The error is considered resolved when the file changes */
export class InputPassportElementErrorSourceFile {
  type: 'inputPassportElementErrorSourceFile' = 'inputPassportElementErrorSourceFile';
  extra?: string;
  /** file_hash: bytes — Current hash of the file which has the error */
  file_hash: string = '';
}

/** Decode a TDLib `inputPassportElementErrorSourceFile` object; null input -> null. */
export function decodeInputPassportElementErrorSourceFile(json: TdJsonRaw): InputPassportElementErrorSourceFile | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementErrorSourceFile = new InputPassportElementErrorSourceFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_hash = asString(record['file_hash']);
  return out;
}

/** Encode a `inputPassportElementErrorSourceFile` into TDLib JSON wire shape. */
export function encodeInputPassportElementErrorSourceFile(value: InputPassportElementErrorSourceFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementErrorSourceFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_hash'] = value.file_hash;
  return out;
}

/** inputPassportElementErrorSourceFiles = InputPassportElementErrorSource — The list of attached files contains an error. The error is considered resolved when the file list changes */
export class InputPassportElementErrorSourceFiles {
  type: 'inputPassportElementErrorSourceFiles' = 'inputPassportElementErrorSourceFiles';
  extra?: string;
  /** file_hashes: vector<bytes> — Current hashes of all attached files */
  file_hashes: string[] = [];
}

/** Decode a TDLib `inputPassportElementErrorSourceFiles` object; null input -> null. */
export function decodeInputPassportElementErrorSourceFiles(json: TdJsonRaw): InputPassportElementErrorSourceFiles | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementErrorSourceFiles = new InputPassportElementErrorSourceFiles();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_hashes = decodeVector(record['file_hashes'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `inputPassportElementErrorSourceFiles` into TDLib JSON wire shape. */
export function encodeInputPassportElementErrorSourceFiles(value: InputPassportElementErrorSourceFiles): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementErrorSourceFiles';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_hashes'] = encodeVector(value.file_hashes, (item: string): TdJsonRaw => item);
  return out;
}

/** inputPassportElementErrorSourceFrontSide = InputPassportElementErrorSource — The front side of the document contains an error. The error is considered resolved when the file with the front side of the document changes */
export class InputPassportElementErrorSourceFrontSide {
  type: 'inputPassportElementErrorSourceFrontSide' = 'inputPassportElementErrorSourceFrontSide';
  extra?: string;
  /** file_hash: bytes — Current hash of the file containing the front side */
  file_hash: string = '';
}

/** Decode a TDLib `inputPassportElementErrorSourceFrontSide` object; null input -> null. */
export function decodeInputPassportElementErrorSourceFrontSide(json: TdJsonRaw): InputPassportElementErrorSourceFrontSide | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementErrorSourceFrontSide = new InputPassportElementErrorSourceFrontSide();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_hash = asString(record['file_hash']);
  return out;
}

/** Encode a `inputPassportElementErrorSourceFrontSide` into TDLib JSON wire shape. */
export function encodeInputPassportElementErrorSourceFrontSide(value: InputPassportElementErrorSourceFrontSide): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementErrorSourceFrontSide';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_hash'] = value.file_hash;
  return out;
}

/** inputPassportElementErrorSourceReverseSide = InputPassportElementErrorSource — The reverse side of the document contains an error. The error is considered resolved when the file with the reverse side of the document changes */
export class InputPassportElementErrorSourceReverseSide {
  type: 'inputPassportElementErrorSourceReverseSide' = 'inputPassportElementErrorSourceReverseSide';
  extra?: string;
  /** file_hash: bytes — Current hash of the file containing the reverse side */
  file_hash: string = '';
}

/** Decode a TDLib `inputPassportElementErrorSourceReverseSide` object; null input -> null. */
export function decodeInputPassportElementErrorSourceReverseSide(json: TdJsonRaw): InputPassportElementErrorSourceReverseSide | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementErrorSourceReverseSide = new InputPassportElementErrorSourceReverseSide();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_hash = asString(record['file_hash']);
  return out;
}

/** Encode a `inputPassportElementErrorSourceReverseSide` into TDLib JSON wire shape. */
export function encodeInputPassportElementErrorSourceReverseSide(value: InputPassportElementErrorSourceReverseSide): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementErrorSourceReverseSide';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_hash'] = value.file_hash;
  return out;
}

/** inputPassportElementErrorSourceSelfie = InputPassportElementErrorSource — The selfie contains an error. The error is considered resolved when the file with the selfie changes */
export class InputPassportElementErrorSourceSelfie {
  type: 'inputPassportElementErrorSourceSelfie' = 'inputPassportElementErrorSourceSelfie';
  extra?: string;
  /** file_hash: bytes — Current hash of the file containing the selfie */
  file_hash: string = '';
}

/** Decode a TDLib `inputPassportElementErrorSourceSelfie` object; null input -> null. */
export function decodeInputPassportElementErrorSourceSelfie(json: TdJsonRaw): InputPassportElementErrorSourceSelfie | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementErrorSourceSelfie = new InputPassportElementErrorSourceSelfie();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_hash = asString(record['file_hash']);
  return out;
}

/** Encode a `inputPassportElementErrorSourceSelfie` into TDLib JSON wire shape. */
export function encodeInputPassportElementErrorSourceSelfie(value: InputPassportElementErrorSourceSelfie): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementErrorSourceSelfie';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_hash'] = value.file_hash;
  return out;
}

/** inputPassportElementErrorSourceTranslationFile = InputPassportElementErrorSource — One of the files containing the translation of the document contains an error. The error is considered resolved when the file with the translation changes */
export class InputPassportElementErrorSourceTranslationFile {
  type: 'inputPassportElementErrorSourceTranslationFile' = 'inputPassportElementErrorSourceTranslationFile';
  extra?: string;
  /** file_hash: bytes — Current hash of the file containing the translation */
  file_hash: string = '';
}

/** Decode a TDLib `inputPassportElementErrorSourceTranslationFile` object; null input -> null. */
export function decodeInputPassportElementErrorSourceTranslationFile(json: TdJsonRaw): InputPassportElementErrorSourceTranslationFile | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementErrorSourceTranslationFile = new InputPassportElementErrorSourceTranslationFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_hash = asString(record['file_hash']);
  return out;
}

/** Encode a `inputPassportElementErrorSourceTranslationFile` into TDLib JSON wire shape. */
export function encodeInputPassportElementErrorSourceTranslationFile(value: InputPassportElementErrorSourceTranslationFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementErrorSourceTranslationFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_hash'] = value.file_hash;
  return out;
}

/** inputPassportElementErrorSourceTranslationFiles = InputPassportElementErrorSource — The translation of the document contains an error. The error is considered resolved when the list of files changes */
export class InputPassportElementErrorSourceTranslationFiles {
  type: 'inputPassportElementErrorSourceTranslationFiles' = 'inputPassportElementErrorSourceTranslationFiles';
  extra?: string;
  /** file_hashes: vector<bytes> — Current hashes of all files with the translation */
  file_hashes: string[] = [];
}

/** Decode a TDLib `inputPassportElementErrorSourceTranslationFiles` object; null input -> null. */
export function decodeInputPassportElementErrorSourceTranslationFiles(json: TdJsonRaw): InputPassportElementErrorSourceTranslationFiles | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementErrorSourceTranslationFiles = new InputPassportElementErrorSourceTranslationFiles();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_hashes = decodeVector(record['file_hashes'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `inputPassportElementErrorSourceTranslationFiles` into TDLib JSON wire shape. */
export function encodeInputPassportElementErrorSourceTranslationFiles(value: InputPassportElementErrorSourceTranslationFiles): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementErrorSourceTranslationFiles';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_hashes'] = encodeVector(value.file_hashes, (item: string): TdJsonRaw => item);
  return out;
}

/** inputPassportElementErrorSourceUnspecified = InputPassportElementErrorSource — The element contains an error in an unspecified place. The error will be considered resolved when new data is added */
export class InputPassportElementErrorSourceUnspecified {
  type: 'inputPassportElementErrorSourceUnspecified' = 'inputPassportElementErrorSourceUnspecified';
  extra?: string;
  /** element_hash: bytes — Current hash of the entire element */
  element_hash: string = '';
}

/** Decode a TDLib `inputPassportElementErrorSourceUnspecified` object; null input -> null. */
export function decodeInputPassportElementErrorSourceUnspecified(json: TdJsonRaw): InputPassportElementErrorSourceUnspecified | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementErrorSourceUnspecified = new InputPassportElementErrorSourceUnspecified();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.element_hash = asString(record['element_hash']);
  return out;
}

/** Encode a `inputPassportElementErrorSourceUnspecified` into TDLib JSON wire shape. */
export function encodeInputPassportElementErrorSourceUnspecified(value: InputPassportElementErrorSourceUnspecified): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementErrorSourceUnspecified';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['element_hash'] = value.element_hash;
  return out;
}

/** inputPassportElementIdentityCard = InputPassportElement — A Telegram Passport element to be saved containing the user's identity card */
export class InputPassportElementIdentityCard {
  type: 'inputPassportElementIdentityCard' = 'inputPassportElementIdentityCard';
  extra?: string;
  /** identity_card: inputIdentityDocument — The identity card to be saved */
  identity_card: InputIdentityDocument | null = null;
}

/** Decode a TDLib `inputPassportElementIdentityCard` object; null input -> null. */
export function decodeInputPassportElementIdentityCard(json: TdJsonRaw): InputPassportElementIdentityCard | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementIdentityCard = new InputPassportElementIdentityCard();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.identity_card = decodeInputIdentityDocument(record['identity_card']);
  return out;
}

/** Encode a `inputPassportElementIdentityCard` into TDLib JSON wire shape. */
export function encodeInputPassportElementIdentityCard(value: InputPassportElementIdentityCard): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementIdentityCard';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['identity_card'] = value.identity_card === null ? null : encodeInputIdentityDocument(value.identity_card);
  return out;
}

/** inputPassportElementInternalPassport = InputPassportElement — A Telegram Passport element to be saved containing the user's internal passport */
export class InputPassportElementInternalPassport {
  type: 'inputPassportElementInternalPassport' = 'inputPassportElementInternalPassport';
  extra?: string;
  /** internal_passport: inputIdentityDocument — The internal passport to be saved */
  internal_passport: InputIdentityDocument | null = null;
}

/** Decode a TDLib `inputPassportElementInternalPassport` object; null input -> null. */
export function decodeInputPassportElementInternalPassport(json: TdJsonRaw): InputPassportElementInternalPassport | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementInternalPassport = new InputPassportElementInternalPassport();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.internal_passport = decodeInputIdentityDocument(record['internal_passport']);
  return out;
}

/** Encode a `inputPassportElementInternalPassport` into TDLib JSON wire shape. */
export function encodeInputPassportElementInternalPassport(value: InputPassportElementInternalPassport): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementInternalPassport';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['internal_passport'] = value.internal_passport === null ? null : encodeInputIdentityDocument(value.internal_passport);
  return out;
}

/** inputPassportElementPassport = InputPassportElement — A Telegram Passport element to be saved containing the user's passport */
export class InputPassportElementPassport {
  type: 'inputPassportElementPassport' = 'inputPassportElementPassport';
  extra?: string;
  /** passport: inputIdentityDocument — The passport to be saved */
  passport: InputIdentityDocument | null = null;
}

/** Decode a TDLib `inputPassportElementPassport` object; null input -> null. */
export function decodeInputPassportElementPassport(json: TdJsonRaw): InputPassportElementPassport | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementPassport = new InputPassportElementPassport();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.passport = decodeInputIdentityDocument(record['passport']);
  return out;
}

/** Encode a `inputPassportElementPassport` into TDLib JSON wire shape. */
export function encodeInputPassportElementPassport(value: InputPassportElementPassport): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementPassport';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['passport'] = value.passport === null ? null : encodeInputIdentityDocument(value.passport);
  return out;
}

/** inputPassportElementPassportRegistration = InputPassportElement — A Telegram Passport element to be saved containing the user's passport registration */
export class InputPassportElementPassportRegistration {
  type: 'inputPassportElementPassportRegistration' = 'inputPassportElementPassportRegistration';
  extra?: string;
  /** passport_registration: inputPersonalDocument — The passport registration page to be saved */
  passport_registration: InputPersonalDocument | null = null;
}

/** Decode a TDLib `inputPassportElementPassportRegistration` object; null input -> null. */
export function decodeInputPassportElementPassportRegistration(json: TdJsonRaw): InputPassportElementPassportRegistration | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementPassportRegistration = new InputPassportElementPassportRegistration();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.passport_registration = decodeInputPersonalDocument(record['passport_registration']);
  return out;
}

/** Encode a `inputPassportElementPassportRegistration` into TDLib JSON wire shape. */
export function encodeInputPassportElementPassportRegistration(value: InputPassportElementPassportRegistration): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementPassportRegistration';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['passport_registration'] = value.passport_registration === null ? null : encodeInputPersonalDocument(value.passport_registration);
  return out;
}

/** inputPassportElementPersonalDetails = InputPassportElement — A Telegram Passport element to be saved containing the user's personal details */
export class InputPassportElementPersonalDetails {
  type: 'inputPassportElementPersonalDetails' = 'inputPassportElementPersonalDetails';
  extra?: string;
  /** personal_details: personalDetails — Personal details of the user */
  personal_details: PersonalDetails | null = null;
}

/** Decode a TDLib `inputPassportElementPersonalDetails` object; null input -> null. */
export function decodeInputPassportElementPersonalDetails(json: TdJsonRaw): InputPassportElementPersonalDetails | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementPersonalDetails = new InputPassportElementPersonalDetails();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.personal_details = decodePersonalDetails(record['personal_details']);
  return out;
}

/** Encode a `inputPassportElementPersonalDetails` into TDLib JSON wire shape. */
export function encodeInputPassportElementPersonalDetails(value: InputPassportElementPersonalDetails): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementPersonalDetails';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['personal_details'] = value.personal_details === null ? null : encodePersonalDetails(value.personal_details);
  return out;
}

/** inputPassportElementPhoneNumber = InputPassportElement — A Telegram Passport element to be saved containing the user's phone number */
export class InputPassportElementPhoneNumber {
  type: 'inputPassportElementPhoneNumber' = 'inputPassportElementPhoneNumber';
  extra?: string;
  /** phone_number: string — The phone number to be saved */
  phone_number: string = '';
}

/** Decode a TDLib `inputPassportElementPhoneNumber` object; null input -> null. */
export function decodeInputPassportElementPhoneNumber(json: TdJsonRaw): InputPassportElementPhoneNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementPhoneNumber = new InputPassportElementPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  return out;
}

/** Encode a `inputPassportElementPhoneNumber` into TDLib JSON wire shape. */
export function encodeInputPassportElementPhoneNumber(value: InputPassportElementPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  return out;
}

/** inputPassportElementRentalAgreement = InputPassportElement — A Telegram Passport element to be saved containing the user's rental agreement */
export class InputPassportElementRentalAgreement {
  type: 'inputPassportElementRentalAgreement' = 'inputPassportElementRentalAgreement';
  extra?: string;
  /** rental_agreement: inputPersonalDocument — The rental agreement to be saved */
  rental_agreement: InputPersonalDocument | null = null;
}

/** Decode a TDLib `inputPassportElementRentalAgreement` object; null input -> null. */
export function decodeInputPassportElementRentalAgreement(json: TdJsonRaw): InputPassportElementRentalAgreement | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementRentalAgreement = new InputPassportElementRentalAgreement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.rental_agreement = decodeInputPersonalDocument(record['rental_agreement']);
  return out;
}

/** Encode a `inputPassportElementRentalAgreement` into TDLib JSON wire shape. */
export function encodeInputPassportElementRentalAgreement(value: InputPassportElementRentalAgreement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementRentalAgreement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['rental_agreement'] = value.rental_agreement === null ? null : encodeInputPersonalDocument(value.rental_agreement);
  return out;
}

/** inputPassportElementTemporaryRegistration = InputPassportElement — A Telegram Passport element to be saved containing the user's temporary registration */
export class InputPassportElementTemporaryRegistration {
  type: 'inputPassportElementTemporaryRegistration' = 'inputPassportElementTemporaryRegistration';
  extra?: string;
  /** temporary_registration: inputPersonalDocument — The temporary registration document to be saved */
  temporary_registration: InputPersonalDocument | null = null;
}

/** Decode a TDLib `inputPassportElementTemporaryRegistration` object; null input -> null. */
export function decodeInputPassportElementTemporaryRegistration(json: TdJsonRaw): InputPassportElementTemporaryRegistration | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementTemporaryRegistration = new InputPassportElementTemporaryRegistration();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.temporary_registration = decodeInputPersonalDocument(record['temporary_registration']);
  return out;
}

/** Encode a `inputPassportElementTemporaryRegistration` into TDLib JSON wire shape. */
export function encodeInputPassportElementTemporaryRegistration(value: InputPassportElementTemporaryRegistration): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementTemporaryRegistration';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['temporary_registration'] = value.temporary_registration === null ? null : encodeInputPersonalDocument(value.temporary_registration);
  return out;
}

/** inputPassportElementUtilityBill = InputPassportElement — A Telegram Passport element to be saved containing the user's utility bill */
export class InputPassportElementUtilityBill {
  type: 'inputPassportElementUtilityBill' = 'inputPassportElementUtilityBill';
  extra?: string;
  /** utility_bill: inputPersonalDocument — The utility bill to be saved */
  utility_bill: InputPersonalDocument | null = null;
}

/** Decode a TDLib `inputPassportElementUtilityBill` object; null input -> null. */
export function decodeInputPassportElementUtilityBill(json: TdJsonRaw): InputPassportElementUtilityBill | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPassportElementUtilityBill = new InputPassportElementUtilityBill();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.utility_bill = decodeInputPersonalDocument(record['utility_bill']);
  return out;
}

/** Encode a `inputPassportElementUtilityBill` into TDLib JSON wire shape. */
export function encodeInputPassportElementUtilityBill(value: InputPassportElementUtilityBill): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPassportElementUtilityBill';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['utility_bill'] = value.utility_bill === null ? null : encodeInputPersonalDocument(value.utility_bill);
  return out;
}

/** inputPersonalDocument = InputPersonalDocument — A personal document to be saved to Telegram Passport */
export class InputPersonalDocument {
  type: 'inputPersonalDocument' = 'inputPersonalDocument';
  extra?: string;
  /** files: vector<InputFile> — List of files containing the pages of the document */
  files: TdInputFile[] = [];
  /** translation: vector<InputFile> — List of files containing a certified English translation of the document */
  translation: TdInputFile[] = [];
}

/** Decode a TDLib `inputPersonalDocument` object; null input -> null. */
export function decodeInputPersonalDocument(json: TdJsonRaw): InputPersonalDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPersonalDocument = new InputPersonalDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.files = decodeVector(record['files'], (item: TdJsonRaw): TdInputFile => decodeTdInputFile(item) as TdInputFile);
  out.translation = decodeVector(record['translation'], (item: TdJsonRaw): TdInputFile => decodeTdInputFile(item) as TdInputFile);
  return out;
}

/** Encode a `inputPersonalDocument` into TDLib JSON wire shape. */
export function encodeInputPersonalDocument(value: InputPersonalDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPersonalDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['files'] = encodeVector(value.files, (item: TdInputFile | null): TdJsonRaw => item === null ? null : encodeTdInputFile(item));
  out['translation'] = encodeVector(value.translation, (item: TdInputFile | null): TdJsonRaw => item === null ? null : encodeTdInputFile(item));
  return out;
}

/** inputPhoto = InputPhoto — A photo to be sent */
export class InputPhoto {
  type: 'inputPhoto' = 'inputPhoto';
  extra?: string;
  /** photo: InputFile — Photo to be sent. The photo must be at most 10 MB in size. The photo's width and height must not exceed 10000 in total. Width and height ratio must be at most 20 */
  photo: TdInputFile | null = null;
  /** thumbnail: inputThumbnail — Photo thumbnail; pass null to skip thumbnail uploading. The thumbnail is sent to the other party only in secret chats */
  thumbnail: InputThumbnail | null = null;
  /** video: InputFile — Video of the live photo; not supported in secret chats; pass null if the photo isn't a live photo */
  video: TdInputFile | null = null;
  /** added_sticker_file_ids: vector<int32> — File identifiers of the stickers added to the photo, if applicable */
  added_sticker_file_ids: number[] = [];
  /** width: int32 — Photo width; may be replaced by the server */
  width: number = 0;
  /** height: int32 — Photo height; may be replaced by the server */
  height: number = 0;
}

/** Decode a TDLib `inputPhoto` object; null input -> null. */
export function decodeInputPhoto(json: TdJsonRaw): InputPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPhoto = new InputPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeTdInputFile(record['photo']);
  out.thumbnail = decodeInputThumbnail(record['thumbnail']);
  out.video = decodeTdInputFile(record['video']);
  out.added_sticker_file_ids = decodeVector(record['added_sticker_file_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  return out;
}

/** Encode a `inputPhoto` into TDLib JSON wire shape. */
export function encodeInputPhoto(value: InputPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeTdInputFile(value.photo);
  out['thumbnail'] = value.thumbnail === null ? null : encodeInputThumbnail(value.thumbnail);
  out['video'] = value.video === null ? null : encodeTdInputFile(value.video);
  out['added_sticker_file_ids'] = encodeVector(value.added_sticker_file_ids, (item: number): TdJsonRaw => item);
  out['width'] = value.width;
  out['height'] = value.height;
  return out;
}

/** inputPollMediaAnimation = InputPollMedia — An animation */
export class InputPollMediaAnimation {
  type: 'inputPollMediaAnimation' = 'inputPollMediaAnimation';
  extra?: string;
  /** animation: inputAnimation — The animation to be sent */
  animation: InputAnimation | null = null;
}

/** Decode a TDLib `inputPollMediaAnimation` object; null input -> null. */
export function decodeInputPollMediaAnimation(json: TdJsonRaw): InputPollMediaAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollMediaAnimation = new InputPollMediaAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeInputAnimation(record['animation']);
  return out;
}

/** Encode a `inputPollMediaAnimation` into TDLib JSON wire shape. */
export function encodeInputPollMediaAnimation(value: InputPollMediaAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollMediaAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeInputAnimation(value.animation);
  return out;
}

/** inputPollMediaAudio = InputPollMedia — An audio */
export class InputPollMediaAudio {
  type: 'inputPollMediaAudio' = 'inputPollMediaAudio';
  extra?: string;
  /** audio: inputAudio — The audio to be sent */
  audio: InputAudio | null = null;
}

/** Decode a TDLib `inputPollMediaAudio` object; null input -> null. */
export function decodeInputPollMediaAudio(json: TdJsonRaw): InputPollMediaAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollMediaAudio = new InputPollMediaAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeInputAudio(record['audio']);
  return out;
}

/** Encode a `inputPollMediaAudio` into TDLib JSON wire shape. */
export function encodeInputPollMediaAudio(value: InputPollMediaAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollMediaAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeInputAudio(value.audio);
  return out;
}

/** inputPollMediaDocument = InputPollMedia — A document (general file) */
export class InputPollMediaDocument {
  type: 'inputPollMediaDocument' = 'inputPollMediaDocument';
  extra?: string;
  /** document: inputDocument — The document to be sent */
  document: InputDocument | null = null;
}

/** Decode a TDLib `inputPollMediaDocument` object; null input -> null. */
export function decodeInputPollMediaDocument(json: TdJsonRaw): InputPollMediaDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollMediaDocument = new InputPollMediaDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeInputDocument(record['document']);
  return out;
}

/** Encode a `inputPollMediaDocument` into TDLib JSON wire shape. */
export function encodeInputPollMediaDocument(value: InputPollMediaDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollMediaDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeInputDocument(value.document);
  return out;
}

/** inputPollMediaLink = InputPollMedia — A link */
export class InputPollMediaLink {
  type: 'inputPollMediaLink' = 'inputPollMediaLink';
  extra?: string;
  /** url: string — URL of the link */
  url: string = '';
}

/** Decode a TDLib `inputPollMediaLink` object; null input -> null. */
export function decodeInputPollMediaLink(json: TdJsonRaw): InputPollMediaLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollMediaLink = new InputPollMediaLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `inputPollMediaLink` into TDLib JSON wire shape. */
export function encodeInputPollMediaLink(value: InputPollMediaLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollMediaLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** inputPollMediaLocation = InputPollMedia — A location */
export class InputPollMediaLocation {
  type: 'inputPollMediaLocation' = 'inputPollMediaLocation';
  extra?: string;
  /** location: location — Location to be sent */
  location: Location | null = null;
}

/** Decode a TDLib `inputPollMediaLocation` object; null input -> null. */
export function decodeInputPollMediaLocation(json: TdJsonRaw): InputPollMediaLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollMediaLocation = new InputPollMediaLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  return out;
}

/** Encode a `inputPollMediaLocation` into TDLib JSON wire shape. */
export function encodeInputPollMediaLocation(value: InputPollMediaLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollMediaLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  return out;
}

/** inputPollMediaPhoto = InputPollMedia — A photo */
export class InputPollMediaPhoto {
  type: 'inputPollMediaPhoto' = 'inputPollMediaPhoto';
  extra?: string;
  /** photo: inputPhoto — Photo to be sent */
  photo: InputPhoto | null = null;
}

/** Decode a TDLib `inputPollMediaPhoto` object; null input -> null. */
export function decodeInputPollMediaPhoto(json: TdJsonRaw): InputPollMediaPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollMediaPhoto = new InputPollMediaPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeInputPhoto(record['photo']);
  return out;
}

/** Encode a `inputPollMediaPhoto` into TDLib JSON wire shape. */
export function encodeInputPollMediaPhoto(value: InputPollMediaPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollMediaPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeInputPhoto(value.photo);
  return out;
}

/** inputPollMediaSticker = InputPollMedia — A sticker */
export class InputPollMediaSticker {
  type: 'inputPollMediaSticker' = 'inputPollMediaSticker';
  extra?: string;
  /** sticker: inputSticker — Sticker to be sent */
  sticker: InputSticker | null = null;
}

/** Decode a TDLib `inputPollMediaSticker` object; null input -> null. */
export function decodeInputPollMediaSticker(json: TdJsonRaw): InputPollMediaSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollMediaSticker = new InputPollMediaSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeInputSticker(record['sticker']);
  return out;
}

/** Encode a `inputPollMediaSticker` into TDLib JSON wire shape. */
export function encodeInputPollMediaSticker(value: InputPollMediaSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollMediaSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeInputSticker(value.sticker);
  return out;
}

/** inputPollMediaVenue = InputPollMedia — A venue */
export class InputPollMediaVenue {
  type: 'inputPollMediaVenue' = 'inputPollMediaVenue';
  extra?: string;
  /** venue: venue — Venue to send */
  venue: Venue | null = null;
}

/** Decode a TDLib `inputPollMediaVenue` object; null input -> null. */
export function decodeInputPollMediaVenue(json: TdJsonRaw): InputPollMediaVenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollMediaVenue = new InputPollMediaVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.venue = decodeVenue(record['venue']);
  return out;
}

/** Encode a `inputPollMediaVenue` into TDLib JSON wire shape. */
export function encodeInputPollMediaVenue(value: InputPollMediaVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollMediaVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['venue'] = value.venue === null ? null : encodeVenue(value.venue);
  return out;
}

/** inputPollMediaVideo = InputPollMedia — A video */
export class InputPollMediaVideo {
  type: 'inputPollMediaVideo' = 'inputPollMediaVideo';
  extra?: string;
  /** video: inputVideo — The video to be sent */
  video: InputVideo | null = null;
}

/** Decode a TDLib `inputPollMediaVideo` object; null input -> null. */
export function decodeInputPollMediaVideo(json: TdJsonRaw): InputPollMediaVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollMediaVideo = new InputPollMediaVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeInputVideo(record['video']);
  return out;
}

/** Encode a `inputPollMediaVideo` into TDLib JSON wire shape. */
export function encodeInputPollMediaVideo(value: InputPollMediaVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollMediaVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeInputVideo(value.video);
  return out;
}

/** inputPollOption = InputPollOption — Describes one answer option of a poll to be created inputPollMediaAnimation, inputPollMediaLink, inputPollMediaLocation, inputPollMediaPhoto, inputPollMediaSticker, inputPollMediaVenue, or inputPollMed... */
export class InputPollOption {
  type: 'inputPollOption' = 'inputPollOption';
  extra?: string;
  /** text: formattedText — Option text; 1-100 characters. Only custom emoji entities are allowed to be added and only by Premium users */
  text: FormattedText | null = null;
  /** media: InputPollMedia — Option media; pass null if none. Must be one of the following types: */
  media: TdInputPollMedia | null = null;
}

/** Decode a TDLib `inputPollOption` object; null input -> null. */
export function decodeInputPollOption(json: TdJsonRaw): InputPollOption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollOption = new InputPollOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.media = decodeTdInputPollMedia(record['media']);
  return out;
}

/** Encode a `inputPollOption` into TDLib JSON wire shape. */
export function encodeInputPollOption(value: InputPollOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['media'] = value.media === null ? null : encodeTdInputPollMedia(value.media);
  return out;
}

/** inputPollTypeQuiz = InputPollType — A poll in quiz mode, which has predefined correct answers inputPollMediaAnimation, inputPollMediaAudio, inputPollMediaDocument, inputPollMediaLocation, inputPollMediaPhoto, inputPollMediaVenue, or inpu... */
export class InputPollTypeQuiz {
  type: 'inputPollTypeQuiz' = 'inputPollTypeQuiz';
  extra?: string;
  /** correct_option_ids: vector<int32> — Increasing list of 0-based identifiers of the correct answer options; must be non-empty */
  correct_option_ids: number[] = [];
  /** explanation: formattedText — Text that is shown when the user chooses an incorrect answer or taps on the lamp icon; 0-200 characters with at most 2 line feeds */
  explanation: FormattedText | null = null;
  /** explanation_media: InputPollMedia — Media that is shown when the user chooses an incorrect answer or taps on the lamp icon; pass null if none. Must be one of the following types: */
  explanation_media: TdInputPollMedia | null = null;
}

/** Decode a TDLib `inputPollTypeQuiz` object; null input -> null. */
export function decodeInputPollTypeQuiz(json: TdJsonRaw): InputPollTypeQuiz | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollTypeQuiz = new InputPollTypeQuiz();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.correct_option_ids = decodeVector(record['correct_option_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.explanation = decodeFormattedText(record['explanation']);
  out.explanation_media = decodeTdInputPollMedia(record['explanation_media']);
  return out;
}

/** Encode a `inputPollTypeQuiz` into TDLib JSON wire shape. */
export function encodeInputPollTypeQuiz(value: InputPollTypeQuiz): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollTypeQuiz';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['correct_option_ids'] = encodeVector(value.correct_option_ids, (item: number): TdJsonRaw => item);
  out['explanation'] = value.explanation === null ? null : encodeFormattedText(value.explanation);
  out['explanation_media'] = value.explanation_media === null ? null : encodeTdInputPollMedia(value.explanation_media);
  return out;
}

/** inputPollTypeRegular = InputPollType — A regular poll */
export class InputPollTypeRegular {
  type: 'inputPollTypeRegular' = 'inputPollTypeRegular';
  extra?: string;
  /** allow_adding_options: Bool — True, if answer options can be added to the poll after creation; not supported in channel chats and for anonymous polls */
  allow_adding_options: boolean = false;
}

/** Decode a TDLib `inputPollTypeRegular` object; null input -> null. */
export function decodeInputPollTypeRegular(json: TdJsonRaw): InputPollTypeRegular | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputPollTypeRegular = new InputPollTypeRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.allow_adding_options = asBool(record['allow_adding_options']);
  return out;
}

/** Encode a `inputPollTypeRegular` into TDLib JSON wire shape. */
export function encodeInputPollTypeRegular(value: InputPollTypeRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputPollTypeRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['allow_adding_options'] = value.allow_adding_options;
  return out;
}

/** inputRichMessage = InputRichMessage — A rich message to send. Total length of all texts, including custom emoji alternative text and formula source, must not exceed getOption("rich_message_text_length_max"). The total number of all block... */
export class InputRichMessage {
  type: 'inputRichMessage' = 'inputRichMessage';
  extra?: string;
  /** source: RichMessageSource — Source of the rich message */
  source: TdRichMessageSource | null = null;
  /** is_rtl: Bool — Pass true if the message must be shown from right to left */
  is_rtl: boolean = false;
  /** detect_automatic_blocks: Bool — Pass true to enable detection of URLs, email addresses and other automatic blocks */
  detect_automatic_blocks: boolean = false;
}

/** Decode a TDLib `inputRichMessage` object; null input -> null. */
export function decodeInputRichMessage(json: TdJsonRaw): InputRichMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputRichMessage = new InputRichMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.source = decodeTdRichMessageSource(record['source']);
  out.is_rtl = asBool(record['is_rtl']);
  out.detect_automatic_blocks = asBool(record['detect_automatic_blocks']);
  return out;
}

/** Encode a `inputRichMessage` into TDLib JSON wire shape. */
export function encodeInputRichMessage(value: InputRichMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputRichMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['source'] = value.source === null ? null : encodeTdRichMessageSource(value.source);
  out['is_rtl'] = value.is_rtl;
  out['detect_automatic_blocks'] = value.detect_automatic_blocks;
  return out;
}

/** inputRichMessageMedia = InputRichMessageMedia — Describes a media to be used in a sent rich message */
export class InputRichMessageMedia {
  type: 'inputRichMessageMedia' = 'inputRichMessageMedia';
  extra?: string;
  /** id: string — Unique identifier of the media; 1-64 base64url characters */
  id: string = '';
  /** media: InputMessageContent — The media to send. Must be one of the following types: inputMessageAnimation, inputMessageAudio, inputMessagePhoto, inputMessageVideo, or inputMessageVoiceNote */
  media: TdInputMessageContent | null = null;
}

/** Decode a TDLib `inputRichMessageMedia` object; null input -> null. */
export function decodeInputRichMessageMedia(json: TdJsonRaw): InputRichMessageMedia | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputRichMessageMedia = new InputRichMessageMedia();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.media = decodeTdInputMessageContent(record['media']);
  return out;
}

/** Encode a `inputRichMessageMedia` into TDLib JSON wire shape. */
export function encodeInputRichMessageMedia(value: InputRichMessageMedia): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputRichMessageMedia';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['media'] = value.media === null ? null : encodeTdInputMessageContent(value.media);
  return out;
}

/** inputSticker = InputSticker — A sticker to be sent */
export class InputSticker {
  type: 'inputSticker' = 'inputSticker';
  extra?: string;
  /** sticker: InputFile — Sticker to be sent */
  sticker: TdInputFile | null = null;
  /** thumbnail: inputThumbnail — Sticker thumbnail; pass null to skip thumbnail uploading */
  thumbnail: InputThumbnail | null = null;
  /** width: int32 — Sticker width */
  width: number = 0;
  /** height: int32 — Sticker height */
  height: number = 0;
}

/** Decode a TDLib `inputSticker` object; null input -> null. */
export function decodeInputSticker(json: TdJsonRaw): InputSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputSticker = new InputSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  out.thumbnail = decodeInputThumbnail(record['thumbnail']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  return out;
}

/** Encode a `inputSticker` into TDLib JSON wire shape. */
export function encodeInputSticker(value: InputSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  out['thumbnail'] = value.thumbnail === null ? null : encodeInputThumbnail(value.thumbnail);
  out['width'] = value.width;
  out['height'] = value.height;
  return out;
}

/** inputStoryArea = InputStoryArea — Describes a clickable rectangle area on a story media to be added */
export class InputStoryArea {
  type: 'inputStoryArea' = 'inputStoryArea';
  extra?: string;
  /** position: storyAreaPosition — Position of the area */
  position: StoryAreaPosition | null = null;
  /** type: InputStoryAreaType — Type of the area */
  type_: TdInputStoryAreaType | null = null;
}

/** Decode a TDLib `inputStoryArea` object; null input -> null. */
export function decodeInputStoryArea(json: TdJsonRaw): InputStoryArea | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryArea = new InputStoryArea();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.position = decodeStoryAreaPosition(record['position']);
  out.type_ = decodeTdInputStoryAreaType(record['type']);
  return out;
}

/** Encode a `inputStoryArea` into TDLib JSON wire shape. */
export function encodeInputStoryArea(value: InputStoryArea): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryArea';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['position'] = value.position === null ? null : encodeStoryAreaPosition(value.position);
  out['type'] = value.type_ === null ? null : encodeTdInputStoryAreaType(value.type_);
  return out;
}

/** inputStoryAreaTypeFoundVenue = InputStoryAreaType — An area pointing to a venue found by the bot getOption("venue_search_bot_username") */
export class InputStoryAreaTypeFoundVenue {
  type: 'inputStoryAreaTypeFoundVenue' = 'inputStoryAreaTypeFoundVenue';
  extra?: string;
  /** query_id: int64 — Identifier of the inline query, used to found the venue */
  query_id: string = '';
  /** result_id: string — Identifier of the inline query result */
  result_id: string = '';
}

/** Decode a TDLib `inputStoryAreaTypeFoundVenue` object; null input -> null. */
export function decodeInputStoryAreaTypeFoundVenue(json: TdJsonRaw): InputStoryAreaTypeFoundVenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryAreaTypeFoundVenue = new InputStoryAreaTypeFoundVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query_id = asInt64(record['query_id']);
  out.result_id = asString(record['result_id']);
  return out;
}

/** Encode a `inputStoryAreaTypeFoundVenue` into TDLib JSON wire shape. */
export function encodeInputStoryAreaTypeFoundVenue(value: InputStoryAreaTypeFoundVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryAreaTypeFoundVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query_id'] = value.query_id;
  out['result_id'] = value.result_id;
  return out;
}

/** inputStoryAreaTypeLink = InputStoryAreaType — An area pointing to a HTTP or tg:// link */
export class InputStoryAreaTypeLink {
  type: 'inputStoryAreaTypeLink' = 'inputStoryAreaTypeLink';
  extra?: string;
  /** url: string — HTTP or tg:// URL to be opened when the area is clicked */
  url: string = '';
}

/** Decode a TDLib `inputStoryAreaTypeLink` object; null input -> null. */
export function decodeInputStoryAreaTypeLink(json: TdJsonRaw): InputStoryAreaTypeLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryAreaTypeLink = new InputStoryAreaTypeLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `inputStoryAreaTypeLink` into TDLib JSON wire shape. */
export function encodeInputStoryAreaTypeLink(value: InputStoryAreaTypeLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryAreaTypeLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** inputStoryAreaTypeLocation = InputStoryAreaType — An area pointing to a location */
export class InputStoryAreaTypeLocation {
  type: 'inputStoryAreaTypeLocation' = 'inputStoryAreaTypeLocation';
  extra?: string;
  /** location: location — The location */
  location: Location | null = null;
  /** address: locationAddress — Address of the location; pass null if unknown */
  address: LocationAddress | null = null;
}

/** Decode a TDLib `inputStoryAreaTypeLocation` object; null input -> null. */
export function decodeInputStoryAreaTypeLocation(json: TdJsonRaw): InputStoryAreaTypeLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryAreaTypeLocation = new InputStoryAreaTypeLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  out.address = decodeLocationAddress(record['address']);
  return out;
}

/** Encode a `inputStoryAreaTypeLocation` into TDLib JSON wire shape. */
export function encodeInputStoryAreaTypeLocation(value: InputStoryAreaTypeLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryAreaTypeLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['address'] = value.address === null ? null : encodeLocationAddress(value.address);
  return out;
}

/** inputStoryAreaTypeMessage = InputStoryAreaType — An area pointing to a message */
export class InputStoryAreaTypeMessage {
  type: 'inputStoryAreaTypeMessage' = 'inputStoryAreaTypeMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message. Currently, the chat must be a supergroup or a channel chat */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use messageProperties.can_be_shared_in_story to check whether the message is suitable */
  message_id: number = 0;
}

/** Decode a TDLib `inputStoryAreaTypeMessage` object; null input -> null. */
export function decodeInputStoryAreaTypeMessage(json: TdJsonRaw): InputStoryAreaTypeMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryAreaTypeMessage = new InputStoryAreaTypeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `inputStoryAreaTypeMessage` into TDLib JSON wire shape. */
export function encodeInputStoryAreaTypeMessage(value: InputStoryAreaTypeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryAreaTypeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** inputStoryAreaTypePreviousVenue = InputStoryAreaType — An area pointing to a venue already added to the story */
export class InputStoryAreaTypePreviousVenue {
  type: 'inputStoryAreaTypePreviousVenue' = 'inputStoryAreaTypePreviousVenue';
  extra?: string;
  /** venue_provider: string — Provider of the venue */
  venue_provider: string = '';
  /** venue_id: string — Identifier of the venue in the provider database */
  venue_id: string = '';
}

/** Decode a TDLib `inputStoryAreaTypePreviousVenue` object; null input -> null. */
export function decodeInputStoryAreaTypePreviousVenue(json: TdJsonRaw): InputStoryAreaTypePreviousVenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryAreaTypePreviousVenue = new InputStoryAreaTypePreviousVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.venue_provider = asString(record['venue_provider']);
  out.venue_id = asString(record['venue_id']);
  return out;
}

/** Encode a `inputStoryAreaTypePreviousVenue` into TDLib JSON wire shape. */
export function encodeInputStoryAreaTypePreviousVenue(value: InputStoryAreaTypePreviousVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryAreaTypePreviousVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['venue_provider'] = value.venue_provider;
  out['venue_id'] = value.venue_id;
  return out;
}

/** inputStoryAreaTypeSuggestedReaction = InputStoryAreaType — An area pointing to a suggested reaction */
export class InputStoryAreaTypeSuggestedReaction {
  type: 'inputStoryAreaTypeSuggestedReaction' = 'inputStoryAreaTypeSuggestedReaction';
  extra?: string;
  /** reaction_type: ReactionType — Type of the reaction */
  reaction_type: TdReactionType | null = null;
  /** is_dark: Bool — True, if reaction has a dark background */
  is_dark: boolean = false;
  /** is_flipped: Bool — True, if reaction corner is flipped */
  is_flipped: boolean = false;
}

/** Decode a TDLib `inputStoryAreaTypeSuggestedReaction` object; null input -> null. */
export function decodeInputStoryAreaTypeSuggestedReaction(json: TdJsonRaw): InputStoryAreaTypeSuggestedReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryAreaTypeSuggestedReaction = new InputStoryAreaTypeSuggestedReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reaction_type = decodeTdReactionType(record['reaction_type']);
  out.is_dark = asBool(record['is_dark']);
  out.is_flipped = asBool(record['is_flipped']);
  return out;
}

/** Encode a `inputStoryAreaTypeSuggestedReaction` into TDLib JSON wire shape. */
export function encodeInputStoryAreaTypeSuggestedReaction(value: InputStoryAreaTypeSuggestedReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryAreaTypeSuggestedReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reaction_type'] = value.reaction_type === null ? null : encodeTdReactionType(value.reaction_type);
  out['is_dark'] = value.is_dark;
  out['is_flipped'] = value.is_flipped;
  return out;
}

/** inputStoryAreaTypeUpgradedGift = InputStoryAreaType — An area with an upgraded gift */
export class InputStoryAreaTypeUpgradedGift {
  type: 'inputStoryAreaTypeUpgradedGift' = 'inputStoryAreaTypeUpgradedGift';
  extra?: string;
  /** gift_name: string — Unique name of the upgraded gift */
  gift_name: string = '';
}

/** Decode a TDLib `inputStoryAreaTypeUpgradedGift` object; null input -> null. */
export function decodeInputStoryAreaTypeUpgradedGift(json: TdJsonRaw): InputStoryAreaTypeUpgradedGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryAreaTypeUpgradedGift = new InputStoryAreaTypeUpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_name = asString(record['gift_name']);
  return out;
}

/** Encode a `inputStoryAreaTypeUpgradedGift` into TDLib JSON wire shape. */
export function encodeInputStoryAreaTypeUpgradedGift(value: InputStoryAreaTypeUpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryAreaTypeUpgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_name'] = value.gift_name;
  return out;
}

/** inputStoryAreaTypeWeather = InputStoryAreaType — An area with information about weather */
export class InputStoryAreaTypeWeather {
  type: 'inputStoryAreaTypeWeather' = 'inputStoryAreaTypeWeather';
  extra?: string;
  /** temperature: double — Temperature, in degree Celsius */
  temperature: number = 0;
  /** emoji: string — Emoji representing the weather */
  emoji: string = '';
  /** background_color: int32 — A color of the area background in the ARGB format */
  background_color: number = 0;
}

/** Decode a TDLib `inputStoryAreaTypeWeather` object; null input -> null. */
export function decodeInputStoryAreaTypeWeather(json: TdJsonRaw): InputStoryAreaTypeWeather | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryAreaTypeWeather = new InputStoryAreaTypeWeather();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.temperature = asNumber(record['temperature']);
  out.emoji = asString(record['emoji']);
  out.background_color = asNumber(record['background_color']);
  return out;
}

/** Encode a `inputStoryAreaTypeWeather` into TDLib JSON wire shape. */
export function encodeInputStoryAreaTypeWeather(value: InputStoryAreaTypeWeather): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryAreaTypeWeather';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['temperature'] = value.temperature;
  out['emoji'] = value.emoji;
  out['background_color'] = value.background_color;
  return out;
}

/** inputStoryAreas = InputStoryAreas — Contains a list of story areas to be added up to 10 inputStoryAreaTypeLocation, inputStoryAreaTypeFoundVenue, and inputStoryAreaTypePreviousVenue areas, up to getOption("story_suggested_reaction_area_c... */
export class InputStoryAreas {
  type: 'inputStoryAreas' = 'inputStoryAreas';
  extra?: string;
  /** areas: vector<inputStoryArea> — List of input story areas. Currently, a story can have */
  areas: InputStoryArea[] = [];
}

/** Decode a TDLib `inputStoryAreas` object; null input -> null. */
export function decodeInputStoryAreas(json: TdJsonRaw): InputStoryAreas | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryAreas = new InputStoryAreas();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.areas = decodeVector(record['areas'], (item: TdJsonRaw): InputStoryArea => decodeInputStoryArea(item) as InputStoryArea);
  return out;
}

/** Encode a `inputStoryAreas` into TDLib JSON wire shape. */
export function encodeInputStoryAreas(value: InputStoryAreas): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryAreas';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['areas'] = encodeVector(value.areas, (item: InputStoryArea | null): TdJsonRaw => item === null ? null : encodeInputStoryArea(item));
  return out;
}

/** inputStoryContentPhoto = InputStoryContent — A photo story */
export class InputStoryContentPhoto {
  type: 'inputStoryContentPhoto' = 'inputStoryContentPhoto';
  extra?: string;
  /** photo: InputFile — Photo to send. The photo must be at most 10 MB in size. The photo size must be 1080x1920 */
  photo: TdInputFile | null = null;
  /** added_sticker_file_ids: vector<int32> — File identifiers of the stickers added to the photo, if applicable */
  added_sticker_file_ids: number[] = [];
}

/** Decode a TDLib `inputStoryContentPhoto` object; null input -> null. */
export function decodeInputStoryContentPhoto(json: TdJsonRaw): InputStoryContentPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryContentPhoto = new InputStoryContentPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeTdInputFile(record['photo']);
  out.added_sticker_file_ids = decodeVector(record['added_sticker_file_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `inputStoryContentPhoto` into TDLib JSON wire shape. */
export function encodeInputStoryContentPhoto(value: InputStoryContentPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryContentPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeTdInputFile(value.photo);
  out['added_sticker_file_ids'] = encodeVector(value.added_sticker_file_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** inputStoryContentVideo = InputStoryContent — A video story */
export class InputStoryContentVideo {
  type: 'inputStoryContentVideo' = 'inputStoryContentVideo';
  extra?: string;
  /** video: InputFile — Video to be sent. The video size must be 720x1280. The video must be streamable and stored in MPEG4 format, after encoding with H.265 codec and key frames added each second */
  video: TdInputFile | null = null;
  /** added_sticker_file_ids: vector<int32> — File identifiers of the stickers added to the video, if applicable */
  added_sticker_file_ids: number[] = [];
  /** duration: double — Precise duration of the video, in seconds; 0-60 */
  duration: number = 0;
  /** cover_frame_timestamp: double — Timestamp of the frame, which will be used as video thumbnail */
  cover_frame_timestamp: number = 0;
  /** is_animation: Bool — True, if the video has no sound */
  is_animation: boolean = false;
}

/** Decode a TDLib `inputStoryContentVideo` object; null input -> null. */
export function decodeInputStoryContentVideo(json: TdJsonRaw): InputStoryContentVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputStoryContentVideo = new InputStoryContentVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeTdInputFile(record['video']);
  out.added_sticker_file_ids = decodeVector(record['added_sticker_file_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.duration = asNumber(record['duration']);
  out.cover_frame_timestamp = asNumber(record['cover_frame_timestamp']);
  out.is_animation = asBool(record['is_animation']);
  return out;
}

/** Encode a `inputStoryContentVideo` into TDLib JSON wire shape. */
export function encodeInputStoryContentVideo(value: InputStoryContentVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputStoryContentVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeTdInputFile(value.video);
  out['added_sticker_file_ids'] = encodeVector(value.added_sticker_file_ids, (item: number): TdJsonRaw => item);
  out['duration'] = value.duration;
  out['cover_frame_timestamp'] = value.cover_frame_timestamp;
  out['is_animation'] = value.is_animation;
  return out;
}

/** inputSuggestedPostInfo = InputSuggestedPostInfo — Contains information about a post to suggest and doesn't have enough funds to pay for the post, then the error "BALANCE_TOO_LOW" will be returned immediately then the date must be getOpti... */
export class InputSuggestedPostInfo {
  type: 'inputSuggestedPostInfo' = 'inputSuggestedPostInfo';
  extra?: string;
  /** price: SuggestedPostPrice — Price of the suggested post; pass null to suggest a post without payment. If the current user isn't an administrator of the channel direct messages chat */
  price: TdSuggestedPostPrice | null = null;
  /** send_date: int32 — Point in time (Unix timestamp) when the post is expected to be published; pass 0 if the date isn't restricted. If specified, */
  send_date: number = 0;
}

/** Decode a TDLib `inputSuggestedPostInfo` object; null input -> null. */
export function decodeInputSuggestedPostInfo(json: TdJsonRaw): InputSuggestedPostInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputSuggestedPostInfo = new InputSuggestedPostInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.price = decodeTdSuggestedPostPrice(record['price']);
  out.send_date = asNumber(record['send_date']);
  return out;
}

/** Encode a `inputSuggestedPostInfo` into TDLib JSON wire shape. */
export function encodeInputSuggestedPostInfo(value: InputSuggestedPostInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputSuggestedPostInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['price'] = value.price === null ? null : encodeTdSuggestedPostPrice(value.price);
  out['send_date'] = value.send_date;
  return out;
}

/** inputTextQuote = InputTextQuote — Describes manually chosen quote from another message */
export class InputTextQuote {
  type: 'inputTextQuote' = 'inputTextQuote';
  extra?: string;
  /** text: formattedText — Text of the quote; 0-getOption("message_reply_quote_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed to be kept and must be kept in the quote */
  text: FormattedText | null = null;
  /** position: int32 — Quote position in the original message in UTF-16 code units */
  position: number = 0;
}

/** Decode a TDLib `inputTextQuote` object; null input -> null. */
export function decodeInputTextQuote(json: TdJsonRaw): InputTextQuote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputTextQuote = new InputTextQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.position = asNumber(record['position']);
  return out;
}

/** Encode a `inputTextQuote` into TDLib JSON wire shape. */
export function encodeInputTextQuote(value: InputTextQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputTextQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['position'] = value.position;
  return out;
}

/** inputThumbnail = InputThumbnail — A thumbnail to be sent along with a file; must be in JPEG or WEBP format for stickers, and less than 200 KB in size */
export class InputThumbnail {
  type: 'inputThumbnail' = 'inputThumbnail';
  extra?: string;
  /** thumbnail: InputFile — Thumbnail file to send. Sending thumbnails by file_id is currently not supported */
  thumbnail: TdInputFile | null = null;
  /** width: int32 — Thumbnail width, usually shouldn't exceed 320. Use 0 if unknown */
  width: number = 0;
  /** height: int32 — Thumbnail height, usually shouldn't exceed 320. Use 0 if unknown */
  height: number = 0;
}

/** Decode a TDLib `inputThumbnail` object; null input -> null. */
export function decodeInputThumbnail(json: TdJsonRaw): InputThumbnail | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputThumbnail = new InputThumbnail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.thumbnail = decodeTdInputFile(record['thumbnail']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  return out;
}

/** Encode a `inputThumbnail` into TDLib JSON wire shape. */
export function encodeInputThumbnail(value: InputThumbnail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputThumbnail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['thumbnail'] = value.thumbnail === null ? null : encodeTdInputFile(value.thumbnail);
  out['width'] = value.width;
  out['height'] = value.height;
  return out;
}

/** inputVideo = InputVideo — A video to be sent */
export class InputVideo {
  type: 'inputVideo' = 'inputVideo';
  extra?: string;
  /** video: InputFile — Video file to be sent. The video is expected to be re-encoded to MPEG4 format with H.264 codec by the sender */
  video: TdInputFile | null = null;
  /** thumbnail: inputThumbnail — Video thumbnail; pass null to skip thumbnail uploading */
  thumbnail: InputThumbnail | null = null;
  /** cover: InputFile — Cover of the video; pass null to skip cover uploading; not supported in secret chats and for self-destructing messages */
  cover: TdInputFile | null = null;
  /** start_timestamp: int32 — Timestamp from which the video playing must start, in seconds */
  start_timestamp: number = 0;
  /** added_sticker_file_ids: vector<int32> — File identifiers of the stickers added to the video, if applicable */
  added_sticker_file_ids: number[] = [];
  /** duration: int32 — Duration of the video, in seconds */
  duration: number = 0;
  /** width: int32 — Video width */
  width: number = 0;
  /** height: int32 — Video height */
  height: number = 0;
  /** supports_streaming: Bool — True, if the video is expected to be streamed */
  supports_streaming: boolean = false;
}

/** Decode a TDLib `inputVideo` object; null input -> null. */
export function decodeInputVideo(json: TdJsonRaw): InputVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputVideo = new InputVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeTdInputFile(record['video']);
  out.thumbnail = decodeInputThumbnail(record['thumbnail']);
  out.cover = decodeTdInputFile(record['cover']);
  out.start_timestamp = asNumber(record['start_timestamp']);
  out.added_sticker_file_ids = decodeVector(record['added_sticker_file_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.duration = asNumber(record['duration']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.supports_streaming = asBool(record['supports_streaming']);
  return out;
}

/** Encode a `inputVideo` into TDLib JSON wire shape. */
export function encodeInputVideo(value: InputVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeTdInputFile(value.video);
  out['thumbnail'] = value.thumbnail === null ? null : encodeInputThumbnail(value.thumbnail);
  out['cover'] = value.cover === null ? null : encodeTdInputFile(value.cover);
  out['start_timestamp'] = value.start_timestamp;
  out['added_sticker_file_ids'] = encodeVector(value.added_sticker_file_ids, (item: number): TdJsonRaw => item);
  out['duration'] = value.duration;
  out['width'] = value.width;
  out['height'] = value.height;
  out['supports_streaming'] = value.supports_streaming;
  return out;
}

/** inputVideoNote = InputVideoNote — A video note to be sent */
export class InputVideoNote {
  type: 'inputVideoNote' = 'inputVideoNote';
  extra?: string;
  /** video_note: InputFile — Video note file to be sent. The video is expected to be encoded to MPEG4 format with H.264 codec and have no data outside of the visible circle */
  video_note: TdInputFile | null = null;
  /** thumbnail: inputThumbnail — Video thumbnail; may be null if empty; pass null to skip thumbnail uploading */
  thumbnail: InputThumbnail | null = null;
  /** duration: int32 — Duration of the video, in seconds; 0-60 */
  duration: number = 0;
  /** length: int32 — Video width and height; must be positive and not greater than 640 */
  length: number = 0;
}

/** Decode a TDLib `inputVideoNote` object; null input -> null. */
export function decodeInputVideoNote(json: TdJsonRaw): InputVideoNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputVideoNote = new InputVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video_note = decodeTdInputFile(record['video_note']);
  out.thumbnail = decodeInputThumbnail(record['thumbnail']);
  out.duration = asNumber(record['duration']);
  out.length = asNumber(record['length']);
  return out;
}

/** Encode a `inputVideoNote` into TDLib JSON wire shape. */
export function encodeInputVideoNote(value: InputVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video_note'] = value.video_note === null ? null : encodeTdInputFile(value.video_note);
  out['thumbnail'] = value.thumbnail === null ? null : encodeInputThumbnail(value.thumbnail);
  out['duration'] = value.duration;
  out['length'] = value.length;
  return out;
}

/** inputVoiceNote = InputVoiceNote — A video note to be sent */
export class InputVoiceNote {
  type: 'inputVoiceNote' = 'inputVoiceNote';
  extra?: string;
  /** voice_note: InputFile — Voice note file to be sent. The voice note must be encoded with the Opus codec and stored inside an OGG container with a single audio channel, or be in MP3 or M4A format as regular audio */
  voice_note: TdInputFile | null = null;
  /** duration: int32 — Duration of the voice note, in seconds */
  duration: number = 0;
  /** waveform: bytes — Waveform representation of the voice note in 5-bit format */
  waveform: string = '';
}

/** Decode a TDLib `inputVoiceNote` object; null input -> null. */
export function decodeInputVoiceNote(json: TdJsonRaw): InputVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InputVoiceNote = new InputVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.voice_note = decodeTdInputFile(record['voice_note']);
  out.duration = asNumber(record['duration']);
  out.waveform = asString(record['waveform']);
  return out;
}

/** Encode a `inputVoiceNote` into TDLib JSON wire shape. */
export function encodeInputVoiceNote(value: InputVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inputVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['voice_note'] = value.voice_note === null ? null : encodeTdInputFile(value.voice_note);
  out['duration'] = value.duration;
  out['waveform'] = value.waveform;
  return out;
}

/** internalLinkTypeAttachmentMenuBot = InternalLinkType — The link is a link to an attachment menu bot to be opened in the specified or a chosen chat. Process given target_chat to open the chat. Then, call searchPublicChat with the given bo... */
export class InternalLinkTypeAttachmentMenuBot {
  type: 'internalLinkTypeAttachmentMenuBot' = 'internalLinkTypeAttachmentMenuBot';
  extra?: string;
  /** target_chat: TargetChat — Target chat to be opened */
  target_chat: TdTargetChat | null = null;
  /** bot_username: string — Username of the bot */
  bot_username: string = '';
  /** url: string — URL to be passed to openWebApp */
  url: string = '';
}

/** Decode a TDLib `internalLinkTypeAttachmentMenuBot` object; null input -> null. */
export function decodeInternalLinkTypeAttachmentMenuBot(json: TdJsonRaw): InternalLinkTypeAttachmentMenuBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeAttachmentMenuBot = new InternalLinkTypeAttachmentMenuBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.target_chat = decodeTdTargetChat(record['target_chat']);
  out.bot_username = asString(record['bot_username']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `internalLinkTypeAttachmentMenuBot` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeAttachmentMenuBot(value: InternalLinkTypeAttachmentMenuBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeAttachmentMenuBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['target_chat'] = value.target_chat === null ? null : encodeTdTargetChat(value.target_chat);
  out['bot_username'] = value.bot_username;
  out['url'] = value.url;
  return out;
}

/** internalLinkTypeAuthenticationCode = InternalLinkType — The link contains an authentication code. Call checkAuthenticationCode with the code if the current authorization state is authorizationStateWaitCode */
export class InternalLinkTypeAuthenticationCode {
  type: 'internalLinkTypeAuthenticationCode' = 'internalLinkTypeAuthenticationCode';
  extra?: string;
  /** code: string — The authentication code */
  code: string = '';
}

/** Decode a TDLib `internalLinkTypeAuthenticationCode` object; null input -> null. */
export function decodeInternalLinkTypeAuthenticationCode(json: TdJsonRaw): InternalLinkTypeAuthenticationCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeAuthenticationCode = new InternalLinkTypeAuthenticationCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asString(record['code']);
  return out;
}

/** Encode a `internalLinkTypeAuthenticationCode` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeAuthenticationCode(value: InternalLinkTypeAuthenticationCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeAuthenticationCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  return out;
}

/** internalLinkTypeBackground = InternalLinkType — The link is a link to a background. Call searchBackground with the given background name to process the link. If background is found and the user wants to apply it, then call setDefaultBack... */
export class InternalLinkTypeBackground {
  type: 'internalLinkTypeBackground' = 'internalLinkTypeBackground';
  extra?: string;
  /** background_name: string — Name of the background */
  background_name: string = '';
}

/** Decode a TDLib `internalLinkTypeBackground` object; null input -> null. */
export function decodeInternalLinkTypeBackground(json: TdJsonRaw): InternalLinkTypeBackground | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeBackground = new InternalLinkTypeBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.background_name = asString(record['background_name']);
  return out;
}

/** Encode a `internalLinkTypeBackground` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeBackground(value: InternalLinkTypeBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['background_name'] = value.background_name;
  return out;
}

/** internalLinkTypeBotAddToChannel = InternalLinkType — The link is a link to a Telegram bot, which is expected to be added to a channel chat as an administrator. Call searchPublicChat with the given bot username and check that the user is ... */
export class InternalLinkTypeBotAddToChannel {
  type: 'internalLinkTypeBotAddToChannel' = 'internalLinkTypeBotAddToChannel';
  extra?: string;
  /** bot_username: string — Username of the bot */
  bot_username: string = '';
  /** administrator_rights: chatAdministratorRights — Expected administrator rights for the bot */
  administrator_rights: ChatAdministratorRights | null = null;
}

/** Decode a TDLib `internalLinkTypeBotAddToChannel` object; null input -> null. */
export function decodeInternalLinkTypeBotAddToChannel(json: TdJsonRaw): InternalLinkTypeBotAddToChannel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeBotAddToChannel = new InternalLinkTypeBotAddToChannel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_username = asString(record['bot_username']);
  out.administrator_rights = decodeChatAdministratorRights(record['administrator_rights']);
  return out;
}

/** Encode a `internalLinkTypeBotAddToChannel` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeBotAddToChannel(value: InternalLinkTypeBotAddToChannel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeBotAddToChannel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_username'] = value.bot_username;
  out['administrator_rights'] = value.administrator_rights === null ? null : encodeChatAdministratorRights(value.administrator_rights);
  return out;
}

/** internalLinkTypeBotStart = InternalLinkType — The link is a link to a chat with a Telegram bot. Call searchPublicChat with the given bot username, check that the user is a bot, show START button in the chat with the bot, and then call se... */
export class InternalLinkTypeBotStart {
  type: 'internalLinkTypeBotStart' = 'internalLinkTypeBotStart';
  extra?: string;
  /** bot_username: string — Username of the bot */
  bot_username: string = '';
  /** start_parameter: string — The parameter to be passed to sendBotStartMessage */
  start_parameter: string = '';
  /** autostart: Bool — True, if sendBotStartMessage must be called automatically without showing the START button */
  autostart: boolean = false;
}

/** Decode a TDLib `internalLinkTypeBotStart` object; null input -> null. */
export function decodeInternalLinkTypeBotStart(json: TdJsonRaw): InternalLinkTypeBotStart | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeBotStart = new InternalLinkTypeBotStart();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_username = asString(record['bot_username']);
  out.start_parameter = asString(record['start_parameter']);
  out.autostart = asBool(record['autostart']);
  return out;
}

/** Encode a `internalLinkTypeBotStart` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeBotStart(value: InternalLinkTypeBotStart): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeBotStart';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_username'] = value.bot_username;
  out['start_parameter'] = value.start_parameter;
  out['autostart'] = value.autostart;
  return out;
}

/** internalLinkTypeBotStartInGroup = InternalLinkType — The link is a link to a Telegram bot, which is expected to be added to a group chat. Call searchPublicChat with the given bot username, check that the user is a bot and can be added to... */
export class InternalLinkTypeBotStartInGroup {
  type: 'internalLinkTypeBotStartInGroup' = 'internalLinkTypeBotStartInGroup';
  extra?: string;
  /** bot_username: string — Username of the bot */
  bot_username: string = '';
  /** start_parameter: string — The parameter to be passed to sendBotStartMessage */
  start_parameter: string = '';
  /** administrator_rights: chatAdministratorRights — Expected administrator rights for the bot; may be null */
  administrator_rights: ChatAdministratorRights | null = null;
}

/** Decode a TDLib `internalLinkTypeBotStartInGroup` object; null input -> null. */
export function decodeInternalLinkTypeBotStartInGroup(json: TdJsonRaw): InternalLinkTypeBotStartInGroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeBotStartInGroup = new InternalLinkTypeBotStartInGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_username = asString(record['bot_username']);
  out.start_parameter = asString(record['start_parameter']);
  out.administrator_rights = decodeChatAdministratorRights(record['administrator_rights']);
  return out;
}

/** Encode a `internalLinkTypeBotStartInGroup` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeBotStartInGroup(value: InternalLinkTypeBotStartInGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeBotStartInGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_username'] = value.bot_username;
  out['start_parameter'] = value.start_parameter;
  out['administrator_rights'] = value.administrator_rights === null ? null : encodeChatAdministratorRights(value.administrator_rights);
  return out;
}

/** internalLinkTypeBusinessChat = InternalLinkType — The link is a link to a business chat. Use getBusinessChatLinkInfo with the provided link name to get information about the link, then open received private chat and replace chat draft wi... */
export class InternalLinkTypeBusinessChat {
  type: 'internalLinkTypeBusinessChat' = 'internalLinkTypeBusinessChat';
  extra?: string;
  /** link_name: string — Name of the link */
  link_name: string = '';
}

/** Decode a TDLib `internalLinkTypeBusinessChat` object; null input -> null. */
export function decodeInternalLinkTypeBusinessChat(json: TdJsonRaw): InternalLinkTypeBusinessChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeBusinessChat = new InternalLinkTypeBusinessChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link_name = asString(record['link_name']);
  return out;
}

/** Encode a `internalLinkTypeBusinessChat` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeBusinessChat(value: InternalLinkTypeBusinessChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeBusinessChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link_name'] = value.link_name;
  return out;
}

/** internalLinkTypeCallsPage = InternalLinkType — The link is a link to the Call tab or page "", "all", "missed", "edit", "show-tab", "start-call" */
export class InternalLinkTypeCallsPage {
  type: 'internalLinkTypeCallsPage' = 'internalLinkTypeCallsPage';
  extra?: string;
  /** section: string — Section of the page; may be one of */
  section: string = '';
}

/** Decode a TDLib `internalLinkTypeCallsPage` object; null input -> null. */
export function decodeInternalLinkTypeCallsPage(json: TdJsonRaw): InternalLinkTypeCallsPage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeCallsPage = new InternalLinkTypeCallsPage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.section = asString(record['section']);
  return out;
}

/** Encode a `internalLinkTypeCallsPage` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeCallsPage(value: InternalLinkTypeCallsPage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeCallsPage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['section'] = value.section;
  return out;
}

/** internalLinkTypeChatAffiliateProgram = InternalLinkType — The link is an affiliate program link. Call searchChatAffiliateProgram with the given username and referrer to process the link */
export class InternalLinkTypeChatAffiliateProgram {
  type: 'internalLinkTypeChatAffiliateProgram' = 'internalLinkTypeChatAffiliateProgram';
  extra?: string;
  /** username: string — Username to be passed to searchChatAffiliateProgram */
  username: string = '';
  /** referrer: string — Referrer to be passed to searchChatAffiliateProgram */
  referrer: string = '';
}

/** Decode a TDLib `internalLinkTypeChatAffiliateProgram` object; null input -> null. */
export function decodeInternalLinkTypeChatAffiliateProgram(json: TdJsonRaw): InternalLinkTypeChatAffiliateProgram | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeChatAffiliateProgram = new InternalLinkTypeChatAffiliateProgram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  out.referrer = asString(record['referrer']);
  return out;
}

/** Encode a `internalLinkTypeChatAffiliateProgram` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeChatAffiliateProgram(value: InternalLinkTypeChatAffiliateProgram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeChatAffiliateProgram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  out['referrer'] = value.referrer;
  return out;
}

/** internalLinkTypeChatBoost = InternalLinkType — The link is a link to boost a Telegram chat. Call getChatBoostLinkInfo with the given URL to process the link. If the chat is found, then call getChatBoostStatus and getAvailableChatBoostSlo... */
export class InternalLinkTypeChatBoost {
  type: 'internalLinkTypeChatBoost' = 'internalLinkTypeChatBoost';
  extra?: string;
  /** url: string — URL to be passed to getChatBoostLinkInfo */
  url: string = '';
}

/** Decode a TDLib `internalLinkTypeChatBoost` object; null input -> null. */
export function decodeInternalLinkTypeChatBoost(json: TdJsonRaw): InternalLinkTypeChatBoost | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeChatBoost = new InternalLinkTypeChatBoost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `internalLinkTypeChatBoost` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeChatBoost(value: InternalLinkTypeChatBoost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeChatBoost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** internalLinkTypeChatFolderInvite = InternalLinkType — The link is an invite link to a chat folder. Call checkChatFolderInviteLink with the given invite link to process the link. If the link is valid and the user wants to join the chat fo... */
export class InternalLinkTypeChatFolderInvite {
  type: 'internalLinkTypeChatFolderInvite' = 'internalLinkTypeChatFolderInvite';
  extra?: string;
  /** invite_link: string — Internal representation of the invite link */
  invite_link: string = '';
}

/** Decode a TDLib `internalLinkTypeChatFolderInvite` object; null input -> null. */
export function decodeInternalLinkTypeChatFolderInvite(json: TdJsonRaw): InternalLinkTypeChatFolderInvite | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeChatFolderInvite = new InternalLinkTypeChatFolderInvite();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `internalLinkTypeChatFolderInvite` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeChatFolderInvite(value: InternalLinkTypeChatFolderInvite): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeChatFolderInvite';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  return out;
}

/** internalLinkTypeChatInvite = InternalLinkType — The link is a chat invite link. Call checkChatInviteLink with the given invite link to process the link. If the link is valid and the user wants to join the chat, then call joinChatByInvite... */
export class InternalLinkTypeChatInvite {
  type: 'internalLinkTypeChatInvite' = 'internalLinkTypeChatInvite';
  extra?: string;
  /** invite_link: string — Internal representation of the invite link */
  invite_link: string = '';
}

/** Decode a TDLib `internalLinkTypeChatInvite` object; null input -> null. */
export function decodeInternalLinkTypeChatInvite(json: TdJsonRaw): InternalLinkTypeChatInvite | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeChatInvite = new InternalLinkTypeChatInvite();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `internalLinkTypeChatInvite` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeChatInvite(value: InternalLinkTypeChatInvite): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeChatInvite';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  return out;
}

/** internalLinkTypeChatSelection = InternalLinkType — The link is a link that allows to select some chats */
export class InternalLinkTypeChatSelection {
  type: 'internalLinkTypeChatSelection' = 'internalLinkTypeChatSelection';
  extra?: string;
}

/** Decode a TDLib `internalLinkTypeChatSelection` object; null input -> null. */
export function decodeInternalLinkTypeChatSelection(json: TdJsonRaw): InternalLinkTypeChatSelection | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeChatSelection = new InternalLinkTypeChatSelection();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `internalLinkTypeChatSelection` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeChatSelection(value: InternalLinkTypeChatSelection): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeChatSelection';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** internalLinkTypeContactsPage = InternalLinkType — The link is a link to the Contacts tab or page "", "search", "sort", "new", "invite", "manage" */
export class InternalLinkTypeContactsPage {
  type: 'internalLinkTypeContactsPage' = 'internalLinkTypeContactsPage';
  extra?: string;
  /** section: string — Section of the page; may be one of */
  section: string = '';
}

/** Decode a TDLib `internalLinkTypeContactsPage` object; null input -> null. */
export function decodeInternalLinkTypeContactsPage(json: TdJsonRaw): InternalLinkTypeContactsPage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeContactsPage = new InternalLinkTypeContactsPage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.section = asString(record['section']);
  return out;
}

/** Encode a `internalLinkTypeContactsPage` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeContactsPage(value: InternalLinkTypeContactsPage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeContactsPage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['section'] = value.section;
  return out;
}

/** internalLinkTypeDirectMessagesChat = InternalLinkType — The link is a link to a channel direct messages chat by username of the channel. Call searchPublicChat with the given chat username to process the link. If the chat is found and is ... */
export class InternalLinkTypeDirectMessagesChat {
  type: 'internalLinkTypeDirectMessagesChat' = 'internalLinkTypeDirectMessagesChat';
  extra?: string;
  /** channel_username: string — Username of the channel */
  channel_username: string = '';
}

/** Decode a TDLib `internalLinkTypeDirectMessagesChat` object; null input -> null. */
export function decodeInternalLinkTypeDirectMessagesChat(json: TdJsonRaw): InternalLinkTypeDirectMessagesChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeDirectMessagesChat = new InternalLinkTypeDirectMessagesChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.channel_username = asString(record['channel_username']);
  return out;
}

/** Encode a `internalLinkTypeDirectMessagesChat` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeDirectMessagesChat(value: InternalLinkTypeDirectMessagesChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeDirectMessagesChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['channel_username'] = value.channel_username;
  return out;
}

/** internalLinkTypeGame = InternalLinkType — The link is a link to a game. Call searchPublicChat with the given bot username, check that the user is a bot, ask the current user to select a chat to send the game, and then call sendMessage wi... */
export class InternalLinkTypeGame {
  type: 'internalLinkTypeGame' = 'internalLinkTypeGame';
  extra?: string;
  /** bot_username: string — Username of the bot that owns the game */
  bot_username: string = '';
  /** game_short_name: string — Short name of the game */
  game_short_name: string = '';
}

/** Decode a TDLib `internalLinkTypeGame` object; null input -> null. */
export function decodeInternalLinkTypeGame(json: TdJsonRaw): InternalLinkTypeGame | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeGame = new InternalLinkTypeGame();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_username = asString(record['bot_username']);
  out.game_short_name = asString(record['game_short_name']);
  return out;
}

/** Encode a `internalLinkTypeGame` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeGame(value: InternalLinkTypeGame): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeGame';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_username'] = value.bot_username;
  out['game_short_name'] = value.game_short_name;
  return out;
}

/** internalLinkTypeGiftAuction = InternalLinkType — The link is a link to a gift auction. Call getGiftAuctionState with the given auction identifier to process the link */
export class InternalLinkTypeGiftAuction {
  type: 'internalLinkTypeGiftAuction' = 'internalLinkTypeGiftAuction';
  extra?: string;
  /** auction_id: string — Unique identifier of the auction */
  auction_id: string = '';
}

/** Decode a TDLib `internalLinkTypeGiftAuction` object; null input -> null. */
export function decodeInternalLinkTypeGiftAuction(json: TdJsonRaw): InternalLinkTypeGiftAuction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeGiftAuction = new InternalLinkTypeGiftAuction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.auction_id = asString(record['auction_id']);
  return out;
}

/** Encode a `internalLinkTypeGiftAuction` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeGiftAuction(value: InternalLinkTypeGiftAuction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeGiftAuction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['auction_id'] = value.auction_id;
  return out;
}

/** internalLinkTypeGiftCollection = InternalLinkType — The link is a link to a gift collection. Call searchPublicChat with the given username, then call getReceivedGifts with the received gift owner identifier and the given collection ident... */
export class InternalLinkTypeGiftCollection {
  type: 'internalLinkTypeGiftCollection' = 'internalLinkTypeGiftCollection';
  extra?: string;
  /** gift_owner_username: string — Username of the owner of the gift collection */
  gift_owner_username: string = '';
  /** collection_id: int32 — Gift collection identifier */
  collection_id: number = 0;
}

/** Decode a TDLib `internalLinkTypeGiftCollection` object; null input -> null. */
export function decodeInternalLinkTypeGiftCollection(json: TdJsonRaw): InternalLinkTypeGiftCollection | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeGiftCollection = new InternalLinkTypeGiftCollection();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_owner_username = asString(record['gift_owner_username']);
  out.collection_id = asNumber(record['collection_id']);
  return out;
}

/** Encode a `internalLinkTypeGiftCollection` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeGiftCollection(value: InternalLinkTypeGiftCollection): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeGiftCollection';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_owner_username'] = value.gift_owner_username;
  out['collection_id'] = value.collection_id;
  return out;
}

/** internalLinkTypeGroupCall = InternalLinkType — The link is a link to a group call that isn't bound to a chat. Use getGroupCallParticipants to get the list of group call participants and show them on the join group call screen. Call joinG... */
export class InternalLinkTypeGroupCall {
  type: 'internalLinkTypeGroupCall' = 'internalLinkTypeGroupCall';
  extra?: string;
  /** invite_link: string — Internal representation of the invite link */
  invite_link: string = '';
}

/** Decode a TDLib `internalLinkTypeGroupCall` object; null input -> null. */
export function decodeInternalLinkTypeGroupCall(json: TdJsonRaw): InternalLinkTypeGroupCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeGroupCall = new InternalLinkTypeGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `internalLinkTypeGroupCall` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeGroupCall(value: InternalLinkTypeGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  return out;
}

/** internalLinkTypeInstantView = InternalLinkType — The link must be opened in an Instant View. Call getWebPageInstantView with the given URL to process the link. If Instant View is found, then show it, otherwise, open the fallback URL in a... */
export class InternalLinkTypeInstantView {
  type: 'internalLinkTypeInstantView' = 'internalLinkTypeInstantView';
  extra?: string;
  /** url: string — URL to be passed to getWebPageInstantView */
  url: string = '';
  /** fallback_url: string — An URL to open if getWebPageInstantView fails */
  fallback_url: string = '';
}

/** Decode a TDLib `internalLinkTypeInstantView` object; null input -> null. */
export function decodeInternalLinkTypeInstantView(json: TdJsonRaw): InternalLinkTypeInstantView | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeInstantView = new InternalLinkTypeInstantView();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.fallback_url = asString(record['fallback_url']);
  return out;
}

/** Encode a `internalLinkTypeInstantView` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeInstantView(value: InternalLinkTypeInstantView): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeInstantView';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['fallback_url'] = value.fallback_url;
  return out;
}

/** internalLinkTypeInvoice = InternalLinkType — The link is a link to an invoice. Call getPaymentForm with the given invoice name to process the link */
export class InternalLinkTypeInvoice {
  type: 'internalLinkTypeInvoice' = 'internalLinkTypeInvoice';
  extra?: string;
  /** invoice_name: string — Name of the invoice */
  invoice_name: string = '';
}

/** Decode a TDLib `internalLinkTypeInvoice` object; null input -> null. */
export function decodeInternalLinkTypeInvoice(json: TdJsonRaw): InternalLinkTypeInvoice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeInvoice = new InternalLinkTypeInvoice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invoice_name = asString(record['invoice_name']);
  return out;
}

/** Encode a `internalLinkTypeInvoice` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeInvoice(value: InternalLinkTypeInvoice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeInvoice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invoice_name'] = value.invoice_name;
  return out;
}

/** internalLinkTypeLanguagePack = InternalLinkType — The link is a link to a language pack. Call getLanguagePackInfo with the given language pack identifier to process the link. If the language pack is found and the user wants to apply it, ... */
export class InternalLinkTypeLanguagePack {
  type: 'internalLinkTypeLanguagePack' = 'internalLinkTypeLanguagePack';
  extra?: string;
  /** language_pack_id: string — Language pack identifier */
  language_pack_id: string = '';
}

/** Decode a TDLib `internalLinkTypeLanguagePack` object; null input -> null. */
export function decodeInternalLinkTypeLanguagePack(json: TdJsonRaw): InternalLinkTypeLanguagePack | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeLanguagePack = new InternalLinkTypeLanguagePack();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_pack_id = asString(record['language_pack_id']);
  return out;
}

/** Encode a `internalLinkTypeLanguagePack` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeLanguagePack(value: InternalLinkTypeLanguagePack): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeLanguagePack';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_pack_id'] = value.language_pack_id;
  return out;
}

/** internalLinkTypeLiveStory = InternalLinkType — The link is a link to a live story. Call searchPublicChat with the given chat username, then getChatActiveStories to get active stories in the chat, then find a live story among active stori... */
export class InternalLinkTypeLiveStory {
  type: 'internalLinkTypeLiveStory' = 'internalLinkTypeLiveStory';
  extra?: string;
  /** story_poster_username: string — Username of the poster of the story */
  story_poster_username: string = '';
}

/** Decode a TDLib `internalLinkTypeLiveStory` object; null input -> null. */
export function decodeInternalLinkTypeLiveStory(json: TdJsonRaw): InternalLinkTypeLiveStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeLiveStory = new InternalLinkTypeLiveStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_username = asString(record['story_poster_username']);
  return out;
}

/** Encode a `internalLinkTypeLiveStory` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeLiveStory(value: InternalLinkTypeLiveStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeLiveStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_username'] = value.story_poster_username;
  return out;
}

/** internalLinkTypeMainWebApp = InternalLinkType — The link is a link to the main Web App of a bot. Call searchPublicChat with the given bot username, check that the user is a bot and has the main Web App. If the bot can be added to attachm... */
export class InternalLinkTypeMainWebApp {
  type: 'internalLinkTypeMainWebApp' = 'internalLinkTypeMainWebApp';
  extra?: string;
  /** bot_username: string — Username of the bot */
  bot_username: string = '';
  /** start_parameter: string — Start parameter to be passed to getMainWebApp */
  start_parameter: string = '';
  /** mode: WebAppOpenMode — The mode to be passed to getMainWebApp */
  mode: TdWebAppOpenMode | null = null;
}

/** Decode a TDLib `internalLinkTypeMainWebApp` object; null input -> null. */
export function decodeInternalLinkTypeMainWebApp(json: TdJsonRaw): InternalLinkTypeMainWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeMainWebApp = new InternalLinkTypeMainWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_username = asString(record['bot_username']);
  out.start_parameter = asString(record['start_parameter']);
  out.mode = decodeTdWebAppOpenMode(record['mode']);
  return out;
}

/** Encode a `internalLinkTypeMainWebApp` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeMainWebApp(value: InternalLinkTypeMainWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeMainWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_username'] = value.bot_username;
  out['start_parameter'] = value.start_parameter;
  out['mode'] = value.mode === null ? null : encodeTdWebAppOpenMode(value.mode);
  return out;
}

/** internalLinkTypeMessage = InternalLinkType — The link is a link to a Telegram message or a forum topic. Call getMessageLinkInfo with the given URL to process the link, and then open received forum topic or chat and show the message there */
export class InternalLinkTypeMessage {
  type: 'internalLinkTypeMessage' = 'internalLinkTypeMessage';
  extra?: string;
  /** url: string — URL to be passed to getMessageLinkInfo */
  url: string = '';
}

/** Decode a TDLib `internalLinkTypeMessage` object; null input -> null. */
export function decodeInternalLinkTypeMessage(json: TdJsonRaw): InternalLinkTypeMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeMessage = new InternalLinkTypeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `internalLinkTypeMessage` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeMessage(value: InternalLinkTypeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** internalLinkTypeMessageDraft = InternalLinkType — The link contains a message draft text. A share screen needs to be shown to the user, then the chosen chat must be opened and the text is added to the input field */
export class InternalLinkTypeMessageDraft {
  type: 'internalLinkTypeMessageDraft' = 'internalLinkTypeMessageDraft';
  extra?: string;
  /** text: formattedText — Message draft text */
  text: FormattedText | null = null;
  /** contains_link: Bool — True, if the first line of the text contains a link. If true, the input field needs to be focused and the text after the link must be selected */
  contains_link: boolean = false;
}

/** Decode a TDLib `internalLinkTypeMessageDraft` object; null input -> null. */
export function decodeInternalLinkTypeMessageDraft(json: TdJsonRaw): InternalLinkTypeMessageDraft | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeMessageDraft = new InternalLinkTypeMessageDraft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.contains_link = asBool(record['contains_link']);
  return out;
}

/** Encode a `internalLinkTypeMessageDraft` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeMessageDraft(value: InternalLinkTypeMessageDraft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeMessageDraft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['contains_link'] = value.contains_link;
  return out;
}

/** internalLinkTypeMyProfilePage = InternalLinkType — The link is a link to the My Profile application page "", "posts", "posts/all-stories", "posts/add-album", "gifts", "archived-posts" */
export class InternalLinkTypeMyProfilePage {
  type: 'internalLinkTypeMyProfilePage' = 'internalLinkTypeMyProfilePage';
  extra?: string;
  /** section: string — Section of the page; may be one of */
  section: string = '';
}

/** Decode a TDLib `internalLinkTypeMyProfilePage` object; null input -> null. */
export function decodeInternalLinkTypeMyProfilePage(json: TdJsonRaw): InternalLinkTypeMyProfilePage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeMyProfilePage = new InternalLinkTypeMyProfilePage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.section = asString(record['section']);
  return out;
}

/** Encode a `internalLinkTypeMyProfilePage` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeMyProfilePage(value: InternalLinkTypeMyProfilePage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeMyProfilePage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['section'] = value.section;
  return out;
}

/** internalLinkTypeNewChannelChat = InternalLinkType — The link is a link to the screen for creating a new channel chat */
export class InternalLinkTypeNewChannelChat {
  type: 'internalLinkTypeNewChannelChat' = 'internalLinkTypeNewChannelChat';
  extra?: string;
}

/** Decode a TDLib `internalLinkTypeNewChannelChat` object; null input -> null. */
export function decodeInternalLinkTypeNewChannelChat(json: TdJsonRaw): InternalLinkTypeNewChannelChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeNewChannelChat = new InternalLinkTypeNewChannelChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `internalLinkTypeNewChannelChat` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeNewChannelChat(value: InternalLinkTypeNewChannelChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeNewChannelChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** internalLinkTypeNewGroupChat = InternalLinkType — The link is a link to the screen for creating a new group chat */
export class InternalLinkTypeNewGroupChat {
  type: 'internalLinkTypeNewGroupChat' = 'internalLinkTypeNewGroupChat';
  extra?: string;
}

/** Decode a TDLib `internalLinkTypeNewGroupChat` object; null input -> null. */
export function decodeInternalLinkTypeNewGroupChat(json: TdJsonRaw): InternalLinkTypeNewGroupChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeNewGroupChat = new InternalLinkTypeNewGroupChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `internalLinkTypeNewGroupChat` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeNewGroupChat(value: InternalLinkTypeNewGroupChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeNewGroupChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** internalLinkTypeNewPrivateChat = InternalLinkType — The link is a link to the screen for creating a new private chat with a contact */
export class InternalLinkTypeNewPrivateChat {
  type: 'internalLinkTypeNewPrivateChat' = 'internalLinkTypeNewPrivateChat';
  extra?: string;
}

/** Decode a TDLib `internalLinkTypeNewPrivateChat` object; null input -> null. */
export function decodeInternalLinkTypeNewPrivateChat(json: TdJsonRaw): InternalLinkTypeNewPrivateChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeNewPrivateChat = new InternalLinkTypeNewPrivateChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `internalLinkTypeNewPrivateChat` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeNewPrivateChat(value: InternalLinkTypeNewPrivateChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeNewPrivateChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** internalLinkTypeNewStory = InternalLinkType — The link is a link to open the story posting interface */
export class InternalLinkTypeNewStory {
  type: 'internalLinkTypeNewStory' = 'internalLinkTypeNewStory';
  extra?: string;
  /** content_type: StoryContentType — The type of the content of the story to post; may be null if unspecified */
  content_type: TdStoryContentType | null = null;
}

/** Decode a TDLib `internalLinkTypeNewStory` object; null input -> null. */
export function decodeInternalLinkTypeNewStory(json: TdJsonRaw): InternalLinkTypeNewStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeNewStory = new InternalLinkTypeNewStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.content_type = decodeTdStoryContentType(record['content_type']);
  return out;
}

/** Encode a `internalLinkTypeNewStory` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeNewStory(value: InternalLinkTypeNewStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeNewStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['content_type'] = value.content_type === null ? null : encodeTdStoryContentType(value.content_type);
  return out;
}

/** internalLinkTypeOauth = InternalLinkType — The link is an OAuth link. Call getOauthLinkInfo with the given URL to process the link if the link was received from outside of the application; otherwise, ignore it. After getOauthLinkInfo, sh... */
export class InternalLinkTypeOauth {
  type: 'internalLinkTypeOauth' = 'internalLinkTypeOauth';
  extra?: string;
  /** url: string — URL to be passed to getOauthLinkInfo */
  url: string = '';
}

/** Decode a TDLib `internalLinkTypeOauth` object; null input -> null. */
export function decodeInternalLinkTypeOauth(json: TdJsonRaw): InternalLinkTypeOauth | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeOauth = new InternalLinkTypeOauth();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `internalLinkTypeOauth` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeOauth(value: InternalLinkTypeOauth): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeOauth';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** internalLinkTypePassportDataRequest = InternalLinkType — The link contains a request of Telegram passport data. Call getPassportAuthorizationForm with the given parameters to process the link if the link was received from outside of the ... */
export class InternalLinkTypePassportDataRequest {
  type: 'internalLinkTypePassportDataRequest' = 'internalLinkTypePassportDataRequest';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the service's bot; the corresponding user may be unknown yet */
  bot_user_id: number = 0;
  /** scope: string — Telegram Passport element types requested by the service */
  scope: string = '';
  /** public_key: string — Service's public key */
  public_key: string = '';
  /** nonce: string — Unique request identifier provided by the service */
  nonce: string = '';
  /** callback_url: string — An HTTP URL to open once the request is finished, canceled, or failed with the parameters tg_passport=success, tg_passport=cancel, or tg_passport=error&error=... respectively. */
  callback_url: string = '';
}

/** Decode a TDLib `internalLinkTypePassportDataRequest` object; null input -> null. */
export function decodeInternalLinkTypePassportDataRequest(json: TdJsonRaw): InternalLinkTypePassportDataRequest | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypePassportDataRequest = new InternalLinkTypePassportDataRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.scope = asString(record['scope']);
  out.public_key = asString(record['public_key']);
  out.nonce = asString(record['nonce']);
  out.callback_url = asString(record['callback_url']);
  return out;
}

/** Encode a `internalLinkTypePassportDataRequest` into TDLib JSON wire shape. */
export function encodeInternalLinkTypePassportDataRequest(value: InternalLinkTypePassportDataRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypePassportDataRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['scope'] = value.scope;
  out['public_key'] = value.public_key;
  out['nonce'] = value.nonce;
  out['callback_url'] = value.callback_url;
  return out;
}

/** internalLinkTypePhoneNumberConfirmation = InternalLinkType — The link can be used to confirm ownership of a phone number to prevent account deletion. Call sendPhoneNumberCode with the given phone number and with phoneNumberCodeTypeConfir... */
export class InternalLinkTypePhoneNumberConfirmation {
  type: 'internalLinkTypePhoneNumberConfirmation' = 'internalLinkTypePhoneNumberConfirmation';
  extra?: string;
  /** hash: string — Hash value from the link */
  hash: string = '';
  /** phone_number: string — Phone number value from the link */
  phone_number: string = '';
}

/** Decode a TDLib `internalLinkTypePhoneNumberConfirmation` object; null input -> null. */
export function decodeInternalLinkTypePhoneNumberConfirmation(json: TdJsonRaw): InternalLinkTypePhoneNumberConfirmation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypePhoneNumberConfirmation = new InternalLinkTypePhoneNumberConfirmation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.hash = asString(record['hash']);
  out.phone_number = asString(record['phone_number']);
  return out;
}

/** Encode a `internalLinkTypePhoneNumberConfirmation` into TDLib JSON wire shape. */
export function encodeInternalLinkTypePhoneNumberConfirmation(value: InternalLinkTypePhoneNumberConfirmation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypePhoneNumberConfirmation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['hash'] = value.hash;
  out['phone_number'] = value.phone_number;
  return out;
}

/** internalLinkTypePremiumFeaturesPage = InternalLinkType — The link is a link to the Premium features screen of the application from which the user can subscribe to Telegram Premium. Call getPremiumFeatures with the given referrer to proce... */
export class InternalLinkTypePremiumFeaturesPage {
  type: 'internalLinkTypePremiumFeaturesPage' = 'internalLinkTypePremiumFeaturesPage';
  extra?: string;
  /** referrer: string — Referrer specified in the link */
  referrer: string = '';
}

/** Decode a TDLib `internalLinkTypePremiumFeaturesPage` object; null input -> null. */
export function decodeInternalLinkTypePremiumFeaturesPage(json: TdJsonRaw): InternalLinkTypePremiumFeaturesPage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypePremiumFeaturesPage = new InternalLinkTypePremiumFeaturesPage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.referrer = asString(record['referrer']);
  return out;
}

/** Encode a `internalLinkTypePremiumFeaturesPage` into TDLib JSON wire shape. */
export function encodeInternalLinkTypePremiumFeaturesPage(value: InternalLinkTypePremiumFeaturesPage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypePremiumFeaturesPage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['referrer'] = value.referrer;
  return out;
}

/** internalLinkTypePremiumGiftCode = InternalLinkType — The link is a link with a Telegram Premium gift code. Call checkPremiumGiftCode with the given code to process the link. If the code is valid and the user wants to apply it, then call ... */
export class InternalLinkTypePremiumGiftCode {
  type: 'internalLinkTypePremiumGiftCode' = 'internalLinkTypePremiumGiftCode';
  extra?: string;
  /** code: string — The Telegram Premium gift code */
  code: string = '';
}

/** Decode a TDLib `internalLinkTypePremiumGiftCode` object; null input -> null. */
export function decodeInternalLinkTypePremiumGiftCode(json: TdJsonRaw): InternalLinkTypePremiumGiftCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypePremiumGiftCode = new InternalLinkTypePremiumGiftCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asString(record['code']);
  return out;
}

/** Encode a `internalLinkTypePremiumGiftCode` into TDLib JSON wire shape. */
export function encodeInternalLinkTypePremiumGiftCode(value: InternalLinkTypePremiumGiftCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypePremiumGiftCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  return out;
}

/** internalLinkTypePremiumGiftPurchase = InternalLinkType — The link is a link to the screen for gifting Telegram Premium subscriptions to friends via inputInvoiceTelegram with telegramPaymentPurposePremiumGift payments or in-store purchases */
export class InternalLinkTypePremiumGiftPurchase {
  type: 'internalLinkTypePremiumGiftPurchase' = 'internalLinkTypePremiumGiftPurchase';
  extra?: string;
  /** referrer: string — Referrer specified in the link */
  referrer: string = '';
}

/** Decode a TDLib `internalLinkTypePremiumGiftPurchase` object; null input -> null. */
export function decodeInternalLinkTypePremiumGiftPurchase(json: TdJsonRaw): InternalLinkTypePremiumGiftPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypePremiumGiftPurchase = new InternalLinkTypePremiumGiftPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.referrer = asString(record['referrer']);
  return out;
}

/** Encode a `internalLinkTypePremiumGiftPurchase` into TDLib JSON wire shape. */
export function encodeInternalLinkTypePremiumGiftPurchase(value: InternalLinkTypePremiumGiftPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypePremiumGiftPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['referrer'] = value.referrer;
  return out;
}

/** internalLinkTypeProxy = InternalLinkType — The link is a link to a proxy. Call addProxy with the given parameters to process the link and add the proxy */
export class InternalLinkTypeProxy {
  type: 'internalLinkTypeProxy' = 'internalLinkTypeProxy';
  extra?: string;
  /** proxy: proxy — The proxy; may be null if the proxy is unsupported, in which case an alert can be shown to the user */
  proxy: ProxyValue | null = null;
}

/** Decode a TDLib `internalLinkTypeProxy` object; null input -> null. */
export function decodeInternalLinkTypeProxy(json: TdJsonRaw): InternalLinkTypeProxy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeProxy = new InternalLinkTypeProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.proxy = decodeProxyValue(record['proxy']);
  return out;
}

/** Encode a `internalLinkTypeProxy` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeProxy(value: InternalLinkTypeProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['proxy'] = value.proxy === null ? null : encodeProxyValue(value.proxy);
  return out;
}

/** internalLinkTypePublicChat = InternalLinkType — The link is a link to a chat by its username. Call searchPublicChat with the given chat username to process the link. If the chat is found, open its profile information screen or the chat i... */
export class InternalLinkTypePublicChat {
  type: 'internalLinkTypePublicChat' = 'internalLinkTypePublicChat';
  extra?: string;
  /** chat_username: string — Username of the chat */
  chat_username: string = '';
  /** draft_text: string — Draft text for message to send in the chat */
  draft_text: string = '';
  /** open_profile: Bool — True, if chat profile information screen must be opened; otherwise, the chat itself must be opened */
  open_profile: boolean = false;
}

/** Decode a TDLib `internalLinkTypePublicChat` object; null input -> null. */
export function decodeInternalLinkTypePublicChat(json: TdJsonRaw): InternalLinkTypePublicChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypePublicChat = new InternalLinkTypePublicChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_username = asString(record['chat_username']);
  out.draft_text = asString(record['draft_text']);
  out.open_profile = asBool(record['open_profile']);
  return out;
}

/** Encode a `internalLinkTypePublicChat` into TDLib JSON wire shape. */
export function encodeInternalLinkTypePublicChat(value: InternalLinkTypePublicChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypePublicChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_username'] = value.chat_username;
  out['draft_text'] = value.draft_text;
  out['open_profile'] = value.open_profile;
  return out;
}

/** internalLinkTypeQrCodeAuthentication = InternalLinkType — The link can be used to login the current user on another device, but it must be scanned from QR-code using in-app camera. An alert similar to "This code can be used to allow some... */
export class InternalLinkTypeQrCodeAuthentication {
  type: 'internalLinkTypeQrCodeAuthentication' = 'internalLinkTypeQrCodeAuthentication';
  extra?: string;
}

/** Decode a TDLib `internalLinkTypeQrCodeAuthentication` object; null input -> null. */
export function decodeInternalLinkTypeQrCodeAuthentication(json: TdJsonRaw): InternalLinkTypeQrCodeAuthentication | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeQrCodeAuthentication = new InternalLinkTypeQrCodeAuthentication();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `internalLinkTypeQrCodeAuthentication` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeQrCodeAuthentication(value: InternalLinkTypeQrCodeAuthentication): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeQrCodeAuthentication';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** internalLinkTypeRequestManagedBot = InternalLinkType — The link is a link to a dialog for creating of a managed bot. Call searchPublicChat with the given manager bot username. If the chat is found, the chat is a chat with a bot and the b... */
export class InternalLinkTypeRequestManagedBot {
  type: 'internalLinkTypeRequestManagedBot' = 'internalLinkTypeRequestManagedBot';
  extra?: string;
  /** manager_bot_username: string — Username of the bot which will manage the new bot */
  manager_bot_username: string = '';
  /** suggested_bot_username: string — Suggested username for the bot; always ends with "bot" case-insensitive */
  suggested_bot_username: string = '';
  /** suggested_bot_name: string — Suggested name for the bot; may be empty if not specified */
  suggested_bot_name: string = '';
}

/** Decode a TDLib `internalLinkTypeRequestManagedBot` object; null input -> null. */
export function decodeInternalLinkTypeRequestManagedBot(json: TdJsonRaw): InternalLinkTypeRequestManagedBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeRequestManagedBot = new InternalLinkTypeRequestManagedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.manager_bot_username = asString(record['manager_bot_username']);
  out.suggested_bot_username = asString(record['suggested_bot_username']);
  out.suggested_bot_name = asString(record['suggested_bot_name']);
  return out;
}

/** Encode a `internalLinkTypeRequestManagedBot` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeRequestManagedBot(value: InternalLinkTypeRequestManagedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeRequestManagedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['manager_bot_username'] = value.manager_bot_username;
  out['suggested_bot_username'] = value.suggested_bot_username;
  out['suggested_bot_name'] = value.suggested_bot_name;
  return out;
}

/** internalLinkTypeRestorePurchases = InternalLinkType — The link forces restore of App Store purchases when opened. For official iOS application only */
export class InternalLinkTypeRestorePurchases {
  type: 'internalLinkTypeRestorePurchases' = 'internalLinkTypeRestorePurchases';
  extra?: string;
}

/** Decode a TDLib `internalLinkTypeRestorePurchases` object; null input -> null. */
export function decodeInternalLinkTypeRestorePurchases(json: TdJsonRaw): InternalLinkTypeRestorePurchases | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeRestorePurchases = new InternalLinkTypeRestorePurchases();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `internalLinkTypeRestorePurchases` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeRestorePurchases(value: InternalLinkTypeRestorePurchases): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeRestorePurchases';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** internalLinkTypeSavedMessages = InternalLinkType — The link is a link to the Saved Messages chat. Call createPrivateChat with getOption("my_id") and open the chat */
export class InternalLinkTypeSavedMessages {
  type: 'internalLinkTypeSavedMessages' = 'internalLinkTypeSavedMessages';
  extra?: string;
}

/** Decode a TDLib `internalLinkTypeSavedMessages` object; null input -> null. */
export function decodeInternalLinkTypeSavedMessages(json: TdJsonRaw): InternalLinkTypeSavedMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeSavedMessages = new InternalLinkTypeSavedMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `internalLinkTypeSavedMessages` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeSavedMessages(value: InternalLinkTypeSavedMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeSavedMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** internalLinkTypeSearch = InternalLinkType — The link is a link to the global chat and messages search field */
export class InternalLinkTypeSearch {
  type: 'internalLinkTypeSearch' = 'internalLinkTypeSearch';
  extra?: string;
}

/** Decode a TDLib `internalLinkTypeSearch` object; null input -> null. */
export function decodeInternalLinkTypeSearch(json: TdJsonRaw): InternalLinkTypeSearch | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeSearch = new InternalLinkTypeSearch();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `internalLinkTypeSearch` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeSearch(value: InternalLinkTypeSearch): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeSearch';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** internalLinkTypeSettings = InternalLinkType — The link is a link to application settings */
export class InternalLinkTypeSettings {
  type: 'internalLinkTypeSettings' = 'internalLinkTypeSettings';
  extra?: string;
  /** section: SettingsSection — Section of the application settings to open; may be null if none */
  section: TdSettingsSection | null = null;
}

/** Decode a TDLib `internalLinkTypeSettings` object; null input -> null. */
export function decodeInternalLinkTypeSettings(json: TdJsonRaw): InternalLinkTypeSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeSettings = new InternalLinkTypeSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.section = decodeTdSettingsSection(record['section']);
  return out;
}

/** Encode a `internalLinkTypeSettings` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeSettings(value: InternalLinkTypeSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['section'] = value.section === null ? null : encodeTdSettingsSection(value.section);
  return out;
}

/** internalLinkTypeStarPurchase = InternalLinkType — The link is a link to the Telegram Star purchase section of the application */
export class InternalLinkTypeStarPurchase {
  type: 'internalLinkTypeStarPurchase' = 'internalLinkTypeStarPurchase';
  extra?: string;
  /** star_count: int53 — The number of Telegram Stars that must be owned by the user */
  star_count: number = 0;
  /** purpose: string — Purpose of Telegram Star purchase. Arbitrary string specified by the server, for example, "subs" if the Telegram Stars are required to extend channel subscriptions */
  purpose: string = '';
}

/** Decode a TDLib `internalLinkTypeStarPurchase` object; null input -> null. */
export function decodeInternalLinkTypeStarPurchase(json: TdJsonRaw): InternalLinkTypeStarPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeStarPurchase = new InternalLinkTypeStarPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.purpose = asString(record['purpose']);
  return out;
}

/** Encode a `internalLinkTypeStarPurchase` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeStarPurchase(value: InternalLinkTypeStarPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeStarPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['purpose'] = value.purpose;
  return out;
}

/** internalLinkTypeStickerSet = InternalLinkType — The link is a link to a sticker set. Call searchStickerSet with the given sticker set name to process the link and show the sticker set. If the sticker set is found and the user wants to ad... */
export class InternalLinkTypeStickerSet {
  type: 'internalLinkTypeStickerSet' = 'internalLinkTypeStickerSet';
  extra?: string;
  /** sticker_set_name: string — Name of the sticker set */
  sticker_set_name: string = '';
  /** expect_custom_emoji: Bool — True, if the sticker set is expected to contain custom emoji */
  expect_custom_emoji: boolean = false;
}

/** Decode a TDLib `internalLinkTypeStickerSet` object; null input -> null. */
export function decodeInternalLinkTypeStickerSet(json: TdJsonRaw): InternalLinkTypeStickerSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeStickerSet = new InternalLinkTypeStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_set_name = asString(record['sticker_set_name']);
  out.expect_custom_emoji = asBool(record['expect_custom_emoji']);
  return out;
}

/** Encode a `internalLinkTypeStickerSet` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeStickerSet(value: InternalLinkTypeStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_set_name'] = value.sticker_set_name;
  out['expect_custom_emoji'] = value.expect_custom_emoji;
  return out;
}

/** internalLinkTypeStory = InternalLinkType — The link is a link to a story. Call searchPublicChat with the given poster username, then call getStory with the received chat identifier and the given story identifier, then show the story if r... */
export class InternalLinkTypeStory {
  type: 'internalLinkTypeStory' = 'internalLinkTypeStory';
  extra?: string;
  /** story_poster_username: string — Username of the poster of the story */
  story_poster_username: string = '';
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
}

/** Decode a TDLib `internalLinkTypeStory` object; null input -> null. */
export function decodeInternalLinkTypeStory(json: TdJsonRaw): InternalLinkTypeStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeStory = new InternalLinkTypeStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_username = asString(record['story_poster_username']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `internalLinkTypeStory` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeStory(value: InternalLinkTypeStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_username'] = value.story_poster_username;
  out['story_id'] = value.story_id;
  return out;
}

/** internalLinkTypeStoryAlbum = InternalLinkType — The link is a link to an album of stories. Call searchPublicChat with the given username, then call getStoryAlbumStories with the received chat identifier and the given story album identifi... */
export class InternalLinkTypeStoryAlbum {
  type: 'internalLinkTypeStoryAlbum' = 'internalLinkTypeStoryAlbum';
  extra?: string;
  /** story_album_owner_username: string — Username of the owner of the story album */
  story_album_owner_username: string = '';
  /** story_album_id: int32 — Story album identifier */
  story_album_id: number = 0;
}

/** Decode a TDLib `internalLinkTypeStoryAlbum` object; null input -> null. */
export function decodeInternalLinkTypeStoryAlbum(json: TdJsonRaw): InternalLinkTypeStoryAlbum | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeStoryAlbum = new InternalLinkTypeStoryAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_album_owner_username = asString(record['story_album_owner_username']);
  out.story_album_id = asNumber(record['story_album_id']);
  return out;
}

/** Encode a `internalLinkTypeStoryAlbum` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeStoryAlbum(value: InternalLinkTypeStoryAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeStoryAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_album_owner_username'] = value.story_album_owner_username;
  out['story_album_id'] = value.story_album_id;
  return out;
}

/** internalLinkTypeTextCompositionStyle = InternalLinkType — The link is a link to a text composition style. Call searchTextCompositionStyle with the given style name to get information about the style. If the style is found and the user wa... */
export class InternalLinkTypeTextCompositionStyle {
  type: 'internalLinkTypeTextCompositionStyle' = 'internalLinkTypeTextCompositionStyle';
  extra?: string;
  /** style_name: string — Name of the style */
  style_name: string = '';
}

/** Decode a TDLib `internalLinkTypeTextCompositionStyle` object; null input -> null. */
export function decodeInternalLinkTypeTextCompositionStyle(json: TdJsonRaw): InternalLinkTypeTextCompositionStyle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeTextCompositionStyle = new InternalLinkTypeTextCompositionStyle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.style_name = asString(record['style_name']);
  return out;
}

/** Encode a `internalLinkTypeTextCompositionStyle` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeTextCompositionStyle(value: InternalLinkTypeTextCompositionStyle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeTextCompositionStyle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['style_name'] = value.style_name;
  return out;
}

/** internalLinkTypeTheme = InternalLinkType — The link is a link to a cloud theme. TDLib has no theme support yet */
export class InternalLinkTypeTheme {
  type: 'internalLinkTypeTheme' = 'internalLinkTypeTheme';
  extra?: string;
  /** theme_name: string — Name of the theme */
  theme_name: string = '';
}

/** Decode a TDLib `internalLinkTypeTheme` object; null input -> null. */
export function decodeInternalLinkTypeTheme(json: TdJsonRaw): InternalLinkTypeTheme | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeTheme = new InternalLinkTypeTheme();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.theme_name = asString(record['theme_name']);
  return out;
}

/** Encode a `internalLinkTypeTheme` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeTheme(value: InternalLinkTypeTheme): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeTheme';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['theme_name'] = value.theme_name;
  return out;
}

/** internalLinkTypeUnknownDeepLink = InternalLinkType — The link is an unknown tg: link. Call getDeepLinkInfo to process the link */
export class InternalLinkTypeUnknownDeepLink {
  type: 'internalLinkTypeUnknownDeepLink' = 'internalLinkTypeUnknownDeepLink';
  extra?: string;
  /** link: string — Link to be passed to getDeepLinkInfo */
  link: string = '';
}

/** Decode a TDLib `internalLinkTypeUnknownDeepLink` object; null input -> null. */
export function decodeInternalLinkTypeUnknownDeepLink(json: TdJsonRaw): InternalLinkTypeUnknownDeepLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeUnknownDeepLink = new InternalLinkTypeUnknownDeepLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  return out;
}

/** Encode a `internalLinkTypeUnknownDeepLink` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeUnknownDeepLink(value: InternalLinkTypeUnknownDeepLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeUnknownDeepLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  return out;
}

/** internalLinkTypeUpgradedGift = InternalLinkType — The link is a link to an upgraded gift. Call getUpgradedGift with the given name to process the link */
export class InternalLinkTypeUpgradedGift {
  type: 'internalLinkTypeUpgradedGift' = 'internalLinkTypeUpgradedGift';
  extra?: string;
  /** name: string — Name of the unique gift */
  name: string = '';
}

/** Decode a TDLib `internalLinkTypeUpgradedGift` object; null input -> null. */
export function decodeInternalLinkTypeUpgradedGift(json: TdJsonRaw): InternalLinkTypeUpgradedGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeUpgradedGift = new InternalLinkTypeUpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `internalLinkTypeUpgradedGift` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeUpgradedGift(value: InternalLinkTypeUpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeUpgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** internalLinkTypeUserPhoneNumber = InternalLinkType — The link is a link to a user by its phone number. Call searchUserByPhoneNumber with the given phone number to process the link. If the user is found, then call createPrivateChat and op... */
export class InternalLinkTypeUserPhoneNumber {
  type: 'internalLinkTypeUserPhoneNumber' = 'internalLinkTypeUserPhoneNumber';
  extra?: string;
  /** phone_number: string — Phone number of the user */
  phone_number: string = '';
  /** draft_text: string — Draft text for message to send in the chat */
  draft_text: string = '';
  /** open_profile: Bool — True, if user's profile information screen must be opened; otherwise, the chat itself must be opened */
  open_profile: boolean = false;
}

/** Decode a TDLib `internalLinkTypeUserPhoneNumber` object; null input -> null. */
export function decodeInternalLinkTypeUserPhoneNumber(json: TdJsonRaw): InternalLinkTypeUserPhoneNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeUserPhoneNumber = new InternalLinkTypeUserPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  out.draft_text = asString(record['draft_text']);
  out.open_profile = asBool(record['open_profile']);
  return out;
}

/** Encode a `internalLinkTypeUserPhoneNumber` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeUserPhoneNumber(value: InternalLinkTypeUserPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeUserPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  out['draft_text'] = value.draft_text;
  out['open_profile'] = value.open_profile;
  return out;
}

/** internalLinkTypeUserToken = InternalLinkType — The link is a link to a user by a temporary token. Call searchUserByToken with the given token to process the link. If the user is found, then call createPrivateChat and open the chat */
export class InternalLinkTypeUserToken {
  type: 'internalLinkTypeUserToken' = 'internalLinkTypeUserToken';
  extra?: string;
  /** token: string — The token */
  token: string = '';
}

/** Decode a TDLib `internalLinkTypeUserToken` object; null input -> null. */
export function decodeInternalLinkTypeUserToken(json: TdJsonRaw): InternalLinkTypeUserToken | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeUserToken = new InternalLinkTypeUserToken();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `internalLinkTypeUserToken` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeUserToken(value: InternalLinkTypeUserToken): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeUserToken';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** internalLinkTypeVideoChat = InternalLinkType — The link is a link to a video chat. Call searchPublicChat with the given chat username, and then joinVideoChat with the given invite hash to process the link */
export class InternalLinkTypeVideoChat {
  type: 'internalLinkTypeVideoChat' = 'internalLinkTypeVideoChat';
  extra?: string;
  /** chat_username: string — Username of the chat with the video chat */
  chat_username: string = '';
  /** invite_hash: string — If non-empty, invite hash to be used to join the video chat without being muted by administrators */
  invite_hash: string = '';
  /** is_live_stream: Bool — True, if the video chat is expected to be a live stream in a channel or a broadcast group */
  is_live_stream: boolean = false;
}

/** Decode a TDLib `internalLinkTypeVideoChat` object; null input -> null. */
export function decodeInternalLinkTypeVideoChat(json: TdJsonRaw): InternalLinkTypeVideoChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeVideoChat = new InternalLinkTypeVideoChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_username = asString(record['chat_username']);
  out.invite_hash = asString(record['invite_hash']);
  out.is_live_stream = asBool(record['is_live_stream']);
  return out;
}

/** Encode a `internalLinkTypeVideoChat` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeVideoChat(value: InternalLinkTypeVideoChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeVideoChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_username'] = value.chat_username;
  out['invite_hash'] = value.invite_hash;
  out['is_live_stream'] = value.is_live_stream;
  return out;
}

/** internalLinkTypeWebApp = InternalLinkType — The link is a link to a Web App. Call searchPublicChat with the given bot username, check that the user is a bot. If the bot is restricted for the current user, then show an error message. Othe... */
export class InternalLinkTypeWebApp {
  type: 'internalLinkTypeWebApp' = 'internalLinkTypeWebApp';
  extra?: string;
  /** bot_username: string — Username of the bot that owns the Web App */
  bot_username: string = '';
  /** web_app_short_name: string — Short name of the Web App */
  web_app_short_name: string = '';
  /** start_parameter: string — Start parameter to be passed to getWebAppLinkUrl */
  start_parameter: string = '';
  /** mode: WebAppOpenMode — The mode in which the Web App must be opened */
  mode: TdWebAppOpenMode | null = null;
}

/** Decode a TDLib `internalLinkTypeWebApp` object; null input -> null. */
export function decodeInternalLinkTypeWebApp(json: TdJsonRaw): InternalLinkTypeWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InternalLinkTypeWebApp = new InternalLinkTypeWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_username = asString(record['bot_username']);
  out.web_app_short_name = asString(record['web_app_short_name']);
  out.start_parameter = asString(record['start_parameter']);
  out.mode = decodeTdWebAppOpenMode(record['mode']);
  return out;
}

/** Encode a `internalLinkTypeWebApp` into TDLib JSON wire shape. */
export function encodeInternalLinkTypeWebApp(value: InternalLinkTypeWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'internalLinkTypeWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_username'] = value.bot_username;
  out['web_app_short_name'] = value.web_app_short_name;
  out['start_parameter'] = value.start_parameter;
  out['mode'] = value.mode === null ? null : encodeTdWebAppOpenMode(value.mode);
  return out;
}

/** inviteGroupCallParticipant = InviteGroupCallParticipantResult — Invites a user to an active group call; for group calls not bound to a chat only. Sends a service message of the type messageGroupCall. The group call can have at most getOp... */
export class InviteGroupCallParticipant {
  type: 'inviteGroupCallParticipant' = 'inviteGroupCallParticipant';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** is_video: Bool — Pass true if the group call is a video call */
  is_video: boolean = false;
}

/** Decode a TDLib `inviteGroupCallParticipant` object; null input -> null. */
export function decodeInviteGroupCallParticipant(json: TdJsonRaw): InviteGroupCallParticipant | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InviteGroupCallParticipant = new InviteGroupCallParticipant();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.user_id = asNumber(record['user_id']);
  out.is_video = asBool(record['is_video']);
  return out;
}

/** Encode a `inviteGroupCallParticipant` into TDLib JSON wire shape. */
export function encodeInviteGroupCallParticipant(value: InviteGroupCallParticipant): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inviteGroupCallParticipant';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['user_id'] = value.user_id;
  out['is_video'] = value.is_video;
  return out;
}

/** inviteGroupCallParticipantResultSuccess = InviteGroupCallParticipantResult — The user was invited and a service message of the type messageGroupCall was sent which can be used in declineGroupCallInvitation to cancel the invitation */
export class InviteGroupCallParticipantResultSuccess {
  type: 'inviteGroupCallParticipantResultSuccess' = 'inviteGroupCallParticipantResultSuccess';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the invitation message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `inviteGroupCallParticipantResultSuccess` object; null input -> null. */
export function decodeInviteGroupCallParticipantResultSuccess(json: TdJsonRaw): InviteGroupCallParticipantResultSuccess | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InviteGroupCallParticipantResultSuccess = new InviteGroupCallParticipantResultSuccess();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `inviteGroupCallParticipantResultSuccess` into TDLib JSON wire shape. */
export function encodeInviteGroupCallParticipantResultSuccess(value: InviteGroupCallParticipantResultSuccess): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inviteGroupCallParticipantResultSuccess';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** inviteGroupCallParticipantResultUserAlreadyParticipant = InviteGroupCallParticipantResult — The user can't be invited because they are already a participant of the call */
export class InviteGroupCallParticipantResultUserAlreadyParticipant {
  type: 'inviteGroupCallParticipantResultUserAlreadyParticipant' = 'inviteGroupCallParticipantResultUserAlreadyParticipant';
  extra?: string;
}

/** Decode a TDLib `inviteGroupCallParticipantResultUserAlreadyParticipant` object; null input -> null. */
export function decodeInviteGroupCallParticipantResultUserAlreadyParticipant(json: TdJsonRaw): InviteGroupCallParticipantResultUserAlreadyParticipant | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InviteGroupCallParticipantResultUserAlreadyParticipant = new InviteGroupCallParticipantResultUserAlreadyParticipant();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inviteGroupCallParticipantResultUserAlreadyParticipant` into TDLib JSON wire shape. */
export function encodeInviteGroupCallParticipantResultUserAlreadyParticipant(value: InviteGroupCallParticipantResultUserAlreadyParticipant): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inviteGroupCallParticipantResultUserAlreadyParticipant';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inviteGroupCallParticipantResultUserPrivacyRestricted = InviteGroupCallParticipantResult — The user can't be invited due to their privacy settings */
export class InviteGroupCallParticipantResultUserPrivacyRestricted {
  type: 'inviteGroupCallParticipantResultUserPrivacyRestricted' = 'inviteGroupCallParticipantResultUserPrivacyRestricted';
  extra?: string;
}

/** Decode a TDLib `inviteGroupCallParticipantResultUserPrivacyRestricted` object; null input -> null. */
export function decodeInviteGroupCallParticipantResultUserPrivacyRestricted(json: TdJsonRaw): InviteGroupCallParticipantResultUserPrivacyRestricted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InviteGroupCallParticipantResultUserPrivacyRestricted = new InviteGroupCallParticipantResultUserPrivacyRestricted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inviteGroupCallParticipantResultUserPrivacyRestricted` into TDLib JSON wire shape. */
export function encodeInviteGroupCallParticipantResultUserPrivacyRestricted(value: InviteGroupCallParticipantResultUserPrivacyRestricted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inviteGroupCallParticipantResultUserPrivacyRestricted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inviteGroupCallParticipantResultUserWasBanned = InviteGroupCallParticipantResult — The user can't be invited because they were banned by the owner of the call and can be invited back only by the owner of the group call */
export class InviteGroupCallParticipantResultUserWasBanned {
  type: 'inviteGroupCallParticipantResultUserWasBanned' = 'inviteGroupCallParticipantResultUserWasBanned';
  extra?: string;
}

/** Decode a TDLib `inviteGroupCallParticipantResultUserWasBanned` object; null input -> null. */
export function decodeInviteGroupCallParticipantResultUserWasBanned(json: TdJsonRaw): InviteGroupCallParticipantResultUserWasBanned | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InviteGroupCallParticipantResultUserWasBanned = new InviteGroupCallParticipantResultUserWasBanned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inviteGroupCallParticipantResultUserWasBanned` into TDLib JSON wire shape. */
export function encodeInviteGroupCallParticipantResultUserWasBanned(value: InviteGroupCallParticipantResultUserWasBanned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inviteGroupCallParticipantResultUserWasBanned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inviteLinkChatTypeBasicGroup = InviteLinkChatType — The link is an invite link for a basic group */
export class InviteLinkChatTypeBasicGroup {
  type: 'inviteLinkChatTypeBasicGroup' = 'inviteLinkChatTypeBasicGroup';
  extra?: string;
}

/** Decode a TDLib `inviteLinkChatTypeBasicGroup` object; null input -> null. */
export function decodeInviteLinkChatTypeBasicGroup(json: TdJsonRaw): InviteLinkChatTypeBasicGroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InviteLinkChatTypeBasicGroup = new InviteLinkChatTypeBasicGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inviteLinkChatTypeBasicGroup` into TDLib JSON wire shape. */
export function encodeInviteLinkChatTypeBasicGroup(value: InviteLinkChatTypeBasicGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inviteLinkChatTypeBasicGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inviteLinkChatTypeChannel = InviteLinkChatType — The link is an invite link for a channel */
export class InviteLinkChatTypeChannel {
  type: 'inviteLinkChatTypeChannel' = 'inviteLinkChatTypeChannel';
  extra?: string;
}

/** Decode a TDLib `inviteLinkChatTypeChannel` object; null input -> null. */
export function decodeInviteLinkChatTypeChannel(json: TdJsonRaw): InviteLinkChatTypeChannel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InviteLinkChatTypeChannel = new InviteLinkChatTypeChannel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inviteLinkChatTypeChannel` into TDLib JSON wire shape. */
export function encodeInviteLinkChatTypeChannel(value: InviteLinkChatTypeChannel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inviteLinkChatTypeChannel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inviteLinkChatTypeSupergroup = InviteLinkChatType — The link is an invite link for a supergroup */
export class InviteLinkChatTypeSupergroup {
  type: 'inviteLinkChatTypeSupergroup' = 'inviteLinkChatTypeSupergroup';
  extra?: string;
}

/** Decode a TDLib `inviteLinkChatTypeSupergroup` object; null input -> null. */
export function decodeInviteLinkChatTypeSupergroup(json: TdJsonRaw): InviteLinkChatTypeSupergroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InviteLinkChatTypeSupergroup = new InviteLinkChatTypeSupergroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `inviteLinkChatTypeSupergroup` into TDLib JSON wire shape. */
export function encodeInviteLinkChatTypeSupergroup(value: InviteLinkChatTypeSupergroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inviteLinkChatTypeSupergroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** inviteVideoChatParticipants = Ok — Invites users to an active video chat. Sends a service message of the type messageInviteVideoChatParticipants to the chat bound to the group call */
export class InviteVideoChatParticipants {
  type: 'inviteVideoChatParticipants' = 'inviteVideoChatParticipants';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** user_ids: vector<int53> — User identifiers. At most 10 users can be invited simultaneously */
  user_ids: number[] = [];
}

/** Decode a TDLib `inviteVideoChatParticipants` object; null input -> null. */
export function decodeInviteVideoChatParticipants(json: TdJsonRaw): InviteVideoChatParticipants | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: InviteVideoChatParticipants = new InviteVideoChatParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `inviteVideoChatParticipants` into TDLib JSON wire shape. */
export function encodeInviteVideoChatParticipants(value: InviteVideoChatParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'inviteVideoChatParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** invoice = Invoice — Product invoice */
export class Invoice {
  type: 'invoice' = 'invoice';
  extra?: string;
  /** currency: string — ISO 4217 currency code */
  currency: string = '';
  /** price_parts: vector<labeledPricePart> — A list of objects used to calculate the total price of the product */
  price_parts: LabeledPricePart[] = [];
  /** subscription_period: int32 — The number of seconds between consecutive Telegram Star debiting for subscription invoices; 0 if the invoice doesn't create subscription */
  subscription_period: number = 0;
  /** max_tip_amount: int53 — The maximum allowed amount of tip in the smallest units of the currency */
  max_tip_amount: number = 0;
  /** suggested_tip_amounts: vector<int53> — Suggested amounts of tip in the smallest units of the currency */
  suggested_tip_amounts: number[] = [];
  /** recurring_payment_terms_of_service_url: string — An HTTP URL with terms of service for recurring payments. If non-empty, the invoice payment will result in recurring payments and the user must accept the terms of service before allowed t... */
  recurring_payment_terms_of_service_url: string = '';
  /** terms_of_service_url: string — An HTTP URL with terms of service for non-recurring payments. If non-empty, then the user must accept the terms of service before allowed to pay */
  terms_of_service_url: string = '';
  /** is_test: Bool — True, if the payment is a test payment */
  is_test: boolean = false;
  /** need_name: Bool — True, if the user's name is needed for payment */
  need_name: boolean = false;
  /** need_phone_number: Bool — True, if the user's phone number is needed for payment */
  need_phone_number: boolean = false;
  /** need_email_address: Bool — True, if the user's email address is needed for payment */
  need_email_address: boolean = false;
  /** need_shipping_address: Bool — True, if the user's shipping address is needed for payment */
  need_shipping_address: boolean = false;
  /** send_phone_number_to_provider: Bool — True, if the user's phone number will be sent to the provider */
  send_phone_number_to_provider: boolean = false;
  /** send_email_address_to_provider: Bool — True, if the user's email address will be sent to the provider */
  send_email_address_to_provider: boolean = false;
  /** is_flexible: Bool — True, if the total price depends on the shipping method */
  is_flexible: boolean = false;
}

/** Decode a TDLib `invoice` object; null input -> null. */
export function decodeInvoice(json: TdJsonRaw): Invoice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Invoice = new Invoice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.price_parts = decodeVector(record['price_parts'], (item: TdJsonRaw): LabeledPricePart => decodeLabeledPricePart(item) as LabeledPricePart);
  out.subscription_period = asNumber(record['subscription_period']);
  out.max_tip_amount = asNumber(record['max_tip_amount']);
  out.suggested_tip_amounts = decodeVector(record['suggested_tip_amounts'], (item: TdJsonRaw): number => asNumber(item));
  out.recurring_payment_terms_of_service_url = asString(record['recurring_payment_terms_of_service_url']);
  out.terms_of_service_url = asString(record['terms_of_service_url']);
  out.is_test = asBool(record['is_test']);
  out.need_name = asBool(record['need_name']);
  out.need_phone_number = asBool(record['need_phone_number']);
  out.need_email_address = asBool(record['need_email_address']);
  out.need_shipping_address = asBool(record['need_shipping_address']);
  out.send_phone_number_to_provider = asBool(record['send_phone_number_to_provider']);
  out.send_email_address_to_provider = asBool(record['send_email_address_to_provider']);
  out.is_flexible = asBool(record['is_flexible']);
  return out;
}

/** Encode a `invoice` into TDLib JSON wire shape. */
export function encodeInvoice(value: Invoice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'invoice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['price_parts'] = encodeVector(value.price_parts, (item: LabeledPricePart | null): TdJsonRaw => item === null ? null : encodeLabeledPricePart(item));
  out['subscription_period'] = value.subscription_period;
  out['max_tip_amount'] = value.max_tip_amount;
  out['suggested_tip_amounts'] = encodeVector(value.suggested_tip_amounts, (item: number): TdJsonRaw => item);
  out['recurring_payment_terms_of_service_url'] = value.recurring_payment_terms_of_service_url;
  out['terms_of_service_url'] = value.terms_of_service_url;
  out['is_test'] = value.is_test;
  out['need_name'] = value.need_name;
  out['need_phone_number'] = value.need_phone_number;
  out['need_email_address'] = value.need_email_address;
  out['need_shipping_address'] = value.need_shipping_address;
  out['send_phone_number_to_provider'] = value.send_phone_number_to_provider;
  out['send_email_address_to_provider'] = value.send_email_address_to_provider;
  out['is_flexible'] = value.is_flexible;
  return out;
}

/** isLoginEmailAddressRequired = Ok — Checks whether the current user is required to set login email address */
export class IsLoginEmailAddressRequired {
  type: 'isLoginEmailAddressRequired' = 'isLoginEmailAddressRequired';
  extra?: string;
}

/** Decode a TDLib `isLoginEmailAddressRequired` object; null input -> null. */
export function decodeIsLoginEmailAddressRequired(json: TdJsonRaw): IsLoginEmailAddressRequired | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: IsLoginEmailAddressRequired = new IsLoginEmailAddressRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `isLoginEmailAddressRequired` into TDLib JSON wire shape. */
export function encodeIsLoginEmailAddressRequired(value: IsLoginEmailAddressRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'isLoginEmailAddressRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** isProfileAudio = Ok — Checks whether a file is in the profile audio files of the current user. Returns a 404 error if it isn't */
export class IsProfileAudio {
  type: 'isProfileAudio' = 'isProfileAudio';
  extra?: string;
  /** file_id: int32 — Identifier of the audio file to check */
  file_id: number = 0;
}

/** Decode a TDLib `isProfileAudio` object; null input -> null. */
export function decodeIsProfileAudio(json: TdJsonRaw): IsProfileAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: IsProfileAudio = new IsProfileAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  return out;
}

/** Encode a `isProfileAudio` into TDLib JSON wire shape. */
export function encodeIsProfileAudio(value: IsProfileAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'isProfileAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  return out;
}
