/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { AcceptedGiftTypes, Animation, AttributeCraftPersistenceProbability, decodeAcceptedGiftTypes, decodeAnimation, decodeAttributeCraftPersistenceProbability, encodeAcceptedGiftTypes, encodeAnimation, encodeAttributeCraftPersistenceProbability } from './TdTypes_A';
import { ChatEventLogFilters, ChatFolder, ChatInviteLinkMember, ChatJoinRequest, decodeChatEventLogFilters, decodeChatFolder, decodeChatInviteLinkMember, decodeChatJoinRequest, encodeChatEventLogFilters, encodeChatFolder, encodeChatInviteLinkMember, encodeChatJoinRequest } from './TdTypes_C';
import { FormattedText, decodeFormattedText, encodeFormattedText } from './TdTypes_F';
import { LinkPreviewOptions, Location, decodeLinkPreviewOptions, decodeLocation, encodeLinkPreviewOptions, encodeLocation } from './TdTypes_L';
import { Photo, decodePhoto, encodePhoto } from './TdTypes_P';
import { ReceivedGift, decodeReceivedGift, encodeReceivedGift } from './TdTypes_R';
import { Sticker, decodeSticker, encodeSticker } from './TdTypes_S';
import { ThemeParameters, ThemeSettings, decodeThemeParameters, decodeThemeSettings, encodeThemeParameters, encodeThemeSettings } from './TdTypes_T';
import { UpgradedGift, UpgradedGiftBackdrop, UpgradedGiftBackdropCount, UpgradedGiftModel, UpgradedGiftModelCount, UpgradedGiftSymbol, UpgradedGiftSymbolCount, decodeUpgradedGift, decodeUpgradedGiftBackdrop, decodeUpgradedGiftBackdropCount, decodeUpgradedGiftModel, decodeUpgradedGiftModelCount, decodeUpgradedGiftSymbol, decodeUpgradedGiftSymbolCount, encodeUpgradedGift, encodeUpgradedGiftBackdrop, encodeUpgradedGiftBackdropCount, encodeUpgradedGiftModel, encodeUpgradedGiftModelCount, encodeUpgradedGiftSymbol, encodeUpgradedGiftSymbolCount } from './TdTypes_U';
import { WebAppOpenParameters, decodeWebAppOpenParameters, encodeWebAppOpenParameters } from './TdTypes_W';
import { TdAffiliateType, TdAuctionState, decodeTdAffiliateType, decodeTdAuctionState, encodeTdAffiliateType, encodeTdAuctionState } from './TdUnions_A';
import { TdBackgroundType, TdBlockList, TdBotCommandScope, TdBusinessFeature, decodeTdBackgroundType, decodeTdBlockList, decodeTdBotCommandScope, decodeTdBusinessFeature, encodeTdBackgroundType, encodeTdBlockList, encodeTdBotCommandScope, encodeTdBusinessFeature } from './TdUnions_B';
import { TdCallbackQueryPayload, TdChatList, TdCollectibleItemType, decodeTdCallbackQueryPayload, decodeTdChatList, decodeTdCollectibleItemType, encodeTdCallbackQueryPayload, encodeTdChatList, encodeTdCollectibleItemType } from './TdUnions_C';
import { TdEmojiCategoryType, decodeTdEmojiCategoryType, encodeTdEmojiCategoryType } from './TdUnions_E';
import { TdFileType, decodeTdFileType, encodeTdFileType } from './TdUnions_F';
import { TdGiftResalePrice, TdGiveawayParticipantStatus, TdGroupCallVideoQuality, decodeTdGiftResalePrice, decodeTdGiveawayParticipantStatus, decodeTdGroupCallVideoQuality, encodeTdGiftResalePrice, encodeTdGiveawayParticipantStatus, encodeTdGroupCallVideoQuality } from './TdUnions_G';
import { TdInputFile, TdInputGroupCall, TdInputInvoice, TdInternalLinkType, decodeTdInputFile, decodeTdInputGroupCall, decodeTdInputInvoice, decodeTdInternalLinkType, encodeTdInputFile, encodeTdInputGroupCall, encodeTdInputInvoice, encodeTdInternalLinkType } from './TdUnions_I';
import { TdJsonValue, decodeTdJsonValue, encodeTdJsonValue } from './TdUnions_J';
import { TdMessageSender, TdMessageTopic, decodeTdMessageSender, decodeTdMessageTopic, encodeTdMessageSender, encodeTdMessageTopic } from './TdUnions_M';
import { TdNotificationSettingsScope, decodeTdNotificationSettingsScope, encodeTdNotificationSettingsScope } from './TdUnions_N';
import { TdPassportElementType, TdPremiumLimitType, TdPremiumSource, TdPublicChatType, decodeTdPassportElementType, decodeTdPremiumLimitType, decodeTdPremiumSource, decodeTdPublicChatType, encodeTdPassportElementType, encodeTdPremiumLimitType, encodeTdPremiumSource, encodeTdPublicChatType } from './TdUnions_P';
import { TdReactionType, decodeTdReactionType, encodeTdReactionType } from './TdUnions_R';
import { TdSearchMessagesFilter, TdStatisticalGraph, TdStickerType, TdSupergroupMembersFilter, decodeTdSearchMessagesFilter, decodeTdStatisticalGraph, decodeTdStickerType, decodeTdSupergroupMembersFilter, encodeTdSearchMessagesFilter, encodeTdStatisticalGraph, encodeTdStickerType, encodeTdSupergroupMembersFilter } from './TdUnions_S';
import { TdTopChatCategory, TdTransactionDirection, decodeTdTopChatCategory, decodeTdTransactionDirection, encodeTdTopChatCategory, encodeTdTransactionDirection } from './TdUnions_T';
import { TdUserPrivacySetting, decodeTdUserPrivacySetting, encodeTdUserPrivacySetting } from './TdUnions_U';

/** game = Game — Describes a game. Use getInternalLink with internalLinkTypeGame to share the game */
export class Game {
  type: 'game' = 'game';
  extra?: string;
  /** id: int64 — Unique game identifier */
  id: string = '';
  /** short_name: string — Game short name */
  short_name: string = '';
  /** title: string — Game title */
  title: string = '';
  /** text: formattedText — Game text, usually containing scoreboards for a game */
  text: FormattedText | null = null;
  /** description: string */
  description: string = '';
  /** photo: photo — Game photo */
  photo: Photo | null = null;
  /** animation: animation — Game animation; may be null */
  animation: Animation | null = null;
}

/** Decode a TDLib `game` object; null input -> null. */
export function decodeGame(json: TdJsonRaw): Game | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Game = new Game();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.short_name = asString(record['short_name']);
  out.title = asString(record['title']);
  out.text = decodeFormattedText(record['text']);
  out.description = asString(record['description']);
  out.photo = decodePhoto(record['photo']);
  out.animation = decodeAnimation(record['animation']);
  return out;
}

/** Encode a `game` into TDLib JSON wire shape. */
export function encodeGame(value: Game): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'game';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['short_name'] = value.short_name;
  out['title'] = value.title;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['description'] = value.description;
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  return out;
}

/** gameHighScore = GameHighScore — Contains one row of the game high score table */
export class GameHighScore {
  type: 'gameHighScore' = 'gameHighScore';
  extra?: string;
  /** position: int32 — Position in the high score table */
  position: number = 0;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** score: int32 — User score */
  score: number = 0;
}

/** Decode a TDLib `gameHighScore` object; null input -> null. */
export function decodeGameHighScore(json: TdJsonRaw): GameHighScore | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GameHighScore = new GameHighScore();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.position = asNumber(record['position']);
  out.user_id = asNumber(record['user_id']);
  out.score = asNumber(record['score']);
  return out;
}

/** Encode a `gameHighScore` into TDLib JSON wire shape. */
export function encodeGameHighScore(value: GameHighScore): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'gameHighScore';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['position'] = value.position;
  out['user_id'] = value.user_id;
  out['score'] = value.score;
  return out;
}

/** gameHighScores = GameHighScores — Contains a list of game high scores */
export class GameHighScores {
  type: 'gameHighScores' = 'gameHighScores';
  extra?: string;
  /** scores: vector<gameHighScore> — A list of game high scores */
  scores: GameHighScore[] = [];
}

/** Decode a TDLib `gameHighScores` object; null input -> null. */
export function decodeGameHighScores(json: TdJsonRaw): GameHighScores | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GameHighScores = new GameHighScores();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scores = decodeVector(record['scores'], (item: TdJsonRaw): GameHighScore => decodeGameHighScore(item) as GameHighScore);
  return out;
}

/** Encode a `gameHighScores` into TDLib JSON wire shape. */
export function encodeGameHighScores(value: GameHighScores): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'gameHighScores';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scores'] = encodeVector(value.scores, (item: GameHighScore | null): TdJsonRaw => item === null ? null : encodeGameHighScore(item));
  return out;
}

/** getAccountTtl = AccountTtl — Returns the period of inactivity after which the account of the current user will automatically be deleted */
export class GetAccountTtl {
  type: 'getAccountTtl' = 'getAccountTtl';
  extra?: string;
}

/** Decode a TDLib `getAccountTtl` object; null input -> null. */
export function decodeGetAccountTtl(json: TdJsonRaw): GetAccountTtl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAccountTtl = new GetAccountTtl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getAccountTtl` into TDLib JSON wire shape. */
export function encodeGetAccountTtl(value: GetAccountTtl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAccountTtl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getActiveSessions = Sessions — Returns all active sessions of the current user. Additionally, getBusinessConnectedBot must be used to show the bot on top of active sessions */
export class GetActiveSessions {
  type: 'getActiveSessions' = 'getActiveSessions';
  extra?: string;
}

/** Decode a TDLib `getActiveSessions` object; null input -> null. */
export function decodeGetActiveSessions(json: TdJsonRaw): GetActiveSessions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetActiveSessions = new GetActiveSessions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getActiveSessions` into TDLib JSON wire shape. */
export function encodeGetActiveSessions(value: GetActiveSessions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getActiveSessions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getAllPassportElements = PassportElements — Returns all available Telegram Passport elements */
export class GetAllPassportElements {
  type: 'getAllPassportElements' = 'getAllPassportElements';
  extra?: string;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
}

/** Decode a TDLib `getAllPassportElements` object; null input -> null. */
export function decodeGetAllPassportElements(json: TdJsonRaw): GetAllPassportElements | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAllPassportElements = new GetAllPassportElements();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.password = asString(record['password']);
  return out;
}

/** Encode a `getAllPassportElements` into TDLib JSON wire shape. */
export function encodeGetAllPassportElements(value: GetAllPassportElements): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAllPassportElements';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['password'] = value.password;
  return out;
}

/** getAllStickerEmojis = Emojis — Returns unique emoji that correspond to stickers to be found by the getStickers(sticker_type, query, 1000000, chat_id) */
export class GetAllStickerEmojis {
  type: 'getAllStickerEmojis' = 'getAllStickerEmojis';
  extra?: string;
  /** sticker_type: StickerType — Type of the stickers to search for */
  sticker_type: TdStickerType | null = null;
  /** query: string — Search query */
  query: string = '';
  /** chat_id: int53 — Chat identifier for which to find stickers */
  chat_id: number = 0;
  /** return_only_main_emoji: Bool — Pass true if only main emoji for each found sticker must be included in the result */
  return_only_main_emoji: boolean = false;
}

/** Decode a TDLib `getAllStickerEmojis` object; null input -> null. */
export function decodeGetAllStickerEmojis(json: TdJsonRaw): GetAllStickerEmojis | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAllStickerEmojis = new GetAllStickerEmojis();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.query = asString(record['query']);
  out.chat_id = asNumber(record['chat_id']);
  out.return_only_main_emoji = asBool(record['return_only_main_emoji']);
  return out;
}

/** Encode a `getAllStickerEmojis` into TDLib JSON wire shape. */
export function encodeGetAllStickerEmojis(value: GetAllStickerEmojis): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAllStickerEmojis';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['query'] = value.query;
  out['chat_id'] = value.chat_id;
  out['return_only_main_emoji'] = value.return_only_main_emoji;
  return out;
}

/** getAnimatedEmoji = AnimatedEmoji — Returns an animated emoji corresponding to a given emoji. Returns a 404 error if the emoji has no animated emoji */
export class GetAnimatedEmoji {
  type: 'getAnimatedEmoji' = 'getAnimatedEmoji';
  extra?: string;
  /** emoji: string — The emoji */
  emoji: string = '';
}

/** Decode a TDLib `getAnimatedEmoji` object; null input -> null. */
export function decodeGetAnimatedEmoji(json: TdJsonRaw): GetAnimatedEmoji | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAnimatedEmoji = new GetAnimatedEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji = asString(record['emoji']);
  return out;
}

/** Encode a `getAnimatedEmoji` into TDLib JSON wire shape. */
export function encodeGetAnimatedEmoji(value: GetAnimatedEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAnimatedEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji'] = value.emoji;
  return out;
}

/** getApplicationConfig = JsonValue — Returns application config, provided by the server. Can be called before authorization */
export class GetApplicationConfig {
  type: 'getApplicationConfig' = 'getApplicationConfig';
  extra?: string;
}

/** Decode a TDLib `getApplicationConfig` object; null input -> null. */
export function decodeGetApplicationConfig(json: TdJsonRaw): GetApplicationConfig | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetApplicationConfig = new GetApplicationConfig();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getApplicationConfig` into TDLib JSON wire shape. */
export function encodeGetApplicationConfig(value: GetApplicationConfig): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getApplicationConfig';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getApplicationDownloadLink = HttpUrl — Returns the link for downloading official Telegram application to be used when the current user invites friends to Telegram */
export class GetApplicationDownloadLink {
  type: 'getApplicationDownloadLink' = 'getApplicationDownloadLink';
  extra?: string;
}

/** Decode a TDLib `getApplicationDownloadLink` object; null input -> null. */
export function decodeGetApplicationDownloadLink(json: TdJsonRaw): GetApplicationDownloadLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetApplicationDownloadLink = new GetApplicationDownloadLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getApplicationDownloadLink` into TDLib JSON wire shape. */
export function encodeGetApplicationDownloadLink(value: GetApplicationDownloadLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getApplicationDownloadLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getArchiveChatListSettings = ArchiveChatListSettings — Returns settings for automatic moving of chats to and from the Archive chat lists */
export class GetArchiveChatListSettings {
  type: 'getArchiveChatListSettings' = 'getArchiveChatListSettings';
  extra?: string;
}

/** Decode a TDLib `getArchiveChatListSettings` object; null input -> null. */
export function decodeGetArchiveChatListSettings(json: TdJsonRaw): GetArchiveChatListSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetArchiveChatListSettings = new GetArchiveChatListSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getArchiveChatListSettings` into TDLib JSON wire shape. */
export function encodeGetArchiveChatListSettings(value: GetArchiveChatListSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getArchiveChatListSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getArchivedStickerSets = StickerSets — Returns a list of archived sticker sets */
export class GetArchivedStickerSets {
  type: 'getArchivedStickerSets' = 'getArchivedStickerSets';
  extra?: string;
  /** sticker_type: StickerType — Type of the sticker sets to return */
  sticker_type: TdStickerType | null = null;
  /** offset_sticker_set_id: int64 — Identifier of the sticker set from which to return the result; use 0 to get results from the beginning */
  offset_sticker_set_id: string = '';
  /** limit: int32 — The maximum number of sticker sets to return; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `getArchivedStickerSets` object; null input -> null. */
export function decodeGetArchivedStickerSets(json: TdJsonRaw): GetArchivedStickerSets | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetArchivedStickerSets = new GetArchivedStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.offset_sticker_set_id = asInt64(record['offset_sticker_set_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getArchivedStickerSets` into TDLib JSON wire shape. */
export function encodeGetArchivedStickerSets(value: GetArchivedStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getArchivedStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['offset_sticker_set_id'] = value.offset_sticker_set_id;
  out['limit'] = value.limit;
  return out;
}

/** getAttachedStickerSets = StickerSets — Returns a list of sticker sets attached to a file, including regular, mask, and emoji sticker sets. Currently, only animations, photos, and videos can have attached sticker sets */
export class GetAttachedStickerSets {
  type: 'getAttachedStickerSets' = 'getAttachedStickerSets';
  extra?: string;
  /** file_id: int32 — File identifier */
  file_id: number = 0;
}

/** Decode a TDLib `getAttachedStickerSets` object; null input -> null. */
export function decodeGetAttachedStickerSets(json: TdJsonRaw): GetAttachedStickerSets | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAttachedStickerSets = new GetAttachedStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  return out;
}

/** Encode a `getAttachedStickerSets` into TDLib JSON wire shape. */
export function encodeGetAttachedStickerSets(value: GetAttachedStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAttachedStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  return out;
}

/** getAttachmentMenuBot = AttachmentMenuBot — Returns information about a bot that can be added to attachment or side menu */
export class GetAttachmentMenuBot {
  type: 'getAttachmentMenuBot' = 'getAttachmentMenuBot';
  extra?: string;
  /** bot_user_id: int53 — Bot's user identifier */
  bot_user_id: number = 0;
}

/** Decode a TDLib `getAttachmentMenuBot` object; null input -> null. */
export function decodeGetAttachmentMenuBot(json: TdJsonRaw): GetAttachmentMenuBot | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAttachmentMenuBot = new GetAttachmentMenuBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `getAttachmentMenuBot` into TDLib JSON wire shape. */
export function encodeGetAttachmentMenuBot(value: GetAttachmentMenuBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAttachmentMenuBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** getAuthenticationPasskeyParameters = Text — Returns parameters for authentication using a passkey as JSON-serialized string */
export class GetAuthenticationPasskeyParameters {
  type: 'getAuthenticationPasskeyParameters' = 'getAuthenticationPasskeyParameters';
  extra?: string;
}

/** Decode a TDLib `getAuthenticationPasskeyParameters` object; null input -> null. */
export function decodeGetAuthenticationPasskeyParameters(json: TdJsonRaw): GetAuthenticationPasskeyParameters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAuthenticationPasskeyParameters = new GetAuthenticationPasskeyParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getAuthenticationPasskeyParameters` into TDLib JSON wire shape. */
export function encodeGetAuthenticationPasskeyParameters(value: GetAuthenticationPasskeyParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAuthenticationPasskeyParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getAuthorizationState = AuthorizationState — Returns the current authorization state. This is an offline method. For informational purposes only. Use updateAuthorizationState instead to maintain the current authorization state. Can be ca... */
export class GetAuthorizationState {
  type: 'getAuthorizationState' = 'getAuthorizationState';
  extra?: string;
}

/** Decode a TDLib `getAuthorizationState` object; null input -> null. */
export function decodeGetAuthorizationState(json: TdJsonRaw): GetAuthorizationState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAuthorizationState = new GetAuthorizationState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getAuthorizationState` into TDLib JSON wire shape. */
export function encodeGetAuthorizationState(value: GetAuthorizationState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAuthorizationState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getAutoDownloadSettingsPresets = AutoDownloadSettingsPresets — Returns auto-download settings presets for the current user */
export class GetAutoDownloadSettingsPresets {
  type: 'getAutoDownloadSettingsPresets' = 'getAutoDownloadSettingsPresets';
  extra?: string;
}

/** Decode a TDLib `getAutoDownloadSettingsPresets` object; null input -> null. */
export function decodeGetAutoDownloadSettingsPresets(json: TdJsonRaw): GetAutoDownloadSettingsPresets | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAutoDownloadSettingsPresets = new GetAutoDownloadSettingsPresets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getAutoDownloadSettingsPresets` into TDLib JSON wire shape. */
export function encodeGetAutoDownloadSettingsPresets(value: GetAutoDownloadSettingsPresets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAutoDownloadSettingsPresets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getAutosaveSettings = AutosaveSettings — Returns autosave settings for the current user */
export class GetAutosaveSettings {
  type: 'getAutosaveSettings' = 'getAutosaveSettings';
  extra?: string;
}

/** Decode a TDLib `getAutosaveSettings` object; null input -> null. */
export function decodeGetAutosaveSettings(json: TdJsonRaw): GetAutosaveSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAutosaveSettings = new GetAutosaveSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getAutosaveSettings` into TDLib JSON wire shape. */
export function encodeGetAutosaveSettings(value: GetAutosaveSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAutosaveSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getAvailableChatBoostSlots = ChatBoostSlots — Returns the list of available chat boost slots for the current user */
export class GetAvailableChatBoostSlots {
  type: 'getAvailableChatBoostSlots' = 'getAvailableChatBoostSlots';
  extra?: string;
}

/** Decode a TDLib `getAvailableChatBoostSlots` object; null input -> null. */
export function decodeGetAvailableChatBoostSlots(json: TdJsonRaw): GetAvailableChatBoostSlots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAvailableChatBoostSlots = new GetAvailableChatBoostSlots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getAvailableChatBoostSlots` into TDLib JSON wire shape. */
export function encodeGetAvailableChatBoostSlots(value: GetAvailableChatBoostSlots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAvailableChatBoostSlots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getAvailableGifts = AvailableGifts — Returns gifts that can be sent to other users and channel chats */
export class GetAvailableGifts {
  type: 'getAvailableGifts' = 'getAvailableGifts';
  extra?: string;
}

/** Decode a TDLib `getAvailableGifts` object; null input -> null. */
export function decodeGetAvailableGifts(json: TdJsonRaw): GetAvailableGifts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetAvailableGifts = new GetAvailableGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getAvailableGifts` into TDLib JSON wire shape. */
export function encodeGetAvailableGifts(value: GetAvailableGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getAvailableGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getBackgroundUrl = HttpUrl — Constructs a persistent HTTP URL for a background */
export class GetBackgroundUrl {
  type: 'getBackgroundUrl' = 'getBackgroundUrl';
  extra?: string;
  /** name: string — Background name */
  name: string = '';
  /** type: BackgroundType — Background type; backgroundTypeChatTheme isn't supported */
  type_: TdBackgroundType | null = null;
}

/** Decode a TDLib `getBackgroundUrl` object; null input -> null. */
export function decodeGetBackgroundUrl(json: TdJsonRaw): GetBackgroundUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBackgroundUrl = new GetBackgroundUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.type_ = decodeTdBackgroundType(record['type']);
  return out;
}

/** Encode a `getBackgroundUrl` into TDLib JSON wire shape. */
export function encodeGetBackgroundUrl(value: GetBackgroundUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBackgroundUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['type'] = value.type_ === null ? null : encodeTdBackgroundType(value.type_);
  return out;
}

/** getBankCardInfo = BankCardInfo — Returns information about a bank card */
export class GetBankCardInfo {
  type: 'getBankCardInfo' = 'getBankCardInfo';
  extra?: string;
  /** bank_card_number: string — The bank card number */
  bank_card_number: string = '';
}

/** Decode a TDLib `getBankCardInfo` object; null input -> null. */
export function decodeGetBankCardInfo(json: TdJsonRaw): GetBankCardInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBankCardInfo = new GetBankCardInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bank_card_number = asString(record['bank_card_number']);
  return out;
}

/** Encode a `getBankCardInfo` into TDLib JSON wire shape. */
export function encodeGetBankCardInfo(value: GetBankCardInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBankCardInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bank_card_number'] = value.bank_card_number;
  return out;
}

/** getBasicGroup = BasicGroup — Returns information about a basic group by its identifier. This is an offline method if the current user is not a bot */
export class GetBasicGroup {
  type: 'getBasicGroup' = 'getBasicGroup';
  extra?: string;
  /** basic_group_id: int53 — Basic group identifier */
  basic_group_id: number = 0;
}

/** Decode a TDLib `getBasicGroup` object; null input -> null. */
export function decodeGetBasicGroup(json: TdJsonRaw): GetBasicGroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBasicGroup = new GetBasicGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.basic_group_id = asNumber(record['basic_group_id']);
  return out;
}

/** Encode a `getBasicGroup` into TDLib JSON wire shape. */
export function encodeGetBasicGroup(value: GetBasicGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBasicGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['basic_group_id'] = value.basic_group_id;
  return out;
}

/** getBasicGroupFullInfo = BasicGroupFullInfo — Returns full information about a basic group by its identifier */
export class GetBasicGroupFullInfo {
  type: 'getBasicGroupFullInfo' = 'getBasicGroupFullInfo';
  extra?: string;
  /** basic_group_id: int53 — Basic group identifier */
  basic_group_id: number = 0;
}

/** Decode a TDLib `getBasicGroupFullInfo` object; null input -> null. */
export function decodeGetBasicGroupFullInfo(json: TdJsonRaw): GetBasicGroupFullInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBasicGroupFullInfo = new GetBasicGroupFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.basic_group_id = asNumber(record['basic_group_id']);
  return out;
}

/** Encode a `getBasicGroupFullInfo` into TDLib JSON wire shape. */
export function encodeGetBasicGroupFullInfo(value: GetBasicGroupFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBasicGroupFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['basic_group_id'] = value.basic_group_id;
  return out;
}

/** getBlockedMessageSenders = MessageSenders — Returns users and chats that were blocked by the current user */
export class GetBlockedMessageSenders {
  type: 'getBlockedMessageSenders' = 'getBlockedMessageSenders';
  extra?: string;
  /** block_list: BlockList — Block list from which to return users */
  block_list: TdBlockList | null = null;
  /** offset: int32 — Number of users and chats to skip in the result; must be non-negative */
  offset: number = 0;
  /** limit: int32 — The maximum number of users and chats to return; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `getBlockedMessageSenders` object; null input -> null. */
export function decodeGetBlockedMessageSenders(json: TdJsonRaw): GetBlockedMessageSenders | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBlockedMessageSenders = new GetBlockedMessageSenders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.block_list = decodeTdBlockList(record['block_list']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getBlockedMessageSenders` into TDLib JSON wire shape. */
export function encodeGetBlockedMessageSenders(value: GetBlockedMessageSenders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBlockedMessageSenders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['block_list'] = value.block_list === null ? null : encodeTdBlockList(value.block_list);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getBotInfoDescription = Text — Returns the text shown in the chat with a bot if the chat is empty in the given language. Can be called only if userTypeBot.can_be_edited == true */
export class GetBotInfoDescription {
  type: 'getBotInfoDescription' = 'getBotInfoDescription';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** language_code: string — A two-letter ISO 639-1 language code or an empty string */
  language_code: string = '';
}

/** Decode a TDLib `getBotInfoDescription` object; null input -> null. */
export function decodeGetBotInfoDescription(json: TdJsonRaw): GetBotInfoDescription | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBotInfoDescription = new GetBotInfoDescription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  return out;
}

/** Encode a `getBotInfoDescription` into TDLib JSON wire shape. */
export function encodeGetBotInfoDescription(value: GetBotInfoDescription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBotInfoDescription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  return out;
}

/** getBotInfoShortDescription = Text — Returns the text shown on a bot's profile page and sent together with the link when users share the bot in the given language. Can be called only if userTypeBot.can_be_edited == true */
export class GetBotInfoShortDescription {
  type: 'getBotInfoShortDescription' = 'getBotInfoShortDescription';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** language_code: string — A two-letter ISO 639-1 language code or an empty string */
  language_code: string = '';
}

/** Decode a TDLib `getBotInfoShortDescription` object; null input -> null. */
export function decodeGetBotInfoShortDescription(json: TdJsonRaw): GetBotInfoShortDescription | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBotInfoShortDescription = new GetBotInfoShortDescription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  return out;
}

/** Encode a `getBotInfoShortDescription` into TDLib JSON wire shape. */
export function encodeGetBotInfoShortDescription(value: GetBotInfoShortDescription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBotInfoShortDescription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  return out;
}

/** getBotMediaPreviewInfo = BotMediaPreviewInfo — Returns the list of media previews for the given language and the list of languages for which the bot has dedicated previews */
export class GetBotMediaPreviewInfo {
  type: 'getBotMediaPreviewInfo' = 'getBotMediaPreviewInfo';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot. The bot must be owned and must have the main Web App */
  bot_user_id: number = 0;
  /** language_code: string — A two-letter ISO 639-1 language code for which to get previews. If empty, then default previews are returned */
  language_code: string = '';
}

/** Decode a TDLib `getBotMediaPreviewInfo` object; null input -> null. */
export function decodeGetBotMediaPreviewInfo(json: TdJsonRaw): GetBotMediaPreviewInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBotMediaPreviewInfo = new GetBotMediaPreviewInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  return out;
}

/** Encode a `getBotMediaPreviewInfo` into TDLib JSON wire shape. */
export function encodeGetBotMediaPreviewInfo(value: GetBotMediaPreviewInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBotMediaPreviewInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  return out;
}

/** getBotMediaPreviews = BotMediaPreviews — Returns the list of media previews of a bot */
export class GetBotMediaPreviews {
  type: 'getBotMediaPreviews' = 'getBotMediaPreviews';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot. The bot must have the main Web App */
  bot_user_id: number = 0;
}

/** Decode a TDLib `getBotMediaPreviews` object; null input -> null. */
export function decodeGetBotMediaPreviews(json: TdJsonRaw): GetBotMediaPreviews | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBotMediaPreviews = new GetBotMediaPreviews();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `getBotMediaPreviews` into TDLib JSON wire shape. */
export function encodeGetBotMediaPreviews(value: GetBotMediaPreviews): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBotMediaPreviews';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** getBotName = Text — Returns the name of a bot in the given language. Can be called only if userTypeBot.can_be_edited == true */
export class GetBotName {
  type: 'getBotName' = 'getBotName';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** language_code: string — A two-letter ISO 639-1 language code or an empty string */
  language_code: string = '';
}

/** Decode a TDLib `getBotName` object; null input -> null. */
export function decodeGetBotName(json: TdJsonRaw): GetBotName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBotName = new GetBotName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  return out;
}

/** Encode a `getBotName` into TDLib JSON wire shape. */
export function encodeGetBotName(value: GetBotName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBotName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  return out;
}

/** getBotSimilarBotCount = Count — Returns approximate number of bots similar to the given bot */
export class GetBotSimilarBotCount {
  type: 'getBotSimilarBotCount' = 'getBotSimilarBotCount';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the target bot */
  bot_user_id: number = 0;
  /** return_local: Bool — Pass true to get the number of bots without sending network requests, or -1 if the number of bots is unknown locally */
  return_local: boolean = false;
}

/** Decode a TDLib `getBotSimilarBotCount` object; null input -> null. */
export function decodeGetBotSimilarBotCount(json: TdJsonRaw): GetBotSimilarBotCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBotSimilarBotCount = new GetBotSimilarBotCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.return_local = asBool(record['return_local']);
  return out;
}

/** Encode a `getBotSimilarBotCount` into TDLib JSON wire shape. */
export function encodeGetBotSimilarBotCount(value: GetBotSimilarBotCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBotSimilarBotCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['return_local'] = value.return_local;
  return out;
}

/** getBotSimilarBots = Users — Returns a list of bots similar to the given bot */
export class GetBotSimilarBots {
  type: 'getBotSimilarBots' = 'getBotSimilarBots';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the target bot */
  bot_user_id: number = 0;
}

/** Decode a TDLib `getBotSimilarBots` object; null input -> null. */
export function decodeGetBotSimilarBots(json: TdJsonRaw): GetBotSimilarBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBotSimilarBots = new GetBotSimilarBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `getBotSimilarBots` into TDLib JSON wire shape. */
export function encodeGetBotSimilarBots(value: GetBotSimilarBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBotSimilarBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** getBusinessAccountStarAmount = StarAmount — Returns the Telegram Star amount owned by a business account; for bots only */
export class GetBusinessAccountStarAmount {
  type: 'getBusinessAccountStarAmount' = 'getBusinessAccountStarAmount';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection */
  business_connection_id: string = '';
}

/** Decode a TDLib `getBusinessAccountStarAmount` object; null input -> null. */
export function decodeGetBusinessAccountStarAmount(json: TdJsonRaw): GetBusinessAccountStarAmount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBusinessAccountStarAmount = new GetBusinessAccountStarAmount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  return out;
}

/** Encode a `getBusinessAccountStarAmount` into TDLib JSON wire shape. */
export function encodeGetBusinessAccountStarAmount(value: GetBusinessAccountStarAmount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBusinessAccountStarAmount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  return out;
}

/** getBusinessChatLinkInfo = BusinessChatLinkInfo — Returns information about a business chat link */
export class GetBusinessChatLinkInfo {
  type: 'getBusinessChatLinkInfo' = 'getBusinessChatLinkInfo';
  extra?: string;
  /** link_name: string — Name of the link */
  link_name: string = '';
}

/** Decode a TDLib `getBusinessChatLinkInfo` object; null input -> null. */
export function decodeGetBusinessChatLinkInfo(json: TdJsonRaw): GetBusinessChatLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBusinessChatLinkInfo = new GetBusinessChatLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link_name = asString(record['link_name']);
  return out;
}

/** Encode a `getBusinessChatLinkInfo` into TDLib JSON wire shape. */
export function encodeGetBusinessChatLinkInfo(value: GetBusinessChatLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBusinessChatLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link_name'] = value.link_name;
  return out;
}

/** getBusinessChatLinks = BusinessChatLinks — Returns business chat links created for the current account */
export class GetBusinessChatLinks {
  type: 'getBusinessChatLinks' = 'getBusinessChatLinks';
  extra?: string;
}

/** Decode a TDLib `getBusinessChatLinks` object; null input -> null. */
export function decodeGetBusinessChatLinks(json: TdJsonRaw): GetBusinessChatLinks | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBusinessChatLinks = new GetBusinessChatLinks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getBusinessChatLinks` into TDLib JSON wire shape. */
export function encodeGetBusinessChatLinks(value: GetBusinessChatLinks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBusinessChatLinks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getBusinessConnectedBot = BusinessConnectedBotInfo — Returns information about the business bot that is connected to the current user account. Returns a 404 error if there is no connected bot */
export class GetBusinessConnectedBot {
  type: 'getBusinessConnectedBot' = 'getBusinessConnectedBot';
  extra?: string;
}

/** Decode a TDLib `getBusinessConnectedBot` object; null input -> null. */
export function decodeGetBusinessConnectedBot(json: TdJsonRaw): GetBusinessConnectedBot | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBusinessConnectedBot = new GetBusinessConnectedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getBusinessConnectedBot` into TDLib JSON wire shape. */
export function encodeGetBusinessConnectedBot(value: GetBusinessConnectedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBusinessConnectedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getBusinessConnection = BusinessConnection — Returns information about a business connection by its identifier; for bots only */
export class GetBusinessConnection {
  type: 'getBusinessConnection' = 'getBusinessConnection';
  extra?: string;
  /** connection_id: string — Identifier of the business connection to return */
  connection_id: string = '';
}

/** Decode a TDLib `getBusinessConnection` object; null input -> null. */
export function decodeGetBusinessConnection(json: TdJsonRaw): GetBusinessConnection | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBusinessConnection = new GetBusinessConnection();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.connection_id = asString(record['connection_id']);
  return out;
}

/** Encode a `getBusinessConnection` into TDLib JSON wire shape. */
export function encodeGetBusinessConnection(value: GetBusinessConnection): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBusinessConnection';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['connection_id'] = value.connection_id;
  return out;
}

/** getBusinessFeatures = BusinessFeatures — Returns information about features, available to Business users */
export class GetBusinessFeatures {
  type: 'getBusinessFeatures' = 'getBusinessFeatures';
  extra?: string;
  /** source: BusinessFeature — Source of the request; pass null if the method is called from settings or some non-standard source */
  source: TdBusinessFeature | null = null;
}

/** Decode a TDLib `getBusinessFeatures` object; null input -> null. */
export function decodeGetBusinessFeatures(json: TdJsonRaw): GetBusinessFeatures | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetBusinessFeatures = new GetBusinessFeatures();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.source = decodeTdBusinessFeature(record['source']);
  return out;
}

/** Encode a `getBusinessFeatures` into TDLib JSON wire shape. */
export function encodeGetBusinessFeatures(value: GetBusinessFeatures): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getBusinessFeatures';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['source'] = value.source === null ? null : encodeTdBusinessFeature(value.source);
  return out;
}

/** getCallbackQueryAnswer = CallbackQueryAnswer — Sends a callback query to a bot and returns an answer. Returns an error with code 502 if the bot fails to answer the query before the query timeout expires */
export class GetCallbackQueryAnswer {
  type: 'getCallbackQueryAnswer' = 'getCallbackQueryAnswer';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message from which the query originated. The message must not be scheduled */
  message_id: number = 0;
  /** payload: CallbackQueryPayload — Query payload */
  payload: TdCallbackQueryPayload | null = null;
}

/** Decode a TDLib `getCallbackQueryAnswer` object; null input -> null. */
export function decodeGetCallbackQueryAnswer(json: TdJsonRaw): GetCallbackQueryAnswer | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCallbackQueryAnswer = new GetCallbackQueryAnswer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.payload = decodeTdCallbackQueryPayload(record['payload']);
  return out;
}

/** Encode a `getCallbackQueryAnswer` into TDLib JSON wire shape. */
export function encodeGetCallbackQueryAnswer(value: GetCallbackQueryAnswer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCallbackQueryAnswer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['payload'] = value.payload === null ? null : encodeTdCallbackQueryPayload(value.payload);
  return out;
}

/** getCallbackQueryMessage = Message — Returns information about a message with the callback button that originated a callback query; for bots only */
export class GetCallbackQueryMessage {
  type: 'getCallbackQueryMessage' = 'getCallbackQueryMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** callback_query_id: int64 — Identifier of the callback query */
  callback_query_id: string = '';
}

/** Decode a TDLib `getCallbackQueryMessage` object; null input -> null. */
export function decodeGetCallbackQueryMessage(json: TdJsonRaw): GetCallbackQueryMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCallbackQueryMessage = new GetCallbackQueryMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.callback_query_id = asInt64(record['callback_query_id']);
  return out;
}

/** Encode a `getCallbackQueryMessage` into TDLib JSON wire shape. */
export function encodeGetCallbackQueryMessage(value: GetCallbackQueryMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCallbackQueryMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['callback_query_id'] = value.callback_query_id;
  return out;
}

/** getChat = Chat — Returns information about a chat by its identifier. This is an offline method if the current user is not a bot */
export class GetChat {
  type: 'getChat' = 'getChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChat` object; null input -> null. */
export function decodeGetChat(json: TdJsonRaw): GetChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChat = new GetChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChat` into TDLib JSON wire shape. */
export function encodeGetChat(value: GetChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatActiveStories = ChatActiveStories — Returns the list of active stories posted by the given chat */
export class GetChatActiveStories {
  type: 'getChatActiveStories' = 'getChatActiveStories';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatActiveStories` object; null input -> null. */
export function decodeGetChatActiveStories(json: TdJsonRaw): GetChatActiveStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatActiveStories = new GetChatActiveStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatActiveStories` into TDLib JSON wire shape. */
export function encodeGetChatActiveStories(value: GetChatActiveStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatActiveStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatAdministrators = ChatAdministrators — Returns a list of administrators of the chat with their custom titles */
export class GetChatAdministrators {
  type: 'getChatAdministrators' = 'getChatAdministrators';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatAdministrators` object; null input -> null. */
export function decodeGetChatAdministrators(json: TdJsonRaw): GetChatAdministrators | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatAdministrators = new GetChatAdministrators();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatAdministrators` into TDLib JSON wire shape. */
export function encodeGetChatAdministrators(value: GetChatAdministrators): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatAdministrators';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatArchivedStories = Stories — Returns the list of all stories posted by the given chat; requires can_edit_stories administrator right in the chat. The stories are returned in reverse chronological order (i.e., in order of decreasing... */
export class GetChatArchivedStories {
  type: 'getChatArchivedStories' = 'getChatArchivedStories';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** from_story_id: int32 — Identifier of the story starting from which stories must be returned; use 0 to get results from the last story */
  from_story_id: number = 0;
  /** limit: int32 — The maximum number of stories to be returned. */
  limit: number = 0;
}

/** Decode a TDLib `getChatArchivedStories` object; null input -> null. */
export function decodeGetChatArchivedStories(json: TdJsonRaw): GetChatArchivedStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatArchivedStories = new GetChatArchivedStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.from_story_id = asNumber(record['from_story_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getChatArchivedStories` into TDLib JSON wire shape. */
export function encodeGetChatArchivedStories(value: GetChatArchivedStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatArchivedStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['from_story_id'] = value.from_story_id;
  out['limit'] = value.limit;
  return out;
}

/** getChatAvailableMessageSenders = ChatMessageSenders — Returns the list of message sender identifiers, which can be used to send messages in a chat */
export class GetChatAvailableMessageSenders {
  type: 'getChatAvailableMessageSenders' = 'getChatAvailableMessageSenders';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatAvailableMessageSenders` object; null input -> null. */
export function decodeGetChatAvailableMessageSenders(json: TdJsonRaw): GetChatAvailableMessageSenders | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatAvailableMessageSenders = new GetChatAvailableMessageSenders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatAvailableMessageSenders` into TDLib JSON wire shape. */
export function encodeGetChatAvailableMessageSenders(value: GetChatAvailableMessageSenders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatAvailableMessageSenders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatAvailablePaidMessageReactionSenders = MessageSenders — Returns the list of message sender identifiers, which can be used to send a paid reaction in a chat */
export class GetChatAvailablePaidMessageReactionSenders {
  type: 'getChatAvailablePaidMessageReactionSenders' = 'getChatAvailablePaidMessageReactionSenders';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatAvailablePaidMessageReactionSenders` object; null input -> null. */
export function decodeGetChatAvailablePaidMessageReactionSenders(json: TdJsonRaw): GetChatAvailablePaidMessageReactionSenders | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatAvailablePaidMessageReactionSenders = new GetChatAvailablePaidMessageReactionSenders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatAvailablePaidMessageReactionSenders` into TDLib JSON wire shape. */
export function encodeGetChatAvailablePaidMessageReactionSenders(value: GetChatAvailablePaidMessageReactionSenders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatAvailablePaidMessageReactionSenders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatBoostFeatures = ChatBoostFeatures — Returns the list of features available for different chat boost levels. This is an offline method */
export class GetChatBoostFeatures {
  type: 'getChatBoostFeatures' = 'getChatBoostFeatures';
  extra?: string;
  /** is_channel: Bool — Pass true to get the list of features for channels; pass false to get the list of features for supergroups */
  is_channel: boolean = false;
}

/** Decode a TDLib `getChatBoostFeatures` object; null input -> null. */
export function decodeGetChatBoostFeatures(json: TdJsonRaw): GetChatBoostFeatures | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatBoostFeatures = new GetChatBoostFeatures();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_channel = asBool(record['is_channel']);
  return out;
}

/** Encode a `getChatBoostFeatures` into TDLib JSON wire shape. */
export function encodeGetChatBoostFeatures(value: GetChatBoostFeatures): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatBoostFeatures';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_channel'] = value.is_channel;
  return out;
}

/** getChatBoostLevelFeatures = ChatBoostLevelFeatures — Returns the list of features available on the specific chat boost level. This is an offline method */
export class GetChatBoostLevelFeatures {
  type: 'getChatBoostLevelFeatures' = 'getChatBoostLevelFeatures';
  extra?: string;
  /** is_channel: Bool — Pass true to get the list of features for channels; pass false to get the list of features for supergroups */
  is_channel: boolean = false;
  /** level: int32 — Chat boost level */
  level: number = 0;
}

/** Decode a TDLib `getChatBoostLevelFeatures` object; null input -> null. */
export function decodeGetChatBoostLevelFeatures(json: TdJsonRaw): GetChatBoostLevelFeatures | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatBoostLevelFeatures = new GetChatBoostLevelFeatures();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_channel = asBool(record['is_channel']);
  out.level = asNumber(record['level']);
  return out;
}

/** Encode a `getChatBoostLevelFeatures` into TDLib JSON wire shape. */
export function encodeGetChatBoostLevelFeatures(value: GetChatBoostLevelFeatures): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatBoostLevelFeatures';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_channel'] = value.is_channel;
  out['level'] = value.level;
  return out;
}

/** getChatBoostLink = ChatBoostLink — Returns an HTTPS link to boost the specified supergroup or channel chat */
export class GetChatBoostLink {
  type: 'getChatBoostLink' = 'getChatBoostLink';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatBoostLink` object; null input -> null. */
export function decodeGetChatBoostLink(json: TdJsonRaw): GetChatBoostLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatBoostLink = new GetChatBoostLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatBoostLink` into TDLib JSON wire shape. */
export function encodeGetChatBoostLink(value: GetChatBoostLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatBoostLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatBoostLinkInfo = ChatBoostLinkInfo — Returns information about a link to boost a chat. Can be called for any internal link of the type internalLinkTypeChatBoost */
export class GetChatBoostLinkInfo {
  type: 'getChatBoostLinkInfo' = 'getChatBoostLinkInfo';
  extra?: string;
  /** url: string — The link to boost a chat */
  url: string = '';
}

/** Decode a TDLib `getChatBoostLinkInfo` object; null input -> null. */
export function decodeGetChatBoostLinkInfo(json: TdJsonRaw): GetChatBoostLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatBoostLinkInfo = new GetChatBoostLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `getChatBoostLinkInfo` into TDLib JSON wire shape. */
export function encodeGetChatBoostLinkInfo(value: GetChatBoostLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatBoostLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** getChatBoostStatus = ChatBoostStatus — Returns the current boost status for a supergroup or a channel chat */
export class GetChatBoostStatus {
  type: 'getChatBoostStatus' = 'getChatBoostStatus';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatBoostStatus` object; null input -> null. */
export function decodeGetChatBoostStatus(json: TdJsonRaw): GetChatBoostStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatBoostStatus = new GetChatBoostStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatBoostStatus` into TDLib JSON wire shape. */
export function encodeGetChatBoostStatus(value: GetChatBoostStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatBoostStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatBoosts = FoundChatBoosts — Returns the list of boosts applied to a chat; requires administrator rights in the chat */
export class GetChatBoosts {
  type: 'getChatBoosts' = 'getChatBoosts';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** only_gift_codes: Bool — Pass true to receive only boosts received from gift codes and giveaways created by the chat */
  only_gift_codes: boolean = false;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of boosts to be returned; up to 100. For optimal performance, the number of returned boosts can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `getChatBoosts` object; null input -> null. */
export function decodeGetChatBoosts(json: TdJsonRaw): GetChatBoosts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatBoosts = new GetChatBoosts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.only_gift_codes = asBool(record['only_gift_codes']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getChatBoosts` into TDLib JSON wire shape. */
export function encodeGetChatBoosts(value: GetChatBoosts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatBoosts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['only_gift_codes'] = value.only_gift_codes;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getChatEventLog = ChatEvents — Returns a list of service actions taken by chat members and administrators in the last 48 hours. Available only in supergroups and channels. Requires administrator rights. Returns results in reverse chronol... */
export class GetChatEventLog {
  type: 'getChatEventLog' = 'getChatEventLog';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** query: string — Search query by which to filter events */
  query: string = '';
  /** from_event_id: int64 — Identifier of an event from which to return results. Use 0 to get results from the latest events */
  from_event_id: string = '';
  /** limit: int32 — The maximum number of events to return; up to 100 */
  limit: number = 0;
  /** filters: chatEventLogFilters — The types of events to return; pass null to get chat events of all types */
  filters: ChatEventLogFilters | null = null;
  /** user_ids: vector<int53> — User identifiers by which to filter events. By default, events relating to all users will be returned */
  user_ids: number[] = [];
}

/** Decode a TDLib `getChatEventLog` object; null input -> null. */
export function decodeGetChatEventLog(json: TdJsonRaw): GetChatEventLog | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatEventLog = new GetChatEventLog();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.query = asString(record['query']);
  out.from_event_id = asInt64(record['from_event_id']);
  out.limit = asNumber(record['limit']);
  out.filters = decodeChatEventLogFilters(record['filters']);
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `getChatEventLog` into TDLib JSON wire shape. */
export function encodeGetChatEventLog(value: GetChatEventLog): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatEventLog';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['query'] = value.query;
  out['from_event_id'] = value.from_event_id;
  out['limit'] = value.limit;
  out['filters'] = value.filters === null ? null : encodeChatEventLogFilters(value.filters);
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** getChatFolder = ChatFolder — Returns information about a chat folder by its identifier */
export class GetChatFolder {
  type: 'getChatFolder' = 'getChatFolder';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
}

/** Decode a TDLib `getChatFolder` object; null input -> null. */
export function decodeGetChatFolder(json: TdJsonRaw): GetChatFolder | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatFolder = new GetChatFolder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  return out;
}

/** Encode a `getChatFolder` into TDLib JSON wire shape. */
export function encodeGetChatFolder(value: GetChatFolder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatFolder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  return out;
}

/** getChatFolderChatCount = Count — Returns approximate number of chats in a being created chat folder. Main and archive chat lists must be fully preloaded for this function to work correctly */
export class GetChatFolderChatCount {
  type: 'getChatFolderChatCount' = 'getChatFolderChatCount';
  extra?: string;
  /** folder: chatFolder — The new chat folder */
  folder: ChatFolder | null = null;
}

/** Decode a TDLib `getChatFolderChatCount` object; null input -> null. */
export function decodeGetChatFolderChatCount(json: TdJsonRaw): GetChatFolderChatCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatFolderChatCount = new GetChatFolderChatCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.folder = decodeChatFolder(record['folder']);
  return out;
}

/** Encode a `getChatFolderChatCount` into TDLib JSON wire shape. */
export function encodeGetChatFolderChatCount(value: GetChatFolderChatCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatFolderChatCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['folder'] = value.folder === null ? null : encodeChatFolder(value.folder);
  return out;
}

/** getChatFolderChatsToLeave = Chats — Returns identifiers of pinned or always included chats from a chat folder, which are suggested to be left when the chat folder is deleted */
export class GetChatFolderChatsToLeave {
  type: 'getChatFolderChatsToLeave' = 'getChatFolderChatsToLeave';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
}

/** Decode a TDLib `getChatFolderChatsToLeave` object; null input -> null. */
export function decodeGetChatFolderChatsToLeave(json: TdJsonRaw): GetChatFolderChatsToLeave | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatFolderChatsToLeave = new GetChatFolderChatsToLeave();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  return out;
}

/** Encode a `getChatFolderChatsToLeave` into TDLib JSON wire shape. */
export function encodeGetChatFolderChatsToLeave(value: GetChatFolderChatsToLeave): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatFolderChatsToLeave';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  return out;
}

/** getChatFolderDefaultIconName = ChatFolderIcon — Returns default icon name for a folder. Can be called synchronously */
export class GetChatFolderDefaultIconName {
  type: 'getChatFolderDefaultIconName' = 'getChatFolderDefaultIconName';
  extra?: string;
  /** folder: chatFolder — Chat folder */
  folder: ChatFolder | null = null;
}

/** Decode a TDLib `getChatFolderDefaultIconName` object; null input -> null. */
export function decodeGetChatFolderDefaultIconName(json: TdJsonRaw): GetChatFolderDefaultIconName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatFolderDefaultIconName = new GetChatFolderDefaultIconName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.folder = decodeChatFolder(record['folder']);
  return out;
}

/** Encode a `getChatFolderDefaultIconName` into TDLib JSON wire shape. */
export function encodeGetChatFolderDefaultIconName(value: GetChatFolderDefaultIconName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatFolderDefaultIconName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['folder'] = value.folder === null ? null : encodeChatFolder(value.folder);
  return out;
}

/** getChatFolderInviteLinks = ChatFolderInviteLinks — Returns invite links created by the current user for a shareable chat folder */
export class GetChatFolderInviteLinks {
  type: 'getChatFolderInviteLinks' = 'getChatFolderInviteLinks';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
}

/** Decode a TDLib `getChatFolderInviteLinks` object; null input -> null. */
export function decodeGetChatFolderInviteLinks(json: TdJsonRaw): GetChatFolderInviteLinks | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatFolderInviteLinks = new GetChatFolderInviteLinks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  return out;
}

/** Encode a `getChatFolderInviteLinks` into TDLib JSON wire shape. */
export function encodeGetChatFolderInviteLinks(value: GetChatFolderInviteLinks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatFolderInviteLinks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  return out;
}

/** getChatFolderNewChats = Chats — Returns new chats added to a shareable chat folder by its owner. The method must be called at most once in getOption("chat_folder_new_chats_update_period") for the given chat folder */
export class GetChatFolderNewChats {
  type: 'getChatFolderNewChats' = 'getChatFolderNewChats';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
}

/** Decode a TDLib `getChatFolderNewChats` object; null input -> null. */
export function decodeGetChatFolderNewChats(json: TdJsonRaw): GetChatFolderNewChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatFolderNewChats = new GetChatFolderNewChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  return out;
}

/** Encode a `getChatFolderNewChats` into TDLib JSON wire shape. */
export function encodeGetChatFolderNewChats(value: GetChatFolderNewChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatFolderNewChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  return out;
}

/** getChatHistory = Messages — Returns messages in a chat. The messages are returned in reverse chronological order (i.e., in order of decreasing message_id). For optimal performance, the number of returned messages is chosen by TDLib. This... */
export class GetChatHistory {
  type: 'getChatHistory' = 'getChatHistory';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** from_message_id: int53 — Identifier of the message starting from which history must be fetched; use 0 to get results from the last message */
  from_message_id: number = 0;
  /** offset: int32 — Specify 0 to get results from exactly the message from_message_id or a negative number from -99 to -1 to get additionally -offset newer messages */
  offset: number = 0;
  /** limit: int32 — The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, then the limit must be greater than or equal to -offset. */
  limit: number = 0;
  /** only_local: Bool — Pass true to get only messages that are available without sending network requests */
  only_local: boolean = false;
}

/** Decode a TDLib `getChatHistory` object; null input -> null. */
export function decodeGetChatHistory(json: TdJsonRaw): GetChatHistory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatHistory = new GetChatHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.from_message_id = asNumber(record['from_message_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  out.only_local = asBool(record['only_local']);
  return out;
}

/** Encode a `getChatHistory` into TDLib JSON wire shape. */
export function encodeGetChatHistory(value: GetChatHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['from_message_id'] = value.from_message_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  out['only_local'] = value.only_local;
  return out;
}

/** getChatInviteLink = ChatInviteLink — Returns information about an invite link. Requires administrator privileges and can_invite_users right in the chat to get own links and owner privileges to get other links */
export class GetChatInviteLink {
  type: 'getChatInviteLink' = 'getChatInviteLink';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** invite_link: string — Invite link to get */
  invite_link: string = '';
}

/** Decode a TDLib `getChatInviteLink` object; null input -> null. */
export function decodeGetChatInviteLink(json: TdJsonRaw): GetChatInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatInviteLink = new GetChatInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `getChatInviteLink` into TDLib JSON wire shape. */
export function encodeGetChatInviteLink(value: GetChatInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['invite_link'] = value.invite_link;
  return out;
}

/** getChatInviteLinkCounts = ChatInviteLinkCounts — Returns the list of chat administrators with number of their invite links. Requires owner privileges in the chat */
export class GetChatInviteLinkCounts {
  type: 'getChatInviteLinkCounts' = 'getChatInviteLinkCounts';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatInviteLinkCounts` object; null input -> null. */
export function decodeGetChatInviteLinkCounts(json: TdJsonRaw): GetChatInviteLinkCounts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatInviteLinkCounts = new GetChatInviteLinkCounts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatInviteLinkCounts` into TDLib JSON wire shape. */
export function encodeGetChatInviteLinkCounts(value: GetChatInviteLinkCounts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatInviteLinkCounts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatInviteLinkMembers = ChatInviteLinkMembers — Returns chat members joined a chat via an invite link. Requires administrator privileges and can_invite_users right in the chat for own links and owner privileges for other links */
export class GetChatInviteLinkMembers {
  type: 'getChatInviteLinkMembers' = 'getChatInviteLinkMembers';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** invite_link: string — Invite link for which to return chat members */
  invite_link: string = '';
  /** only_with_expired_subscription: Bool — Pass true if the link is a subscription link and only members with expired subscription must be returned */
  only_with_expired_subscription: boolean = false;
  /** offset_member: chatInviteLinkMember — A chat member from which to return next chat members; pass null to get results from the beginning */
  offset_member: ChatInviteLinkMember | null = null;
  /** limit: int32 — The maximum number of chat members to return; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `getChatInviteLinkMembers` object; null input -> null. */
export function decodeGetChatInviteLinkMembers(json: TdJsonRaw): GetChatInviteLinkMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatInviteLinkMembers = new GetChatInviteLinkMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.invite_link = asString(record['invite_link']);
  out.only_with_expired_subscription = asBool(record['only_with_expired_subscription']);
  out.offset_member = decodeChatInviteLinkMember(record['offset_member']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getChatInviteLinkMembers` into TDLib JSON wire shape. */
export function encodeGetChatInviteLinkMembers(value: GetChatInviteLinkMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatInviteLinkMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['invite_link'] = value.invite_link;
  out['only_with_expired_subscription'] = value.only_with_expired_subscription;
  out['offset_member'] = value.offset_member === null ? null : encodeChatInviteLinkMember(value.offset_member);
  out['limit'] = value.limit;
  return out;
}

/** getChatInviteLinks = ChatInviteLinks — Returns invite links for a chat created by specified administrator. Requires administrator privileges and can_invite_users right in the chat to get own links and owner privileges to get other links */
export class GetChatInviteLinks {
  type: 'getChatInviteLinks' = 'getChatInviteLinks';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** creator_user_id: int53 — User identifier of a chat administrator. Must be an identifier of the current user for non-owner */
  creator_user_id: number = 0;
  /** is_revoked: Bool — Pass true if revoked links need to be returned instead of active or expired */
  is_revoked: boolean = false;
  /** offset_date: int32 — Creation date of an invite link starting after which to return invite links; use 0 to get results from the beginning */
  offset_date: number = 0;
  /** offset_invite_link: string — Invite link starting after which to return invite links; use empty string to get results from the beginning */
  offset_invite_link: string = '';
  /** limit: int32 — The maximum number of invite links to return; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `getChatInviteLinks` object; null input -> null. */
export function decodeGetChatInviteLinks(json: TdJsonRaw): GetChatInviteLinks | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatInviteLinks = new GetChatInviteLinks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.creator_user_id = asNumber(record['creator_user_id']);
  out.is_revoked = asBool(record['is_revoked']);
  out.offset_date = asNumber(record['offset_date']);
  out.offset_invite_link = asString(record['offset_invite_link']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getChatInviteLinks` into TDLib JSON wire shape. */
export function encodeGetChatInviteLinks(value: GetChatInviteLinks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatInviteLinks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['creator_user_id'] = value.creator_user_id;
  out['is_revoked'] = value.is_revoked;
  out['offset_date'] = value.offset_date;
  out['offset_invite_link'] = value.offset_invite_link;
  out['limit'] = value.limit;
  return out;
}

/** getChatJoinRequests = ChatJoinRequests — Returns pending join requests in a chat */
export class GetChatJoinRequests {
  type: 'getChatJoinRequests' = 'getChatJoinRequests';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** invite_link: string — Invite link for which to return join requests. If empty, all join requests will be returned. Requires administrator privileges and can_invite_users right in the chat for own links and owner privileges for other links */
  invite_link: string = '';
  /** query: string — A query to search for in the first names, last names and usernames of the users to return */
  query: string = '';
  /** offset_request: chatJoinRequest — A chat join request from which to return next requests; pass null to get results from the beginning */
  offset_request: ChatJoinRequest | null = null;
  /** limit: int32 — The maximum number of requests to join the chat to return */
  limit: number = 0;
}

/** Decode a TDLib `getChatJoinRequests` object; null input -> null. */
export function decodeGetChatJoinRequests(json: TdJsonRaw): GetChatJoinRequests | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatJoinRequests = new GetChatJoinRequests();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.invite_link = asString(record['invite_link']);
  out.query = asString(record['query']);
  out.offset_request = decodeChatJoinRequest(record['offset_request']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getChatJoinRequests` into TDLib JSON wire shape. */
export function encodeGetChatJoinRequests(value: GetChatJoinRequests): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatJoinRequests';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['invite_link'] = value.invite_link;
  out['query'] = value.query;
  out['offset_request'] = value.offset_request === null ? null : encodeChatJoinRequest(value.offset_request);
  out['limit'] = value.limit;
  return out;
}

/** getChatListsToAddChat = ChatLists — Returns chat lists to which the chat can be added. This is an offline method */
export class GetChatListsToAddChat {
  type: 'getChatListsToAddChat' = 'getChatListsToAddChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatListsToAddChat` object; null input -> null. */
export function decodeGetChatListsToAddChat(json: TdJsonRaw): GetChatListsToAddChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatListsToAddChat = new GetChatListsToAddChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatListsToAddChat` into TDLib JSON wire shape. */
export function encodeGetChatListsToAddChat(value: GetChatListsToAddChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatListsToAddChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatMember = ChatMember — Returns information about a single member of a chat */
export class GetChatMember {
  type: 'getChatMember' = 'getChatMember';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** member_id: MessageSender — Member identifier */
  member_id: TdMessageSender | null = null;
}

/** Decode a TDLib `getChatMember` object; null input -> null. */
export function decodeGetChatMember(json: TdJsonRaw): GetChatMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatMember = new GetChatMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.member_id = decodeTdMessageSender(record['member_id']);
  return out;
}

/** Encode a `getChatMember` into TDLib JSON wire shape. */
export function encodeGetChatMember(value: GetChatMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['member_id'] = value.member_id === null ? null : encodeTdMessageSender(value.member_id);
  return out;
}

/** getChatMessageByDate = Message — Returns the last message sent in a chat no later than the specified date. Returns a 404 error if such message doesn't exist */
export class GetChatMessageByDate {
  type: 'getChatMessageByDate' = 'getChatMessageByDate';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) relative to which to search for messages */
  date: number = 0;
}

/** Decode a TDLib `getChatMessageByDate` object; null input -> null. */
export function decodeGetChatMessageByDate(json: TdJsonRaw): GetChatMessageByDate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatMessageByDate = new GetChatMessageByDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `getChatMessageByDate` into TDLib JSON wire shape. */
export function encodeGetChatMessageByDate(value: GetChatMessageByDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatMessageByDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['date'] = value.date;
  return out;
}

/** getChatMessageCalendar = MessageCalendar — Returns information about the next messages of the specified type in the chat split by days. Returns the results in reverse chronological order. Can return partial result for the last returned d... */
export class GetChatMessageCalendar {
  type: 'getChatMessageCalendar' = 'getChatMessageCalendar';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in which to return information about messages */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Pass topic identifier to get the result only in specific topic; pass null to get the result in all topics; forum topics and message threads aren't supported */
  topic_id: TdMessageTopic | null = null;
  /** filter: SearchMessagesFilter — Filter for message content. Filters searchMessagesFilterEmpty, searchMessagesFilterMention, searchMessagesFilterUnreadMention, searchMessagesFilterUnreadReaction, */
  filter: TdSearchMessagesFilter | null = null;
  /** from_message_id: int53 — The message identifier from which to return information about messages; use 0 to get results from the last message */
  from_message_id: number = 0;
}

/** Decode a TDLib `getChatMessageCalendar` object; null input -> null. */
export function decodeGetChatMessageCalendar(json: TdJsonRaw): GetChatMessageCalendar | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatMessageCalendar = new GetChatMessageCalendar();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.filter = decodeTdSearchMessagesFilter(record['filter']);
  out.from_message_id = asNumber(record['from_message_id']);
  return out;
}

/** Encode a `getChatMessageCalendar` into TDLib JSON wire shape. */
export function encodeGetChatMessageCalendar(value: GetChatMessageCalendar): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatMessageCalendar';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['filter'] = value.filter === null ? null : encodeTdSearchMessagesFilter(value.filter);
  out['from_message_id'] = value.from_message_id;
  return out;
}

/** getChatMessageCount = Count — Returns approximate number of messages of the specified type in the chat or its topic */
export class GetChatMessageCount {
  type: 'getChatMessageCount' = 'getChatMessageCount';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in which to count messages */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Pass topic identifier to get number of messages only in specific topic; pass null to get number of messages in all topics; message threads aren't supported */
  topic_id: TdMessageTopic | null = null;
  /** filter: SearchMessagesFilter — Filter for message content; searchMessagesFilterEmpty is unsupported in this function */
  filter: TdSearchMessagesFilter | null = null;
  /** return_local: Bool — Pass true to get the number of messages without sending network requests, or -1 if the number of messages is unknown locally */
  return_local: boolean = false;
}

/** Decode a TDLib `getChatMessageCount` object; null input -> null. */
export function decodeGetChatMessageCount(json: TdJsonRaw): GetChatMessageCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatMessageCount = new GetChatMessageCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.filter = decodeTdSearchMessagesFilter(record['filter']);
  out.return_local = asBool(record['return_local']);
  return out;
}

/** Encode a `getChatMessageCount` into TDLib JSON wire shape. */
export function encodeGetChatMessageCount(value: GetChatMessageCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatMessageCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['filter'] = value.filter === null ? null : encodeTdSearchMessagesFilter(value.filter);
  out['return_local'] = value.return_local;
  return out;
}

/** getChatMessagePosition = Count — Returns approximate 1-based position of a message among messages, which can be found by the specified filter in the chat and topic. Cannot be used in secret chats and searchMessagesFilterFailedToSend are ... */
export class GetChatMessagePosition {
  type: 'getChatMessagePosition' = 'getChatMessagePosition';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in which to find message position */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Pass topic identifier to get position among messages only in specific topic; pass null to get position among all chat messages; message threads aren't supported */
  topic_id: TdMessageTopic | null = null;
  /** filter: SearchMessagesFilter — Filter for message content; searchMessagesFilterEmpty, searchMessagesFilterUnreadMention, searchMessagesFilterUnreadReaction, searchMessagesFilterUnreadPollVote, */
  filter: TdSearchMessagesFilter | null = null;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
}

/** Decode a TDLib `getChatMessagePosition` object; null input -> null. */
export function decodeGetChatMessagePosition(json: TdJsonRaw): GetChatMessagePosition | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatMessagePosition = new GetChatMessagePosition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.filter = decodeTdSearchMessagesFilter(record['filter']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getChatMessagePosition` into TDLib JSON wire shape. */
export function encodeGetChatMessagePosition(value: GetChatMessagePosition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatMessagePosition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['filter'] = value.filter === null ? null : encodeTdSearchMessagesFilter(value.filter);
  out['message_id'] = value.message_id;
  return out;
}

/** getChatNotificationSettingsExceptions = Chats — Returns the list of chats with non-default notification settings for new messages */
export class GetChatNotificationSettingsExceptions {
  type: 'getChatNotificationSettingsExceptions' = 'getChatNotificationSettingsExceptions';
  extra?: string;
  /** scope: NotificationSettingsScope — If specified, only chats from the scope will be returned; pass null to return chats from all scopes */
  scope: TdNotificationSettingsScope | null = null;
  /** compare_sound: Bool — Pass true to include in the response chats with only non-default sound */
  compare_sound: boolean = false;
}

/** Decode a TDLib `getChatNotificationSettingsExceptions` object; null input -> null. */
export function decodeGetChatNotificationSettingsExceptions(json: TdJsonRaw): GetChatNotificationSettingsExceptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatNotificationSettingsExceptions = new GetChatNotificationSettingsExceptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scope = decodeTdNotificationSettingsScope(record['scope']);
  out.compare_sound = asBool(record['compare_sound']);
  return out;
}

/** Encode a `getChatNotificationSettingsExceptions` into TDLib JSON wire shape. */
export function encodeGetChatNotificationSettingsExceptions(value: GetChatNotificationSettingsExceptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatNotificationSettingsExceptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scope'] = value.scope === null ? null : encodeTdNotificationSettingsScope(value.scope);
  out['compare_sound'] = value.compare_sound;
  return out;
}

/** getChatOwnerAfterLeaving = User — Returns the user who will become the owner of the chat after 7 days if the current user does not return to the supergroup or channel during that period or immediately for basic groups; requires owner pri... */
export class GetChatOwnerAfterLeaving {
  type: 'getChatOwnerAfterLeaving' = 'getChatOwnerAfterLeaving';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatOwnerAfterLeaving` object; null input -> null. */
export function decodeGetChatOwnerAfterLeaving(json: TdJsonRaw): GetChatOwnerAfterLeaving | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatOwnerAfterLeaving = new GetChatOwnerAfterLeaving();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatOwnerAfterLeaving` into TDLib JSON wire shape. */
export function encodeGetChatOwnerAfterLeaving(value: GetChatOwnerAfterLeaving): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatOwnerAfterLeaving';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatPinnedMessage = Message — Returns information about a newest pinned message in the chat. Returns a 404 error if the message doesn't exist */
export class GetChatPinnedMessage {
  type: 'getChatPinnedMessage' = 'getChatPinnedMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat the message belongs to */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatPinnedMessage` object; null input -> null. */
export function decodeGetChatPinnedMessage(json: TdJsonRaw): GetChatPinnedMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatPinnedMessage = new GetChatPinnedMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatPinnedMessage` into TDLib JSON wire shape. */
export function encodeGetChatPinnedMessage(value: GetChatPinnedMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatPinnedMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatPostedToChatPageStories = Stories — Returns the list of stories that posted by the given chat to its chat page. If from_story_id == 0, then pinned stories are returned first. Then, stories are returned in reverse chronological ord... */
export class GetChatPostedToChatPageStories {
  type: 'getChatPostedToChatPageStories' = 'getChatPostedToChatPageStories';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** from_story_id: int32 — Identifier of the story starting from which stories must be returned; use 0 to get results from pinned and the newest story */
  from_story_id: number = 0;
  /** limit: int32 — The maximum number of stories to be returned. */
  limit: number = 0;
}

/** Decode a TDLib `getChatPostedToChatPageStories` object; null input -> null. */
export function decodeGetChatPostedToChatPageStories(json: TdJsonRaw): GetChatPostedToChatPageStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatPostedToChatPageStories = new GetChatPostedToChatPageStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.from_story_id = asNumber(record['from_story_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getChatPostedToChatPageStories` into TDLib JSON wire shape. */
export function encodeGetChatPostedToChatPageStories(value: GetChatPostedToChatPageStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatPostedToChatPageStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['from_story_id'] = value.from_story_id;
  out['limit'] = value.limit;
  return out;
}

/** getChatRevenueStatistics = ChatRevenueStatistics — Returns detailed revenue statistics about a chat. Currently, this method can be used only for channels if supergroupFullInfo.can_get_revenue_statistics == true or bots if userFullInfo.bo... */
export class GetChatRevenueStatistics {
  type: 'getChatRevenueStatistics' = 'getChatRevenueStatistics';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** is_dark: Bool — Pass true if a dark theme is used by the application */
  is_dark: boolean = false;
}

/** Decode a TDLib `getChatRevenueStatistics` object; null input -> null. */
export function decodeGetChatRevenueStatistics(json: TdJsonRaw): GetChatRevenueStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatRevenueStatistics = new GetChatRevenueStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.is_dark = asBool(record['is_dark']);
  return out;
}

/** Encode a `getChatRevenueStatistics` into TDLib JSON wire shape. */
export function encodeGetChatRevenueStatistics(value: GetChatRevenueStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatRevenueStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['is_dark'] = value.is_dark;
  return out;
}

/** getChatRevenueTransactions = ChatRevenueTransactions — Returns the list of revenue transactions for a chat. Currently, this method can be used only for channels if supergroupFullInfo.can_get_revenue_statistics == true or bots if userFull... */
export class GetChatRevenueTransactions {
  type: 'getChatRevenueTransactions' = 'getChatRevenueTransactions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** offset: string — Offset of the first transaction to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of transactions to be returned; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `getChatRevenueTransactions` object; null input -> null. */
export function decodeGetChatRevenueTransactions(json: TdJsonRaw): GetChatRevenueTransactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatRevenueTransactions = new GetChatRevenueTransactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getChatRevenueTransactions` into TDLib JSON wire shape. */
export function encodeGetChatRevenueTransactions(value: GetChatRevenueTransactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatRevenueTransactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getChatRevenueWithdrawalUrl = HttpUrl — Returns a URL for chat revenue withdrawal; requires owner privileges in the channel chat or the bot. Currently, this method can be used only if getOption("can_withdraw_chat_revenue") for channels w... */
export class GetChatRevenueWithdrawalUrl {
  type: 'getChatRevenueWithdrawalUrl' = 'getChatRevenueWithdrawalUrl';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
}

/** Decode a TDLib `getChatRevenueWithdrawalUrl` object; null input -> null. */
export function decodeGetChatRevenueWithdrawalUrl(json: TdJsonRaw): GetChatRevenueWithdrawalUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatRevenueWithdrawalUrl = new GetChatRevenueWithdrawalUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.password = asString(record['password']);
  return out;
}

/** Encode a `getChatRevenueWithdrawalUrl` into TDLib JSON wire shape. */
export function encodeGetChatRevenueWithdrawalUrl(value: GetChatRevenueWithdrawalUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatRevenueWithdrawalUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['password'] = value.password;
  return out;
}

/** getChatScheduledMessages = Messages — Returns all scheduled messages in a chat. The messages are returned in reverse chronological order (i.e., in order of decreasing message_id) */
export class GetChatScheduledMessages {
  type: 'getChatScheduledMessages' = 'getChatScheduledMessages';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatScheduledMessages` object; null input -> null. */
export function decodeGetChatScheduledMessages(json: TdJsonRaw): GetChatScheduledMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatScheduledMessages = new GetChatScheduledMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatScheduledMessages` into TDLib JSON wire shape. */
export function encodeGetChatScheduledMessages(value: GetChatScheduledMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatScheduledMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatSimilarChatCount = Count — Returns approximate number of chats similar to the given chat */
export class GetChatSimilarChatCount {
  type: 'getChatSimilarChatCount' = 'getChatSimilarChatCount';
  extra?: string;
  /** chat_id: int53 — Identifier of the target chat; must be an identifier of a channel chat */
  chat_id: number = 0;
  /** return_local: Bool — Pass true to get the number of chats without sending network requests, or -1 if the number of chats is unknown locally */
  return_local: boolean = false;
}

/** Decode a TDLib `getChatSimilarChatCount` object; null input -> null. */
export function decodeGetChatSimilarChatCount(json: TdJsonRaw): GetChatSimilarChatCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatSimilarChatCount = new GetChatSimilarChatCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.return_local = asBool(record['return_local']);
  return out;
}

/** Encode a `getChatSimilarChatCount` into TDLib JSON wire shape. */
export function encodeGetChatSimilarChatCount(value: GetChatSimilarChatCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatSimilarChatCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['return_local'] = value.return_local;
  return out;
}

/** getChatSimilarChats = Chats — Returns a list of chats similar to the given chat */
export class GetChatSimilarChats {
  type: 'getChatSimilarChats' = 'getChatSimilarChats';
  extra?: string;
  /** chat_id: int53 — Identifier of the target chat; must be an identifier of a channel chat */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatSimilarChats` object; null input -> null. */
export function decodeGetChatSimilarChats(json: TdJsonRaw): GetChatSimilarChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatSimilarChats = new GetChatSimilarChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatSimilarChats` into TDLib JSON wire shape. */
export function encodeGetChatSimilarChats(value: GetChatSimilarChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatSimilarChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatSparseMessagePositions = MessagePositions — Returns sparse positions of messages of the specified type in the chat to be used for Shared Media scroll implementation. Returns the results in reverse chronological order (i.e., in ord... */
export class GetChatSparseMessagePositions {
  type: 'getChatSparseMessagePositions' = 'getChatSparseMessagePositions';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in which to return information about message positions */
  chat_id: number = 0;
  /** filter: SearchMessagesFilter — Filter for message content. Filters searchMessagesFilterEmpty, searchMessagesFilterMention, searchMessagesFilterUnreadMention, searchMessagesFilterUnreadReaction, */
  filter: TdSearchMessagesFilter | null = null;
  /** from_message_id: int53 — The message identifier from which to return information about message positions */
  from_message_id: number = 0;
  /** limit: int32 — The expected number of message positions to be returned; 50-2000. A smaller number of positions can be returned, if there are not enough appropriate messages */
  limit: number = 0;
  /** saved_messages_topic_id: int53 — If not 0, only messages in the specified Saved Messages topic will be considered; pass 0 to consider all messages, or for chats other than Saved Messages */
  saved_messages_topic_id: number = 0;
}

/** Decode a TDLib `getChatSparseMessagePositions` object; null input -> null. */
export function decodeGetChatSparseMessagePositions(json: TdJsonRaw): GetChatSparseMessagePositions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatSparseMessagePositions = new GetChatSparseMessagePositions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.filter = decodeTdSearchMessagesFilter(record['filter']);
  out.from_message_id = asNumber(record['from_message_id']);
  out.limit = asNumber(record['limit']);
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  return out;
}

/** Encode a `getChatSparseMessagePositions` into TDLib JSON wire shape. */
export function encodeGetChatSparseMessagePositions(value: GetChatSparseMessagePositions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatSparseMessagePositions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['filter'] = value.filter === null ? null : encodeTdSearchMessagesFilter(value.filter);
  out['from_message_id'] = value.from_message_id;
  out['limit'] = value.limit;
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  return out;
}

/** getChatSponsoredMessages = SponsoredMessages — Returns sponsored messages to be shown in a chat; for channel chats and chats with bots only */
export class GetChatSponsoredMessages {
  type: 'getChatSponsoredMessages' = 'getChatSponsoredMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatSponsoredMessages` object; null input -> null. */
export function decodeGetChatSponsoredMessages(json: TdJsonRaw): GetChatSponsoredMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatSponsoredMessages = new GetChatSponsoredMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatSponsoredMessages` into TDLib JSON wire shape. */
export function encodeGetChatSponsoredMessages(value: GetChatSponsoredMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatSponsoredMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatStatistics = ChatStatistics — Returns detailed statistics about a chat. Currently, this method can be used only for supergroups and channels. Can be used only if supergroupFullInfo.can_get_statistics == true */
export class GetChatStatistics {
  type: 'getChatStatistics' = 'getChatStatistics';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** is_dark: Bool — Pass true if a dark theme is used by the application */
  is_dark: boolean = false;
}

/** Decode a TDLib `getChatStatistics` object; null input -> null. */
export function decodeGetChatStatistics(json: TdJsonRaw): GetChatStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatStatistics = new GetChatStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.is_dark = asBool(record['is_dark']);
  return out;
}

/** Encode a `getChatStatistics` into TDLib JSON wire shape. */
export function encodeGetChatStatistics(value: GetChatStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['is_dark'] = value.is_dark;
  return out;
}

/** getChatStoryAlbums = StoryAlbums — Returns the list of story albums owned by the given chat */
export class GetChatStoryAlbums {
  type: 'getChatStoryAlbums' = 'getChatStoryAlbums';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getChatStoryAlbums` object; null input -> null. */
export function decodeGetChatStoryAlbums(json: TdJsonRaw): GetChatStoryAlbums | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatStoryAlbums = new GetChatStoryAlbums();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getChatStoryAlbums` into TDLib JSON wire shape. */
export function encodeGetChatStoryAlbums(value: GetChatStoryAlbums): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatStoryAlbums';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getChatStoryInteractions = StoryInteractions — Returns interactions with a story posted in a chat. Can be used only if story is posted on behalf of a chat and the user is an administrator in the chat */
export class GetChatStoryInteractions {
  type: 'getChatStoryInteractions' = 'getChatStoryInteractions';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the poster of the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
  /** reaction_type: ReactionType — Pass the default heart reaction or a suggested reaction type to receive only interactions with the specified reaction type; pass null to receive all interactions; reactionTypePaid isn't supported */
  reaction_type: TdReactionType | null = null;
  /** prefer_forwards: Bool — Pass true to get forwards and reposts first, then reactions, then other views; pass false to get interactions sorted just by interaction date */
  prefer_forwards: boolean = false;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of story interactions to return */
  limit: number = 0;
}

/** Decode a TDLib `getChatStoryInteractions` object; null input -> null. */
export function decodeGetChatStoryInteractions(json: TdJsonRaw): GetChatStoryInteractions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatStoryInteractions = new GetChatStoryInteractions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.reaction_type = decodeTdReactionType(record['reaction_type']);
  out.prefer_forwards = asBool(record['prefer_forwards']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getChatStoryInteractions` into TDLib JSON wire shape. */
export function encodeGetChatStoryInteractions(value: GetChatStoryInteractions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatStoryInteractions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['reaction_type'] = value.reaction_type === null ? null : encodeTdReactionType(value.reaction_type);
  out['prefer_forwards'] = value.prefer_forwards;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getChats = Chats — Returns an ordered list of chats from the beginning of a chat list. For informational purposes only. Use loadChats and updates processing instead to maintain chat lists in a consistent state */
export class GetChats {
  type: 'getChats' = 'getChats';
  extra?: string;
  /** chat_list: ChatList — The chat list in which to return chats; pass null to get chats from the main chat list */
  chat_list: TdChatList | null = null;
  /** limit: int32 — The maximum number of chats to be returned */
  limit: number = 0;
}

/** Decode a TDLib `getChats` object; null input -> null. */
export function decodeGetChats(json: TdJsonRaw): GetChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChats = new GetChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_list = decodeTdChatList(record['chat_list']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getChats` into TDLib JSON wire shape. */
export function encodeGetChats(value: GetChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  out['limit'] = value.limit;
  return out;
}

/** getChatsForChatFolderInviteLink = Chats — Returns identifiers of chats from a chat folder, suitable for adding to a chat folder invite link */
export class GetChatsForChatFolderInviteLink {
  type: 'getChatsForChatFolderInviteLink' = 'getChatsForChatFolderInviteLink';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
}

/** Decode a TDLib `getChatsForChatFolderInviteLink` object; null input -> null. */
export function decodeGetChatsForChatFolderInviteLink(json: TdJsonRaw): GetChatsForChatFolderInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatsForChatFolderInviteLink = new GetChatsForChatFolderInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  return out;
}

/** Encode a `getChatsForChatFolderInviteLink` into TDLib JSON wire shape. */
export function encodeGetChatsForChatFolderInviteLink(value: GetChatsForChatFolderInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatsForChatFolderInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  return out;
}

/** getChatsToPostStories = Chats — Returns supergroup and channel chats in which the current user has the right to post stories. The chats must be rechecked with canPostStory before actually trying to post a story there */
export class GetChatsToPostStories {
  type: 'getChatsToPostStories' = 'getChatsToPostStories';
  extra?: string;
}

/** Decode a TDLib `getChatsToPostStories` object; null input -> null. */
export function decodeGetChatsToPostStories(json: TdJsonRaw): GetChatsToPostStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetChatsToPostStories = new GetChatsToPostStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getChatsToPostStories` into TDLib JSON wire shape. */
export function encodeGetChatsToPostStories(value: GetChatsToPostStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getChatsToPostStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getCloseFriends = Users — Returns all close friends of the current user */
export class GetCloseFriends {
  type: 'getCloseFriends' = 'getCloseFriends';
  extra?: string;
}

/** Decode a TDLib `getCloseFriends` object; null input -> null. */
export function decodeGetCloseFriends(json: TdJsonRaw): GetCloseFriends | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCloseFriends = new GetCloseFriends();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getCloseFriends` into TDLib JSON wire shape. */
export function encodeGetCloseFriends(value: GetCloseFriends): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCloseFriends';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getCollectibleItemInfo = CollectibleItemInfo — Returns information about a given collectible item that was purchased at https://fragment.com */
export class GetCollectibleItemInfo {
  type: 'getCollectibleItemInfo' = 'getCollectibleItemInfo';
  extra?: string;
  /** type: CollectibleItemType — Type of the collectible item. The item must be used by a user and must be visible to the current user */
  type_: TdCollectibleItemType | null = null;
}

/** Decode a TDLib `getCollectibleItemInfo` object; null input -> null. */
export function decodeGetCollectibleItemInfo(json: TdJsonRaw): GetCollectibleItemInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCollectibleItemInfo = new GetCollectibleItemInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdCollectibleItemType(record['type']);
  return out;
}

/** Encode a `getCollectibleItemInfo` into TDLib JSON wire shape. */
export function encodeGetCollectibleItemInfo(value: GetCollectibleItemInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCollectibleItemInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdCollectibleItemType(value.type_);
  return out;
}

/** getCommands = BotCommands — Returns the list of commands supported by the bot for the given user scope and language; for bots only */
export class GetCommands {
  type: 'getCommands' = 'getCommands';
  extra?: string;
  /** scope: BotCommandScope — The scope to which the commands are relevant; pass null to get commands in the default bot command scope */
  scope: TdBotCommandScope | null = null;
  /** language_code: string — A two-letter ISO 639-1 language code or an empty string */
  language_code: string = '';
}

/** Decode a TDLib `getCommands` object; null input -> null. */
export function decodeGetCommands(json: TdJsonRaw): GetCommands | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCommands = new GetCommands();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scope = decodeTdBotCommandScope(record['scope']);
  out.language_code = asString(record['language_code']);
  return out;
}

/** Encode a `getCommands` into TDLib JSON wire shape. */
export function encodeGetCommands(value: GetCommands): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCommands';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scope'] = value.scope === null ? null : encodeTdBotCommandScope(value.scope);
  out['language_code'] = value.language_code;
  return out;
}

/** getConnectedAffiliateProgram = ConnectedAffiliateProgram — Returns an affiliate program that was connected to the given affiliate by identifier of the bot that created the program */
export class GetConnectedAffiliateProgram {
  type: 'getConnectedAffiliateProgram' = 'getConnectedAffiliateProgram';
  extra?: string;
  /** affiliate: AffiliateType — The affiliate to which the affiliate program will be connected */
  affiliate: TdAffiliateType | null = null;
  /** bot_user_id: int53 — Identifier of the bot that created the program */
  bot_user_id: number = 0;
}

/** Decode a TDLib `getConnectedAffiliateProgram` object; null input -> null. */
export function decodeGetConnectedAffiliateProgram(json: TdJsonRaw): GetConnectedAffiliateProgram | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetConnectedAffiliateProgram = new GetConnectedAffiliateProgram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.affiliate = decodeTdAffiliateType(record['affiliate']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `getConnectedAffiliateProgram` into TDLib JSON wire shape. */
export function encodeGetConnectedAffiliateProgram(value: GetConnectedAffiliateProgram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getConnectedAffiliateProgram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['affiliate'] = value.affiliate === null ? null : encodeTdAffiliateType(value.affiliate);
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** getConnectedAffiliatePrograms = ConnectedAffiliatePrograms — Returns affiliate programs that were connected to the given affiliate */
export class GetConnectedAffiliatePrograms {
  type: 'getConnectedAffiliatePrograms' = 'getConnectedAffiliatePrograms';
  extra?: string;
  /** affiliate: AffiliateType — The affiliate to which the affiliate programs were connected */
  affiliate: TdAffiliateType | null = null;
  /** offset: string — Offset of the first affiliate program to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of affiliate programs to return */
  limit: number = 0;
}

/** Decode a TDLib `getConnectedAffiliatePrograms` object; null input -> null. */
export function decodeGetConnectedAffiliatePrograms(json: TdJsonRaw): GetConnectedAffiliatePrograms | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetConnectedAffiliatePrograms = new GetConnectedAffiliatePrograms();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.affiliate = decodeTdAffiliateType(record['affiliate']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getConnectedAffiliatePrograms` into TDLib JSON wire shape. */
export function encodeGetConnectedAffiliatePrograms(value: GetConnectedAffiliatePrograms): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getConnectedAffiliatePrograms';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['affiliate'] = value.affiliate === null ? null : encodeTdAffiliateType(value.affiliate);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getConnectedWebsites = ConnectedWebsites — Returns all website where the current user used Telegram to log in */
export class GetConnectedWebsites {
  type: 'getConnectedWebsites' = 'getConnectedWebsites';
  extra?: string;
}

/** Decode a TDLib `getConnectedWebsites` object; null input -> null. */
export function decodeGetConnectedWebsites(json: TdJsonRaw): GetConnectedWebsites | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetConnectedWebsites = new GetConnectedWebsites();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getConnectedWebsites` into TDLib JSON wire shape. */
export function encodeGetConnectedWebsites(value: GetConnectedWebsites): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getConnectedWebsites';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getContacts = Users — Returns all contacts of the user */
export class GetContacts {
  type: 'getContacts' = 'getContacts';
  extra?: string;
}

/** Decode a TDLib `getContacts` object; null input -> null. */
export function decodeGetContacts(json: TdJsonRaw): GetContacts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetContacts = new GetContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getContacts` into TDLib JSON wire shape. */
export function encodeGetContacts(value: GetContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getCountries = Countries — Returns information about existing countries. Can be called before authorization */
export class GetCountries {
  type: 'getCountries' = 'getCountries';
  extra?: string;
}

/** Decode a TDLib `getCountries` object; null input -> null. */
export function decodeGetCountries(json: TdJsonRaw): GetCountries | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCountries = new GetCountries();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getCountries` into TDLib JSON wire shape. */
export function encodeGetCountries(value: GetCountries): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCountries';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getCountry = CountryInfo — Returns information about an existing country. Can be called before authorization */
export class GetCountry {
  type: 'getCountry' = 'getCountry';
  extra?: string;
  /** country_code: string — A two-letter ISO 3166-1 alpha-2 country code */
  country_code: string = '';
}

/** Decode a TDLib `getCountry` object; null input -> null. */
export function decodeGetCountry(json: TdJsonRaw): GetCountry | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCountry = new GetCountry();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.country_code = asString(record['country_code']);
  return out;
}

/** Encode a `getCountry` into TDLib JSON wire shape. */
export function encodeGetCountry(value: GetCountry): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCountry';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['country_code'] = value.country_code;
  return out;
}

/** getCountryCode = Text — Uses the current IP address to find the current country. Returns two-letter ISO 3166-1 alpha-2 country code. Can be called before authorization */
export class GetCountryCode {
  type: 'getCountryCode' = 'getCountryCode';
  extra?: string;
}

/** Decode a TDLib `getCountryCode` object; null input -> null. */
export function decodeGetCountryCode(json: TdJsonRaw): GetCountryCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCountryCode = new GetCountryCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getCountryCode` into TDLib JSON wire shape. */
export function encodeGetCountryCode(value: GetCountryCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCountryCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getCountryFlagEmoji = Text — Returns an emoji for the flag of the given country. Returns an empty string on failure. Can be called synchronously */
export class GetCountryFlagEmoji {
  type: 'getCountryFlagEmoji' = 'getCountryFlagEmoji';
  extra?: string;
  /** country_code: string — A two-letter ISO 3166-1 alpha-2 country code as received from getCountries */
  country_code: string = '';
}

/** Decode a TDLib `getCountryFlagEmoji` object; null input -> null. */
export function decodeGetCountryFlagEmoji(json: TdJsonRaw): GetCountryFlagEmoji | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCountryFlagEmoji = new GetCountryFlagEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.country_code = asString(record['country_code']);
  return out;
}

/** Encode a `getCountryFlagEmoji` into TDLib JSON wire shape. */
export function encodeGetCountryFlagEmoji(value: GetCountryFlagEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCountryFlagEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['country_code'] = value.country_code;
  return out;
}

/** getCreatedPublicChats = Chats — Returns a list of public chats of the specified type, owned by the user */
export class GetCreatedPublicChats {
  type: 'getCreatedPublicChats' = 'getCreatedPublicChats';
  extra?: string;
  /** type: PublicChatType — Type of the public chats to return */
  type_: TdPublicChatType | null = null;
}

/** Decode a TDLib `getCreatedPublicChats` object; null input -> null. */
export function decodeGetCreatedPublicChats(json: TdJsonRaw): GetCreatedPublicChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCreatedPublicChats = new GetCreatedPublicChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPublicChatType(record['type']);
  return out;
}

/** Encode a `getCreatedPublicChats` into TDLib JSON wire shape. */
export function encodeGetCreatedPublicChats(value: GetCreatedPublicChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCreatedPublicChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPublicChatType(value.type_);
  return out;
}

/** getCurrentState = Updates — Returns all updates needed to restore current TDLib state, i.e. all actual updateAuthorizationState/updateUser/updateNewChat and others. This is especially useful if TDLib is run in a separate process. Can be ... */
export class GetCurrentState {
  type: 'getCurrentState' = 'getCurrentState';
  extra?: string;
}

/** Decode a TDLib `getCurrentState` object; null input -> null. */
export function decodeGetCurrentState(json: TdJsonRaw): GetCurrentState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCurrentState = new GetCurrentState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getCurrentState` into TDLib JSON wire shape. */
export function encodeGetCurrentState(value: GetCurrentState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCurrentState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getCurrentWeather = CurrentWeather — Returns the current weather in the given location */
export class GetCurrentWeather {
  type: 'getCurrentWeather' = 'getCurrentWeather';
  extra?: string;
  /** location: location — The location */
  location: Location | null = null;
}

/** Decode a TDLib `getCurrentWeather` object; null input -> null. */
export function decodeGetCurrentWeather(json: TdJsonRaw): GetCurrentWeather | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCurrentWeather = new GetCurrentWeather();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  return out;
}

/** Encode a `getCurrentWeather` into TDLib JSON wire shape. */
export function encodeGetCurrentWeather(value: GetCurrentWeather): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCurrentWeather';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  return out;
}

/** getCustomEmojiReactionAnimations = Stickers — Returns TGS stickers with generic animations for custom emoji reactions */
export class GetCustomEmojiReactionAnimations {
  type: 'getCustomEmojiReactionAnimations' = 'getCustomEmojiReactionAnimations';
  extra?: string;
}

/** Decode a TDLib `getCustomEmojiReactionAnimations` object; null input -> null. */
export function decodeGetCustomEmojiReactionAnimations(json: TdJsonRaw): GetCustomEmojiReactionAnimations | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCustomEmojiReactionAnimations = new GetCustomEmojiReactionAnimations();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getCustomEmojiReactionAnimations` into TDLib JSON wire shape. */
export function encodeGetCustomEmojiReactionAnimations(value: GetCustomEmojiReactionAnimations): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCustomEmojiReactionAnimations';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getCustomEmojiStickers = Stickers — Returns the list of custom emoji stickers by their identifiers. Stickers are returned in arbitrary order. Only found stickers are returned */
export class GetCustomEmojiStickers {
  type: 'getCustomEmojiStickers' = 'getCustomEmojiStickers';
  extra?: string;
  /** custom_emoji_ids: vector<int64> — Identifiers of custom emoji stickers. At most 200 custom emoji stickers can be received simultaneously */
  custom_emoji_ids: string[] = [];
}

/** Decode a TDLib `getCustomEmojiStickers` object; null input -> null. */
export function decodeGetCustomEmojiStickers(json: TdJsonRaw): GetCustomEmojiStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetCustomEmojiStickers = new GetCustomEmojiStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_emoji_ids = decodeVector(record['custom_emoji_ids'], (item: TdJsonRaw): string => asInt64(item));
  return out;
}

/** Encode a `getCustomEmojiStickers` into TDLib JSON wire shape. */
export function encodeGetCustomEmojiStickers(value: GetCustomEmojiStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getCustomEmojiStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_emoji_ids'] = encodeVector(value.custom_emoji_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** getDatabaseStatistics = DatabaseStatistics — Returns database statistics */
export class GetDatabaseStatistics {
  type: 'getDatabaseStatistics' = 'getDatabaseStatistics';
  extra?: string;
}

/** Decode a TDLib `getDatabaseStatistics` object; null input -> null. */
export function decodeGetDatabaseStatistics(json: TdJsonRaw): GetDatabaseStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDatabaseStatistics = new GetDatabaseStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getDatabaseStatistics` into TDLib JSON wire shape. */
export function encodeGetDatabaseStatistics(value: GetDatabaseStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDatabaseStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getDeepLinkInfo = DeepLinkInfo — Returns information about a tg:// deep link. Use "tg://need_update_for_some_feature" or "tg:some_unsupported_feature" for testing. Returns a 404 error for unknown links. Can be called before authorization */
export class GetDeepLinkInfo {
  type: 'getDeepLinkInfo' = 'getDeepLinkInfo';
  extra?: string;
  /** link: string — The link */
  link: string = '';
}

/** Decode a TDLib `getDeepLinkInfo` object; null input -> null. */
export function decodeGetDeepLinkInfo(json: TdJsonRaw): GetDeepLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDeepLinkInfo = new GetDeepLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  return out;
}

/** Encode a `getDeepLinkInfo` into TDLib JSON wire shape. */
export function encodeGetDeepLinkInfo(value: GetDeepLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDeepLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  return out;
}

/** getDefaultBackgroundCustomEmojiStickers = Stickers — Returns default list of custom emoji stickers for reply background */
export class GetDefaultBackgroundCustomEmojiStickers {
  type: 'getDefaultBackgroundCustomEmojiStickers' = 'getDefaultBackgroundCustomEmojiStickers';
  extra?: string;
}

/** Decode a TDLib `getDefaultBackgroundCustomEmojiStickers` object; null input -> null. */
export function decodeGetDefaultBackgroundCustomEmojiStickers(json: TdJsonRaw): GetDefaultBackgroundCustomEmojiStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDefaultBackgroundCustomEmojiStickers = new GetDefaultBackgroundCustomEmojiStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getDefaultBackgroundCustomEmojiStickers` into TDLib JSON wire shape. */
export function encodeGetDefaultBackgroundCustomEmojiStickers(value: GetDefaultBackgroundCustomEmojiStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDefaultBackgroundCustomEmojiStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getDefaultChatEmojiStatuses = EmojiStatusCustomEmojis — Returns default emoji statuses for chats */
export class GetDefaultChatEmojiStatuses {
  type: 'getDefaultChatEmojiStatuses' = 'getDefaultChatEmojiStatuses';
  extra?: string;
}

/** Decode a TDLib `getDefaultChatEmojiStatuses` object; null input -> null. */
export function decodeGetDefaultChatEmojiStatuses(json: TdJsonRaw): GetDefaultChatEmojiStatuses | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDefaultChatEmojiStatuses = new GetDefaultChatEmojiStatuses();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getDefaultChatEmojiStatuses` into TDLib JSON wire shape. */
export function encodeGetDefaultChatEmojiStatuses(value: GetDefaultChatEmojiStatuses): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDefaultChatEmojiStatuses';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getDefaultChatPhotoCustomEmojiStickers = Stickers — Returns default list of custom emoji stickers for placing on a chat photo */
export class GetDefaultChatPhotoCustomEmojiStickers {
  type: 'getDefaultChatPhotoCustomEmojiStickers' = 'getDefaultChatPhotoCustomEmojiStickers';
  extra?: string;
}

/** Decode a TDLib `getDefaultChatPhotoCustomEmojiStickers` object; null input -> null. */
export function decodeGetDefaultChatPhotoCustomEmojiStickers(json: TdJsonRaw): GetDefaultChatPhotoCustomEmojiStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDefaultChatPhotoCustomEmojiStickers = new GetDefaultChatPhotoCustomEmojiStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getDefaultChatPhotoCustomEmojiStickers` into TDLib JSON wire shape. */
export function encodeGetDefaultChatPhotoCustomEmojiStickers(value: GetDefaultChatPhotoCustomEmojiStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDefaultChatPhotoCustomEmojiStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getDefaultEmojiStatuses = EmojiStatusCustomEmojis — Returns default emoji statuses for self status */
export class GetDefaultEmojiStatuses {
  type: 'getDefaultEmojiStatuses' = 'getDefaultEmojiStatuses';
  extra?: string;
}

/** Decode a TDLib `getDefaultEmojiStatuses` object; null input -> null. */
export function decodeGetDefaultEmojiStatuses(json: TdJsonRaw): GetDefaultEmojiStatuses | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDefaultEmojiStatuses = new GetDefaultEmojiStatuses();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getDefaultEmojiStatuses` into TDLib JSON wire shape. */
export function encodeGetDefaultEmojiStatuses(value: GetDefaultEmojiStatuses): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDefaultEmojiStatuses';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getDefaultMessageAutoDeleteTime = MessageAutoDeleteTime — Returns default message auto-delete time setting for new chats */
export class GetDefaultMessageAutoDeleteTime {
  type: 'getDefaultMessageAutoDeleteTime' = 'getDefaultMessageAutoDeleteTime';
  extra?: string;
}

/** Decode a TDLib `getDefaultMessageAutoDeleteTime` object; null input -> null. */
export function decodeGetDefaultMessageAutoDeleteTime(json: TdJsonRaw): GetDefaultMessageAutoDeleteTime | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDefaultMessageAutoDeleteTime = new GetDefaultMessageAutoDeleteTime();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getDefaultMessageAutoDeleteTime` into TDLib JSON wire shape. */
export function encodeGetDefaultMessageAutoDeleteTime(value: GetDefaultMessageAutoDeleteTime): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDefaultMessageAutoDeleteTime';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getDefaultProfilePhotoCustomEmojiStickers = Stickers — Returns default list of custom emoji stickers for placing on a profile photo */
export class GetDefaultProfilePhotoCustomEmojiStickers {
  type: 'getDefaultProfilePhotoCustomEmojiStickers' = 'getDefaultProfilePhotoCustomEmojiStickers';
  extra?: string;
}

/** Decode a TDLib `getDefaultProfilePhotoCustomEmojiStickers` object; null input -> null. */
export function decodeGetDefaultProfilePhotoCustomEmojiStickers(json: TdJsonRaw): GetDefaultProfilePhotoCustomEmojiStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDefaultProfilePhotoCustomEmojiStickers = new GetDefaultProfilePhotoCustomEmojiStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getDefaultProfilePhotoCustomEmojiStickers` into TDLib JSON wire shape. */
export function encodeGetDefaultProfilePhotoCustomEmojiStickers(value: GetDefaultProfilePhotoCustomEmojiStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDefaultProfilePhotoCustomEmojiStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getDirectMessagesChatTopic = DirectMessagesChatTopic — Returns information about the topic in a channel direct messages chat administered by the current user */
export class GetDirectMessagesChatTopic {
  type: 'getDirectMessagesChatTopic' = 'getDirectMessagesChatTopic';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** topic_id: int53 — Identifier of the topic to get */
  topic_id: number = 0;
}

/** Decode a TDLib `getDirectMessagesChatTopic` object; null input -> null. */
export function decodeGetDirectMessagesChatTopic(json: TdJsonRaw): GetDirectMessagesChatTopic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDirectMessagesChatTopic = new GetDirectMessagesChatTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  return out;
}

/** Encode a `getDirectMessagesChatTopic` into TDLib JSON wire shape. */
export function encodeGetDirectMessagesChatTopic(value: GetDirectMessagesChatTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDirectMessagesChatTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  return out;
}

/** getDirectMessagesChatTopicHistory = Messages — Returns messages in the topic in a channel direct messages chat administered by the current user. The messages are returned in reverse chronological order (i.e., in order of decreasing messa... */
export class GetDirectMessagesChatTopicHistory {
  type: 'getDirectMessagesChatTopicHistory' = 'getDirectMessagesChatTopicHistory';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** topic_id: int53 — Identifier of the topic which messages will be fetched */
  topic_id: number = 0;
  /** from_message_id: int53 — Identifier of the message starting from which messages must be fetched; use 0 to get results from the last message */
  from_message_id: number = 0;
  /** offset: int32 — Specify 0 to get results from exactly the message from_message_id or a negative number from -99 to -1 to get additionally -offset newer messages */
  offset: number = 0;
  /** limit: int32 — The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, then the limit must be greater than or equal to -offset. */
  limit: number = 0;
}

/** Decode a TDLib `getDirectMessagesChatTopicHistory` object; null input -> null. */
export function decodeGetDirectMessagesChatTopicHistory(json: TdJsonRaw): GetDirectMessagesChatTopicHistory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDirectMessagesChatTopicHistory = new GetDirectMessagesChatTopicHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  out.from_message_id = asNumber(record['from_message_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getDirectMessagesChatTopicHistory` into TDLib JSON wire shape. */
export function encodeGetDirectMessagesChatTopicHistory(value: GetDirectMessagesChatTopicHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDirectMessagesChatTopicHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  out['from_message_id'] = value.from_message_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getDirectMessagesChatTopicMessageByDate = Message — Returns the last message sent in the topic in a channel direct messages chat administered by the current user no later than the specified date */
export class GetDirectMessagesChatTopicMessageByDate {
  type: 'getDirectMessagesChatTopicMessageByDate' = 'getDirectMessagesChatTopicMessageByDate';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** topic_id: int53 — Identifier of the topic which messages will be fetched */
  topic_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) relative to which to search for messages */
  date: number = 0;
}

/** Decode a TDLib `getDirectMessagesChatTopicMessageByDate` object; null input -> null. */
export function decodeGetDirectMessagesChatTopicMessageByDate(json: TdJsonRaw): GetDirectMessagesChatTopicMessageByDate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDirectMessagesChatTopicMessageByDate = new GetDirectMessagesChatTopicMessageByDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `getDirectMessagesChatTopicMessageByDate` into TDLib JSON wire shape. */
export function encodeGetDirectMessagesChatTopicMessageByDate(value: GetDirectMessagesChatTopicMessageByDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDirectMessagesChatTopicMessageByDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  out['date'] = value.date;
  return out;
}

/** getDirectMessagesChatTopicRevenue = StarCount — Returns the total number of Telegram Stars received by the channel chat for direct messages from the given topic */
export class GetDirectMessagesChatTopicRevenue {
  type: 'getDirectMessagesChatTopicRevenue' = 'getDirectMessagesChatTopicRevenue';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat administered by the current user */
  chat_id: number = 0;
  /** topic_id: int53 — Identifier of the topic */
  topic_id: number = 0;
}

/** Decode a TDLib `getDirectMessagesChatTopicRevenue` object; null input -> null. */
export function decodeGetDirectMessagesChatTopicRevenue(json: TdJsonRaw): GetDirectMessagesChatTopicRevenue | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDirectMessagesChatTopicRevenue = new GetDirectMessagesChatTopicRevenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  return out;
}

/** Encode a `getDirectMessagesChatTopicRevenue` into TDLib JSON wire shape. */
export function encodeGetDirectMessagesChatTopicRevenue(value: GetDirectMessagesChatTopicRevenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDirectMessagesChatTopicRevenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  return out;
}

/** getDisallowedChatEmojiStatuses = EmojiStatusCustomEmojis — Returns the list of emoji statuses, which can't be used as chat emoji status, even if they are from a sticker set with is_allowed_as_chat_emoji_status == true */
export class GetDisallowedChatEmojiStatuses {
  type: 'getDisallowedChatEmojiStatuses' = 'getDisallowedChatEmojiStatuses';
  extra?: string;
}

/** Decode a TDLib `getDisallowedChatEmojiStatuses` object; null input -> null. */
export function decodeGetDisallowedChatEmojiStatuses(json: TdJsonRaw): GetDisallowedChatEmojiStatuses | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetDisallowedChatEmojiStatuses = new GetDisallowedChatEmojiStatuses();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getDisallowedChatEmojiStatuses` into TDLib JSON wire shape. */
export function encodeGetDisallowedChatEmojiStatuses(value: GetDisallowedChatEmojiStatuses): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getDisallowedChatEmojiStatuses';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getEmojiCategories = EmojiCategories — Returns available emoji categories */
export class GetEmojiCategories {
  type: 'getEmojiCategories' = 'getEmojiCategories';
  extra?: string;
  /** type: EmojiCategoryType — Type of emoji categories to return; pass null to get default emoji categories */
  type_: TdEmojiCategoryType | null = null;
}

/** Decode a TDLib `getEmojiCategories` object; null input -> null. */
export function decodeGetEmojiCategories(json: TdJsonRaw): GetEmojiCategories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetEmojiCategories = new GetEmojiCategories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdEmojiCategoryType(record['type']);
  return out;
}

/** Encode a `getEmojiCategories` into TDLib JSON wire shape. */
export function encodeGetEmojiCategories(value: GetEmojiCategories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getEmojiCategories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdEmojiCategoryType(value.type_);
  return out;
}

/** getEmojiReaction = EmojiReaction — Returns information about an emoji reaction. Returns a 404 error if the reaction is not found */
export class GetEmojiReaction {
  type: 'getEmojiReaction' = 'getEmojiReaction';
  extra?: string;
  /** emoji: string — Text representation of the reaction */
  emoji: string = '';
}

/** Decode a TDLib `getEmojiReaction` object; null input -> null. */
export function decodeGetEmojiReaction(json: TdJsonRaw): GetEmojiReaction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetEmojiReaction = new GetEmojiReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji = asString(record['emoji']);
  return out;
}

/** Encode a `getEmojiReaction` into TDLib JSON wire shape. */
export function encodeGetEmojiReaction(value: GetEmojiReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getEmojiReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji'] = value.emoji;
  return out;
}

/** getEmojiSuggestionsUrl = HttpUrl — Returns an HTTP URL which can be used to automatically log in to the translation platform and suggest new emoji replacements. The URL will be valid for 30 seconds after generation */
export class GetEmojiSuggestionsUrl {
  type: 'getEmojiSuggestionsUrl' = 'getEmojiSuggestionsUrl';
  extra?: string;
  /** language_code: string — Language code for which the emoji replacements will be suggested */
  language_code: string = '';
}

/** Decode a TDLib `getEmojiSuggestionsUrl` object; null input -> null. */
export function decodeGetEmojiSuggestionsUrl(json: TdJsonRaw): GetEmojiSuggestionsUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetEmojiSuggestionsUrl = new GetEmojiSuggestionsUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_code = asString(record['language_code']);
  return out;
}

/** Encode a `getEmojiSuggestionsUrl` into TDLib JSON wire shape. */
export function encodeGetEmojiSuggestionsUrl(value: GetEmojiSuggestionsUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getEmojiSuggestionsUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_code'] = value.language_code;
  return out;
}

/** getExternalLink = HttpUrl — Returns an HTTP URL which can be used to automatically authorize the current user on a website after clicking an HTTP link. Use the method getExternalLinkInfo to find whether a prior user confirmation is neede... */
export class GetExternalLink {
  type: 'getExternalLink' = 'getExternalLink';
  extra?: string;
  /** link: string — The HTTP link */
  link: string = '';
  /** allow_write_access: Bool — Pass true if the current user allowed the bot that was returned in getExternalLinkInfo, to send them messages */
  allow_write_access: boolean = false;
}

/** Decode a TDLib `getExternalLink` object; null input -> null. */
export function decodeGetExternalLink(json: TdJsonRaw): GetExternalLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetExternalLink = new GetExternalLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  out.allow_write_access = asBool(record['allow_write_access']);
  return out;
}

/** Encode a `getExternalLink` into TDLib JSON wire shape. */
export function encodeGetExternalLink(value: GetExternalLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getExternalLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  out['allow_write_access'] = value.allow_write_access;
  return out;
}

/** getExternalLinkInfo = LoginUrlInfo — Returns information about an action to be done when the current user clicks an external link. Don't use this method for links from secret chats if link preview is disabled in secret chats, and use dir... */
export class GetExternalLinkInfo {
  type: 'getExternalLinkInfo' = 'getExternalLinkInfo';
  extra?: string;
  /** link: string — The link */
  link: string = '';
}

/** Decode a TDLib `getExternalLinkInfo` object; null input -> null. */
export function decodeGetExternalLinkInfo(json: TdJsonRaw): GetExternalLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetExternalLinkInfo = new GetExternalLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  return out;
}

/** Encode a `getExternalLinkInfo` into TDLib JSON wire shape. */
export function encodeGetExternalLinkInfo(value: GetExternalLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getExternalLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  return out;
}

/** getFavoriteStickers = Stickers — Returns favorite stickers */
export class GetFavoriteStickers {
  type: 'getFavoriteStickers' = 'getFavoriteStickers';
  extra?: string;
}

/** Decode a TDLib `getFavoriteStickers` object; null input -> null. */
export function decodeGetFavoriteStickers(json: TdJsonRaw): GetFavoriteStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetFavoriteStickers = new GetFavoriteStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getFavoriteStickers` into TDLib JSON wire shape. */
export function encodeGetFavoriteStickers(value: GetFavoriteStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getFavoriteStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getFile = File — Returns information about a file. This is an offline method */
export class GetFile {
  type: 'getFile' = 'getFile';
  extra?: string;
  /** file_id: int32 — Identifier of the file to get */
  file_id: number = 0;
}

/** Decode a TDLib `getFile` object; null input -> null. */
export function decodeGetFile(json: TdJsonRaw): GetFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetFile = new GetFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  return out;
}

/** Encode a `getFile` into TDLib JSON wire shape. */
export function encodeGetFile(value: GetFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  return out;
}

/** getFileDownloadedPrefixSize = FileDownloadedPrefixSize — Returns file downloaded prefix size from a given offset, in bytes */
export class GetFileDownloadedPrefixSize {
  type: 'getFileDownloadedPrefixSize' = 'getFileDownloadedPrefixSize';
  extra?: string;
  /** file_id: int32 — Identifier of the file */
  file_id: number = 0;
  /** offset: int53 — Offset from which downloaded prefix size needs to be calculated */
  offset: number = 0;
}

/** Decode a TDLib `getFileDownloadedPrefixSize` object; null input -> null. */
export function decodeGetFileDownloadedPrefixSize(json: TdJsonRaw): GetFileDownloadedPrefixSize | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetFileDownloadedPrefixSize = new GetFileDownloadedPrefixSize();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.offset = asNumber(record['offset']);
  return out;
}

/** Encode a `getFileDownloadedPrefixSize` into TDLib JSON wire shape. */
export function encodeGetFileDownloadedPrefixSize(value: GetFileDownloadedPrefixSize): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getFileDownloadedPrefixSize';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['offset'] = value.offset;
  return out;
}

/** getFileExtension = Text — Returns the extension of a file, guessed by its MIME type. Returns an empty string on failure. Can be called synchronously */
export class GetFileExtension {
  type: 'getFileExtension' = 'getFileExtension';
  extra?: string;
  /** mime_type: string — The MIME type of the file */
  mime_type: string = '';
}

/** Decode a TDLib `getFileExtension` object; null input -> null. */
export function decodeGetFileExtension(json: TdJsonRaw): GetFileExtension | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetFileExtension = new GetFileExtension();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.mime_type = asString(record['mime_type']);
  return out;
}

/** Encode a `getFileExtension` into TDLib JSON wire shape. */
export function encodeGetFileExtension(value: GetFileExtension): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getFileExtension';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['mime_type'] = value.mime_type;
  return out;
}

/** getFileMimeType = Text — Returns the MIME type of a file, guessed by its extension. Returns an empty string on failure. Can be called synchronously */
export class GetFileMimeType {
  type: 'getFileMimeType' = 'getFileMimeType';
  extra?: string;
  /** file_name: string — The name of the file or path to the file */
  file_name: string = '';
}

/** Decode a TDLib `getFileMimeType` object; null input -> null. */
export function decodeGetFileMimeType(json: TdJsonRaw): GetFileMimeType | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetFileMimeType = new GetFileMimeType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_name = asString(record['file_name']);
  return out;
}

/** Encode a `getFileMimeType` into TDLib JSON wire shape. */
export function encodeGetFileMimeType(value: GetFileMimeType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getFileMimeType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_name'] = value.file_name;
  return out;
}

/** getForumTopic = ForumTopic — Returns information about a topic in a forum supergroup chat or a chat with a bot with topics */
export class GetForumTopic {
  type: 'getForumTopic' = 'getForumTopic';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier */
  forum_topic_id: number = 0;
}

/** Decode a TDLib `getForumTopic` object; null input -> null. */
export function decodeGetForumTopic(json: TdJsonRaw): GetForumTopic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetForumTopic = new GetForumTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  return out;
}

/** Encode a `getForumTopic` into TDLib JSON wire shape. */
export function encodeGetForumTopic(value: GetForumTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getForumTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  return out;
}

/** getForumTopicDefaultIcons = Stickers — Returns the list of custom emoji, which can be used as forum topic icon by all users */
export class GetForumTopicDefaultIcons {
  type: 'getForumTopicDefaultIcons' = 'getForumTopicDefaultIcons';
  extra?: string;
}

/** Decode a TDLib `getForumTopicDefaultIcons` object; null input -> null. */
export function decodeGetForumTopicDefaultIcons(json: TdJsonRaw): GetForumTopicDefaultIcons | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetForumTopicDefaultIcons = new GetForumTopicDefaultIcons();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getForumTopicDefaultIcons` into TDLib JSON wire shape. */
export function encodeGetForumTopicDefaultIcons(value: GetForumTopicDefaultIcons): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getForumTopicDefaultIcons';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getForumTopicHistory = Messages — Returns messages in a topic in a forum supergroup chat or a chat with a bot with topics. The messages are returned in reverse chronological order (i.e., in order of decreasing message_id). For optimal pe... */
export class GetForumTopicHistory {
  type: 'getForumTopicHistory' = 'getForumTopicHistory';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier */
  forum_topic_id: number = 0;
  /** from_message_id: int53 — Identifier of the message starting from which history must be fetched; use 0 to get results from the last message */
  from_message_id: number = 0;
  /** offset: int32 — Specify 0 to get results from exactly the message from_message_id or a negative number from -99 to -1 to get additionally -offset newer messages */
  offset: number = 0;
  /** limit: int32 — The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, then the limit must be greater than or equal to -offset. */
  limit: number = 0;
}

/** Decode a TDLib `getForumTopicHistory` object; null input -> null. */
export function decodeGetForumTopicHistory(json: TdJsonRaw): GetForumTopicHistory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetForumTopicHistory = new GetForumTopicHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.from_message_id = asNumber(record['from_message_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getForumTopicHistory` into TDLib JSON wire shape. */
export function encodeGetForumTopicHistory(value: GetForumTopicHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getForumTopicHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['from_message_id'] = value.from_message_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getForumTopicLink = MessageLink — Returns an HTTPS link to a topic in a forum supergroup chat. This is an offline method */
export class GetForumTopicLink {
  type: 'getForumTopicLink' = 'getForumTopicLink';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier */
  forum_topic_id: number = 0;
}

/** Decode a TDLib `getForumTopicLink` object; null input -> null. */
export function decodeGetForumTopicLink(json: TdJsonRaw): GetForumTopicLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetForumTopicLink = new GetForumTopicLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  return out;
}

/** Encode a `getForumTopicLink` into TDLib JSON wire shape. */
export function encodeGetForumTopicLink(value: GetForumTopicLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getForumTopicLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  return out;
}

/** getForumTopics = ForumTopics — Returns found forum topics in a forum supergroup chat or a chat with a bot with topics. This is a temporary method for getting information about topic list from the server */
export class GetForumTopics {
  type: 'getForumTopics' = 'getForumTopics';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** query: string — Query to search for in the forum topic's name */
  query: string = '';
  /** offset_date: int32 — The date starting from which the results need to be fetched. Use 0 or any date in the future to get results from the last topic */
  offset_date: number = 0;
  /** offset_message_id: int53 — The message identifier of the last message in the last found topic, or 0 for the first request */
  offset_message_id: number = 0;
  /** offset_forum_topic_id: int32 — The forum topic identifier of the last found topic, or 0 for the first request */
  offset_forum_topic_id: number = 0;
  /** limit: int32 — The maximum number of forum topics to be returned; up to 100. For optimal performance, the number of returned forum topics is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `getForumTopics` object; null input -> null. */
export function decodeGetForumTopics(json: TdJsonRaw): GetForumTopics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetForumTopics = new GetForumTopics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.query = asString(record['query']);
  out.offset_date = asNumber(record['offset_date']);
  out.offset_message_id = asNumber(record['offset_message_id']);
  out.offset_forum_topic_id = asNumber(record['offset_forum_topic_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getForumTopics` into TDLib JSON wire shape. */
export function encodeGetForumTopics(value: GetForumTopics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getForumTopics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['query'] = value.query;
  out['offset_date'] = value.offset_date;
  out['offset_message_id'] = value.offset_message_id;
  out['offset_forum_topic_id'] = value.offset_forum_topic_id;
  out['limit'] = value.limit;
  return out;
}

/** getFullRichMessage = RichMessage — Returns the full version of a rich message */
export class GetFullRichMessage {
  type: 'getFullRichMessage' = 'getFullRichMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat the messages belong to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `getFullRichMessage` object; null input -> null. */
export function decodeGetFullRichMessage(json: TdJsonRaw): GetFullRichMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetFullRichMessage = new GetFullRichMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getFullRichMessage` into TDLib JSON wire shape. */
export function encodeGetFullRichMessage(value: GetFullRichMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getFullRichMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getGameHighScores = GameHighScores — Returns the high scores for a game and some part of the high score table in the range of the specified user; for bots only */
export class GetGameHighScores {
  type: 'getGameHighScores' = 'getGameHighScores';
  extra?: string;
  /** chat_id: int53 — The chat that contains the message with the game */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
}

/** Decode a TDLib `getGameHighScores` object; null input -> null. */
export function decodeGetGameHighScores(json: TdJsonRaw): GetGameHighScores | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGameHighScores = new GetGameHighScores();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `getGameHighScores` into TDLib JSON wire shape. */
export function encodeGetGameHighScores(value: GetGameHighScores): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGameHighScores';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['user_id'] = value.user_id;
  return out;
}

/** getGiftAuctionAcquiredGifts = GiftAuctionAcquiredGifts — Returns the gifts that were acquired by the current user on a gift auction */
export class GetGiftAuctionAcquiredGifts {
  type: 'getGiftAuctionAcquiredGifts' = 'getGiftAuctionAcquiredGifts';
  extra?: string;
  /** gift_id: int64 — Identifier of the auctioned gift */
  gift_id: string = '';
}

/** Decode a TDLib `getGiftAuctionAcquiredGifts` object; null input -> null. */
export function decodeGetGiftAuctionAcquiredGifts(json: TdJsonRaw): GetGiftAuctionAcquiredGifts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGiftAuctionAcquiredGifts = new GetGiftAuctionAcquiredGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_id = asInt64(record['gift_id']);
  return out;
}

/** Encode a `getGiftAuctionAcquiredGifts` into TDLib JSON wire shape. */
export function encodeGetGiftAuctionAcquiredGifts(value: GetGiftAuctionAcquiredGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGiftAuctionAcquiredGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_id'] = value.gift_id;
  return out;
}

/** getGiftAuctionState = GiftAuctionState — Returns auction state for a gift */
export class GetGiftAuctionState {
  type: 'getGiftAuctionState' = 'getGiftAuctionState';
  extra?: string;
  /** auction_id: string — Unique identifier of the auction */
  auction_id: string = '';
}

/** Decode a TDLib `getGiftAuctionState` object; null input -> null. */
export function decodeGetGiftAuctionState(json: TdJsonRaw): GetGiftAuctionState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGiftAuctionState = new GetGiftAuctionState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.auction_id = asString(record['auction_id']);
  return out;
}

/** Encode a `getGiftAuctionState` into TDLib JSON wire shape. */
export function encodeGetGiftAuctionState(value: GetGiftAuctionState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGiftAuctionState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['auction_id'] = value.auction_id;
  return out;
}

/** getGiftChatThemes = GiftChatThemes — Returns available to the current user gift chat themes */
export class GetGiftChatThemes {
  type: 'getGiftChatThemes' = 'getGiftChatThemes';
  extra?: string;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of chat themes to return */
  limit: number = 0;
}

/** Decode a TDLib `getGiftChatThemes` object; null input -> null. */
export function decodeGetGiftChatThemes(json: TdJsonRaw): GetGiftChatThemes | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGiftChatThemes = new GetGiftChatThemes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getGiftChatThemes` into TDLib JSON wire shape. */
export function encodeGetGiftChatThemes(value: GetGiftChatThemes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGiftChatThemes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getGiftCollections = GiftCollections — Returns collections of gifts owned by the given user or chat */
export class GetGiftCollections {
  type: 'getGiftCollections' = 'getGiftCollections';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that received the gifts */
  owner_id: TdMessageSender | null = null;
}

/** Decode a TDLib `getGiftCollections` object; null input -> null. */
export function decodeGetGiftCollections(json: TdJsonRaw): GetGiftCollections | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGiftCollections = new GetGiftCollections();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  return out;
}

/** Encode a `getGiftCollections` into TDLib JSON wire shape. */
export function encodeGetGiftCollections(value: GetGiftCollections): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGiftCollections';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  return out;
}

/** getGiftUpgradePreview = GiftUpgradePreview — Returns examples of possible upgraded gifts for a regular gift */
export class GetGiftUpgradePreview {
  type: 'getGiftUpgradePreview' = 'getGiftUpgradePreview';
  extra?: string;
  /** regular_gift_id: int64 — Identifier of the regular gift */
  regular_gift_id: string = '';
}

/** Decode a TDLib `getGiftUpgradePreview` object; null input -> null. */
export function decodeGetGiftUpgradePreview(json: TdJsonRaw): GetGiftUpgradePreview | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGiftUpgradePreview = new GetGiftUpgradePreview();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.regular_gift_id = asInt64(record['regular_gift_id']);
  return out;
}

/** Encode a `getGiftUpgradePreview` into TDLib JSON wire shape. */
export function encodeGetGiftUpgradePreview(value: GetGiftUpgradePreview): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGiftUpgradePreview';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['regular_gift_id'] = value.regular_gift_id;
  return out;
}

/** getGiftsForCrafting = GiftsForCrafting — Returns upgraded gifts of the current user who can be used to craft another gifts */
export class GetGiftsForCrafting {
  type: 'getGiftsForCrafting' = 'getGiftsForCrafting';
  extra?: string;
  /** regular_gift_id: int64 — Identifier of the regular gift that will be used for crafting */
  regular_gift_id: string = '';
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of gifts to be returned; must be positive and can't be greater than 100. For optimal performance, the number of returned objects is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `getGiftsForCrafting` object; null input -> null. */
export function decodeGetGiftsForCrafting(json: TdJsonRaw): GetGiftsForCrafting | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGiftsForCrafting = new GetGiftsForCrafting();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.regular_gift_id = asInt64(record['regular_gift_id']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getGiftsForCrafting` into TDLib JSON wire shape. */
export function encodeGetGiftsForCrafting(value: GetGiftsForCrafting): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGiftsForCrafting';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['regular_gift_id'] = value.regular_gift_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getGiveawayInfo = GiveawayInfo — Returns information about a giveaway */
export class GetGiveawayInfo {
  type: 'getGiveawayInfo' = 'getGiveawayInfo';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat which started the giveaway */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the giveaway or a giveaway winners message in the chat */
  message_id: number = 0;
}

/** Decode a TDLib `getGiveawayInfo` object; null input -> null. */
export function decodeGetGiveawayInfo(json: TdJsonRaw): GetGiveawayInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGiveawayInfo = new GetGiveawayInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getGiveawayInfo` into TDLib JSON wire shape. */
export function encodeGetGiveawayInfo(value: GetGiveawayInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGiveawayInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getGramRevenueStatistics = GramRevenueStatistics — Returns detailed TON Gram revenue statistics of the current user */
export class GetGramRevenueStatistics {
  type: 'getGramRevenueStatistics' = 'getGramRevenueStatistics';
  extra?: string;
  /** is_dark: Bool — Pass true if a dark theme is used by the application */
  is_dark: boolean = false;
}

/** Decode a TDLib `getGramRevenueStatistics` object; null input -> null. */
export function decodeGetGramRevenueStatistics(json: TdJsonRaw): GetGramRevenueStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGramRevenueStatistics = new GetGramRevenueStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_dark = asBool(record['is_dark']);
  return out;
}

/** Encode a `getGramRevenueStatistics` into TDLib JSON wire shape. */
export function encodeGetGramRevenueStatistics(value: GetGramRevenueStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGramRevenueStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_dark'] = value.is_dark;
  return out;
}

/** getGramWithdrawalUrl = HttpUrl — Returns a URL for TON Gram withdrawal from the current user's account. The user must have at least 10 Grams to withdraw and can withdraw up to 100000 Grams in one transaction */
export class GetGramWithdrawalUrl {
  type: 'getGramWithdrawalUrl' = 'getGramWithdrawalUrl';
  extra?: string;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
}

/** Decode a TDLib `getGramWithdrawalUrl` object; null input -> null. */
export function decodeGetGramWithdrawalUrl(json: TdJsonRaw): GetGramWithdrawalUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGramWithdrawalUrl = new GetGramWithdrawalUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.password = asString(record['password']);
  return out;
}

/** Encode a `getGramWithdrawalUrl` into TDLib JSON wire shape. */
export function encodeGetGramWithdrawalUrl(value: GetGramWithdrawalUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGramWithdrawalUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['password'] = value.password;
  return out;
}

/** getGreetingStickers = Stickers — Returns greeting stickers from regular sticker sets that can be used for the start page of other users */
export class GetGreetingStickers {
  type: 'getGreetingStickers' = 'getGreetingStickers';
  extra?: string;
}

/** Decode a TDLib `getGreetingStickers` object; null input -> null. */
export function decodeGetGreetingStickers(json: TdJsonRaw): GetGreetingStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGreetingStickers = new GetGreetingStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getGreetingStickers` into TDLib JSON wire shape. */
export function encodeGetGreetingStickers(value: GetGreetingStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGreetingStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getGrossingWebAppBots = FoundUsers — Returns the most grossing Web App bots */
export class GetGrossingWebAppBots {
  type: 'getGrossingWebAppBots' = 'getGrossingWebAppBots';
  extra?: string;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of bots to be returned; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `getGrossingWebAppBots` object; null input -> null. */
export function decodeGetGrossingWebAppBots(json: TdJsonRaw): GetGrossingWebAppBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGrossingWebAppBots = new GetGrossingWebAppBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getGrossingWebAppBots` into TDLib JSON wire shape. */
export function encodeGetGrossingWebAppBots(value: GetGrossingWebAppBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGrossingWebAppBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getGroupCall = GroupCall — Returns information about a group call */
export class GetGroupCall {
  type: 'getGroupCall' = 'getGroupCall';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `getGroupCall` object; null input -> null. */
export function decodeGetGroupCall(json: TdJsonRaw): GetGroupCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGroupCall = new GetGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `getGroupCall` into TDLib JSON wire shape. */
export function encodeGetGroupCall(value: GetGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** getGroupCallParticipants = GroupCallParticipants — Returns information about participants of a non-joined group call that is not bound to a chat */
export class GetGroupCallParticipants {
  type: 'getGroupCallParticipants' = 'getGroupCallParticipants';
  extra?: string;
  /** input_group_call: InputGroupCall — The group call which participants will be returned */
  input_group_call: TdInputGroupCall | null = null;
  /** limit: int32 — The maximum number of participants to return; must be positive */
  limit: number = 0;
}

/** Decode a TDLib `getGroupCallParticipants` object; null input -> null. */
export function decodeGetGroupCallParticipants(json: TdJsonRaw): GetGroupCallParticipants | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGroupCallParticipants = new GetGroupCallParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.input_group_call = decodeTdInputGroupCall(record['input_group_call']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getGroupCallParticipants` into TDLib JSON wire shape. */
export function encodeGetGroupCallParticipants(value: GetGroupCallParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGroupCallParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['input_group_call'] = value.input_group_call === null ? null : encodeTdInputGroupCall(value.input_group_call);
  out['limit'] = value.limit;
  return out;
}

/** getGroupCallStreamSegment = Data — Returns a file with a segment of a video chat or live story in a modified OGG format for audio or MPEG-4 format for video */
export class GetGroupCallStreamSegment {
  type: 'getGroupCallStreamSegment' = 'getGroupCallStreamSegment';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** time_offset: int53 — Point in time when the stream segment begins; Unix timestamp in milliseconds */
  time_offset: number = 0;
  /** scale: int32 — Segment duration scale; 0-1. Segment's duration is 1000/(2**scale) milliseconds */
  scale: number = 0;
  /** channel_id: int32 — Identifier of an audio/video channel to get as received from tgcalls */
  channel_id: number = 0;
  /** video_quality: GroupCallVideoQuality — Video quality as received from tgcalls; pass null to get the worst available quality */
  video_quality: TdGroupCallVideoQuality | null = null;
}

/** Decode a TDLib `getGroupCallStreamSegment` object; null input -> null. */
export function decodeGetGroupCallStreamSegment(json: TdJsonRaw): GetGroupCallStreamSegment | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGroupCallStreamSegment = new GetGroupCallStreamSegment();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.time_offset = asNumber(record['time_offset']);
  out.scale = asNumber(record['scale']);
  out.channel_id = asNumber(record['channel_id']);
  out.video_quality = decodeTdGroupCallVideoQuality(record['video_quality']);
  return out;
}

/** Encode a `getGroupCallStreamSegment` into TDLib JSON wire shape. */
export function encodeGetGroupCallStreamSegment(value: GetGroupCallStreamSegment): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGroupCallStreamSegment';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['time_offset'] = value.time_offset;
  out['scale'] = value.scale;
  out['channel_id'] = value.channel_id;
  out['video_quality'] = value.video_quality === null ? null : encodeTdGroupCallVideoQuality(value.video_quality);
  return out;
}

/** getGroupCallStreams = GroupCallStreams — Returns information about available streams in a video chat or a live story */
export class GetGroupCallStreams {
  type: 'getGroupCallStreams' = 'getGroupCallStreams';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `getGroupCallStreams` object; null input -> null. */
export function decodeGetGroupCallStreams(json: TdJsonRaw): GetGroupCallStreams | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGroupCallStreams = new GetGroupCallStreams();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `getGroupCallStreams` into TDLib JSON wire shape. */
export function encodeGetGroupCallStreams(value: GetGroupCallStreams): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGroupCallStreams';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** getGroupsInCommon = Chats — Returns a list of common group chats with a given user. Chats are sorted by their type and creation date */
export class GetGroupsInCommon {
  type: 'getGroupsInCommon' = 'getGroupsInCommon';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** offset_chat_id: int53 — Chat identifier starting from which to return chats; use 0 for the first request */
  offset_chat_id: number = 0;
  /** limit: int32 — The maximum number of chats to be returned; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `getGroupsInCommon` object; null input -> null. */
export function decodeGetGroupsInCommon(json: TdJsonRaw): GetGroupsInCommon | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGroupsInCommon = new GetGroupsInCommon();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.offset_chat_id = asNumber(record['offset_chat_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getGroupsInCommon` into TDLib JSON wire shape. */
export function encodeGetGroupsInCommon(value: GetGroupsInCommon): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGroupsInCommon';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['offset_chat_id'] = value.offset_chat_id;
  out['limit'] = value.limit;
  return out;
}

/** getGuardBotWebAppUrl = WebAppUrl — Returns an HTTPS URL of a Web App of a guard bot to open after receiving chatJoinResultGuardBotApprovalRequired */
export class GetGuardBotWebAppUrl {
  type: 'getGuardBotWebAppUrl' = 'getGuardBotWebAppUrl';
  extra?: string;
  /** query_id: int64 — Unique identifier of the join request as received in chatJoinResultGuardBotApprovalRequired */
  query_id: string = '';
  /** parameters: webAppOpenParameters — Parameters to use to open the Web App */
  parameters: WebAppOpenParameters | null = null;
}

/** Decode a TDLib `getGuardBotWebAppUrl` object; null input -> null. */
export function decodeGetGuardBotWebAppUrl(json: TdJsonRaw): GetGuardBotWebAppUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetGuardBotWebAppUrl = new GetGuardBotWebAppUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query_id = asInt64(record['query_id']);
  out.parameters = decodeWebAppOpenParameters(record['parameters']);
  return out;
}

/** Encode a `getGuardBotWebAppUrl` into TDLib JSON wire shape. */
export function encodeGetGuardBotWebAppUrl(value: GetGuardBotWebAppUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getGuardBotWebAppUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query_id'] = value.query_id;
  out['parameters'] = value.parameters === null ? null : encodeWebAppOpenParameters(value.parameters);
  return out;
}

/** getImportedContactCount = Count — Returns the total number of imported contacts */
export class GetImportedContactCount {
  type: 'getImportedContactCount' = 'getImportedContactCount';
  extra?: string;
}

/** Decode a TDLib `getImportedContactCount` object; null input -> null. */
export function decodeGetImportedContactCount(json: TdJsonRaw): GetImportedContactCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetImportedContactCount = new GetImportedContactCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getImportedContactCount` into TDLib JSON wire shape. */
export function encodeGetImportedContactCount(value: GetImportedContactCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getImportedContactCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getInactiveSupergroupChats = Chats — Returns a list of recently inactive supergroups and channels. Can be used when user reaches limit on the number of joined supergroups and channels and receives the error "CHANNELS_TOO_MUCH". Also, the... */
export class GetInactiveSupergroupChats {
  type: 'getInactiveSupergroupChats' = 'getInactiveSupergroupChats';
  extra?: string;
}

/** Decode a TDLib `getInactiveSupergroupChats` object; null input -> null. */
export function decodeGetInactiveSupergroupChats(json: TdJsonRaw): GetInactiveSupergroupChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetInactiveSupergroupChats = new GetInactiveSupergroupChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getInactiveSupergroupChats` into TDLib JSON wire shape. */
export function encodeGetInactiveSupergroupChats(value: GetInactiveSupergroupChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getInactiveSupergroupChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getInlineGameHighScores = GameHighScores — Returns game high scores and some part of the high score table in the range of the specified user; for bots only */
export class GetInlineGameHighScores {
  type: 'getInlineGameHighScores' = 'getInlineGameHighScores';
  extra?: string;
  /** inline_message_id: string — Inline message identifier */
  inline_message_id: string = '';
  /** user_id: int53 — User identifier */
  user_id: number = 0;
}

/** Decode a TDLib `getInlineGameHighScores` object; null input -> null. */
export function decodeGetInlineGameHighScores(json: TdJsonRaw): GetInlineGameHighScores | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetInlineGameHighScores = new GetInlineGameHighScores();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_message_id = asString(record['inline_message_id']);
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `getInlineGameHighScores` into TDLib JSON wire shape. */
export function encodeGetInlineGameHighScores(value: GetInlineGameHighScores): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getInlineGameHighScores';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_message_id'] = value.inline_message_id;
  out['user_id'] = value.user_id;
  return out;
}

/** getInlineQueryResults = InlineQueryResults — Sends an inline query to a bot and returns its results. Returns an error with code 502 if the bot fails to answer the query before the query timeout expires */
export class GetInlineQueryResults {
  type: 'getInlineQueryResults' = 'getInlineQueryResults';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** chat_id: int53 — Identifier of the chat where the query was sent */
  chat_id: number = 0;
  /** user_location: location — Location of the user; pass null if unknown or the bot doesn't need user's location */
  user_location: Location | null = null;
  /** query: string — Text of the query */
  query: string = '';
  /** offset: string — Offset of the first entry to return; use empty string to get the first chunk of results */
  offset: string = '';
}

/** Decode a TDLib `getInlineQueryResults` object; null input -> null. */
export function decodeGetInlineQueryResults(json: TdJsonRaw): GetInlineQueryResults | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetInlineQueryResults = new GetInlineQueryResults();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.user_location = decodeLocation(record['user_location']);
  out.query = asString(record['query']);
  out.offset = asString(record['offset']);
  return out;
}

/** Encode a `getInlineQueryResults` into TDLib JSON wire shape. */
export function encodeGetInlineQueryResults(value: GetInlineQueryResults): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getInlineQueryResults';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['chat_id'] = value.chat_id;
  out['user_location'] = value.user_location === null ? null : encodeLocation(value.user_location);
  out['query'] = value.query;
  out['offset'] = value.offset;
  return out;
}

/** getInstalledBackgrounds = Backgrounds — Returns backgrounds installed by the user */
export class GetInstalledBackgrounds {
  type: 'getInstalledBackgrounds' = 'getInstalledBackgrounds';
  extra?: string;
  /** for_dark_theme: Bool — Pass true to order returned backgrounds for a dark theme */
  for_dark_theme: boolean = false;
}

/** Decode a TDLib `getInstalledBackgrounds` object; null input -> null. */
export function decodeGetInstalledBackgrounds(json: TdJsonRaw): GetInstalledBackgrounds | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetInstalledBackgrounds = new GetInstalledBackgrounds();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.for_dark_theme = asBool(record['for_dark_theme']);
  return out;
}

/** Encode a `getInstalledBackgrounds` into TDLib JSON wire shape. */
export function encodeGetInstalledBackgrounds(value: GetInstalledBackgrounds): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getInstalledBackgrounds';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['for_dark_theme'] = value.for_dark_theme;
  return out;
}

/** getInstalledStickerSets = StickerSets — Returns a list of installed sticker sets */
export class GetInstalledStickerSets {
  type: 'getInstalledStickerSets' = 'getInstalledStickerSets';
  extra?: string;
  /** sticker_type: StickerType — Type of the sticker sets to return */
  sticker_type: TdStickerType | null = null;
}

/** Decode a TDLib `getInstalledStickerSets` object; null input -> null. */
export function decodeGetInstalledStickerSets(json: TdJsonRaw): GetInstalledStickerSets | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetInstalledStickerSets = new GetInstalledStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  return out;
}

/** Encode a `getInstalledStickerSets` into TDLib JSON wire shape. */
export function encodeGetInstalledStickerSets(value: GetInstalledStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getInstalledStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  return out;
}

/** getInternalLink = HttpUrl — Returns an HTTPS or a tg: link with the given type. Can be called before authorization */
export class GetInternalLink {
  type: 'getInternalLink' = 'getInternalLink';
  extra?: string;
  /** type: InternalLinkType — Expected type of the link */
  type_: TdInternalLinkType | null = null;
  /** is_http: Bool — Pass true to create an HTTPS link (only available for some link types); pass false to create a tg: link */
  is_http: boolean = false;
}

/** Decode a TDLib `getInternalLink` object; null input -> null. */
export function decodeGetInternalLink(json: TdJsonRaw): GetInternalLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetInternalLink = new GetInternalLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdInternalLinkType(record['type']);
  out.is_http = asBool(record['is_http']);
  return out;
}

/** Encode a `getInternalLink` into TDLib JSON wire shape. */
export function encodeGetInternalLink(value: GetInternalLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getInternalLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdInternalLinkType(value.type_);
  out['is_http'] = value.is_http;
  return out;
}

/** getInternalLinkType = InternalLinkType — Returns information about the type of internal link. Returns a 404 error if the link is not internal. Can be called before authorization */
export class GetInternalLinkType {
  type: 'getInternalLinkType' = 'getInternalLinkType';
  extra?: string;
  /** link: string — The link */
  link: string = '';
}

/** Decode a TDLib `getInternalLinkType` object; null input -> null. */
export function decodeGetInternalLinkType(json: TdJsonRaw): GetInternalLinkType | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetInternalLinkType = new GetInternalLinkType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  return out;
}

/** Encode a `getInternalLinkType` into TDLib JSON wire shape. */
export function encodeGetInternalLinkType(value: GetInternalLinkType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getInternalLinkType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  return out;
}

/** getJsonString = Text — Converts a JsonValue object to corresponding JSON-serialized string. Can be called synchronously */
export class GetJsonString {
  type: 'getJsonString' = 'getJsonString';
  extra?: string;
  /** json_value: JsonValue — The JsonValue object */
  json_value: TdJsonValue | null = null;
}

/** Decode a TDLib `getJsonString` object; null input -> null. */
export function decodeGetJsonString(json: TdJsonRaw): GetJsonString | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetJsonString = new GetJsonString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.json_value = decodeTdJsonValue(record['json_value']);
  return out;
}

/** Encode a `getJsonString` into TDLib JSON wire shape. */
export function encodeGetJsonString(value: GetJsonString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getJsonString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['json_value'] = value.json_value === null ? null : encodeTdJsonValue(value.json_value);
  return out;
}

/** getJsonValue = JsonValue — Converts a JSON-serialized string to corresponding JsonValue object. Can be called synchronously */
export class GetJsonValue {
  type: 'getJsonValue' = 'getJsonValue';
  extra?: string;
  /** json: string — The JSON-serialized string */
  json: string = '';
}

/** Decode a TDLib `getJsonValue` object; null input -> null. */
export function decodeGetJsonValue(json: TdJsonRaw): GetJsonValue | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetJsonValue = new GetJsonValue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.json = asString(record['json']);
  return out;
}

/** Encode a `getJsonValue` into TDLib JSON wire shape. */
export function encodeGetJsonValue(value: GetJsonValue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getJsonValue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['json'] = value.json;
  return out;
}

/** getKeywordEmojis = Emojis — Returns emojis matching the keyword. Supported only if the file database is enabled. Order of results is unspecified */
export class GetKeywordEmojis {
  type: 'getKeywordEmojis' = 'getKeywordEmojis';
  extra?: string;
  /** text: string — Text to search for */
  text: string = '';
  /** input_language_codes: vector<string> — List of possible IETF language tags of the user's input language; may be empty if unknown */
  input_language_codes: string[] = [];
}

/** Decode a TDLib `getKeywordEmojis` object; null input -> null. */
export function decodeGetKeywordEmojis(json: TdJsonRaw): GetKeywordEmojis | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetKeywordEmojis = new GetKeywordEmojis();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.input_language_codes = decodeVector(record['input_language_codes'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `getKeywordEmojis` into TDLib JSON wire shape. */
export function encodeGetKeywordEmojis(value: GetKeywordEmojis): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getKeywordEmojis';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['input_language_codes'] = encodeVector(value.input_language_codes, (item: string): TdJsonRaw => item);
  return out;
}

/** getLanguagePackInfo = LanguagePackInfo — Returns information about a language pack. Returned language pack identifier may be different from a provided one. Can be called before authorization */
export class GetLanguagePackInfo {
  type: 'getLanguagePackInfo' = 'getLanguagePackInfo';
  extra?: string;
  /** language_pack_id: string — Language pack identifier */
  language_pack_id: string = '';
}

/** Decode a TDLib `getLanguagePackInfo` object; null input -> null. */
export function decodeGetLanguagePackInfo(json: TdJsonRaw): GetLanguagePackInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLanguagePackInfo = new GetLanguagePackInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_pack_id = asString(record['language_pack_id']);
  return out;
}

/** Encode a `getLanguagePackInfo` into TDLib JSON wire shape. */
export function encodeGetLanguagePackInfo(value: GetLanguagePackInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLanguagePackInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_pack_id'] = value.language_pack_id;
  return out;
}

/** getLanguagePackString = LanguagePackStringValue — Returns a string stored in the local database from the specified localization target and language pack by its key. Returns a 404 error if the string is not found. Can be called synchronously */
export class GetLanguagePackString {
  type: 'getLanguagePackString' = 'getLanguagePackString';
  extra?: string;
  /** language_pack_database_path: string — Path to the language pack database in which strings are stored */
  language_pack_database_path: string = '';
  /** localization_target: string — Localization target to which the language pack belongs */
  localization_target: string = '';
  /** language_pack_id: string — Language pack identifier */
  language_pack_id: string = '';
  /** key: string — Language pack key of the string to be returned */
  key: string = '';
}

/** Decode a TDLib `getLanguagePackString` object; null input -> null. */
export function decodeGetLanguagePackString(json: TdJsonRaw): GetLanguagePackString | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLanguagePackString = new GetLanguagePackString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_pack_database_path = asString(record['language_pack_database_path']);
  out.localization_target = asString(record['localization_target']);
  out.language_pack_id = asString(record['language_pack_id']);
  out.key = asString(record['key']);
  return out;
}

/** Encode a `getLanguagePackString` into TDLib JSON wire shape. */
export function encodeGetLanguagePackString(value: GetLanguagePackString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLanguagePackString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_pack_database_path'] = value.language_pack_database_path;
  out['localization_target'] = value.localization_target;
  out['language_pack_id'] = value.language_pack_id;
  out['key'] = value.key;
  return out;
}

/** getLanguagePackStrings = LanguagePackStrings — Returns strings from a language pack in the current localization target by their keys. Can be called before authorization */
export class GetLanguagePackStrings {
  type: 'getLanguagePackStrings' = 'getLanguagePackStrings';
  extra?: string;
  /** language_pack_id: string — Language pack identifier of the strings to be returned */
  language_pack_id: string = '';
  /** keys: vector<string> — Language pack keys of the strings to be returned; leave empty to request all available strings */
  keys: string[] = [];
}

/** Decode a TDLib `getLanguagePackStrings` object; null input -> null. */
export function decodeGetLanguagePackStrings(json: TdJsonRaw): GetLanguagePackStrings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLanguagePackStrings = new GetLanguagePackStrings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_pack_id = asString(record['language_pack_id']);
  out.keys = decodeVector(record['keys'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `getLanguagePackStrings` into TDLib JSON wire shape. */
export function encodeGetLanguagePackStrings(value: GetLanguagePackStrings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLanguagePackStrings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_pack_id'] = value.language_pack_id;
  out['keys'] = encodeVector(value.keys, (item: string): TdJsonRaw => item);
  return out;
}

/** getLinkPreview = LinkPreview — Returns a link preview by the text of a message. Do not call this function too often. Returns a 404 error if the text has no link preview */
export class GetLinkPreview {
  type: 'getLinkPreview' = 'getLinkPreview';
  extra?: string;
  /** text: formattedText — Message text with formatting */
  text: FormattedText | null = null;
  /** link_preview_options: linkPreviewOptions — Options to be used for generation of the link preview; pass null to use default link preview options */
  link_preview_options: LinkPreviewOptions | null = null;
}

/** Decode a TDLib `getLinkPreview` object; null input -> null. */
export function decodeGetLinkPreview(json: TdJsonRaw): GetLinkPreview | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLinkPreview = new GetLinkPreview();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.link_preview_options = decodeLinkPreviewOptions(record['link_preview_options']);
  return out;
}

/** Encode a `getLinkPreview` into TDLib JSON wire shape. */
export function encodeGetLinkPreview(value: GetLinkPreview): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLinkPreview';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['link_preview_options'] = value.link_preview_options === null ? null : encodeLinkPreviewOptions(value.link_preview_options);
  return out;
}

/** getLinkWebBrowserType = WebBrowserType — Returns a type of the web browser which must be used to open the link */
export class GetLinkWebBrowserType {
  type: 'getLinkWebBrowserType' = 'getLinkWebBrowserType';
  extra?: string;
  /** link: string — The HTTP link */
  link: string = '';
}

/** Decode a TDLib `getLinkWebBrowserType` object; null input -> null. */
export function decodeGetLinkWebBrowserType(json: TdJsonRaw): GetLinkWebBrowserType | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLinkWebBrowserType = new GetLinkWebBrowserType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  return out;
}

/** Encode a `getLinkWebBrowserType` into TDLib JSON wire shape. */
export function encodeGetLinkWebBrowserType(value: GetLinkWebBrowserType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLinkWebBrowserType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  return out;
}

/** getLiveStoryAvailableMessageSenders = ChatMessageSenders — Returns the list of message sender identifiers, on whose behalf messages can be sent to a live story */
export class GetLiveStoryAvailableMessageSenders {
  type: 'getLiveStoryAvailableMessageSenders' = 'getLiveStoryAvailableMessageSenders';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `getLiveStoryAvailableMessageSenders` object; null input -> null. */
export function decodeGetLiveStoryAvailableMessageSenders(json: TdJsonRaw): GetLiveStoryAvailableMessageSenders | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLiveStoryAvailableMessageSenders = new GetLiveStoryAvailableMessageSenders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `getLiveStoryAvailableMessageSenders` into TDLib JSON wire shape. */
export function encodeGetLiveStoryAvailableMessageSenders(value: GetLiveStoryAvailableMessageSenders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLiveStoryAvailableMessageSenders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** getLiveStoryRtmpUrl = RtmpUrl — Returns RTMP URL for streaming to a live story; requires can_post_stories administrator right for channel chats */
export class GetLiveStoryRtmpUrl {
  type: 'getLiveStoryRtmpUrl' = 'getLiveStoryRtmpUrl';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getLiveStoryRtmpUrl` object; null input -> null. */
export function decodeGetLiveStoryRtmpUrl(json: TdJsonRaw): GetLiveStoryRtmpUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLiveStoryRtmpUrl = new GetLiveStoryRtmpUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getLiveStoryRtmpUrl` into TDLib JSON wire shape. */
export function encodeGetLiveStoryRtmpUrl(value: GetLiveStoryRtmpUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLiveStoryRtmpUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getLiveStoryStreamer = GroupCallParticipant — Returns information about the user or the chat that streams to a live story; for live stories that aren't an RTMP stream only */
export class GetLiveStoryStreamer {
  type: 'getLiveStoryStreamer' = 'getLiveStoryStreamer';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `getLiveStoryStreamer` object; null input -> null. */
export function decodeGetLiveStoryStreamer(json: TdJsonRaw): GetLiveStoryStreamer | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLiveStoryStreamer = new GetLiveStoryStreamer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `getLiveStoryStreamer` into TDLib JSON wire shape. */
export function encodeGetLiveStoryStreamer(value: GetLiveStoryStreamer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLiveStoryStreamer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** getLiveStoryTopDonors = LiveStoryDonors — Returns the list of top live story donors */
export class GetLiveStoryTopDonors {
  type: 'getLiveStoryTopDonors' = 'getLiveStoryTopDonors';
  extra?: string;
  /** group_call_id: int32 — Group call identifier of the live story */
  group_call_id: number = 0;
}

/** Decode a TDLib `getLiveStoryTopDonors` object; null input -> null. */
export function decodeGetLiveStoryTopDonors(json: TdJsonRaw): GetLiveStoryTopDonors | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLiveStoryTopDonors = new GetLiveStoryTopDonors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `getLiveStoryTopDonors` into TDLib JSON wire shape. */
export function encodeGetLiveStoryTopDonors(value: GetLiveStoryTopDonors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLiveStoryTopDonors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** getLocalizationTargetInfo = LocalizationTargetInfo — Returns information about the current localization target. This is an offline method if only_local is true. Can be called before authorization */
export class GetLocalizationTargetInfo {
  type: 'getLocalizationTargetInfo' = 'getLocalizationTargetInfo';
  extra?: string;
  /** only_local: Bool — Pass true to get only locally available information without sending network requests */
  only_local: boolean = false;
}

/** Decode a TDLib `getLocalizationTargetInfo` object; null input -> null. */
export function decodeGetLocalizationTargetInfo(json: TdJsonRaw): GetLocalizationTargetInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLocalizationTargetInfo = new GetLocalizationTargetInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.only_local = asBool(record['only_local']);
  return out;
}

/** Encode a `getLocalizationTargetInfo` into TDLib JSON wire shape. */
export function encodeGetLocalizationTargetInfo(value: GetLocalizationTargetInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLocalizationTargetInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['only_local'] = value.only_local;
  return out;
}

/** getLogStream = LogStream — Returns information about currently used log stream for internal logging of TDLib. Can be called synchronously */
export class GetLogStream {
  type: 'getLogStream' = 'getLogStream';
  extra?: string;
}

/** Decode a TDLib `getLogStream` object; null input -> null. */
export function decodeGetLogStream(json: TdJsonRaw): GetLogStream | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLogStream = new GetLogStream();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getLogStream` into TDLib JSON wire shape. */
export function encodeGetLogStream(value: GetLogStream): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLogStream';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getLogTagVerbosityLevel = LogVerbosityLevel — Returns current verbosity level for a specified TDLib internal log tag. Can be called synchronously */
export class GetLogTagVerbosityLevel {
  type: 'getLogTagVerbosityLevel' = 'getLogTagVerbosityLevel';
  extra?: string;
  /** tag: string — Logging tag to change verbosity level */
  tag: string = '';
}

/** Decode a TDLib `getLogTagVerbosityLevel` object; null input -> null. */
export function decodeGetLogTagVerbosityLevel(json: TdJsonRaw): GetLogTagVerbosityLevel | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLogTagVerbosityLevel = new GetLogTagVerbosityLevel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.tag = asString(record['tag']);
  return out;
}

/** Encode a `getLogTagVerbosityLevel` into TDLib JSON wire shape. */
export function encodeGetLogTagVerbosityLevel(value: GetLogTagVerbosityLevel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLogTagVerbosityLevel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['tag'] = value.tag;
  return out;
}

/** getLogTags = LogTags — Returns the list of available TDLib internal log tags, for example, ["actor", "binlog", "connections", "notifications", "proxy"]. Can be called synchronously */
export class GetLogTags {
  type: 'getLogTags' = 'getLogTags';
  extra?: string;
}

/** Decode a TDLib `getLogTags` object; null input -> null. */
export function decodeGetLogTags(json: TdJsonRaw): GetLogTags | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLogTags = new GetLogTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getLogTags` into TDLib JSON wire shape. */
export function encodeGetLogTags(value: GetLogTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLogTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getLogVerbosityLevel = LogVerbosityLevel — Returns current verbosity level of the internal logging of TDLib. Can be called synchronously */
export class GetLogVerbosityLevel {
  type: 'getLogVerbosityLevel' = 'getLogVerbosityLevel';
  extra?: string;
}

/** Decode a TDLib `getLogVerbosityLevel` object; null input -> null. */
export function decodeGetLogVerbosityLevel(json: TdJsonRaw): GetLogVerbosityLevel | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLogVerbosityLevel = new GetLogVerbosityLevel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getLogVerbosityLevel` into TDLib JSON wire shape. */
export function encodeGetLogVerbosityLevel(value: GetLogVerbosityLevel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLogVerbosityLevel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getLoginPasskeys = Passkeys — Returns the list of passkeys allowed to be used for the login by the current user */
export class GetLoginPasskeys {
  type: 'getLoginPasskeys' = 'getLoginPasskeys';
  extra?: string;
}

/** Decode a TDLib `getLoginPasskeys` object; null input -> null. */
export function decodeGetLoginPasskeys(json: TdJsonRaw): GetLoginPasskeys | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLoginPasskeys = new GetLoginPasskeys();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getLoginPasskeys` into TDLib JSON wire shape. */
export function encodeGetLoginPasskeys(value: GetLoginPasskeys): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLoginPasskeys';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getLoginUrl = HttpUrl — Returns an HTTP URL which can be used to automatically authorize the user on a website after clicking an inline button of type inlineKeyboardButtonTypeLoginUrl. Use the method getLoginUrlInfo to find whether a pri... */
export class GetLoginUrl {
  type: 'getLoginUrl' = 'getLoginUrl';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the message with the button */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier of the message with the button */
  message_id: number = 0;
  /** button_id: int53 — Button identifier */
  button_id: number = 0;
  /** allow_write_access: Bool — Pass true to allow the bot to send messages to the current user. Phone number access can't be requested using the button */
  allow_write_access: boolean = false;
}

/** Decode a TDLib `getLoginUrl` object; null input -> null. */
export function decodeGetLoginUrl(json: TdJsonRaw): GetLoginUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLoginUrl = new GetLoginUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.button_id = asNumber(record['button_id']);
  out.allow_write_access = asBool(record['allow_write_access']);
  return out;
}

/** Encode a `getLoginUrl` into TDLib JSON wire shape. */
export function encodeGetLoginUrl(value: GetLoginUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLoginUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['button_id'] = value.button_id;
  out['allow_write_access'] = value.allow_write_access;
  return out;
}

/** getLoginUrlInfo = LoginUrlInfo — Returns information about a button of type inlineKeyboardButtonTypeLoginUrl. The method needs to be called when the user presses the button */
export class GetLoginUrlInfo {
  type: 'getLoginUrlInfo' = 'getLoginUrlInfo';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the message with the button */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier of the message with the button. The message must not be scheduled */
  message_id: number = 0;
  /** button_id: int53 — Button identifier */
  button_id: number = 0;
}

/** Decode a TDLib `getLoginUrlInfo` object; null input -> null. */
export function decodeGetLoginUrlInfo(json: TdJsonRaw): GetLoginUrlInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetLoginUrlInfo = new GetLoginUrlInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.button_id = asNumber(record['button_id']);
  return out;
}

/** Encode a `getLoginUrlInfo` into TDLib JSON wire shape. */
export function encodeGetLoginUrlInfo(value: GetLoginUrlInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getLoginUrlInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['button_id'] = value.button_id;
  return out;
}

/** getMainWebApp = MainWebApp — Returns information needed to open the main Web App of a bot */
export class GetMainWebApp {
  type: 'getMainWebApp' = 'getMainWebApp';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in which the Web App is opened; pass 0 if none */
  chat_id: number = 0;
  /** bot_user_id: int53 — Identifier of the target bot. If the bot is restricted for the current user, then show an error instead of calling the method */
  bot_user_id: number = 0;
  /** start_parameter: string — Start parameter from internalLinkTypeMainWebApp */
  start_parameter: string = '';
  /** parameters: webAppOpenParameters — Parameters to use to open the Web App */
  parameters: WebAppOpenParameters | null = null;
}

/** Decode a TDLib `getMainWebApp` object; null input -> null. */
export function decodeGetMainWebApp(json: TdJsonRaw): GetMainWebApp | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMainWebApp = new GetMainWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.start_parameter = asString(record['start_parameter']);
  out.parameters = decodeWebAppOpenParameters(record['parameters']);
  return out;
}

/** Encode a `getMainWebApp` into TDLib JSON wire shape. */
export function encodeGetMainWebApp(value: GetMainWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMainWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['bot_user_id'] = value.bot_user_id;
  out['start_parameter'] = value.start_parameter;
  out['parameters'] = value.parameters === null ? null : encodeWebAppOpenParameters(value.parameters);
  return out;
}

/** getManagedBotAccessSettings = BotAccessSettings — Returns access settings of a managed bot; for bots only */
export class GetManagedBotAccessSettings {
  type: 'getManagedBotAccessSettings' = 'getManagedBotAccessSettings';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the managed bot */
  bot_user_id: number = 0;
}

/** Decode a TDLib `getManagedBotAccessSettings` object; null input -> null. */
export function decodeGetManagedBotAccessSettings(json: TdJsonRaw): GetManagedBotAccessSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetManagedBotAccessSettings = new GetManagedBotAccessSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `getManagedBotAccessSettings` into TDLib JSON wire shape. */
export function encodeGetManagedBotAccessSettings(value: GetManagedBotAccessSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getManagedBotAccessSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** getManagedBotToken = Text — Returns token of a managed bot; for bots only */
export class GetManagedBotToken {
  type: 'getManagedBotToken' = 'getManagedBotToken';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the managed bot */
  bot_user_id: number = 0;
  /** revoke: Bool — Pass true to revoke the current token and create a new one */
  revoke: boolean = false;
}

/** Decode a TDLib `getManagedBotToken` object; null input -> null. */
export function decodeGetManagedBotToken(json: TdJsonRaw): GetManagedBotToken | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetManagedBotToken = new GetManagedBotToken();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.revoke = asBool(record['revoke']);
  return out;
}

/** Encode a `getManagedBotToken` into TDLib JSON wire shape. */
export function encodeGetManagedBotToken(value: GetManagedBotToken): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getManagedBotToken';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['revoke'] = value.revoke;
  return out;
}

/** getMapThumbnailFile = File — Returns information about a file with a map thumbnail in PNG format. Only map thumbnail files with size less than 1MB can be downloaded */
export class GetMapThumbnailFile {
  type: 'getMapThumbnailFile' = 'getMapThumbnailFile';
  extra?: string;
  /** location: location — Location of the map center */
  location: Location | null = null;
  /** zoom: int32 — Map zoom level; 13-20 */
  zoom: number = 0;
  /** width: int32 — Map width in pixels before applying scale; 16-1024 */
  width: number = 0;
  /** height: int32 — Map height in pixels before applying scale; 16-1024 */
  height: number = 0;
  /** scale: int32 — Map scale; 1-3 */
  scale: number = 0;
  /** chat_id: int53 — Identifier of a chat in which the thumbnail will be shown. Use 0 if unknown */
  chat_id: number = 0;
}

/** Decode a TDLib `getMapThumbnailFile` object; null input -> null. */
export function decodeGetMapThumbnailFile(json: TdJsonRaw): GetMapThumbnailFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMapThumbnailFile = new GetMapThumbnailFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  out.zoom = asNumber(record['zoom']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.scale = asNumber(record['scale']);
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getMapThumbnailFile` into TDLib JSON wire shape. */
export function encodeGetMapThumbnailFile(value: GetMapThumbnailFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMapThumbnailFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['zoom'] = value.zoom;
  out['width'] = value.width;
  out['height'] = value.height;
  out['scale'] = value.scale;
  out['chat_id'] = value.chat_id;
  return out;
}

/** getMarkdownText = FormattedText — Replaces text entities with Markdown formatting in a human-friendly format. Entities that can't be represented in Markdown unambiguously are kept as is. Can be called synchronously */
export class GetMarkdownText {
  type: 'getMarkdownText' = 'getMarkdownText';
  extra?: string;
  /** text: formattedText — The text */
  text: FormattedText | null = null;
}

/** Decode a TDLib `getMarkdownText` object; null input -> null. */
export function decodeGetMarkdownText(json: TdJsonRaw): GetMarkdownText | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMarkdownText = new GetMarkdownText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `getMarkdownText` into TDLib JSON wire shape. */
export function encodeGetMarkdownText(value: GetMarkdownText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMarkdownText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** getMe = User — Returns the current user */
export class GetMe {
  type: 'getMe' = 'getMe';
  extra?: string;
}

/** Decode a TDLib `getMe` object; null input -> null. */
export function decodeGetMe(json: TdJsonRaw): GetMe | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMe = new GetMe();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getMe` into TDLib JSON wire shape. */
export function encodeGetMe(value: GetMe): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMe';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getMenuButton = BotMenuButton — Returns menu button set by the bot for the given user; for bots only */
export class GetMenuButton {
  type: 'getMenuButton' = 'getMenuButton';
  extra?: string;
  /** user_id: int53 — Identifier of the user or 0 to get the default menu button */
  user_id: number = 0;
}

/** Decode a TDLib `getMenuButton` object; null input -> null. */
export function decodeGetMenuButton(json: TdJsonRaw): GetMenuButton | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMenuButton = new GetMenuButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `getMenuButton` into TDLib JSON wire shape. */
export function encodeGetMenuButton(value: GetMenuButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMenuButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** getMessage = Message — Returns information about a message. Returns a 404 error if the message doesn't exist */
export class GetMessage {
  type: 'getMessage' = 'getMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message to get */
  message_id: number = 0;
}

/** Decode a TDLib `getMessage` object; null input -> null. */
export function decodeGetMessage(json: TdJsonRaw): GetMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessage = new GetMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getMessage` into TDLib JSON wire shape. */
export function encodeGetMessage(value: GetMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getMessageAddedReactions = AddedReactions — Returns reactions added for a message, along with their sender */
export class GetMessageAddedReactions {
  type: 'getMessageAddedReactions' = 'getMessageAddedReactions';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use message.interaction_info.reactions.can_get_added_reactions to check whether added reactions can be received for the message */
  message_id: number = 0;
  /** reaction_type: ReactionType — Type of the reactions to return; pass null to return all added reactions; reactionTypePaid isn't supported */
  reaction_type: TdReactionType | null = null;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of reactions to be returned; must be positive and can't be greater than 100 */
  limit: number = 0;
}

/** Decode a TDLib `getMessageAddedReactions` object; null input -> null. */
export function decodeGetMessageAddedReactions(json: TdJsonRaw): GetMessageAddedReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageAddedReactions = new GetMessageAddedReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reaction_type = decodeTdReactionType(record['reaction_type']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getMessageAddedReactions` into TDLib JSON wire shape. */
export function encodeGetMessageAddedReactions(value: GetMessageAddedReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageAddedReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reaction_type'] = value.reaction_type === null ? null : encodeTdReactionType(value.reaction_type);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getMessageAuthor = User — Returns information about actual author of a message sent on behalf of a channel. The method can be called if messageProperties.can_get_author == true */
export class GetMessageAuthor {
  type: 'getMessageAuthor' = 'getMessageAuthor';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `getMessageAuthor` object; null input -> null. */
export function decodeGetMessageAuthor(json: TdJsonRaw): GetMessageAuthor | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageAuthor = new GetMessageAuthor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getMessageAuthor` into TDLib JSON wire shape. */
export function encodeGetMessageAuthor(value: GetMessageAuthor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageAuthor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getMessageAvailableReactions = AvailableReactions — Returns reactions, which can be added to a message. The list can change after updateActiveEmojiReactions, updateChatAvailableReactions for the chat, or updateMessageInteractionInfo for ... */
export class GetMessageAvailableReactions {
  type: 'getMessageAvailableReactions' = 'getMessageAvailableReactions';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** row_size: int32 — Number of reaction per row, 5-25 */
  row_size: number = 0;
}

/** Decode a TDLib `getMessageAvailableReactions` object; null input -> null. */
export function decodeGetMessageAvailableReactions(json: TdJsonRaw): GetMessageAvailableReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageAvailableReactions = new GetMessageAvailableReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.row_size = asNumber(record['row_size']);
  return out;
}

/** Encode a `getMessageAvailableReactions` into TDLib JSON wire shape. */
export function encodeGetMessageAvailableReactions(value: GetMessageAvailableReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageAvailableReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['row_size'] = value.row_size;
  return out;
}

/** getMessageEffect = MessageEffect — Returns information about a message effect. Returns a 404 error if the effect is not found */
export class GetMessageEffect {
  type: 'getMessageEffect' = 'getMessageEffect';
  extra?: string;
  /** effect_id: int64 — Unique identifier of the effect */
  effect_id: string = '';
}

/** Decode a TDLib `getMessageEffect` object; null input -> null. */
export function decodeGetMessageEffect(json: TdJsonRaw): GetMessageEffect | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageEffect = new GetMessageEffect();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.effect_id = asInt64(record['effect_id']);
  return out;
}

/** Encode a `getMessageEffect` into TDLib JSON wire shape. */
export function encodeGetMessageEffect(value: GetMessageEffect): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageEffect';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['effect_id'] = value.effect_id;
  return out;
}

/** getMessageEmbeddingCode = Text — Returns an HTML code for embedding the message. Available only if messageProperties.can_get_embedding_code */
export class GetMessageEmbeddingCode {
  type: 'getMessageEmbeddingCode' = 'getMessageEmbeddingCode';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** for_album: Bool — Pass true to return an HTML code for embedding of the whole media album */
  for_album: boolean = false;
}

/** Decode a TDLib `getMessageEmbeddingCode` object; null input -> null. */
export function decodeGetMessageEmbeddingCode(json: TdJsonRaw): GetMessageEmbeddingCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageEmbeddingCode = new GetMessageEmbeddingCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.for_album = asBool(record['for_album']);
  return out;
}

/** Encode a `getMessageEmbeddingCode` into TDLib JSON wire shape. */
export function encodeGetMessageEmbeddingCode(value: GetMessageEmbeddingCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageEmbeddingCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['for_album'] = value.for_album;
  return out;
}

/** getMessageFileType = MessageFileType — Returns information about a file with messages exported from another application */
export class GetMessageFileType {
  type: 'getMessageFileType' = 'getMessageFileType';
  extra?: string;
  /** message_file_head: string — Beginning of the message file; up to 100 first lines */
  message_file_head: string = '';
}

/** Decode a TDLib `getMessageFileType` object; null input -> null. */
export function decodeGetMessageFileType(json: TdJsonRaw): GetMessageFileType | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageFileType = new GetMessageFileType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_file_head = asString(record['message_file_head']);
  return out;
}

/** Encode a `getMessageFileType` into TDLib JSON wire shape. */
export function encodeGetMessageFileType(value: GetMessageFileType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageFileType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_file_head'] = value.message_file_head;
  return out;
}

/** getMessageImportConfirmationText = Text — Returns a confirmation text to be shown to the user before starting message import */
export class GetMessageImportConfirmationText {
  type: 'getMessageImportConfirmationText' = 'getMessageImportConfirmationText';
  extra?: string;
  /** chat_id: int53 — Identifier of a chat to which the messages will be imported. It must be an identifier of a private chat with a mutual contact or an identifier of a supergroup chat with can_change_info member right */
  chat_id: number = 0;
}

/** Decode a TDLib `getMessageImportConfirmationText` object; null input -> null. */
export function decodeGetMessageImportConfirmationText(json: TdJsonRaw): GetMessageImportConfirmationText | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageImportConfirmationText = new GetMessageImportConfirmationText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getMessageImportConfirmationText` into TDLib JSON wire shape. */
export function encodeGetMessageImportConfirmationText(value: GetMessageImportConfirmationText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageImportConfirmationText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getMessageLink = MessageLink — Returns an HTTPS link to a message in a chat. Available only if messageProperties.can_get_link, or if messageProperties.can_get_media_timestamp_links and a media timestamp link is generated. This is an offl... */
export class GetMessageLink {
  type: 'getMessageLink' = 'getMessageLink';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** media_timestamp: int32 — If not 0, timestamp from which the video/audio/video note/voice note/story playing must start, in seconds. The media can be in the message content or in its link preview */
  media_timestamp: number = 0;
  /** checklist_task_id: int32 — If not 0, identifier of the checklist task in the message to be linked */
  checklist_task_id: number = 0;
  /** poll_option_id: string — If not empty, identifier of the poll option in the message to be linked */
  poll_option_id: string = '';
  /** for_album: Bool — Pass true to create a link for the whole media album */
  for_album: boolean = false;
  /** in_message_thread: Bool — Pass true to create a link to the message as a channel post comment, in a message thread, or a forum topic */
  in_message_thread: boolean = false;
}

/** Decode a TDLib `getMessageLink` object; null input -> null. */
export function decodeGetMessageLink(json: TdJsonRaw): GetMessageLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageLink = new GetMessageLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.media_timestamp = asNumber(record['media_timestamp']);
  out.checklist_task_id = asNumber(record['checklist_task_id']);
  out.poll_option_id = asString(record['poll_option_id']);
  out.for_album = asBool(record['for_album']);
  out.in_message_thread = asBool(record['in_message_thread']);
  return out;
}

/** Encode a `getMessageLink` into TDLib JSON wire shape. */
export function encodeGetMessageLink(value: GetMessageLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['media_timestamp'] = value.media_timestamp;
  out['checklist_task_id'] = value.checklist_task_id;
  out['poll_option_id'] = value.poll_option_id;
  out['for_album'] = value.for_album;
  out['in_message_thread'] = value.in_message_thread;
  return out;
}

/** getMessageLinkInfo = MessageLinkInfo — Returns information about a public or private message link. Can be called for any internal link of the type internalLinkTypeMessage */
export class GetMessageLinkInfo {
  type: 'getMessageLinkInfo' = 'getMessageLinkInfo';
  extra?: string;
  /** url: string — The message link */
  url: string = '';
}

/** Decode a TDLib `getMessageLinkInfo` object; null input -> null. */
export function decodeGetMessageLinkInfo(json: TdJsonRaw): GetMessageLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageLinkInfo = new GetMessageLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `getMessageLinkInfo` into TDLib JSON wire shape. */
export function encodeGetMessageLinkInfo(value: GetMessageLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** getMessageLocally = Message — Returns information about a message, if it is available without sending network request. Returns a 404 error if message isn't available locally. This is an offline method */
export class GetMessageLocally {
  type: 'getMessageLocally' = 'getMessageLocally';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message to get */
  message_id: number = 0;
}

/** Decode a TDLib `getMessageLocally` object; null input -> null. */
export function decodeGetMessageLocally(json: TdJsonRaw): GetMessageLocally | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageLocally = new GetMessageLocally();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getMessageLocally` into TDLib JSON wire shape. */
export function encodeGetMessageLocally(value: GetMessageLocally): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageLocally';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getMessageProperties = MessageProperties — Returns properties of a message. This is an offline method */
export class GetMessageProperties {
  type: 'getMessageProperties' = 'getMessageProperties';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `getMessageProperties` object; null input -> null. */
export function decodeGetMessageProperties(json: TdJsonRaw): GetMessageProperties | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageProperties = new GetMessageProperties();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getMessageProperties` into TDLib JSON wire shape. */
export function encodeGetMessageProperties(value: GetMessageProperties): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageProperties';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getMessagePublicForwards = PublicForwards — Returns forwarded copies of a channel message to different public channels and public reposts as a story. Can be used only if messageProperties.can_get_statistics == true. For optimal performan... */
export class GetMessagePublicForwards {
  type: 'getMessagePublicForwards' = 'getMessagePublicForwards';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the message */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of messages and stories to be returned; must be positive and can't be greater than 100. For optimal performance, the number of returned objects is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `getMessagePublicForwards` object; null input -> null. */
export function decodeGetMessagePublicForwards(json: TdJsonRaw): GetMessagePublicForwards | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessagePublicForwards = new GetMessagePublicForwards();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getMessagePublicForwards` into TDLib JSON wire shape. */
export function encodeGetMessagePublicForwards(value: GetMessagePublicForwards): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessagePublicForwards';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getMessageReadDate = MessageReadDate — Returns read date of a recent outgoing message in a private chat. The method can be called if messageProperties.can_get_read_date == true */
export class GetMessageReadDate {
  type: 'getMessageReadDate' = 'getMessageReadDate';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `getMessageReadDate` object; null input -> null. */
export function decodeGetMessageReadDate(json: TdJsonRaw): GetMessageReadDate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageReadDate = new GetMessageReadDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getMessageReadDate` into TDLib JSON wire shape. */
export function encodeGetMessageReadDate(value: GetMessageReadDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageReadDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getMessageStatistics = MessageStatistics — Returns detailed statistics about a message. Can be used only if messageProperties.can_get_statistics == true */
export class GetMessageStatistics {
  type: 'getMessageStatistics' = 'getMessageStatistics';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** is_dark: Bool — Pass true if a dark theme is used by the application */
  is_dark: boolean = false;
}

/** Decode a TDLib `getMessageStatistics` object; null input -> null. */
export function decodeGetMessageStatistics(json: TdJsonRaw): GetMessageStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageStatistics = new GetMessageStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.is_dark = asBool(record['is_dark']);
  return out;
}

/** Encode a `getMessageStatistics` into TDLib JSON wire shape. */
export function encodeGetMessageStatistics(value: GetMessageStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['is_dark'] = value.is_dark;
  return out;
}

/** getMessageThread = MessageThreadInfo — Returns information about a message thread. Can be used only if messageProperties.can_get_message_thread == true */
export class GetMessageThread {
  type: 'getMessageThread' = 'getMessageThread';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `getMessageThread` object; null input -> null. */
export function decodeGetMessageThread(json: TdJsonRaw): GetMessageThread | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageThread = new GetMessageThread();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getMessageThread` into TDLib JSON wire shape. */
export function encodeGetMessageThread(value: GetMessageThread): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageThread';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getMessageThreadHistory = Messages — Returns messages in a message thread of a message. Can be used only if messageProperties.can_get_message_thread == true. Message thread of a channel message is in the channel's linked supergroup. The ... */
export class GetMessageThreadHistory {
  type: 'getMessageThreadHistory' = 'getMessageThreadHistory';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier, which thread history needs to be returned */
  message_id: number = 0;
  /** from_message_id: int53 — Identifier of the message starting from which history must be fetched; use 0 to get results from the last message */
  from_message_id: number = 0;
  /** offset: int32 — Specify 0 to get results from exactly the message from_message_id or a negative number from -99 to -1 to get additionally -offset newer messages */
  offset: number = 0;
  /** limit: int32 — The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, then the limit must be greater than or equal to -offset. */
  limit: number = 0;
}

/** Decode a TDLib `getMessageThreadHistory` object; null input -> null. */
export function decodeGetMessageThreadHistory(json: TdJsonRaw): GetMessageThreadHistory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageThreadHistory = new GetMessageThreadHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.from_message_id = asNumber(record['from_message_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getMessageThreadHistory` into TDLib JSON wire shape. */
export function encodeGetMessageThreadHistory(value: GetMessageThreadHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageThreadHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['from_message_id'] = value.from_message_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getMessageViewers = MessageViewers — Returns viewers of a recent outgoing message in a basic group or a supergroup chat. For video notes and voice notes only users, opened content of the message, are returned. The method can be called if... */
export class GetMessageViewers {
  type: 'getMessageViewers' = 'getMessageViewers';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `getMessageViewers` object; null input -> null. */
export function decodeGetMessageViewers(json: TdJsonRaw): GetMessageViewers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessageViewers = new GetMessageViewers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getMessageViewers` into TDLib JSON wire shape. */
export function encodeGetMessageViewers(value: GetMessageViewers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessageViewers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getMessages = Messages — Returns information about messages. If a message is not found, returns null on the corresponding position of the result */
export class GetMessages {
  type: 'getMessages' = 'getMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat the messages belong to */
  chat_id: number = 0;
  /** message_ids: vector<int53> — Identifiers of the messages to get */
  message_ids: number[] = [];
}

/** Decode a TDLib `getMessages` object; null input -> null. */
export function decodeGetMessages(json: TdJsonRaw): GetMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetMessages = new GetMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `getMessages` into TDLib JSON wire shape. */
export function encodeGetMessages(value: GetMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** getNetworkStatistics = NetworkStatistics — Returns network data usage statistics. Can be called before authorization */
export class GetNetworkStatistics {
  type: 'getNetworkStatistics' = 'getNetworkStatistics';
  extra?: string;
  /** only_current: Bool — Pass true to get statistics only for the current library launch */
  only_current: boolean = false;
}

/** Decode a TDLib `getNetworkStatistics` object; null input -> null. */
export function decodeGetNetworkStatistics(json: TdJsonRaw): GetNetworkStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetNetworkStatistics = new GetNetworkStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.only_current = asBool(record['only_current']);
  return out;
}

/** Encode a `getNetworkStatistics` into TDLib JSON wire shape. */
export function encodeGetNetworkStatistics(value: GetNetworkStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getNetworkStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['only_current'] = value.only_current;
  return out;
}

/** getNewChatPrivacySettings = NewChatPrivacySettings — Returns privacy settings for new chat creation */
export class GetNewChatPrivacySettings {
  type: 'getNewChatPrivacySettings' = 'getNewChatPrivacySettings';
  extra?: string;
}

/** Decode a TDLib `getNewChatPrivacySettings` object; null input -> null. */
export function decodeGetNewChatPrivacySettings(json: TdJsonRaw): GetNewChatPrivacySettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetNewChatPrivacySettings = new GetNewChatPrivacySettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getNewChatPrivacySettings` into TDLib JSON wire shape. */
export function encodeGetNewChatPrivacySettings(value: GetNewChatPrivacySettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getNewChatPrivacySettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getOauthLinkInfo = OauthLinkInfo — Returns information about an OAuth deep link. Use checkOauthRequestMatchCode, acceptOauthRequest or declineOauthRequest to process the link */
export class GetOauthLinkInfo {
  type: 'getOauthLinkInfo' = 'getOauthLinkInfo';
  extra?: string;
  /** url: string — URL of the link */
  url: string = '';
  /** in_app_origin: string — Origin of the OAuth request if the request was received from the in-app browser; pass an empty string otherwise */
  in_app_origin: string = '';
}

/** Decode a TDLib `getOauthLinkInfo` object; null input -> null. */
export function decodeGetOauthLinkInfo(json: TdJsonRaw): GetOauthLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetOauthLinkInfo = new GetOauthLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.in_app_origin = asString(record['in_app_origin']);
  return out;
}

/** Encode a `getOauthLinkInfo` into TDLib JSON wire shape. */
export function encodeGetOauthLinkInfo(value: GetOauthLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getOauthLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['in_app_origin'] = value.in_app_origin;
  return out;
}

/** getOption = OptionValue — Returns the value of an option by its name. (Check the list of available options on https://core.telegram.org/tdlib/options.) Can be called before authorization. Can be called synchronously for options "version"... */
export class GetOption {
  type: 'getOption' = 'getOption';
  extra?: string;
  /** name: string — The name of the option */
  name: string = '';
}

/** Decode a TDLib `getOption` object; null input -> null. */
export function decodeGetOption(json: TdJsonRaw): GetOption | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetOption = new GetOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `getOption` into TDLib JSON wire shape. */
export function encodeGetOption(value: GetOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** getOwnedBots = Users — Returns the list of bots owned by the current user */
export class GetOwnedBots {
  type: 'getOwnedBots' = 'getOwnedBots';
  extra?: string;
}

/** Decode a TDLib `getOwnedBots` object; null input -> null. */
export function decodeGetOwnedBots(json: TdJsonRaw): GetOwnedBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetOwnedBots = new GetOwnedBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getOwnedBots` into TDLib JSON wire shape. */
export function encodeGetOwnedBots(value: GetOwnedBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getOwnedBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getOwnedStickerSets = StickerSets — Returns sticker sets owned by the current user */
export class GetOwnedStickerSets {
  type: 'getOwnedStickerSets' = 'getOwnedStickerSets';
  extra?: string;
  /** offset_sticker_set_id: int64 — Identifier of the sticker set from which to return owned sticker sets; use 0 to get results from the beginning */
  offset_sticker_set_id: string = '';
  /** limit: int32 — The maximum number of sticker sets to be returned; must be positive and can't be greater than 100. For optimal performance, the number of returned objects is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `getOwnedStickerSets` object; null input -> null. */
export function decodeGetOwnedStickerSets(json: TdJsonRaw): GetOwnedStickerSets | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetOwnedStickerSets = new GetOwnedStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.offset_sticker_set_id = asInt64(record['offset_sticker_set_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getOwnedStickerSets` into TDLib JSON wire shape. */
export function encodeGetOwnedStickerSets(value: GetOwnedStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getOwnedStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['offset_sticker_set_id'] = value.offset_sticker_set_id;
  out['limit'] = value.limit;
  return out;
}

/** getPaidMessageRevenue = StarCount — Returns the total number of Telegram Stars received by the current user for paid messages from the given user */
export class GetPaidMessageRevenue {
  type: 'getPaidMessageRevenue' = 'getPaidMessageRevenue';
  extra?: string;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
}

/** Decode a TDLib `getPaidMessageRevenue` object; null input -> null. */
export function decodeGetPaidMessageRevenue(json: TdJsonRaw): GetPaidMessageRevenue | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPaidMessageRevenue = new GetPaidMessageRevenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `getPaidMessageRevenue` into TDLib JSON wire shape. */
export function encodeGetPaidMessageRevenue(value: GetPaidMessageRevenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPaidMessageRevenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** getPasskeyParameters = Text — Returns parameters for creating of a new passkey as JSON-serialized string */
export class GetPasskeyParameters {
  type: 'getPasskeyParameters' = 'getPasskeyParameters';
  extra?: string;
}

/** Decode a TDLib `getPasskeyParameters` object; null input -> null. */
export function decodeGetPasskeyParameters(json: TdJsonRaw): GetPasskeyParameters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPasskeyParameters = new GetPasskeyParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getPasskeyParameters` into TDLib JSON wire shape. */
export function encodeGetPasskeyParameters(value: GetPasskeyParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPasskeyParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getPassportAuthorizationForm = PassportAuthorizationForm — Returns a Telegram Passport authorization form for sharing data with a service */
export class GetPassportAuthorizationForm {
  type: 'getPassportAuthorizationForm' = 'getPassportAuthorizationForm';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the service's bot */
  bot_user_id: number = 0;
  /** scope: string — Telegram Passport element types requested by the service */
  scope: string = '';
  /** public_key: string — Service's public key */
  public_key: string = '';
  /** nonce: string — Unique request identifier provided by the service */
  nonce: string = '';
}

/** Decode a TDLib `getPassportAuthorizationForm` object; null input -> null. */
export function decodeGetPassportAuthorizationForm(json: TdJsonRaw): GetPassportAuthorizationForm | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPassportAuthorizationForm = new GetPassportAuthorizationForm();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.scope = asString(record['scope']);
  out.public_key = asString(record['public_key']);
  out.nonce = asString(record['nonce']);
  return out;
}

/** Encode a `getPassportAuthorizationForm` into TDLib JSON wire shape. */
export function encodeGetPassportAuthorizationForm(value: GetPassportAuthorizationForm): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPassportAuthorizationForm';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['scope'] = value.scope;
  out['public_key'] = value.public_key;
  out['nonce'] = value.nonce;
  return out;
}

/** getPassportAuthorizationFormAvailableElements = PassportElementsWithErrors — Returns already available Telegram Passport elements suitable for completing a Telegram Passport authorization form. Result can be received only once for each a... */
export class GetPassportAuthorizationFormAvailableElements {
  type: 'getPassportAuthorizationFormAvailableElements' = 'getPassportAuthorizationFormAvailableElements';
  extra?: string;
  /** authorization_form_id: int32 — Authorization form identifier */
  authorization_form_id: number = 0;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
}

/** Decode a TDLib `getPassportAuthorizationFormAvailableElements` object; null input -> null. */
export function decodeGetPassportAuthorizationFormAvailableElements(json: TdJsonRaw): GetPassportAuthorizationFormAvailableElements | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPassportAuthorizationFormAvailableElements = new GetPassportAuthorizationFormAvailableElements();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.authorization_form_id = asNumber(record['authorization_form_id']);
  out.password = asString(record['password']);
  return out;
}

/** Encode a `getPassportAuthorizationFormAvailableElements` into TDLib JSON wire shape. */
export function encodeGetPassportAuthorizationFormAvailableElements(value: GetPassportAuthorizationFormAvailableElements): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPassportAuthorizationFormAvailableElements';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['authorization_form_id'] = value.authorization_form_id;
  out['password'] = value.password;
  return out;
}

/** getPassportElement = PassportElement — Returns one of the available Telegram Passport elements */
export class GetPassportElement {
  type: 'getPassportElement' = 'getPassportElement';
  extra?: string;
  /** type: PassportElementType — Telegram Passport element type */
  type_: TdPassportElementType | null = null;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
}

/** Decode a TDLib `getPassportElement` object; null input -> null. */
export function decodeGetPassportElement(json: TdJsonRaw): GetPassportElement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPassportElement = new GetPassportElement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPassportElementType(record['type']);
  out.password = asString(record['password']);
  return out;
}

/** Encode a `getPassportElement` into TDLib JSON wire shape. */
export function encodeGetPassportElement(value: GetPassportElement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPassportElement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPassportElementType(value.type_);
  out['password'] = value.password;
  return out;
}

/** getPasswordState = PasswordState — Returns the current state of 2-step verification */
export class GetPasswordState {
  type: 'getPasswordState' = 'getPasswordState';
  extra?: string;
}

/** Decode a TDLib `getPasswordState` object; null input -> null. */
export function decodeGetPasswordState(json: TdJsonRaw): GetPasswordState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPasswordState = new GetPasswordState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getPasswordState` into TDLib JSON wire shape. */
export function encodeGetPasswordState(value: GetPasswordState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPasswordState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getPaymentForm = PaymentForm — Returns an invoice payment form. This method must be called when the user presses inline button of the type inlineKeyboardButtonTypeBuy, or wants to buy access to media in a messagePaidMedia message */
export class GetPaymentForm {
  type: 'getPaymentForm' = 'getPaymentForm';
  extra?: string;
  /** input_invoice: InputInvoice — The invoice */
  input_invoice: TdInputInvoice | null = null;
  /** theme: themeParameters — Preferred payment form theme; pass null to use the default theme */
  theme: ThemeParameters | null = null;
}

/** Decode a TDLib `getPaymentForm` object; null input -> null. */
export function decodeGetPaymentForm(json: TdJsonRaw): GetPaymentForm | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPaymentForm = new GetPaymentForm();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.input_invoice = decodeTdInputInvoice(record['input_invoice']);
  out.theme = decodeThemeParameters(record['theme']);
  return out;
}

/** Encode a `getPaymentForm` into TDLib JSON wire shape. */
export function encodeGetPaymentForm(value: GetPaymentForm): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPaymentForm';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['input_invoice'] = value.input_invoice === null ? null : encodeTdInputInvoice(value.input_invoice);
  out['theme'] = value.theme === null ? null : encodeThemeParameters(value.theme);
  return out;
}

/** getPaymentReceipt = PaymentReceipt — Returns information about a successful payment */
export class GetPaymentReceipt {
  type: 'getPaymentReceipt' = 'getPaymentReceipt';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the messagePaymentSuccessful message */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
}

/** Decode a TDLib `getPaymentReceipt` object; null input -> null. */
export function decodeGetPaymentReceipt(json: TdJsonRaw): GetPaymentReceipt | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPaymentReceipt = new GetPaymentReceipt();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getPaymentReceipt` into TDLib JSON wire shape. */
export function encodeGetPaymentReceipt(value: GetPaymentReceipt): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPaymentReceipt';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getPersonalChatHistory = Messages — Returns messages in the personal chat of a given user; for bots only */
export class GetPersonalChatHistory {
  type: 'getPersonalChatHistory' = 'getPersonalChatHistory';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** limit: int32 — The maximum number of messages to be returned; 1-20 */
  limit: number = 0;
}

/** Decode a TDLib `getPersonalChatHistory` object; null input -> null. */
export function decodeGetPersonalChatHistory(json: TdJsonRaw): GetPersonalChatHistory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPersonalChatHistory = new GetPersonalChatHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getPersonalChatHistory` into TDLib JSON wire shape. */
export function encodeGetPersonalChatHistory(value: GetPersonalChatHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPersonalChatHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['limit'] = value.limit;
  return out;
}

/** getPhoneNumberInfo = PhoneNumberInfo — Returns information about a phone number by its prefix. Can be called before authorization */
export class GetPhoneNumberInfo {
  type: 'getPhoneNumberInfo' = 'getPhoneNumberInfo';
  extra?: string;
  /** phone_number_prefix: string — The phone number prefix */
  phone_number_prefix: string = '';
}

/** Decode a TDLib `getPhoneNumberInfo` object; null input -> null. */
export function decodeGetPhoneNumberInfo(json: TdJsonRaw): GetPhoneNumberInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPhoneNumberInfo = new GetPhoneNumberInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number_prefix = asString(record['phone_number_prefix']);
  return out;
}

/** Encode a `getPhoneNumberInfo` into TDLib JSON wire shape. */
export function encodeGetPhoneNumberInfo(value: GetPhoneNumberInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPhoneNumberInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number_prefix'] = value.phone_number_prefix;
  return out;
}

/** getPhoneNumberInfoSync = PhoneNumberInfo — Returns information about a phone number by its prefix synchronously. getCountries must be called at least once after changing localization to the specified language if properly localized countr... */
export class GetPhoneNumberInfoSync {
  type: 'getPhoneNumberInfoSync' = 'getPhoneNumberInfoSync';
  extra?: string;
  /** language_code: string — A two-letter ISO 639-1 language code for country information localization */
  language_code: string = '';
  /** phone_number_prefix: string — The phone number prefix */
  phone_number_prefix: string = '';
}

/** Decode a TDLib `getPhoneNumberInfoSync` object; null input -> null. */
export function decodeGetPhoneNumberInfoSync(json: TdJsonRaw): GetPhoneNumberInfoSync | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPhoneNumberInfoSync = new GetPhoneNumberInfoSync();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_code = asString(record['language_code']);
  out.phone_number_prefix = asString(record['phone_number_prefix']);
  return out;
}

/** Encode a `getPhoneNumberInfoSync` into TDLib JSON wire shape. */
export function encodeGetPhoneNumberInfoSync(value: GetPhoneNumberInfoSync): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPhoneNumberInfoSync';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_code'] = value.language_code;
  out['phone_number_prefix'] = value.phone_number_prefix;
  return out;
}

/** getPollOptionProperties = PollOptionProperties — Returns properties of a poll option. This is an offline method */
export class GetPollOptionProperties {
  type: 'getPollOptionProperties' = 'getPollOptionProperties';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** poll_option_id: string — Unique identifier of the answer option, which properties will be returned */
  poll_option_id: string = '';
}

/** Decode a TDLib `getPollOptionProperties` object; null input -> null. */
export function decodeGetPollOptionProperties(json: TdJsonRaw): GetPollOptionProperties | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPollOptionProperties = new GetPollOptionProperties();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.poll_option_id = asString(record['poll_option_id']);
  return out;
}

/** Encode a `getPollOptionProperties` into TDLib JSON wire shape. */
export function encodeGetPollOptionProperties(value: GetPollOptionProperties): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPollOptionProperties';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['poll_option_id'] = value.poll_option_id;
  return out;
}

/** getPollVoteStatistics = PollVoteStatistics — Returns statistics of poll votes in a poll */
export class GetPollVoteStatistics {
  type: 'getPollVoteStatistics' = 'getPollVoteStatistics';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the poll belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the poll. Use messageProperties.can_get_poll_vote_statistics to check whether the method can be used for a message */
  message_id: number = 0;
  /** is_dark: Bool — Pass true if a dark theme is used by the application */
  is_dark: boolean = false;
}

/** Decode a TDLib `getPollVoteStatistics` object; null input -> null. */
export function decodeGetPollVoteStatistics(json: TdJsonRaw): GetPollVoteStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPollVoteStatistics = new GetPollVoteStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.is_dark = asBool(record['is_dark']);
  return out;
}

/** Encode a `getPollVoteStatistics` into TDLib JSON wire shape. */
export function encodeGetPollVoteStatistics(value: GetPollVoteStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPollVoteStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['is_dark'] = value.is_dark;
  return out;
}

/** getPollVoters = PollVoters — Returns message senders voted for the specified option in a poll; use poll.can_get_voters to check whether the method can be used. For optimal performance, the number of returned users is chosen by TDLib */
export class GetPollVoters {
  type: 'getPollVoters' = 'getPollVoters';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the poll belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the poll */
  message_id: number = 0;
  /** option_id: int32 — 0-based identifier of the answer option */
  option_id: number = 0;
  /** offset: int32 — Number of voters to skip in the result; must be non-negative */
  offset: number = 0;
  /** limit: int32 — The maximum number of voters to be returned; must be positive and can't be greater than 50. For optimal performance, the number of returned voters is chosen by TDLib and can be smaller than the specified limit, even if the... */
  limit: number = 0;
}

/** Decode a TDLib `getPollVoters` object; null input -> null. */
export function decodeGetPollVoters(json: TdJsonRaw): GetPollVoters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPollVoters = new GetPollVoters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.option_id = asNumber(record['option_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getPollVoters` into TDLib JSON wire shape. */
export function encodeGetPollVoters(value: GetPollVoters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPollVoters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['option_id'] = value.option_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getPreferredCountryLanguage = Text — Returns an IETF language tag of the language preferred in the country, which must be used to fill native fields in Telegram Passport personal details. Returns a 404 error if unknown */
export class GetPreferredCountryLanguage {
  type: 'getPreferredCountryLanguage' = 'getPreferredCountryLanguage';
  extra?: string;
  /** country_code: string — A two-letter ISO 3166-1 alpha-2 country code */
  country_code: string = '';
}

/** Decode a TDLib `getPreferredCountryLanguage` object; null input -> null. */
export function decodeGetPreferredCountryLanguage(json: TdJsonRaw): GetPreferredCountryLanguage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPreferredCountryLanguage = new GetPreferredCountryLanguage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.country_code = asString(record['country_code']);
  return out;
}

/** Encode a `getPreferredCountryLanguage` into TDLib JSON wire shape. */
export function encodeGetPreferredCountryLanguage(value: GetPreferredCountryLanguage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPreferredCountryLanguage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['country_code'] = value.country_code;
  return out;
}

/** getPremiumFeatures = PremiumFeatures — Returns information about features, available to Premium users */
export class GetPremiumFeatures {
  type: 'getPremiumFeatures' = 'getPremiumFeatures';
  extra?: string;
  /** source: PremiumSource — Source of the request; pass null if the method is called from some non-standard source */
  source: TdPremiumSource | null = null;
}

/** Decode a TDLib `getPremiumFeatures` object; null input -> null. */
export function decodeGetPremiumFeatures(json: TdJsonRaw): GetPremiumFeatures | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPremiumFeatures = new GetPremiumFeatures();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.source = decodeTdPremiumSource(record['source']);
  return out;
}

/** Encode a `getPremiumFeatures` into TDLib JSON wire shape. */
export function encodeGetPremiumFeatures(value: GetPremiumFeatures): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPremiumFeatures';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['source'] = value.source === null ? null : encodeTdPremiumSource(value.source);
  return out;
}

/** getPremiumGiftPaymentOptions = PremiumGiftPaymentOptions — Returns available options for gifting Telegram Premium to a user */
export class GetPremiumGiftPaymentOptions {
  type: 'getPremiumGiftPaymentOptions' = 'getPremiumGiftPaymentOptions';
  extra?: string;
}

/** Decode a TDLib `getPremiumGiftPaymentOptions` object; null input -> null. */
export function decodeGetPremiumGiftPaymentOptions(json: TdJsonRaw): GetPremiumGiftPaymentOptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPremiumGiftPaymentOptions = new GetPremiumGiftPaymentOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getPremiumGiftPaymentOptions` into TDLib JSON wire shape. */
export function encodeGetPremiumGiftPaymentOptions(value: GetPremiumGiftPaymentOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPremiumGiftPaymentOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getPremiumGiveawayPaymentOptions = PremiumGiveawayPaymentOptions — Returns available options for creating of Telegram Premium giveaway or manual distribution of Telegram Premium among chat members */
export class GetPremiumGiveawayPaymentOptions {
  type: 'getPremiumGiveawayPaymentOptions' = 'getPremiumGiveawayPaymentOptions';
  extra?: string;
  /** boosted_chat_id: int53 — Identifier of the supergroup or channel chat, which will be automatically boosted by receivers of the gift codes and which is administered by the user */
  boosted_chat_id: number = 0;
}

/** Decode a TDLib `getPremiumGiveawayPaymentOptions` object; null input -> null. */
export function decodeGetPremiumGiveawayPaymentOptions(json: TdJsonRaw): GetPremiumGiveawayPaymentOptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPremiumGiveawayPaymentOptions = new GetPremiumGiveawayPaymentOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.boosted_chat_id = asNumber(record['boosted_chat_id']);
  return out;
}

/** Encode a `getPremiumGiveawayPaymentOptions` into TDLib JSON wire shape. */
export function encodeGetPremiumGiveawayPaymentOptions(value: GetPremiumGiveawayPaymentOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPremiumGiveawayPaymentOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['boosted_chat_id'] = value.boosted_chat_id;
  return out;
}

/** getPremiumInfoSticker = Sticker — Returns the sticker to be used as representation of the Telegram Premium subscription */
export class GetPremiumInfoSticker {
  type: 'getPremiumInfoSticker' = 'getPremiumInfoSticker';
  extra?: string;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active */
  month_count: number = 0;
}

/** Decode a TDLib `getPremiumInfoSticker` object; null input -> null. */
export function decodeGetPremiumInfoSticker(json: TdJsonRaw): GetPremiumInfoSticker | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPremiumInfoSticker = new GetPremiumInfoSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.month_count = asNumber(record['month_count']);
  return out;
}

/** Encode a `getPremiumInfoSticker` into TDLib JSON wire shape. */
export function encodeGetPremiumInfoSticker(value: GetPremiumInfoSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPremiumInfoSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['month_count'] = value.month_count;
  return out;
}

/** getPremiumLimit = PremiumLimit — Returns information about a limit, increased for Premium users. Returns a 404 error if the limit is unknown */
export class GetPremiumLimit {
  type: 'getPremiumLimit' = 'getPremiumLimit';
  extra?: string;
  /** limit_type: PremiumLimitType — Type of the limit */
  limit_type: TdPremiumLimitType | null = null;
}

/** Decode a TDLib `getPremiumLimit` object; null input -> null. */
export function decodeGetPremiumLimit(json: TdJsonRaw): GetPremiumLimit | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPremiumLimit = new GetPremiumLimit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.limit_type = decodeTdPremiumLimitType(record['limit_type']);
  return out;
}

/** Encode a `getPremiumLimit` into TDLib JSON wire shape. */
export function encodeGetPremiumLimit(value: GetPremiumLimit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPremiumLimit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['limit_type'] = value.limit_type === null ? null : encodeTdPremiumLimitType(value.limit_type);
  return out;
}

/** getPremiumState = PremiumState — Returns state of Telegram Premium subscription and promotion videos for Premium features */
export class GetPremiumState {
  type: 'getPremiumState' = 'getPremiumState';
  extra?: string;
}

/** Decode a TDLib `getPremiumState` object; null input -> null. */
export function decodeGetPremiumState(json: TdJsonRaw): GetPremiumState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPremiumState = new GetPremiumState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getPremiumState` into TDLib JSON wire shape. */
export function encodeGetPremiumState(value: GetPremiumState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPremiumState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getPremiumStickerExamples = Stickers — Returns examples of premium stickers for demonstration purposes */
export class GetPremiumStickerExamples {
  type: 'getPremiumStickerExamples' = 'getPremiumStickerExamples';
  extra?: string;
}

/** Decode a TDLib `getPremiumStickerExamples` object; null input -> null. */
export function decodeGetPremiumStickerExamples(json: TdJsonRaw): GetPremiumStickerExamples | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPremiumStickerExamples = new GetPremiumStickerExamples();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getPremiumStickerExamples` into TDLib JSON wire shape. */
export function encodeGetPremiumStickerExamples(value: GetPremiumStickerExamples): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPremiumStickerExamples';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getPremiumStickers = Stickers — Returns premium stickers from regular sticker sets */
export class GetPremiumStickers {
  type: 'getPremiumStickers' = 'getPremiumStickers';
  extra?: string;
  /** limit: int32 — The maximum number of stickers to be returned; 0-100 */
  limit: number = 0;
}

/** Decode a TDLib `getPremiumStickers` object; null input -> null. */
export function decodeGetPremiumStickers(json: TdJsonRaw): GetPremiumStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPremiumStickers = new GetPremiumStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getPremiumStickers` into TDLib JSON wire shape. */
export function encodeGetPremiumStickers(value: GetPremiumStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPremiumStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['limit'] = value.limit;
  return out;
}

/** getPreparedInlineMessage = PreparedInlineMessage — Saves an inline message to be sent by the given user */
export class GetPreparedInlineMessage {
  type: 'getPreparedInlineMessage' = 'getPreparedInlineMessage';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the bot that created the message */
  bot_user_id: number = 0;
  /** prepared_message_id: string — Identifier of the prepared message */
  prepared_message_id: string = '';
}

/** Decode a TDLib `getPreparedInlineMessage` object; null input -> null. */
export function decodeGetPreparedInlineMessage(json: TdJsonRaw): GetPreparedInlineMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPreparedInlineMessage = new GetPreparedInlineMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.prepared_message_id = asString(record['prepared_message_id']);
  return out;
}

/** Encode a `getPreparedInlineMessage` into TDLib JSON wire shape. */
export function encodeGetPreparedInlineMessage(value: GetPreparedInlineMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPreparedInlineMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['prepared_message_id'] = value.prepared_message_id;
  return out;
}

/** getPreparedKeyboardButton = KeyboardButton — Returns a keyboard button prepared by the bot for the user. The button will be of the type keyboardButtonTypeRequestUsers, keyboardButtonTypeRequestChat, or keyboardButtonTypeRequestManagedBot */
export class GetPreparedKeyboardButton {
  type: 'getPreparedKeyboardButton' = 'getPreparedKeyboardButton';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the bot that created the button */
  bot_user_id: number = 0;
  /** prepared_button_id: string — Identifier of the prepared button */
  prepared_button_id: string = '';
}

/** Decode a TDLib `getPreparedKeyboardButton` object; null input -> null. */
export function decodeGetPreparedKeyboardButton(json: TdJsonRaw): GetPreparedKeyboardButton | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPreparedKeyboardButton = new GetPreparedKeyboardButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.prepared_button_id = asString(record['prepared_button_id']);
  return out;
}

/** Encode a `getPreparedKeyboardButton` into TDLib JSON wire shape. */
export function encodeGetPreparedKeyboardButton(value: GetPreparedKeyboardButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPreparedKeyboardButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['prepared_button_id'] = value.prepared_button_id;
  return out;
}

/** getProxies = AddedProxies — Returns the list of proxies that are currently set up. Can be called before authorization */
export class GetProxies {
  type: 'getProxies' = 'getProxies';
  extra?: string;
}

/** Decode a TDLib `getProxies` object; null input -> null. */
export function decodeGetProxies(json: TdJsonRaw): GetProxies | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetProxies = new GetProxies();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getProxies` into TDLib JSON wire shape. */
export function encodeGetProxies(value: GetProxies): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getProxies';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getPublicPostSearchLimits = PublicPostSearchLimits — Checks public post search limits without actually performing the search */
export class GetPublicPostSearchLimits {
  type: 'getPublicPostSearchLimits' = 'getPublicPostSearchLimits';
  extra?: string;
  /** query: string — Query that will be searched for */
  query: string = '';
}

/** Decode a TDLib `getPublicPostSearchLimits` object; null input -> null. */
export function decodeGetPublicPostSearchLimits(json: TdJsonRaw): GetPublicPostSearchLimits | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPublicPostSearchLimits = new GetPublicPostSearchLimits();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  return out;
}

/** Encode a `getPublicPostSearchLimits` into TDLib JSON wire shape. */
export function encodeGetPublicPostSearchLimits(value: GetPublicPostSearchLimits): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPublicPostSearchLimits';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  return out;
}

/** getPushReceiverId = PushReceiverId — Returns a globally unique push notification subscription identifier for identification of an account, which has received a push notification. Can be called synchronously */
export class GetPushReceiverId {
  type: 'getPushReceiverId' = 'getPushReceiverId';
  extra?: string;
  /** payload: string — JSON-encoded push notification payload */
  payload: string = '';
}

/** Decode a TDLib `getPushReceiverId` object; null input -> null. */
export function decodeGetPushReceiverId(json: TdJsonRaw): GetPushReceiverId | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetPushReceiverId = new GetPushReceiverId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.payload = asString(record['payload']);
  return out;
}

/** Encode a `getPushReceiverId` into TDLib JSON wire shape. */
export function encodeGetPushReceiverId(value: GetPushReceiverId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getPushReceiverId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['payload'] = value.payload;
  return out;
}

/** getReadDatePrivacySettings = ReadDatePrivacySettings — Returns privacy settings for message read date */
export class GetReadDatePrivacySettings {
  type: 'getReadDatePrivacySettings' = 'getReadDatePrivacySettings';
  extra?: string;
}

/** Decode a TDLib `getReadDatePrivacySettings` object; null input -> null. */
export function decodeGetReadDatePrivacySettings(json: TdJsonRaw): GetReadDatePrivacySettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetReadDatePrivacySettings = new GetReadDatePrivacySettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getReadDatePrivacySettings` into TDLib JSON wire shape. */
export function encodeGetReadDatePrivacySettings(value: GetReadDatePrivacySettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getReadDatePrivacySettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getReceivedGift = ReceivedGift — Returns information about a received gift */
export class GetReceivedGift {
  type: 'getReceivedGift' = 'getReceivedGift';
  extra?: string;
  /** received_gift_id: string — Identifier of the gift */
  received_gift_id: string = '';
}

/** Decode a TDLib `getReceivedGift` object; null input -> null. */
export function decodeGetReceivedGift(json: TdJsonRaw): GetReceivedGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetReceivedGift = new GetReceivedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.received_gift_id = asString(record['received_gift_id']);
  return out;
}

/** Encode a `getReceivedGift` into TDLib JSON wire shape. */
export function encodeGetReceivedGift(value: GetReceivedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getReceivedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['received_gift_id'] = value.received_gift_id;
  return out;
}

/** getReceivedGifts = ReceivedGifts — Returns gifts received by the given user or chat */
export class GetReceivedGifts {
  type: 'getReceivedGifts' = 'getReceivedGifts';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which to send the request; for bots only */
  business_connection_id: string = '';
  /** owner_id: MessageSender — Identifier of the gift receiver */
  owner_id: TdMessageSender | null = null;
  /** collection_id: int32 — Pass collection identifier to get gifts only from the specified collection; pass 0 to get gifts regardless of collections */
  collection_id: number = 0;
  /** exclude_unsaved: Bool — Pass true to exclude gifts that aren't saved to the chat's profile page. Always true for gifts received by other users and channel chats without can_post_messages administrator right */
  exclude_unsaved: boolean = false;
  /** exclude_saved: Bool — Pass true to exclude gifts that are saved to the chat's profile page. Always false for gifts received by other users and channel chats without can_post_messages administrator right */
  exclude_saved: boolean = false;
  /** exclude_unlimited: Bool — Pass true to exclude gifts that can be purchased unlimited number of times */
  exclude_unlimited: boolean = false;
  /** exclude_upgradable: Bool — Pass true to exclude gifts that can be purchased limited number of times and can be upgraded */
  exclude_upgradable: boolean = false;
  /** exclude_non_upgradable: Bool — Pass true to exclude gifts that can be purchased limited number of times and can't be upgraded */
  exclude_non_upgradable: boolean = false;
  /** exclude_upgraded: Bool — Pass true to exclude upgraded gifts */
  exclude_upgraded: boolean = false;
  /** exclude_without_colors: Bool — Pass true to exclude gifts that can't be used in setUpgradedGiftColors */
  exclude_without_colors: boolean = false;
  /** exclude_hosted: Bool — Pass true to exclude gifts that are just hosted and are not owned by the owner */
  exclude_hosted: boolean = false;
  /** sort_by_price: Bool — Pass true to sort results by gift price instead of send date */
  sort_by_price: boolean = false;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of gifts to be returned; must be positive and can't be greater than 100. For optimal performance, the number of returned objects is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `getReceivedGifts` object; null input -> null. */
export function decodeGetReceivedGifts(json: TdJsonRaw): GetReceivedGifts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetReceivedGifts = new GetReceivedGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.collection_id = asNumber(record['collection_id']);
  out.exclude_unsaved = asBool(record['exclude_unsaved']);
  out.exclude_saved = asBool(record['exclude_saved']);
  out.exclude_unlimited = asBool(record['exclude_unlimited']);
  out.exclude_upgradable = asBool(record['exclude_upgradable']);
  out.exclude_non_upgradable = asBool(record['exclude_non_upgradable']);
  out.exclude_upgraded = asBool(record['exclude_upgraded']);
  out.exclude_without_colors = asBool(record['exclude_without_colors']);
  out.exclude_hosted = asBool(record['exclude_hosted']);
  out.sort_by_price = asBool(record['sort_by_price']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getReceivedGifts` into TDLib JSON wire shape. */
export function encodeGetReceivedGifts(value: GetReceivedGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getReceivedGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['collection_id'] = value.collection_id;
  out['exclude_unsaved'] = value.exclude_unsaved;
  out['exclude_saved'] = value.exclude_saved;
  out['exclude_unlimited'] = value.exclude_unlimited;
  out['exclude_upgradable'] = value.exclude_upgradable;
  out['exclude_non_upgradable'] = value.exclude_non_upgradable;
  out['exclude_upgraded'] = value.exclude_upgraded;
  out['exclude_without_colors'] = value.exclude_without_colors;
  out['exclude_hosted'] = value.exclude_hosted;
  out['sort_by_price'] = value.sort_by_price;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getRecentEmojiStatuses = EmojiStatuses — Returns recent emoji statuses for self status */
export class GetRecentEmojiStatuses {
  type: 'getRecentEmojiStatuses' = 'getRecentEmojiStatuses';
  extra?: string;
}

/** Decode a TDLib `getRecentEmojiStatuses` object; null input -> null. */
export function decodeGetRecentEmojiStatuses(json: TdJsonRaw): GetRecentEmojiStatuses | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRecentEmojiStatuses = new GetRecentEmojiStatuses();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getRecentEmojiStatuses` into TDLib JSON wire shape. */
export function encodeGetRecentEmojiStatuses(value: GetRecentEmojiStatuses): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRecentEmojiStatuses';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getRecentInlineBots = Users — Returns up to 20 recently used inline bots in the order of their last usage */
export class GetRecentInlineBots {
  type: 'getRecentInlineBots' = 'getRecentInlineBots';
  extra?: string;
}

/** Decode a TDLib `getRecentInlineBots` object; null input -> null. */
export function decodeGetRecentInlineBots(json: TdJsonRaw): GetRecentInlineBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRecentInlineBots = new GetRecentInlineBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getRecentInlineBots` into TDLib JSON wire shape. */
export function encodeGetRecentInlineBots(value: GetRecentInlineBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRecentInlineBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getRecentStickers = Stickers — Returns a list of recently used stickers */
export class GetRecentStickers {
  type: 'getRecentStickers' = 'getRecentStickers';
  extra?: string;
  /** is_attached: Bool — Pass true to return stickers and masks that were recently attached to photos or video files; pass false to return recently sent stickers */
  is_attached: boolean = false;
}

/** Decode a TDLib `getRecentStickers` object; null input -> null. */
export function decodeGetRecentStickers(json: TdJsonRaw): GetRecentStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRecentStickers = new GetRecentStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_attached = asBool(record['is_attached']);
  return out;
}

/** Encode a `getRecentStickers` into TDLib JSON wire shape. */
export function encodeGetRecentStickers(value: GetRecentStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRecentStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_attached'] = value.is_attached;
  return out;
}

/** getRecentlyOpenedChats = Chats — Returns recently opened chats. This is an offline method. Returns chats in the order of last opening */
export class GetRecentlyOpenedChats {
  type: 'getRecentlyOpenedChats' = 'getRecentlyOpenedChats';
  extra?: string;
  /** limit: int32 — The maximum number of chats to be returned */
  limit: number = 0;
}

/** Decode a TDLib `getRecentlyOpenedChats` object; null input -> null. */
export function decodeGetRecentlyOpenedChats(json: TdJsonRaw): GetRecentlyOpenedChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRecentlyOpenedChats = new GetRecentlyOpenedChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getRecentlyOpenedChats` into TDLib JSON wire shape. */
export function encodeGetRecentlyOpenedChats(value: GetRecentlyOpenedChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRecentlyOpenedChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['limit'] = value.limit;
  return out;
}

/** getRecentlyVisitedTMeUrls = TMeUrls — Returns t.me URLs recently visited by a newly registered user */
export class GetRecentlyVisitedTMeUrls {
  type: 'getRecentlyVisitedTMeUrls' = 'getRecentlyVisitedTMeUrls';
  extra?: string;
  /** referrer: string — Google Play referrer to identify the user */
  referrer: string = '';
}

/** Decode a TDLib `getRecentlyVisitedTMeUrls` object; null input -> null. */
export function decodeGetRecentlyVisitedTMeUrls(json: TdJsonRaw): GetRecentlyVisitedTMeUrls | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRecentlyVisitedTMeUrls = new GetRecentlyVisitedTMeUrls();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.referrer = asString(record['referrer']);
  return out;
}

/** Encode a `getRecentlyVisitedTMeUrls` into TDLib JSON wire shape. */
export function encodeGetRecentlyVisitedTMeUrls(value: GetRecentlyVisitedTMeUrls): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRecentlyVisitedTMeUrls';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['referrer'] = value.referrer;
  return out;
}

/** getRecommendedChatFolders = RecommendedChatFolders — Returns recommended chat folders for the current user */
export class GetRecommendedChatFolders {
  type: 'getRecommendedChatFolders' = 'getRecommendedChatFolders';
  extra?: string;
}

/** Decode a TDLib `getRecommendedChatFolders` object; null input -> null. */
export function decodeGetRecommendedChatFolders(json: TdJsonRaw): GetRecommendedChatFolders | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRecommendedChatFolders = new GetRecommendedChatFolders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getRecommendedChatFolders` into TDLib JSON wire shape. */
export function encodeGetRecommendedChatFolders(value: GetRecommendedChatFolders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRecommendedChatFolders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getRecommendedChats = Chats — Returns a list of channel chats recommended to the current user */
export class GetRecommendedChats {
  type: 'getRecommendedChats' = 'getRecommendedChats';
  extra?: string;
}

/** Decode a TDLib `getRecommendedChats` object; null input -> null. */
export function decodeGetRecommendedChats(json: TdJsonRaw): GetRecommendedChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRecommendedChats = new GetRecommendedChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getRecommendedChats` into TDLib JSON wire shape. */
export function encodeGetRecommendedChats(value: GetRecommendedChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRecommendedChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getRecoveryEmailAddress = RecoveryEmailAddress — Returns a 2-step verification recovery email address that was previously set up. This method can be used to verify a password provided by the user */
export class GetRecoveryEmailAddress {
  type: 'getRecoveryEmailAddress' = 'getRecoveryEmailAddress';
  extra?: string;
  /** password: string — The 2-step verification password for the current user */
  password: string = '';
}

/** Decode a TDLib `getRecoveryEmailAddress` object; null input -> null. */
export function decodeGetRecoveryEmailAddress(json: TdJsonRaw): GetRecoveryEmailAddress | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRecoveryEmailAddress = new GetRecoveryEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.password = asString(record['password']);
  return out;
}

/** Encode a `getRecoveryEmailAddress` into TDLib JSON wire shape. */
export function encodeGetRecoveryEmailAddress(value: GetRecoveryEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRecoveryEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['password'] = value.password;
  return out;
}

/** getRemoteFile = File — Returns information about a file by its remote identifier. This is an offline method. Can be used to register a URL as a file for further uploading, or sending as a message. Even if the request succeeds, the file c... */
export class GetRemoteFile {
  type: 'getRemoteFile' = 'getRemoteFile';
  extra?: string;
  /** remote_file_id: string — Remote identifier of the file to get */
  remote_file_id: string = '';
  /** file_type: FileType — File type; pass null if unknown */
  file_type: TdFileType | null = null;
}

/** Decode a TDLib `getRemoteFile` object; null input -> null. */
export function decodeGetRemoteFile(json: TdJsonRaw): GetRemoteFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRemoteFile = new GetRemoteFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.remote_file_id = asString(record['remote_file_id']);
  out.file_type = decodeTdFileType(record['file_type']);
  return out;
}

/** Encode a `getRemoteFile` into TDLib JSON wire shape. */
export function encodeGetRemoteFile(value: GetRemoteFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRemoteFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['remote_file_id'] = value.remote_file_id;
  out['file_type'] = value.file_type === null ? null : encodeTdFileType(value.file_type);
  return out;
}

/** getRepliedMessage = Message — Returns information about a non-bundled message that is replied by a given message. Also, returns the pinned message for messagePinMessage, the game message for messageGameScore, the invoice message for mess... */
export class GetRepliedMessage {
  type: 'getRepliedMessage' = 'getRepliedMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the reply message */
  message_id: number = 0;
}

/** Decode a TDLib `getRepliedMessage` object; null input -> null. */
export function decodeGetRepliedMessage(json: TdJsonRaw): GetRepliedMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetRepliedMessage = new GetRepliedMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getRepliedMessage` into TDLib JSON wire shape. */
export function encodeGetRepliedMessage(value: GetRepliedMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getRepliedMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getSavedAnimations = Animations — Returns saved animations */
export class GetSavedAnimations {
  type: 'getSavedAnimations' = 'getSavedAnimations';
  extra?: string;
}

/** Decode a TDLib `getSavedAnimations` object; null input -> null. */
export function decodeGetSavedAnimations(json: TdJsonRaw): GetSavedAnimations | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSavedAnimations = new GetSavedAnimations();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getSavedAnimations` into TDLib JSON wire shape. */
export function encodeGetSavedAnimations(value: GetSavedAnimations): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSavedAnimations';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getSavedMessagesTags = SavedMessagesTags — Returns tags used in Saved Messages or a Saved Messages topic */
export class GetSavedMessagesTags {
  type: 'getSavedMessagesTags' = 'getSavedMessagesTags';
  extra?: string;
  /** saved_messages_topic_id: int53 — Identifier of Saved Messages topic which tags will be returned; pass 0 to get all Saved Messages tags */
  saved_messages_topic_id: number = 0;
}

/** Decode a TDLib `getSavedMessagesTags` object; null input -> null. */
export function decodeGetSavedMessagesTags(json: TdJsonRaw): GetSavedMessagesTags | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSavedMessagesTags = new GetSavedMessagesTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  return out;
}

/** Encode a `getSavedMessagesTags` into TDLib JSON wire shape. */
export function encodeGetSavedMessagesTags(value: GetSavedMessagesTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSavedMessagesTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  return out;
}

/** getSavedMessagesTopicHistory = Messages — Returns messages in a Saved Messages topic. The messages are returned in reverse chronological order (i.e., in order of decreasing message_id) For optimal performance, the number of returned mess... */
export class GetSavedMessagesTopicHistory {
  type: 'getSavedMessagesTopicHistory' = 'getSavedMessagesTopicHistory';
  extra?: string;
  /** saved_messages_topic_id: int53 — Identifier of Saved Messages topic which messages will be fetched */
  saved_messages_topic_id: number = 0;
  /** from_message_id: int53 — Identifier of the message starting from which messages must be fetched; use 0 to get results from the last message */
  from_message_id: number = 0;
  /** offset: int32 — Specify 0 to get results from exactly the message from_message_id or a negative number from -99 to -1 to get additionally -offset newer messages */
  offset: number = 0;
  /** limit: int32 — The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, then the limit must be greater than or equal to -offset. */
  limit: number = 0;
}

/** Decode a TDLib `getSavedMessagesTopicHistory` object; null input -> null. */
export function decodeGetSavedMessagesTopicHistory(json: TdJsonRaw): GetSavedMessagesTopicHistory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSavedMessagesTopicHistory = new GetSavedMessagesTopicHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  out.from_message_id = asNumber(record['from_message_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getSavedMessagesTopicHistory` into TDLib JSON wire shape. */
export function encodeGetSavedMessagesTopicHistory(value: GetSavedMessagesTopicHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSavedMessagesTopicHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  out['from_message_id'] = value.from_message_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getSavedMessagesTopicMessageByDate = Message — Returns the last message sent in a Saved Messages topic no later than the specified date */
export class GetSavedMessagesTopicMessageByDate {
  type: 'getSavedMessagesTopicMessageByDate' = 'getSavedMessagesTopicMessageByDate';
  extra?: string;
  /** saved_messages_topic_id: int53 — Identifier of Saved Messages topic which message will be returned */
  saved_messages_topic_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) relative to which to search for messages */
  date: number = 0;
}

/** Decode a TDLib `getSavedMessagesTopicMessageByDate` object; null input -> null. */
export function decodeGetSavedMessagesTopicMessageByDate(json: TdJsonRaw): GetSavedMessagesTopicMessageByDate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSavedMessagesTopicMessageByDate = new GetSavedMessagesTopicMessageByDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `getSavedMessagesTopicMessageByDate` into TDLib JSON wire shape. */
export function encodeGetSavedMessagesTopicMessageByDate(value: GetSavedMessagesTopicMessageByDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSavedMessagesTopicMessageByDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  out['date'] = value.date;
  return out;
}

/** getSavedNotificationSound = NotificationSound — Returns saved notification sound by its identifier. Returns a 404 error if there is no saved notification sound with the specified identifier */
export class GetSavedNotificationSound {
  type: 'getSavedNotificationSound' = 'getSavedNotificationSound';
  extra?: string;
  /** notification_sound_id: int64 — Identifier of the notification sound */
  notification_sound_id: string = '';
}

/** Decode a TDLib `getSavedNotificationSound` object; null input -> null. */
export function decodeGetSavedNotificationSound(json: TdJsonRaw): GetSavedNotificationSound | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSavedNotificationSound = new GetSavedNotificationSound();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_sound_id = asInt64(record['notification_sound_id']);
  return out;
}

/** Encode a `getSavedNotificationSound` into TDLib JSON wire shape. */
export function encodeGetSavedNotificationSound(value: GetSavedNotificationSound): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSavedNotificationSound';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_sound_id'] = value.notification_sound_id;
  return out;
}

/** getSavedNotificationSounds = NotificationSounds — Returns the list of saved notification sounds. If a sound isn't in the list, then default sound needs to be used */
export class GetSavedNotificationSounds {
  type: 'getSavedNotificationSounds' = 'getSavedNotificationSounds';
  extra?: string;
}

/** Decode a TDLib `getSavedNotificationSounds` object; null input -> null. */
export function decodeGetSavedNotificationSounds(json: TdJsonRaw): GetSavedNotificationSounds | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSavedNotificationSounds = new GetSavedNotificationSounds();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getSavedNotificationSounds` into TDLib JSON wire shape. */
export function encodeGetSavedNotificationSounds(value: GetSavedNotificationSounds): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSavedNotificationSounds';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getSavedOrderInfo = OrderInfo — Returns saved order information. Returns a 404 error if there is no saved order information */
export class GetSavedOrderInfo {
  type: 'getSavedOrderInfo' = 'getSavedOrderInfo';
  extra?: string;
}

/** Decode a TDLib `getSavedOrderInfo` object; null input -> null. */
export function decodeGetSavedOrderInfo(json: TdJsonRaw): GetSavedOrderInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSavedOrderInfo = new GetSavedOrderInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getSavedOrderInfo` into TDLib JSON wire shape. */
export function encodeGetSavedOrderInfo(value: GetSavedOrderInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSavedOrderInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getScopeNotificationSettings = ScopeNotificationSettings — Returns the notification settings for chats of a given type */
export class GetScopeNotificationSettings {
  type: 'getScopeNotificationSettings' = 'getScopeNotificationSettings';
  extra?: string;
  /** scope: NotificationSettingsScope — Types of chats for which to return the notification settings information */
  scope: TdNotificationSettingsScope | null = null;
}

/** Decode a TDLib `getScopeNotificationSettings` object; null input -> null. */
export function decodeGetScopeNotificationSettings(json: TdJsonRaw): GetScopeNotificationSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetScopeNotificationSettings = new GetScopeNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scope = decodeTdNotificationSettingsScope(record['scope']);
  return out;
}

/** Encode a `getScopeNotificationSettings` into TDLib JSON wire shape. */
export function encodeGetScopeNotificationSettings(value: GetScopeNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getScopeNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scope'] = value.scope === null ? null : encodeTdNotificationSettingsScope(value.scope);
  return out;
}

/** getSearchSponsoredChats = SponsoredChats — Returns sponsored chats to be shown in the search results */
export class GetSearchSponsoredChats {
  type: 'getSearchSponsoredChats' = 'getSearchSponsoredChats';
  extra?: string;
  /** query: string — Query the user searches for */
  query: string = '';
}

/** Decode a TDLib `getSearchSponsoredChats` object; null input -> null. */
export function decodeGetSearchSponsoredChats(json: TdJsonRaw): GetSearchSponsoredChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSearchSponsoredChats = new GetSearchSponsoredChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query = asString(record['query']);
  return out;
}

/** Encode a `getSearchSponsoredChats` into TDLib JSON wire shape. */
export function encodeGetSearchSponsoredChats(value: GetSearchSponsoredChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSearchSponsoredChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query'] = value.query;
  return out;
}

/** getSearchedForTags = Hashtags — Returns recently searched for hashtags or cashtags by their prefix */
export class GetSearchedForTags {
  type: 'getSearchedForTags' = 'getSearchedForTags';
  extra?: string;
  /** tag_prefix: string — Prefix of hashtags or cashtags to return */
  tag_prefix: string = '';
  /** limit: int32 — The maximum number of items to be returned */
  limit: number = 0;
}

/** Decode a TDLib `getSearchedForTags` object; null input -> null. */
export function decodeGetSearchedForTags(json: TdJsonRaw): GetSearchedForTags | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSearchedForTags = new GetSearchedForTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.tag_prefix = asString(record['tag_prefix']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getSearchedForTags` into TDLib JSON wire shape. */
export function encodeGetSearchedForTags(value: GetSearchedForTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSearchedForTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['tag_prefix'] = value.tag_prefix;
  out['limit'] = value.limit;
  return out;
}

/** getSecretChat = SecretChat — Returns information about a secret chat by its identifier. This is an offline method */
export class GetSecretChat {
  type: 'getSecretChat' = 'getSecretChat';
  extra?: string;
  /** secret_chat_id: int32 — Secret chat identifier */
  secret_chat_id: number = 0;
}

/** Decode a TDLib `getSecretChat` object; null input -> null. */
export function decodeGetSecretChat(json: TdJsonRaw): GetSecretChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSecretChat = new GetSecretChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.secret_chat_id = asNumber(record['secret_chat_id']);
  return out;
}

/** Encode a `getSecretChat` into TDLib JSON wire shape. */
export function encodeGetSecretChat(value: GetSecretChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSecretChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['secret_chat_id'] = value.secret_chat_id;
  return out;
}

/** getStakeDiceState = StakeDiceState — Returns the current state of stake dice */
export class GetStakeDiceState {
  type: 'getStakeDiceState' = 'getStakeDiceState';
  extra?: string;
}

/** Decode a TDLib `getStakeDiceState` object; null input -> null. */
export function decodeGetStakeDiceState(json: TdJsonRaw): GetStakeDiceState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStakeDiceState = new GetStakeDiceState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getStakeDiceState` into TDLib JSON wire shape. */
export function encodeGetStakeDiceState(value: GetStakeDiceState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStakeDiceState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getStarAdAccountUrl = HttpUrl — Returns a URL for a Telegram Ad platform account that can be used to set up advertisements for the chat paid in the owned Telegram Stars */
export class GetStarAdAccountUrl {
  type: 'getStarAdAccountUrl' = 'getStarAdAccountUrl';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the owner of the Telegram Stars; can be identifier of an owned bot, or identifier of an owned channel chat */
  owner_id: TdMessageSender | null = null;
}

/** Decode a TDLib `getStarAdAccountUrl` object; null input -> null. */
export function decodeGetStarAdAccountUrl(json: TdJsonRaw): GetStarAdAccountUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStarAdAccountUrl = new GetStarAdAccountUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  return out;
}

/** Encode a `getStarAdAccountUrl` into TDLib JSON wire shape. */
export function encodeGetStarAdAccountUrl(value: GetStarAdAccountUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStarAdAccountUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  return out;
}

/** getStarGiftPaymentOptions = StarPaymentOptions — Returns available options for Telegram Stars gifting */
export class GetStarGiftPaymentOptions {
  type: 'getStarGiftPaymentOptions' = 'getStarGiftPaymentOptions';
  extra?: string;
  /** user_id: int53 — Identifier of the user who will receive Telegram Stars; pass 0 to get options for an unspecified user */
  user_id: number = 0;
}

/** Decode a TDLib `getStarGiftPaymentOptions` object; null input -> null. */
export function decodeGetStarGiftPaymentOptions(json: TdJsonRaw): GetStarGiftPaymentOptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStarGiftPaymentOptions = new GetStarGiftPaymentOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `getStarGiftPaymentOptions` into TDLib JSON wire shape. */
export function encodeGetStarGiftPaymentOptions(value: GetStarGiftPaymentOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStarGiftPaymentOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** getStarGiveawayPaymentOptions = StarGiveawayPaymentOptions — Returns available options for Telegram Star giveaway creation */
export class GetStarGiveawayPaymentOptions {
  type: 'getStarGiveawayPaymentOptions' = 'getStarGiveawayPaymentOptions';
  extra?: string;
}

/** Decode a TDLib `getStarGiveawayPaymentOptions` object; null input -> null. */
export function decodeGetStarGiveawayPaymentOptions(json: TdJsonRaw): GetStarGiveawayPaymentOptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStarGiveawayPaymentOptions = new GetStarGiveawayPaymentOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getStarGiveawayPaymentOptions` into TDLib JSON wire shape. */
export function encodeGetStarGiveawayPaymentOptions(value: GetStarGiveawayPaymentOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStarGiveawayPaymentOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getStarPaymentOptions = StarPaymentOptions — Returns available options for Telegram Stars purchase */
export class GetStarPaymentOptions {
  type: 'getStarPaymentOptions' = 'getStarPaymentOptions';
  extra?: string;
}

/** Decode a TDLib `getStarPaymentOptions` object; null input -> null. */
export function decodeGetStarPaymentOptions(json: TdJsonRaw): GetStarPaymentOptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStarPaymentOptions = new GetStarPaymentOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getStarPaymentOptions` into TDLib JSON wire shape. */
export function encodeGetStarPaymentOptions(value: GetStarPaymentOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStarPaymentOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getStarRevenueStatistics = StarRevenueStatistics — Returns detailed Telegram Star revenue statistics */
export class GetStarRevenueStatistics {
  type: 'getStarRevenueStatistics' = 'getStarRevenueStatistics';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the owner of the Telegram Stars; can be identifier of the current user, an owned bot, or a supergroup or a channel chat with supergroupFullInfo.can_get_star_revenue_statistics == true */
  owner_id: TdMessageSender | null = null;
  /** is_dark: Bool — Pass true if a dark theme is used by the application */
  is_dark: boolean = false;
}

/** Decode a TDLib `getStarRevenueStatistics` object; null input -> null. */
export function decodeGetStarRevenueStatistics(json: TdJsonRaw): GetStarRevenueStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStarRevenueStatistics = new GetStarRevenueStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.is_dark = asBool(record['is_dark']);
  return out;
}

/** Encode a `getStarRevenueStatistics` into TDLib JSON wire shape. */
export function encodeGetStarRevenueStatistics(value: GetStarRevenueStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStarRevenueStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['is_dark'] = value.is_dark;
  return out;
}

/** getStarSubscriptions = StarSubscriptions — Returns the list of Telegram Star subscriptions for the current user */
export class GetStarSubscriptions {
  type: 'getStarSubscriptions' = 'getStarSubscriptions';
  extra?: string;
  /** only_expiring: Bool — Pass true to receive only expiring subscriptions for which there aren't enough Telegram Stars to extend */
  only_expiring: boolean = false;
  /** offset: string — Offset of the first subscription to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
}

/** Decode a TDLib `getStarSubscriptions` object; null input -> null. */
export function decodeGetStarSubscriptions(json: TdJsonRaw): GetStarSubscriptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStarSubscriptions = new GetStarSubscriptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.only_expiring = asBool(record['only_expiring']);
  out.offset = asString(record['offset']);
  return out;
}

/** Encode a `getStarSubscriptions` into TDLib JSON wire shape. */
export function encodeGetStarSubscriptions(value: GetStarSubscriptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStarSubscriptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['only_expiring'] = value.only_expiring;
  out['offset'] = value.offset;
  return out;
}

/** getStarTransactions = StarTransactions — Returns the list of Telegram Star transactions for the specified owner or identifier of a supergroup or a channel chat with supergroupFullInfo.can_get_star_revenue_statistics == true */
export class GetStarTransactions {
  type: 'getStarTransactions' = 'getStarTransactions';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the owner of the Telegram Stars; can be the identifier of the current user, identifier of an owned bot, */
  owner_id: TdMessageSender | null = null;
  /** subscription_id: string — If non-empty, only transactions related to the Star Subscription will be returned */
  subscription_id: string = '';
  /** direction: TransactionDirection — Direction of the transactions to receive; pass null to get all transactions */
  direction: TdTransactionDirection | null = null;
  /** offset: string — Offset of the first transaction to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of transactions to return */
  limit: number = 0;
}

/** Decode a TDLib `getStarTransactions` object; null input -> null. */
export function decodeGetStarTransactions(json: TdJsonRaw): GetStarTransactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStarTransactions = new GetStarTransactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.subscription_id = asString(record['subscription_id']);
  out.direction = decodeTdTransactionDirection(record['direction']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getStarTransactions` into TDLib JSON wire shape. */
export function encodeGetStarTransactions(value: GetStarTransactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStarTransactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['subscription_id'] = value.subscription_id;
  out['direction'] = value.direction === null ? null : encodeTdTransactionDirection(value.direction);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getStarWithdrawalUrl = HttpUrl — Returns a URL for Telegram Star withdrawal */
export class GetStarWithdrawalUrl {
  type: 'getStarWithdrawalUrl' = 'getStarWithdrawalUrl';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the owner of the Telegram Stars; can be identifier of the current user, an owned bot, or an owned supergroup or channel chat */
  owner_id: TdMessageSender | null = null;
  /** star_count: int53 — The number of Telegram Stars to withdraw; must be between getOption("star_withdrawal_count_min") and getOption("star_withdrawal_count_max") */
  star_count: number = 0;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
}

/** Decode a TDLib `getStarWithdrawalUrl` object; null input -> null. */
export function decodeGetStarWithdrawalUrl(json: TdJsonRaw): GetStarWithdrawalUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStarWithdrawalUrl = new GetStarWithdrawalUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.star_count = asNumber(record['star_count']);
  out.password = asString(record['password']);
  return out;
}

/** Encode a `getStarWithdrawalUrl` into TDLib JSON wire shape. */
export function encodeGetStarWithdrawalUrl(value: GetStarWithdrawalUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStarWithdrawalUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['star_count'] = value.star_count;
  out['password'] = value.password;
  return out;
}

/** getStatisticalGraph = StatisticalGraph — Loads an asynchronous or a zoomed in statistical graph */
export class GetStatisticalGraph {
  type: 'getStatisticalGraph' = 'getStatisticalGraph';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** token: string — The token for graph loading */
  token: string = '';
  /** x: int53 — X-value for zoomed in graph or 0 otherwise */
  x: number = 0;
}

/** Decode a TDLib `getStatisticalGraph` object; null input -> null. */
export function decodeGetStatisticalGraph(json: TdJsonRaw): GetStatisticalGraph | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStatisticalGraph = new GetStatisticalGraph();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.token = asString(record['token']);
  out.x = asNumber(record['x']);
  return out;
}

/** Encode a `getStatisticalGraph` into TDLib JSON wire shape. */
export function encodeGetStatisticalGraph(value: GetStatisticalGraph): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStatisticalGraph';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['token'] = value.token;
  out['x'] = value.x;
  return out;
}

/** getStickerEmojis = Emojis — Returns emoji corresponding to a sticker. The list is only for informational purposes, because a sticker is always sent with a fixed emoji from the corresponding Sticker object */
export class GetStickerEmojis {
  type: 'getStickerEmojis' = 'getStickerEmojis';
  extra?: string;
  /** sticker: InputFile — Sticker file identifier */
  sticker: TdInputFile | null = null;
}

/** Decode a TDLib `getStickerEmojis` object; null input -> null. */
export function decodeGetStickerEmojis(json: TdJsonRaw): GetStickerEmojis | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStickerEmojis = new GetStickerEmojis();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  return out;
}

/** Encode a `getStickerEmojis` into TDLib JSON wire shape. */
export function encodeGetStickerEmojis(value: GetStickerEmojis): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStickerEmojis';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  return out;
}

/** getStickerOutline = Outline — Returns outline of a sticker. This is an offline method. Returns a 404 error if the outline isn't known */
export class GetStickerOutline {
  type: 'getStickerOutline' = 'getStickerOutline';
  extra?: string;
  /** sticker_file_id: int32 — File identifier of the sticker */
  sticker_file_id: number = 0;
  /** for_animated_emoji: Bool — Pass true to get the outline scaled for animated emoji */
  for_animated_emoji: boolean = false;
  /** for_clicked_animated_emoji_message: Bool — Pass true to get the outline scaled for clicked animated emoji message */
  for_clicked_animated_emoji_message: boolean = false;
}

/** Decode a TDLib `getStickerOutline` object; null input -> null. */
export function decodeGetStickerOutline(json: TdJsonRaw): GetStickerOutline | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStickerOutline = new GetStickerOutline();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_file_id = asNumber(record['sticker_file_id']);
  out.for_animated_emoji = asBool(record['for_animated_emoji']);
  out.for_clicked_animated_emoji_message = asBool(record['for_clicked_animated_emoji_message']);
  return out;
}

/** Encode a `getStickerOutline` into TDLib JSON wire shape. */
export function encodeGetStickerOutline(value: GetStickerOutline): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStickerOutline';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_file_id'] = value.sticker_file_id;
  out['for_animated_emoji'] = value.for_animated_emoji;
  out['for_clicked_animated_emoji_message'] = value.for_clicked_animated_emoji_message;
  return out;
}

/** getStickerOutlineSvgPath = Text — Returns outline of a sticker as an SVG path. This is an offline method. Returns an empty string if the outline isn't known */
export class GetStickerOutlineSvgPath {
  type: 'getStickerOutlineSvgPath' = 'getStickerOutlineSvgPath';
  extra?: string;
  /** sticker_file_id: int32 — File identifier of the sticker */
  sticker_file_id: number = 0;
  /** for_animated_emoji: Bool — Pass true to get the outline scaled for animated emoji */
  for_animated_emoji: boolean = false;
  /** for_clicked_animated_emoji_message: Bool — Pass true to get the outline scaled for clicked animated emoji message */
  for_clicked_animated_emoji_message: boolean = false;
}

/** Decode a TDLib `getStickerOutlineSvgPath` object; null input -> null. */
export function decodeGetStickerOutlineSvgPath(json: TdJsonRaw): GetStickerOutlineSvgPath | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStickerOutlineSvgPath = new GetStickerOutlineSvgPath();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_file_id = asNumber(record['sticker_file_id']);
  out.for_animated_emoji = asBool(record['for_animated_emoji']);
  out.for_clicked_animated_emoji_message = asBool(record['for_clicked_animated_emoji_message']);
  return out;
}

/** Encode a `getStickerOutlineSvgPath` into TDLib JSON wire shape. */
export function encodeGetStickerOutlineSvgPath(value: GetStickerOutlineSvgPath): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStickerOutlineSvgPath';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_file_id'] = value.sticker_file_id;
  out['for_animated_emoji'] = value.for_animated_emoji;
  out['for_clicked_animated_emoji_message'] = value.for_clicked_animated_emoji_message;
  return out;
}

/** getStickerSet = StickerSet — Returns information about a sticker set by its identifier */
export class GetStickerSet {
  type: 'getStickerSet' = 'getStickerSet';
  extra?: string;
  /** set_id: int64 — Identifier of the sticker set */
  set_id: string = '';
}

/** Decode a TDLib `getStickerSet` object; null input -> null. */
export function decodeGetStickerSet(json: TdJsonRaw): GetStickerSet | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStickerSet = new GetStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.set_id = asInt64(record['set_id']);
  return out;
}

/** Encode a `getStickerSet` into TDLib JSON wire shape. */
export function encodeGetStickerSet(value: GetStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['set_id'] = value.set_id;
  return out;
}

/** getStickerSetName = Text — Returns name of a sticker set by its identifier */
export class GetStickerSetName {
  type: 'getStickerSetName' = 'getStickerSetName';
  extra?: string;
  /** set_id: int64 — Identifier of the sticker set */
  set_id: string = '';
}

/** Decode a TDLib `getStickerSetName` object; null input -> null. */
export function decodeGetStickerSetName(json: TdJsonRaw): GetStickerSetName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStickerSetName = new GetStickerSetName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.set_id = asInt64(record['set_id']);
  return out;
}

/** Encode a `getStickerSetName` into TDLib JSON wire shape. */
export function encodeGetStickerSetName(value: GetStickerSetName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStickerSetName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['set_id'] = value.set_id;
  return out;
}

/** getStickers = Stickers — Returns stickers from the installed sticker sets that correspond to any of the given emoji or can be found by sticker-specific keywords. If the query is non-empty, then favorite, recently used or trending sticker... */
export class GetStickers {
  type: 'getStickers' = 'getStickers';
  extra?: string;
  /** sticker_type: StickerType — Type of the stickers to return */
  sticker_type: TdStickerType | null = null;
  /** query: string — Search query; a space-separated list of emojis or a keyword prefix. If empty, returns all known installed stickers */
  query: string = '';
  /** limit: int32 — The maximum number of stickers to be returned */
  limit: number = 0;
  /** chat_id: int53 — Chat identifier for which to return stickers. Available custom emoji stickers may be different for different chats */
  chat_id: number = 0;
}

/** Decode a TDLib `getStickers` object; null input -> null. */
export function decodeGetStickers(json: TdJsonRaw): GetStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStickers = new GetStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.query = asString(record['query']);
  out.limit = asNumber(record['limit']);
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getStickers` into TDLib JSON wire shape. */
export function encodeGetStickers(value: GetStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['query'] = value.query;
  out['limit'] = value.limit;
  out['chat_id'] = value.chat_id;
  return out;
}

/** getStorageStatistics = StorageStatistics — Returns storage usage statistics. Can be called before authorization */
export class GetStorageStatistics {
  type: 'getStorageStatistics' = 'getStorageStatistics';
  extra?: string;
  /** chat_limit: int32 — The maximum number of chats with the largest storage usage for which separate statistics need to be returned. All other chats will be grouped in entries with chat_id == 0. If the chat info database is not used, the ch... */
  chat_limit: number = 0;
}

/** Decode a TDLib `getStorageStatistics` object; null input -> null. */
export function decodeGetStorageStatistics(json: TdJsonRaw): GetStorageStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStorageStatistics = new GetStorageStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_limit = asNumber(record['chat_limit']);
  return out;
}

/** Encode a `getStorageStatistics` into TDLib JSON wire shape. */
export function encodeGetStorageStatistics(value: GetStorageStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStorageStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_limit'] = value.chat_limit;
  return out;
}

/** getStorageStatisticsFast = StorageStatisticsFast — Quickly returns approximate storage usage statistics. Can be called before authorization */
export class GetStorageStatisticsFast {
  type: 'getStorageStatisticsFast' = 'getStorageStatisticsFast';
  extra?: string;
}

/** Decode a TDLib `getStorageStatisticsFast` object; null input -> null. */
export function decodeGetStorageStatisticsFast(json: TdJsonRaw): GetStorageStatisticsFast | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStorageStatisticsFast = new GetStorageStatisticsFast();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getStorageStatisticsFast` into TDLib JSON wire shape. */
export function encodeGetStorageStatisticsFast(value: GetStorageStatisticsFast): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStorageStatisticsFast';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getStory = Story — Returns a story */
export class GetStory {
  type: 'getStory' = 'getStory';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
  /** only_local: Bool — Pass true to get only locally available information without sending network requests */
  only_local: boolean = false;
}

/** Decode a TDLib `getStory` object; null input -> null. */
export function decodeGetStory(json: TdJsonRaw): GetStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStory = new GetStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.only_local = asBool(record['only_local']);
  return out;
}

/** Encode a `getStory` into TDLib JSON wire shape. */
export function encodeGetStory(value: GetStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['only_local'] = value.only_local;
  return out;
}

/** getStoryAlbumStories = Stories — Returns the list of stories added to the given story album. For optimal performance, the number of returned stories is chosen by TDLib */
export class GetStoryAlbumStories {
  type: 'getStoryAlbumStories' = 'getStoryAlbumStories';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** story_album_id: int32 — Story album identifier */
  story_album_id: number = 0;
  /** offset: int32 — Offset of the first entry to return; use 0 to get results from the first album story */
  offset: number = 0;
  /** limit: int32 — The maximum number of stories to be returned. For optimal performance, the number of returned stories is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `getStoryAlbumStories` object; null input -> null. */
export function decodeGetStoryAlbumStories(json: TdJsonRaw): GetStoryAlbumStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStoryAlbumStories = new GetStoryAlbumStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_album_id = asNumber(record['story_album_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getStoryAlbumStories` into TDLib JSON wire shape. */
export function encodeGetStoryAlbumStories(value: GetStoryAlbumStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStoryAlbumStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_album_id'] = value.story_album_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getStoryAvailableReactions = AvailableReactions — Returns reactions, which can be chosen for a story */
export class GetStoryAvailableReactions {
  type: 'getStoryAvailableReactions' = 'getStoryAvailableReactions';
  extra?: string;
  /** row_size: int32 — Number of reaction per row, 5-25 */
  row_size: number = 0;
}

/** Decode a TDLib `getStoryAvailableReactions` object; null input -> null. */
export function decodeGetStoryAvailableReactions(json: TdJsonRaw): GetStoryAvailableReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStoryAvailableReactions = new GetStoryAvailableReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.row_size = asNumber(record['row_size']);
  return out;
}

/** Encode a `getStoryAvailableReactions` into TDLib JSON wire shape. */
export function encodeGetStoryAvailableReactions(value: GetStoryAvailableReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStoryAvailableReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['row_size'] = value.row_size;
  return out;
}

/** getStoryInteractions = StoryInteractions — Returns interactions with a story. The method can be called only for stories posted on behalf of the current user */
export class GetStoryInteractions {
  type: 'getStoryInteractions' = 'getStoryInteractions';
  extra?: string;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
  /** query: string — Query to search for in names, usernames and titles; may be empty to get all relevant interactions */
  query: string = '';
  /** only_contacts: Bool — Pass true to get only interactions by contacts; pass false to get all relevant interactions */
  only_contacts: boolean = false;
  /** prefer_forwards: Bool — Pass true to get forwards and reposts first, then reactions, then other views; pass false to get interactions sorted just by interaction date */
  prefer_forwards: boolean = false;
  /** prefer_with_reaction: Bool — Pass true to get interactions with reaction first; pass false to get interactions sorted just by interaction date. Ignored if prefer_forwards == true */
  prefer_with_reaction: boolean = false;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of story interactions to return */
  limit: number = 0;
}

/** Decode a TDLib `getStoryInteractions` object; null input -> null. */
export function decodeGetStoryInteractions(json: TdJsonRaw): GetStoryInteractions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStoryInteractions = new GetStoryInteractions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_id = asNumber(record['story_id']);
  out.query = asString(record['query']);
  out.only_contacts = asBool(record['only_contacts']);
  out.prefer_forwards = asBool(record['prefer_forwards']);
  out.prefer_with_reaction = asBool(record['prefer_with_reaction']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getStoryInteractions` into TDLib JSON wire shape. */
export function encodeGetStoryInteractions(value: GetStoryInteractions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStoryInteractions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_id'] = value.story_id;
  out['query'] = value.query;
  out['only_contacts'] = value.only_contacts;
  out['prefer_forwards'] = value.prefer_forwards;
  out['prefer_with_reaction'] = value.prefer_with_reaction;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getStoryNotificationSettingsExceptions = Chats — Returns the list of chats with non-default notification settings for stories */
export class GetStoryNotificationSettingsExceptions {
  type: 'getStoryNotificationSettingsExceptions' = 'getStoryNotificationSettingsExceptions';
  extra?: string;
}

/** Decode a TDLib `getStoryNotificationSettingsExceptions` object; null input -> null. */
export function decodeGetStoryNotificationSettingsExceptions(json: TdJsonRaw): GetStoryNotificationSettingsExceptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStoryNotificationSettingsExceptions = new GetStoryNotificationSettingsExceptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getStoryNotificationSettingsExceptions` into TDLib JSON wire shape. */
export function encodeGetStoryNotificationSettingsExceptions(value: GetStoryNotificationSettingsExceptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStoryNotificationSettingsExceptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getStoryPublicForwards = PublicForwards — Returns forwards of a story as a message to public chats and reposts by public channels. Can be used only if the story is posted on behalf of the current user or story.can_get_statistics == true.... */
export class GetStoryPublicForwards {
  type: 'getStoryPublicForwards' = 'getStoryPublicForwards';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the poster of the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — The identifier of the story */
  story_id: number = 0;
  /** offset: string — Offset of the first entry to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of messages and stories to be returned; must be positive and can't be greater than 100. For optimal performance, the number of returned objects is chosen by TDLib and can be smaller than the specified limit */
  limit: number = 0;
}

/** Decode a TDLib `getStoryPublicForwards` object; null input -> null. */
export function decodeGetStoryPublicForwards(json: TdJsonRaw): GetStoryPublicForwards | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStoryPublicForwards = new GetStoryPublicForwards();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getStoryPublicForwards` into TDLib JSON wire shape. */
export function encodeGetStoryPublicForwards(value: GetStoryPublicForwards): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStoryPublicForwards';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getStoryStatistics = StoryStatistics — Returns detailed statistics about a story. Can be used only if story.can_get_statistics == true */
export class GetStoryStatistics {
  type: 'getStoryStatistics' = 'getStoryStatistics';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
  /** is_dark: Bool — Pass true if a dark theme is used by the application */
  is_dark: boolean = false;
}

/** Decode a TDLib `getStoryStatistics` object; null input -> null. */
export function decodeGetStoryStatistics(json: TdJsonRaw): GetStoryStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetStoryStatistics = new GetStoryStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.is_dark = asBool(record['is_dark']);
  return out;
}

/** Encode a `getStoryStatistics` into TDLib JSON wire shape. */
export function encodeGetStoryStatistics(value: GetStoryStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getStoryStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_id'] = value.story_id;
  out['is_dark'] = value.is_dark;
  return out;
}

/** getSuggestedFileName = Text — Returns suggested name for saving a file in a given directory */
export class GetSuggestedFileName {
  type: 'getSuggestedFileName' = 'getSuggestedFileName';
  extra?: string;
  /** file_id: int32 — Identifier of the file */
  file_id: number = 0;
  /** directory: string — Directory in which the file is expected to be saved */
  directory: string = '';
}

/** Decode a TDLib `getSuggestedFileName` object; null input -> null. */
export function decodeGetSuggestedFileName(json: TdJsonRaw): GetSuggestedFileName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSuggestedFileName = new GetSuggestedFileName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.directory = asString(record['directory']);
  return out;
}

/** Encode a `getSuggestedFileName` into TDLib JSON wire shape. */
export function encodeGetSuggestedFileName(value: GetSuggestedFileName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSuggestedFileName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['directory'] = value.directory;
  return out;
}

/** getSuggestedStickerSetName = Text — Returns a suggested name for a new sticker set with a given title */
export class GetSuggestedStickerSetName {
  type: 'getSuggestedStickerSetName' = 'getSuggestedStickerSetName';
  extra?: string;
  /** title: string — Sticker set title; 1-64 characters */
  title: string = '';
}

/** Decode a TDLib `getSuggestedStickerSetName` object; null input -> null. */
export function decodeGetSuggestedStickerSetName(json: TdJsonRaw): GetSuggestedStickerSetName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSuggestedStickerSetName = new GetSuggestedStickerSetName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  return out;
}

/** Encode a `getSuggestedStickerSetName` into TDLib JSON wire shape. */
export function encodeGetSuggestedStickerSetName(value: GetSuggestedStickerSetName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSuggestedStickerSetName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  return out;
}

/** getSuitableDiscussionChats = Chats — Returns a list of basic group and supergroup chats, which can be used as a discussion group for a channel. Returned basic group chats must be first upgraded to supergroups before they can be set as a ... */
export class GetSuitableDiscussionChats {
  type: 'getSuitableDiscussionChats' = 'getSuitableDiscussionChats';
  extra?: string;
}

/** Decode a TDLib `getSuitableDiscussionChats` object; null input -> null. */
export function decodeGetSuitableDiscussionChats(json: TdJsonRaw): GetSuitableDiscussionChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSuitableDiscussionChats = new GetSuitableDiscussionChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getSuitableDiscussionChats` into TDLib JSON wire shape. */
export function encodeGetSuitableDiscussionChats(value: GetSuitableDiscussionChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSuitableDiscussionChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getSuitablePersonalChats = Chats — Returns a list of channel chats, which can be used as a personal chat */
export class GetSuitablePersonalChats {
  type: 'getSuitablePersonalChats' = 'getSuitablePersonalChats';
  extra?: string;
}

/** Decode a TDLib `getSuitablePersonalChats` object; null input -> null. */
export function decodeGetSuitablePersonalChats(json: TdJsonRaw): GetSuitablePersonalChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSuitablePersonalChats = new GetSuitablePersonalChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getSuitablePersonalChats` into TDLib JSON wire shape. */
export function encodeGetSuitablePersonalChats(value: GetSuitablePersonalChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSuitablePersonalChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getSupergroup = Supergroup — Returns information about a supergroup or a channel by its identifier. This is an offline method if the current user is not a bot */
export class GetSupergroup {
  type: 'getSupergroup' = 'getSupergroup';
  extra?: string;
  /** supergroup_id: int53 — Supergroup or channel identifier */
  supergroup_id: number = 0;
}

/** Decode a TDLib `getSupergroup` object; null input -> null. */
export function decodeGetSupergroup(json: TdJsonRaw): GetSupergroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSupergroup = new GetSupergroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  return out;
}

/** Encode a `getSupergroup` into TDLib JSON wire shape. */
export function encodeGetSupergroup(value: GetSupergroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSupergroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  return out;
}

/** getSupergroupFullInfo = SupergroupFullInfo — Returns full information about a supergroup or a channel by its identifier, cached for up to 1 minute */
export class GetSupergroupFullInfo {
  type: 'getSupergroupFullInfo' = 'getSupergroupFullInfo';
  extra?: string;
  /** supergroup_id: int53 — Supergroup or channel identifier */
  supergroup_id: number = 0;
}

/** Decode a TDLib `getSupergroupFullInfo` object; null input -> null. */
export function decodeGetSupergroupFullInfo(json: TdJsonRaw): GetSupergroupFullInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSupergroupFullInfo = new GetSupergroupFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  return out;
}

/** Encode a `getSupergroupFullInfo` into TDLib JSON wire shape. */
export function encodeGetSupergroupFullInfo(value: GetSupergroupFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSupergroupFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  return out;
}

/** getSupergroupMembers = ChatMembers — Returns information about members or banned users in a supergroup or channel. Can be used only if supergroupFullInfo.can_get_members == true; additionally, administrator privileges may be required for... */
export class GetSupergroupMembers {
  type: 'getSupergroupMembers' = 'getSupergroupMembers';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup or channel */
  supergroup_id: number = 0;
  /** filter: SupergroupMembersFilter — The type of users to return; pass null to use supergroupMembersFilterRecent */
  filter: TdSupergroupMembersFilter | null = null;
  /** offset: int32 — Number of users to skip */
  offset: number = 0;
  /** limit: int32 — The maximum number of users to be returned; up to 200 */
  limit: number = 0;
}

/** Decode a TDLib `getSupergroupMembers` object; null input -> null. */
export function decodeGetSupergroupMembers(json: TdJsonRaw): GetSupergroupMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSupergroupMembers = new GetSupergroupMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.filter = decodeTdSupergroupMembersFilter(record['filter']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getSupergroupMembers` into TDLib JSON wire shape. */
export function encodeGetSupergroupMembers(value: GetSupergroupMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSupergroupMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['filter'] = value.filter === null ? null : encodeTdSupergroupMembersFilter(value.filter);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getSupportName = Text — Returns localized name of the Telegram support user; for Telegram support only */
export class GetSupportName {
  type: 'getSupportName' = 'getSupportName';
  extra?: string;
}

/** Decode a TDLib `getSupportName` object; null input -> null. */
export function decodeGetSupportName(json: TdJsonRaw): GetSupportName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSupportName = new GetSupportName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getSupportName` into TDLib JSON wire shape. */
export function encodeGetSupportName(value: GetSupportName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSupportName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getSupportUser = User — Returns a user who can be contacted to get support */
export class GetSupportUser {
  type: 'getSupportUser' = 'getSupportUser';
  extra?: string;
}

/** Decode a TDLib `getSupportUser` object; null input -> null. */
export function decodeGetSupportUser(json: TdJsonRaw): GetSupportUser | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetSupportUser = new GetSupportUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getSupportUser` into TDLib JSON wire shape. */
export function encodeGetSupportUser(value: GetSupportUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getSupportUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getTemporaryPasswordState = TemporaryPasswordState — Returns information about the current temporary password */
export class GetTemporaryPasswordState {
  type: 'getTemporaryPasswordState' = 'getTemporaryPasswordState';
  extra?: string;
}

/** Decode a TDLib `getTemporaryPasswordState` object; null input -> null. */
export function decodeGetTemporaryPasswordState(json: TdJsonRaw): GetTemporaryPasswordState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetTemporaryPasswordState = new GetTemporaryPasswordState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getTemporaryPasswordState` into TDLib JSON wire shape. */
export function encodeGetTemporaryPasswordState(value: GetTemporaryPasswordState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getTemporaryPasswordState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getTextCompositionStyleExample = TextCompositionStyleExample — Returns an example of usage of a custom text composition style */
export class GetTextCompositionStyleExample {
  type: 'getTextCompositionStyleExample' = 'getTextCompositionStyleExample';
  extra?: string;
  /** name: string — Name of the style */
  name: string = '';
  /** example_number: int32 — 0-based unique number of the requested example; must be non-negative and less than getOption("text_composition_style_example_count") */
  example_number: number = 0;
}

/** Decode a TDLib `getTextCompositionStyleExample` object; null input -> null. */
export function decodeGetTextCompositionStyleExample(json: TdJsonRaw): GetTextCompositionStyleExample | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetTextCompositionStyleExample = new GetTextCompositionStyleExample();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.example_number = asNumber(record['example_number']);
  return out;
}

/** Encode a `getTextCompositionStyleExample` into TDLib JSON wire shape. */
export function encodeGetTextCompositionStyleExample(value: GetTextCompositionStyleExample): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getTextCompositionStyleExample';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['example_number'] = value.example_number;
  return out;
}

/** getTextEntities = TextEntities — Returns all entities (mentions, hashtags, cashtags, bot commands, bank card numbers, URLs, and email addresses) found in the text. Can be called synchronously */
export class GetTextEntities {
  type: 'getTextEntities' = 'getTextEntities';
  extra?: string;
  /** text: string — The text in which to look for entities */
  text: string = '';
}

/** Decode a TDLib `getTextEntities` object; null input -> null. */
export function decodeGetTextEntities(json: TdJsonRaw): GetTextEntities | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetTextEntities = new GetTextEntities();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  return out;
}

/** Encode a `getTextEntities` into TDLib JSON wire shape. */
export function encodeGetTextEntities(value: GetTextEntities): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getTextEntities';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  return out;
}

/** getThemeParametersJsonString = Text — Converts a themeParameters object to corresponding JSON-serialized string. Can be called synchronously */
export class GetThemeParametersJsonString {
  type: 'getThemeParametersJsonString' = 'getThemeParametersJsonString';
  extra?: string;
  /** theme: themeParameters — Theme parameters to convert to JSON */
  theme: ThemeParameters | null = null;
}

/** Decode a TDLib `getThemeParametersJsonString` object; null input -> null. */
export function decodeGetThemeParametersJsonString(json: TdJsonRaw): GetThemeParametersJsonString | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetThemeParametersJsonString = new GetThemeParametersJsonString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.theme = decodeThemeParameters(record['theme']);
  return out;
}

/** Encode a `getThemeParametersJsonString` into TDLib JSON wire shape. */
export function encodeGetThemeParametersJsonString(value: GetThemeParametersJsonString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getThemeParametersJsonString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['theme'] = value.theme === null ? null : encodeThemeParameters(value.theme);
  return out;
}

/** getThemedChatEmojiStatuses = EmojiStatusCustomEmojis — Returns up to 8 emoji statuses, which must be shown in the emoji status list for chats */
export class GetThemedChatEmojiStatuses {
  type: 'getThemedChatEmojiStatuses' = 'getThemedChatEmojiStatuses';
  extra?: string;
}

/** Decode a TDLib `getThemedChatEmojiStatuses` object; null input -> null. */
export function decodeGetThemedChatEmojiStatuses(json: TdJsonRaw): GetThemedChatEmojiStatuses | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetThemedChatEmojiStatuses = new GetThemedChatEmojiStatuses();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getThemedChatEmojiStatuses` into TDLib JSON wire shape. */
export function encodeGetThemedChatEmojiStatuses(value: GetThemedChatEmojiStatuses): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getThemedChatEmojiStatuses';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getThemedEmojiStatuses = EmojiStatusCustomEmojis — Returns up to 8 emoji statuses, which must be shown right after the default Premium Badge in the emoji status list for self status */
export class GetThemedEmojiStatuses {
  type: 'getThemedEmojiStatuses' = 'getThemedEmojiStatuses';
  extra?: string;
}

/** Decode a TDLib `getThemedEmojiStatuses` object; null input -> null. */
export function decodeGetThemedEmojiStatuses(json: TdJsonRaw): GetThemedEmojiStatuses | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetThemedEmojiStatuses = new GetThemedEmojiStatuses();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getThemedEmojiStatuses` into TDLib JSON wire shape. */
export function encodeGetThemedEmojiStatuses(value: GetThemedEmojiStatuses): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getThemedEmojiStatuses';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getTimeZones = TimeZones — Returns the list of supported time zones */
export class GetTimeZones {
  type: 'getTimeZones' = 'getTimeZones';
  extra?: string;
}

/** Decode a TDLib `getTimeZones` object; null input -> null. */
export function decodeGetTimeZones(json: TdJsonRaw): GetTimeZones | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetTimeZones = new GetTimeZones();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getTimeZones` into TDLib JSON wire shape. */
export function encodeGetTimeZones(value: GetTimeZones): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getTimeZones';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getTonTransactions = TonTransactions — Returns the list of TON blockchain transactions of the current user */
export class GetTonTransactions {
  type: 'getTonTransactions' = 'getTonTransactions';
  extra?: string;
  /** direction: TransactionDirection — Direction of the transactions to receive; pass null to get all transactions */
  direction: TdTransactionDirection | null = null;
  /** offset: string — Offset of the first transaction to return as received from the previous request; use empty string to get the first chunk of results */
  offset: string = '';
  /** limit: int32 — The maximum number of transactions to return */
  limit: number = 0;
}

/** Decode a TDLib `getTonTransactions` object; null input -> null. */
export function decodeGetTonTransactions(json: TdJsonRaw): GetTonTransactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetTonTransactions = new GetTonTransactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.direction = decodeTdTransactionDirection(record['direction']);
  out.offset = asString(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getTonTransactions` into TDLib JSON wire shape. */
export function encodeGetTonTransactions(value: GetTonTransactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getTonTransactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['direction'] = value.direction === null ? null : encodeTdTransactionDirection(value.direction);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getTopChats = Chats — Returns a list of frequently used chats */
export class GetTopChats {
  type: 'getTopChats' = 'getTopChats';
  extra?: string;
  /** category: TopChatCategory — Category of chats to be returned */
  category: TdTopChatCategory | null = null;
  /** limit: int32 — The maximum number of chats to be returned; up to 30 */
  limit: number = 0;
}

/** Decode a TDLib `getTopChats` object; null input -> null. */
export function decodeGetTopChats(json: TdJsonRaw): GetTopChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetTopChats = new GetTopChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.category = decodeTdTopChatCategory(record['category']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getTopChats` into TDLib JSON wire shape. */
export function encodeGetTopChats(value: GetTopChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getTopChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['category'] = value.category === null ? null : encodeTdTopChatCategory(value.category);
  out['limit'] = value.limit;
  return out;
}

/** getTrendingStickerSets = TrendingStickerSets — Returns a list of trending sticker sets. For optimal performance, the number of returned sticker sets is chosen by TDLib */
export class GetTrendingStickerSets {
  type: 'getTrendingStickerSets' = 'getTrendingStickerSets';
  extra?: string;
  /** sticker_type: StickerType — Type of the sticker sets to return */
  sticker_type: TdStickerType | null = null;
  /** offset: int32 — The offset from which to return the sticker sets; must be non-negative */
  offset: number = 0;
  /** limit: int32 — The maximum number of sticker sets to be returned; up to 100. For optimal performance, the number of returned sticker sets is chosen by TDLib and can be smaller than the specified limit, even if the end of the list has not... */
  limit: number = 0;
}

/** Decode a TDLib `getTrendingStickerSets` object; null input -> null. */
export function decodeGetTrendingStickerSets(json: TdJsonRaw): GetTrendingStickerSets | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetTrendingStickerSets = new GetTrendingStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getTrendingStickerSets` into TDLib JSON wire shape. */
export function encodeGetTrendingStickerSets(value: GetTrendingStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getTrendingStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getUpgradedGift = UpgradedGift — Returns information about an upgraded gift by its name */
export class GetUpgradedGift {
  type: 'getUpgradedGift' = 'getUpgradedGift';
  extra?: string;
  /** name: string — Unique name of the upgraded gift */
  name: string = '';
}

/** Decode a TDLib `getUpgradedGift` object; null input -> null. */
export function decodeGetUpgradedGift(json: TdJsonRaw): GetUpgradedGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUpgradedGift = new GetUpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `getUpgradedGift` into TDLib JSON wire shape. */
export function encodeGetUpgradedGift(value: GetUpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUpgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** getUpgradedGiftEmojiStatuses = EmojiStatuses — Returns available upgraded gift emoji statuses for self status */
export class GetUpgradedGiftEmojiStatuses {
  type: 'getUpgradedGiftEmojiStatuses' = 'getUpgradedGiftEmojiStatuses';
  extra?: string;
}

/** Decode a TDLib `getUpgradedGiftEmojiStatuses` object; null input -> null. */
export function decodeGetUpgradedGiftEmojiStatuses(json: TdJsonRaw): GetUpgradedGiftEmojiStatuses | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUpgradedGiftEmojiStatuses = new GetUpgradedGiftEmojiStatuses();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getUpgradedGiftEmojiStatuses` into TDLib JSON wire shape. */
export function encodeGetUpgradedGiftEmojiStatuses(value: GetUpgradedGiftEmojiStatuses): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUpgradedGiftEmojiStatuses';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getUpgradedGiftValueInfo = UpgradedGiftValueInfo — Returns information about value of an upgraded gift by its name */
export class GetUpgradedGiftValueInfo {
  type: 'getUpgradedGiftValueInfo' = 'getUpgradedGiftValueInfo';
  extra?: string;
  /** name: string — Unique name of the upgraded gift */
  name: string = '';
}

/** Decode a TDLib `getUpgradedGiftValueInfo` object; null input -> null. */
export function decodeGetUpgradedGiftValueInfo(json: TdJsonRaw): GetUpgradedGiftValueInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUpgradedGiftValueInfo = new GetUpgradedGiftValueInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `getUpgradedGiftValueInfo` into TDLib JSON wire shape. */
export function encodeGetUpgradedGiftValueInfo(value: GetUpgradedGiftValueInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUpgradedGiftValueInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** getUpgradedGiftVariants = GiftUpgradeVariants — Returns all possible variants of upgraded gifts for a regular gift */
export class GetUpgradedGiftVariants {
  type: 'getUpgradedGiftVariants' = 'getUpgradedGiftVariants';
  extra?: string;
  /** regular_gift_id: int64 — Identifier of the regular gift */
  regular_gift_id: string = '';
  /** return_upgrade_models: Bool — Pass true to get models that can be obtained by upgrading a regular gift */
  return_upgrade_models: boolean = false;
  /** return_craft_models: Bool — Pass true to get models that can be obtained by crafting a gift from upgraded gifts */
  return_craft_models: boolean = false;
}

/** Decode a TDLib `getUpgradedGiftVariants` object; null input -> null. */
export function decodeGetUpgradedGiftVariants(json: TdJsonRaw): GetUpgradedGiftVariants | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUpgradedGiftVariants = new GetUpgradedGiftVariants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.regular_gift_id = asInt64(record['regular_gift_id']);
  out.return_upgrade_models = asBool(record['return_upgrade_models']);
  out.return_craft_models = asBool(record['return_craft_models']);
  return out;
}

/** Encode a `getUpgradedGiftVariants` into TDLib JSON wire shape. */
export function encodeGetUpgradedGiftVariants(value: GetUpgradedGiftVariants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUpgradedGiftVariants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['regular_gift_id'] = value.regular_gift_id;
  out['return_upgrade_models'] = value.return_upgrade_models;
  out['return_craft_models'] = value.return_craft_models;
  return out;
}

/** getUpgradedGiftWithdrawalUrl = HttpUrl — Returns a URL for upgraded gift withdrawal in the TON blockchain as an NFT; requires owner privileges for gifts owned by a chat */
export class GetUpgradedGiftWithdrawalUrl {
  type: 'getUpgradedGiftWithdrawalUrl' = 'getUpgradedGiftWithdrawalUrl';
  extra?: string;
  /** received_gift_id: string — Identifier of the gift */
  received_gift_id: string = '';
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
}

/** Decode a TDLib `getUpgradedGiftWithdrawalUrl` object; null input -> null. */
export function decodeGetUpgradedGiftWithdrawalUrl(json: TdJsonRaw): GetUpgradedGiftWithdrawalUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUpgradedGiftWithdrawalUrl = new GetUpgradedGiftWithdrawalUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.received_gift_id = asString(record['received_gift_id']);
  out.password = asString(record['password']);
  return out;
}

/** Encode a `getUpgradedGiftWithdrawalUrl` into TDLib JSON wire shape. */
export function encodeGetUpgradedGiftWithdrawalUrl(value: GetUpgradedGiftWithdrawalUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUpgradedGiftWithdrawalUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['received_gift_id'] = value.received_gift_id;
  out['password'] = value.password;
  return out;
}

/** getUpgradedGiftsPromotionalAnimation = Animation — Returns promotional animation for upgraded gifts */
export class GetUpgradedGiftsPromotionalAnimation {
  type: 'getUpgradedGiftsPromotionalAnimation' = 'getUpgradedGiftsPromotionalAnimation';
  extra?: string;
}

/** Decode a TDLib `getUpgradedGiftsPromotionalAnimation` object; null input -> null. */
export function decodeGetUpgradedGiftsPromotionalAnimation(json: TdJsonRaw): GetUpgradedGiftsPromotionalAnimation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUpgradedGiftsPromotionalAnimation = new GetUpgradedGiftsPromotionalAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getUpgradedGiftsPromotionalAnimation` into TDLib JSON wire shape. */
export function encodeGetUpgradedGiftsPromotionalAnimation(value: GetUpgradedGiftsPromotionalAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUpgradedGiftsPromotionalAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getUser = User — Returns information about a user by their identifier. This is an offline method if the current user is not a bot */
export class GetUser {
  type: 'getUser' = 'getUser';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
}

/** Decode a TDLib `getUser` object; null input -> null. */
export function decodeGetUser(json: TdJsonRaw): GetUser | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUser = new GetUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `getUser` into TDLib JSON wire shape. */
export function encodeGetUser(value: GetUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** getUserChatBoosts = FoundChatBoosts — Returns the list of boosts applied to a chat by a given user; requires administrator rights in the chat; for bots only */
export class GetUserChatBoosts {
  type: 'getUserChatBoosts' = 'getUserChatBoosts';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
}

/** Decode a TDLib `getUserChatBoosts` object; null input -> null. */
export function decodeGetUserChatBoosts(json: TdJsonRaw): GetUserChatBoosts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUserChatBoosts = new GetUserChatBoosts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `getUserChatBoosts` into TDLib JSON wire shape. */
export function encodeGetUserChatBoosts(value: GetUserChatBoosts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUserChatBoosts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['user_id'] = value.user_id;
  return out;
}

/** getUserFullInfo = UserFullInfo — Returns full information about a user by their identifier */
export class GetUserFullInfo {
  type: 'getUserFullInfo' = 'getUserFullInfo';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
}

/** Decode a TDLib `getUserFullInfo` object; null input -> null. */
export function decodeGetUserFullInfo(json: TdJsonRaw): GetUserFullInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUserFullInfo = new GetUserFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `getUserFullInfo` into TDLib JSON wire shape. */
export function encodeGetUserFullInfo(value: GetUserFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUserFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** getUserLink = UserLink — Returns an HTTPS link, which can be used to get information about the current user */
export class GetUserLink {
  type: 'getUserLink' = 'getUserLink';
  extra?: string;
}

/** Decode a TDLib `getUserLink` object; null input -> null. */
export function decodeGetUserLink(json: TdJsonRaw): GetUserLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUserLink = new GetUserLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `getUserLink` into TDLib JSON wire shape. */
export function encodeGetUserLink(value: GetUserLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUserLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** getUserPrivacySettingRules = UserPrivacySettingRules — Returns the current privacy settings */
export class GetUserPrivacySettingRules {
  type: 'getUserPrivacySettingRules' = 'getUserPrivacySettingRules';
  extra?: string;
  /** setting: UserPrivacySetting — The privacy setting */
  setting: TdUserPrivacySetting | null = null;
}

/** Decode a TDLib `getUserPrivacySettingRules` object; null input -> null. */
export function decodeGetUserPrivacySettingRules(json: TdJsonRaw): GetUserPrivacySettingRules | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUserPrivacySettingRules = new GetUserPrivacySettingRules();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.setting = decodeTdUserPrivacySetting(record['setting']);
  return out;
}

/** Encode a `getUserPrivacySettingRules` into TDLib JSON wire shape. */
export function encodeGetUserPrivacySettingRules(value: GetUserPrivacySettingRules): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUserPrivacySettingRules';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['setting'] = value.setting === null ? null : encodeTdUserPrivacySetting(value.setting);
  return out;
}

/** getUserProfileAudios = Audios — Returns the list of profile audio files of a user */
export class GetUserProfileAudios {
  type: 'getUserProfileAudios' = 'getUserProfileAudios';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** offset: int32 — The number of audio files to skip; must be non-negative */
  offset: number = 0;
  /** limit: int32 — The maximum number of audio files to be returned; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `getUserProfileAudios` object; null input -> null. */
export function decodeGetUserProfileAudios(json: TdJsonRaw): GetUserProfileAudios | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUserProfileAudios = new GetUserProfileAudios();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getUserProfileAudios` into TDLib JSON wire shape. */
export function encodeGetUserProfileAudios(value: GetUserProfileAudios): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUserProfileAudios';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getUserProfilePhotos = ChatPhotos — Returns the profile photos of a user. Personal and public photo aren't returned */
export class GetUserProfilePhotos {
  type: 'getUserProfilePhotos' = 'getUserProfilePhotos';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** offset: int32 — The number of photos to skip; must be non-negative */
  offset: number = 0;
  /** limit: int32 — The maximum number of photos to be returned; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `getUserProfilePhotos` object; null input -> null. */
export function decodeGetUserProfilePhotos(json: TdJsonRaw): GetUserProfilePhotos | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUserProfilePhotos = new GetUserProfilePhotos();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.offset = asNumber(record['offset']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `getUserProfilePhotos` into TDLib JSON wire shape. */
export function encodeGetUserProfilePhotos(value: GetUserProfilePhotos): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUserProfilePhotos';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['offset'] = value.offset;
  out['limit'] = value.limit;
  return out;
}

/** getUserSupportInfo = UserSupportInfo — Returns support information for the given user; for Telegram support only */
export class GetUserSupportInfo {
  type: 'getUserSupportInfo' = 'getUserSupportInfo';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
}

/** Decode a TDLib `getUserSupportInfo` object; null input -> null. */
export function decodeGetUserSupportInfo(json: TdJsonRaw): GetUserSupportInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetUserSupportInfo = new GetUserSupportInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `getUserSupportInfo` into TDLib JSON wire shape. */
export function encodeGetUserSupportInfo(value: GetUserSupportInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getUserSupportInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** getVideoChatAvailableParticipants = MessageSenders — Returns the list of participant identifiers, on whose behalf a video chat in the chat can be joined */
export class GetVideoChatAvailableParticipants {
  type: 'getVideoChatAvailableParticipants' = 'getVideoChatAvailableParticipants';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getVideoChatAvailableParticipants` object; null input -> null. */
export function decodeGetVideoChatAvailableParticipants(json: TdJsonRaw): GetVideoChatAvailableParticipants | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetVideoChatAvailableParticipants = new GetVideoChatAvailableParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getVideoChatAvailableParticipants` into TDLib JSON wire shape. */
export function encodeGetVideoChatAvailableParticipants(value: GetVideoChatAvailableParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getVideoChatAvailableParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getVideoChatInviteLink = HttpUrl — Returns invite link to a video chat in a public chat */
export class GetVideoChatInviteLink {
  type: 'getVideoChatInviteLink' = 'getVideoChatInviteLink';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** can_self_unmute: Bool — Pass true if the invite link needs to contain an invite hash, passing which to joinVideoChat would allow the invited user to unmute themselves. Requires groupCall.can_be_managed right */
  can_self_unmute: boolean = false;
}

/** Decode a TDLib `getVideoChatInviteLink` object; null input -> null. */
export function decodeGetVideoChatInviteLink(json: TdJsonRaw): GetVideoChatInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetVideoChatInviteLink = new GetVideoChatInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.can_self_unmute = asBool(record['can_self_unmute']);
  return out;
}

/** Encode a `getVideoChatInviteLink` into TDLib JSON wire shape. */
export function encodeGetVideoChatInviteLink(value: GetVideoChatInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getVideoChatInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['can_self_unmute'] = value.can_self_unmute;
  return out;
}

/** getVideoChatRtmpUrl = RtmpUrl — Returns RTMP URL for streaming to the video chat of a chat; requires can_manage_video_chats administrator right */
export class GetVideoChatRtmpUrl {
  type: 'getVideoChatRtmpUrl' = 'getVideoChatRtmpUrl';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `getVideoChatRtmpUrl` object; null input -> null. */
export function decodeGetVideoChatRtmpUrl(json: TdJsonRaw): GetVideoChatRtmpUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetVideoChatRtmpUrl = new GetVideoChatRtmpUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `getVideoChatRtmpUrl` into TDLib JSON wire shape. */
export function encodeGetVideoChatRtmpUrl(value: GetVideoChatRtmpUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getVideoChatRtmpUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** getVideoMessageAdvertisements = VideoMessageAdvertisements — Returns advertisements to be shown while a video from a message is watched. Available only if messageProperties.can_get_video_advertisements */
export class GetVideoMessageAdvertisements {
  type: 'getVideoMessageAdvertisements' = 'getVideoMessageAdvertisements';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `getVideoMessageAdvertisements` object; null input -> null. */
export function decodeGetVideoMessageAdvertisements(json: TdJsonRaw): GetVideoMessageAdvertisements | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetVideoMessageAdvertisements = new GetVideoMessageAdvertisements();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `getVideoMessageAdvertisements` into TDLib JSON wire shape. */
export function encodeGetVideoMessageAdvertisements(value: GetVideoMessageAdvertisements): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getVideoMessageAdvertisements';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** getWebAppLinkUrl = WebAppUrl — Returns an HTTPS URL of a Web App to open after a link of the type internalLinkTypeWebApp is clicked */
export class GetWebAppLinkUrl {
  type: 'getWebAppLinkUrl' = 'getWebAppLinkUrl';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in which the link was clicked; pass 0 if none */
  chat_id: number = 0;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** web_app_short_name: string — Short name of the Web App */
  web_app_short_name: string = '';
  /** start_parameter: string — Start parameter from internalLinkTypeWebApp */
  start_parameter: string = '';
  /** allow_write_access: Bool — Pass true if the current user allowed the bot to send them messages */
  allow_write_access: boolean = false;
  /** parameters: webAppOpenParameters — Parameters to use to open the Web App */
  parameters: WebAppOpenParameters | null = null;
}

/** Decode a TDLib `getWebAppLinkUrl` object; null input -> null. */
export function decodeGetWebAppLinkUrl(json: TdJsonRaw): GetWebAppLinkUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetWebAppLinkUrl = new GetWebAppLinkUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.web_app_short_name = asString(record['web_app_short_name']);
  out.start_parameter = asString(record['start_parameter']);
  out.allow_write_access = asBool(record['allow_write_access']);
  out.parameters = decodeWebAppOpenParameters(record['parameters']);
  return out;
}

/** Encode a `getWebAppLinkUrl` into TDLib JSON wire shape. */
export function encodeGetWebAppLinkUrl(value: GetWebAppLinkUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getWebAppLinkUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['bot_user_id'] = value.bot_user_id;
  out['web_app_short_name'] = value.web_app_short_name;
  out['start_parameter'] = value.start_parameter;
  out['allow_write_access'] = value.allow_write_access;
  out['parameters'] = value.parameters === null ? null : encodeWebAppOpenParameters(value.parameters);
  return out;
}

/** getWebAppPlaceholder = Outline — Returns a default placeholder for Web Apps of a bot. This is an offline method. Returns a 404 error if the placeholder isn't known */
export class GetWebAppPlaceholder {
  type: 'getWebAppPlaceholder' = 'getWebAppPlaceholder';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
}

/** Decode a TDLib `getWebAppPlaceholder` object; null input -> null. */
export function decodeGetWebAppPlaceholder(json: TdJsonRaw): GetWebAppPlaceholder | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetWebAppPlaceholder = new GetWebAppPlaceholder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `getWebAppPlaceholder` into TDLib JSON wire shape. */
export function encodeGetWebAppPlaceholder(value: GetWebAppPlaceholder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getWebAppPlaceholder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** getWebAppUrl = WebAppUrl — Returns an HTTPS URL of a Web App to open from the side menu, a keyboardButtonTypeWebApp button, or an inlineQueryResultsButtonTypeWebApp button */
export class GetWebAppUrl {
  type: 'getWebAppUrl' = 'getWebAppUrl';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot. If the bot is restricted for the current user, then show an error instead of calling the method */
  bot_user_id: number = 0;
  /** url: string — The URL from a keyboardButtonTypeWebApp button, inlineQueryResultsButtonTypeWebApp button, or an empty string when the bot is opened from the side menu */
  url: string = '';
  /** parameters: webAppOpenParameters — Parameters to use to open the Web App */
  parameters: WebAppOpenParameters | null = null;
}

/** Decode a TDLib `getWebAppUrl` object; null input -> null. */
export function decodeGetWebAppUrl(json: TdJsonRaw): GetWebAppUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetWebAppUrl = new GetWebAppUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.url = asString(record['url']);
  out.parameters = decodeWebAppOpenParameters(record['parameters']);
  return out;
}

/** Encode a `getWebAppUrl` into TDLib JSON wire shape. */
export function encodeGetWebAppUrl(value: GetWebAppUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getWebAppUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['url'] = value.url;
  out['parameters'] = value.parameters === null ? null : encodeWebAppOpenParameters(value.parameters);
  return out;
}

/** getWebPageInstantView = WebPageInstantView — Returns an instant view version of a web page if available. This is an offline method if only_local is true. Returns a 404 error if the web page has no instant view page */
export class GetWebPageInstantView {
  type: 'getWebPageInstantView' = 'getWebPageInstantView';
  extra?: string;
  /** url: string — The web page URL */
  url: string = '';
  /** only_local: Bool — Pass true to get only locally available information without sending network requests */
  only_local: boolean = false;
}

/** Decode a TDLib `getWebPageInstantView` object; null input -> null. */
export function decodeGetWebPageInstantView(json: TdJsonRaw): GetWebPageInstantView | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GetWebPageInstantView = new GetWebPageInstantView();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.only_local = asBool(record['only_local']);
  return out;
}

/** Encode a `getWebPageInstantView` into TDLib JSON wire shape. */
export function encodeGetWebPageInstantView(value: GetWebPageInstantView): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'getWebPageInstantView';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['only_local'] = value.only_local;
  return out;
}

/** gift = Gift — Describes a gift that can be sent to another user or channel chat If the date is in the future, then call canSendGift to get the reason, why the gift can't be sent now */
export class Gift {
  type: 'gift' = 'gift';
  extra?: string;
  /** id: int64 — Unique identifier of the gift */
  id: string = '';
  /** publisher_chat_id: int53 — Identifier of the chat that published the gift; 0 if none */
  publisher_chat_id: number = 0;
  /** sticker: sticker — The sticker representing the gift */
  sticker: Sticker | null = null;
  /** star_count: int53 — Number of Telegram Stars that must be paid for the gift */
  star_count: number = 0;
  /** default_sell_star_count: int53 — Number of Telegram Stars that can be claimed by the receiver instead of the regular gift by default. If the gift was paid with just bought Telegram Stars, then full value can be claimed */
  default_sell_star_count: number = 0;
  /** upgrade_star_count: int53 — Number of Telegram Stars that must be paid to upgrade the gift; 0 if upgrade isn't possible */
  upgrade_star_count: number = 0;
  /** upgrade_variant_count: int32 — Number of unique gift variants that are available for the upgraded gift; 0 if unknown */
  upgrade_variant_count: number = 0;
  /** has_colors: Bool — True, if the gift can be used to customize the user's name, and backgrounds of profile photo, reply header, and link preview */
  has_colors: boolean = false;
  /** is_for_birthday: Bool — True, if the gift is a birthday gift */
  is_for_birthday: boolean = false;
  /** is_premium: Bool — True, if the gift can be bought only by Telegram Premium subscribers */
  is_premium: boolean = false;
  /** auction_info: giftAuction — Information about the auction on which the gift can be purchased; may be null if the gift can be purchased directly */
  auction_info: GiftAuction | null = null;
  /** next_send_date: int32 — Point in time (Unix timestamp) when the gift can be sent next time by the current user; may be 0 or a date in the past. */
  next_send_date: number = 0;
  /** user_limits: giftPurchaseLimits — Number of times the gift can be purchased by the current user; may be null if not limited */
  user_limits: GiftPurchaseLimits | null = null;
  /** overall_limits: giftPurchaseLimits — Number of times the gift can be purchased by all users; may be null if not limited */
  overall_limits: GiftPurchaseLimits | null = null;
  /** background: giftBackground — Background of the gift */
  background: GiftBackground | null = null;
  /** first_send_date: int32 — Point in time (Unix timestamp) when the gift was sent for the first time; for sold out gifts only */
  first_send_date: number = 0;
  /** last_send_date: int32 — Point in time (Unix timestamp) when the gift was sent for the last time; for sold out gifts only */
  last_send_date: number = 0;
}

/** Decode a TDLib `gift` object; null input -> null. */
export function decodeGift(json: TdJsonRaw): Gift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Gift = new Gift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.publisher_chat_id = asNumber(record['publisher_chat_id']);
  out.sticker = decodeSticker(record['sticker']);
  out.star_count = asNumber(record['star_count']);
  out.default_sell_star_count = asNumber(record['default_sell_star_count']);
  out.upgrade_star_count = asNumber(record['upgrade_star_count']);
  out.upgrade_variant_count = asNumber(record['upgrade_variant_count']);
  out.has_colors = asBool(record['has_colors']);
  out.is_for_birthday = asBool(record['is_for_birthday']);
  out.is_premium = asBool(record['is_premium']);
  out.auction_info = decodeGiftAuction(record['auction_info']);
  out.next_send_date = asNumber(record['next_send_date']);
  out.user_limits = decodeGiftPurchaseLimits(record['user_limits']);
  out.overall_limits = decodeGiftPurchaseLimits(record['overall_limits']);
  out.background = decodeGiftBackground(record['background']);
  out.first_send_date = asNumber(record['first_send_date']);
  out.last_send_date = asNumber(record['last_send_date']);
  return out;
}

/** Encode a `gift` into TDLib JSON wire shape. */
export function encodeGift(value: Gift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'gift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['publisher_chat_id'] = value.publisher_chat_id;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  out['star_count'] = value.star_count;
  out['default_sell_star_count'] = value.default_sell_star_count;
  out['upgrade_star_count'] = value.upgrade_star_count;
  out['upgrade_variant_count'] = value.upgrade_variant_count;
  out['has_colors'] = value.has_colors;
  out['is_for_birthday'] = value.is_for_birthday;
  out['is_premium'] = value.is_premium;
  out['auction_info'] = value.auction_info === null ? null : encodeGiftAuction(value.auction_info);
  out['next_send_date'] = value.next_send_date;
  out['user_limits'] = value.user_limits === null ? null : encodeGiftPurchaseLimits(value.user_limits);
  out['overall_limits'] = value.overall_limits === null ? null : encodeGiftPurchaseLimits(value.overall_limits);
  out['background'] = value.background === null ? null : encodeGiftBackground(value.background);
  out['first_send_date'] = value.first_send_date;
  out['last_send_date'] = value.last_send_date;
  return out;
}

/** giftAuction = GiftAuction — Describes an auction on which a gift can be purchased */
export class GiftAuction {
  type: 'giftAuction' = 'giftAuction';
  extra?: string;
  /** id: string — Identifier of the auction */
  id: string = '';
  /** gifts_per_round: int32 — Number of gifts distributed in each round */
  gifts_per_round: number = 0;
  /** start_date: int32 — Point in time (Unix timestamp) when the auction will start */
  start_date: number = 0;
}

/** Decode a TDLib `giftAuction` object; null input -> null. */
export function decodeGiftAuction(json: TdJsonRaw): GiftAuction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftAuction = new GiftAuction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.gifts_per_round = asNumber(record['gifts_per_round']);
  out.start_date = asNumber(record['start_date']);
  return out;
}

/** Encode a `giftAuction` into TDLib JSON wire shape. */
export function encodeGiftAuction(value: GiftAuction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftAuction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['gifts_per_round'] = value.gifts_per_round;
  out['start_date'] = value.start_date;
  return out;
}

/** giftAuctionAcquiredGift = GiftAuctionAcquiredGift — Represents a gift that was acquired by the current user on an auction */
export class GiftAuctionAcquiredGift {
  type: 'giftAuctionAcquiredGift' = 'giftAuctionAcquiredGift';
  extra?: string;
  /** receiver_id: MessageSender — Receiver of the gift */
  receiver_id: TdMessageSender | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the gift was acquired */
  date: number = 0;
  /** star_count: int53 — The number of Telegram Stars that were paid for the gift */
  star_count: number = 0;
  /** auction_round_number: int32 — Identifier of the auction round in which the gift was acquired */
  auction_round_number: number = 0;
  /** auction_round_position: int32 — Position of the user in the round among all auction participants */
  auction_round_position: number = 0;
  /** unique_gift_number: int32 — Unique number of the gift among gifts upgraded from the same gift after upgrade; 0 if yet unassigned */
  unique_gift_number: number = 0;
  /** text: formattedText — Message added to the gift */
  text: FormattedText | null = null;
  /** is_private: Bool — True, if the sender and gift text are shown only to the gift receiver; otherwise, everyone will be able to see them */
  is_private: boolean = false;
}

/** Decode a TDLib `giftAuctionAcquiredGift` object; null input -> null. */
export function decodeGiftAuctionAcquiredGift(json: TdJsonRaw): GiftAuctionAcquiredGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftAuctionAcquiredGift = new GiftAuctionAcquiredGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.receiver_id = decodeTdMessageSender(record['receiver_id']);
  out.date = asNumber(record['date']);
  out.star_count = asNumber(record['star_count']);
  out.auction_round_number = asNumber(record['auction_round_number']);
  out.auction_round_position = asNumber(record['auction_round_position']);
  out.unique_gift_number = asNumber(record['unique_gift_number']);
  out.text = decodeFormattedText(record['text']);
  out.is_private = asBool(record['is_private']);
  return out;
}

/** Encode a `giftAuctionAcquiredGift` into TDLib JSON wire shape. */
export function encodeGiftAuctionAcquiredGift(value: GiftAuctionAcquiredGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftAuctionAcquiredGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['receiver_id'] = value.receiver_id === null ? null : encodeTdMessageSender(value.receiver_id);
  out['date'] = value.date;
  out['star_count'] = value.star_count;
  out['auction_round_number'] = value.auction_round_number;
  out['auction_round_position'] = value.auction_round_position;
  out['unique_gift_number'] = value.unique_gift_number;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['is_private'] = value.is_private;
  return out;
}

/** giftAuctionAcquiredGifts = GiftAuctionAcquiredGifts — Represents a list of gifts that were acquired by the current user on an auction */
export class GiftAuctionAcquiredGifts {
  type: 'giftAuctionAcquiredGifts' = 'giftAuctionAcquiredGifts';
  extra?: string;
  /** gifts: vector<giftAuctionAcquiredGift> — The list of acquired gifts */
  gifts: GiftAuctionAcquiredGift[] = [];
}

/** Decode a TDLib `giftAuctionAcquiredGifts` object; null input -> null. */
export function decodeGiftAuctionAcquiredGifts(json: TdJsonRaw): GiftAuctionAcquiredGifts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftAuctionAcquiredGifts = new GiftAuctionAcquiredGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gifts = decodeVector(record['gifts'], (item: TdJsonRaw): GiftAuctionAcquiredGift => decodeGiftAuctionAcquiredGift(item) as GiftAuctionAcquiredGift);
  return out;
}

/** Encode a `giftAuctionAcquiredGifts` into TDLib JSON wire shape. */
export function encodeGiftAuctionAcquiredGifts(value: GiftAuctionAcquiredGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftAuctionAcquiredGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gifts'] = encodeVector(value.gifts, (item: GiftAuctionAcquiredGift | null): TdJsonRaw => item === null ? null : encodeGiftAuctionAcquiredGift(item));
  return out;
}

/** giftAuctionState = GiftAuctionState — Represent auction state of a gift */
export class GiftAuctionState {
  type: 'giftAuctionState' = 'giftAuctionState';
  extra?: string;
  /** gift: gift — The gift */
  gift: Gift | null = null;
  /** state: AuctionState — Auction state of the gift */
  state: TdAuctionState | null = null;
}

/** Decode a TDLib `giftAuctionState` object; null input -> null. */
export function decodeGiftAuctionState(json: TdJsonRaw): GiftAuctionState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftAuctionState = new GiftAuctionState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeGift(record['gift']);
  out.state = decodeTdAuctionState(record['state']);
  return out;
}

/** Encode a `giftAuctionState` into TDLib JSON wire shape. */
export function encodeGiftAuctionState(value: GiftAuctionState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftAuctionState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  out['state'] = value.state === null ? null : encodeTdAuctionState(value.state);
  return out;
}

/** giftBackground = GiftBackground — Describes background of a gift */
export class GiftBackground {
  type: 'giftBackground' = 'giftBackground';
  extra?: string;
  /** center_color: int32 — Center color in RGB format */
  center_color: number = 0;
  /** edge_color: int32 — Edge color in RGB format */
  edge_color: number = 0;
  /** text_color: int32 — Text color in RGB format */
  text_color: number = 0;
}

/** Decode a TDLib `giftBackground` object; null input -> null. */
export function decodeGiftBackground(json: TdJsonRaw): GiftBackground | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftBackground = new GiftBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.center_color = asNumber(record['center_color']);
  out.edge_color = asNumber(record['edge_color']);
  out.text_color = asNumber(record['text_color']);
  return out;
}

/** Encode a `giftBackground` into TDLib JSON wire shape. */
export function encodeGiftBackground(value: GiftBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['center_color'] = value.center_color;
  out['edge_color'] = value.edge_color;
  out['text_color'] = value.text_color;
  return out;
}

/** giftChatTheme = GiftChatTheme — Describes a chat theme based on an upgraded gift */
export class GiftChatTheme {
  type: 'giftChatTheme' = 'giftChatTheme';
  extra?: string;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
  /** light_settings: themeSettings — Theme settings for a light chat theme */
  light_settings: ThemeSettings | null = null;
  /** dark_settings: themeSettings — Theme settings for a dark chat theme */
  dark_settings: ThemeSettings | null = null;
}

/** Decode a TDLib `giftChatTheme` object; null input -> null. */
export function decodeGiftChatTheme(json: TdJsonRaw): GiftChatTheme | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftChatTheme = new GiftChatTheme();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  out.light_settings = decodeThemeSettings(record['light_settings']);
  out.dark_settings = decodeThemeSettings(record['dark_settings']);
  return out;
}

/** Encode a `giftChatTheme` into TDLib JSON wire shape. */
export function encodeGiftChatTheme(value: GiftChatTheme): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftChatTheme';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  out['light_settings'] = value.light_settings === null ? null : encodeThemeSettings(value.light_settings);
  out['dark_settings'] = value.dark_settings === null ? null : encodeThemeSettings(value.dark_settings);
  return out;
}

/** giftChatThemes = GiftChatThemes — Contains a list of chat themes based on upgraded gifts */
export class GiftChatThemes {
  type: 'giftChatThemes' = 'giftChatThemes';
  extra?: string;
  /** themes: vector<giftChatTheme> — A list of chat themes */
  themes: GiftChatTheme[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `giftChatThemes` object; null input -> null. */
export function decodeGiftChatThemes(json: TdJsonRaw): GiftChatThemes | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftChatThemes = new GiftChatThemes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.themes = decodeVector(record['themes'], (item: TdJsonRaw): GiftChatTheme => decodeGiftChatTheme(item) as GiftChatTheme);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `giftChatThemes` into TDLib JSON wire shape. */
export function encodeGiftChatThemes(value: GiftChatThemes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftChatThemes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['themes'] = encodeVector(value.themes, (item: GiftChatTheme | null): TdJsonRaw => item === null ? null : encodeGiftChatTheme(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** giftCollection = GiftCollection — Describes collection of gifts */
export class GiftCollection {
  type: 'giftCollection' = 'giftCollection';
  extra?: string;
  /** id: int32 — Unique identifier of the collection */
  id: number = 0;
  /** name: string — Name of the collection */
  name: string = '';
  /** icon: sticker — Icon of the collection; may be null if none */
  icon: Sticker | null = null;
  /** gift_count: int32 — Total number of gifts in the collection */
  gift_count: number = 0;
}

/** Decode a TDLib `giftCollection` object; null input -> null. */
export function decodeGiftCollection(json: TdJsonRaw): GiftCollection | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftCollection = new GiftCollection();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.name = asString(record['name']);
  out.icon = decodeSticker(record['icon']);
  out.gift_count = asNumber(record['gift_count']);
  return out;
}

/** Encode a `giftCollection` into TDLib JSON wire shape. */
export function encodeGiftCollection(value: GiftCollection): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftCollection';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['name'] = value.name;
  out['icon'] = value.icon === null ? null : encodeSticker(value.icon);
  out['gift_count'] = value.gift_count;
  return out;
}

/** giftCollections = GiftCollections — Contains a list of gift collections */
export class GiftCollections {
  type: 'giftCollections' = 'giftCollections';
  extra?: string;
  /** collections: vector<giftCollection> — List of gift collections */
  collections: GiftCollection[] = [];
}

/** Decode a TDLib `giftCollections` object; null input -> null. */
export function decodeGiftCollections(json: TdJsonRaw): GiftCollections | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftCollections = new GiftCollections();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.collections = decodeVector(record['collections'], (item: TdJsonRaw): GiftCollection => decodeGiftCollection(item) as GiftCollection);
  return out;
}

/** Encode a `giftCollections` into TDLib JSON wire shape. */
export function encodeGiftCollections(value: GiftCollections): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftCollections';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['collections'] = encodeVector(value.collections, (item: GiftCollection | null): TdJsonRaw => item === null ? null : encodeGiftCollection(item));
  return out;
}

/** giftForResale = GiftForResale — Describes a gift available for resale */
export class GiftForResale {
  type: 'giftForResale' = 'giftForResale';
  extra?: string;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
  /** received_gift_id: string — Unique identifier of the received gift for the current user; only for the gifts owned by the current user */
  received_gift_id: string = '';
}

/** Decode a TDLib `giftForResale` object; null input -> null. */
export function decodeGiftForResale(json: TdJsonRaw): GiftForResale | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftForResale = new GiftForResale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  out.received_gift_id = asString(record['received_gift_id']);
  return out;
}

/** Encode a `giftForResale` into TDLib JSON wire shape. */
export function encodeGiftForResale(value: GiftForResale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftForResale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  out['received_gift_id'] = value.received_gift_id;
  return out;
}

/** giftForResaleOrderNumber = GiftForResaleOrder — The gifts will be sorted by their number from the smallest to the largest */
export class GiftForResaleOrderNumber {
  type: 'giftForResaleOrderNumber' = 'giftForResaleOrderNumber';
  extra?: string;
}

/** Decode a TDLib `giftForResaleOrderNumber` object; null input -> null. */
export function decodeGiftForResaleOrderNumber(json: TdJsonRaw): GiftForResaleOrderNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftForResaleOrderNumber = new GiftForResaleOrderNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `giftForResaleOrderNumber` into TDLib JSON wire shape. */
export function encodeGiftForResaleOrderNumber(value: GiftForResaleOrderNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftForResaleOrderNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** giftForResaleOrderPrice = GiftForResaleOrder — The gifts will be sorted by their price from the lowest to the highest */
export class GiftForResaleOrderPrice {
  type: 'giftForResaleOrderPrice' = 'giftForResaleOrderPrice';
  extra?: string;
}

/** Decode a TDLib `giftForResaleOrderPrice` object; null input -> null. */
export function decodeGiftForResaleOrderPrice(json: TdJsonRaw): GiftForResaleOrderPrice | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftForResaleOrderPrice = new GiftForResaleOrderPrice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `giftForResaleOrderPrice` into TDLib JSON wire shape. */
export function encodeGiftForResaleOrderPrice(value: GiftForResaleOrderPrice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftForResaleOrderPrice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** giftForResaleOrderPriceChangeDate = GiftForResaleOrder — The gifts will be sorted by the last date when their price was changed from the newest to the oldest */
export class GiftForResaleOrderPriceChangeDate {
  type: 'giftForResaleOrderPriceChangeDate' = 'giftForResaleOrderPriceChangeDate';
  extra?: string;
}

/** Decode a TDLib `giftForResaleOrderPriceChangeDate` object; null input -> null. */
export function decodeGiftForResaleOrderPriceChangeDate(json: TdJsonRaw): GiftForResaleOrderPriceChangeDate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftForResaleOrderPriceChangeDate = new GiftForResaleOrderPriceChangeDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `giftForResaleOrderPriceChangeDate` into TDLib JSON wire shape. */
export function encodeGiftForResaleOrderPriceChangeDate(value: GiftForResaleOrderPriceChangeDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftForResaleOrderPriceChangeDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** giftPremiumWithStars = Ok — Allows to buy a Telegram Premium subscription for another user with payment in Telegram Stars; for bots only */
export class GiftPremiumWithStars {
  type: 'giftPremiumWithStars' = 'giftPremiumWithStars';
  extra?: string;
  /** user_id: int53 — Identifier of the user who will receive Telegram Premium */
  user_id: number = 0;
  /** star_count: int53 — The number of Telegram Stars to pay for subscription */
  star_count: number = 0;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active for the user */
  month_count: number = 0;
  /** text: formattedText — Text to show to the user receiving Telegram Premium; 0-getOption("gift_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed */
  text: FormattedText | null = null;
}

/** Decode a TDLib `giftPremiumWithStars` object; null input -> null. */
export function decodeGiftPremiumWithStars(json: TdJsonRaw): GiftPremiumWithStars | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftPremiumWithStars = new GiftPremiumWithStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.star_count = asNumber(record['star_count']);
  out.month_count = asNumber(record['month_count']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `giftPremiumWithStars` into TDLib JSON wire shape. */
export function encodeGiftPremiumWithStars(value: GiftPremiumWithStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftPremiumWithStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['star_count'] = value.star_count;
  out['month_count'] = value.month_count;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** giftPurchaseLimits = GiftPurchaseLimits — Describes the maximum number of times that a specific gift can be purchased */
export class GiftPurchaseLimits {
  type: 'giftPurchaseLimits' = 'giftPurchaseLimits';
  extra?: string;
  /** total_count: int32 — The maximum number of times the gifts can be purchased */
  total_count: number = 0;
  /** remaining_count: int32 — Number of remaining times the gift can be purchased */
  remaining_count: number = 0;
}

/** Decode a TDLib `giftPurchaseLimits` object; null input -> null. */
export function decodeGiftPurchaseLimits(json: TdJsonRaw): GiftPurchaseLimits | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftPurchaseLimits = new GiftPurchaseLimits();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.remaining_count = asNumber(record['remaining_count']);
  return out;
}

/** Encode a `giftPurchaseLimits` into TDLib JSON wire shape. */
export function encodeGiftPurchaseLimits(value: GiftPurchaseLimits): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftPurchaseLimits';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['remaining_count'] = value.remaining_count;
  return out;
}

/** giftPurchaseOfferStateAccepted = GiftPurchaseOfferState — The offer was accepted */
export class GiftPurchaseOfferStateAccepted {
  type: 'giftPurchaseOfferStateAccepted' = 'giftPurchaseOfferStateAccepted';
  extra?: string;
}

/** Decode a TDLib `giftPurchaseOfferStateAccepted` object; null input -> null. */
export function decodeGiftPurchaseOfferStateAccepted(json: TdJsonRaw): GiftPurchaseOfferStateAccepted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftPurchaseOfferStateAccepted = new GiftPurchaseOfferStateAccepted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `giftPurchaseOfferStateAccepted` into TDLib JSON wire shape. */
export function encodeGiftPurchaseOfferStateAccepted(value: GiftPurchaseOfferStateAccepted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftPurchaseOfferStateAccepted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** giftPurchaseOfferStatePending = GiftPurchaseOfferState — The offer must be accepted or rejected */
export class GiftPurchaseOfferStatePending {
  type: 'giftPurchaseOfferStatePending' = 'giftPurchaseOfferStatePending';
  extra?: string;
}

/** Decode a TDLib `giftPurchaseOfferStatePending` object; null input -> null. */
export function decodeGiftPurchaseOfferStatePending(json: TdJsonRaw): GiftPurchaseOfferStatePending | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftPurchaseOfferStatePending = new GiftPurchaseOfferStatePending();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `giftPurchaseOfferStatePending` into TDLib JSON wire shape. */
export function encodeGiftPurchaseOfferStatePending(value: GiftPurchaseOfferStatePending): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftPurchaseOfferStatePending';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** giftPurchaseOfferStateRejected = GiftPurchaseOfferState — The offer was rejected */
export class GiftPurchaseOfferStateRejected {
  type: 'giftPurchaseOfferStateRejected' = 'giftPurchaseOfferStateRejected';
  extra?: string;
}

/** Decode a TDLib `giftPurchaseOfferStateRejected` object; null input -> null. */
export function decodeGiftPurchaseOfferStateRejected(json: TdJsonRaw): GiftPurchaseOfferStateRejected | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftPurchaseOfferStateRejected = new GiftPurchaseOfferStateRejected();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `giftPurchaseOfferStateRejected` into TDLib JSON wire shape. */
export function encodeGiftPurchaseOfferStateRejected(value: GiftPurchaseOfferStateRejected): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftPurchaseOfferStateRejected';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** giftResaleParameters = GiftResaleParameters — Describes parameters of a unique gift available for resale */
export class GiftResaleParameters {
  type: 'giftResaleParameters' = 'giftResaleParameters';
  extra?: string;
  /** star_count: int53 — Resale price of the gift in Telegram Stars */
  star_count: number = 0;
  /** gram_cent_count: int53 — Resale price of the gift in 1/100 of TON Gram */
  gram_cent_count: number = 0;
  /** gram_only: Bool — True, if the gift can be bought only using Grams */
  gram_only: boolean = false;
}

/** Decode a TDLib `giftResaleParameters` object; null input -> null. */
export function decodeGiftResaleParameters(json: TdJsonRaw): GiftResaleParameters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftResaleParameters = new GiftResaleParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.gram_cent_count = asNumber(record['gram_cent_count']);
  out.gram_only = asBool(record['gram_only']);
  return out;
}

/** Encode a `giftResaleParameters` into TDLib JSON wire shape. */
export function encodeGiftResaleParameters(value: GiftResaleParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftResaleParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['gram_cent_count'] = value.gram_cent_count;
  out['gram_only'] = value.gram_only;
  return out;
}

/** giftResalePriceGram = GiftResalePrice — Describes price of a resold gift in TON Grams getOption("gift_resale_gram_cent_count_min")-getOption("gift_resale_gram_cent_count_max") */
export class GiftResalePriceGram {
  type: 'giftResalePriceGram' = 'giftResalePriceGram';
  extra?: string;
  /** gram_cent_count: int53 — The amount of 1/100 of Gram expected to be paid for the gift. Must be in the range */
  gram_cent_count: number = 0;
}

/** Decode a TDLib `giftResalePriceGram` object; null input -> null. */
export function decodeGiftResalePriceGram(json: TdJsonRaw): GiftResalePriceGram | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftResalePriceGram = new GiftResalePriceGram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gram_cent_count = asNumber(record['gram_cent_count']);
  return out;
}

/** Encode a `giftResalePriceGram` into TDLib JSON wire shape. */
export function encodeGiftResalePriceGram(value: GiftResalePriceGram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftResalePriceGram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gram_cent_count'] = value.gram_cent_count;
  return out;
}

/** giftResalePriceStar = GiftResalePrice — Describes price of a resold gift in Telegram Stars getOption("gift_resale_star_count_min")-getOption("gift_resale_star_count_max") for gifts put for resale */
export class GiftResalePriceStar {
  type: 'giftResalePriceStar' = 'giftResalePriceStar';
  extra?: string;
  /** star_count: int53 — The Telegram Star amount expected to be paid for the gift. Must be in the range */
  star_count: number = 0;
}

/** Decode a TDLib `giftResalePriceStar` object; null input -> null. */
export function decodeGiftResalePriceStar(json: TdJsonRaw): GiftResalePriceStar | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftResalePriceStar = new GiftResalePriceStar();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `giftResalePriceStar` into TDLib JSON wire shape. */
export function encodeGiftResalePriceStar(value: GiftResalePriceStar): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftResalePriceStar';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  return out;
}

/** giftResaleResultOk = GiftResaleResult — Operation was successfully completed */
export class GiftResaleResultOk {
  type: 'giftResaleResultOk' = 'giftResaleResultOk';
  extra?: string;
  /** received_gift_id: string — Unique identifier of the received gift; only for the gifts sent to the current user */
  received_gift_id: string = '';
}

/** Decode a TDLib `giftResaleResultOk` object; null input -> null. */
export function decodeGiftResaleResultOk(json: TdJsonRaw): GiftResaleResultOk | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftResaleResultOk = new GiftResaleResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.received_gift_id = asString(record['received_gift_id']);
  return out;
}

/** Encode a `giftResaleResultOk` into TDLib JSON wire shape. */
export function encodeGiftResaleResultOk(value: GiftResaleResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftResaleResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['received_gift_id'] = value.received_gift_id;
  return out;
}

/** giftResaleResultPriceIncreased = GiftResaleResult — Operation has failed, because price has increased. If the price has decreased, then the buying will succeed anyway */
export class GiftResaleResultPriceIncreased {
  type: 'giftResaleResultPriceIncreased' = 'giftResaleResultPriceIncreased';
  extra?: string;
  /** price: GiftResalePrice — New price for the gift */
  price: TdGiftResalePrice | null = null;
}

/** Decode a TDLib `giftResaleResultPriceIncreased` object; null input -> null. */
export function decodeGiftResaleResultPriceIncreased(json: TdJsonRaw): GiftResaleResultPriceIncreased | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftResaleResultPriceIncreased = new GiftResaleResultPriceIncreased();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.price = decodeTdGiftResalePrice(record['price']);
  return out;
}

/** Encode a `giftResaleResultPriceIncreased` into TDLib JSON wire shape. */
export function encodeGiftResaleResultPriceIncreased(value: GiftResaleResultPriceIncreased): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftResaleResultPriceIncreased';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['price'] = value.price === null ? null : encodeTdGiftResalePrice(value.price);
  return out;
}

/** giftSettings = GiftSettings — Contains settings for gift receiving for a user */
export class GiftSettings {
  type: 'giftSettings' = 'giftSettings';
  extra?: string;
  /** show_gift_button: Bool — True, if a button for sending a gift to the user or by the user must always be shown in the input field */
  show_gift_button: boolean = false;
  /** accepted_gift_types: acceptedGiftTypes — Types of gifts accepted by the user; for Telegram Premium users only */
  accepted_gift_types: AcceptedGiftTypes | null = null;
}

/** Decode a TDLib `giftSettings` object; null input -> null. */
export function decodeGiftSettings(json: TdJsonRaw): GiftSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftSettings = new GiftSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.show_gift_button = asBool(record['show_gift_button']);
  out.accepted_gift_types = decodeAcceptedGiftTypes(record['accepted_gift_types']);
  return out;
}

/** Encode a `giftSettings` into TDLib JSON wire shape. */
export function encodeGiftSettings(value: GiftSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['show_gift_button'] = value.show_gift_button;
  out['accepted_gift_types'] = value.accepted_gift_types === null ? null : encodeAcceptedGiftTypes(value.accepted_gift_types);
  return out;
}

/** giftUpgradePreview = GiftUpgradePreview — Contains examples of possible upgraded gifts for the given regular gift */
export class GiftUpgradePreview {
  type: 'giftUpgradePreview' = 'giftUpgradePreview';
  extra?: string;
  /** models: vector<upgradedGiftModel> — Examples of possible models that can be chosen for the gift after upgrade */
  models: UpgradedGiftModel[] = [];
  /** symbols: vector<upgradedGiftSymbol> — Examples of possible symbols that can be chosen for the gift after upgrade */
  symbols: UpgradedGiftSymbol[] = [];
  /** backdrops: vector<upgradedGiftBackdrop> — Examples of possible backdrops that can be chosen for the gift after upgrade */
  backdrops: UpgradedGiftBackdrop[] = [];
  /** prices: vector<giftUpgradePrice> — Examples of price for gift upgrade from the maximum price to the minimum price */
  prices: GiftUpgradePrice[] = [];
  /** next_prices: vector<giftUpgradePrice> — Next changes for the price for gift upgrade with more granularity than in prices */
  next_prices: GiftUpgradePrice[] = [];
}

/** Decode a TDLib `giftUpgradePreview` object; null input -> null. */
export function decodeGiftUpgradePreview(json: TdJsonRaw): GiftUpgradePreview | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftUpgradePreview = new GiftUpgradePreview();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.models = decodeVector(record['models'], (item: TdJsonRaw): UpgradedGiftModel => decodeUpgradedGiftModel(item) as UpgradedGiftModel);
  out.symbols = decodeVector(record['symbols'], (item: TdJsonRaw): UpgradedGiftSymbol => decodeUpgradedGiftSymbol(item) as UpgradedGiftSymbol);
  out.backdrops = decodeVector(record['backdrops'], (item: TdJsonRaw): UpgradedGiftBackdrop => decodeUpgradedGiftBackdrop(item) as UpgradedGiftBackdrop);
  out.prices = decodeVector(record['prices'], (item: TdJsonRaw): GiftUpgradePrice => decodeGiftUpgradePrice(item) as GiftUpgradePrice);
  out.next_prices = decodeVector(record['next_prices'], (item: TdJsonRaw): GiftUpgradePrice => decodeGiftUpgradePrice(item) as GiftUpgradePrice);
  return out;
}

/** Encode a `giftUpgradePreview` into TDLib JSON wire shape. */
export function encodeGiftUpgradePreview(value: GiftUpgradePreview): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftUpgradePreview';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['models'] = encodeVector(value.models, (item: UpgradedGiftModel | null): TdJsonRaw => item === null ? null : encodeUpgradedGiftModel(item));
  out['symbols'] = encodeVector(value.symbols, (item: UpgradedGiftSymbol | null): TdJsonRaw => item === null ? null : encodeUpgradedGiftSymbol(item));
  out['backdrops'] = encodeVector(value.backdrops, (item: UpgradedGiftBackdrop | null): TdJsonRaw => item === null ? null : encodeUpgradedGiftBackdrop(item));
  out['prices'] = encodeVector(value.prices, (item: GiftUpgradePrice | null): TdJsonRaw => item === null ? null : encodeGiftUpgradePrice(item));
  out['next_prices'] = encodeVector(value.next_prices, (item: GiftUpgradePrice | null): TdJsonRaw => item === null ? null : encodeGiftUpgradePrice(item));
  return out;
}

/** giftUpgradePrice = GiftUpgradePrice — Describes a price required to pay to upgrade a gift */
export class GiftUpgradePrice {
  type: 'giftUpgradePrice' = 'giftUpgradePrice';
  extra?: string;
  /** date: int32 — Point in time (Unix timestamp) when the price will be in effect */
  date: number = 0;
  /** star_count: int53 — The Telegram Star amount required to pay to upgrade the gift */
  star_count: number = 0;
}

/** Decode a TDLib `giftUpgradePrice` object; null input -> null. */
export function decodeGiftUpgradePrice(json: TdJsonRaw): GiftUpgradePrice | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftUpgradePrice = new GiftUpgradePrice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.date = asNumber(record['date']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `giftUpgradePrice` into TDLib JSON wire shape. */
export function encodeGiftUpgradePrice(value: GiftUpgradePrice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftUpgradePrice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['date'] = value.date;
  out['star_count'] = value.star_count;
  return out;
}

/** giftUpgradeVariants = GiftUpgradeVariants — Contains all possible variants of upgraded gifts for the given regular gift */
export class GiftUpgradeVariants {
  type: 'giftUpgradeVariants' = 'giftUpgradeVariants';
  extra?: string;
  /** models: vector<upgradedGiftModel> — Models that can be chosen for the gift after upgrade */
  models: UpgradedGiftModel[] = [];
  /** symbols: vector<upgradedGiftSymbol> — Symbols that can be chosen for the gift after upgrade */
  symbols: UpgradedGiftSymbol[] = [];
  /** backdrops: vector<upgradedGiftBackdrop> — Backdrops that can be chosen for the gift after upgrade */
  backdrops: UpgradedGiftBackdrop[] = [];
}

/** Decode a TDLib `giftUpgradeVariants` object; null input -> null. */
export function decodeGiftUpgradeVariants(json: TdJsonRaw): GiftUpgradeVariants | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftUpgradeVariants = new GiftUpgradeVariants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.models = decodeVector(record['models'], (item: TdJsonRaw): UpgradedGiftModel => decodeUpgradedGiftModel(item) as UpgradedGiftModel);
  out.symbols = decodeVector(record['symbols'], (item: TdJsonRaw): UpgradedGiftSymbol => decodeUpgradedGiftSymbol(item) as UpgradedGiftSymbol);
  out.backdrops = decodeVector(record['backdrops'], (item: TdJsonRaw): UpgradedGiftBackdrop => decodeUpgradedGiftBackdrop(item) as UpgradedGiftBackdrop);
  return out;
}

/** Encode a `giftUpgradeVariants` into TDLib JSON wire shape. */
export function encodeGiftUpgradeVariants(value: GiftUpgradeVariants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftUpgradeVariants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['models'] = encodeVector(value.models, (item: UpgradedGiftModel | null): TdJsonRaw => item === null ? null : encodeUpgradedGiftModel(item));
  out['symbols'] = encodeVector(value.symbols, (item: UpgradedGiftSymbol | null): TdJsonRaw => item === null ? null : encodeUpgradedGiftSymbol(item));
  out['backdrops'] = encodeVector(value.backdrops, (item: UpgradedGiftBackdrop | null): TdJsonRaw => item === null ? null : encodeUpgradedGiftBackdrop(item));
  return out;
}

/** giftsForCrafting = GiftsForCrafting — Represents a list of gifts received by a user or a chat for the cases when 1, 2, 3 or 4 gifts are used in the craft correspondingly */
export class GiftsForCrafting {
  type: 'giftsForCrafting' = 'giftsForCrafting';
  extra?: string;
  /** total_count: int32 — The total number of received gifts */
  total_count: number = 0;
  /** gifts: vector<receivedGift> — The list of gifts */
  gifts: ReceivedGift[] = [];
  /** attribute_persistence_probabilities: vector<attributeCraftPersistenceProbability> — The 4 objects that describe probabilities of the crafted gift to have the backdrop or symbol of one of the original gifts */
  attribute_persistence_probabilities: AttributeCraftPersistenceProbability[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `giftsForCrafting` object; null input -> null. */
export function decodeGiftsForCrafting(json: TdJsonRaw): GiftsForCrafting | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftsForCrafting = new GiftsForCrafting();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.gifts = decodeVector(record['gifts'], (item: TdJsonRaw): ReceivedGift => decodeReceivedGift(item) as ReceivedGift);
  out.attribute_persistence_probabilities = decodeVector(record['attribute_persistence_probabilities'], (item: TdJsonRaw): AttributeCraftPersistenceProbability => decodeAttributeCraftPersistenceProbability(item) as AttributeCraftPersistenceProbability);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `giftsForCrafting` into TDLib JSON wire shape. */
export function encodeGiftsForCrafting(value: GiftsForCrafting): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftsForCrafting';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['gifts'] = encodeVector(value.gifts, (item: ReceivedGift | null): TdJsonRaw => item === null ? null : encodeReceivedGift(item));
  out['attribute_persistence_probabilities'] = encodeVector(value.attribute_persistence_probabilities, (item: AttributeCraftPersistenceProbability | null): TdJsonRaw => item === null ? null : encodeAttributeCraftPersistenceProbability(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** giftsForResale = GiftsForResale — Describes gifts available for resale */
export class GiftsForResale {
  type: 'giftsForResale' = 'giftsForResale';
  extra?: string;
  /** total_count: int32 — Total number of gifts found */
  total_count: number = 0;
  /** gifts: vector<giftForResale> — The gifts */
  gifts: GiftForResale[] = [];
  /** models: vector<upgradedGiftModelCount> — Available models; for searchGiftsForResale requests without offset and attributes only */
  models: UpgradedGiftModelCount[] = [];
  /** symbols: vector<upgradedGiftSymbolCount> — Available symbols; for searchGiftsForResale requests without offset and attributes only */
  symbols: UpgradedGiftSymbolCount[] = [];
  /** backdrops: vector<upgradedGiftBackdropCount> — Available backdrops; for searchGiftsForResale requests without offset and attributes only */
  backdrops: UpgradedGiftBackdropCount[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `giftsForResale` object; null input -> null. */
export function decodeGiftsForResale(json: TdJsonRaw): GiftsForResale | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiftsForResale = new GiftsForResale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.gifts = decodeVector(record['gifts'], (item: TdJsonRaw): GiftForResale => decodeGiftForResale(item) as GiftForResale);
  out.models = decodeVector(record['models'], (item: TdJsonRaw): UpgradedGiftModelCount => decodeUpgradedGiftModelCount(item) as UpgradedGiftModelCount);
  out.symbols = decodeVector(record['symbols'], (item: TdJsonRaw): UpgradedGiftSymbolCount => decodeUpgradedGiftSymbolCount(item) as UpgradedGiftSymbolCount);
  out.backdrops = decodeVector(record['backdrops'], (item: TdJsonRaw): UpgradedGiftBackdropCount => decodeUpgradedGiftBackdropCount(item) as UpgradedGiftBackdropCount);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `giftsForResale` into TDLib JSON wire shape. */
export function encodeGiftsForResale(value: GiftsForResale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giftsForResale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['gifts'] = encodeVector(value.gifts, (item: GiftForResale | null): TdJsonRaw => item === null ? null : encodeGiftForResale(item));
  out['models'] = encodeVector(value.models, (item: UpgradedGiftModelCount | null): TdJsonRaw => item === null ? null : encodeUpgradedGiftModelCount(item));
  out['symbols'] = encodeVector(value.symbols, (item: UpgradedGiftSymbolCount | null): TdJsonRaw => item === null ? null : encodeUpgradedGiftSymbolCount(item));
  out['backdrops'] = encodeVector(value.backdrops, (item: UpgradedGiftBackdropCount | null): TdJsonRaw => item === null ? null : encodeUpgradedGiftBackdropCount(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** giveawayInfoCompleted = GiveawayInfo — Describes a completed giveaway */
export class GiveawayInfoCompleted {
  type: 'giveawayInfoCompleted' = 'giveawayInfoCompleted';
  extra?: string;
  /** creation_date: int32 — Point in time (Unix timestamp) when the giveaway was created */
  creation_date: number = 0;
  /** actual_winners_selection_date: int32 — Point in time (Unix timestamp) when the winners were selected. May be bigger than winners selection date specified in parameters of the giveaway */
  actual_winners_selection_date: number = 0;
  /** was_refunded: Bool — True, if the giveaway was canceled and was fully refunded */
  was_refunded: boolean = false;
  /** is_winner: Bool — True, if the current user is a winner of the giveaway */
  is_winner: boolean = false;
  /** winner_count: int32 — Number of winners in the giveaway */
  winner_count: number = 0;
  /** activation_count: int32 — Number of winners, which activated their gift codes; for Telegram Premium giveaways only */
  activation_count: number = 0;
  /** gift_code: string — Telegram Premium gift code that was received by the current user; empty if the user isn't a winner in the giveaway or the giveaway isn't a Telegram Premium giveaway */
  gift_code: string = '';
  /** won_star_count: int53 — The Telegram Star amount won by the current user; 0 if the user isn't a winner in the giveaway or the giveaway isn't a Telegram Star giveaway */
  won_star_count: number = 0;
}

/** Decode a TDLib `giveawayInfoCompleted` object; null input -> null. */
export function decodeGiveawayInfoCompleted(json: TdJsonRaw): GiveawayInfoCompleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayInfoCompleted = new GiveawayInfoCompleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.creation_date = asNumber(record['creation_date']);
  out.actual_winners_selection_date = asNumber(record['actual_winners_selection_date']);
  out.was_refunded = asBool(record['was_refunded']);
  out.is_winner = asBool(record['is_winner']);
  out.winner_count = asNumber(record['winner_count']);
  out.activation_count = asNumber(record['activation_count']);
  out.gift_code = asString(record['gift_code']);
  out.won_star_count = asNumber(record['won_star_count']);
  return out;
}

/** Encode a `giveawayInfoCompleted` into TDLib JSON wire shape. */
export function encodeGiveawayInfoCompleted(value: GiveawayInfoCompleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayInfoCompleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['creation_date'] = value.creation_date;
  out['actual_winners_selection_date'] = value.actual_winners_selection_date;
  out['was_refunded'] = value.was_refunded;
  out['is_winner'] = value.is_winner;
  out['winner_count'] = value.winner_count;
  out['activation_count'] = value.activation_count;
  out['gift_code'] = value.gift_code;
  out['won_star_count'] = value.won_star_count;
  return out;
}

/** giveawayInfoOngoing = GiveawayInfo — Describes an ongoing giveaway */
export class GiveawayInfoOngoing {
  type: 'giveawayInfoOngoing' = 'giveawayInfoOngoing';
  extra?: string;
  /** creation_date: int32 — Point in time (Unix timestamp) when the giveaway was created */
  creation_date: number = 0;
  /** status: GiveawayParticipantStatus — Status of the current user in the giveaway */
  status: TdGiveawayParticipantStatus | null = null;
  /** is_ended: Bool — True, if the giveaway has ended and results are being prepared */
  is_ended: boolean = false;
}

/** Decode a TDLib `giveawayInfoOngoing` object; null input -> null. */
export function decodeGiveawayInfoOngoing(json: TdJsonRaw): GiveawayInfoOngoing | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayInfoOngoing = new GiveawayInfoOngoing();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.creation_date = asNumber(record['creation_date']);
  out.status = decodeTdGiveawayParticipantStatus(record['status']);
  out.is_ended = asBool(record['is_ended']);
  return out;
}

/** Encode a `giveawayInfoOngoing` into TDLib JSON wire shape. */
export function encodeGiveawayInfoOngoing(value: GiveawayInfoOngoing): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayInfoOngoing';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['creation_date'] = value.creation_date;
  out['status'] = value.status === null ? null : encodeTdGiveawayParticipantStatus(value.status);
  out['is_ended'] = value.is_ended;
  return out;
}

/** giveawayParameters = GiveawayParameters — Describes parameters of a giveaway or for the specified time. If the chat is a channel, then can_post_messages administrator right is required in the channel, otherwise, the user must be an admin... */
export class GiveawayParameters {
  type: 'giveawayParameters' = 'giveawayParameters';
  extra?: string;
  /** boosted_chat_id: int53 — Identifier of the supergroup or channel chat, which will be automatically boosted by the winners of the giveaway for duration of the Telegram Premium subscription, */
  boosted_chat_id: number = 0;
  /** additional_chat_ids: vector<int53> — Identifiers of other supergroup or channel chats that must be subscribed by the users to be eligible for the giveaway. There can be up to getOption("giveaway_additional_chat_count_max") additional chats */
  additional_chat_ids: number[] = [];
  /** winners_selection_date: int32 — Point in time (Unix timestamp) when the giveaway is expected to be performed; must be 60-getOption("giveaway_duration_max") seconds in the future in scheduled giveaways */
  winners_selection_date: number = 0;
  /** only_new_members: Bool — True, if only new members of the chats will be eligible for the giveaway */
  only_new_members: boolean = false;
  /** has_public_winners: Bool — True, if the list of winners of the giveaway will be available to everyone */
  has_public_winners: boolean = false;
  /** country_codes: vector<string> — The list of two-letter ISO 3166-1 alpha-2 codes of countries, users from which will be eligible for the giveaway. If empty, then all users can participate in the giveaway. */
  country_codes: string[] = [];
  /** prize_description: string — Additional description of the giveaway prize; 0-128 characters */
  prize_description: string = '';
}

/** Decode a TDLib `giveawayParameters` object; null input -> null. */
export function decodeGiveawayParameters(json: TdJsonRaw): GiveawayParameters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayParameters = new GiveawayParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.boosted_chat_id = asNumber(record['boosted_chat_id']);
  out.additional_chat_ids = decodeVector(record['additional_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.winners_selection_date = asNumber(record['winners_selection_date']);
  out.only_new_members = asBool(record['only_new_members']);
  out.has_public_winners = asBool(record['has_public_winners']);
  out.country_codes = decodeVector(record['country_codes'], (item: TdJsonRaw): string => asString(item));
  out.prize_description = asString(record['prize_description']);
  return out;
}

/** Encode a `giveawayParameters` into TDLib JSON wire shape. */
export function encodeGiveawayParameters(value: GiveawayParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['boosted_chat_id'] = value.boosted_chat_id;
  out['additional_chat_ids'] = encodeVector(value.additional_chat_ids, (item: number): TdJsonRaw => item);
  out['winners_selection_date'] = value.winners_selection_date;
  out['only_new_members'] = value.only_new_members;
  out['has_public_winners'] = value.has_public_winners;
  out['country_codes'] = encodeVector(value.country_codes, (item: string): TdJsonRaw => item);
  out['prize_description'] = value.prize_description;
  return out;
}

/** giveawayParticipantStatusAdministrator = GiveawayParticipantStatus — The user can't participate in the giveaway, because they are an administrator in one of the chats that created the giveaway */
export class GiveawayParticipantStatusAdministrator {
  type: 'giveawayParticipantStatusAdministrator' = 'giveawayParticipantStatusAdministrator';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat administered by the user */
  chat_id: number = 0;
}

/** Decode a TDLib `giveawayParticipantStatusAdministrator` object; null input -> null. */
export function decodeGiveawayParticipantStatusAdministrator(json: TdJsonRaw): GiveawayParticipantStatusAdministrator | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayParticipantStatusAdministrator = new GiveawayParticipantStatusAdministrator();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `giveawayParticipantStatusAdministrator` into TDLib JSON wire shape. */
export function encodeGiveawayParticipantStatusAdministrator(value: GiveawayParticipantStatusAdministrator): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayParticipantStatusAdministrator';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** giveawayParticipantStatusAlreadyWasMember = GiveawayParticipantStatus — The user can't participate in the giveaway, because they have already been member of the chat */
export class GiveawayParticipantStatusAlreadyWasMember {
  type: 'giveawayParticipantStatusAlreadyWasMember' = 'giveawayParticipantStatusAlreadyWasMember';
  extra?: string;
  /** joined_chat_date: int32 — Point in time (Unix timestamp) when the user joined the chat */
  joined_chat_date: number = 0;
}

/** Decode a TDLib `giveawayParticipantStatusAlreadyWasMember` object; null input -> null. */
export function decodeGiveawayParticipantStatusAlreadyWasMember(json: TdJsonRaw): GiveawayParticipantStatusAlreadyWasMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayParticipantStatusAlreadyWasMember = new GiveawayParticipantStatusAlreadyWasMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.joined_chat_date = asNumber(record['joined_chat_date']);
  return out;
}

/** Encode a `giveawayParticipantStatusAlreadyWasMember` into TDLib JSON wire shape. */
export function encodeGiveawayParticipantStatusAlreadyWasMember(value: GiveawayParticipantStatusAlreadyWasMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayParticipantStatusAlreadyWasMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['joined_chat_date'] = value.joined_chat_date;
  return out;
}

/** giveawayParticipantStatusDisallowedCountry = GiveawayParticipantStatus — The user can't participate in the giveaway, because their phone number is from a disallowed country */
export class GiveawayParticipantStatusDisallowedCountry {
  type: 'giveawayParticipantStatusDisallowedCountry' = 'giveawayParticipantStatusDisallowedCountry';
  extra?: string;
  /** user_country_code: string — A two-letter ISO 3166-1 alpha-2 country code of the user's country */
  user_country_code: string = '';
}

/** Decode a TDLib `giveawayParticipantStatusDisallowedCountry` object; null input -> null. */
export function decodeGiveawayParticipantStatusDisallowedCountry(json: TdJsonRaw): GiveawayParticipantStatusDisallowedCountry | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayParticipantStatusDisallowedCountry = new GiveawayParticipantStatusDisallowedCountry();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_country_code = asString(record['user_country_code']);
  return out;
}

/** Encode a `giveawayParticipantStatusDisallowedCountry` into TDLib JSON wire shape. */
export function encodeGiveawayParticipantStatusDisallowedCountry(value: GiveawayParticipantStatusDisallowedCountry): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayParticipantStatusDisallowedCountry';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_country_code'] = value.user_country_code;
  return out;
}

/** giveawayParticipantStatusEligible = GiveawayParticipantStatus — The user is eligible for the giveaway */
export class GiveawayParticipantStatusEligible {
  type: 'giveawayParticipantStatusEligible' = 'giveawayParticipantStatusEligible';
  extra?: string;
}

/** Decode a TDLib `giveawayParticipantStatusEligible` object; null input -> null. */
export function decodeGiveawayParticipantStatusEligible(json: TdJsonRaw): GiveawayParticipantStatusEligible | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayParticipantStatusEligible = new GiveawayParticipantStatusEligible();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `giveawayParticipantStatusEligible` into TDLib JSON wire shape. */
export function encodeGiveawayParticipantStatusEligible(value: GiveawayParticipantStatusEligible): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayParticipantStatusEligible';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** giveawayParticipantStatusParticipating = GiveawayParticipantStatus — The user participates in the giveaway */
export class GiveawayParticipantStatusParticipating {
  type: 'giveawayParticipantStatusParticipating' = 'giveawayParticipantStatusParticipating';
  extra?: string;
}

/** Decode a TDLib `giveawayParticipantStatusParticipating` object; null input -> null. */
export function decodeGiveawayParticipantStatusParticipating(json: TdJsonRaw): GiveawayParticipantStatusParticipating | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayParticipantStatusParticipating = new GiveawayParticipantStatusParticipating();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `giveawayParticipantStatusParticipating` into TDLib JSON wire shape. */
export function encodeGiveawayParticipantStatusParticipating(value: GiveawayParticipantStatusParticipating): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayParticipantStatusParticipating';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** giveawayPrizePremium = GiveawayPrize — The giveaway sends Telegram Premium subscriptions to the winners */
export class GiveawayPrizePremium {
  type: 'giveawayPrizePremium' = 'giveawayPrizePremium';
  extra?: string;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active after code activation */
  month_count: number = 0;
}

/** Decode a TDLib `giveawayPrizePremium` object; null input -> null. */
export function decodeGiveawayPrizePremium(json: TdJsonRaw): GiveawayPrizePremium | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayPrizePremium = new GiveawayPrizePremium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.month_count = asNumber(record['month_count']);
  return out;
}

/** Encode a `giveawayPrizePremium` into TDLib JSON wire shape. */
export function encodeGiveawayPrizePremium(value: GiveawayPrizePremium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayPrizePremium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['month_count'] = value.month_count;
  return out;
}

/** giveawayPrizeStars = GiveawayPrize — The giveaway sends Telegram Stars to the winners */
export class GiveawayPrizeStars {
  type: 'giveawayPrizeStars' = 'giveawayPrizeStars';
  extra?: string;
  /** star_count: int53 — Number of Telegram Stars that will be shared by all winners */
  star_count: number = 0;
}

/** Decode a TDLib `giveawayPrizeStars` object; null input -> null. */
export function decodeGiveawayPrizeStars(json: TdJsonRaw): GiveawayPrizeStars | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GiveawayPrizeStars = new GiveawayPrizeStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `giveawayPrizeStars` into TDLib JSON wire shape. */
export function encodeGiveawayPrizeStars(value: GiveawayPrizeStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'giveawayPrizeStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  return out;
}

/** gramRevenueStatistics = GramRevenueStatistics — A detailed statistics about TON Grams earned by the current user */
export class GramRevenueStatistics {
  type: 'gramRevenueStatistics' = 'gramRevenueStatistics';
  extra?: string;
  /** revenue_by_day_graph: StatisticalGraph — A graph containing amount of revenue in a given day */
  revenue_by_day_graph: TdStatisticalGraph | null = null;
  /** status: gramRevenueStatus — Amount of earned revenue */
  status: GramRevenueStatus | null = null;
  /** usd_rate: double — Current conversion rate of nanogram to USD cents */
  usd_rate: number = 0;
}

/** Decode a TDLib `gramRevenueStatistics` object; null input -> null. */
export function decodeGramRevenueStatistics(json: TdJsonRaw): GramRevenueStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GramRevenueStatistics = new GramRevenueStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.revenue_by_day_graph = decodeTdStatisticalGraph(record['revenue_by_day_graph']);
  out.status = decodeGramRevenueStatus(record['status']);
  out.usd_rate = asNumber(record['usd_rate']);
  return out;
}

/** Encode a `gramRevenueStatistics` into TDLib JSON wire shape. */
export function encodeGramRevenueStatistics(value: GramRevenueStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'gramRevenueStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['revenue_by_day_graph'] = value.revenue_by_day_graph === null ? null : encodeTdStatisticalGraph(value.revenue_by_day_graph);
  out['status'] = value.status === null ? null : encodeGramRevenueStatus(value.status);
  out['usd_rate'] = value.usd_rate;
  return out;
}

/** gramRevenueStatus = GramRevenueStatus — Contains information about TON Grams earned by the current user */
export class GramRevenueStatus {
  type: 'gramRevenueStatus' = 'gramRevenueStatus';
  extra?: string;
  /** total_amount: int64 — Total Gram amount earned; in the smallest units of the cryptocurrency */
  total_amount: string = '';
  /** balance_amount: int64 — The Gram amount that isn't withdrawn yet; in the smallest units of the cryptocurrency */
  balance_amount: string = '';
  /** available_amount: int64 — The Gram amount that is available for withdrawal; in the smallest units of the cryptocurrency */
  available_amount: string = '';
  /** withdrawal_enabled: Bool — True, if Grams can be withdrawn */
  withdrawal_enabled: boolean = false;
}

/** Decode a TDLib `gramRevenueStatus` object; null input -> null. */
export function decodeGramRevenueStatus(json: TdJsonRaw): GramRevenueStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GramRevenueStatus = new GramRevenueStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_amount = asInt64(record['total_amount']);
  out.balance_amount = asInt64(record['balance_amount']);
  out.available_amount = asInt64(record['available_amount']);
  out.withdrawal_enabled = asBool(record['withdrawal_enabled']);
  return out;
}

/** Encode a `gramRevenueStatus` into TDLib JSON wire shape. */
export function encodeGramRevenueStatus(value: GramRevenueStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'gramRevenueStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_amount'] = value.total_amount;
  out['balance_amount'] = value.balance_amount;
  out['available_amount'] = value.available_amount;
  out['withdrawal_enabled'] = value.withdrawal_enabled;
  return out;
}

/** groupCall = GroupCall — Describes a group call For live stories in chats with username call getInternalLink with internalLinkTypeLiveStory */
export class GroupCall {
  type: 'groupCall' = 'groupCall';
  extra?: string;
  /** id: int32 — Group call identifier */
  id: number = 0;
  /** unique_id: int64 — Persistent unique group call identifier */
  unique_id: string = '';
  /** title: string — Group call title; for video chats only */
  title: string = '';
  /** invite_link: string — Invite link for the group call; for group calls that aren't bound to a chat. For video chats call getVideoChatInviteLink to get the link. */
  invite_link: string = '';
  /** paid_message_star_count: int53 — The minimum number of Telegram Stars that must be paid by general participant for each sent message to the call; for live stories only */
  paid_message_star_count: number = 0;
  /** scheduled_start_date: int32 — Point in time (Unix timestamp) when the group call is expected to be started by an administrator; 0 if it is already active or was ended; for video chats only */
  scheduled_start_date: number = 0;
  /** enabled_start_notification: Bool — True, if the group call is scheduled and the current user will receive a notification when the group call starts; for video chats only */
  enabled_start_notification: boolean = false;
  /** is_active: Bool — True, if the call is active */
  is_active: boolean = false;
  /** is_video_chat: Bool — True, if the call is bound to a chat */
  is_video_chat: boolean = false;
  /** is_live_story: Bool — True, if the call is a live story of a chat */
  is_live_story: boolean = false;
  /** is_rtmp_stream: Bool — True, if the call is an RTMP stream instead of an ordinary video chat; for video chats and live stories only */
  is_rtmp_stream: boolean = false;
  /** is_joined: Bool — True, if the call is joined */
  is_joined: boolean = false;
  /** need_rejoin: Bool — True, if user was kicked from the call because of network loss and the call needs to be rejoined */
  need_rejoin: boolean = false;
  /** is_owned: Bool — True, if the user is the owner of the call and can end the call, change volume level of other users, or ban users there; for group calls that aren't bound to a chat */
  is_owned: boolean = false;
  /** can_be_managed: Bool — True, if the current user can manage the group call; for video chats and live stories only */
  can_be_managed: boolean = false;
  /** participant_count: int32 — Number of participants in the group call */
  participant_count: number = 0;
  /** has_hidden_listeners: Bool — True, if group call participants, which are muted, aren't returned in participant list; for video chats only */
  has_hidden_listeners: boolean = false;
  /** loaded_all_participants: Bool — True, if all group call participants are loaded */
  loaded_all_participants: boolean = false;
  /** message_sender_id: MessageSender — Message sender chosen to send messages to the group call; for live stories only; may be null if the call isn't a live story */
  message_sender_id: TdMessageSender | null = null;
  /** recent_speakers: vector<groupCallRecentSpeaker> — At most 3 recently speaking users in the group call */
  recent_speakers: GroupCallRecentSpeaker[] = [];
  /** is_my_video_enabled: Bool — True, if the current user's video is enabled */
  is_my_video_enabled: boolean = false;
  /** is_my_video_paused: Bool — True, if the current user's video is paused */
  is_my_video_paused: boolean = false;
  /** can_enable_video: Bool — True, if the current user can broadcast video or share screen */
  can_enable_video: boolean = false;
  /** mute_new_participants: Bool — True, if only group call administrators can unmute new participants; for video chats only */
  mute_new_participants: boolean = false;
  /** can_toggle_mute_new_participants: Bool — True, if the current user can enable or disable mute_new_participants setting; for video chats only */
  can_toggle_mute_new_participants: boolean = false;
  /** can_send_messages: Bool — True, if the current user can send messages to the group call */
  can_send_messages: boolean = false;
  /** are_messages_allowed: Bool — True, if sending of messages is allowed in the group call */
  are_messages_allowed: boolean = false;
  /** can_toggle_are_messages_allowed: Bool — True, if the current user can enable or disable sending of messages in the group call */
  can_toggle_are_messages_allowed: boolean = false;
  /** can_delete_messages: Bool — True, if the user can delete messages in the group call */
  can_delete_messages: boolean = false;
  /** record_duration: int32 — Duration of the ongoing group call recording, in seconds; 0 if none. An updateGroupCall update is not triggered when value of this field changes, but the same recording goes on */
  record_duration: number = 0;
  /** is_video_recorded: Bool — True, if a video file is being recorded for the call */
  is_video_recorded: boolean = false;
  /** duration: int32 — Call duration, in seconds; for ended calls only */
  duration: number = 0;
}

/** Decode a TDLib `groupCall` object; null input -> null. */
export function decodeGroupCall(json: TdJsonRaw): GroupCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCall = new GroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.unique_id = asInt64(record['unique_id']);
  out.title = asString(record['title']);
  out.invite_link = asString(record['invite_link']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  out.scheduled_start_date = asNumber(record['scheduled_start_date']);
  out.enabled_start_notification = asBool(record['enabled_start_notification']);
  out.is_active = asBool(record['is_active']);
  out.is_video_chat = asBool(record['is_video_chat']);
  out.is_live_story = asBool(record['is_live_story']);
  out.is_rtmp_stream = asBool(record['is_rtmp_stream']);
  out.is_joined = asBool(record['is_joined']);
  out.need_rejoin = asBool(record['need_rejoin']);
  out.is_owned = asBool(record['is_owned']);
  out.can_be_managed = asBool(record['can_be_managed']);
  out.participant_count = asNumber(record['participant_count']);
  out.has_hidden_listeners = asBool(record['has_hidden_listeners']);
  out.loaded_all_participants = asBool(record['loaded_all_participants']);
  out.message_sender_id = decodeTdMessageSender(record['message_sender_id']);
  out.recent_speakers = decodeVector(record['recent_speakers'], (item: TdJsonRaw): GroupCallRecentSpeaker => decodeGroupCallRecentSpeaker(item) as GroupCallRecentSpeaker);
  out.is_my_video_enabled = asBool(record['is_my_video_enabled']);
  out.is_my_video_paused = asBool(record['is_my_video_paused']);
  out.can_enable_video = asBool(record['can_enable_video']);
  out.mute_new_participants = asBool(record['mute_new_participants']);
  out.can_toggle_mute_new_participants = asBool(record['can_toggle_mute_new_participants']);
  out.can_send_messages = asBool(record['can_send_messages']);
  out.are_messages_allowed = asBool(record['are_messages_allowed']);
  out.can_toggle_are_messages_allowed = asBool(record['can_toggle_are_messages_allowed']);
  out.can_delete_messages = asBool(record['can_delete_messages']);
  out.record_duration = asNumber(record['record_duration']);
  out.is_video_recorded = asBool(record['is_video_recorded']);
  out.duration = asNumber(record['duration']);
  return out;
}

/** Encode a `groupCall` into TDLib JSON wire shape. */
export function encodeGroupCall(value: GroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['unique_id'] = value.unique_id;
  out['title'] = value.title;
  out['invite_link'] = value.invite_link;
  out['paid_message_star_count'] = value.paid_message_star_count;
  out['scheduled_start_date'] = value.scheduled_start_date;
  out['enabled_start_notification'] = value.enabled_start_notification;
  out['is_active'] = value.is_active;
  out['is_video_chat'] = value.is_video_chat;
  out['is_live_story'] = value.is_live_story;
  out['is_rtmp_stream'] = value.is_rtmp_stream;
  out['is_joined'] = value.is_joined;
  out['need_rejoin'] = value.need_rejoin;
  out['is_owned'] = value.is_owned;
  out['can_be_managed'] = value.can_be_managed;
  out['participant_count'] = value.participant_count;
  out['has_hidden_listeners'] = value.has_hidden_listeners;
  out['loaded_all_participants'] = value.loaded_all_participants;
  out['message_sender_id'] = value.message_sender_id === null ? null : encodeTdMessageSender(value.message_sender_id);
  out['recent_speakers'] = encodeVector(value.recent_speakers, (item: GroupCallRecentSpeaker | null): TdJsonRaw => item === null ? null : encodeGroupCallRecentSpeaker(item));
  out['is_my_video_enabled'] = value.is_my_video_enabled;
  out['is_my_video_paused'] = value.is_my_video_paused;
  out['can_enable_video'] = value.can_enable_video;
  out['mute_new_participants'] = value.mute_new_participants;
  out['can_toggle_mute_new_participants'] = value.can_toggle_mute_new_participants;
  out['can_send_messages'] = value.can_send_messages;
  out['are_messages_allowed'] = value.are_messages_allowed;
  out['can_toggle_are_messages_allowed'] = value.can_toggle_are_messages_allowed;
  out['can_delete_messages'] = value.can_delete_messages;
  out['record_duration'] = value.record_duration;
  out['is_video_recorded'] = value.is_video_recorded;
  out['duration'] = value.duration;
  return out;
}

/** groupCallDataChannelMain = GroupCallDataChannel — The main data channel for audio and video data */
export class GroupCallDataChannelMain {
  type: 'groupCallDataChannelMain' = 'groupCallDataChannelMain';
  extra?: string;
}

/** Decode a TDLib `groupCallDataChannelMain` object; null input -> null. */
export function decodeGroupCallDataChannelMain(json: TdJsonRaw): GroupCallDataChannelMain | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallDataChannelMain = new GroupCallDataChannelMain();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `groupCallDataChannelMain` into TDLib JSON wire shape. */
export function encodeGroupCallDataChannelMain(value: GroupCallDataChannelMain): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallDataChannelMain';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** groupCallDataChannelScreenSharing = GroupCallDataChannel — The data channel for screen sharing */
export class GroupCallDataChannelScreenSharing {
  type: 'groupCallDataChannelScreenSharing' = 'groupCallDataChannelScreenSharing';
  extra?: string;
}

/** Decode a TDLib `groupCallDataChannelScreenSharing` object; null input -> null. */
export function decodeGroupCallDataChannelScreenSharing(json: TdJsonRaw): GroupCallDataChannelScreenSharing | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallDataChannelScreenSharing = new GroupCallDataChannelScreenSharing();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `groupCallDataChannelScreenSharing` into TDLib JSON wire shape. */
export function encodeGroupCallDataChannelScreenSharing(value: GroupCallDataChannelScreenSharing): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallDataChannelScreenSharing';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** groupCallId = GroupCallId — Contains the group call identifier */
export class GroupCallId {
  type: 'groupCallId' = 'groupCallId';
  extra?: string;
  /** id: int32 — Group call identifier */
  id: number = 0;
}

/** Decode a TDLib `groupCallId` object; null input -> null. */
export function decodeGroupCallId(json: TdJsonRaw): GroupCallId | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallId = new GroupCallId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  return out;
}

/** Encode a `groupCallId` into TDLib JSON wire shape. */
export function encodeGroupCallId(value: GroupCallId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  return out;
}

/** groupCallInfo = GroupCallInfo — Contains information about a just created or just joined group call */
export class GroupCallInfo {
  type: 'groupCallInfo' = 'groupCallInfo';
  extra?: string;
  /** group_call_id: int32 — Identifier of the group call */
  group_call_id: number = 0;
  /** join_payload: string — Join response payload for tgcalls; empty if the call isn't joined */
  join_payload: string = '';
}

/** Decode a TDLib `groupCallInfo` object; null input -> null. */
export function decodeGroupCallInfo(json: TdJsonRaw): GroupCallInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallInfo = new GroupCallInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.join_payload = asString(record['join_payload']);
  return out;
}

/** Encode a `groupCallInfo` into TDLib JSON wire shape. */
export function encodeGroupCallInfo(value: GroupCallInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['join_payload'] = value.join_payload;
  return out;
}

/** groupCallJoinParameters = GroupCallJoinParameters — Describes parameters used to join a group call */
export class GroupCallJoinParameters {
  type: 'groupCallJoinParameters' = 'groupCallJoinParameters';
  extra?: string;
  /** audio_source_id: int32 — Audio channel synchronization source identifier; received from tgcalls */
  audio_source_id: number = 0;
  /** payload: string — Group call join payload; received from tgcalls */
  payload: string = '';
  /** is_muted: Bool — Pass true to join the call with muted microphone */
  is_muted: boolean = false;
  /** is_my_video_enabled: Bool — Pass true if the user's video is enabled */
  is_my_video_enabled: boolean = false;
}

/** Decode a TDLib `groupCallJoinParameters` object; null input -> null. */
export function decodeGroupCallJoinParameters(json: TdJsonRaw): GroupCallJoinParameters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallJoinParameters = new GroupCallJoinParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio_source_id = asNumber(record['audio_source_id']);
  out.payload = asString(record['payload']);
  out.is_muted = asBool(record['is_muted']);
  out.is_my_video_enabled = asBool(record['is_my_video_enabled']);
  return out;
}

/** Encode a `groupCallJoinParameters` into TDLib JSON wire shape. */
export function encodeGroupCallJoinParameters(value: GroupCallJoinParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallJoinParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio_source_id'] = value.audio_source_id;
  out['payload'] = value.payload;
  out['is_muted'] = value.is_muted;
  out['is_my_video_enabled'] = value.is_my_video_enabled;
  return out;
}

/** groupCallMessage = GroupCallMessage — Represents a message sent in a group call */
export class GroupCallMessage {
  type: 'groupCallMessage' = 'groupCallMessage';
  extra?: string;
  /** message_id: int32 — Unique message identifier within the group call */
  message_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of the message */
  sender_id: TdMessageSender | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the message was sent */
  date: number = 0;
  /** text: formattedText — Text of the message. If empty, then the message is a paid reaction in a live story */
  text: FormattedText | null = null;
  /** paid_message_star_count: int53 — The number of Telegram Stars that were paid to send the message; for live stories only */
  paid_message_star_count: number = 0;
  /** is_from_owner: Bool — True, if the message is sent by the owner of the call and must be treated as a message of the maximum level; for live stories only */
  is_from_owner: boolean = false;
  /** can_be_deleted: Bool — True, if the message can be deleted by the current user; for live stories only */
  can_be_deleted: boolean = false;
}

/** Decode a TDLib `groupCallMessage` object; null input -> null. */
export function decodeGroupCallMessage(json: TdJsonRaw): GroupCallMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallMessage = new GroupCallMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_id = asNumber(record['message_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.date = asNumber(record['date']);
  out.text = decodeFormattedText(record['text']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  out.is_from_owner = asBool(record['is_from_owner']);
  out.can_be_deleted = asBool(record['can_be_deleted']);
  return out;
}

/** Encode a `groupCallMessage` into TDLib JSON wire shape. */
export function encodeGroupCallMessage(value: GroupCallMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_id'] = value.message_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['date'] = value.date;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['paid_message_star_count'] = value.paid_message_star_count;
  out['is_from_owner'] = value.is_from_owner;
  out['can_be_deleted'] = value.can_be_deleted;
  return out;
}

/** groupCallMessageLevel = GroupCallMessageLevel — Represents a level of features for a message sent in a live story group call */
export class GroupCallMessageLevel {
  type: 'groupCallMessageLevel' = 'groupCallMessageLevel';
  extra?: string;
  /** min_star_count: int53 — The minimum number of Telegram Stars required to get features of the level */
  min_star_count: number = 0;
  /** pin_duration: int32 — The amount of time the message of this level will be pinned, in seconds */
  pin_duration: number = 0;
  /** max_text_length: int32 — The maximum allowed length of the message text */
  max_text_length: number = 0;
  /** max_custom_emoji_count: int32 — The maximum allowed number of custom emoji in the message text */
  max_custom_emoji_count: number = 0;
  /** first_color: int32 — The first color used to show the message text in the RGB format */
  first_color: number = 0;
  /** second_color: int32 — The second color used to show the message text in the RGB format */
  second_color: number = 0;
  /** background_color: int32 — Background color for the message the RGB format */
  background_color: number = 0;
}

/** Decode a TDLib `groupCallMessageLevel` object; null input -> null. */
export function decodeGroupCallMessageLevel(json: TdJsonRaw): GroupCallMessageLevel | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallMessageLevel = new GroupCallMessageLevel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.min_star_count = asNumber(record['min_star_count']);
  out.pin_duration = asNumber(record['pin_duration']);
  out.max_text_length = asNumber(record['max_text_length']);
  out.max_custom_emoji_count = asNumber(record['max_custom_emoji_count']);
  out.first_color = asNumber(record['first_color']);
  out.second_color = asNumber(record['second_color']);
  out.background_color = asNumber(record['background_color']);
  return out;
}

/** Encode a `groupCallMessageLevel` into TDLib JSON wire shape. */
export function encodeGroupCallMessageLevel(value: GroupCallMessageLevel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallMessageLevel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['min_star_count'] = value.min_star_count;
  out['pin_duration'] = value.pin_duration;
  out['max_text_length'] = value.max_text_length;
  out['max_custom_emoji_count'] = value.max_custom_emoji_count;
  out['first_color'] = value.first_color;
  out['second_color'] = value.second_color;
  out['background_color'] = value.background_color;
  return out;
}

/** groupCallParticipant = GroupCallParticipant — Represents a group call participant */
export class GroupCallParticipant {
  type: 'groupCallParticipant' = 'groupCallParticipant';
  extra?: string;
  /** participant_id: MessageSender — Identifier of the group call participant */
  participant_id: TdMessageSender | null = null;
  /** audio_source_id: int32 — User's audio channel synchronization source identifier */
  audio_source_id: number = 0;
  /** screen_sharing_audio_source_id: int32 — User's screen sharing audio channel synchronization source identifier */
  screen_sharing_audio_source_id: number = 0;
  /** video_info: groupCallParticipantVideoInfo — Information about user's video channel; may be null if there is no active video */
  video_info: GroupCallParticipantVideoInfo | null = null;
  /** screen_sharing_video_info: groupCallParticipantVideoInfo — Information about user's screen sharing video channel; may be null if there is no active screen sharing video */
  screen_sharing_video_info: GroupCallParticipantVideoInfo | null = null;
  /** bio: string — The participant user's bio or the participant chat's description */
  bio: string = '';
  /** is_current_user: Bool — True, if the participant is the current user */
  is_current_user: boolean = false;
  /** is_speaking: Bool — True, if the participant is speaking as set by setGroupCallParticipantIsSpeaking */
  is_speaking: boolean = false;
  /** is_hand_raised: Bool — True, if the participant hand is raised */
  is_hand_raised: boolean = false;
  /** can_be_muted_for_all_users: Bool — True, if the current user can mute the participant for all other group call participants */
  can_be_muted_for_all_users: boolean = false;
  /** can_be_unmuted_for_all_users: Bool — True, if the current user can allow the participant to unmute themselves or unmute the participant (if the participant is the current user) */
  can_be_unmuted_for_all_users: boolean = false;
  /** can_be_muted_for_current_user: Bool — True, if the current user can mute the participant only for self */
  can_be_muted_for_current_user: boolean = false;
  /** can_be_unmuted_for_current_user: Bool — True, if the current user can unmute the participant for self */
  can_be_unmuted_for_current_user: boolean = false;
  /** is_muted_for_all_users: Bool — True, if the participant is muted for all users */
  is_muted_for_all_users: boolean = false;
  /** is_muted_for_current_user: Bool — True, if the participant is muted for the current user */
  is_muted_for_current_user: boolean = false;
  /** can_unmute_self: Bool — True, if the participant is muted for all users, but can unmute themselves */
  can_unmute_self: boolean = false;
  /** volume_level: int32 — Participant's volume level; 1-20000 in hundreds of percents */
  volume_level: number = 0;
  /** order: string — User's order in the group call participant list. Orders must be compared lexicographically. The bigger is order, the higher is user in the list. If order is empty, the user must be removed from the participant list */
  order: string = '';
}

/** Decode a TDLib `groupCallParticipant` object; null input -> null. */
export function decodeGroupCallParticipant(json: TdJsonRaw): GroupCallParticipant | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallParticipant = new GroupCallParticipant();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.participant_id = decodeTdMessageSender(record['participant_id']);
  out.audio_source_id = asNumber(record['audio_source_id']);
  out.screen_sharing_audio_source_id = asNumber(record['screen_sharing_audio_source_id']);
  out.video_info = decodeGroupCallParticipantVideoInfo(record['video_info']);
  out.screen_sharing_video_info = decodeGroupCallParticipantVideoInfo(record['screen_sharing_video_info']);
  out.bio = asString(record['bio']);
  out.is_current_user = asBool(record['is_current_user']);
  out.is_speaking = asBool(record['is_speaking']);
  out.is_hand_raised = asBool(record['is_hand_raised']);
  out.can_be_muted_for_all_users = asBool(record['can_be_muted_for_all_users']);
  out.can_be_unmuted_for_all_users = asBool(record['can_be_unmuted_for_all_users']);
  out.can_be_muted_for_current_user = asBool(record['can_be_muted_for_current_user']);
  out.can_be_unmuted_for_current_user = asBool(record['can_be_unmuted_for_current_user']);
  out.is_muted_for_all_users = asBool(record['is_muted_for_all_users']);
  out.is_muted_for_current_user = asBool(record['is_muted_for_current_user']);
  out.can_unmute_self = asBool(record['can_unmute_self']);
  out.volume_level = asNumber(record['volume_level']);
  out.order = asString(record['order']);
  return out;
}

/** Encode a `groupCallParticipant` into TDLib JSON wire shape. */
export function encodeGroupCallParticipant(value: GroupCallParticipant): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallParticipant';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['participant_id'] = value.participant_id === null ? null : encodeTdMessageSender(value.participant_id);
  out['audio_source_id'] = value.audio_source_id;
  out['screen_sharing_audio_source_id'] = value.screen_sharing_audio_source_id;
  out['video_info'] = value.video_info === null ? null : encodeGroupCallParticipantVideoInfo(value.video_info);
  out['screen_sharing_video_info'] = value.screen_sharing_video_info === null ? null : encodeGroupCallParticipantVideoInfo(value.screen_sharing_video_info);
  out['bio'] = value.bio;
  out['is_current_user'] = value.is_current_user;
  out['is_speaking'] = value.is_speaking;
  out['is_hand_raised'] = value.is_hand_raised;
  out['can_be_muted_for_all_users'] = value.can_be_muted_for_all_users;
  out['can_be_unmuted_for_all_users'] = value.can_be_unmuted_for_all_users;
  out['can_be_muted_for_current_user'] = value.can_be_muted_for_current_user;
  out['can_be_unmuted_for_current_user'] = value.can_be_unmuted_for_current_user;
  out['is_muted_for_all_users'] = value.is_muted_for_all_users;
  out['is_muted_for_current_user'] = value.is_muted_for_current_user;
  out['can_unmute_self'] = value.can_unmute_self;
  out['volume_level'] = value.volume_level;
  out['order'] = value.order;
  return out;
}

/** groupCallParticipantVideoInfo = GroupCallParticipantVideoInfo — Contains information about a group call participant's video channel */
export class GroupCallParticipantVideoInfo {
  type: 'groupCallParticipantVideoInfo' = 'groupCallParticipantVideoInfo';
  extra?: string;
  /** source_groups: vector<groupCallVideoSourceGroup> — List of synchronization source groups of the video */
  source_groups: GroupCallVideoSourceGroup[] = [];
  /** endpoint_id: string — Video channel endpoint identifier */
  endpoint_id: string = '';
  /** is_paused: Bool — True, if the video is paused. This flag needs to be ignored, if new video frames are received */
  is_paused: boolean = false;
}

/** Decode a TDLib `groupCallParticipantVideoInfo` object; null input -> null. */
export function decodeGroupCallParticipantVideoInfo(json: TdJsonRaw): GroupCallParticipantVideoInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallParticipantVideoInfo = new GroupCallParticipantVideoInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.source_groups = decodeVector(record['source_groups'], (item: TdJsonRaw): GroupCallVideoSourceGroup => decodeGroupCallVideoSourceGroup(item) as GroupCallVideoSourceGroup);
  out.endpoint_id = asString(record['endpoint_id']);
  out.is_paused = asBool(record['is_paused']);
  return out;
}

/** Encode a `groupCallParticipantVideoInfo` into TDLib JSON wire shape. */
export function encodeGroupCallParticipantVideoInfo(value: GroupCallParticipantVideoInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallParticipantVideoInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['source_groups'] = encodeVector(value.source_groups, (item: GroupCallVideoSourceGroup | null): TdJsonRaw => item === null ? null : encodeGroupCallVideoSourceGroup(item));
  out['endpoint_id'] = value.endpoint_id;
  out['is_paused'] = value.is_paused;
  return out;
}

/** groupCallParticipants = GroupCallParticipants — Contains identifiers of group call participants */
export class GroupCallParticipants {
  type: 'groupCallParticipants' = 'groupCallParticipants';
  extra?: string;
  /** total_count: int32 — Total number of group call participants */
  total_count: number = 0;
  /** participant_ids: vector<MessageSender> — Identifiers of the participants */
  participant_ids: TdMessageSender[] = [];
}

/** Decode a TDLib `groupCallParticipants` object; null input -> null. */
export function decodeGroupCallParticipants(json: TdJsonRaw): GroupCallParticipants | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallParticipants = new GroupCallParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.participant_ids = decodeVector(record['participant_ids'], (item: TdJsonRaw): TdMessageSender => decodeTdMessageSender(item) as TdMessageSender);
  return out;
}

/** Encode a `groupCallParticipants` into TDLib JSON wire shape. */
export function encodeGroupCallParticipants(value: GroupCallParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['participant_ids'] = encodeVector(value.participant_ids, (item: TdMessageSender | null): TdJsonRaw => item === null ? null : encodeTdMessageSender(item));
  return out;
}

/** groupCallRecentSpeaker = GroupCallRecentSpeaker — Describes a recently speaking participant in a group call */
export class GroupCallRecentSpeaker {
  type: 'groupCallRecentSpeaker' = 'groupCallRecentSpeaker';
  extra?: string;
  /** participant_id: MessageSender — Group call participant identifier */
  participant_id: TdMessageSender | null = null;
  /** is_speaking: Bool — True, is the user has spoken recently */
  is_speaking: boolean = false;
}

/** Decode a TDLib `groupCallRecentSpeaker` object; null input -> null. */
export function decodeGroupCallRecentSpeaker(json: TdJsonRaw): GroupCallRecentSpeaker | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallRecentSpeaker = new GroupCallRecentSpeaker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.participant_id = decodeTdMessageSender(record['participant_id']);
  out.is_speaking = asBool(record['is_speaking']);
  return out;
}

/** Encode a `groupCallRecentSpeaker` into TDLib JSON wire shape. */
export function encodeGroupCallRecentSpeaker(value: GroupCallRecentSpeaker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallRecentSpeaker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['participant_id'] = value.participant_id === null ? null : encodeTdMessageSender(value.participant_id);
  out['is_speaking'] = value.is_speaking;
  return out;
}

/** groupCallStream = GroupCallStream — Describes an available stream in a video chat or a live story */
export class GroupCallStream {
  type: 'groupCallStream' = 'groupCallStream';
  extra?: string;
  /** channel_id: int32 — Identifier of an audio/video channel */
  channel_id: number = 0;
  /** scale: int32 — Scale of segment durations in the stream. The duration is 1000/(2**scale) milliseconds */
  scale: number = 0;
  /** time_offset: int53 — Point in time when the stream currently ends; Unix timestamp in milliseconds */
  time_offset: number = 0;
}

/** Decode a TDLib `groupCallStream` object; null input -> null. */
export function decodeGroupCallStream(json: TdJsonRaw): GroupCallStream | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallStream = new GroupCallStream();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.channel_id = asNumber(record['channel_id']);
  out.scale = asNumber(record['scale']);
  out.time_offset = asNumber(record['time_offset']);
  return out;
}

/** Encode a `groupCallStream` into TDLib JSON wire shape. */
export function encodeGroupCallStream(value: GroupCallStream): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallStream';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['channel_id'] = value.channel_id;
  out['scale'] = value.scale;
  out['time_offset'] = value.time_offset;
  return out;
}

/** groupCallStreams = GroupCallStreams — Represents a list of group call streams */
export class GroupCallStreams {
  type: 'groupCallStreams' = 'groupCallStreams';
  extra?: string;
  /** streams: vector<groupCallStream> — A list of group call streams */
  streams: GroupCallStream[] = [];
}

/** Decode a TDLib `groupCallStreams` object; null input -> null. */
export function decodeGroupCallStreams(json: TdJsonRaw): GroupCallStreams | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallStreams = new GroupCallStreams();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.streams = decodeVector(record['streams'], (item: TdJsonRaw): GroupCallStream => decodeGroupCallStream(item) as GroupCallStream);
  return out;
}

/** Encode a `groupCallStreams` into TDLib JSON wire shape. */
export function encodeGroupCallStreams(value: GroupCallStreams): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallStreams';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['streams'] = encodeVector(value.streams, (item: GroupCallStream | null): TdJsonRaw => item === null ? null : encodeGroupCallStream(item));
  return out;
}

/** groupCallVideoQualityFull = GroupCallVideoQuality — The best available video quality */
export class GroupCallVideoQualityFull {
  type: 'groupCallVideoQualityFull' = 'groupCallVideoQualityFull';
  extra?: string;
}

/** Decode a TDLib `groupCallVideoQualityFull` object; null input -> null. */
export function decodeGroupCallVideoQualityFull(json: TdJsonRaw): GroupCallVideoQualityFull | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallVideoQualityFull = new GroupCallVideoQualityFull();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `groupCallVideoQualityFull` into TDLib JSON wire shape. */
export function encodeGroupCallVideoQualityFull(value: GroupCallVideoQualityFull): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallVideoQualityFull';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** groupCallVideoQualityMedium = GroupCallVideoQuality — The medium video quality */
export class GroupCallVideoQualityMedium {
  type: 'groupCallVideoQualityMedium' = 'groupCallVideoQualityMedium';
  extra?: string;
}

/** Decode a TDLib `groupCallVideoQualityMedium` object; null input -> null. */
export function decodeGroupCallVideoQualityMedium(json: TdJsonRaw): GroupCallVideoQualityMedium | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallVideoQualityMedium = new GroupCallVideoQualityMedium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `groupCallVideoQualityMedium` into TDLib JSON wire shape. */
export function encodeGroupCallVideoQualityMedium(value: GroupCallVideoQualityMedium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallVideoQualityMedium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** groupCallVideoQualityThumbnail = GroupCallVideoQuality — The worst available video quality */
export class GroupCallVideoQualityThumbnail {
  type: 'groupCallVideoQualityThumbnail' = 'groupCallVideoQualityThumbnail';
  extra?: string;
}

/** Decode a TDLib `groupCallVideoQualityThumbnail` object; null input -> null. */
export function decodeGroupCallVideoQualityThumbnail(json: TdJsonRaw): GroupCallVideoQualityThumbnail | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallVideoQualityThumbnail = new GroupCallVideoQualityThumbnail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `groupCallVideoQualityThumbnail` into TDLib JSON wire shape. */
export function encodeGroupCallVideoQualityThumbnail(value: GroupCallVideoQualityThumbnail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallVideoQualityThumbnail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** groupCallVideoSourceGroup = GroupCallVideoSourceGroup — Describes a group of video synchronization source identifiers */
export class GroupCallVideoSourceGroup {
  type: 'groupCallVideoSourceGroup' = 'groupCallVideoSourceGroup';
  extra?: string;
  /** semantics: string — The semantics of sources, one of "SIM" or "FID" */
  semantics: string = '';
  /** source_ids: vector<int32> — The list of synchronization source identifiers */
  source_ids: number[] = [];
}

/** Decode a TDLib `groupCallVideoSourceGroup` object; null input -> null. */
export function decodeGroupCallVideoSourceGroup(json: TdJsonRaw): GroupCallVideoSourceGroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: GroupCallVideoSourceGroup = new GroupCallVideoSourceGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.semantics = asString(record['semantics']);
  out.source_ids = decodeVector(record['source_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `groupCallVideoSourceGroup` into TDLib JSON wire shape. */
export function encodeGroupCallVideoSourceGroup(value: GroupCallVideoSourceGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'groupCallVideoSourceGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['semantics'] = value.semantics;
  out['source_ids'] = encodeVector(value.source_ids, (item: number): TdJsonRaw => item);
  return out;
}
