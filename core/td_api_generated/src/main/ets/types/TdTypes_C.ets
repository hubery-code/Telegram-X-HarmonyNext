/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { AccountInfo, AffiliateProgramParameters, AnimatedChatPhoto, decodeAccountInfo, decodeAffiliateProgramParameters, decodeAnimatedChatPhoto, encodeAccountInfo, encodeAffiliateProgramParameters, encodeAnimatedChatPhoto } from './TdTypes_A';
import { Background, Birthdate, BusinessBotManageBar, decodeBackground, decodeBirthdate, decodeBusinessBotManageBar, encodeBackground, encodeBirthdate, encodeBusinessBotManageBar } from './TdTypes_B';
import { DateRange, DraftMessage, decodeDateRange, decodeDraftMessage, encodeDateRange, encodeDraftMessage } from './TdTypes_D';
import { EmojiStatus, ErrorValue, decodeEmojiStatus, decodeErrorValue, encodeEmojiStatus, encodeErrorValue } from './TdTypes_E';
import { FailedToAddMembers, File, FormattedText, ForumTopicIcon, ForumTopicInfo, decodeFailedToAddMembers, decodeFile, decodeFormattedText, decodeForumTopicIcon, decodeForumTopicInfo, encodeFailedToAddMembers, encodeFile, encodeFormattedText, encodeForumTopicIcon, encodeForumTopicInfo } from './TdTypes_F';
import { GiftChatTheme, GroupCallJoinParameters, decodeGiftChatTheme, decodeGroupCallJoinParameters, encodeGiftChatTheme, encodeGroupCallJoinParameters } from './TdTypes_G';
import { ImportedContact, InputBusinessChatLink, InputRichMessage, decodeImportedContact, decodeInputBusinessChatLink, decodeInputRichMessage, encodeImportedContact, encodeInputBusinessChatLink, encodeInputRichMessage } from './TdTypes_I';
import { Location, decodeLocation, encodeLocation } from './TdTypes_L';
import { Message, Minithumbnail, decodeMessage, decodeMinithumbnail, encodeMessage, encodeMinithumbnail } from './TdTypes_M';
import { NewSticker, decodeNewSticker, encodeNewSticker } from './TdTypes_N';
import { PhotoSize, PrepaidGiveaway, decodePhotoSize, decodePrepaidGiveaway, encodePhotoSize, encodePrepaidGiveaway } from './TdTypes_P';
import { StarSubscriptionPricing, StatisticalValue, StoryInfo, decodeStarSubscriptionPricing, decodeStatisticalValue, decodeStoryInfo, encodeStarSubscriptionPricing, encodeStatisticalValue, encodeStoryInfo } from './TdTypes_S';
import { UpgradedGift, UpgradedGiftColors, decodeUpgradedGift, decodeUpgradedGiftColors, encodeUpgradedGift, encodeUpgradedGiftColors } from './TdTypes_U';
import { VerificationStatus, VideoChat, decodeVerificationStatus, decodeVideoChat, encodeVerificationStatus, encodeVideoChat } from './TdTypes_V';
import { TdAffiliateType, decodeTdAffiliateType, encodeTdAffiliateType } from './TdUnions_A';
import { TdBackgroundFill, TdBlockList, decodeTdBackgroundFill, decodeTdBlockList, encodeTdBackgroundFill, encodeTdBlockList } from './TdUnions_B';
import { TdCallDiscardReason, TdCallServerType, TdCallState, TdChatActionBar, TdChatAvailableReactions, TdChatBoostSource, TdChatEventAction, TdChatList, TdChatMemberStatus, TdChatPhotoStickerType, TdChatRevenueTransactionType, TdChatSource, TdChatStatisticsObjectType, TdChatTheme, TdChatType, TdCommunityMemberStatus, decodeTdCallDiscardReason, decodeTdCallServerType, decodeTdCallState, decodeTdChatActionBar, decodeTdChatAvailableReactions, decodeTdChatBoostSource, decodeTdChatEventAction, decodeTdChatList, decodeTdChatMemberStatus, decodeTdChatPhotoStickerType, decodeTdChatRevenueTransactionType, decodeTdChatSource, decodeTdChatStatisticsObjectType, decodeTdChatTheme, decodeTdChatType, decodeTdCommunityMemberStatus, encodeTdCallDiscardReason, encodeTdCallServerType, encodeTdCallState, encodeTdChatActionBar, encodeTdChatAvailableReactions, encodeTdChatBoostSource, encodeTdChatEventAction, encodeTdChatList, encodeTdChatMemberStatus, encodeTdChatPhotoStickerType, encodeTdChatRevenueTransactionType, encodeTdChatSource, encodeTdChatStatisticsObjectType, encodeTdChatTheme, encodeTdChatType, encodeTdCommunityMemberStatus } from './TdUnions_C';
import { TdEmailAddressAuthentication, decodeTdEmailAddressAuthentication, encodeTdEmailAddressAuthentication } from './TdUnions_E';
import { TdInputMessageContent, TdInviteLinkChatType, decodeTdInputMessageContent, decodeTdInviteLinkChatType, encodeTdInputMessageContent, encodeTdInviteLinkChatType } from './TdUnions_I';
import { TdMessageSender, TdMessageTopic, decodeTdMessageSender, decodeTdMessageTopic, encodeTdMessageSender, encodeTdMessageTopic } from './TdUnions_M';
import { TdPublicChatType, decodeTdPublicChatType, encodeTdPublicChatType } from './TdUnions_P';
import { TdReactionType, TdRevenueWithdrawalState, decodeTdReactionType, decodeTdRevenueWithdrawalState, encodeTdReactionType, encodeTdRevenueWithdrawalState } from './TdUnions_R';
import { TdStatisticalGraph, TdStickerType, TdStorePaymentPurpose, TdStoryList, decodeTdStatisticalGraph, decodeTdStickerType, decodeTdStorePaymentPurpose, decodeTdStoryList, encodeTdStatisticalGraph, encodeTdStickerType, encodeTdStorePaymentPurpose, encodeTdStoryList } from './TdUnions_S';
import { TdVectorPathCommand, decodeTdVectorPathCommand, encodeTdVectorPathCommand } from './TdUnions_V';

/** call = Call — Describes a call */
export class Call {
  type: 'call' = 'call';
  extra?: string;
  /** id: int32 — Call identifier, not persistent */
  id: number = 0;
  /** unique_id: int64 — Persistent unique call identifier; 0 if isn't assigned yet by the server */
  unique_id: string = '';
  /** user_id: int53 — User identifier of the other call participant */
  user_id: number = 0;
  /** is_outgoing: Bool — True, if the call is outgoing */
  is_outgoing: boolean = false;
  /** is_video: Bool — True, if the call is a video call */
  is_video: boolean = false;
  /** state: CallState — Call state */
  state: TdCallState | null = null;
}

/** Decode a TDLib `call` object; null input -> null. */
export function decodeCall(json: TdJsonRaw): Call | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Call = new Call();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.unique_id = asInt64(record['unique_id']);
  out.user_id = asNumber(record['user_id']);
  out.is_outgoing = asBool(record['is_outgoing']);
  out.is_video = asBool(record['is_video']);
  out.state = decodeTdCallState(record['state']);
  return out;
}

/** Encode a `call` into TDLib JSON wire shape. */
export function encodeCall(value: Call): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'call';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['unique_id'] = value.unique_id;
  out['user_id'] = value.user_id;
  out['is_outgoing'] = value.is_outgoing;
  out['is_video'] = value.is_video;
  out['state'] = value.state === null ? null : encodeTdCallState(value.state);
  return out;
}

/** callDiscardReasonDeclined = CallDiscardReason — The call was ended before the conversation started. It was declined by the other party */
export class CallDiscardReasonDeclined {
  type: 'callDiscardReasonDeclined' = 'callDiscardReasonDeclined';
  extra?: string;
}

/** Decode a TDLib `callDiscardReasonDeclined` object; null input -> null. */
export function decodeCallDiscardReasonDeclined(json: TdJsonRaw): CallDiscardReasonDeclined | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallDiscardReasonDeclined = new CallDiscardReasonDeclined();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callDiscardReasonDeclined` into TDLib JSON wire shape. */
export function encodeCallDiscardReasonDeclined(value: CallDiscardReasonDeclined): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callDiscardReasonDeclined';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callDiscardReasonDisconnected = CallDiscardReason — The call was ended during the conversation because the users were disconnected */
export class CallDiscardReasonDisconnected {
  type: 'callDiscardReasonDisconnected' = 'callDiscardReasonDisconnected';
  extra?: string;
}

/** Decode a TDLib `callDiscardReasonDisconnected` object; null input -> null. */
export function decodeCallDiscardReasonDisconnected(json: TdJsonRaw): CallDiscardReasonDisconnected | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallDiscardReasonDisconnected = new CallDiscardReasonDisconnected();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callDiscardReasonDisconnected` into TDLib JSON wire shape. */
export function encodeCallDiscardReasonDisconnected(value: CallDiscardReasonDisconnected): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callDiscardReasonDisconnected';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callDiscardReasonEmpty = CallDiscardReason — The call wasn't discarded, or the reason is unknown */
export class CallDiscardReasonEmpty {
  type: 'callDiscardReasonEmpty' = 'callDiscardReasonEmpty';
  extra?: string;
}

/** Decode a TDLib `callDiscardReasonEmpty` object; null input -> null. */
export function decodeCallDiscardReasonEmpty(json: TdJsonRaw): CallDiscardReasonEmpty | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallDiscardReasonEmpty = new CallDiscardReasonEmpty();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callDiscardReasonEmpty` into TDLib JSON wire shape. */
export function encodeCallDiscardReasonEmpty(value: CallDiscardReasonEmpty): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callDiscardReasonEmpty';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callDiscardReasonHungUp = CallDiscardReason — The call was ended because one of the parties hung up */
export class CallDiscardReasonHungUp {
  type: 'callDiscardReasonHungUp' = 'callDiscardReasonHungUp';
  extra?: string;
}

/** Decode a TDLib `callDiscardReasonHungUp` object; null input -> null. */
export function decodeCallDiscardReasonHungUp(json: TdJsonRaw): CallDiscardReasonHungUp | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallDiscardReasonHungUp = new CallDiscardReasonHungUp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callDiscardReasonHungUp` into TDLib JSON wire shape. */
export function encodeCallDiscardReasonHungUp(value: CallDiscardReasonHungUp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callDiscardReasonHungUp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callDiscardReasonMissed = CallDiscardReason — The call was ended before the conversation started. It was canceled by the caller or missed by the other party */
export class CallDiscardReasonMissed {
  type: 'callDiscardReasonMissed' = 'callDiscardReasonMissed';
  extra?: string;
}

/** Decode a TDLib `callDiscardReasonMissed` object; null input -> null. */
export function decodeCallDiscardReasonMissed(json: TdJsonRaw): CallDiscardReasonMissed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallDiscardReasonMissed = new CallDiscardReasonMissed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callDiscardReasonMissed` into TDLib JSON wire shape. */
export function encodeCallDiscardReasonMissed(value: CallDiscardReasonMissed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callDiscardReasonMissed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callDiscardReasonUpgradeToGroupCall = CallDiscardReason — The call was ended because it has been upgraded to a group call */
export class CallDiscardReasonUpgradeToGroupCall {
  type: 'callDiscardReasonUpgradeToGroupCall' = 'callDiscardReasonUpgradeToGroupCall';
  extra?: string;
  /** invite_link: string — Invite link for the group call */
  invite_link: string = '';
}

/** Decode a TDLib `callDiscardReasonUpgradeToGroupCall` object; null input -> null. */
export function decodeCallDiscardReasonUpgradeToGroupCall(json: TdJsonRaw): CallDiscardReasonUpgradeToGroupCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallDiscardReasonUpgradeToGroupCall = new CallDiscardReasonUpgradeToGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `callDiscardReasonUpgradeToGroupCall` into TDLib JSON wire shape. */
export function encodeCallDiscardReasonUpgradeToGroupCall(value: CallDiscardReasonUpgradeToGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callDiscardReasonUpgradeToGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  return out;
}

/** callId = CallId — Contains the call identifier */
export class CallId {
  type: 'callId' = 'callId';
  extra?: string;
  /** id: int32 — Call identifier */
  id: number = 0;
}

/** Decode a TDLib `callId` object; null input -> null. */
export function decodeCallId(json: TdJsonRaw): CallId | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallId = new CallId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  return out;
}

/** Encode a `callId` into TDLib JSON wire shape. */
export function encodeCallId(value: CallId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  return out;
}

/** callProblemDistortedSpeech = CallProblem — The speech was distorted */
export class CallProblemDistortedSpeech {
  type: 'callProblemDistortedSpeech' = 'callProblemDistortedSpeech';
  extra?: string;
}

/** Decode a TDLib `callProblemDistortedSpeech` object; null input -> null. */
export function decodeCallProblemDistortedSpeech(json: TdJsonRaw): CallProblemDistortedSpeech | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProblemDistortedSpeech = new CallProblemDistortedSpeech();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callProblemDistortedSpeech` into TDLib JSON wire shape. */
export function encodeCallProblemDistortedSpeech(value: CallProblemDistortedSpeech): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProblemDistortedSpeech';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callProblemDistortedVideo = CallProblem — The video was distorted */
export class CallProblemDistortedVideo {
  type: 'callProblemDistortedVideo' = 'callProblemDistortedVideo';
  extra?: string;
}

/** Decode a TDLib `callProblemDistortedVideo` object; null input -> null. */
export function decodeCallProblemDistortedVideo(json: TdJsonRaw): CallProblemDistortedVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProblemDistortedVideo = new CallProblemDistortedVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callProblemDistortedVideo` into TDLib JSON wire shape. */
export function encodeCallProblemDistortedVideo(value: CallProblemDistortedVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProblemDistortedVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callProblemDropped = CallProblem — The call ended unexpectedly */
export class CallProblemDropped {
  type: 'callProblemDropped' = 'callProblemDropped';
  extra?: string;
}

/** Decode a TDLib `callProblemDropped` object; null input -> null. */
export function decodeCallProblemDropped(json: TdJsonRaw): CallProblemDropped | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProblemDropped = new CallProblemDropped();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callProblemDropped` into TDLib JSON wire shape. */
export function encodeCallProblemDropped(value: CallProblemDropped): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProblemDropped';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callProblemEcho = CallProblem — The user heard their own voice */
export class CallProblemEcho {
  type: 'callProblemEcho' = 'callProblemEcho';
  extra?: string;
}

/** Decode a TDLib `callProblemEcho` object; null input -> null. */
export function decodeCallProblemEcho(json: TdJsonRaw): CallProblemEcho | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProblemEcho = new CallProblemEcho();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callProblemEcho` into TDLib JSON wire shape. */
export function encodeCallProblemEcho(value: CallProblemEcho): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProblemEcho';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callProblemInterruptions = CallProblem — The other side kept disappearing */
export class CallProblemInterruptions {
  type: 'callProblemInterruptions' = 'callProblemInterruptions';
  extra?: string;
}

/** Decode a TDLib `callProblemInterruptions` object; null input -> null. */
export function decodeCallProblemInterruptions(json: TdJsonRaw): CallProblemInterruptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProblemInterruptions = new CallProblemInterruptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callProblemInterruptions` into TDLib JSON wire shape. */
export function encodeCallProblemInterruptions(value: CallProblemInterruptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProblemInterruptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callProblemNoise = CallProblem — The user heard background noise */
export class CallProblemNoise {
  type: 'callProblemNoise' = 'callProblemNoise';
  extra?: string;
}

/** Decode a TDLib `callProblemNoise` object; null input -> null. */
export function decodeCallProblemNoise(json: TdJsonRaw): CallProblemNoise | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProblemNoise = new CallProblemNoise();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callProblemNoise` into TDLib JSON wire shape. */
export function encodeCallProblemNoise(value: CallProblemNoise): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProblemNoise';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callProblemPixelatedVideo = CallProblem — The video was pixelated */
export class CallProblemPixelatedVideo {
  type: 'callProblemPixelatedVideo' = 'callProblemPixelatedVideo';
  extra?: string;
}

/** Decode a TDLib `callProblemPixelatedVideo` object; null input -> null. */
export function decodeCallProblemPixelatedVideo(json: TdJsonRaw): CallProblemPixelatedVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProblemPixelatedVideo = new CallProblemPixelatedVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callProblemPixelatedVideo` into TDLib JSON wire shape. */
export function encodeCallProblemPixelatedVideo(value: CallProblemPixelatedVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProblemPixelatedVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callProblemSilentLocal = CallProblem — The user couldn't hear the other side */
export class CallProblemSilentLocal {
  type: 'callProblemSilentLocal' = 'callProblemSilentLocal';
  extra?: string;
}

/** Decode a TDLib `callProblemSilentLocal` object; null input -> null. */
export function decodeCallProblemSilentLocal(json: TdJsonRaw): CallProblemSilentLocal | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProblemSilentLocal = new CallProblemSilentLocal();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callProblemSilentLocal` into TDLib JSON wire shape. */
export function encodeCallProblemSilentLocal(value: CallProblemSilentLocal): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProblemSilentLocal';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callProblemSilentRemote = CallProblem — The other side couldn't hear the user */
export class CallProblemSilentRemote {
  type: 'callProblemSilentRemote' = 'callProblemSilentRemote';
  extra?: string;
}

/** Decode a TDLib `callProblemSilentRemote` object; null input -> null. */
export function decodeCallProblemSilentRemote(json: TdJsonRaw): CallProblemSilentRemote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProblemSilentRemote = new CallProblemSilentRemote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callProblemSilentRemote` into TDLib JSON wire shape. */
export function encodeCallProblemSilentRemote(value: CallProblemSilentRemote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProblemSilentRemote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callProtocol = CallProtocol — Specifies the supported call protocols */
export class CallProtocol {
  type: 'callProtocol' = 'callProtocol';
  extra?: string;
  /** udp_p2p: Bool — True, if UDP peer-to-peer connections are supported */
  udp_p2p: boolean = false;
  /** udp_reflector: Bool — True, if connection through UDP reflectors is supported */
  udp_reflector: boolean = false;
  /** min_layer: int32 — The minimum supported API layer; use 65 */
  min_layer: number = 0;
  /** max_layer: int32 — The maximum supported API layer; use 92 */
  max_layer: number = 0;
  /** library_versions: vector<string> — List of supported tgcalls versions */
  library_versions: string[] = [];
}

/** Decode a TDLib `callProtocol` object; null input -> null. */
export function decodeCallProtocol(json: TdJsonRaw): CallProtocol | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallProtocol = new CallProtocol();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.udp_p2p = asBool(record['udp_p2p']);
  out.udp_reflector = asBool(record['udp_reflector']);
  out.min_layer = asNumber(record['min_layer']);
  out.max_layer = asNumber(record['max_layer']);
  out.library_versions = decodeVector(record['library_versions'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `callProtocol` into TDLib JSON wire shape. */
export function encodeCallProtocol(value: CallProtocol): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callProtocol';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['udp_p2p'] = value.udp_p2p;
  out['udp_reflector'] = value.udp_reflector;
  out['min_layer'] = value.min_layer;
  out['max_layer'] = value.max_layer;
  out['library_versions'] = encodeVector(value.library_versions, (item: string): TdJsonRaw => item);
  return out;
}

/** callServer = CallServer — Describes a server for relaying call data */
export class CallServer {
  type: 'callServer' = 'callServer';
  extra?: string;
  /** id: int64 — Server identifier */
  id: string = '';
  /** ip_address: string — Server IPv4 address */
  ip_address: string = '';
  /** ipv6_address: string — Server IPv6 address */
  ipv6_address: string = '';
  /** port: int32 — Server port number */
  port: number = 0;
  /** type: CallServerType — Server type */
  type_: TdCallServerType | null = null;
}

/** Decode a TDLib `callServer` object; null input -> null. */
export function decodeCallServer(json: TdJsonRaw): CallServer | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallServer = new CallServer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.ip_address = asString(record['ip_address']);
  out.ipv6_address = asString(record['ipv6_address']);
  out.port = asNumber(record['port']);
  out.type_ = decodeTdCallServerType(record['type']);
  return out;
}

/** Encode a `callServer` into TDLib JSON wire shape. */
export function encodeCallServer(value: CallServer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callServer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['ip_address'] = value.ip_address;
  out['ipv6_address'] = value.ipv6_address;
  out['port'] = value.port;
  out['type'] = value.type_ === null ? null : encodeTdCallServerType(value.type_);
  return out;
}

/** callServerTypeTelegramReflector = CallServerType — A Telegram call reflector */
export class CallServerTypeTelegramReflector {
  type: 'callServerTypeTelegramReflector' = 'callServerTypeTelegramReflector';
  extra?: string;
  /** peer_tag: bytes — A peer tag to be used with the reflector */
  peer_tag: string = '';
  /** is_tcp: Bool — True, if the server uses TCP instead of UDP */
  is_tcp: boolean = false;
}

/** Decode a TDLib `callServerTypeTelegramReflector` object; null input -> null. */
export function decodeCallServerTypeTelegramReflector(json: TdJsonRaw): CallServerTypeTelegramReflector | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallServerTypeTelegramReflector = new CallServerTypeTelegramReflector();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.peer_tag = asString(record['peer_tag']);
  out.is_tcp = asBool(record['is_tcp']);
  return out;
}

/** Encode a `callServerTypeTelegramReflector` into TDLib JSON wire shape. */
export function encodeCallServerTypeTelegramReflector(value: CallServerTypeTelegramReflector): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callServerTypeTelegramReflector';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['peer_tag'] = value.peer_tag;
  out['is_tcp'] = value.is_tcp;
  return out;
}

/** callServerTypeWebrtc = CallServerType — A WebRTC server */
export class CallServerTypeWebrtc {
  type: 'callServerTypeWebrtc' = 'callServerTypeWebrtc';
  extra?: string;
  /** username: string — Username to be used for authentication */
  username: string = '';
  /** password: string — Authentication password */
  password: string = '';
  /** supports_turn: Bool — True, if the server supports TURN */
  supports_turn: boolean = false;
  /** supports_stun: Bool — True, if the server supports STUN */
  supports_stun: boolean = false;
}

/** Decode a TDLib `callServerTypeWebrtc` object; null input -> null. */
export function decodeCallServerTypeWebrtc(json: TdJsonRaw): CallServerTypeWebrtc | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallServerTypeWebrtc = new CallServerTypeWebrtc();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  out.password = asString(record['password']);
  out.supports_turn = asBool(record['supports_turn']);
  out.supports_stun = asBool(record['supports_stun']);
  return out;
}

/** Encode a `callServerTypeWebrtc` into TDLib JSON wire shape. */
export function encodeCallServerTypeWebrtc(value: CallServerTypeWebrtc): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callServerTypeWebrtc';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  out['password'] = value.password;
  out['supports_turn'] = value.supports_turn;
  out['supports_stun'] = value.supports_stun;
  return out;
}

/** callStateDiscarded = CallState — The call has ended successfully */
export class CallStateDiscarded {
  type: 'callStateDiscarded' = 'callStateDiscarded';
  extra?: string;
  /** reason: CallDiscardReason — The reason why the call has ended */
  reason: TdCallDiscardReason | null = null;
  /** need_rating: Bool — True, if the call rating must be sent to the server */
  need_rating: boolean = false;
  /** need_debug_information: Bool — True, if the call debug information must be sent to the server */
  need_debug_information: boolean = false;
  /** need_log: Bool — True, if the call log must be sent to the server */
  need_log: boolean = false;
}

/** Decode a TDLib `callStateDiscarded` object; null input -> null. */
export function decodeCallStateDiscarded(json: TdJsonRaw): CallStateDiscarded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallStateDiscarded = new CallStateDiscarded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reason = decodeTdCallDiscardReason(record['reason']);
  out.need_rating = asBool(record['need_rating']);
  out.need_debug_information = asBool(record['need_debug_information']);
  out.need_log = asBool(record['need_log']);
  return out;
}

/** Encode a `callStateDiscarded` into TDLib JSON wire shape. */
export function encodeCallStateDiscarded(value: CallStateDiscarded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callStateDiscarded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reason'] = value.reason === null ? null : encodeTdCallDiscardReason(value.reason);
  out['need_rating'] = value.need_rating;
  out['need_debug_information'] = value.need_debug_information;
  out['need_log'] = value.need_log;
  return out;
}

/** callStateError = CallState — The call has ended with an error */
export class CallStateError {
  type: 'callStateError' = 'callStateError';
  extra?: string;
  /** error: error — Error. An error with the code 4005000 will be returned if an outgoing call is missed because of an expired timeout */
  error: ErrorValue | null = null;
}

/** Decode a TDLib `callStateError` object; null input -> null. */
export function decodeCallStateError(json: TdJsonRaw): CallStateError | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallStateError = new CallStateError();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.error = decodeErrorValue(record['error']);
  return out;
}

/** Encode a `callStateError` into TDLib JSON wire shape. */
export function encodeCallStateError(value: CallStateError): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callStateError';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['error'] = value.error === null ? null : encodeErrorValue(value.error);
  return out;
}

/** callStateExchangingKeys = CallState — The call has been answered and encryption keys are being exchanged */
export class CallStateExchangingKeys {
  type: 'callStateExchangingKeys' = 'callStateExchangingKeys';
  extra?: string;
}

/** Decode a TDLib `callStateExchangingKeys` object; null input -> null. */
export function decodeCallStateExchangingKeys(json: TdJsonRaw): CallStateExchangingKeys | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallStateExchangingKeys = new CallStateExchangingKeys();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callStateExchangingKeys` into TDLib JSON wire shape. */
export function encodeCallStateExchangingKeys(value: CallStateExchangingKeys): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callStateExchangingKeys';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callStateHangingUp = CallState — The call is hanging up after discardCall has been called */
export class CallStateHangingUp {
  type: 'callStateHangingUp' = 'callStateHangingUp';
  extra?: string;
}

/** Decode a TDLib `callStateHangingUp` object; null input -> null. */
export function decodeCallStateHangingUp(json: TdJsonRaw): CallStateHangingUp | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallStateHangingUp = new CallStateHangingUp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `callStateHangingUp` into TDLib JSON wire shape. */
export function encodeCallStateHangingUp(value: CallStateHangingUp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callStateHangingUp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** callStatePending = CallState — The call is pending, waiting to be accepted by a user */
export class CallStatePending {
  type: 'callStatePending' = 'callStatePending';
  extra?: string;
  /** is_created: Bool — True, if the call has already been created by the server */
  is_created: boolean = false;
  /** is_received: Bool — True, if the call has already been received by the other party */
  is_received: boolean = false;
}

/** Decode a TDLib `callStatePending` object; null input -> null. */
export function decodeCallStatePending(json: TdJsonRaw): CallStatePending | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallStatePending = new CallStatePending();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_created = asBool(record['is_created']);
  out.is_received = asBool(record['is_received']);
  return out;
}

/** Encode a `callStatePending` into TDLib JSON wire shape. */
export function encodeCallStatePending(value: CallStatePending): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callStatePending';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_created'] = value.is_created;
  out['is_received'] = value.is_received;
  return out;
}

/** callStateReady = CallState — The call is ready to use */
export class CallStateReady {
  type: 'callStateReady' = 'callStateReady';
  extra?: string;
  /** protocol: callProtocol — Call protocols supported by the other call participant */
  protocol: CallProtocol | null = null;
  /** servers: vector<callServer> — List of available call servers */
  servers: CallServer[] = [];
  /** config: string — A JSON-encoded call config */
  config: string = '';
  /** encryption_key: bytes — Call encryption key */
  encryption_key: string = '';
  /** emojis: vector<string> — Encryption key fingerprint represented as 4 emoji */
  emojis: string[] = [];
  /** allow_p2p: Bool — True, if peer-to-peer connection is allowed by users privacy settings */
  allow_p2p: boolean = false;
  /** is_group_call_supported: Bool — True, if the other party supports upgrading of the call to a group call */
  is_group_call_supported: boolean = false;
  /** custom_parameters: string — Custom JSON-encoded call parameters to be passed to tgcalls */
  custom_parameters: string = '';
}

/** Decode a TDLib `callStateReady` object; null input -> null. */
export function decodeCallStateReady(json: TdJsonRaw): CallStateReady | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallStateReady = new CallStateReady();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.protocol = decodeCallProtocol(record['protocol']);
  out.servers = decodeVector(record['servers'], (item: TdJsonRaw): CallServer => decodeCallServer(item) as CallServer);
  out.config = asString(record['config']);
  out.encryption_key = asString(record['encryption_key']);
  out.emojis = decodeVector(record['emojis'], (item: TdJsonRaw): string => asString(item));
  out.allow_p2p = asBool(record['allow_p2p']);
  out.is_group_call_supported = asBool(record['is_group_call_supported']);
  out.custom_parameters = asString(record['custom_parameters']);
  return out;
}

/** Encode a `callStateReady` into TDLib JSON wire shape. */
export function encodeCallStateReady(value: CallStateReady): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callStateReady';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['protocol'] = value.protocol === null ? null : encodeCallProtocol(value.protocol);
  out['servers'] = encodeVector(value.servers, (item: CallServer | null): TdJsonRaw => item === null ? null : encodeCallServer(item));
  out['config'] = value.config;
  out['encryption_key'] = value.encryption_key;
  out['emojis'] = encodeVector(value.emojis, (item: string): TdJsonRaw => item);
  out['allow_p2p'] = value.allow_p2p;
  out['is_group_call_supported'] = value.is_group_call_supported;
  out['custom_parameters'] = value.custom_parameters;
  return out;
}

/** callbackQueryAnswer = CallbackQueryAnswer — Contains a bot's answer to a callback query */
export class CallbackQueryAnswer {
  type: 'callbackQueryAnswer' = 'callbackQueryAnswer';
  extra?: string;
  /** text: string — Text of the answer */
  text: string = '';
  /** show_alert: Bool — True, if an alert must be shown to the user instead of a toast notification */
  show_alert: boolean = false;
  /** url: string — URL to be opened */
  url: string = '';
}

/** Decode a TDLib `callbackQueryAnswer` object; null input -> null. */
export function decodeCallbackQueryAnswer(json: TdJsonRaw): CallbackQueryAnswer | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallbackQueryAnswer = new CallbackQueryAnswer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.show_alert = asBool(record['show_alert']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `callbackQueryAnswer` into TDLib JSON wire shape. */
export function encodeCallbackQueryAnswer(value: CallbackQueryAnswer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callbackQueryAnswer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['show_alert'] = value.show_alert;
  out['url'] = value.url;
  return out;
}

/** callbackQueryPayloadData = CallbackQueryPayload — The payload for a general callback button */
export class CallbackQueryPayloadData {
  type: 'callbackQueryPayloadData' = 'callbackQueryPayloadData';
  extra?: string;
  /** data: bytes — Data that was attached to the callback button */
  data: string = '';
}

/** Decode a TDLib `callbackQueryPayloadData` object; null input -> null. */
export function decodeCallbackQueryPayloadData(json: TdJsonRaw): CallbackQueryPayloadData | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallbackQueryPayloadData = new CallbackQueryPayloadData();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.data = asString(record['data']);
  return out;
}

/** Encode a `callbackQueryPayloadData` into TDLib JSON wire shape. */
export function encodeCallbackQueryPayloadData(value: CallbackQueryPayloadData): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callbackQueryPayloadData';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['data'] = value.data;
  return out;
}

/** callbackQueryPayloadDataWithPassword = CallbackQueryPayload — The payload for a callback button requiring password */
export class CallbackQueryPayloadDataWithPassword {
  type: 'callbackQueryPayloadDataWithPassword' = 'callbackQueryPayloadDataWithPassword';
  extra?: string;
  /** password: string — The 2-step verification password for the current user */
  password: string = '';
  /** data: bytes — Data that was attached to the callback button */
  data: string = '';
}

/** Decode a TDLib `callbackQueryPayloadDataWithPassword` object; null input -> null. */
export function decodeCallbackQueryPayloadDataWithPassword(json: TdJsonRaw): CallbackQueryPayloadDataWithPassword | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallbackQueryPayloadDataWithPassword = new CallbackQueryPayloadDataWithPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.password = asString(record['password']);
  out.data = asString(record['data']);
  return out;
}

/** Encode a `callbackQueryPayloadDataWithPassword` into TDLib JSON wire shape. */
export function encodeCallbackQueryPayloadDataWithPassword(value: CallbackQueryPayloadDataWithPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callbackQueryPayloadDataWithPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['password'] = value.password;
  out['data'] = value.data;
  return out;
}

/** callbackQueryPayloadGame = CallbackQueryPayload — The payload for a game callback button */
export class CallbackQueryPayloadGame {
  type: 'callbackQueryPayloadGame' = 'callbackQueryPayloadGame';
  extra?: string;
  /** game_short_name: string — A short name of the game that was attached to the callback button */
  game_short_name: string = '';
}

/** Decode a TDLib `callbackQueryPayloadGame` object; null input -> null. */
export function decodeCallbackQueryPayloadGame(json: TdJsonRaw): CallbackQueryPayloadGame | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CallbackQueryPayloadGame = new CallbackQueryPayloadGame();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.game_short_name = asString(record['game_short_name']);
  return out;
}

/** Encode a `callbackQueryPayloadGame` into TDLib JSON wire shape. */
export function encodeCallbackQueryPayloadGame(value: CallbackQueryPayloadGame): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'callbackQueryPayloadGame';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['game_short_name'] = value.game_short_name;
  return out;
}

/** canBotSendMessages = Ok — Checks whether the specified bot can send messages to the user. Returns a 404 error if can't and the access can be granted by call to allowBotToSendMessages */
export class CanBotSendMessages {
  type: 'canBotSendMessages' = 'canBotSendMessages';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
}

/** Decode a TDLib `canBotSendMessages` object; null input -> null. */
export function decodeCanBotSendMessages(json: TdJsonRaw): CanBotSendMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanBotSendMessages = new CanBotSendMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `canBotSendMessages` into TDLib JSON wire shape. */
export function encodeCanBotSendMessages(value: CanBotSendMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canBotSendMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** canPostStory = CanPostStoryResult — Checks whether the current user can post a story on behalf of a chat; requires can_post_stories administrator right for supergroup and channel chats */
export class CanPostStory {
  type: 'canPostStory' = 'canPostStory';
  extra?: string;
  /** chat_id: int53 — Chat identifier. Pass Saved Messages chat identifier when posting a story on behalf of the current user */
  chat_id: number = 0;
}

/** Decode a TDLib `canPostStory` object; null input -> null. */
export function decodeCanPostStory(json: TdJsonRaw): CanPostStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanPostStory = new CanPostStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `canPostStory` into TDLib JSON wire shape. */
export function encodeCanPostStory(value: CanPostStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canPostStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** canPostStoryResultActiveStoryLimitExceeded = CanPostStoryResult — The limit for the number of active stories exceeded. The user can buy Telegram Premium, delete an active story, or wait for the oldest story to expire */
export class CanPostStoryResultActiveStoryLimitExceeded {
  type: 'canPostStoryResultActiveStoryLimitExceeded' = 'canPostStoryResultActiveStoryLimitExceeded';
  extra?: string;
}

/** Decode a TDLib `canPostStoryResultActiveStoryLimitExceeded` object; null input -> null. */
export function decodeCanPostStoryResultActiveStoryLimitExceeded(json: TdJsonRaw): CanPostStoryResultActiveStoryLimitExceeded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanPostStoryResultActiveStoryLimitExceeded = new CanPostStoryResultActiveStoryLimitExceeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canPostStoryResultActiveStoryLimitExceeded` into TDLib JSON wire shape. */
export function encodeCanPostStoryResultActiveStoryLimitExceeded(value: CanPostStoryResultActiveStoryLimitExceeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canPostStoryResultActiveStoryLimitExceeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canPostStoryResultBoostNeeded = CanPostStoryResult — The chat must be boosted first by Telegram Premium subscribers to post more stories. Call getChatBoostStatus to get current boost status of the chat */
export class CanPostStoryResultBoostNeeded {
  type: 'canPostStoryResultBoostNeeded' = 'canPostStoryResultBoostNeeded';
  extra?: string;
}

/** Decode a TDLib `canPostStoryResultBoostNeeded` object; null input -> null. */
export function decodeCanPostStoryResultBoostNeeded(json: TdJsonRaw): CanPostStoryResultBoostNeeded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanPostStoryResultBoostNeeded = new CanPostStoryResultBoostNeeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canPostStoryResultBoostNeeded` into TDLib JSON wire shape. */
export function encodeCanPostStoryResultBoostNeeded(value: CanPostStoryResultBoostNeeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canPostStoryResultBoostNeeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canPostStoryResultLiveStoryIsActive = CanPostStoryResult — The user or the chat has an active live story. The live story must be deleted first */
export class CanPostStoryResultLiveStoryIsActive {
  type: 'canPostStoryResultLiveStoryIsActive' = 'canPostStoryResultLiveStoryIsActive';
  extra?: string;
  /** story_id: int32 — Identifier of the active live story */
  story_id: number = 0;
}

/** Decode a TDLib `canPostStoryResultLiveStoryIsActive` object; null input -> null. */
export function decodeCanPostStoryResultLiveStoryIsActive(json: TdJsonRaw): CanPostStoryResultLiveStoryIsActive | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanPostStoryResultLiveStoryIsActive = new CanPostStoryResultLiveStoryIsActive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `canPostStoryResultLiveStoryIsActive` into TDLib JSON wire shape. */
export function encodeCanPostStoryResultLiveStoryIsActive(value: CanPostStoryResultLiveStoryIsActive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canPostStoryResultLiveStoryIsActive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_id'] = value.story_id;
  return out;
}

/** canPostStoryResultMonthlyLimitExceeded = CanPostStoryResult — The monthly limit for the number of posted stories exceeded. The user needs to buy Telegram Premium or wait specified time */
export class CanPostStoryResultMonthlyLimitExceeded {
  type: 'canPostStoryResultMonthlyLimitExceeded' = 'canPostStoryResultMonthlyLimitExceeded';
  extra?: string;
  /** retry_after: int32 — Time left before the user can post the next story, in seconds */
  retry_after: number = 0;
}

/** Decode a TDLib `canPostStoryResultMonthlyLimitExceeded` object; null input -> null. */
export function decodeCanPostStoryResultMonthlyLimitExceeded(json: TdJsonRaw): CanPostStoryResultMonthlyLimitExceeded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanPostStoryResultMonthlyLimitExceeded = new CanPostStoryResultMonthlyLimitExceeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.retry_after = asNumber(record['retry_after']);
  return out;
}

/** Encode a `canPostStoryResultMonthlyLimitExceeded` into TDLib JSON wire shape. */
export function encodeCanPostStoryResultMonthlyLimitExceeded(value: CanPostStoryResultMonthlyLimitExceeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canPostStoryResultMonthlyLimitExceeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['retry_after'] = value.retry_after;
  return out;
}

/** canPostStoryResultOk = CanPostStoryResult — A story can be sent */
export class CanPostStoryResultOk {
  type: 'canPostStoryResultOk' = 'canPostStoryResultOk';
  extra?: string;
  /** story_count: int32 — Number of stories that can be posted by the user */
  story_count: number = 0;
}

/** Decode a TDLib `canPostStoryResultOk` object; null input -> null. */
export function decodeCanPostStoryResultOk(json: TdJsonRaw): CanPostStoryResultOk | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanPostStoryResultOk = new CanPostStoryResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_count = asNumber(record['story_count']);
  return out;
}

/** Encode a `canPostStoryResultOk` into TDLib JSON wire shape. */
export function encodeCanPostStoryResultOk(value: CanPostStoryResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canPostStoryResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_count'] = value.story_count;
  return out;
}

/** canPostStoryResultPremiumNeeded = CanPostStoryResult — The user must subscribe to Telegram Premium to be able to post stories */
export class CanPostStoryResultPremiumNeeded {
  type: 'canPostStoryResultPremiumNeeded' = 'canPostStoryResultPremiumNeeded';
  extra?: string;
}

/** Decode a TDLib `canPostStoryResultPremiumNeeded` object; null input -> null. */
export function decodeCanPostStoryResultPremiumNeeded(json: TdJsonRaw): CanPostStoryResultPremiumNeeded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanPostStoryResultPremiumNeeded = new CanPostStoryResultPremiumNeeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canPostStoryResultPremiumNeeded` into TDLib JSON wire shape. */
export function encodeCanPostStoryResultPremiumNeeded(value: CanPostStoryResultPremiumNeeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canPostStoryResultPremiumNeeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canPostStoryResultWeeklyLimitExceeded = CanPostStoryResult — The weekly limit for the number of posted stories exceeded. The user needs to buy Telegram Premium or wait specified time */
export class CanPostStoryResultWeeklyLimitExceeded {
  type: 'canPostStoryResultWeeklyLimitExceeded' = 'canPostStoryResultWeeklyLimitExceeded';
  extra?: string;
  /** retry_after: int32 — Time left before the user can post the next story, in seconds */
  retry_after: number = 0;
}

/** Decode a TDLib `canPostStoryResultWeeklyLimitExceeded` object; null input -> null. */
export function decodeCanPostStoryResultWeeklyLimitExceeded(json: TdJsonRaw): CanPostStoryResultWeeklyLimitExceeded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanPostStoryResultWeeklyLimitExceeded = new CanPostStoryResultWeeklyLimitExceeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.retry_after = asNumber(record['retry_after']);
  return out;
}

/** Encode a `canPostStoryResultWeeklyLimitExceeded` into TDLib JSON wire shape. */
export function encodeCanPostStoryResultWeeklyLimitExceeded(value: CanPostStoryResultWeeklyLimitExceeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canPostStoryResultWeeklyLimitExceeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['retry_after'] = value.retry_after;
  return out;
}

/** canPurchaseFromStore = Ok — Checks whether an in-store purchase is possible. Must be called before any in-store purchase. For official applications only */
export class CanPurchaseFromStore {
  type: 'canPurchaseFromStore' = 'canPurchaseFromStore';
  extra?: string;
  /** purpose: StorePaymentPurpose — Transaction purpose */
  purpose: TdStorePaymentPurpose | null = null;
}

/** Decode a TDLib `canPurchaseFromStore` object; null input -> null. */
export function decodeCanPurchaseFromStore(json: TdJsonRaw): CanPurchaseFromStore | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanPurchaseFromStore = new CanPurchaseFromStore();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.purpose = decodeTdStorePaymentPurpose(record['purpose']);
  return out;
}

/** Encode a `canPurchaseFromStore` into TDLib JSON wire shape. */
export function encodeCanPurchaseFromStore(value: CanPurchaseFromStore): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canPurchaseFromStore';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['purpose'] = value.purpose === null ? null : encodeTdStorePaymentPurpose(value.purpose);
  return out;
}

/** canSendGift = CanSendGiftResult — Checks whether a gift with next_send_date in the future can be sent already */
export class CanSendGift {
  type: 'canSendGift' = 'canSendGift';
  extra?: string;
  /** gift_id: int64 — Identifier of the gift to send */
  gift_id: string = '';
}

/** Decode a TDLib `canSendGift` object; null input -> null. */
export function decodeCanSendGift(json: TdJsonRaw): CanSendGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanSendGift = new CanSendGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_id = asInt64(record['gift_id']);
  return out;
}

/** Encode a `canSendGift` into TDLib JSON wire shape. */
export function encodeCanSendGift(value: CanSendGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canSendGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_id'] = value.gift_id;
  return out;
}

/** canSendGiftResultFail = CanSendGiftResult — The gift can't be sent now by the current user */
export class CanSendGiftResultFail {
  type: 'canSendGiftResultFail' = 'canSendGiftResultFail';
  extra?: string;
  /** reason: formattedText — Reason to be shown to the user */
  reason: FormattedText | null = null;
}

/** Decode a TDLib `canSendGiftResultFail` object; null input -> null. */
export function decodeCanSendGiftResultFail(json: TdJsonRaw): CanSendGiftResultFail | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanSendGiftResultFail = new CanSendGiftResultFail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reason = decodeFormattedText(record['reason']);
  return out;
}

/** Encode a `canSendGiftResultFail` into TDLib JSON wire shape. */
export function encodeCanSendGiftResultFail(value: CanSendGiftResultFail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canSendGiftResultFail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reason'] = value.reason === null ? null : encodeFormattedText(value.reason);
  return out;
}

/** canSendGiftResultOk = CanSendGiftResult — The gift can be sent now by the current user */
export class CanSendGiftResultOk {
  type: 'canSendGiftResultOk' = 'canSendGiftResultOk';
  extra?: string;
}

/** Decode a TDLib `canSendGiftResultOk` object; null input -> null. */
export function decodeCanSendGiftResultOk(json: TdJsonRaw): CanSendGiftResultOk | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanSendGiftResultOk = new CanSendGiftResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canSendGiftResultOk` into TDLib JSON wire shape. */
export function encodeCanSendGiftResultOk(value: CanSendGiftResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canSendGiftResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canSendMessageToUser = CanSendMessageToUserResult — Checks whether the current user can message another user or try to create a chat with them */
export class CanSendMessageToUser {
  type: 'canSendMessageToUser' = 'canSendMessageToUser';
  extra?: string;
  /** user_id: int53 — Identifier of the other user */
  user_id: number = 0;
  /** only_local: Bool — Pass true to get only locally available information without sending network requests */
  only_local: boolean = false;
}

/** Decode a TDLib `canSendMessageToUser` object; null input -> null. */
export function decodeCanSendMessageToUser(json: TdJsonRaw): CanSendMessageToUser | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanSendMessageToUser = new CanSendMessageToUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.only_local = asBool(record['only_local']);
  return out;
}

/** Encode a `canSendMessageToUser` into TDLib JSON wire shape. */
export function encodeCanSendMessageToUser(value: CanSendMessageToUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canSendMessageToUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['only_local'] = value.only_local;
  return out;
}

/** canSendMessageToUserResultOk = CanSendMessageToUserResult — The user can be messaged */
export class CanSendMessageToUserResultOk {
  type: 'canSendMessageToUserResultOk' = 'canSendMessageToUserResultOk';
  extra?: string;
}

/** Decode a TDLib `canSendMessageToUserResultOk` object; null input -> null. */
export function decodeCanSendMessageToUserResultOk(json: TdJsonRaw): CanSendMessageToUserResultOk | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanSendMessageToUserResultOk = new CanSendMessageToUserResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canSendMessageToUserResultOk` into TDLib JSON wire shape. */
export function encodeCanSendMessageToUserResultOk(value: CanSendMessageToUserResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canSendMessageToUserResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canSendMessageToUserResultUserHasPaidMessages = CanSendMessageToUserResult — The user can be messaged, but the messages are paid */
export class CanSendMessageToUserResultUserHasPaidMessages {
  type: 'canSendMessageToUserResultUserHasPaidMessages' = 'canSendMessageToUserResultUserHasPaidMessages';
  extra?: string;
  /** outgoing_paid_message_star_count: int53 — Number of Telegram Stars that must be paid by the current user for each sent message to the user */
  outgoing_paid_message_star_count: number = 0;
}

/** Decode a TDLib `canSendMessageToUserResultUserHasPaidMessages` object; null input -> null. */
export function decodeCanSendMessageToUserResultUserHasPaidMessages(json: TdJsonRaw): CanSendMessageToUserResultUserHasPaidMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanSendMessageToUserResultUserHasPaidMessages = new CanSendMessageToUserResultUserHasPaidMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.outgoing_paid_message_star_count = asNumber(record['outgoing_paid_message_star_count']);
  return out;
}

/** Encode a `canSendMessageToUserResultUserHasPaidMessages` into TDLib JSON wire shape. */
export function encodeCanSendMessageToUserResultUserHasPaidMessages(value: CanSendMessageToUserResultUserHasPaidMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canSendMessageToUserResultUserHasPaidMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['outgoing_paid_message_star_count'] = value.outgoing_paid_message_star_count;
  return out;
}

/** canSendMessageToUserResultUserIsDeleted = CanSendMessageToUserResult — The user can't be messaged, because they are deleted or unknown */
export class CanSendMessageToUserResultUserIsDeleted {
  type: 'canSendMessageToUserResultUserIsDeleted' = 'canSendMessageToUserResultUserIsDeleted';
  extra?: string;
}

/** Decode a TDLib `canSendMessageToUserResultUserIsDeleted` object; null input -> null. */
export function decodeCanSendMessageToUserResultUserIsDeleted(json: TdJsonRaw): CanSendMessageToUserResultUserIsDeleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanSendMessageToUserResultUserIsDeleted = new CanSendMessageToUserResultUserIsDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canSendMessageToUserResultUserIsDeleted` into TDLib JSON wire shape. */
export function encodeCanSendMessageToUserResultUserIsDeleted(value: CanSendMessageToUserResultUserIsDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canSendMessageToUserResultUserIsDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canSendMessageToUserResultUserRestrictsNewChats = CanSendMessageToUserResult — The user can't be messaged, because they restrict new chats with non-contacts */
export class CanSendMessageToUserResultUserRestrictsNewChats {
  type: 'canSendMessageToUserResultUserRestrictsNewChats' = 'canSendMessageToUserResultUserRestrictsNewChats';
  extra?: string;
}

/** Decode a TDLib `canSendMessageToUserResultUserRestrictsNewChats` object; null input -> null. */
export function decodeCanSendMessageToUserResultUserRestrictsNewChats(json: TdJsonRaw): CanSendMessageToUserResultUserRestrictsNewChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanSendMessageToUserResultUserRestrictsNewChats = new CanSendMessageToUserResultUserRestrictsNewChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canSendMessageToUserResultUserRestrictsNewChats` into TDLib JSON wire shape. */
export function encodeCanSendMessageToUserResultUserRestrictsNewChats(value: CanSendMessageToUserResultUserRestrictsNewChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canSendMessageToUserResultUserRestrictsNewChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canTransferOwnership = CanTransferOwnershipResult — Checks whether the current session can be used to transfer a chat ownership to another user */
export class CanTransferOwnership {
  type: 'canTransferOwnership' = 'canTransferOwnership';
  extra?: string;
}

/** Decode a TDLib `canTransferOwnership` object; null input -> null. */
export function decodeCanTransferOwnership(json: TdJsonRaw): CanTransferOwnership | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanTransferOwnership = new CanTransferOwnership();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canTransferOwnership` into TDLib JSON wire shape. */
export function encodeCanTransferOwnership(value: CanTransferOwnership): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canTransferOwnership';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canTransferOwnershipResultOk = CanTransferOwnershipResult — The session can be used */
export class CanTransferOwnershipResultOk {
  type: 'canTransferOwnershipResultOk' = 'canTransferOwnershipResultOk';
  extra?: string;
}

/** Decode a TDLib `canTransferOwnershipResultOk` object; null input -> null. */
export function decodeCanTransferOwnershipResultOk(json: TdJsonRaw): CanTransferOwnershipResultOk | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanTransferOwnershipResultOk = new CanTransferOwnershipResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canTransferOwnershipResultOk` into TDLib JSON wire shape. */
export function encodeCanTransferOwnershipResultOk(value: CanTransferOwnershipResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canTransferOwnershipResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canTransferOwnershipResultPasswordNeeded = CanTransferOwnershipResult — The 2-step verification needs to be enabled first */
export class CanTransferOwnershipResultPasswordNeeded {
  type: 'canTransferOwnershipResultPasswordNeeded' = 'canTransferOwnershipResultPasswordNeeded';
  extra?: string;
}

/** Decode a TDLib `canTransferOwnershipResultPasswordNeeded` object; null input -> null. */
export function decodeCanTransferOwnershipResultPasswordNeeded(json: TdJsonRaw): CanTransferOwnershipResultPasswordNeeded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanTransferOwnershipResultPasswordNeeded = new CanTransferOwnershipResultPasswordNeeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `canTransferOwnershipResultPasswordNeeded` into TDLib JSON wire shape. */
export function encodeCanTransferOwnershipResultPasswordNeeded(value: CanTransferOwnershipResultPasswordNeeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canTransferOwnershipResultPasswordNeeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** canTransferOwnershipResultPasswordTooFresh = CanTransferOwnershipResult — The 2-step verification was enabled recently, user needs to wait */
export class CanTransferOwnershipResultPasswordTooFresh {
  type: 'canTransferOwnershipResultPasswordTooFresh' = 'canTransferOwnershipResultPasswordTooFresh';
  extra?: string;
  /** retry_after: int32 — Time left before the session can be used to transfer ownership of a chat, in seconds */
  retry_after: number = 0;
}

/** Decode a TDLib `canTransferOwnershipResultPasswordTooFresh` object; null input -> null. */
export function decodeCanTransferOwnershipResultPasswordTooFresh(json: TdJsonRaw): CanTransferOwnershipResultPasswordTooFresh | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanTransferOwnershipResultPasswordTooFresh = new CanTransferOwnershipResultPasswordTooFresh();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.retry_after = asNumber(record['retry_after']);
  return out;
}

/** Encode a `canTransferOwnershipResultPasswordTooFresh` into TDLib JSON wire shape. */
export function encodeCanTransferOwnershipResultPasswordTooFresh(value: CanTransferOwnershipResultPasswordTooFresh): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canTransferOwnershipResultPasswordTooFresh';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['retry_after'] = value.retry_after;
  return out;
}

/** canTransferOwnershipResultSessionTooFresh = CanTransferOwnershipResult — The session was created recently, user needs to wait */
export class CanTransferOwnershipResultSessionTooFresh {
  type: 'canTransferOwnershipResultSessionTooFresh' = 'canTransferOwnershipResultSessionTooFresh';
  extra?: string;
  /** retry_after: int32 — Time left before the session can be used to transfer ownership of a chat, in seconds */
  retry_after: number = 0;
}

/** Decode a TDLib `canTransferOwnershipResultSessionTooFresh` object; null input -> null. */
export function decodeCanTransferOwnershipResultSessionTooFresh(json: TdJsonRaw): CanTransferOwnershipResultSessionTooFresh | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CanTransferOwnershipResultSessionTooFresh = new CanTransferOwnershipResultSessionTooFresh();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.retry_after = asNumber(record['retry_after']);
  return out;
}

/** Encode a `canTransferOwnershipResultSessionTooFresh` into TDLib JSON wire shape. */
export function encodeCanTransferOwnershipResultSessionTooFresh(value: CanTransferOwnershipResultSessionTooFresh): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'canTransferOwnershipResultSessionTooFresh';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['retry_after'] = value.retry_after;
  return out;
}

/** cancelDownloadFile = Ok — Stops the downloading of a file. If a file has already been downloaded, does nothing */
export class CancelDownloadFile {
  type: 'cancelDownloadFile' = 'cancelDownloadFile';
  extra?: string;
  /** file_id: int32 — Identifier of a file to stop downloading */
  file_id: number = 0;
  /** only_if_pending: Bool — Pass true to stop downloading only if it hasn't been started, i.e. request hasn't been sent to server */
  only_if_pending: boolean = false;
}

/** Decode a TDLib `cancelDownloadFile` object; null input -> null. */
export function decodeCancelDownloadFile(json: TdJsonRaw): CancelDownloadFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CancelDownloadFile = new CancelDownloadFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.only_if_pending = asBool(record['only_if_pending']);
  return out;
}

/** Encode a `cancelDownloadFile` into TDLib JSON wire shape. */
export function encodeCancelDownloadFile(value: CancelDownloadFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'cancelDownloadFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['only_if_pending'] = value.only_if_pending;
  return out;
}

/** cancelPasswordReset = Ok — Cancels reset of 2-step verification password. The method can be called if passwordState.pending_reset_date > 0 */
export class CancelPasswordReset {
  type: 'cancelPasswordReset' = 'cancelPasswordReset';
  extra?: string;
}

/** Decode a TDLib `cancelPasswordReset` object; null input -> null. */
export function decodeCancelPasswordReset(json: TdJsonRaw): CancelPasswordReset | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CancelPasswordReset = new CancelPasswordReset();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `cancelPasswordReset` into TDLib JSON wire shape. */
export function encodeCancelPasswordReset(value: CancelPasswordReset): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'cancelPasswordReset';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** cancelPreliminaryUploadFile = Ok — Stops the preliminary uploading of a file. Supported only for files uploaded by using preliminaryUploadFile */
export class CancelPreliminaryUploadFile {
  type: 'cancelPreliminaryUploadFile' = 'cancelPreliminaryUploadFile';
  extra?: string;
  /** file_id: int32 — Identifier of the file to stop uploading */
  file_id: number = 0;
}

/** Decode a TDLib `cancelPreliminaryUploadFile` object; null input -> null. */
export function decodeCancelPreliminaryUploadFile(json: TdJsonRaw): CancelPreliminaryUploadFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CancelPreliminaryUploadFile = new CancelPreliminaryUploadFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  return out;
}

/** Encode a `cancelPreliminaryUploadFile` into TDLib JSON wire shape. */
export function encodeCancelPreliminaryUploadFile(value: CancelPreliminaryUploadFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'cancelPreliminaryUploadFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  return out;
}

/** cancelRecoveryEmailAddressVerification = PasswordState — Cancels verification of the 2-step verification recovery email address */
export class CancelRecoveryEmailAddressVerification {
  type: 'cancelRecoveryEmailAddressVerification' = 'cancelRecoveryEmailAddressVerification';
  extra?: string;
}

/** Decode a TDLib `cancelRecoveryEmailAddressVerification` object; null input -> null. */
export function decodeCancelRecoveryEmailAddressVerification(json: TdJsonRaw): CancelRecoveryEmailAddressVerification | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CancelRecoveryEmailAddressVerification = new CancelRecoveryEmailAddressVerification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `cancelRecoveryEmailAddressVerification` into TDLib JSON wire shape. */
export function encodeCancelRecoveryEmailAddressVerification(value: CancelRecoveryEmailAddressVerification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'cancelRecoveryEmailAddressVerification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** changeImportedContacts = ImportedContacts — Changes imported contacts using the list of contacts saved on the device. Imports newly added contacts and, if at least the file database is enabled, deletes recently deleted contacts. Query re... */
export class ChangeImportedContacts {
  type: 'changeImportedContacts' = 'changeImportedContacts';
  extra?: string;
  /** contacts: vector<importedContact> — The new list of contacts to import */
  contacts: ImportedContact[] = [];
}

/** Decode a TDLib `changeImportedContacts` object; null input -> null. */
export function decodeChangeImportedContacts(json: TdJsonRaw): ChangeImportedContacts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChangeImportedContacts = new ChangeImportedContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.contacts = decodeVector(record['contacts'], (item: TdJsonRaw): ImportedContact => decodeImportedContact(item) as ImportedContact);
  return out;
}

/** Encode a `changeImportedContacts` into TDLib JSON wire shape. */
export function encodeChangeImportedContacts(value: ChangeImportedContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'changeImportedContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['contacts'] = encodeVector(value.contacts, (item: ImportedContact | null): TdJsonRaw => item === null ? null : encodeImportedContact(item));
  return out;
}

/** changeStickerSet = Ok — Installs/uninstalls or activates/archives a sticker set */
export class ChangeStickerSet {
  type: 'changeStickerSet' = 'changeStickerSet';
  extra?: string;
  /** set_id: int64 — Identifier of the sticker set */
  set_id: string = '';
  /** is_installed: Bool — The new value of is_installed */
  is_installed: boolean = false;
  /** is_archived: Bool — The new value of is_archived. A sticker set can't be installed and archived simultaneously */
  is_archived: boolean = false;
}

/** Decode a TDLib `changeStickerSet` object; null input -> null. */
export function decodeChangeStickerSet(json: TdJsonRaw): ChangeStickerSet | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChangeStickerSet = new ChangeStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.set_id = asInt64(record['set_id']);
  out.is_installed = asBool(record['is_installed']);
  out.is_archived = asBool(record['is_archived']);
  return out;
}

/** Encode a `changeStickerSet` into TDLib JSON wire shape. */
export function encodeChangeStickerSet(value: ChangeStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'changeStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['set_id'] = value.set_id;
  out['is_installed'] = value.is_installed;
  out['is_archived'] = value.is_archived;
  return out;
}

/** changeWebBrowserSettings = Ok — Changes web browser settings */
export class ChangeWebBrowserSettings {
  type: 'changeWebBrowserSettings' = 'changeWebBrowserSettings';
  extra?: string;
  /** open_external_browser: Bool — Pass true if links must be opened in an external browser by default */
  open_external_browser: boolean = false;
  /** display_close_button: Bool — Pass true if a close button must be shown in the in-app browser; for Android app only */
  display_close_button: boolean = false;
}

/** Decode a TDLib `changeWebBrowserSettings` object; null input -> null. */
export function decodeChangeWebBrowserSettings(json: TdJsonRaw): ChangeWebBrowserSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChangeWebBrowserSettings = new ChangeWebBrowserSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.open_external_browser = asBool(record['open_external_browser']);
  out.display_close_button = asBool(record['display_close_button']);
  return out;
}

/** Encode a `changeWebBrowserSettings` into TDLib JSON wire shape. */
export function encodeChangeWebBrowserSettings(value: ChangeWebBrowserSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'changeWebBrowserSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['open_external_browser'] = value.open_external_browser;
  out['display_close_button'] = value.display_close_button;
  return out;
}

/** chat = Chat — A chat. (Can be a private chat, basic group, supergroup, or secret chat) */
export class Chat {
  type: 'chat' = 'chat';
  extra?: string;
  /** id: int53 — Chat unique identifier */
  id: number = 0;
  /** type: ChatType — Type of the chat */
  type_: TdChatType | null = null;
  /** title: string — Chat title */
  title: string = '';
  /** photo: chatPhotoInfo — Chat photo; may be null */
  photo: ChatPhotoInfo | null = null;
  /** accent_color_id: int32 — Identifier of the accent color for message sender name, and backgrounds of chat photo, reply header, and link preview */
  accent_color_id: number = 0;
  /** background_custom_emoji_id: int64 — Identifier of a custom emoji to be shown on the reply header and link preview background for messages sent by the chat; 0 if none */
  background_custom_emoji_id: string = '';
  /** upgraded_gift_colors: upgradedGiftColors — Color scheme based on an upgraded gift to be used for the chat instead of accent_color_id and background_custom_emoji_id; may be null if none */
  upgraded_gift_colors: UpgradedGiftColors | null = null;
  /** profile_accent_color_id: int32 — Identifier of the profile accent color for the chat's profile; -1 if none */
  profile_accent_color_id: number = 0;
  /** profile_background_custom_emoji_id: int64 — Identifier of a custom emoji to be shown on the background of the chat's profile; 0 if none */
  profile_background_custom_emoji_id: string = '';
  /** permissions: chatPermissions — Actions that non-administrator chat members are allowed to take in the chat */
  permissions: ChatPermissions | null = null;
  /** last_message: message — Last message in the chat; may be null if none or unknown */
  last_message: Message | null = null;
  /** positions: vector<chatPosition> — Positions of the chat in chat lists */
  positions: ChatPosition[] = [];
  /** chat_lists: vector<ChatList> — Chat lists to which the chat belongs. A chat can have a non-zero position in a chat list even if it doesn't belong to the chat list and have no position in a chat list even if it belongs to the chat list */
  chat_lists: TdChatList[] = [];
  /** message_sender_id: MessageSender — Identifier of a user or chat that is selected to send messages in the chat; may be null if the user can't change message sender */
  message_sender_id: TdMessageSender | null = null;
  /** block_list: BlockList — Block list to which the chat is added; may be null if none */
  block_list: TdBlockList | null = null;
  /** has_protected_content: Bool — True, if chat content can't be saved locally, forwarded, or copied */
  has_protected_content: boolean = false;
  /** is_translatable: Bool — True, if translation of all messages in the chat must be suggested to the user */
  is_translatable: boolean = false;
  /** is_marked_as_unread: Bool — True, if the chat is marked as unread */
  is_marked_as_unread: boolean = false;
  /** view_as_topics: Bool — True, if the chat is a forum supergroup that must be shown in the "View as topics" mode, or Saved Messages chat that must be shown in the "View as chats" */
  view_as_topics: boolean = false;
  /** has_scheduled_messages: Bool — True, if the chat has scheduled messages */
  has_scheduled_messages: boolean = false;
  /** has_welcome_messages: Bool — True, if the chat has welcome messages; for chat administrators with can_change_info administrator right only */
  has_welcome_messages: boolean = false;
  /** can_be_deleted_only_for_self: Bool — True, if the chat messages can be deleted only for the current user while other users will continue to see the messages */
  can_be_deleted_only_for_self: boolean = false;
  /** can_be_deleted_for_all_users: Bool — True, if the chat messages can be deleted for all users */
  can_be_deleted_for_all_users: boolean = false;
  /** can_be_reported: Bool — True, if the chat can be reported to Telegram moderators through reportChat or reportChatPhoto */
  can_be_reported: boolean = false;
  /** default_disable_notification: Bool — Default value of the disable_notification parameter, used when a message is sent to the chat */
  default_disable_notification: boolean = false;
  /** unread_count: int32 — Number of unread messages in the chat */
  unread_count: number = 0;
  /** last_read_inbox_message_id: int53 — Identifier of the last read incoming message */
  last_read_inbox_message_id: number = 0;
  /** last_read_outbox_message_id: int53 — Identifier of the last read outgoing message */
  last_read_outbox_message_id: number = 0;
  /** unread_mention_count: int32 — Number of unread messages with a mention/reply in the chat */
  unread_mention_count: number = 0;
  /** unread_reaction_count: int32 — Number of messages with unread reactions in the chat */
  unread_reaction_count: number = 0;
  /** unread_poll_vote_count: int32 — Number of messages with unread poll votes in the chat */
  unread_poll_vote_count: number = 0;
  /** notification_settings: chatNotificationSettings — Notification settings for the chat */
  notification_settings: ChatNotificationSettings | null = null;
  /** available_reactions: ChatAvailableReactions — Types of reaction, available in the chat */
  available_reactions: TdChatAvailableReactions | null = null;
  /** message_auto_delete_time: int32 — Current message auto-delete or self-destruct timer setting for the chat, in seconds; 0 if disabled. Self-destruct timer in secret chats starts after the message or its content is viewed. Auto-delete time... */
  message_auto_delete_time: number = 0;
  /** emoji_status: emojiStatus — Emoji status to be shown along with chat title; may be null */
  emoji_status: EmojiStatus | null = null;
  /** background: chatBackground — Background set for the chat; may be null if none */
  background: ChatBackground | null = null;
  /** theme: ChatTheme — Theme set for the chat; may be null if none */
  theme: TdChatTheme | null = null;
  /** action_bar: ChatActionBar — Information about actions which must be possible to do through the chat action bar; may be null if none */
  action_bar: TdChatActionBar | null = null;
  /** business_bot_manage_bar: businessBotManageBar — Information about bar for managing a business bot in the chat; may be null if none */
  business_bot_manage_bar: BusinessBotManageBar | null = null;
  /** video_chat: videoChat — Information about video chat of the chat */
  video_chat: VideoChat | null = null;
  /** pending_join_requests: chatJoinRequestsInfo — Information about pending join requests; may be null if none */
  pending_join_requests: ChatJoinRequestsInfo | null = null;
  /** reply_markup_message_id: int53 — Identifier of the message from which reply markup needs to be used; 0 if there is no reply markup in the chat */
  reply_markup_message_id: number = 0;
  /** draft_message: draftMessage — A draft of a message in the chat; may be null if none */
  draft_message: DraftMessage | null = null;
  /** client_data: string — Application-specific data associated with the chat. (For example, the chat scroll position or local chat notification settings can be stored here.) Persistent if the message database is used */
  client_data: string = '';
}

/** Decode a TDLib `chat` object; null input -> null. */
export function decodeChat(json: TdJsonRaw): Chat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Chat = new Chat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.type_ = decodeTdChatType(record['type']);
  out.title = asString(record['title']);
  out.photo = decodeChatPhotoInfo(record['photo']);
  out.accent_color_id = asNumber(record['accent_color_id']);
  out.background_custom_emoji_id = asInt64(record['background_custom_emoji_id']);
  out.upgraded_gift_colors = decodeUpgradedGiftColors(record['upgraded_gift_colors']);
  out.profile_accent_color_id = asNumber(record['profile_accent_color_id']);
  out.profile_background_custom_emoji_id = asInt64(record['profile_background_custom_emoji_id']);
  out.permissions = decodeChatPermissions(record['permissions']);
  out.last_message = decodeMessage(record['last_message']);
  out.positions = decodeVector(record['positions'], (item: TdJsonRaw): ChatPosition => decodeChatPosition(item) as ChatPosition);
  out.chat_lists = decodeVector(record['chat_lists'], (item: TdJsonRaw): TdChatList => decodeTdChatList(item) as TdChatList);
  out.message_sender_id = decodeTdMessageSender(record['message_sender_id']);
  out.block_list = decodeTdBlockList(record['block_list']);
  out.has_protected_content = asBool(record['has_protected_content']);
  out.is_translatable = asBool(record['is_translatable']);
  out.is_marked_as_unread = asBool(record['is_marked_as_unread']);
  out.view_as_topics = asBool(record['view_as_topics']);
  out.has_scheduled_messages = asBool(record['has_scheduled_messages']);
  out.has_welcome_messages = asBool(record['has_welcome_messages']);
  out.can_be_deleted_only_for_self = asBool(record['can_be_deleted_only_for_self']);
  out.can_be_deleted_for_all_users = asBool(record['can_be_deleted_for_all_users']);
  out.can_be_reported = asBool(record['can_be_reported']);
  out.default_disable_notification = asBool(record['default_disable_notification']);
  out.unread_count = asNumber(record['unread_count']);
  out.last_read_inbox_message_id = asNumber(record['last_read_inbox_message_id']);
  out.last_read_outbox_message_id = asNumber(record['last_read_outbox_message_id']);
  out.unread_mention_count = asNumber(record['unread_mention_count']);
  out.unread_reaction_count = asNumber(record['unread_reaction_count']);
  out.unread_poll_vote_count = asNumber(record['unread_poll_vote_count']);
  out.notification_settings = decodeChatNotificationSettings(record['notification_settings']);
  out.available_reactions = decodeTdChatAvailableReactions(record['available_reactions']);
  out.message_auto_delete_time = asNumber(record['message_auto_delete_time']);
  out.emoji_status = decodeEmojiStatus(record['emoji_status']);
  out.background = decodeChatBackground(record['background']);
  out.theme = decodeTdChatTheme(record['theme']);
  out.action_bar = decodeTdChatActionBar(record['action_bar']);
  out.business_bot_manage_bar = decodeBusinessBotManageBar(record['business_bot_manage_bar']);
  out.video_chat = decodeVideoChat(record['video_chat']);
  out.pending_join_requests = decodeChatJoinRequestsInfo(record['pending_join_requests']);
  out.reply_markup_message_id = asNumber(record['reply_markup_message_id']);
  out.draft_message = decodeDraftMessage(record['draft_message']);
  out.client_data = asString(record['client_data']);
  return out;
}

/** Encode a `chat` into TDLib JSON wire shape. */
export function encodeChat(value: Chat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['type'] = value.type_ === null ? null : encodeTdChatType(value.type_);
  out['title'] = value.title;
  out['photo'] = value.photo === null ? null : encodeChatPhotoInfo(value.photo);
  out['accent_color_id'] = value.accent_color_id;
  out['background_custom_emoji_id'] = value.background_custom_emoji_id;
  out['upgraded_gift_colors'] = value.upgraded_gift_colors === null ? null : encodeUpgradedGiftColors(value.upgraded_gift_colors);
  out['profile_accent_color_id'] = value.profile_accent_color_id;
  out['profile_background_custom_emoji_id'] = value.profile_background_custom_emoji_id;
  out['permissions'] = value.permissions === null ? null : encodeChatPermissions(value.permissions);
  out['last_message'] = value.last_message === null ? null : encodeMessage(value.last_message);
  out['positions'] = encodeVector(value.positions, (item: ChatPosition | null): TdJsonRaw => item === null ? null : encodeChatPosition(item));
  out['chat_lists'] = encodeVector(value.chat_lists, (item: TdChatList | null): TdJsonRaw => item === null ? null : encodeTdChatList(item));
  out['message_sender_id'] = value.message_sender_id === null ? null : encodeTdMessageSender(value.message_sender_id);
  out['block_list'] = value.block_list === null ? null : encodeTdBlockList(value.block_list);
  out['has_protected_content'] = value.has_protected_content;
  out['is_translatable'] = value.is_translatable;
  out['is_marked_as_unread'] = value.is_marked_as_unread;
  out['view_as_topics'] = value.view_as_topics;
  out['has_scheduled_messages'] = value.has_scheduled_messages;
  out['has_welcome_messages'] = value.has_welcome_messages;
  out['can_be_deleted_only_for_self'] = value.can_be_deleted_only_for_self;
  out['can_be_deleted_for_all_users'] = value.can_be_deleted_for_all_users;
  out['can_be_reported'] = value.can_be_reported;
  out['default_disable_notification'] = value.default_disable_notification;
  out['unread_count'] = value.unread_count;
  out['last_read_inbox_message_id'] = value.last_read_inbox_message_id;
  out['last_read_outbox_message_id'] = value.last_read_outbox_message_id;
  out['unread_mention_count'] = value.unread_mention_count;
  out['unread_reaction_count'] = value.unread_reaction_count;
  out['unread_poll_vote_count'] = value.unread_poll_vote_count;
  out['notification_settings'] = value.notification_settings === null ? null : encodeChatNotificationSettings(value.notification_settings);
  out['available_reactions'] = value.available_reactions === null ? null : encodeTdChatAvailableReactions(value.available_reactions);
  out['message_auto_delete_time'] = value.message_auto_delete_time;
  out['emoji_status'] = value.emoji_status === null ? null : encodeEmojiStatus(value.emoji_status);
  out['background'] = value.background === null ? null : encodeChatBackground(value.background);
  out['theme'] = value.theme === null ? null : encodeTdChatTheme(value.theme);
  out['action_bar'] = value.action_bar === null ? null : encodeTdChatActionBar(value.action_bar);
  out['business_bot_manage_bar'] = value.business_bot_manage_bar === null ? null : encodeBusinessBotManageBar(value.business_bot_manage_bar);
  out['video_chat'] = value.video_chat === null ? null : encodeVideoChat(value.video_chat);
  out['pending_join_requests'] = value.pending_join_requests === null ? null : encodeChatJoinRequestsInfo(value.pending_join_requests);
  out['reply_markup_message_id'] = value.reply_markup_message_id;
  out['draft_message'] = value.draft_message === null ? null : encodeDraftMessage(value.draft_message);
  out['client_data'] = value.client_data;
  return out;
}

/** chatActionBarAddContact = ChatActionBar — The chat is a private or secret chat and the other user can be added to the contact list using the method addContact */
export class ChatActionBarAddContact {
  type: 'chatActionBarAddContact' = 'chatActionBarAddContact';
  extra?: string;
}

/** Decode a TDLib `chatActionBarAddContact` object; null input -> null. */
export function decodeChatActionBarAddContact(json: TdJsonRaw): ChatActionBarAddContact | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionBarAddContact = new ChatActionBarAddContact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionBarAddContact` into TDLib JSON wire shape. */
export function encodeChatActionBarAddContact(value: ChatActionBarAddContact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionBarAddContact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionBarInviteMembers = ChatActionBar — The chat is a recently created group chat to which new members can be invited */
export class ChatActionBarInviteMembers {
  type: 'chatActionBarInviteMembers' = 'chatActionBarInviteMembers';
  extra?: string;
}

/** Decode a TDLib `chatActionBarInviteMembers` object; null input -> null. */
export function decodeChatActionBarInviteMembers(json: TdJsonRaw): ChatActionBarInviteMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionBarInviteMembers = new ChatActionBarInviteMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionBarInviteMembers` into TDLib JSON wire shape. */
export function encodeChatActionBarInviteMembers(value: ChatActionBarInviteMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionBarInviteMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionBarJoinRequest = ChatActionBar — The chat is a private chat with an administrator of a chat to which the user sent join request */
export class ChatActionBarJoinRequest {
  type: 'chatActionBarJoinRequest' = 'chatActionBarJoinRequest';
  extra?: string;
  /** title: string — Title of the chat to which the join request was sent */
  title: string = '';
  /** is_channel: Bool — True, if the join request was sent to a channel chat */
  is_channel: boolean = false;
  /** request_date: int32 — Point in time (Unix timestamp) when the join request was sent */
  request_date: number = 0;
}

/** Decode a TDLib `chatActionBarJoinRequest` object; null input -> null. */
export function decodeChatActionBarJoinRequest(json: TdJsonRaw): ChatActionBarJoinRequest | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionBarJoinRequest = new ChatActionBarJoinRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.is_channel = asBool(record['is_channel']);
  out.request_date = asNumber(record['request_date']);
  return out;
}

/** Encode a `chatActionBarJoinRequest` into TDLib JSON wire shape. */
export function encodeChatActionBarJoinRequest(value: ChatActionBarJoinRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionBarJoinRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['is_channel'] = value.is_channel;
  out['request_date'] = value.request_date;
  return out;
}

/** chatActionBarReportAddBlock = ChatActionBar — The chat is a private or secret chat, which can be reported using the method reportChat, or the other user can be blocked using the method setMessageSenderBlockList, or the other user can be ... */
export class ChatActionBarReportAddBlock {
  type: 'chatActionBarReportAddBlock' = 'chatActionBarReportAddBlock';
  extra?: string;
  /** can_unarchive: Bool — If true, the chat was automatically archived and can be moved back to the main chat list using addChatToList simultaneously with setting chat notification settings to default using setChatNotificationSettings */
  can_unarchive: boolean = false;
  /** account_info: accountInfo — Basic information about the other user in the chat; may be null if unknown */
  account_info: AccountInfo | null = null;
}

/** Decode a TDLib `chatActionBarReportAddBlock` object; null input -> null. */
export function decodeChatActionBarReportAddBlock(json: TdJsonRaw): ChatActionBarReportAddBlock | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionBarReportAddBlock = new ChatActionBarReportAddBlock();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_unarchive = asBool(record['can_unarchive']);
  out.account_info = decodeAccountInfo(record['account_info']);
  return out;
}

/** Encode a `chatActionBarReportAddBlock` into TDLib JSON wire shape. */
export function encodeChatActionBarReportAddBlock(value: ChatActionBarReportAddBlock): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionBarReportAddBlock';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_unarchive'] = value.can_unarchive;
  out['account_info'] = value.account_info === null ? null : encodeAccountInfo(value.account_info);
  return out;
}

/** chatActionBarReportSpam = ChatActionBar — The chat can be reported as spam using the method reportChat with an empty option_id and message_ids. If the chat is a private chat with a user with an emoji status, then a notice about emoji sta... */
export class ChatActionBarReportSpam {
  type: 'chatActionBarReportSpam' = 'chatActionBarReportSpam';
  extra?: string;
  /** can_unarchive: Bool — If true, the chat was automatically archived and can be moved back to the main chat list using addChatToList simultaneously with setting chat notification settings to default using setChatNotificationSettings */
  can_unarchive: boolean = false;
}

/** Decode a TDLib `chatActionBarReportSpam` object; null input -> null. */
export function decodeChatActionBarReportSpam(json: TdJsonRaw): ChatActionBarReportSpam | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionBarReportSpam = new ChatActionBarReportSpam();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_unarchive = asBool(record['can_unarchive']);
  return out;
}

/** Encode a `chatActionBarReportSpam` into TDLib JSON wire shape. */
export function encodeChatActionBarReportSpam(value: ChatActionBarReportSpam): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionBarReportSpam';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_unarchive'] = value.can_unarchive;
  return out;
}

/** chatActionBarSharePhoneNumber = ChatActionBar — The chat is a private or secret chat with a mutual contact and the user's phone number can be shared with the other user using the method sharePhoneNumber */
export class ChatActionBarSharePhoneNumber {
  type: 'chatActionBarSharePhoneNumber' = 'chatActionBarSharePhoneNumber';
  extra?: string;
}

/** Decode a TDLib `chatActionBarSharePhoneNumber` object; null input -> null. */
export function decodeChatActionBarSharePhoneNumber(json: TdJsonRaw): ChatActionBarSharePhoneNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionBarSharePhoneNumber = new ChatActionBarSharePhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionBarSharePhoneNumber` into TDLib JSON wire shape. */
export function encodeChatActionBarSharePhoneNumber(value: ChatActionBarSharePhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionBarSharePhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionCancel = ChatAction — The user has canceled the previous action */
export class ChatActionCancel {
  type: 'chatActionCancel' = 'chatActionCancel';
  extra?: string;
}

/** Decode a TDLib `chatActionCancel` object; null input -> null. */
export function decodeChatActionCancel(json: TdJsonRaw): ChatActionCancel | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionCancel = new ChatActionCancel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionCancel` into TDLib JSON wire shape. */
export function encodeChatActionCancel(value: ChatActionCancel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionCancel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionChoosingContact = ChatAction — The user is picking a contact to send */
export class ChatActionChoosingContact {
  type: 'chatActionChoosingContact' = 'chatActionChoosingContact';
  extra?: string;
}

/** Decode a TDLib `chatActionChoosingContact` object; null input -> null. */
export function decodeChatActionChoosingContact(json: TdJsonRaw): ChatActionChoosingContact | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionChoosingContact = new ChatActionChoosingContact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionChoosingContact` into TDLib JSON wire shape. */
export function encodeChatActionChoosingContact(value: ChatActionChoosingContact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionChoosingContact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionChoosingLocation = ChatAction — The user is picking a location or venue to send */
export class ChatActionChoosingLocation {
  type: 'chatActionChoosingLocation' = 'chatActionChoosingLocation';
  extra?: string;
}

/** Decode a TDLib `chatActionChoosingLocation` object; null input -> null. */
export function decodeChatActionChoosingLocation(json: TdJsonRaw): ChatActionChoosingLocation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionChoosingLocation = new ChatActionChoosingLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionChoosingLocation` into TDLib JSON wire shape. */
export function encodeChatActionChoosingLocation(value: ChatActionChoosingLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionChoosingLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionChoosingSticker = ChatAction — The user is picking a sticker to send */
export class ChatActionChoosingSticker {
  type: 'chatActionChoosingSticker' = 'chatActionChoosingSticker';
  extra?: string;
}

/** Decode a TDLib `chatActionChoosingSticker` object; null input -> null. */
export function decodeChatActionChoosingSticker(json: TdJsonRaw): ChatActionChoosingSticker | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionChoosingSticker = new ChatActionChoosingSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionChoosingSticker` into TDLib JSON wire shape. */
export function encodeChatActionChoosingSticker(value: ChatActionChoosingSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionChoosingSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionRecordingVideo = ChatAction — The user is recording a video */
export class ChatActionRecordingVideo {
  type: 'chatActionRecordingVideo' = 'chatActionRecordingVideo';
  extra?: string;
}

/** Decode a TDLib `chatActionRecordingVideo` object; null input -> null. */
export function decodeChatActionRecordingVideo(json: TdJsonRaw): ChatActionRecordingVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionRecordingVideo = new ChatActionRecordingVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionRecordingVideo` into TDLib JSON wire shape. */
export function encodeChatActionRecordingVideo(value: ChatActionRecordingVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionRecordingVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionRecordingVideoNote = ChatAction — The user is recording a video note */
export class ChatActionRecordingVideoNote {
  type: 'chatActionRecordingVideoNote' = 'chatActionRecordingVideoNote';
  extra?: string;
}

/** Decode a TDLib `chatActionRecordingVideoNote` object; null input -> null. */
export function decodeChatActionRecordingVideoNote(json: TdJsonRaw): ChatActionRecordingVideoNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionRecordingVideoNote = new ChatActionRecordingVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionRecordingVideoNote` into TDLib JSON wire shape. */
export function encodeChatActionRecordingVideoNote(value: ChatActionRecordingVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionRecordingVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionRecordingVoiceNote = ChatAction — The user is recording a voice note */
export class ChatActionRecordingVoiceNote {
  type: 'chatActionRecordingVoiceNote' = 'chatActionRecordingVoiceNote';
  extra?: string;
}

/** Decode a TDLib `chatActionRecordingVoiceNote` object; null input -> null. */
export function decodeChatActionRecordingVoiceNote(json: TdJsonRaw): ChatActionRecordingVoiceNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionRecordingVoiceNote = new ChatActionRecordingVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionRecordingVoiceNote` into TDLib JSON wire shape. */
export function encodeChatActionRecordingVoiceNote(value: ChatActionRecordingVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionRecordingVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionStartPlayingGame = ChatAction — The user has started to play a game */
export class ChatActionStartPlayingGame {
  type: 'chatActionStartPlayingGame' = 'chatActionStartPlayingGame';
  extra?: string;
}

/** Decode a TDLib `chatActionStartPlayingGame` object; null input -> null. */
export function decodeChatActionStartPlayingGame(json: TdJsonRaw): ChatActionStartPlayingGame | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionStartPlayingGame = new ChatActionStartPlayingGame();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionStartPlayingGame` into TDLib JSON wire shape. */
export function encodeChatActionStartPlayingGame(value: ChatActionStartPlayingGame): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionStartPlayingGame';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionTyping = ChatAction — The user is typing a message */
export class ChatActionTyping {
  type: 'chatActionTyping' = 'chatActionTyping';
  extra?: string;
}

/** Decode a TDLib `chatActionTyping` object; null input -> null. */
export function decodeChatActionTyping(json: TdJsonRaw): ChatActionTyping | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionTyping = new ChatActionTyping();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatActionTyping` into TDLib JSON wire shape. */
export function encodeChatActionTyping(value: ChatActionTyping): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionTyping';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatActionUploadingDocument = ChatAction — The user is uploading a document */
export class ChatActionUploadingDocument {
  type: 'chatActionUploadingDocument' = 'chatActionUploadingDocument';
  extra?: string;
  /** progress: int32 — Upload progress, as a percentage */
  progress: number = 0;
}

/** Decode a TDLib `chatActionUploadingDocument` object; null input -> null. */
export function decodeChatActionUploadingDocument(json: TdJsonRaw): ChatActionUploadingDocument | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionUploadingDocument = new ChatActionUploadingDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.progress = asNumber(record['progress']);
  return out;
}

/** Encode a `chatActionUploadingDocument` into TDLib JSON wire shape. */
export function encodeChatActionUploadingDocument(value: ChatActionUploadingDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionUploadingDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['progress'] = value.progress;
  return out;
}

/** chatActionUploadingPhoto = ChatAction — The user is uploading a photo */
export class ChatActionUploadingPhoto {
  type: 'chatActionUploadingPhoto' = 'chatActionUploadingPhoto';
  extra?: string;
  /** progress: int32 — Upload progress, as a percentage */
  progress: number = 0;
}

/** Decode a TDLib `chatActionUploadingPhoto` object; null input -> null. */
export function decodeChatActionUploadingPhoto(json: TdJsonRaw): ChatActionUploadingPhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionUploadingPhoto = new ChatActionUploadingPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.progress = asNumber(record['progress']);
  return out;
}

/** Encode a `chatActionUploadingPhoto` into TDLib JSON wire shape. */
export function encodeChatActionUploadingPhoto(value: ChatActionUploadingPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionUploadingPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['progress'] = value.progress;
  return out;
}

/** chatActionUploadingVideo = ChatAction — The user is uploading a video */
export class ChatActionUploadingVideo {
  type: 'chatActionUploadingVideo' = 'chatActionUploadingVideo';
  extra?: string;
  /** progress: int32 — Upload progress, as a percentage */
  progress: number = 0;
}

/** Decode a TDLib `chatActionUploadingVideo` object; null input -> null. */
export function decodeChatActionUploadingVideo(json: TdJsonRaw): ChatActionUploadingVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionUploadingVideo = new ChatActionUploadingVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.progress = asNumber(record['progress']);
  return out;
}

/** Encode a `chatActionUploadingVideo` into TDLib JSON wire shape. */
export function encodeChatActionUploadingVideo(value: ChatActionUploadingVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionUploadingVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['progress'] = value.progress;
  return out;
}

/** chatActionUploadingVideoNote = ChatAction — The user is uploading a video note */
export class ChatActionUploadingVideoNote {
  type: 'chatActionUploadingVideoNote' = 'chatActionUploadingVideoNote';
  extra?: string;
  /** progress: int32 — Upload progress, as a percentage */
  progress: number = 0;
}

/** Decode a TDLib `chatActionUploadingVideoNote` object; null input -> null. */
export function decodeChatActionUploadingVideoNote(json: TdJsonRaw): ChatActionUploadingVideoNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionUploadingVideoNote = new ChatActionUploadingVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.progress = asNumber(record['progress']);
  return out;
}

/** Encode a `chatActionUploadingVideoNote` into TDLib JSON wire shape. */
export function encodeChatActionUploadingVideoNote(value: ChatActionUploadingVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionUploadingVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['progress'] = value.progress;
  return out;
}

/** chatActionUploadingVoiceNote = ChatAction — The user is uploading a voice note */
export class ChatActionUploadingVoiceNote {
  type: 'chatActionUploadingVoiceNote' = 'chatActionUploadingVoiceNote';
  extra?: string;
  /** progress: int32 — Upload progress, as a percentage */
  progress: number = 0;
}

/** Decode a TDLib `chatActionUploadingVoiceNote` object; null input -> null. */
export function decodeChatActionUploadingVoiceNote(json: TdJsonRaw): ChatActionUploadingVoiceNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionUploadingVoiceNote = new ChatActionUploadingVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.progress = asNumber(record['progress']);
  return out;
}

/** Encode a `chatActionUploadingVoiceNote` into TDLib JSON wire shape. */
export function encodeChatActionUploadingVoiceNote(value: ChatActionUploadingVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionUploadingVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['progress'] = value.progress;
  return out;
}

/** chatActionWatchingAnimations = ChatAction — The user is watching animations sent by the other party by clicking on an animated emoji */
export class ChatActionWatchingAnimations {
  type: 'chatActionWatchingAnimations' = 'chatActionWatchingAnimations';
  extra?: string;
  /** emoji: string — The animated emoji */
  emoji: string = '';
}

/** Decode a TDLib `chatActionWatchingAnimations` object; null input -> null. */
export function decodeChatActionWatchingAnimations(json: TdJsonRaw): ChatActionWatchingAnimations | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActionWatchingAnimations = new ChatActionWatchingAnimations();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji = asString(record['emoji']);
  return out;
}

/** Encode a `chatActionWatchingAnimations` into TDLib JSON wire shape. */
export function encodeChatActionWatchingAnimations(value: ChatActionWatchingAnimations): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActionWatchingAnimations';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji'] = value.emoji;
  return out;
}

/** chatActiveStories = ChatActiveStories — Describes active stories posted by a chat only by calling removeTopChat with topChatCategoryUsers and the chat_id. Stories of the current user can't be archived nor hidden using removeTopChat */
export class ChatActiveStories {
  type: 'chatActiveStories' = 'chatActiveStories';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that posted the stories */
  chat_id: number = 0;
  /** list: StoryList — Identifier of the story list in which the stories are shown; may be null if the stories aren't shown in a story list */
  list: TdStoryList | null = null;
  /** order: int53 — A parameter used to determine order of the stories in the story list; 0 if the stories don't need to be shown in the story list. Stories must be sorted by the pair (order, story_poster_chat_id) in descending order */
  order: number = 0;
  /** can_be_archived: Bool — True, if the stories are shown in the main story list and can be archived; otherwise, the stories can be hidden from the main story list */
  can_be_archived: boolean = false;
  /** max_read_story_id: int32 — Identifier of the last read active story */
  max_read_story_id: number = 0;
  /** stories: vector<storyInfo> — Basic information about the stories; use getStory to get full information about the stories. The stories are in chronological order (i.e., in order of increasing story identifiers) */
  stories: StoryInfo[] = [];
}

/** Decode a TDLib `chatActiveStories` object; null input -> null. */
export function decodeChatActiveStories(json: TdJsonRaw): ChatActiveStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatActiveStories = new ChatActiveStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.list = decodeTdStoryList(record['list']);
  out.order = asNumber(record['order']);
  out.can_be_archived = asBool(record['can_be_archived']);
  out.max_read_story_id = asNumber(record['max_read_story_id']);
  out.stories = decodeVector(record['stories'], (item: TdJsonRaw): StoryInfo => decodeStoryInfo(item) as StoryInfo);
  return out;
}

/** Encode a `chatActiveStories` into TDLib JSON wire shape. */
export function encodeChatActiveStories(value: ChatActiveStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatActiveStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['list'] = value.list === null ? null : encodeTdStoryList(value.list);
  out['order'] = value.order;
  out['can_be_archived'] = value.can_be_archived;
  out['max_read_story_id'] = value.max_read_story_id;
  out['stories'] = encodeVector(value.stories, (item: StoryInfo | null): TdJsonRaw => item === null ? null : encodeStoryInfo(item));
  return out;
}

/** chatAdministrator = ChatAdministrator — Contains information about a chat administrator */
export class ChatAdministrator {
  type: 'chatAdministrator' = 'chatAdministrator';
  extra?: string;
  /** user_id: int53 — User identifier of the administrator */
  user_id: number = 0;
  /** custom_title: string — Custom title of the administrator */
  custom_title: string = '';
  /** is_owner: Bool — True, if the user is the owner of the chat */
  is_owner: boolean = false;
  /** can_be_edited: Bool — True, if the current user can edit the administrator privileges for the administrator */
  can_be_edited: boolean = false;
}

/** Decode a TDLib `chatAdministrator` object; null input -> null. */
export function decodeChatAdministrator(json: TdJsonRaw): ChatAdministrator | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatAdministrator = new ChatAdministrator();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.custom_title = asString(record['custom_title']);
  out.is_owner = asBool(record['is_owner']);
  out.can_be_edited = asBool(record['can_be_edited']);
  return out;
}

/** Encode a `chatAdministrator` into TDLib JSON wire shape. */
export function encodeChatAdministrator(value: ChatAdministrator): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatAdministrator';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['custom_title'] = value.custom_title;
  out['is_owner'] = value.is_owner;
  out['can_be_edited'] = value.can_be_edited;
  return out;
}

/** chatAdministratorRights = ChatAdministratorRights — Describes rights of the administrator ignore slow mode, and send messages to the chat without paying Telegram Stars. Implied by any other privilege; applicable to supergroups and channe... */
export class ChatAdministratorRights {
  type: 'chatAdministratorRights' = 'chatAdministratorRights';
  extra?: string;
  /** can_manage_chat: Bool — True, if the administrator can access the chat event log, get boost list, see hidden supergroup and channel members, report supergroup spam messages, */
  can_manage_chat: boolean = false;
  /** can_change_info: Bool — True, if the administrator can change the chat title, photo, and other settings */
  can_change_info: boolean = false;
  /** can_post_messages: Bool — True, if the administrator can create channel posts, approve suggested channel posts, or view channel statistics; applicable to channels only */
  can_post_messages: boolean = false;
  /** can_edit_messages: Bool — True, if the administrator can edit messages of other users and pin messages; applicable to channels only */
  can_edit_messages: boolean = false;
  /** can_delete_messages: Bool — True, if the administrator can delete messages of other users */
  can_delete_messages: boolean = false;
  /** can_invite_users: Bool — True, if the administrator can invite new users to the chat */
  can_invite_users: boolean = false;
  /** can_restrict_members: Bool — True, if the administrator can restrict, ban, or unban chat members or view supergroup statistics */
  can_restrict_members: boolean = false;
  /** can_pin_messages: Bool — True, if the administrator can pin messages; applicable to basic groups and supergroups only */
  can_pin_messages: boolean = false;
  /** can_manage_topics: Bool — True, if the administrator can create, rename, close, reopen, hide, and unhide forum topics; applicable to forum supergroups only */
  can_manage_topics: boolean = false;
  /** can_promote_members: Bool — True, if the administrator can add new administrators with a subset of their own privileges or demote administrators that were directly or indirectly promoted by them; applicable to supergroups and channels only */
  can_promote_members: boolean = false;
  /** can_manage_video_chats: Bool — True, if the administrator can manage video chats */
  can_manage_video_chats: boolean = false;
  /** can_post_stories: Bool — True, if the administrator can create new chat stories, or edit and delete posted stories; applicable to supergroups and channels only */
  can_post_stories: boolean = false;
  /** can_edit_stories: Bool — True, if the administrator can edit stories posted by other users, post stories to the chat page, pin chat stories, and access story archive; applicable to supergroups and channels only */
  can_edit_stories: boolean = false;
  /** can_delete_stories: Bool — True, if the administrator can delete stories posted by other users; applicable to supergroups and channels only */
  can_delete_stories: boolean = false;
  /** can_manage_direct_messages: Bool — True, if the administrator can answer to channel direct messages; applicable to channels only */
  can_manage_direct_messages: boolean = false;
  /** can_manage_tags: Bool — True, if the administrator can change tags of other users; applicable to basic groups and supergroups only */
  can_manage_tags: boolean = false;
  /** can_send_welcome_messages: Bool — True, if the administrator can manage and send welcome messages */
  can_send_welcome_messages: boolean = false;
  /** is_anonymous: Bool — True, if the administrator isn't shown in the chat member list and sends messages anonymously; applicable to supergroups only */
  is_anonymous: boolean = false;
}

/** Decode a TDLib `chatAdministratorRights` object; null input -> null. */
export function decodeChatAdministratorRights(json: TdJsonRaw): ChatAdministratorRights | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatAdministratorRights = new ChatAdministratorRights();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_manage_chat = asBool(record['can_manage_chat']);
  out.can_change_info = asBool(record['can_change_info']);
  out.can_post_messages = asBool(record['can_post_messages']);
  out.can_edit_messages = asBool(record['can_edit_messages']);
  out.can_delete_messages = asBool(record['can_delete_messages']);
  out.can_invite_users = asBool(record['can_invite_users']);
  out.can_restrict_members = asBool(record['can_restrict_members']);
  out.can_pin_messages = asBool(record['can_pin_messages']);
  out.can_manage_topics = asBool(record['can_manage_topics']);
  out.can_promote_members = asBool(record['can_promote_members']);
  out.can_manage_video_chats = asBool(record['can_manage_video_chats']);
  out.can_post_stories = asBool(record['can_post_stories']);
  out.can_edit_stories = asBool(record['can_edit_stories']);
  out.can_delete_stories = asBool(record['can_delete_stories']);
  out.can_manage_direct_messages = asBool(record['can_manage_direct_messages']);
  out.can_manage_tags = asBool(record['can_manage_tags']);
  out.can_send_welcome_messages = asBool(record['can_send_welcome_messages']);
  out.is_anonymous = asBool(record['is_anonymous']);
  return out;
}

/** Encode a `chatAdministratorRights` into TDLib JSON wire shape. */
export function encodeChatAdministratorRights(value: ChatAdministratorRights): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatAdministratorRights';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_manage_chat'] = value.can_manage_chat;
  out['can_change_info'] = value.can_change_info;
  out['can_post_messages'] = value.can_post_messages;
  out['can_edit_messages'] = value.can_edit_messages;
  out['can_delete_messages'] = value.can_delete_messages;
  out['can_invite_users'] = value.can_invite_users;
  out['can_restrict_members'] = value.can_restrict_members;
  out['can_pin_messages'] = value.can_pin_messages;
  out['can_manage_topics'] = value.can_manage_topics;
  out['can_promote_members'] = value.can_promote_members;
  out['can_manage_video_chats'] = value.can_manage_video_chats;
  out['can_post_stories'] = value.can_post_stories;
  out['can_edit_stories'] = value.can_edit_stories;
  out['can_delete_stories'] = value.can_delete_stories;
  out['can_manage_direct_messages'] = value.can_manage_direct_messages;
  out['can_manage_tags'] = value.can_manage_tags;
  out['can_send_welcome_messages'] = value.can_send_welcome_messages;
  out['is_anonymous'] = value.is_anonymous;
  return out;
}

/** chatAdministrators = ChatAdministrators — Represents a list of chat administrators */
export class ChatAdministrators {
  type: 'chatAdministrators' = 'chatAdministrators';
  extra?: string;
  /** administrators: vector<chatAdministrator> — A list of chat administrators */
  administrators: ChatAdministrator[] = [];
}

/** Decode a TDLib `chatAdministrators` object; null input -> null. */
export function decodeChatAdministrators(json: TdJsonRaw): ChatAdministrators | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatAdministrators = new ChatAdministrators();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.administrators = decodeVector(record['administrators'], (item: TdJsonRaw): ChatAdministrator => decodeChatAdministrator(item) as ChatAdministrator);
  return out;
}

/** Encode a `chatAdministrators` into TDLib JSON wire shape. */
export function encodeChatAdministrators(value: ChatAdministrators): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatAdministrators';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['administrators'] = encodeVector(value.administrators, (item: ChatAdministrator | null): TdJsonRaw => item === null ? null : encodeChatAdministrator(item));
  return out;
}

/** chatAvailableReactionsAll = ChatAvailableReactions — All reactions are available in the chat, excluding the paid reaction and custom reactions in channel chats */
export class ChatAvailableReactionsAll {
  type: 'chatAvailableReactionsAll' = 'chatAvailableReactionsAll';
  extra?: string;
  /** max_reaction_count: int32 — The maximum allowed number of reactions per message; 1-11 */
  max_reaction_count: number = 0;
}

/** Decode a TDLib `chatAvailableReactionsAll` object; null input -> null. */
export function decodeChatAvailableReactionsAll(json: TdJsonRaw): ChatAvailableReactionsAll | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatAvailableReactionsAll = new ChatAvailableReactionsAll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.max_reaction_count = asNumber(record['max_reaction_count']);
  return out;
}

/** Encode a `chatAvailableReactionsAll` into TDLib JSON wire shape. */
export function encodeChatAvailableReactionsAll(value: ChatAvailableReactionsAll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatAvailableReactionsAll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['max_reaction_count'] = value.max_reaction_count;
  return out;
}

/** chatAvailableReactionsSome = ChatAvailableReactions — Only specific reactions are available in the chat */
export class ChatAvailableReactionsSome {
  type: 'chatAvailableReactionsSome' = 'chatAvailableReactionsSome';
  extra?: string;
  /** reactions: vector<ReactionType> — The list of reactions */
  reactions: TdReactionType[] = [];
  /** max_reaction_count: int32 — The maximum allowed number of reactions per message; 1-11 */
  max_reaction_count: number = 0;
}

/** Decode a TDLib `chatAvailableReactionsSome` object; null input -> null. */
export function decodeChatAvailableReactionsSome(json: TdJsonRaw): ChatAvailableReactionsSome | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatAvailableReactionsSome = new ChatAvailableReactionsSome();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reactions = decodeVector(record['reactions'], (item: TdJsonRaw): TdReactionType => decodeTdReactionType(item) as TdReactionType);
  out.max_reaction_count = asNumber(record['max_reaction_count']);
  return out;
}

/** Encode a `chatAvailableReactionsSome` into TDLib JSON wire shape. */
export function encodeChatAvailableReactionsSome(value: ChatAvailableReactionsSome): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatAvailableReactionsSome';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reactions'] = encodeVector(value.reactions, (item: TdReactionType | null): TdJsonRaw => item === null ? null : encodeTdReactionType(item));
  out['max_reaction_count'] = value.max_reaction_count;
  return out;
}

/** chatBackground = ChatBackground — Describes a background set for a specific chat */
export class ChatBackground {
  type: 'chatBackground' = 'chatBackground';
  extra?: string;
  /** background: background — The background */
  background: Background | null = null;
  /** dark_theme_dimming: int32 — Dimming of the background in dark themes, as a percentage; 0-100. Applied only to Wallpaper and Fill types of background */
  dark_theme_dimming: number = 0;
}

/** Decode a TDLib `chatBackground` object; null input -> null. */
export function decodeChatBackground(json: TdJsonRaw): ChatBackground | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBackground = new ChatBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.background = decodeBackground(record['background']);
  out.dark_theme_dimming = asNumber(record['dark_theme_dimming']);
  return out;
}

/** Encode a `chatBackground` into TDLib JSON wire shape. */
export function encodeChatBackground(value: ChatBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['background'] = value.background === null ? null : encodeBackground(value.background);
  out['dark_theme_dimming'] = value.dark_theme_dimming;
  return out;
}

/** chatBoost = ChatBoost — Describes a boost applied to a chat */
export class ChatBoost {
  type: 'chatBoost' = 'chatBoost';
  extra?: string;
  /** id: string — Unique identifier of the boost */
  id: string = '';
  /** count: int32 — The number of identical boosts applied */
  count: number = 0;
  /** source: ChatBoostSource — Source of the boost */
  source: TdChatBoostSource | null = null;
  /** start_date: int32 — Point in time (Unix timestamp) when the chat was boosted */
  start_date: number = 0;
  /** expiration_date: int32 — Point in time (Unix timestamp) when the boost will expire */
  expiration_date: number = 0;
}

/** Decode a TDLib `chatBoost` object; null input -> null. */
export function decodeChatBoost(json: TdJsonRaw): ChatBoost | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoost = new ChatBoost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.count = asNumber(record['count']);
  out.source = decodeTdChatBoostSource(record['source']);
  out.start_date = asNumber(record['start_date']);
  out.expiration_date = asNumber(record['expiration_date']);
  return out;
}

/** Encode a `chatBoost` into TDLib JSON wire shape. */
export function encodeChatBoost(value: ChatBoost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['count'] = value.count;
  out['source'] = value.source === null ? null : encodeTdChatBoostSource(value.source);
  out['start_date'] = value.start_date;
  out['expiration_date'] = value.expiration_date;
  return out;
}

/** chatBoostFeatures = ChatBoostFeatures — Contains a list of features available on the first chat boost levels */
export class ChatBoostFeatures {
  type: 'chatBoostFeatures' = 'chatBoostFeatures';
  extra?: string;
  /** features: vector<chatBoostLevelFeatures> — The list of features */
  features: ChatBoostLevelFeatures[] = [];
  /** min_profile_background_custom_emoji_boost_level: int32 — The minimum boost level required to set custom emoji for profile background */
  min_profile_background_custom_emoji_boost_level: number = 0;
  /** min_background_custom_emoji_boost_level: int32 — The minimum boost level required to set custom emoji for reply header and link preview background; for channel chats only */
  min_background_custom_emoji_boost_level: number = 0;
  /** min_emoji_status_boost_level: int32 — The minimum boost level required to set emoji status */
  min_emoji_status_boost_level: number = 0;
  /** min_chat_theme_background_boost_level: int32 — The minimum boost level required to set a chat theme background as chat background */
  min_chat_theme_background_boost_level: number = 0;
  /** min_custom_background_boost_level: int32 — The minimum boost level required to set custom chat background */
  min_custom_background_boost_level: number = 0;
  /** min_custom_emoji_sticker_set_boost_level: int32 — The minimum boost level required to set custom emoji sticker set for the chat; for supergroup chats only */
  min_custom_emoji_sticker_set_boost_level: number = 0;
  /** min_automatic_translation_boost_level: int32 — The minimum boost level allowing to enable automatic translation of messages for non-Premium users; for channel chats only */
  min_automatic_translation_boost_level: number = 0;
  /** min_speech_recognition_boost_level: int32 — The minimum boost level allowing to recognize speech in video note and voice note messages for non-Premium users; for supergroup chats only */
  min_speech_recognition_boost_level: number = 0;
  /** min_sponsored_message_disable_boost_level: int32 — The minimum boost level allowing to disable sponsored messages in the chat; for channel chats only */
  min_sponsored_message_disable_boost_level: number = 0;
}

/** Decode a TDLib `chatBoostFeatures` object; null input -> null. */
export function decodeChatBoostFeatures(json: TdJsonRaw): ChatBoostFeatures | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostFeatures = new ChatBoostFeatures();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.features = decodeVector(record['features'], (item: TdJsonRaw): ChatBoostLevelFeatures => decodeChatBoostLevelFeatures(item) as ChatBoostLevelFeatures);
  out.min_profile_background_custom_emoji_boost_level = asNumber(record['min_profile_background_custom_emoji_boost_level']);
  out.min_background_custom_emoji_boost_level = asNumber(record['min_background_custom_emoji_boost_level']);
  out.min_emoji_status_boost_level = asNumber(record['min_emoji_status_boost_level']);
  out.min_chat_theme_background_boost_level = asNumber(record['min_chat_theme_background_boost_level']);
  out.min_custom_background_boost_level = asNumber(record['min_custom_background_boost_level']);
  out.min_custom_emoji_sticker_set_boost_level = asNumber(record['min_custom_emoji_sticker_set_boost_level']);
  out.min_automatic_translation_boost_level = asNumber(record['min_automatic_translation_boost_level']);
  out.min_speech_recognition_boost_level = asNumber(record['min_speech_recognition_boost_level']);
  out.min_sponsored_message_disable_boost_level = asNumber(record['min_sponsored_message_disable_boost_level']);
  return out;
}

/** Encode a `chatBoostFeatures` into TDLib JSON wire shape. */
export function encodeChatBoostFeatures(value: ChatBoostFeatures): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostFeatures';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['features'] = encodeVector(value.features, (item: ChatBoostLevelFeatures | null): TdJsonRaw => item === null ? null : encodeChatBoostLevelFeatures(item));
  out['min_profile_background_custom_emoji_boost_level'] = value.min_profile_background_custom_emoji_boost_level;
  out['min_background_custom_emoji_boost_level'] = value.min_background_custom_emoji_boost_level;
  out['min_emoji_status_boost_level'] = value.min_emoji_status_boost_level;
  out['min_chat_theme_background_boost_level'] = value.min_chat_theme_background_boost_level;
  out['min_custom_background_boost_level'] = value.min_custom_background_boost_level;
  out['min_custom_emoji_sticker_set_boost_level'] = value.min_custom_emoji_sticker_set_boost_level;
  out['min_automatic_translation_boost_level'] = value.min_automatic_translation_boost_level;
  out['min_speech_recognition_boost_level'] = value.min_speech_recognition_boost_level;
  out['min_sponsored_message_disable_boost_level'] = value.min_sponsored_message_disable_boost_level;
  return out;
}

/** chatBoostLevelFeatures = ChatBoostLevelFeatures — Contains a list of features available on a specific chat boost level */
export class ChatBoostLevelFeatures {
  type: 'chatBoostLevelFeatures' = 'chatBoostLevelFeatures';
  extra?: string;
  /** level: int32 — Target chat boost level */
  level: number = 0;
  /** story_per_day_count: int32 — Number of stories that the chat can publish daily */
  story_per_day_count: number = 0;
  /** custom_emoji_reaction_count: int32 — Number of custom emoji reactions that can be added to the list of available reactions */
  custom_emoji_reaction_count: number = 0;
  /** title_color_count: int32 — Number of custom colors for chat title */
  title_color_count: number = 0;
  /** profile_accent_color_count: int32 — Number of custom colors for profile photo background */
  profile_accent_color_count: number = 0;
  /** can_set_profile_background_custom_emoji: Bool — True, if custom emoji for profile background can be set */
  can_set_profile_background_custom_emoji: boolean = false;
  /** accent_color_count: int32 — Number of custom colors for background of empty chat photo, replies to messages and link previews */
  accent_color_count: number = 0;
  /** can_set_background_custom_emoji: Bool — True, if custom emoji for reply header and link preview background can be set */
  can_set_background_custom_emoji: boolean = false;
  /** can_set_emoji_status: Bool — True, if emoji status can be set */
  can_set_emoji_status: boolean = false;
  /** chat_theme_background_count: int32 — Number of chat theme backgrounds that can be set as chat background */
  chat_theme_background_count: number = 0;
  /** can_set_custom_background: Bool — True, if custom background can be set in the chat for all users */
  can_set_custom_background: boolean = false;
  /** can_set_custom_emoji_sticker_set: Bool — True, if custom emoji sticker set can be set for the chat */
  can_set_custom_emoji_sticker_set: boolean = false;
  /** can_enable_automatic_translation: Bool — True, if automatic translation of messages can be enabled in the chat */
  can_enable_automatic_translation: boolean = false;
  /** can_recognize_speech: Bool — True, if speech recognition can be used for video note and voice note messages by all users */
  can_recognize_speech: boolean = false;
  /** can_disable_sponsored_messages: Bool — True, if sponsored messages can be disabled in the chat */
  can_disable_sponsored_messages: boolean = false;
}

/** Decode a TDLib `chatBoostLevelFeatures` object; null input -> null. */
export function decodeChatBoostLevelFeatures(json: TdJsonRaw): ChatBoostLevelFeatures | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostLevelFeatures = new ChatBoostLevelFeatures();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.level = asNumber(record['level']);
  out.story_per_day_count = asNumber(record['story_per_day_count']);
  out.custom_emoji_reaction_count = asNumber(record['custom_emoji_reaction_count']);
  out.title_color_count = asNumber(record['title_color_count']);
  out.profile_accent_color_count = asNumber(record['profile_accent_color_count']);
  out.can_set_profile_background_custom_emoji = asBool(record['can_set_profile_background_custom_emoji']);
  out.accent_color_count = asNumber(record['accent_color_count']);
  out.can_set_background_custom_emoji = asBool(record['can_set_background_custom_emoji']);
  out.can_set_emoji_status = asBool(record['can_set_emoji_status']);
  out.chat_theme_background_count = asNumber(record['chat_theme_background_count']);
  out.can_set_custom_background = asBool(record['can_set_custom_background']);
  out.can_set_custom_emoji_sticker_set = asBool(record['can_set_custom_emoji_sticker_set']);
  out.can_enable_automatic_translation = asBool(record['can_enable_automatic_translation']);
  out.can_recognize_speech = asBool(record['can_recognize_speech']);
  out.can_disable_sponsored_messages = asBool(record['can_disable_sponsored_messages']);
  return out;
}

/** Encode a `chatBoostLevelFeatures` into TDLib JSON wire shape. */
export function encodeChatBoostLevelFeatures(value: ChatBoostLevelFeatures): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostLevelFeatures';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['level'] = value.level;
  out['story_per_day_count'] = value.story_per_day_count;
  out['custom_emoji_reaction_count'] = value.custom_emoji_reaction_count;
  out['title_color_count'] = value.title_color_count;
  out['profile_accent_color_count'] = value.profile_accent_color_count;
  out['can_set_profile_background_custom_emoji'] = value.can_set_profile_background_custom_emoji;
  out['accent_color_count'] = value.accent_color_count;
  out['can_set_background_custom_emoji'] = value.can_set_background_custom_emoji;
  out['can_set_emoji_status'] = value.can_set_emoji_status;
  out['chat_theme_background_count'] = value.chat_theme_background_count;
  out['can_set_custom_background'] = value.can_set_custom_background;
  out['can_set_custom_emoji_sticker_set'] = value.can_set_custom_emoji_sticker_set;
  out['can_enable_automatic_translation'] = value.can_enable_automatic_translation;
  out['can_recognize_speech'] = value.can_recognize_speech;
  out['can_disable_sponsored_messages'] = value.can_disable_sponsored_messages;
  return out;
}

/** chatBoostLink = ChatBoostLink — Contains an HTTPS link to boost a chat */
export class ChatBoostLink {
  type: 'chatBoostLink' = 'chatBoostLink';
  extra?: string;
  /** link: string — The link */
  link: string = '';
  /** is_public: Bool — True, if the link will work for non-members of the chat */
  is_public: boolean = false;
}

/** Decode a TDLib `chatBoostLink` object; null input -> null. */
export function decodeChatBoostLink(json: TdJsonRaw): ChatBoostLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostLink = new ChatBoostLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  out.is_public = asBool(record['is_public']);
  return out;
}

/** Encode a `chatBoostLink` into TDLib JSON wire shape. */
export function encodeChatBoostLink(value: ChatBoostLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  out['is_public'] = value.is_public;
  return out;
}

/** chatBoostLinkInfo = ChatBoostLinkInfo — Contains information about a link to boost a chat */
export class ChatBoostLinkInfo {
  type: 'chatBoostLinkInfo' = 'chatBoostLinkInfo';
  extra?: string;
  /** is_public: Bool — True, if the link will work for non-members of the chat */
  is_public: boolean = false;
  /** chat_id: int53 — Identifier of the chat to which the link points; 0 if the chat isn't found */
  chat_id: number = 0;
}

/** Decode a TDLib `chatBoostLinkInfo` object; null input -> null. */
export function decodeChatBoostLinkInfo(json: TdJsonRaw): ChatBoostLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostLinkInfo = new ChatBoostLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_public = asBool(record['is_public']);
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `chatBoostLinkInfo` into TDLib JSON wire shape. */
export function encodeChatBoostLinkInfo(value: ChatBoostLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_public'] = value.is_public;
  out['chat_id'] = value.chat_id;
  return out;
}

/** chatBoostSlot = ChatBoostSlot — Describes a slot for chat boost */
export class ChatBoostSlot {
  type: 'chatBoostSlot' = 'chatBoostSlot';
  extra?: string;
  /** slot_id: int32 — Unique identifier of the slot */
  slot_id: number = 0;
  /** currently_boosted_chat_id: int53 — Identifier of the currently boosted chat; 0 if none */
  currently_boosted_chat_id: number = 0;
  /** start_date: int32 — Point in time (Unix timestamp) when the chat was boosted; 0 if none */
  start_date: number = 0;
  /** expiration_date: int32 — Point in time (Unix timestamp) when the boost will expire */
  expiration_date: number = 0;
  /** cooldown_until_date: int32 — Point in time (Unix timestamp) after which the boost can be used for another chat */
  cooldown_until_date: number = 0;
}

/** Decode a TDLib `chatBoostSlot` object; null input -> null. */
export function decodeChatBoostSlot(json: TdJsonRaw): ChatBoostSlot | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostSlot = new ChatBoostSlot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.slot_id = asNumber(record['slot_id']);
  out.currently_boosted_chat_id = asNumber(record['currently_boosted_chat_id']);
  out.start_date = asNumber(record['start_date']);
  out.expiration_date = asNumber(record['expiration_date']);
  out.cooldown_until_date = asNumber(record['cooldown_until_date']);
  return out;
}

/** Encode a `chatBoostSlot` into TDLib JSON wire shape. */
export function encodeChatBoostSlot(value: ChatBoostSlot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostSlot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['slot_id'] = value.slot_id;
  out['currently_boosted_chat_id'] = value.currently_boosted_chat_id;
  out['start_date'] = value.start_date;
  out['expiration_date'] = value.expiration_date;
  out['cooldown_until_date'] = value.cooldown_until_date;
  return out;
}

/** chatBoostSlots = ChatBoostSlots — Contains a list of chat boost slots */
export class ChatBoostSlots {
  type: 'chatBoostSlots' = 'chatBoostSlots';
  extra?: string;
  /** slots: vector<chatBoostSlot> — List of boost slots */
  slots: ChatBoostSlot[] = [];
}

/** Decode a TDLib `chatBoostSlots` object; null input -> null. */
export function decodeChatBoostSlots(json: TdJsonRaw): ChatBoostSlots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostSlots = new ChatBoostSlots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.slots = decodeVector(record['slots'], (item: TdJsonRaw): ChatBoostSlot => decodeChatBoostSlot(item) as ChatBoostSlot);
  return out;
}

/** Encode a `chatBoostSlots` into TDLib JSON wire shape. */
export function encodeChatBoostSlots(value: ChatBoostSlots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostSlots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['slots'] = encodeVector(value.slots, (item: ChatBoostSlot | null): TdJsonRaw => item === null ? null : encodeChatBoostSlot(item));
  return out;
}

/** chatBoostSourceGiftCode = ChatBoostSource — The chat created a Telegram Premium gift code for a user */
export class ChatBoostSourceGiftCode {
  type: 'chatBoostSourceGiftCode' = 'chatBoostSourceGiftCode';
  extra?: string;
  /** user_id: int53 — Identifier of a user, for which the gift code was created */
  user_id: number = 0;
  /** gift_code: string — The created Telegram Premium gift code, which is known only if this is a gift code for the current user, or it has already been claimed */
  gift_code: string = '';
}

/** Decode a TDLib `chatBoostSourceGiftCode` object; null input -> null. */
export function decodeChatBoostSourceGiftCode(json: TdJsonRaw): ChatBoostSourceGiftCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostSourceGiftCode = new ChatBoostSourceGiftCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.gift_code = asString(record['gift_code']);
  return out;
}

/** Encode a `chatBoostSourceGiftCode` into TDLib JSON wire shape. */
export function encodeChatBoostSourceGiftCode(value: ChatBoostSourceGiftCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostSourceGiftCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['gift_code'] = value.gift_code;
  return out;
}

/** chatBoostSourceGiveaway = ChatBoostSource — The chat created a giveaway */
export class ChatBoostSourceGiveaway {
  type: 'chatBoostSourceGiveaway' = 'chatBoostSourceGiveaway';
  extra?: string;
  /** user_id: int53 — Identifier of a user who won in the giveaway; 0 if none */
  user_id: number = 0;
  /** gift_code: string — The created Telegram Premium gift code if it was used by the user or can be claimed by the current user; an empty string otherwise; for Telegram Premium giveways only */
  gift_code: string = '';
  /** star_count: int53 — Number of Telegram Stars distributed among winners of the giveaway */
  star_count: number = 0;
  /** giveaway_message_id: int53 — Identifier of the corresponding giveaway message; can be an identifier of a deleted message */
  giveaway_message_id: number = 0;
  /** is_unclaimed: Bool — True, if the winner for the corresponding giveaway prize wasn't chosen, because there were not enough participants */
  is_unclaimed: boolean = false;
}

/** Decode a TDLib `chatBoostSourceGiveaway` object; null input -> null. */
export function decodeChatBoostSourceGiveaway(json: TdJsonRaw): ChatBoostSourceGiveaway | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostSourceGiveaway = new ChatBoostSourceGiveaway();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.gift_code = asString(record['gift_code']);
  out.star_count = asNumber(record['star_count']);
  out.giveaway_message_id = asNumber(record['giveaway_message_id']);
  out.is_unclaimed = asBool(record['is_unclaimed']);
  return out;
}

/** Encode a `chatBoostSourceGiveaway` into TDLib JSON wire shape. */
export function encodeChatBoostSourceGiveaway(value: ChatBoostSourceGiveaway): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostSourceGiveaway';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['gift_code'] = value.gift_code;
  out['star_count'] = value.star_count;
  out['giveaway_message_id'] = value.giveaway_message_id;
  out['is_unclaimed'] = value.is_unclaimed;
  return out;
}

/** chatBoostSourcePremium = ChatBoostSource — A user with Telegram Premium subscription or gifted Telegram Premium boosted the chat */
export class ChatBoostSourcePremium {
  type: 'chatBoostSourcePremium' = 'chatBoostSourcePremium';
  extra?: string;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
}

/** Decode a TDLib `chatBoostSourcePremium` object; null input -> null. */
export function decodeChatBoostSourcePremium(json: TdJsonRaw): ChatBoostSourcePremium | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostSourcePremium = new ChatBoostSourcePremium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `chatBoostSourcePremium` into TDLib JSON wire shape. */
export function encodeChatBoostSourcePremium(value: ChatBoostSourcePremium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostSourcePremium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** chatBoostStatus = ChatBoostStatus — Describes current boost status of a chat */
export class ChatBoostStatus {
  type: 'chatBoostStatus' = 'chatBoostStatus';
  extra?: string;
  /** boost_url: string — An HTTP URL, which can be used to boost the chat */
  boost_url: string = '';
  /** applied_slot_ids: vector<int32> — Identifiers of boost slots of the current user applied to the chat */
  applied_slot_ids: number[] = [];
  /** level: int32 — Current boost level of the chat */
  level: number = 0;
  /** gift_code_boost_count: int32 — The number of boosts received by the chat from created Telegram Premium gift codes and giveaways; always 0 if the current user isn't an administrator in the chat */
  gift_code_boost_count: number = 0;
  /** boost_count: int32 — The number of boosts received by the chat */
  boost_count: number = 0;
  /** current_level_boost_count: int32 — The number of boosts added to reach the current level */
  current_level_boost_count: number = 0;
  /** next_level_boost_count: int32 — The number of boosts needed to reach the next level; 0 if the next level isn't available */
  next_level_boost_count: number = 0;
  /** premium_member_count: int32 — Approximate number of Telegram Premium subscribers joined the chat; always 0 if the current user isn't an administrator in the chat */
  premium_member_count: number = 0;
  /** premium_member_percentage: double — A percentage of Telegram Premium subscribers joined the chat; always 0 if the current user isn't an administrator in the chat */
  premium_member_percentage: number = 0;
  /** prepaid_giveaways: vector<prepaidGiveaway> — The list of prepaid giveaways available for the chat; only for chat administrators */
  prepaid_giveaways: PrepaidGiveaway[] = [];
}

/** Decode a TDLib `chatBoostStatus` object; null input -> null. */
export function decodeChatBoostStatus(json: TdJsonRaw): ChatBoostStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatBoostStatus = new ChatBoostStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.boost_url = asString(record['boost_url']);
  out.applied_slot_ids = decodeVector(record['applied_slot_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.level = asNumber(record['level']);
  out.gift_code_boost_count = asNumber(record['gift_code_boost_count']);
  out.boost_count = asNumber(record['boost_count']);
  out.current_level_boost_count = asNumber(record['current_level_boost_count']);
  out.next_level_boost_count = asNumber(record['next_level_boost_count']);
  out.premium_member_count = asNumber(record['premium_member_count']);
  out.premium_member_percentage = asNumber(record['premium_member_percentage']);
  out.prepaid_giveaways = decodeVector(record['prepaid_giveaways'], (item: TdJsonRaw): PrepaidGiveaway => decodePrepaidGiveaway(item) as PrepaidGiveaway);
  return out;
}

/** Encode a `chatBoostStatus` into TDLib JSON wire shape. */
export function encodeChatBoostStatus(value: ChatBoostStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatBoostStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['boost_url'] = value.boost_url;
  out['applied_slot_ids'] = encodeVector(value.applied_slot_ids, (item: number): TdJsonRaw => item);
  out['level'] = value.level;
  out['gift_code_boost_count'] = value.gift_code_boost_count;
  out['boost_count'] = value.boost_count;
  out['current_level_boost_count'] = value.current_level_boost_count;
  out['next_level_boost_count'] = value.next_level_boost_count;
  out['premium_member_count'] = value.premium_member_count;
  out['premium_member_percentage'] = value.premium_member_percentage;
  out['prepaid_giveaways'] = encodeVector(value.prepaid_giveaways, (item: PrepaidGiveaway | null): TdJsonRaw => item === null ? null : encodePrepaidGiveaway(item));
  return out;
}

/** chatEvent = ChatEvent — Represents a chat event */
export class ChatEvent {
  type: 'chatEvent' = 'chatEvent';
  extra?: string;
  /** id: int64 — Chat event identifier */
  id: string = '';
  /** date: int32 — Point in time (Unix timestamp) when the event happened */
  date: number = 0;
  /** member_id: MessageSender — Identifier of the user or chat who performed the action */
  member_id: TdMessageSender | null = null;
  /** action: ChatEventAction — The action */
  action: TdChatEventAction | null = null;
}

/** Decode a TDLib `chatEvent` object; null input -> null. */
export function decodeChatEvent(json: TdJsonRaw): ChatEvent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEvent = new ChatEvent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.date = asNumber(record['date']);
  out.member_id = decodeTdMessageSender(record['member_id']);
  out.action = decodeTdChatEventAction(record['action']);
  return out;
}

/** Encode a `chatEvent` into TDLib JSON wire shape. */
export function encodeChatEvent(value: ChatEvent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEvent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['date'] = value.date;
  out['member_id'] = value.member_id === null ? null : encodeTdMessageSender(value.member_id);
  out['action'] = value.action === null ? null : encodeTdChatEventAction(value.action);
  return out;
}

/** chatEventAccentColorChanged = ChatEventAction — The chat accent color or background custom emoji were changed */
export class ChatEventAccentColorChanged {
  type: 'chatEventAccentColorChanged' = 'chatEventAccentColorChanged';
  extra?: string;
  /** old_accent_color_id: int32 — Previous identifier of chat accent color */
  old_accent_color_id: number = 0;
  /** old_background_custom_emoji_id: int64 — Previous identifier of the custom emoji; 0 if none */
  old_background_custom_emoji_id: string = '';
  /** new_accent_color_id: int32 — New identifier of chat accent color */
  new_accent_color_id: number = 0;
  /** new_background_custom_emoji_id: int64 — New identifier of the custom emoji; 0 if none */
  new_background_custom_emoji_id: string = '';
}

/** Decode a TDLib `chatEventAccentColorChanged` object; null input -> null. */
export function decodeChatEventAccentColorChanged(json: TdJsonRaw): ChatEventAccentColorChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventAccentColorChanged = new ChatEventAccentColorChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_accent_color_id = asNumber(record['old_accent_color_id']);
  out.old_background_custom_emoji_id = asInt64(record['old_background_custom_emoji_id']);
  out.new_accent_color_id = asNumber(record['new_accent_color_id']);
  out.new_background_custom_emoji_id = asInt64(record['new_background_custom_emoji_id']);
  return out;
}

/** Encode a `chatEventAccentColorChanged` into TDLib JSON wire shape. */
export function encodeChatEventAccentColorChanged(value: ChatEventAccentColorChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventAccentColorChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_accent_color_id'] = value.old_accent_color_id;
  out['old_background_custom_emoji_id'] = value.old_background_custom_emoji_id;
  out['new_accent_color_id'] = value.new_accent_color_id;
  out['new_background_custom_emoji_id'] = value.new_background_custom_emoji_id;
  return out;
}

/** chatEventActiveUsernamesChanged = ChatEventAction — The chat active usernames were changed */
export class ChatEventActiveUsernamesChanged {
  type: 'chatEventActiveUsernamesChanged' = 'chatEventActiveUsernamesChanged';
  extra?: string;
  /** old_usernames: vector<string> — Previous list of active usernames */
  old_usernames: string[] = [];
  /** new_usernames: vector<string> — New list of active usernames */
  new_usernames: string[] = [];
}

/** Decode a TDLib `chatEventActiveUsernamesChanged` object; null input -> null. */
export function decodeChatEventActiveUsernamesChanged(json: TdJsonRaw): ChatEventActiveUsernamesChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventActiveUsernamesChanged = new ChatEventActiveUsernamesChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_usernames = decodeVector(record['old_usernames'], (item: TdJsonRaw): string => asString(item));
  out.new_usernames = decodeVector(record['new_usernames'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `chatEventActiveUsernamesChanged` into TDLib JSON wire shape. */
export function encodeChatEventActiveUsernamesChanged(value: ChatEventActiveUsernamesChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventActiveUsernamesChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_usernames'] = encodeVector(value.old_usernames, (item: string): TdJsonRaw => item);
  out['new_usernames'] = encodeVector(value.new_usernames, (item: string): TdJsonRaw => item);
  return out;
}

/** chatEventAutomaticTranslationToggled = ChatEventAction — The has_automatic_translation setting of a channel was toggled */
export class ChatEventAutomaticTranslationToggled {
  type: 'chatEventAutomaticTranslationToggled' = 'chatEventAutomaticTranslationToggled';
  extra?: string;
  /** has_automatic_translation: Bool — New value of has_automatic_translation */
  has_automatic_translation: boolean = false;
}

/** Decode a TDLib `chatEventAutomaticTranslationToggled` object; null input -> null. */
export function decodeChatEventAutomaticTranslationToggled(json: TdJsonRaw): ChatEventAutomaticTranslationToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventAutomaticTranslationToggled = new ChatEventAutomaticTranslationToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.has_automatic_translation = asBool(record['has_automatic_translation']);
  return out;
}

/** Encode a `chatEventAutomaticTranslationToggled` into TDLib JSON wire shape. */
export function encodeChatEventAutomaticTranslationToggled(value: ChatEventAutomaticTranslationToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventAutomaticTranslationToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['has_automatic_translation'] = value.has_automatic_translation;
  return out;
}

/** chatEventAvailableReactionsChanged = ChatEventAction — The chat available reactions were changed */
export class ChatEventAvailableReactionsChanged {
  type: 'chatEventAvailableReactionsChanged' = 'chatEventAvailableReactionsChanged';
  extra?: string;
  /** old_available_reactions: ChatAvailableReactions — Previous chat available reactions */
  old_available_reactions: TdChatAvailableReactions | null = null;
  /** new_available_reactions: ChatAvailableReactions — New chat available reactions */
  new_available_reactions: TdChatAvailableReactions | null = null;
}

/** Decode a TDLib `chatEventAvailableReactionsChanged` object; null input -> null. */
export function decodeChatEventAvailableReactionsChanged(json: TdJsonRaw): ChatEventAvailableReactionsChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventAvailableReactionsChanged = new ChatEventAvailableReactionsChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_available_reactions = decodeTdChatAvailableReactions(record['old_available_reactions']);
  out.new_available_reactions = decodeTdChatAvailableReactions(record['new_available_reactions']);
  return out;
}

/** Encode a `chatEventAvailableReactionsChanged` into TDLib JSON wire shape. */
export function encodeChatEventAvailableReactionsChanged(value: ChatEventAvailableReactionsChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventAvailableReactionsChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_available_reactions'] = value.old_available_reactions === null ? null : encodeTdChatAvailableReactions(value.old_available_reactions);
  out['new_available_reactions'] = value.new_available_reactions === null ? null : encodeTdChatAvailableReactions(value.new_available_reactions);
  return out;
}

/** chatEventBackgroundChanged = ChatEventAction — The chat background was changed */
export class ChatEventBackgroundChanged {
  type: 'chatEventBackgroundChanged' = 'chatEventBackgroundChanged';
  extra?: string;
  /** old_background: chatBackground — Previous background; may be null if none */
  old_background: ChatBackground | null = null;
  /** new_background: chatBackground — New background; may be null if none */
  new_background: ChatBackground | null = null;
}

/** Decode a TDLib `chatEventBackgroundChanged` object; null input -> null. */
export function decodeChatEventBackgroundChanged(json: TdJsonRaw): ChatEventBackgroundChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventBackgroundChanged = new ChatEventBackgroundChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_background = decodeChatBackground(record['old_background']);
  out.new_background = decodeChatBackground(record['new_background']);
  return out;
}

/** Encode a `chatEventBackgroundChanged` into TDLib JSON wire shape. */
export function encodeChatEventBackgroundChanged(value: ChatEventBackgroundChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventBackgroundChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_background'] = value.old_background === null ? null : encodeChatBackground(value.old_background);
  out['new_background'] = value.new_background === null ? null : encodeChatBackground(value.new_background);
  return out;
}

/** chatEventCustomEmojiStickerSetChanged = ChatEventAction — The supergroup sticker set with allowed custom emoji was changed */
export class ChatEventCustomEmojiStickerSetChanged {
  type: 'chatEventCustomEmojiStickerSetChanged' = 'chatEventCustomEmojiStickerSetChanged';
  extra?: string;
  /** old_sticker_set_id: int64 — Previous identifier of the chat sticker set; 0 if none */
  old_sticker_set_id: string = '';
  /** new_sticker_set_id: int64 — New identifier of the chat sticker set; 0 if none */
  new_sticker_set_id: string = '';
}

/** Decode a TDLib `chatEventCustomEmojiStickerSetChanged` object; null input -> null. */
export function decodeChatEventCustomEmojiStickerSetChanged(json: TdJsonRaw): ChatEventCustomEmojiStickerSetChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventCustomEmojiStickerSetChanged = new ChatEventCustomEmojiStickerSetChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_sticker_set_id = asInt64(record['old_sticker_set_id']);
  out.new_sticker_set_id = asInt64(record['new_sticker_set_id']);
  return out;
}

/** Encode a `chatEventCustomEmojiStickerSetChanged` into TDLib JSON wire shape. */
export function encodeChatEventCustomEmojiStickerSetChanged(value: ChatEventCustomEmojiStickerSetChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventCustomEmojiStickerSetChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_sticker_set_id'] = value.old_sticker_set_id;
  out['new_sticker_set_id'] = value.new_sticker_set_id;
  return out;
}

/** chatEventDescriptionChanged = ChatEventAction — The chat description was changed */
export class ChatEventDescriptionChanged {
  type: 'chatEventDescriptionChanged' = 'chatEventDescriptionChanged';
  extra?: string;
  /** old_description: string — Previous chat description */
  old_description: string = '';
  /** new_description: string — New chat description */
  new_description: string = '';
}

/** Decode a TDLib `chatEventDescriptionChanged` object; null input -> null. */
export function decodeChatEventDescriptionChanged(json: TdJsonRaw): ChatEventDescriptionChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventDescriptionChanged = new ChatEventDescriptionChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_description = asString(record['old_description']);
  out.new_description = asString(record['new_description']);
  return out;
}

/** Encode a `chatEventDescriptionChanged` into TDLib JSON wire shape. */
export function encodeChatEventDescriptionChanged(value: ChatEventDescriptionChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventDescriptionChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_description'] = value.old_description;
  out['new_description'] = value.new_description;
  return out;
}

/** chatEventEmojiStatusChanged = ChatEventAction — The chat emoji status was changed */
export class ChatEventEmojiStatusChanged {
  type: 'chatEventEmojiStatusChanged' = 'chatEventEmojiStatusChanged';
  extra?: string;
  /** old_emoji_status: emojiStatus — Previous emoji status; may be null if none */
  old_emoji_status: EmojiStatus | null = null;
  /** new_emoji_status: emojiStatus — New emoji status; may be null if none */
  new_emoji_status: EmojiStatus | null = null;
}

/** Decode a TDLib `chatEventEmojiStatusChanged` object; null input -> null. */
export function decodeChatEventEmojiStatusChanged(json: TdJsonRaw): ChatEventEmojiStatusChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventEmojiStatusChanged = new ChatEventEmojiStatusChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_emoji_status = decodeEmojiStatus(record['old_emoji_status']);
  out.new_emoji_status = decodeEmojiStatus(record['new_emoji_status']);
  return out;
}

/** Encode a `chatEventEmojiStatusChanged` into TDLib JSON wire shape. */
export function encodeChatEventEmojiStatusChanged(value: ChatEventEmojiStatusChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventEmojiStatusChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_emoji_status'] = value.old_emoji_status === null ? null : encodeEmojiStatus(value.old_emoji_status);
  out['new_emoji_status'] = value.new_emoji_status === null ? null : encodeEmojiStatus(value.new_emoji_status);
  return out;
}

/** chatEventForumTopicCreated = ChatEventAction — A new forum topic was created */
export class ChatEventForumTopicCreated {
  type: 'chatEventForumTopicCreated' = 'chatEventForumTopicCreated';
  extra?: string;
  /** topic_info: forumTopicInfo — Information about the topic */
  topic_info: ForumTopicInfo | null = null;
}

/** Decode a TDLib `chatEventForumTopicCreated` object; null input -> null. */
export function decodeChatEventForumTopicCreated(json: TdJsonRaw): ChatEventForumTopicCreated | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventForumTopicCreated = new ChatEventForumTopicCreated();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.topic_info = decodeForumTopicInfo(record['topic_info']);
  return out;
}

/** Encode a `chatEventForumTopicCreated` into TDLib JSON wire shape. */
export function encodeChatEventForumTopicCreated(value: ChatEventForumTopicCreated): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventForumTopicCreated';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['topic_info'] = value.topic_info === null ? null : encodeForumTopicInfo(value.topic_info);
  return out;
}

/** chatEventForumTopicDeleted = ChatEventAction — A forum topic was deleted */
export class ChatEventForumTopicDeleted {
  type: 'chatEventForumTopicDeleted' = 'chatEventForumTopicDeleted';
  extra?: string;
  /** topic_info: forumTopicInfo — Information about the topic */
  topic_info: ForumTopicInfo | null = null;
}

/** Decode a TDLib `chatEventForumTopicDeleted` object; null input -> null. */
export function decodeChatEventForumTopicDeleted(json: TdJsonRaw): ChatEventForumTopicDeleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventForumTopicDeleted = new ChatEventForumTopicDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.topic_info = decodeForumTopicInfo(record['topic_info']);
  return out;
}

/** Encode a `chatEventForumTopicDeleted` into TDLib JSON wire shape. */
export function encodeChatEventForumTopicDeleted(value: ChatEventForumTopicDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventForumTopicDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['topic_info'] = value.topic_info === null ? null : encodeForumTopicInfo(value.topic_info);
  return out;
}

/** chatEventForumTopicEdited = ChatEventAction — A forum topic was edited */
export class ChatEventForumTopicEdited {
  type: 'chatEventForumTopicEdited' = 'chatEventForumTopicEdited';
  extra?: string;
  /** old_topic_info: forumTopicInfo — Old information about the topic */
  old_topic_info: ForumTopicInfo | null = null;
  /** new_topic_info: forumTopicInfo — New information about the topic */
  new_topic_info: ForumTopicInfo | null = null;
}

/** Decode a TDLib `chatEventForumTopicEdited` object; null input -> null. */
export function decodeChatEventForumTopicEdited(json: TdJsonRaw): ChatEventForumTopicEdited | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventForumTopicEdited = new ChatEventForumTopicEdited();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_topic_info = decodeForumTopicInfo(record['old_topic_info']);
  out.new_topic_info = decodeForumTopicInfo(record['new_topic_info']);
  return out;
}

/** Encode a `chatEventForumTopicEdited` into TDLib JSON wire shape. */
export function encodeChatEventForumTopicEdited(value: ChatEventForumTopicEdited): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventForumTopicEdited';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_topic_info'] = value.old_topic_info === null ? null : encodeForumTopicInfo(value.old_topic_info);
  out['new_topic_info'] = value.new_topic_info === null ? null : encodeForumTopicInfo(value.new_topic_info);
  return out;
}

/** chatEventForumTopicPinned = ChatEventAction — A pinned forum topic was changed */
export class ChatEventForumTopicPinned {
  type: 'chatEventForumTopicPinned' = 'chatEventForumTopicPinned';
  extra?: string;
  /** old_topic_info: forumTopicInfo — Information about the old pinned topic; may be null */
  old_topic_info: ForumTopicInfo | null = null;
  /** new_topic_info: forumTopicInfo — Information about the new pinned topic; may be null */
  new_topic_info: ForumTopicInfo | null = null;
}

/** Decode a TDLib `chatEventForumTopicPinned` object; null input -> null. */
export function decodeChatEventForumTopicPinned(json: TdJsonRaw): ChatEventForumTopicPinned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventForumTopicPinned = new ChatEventForumTopicPinned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_topic_info = decodeForumTopicInfo(record['old_topic_info']);
  out.new_topic_info = decodeForumTopicInfo(record['new_topic_info']);
  return out;
}

/** Encode a `chatEventForumTopicPinned` into TDLib JSON wire shape. */
export function encodeChatEventForumTopicPinned(value: ChatEventForumTopicPinned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventForumTopicPinned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_topic_info'] = value.old_topic_info === null ? null : encodeForumTopicInfo(value.old_topic_info);
  out['new_topic_info'] = value.new_topic_info === null ? null : encodeForumTopicInfo(value.new_topic_info);
  return out;
}

/** chatEventForumTopicToggleIsClosed = ChatEventAction — A forum topic was closed or reopened */
export class ChatEventForumTopicToggleIsClosed {
  type: 'chatEventForumTopicToggleIsClosed' = 'chatEventForumTopicToggleIsClosed';
  extra?: string;
  /** topic_info: forumTopicInfo — New information about the topic */
  topic_info: ForumTopicInfo | null = null;
}

/** Decode a TDLib `chatEventForumTopicToggleIsClosed` object; null input -> null. */
export function decodeChatEventForumTopicToggleIsClosed(json: TdJsonRaw): ChatEventForumTopicToggleIsClosed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventForumTopicToggleIsClosed = new ChatEventForumTopicToggleIsClosed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.topic_info = decodeForumTopicInfo(record['topic_info']);
  return out;
}

/** Encode a `chatEventForumTopicToggleIsClosed` into TDLib JSON wire shape. */
export function encodeChatEventForumTopicToggleIsClosed(value: ChatEventForumTopicToggleIsClosed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventForumTopicToggleIsClosed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['topic_info'] = value.topic_info === null ? null : encodeForumTopicInfo(value.topic_info);
  return out;
}

/** chatEventForumTopicToggleIsHidden = ChatEventAction — The General forum topic was hidden or unhidden */
export class ChatEventForumTopicToggleIsHidden {
  type: 'chatEventForumTopicToggleIsHidden' = 'chatEventForumTopicToggleIsHidden';
  extra?: string;
  /** topic_info: forumTopicInfo — New information about the topic */
  topic_info: ForumTopicInfo | null = null;
}

/** Decode a TDLib `chatEventForumTopicToggleIsHidden` object; null input -> null. */
export function decodeChatEventForumTopicToggleIsHidden(json: TdJsonRaw): ChatEventForumTopicToggleIsHidden | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventForumTopicToggleIsHidden = new ChatEventForumTopicToggleIsHidden();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.topic_info = decodeForumTopicInfo(record['topic_info']);
  return out;
}

/** Encode a `chatEventForumTopicToggleIsHidden` into TDLib JSON wire shape. */
export function encodeChatEventForumTopicToggleIsHidden(value: ChatEventForumTopicToggleIsHidden): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventForumTopicToggleIsHidden';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['topic_info'] = value.topic_info === null ? null : encodeForumTopicInfo(value.topic_info);
  return out;
}

/** chatEventHasAggressiveAntiSpamEnabledToggled = ChatEventAction — The has_aggressive_anti_spam_enabled setting of a supergroup was toggled */
export class ChatEventHasAggressiveAntiSpamEnabledToggled {
  type: 'chatEventHasAggressiveAntiSpamEnabledToggled' = 'chatEventHasAggressiveAntiSpamEnabledToggled';
  extra?: string;
  /** has_aggressive_anti_spam_enabled: Bool — New value of has_aggressive_anti_spam_enabled */
  has_aggressive_anti_spam_enabled: boolean = false;
}

/** Decode a TDLib `chatEventHasAggressiveAntiSpamEnabledToggled` object; null input -> null. */
export function decodeChatEventHasAggressiveAntiSpamEnabledToggled(json: TdJsonRaw): ChatEventHasAggressiveAntiSpamEnabledToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventHasAggressiveAntiSpamEnabledToggled = new ChatEventHasAggressiveAntiSpamEnabledToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.has_aggressive_anti_spam_enabled = asBool(record['has_aggressive_anti_spam_enabled']);
  return out;
}

/** Encode a `chatEventHasAggressiveAntiSpamEnabledToggled` into TDLib JSON wire shape. */
export function encodeChatEventHasAggressiveAntiSpamEnabledToggled(value: ChatEventHasAggressiveAntiSpamEnabledToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventHasAggressiveAntiSpamEnabledToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['has_aggressive_anti_spam_enabled'] = value.has_aggressive_anti_spam_enabled;
  return out;
}

/** chatEventHasProtectedContentToggled = ChatEventAction — The has_protected_content setting of a channel was toggled */
export class ChatEventHasProtectedContentToggled {
  type: 'chatEventHasProtectedContentToggled' = 'chatEventHasProtectedContentToggled';
  extra?: string;
  /** has_protected_content: Bool — New value of has_protected_content */
  has_protected_content: boolean = false;
}

/** Decode a TDLib `chatEventHasProtectedContentToggled` object; null input -> null. */
export function decodeChatEventHasProtectedContentToggled(json: TdJsonRaw): ChatEventHasProtectedContentToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventHasProtectedContentToggled = new ChatEventHasProtectedContentToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.has_protected_content = asBool(record['has_protected_content']);
  return out;
}

/** Encode a `chatEventHasProtectedContentToggled` into TDLib JSON wire shape. */
export function encodeChatEventHasProtectedContentToggled(value: ChatEventHasProtectedContentToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventHasProtectedContentToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['has_protected_content'] = value.has_protected_content;
  return out;
}

/** chatEventInviteLinkDeleted = ChatEventAction — A revoked chat invite link was deleted */
export class ChatEventInviteLinkDeleted {
  type: 'chatEventInviteLinkDeleted' = 'chatEventInviteLinkDeleted';
  extra?: string;
  /** invite_link: chatInviteLink — The invite link */
  invite_link: ChatInviteLink | null = null;
}

/** Decode a TDLib `chatEventInviteLinkDeleted` object; null input -> null. */
export function decodeChatEventInviteLinkDeleted(json: TdJsonRaw): ChatEventInviteLinkDeleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventInviteLinkDeleted = new ChatEventInviteLinkDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = decodeChatInviteLink(record['invite_link']);
  return out;
}

/** Encode a `chatEventInviteLinkDeleted` into TDLib JSON wire shape. */
export function encodeChatEventInviteLinkDeleted(value: ChatEventInviteLinkDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventInviteLinkDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link === null ? null : encodeChatInviteLink(value.invite_link);
  return out;
}

/** chatEventInviteLinkEdited = ChatEventAction — A chat invite link was edited */
export class ChatEventInviteLinkEdited {
  type: 'chatEventInviteLinkEdited' = 'chatEventInviteLinkEdited';
  extra?: string;
  /** old_invite_link: chatInviteLink — Previous information about the invite link */
  old_invite_link: ChatInviteLink | null = null;
  /** new_invite_link: chatInviteLink — New information about the invite link */
  new_invite_link: ChatInviteLink | null = null;
}

/** Decode a TDLib `chatEventInviteLinkEdited` object; null input -> null. */
export function decodeChatEventInviteLinkEdited(json: TdJsonRaw): ChatEventInviteLinkEdited | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventInviteLinkEdited = new ChatEventInviteLinkEdited();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_invite_link = decodeChatInviteLink(record['old_invite_link']);
  out.new_invite_link = decodeChatInviteLink(record['new_invite_link']);
  return out;
}

/** Encode a `chatEventInviteLinkEdited` into TDLib JSON wire shape. */
export function encodeChatEventInviteLinkEdited(value: ChatEventInviteLinkEdited): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventInviteLinkEdited';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_invite_link'] = value.old_invite_link === null ? null : encodeChatInviteLink(value.old_invite_link);
  out['new_invite_link'] = value.new_invite_link === null ? null : encodeChatInviteLink(value.new_invite_link);
  return out;
}

/** chatEventInviteLinkRevoked = ChatEventAction — A chat invite link was revoked */
export class ChatEventInviteLinkRevoked {
  type: 'chatEventInviteLinkRevoked' = 'chatEventInviteLinkRevoked';
  extra?: string;
  /** invite_link: chatInviteLink — The invite link */
  invite_link: ChatInviteLink | null = null;
}

/** Decode a TDLib `chatEventInviteLinkRevoked` object; null input -> null. */
export function decodeChatEventInviteLinkRevoked(json: TdJsonRaw): ChatEventInviteLinkRevoked | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventInviteLinkRevoked = new ChatEventInviteLinkRevoked();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = decodeChatInviteLink(record['invite_link']);
  return out;
}

/** Encode a `chatEventInviteLinkRevoked` into TDLib JSON wire shape. */
export function encodeChatEventInviteLinkRevoked(value: ChatEventInviteLinkRevoked): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventInviteLinkRevoked';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link === null ? null : encodeChatInviteLink(value.invite_link);
  return out;
}

/** chatEventInvitesToggled = ChatEventAction — The can_invite_users permission of a supergroup chat was toggled */
export class ChatEventInvitesToggled {
  type: 'chatEventInvitesToggled' = 'chatEventInvitesToggled';
  extra?: string;
  /** can_invite_users: Bool — New value of can_invite_users permission */
  can_invite_users: boolean = false;
}

/** Decode a TDLib `chatEventInvitesToggled` object; null input -> null. */
export function decodeChatEventInvitesToggled(json: TdJsonRaw): ChatEventInvitesToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventInvitesToggled = new ChatEventInvitesToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_invite_users = asBool(record['can_invite_users']);
  return out;
}

/** Encode a `chatEventInvitesToggled` into TDLib JSON wire shape. */
export function encodeChatEventInvitesToggled(value: ChatEventInvitesToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventInvitesToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_invite_users'] = value.can_invite_users;
  return out;
}

/** chatEventIsAllHistoryAvailableToggled = ChatEventAction — The is_all_history_available setting of a supergroup was toggled */
export class ChatEventIsAllHistoryAvailableToggled {
  type: 'chatEventIsAllHistoryAvailableToggled' = 'chatEventIsAllHistoryAvailableToggled';
  extra?: string;
  /** is_all_history_available: Bool — New value of is_all_history_available */
  is_all_history_available: boolean = false;
}

/** Decode a TDLib `chatEventIsAllHistoryAvailableToggled` object; null input -> null. */
export function decodeChatEventIsAllHistoryAvailableToggled(json: TdJsonRaw): ChatEventIsAllHistoryAvailableToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventIsAllHistoryAvailableToggled = new ChatEventIsAllHistoryAvailableToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_all_history_available = asBool(record['is_all_history_available']);
  return out;
}

/** Encode a `chatEventIsAllHistoryAvailableToggled` into TDLib JSON wire shape. */
export function encodeChatEventIsAllHistoryAvailableToggled(value: ChatEventIsAllHistoryAvailableToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventIsAllHistoryAvailableToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_all_history_available'] = value.is_all_history_available;
  return out;
}

/** chatEventIsForumToggled = ChatEventAction — The is_forum setting of a channel was toggled */
export class ChatEventIsForumToggled {
  type: 'chatEventIsForumToggled' = 'chatEventIsForumToggled';
  extra?: string;
  /** is_forum: Bool — New value of is_forum */
  is_forum: boolean = false;
}

/** Decode a TDLib `chatEventIsForumToggled` object; null input -> null. */
export function decodeChatEventIsForumToggled(json: TdJsonRaw): ChatEventIsForumToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventIsForumToggled = new ChatEventIsForumToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_forum = asBool(record['is_forum']);
  return out;
}

/** Encode a `chatEventIsForumToggled` into TDLib JSON wire shape. */
export function encodeChatEventIsForumToggled(value: ChatEventIsForumToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventIsForumToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_forum'] = value.is_forum;
  return out;
}

/** chatEventLinkedChatChanged = ChatEventAction — The linked chat of a supergroup was changed */
export class ChatEventLinkedChatChanged {
  type: 'chatEventLinkedChatChanged' = 'chatEventLinkedChatChanged';
  extra?: string;
  /** old_linked_chat_id: int53 — Previous supergroup linked chat identifier */
  old_linked_chat_id: number = 0;
  /** new_linked_chat_id: int53 — New supergroup linked chat identifier */
  new_linked_chat_id: number = 0;
}

/** Decode a TDLib `chatEventLinkedChatChanged` object; null input -> null. */
export function decodeChatEventLinkedChatChanged(json: TdJsonRaw): ChatEventLinkedChatChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventLinkedChatChanged = new ChatEventLinkedChatChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_linked_chat_id = asNumber(record['old_linked_chat_id']);
  out.new_linked_chat_id = asNumber(record['new_linked_chat_id']);
  return out;
}

/** Encode a `chatEventLinkedChatChanged` into TDLib JSON wire shape. */
export function encodeChatEventLinkedChatChanged(value: ChatEventLinkedChatChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventLinkedChatChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_linked_chat_id'] = value.old_linked_chat_id;
  out['new_linked_chat_id'] = value.new_linked_chat_id;
  return out;
}

/** chatEventLocationChanged = ChatEventAction — The supergroup location was changed */
export class ChatEventLocationChanged {
  type: 'chatEventLocationChanged' = 'chatEventLocationChanged';
  extra?: string;
  /** old_location: chatLocation — Previous location; may be null */
  old_location: ChatLocation | null = null;
  /** new_location: chatLocation — New location; may be null */
  new_location: ChatLocation | null = null;
}

/** Decode a TDLib `chatEventLocationChanged` object; null input -> null. */
export function decodeChatEventLocationChanged(json: TdJsonRaw): ChatEventLocationChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventLocationChanged = new ChatEventLocationChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_location = decodeChatLocation(record['old_location']);
  out.new_location = decodeChatLocation(record['new_location']);
  return out;
}

/** Encode a `chatEventLocationChanged` into TDLib JSON wire shape. */
export function encodeChatEventLocationChanged(value: ChatEventLocationChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventLocationChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_location'] = value.old_location === null ? null : encodeChatLocation(value.old_location);
  out['new_location'] = value.new_location === null ? null : encodeChatLocation(value.new_location);
  return out;
}

/** chatEventLogFilters = ChatEventLogFilters — Represents a set of filters used to obtain a chat event log */
export class ChatEventLogFilters {
  type: 'chatEventLogFilters' = 'chatEventLogFilters';
  extra?: string;
  /** message_edits: Bool — True, if message edits need to be returned */
  message_edits: boolean = false;
  /** message_deletions: Bool — True, if message deletions need to be returned */
  message_deletions: boolean = false;
  /** message_pins: Bool — True, if pin/unpin events need to be returned */
  message_pins: boolean = false;
  /** member_joins: Bool — True, if members joining events need to be returned */
  member_joins: boolean = false;
  /** member_leaves: Bool — True, if members leaving events need to be returned */
  member_leaves: boolean = false;
  /** member_invites: Bool — True, if invited member events need to be returned */
  member_invites: boolean = false;
  /** member_promotions: Bool — True, if member promotion/demotion events need to be returned */
  member_promotions: boolean = false;
  /** member_restrictions: Bool — True, if member restricted/unrestricted/banned/unbanned events need to be returned */
  member_restrictions: boolean = false;
  /** member_tag_changes: Bool — True, if member tag and custom title change events need to be returned */
  member_tag_changes: boolean = false;
  /** info_changes: Bool — True, if changes in chat information need to be returned */
  info_changes: boolean = false;
  /** setting_changes: Bool — True, if changes in chat settings need to be returned */
  setting_changes: boolean = false;
  /** invite_link_changes: Bool — True, if changes to invite links need to be returned */
  invite_link_changes: boolean = false;
  /** video_chat_changes: Bool — True, if video chat actions need to be returned */
  video_chat_changes: boolean = false;
  /** forum_changes: Bool — True, if forum-related actions need to be returned */
  forum_changes: boolean = false;
  /** subscription_extensions: Bool — True, if subscription extensions need to be returned */
  subscription_extensions: boolean = false;
}

/** Decode a TDLib `chatEventLogFilters` object; null input -> null. */
export function decodeChatEventLogFilters(json: TdJsonRaw): ChatEventLogFilters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventLogFilters = new ChatEventLogFilters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_edits = asBool(record['message_edits']);
  out.message_deletions = asBool(record['message_deletions']);
  out.message_pins = asBool(record['message_pins']);
  out.member_joins = asBool(record['member_joins']);
  out.member_leaves = asBool(record['member_leaves']);
  out.member_invites = asBool(record['member_invites']);
  out.member_promotions = asBool(record['member_promotions']);
  out.member_restrictions = asBool(record['member_restrictions']);
  out.member_tag_changes = asBool(record['member_tag_changes']);
  out.info_changes = asBool(record['info_changes']);
  out.setting_changes = asBool(record['setting_changes']);
  out.invite_link_changes = asBool(record['invite_link_changes']);
  out.video_chat_changes = asBool(record['video_chat_changes']);
  out.forum_changes = asBool(record['forum_changes']);
  out.subscription_extensions = asBool(record['subscription_extensions']);
  return out;
}

/** Encode a `chatEventLogFilters` into TDLib JSON wire shape. */
export function encodeChatEventLogFilters(value: ChatEventLogFilters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventLogFilters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_edits'] = value.message_edits;
  out['message_deletions'] = value.message_deletions;
  out['message_pins'] = value.message_pins;
  out['member_joins'] = value.member_joins;
  out['member_leaves'] = value.member_leaves;
  out['member_invites'] = value.member_invites;
  out['member_promotions'] = value.member_promotions;
  out['member_restrictions'] = value.member_restrictions;
  out['member_tag_changes'] = value.member_tag_changes;
  out['info_changes'] = value.info_changes;
  out['setting_changes'] = value.setting_changes;
  out['invite_link_changes'] = value.invite_link_changes;
  out['video_chat_changes'] = value.video_chat_changes;
  out['forum_changes'] = value.forum_changes;
  out['subscription_extensions'] = value.subscription_extensions;
  return out;
}

/** chatEventMemberInvited = ChatEventAction — A new chat member was invited */
export class ChatEventMemberInvited {
  type: 'chatEventMemberInvited' = 'chatEventMemberInvited';
  extra?: string;
  /** user_id: int53 — New member user identifier */
  user_id: number = 0;
  /** status: ChatMemberStatus — New member status */
  status: TdChatMemberStatus | null = null;
}

/** Decode a TDLib `chatEventMemberInvited` object; null input -> null. */
export function decodeChatEventMemberInvited(json: TdJsonRaw): ChatEventMemberInvited | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMemberInvited = new ChatEventMemberInvited();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.status = decodeTdChatMemberStatus(record['status']);
  return out;
}

/** Encode a `chatEventMemberInvited` into TDLib JSON wire shape. */
export function encodeChatEventMemberInvited(value: ChatEventMemberInvited): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMemberInvited';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['status'] = value.status === null ? null : encodeTdChatMemberStatus(value.status);
  return out;
}

/** chatEventMemberJoined = ChatEventAction — A new member joined the chat */
export class ChatEventMemberJoined {
  type: 'chatEventMemberJoined' = 'chatEventMemberJoined';
  extra?: string;
}

/** Decode a TDLib `chatEventMemberJoined` object; null input -> null. */
export function decodeChatEventMemberJoined(json: TdJsonRaw): ChatEventMemberJoined | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMemberJoined = new ChatEventMemberJoined();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatEventMemberJoined` into TDLib JSON wire shape. */
export function encodeChatEventMemberJoined(value: ChatEventMemberJoined): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMemberJoined';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatEventMemberJoinedByInviteLink = ChatEventAction — A new member joined the chat via an invite link */
export class ChatEventMemberJoinedByInviteLink {
  type: 'chatEventMemberJoinedByInviteLink' = 'chatEventMemberJoinedByInviteLink';
  extra?: string;
  /** invite_link: chatInviteLink — Invite link used to join the chat */
  invite_link: ChatInviteLink | null = null;
  /** via_chat_folder_invite_link: Bool — True, if the user has joined the chat using an invite link for a chat folder */
  via_chat_folder_invite_link: boolean = false;
}

/** Decode a TDLib `chatEventMemberJoinedByInviteLink` object; null input -> null. */
export function decodeChatEventMemberJoinedByInviteLink(json: TdJsonRaw): ChatEventMemberJoinedByInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMemberJoinedByInviteLink = new ChatEventMemberJoinedByInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = decodeChatInviteLink(record['invite_link']);
  out.via_chat_folder_invite_link = asBool(record['via_chat_folder_invite_link']);
  return out;
}

/** Encode a `chatEventMemberJoinedByInviteLink` into TDLib JSON wire shape. */
export function encodeChatEventMemberJoinedByInviteLink(value: ChatEventMemberJoinedByInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMemberJoinedByInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link === null ? null : encodeChatInviteLink(value.invite_link);
  out['via_chat_folder_invite_link'] = value.via_chat_folder_invite_link;
  return out;
}

/** chatEventMemberJoinedByRequest = ChatEventAction — A new member was accepted to the chat by an administrator */
export class ChatEventMemberJoinedByRequest {
  type: 'chatEventMemberJoinedByRequest' = 'chatEventMemberJoinedByRequest';
  extra?: string;
  /** approver_user_id: int53 — User identifier of the chat administrator, approved user join request */
  approver_user_id: number = 0;
  /** invite_link: chatInviteLink — Invite link used to join the chat; may be null */
  invite_link: ChatInviteLink | null = null;
}

/** Decode a TDLib `chatEventMemberJoinedByRequest` object; null input -> null. */
export function decodeChatEventMemberJoinedByRequest(json: TdJsonRaw): ChatEventMemberJoinedByRequest | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMemberJoinedByRequest = new ChatEventMemberJoinedByRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.approver_user_id = asNumber(record['approver_user_id']);
  out.invite_link = decodeChatInviteLink(record['invite_link']);
  return out;
}

/** Encode a `chatEventMemberJoinedByRequest` into TDLib JSON wire shape. */
export function encodeChatEventMemberJoinedByRequest(value: ChatEventMemberJoinedByRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMemberJoinedByRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['approver_user_id'] = value.approver_user_id;
  out['invite_link'] = value.invite_link === null ? null : encodeChatInviteLink(value.invite_link);
  return out;
}

/** chatEventMemberLeft = ChatEventAction — A member left the chat */
export class ChatEventMemberLeft {
  type: 'chatEventMemberLeft' = 'chatEventMemberLeft';
  extra?: string;
}

/** Decode a TDLib `chatEventMemberLeft` object; null input -> null. */
export function decodeChatEventMemberLeft(json: TdJsonRaw): ChatEventMemberLeft | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMemberLeft = new ChatEventMemberLeft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatEventMemberLeft` into TDLib JSON wire shape. */
export function encodeChatEventMemberLeft(value: ChatEventMemberLeft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMemberLeft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatEventMemberPromoted = ChatEventAction — A chat member has gained/lost administrator status, or the list of their administrator privileges has changed */
export class ChatEventMemberPromoted {
  type: 'chatEventMemberPromoted' = 'chatEventMemberPromoted';
  extra?: string;
  /** user_id: int53 — Affected chat member user identifier */
  user_id: number = 0;
  /** old_status: ChatMemberStatus — Previous status of the chat member */
  old_status: TdChatMemberStatus | null = null;
  /** new_status: ChatMemberStatus — New status of the chat member */
  new_status: TdChatMemberStatus | null = null;
}

/** Decode a TDLib `chatEventMemberPromoted` object; null input -> null. */
export function decodeChatEventMemberPromoted(json: TdJsonRaw): ChatEventMemberPromoted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMemberPromoted = new ChatEventMemberPromoted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.old_status = decodeTdChatMemberStatus(record['old_status']);
  out.new_status = decodeTdChatMemberStatus(record['new_status']);
  return out;
}

/** Encode a `chatEventMemberPromoted` into TDLib JSON wire shape. */
export function encodeChatEventMemberPromoted(value: ChatEventMemberPromoted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMemberPromoted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['old_status'] = value.old_status === null ? null : encodeTdChatMemberStatus(value.old_status);
  out['new_status'] = value.new_status === null ? null : encodeTdChatMemberStatus(value.new_status);
  return out;
}

/** chatEventMemberRestricted = ChatEventAction — A chat member was restricted/unrestricted or banned/unbanned, or the list of their restrictions has changed */
export class ChatEventMemberRestricted {
  type: 'chatEventMemberRestricted' = 'chatEventMemberRestricted';
  extra?: string;
  /** member_id: MessageSender — Affected chat member identifier */
  member_id: TdMessageSender | null = null;
  /** old_status: ChatMemberStatus — Previous status of the chat member */
  old_status: TdChatMemberStatus | null = null;
  /** new_status: ChatMemberStatus — New status of the chat member */
  new_status: TdChatMemberStatus | null = null;
}

/** Decode a TDLib `chatEventMemberRestricted` object; null input -> null. */
export function decodeChatEventMemberRestricted(json: TdJsonRaw): ChatEventMemberRestricted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMemberRestricted = new ChatEventMemberRestricted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.member_id = decodeTdMessageSender(record['member_id']);
  out.old_status = decodeTdChatMemberStatus(record['old_status']);
  out.new_status = decodeTdChatMemberStatus(record['new_status']);
  return out;
}

/** Encode a `chatEventMemberRestricted` into TDLib JSON wire shape. */
export function encodeChatEventMemberRestricted(value: ChatEventMemberRestricted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMemberRestricted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['member_id'] = value.member_id === null ? null : encodeTdMessageSender(value.member_id);
  out['old_status'] = value.old_status === null ? null : encodeTdChatMemberStatus(value.old_status);
  out['new_status'] = value.new_status === null ? null : encodeTdChatMemberStatus(value.new_status);
  return out;
}

/** chatEventMemberSubscriptionExtended = ChatEventAction — A chat member extended their subscription to the chat */
export class ChatEventMemberSubscriptionExtended {
  type: 'chatEventMemberSubscriptionExtended' = 'chatEventMemberSubscriptionExtended';
  extra?: string;
  /** user_id: int53 — Affected chat member user identifier */
  user_id: number = 0;
  /** old_status: ChatMemberStatus — Previous status of the chat member */
  old_status: TdChatMemberStatus | null = null;
  /** new_status: ChatMemberStatus — New status of the chat member */
  new_status: TdChatMemberStatus | null = null;
}

/** Decode a TDLib `chatEventMemberSubscriptionExtended` object; null input -> null. */
export function decodeChatEventMemberSubscriptionExtended(json: TdJsonRaw): ChatEventMemberSubscriptionExtended | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMemberSubscriptionExtended = new ChatEventMemberSubscriptionExtended();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.old_status = decodeTdChatMemberStatus(record['old_status']);
  out.new_status = decodeTdChatMemberStatus(record['new_status']);
  return out;
}

/** Encode a `chatEventMemberSubscriptionExtended` into TDLib JSON wire shape. */
export function encodeChatEventMemberSubscriptionExtended(value: ChatEventMemberSubscriptionExtended): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMemberSubscriptionExtended';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['old_status'] = value.old_status === null ? null : encodeTdChatMemberStatus(value.old_status);
  out['new_status'] = value.new_status === null ? null : encodeTdChatMemberStatus(value.new_status);
  return out;
}

/** chatEventMemberTagChanged = ChatEventAction — A chat member tag has been changed */
export class ChatEventMemberTagChanged {
  type: 'chatEventMemberTagChanged' = 'chatEventMemberTagChanged';
  extra?: string;
  /** user_id: int53 — Affected chat member user identifier */
  user_id: number = 0;
  /** old_tag: string — Previous tag of the chat member */
  old_tag: string = '';
  /** new_tag: string — New tag of the chat member */
  new_tag: string = '';
}

/** Decode a TDLib `chatEventMemberTagChanged` object; null input -> null. */
export function decodeChatEventMemberTagChanged(json: TdJsonRaw): ChatEventMemberTagChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMemberTagChanged = new ChatEventMemberTagChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.old_tag = asString(record['old_tag']);
  out.new_tag = asString(record['new_tag']);
  return out;
}

/** Encode a `chatEventMemberTagChanged` into TDLib JSON wire shape. */
export function encodeChatEventMemberTagChanged(value: ChatEventMemberTagChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMemberTagChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['old_tag'] = value.old_tag;
  out['new_tag'] = value.new_tag;
  return out;
}

/** chatEventMessageAutoDeleteTimeChanged = ChatEventAction — The message auto-delete timer was changed */
export class ChatEventMessageAutoDeleteTimeChanged {
  type: 'chatEventMessageAutoDeleteTimeChanged' = 'chatEventMessageAutoDeleteTimeChanged';
  extra?: string;
  /** old_message_auto_delete_time: int32 — Previous value of message_auto_delete_time */
  old_message_auto_delete_time: number = 0;
  /** new_message_auto_delete_time: int32 — New value of message_auto_delete_time */
  new_message_auto_delete_time: number = 0;
}

/** Decode a TDLib `chatEventMessageAutoDeleteTimeChanged` object; null input -> null. */
export function decodeChatEventMessageAutoDeleteTimeChanged(json: TdJsonRaw): ChatEventMessageAutoDeleteTimeChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMessageAutoDeleteTimeChanged = new ChatEventMessageAutoDeleteTimeChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_message_auto_delete_time = asNumber(record['old_message_auto_delete_time']);
  out.new_message_auto_delete_time = asNumber(record['new_message_auto_delete_time']);
  return out;
}

/** Encode a `chatEventMessageAutoDeleteTimeChanged` into TDLib JSON wire shape. */
export function encodeChatEventMessageAutoDeleteTimeChanged(value: ChatEventMessageAutoDeleteTimeChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMessageAutoDeleteTimeChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_message_auto_delete_time'] = value.old_message_auto_delete_time;
  out['new_message_auto_delete_time'] = value.new_message_auto_delete_time;
  return out;
}

/** chatEventMessageDeleted = ChatEventAction — A message was deleted */
export class ChatEventMessageDeleted {
  type: 'chatEventMessageDeleted' = 'chatEventMessageDeleted';
  extra?: string;
  /** message: message — Deleted message */
  message: Message | null = null;
  /** can_report_anti_spam_false_positive: Bool — True, if the message deletion can be reported via reportSupergroupAntiSpamFalsePositive */
  can_report_anti_spam_false_positive: boolean = false;
}

/** Decode a TDLib `chatEventMessageDeleted` object; null input -> null. */
export function decodeChatEventMessageDeleted(json: TdJsonRaw): ChatEventMessageDeleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMessageDeleted = new ChatEventMessageDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  out.can_report_anti_spam_false_positive = asBool(record['can_report_anti_spam_false_positive']);
  return out;
}

/** Encode a `chatEventMessageDeleted` into TDLib JSON wire shape. */
export function encodeChatEventMessageDeleted(value: ChatEventMessageDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMessageDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  out['can_report_anti_spam_false_positive'] = value.can_report_anti_spam_false_positive;
  return out;
}

/** chatEventMessageEdited = ChatEventAction — A message was edited */
export class ChatEventMessageEdited {
  type: 'chatEventMessageEdited' = 'chatEventMessageEdited';
  extra?: string;
  /** old_message: message — The original message before the edit */
  old_message: Message | null = null;
  /** new_message: message — The message after it was edited */
  new_message: Message | null = null;
}

/** Decode a TDLib `chatEventMessageEdited` object; null input -> null. */
export function decodeChatEventMessageEdited(json: TdJsonRaw): ChatEventMessageEdited | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMessageEdited = new ChatEventMessageEdited();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_message = decodeMessage(record['old_message']);
  out.new_message = decodeMessage(record['new_message']);
  return out;
}

/** Encode a `chatEventMessageEdited` into TDLib JSON wire shape. */
export function encodeChatEventMessageEdited(value: ChatEventMessageEdited): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMessageEdited';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_message'] = value.old_message === null ? null : encodeMessage(value.old_message);
  out['new_message'] = value.new_message === null ? null : encodeMessage(value.new_message);
  return out;
}

/** chatEventMessagePinned = ChatEventAction — A message was pinned */
export class ChatEventMessagePinned {
  type: 'chatEventMessagePinned' = 'chatEventMessagePinned';
  extra?: string;
  /** message: message — Pinned message */
  message: Message | null = null;
}

/** Decode a TDLib `chatEventMessagePinned` object; null input -> null. */
export function decodeChatEventMessagePinned(json: TdJsonRaw): ChatEventMessagePinned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMessagePinned = new ChatEventMessagePinned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  return out;
}

/** Encode a `chatEventMessagePinned` into TDLib JSON wire shape. */
export function encodeChatEventMessagePinned(value: ChatEventMessagePinned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMessagePinned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  return out;
}

/** chatEventMessageUnpinned = ChatEventAction — A message was unpinned */
export class ChatEventMessageUnpinned {
  type: 'chatEventMessageUnpinned' = 'chatEventMessageUnpinned';
  extra?: string;
  /** message: message — Unpinned message */
  message: Message | null = null;
}

/** Decode a TDLib `chatEventMessageUnpinned` object; null input -> null. */
export function decodeChatEventMessageUnpinned(json: TdJsonRaw): ChatEventMessageUnpinned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventMessageUnpinned = new ChatEventMessageUnpinned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  return out;
}

/** Encode a `chatEventMessageUnpinned` into TDLib JSON wire shape. */
export function encodeChatEventMessageUnpinned(value: ChatEventMessageUnpinned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventMessageUnpinned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  return out;
}

/** chatEventPermissionsChanged = ChatEventAction — The chat permissions were changed */
export class ChatEventPermissionsChanged {
  type: 'chatEventPermissionsChanged' = 'chatEventPermissionsChanged';
  extra?: string;
  /** old_permissions: chatPermissions — Previous chat permissions */
  old_permissions: ChatPermissions | null = null;
  /** new_permissions: chatPermissions — New chat permissions */
  new_permissions: ChatPermissions | null = null;
}

/** Decode a TDLib `chatEventPermissionsChanged` object; null input -> null. */
export function decodeChatEventPermissionsChanged(json: TdJsonRaw): ChatEventPermissionsChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventPermissionsChanged = new ChatEventPermissionsChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_permissions = decodeChatPermissions(record['old_permissions']);
  out.new_permissions = decodeChatPermissions(record['new_permissions']);
  return out;
}

/** Encode a `chatEventPermissionsChanged` into TDLib JSON wire shape. */
export function encodeChatEventPermissionsChanged(value: ChatEventPermissionsChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventPermissionsChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_permissions'] = value.old_permissions === null ? null : encodeChatPermissions(value.old_permissions);
  out['new_permissions'] = value.new_permissions === null ? null : encodeChatPermissions(value.new_permissions);
  return out;
}

/** chatEventPhotoChanged = ChatEventAction — The chat photo was changed */
export class ChatEventPhotoChanged {
  type: 'chatEventPhotoChanged' = 'chatEventPhotoChanged';
  extra?: string;
  /** old_photo: chatPhoto — Previous chat photo value; may be null */
  old_photo: ChatPhoto | null = null;
  /** new_photo: chatPhoto — New chat photo value; may be null */
  new_photo: ChatPhoto | null = null;
}

/** Decode a TDLib `chatEventPhotoChanged` object; null input -> null. */
export function decodeChatEventPhotoChanged(json: TdJsonRaw): ChatEventPhotoChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventPhotoChanged = new ChatEventPhotoChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_photo = decodeChatPhoto(record['old_photo']);
  out.new_photo = decodeChatPhoto(record['new_photo']);
  return out;
}

/** Encode a `chatEventPhotoChanged` into TDLib JSON wire shape. */
export function encodeChatEventPhotoChanged(value: ChatEventPhotoChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventPhotoChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_photo'] = value.old_photo === null ? null : encodeChatPhoto(value.old_photo);
  out['new_photo'] = value.new_photo === null ? null : encodeChatPhoto(value.new_photo);
  return out;
}

/** chatEventPollStopped = ChatEventAction — A poll in a message was stopped */
export class ChatEventPollStopped {
  type: 'chatEventPollStopped' = 'chatEventPollStopped';
  extra?: string;
  /** message: message — The message with the poll */
  message: Message | null = null;
}

/** Decode a TDLib `chatEventPollStopped` object; null input -> null. */
export function decodeChatEventPollStopped(json: TdJsonRaw): ChatEventPollStopped | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventPollStopped = new ChatEventPollStopped();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  return out;
}

/** Encode a `chatEventPollStopped` into TDLib JSON wire shape. */
export function encodeChatEventPollStopped(value: ChatEventPollStopped): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventPollStopped';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  return out;
}

/** chatEventProfileAccentColorChanged = ChatEventAction — The chat's profile accent color or profile background custom emoji were changed */
export class ChatEventProfileAccentColorChanged {
  type: 'chatEventProfileAccentColorChanged' = 'chatEventProfileAccentColorChanged';
  extra?: string;
  /** old_profile_accent_color_id: int32 — Previous identifier of chat's profile accent color; -1 if none */
  old_profile_accent_color_id: number = 0;
  /** old_profile_background_custom_emoji_id: int64 — Previous identifier of the custom emoji; 0 if none */
  old_profile_background_custom_emoji_id: string = '';
  /** new_profile_accent_color_id: int32 — New identifier of chat's profile accent color; -1 if none */
  new_profile_accent_color_id: number = 0;
  /** new_profile_background_custom_emoji_id: int64 — New identifier of the custom emoji; 0 if none */
  new_profile_background_custom_emoji_id: string = '';
}

/** Decode a TDLib `chatEventProfileAccentColorChanged` object; null input -> null. */
export function decodeChatEventProfileAccentColorChanged(json: TdJsonRaw): ChatEventProfileAccentColorChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventProfileAccentColorChanged = new ChatEventProfileAccentColorChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_profile_accent_color_id = asNumber(record['old_profile_accent_color_id']);
  out.old_profile_background_custom_emoji_id = asInt64(record['old_profile_background_custom_emoji_id']);
  out.new_profile_accent_color_id = asNumber(record['new_profile_accent_color_id']);
  out.new_profile_background_custom_emoji_id = asInt64(record['new_profile_background_custom_emoji_id']);
  return out;
}

/** Encode a `chatEventProfileAccentColorChanged` into TDLib JSON wire shape. */
export function encodeChatEventProfileAccentColorChanged(value: ChatEventProfileAccentColorChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventProfileAccentColorChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_profile_accent_color_id'] = value.old_profile_accent_color_id;
  out['old_profile_background_custom_emoji_id'] = value.old_profile_background_custom_emoji_id;
  out['new_profile_accent_color_id'] = value.new_profile_accent_color_id;
  out['new_profile_background_custom_emoji_id'] = value.new_profile_background_custom_emoji_id;
  return out;
}

/** chatEventShowMessageSenderToggled = ChatEventAction — The show_message_sender setting of a channel was toggled */
export class ChatEventShowMessageSenderToggled {
  type: 'chatEventShowMessageSenderToggled' = 'chatEventShowMessageSenderToggled';
  extra?: string;
  /** show_message_sender: Bool — New value of show_message_sender */
  show_message_sender: boolean = false;
}

/** Decode a TDLib `chatEventShowMessageSenderToggled` object; null input -> null. */
export function decodeChatEventShowMessageSenderToggled(json: TdJsonRaw): ChatEventShowMessageSenderToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventShowMessageSenderToggled = new ChatEventShowMessageSenderToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.show_message_sender = asBool(record['show_message_sender']);
  return out;
}

/** Encode a `chatEventShowMessageSenderToggled` into TDLib JSON wire shape. */
export function encodeChatEventShowMessageSenderToggled(value: ChatEventShowMessageSenderToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventShowMessageSenderToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['show_message_sender'] = value.show_message_sender;
  return out;
}

/** chatEventSignMessagesToggled = ChatEventAction — The sign_messages setting of a channel was toggled */
export class ChatEventSignMessagesToggled {
  type: 'chatEventSignMessagesToggled' = 'chatEventSignMessagesToggled';
  extra?: string;
  /** sign_messages: Bool — New value of sign_messages */
  sign_messages: boolean = false;
}

/** Decode a TDLib `chatEventSignMessagesToggled` object; null input -> null. */
export function decodeChatEventSignMessagesToggled(json: TdJsonRaw): ChatEventSignMessagesToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventSignMessagesToggled = new ChatEventSignMessagesToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sign_messages = asBool(record['sign_messages']);
  return out;
}

/** Encode a `chatEventSignMessagesToggled` into TDLib JSON wire shape. */
export function encodeChatEventSignMessagesToggled(value: ChatEventSignMessagesToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventSignMessagesToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sign_messages'] = value.sign_messages;
  return out;
}

/** chatEventSlowModeDelayChanged = ChatEventAction — The slow_mode_delay setting of a supergroup was changed */
export class ChatEventSlowModeDelayChanged {
  type: 'chatEventSlowModeDelayChanged' = 'chatEventSlowModeDelayChanged';
  extra?: string;
  /** old_slow_mode_delay: int32 — Previous value of slow_mode_delay, in seconds */
  old_slow_mode_delay: number = 0;
  /** new_slow_mode_delay: int32 — New value of slow_mode_delay, in seconds */
  new_slow_mode_delay: number = 0;
}

/** Decode a TDLib `chatEventSlowModeDelayChanged` object; null input -> null. */
export function decodeChatEventSlowModeDelayChanged(json: TdJsonRaw): ChatEventSlowModeDelayChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventSlowModeDelayChanged = new ChatEventSlowModeDelayChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_slow_mode_delay = asNumber(record['old_slow_mode_delay']);
  out.new_slow_mode_delay = asNumber(record['new_slow_mode_delay']);
  return out;
}

/** Encode a `chatEventSlowModeDelayChanged` into TDLib JSON wire shape. */
export function encodeChatEventSlowModeDelayChanged(value: ChatEventSlowModeDelayChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventSlowModeDelayChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_slow_mode_delay'] = value.old_slow_mode_delay;
  out['new_slow_mode_delay'] = value.new_slow_mode_delay;
  return out;
}

/** chatEventStickerSetChanged = ChatEventAction — The supergroup sticker set was changed */
export class ChatEventStickerSetChanged {
  type: 'chatEventStickerSetChanged' = 'chatEventStickerSetChanged';
  extra?: string;
  /** old_sticker_set_id: int64 — Previous identifier of the chat sticker set; 0 if none */
  old_sticker_set_id: string = '';
  /** new_sticker_set_id: int64 — New identifier of the chat sticker set; 0 if none */
  new_sticker_set_id: string = '';
}

/** Decode a TDLib `chatEventStickerSetChanged` object; null input -> null. */
export function decodeChatEventStickerSetChanged(json: TdJsonRaw): ChatEventStickerSetChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventStickerSetChanged = new ChatEventStickerSetChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_sticker_set_id = asInt64(record['old_sticker_set_id']);
  out.new_sticker_set_id = asInt64(record['new_sticker_set_id']);
  return out;
}

/** Encode a `chatEventStickerSetChanged` into TDLib JSON wire shape. */
export function encodeChatEventStickerSetChanged(value: ChatEventStickerSetChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventStickerSetChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_sticker_set_id'] = value.old_sticker_set_id;
  out['new_sticker_set_id'] = value.new_sticker_set_id;
  return out;
}

/** chatEventTitleChanged = ChatEventAction — The chat title was changed */
export class ChatEventTitleChanged {
  type: 'chatEventTitleChanged' = 'chatEventTitleChanged';
  extra?: string;
  /** old_title: string — Previous chat title */
  old_title: string = '';
  /** new_title: string — New chat title */
  new_title: string = '';
}

/** Decode a TDLib `chatEventTitleChanged` object; null input -> null. */
export function decodeChatEventTitleChanged(json: TdJsonRaw): ChatEventTitleChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventTitleChanged = new ChatEventTitleChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_title = asString(record['old_title']);
  out.new_title = asString(record['new_title']);
  return out;
}

/** Encode a `chatEventTitleChanged` into TDLib JSON wire shape. */
export function encodeChatEventTitleChanged(value: ChatEventTitleChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventTitleChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_title'] = value.old_title;
  out['new_title'] = value.new_title;
  return out;
}

/** chatEventUsernameChanged = ChatEventAction — The chat editable username was changed */
export class ChatEventUsernameChanged {
  type: 'chatEventUsernameChanged' = 'chatEventUsernameChanged';
  extra?: string;
  /** old_username: string — Previous chat username */
  old_username: string = '';
  /** new_username: string — New chat username */
  new_username: string = '';
}

/** Decode a TDLib `chatEventUsernameChanged` object; null input -> null. */
export function decodeChatEventUsernameChanged(json: TdJsonRaw): ChatEventUsernameChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventUsernameChanged = new ChatEventUsernameChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_username = asString(record['old_username']);
  out.new_username = asString(record['new_username']);
  return out;
}

/** Encode a `chatEventUsernameChanged` into TDLib JSON wire shape. */
export function encodeChatEventUsernameChanged(value: ChatEventUsernameChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventUsernameChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_username'] = value.old_username;
  out['new_username'] = value.new_username;
  return out;
}

/** chatEventVideoChatCreated = ChatEventAction — A video chat was created */
export class ChatEventVideoChatCreated {
  type: 'chatEventVideoChatCreated' = 'chatEventVideoChatCreated';
  extra?: string;
  /** group_call_id: int32 — Identifier of the video chat. The video chat can be received through the method getGroupCall */
  group_call_id: number = 0;
}

/** Decode a TDLib `chatEventVideoChatCreated` object; null input -> null. */
export function decodeChatEventVideoChatCreated(json: TdJsonRaw): ChatEventVideoChatCreated | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventVideoChatCreated = new ChatEventVideoChatCreated();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `chatEventVideoChatCreated` into TDLib JSON wire shape. */
export function encodeChatEventVideoChatCreated(value: ChatEventVideoChatCreated): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventVideoChatCreated';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** chatEventVideoChatEnded = ChatEventAction — A video chat was ended */
export class ChatEventVideoChatEnded {
  type: 'chatEventVideoChatEnded' = 'chatEventVideoChatEnded';
  extra?: string;
  /** group_call_id: int32 — Identifier of the video chat. The video chat can be received through the method getGroupCall */
  group_call_id: number = 0;
}

/** Decode a TDLib `chatEventVideoChatEnded` object; null input -> null. */
export function decodeChatEventVideoChatEnded(json: TdJsonRaw): ChatEventVideoChatEnded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventVideoChatEnded = new ChatEventVideoChatEnded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `chatEventVideoChatEnded` into TDLib JSON wire shape. */
export function encodeChatEventVideoChatEnded(value: ChatEventVideoChatEnded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventVideoChatEnded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** chatEventVideoChatMuteNewParticipantsToggled = ChatEventAction — The mute_new_participants setting of a video chat was toggled */
export class ChatEventVideoChatMuteNewParticipantsToggled {
  type: 'chatEventVideoChatMuteNewParticipantsToggled' = 'chatEventVideoChatMuteNewParticipantsToggled';
  extra?: string;
  /** mute_new_participants: Bool — New value of the mute_new_participants setting */
  mute_new_participants: boolean = false;
}

/** Decode a TDLib `chatEventVideoChatMuteNewParticipantsToggled` object; null input -> null. */
export function decodeChatEventVideoChatMuteNewParticipantsToggled(json: TdJsonRaw): ChatEventVideoChatMuteNewParticipantsToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventVideoChatMuteNewParticipantsToggled = new ChatEventVideoChatMuteNewParticipantsToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.mute_new_participants = asBool(record['mute_new_participants']);
  return out;
}

/** Encode a `chatEventVideoChatMuteNewParticipantsToggled` into TDLib JSON wire shape. */
export function encodeChatEventVideoChatMuteNewParticipantsToggled(value: ChatEventVideoChatMuteNewParticipantsToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventVideoChatMuteNewParticipantsToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['mute_new_participants'] = value.mute_new_participants;
  return out;
}

/** chatEventVideoChatParticipantIsMutedToggled = ChatEventAction — A video chat participant was muted or unmuted */
export class ChatEventVideoChatParticipantIsMutedToggled {
  type: 'chatEventVideoChatParticipantIsMutedToggled' = 'chatEventVideoChatParticipantIsMutedToggled';
  extra?: string;
  /** participant_id: MessageSender — Identifier of the affected group call participant */
  participant_id: TdMessageSender | null = null;
  /** is_muted: Bool — New value of is_muted */
  is_muted: boolean = false;
}

/** Decode a TDLib `chatEventVideoChatParticipantIsMutedToggled` object; null input -> null. */
export function decodeChatEventVideoChatParticipantIsMutedToggled(json: TdJsonRaw): ChatEventVideoChatParticipantIsMutedToggled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventVideoChatParticipantIsMutedToggled = new ChatEventVideoChatParticipantIsMutedToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.participant_id = decodeTdMessageSender(record['participant_id']);
  out.is_muted = asBool(record['is_muted']);
  return out;
}

/** Encode a `chatEventVideoChatParticipantIsMutedToggled` into TDLib JSON wire shape. */
export function encodeChatEventVideoChatParticipantIsMutedToggled(value: ChatEventVideoChatParticipantIsMutedToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventVideoChatParticipantIsMutedToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['participant_id'] = value.participant_id === null ? null : encodeTdMessageSender(value.participant_id);
  out['is_muted'] = value.is_muted;
  return out;
}

/** chatEventVideoChatParticipantVolumeLevelChanged = ChatEventAction — A video chat participant volume level was changed */
export class ChatEventVideoChatParticipantVolumeLevelChanged {
  type: 'chatEventVideoChatParticipantVolumeLevelChanged' = 'chatEventVideoChatParticipantVolumeLevelChanged';
  extra?: string;
  /** participant_id: MessageSender — Identifier of the affected group call participant */
  participant_id: TdMessageSender | null = null;
  /** volume_level: int32 — New value of volume_level; 1-20000 in hundreds of percents */
  volume_level: number = 0;
}

/** Decode a TDLib `chatEventVideoChatParticipantVolumeLevelChanged` object; null input -> null. */
export function decodeChatEventVideoChatParticipantVolumeLevelChanged(json: TdJsonRaw): ChatEventVideoChatParticipantVolumeLevelChanged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEventVideoChatParticipantVolumeLevelChanged = new ChatEventVideoChatParticipantVolumeLevelChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.participant_id = decodeTdMessageSender(record['participant_id']);
  out.volume_level = asNumber(record['volume_level']);
  return out;
}

/** Encode a `chatEventVideoChatParticipantVolumeLevelChanged` into TDLib JSON wire shape. */
export function encodeChatEventVideoChatParticipantVolumeLevelChanged(value: ChatEventVideoChatParticipantVolumeLevelChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEventVideoChatParticipantVolumeLevelChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['participant_id'] = value.participant_id === null ? null : encodeTdMessageSender(value.participant_id);
  out['volume_level'] = value.volume_level;
  return out;
}

/** chatEvents = ChatEvents — Contains a list of chat events */
export class ChatEvents {
  type: 'chatEvents' = 'chatEvents';
  extra?: string;
  /** events: vector<chatEvent> — List of events */
  events: ChatEvent[] = [];
}

/** Decode a TDLib `chatEvents` object; null input -> null. */
export function decodeChatEvents(json: TdJsonRaw): ChatEvents | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatEvents = new ChatEvents();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.events = decodeVector(record['events'], (item: TdJsonRaw): ChatEvent => decodeChatEvent(item) as ChatEvent);
  return out;
}

/** Encode a `chatEvents` into TDLib JSON wire shape. */
export function encodeChatEvents(value: ChatEvents): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatEvents';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['events'] = encodeVector(value.events, (item: ChatEvent | null): TdJsonRaw => item === null ? null : encodeChatEvent(item));
  return out;
}

/** chatFolder = ChatFolder — Represents a folder for user chats */
export class ChatFolder {
  type: 'chatFolder' = 'chatFolder';
  extra?: string;
  /** name: chatFolderName — The name of the folder */
  name: ChatFolderName | null = null;
  /** icon: chatFolderIcon — The chosen icon for the chat folder; may be null. If null, use getChatFolderDefaultIconName to get default icon name for the folder */
  icon: ChatFolderIcon | null = null;
  /** color_id: int32 — The identifier of the chosen color for the chat folder icon; from -1 to 6. If -1, then color is disabled. Can't be changed if folder tags are disabled or the current user doesn't have Telegram Premium subscription */
  color_id: number = 0;
  /** is_shareable: Bool — True, if at least one link has been created for the folder */
  is_shareable: boolean = false;
  /** pinned_chat_ids: vector<int53> — The chat identifiers of pinned chats in the folder. There can be up to getOption("chat_folder_chosen_chat_count_max") pinned and always included non-secret chats and the same number of secret chats, but t... */
  pinned_chat_ids: number[] = [];
  /** included_chat_ids: vector<int53> — The chat identifiers of always included chats in the folder. There can be up to getOption("chat_folder_chosen_chat_count_max") pinned and always included non-secret chats and the same number of secret c... */
  included_chat_ids: number[] = [];
  /** excluded_chat_ids: vector<int53> — The chat identifiers of always excluded chats in the folder. There can be up to getOption("chat_folder_chosen_chat_count_max") always excluded non-secret chats and the same number of secret chats, but t... */
  excluded_chat_ids: number[] = [];
  /** exclude_muted: Bool — True, if muted chats need to be excluded */
  exclude_muted: boolean = false;
  /** exclude_read: Bool — True, if read chats need to be excluded */
  exclude_read: boolean = false;
  /** exclude_archived: Bool — True, if archived chats need to be excluded */
  exclude_archived: boolean = false;
  /** include_contacts: Bool — True, if contacts need to be included */
  include_contacts: boolean = false;
  /** include_non_contacts: Bool — True, if non-contact users need to be included */
  include_non_contacts: boolean = false;
  /** include_bots: Bool — True, if bots need to be included */
  include_bots: boolean = false;
  /** include_groups: Bool — True, if basic groups and supergroups need to be included */
  include_groups: boolean = false;
  /** include_channels: Bool — True, if channels need to be included */
  include_channels: boolean = false;
}

/** Decode a TDLib `chatFolder` object; null input -> null. */
export function decodeChatFolder(json: TdJsonRaw): ChatFolder | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatFolder = new ChatFolder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = decodeChatFolderName(record['name']);
  out.icon = decodeChatFolderIcon(record['icon']);
  out.color_id = asNumber(record['color_id']);
  out.is_shareable = asBool(record['is_shareable']);
  out.pinned_chat_ids = decodeVector(record['pinned_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.included_chat_ids = decodeVector(record['included_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.excluded_chat_ids = decodeVector(record['excluded_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.exclude_muted = asBool(record['exclude_muted']);
  out.exclude_read = asBool(record['exclude_read']);
  out.exclude_archived = asBool(record['exclude_archived']);
  out.include_contacts = asBool(record['include_contacts']);
  out.include_non_contacts = asBool(record['include_non_contacts']);
  out.include_bots = asBool(record['include_bots']);
  out.include_groups = asBool(record['include_groups']);
  out.include_channels = asBool(record['include_channels']);
  return out;
}

/** Encode a `chatFolder` into TDLib JSON wire shape. */
export function encodeChatFolder(value: ChatFolder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatFolder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name === null ? null : encodeChatFolderName(value.name);
  out['icon'] = value.icon === null ? null : encodeChatFolderIcon(value.icon);
  out['color_id'] = value.color_id;
  out['is_shareable'] = value.is_shareable;
  out['pinned_chat_ids'] = encodeVector(value.pinned_chat_ids, (item: number): TdJsonRaw => item);
  out['included_chat_ids'] = encodeVector(value.included_chat_ids, (item: number): TdJsonRaw => item);
  out['excluded_chat_ids'] = encodeVector(value.excluded_chat_ids, (item: number): TdJsonRaw => item);
  out['exclude_muted'] = value.exclude_muted;
  out['exclude_read'] = value.exclude_read;
  out['exclude_archived'] = value.exclude_archived;
  out['include_contacts'] = value.include_contacts;
  out['include_non_contacts'] = value.include_non_contacts;
  out['include_bots'] = value.include_bots;
  out['include_groups'] = value.include_groups;
  out['include_channels'] = value.include_channels;
  return out;
}

/** chatFolderIcon = ChatFolderIcon — Represents an icon for a chat folder "Favorite", "Flower", "Game", "Home", "Love", "Mask", "Party", "Sport", "Study", "Trade", "Travel", "Work", "Airplane", "Book", "Light", "Like", "Money", "Note", "Pal... */
export class ChatFolderIcon {
  type: 'chatFolderIcon' = 'chatFolderIcon';
  extra?: string;
  /** name: string — The chosen icon name for short folder representation; one of "All", "Unread", "Unmuted", "Bots", "Channels", "Groups", "Private", "Custom", "Setup", "Cat", "Crown", */
  name: string = '';
}

/** Decode a TDLib `chatFolderIcon` object; null input -> null. */
export function decodeChatFolderIcon(json: TdJsonRaw): ChatFolderIcon | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatFolderIcon = new ChatFolderIcon();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `chatFolderIcon` into TDLib JSON wire shape. */
export function encodeChatFolderIcon(value: ChatFolderIcon): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatFolderIcon';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** chatFolderInfo = ChatFolderInfo — Contains basic information about a chat folder */
export class ChatFolderInfo {
  type: 'chatFolderInfo' = 'chatFolderInfo';
  extra?: string;
  /** id: int32 — Unique chat folder identifier */
  id: number = 0;
  /** name: chatFolderName — The name of the folder */
  name: ChatFolderName | null = null;
  /** icon: chatFolderIcon — The chosen or default icon for the chat folder */
  icon: ChatFolderIcon | null = null;
  /** color_id: int32 — The identifier of the chosen color for the chat folder icon; from -1 to 6. If -1, then color is disabled */
  color_id: number = 0;
  /** is_shareable: Bool — True, if at least one link has been created for the folder */
  is_shareable: boolean = false;
  /** has_my_invite_links: Bool — True, if the chat folder has invite links created by the current user */
  has_my_invite_links: boolean = false;
}

/** Decode a TDLib `chatFolderInfo` object; null input -> null. */
export function decodeChatFolderInfo(json: TdJsonRaw): ChatFolderInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatFolderInfo = new ChatFolderInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.name = decodeChatFolderName(record['name']);
  out.icon = decodeChatFolderIcon(record['icon']);
  out.color_id = asNumber(record['color_id']);
  out.is_shareable = asBool(record['is_shareable']);
  out.has_my_invite_links = asBool(record['has_my_invite_links']);
  return out;
}

/** Encode a `chatFolderInfo` into TDLib JSON wire shape. */
export function encodeChatFolderInfo(value: ChatFolderInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatFolderInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['name'] = value.name === null ? null : encodeChatFolderName(value.name);
  out['icon'] = value.icon === null ? null : encodeChatFolderIcon(value.icon);
  out['color_id'] = value.color_id;
  out['is_shareable'] = value.is_shareable;
  out['has_my_invite_links'] = value.has_my_invite_links;
  return out;
}

/** chatFolderInviteLink = ChatFolderInviteLink — Contains a chat folder invite link */
export class ChatFolderInviteLink {
  type: 'chatFolderInviteLink' = 'chatFolderInviteLink';
  extra?: string;
  /** invite_link: string — The chat folder invite link */
  invite_link: string = '';
  /** name: string — Name of the link */
  name: string = '';
  /** chat_ids: vector<int53> — Identifiers of chats, included in the link */
  chat_ids: number[] = [];
}

/** Decode a TDLib `chatFolderInviteLink` object; null input -> null. */
export function decodeChatFolderInviteLink(json: TdJsonRaw): ChatFolderInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatFolderInviteLink = new ChatFolderInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  out.name = asString(record['name']);
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `chatFolderInviteLink` into TDLib JSON wire shape. */
export function encodeChatFolderInviteLink(value: ChatFolderInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatFolderInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  out['name'] = value.name;
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** chatFolderInviteLinkInfo = ChatFolderInviteLinkInfo — Contains information about an invite link to a chat folder */
export class ChatFolderInviteLinkInfo {
  type: 'chatFolderInviteLinkInfo' = 'chatFolderInviteLinkInfo';
  extra?: string;
  /** chat_folder_info: chatFolderInfo — Basic information about the chat folder; chat folder identifier will be 0 if the user didn't have the chat folder yet */
  chat_folder_info: ChatFolderInfo | null = null;
  /** missing_chat_ids: vector<int53> — Identifiers of the chats from the link, which aren't added to the folder yet */
  missing_chat_ids: number[] = [];
  /** added_chat_ids: vector<int53> — Identifiers of the chats from the link, which are added to the folder already */
  added_chat_ids: number[] = [];
}

/** Decode a TDLib `chatFolderInviteLinkInfo` object; null input -> null. */
export function decodeChatFolderInviteLinkInfo(json: TdJsonRaw): ChatFolderInviteLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatFolderInviteLinkInfo = new ChatFolderInviteLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_info = decodeChatFolderInfo(record['chat_folder_info']);
  out.missing_chat_ids = decodeVector(record['missing_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.added_chat_ids = decodeVector(record['added_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `chatFolderInviteLinkInfo` into TDLib JSON wire shape. */
export function encodeChatFolderInviteLinkInfo(value: ChatFolderInviteLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatFolderInviteLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_info'] = value.chat_folder_info === null ? null : encodeChatFolderInfo(value.chat_folder_info);
  out['missing_chat_ids'] = encodeVector(value.missing_chat_ids, (item: number): TdJsonRaw => item);
  out['added_chat_ids'] = encodeVector(value.added_chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** chatFolderInviteLinks = ChatFolderInviteLinks — Represents a list of chat folder invite links */
export class ChatFolderInviteLinks {
  type: 'chatFolderInviteLinks' = 'chatFolderInviteLinks';
  extra?: string;
  /** invite_links: vector<chatFolderInviteLink> — List of the invite links */
  invite_links: ChatFolderInviteLink[] = [];
}

/** Decode a TDLib `chatFolderInviteLinks` object; null input -> null. */
export function decodeChatFolderInviteLinks(json: TdJsonRaw): ChatFolderInviteLinks | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatFolderInviteLinks = new ChatFolderInviteLinks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_links = decodeVector(record['invite_links'], (item: TdJsonRaw): ChatFolderInviteLink => decodeChatFolderInviteLink(item) as ChatFolderInviteLink);
  return out;
}

/** Encode a `chatFolderInviteLinks` into TDLib JSON wire shape. */
export function encodeChatFolderInviteLinks(value: ChatFolderInviteLinks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatFolderInviteLinks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_links'] = encodeVector(value.invite_links, (item: ChatFolderInviteLink | null): TdJsonRaw => item === null ? null : encodeChatFolderInviteLink(item));
  return out;
}

/** chatFolderName = ChatFolderName — Describes name of a chat folder */
export class ChatFolderName {
  type: 'chatFolderName' = 'chatFolderName';
  extra?: string;
  /** text: formattedText — The text of the chat folder name; 1-12 characters without line feeds. May contain only CustomEmoji entities */
  text: FormattedText | null = null;
  /** animate_custom_emoji: Bool — True, if custom emoji in the name must be animated */
  animate_custom_emoji: boolean = false;
}

/** Decode a TDLib `chatFolderName` object; null input -> null. */
export function decodeChatFolderName(json: TdJsonRaw): ChatFolderName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatFolderName = new ChatFolderName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.animate_custom_emoji = asBool(record['animate_custom_emoji']);
  return out;
}

/** Encode a `chatFolderName` into TDLib JSON wire shape. */
export function encodeChatFolderName(value: ChatFolderName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatFolderName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['animate_custom_emoji'] = value.animate_custom_emoji;
  return out;
}

/** chatInviteLink = ChatInviteLink — Contains a chat invite link */
export class ChatInviteLink {
  type: 'chatInviteLink' = 'chatInviteLink';
  extra?: string;
  /** invite_link: string — Chat invite link */
  invite_link: string = '';
  /** name: string — Name of the link */
  name: string = '';
  /** creator_user_id: int53 — User identifier of an administrator created the link */
  creator_user_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) when the link was created */
  date: number = 0;
  /** edit_date: int32 — Point in time (Unix timestamp) when the link was last edited; 0 if never or unknown */
  edit_date: number = 0;
  /** expiration_date: int32 — Point in time (Unix timestamp) when the link will expire; 0 if never */
  expiration_date: number = 0;
  /** subscription_pricing: starSubscriptionPricing — Information about subscription plan that is applied to the users joining the chat by the link; may be null if the link doesn't require subscription */
  subscription_pricing: StarSubscriptionPricing | null = null;
  /** member_limit: int32 — The maximum number of members, which can join the chat using the link simultaneously; 0 if not limited. Always 0 if the link requires approval */
  member_limit: number = 0;
  /** member_count: int32 — Number of chat members, which joined the chat using the link */
  member_count: number = 0;
  /** expired_member_count: int32 — Number of chat members, which joined the chat using the link, but have already left because of expired subscription; for subscription links only */
  expired_member_count: number = 0;
  /** pending_join_request_count: int32 — Number of pending join requests created using this link */
  pending_join_request_count: number = 0;
  /** creates_join_request: Bool — True, if the link only creates join request. If true, total number of joining members will be unlimited */
  creates_join_request: boolean = false;
  /** is_primary: Bool — True, if the link is primary. Primary invite link can't have name, expiration date, or usage limit. There is exactly one primary invite link for each administrator with can_invite_users right at a given time */
  is_primary: boolean = false;
  /** is_revoked: Bool — True, if the link was revoked */
  is_revoked: boolean = false;
}

/** Decode a TDLib `chatInviteLink` object; null input -> null. */
export function decodeChatInviteLink(json: TdJsonRaw): ChatInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatInviteLink = new ChatInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  out.name = asString(record['name']);
  out.creator_user_id = asNumber(record['creator_user_id']);
  out.date = asNumber(record['date']);
  out.edit_date = asNumber(record['edit_date']);
  out.expiration_date = asNumber(record['expiration_date']);
  out.subscription_pricing = decodeStarSubscriptionPricing(record['subscription_pricing']);
  out.member_limit = asNumber(record['member_limit']);
  out.member_count = asNumber(record['member_count']);
  out.expired_member_count = asNumber(record['expired_member_count']);
  out.pending_join_request_count = asNumber(record['pending_join_request_count']);
  out.creates_join_request = asBool(record['creates_join_request']);
  out.is_primary = asBool(record['is_primary']);
  out.is_revoked = asBool(record['is_revoked']);
  return out;
}

/** Encode a `chatInviteLink` into TDLib JSON wire shape. */
export function encodeChatInviteLink(value: ChatInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  out['name'] = value.name;
  out['creator_user_id'] = value.creator_user_id;
  out['date'] = value.date;
  out['edit_date'] = value.edit_date;
  out['expiration_date'] = value.expiration_date;
  out['subscription_pricing'] = value.subscription_pricing === null ? null : encodeStarSubscriptionPricing(value.subscription_pricing);
  out['member_limit'] = value.member_limit;
  out['member_count'] = value.member_count;
  out['expired_member_count'] = value.expired_member_count;
  out['pending_join_request_count'] = value.pending_join_request_count;
  out['creates_join_request'] = value.creates_join_request;
  out['is_primary'] = value.is_primary;
  out['is_revoked'] = value.is_revoked;
  return out;
}

/** chatInviteLinkCount = ChatInviteLinkCount — Describes a chat administrator with a number of active and revoked chat invite links */
export class ChatInviteLinkCount {
  type: 'chatInviteLinkCount' = 'chatInviteLinkCount';
  extra?: string;
  /** user_id: int53 — Administrator's user identifier */
  user_id: number = 0;
  /** invite_link_count: int32 — Number of active invite links */
  invite_link_count: number = 0;
  /** revoked_invite_link_count: int32 — Number of revoked invite links */
  revoked_invite_link_count: number = 0;
}

/** Decode a TDLib `chatInviteLinkCount` object; null input -> null. */
export function decodeChatInviteLinkCount(json: TdJsonRaw): ChatInviteLinkCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatInviteLinkCount = new ChatInviteLinkCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.invite_link_count = asNumber(record['invite_link_count']);
  out.revoked_invite_link_count = asNumber(record['revoked_invite_link_count']);
  return out;
}

/** Encode a `chatInviteLinkCount` into TDLib JSON wire shape. */
export function encodeChatInviteLinkCount(value: ChatInviteLinkCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatInviteLinkCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['invite_link_count'] = value.invite_link_count;
  out['revoked_invite_link_count'] = value.revoked_invite_link_count;
  return out;
}

/** chatInviteLinkCounts = ChatInviteLinkCounts — Contains a list of chat invite link counts */
export class ChatInviteLinkCounts {
  type: 'chatInviteLinkCounts' = 'chatInviteLinkCounts';
  extra?: string;
  /** invite_link_counts: vector<chatInviteLinkCount> — List of invite link counts */
  invite_link_counts: ChatInviteLinkCount[] = [];
}

/** Decode a TDLib `chatInviteLinkCounts` object; null input -> null. */
export function decodeChatInviteLinkCounts(json: TdJsonRaw): ChatInviteLinkCounts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatInviteLinkCounts = new ChatInviteLinkCounts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link_counts = decodeVector(record['invite_link_counts'], (item: TdJsonRaw): ChatInviteLinkCount => decodeChatInviteLinkCount(item) as ChatInviteLinkCount);
  return out;
}

/** Encode a `chatInviteLinkCounts` into TDLib JSON wire shape. */
export function encodeChatInviteLinkCounts(value: ChatInviteLinkCounts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatInviteLinkCounts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link_counts'] = encodeVector(value.invite_link_counts, (item: ChatInviteLinkCount | null): TdJsonRaw => item === null ? null : encodeChatInviteLinkCount(item));
  return out;
}

/** chatInviteLinkInfo = ChatInviteLinkInfo — Contains information about a chat invite link */
export class ChatInviteLinkInfo {
  type: 'chatInviteLinkInfo' = 'chatInviteLinkInfo';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the invite link; 0 if the user has no access to the chat before joining */
  chat_id: number = 0;
  /** accessible_for: int32 — If non-zero, the amount of time for which read access to the chat will remain available, in seconds */
  accessible_for: number = 0;
  /** type: InviteLinkChatType — Type of the chat */
  type_: TdInviteLinkChatType | null = null;
  /** title: string — Title of the chat */
  title: string = '';
  /** photo: chatPhotoInfo — Chat photo; may be null */
  photo: ChatPhotoInfo | null = null;
  /** accent_color_id: int32 — Identifier of the accent color for chat title and background of chat photo */
  accent_color_id: number = 0;
  /** description: string */
  description: string = '';
  /** member_count: int32 — Number of members in the chat */
  member_count: number = 0;
  /** member_user_ids: vector<int53> — User identifiers of some chat members that may be known to the current user */
  member_user_ids: number[] = [];
  /** subscription_info: chatInviteLinkSubscriptionInfo — Information about subscription plan that must be paid by the user to use the link; may be null if the link doesn't require subscription */
  subscription_info: ChatInviteLinkSubscriptionInfo | null = null;
  /** creates_join_request: Bool — True, if the link only creates join request */
  creates_join_request: boolean = false;
  /** is_public: Bool — True, if the chat is a public supergroup or channel, i.e. it has a username or it is a location-based supergroup */
  is_public: boolean = false;
  /** verification_status: verificationStatus — Information about verification status of the chat; may be null if none */
  verification_status: VerificationStatus | null = null;
}

/** Decode a TDLib `chatInviteLinkInfo` object; null input -> null. */
export function decodeChatInviteLinkInfo(json: TdJsonRaw): ChatInviteLinkInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatInviteLinkInfo = new ChatInviteLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.accessible_for = asNumber(record['accessible_for']);
  out.type_ = decodeTdInviteLinkChatType(record['type']);
  out.title = asString(record['title']);
  out.photo = decodeChatPhotoInfo(record['photo']);
  out.accent_color_id = asNumber(record['accent_color_id']);
  out.description = asString(record['description']);
  out.member_count = asNumber(record['member_count']);
  out.member_user_ids = decodeVector(record['member_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.subscription_info = decodeChatInviteLinkSubscriptionInfo(record['subscription_info']);
  out.creates_join_request = asBool(record['creates_join_request']);
  out.is_public = asBool(record['is_public']);
  out.verification_status = decodeVerificationStatus(record['verification_status']);
  return out;
}

/** Encode a `chatInviteLinkInfo` into TDLib JSON wire shape. */
export function encodeChatInviteLinkInfo(value: ChatInviteLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatInviteLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['accessible_for'] = value.accessible_for;
  out['type'] = value.type_ === null ? null : encodeTdInviteLinkChatType(value.type_);
  out['title'] = value.title;
  out['photo'] = value.photo === null ? null : encodeChatPhotoInfo(value.photo);
  out['accent_color_id'] = value.accent_color_id;
  out['description'] = value.description;
  out['member_count'] = value.member_count;
  out['member_user_ids'] = encodeVector(value.member_user_ids, (item: number): TdJsonRaw => item);
  out['subscription_info'] = value.subscription_info === null ? null : encodeChatInviteLinkSubscriptionInfo(value.subscription_info);
  out['creates_join_request'] = value.creates_join_request;
  out['is_public'] = value.is_public;
  out['verification_status'] = value.verification_status === null ? null : encodeVerificationStatus(value.verification_status);
  return out;
}

/** chatInviteLinkMember = ChatInviteLinkMember — Describes a chat member joined a chat via an invite link */
export class ChatInviteLinkMember {
  type: 'chatInviteLinkMember' = 'chatInviteLinkMember';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** joined_chat_date: int32 — Point in time (Unix timestamp) when the user joined the chat */
  joined_chat_date: number = 0;
  /** via_chat_folder_invite_link: Bool — True, if the user has joined the chat using an invite link for a chat folder */
  via_chat_folder_invite_link: boolean = false;
  /** approver_user_id: int53 — User identifier of the chat administrator, approved user join request */
  approver_user_id: number = 0;
}

/** Decode a TDLib `chatInviteLinkMember` object; null input -> null. */
export function decodeChatInviteLinkMember(json: TdJsonRaw): ChatInviteLinkMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatInviteLinkMember = new ChatInviteLinkMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.joined_chat_date = asNumber(record['joined_chat_date']);
  out.via_chat_folder_invite_link = asBool(record['via_chat_folder_invite_link']);
  out.approver_user_id = asNumber(record['approver_user_id']);
  return out;
}

/** Encode a `chatInviteLinkMember` into TDLib JSON wire shape. */
export function encodeChatInviteLinkMember(value: ChatInviteLinkMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatInviteLinkMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['joined_chat_date'] = value.joined_chat_date;
  out['via_chat_folder_invite_link'] = value.via_chat_folder_invite_link;
  out['approver_user_id'] = value.approver_user_id;
  return out;
}

/** chatInviteLinkMembers = ChatInviteLinkMembers — Contains a list of chat members joined a chat via an invite link */
export class ChatInviteLinkMembers {
  type: 'chatInviteLinkMembers' = 'chatInviteLinkMembers';
  extra?: string;
  /** total_count: int32 — Approximate total number of chat members found */
  total_count: number = 0;
  /** members: vector<chatInviteLinkMember> — List of chat members, joined a chat via an invite link */
  members: ChatInviteLinkMember[] = [];
}

/** Decode a TDLib `chatInviteLinkMembers` object; null input -> null. */
export function decodeChatInviteLinkMembers(json: TdJsonRaw): ChatInviteLinkMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatInviteLinkMembers = new ChatInviteLinkMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.members = decodeVector(record['members'], (item: TdJsonRaw): ChatInviteLinkMember => decodeChatInviteLinkMember(item) as ChatInviteLinkMember);
  return out;
}

/** Encode a `chatInviteLinkMembers` into TDLib JSON wire shape. */
export function encodeChatInviteLinkMembers(value: ChatInviteLinkMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatInviteLinkMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['members'] = encodeVector(value.members, (item: ChatInviteLinkMember | null): TdJsonRaw => item === null ? null : encodeChatInviteLinkMember(item));
  return out;
}

/** chatInviteLinkSubscriptionInfo = ChatInviteLinkSubscriptionInfo — Contains information about subscription plan that must be paid by the user to use a chat invite link */
export class ChatInviteLinkSubscriptionInfo {
  type: 'chatInviteLinkSubscriptionInfo' = 'chatInviteLinkSubscriptionInfo';
  extra?: string;
  /** pricing: starSubscriptionPricing — Information about subscription plan that must be paid by the user to use the link */
  pricing: StarSubscriptionPricing | null = null;
  /** can_reuse: Bool — True, if the user has already paid for the subscription and can use joinChatByInviteLink to join the subscribed chat again */
  can_reuse: boolean = false;
  /** form_id: int64 — Identifier of the payment form to use for subscription payment; 0 if the subscription can't be paid */
  form_id: string = '';
}

/** Decode a TDLib `chatInviteLinkSubscriptionInfo` object; null input -> null. */
export function decodeChatInviteLinkSubscriptionInfo(json: TdJsonRaw): ChatInviteLinkSubscriptionInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatInviteLinkSubscriptionInfo = new ChatInviteLinkSubscriptionInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.pricing = decodeStarSubscriptionPricing(record['pricing']);
  out.can_reuse = asBool(record['can_reuse']);
  out.form_id = asInt64(record['form_id']);
  return out;
}

/** Encode a `chatInviteLinkSubscriptionInfo` into TDLib JSON wire shape. */
export function encodeChatInviteLinkSubscriptionInfo(value: ChatInviteLinkSubscriptionInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatInviteLinkSubscriptionInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['pricing'] = value.pricing === null ? null : encodeStarSubscriptionPricing(value.pricing);
  out['can_reuse'] = value.can_reuse;
  out['form_id'] = value.form_id;
  return out;
}

/** chatInviteLinks = ChatInviteLinks — Contains a list of chat invite links */
export class ChatInviteLinks {
  type: 'chatInviteLinks' = 'chatInviteLinks';
  extra?: string;
  /** total_count: int32 — Approximate total number of chat invite links found */
  total_count: number = 0;
  /** invite_links: vector<chatInviteLink> — List of invite links */
  invite_links: ChatInviteLink[] = [];
}

/** Decode a TDLib `chatInviteLinks` object; null input -> null. */
export function decodeChatInviteLinks(json: TdJsonRaw): ChatInviteLinks | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatInviteLinks = new ChatInviteLinks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.invite_links = decodeVector(record['invite_links'], (item: TdJsonRaw): ChatInviteLink => decodeChatInviteLink(item) as ChatInviteLink);
  return out;
}

/** Encode a `chatInviteLinks` into TDLib JSON wire shape. */
export function encodeChatInviteLinks(value: ChatInviteLinks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatInviteLinks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['invite_links'] = encodeVector(value.invite_links, (item: ChatInviteLink | null): TdJsonRaw => item === null ? null : encodeChatInviteLink(item));
  return out;
}

/** chatJoinRequest = ChatJoinRequest — Describes a user who sent a join request and waits for administrator approval */
export class ChatJoinRequest {
  type: 'chatJoinRequest' = 'chatJoinRequest';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) when the user sent the join request */
  date: number = 0;
  /** bio: string — A short bio of the user */
  bio: string = '';
}

/** Decode a TDLib `chatJoinRequest` object; null input -> null. */
export function decodeChatJoinRequest(json: TdJsonRaw): ChatJoinRequest | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinRequest = new ChatJoinRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.date = asNumber(record['date']);
  out.bio = asString(record['bio']);
  return out;
}

/** Encode a `chatJoinRequest` into TDLib JSON wire shape. */
export function encodeChatJoinRequest(value: ChatJoinRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['date'] = value.date;
  out['bio'] = value.bio;
  return out;
}

/** chatJoinRequestResultApproved = ChatJoinRequestResult — The request was approved */
export class ChatJoinRequestResultApproved {
  type: 'chatJoinRequestResultApproved' = 'chatJoinRequestResultApproved';
  extra?: string;
}

/** Decode a TDLib `chatJoinRequestResultApproved` object; null input -> null. */
export function decodeChatJoinRequestResultApproved(json: TdJsonRaw): ChatJoinRequestResultApproved | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinRequestResultApproved = new ChatJoinRequestResultApproved();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatJoinRequestResultApproved` into TDLib JSON wire shape. */
export function encodeChatJoinRequestResultApproved(value: ChatJoinRequestResultApproved): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinRequestResultApproved';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatJoinRequestResultDeclined = ChatJoinRequestResult — The request was declined */
export class ChatJoinRequestResultDeclined {
  type: 'chatJoinRequestResultDeclined' = 'chatJoinRequestResultDeclined';
  extra?: string;
}

/** Decode a TDLib `chatJoinRequestResultDeclined` object; null input -> null. */
export function decodeChatJoinRequestResultDeclined(json: TdJsonRaw): ChatJoinRequestResultDeclined | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinRequestResultDeclined = new ChatJoinRequestResultDeclined();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatJoinRequestResultDeclined` into TDLib JSON wire shape. */
export function encodeChatJoinRequestResultDeclined(value: ChatJoinRequestResultDeclined): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinRequestResultDeclined';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatJoinRequestResultQueued = ChatJoinRequestResult — The request was postponed without a decision */
export class ChatJoinRequestResultQueued {
  type: 'chatJoinRequestResultQueued' = 'chatJoinRequestResultQueued';
  extra?: string;
}

/** Decode a TDLib `chatJoinRequestResultQueued` object; null input -> null. */
export function decodeChatJoinRequestResultQueued(json: TdJsonRaw): ChatJoinRequestResultQueued | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinRequestResultQueued = new ChatJoinRequestResultQueued();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatJoinRequestResultQueued` into TDLib JSON wire shape. */
export function encodeChatJoinRequestResultQueued(value: ChatJoinRequestResultQueued): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinRequestResultQueued';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatJoinRequests = ChatJoinRequests — Contains a list of requests to join a chat */
export class ChatJoinRequests {
  type: 'chatJoinRequests' = 'chatJoinRequests';
  extra?: string;
  /** total_count: int32 — Approximate total number of requests found */
  total_count: number = 0;
  /** requests: vector<chatJoinRequest> — List of the requests */
  requests: ChatJoinRequest[] = [];
}

/** Decode a TDLib `chatJoinRequests` object; null input -> null. */
export function decodeChatJoinRequests(json: TdJsonRaw): ChatJoinRequests | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinRequests = new ChatJoinRequests();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.requests = decodeVector(record['requests'], (item: TdJsonRaw): ChatJoinRequest => decodeChatJoinRequest(item) as ChatJoinRequest);
  return out;
}

/** Encode a `chatJoinRequests` into TDLib JSON wire shape. */
export function encodeChatJoinRequests(value: ChatJoinRequests): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinRequests';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['requests'] = encodeVector(value.requests, (item: ChatJoinRequest | null): TdJsonRaw => item === null ? null : encodeChatJoinRequest(item));
  return out;
}

/** chatJoinRequestsInfo = ChatJoinRequestsInfo — Contains information about pending join requests for a chat */
export class ChatJoinRequestsInfo {
  type: 'chatJoinRequestsInfo' = 'chatJoinRequestsInfo';
  extra?: string;
  /** total_count: int32 — Total number of pending join requests */
  total_count: number = 0;
  /** user_ids: vector<int53> — Identifiers of at most 3 users sent the newest pending join requests */
  user_ids: number[] = [];
}

/** Decode a TDLib `chatJoinRequestsInfo` object; null input -> null. */
export function decodeChatJoinRequestsInfo(json: TdJsonRaw): ChatJoinRequestsInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinRequestsInfo = new ChatJoinRequestsInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `chatJoinRequestsInfo` into TDLib JSON wire shape. */
export function encodeChatJoinRequestsInfo(value: ChatJoinRequestsInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinRequestsInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** chatJoinResultDeclined = ChatJoinResult — The join was declined by the guard bot */
export class ChatJoinResultDeclined {
  type: 'chatJoinResultDeclined' = 'chatJoinResultDeclined';
  extra?: string;
}

/** Decode a TDLib `chatJoinResultDeclined` object; null input -> null. */
export function decodeChatJoinResultDeclined(json: TdJsonRaw): ChatJoinResultDeclined | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinResultDeclined = new ChatJoinResultDeclined();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatJoinResultDeclined` into TDLib JSON wire shape. */
export function encodeChatJoinResultDeclined(value: ChatJoinResultDeclined): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinResultDeclined';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatJoinResultGuardBotApprovalRequired = ChatJoinResult — An approval from a guard bot through a Web App is required to join the chat */
export class ChatJoinResultGuardBotApprovalRequired {
  type: 'chatJoinResultGuardBotApprovalRequired' = 'chatJoinResultGuardBotApprovalRequired';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the guard bot */
  bot_user_id: number = 0;
  /** query_id: int64 — Unique identifier of the join request, which will be used in getGuardBotWebAppUrl and updateChatJoinResult */
  query_id: string = '';
}

/** Decode a TDLib `chatJoinResultGuardBotApprovalRequired` object; null input -> null. */
export function decodeChatJoinResultGuardBotApprovalRequired(json: TdJsonRaw): ChatJoinResultGuardBotApprovalRequired | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinResultGuardBotApprovalRequired = new ChatJoinResultGuardBotApprovalRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.query_id = asInt64(record['query_id']);
  return out;
}

/** Encode a `chatJoinResultGuardBotApprovalRequired` into TDLib JSON wire shape. */
export function encodeChatJoinResultGuardBotApprovalRequired(value: ChatJoinResultGuardBotApprovalRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinResultGuardBotApprovalRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['query_id'] = value.query_id;
  return out;
}

/** chatJoinResultRequestSent = ChatJoinResult — The join request was sent and have to be approved by administrators of the chat */
export class ChatJoinResultRequestSent {
  type: 'chatJoinResultRequestSent' = 'chatJoinResultRequestSent';
  extra?: string;
}

/** Decode a TDLib `chatJoinResultRequestSent` object; null input -> null. */
export function decodeChatJoinResultRequestSent(json: TdJsonRaw): ChatJoinResultRequestSent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinResultRequestSent = new ChatJoinResultRequestSent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatJoinResultRequestSent` into TDLib JSON wire shape. */
export function encodeChatJoinResultRequestSent(value: ChatJoinResultRequestSent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinResultRequestSent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatJoinResultSuccess = ChatJoinResult — The chat was joined successfully */
export class ChatJoinResultSuccess {
  type: 'chatJoinResultSuccess' = 'chatJoinResultSuccess';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
}

/** Decode a TDLib `chatJoinResultSuccess` object; null input -> null. */
export function decodeChatJoinResultSuccess(json: TdJsonRaw): ChatJoinResultSuccess | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatJoinResultSuccess = new ChatJoinResultSuccess();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `chatJoinResultSuccess` into TDLib JSON wire shape. */
export function encodeChatJoinResultSuccess(value: ChatJoinResultSuccess): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatJoinResultSuccess';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** chatListArchive = ChatList — A list of chats usually located at the top of the main chat list. Unmuted chats are automatically moved from the Archive to the Main chat list when a new message arrives */
export class ChatListArchive {
  type: 'chatListArchive' = 'chatListArchive';
  extra?: string;
}

/** Decode a TDLib `chatListArchive` object; null input -> null. */
export function decodeChatListArchive(json: TdJsonRaw): ChatListArchive | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatListArchive = new ChatListArchive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatListArchive` into TDLib JSON wire shape. */
export function encodeChatListArchive(value: ChatListArchive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatListArchive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatListFolder = ChatList — A list of chats added to a chat folder */
export class ChatListFolder {
  type: 'chatListFolder' = 'chatListFolder';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
}

/** Decode a TDLib `chatListFolder` object; null input -> null. */
export function decodeChatListFolder(json: TdJsonRaw): ChatListFolder | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatListFolder = new ChatListFolder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  return out;
}

/** Encode a `chatListFolder` into TDLib JSON wire shape. */
export function encodeChatListFolder(value: ChatListFolder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatListFolder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  return out;
}

/** chatListMain = ChatList — A main list of chats */
export class ChatListMain {
  type: 'chatListMain' = 'chatListMain';
  extra?: string;
}

/** Decode a TDLib `chatListMain` object; null input -> null. */
export function decodeChatListMain(json: TdJsonRaw): ChatListMain | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatListMain = new ChatListMain();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatListMain` into TDLib JSON wire shape. */
export function encodeChatListMain(value: ChatListMain): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatListMain';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatLists = ChatLists — Contains a list of chat lists */
export class ChatLists {
  type: 'chatLists' = 'chatLists';
  extra?: string;
  /** chat_lists: vector<ChatList> — List of chat lists */
  chat_lists: TdChatList[] = [];
}

/** Decode a TDLib `chatLists` object; null input -> null. */
export function decodeChatLists(json: TdJsonRaw): ChatLists | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatLists = new ChatLists();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_lists = decodeVector(record['chat_lists'], (item: TdJsonRaw): TdChatList => decodeTdChatList(item) as TdChatList);
  return out;
}

/** Encode a `chatLists` into TDLib JSON wire shape. */
export function encodeChatLists(value: ChatLists): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatLists';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_lists'] = encodeVector(value.chat_lists, (item: TdChatList | null): TdJsonRaw => item === null ? null : encodeTdChatList(item));
  return out;
}

/** chatLocation = ChatLocation — Represents a location to which a chat is connected */
export class ChatLocation {
  type: 'chatLocation' = 'chatLocation';
  extra?: string;
  /** location: location — The location */
  location: Location | null = null;
  /** address: string — Location address; 1-64 characters, as defined by the chat owner */
  address: string = '';
}

/** Decode a TDLib `chatLocation` object; null input -> null. */
export function decodeChatLocation(json: TdJsonRaw): ChatLocation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatLocation = new ChatLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  out.address = asString(record['address']);
  return out;
}

/** Encode a `chatLocation` into TDLib JSON wire shape. */
export function encodeChatLocation(value: ChatLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['address'] = value.address;
  return out;
}

/** chatMember = ChatMember — Describes a user or a chat as a member of another chat */
export class ChatMember {
  type: 'chatMember' = 'chatMember';
  extra?: string;
  /** member_id: MessageSender — Identifier of the chat member. Currently, other chats can be only Left or Banned. Only supergroups and channels can have other chats as Left or Banned members and these chats must be supergroups or channels */
  member_id: TdMessageSender | null = null;
  /** tag: string — Tag of the chat member or its custom title if the member is an administrator of the chat; 0-16 characters without emoji; applicable to basic groups and supergroups only */
  tag: string = '';
  /** inviter_user_id: int53 — Identifier of a user who invited/promoted/banned this member in the chat; 0 if unknown */
  inviter_user_id: number = 0;
  /** joined_chat_date: int32 — Point in time (Unix timestamp) when the user joined/was promoted/was banned in the chat */
  joined_chat_date: number = 0;
  /** status: ChatMemberStatus — Status of the member in the chat */
  status: TdChatMemberStatus | null = null;
}

/** Decode a TDLib `chatMember` object; null input -> null. */
export function decodeChatMember(json: TdJsonRaw): ChatMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMember = new ChatMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.member_id = decodeTdMessageSender(record['member_id']);
  out.tag = asString(record['tag']);
  out.inviter_user_id = asNumber(record['inviter_user_id']);
  out.joined_chat_date = asNumber(record['joined_chat_date']);
  out.status = decodeTdChatMemberStatus(record['status']);
  return out;
}

/** Encode a `chatMember` into TDLib JSON wire shape. */
export function encodeChatMember(value: ChatMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['member_id'] = value.member_id === null ? null : encodeTdMessageSender(value.member_id);
  out['tag'] = value.tag;
  out['inviter_user_id'] = value.inviter_user_id;
  out['joined_chat_date'] = value.joined_chat_date;
  out['status'] = value.status === null ? null : encodeTdChatMemberStatus(value.status);
  return out;
}

/** chatMemberStatusAdministrator = ChatMemberStatus — The user is a member of the chat and has some additional privileges. In basic groups, administrators have all applicable rights. In supergroups and channels, any subset of the rights can... */
export class ChatMemberStatusAdministrator {
  type: 'chatMemberStatusAdministrator' = 'chatMemberStatusAdministrator';
  extra?: string;
  /** can_be_edited: Bool — True, if the current user can edit the administrator privileges for the called user */
  can_be_edited: boolean = false;
  /** rights: chatAdministratorRights — Rights of the administrator */
  rights: ChatAdministratorRights | null = null;
}

/** Decode a TDLib `chatMemberStatusAdministrator` object; null input -> null. */
export function decodeChatMemberStatusAdministrator(json: TdJsonRaw): ChatMemberStatusAdministrator | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMemberStatusAdministrator = new ChatMemberStatusAdministrator();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_be_edited = asBool(record['can_be_edited']);
  out.rights = decodeChatAdministratorRights(record['rights']);
  return out;
}

/** Encode a `chatMemberStatusAdministrator` into TDLib JSON wire shape. */
export function encodeChatMemberStatusAdministrator(value: ChatMemberStatusAdministrator): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMemberStatusAdministrator';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_be_edited'] = value.can_be_edited;
  out['rights'] = value.rights === null ? null : encodeChatAdministratorRights(value.rights);
  return out;
}

/** chatMemberStatusBanned = ChatMemberStatus — The user or the chat was banned (and hence is not a member of the chat). Implies the user can't return to the chat, view messages, or be used as a participant identifier to join a video chat of... */
export class ChatMemberStatusBanned {
  type: 'chatMemberStatusBanned' = 'chatMemberStatusBanned';
  extra?: string;
  /** banned_until_date: int32 — Point in time (Unix timestamp) when the user will be unbanned; 0 if never. If the user is banned for more than 366 days or for less than 30 seconds from the current time, the user is considered to be banned for... */
  banned_until_date: number = 0;
}

/** Decode a TDLib `chatMemberStatusBanned` object; null input -> null. */
export function decodeChatMemberStatusBanned(json: TdJsonRaw): ChatMemberStatusBanned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMemberStatusBanned = new ChatMemberStatusBanned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.banned_until_date = asNumber(record['banned_until_date']);
  return out;
}

/** Encode a `chatMemberStatusBanned` into TDLib JSON wire shape. */
export function encodeChatMemberStatusBanned(value: ChatMemberStatusBanned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMemberStatusBanned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['banned_until_date'] = value.banned_until_date;
  return out;
}

/** chatMemberStatusCreator = ChatMemberStatus — The user is the owner of the chat and has all the administrator privileges */
export class ChatMemberStatusCreator {
  type: 'chatMemberStatusCreator' = 'chatMemberStatusCreator';
  extra?: string;
  /** is_anonymous: Bool — True, if the creator isn't shown in the chat member list and sends messages anonymously; applicable to supergroups only */
  is_anonymous: boolean = false;
  /** is_member: Bool — True, if the user is a member of the chat */
  is_member: boolean = false;
}

/** Decode a TDLib `chatMemberStatusCreator` object; null input -> null. */
export function decodeChatMemberStatusCreator(json: TdJsonRaw): ChatMemberStatusCreator | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMemberStatusCreator = new ChatMemberStatusCreator();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_anonymous = asBool(record['is_anonymous']);
  out.is_member = asBool(record['is_member']);
  return out;
}

/** Encode a `chatMemberStatusCreator` into TDLib JSON wire shape. */
export function encodeChatMemberStatusCreator(value: ChatMemberStatusCreator): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMemberStatusCreator';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_anonymous'] = value.is_anonymous;
  out['is_member'] = value.is_member;
  return out;
}

/** chatMemberStatusLeft = ChatMemberStatus — The user or the chat is not a chat member */
export class ChatMemberStatusLeft {
  type: 'chatMemberStatusLeft' = 'chatMemberStatusLeft';
  extra?: string;
}

/** Decode a TDLib `chatMemberStatusLeft` object; null input -> null. */
export function decodeChatMemberStatusLeft(json: TdJsonRaw): ChatMemberStatusLeft | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMemberStatusLeft = new ChatMemberStatusLeft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatMemberStatusLeft` into TDLib JSON wire shape. */
export function encodeChatMemberStatusLeft(value: ChatMemberStatusLeft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMemberStatusLeft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatMemberStatusMember = ChatMemberStatus — The user is a member of the chat, without any additional privileges or restrictions */
export class ChatMemberStatusMember {
  type: 'chatMemberStatusMember' = 'chatMemberStatusMember';
  extra?: string;
  /** member_until_date: int32 — Point in time (Unix timestamp) when the user will be removed from the chat because of the expired subscription; 0 if never. Ignored in setChatMemberStatus */
  member_until_date: number = 0;
}

/** Decode a TDLib `chatMemberStatusMember` object; null input -> null. */
export function decodeChatMemberStatusMember(json: TdJsonRaw): ChatMemberStatusMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMemberStatusMember = new ChatMemberStatusMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.member_until_date = asNumber(record['member_until_date']);
  return out;
}

/** Encode a `chatMemberStatusMember` into TDLib JSON wire shape. */
export function encodeChatMemberStatusMember(value: ChatMemberStatusMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMemberStatusMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['member_until_date'] = value.member_until_date;
  return out;
}

/** chatMemberStatusRestricted = ChatMemberStatus — The user is under certain restrictions in the chat. Not supported in basic groups and channels */
export class ChatMemberStatusRestricted {
  type: 'chatMemberStatusRestricted' = 'chatMemberStatusRestricted';
  extra?: string;
  /** is_member: Bool — True, if the user is a member of the chat */
  is_member: boolean = false;
  /** restricted_until_date: int32 — Point in time (Unix timestamp) when restrictions will be lifted from the user; 0 if never. If the user is restricted for more than 366 days or for less than 30 seconds from the current time, the user is con... */
  restricted_until_date: number = 0;
  /** permissions: chatPermissions — User permissions in the chat */
  permissions: ChatPermissions | null = null;
}

/** Decode a TDLib `chatMemberStatusRestricted` object; null input -> null. */
export function decodeChatMemberStatusRestricted(json: TdJsonRaw): ChatMemberStatusRestricted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMemberStatusRestricted = new ChatMemberStatusRestricted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_member = asBool(record['is_member']);
  out.restricted_until_date = asNumber(record['restricted_until_date']);
  out.permissions = decodeChatPermissions(record['permissions']);
  return out;
}

/** Encode a `chatMemberStatusRestricted` into TDLib JSON wire shape. */
export function encodeChatMemberStatusRestricted(value: ChatMemberStatusRestricted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMemberStatusRestricted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_member'] = value.is_member;
  out['restricted_until_date'] = value.restricted_until_date;
  out['permissions'] = value.permissions === null ? null : encodeChatPermissions(value.permissions);
  return out;
}

/** chatMembers = ChatMembers — Contains a list of chat members */
export class ChatMembers {
  type: 'chatMembers' = 'chatMembers';
  extra?: string;
  /** total_count: int32 — Approximate total number of chat members found */
  total_count: number = 0;
  /** members: vector<chatMember> — A list of chat members */
  members: ChatMember[] = [];
}

/** Decode a TDLib `chatMembers` object; null input -> null. */
export function decodeChatMembers(json: TdJsonRaw): ChatMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMembers = new ChatMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.members = decodeVector(record['members'], (item: TdJsonRaw): ChatMember => decodeChatMember(item) as ChatMember);
  return out;
}

/** Encode a `chatMembers` into TDLib JSON wire shape. */
export function encodeChatMembers(value: ChatMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['members'] = encodeVector(value.members, (item: ChatMember | null): TdJsonRaw => item === null ? null : encodeChatMember(item));
  return out;
}

/** chatMembersFilterAdministrators = ChatMembersFilter — Returns the owner and administrators */
export class ChatMembersFilterAdministrators {
  type: 'chatMembersFilterAdministrators' = 'chatMembersFilterAdministrators';
  extra?: string;
}

/** Decode a TDLib `chatMembersFilterAdministrators` object; null input -> null. */
export function decodeChatMembersFilterAdministrators(json: TdJsonRaw): ChatMembersFilterAdministrators | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMembersFilterAdministrators = new ChatMembersFilterAdministrators();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatMembersFilterAdministrators` into TDLib JSON wire shape. */
export function encodeChatMembersFilterAdministrators(value: ChatMembersFilterAdministrators): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMembersFilterAdministrators';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatMembersFilterBanned = ChatMembersFilter — Returns users banned from the chat; can be used only by administrators in a supergroup or in a channel */
export class ChatMembersFilterBanned {
  type: 'chatMembersFilterBanned' = 'chatMembersFilterBanned';
  extra?: string;
}

/** Decode a TDLib `chatMembersFilterBanned` object; null input -> null. */
export function decodeChatMembersFilterBanned(json: TdJsonRaw): ChatMembersFilterBanned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMembersFilterBanned = new ChatMembersFilterBanned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatMembersFilterBanned` into TDLib JSON wire shape. */
export function encodeChatMembersFilterBanned(value: ChatMembersFilterBanned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMembersFilterBanned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatMembersFilterBots = ChatMembersFilter — Returns bot members of the chat */
export class ChatMembersFilterBots {
  type: 'chatMembersFilterBots' = 'chatMembersFilterBots';
  extra?: string;
}

/** Decode a TDLib `chatMembersFilterBots` object; null input -> null. */
export function decodeChatMembersFilterBots(json: TdJsonRaw): ChatMembersFilterBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMembersFilterBots = new ChatMembersFilterBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatMembersFilterBots` into TDLib JSON wire shape. */
export function encodeChatMembersFilterBots(value: ChatMembersFilterBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMembersFilterBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatMembersFilterContacts = ChatMembersFilter — Returns contacts of the user */
export class ChatMembersFilterContacts {
  type: 'chatMembersFilterContacts' = 'chatMembersFilterContacts';
  extra?: string;
}

/** Decode a TDLib `chatMembersFilterContacts` object; null input -> null. */
export function decodeChatMembersFilterContacts(json: TdJsonRaw): ChatMembersFilterContacts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMembersFilterContacts = new ChatMembersFilterContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatMembersFilterContacts` into TDLib JSON wire shape. */
export function encodeChatMembersFilterContacts(value: ChatMembersFilterContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMembersFilterContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatMembersFilterMembers = ChatMembersFilter — Returns all chat members, including restricted chat members */
export class ChatMembersFilterMembers {
  type: 'chatMembersFilterMembers' = 'chatMembersFilterMembers';
  extra?: string;
}

/** Decode a TDLib `chatMembersFilterMembers` object; null input -> null. */
export function decodeChatMembersFilterMembers(json: TdJsonRaw): ChatMembersFilterMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMembersFilterMembers = new ChatMembersFilterMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatMembersFilterMembers` into TDLib JSON wire shape. */
export function encodeChatMembersFilterMembers(value: ChatMembersFilterMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMembersFilterMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatMembersFilterMention = ChatMembersFilter — Returns users who can be mentioned in the chat */
export class ChatMembersFilterMention {
  type: 'chatMembersFilterMention' = 'chatMembersFilterMention';
  extra?: string;
  /** topic_id: MessageTopic — Identifier of the topic in which the users will be mentioned; pass null if none */
  topic_id: TdMessageTopic | null = null;
}

/** Decode a TDLib `chatMembersFilterMention` object; null input -> null. */
export function decodeChatMembersFilterMention(json: TdJsonRaw): ChatMembersFilterMention | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMembersFilterMention = new ChatMembersFilterMention();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  return out;
}

/** Encode a `chatMembersFilterMention` into TDLib JSON wire shape. */
export function encodeChatMembersFilterMention(value: ChatMembersFilterMention): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMembersFilterMention';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  return out;
}

/** chatMembersFilterRestricted = ChatMembersFilter — Returns users under certain restrictions in the chat; can be used only by administrators in a supergroup */
export class ChatMembersFilterRestricted {
  type: 'chatMembersFilterRestricted' = 'chatMembersFilterRestricted';
  extra?: string;
}

/** Decode a TDLib `chatMembersFilterRestricted` object; null input -> null. */
export function decodeChatMembersFilterRestricted(json: TdJsonRaw): ChatMembersFilterRestricted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMembersFilterRestricted = new ChatMembersFilterRestricted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatMembersFilterRestricted` into TDLib JSON wire shape. */
export function encodeChatMembersFilterRestricted(value: ChatMembersFilterRestricted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMembersFilterRestricted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatMessageSender = ChatMessageSender — Represents a message sender, which can be used to send messages in a chat */
export class ChatMessageSender {
  type: 'chatMessageSender' = 'chatMessageSender';
  extra?: string;
  /** sender: MessageSender — The message sender */
  sender: TdMessageSender | null = null;
  /** needs_premium: Bool — True, if Telegram Premium is needed to use the message sender */
  needs_premium: boolean = false;
}

/** Decode a TDLib `chatMessageSender` object; null input -> null. */
export function decodeChatMessageSender(json: TdJsonRaw): ChatMessageSender | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMessageSender = new ChatMessageSender();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender = decodeTdMessageSender(record['sender']);
  out.needs_premium = asBool(record['needs_premium']);
  return out;
}

/** Encode a `chatMessageSender` into TDLib JSON wire shape. */
export function encodeChatMessageSender(value: ChatMessageSender): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMessageSender';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender'] = value.sender === null ? null : encodeTdMessageSender(value.sender);
  out['needs_premium'] = value.needs_premium;
  return out;
}

/** chatMessageSenders = ChatMessageSenders — Represents a list of message senders, which can be used to send messages in a chat */
export class ChatMessageSenders {
  type: 'chatMessageSenders' = 'chatMessageSenders';
  extra?: string;
  /** senders: vector<chatMessageSender> — List of available message senders */
  senders: ChatMessageSender[] = [];
}

/** Decode a TDLib `chatMessageSenders` object; null input -> null. */
export function decodeChatMessageSenders(json: TdJsonRaw): ChatMessageSenders | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatMessageSenders = new ChatMessageSenders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.senders = decodeVector(record['senders'], (item: TdJsonRaw): ChatMessageSender => decodeChatMessageSender(item) as ChatMessageSender);
  return out;
}

/** Encode a `chatMessageSenders` into TDLib JSON wire shape. */
export function encodeChatMessageSenders(value: ChatMessageSenders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatMessageSenders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['senders'] = encodeVector(value.senders, (item: ChatMessageSender | null): TdJsonRaw => item === null ? null : encodeChatMessageSender(item));
  return out;
}

/** chatNotificationSettings = ChatNotificationSettings — Contains information about notification settings for a chat or a forum topic */
export class ChatNotificationSettings {
  type: 'chatNotificationSettings' = 'chatNotificationSettings';
  extra?: string;
  /** use_default_mute_for: Bool — If true, the value for the relevant type of chat or the forum chat is used instead of mute_for */
  use_default_mute_for: boolean = false;
  /** mute_for: int32 — Time left before notifications will be unmuted, in seconds */
  mute_for: number = 0;
  /** use_default_sound: Bool — If true, the value for the relevant type of chat or the forum chat is used instead of sound_id */
  use_default_sound: boolean = false;
  /** sound_id: int64 — Identifier of the notification sound to be played for messages; 0 if sound is disabled */
  sound_id: string = '';
  /** use_default_show_preview: Bool — If true, the value for the relevant type of chat or the forum chat is used instead of show_preview */
  use_default_show_preview: boolean = false;
  /** show_preview: Bool — True, if message content must be displayed in notifications */
  show_preview: boolean = false;
  /** use_default_mute_stories: Bool — If true, the value for the relevant type of chat is used instead of mute_stories */
  use_default_mute_stories: boolean = false;
  /** mute_stories: Bool — True, if story notifications are disabled for the chat */
  mute_stories: boolean = false;
  /** use_default_story_sound: Bool — If true, the value for the relevant type of chat is used instead of story_sound_id */
  use_default_story_sound: boolean = false;
  /** story_sound_id: int64 — Identifier of the notification sound to be played for stories; 0 if sound is disabled */
  story_sound_id: string = '';
  /** use_default_show_story_poster: Bool — If true, the value for the relevant type of chat is used instead of show_story_poster */
  use_default_show_story_poster: boolean = false;
  /** show_story_poster: Bool — True, if the chat that posted a story must be displayed in notifications */
  show_story_poster: boolean = false;
  /** use_default_disable_pinned_message_notifications: Bool — If true, the value for the relevant type of chat or the forum chat is used instead of disable_pinned_message_notifications */
  use_default_disable_pinned_message_notifications: boolean = false;
  /** disable_pinned_message_notifications: Bool — If true, notifications for incoming pinned messages will be created as for an ordinary unread message */
  disable_pinned_message_notifications: boolean = false;
  /** use_default_disable_mention_notifications: Bool — If true, the value for the relevant type of chat or the forum chat is used instead of disable_mention_notifications */
  use_default_disable_mention_notifications: boolean = false;
  /** disable_mention_notifications: Bool — If true, notifications for messages with mentions will be created as for an ordinary unread message */
  disable_mention_notifications: boolean = false;
}

/** Decode a TDLib `chatNotificationSettings` object; null input -> null. */
export function decodeChatNotificationSettings(json: TdJsonRaw): ChatNotificationSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatNotificationSettings = new ChatNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.use_default_mute_for = asBool(record['use_default_mute_for']);
  out.mute_for = asNumber(record['mute_for']);
  out.use_default_sound = asBool(record['use_default_sound']);
  out.sound_id = asInt64(record['sound_id']);
  out.use_default_show_preview = asBool(record['use_default_show_preview']);
  out.show_preview = asBool(record['show_preview']);
  out.use_default_mute_stories = asBool(record['use_default_mute_stories']);
  out.mute_stories = asBool(record['mute_stories']);
  out.use_default_story_sound = asBool(record['use_default_story_sound']);
  out.story_sound_id = asInt64(record['story_sound_id']);
  out.use_default_show_story_poster = asBool(record['use_default_show_story_poster']);
  out.show_story_poster = asBool(record['show_story_poster']);
  out.use_default_disable_pinned_message_notifications = asBool(record['use_default_disable_pinned_message_notifications']);
  out.disable_pinned_message_notifications = asBool(record['disable_pinned_message_notifications']);
  out.use_default_disable_mention_notifications = asBool(record['use_default_disable_mention_notifications']);
  out.disable_mention_notifications = asBool(record['disable_mention_notifications']);
  return out;
}

/** Encode a `chatNotificationSettings` into TDLib JSON wire shape. */
export function encodeChatNotificationSettings(value: ChatNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['use_default_mute_for'] = value.use_default_mute_for;
  out['mute_for'] = value.mute_for;
  out['use_default_sound'] = value.use_default_sound;
  out['sound_id'] = value.sound_id;
  out['use_default_show_preview'] = value.use_default_show_preview;
  out['show_preview'] = value.show_preview;
  out['use_default_mute_stories'] = value.use_default_mute_stories;
  out['mute_stories'] = value.mute_stories;
  out['use_default_story_sound'] = value.use_default_story_sound;
  out['story_sound_id'] = value.story_sound_id;
  out['use_default_show_story_poster'] = value.use_default_show_story_poster;
  out['show_story_poster'] = value.show_story_poster;
  out['use_default_disable_pinned_message_notifications'] = value.use_default_disable_pinned_message_notifications;
  out['disable_pinned_message_notifications'] = value.disable_pinned_message_notifications;
  out['use_default_disable_mention_notifications'] = value.use_default_disable_mention_notifications;
  out['disable_mention_notifications'] = value.disable_mention_notifications;
  return out;
}

/** chatPermissions = ChatPermissions — Describes actions that a user is allowed to take in a chat */
export class ChatPermissions {
  type: 'chatPermissions' = 'chatPermissions';
  extra?: string;
  /** can_send_basic_messages: Bool — True, if the user can send text messages, rich messages, contacts, giveaways, giveaway winners, invoices, locations, and venues */
  can_send_basic_messages: boolean = false;
  /** can_send_audios: Bool — True, if the user can send music files */
  can_send_audios: boolean = false;
  /** can_send_documents: Bool — True, if the user can send documents */
  can_send_documents: boolean = false;
  /** can_send_photos: Bool — True, if the user can send photos */
  can_send_photos: boolean = false;
  /** can_send_videos: Bool — True, if the user can send videos */
  can_send_videos: boolean = false;
  /** can_send_video_notes: Bool — True, if the user can send video notes */
  can_send_video_notes: boolean = false;
  /** can_send_voice_notes: Bool — True, if the user can send voice notes */
  can_send_voice_notes: boolean = false;
  /** can_send_polls: Bool — True, if the user can send polls and checklists */
  can_send_polls: boolean = false;
  /** can_send_other_messages: Bool — True, if the user can send animations, games, stickers, and dice and use inline bots */
  can_send_other_messages: boolean = false;
  /** can_add_link_previews: Bool — True, if the user may add a link preview to their messages */
  can_add_link_previews: boolean = false;
  /** can_react_to_messages: Bool — True, if the user can react to messages */
  can_react_to_messages: boolean = false;
  /** can_edit_tag: Bool — True, if the user may change the tag of self */
  can_edit_tag: boolean = false;
  /** can_change_info: Bool — True, if the user can change the chat title, photo, and other settings */
  can_change_info: boolean = false;
  /** can_invite_users: Bool — True, if the user can invite new users to the chat */
  can_invite_users: boolean = false;
  /** can_pin_messages: Bool — True, if the user can pin messages */
  can_pin_messages: boolean = false;
  /** can_create_topics: Bool — True, if the user can create topics */
  can_create_topics: boolean = false;
}

/** Decode a TDLib `chatPermissions` object; null input -> null. */
export function decodeChatPermissions(json: TdJsonRaw): ChatPermissions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatPermissions = new ChatPermissions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_send_basic_messages = asBool(record['can_send_basic_messages']);
  out.can_send_audios = asBool(record['can_send_audios']);
  out.can_send_documents = asBool(record['can_send_documents']);
  out.can_send_photos = asBool(record['can_send_photos']);
  out.can_send_videos = asBool(record['can_send_videos']);
  out.can_send_video_notes = asBool(record['can_send_video_notes']);
  out.can_send_voice_notes = asBool(record['can_send_voice_notes']);
  out.can_send_polls = asBool(record['can_send_polls']);
  out.can_send_other_messages = asBool(record['can_send_other_messages']);
  out.can_add_link_previews = asBool(record['can_add_link_previews']);
  out.can_react_to_messages = asBool(record['can_react_to_messages']);
  out.can_edit_tag = asBool(record['can_edit_tag']);
  out.can_change_info = asBool(record['can_change_info']);
  out.can_invite_users = asBool(record['can_invite_users']);
  out.can_pin_messages = asBool(record['can_pin_messages']);
  out.can_create_topics = asBool(record['can_create_topics']);
  return out;
}

/** Encode a `chatPermissions` into TDLib JSON wire shape. */
export function encodeChatPermissions(value: ChatPermissions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatPermissions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_send_basic_messages'] = value.can_send_basic_messages;
  out['can_send_audios'] = value.can_send_audios;
  out['can_send_documents'] = value.can_send_documents;
  out['can_send_photos'] = value.can_send_photos;
  out['can_send_videos'] = value.can_send_videos;
  out['can_send_video_notes'] = value.can_send_video_notes;
  out['can_send_voice_notes'] = value.can_send_voice_notes;
  out['can_send_polls'] = value.can_send_polls;
  out['can_send_other_messages'] = value.can_send_other_messages;
  out['can_add_link_previews'] = value.can_add_link_previews;
  out['can_react_to_messages'] = value.can_react_to_messages;
  out['can_edit_tag'] = value.can_edit_tag;
  out['can_change_info'] = value.can_change_info;
  out['can_invite_users'] = value.can_invite_users;
  out['can_pin_messages'] = value.can_pin_messages;
  out['can_create_topics'] = value.can_create_topics;
  return out;
}

/** chatPhoto = ChatPhoto — Describes a chat or user profile photo */
export class ChatPhoto {
  type: 'chatPhoto' = 'chatPhoto';
  extra?: string;
  /** id: int64 — Unique photo identifier */
  id: string = '';
  /** added_date: int32 — Point in time (Unix timestamp) when the photo has been added */
  added_date: number = 0;
  /** minithumbnail: minithumbnail — Photo minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
  /** sizes: vector<photoSize> — Available variants of the photo in JPEG format, in different size */
  sizes: PhotoSize[] = [];
  /** animation: animatedChatPhoto — A big (up to 1280x1280) animated variant of the photo in MPEG4 format; may be null */
  animation: AnimatedChatPhoto | null = null;
  /** small_animation: animatedChatPhoto — A small (160x160) animated variant of the photo in MPEG4 format; may be null even if the big animation is available */
  small_animation: AnimatedChatPhoto | null = null;
  /** sticker: chatPhotoSticker — Sticker-based version of the chat photo; may be null */
  sticker: ChatPhotoSticker | null = null;
}

/** Decode a TDLib `chatPhoto` object; null input -> null. */
export function decodeChatPhoto(json: TdJsonRaw): ChatPhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatPhoto = new ChatPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.added_date = asNumber(record['added_date']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  out.sizes = decodeVector(record['sizes'], (item: TdJsonRaw): PhotoSize => decodePhotoSize(item) as PhotoSize);
  out.animation = decodeAnimatedChatPhoto(record['animation']);
  out.small_animation = decodeAnimatedChatPhoto(record['small_animation']);
  out.sticker = decodeChatPhotoSticker(record['sticker']);
  return out;
}

/** Encode a `chatPhoto` into TDLib JSON wire shape. */
export function encodeChatPhoto(value: ChatPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['added_date'] = value.added_date;
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  out['sizes'] = encodeVector(value.sizes, (item: PhotoSize | null): TdJsonRaw => item === null ? null : encodePhotoSize(item));
  out['animation'] = value.animation === null ? null : encodeAnimatedChatPhoto(value.animation);
  out['small_animation'] = value.small_animation === null ? null : encodeAnimatedChatPhoto(value.small_animation);
  out['sticker'] = value.sticker === null ? null : encodeChatPhotoSticker(value.sticker);
  return out;
}

/** chatPhotoInfo = ChatPhotoInfo — Contains basic information about the photo of a chat */
export class ChatPhotoInfo {
  type: 'chatPhotoInfo' = 'chatPhotoInfo';
  extra?: string;
  /** small: file — A small (160x160) chat photo variant in JPEG format. The file can be downloaded only before the photo is changed */
  small: File | null = null;
  /** big: file — A big (640x640) chat photo variant in JPEG format. The file can be downloaded only before the photo is changed */
  big: File | null = null;
  /** minithumbnail: minithumbnail — Chat photo minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
  /** has_animation: Bool — True, if the photo has animated variant */
  has_animation: boolean = false;
  /** is_personal: Bool — True, if the photo is visible only for the current user */
  is_personal: boolean = false;
}

/** Decode a TDLib `chatPhotoInfo` object; null input -> null. */
export function decodeChatPhotoInfo(json: TdJsonRaw): ChatPhotoInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatPhotoInfo = new ChatPhotoInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.small = decodeFile(record['small']);
  out.big = decodeFile(record['big']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  out.has_animation = asBool(record['has_animation']);
  out.is_personal = asBool(record['is_personal']);
  return out;
}

/** Encode a `chatPhotoInfo` into TDLib JSON wire shape. */
export function encodeChatPhotoInfo(value: ChatPhotoInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatPhotoInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['small'] = value.small === null ? null : encodeFile(value.small);
  out['big'] = value.big === null ? null : encodeFile(value.big);
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  out['has_animation'] = value.has_animation;
  out['is_personal'] = value.is_personal;
  return out;
}

/** chatPhotoSticker = ChatPhotoSticker — Information about the sticker, which was used to create the chat photo. The sticker is shown at the center of the photo and occupies at most 67% of it */
export class ChatPhotoSticker {
  type: 'chatPhotoSticker' = 'chatPhotoSticker';
  extra?: string;
  /** type: ChatPhotoStickerType — Type of the sticker */
  type_: TdChatPhotoStickerType | null = null;
  /** background_fill: BackgroundFill — The fill to be used as background for the sticker; rotation angle in backgroundFillGradient isn't supported */
  background_fill: TdBackgroundFill | null = null;
}

/** Decode a TDLib `chatPhotoSticker` object; null input -> null. */
export function decodeChatPhotoSticker(json: TdJsonRaw): ChatPhotoSticker | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatPhotoSticker = new ChatPhotoSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdChatPhotoStickerType(record['type']);
  out.background_fill = decodeTdBackgroundFill(record['background_fill']);
  return out;
}

/** Encode a `chatPhotoSticker` into TDLib JSON wire shape. */
export function encodeChatPhotoSticker(value: ChatPhotoSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatPhotoSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdChatPhotoStickerType(value.type_);
  out['background_fill'] = value.background_fill === null ? null : encodeTdBackgroundFill(value.background_fill);
  return out;
}

/** chatPhotoStickerTypeCustomEmoji = ChatPhotoStickerType — Information about the custom emoji, which was used to create the chat photo */
export class ChatPhotoStickerTypeCustomEmoji {
  type: 'chatPhotoStickerTypeCustomEmoji' = 'chatPhotoStickerTypeCustomEmoji';
  extra?: string;
  /** custom_emoji_id: int64 — Identifier of the custom emoji */
  custom_emoji_id: string = '';
}

/** Decode a TDLib `chatPhotoStickerTypeCustomEmoji` object; null input -> null. */
export function decodeChatPhotoStickerTypeCustomEmoji(json: TdJsonRaw): ChatPhotoStickerTypeCustomEmoji | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatPhotoStickerTypeCustomEmoji = new ChatPhotoStickerTypeCustomEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  return out;
}

/** Encode a `chatPhotoStickerTypeCustomEmoji` into TDLib JSON wire shape. */
export function encodeChatPhotoStickerTypeCustomEmoji(value: ChatPhotoStickerTypeCustomEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatPhotoStickerTypeCustomEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_emoji_id'] = value.custom_emoji_id;
  return out;
}

/** chatPhotoStickerTypeRegularOrMask = ChatPhotoStickerType — Information about the sticker, which was used to create the chat photo */
export class ChatPhotoStickerTypeRegularOrMask {
  type: 'chatPhotoStickerTypeRegularOrMask' = 'chatPhotoStickerTypeRegularOrMask';
  extra?: string;
  /** sticker_set_id: int64 — Sticker set identifier */
  sticker_set_id: string = '';
  /** sticker_id: int64 — Identifier of the sticker in the set */
  sticker_id: string = '';
}

/** Decode a TDLib `chatPhotoStickerTypeRegularOrMask` object; null input -> null. */
export function decodeChatPhotoStickerTypeRegularOrMask(json: TdJsonRaw): ChatPhotoStickerTypeRegularOrMask | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatPhotoStickerTypeRegularOrMask = new ChatPhotoStickerTypeRegularOrMask();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_set_id = asInt64(record['sticker_set_id']);
  out.sticker_id = asInt64(record['sticker_id']);
  return out;
}

/** Encode a `chatPhotoStickerTypeRegularOrMask` into TDLib JSON wire shape. */
export function encodeChatPhotoStickerTypeRegularOrMask(value: ChatPhotoStickerTypeRegularOrMask): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatPhotoStickerTypeRegularOrMask';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_set_id'] = value.sticker_set_id;
  out['sticker_id'] = value.sticker_id;
  return out;
}

/** chatPhotos = ChatPhotos — Contains a list of chat or user profile photos */
export class ChatPhotos {
  type: 'chatPhotos' = 'chatPhotos';
  extra?: string;
  /** total_count: int32 — Total number of photos */
  total_count: number = 0;
  /** photos: vector<chatPhoto> — List of photos */
  photos: ChatPhoto[] = [];
}

/** Decode a TDLib `chatPhotos` object; null input -> null. */
export function decodeChatPhotos(json: TdJsonRaw): ChatPhotos | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatPhotos = new ChatPhotos();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.photos = decodeVector(record['photos'], (item: TdJsonRaw): ChatPhoto => decodeChatPhoto(item) as ChatPhoto);
  return out;
}

/** Encode a `chatPhotos` into TDLib JSON wire shape. */
export function encodeChatPhotos(value: ChatPhotos): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatPhotos';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['photos'] = encodeVector(value.photos, (item: ChatPhoto | null): TdJsonRaw => item === null ? null : encodeChatPhoto(item));
  return out;
}

/** chatPosition = ChatPosition — Describes a position of a chat in a chat list */
export class ChatPosition {
  type: 'chatPosition' = 'chatPosition';
  extra?: string;
  /** list: ChatList — The chat list */
  list: TdChatList | null = null;
  /** order: int64 — A parameter used to determine order of the chat in the chat list. Chats must be sorted by the pair (order, chat.id) in descending order */
  order: string = '';
  /** is_pinned: Bool — True, if the chat is pinned in the chat list */
  is_pinned: boolean = false;
  /** source: ChatSource — Source of the chat in the chat list; may be null */
  source: TdChatSource | null = null;
}

/** Decode a TDLib `chatPosition` object; null input -> null. */
export function decodeChatPosition(json: TdJsonRaw): ChatPosition | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatPosition = new ChatPosition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.list = decodeTdChatList(record['list']);
  out.order = asInt64(record['order']);
  out.is_pinned = asBool(record['is_pinned']);
  out.source = decodeTdChatSource(record['source']);
  return out;
}

/** Encode a `chatPosition` into TDLib JSON wire shape. */
export function encodeChatPosition(value: ChatPosition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatPosition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['list'] = value.list === null ? null : encodeTdChatList(value.list);
  out['order'] = value.order;
  out['is_pinned'] = value.is_pinned;
  out['source'] = value.source === null ? null : encodeTdChatSource(value.source);
  return out;
}

/** chatRevenueAmount = ChatRevenueAmount — Contains information about revenue earned from sponsored messages in a chat */
export class ChatRevenueAmount {
  type: 'chatRevenueAmount' = 'chatRevenueAmount';
  extra?: string;
  /** cryptocurrency: string — Cryptocurrency in which revenue is calculated */
  cryptocurrency: string = '';
  /** total_amount: int64 — Total amount of the cryptocurrency earned, in the smallest units of the cryptocurrency */
  total_amount: string = '';
  /** balance_amount: int64 — Amount of the cryptocurrency that isn't withdrawn yet, in the smallest units of the cryptocurrency */
  balance_amount: string = '';
  /** available_amount: int64 — Amount of the cryptocurrency available for withdrawal, in the smallest units of the cryptocurrency */
  available_amount: string = '';
  /** withdrawal_enabled: Bool — True, if Telegram Stars can be withdrawn now or later */
  withdrawal_enabled: boolean = false;
}

/** Decode a TDLib `chatRevenueAmount` object; null input -> null. */
export function decodeChatRevenueAmount(json: TdJsonRaw): ChatRevenueAmount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatRevenueAmount = new ChatRevenueAmount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.cryptocurrency = asString(record['cryptocurrency']);
  out.total_amount = asInt64(record['total_amount']);
  out.balance_amount = asInt64(record['balance_amount']);
  out.available_amount = asInt64(record['available_amount']);
  out.withdrawal_enabled = asBool(record['withdrawal_enabled']);
  return out;
}

/** Encode a `chatRevenueAmount` into TDLib JSON wire shape. */
export function encodeChatRevenueAmount(value: ChatRevenueAmount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatRevenueAmount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['cryptocurrency'] = value.cryptocurrency;
  out['total_amount'] = value.total_amount;
  out['balance_amount'] = value.balance_amount;
  out['available_amount'] = value.available_amount;
  out['withdrawal_enabled'] = value.withdrawal_enabled;
  return out;
}

/** chatRevenueStatistics = ChatRevenueStatistics — A detailed statistics about revenue earned from sponsored messages in a chat */
export class ChatRevenueStatistics {
  type: 'chatRevenueStatistics' = 'chatRevenueStatistics';
  extra?: string;
  /** revenue_by_hour_graph: StatisticalGraph — A graph containing amount of revenue in a given hour */
  revenue_by_hour_graph: TdStatisticalGraph | null = null;
  /** revenue_graph: StatisticalGraph — A graph containing amount of revenue */
  revenue_graph: TdStatisticalGraph | null = null;
  /** revenue_amount: chatRevenueAmount — Amount of earned revenue */
  revenue_amount: ChatRevenueAmount | null = null;
  /** usd_rate: double — Current conversion rate of the cryptocurrency in which revenue is calculated to USD */
  usd_rate: number = 0;
}

/** Decode a TDLib `chatRevenueStatistics` object; null input -> null. */
export function decodeChatRevenueStatistics(json: TdJsonRaw): ChatRevenueStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatRevenueStatistics = new ChatRevenueStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.revenue_by_hour_graph = decodeTdStatisticalGraph(record['revenue_by_hour_graph']);
  out.revenue_graph = decodeTdStatisticalGraph(record['revenue_graph']);
  out.revenue_amount = decodeChatRevenueAmount(record['revenue_amount']);
  out.usd_rate = asNumber(record['usd_rate']);
  return out;
}

/** Encode a `chatRevenueStatistics` into TDLib JSON wire shape. */
export function encodeChatRevenueStatistics(value: ChatRevenueStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatRevenueStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['revenue_by_hour_graph'] = value.revenue_by_hour_graph === null ? null : encodeTdStatisticalGraph(value.revenue_by_hour_graph);
  out['revenue_graph'] = value.revenue_graph === null ? null : encodeTdStatisticalGraph(value.revenue_graph);
  out['revenue_amount'] = value.revenue_amount === null ? null : encodeChatRevenueAmount(value.revenue_amount);
  out['usd_rate'] = value.usd_rate;
  return out;
}

/** chatRevenueTransaction = ChatRevenueTransaction — Contains a chat revenue transactions */
export class ChatRevenueTransaction {
  type: 'chatRevenueTransaction' = 'chatRevenueTransaction';
  extra?: string;
  /** cryptocurrency: string — Cryptocurrency in which revenue is calculated */
  cryptocurrency: string = '';
  /** cryptocurrency_amount: int64 — The withdrawn amount, in the smallest units of the cryptocurrency */
  cryptocurrency_amount: string = '';
  /** type: ChatRevenueTransactionType — Type of the transaction */
  type_: TdChatRevenueTransactionType | null = null;
}

/** Decode a TDLib `chatRevenueTransaction` object; null input -> null. */
export function decodeChatRevenueTransaction(json: TdJsonRaw): ChatRevenueTransaction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatRevenueTransaction = new ChatRevenueTransaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.cryptocurrency = asString(record['cryptocurrency']);
  out.cryptocurrency_amount = asInt64(record['cryptocurrency_amount']);
  out.type_ = decodeTdChatRevenueTransactionType(record['type']);
  return out;
}

/** Encode a `chatRevenueTransaction` into TDLib JSON wire shape. */
export function encodeChatRevenueTransaction(value: ChatRevenueTransaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatRevenueTransaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['cryptocurrency'] = value.cryptocurrency;
  out['cryptocurrency_amount'] = value.cryptocurrency_amount;
  out['type'] = value.type_ === null ? null : encodeTdChatRevenueTransactionType(value.type_);
  return out;
}

/** chatRevenueTransactionTypeFragmentRefund = ChatRevenueTransactionType — Describes a refund for failed withdrawal of earnings through Fragment */
export class ChatRevenueTransactionTypeFragmentRefund {
  type: 'chatRevenueTransactionTypeFragmentRefund' = 'chatRevenueTransactionTypeFragmentRefund';
  extra?: string;
  /** refund_date: int32 — Point in time (Unix timestamp) when the transaction was refunded */
  refund_date: number = 0;
}

/** Decode a TDLib `chatRevenueTransactionTypeFragmentRefund` object; null input -> null. */
export function decodeChatRevenueTransactionTypeFragmentRefund(json: TdJsonRaw): ChatRevenueTransactionTypeFragmentRefund | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatRevenueTransactionTypeFragmentRefund = new ChatRevenueTransactionTypeFragmentRefund();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.refund_date = asNumber(record['refund_date']);
  return out;
}

/** Encode a `chatRevenueTransactionTypeFragmentRefund` into TDLib JSON wire shape. */
export function encodeChatRevenueTransactionTypeFragmentRefund(value: ChatRevenueTransactionTypeFragmentRefund): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatRevenueTransactionTypeFragmentRefund';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['refund_date'] = value.refund_date;
  return out;
}

/** chatRevenueTransactionTypeFragmentWithdrawal = ChatRevenueTransactionType — Describes a withdrawal of earnings through Fragment */
export class ChatRevenueTransactionTypeFragmentWithdrawal {
  type: 'chatRevenueTransactionTypeFragmentWithdrawal' = 'chatRevenueTransactionTypeFragmentWithdrawal';
  extra?: string;
  /** withdrawal_date: int32 — Point in time (Unix timestamp) when the earnings withdrawal started */
  withdrawal_date: number = 0;
  /** state: RevenueWithdrawalState — State of the withdrawal */
  state: TdRevenueWithdrawalState | null = null;
}

/** Decode a TDLib `chatRevenueTransactionTypeFragmentWithdrawal` object; null input -> null. */
export function decodeChatRevenueTransactionTypeFragmentWithdrawal(json: TdJsonRaw): ChatRevenueTransactionTypeFragmentWithdrawal | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatRevenueTransactionTypeFragmentWithdrawal = new ChatRevenueTransactionTypeFragmentWithdrawal();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.withdrawal_date = asNumber(record['withdrawal_date']);
  out.state = decodeTdRevenueWithdrawalState(record['state']);
  return out;
}

/** Encode a `chatRevenueTransactionTypeFragmentWithdrawal` into TDLib JSON wire shape. */
export function encodeChatRevenueTransactionTypeFragmentWithdrawal(value: ChatRevenueTransactionTypeFragmentWithdrawal): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatRevenueTransactionTypeFragmentWithdrawal';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['withdrawal_date'] = value.withdrawal_date;
  out['state'] = value.state === null ? null : encodeTdRevenueWithdrawalState(value.state);
  return out;
}

/** chatRevenueTransactionTypeSponsoredMessageEarnings = ChatRevenueTransactionType — Describes earnings from sponsored messages in a chat in some time frame */
export class ChatRevenueTransactionTypeSponsoredMessageEarnings {
  type: 'chatRevenueTransactionTypeSponsoredMessageEarnings' = 'chatRevenueTransactionTypeSponsoredMessageEarnings';
  extra?: string;
  /** start_date: int32 — Point in time (Unix timestamp) when the earnings started */
  start_date: number = 0;
  /** end_date: int32 — Point in time (Unix timestamp) when the earnings ended */
  end_date: number = 0;
}

/** Decode a TDLib `chatRevenueTransactionTypeSponsoredMessageEarnings` object; null input -> null. */
export function decodeChatRevenueTransactionTypeSponsoredMessageEarnings(json: TdJsonRaw): ChatRevenueTransactionTypeSponsoredMessageEarnings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatRevenueTransactionTypeSponsoredMessageEarnings = new ChatRevenueTransactionTypeSponsoredMessageEarnings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.start_date = asNumber(record['start_date']);
  out.end_date = asNumber(record['end_date']);
  return out;
}

/** Encode a `chatRevenueTransactionTypeSponsoredMessageEarnings` into TDLib JSON wire shape. */
export function encodeChatRevenueTransactionTypeSponsoredMessageEarnings(value: ChatRevenueTransactionTypeSponsoredMessageEarnings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatRevenueTransactionTypeSponsoredMessageEarnings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['start_date'] = value.start_date;
  out['end_date'] = value.end_date;
  return out;
}

/** chatRevenueTransactionTypeSuggestedPostEarnings = ChatRevenueTransactionType — Describes earnings from a published suggested post */
export class ChatRevenueTransactionTypeSuggestedPostEarnings {
  type: 'chatRevenueTransactionTypeSuggestedPostEarnings' = 'chatRevenueTransactionTypeSuggestedPostEarnings';
  extra?: string;
  /** user_id: int53 — Identifier of the user who paid for the suggested post */
  user_id: number = 0;
}

/** Decode a TDLib `chatRevenueTransactionTypeSuggestedPostEarnings` object; null input -> null. */
export function decodeChatRevenueTransactionTypeSuggestedPostEarnings(json: TdJsonRaw): ChatRevenueTransactionTypeSuggestedPostEarnings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatRevenueTransactionTypeSuggestedPostEarnings = new ChatRevenueTransactionTypeSuggestedPostEarnings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `chatRevenueTransactionTypeSuggestedPostEarnings` into TDLib JSON wire shape. */
export function encodeChatRevenueTransactionTypeSuggestedPostEarnings(value: ChatRevenueTransactionTypeSuggestedPostEarnings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatRevenueTransactionTypeSuggestedPostEarnings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** chatRevenueTransactionTypeUnsupported = ChatRevenueTransactionType — Describes an unsupported transaction */
export class ChatRevenueTransactionTypeUnsupported {
  type: 'chatRevenueTransactionTypeUnsupported' = 'chatRevenueTransactionTypeUnsupported';
  extra?: string;
}

/** Decode a TDLib `chatRevenueTransactionTypeUnsupported` object; null input -> null. */
export function decodeChatRevenueTransactionTypeUnsupported(json: TdJsonRaw): ChatRevenueTransactionTypeUnsupported | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatRevenueTransactionTypeUnsupported = new ChatRevenueTransactionTypeUnsupported();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatRevenueTransactionTypeUnsupported` into TDLib JSON wire shape. */
export function encodeChatRevenueTransactionTypeUnsupported(value: ChatRevenueTransactionTypeUnsupported): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatRevenueTransactionTypeUnsupported';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatRevenueTransactions = ChatRevenueTransactions — Contains a list of chat revenue transactions */
export class ChatRevenueTransactions {
  type: 'chatRevenueTransactions' = 'chatRevenueTransactions';
  extra?: string;
  /** gram_amount: int53 — The amount of owned TON Grams; in the smallest units of the cryptocurrency */
  gram_amount: number = 0;
  /** transactions: vector<chatRevenueTransaction> — List of transactions */
  transactions: ChatRevenueTransaction[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `chatRevenueTransactions` object; null input -> null. */
export function decodeChatRevenueTransactions(json: TdJsonRaw): ChatRevenueTransactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatRevenueTransactions = new ChatRevenueTransactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gram_amount = asNumber(record['gram_amount']);
  out.transactions = decodeVector(record['transactions'], (item: TdJsonRaw): ChatRevenueTransaction => decodeChatRevenueTransaction(item) as ChatRevenueTransaction);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `chatRevenueTransactions` into TDLib JSON wire shape. */
export function encodeChatRevenueTransactions(value: ChatRevenueTransactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatRevenueTransactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gram_amount'] = value.gram_amount;
  out['transactions'] = encodeVector(value.transactions, (item: ChatRevenueTransaction | null): TdJsonRaw => item === null ? null : encodeChatRevenueTransaction(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** chatSourceMtprotoProxy = ChatSource — The chat is sponsored by the user's MTProxy server */
export class ChatSourceMtprotoProxy {
  type: 'chatSourceMtprotoProxy' = 'chatSourceMtprotoProxy';
  extra?: string;
}

/** Decode a TDLib `chatSourceMtprotoProxy` object; null input -> null. */
export function decodeChatSourceMtprotoProxy(json: TdJsonRaw): ChatSourceMtprotoProxy | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatSourceMtprotoProxy = new ChatSourceMtprotoProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `chatSourceMtprotoProxy` into TDLib JSON wire shape. */
export function encodeChatSourceMtprotoProxy(value: ChatSourceMtprotoProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatSourceMtprotoProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** chatSourcePublicServiceAnnouncement = ChatSource — The chat contains a public service announcement */
export class ChatSourcePublicServiceAnnouncement {
  type: 'chatSourcePublicServiceAnnouncement' = 'chatSourcePublicServiceAnnouncement';
  extra?: string;
  /** type: string — The type of the announcement */
  type_: string = '';
  /** text: string — The text of the announcement */
  text: string = '';
}

/** Decode a TDLib `chatSourcePublicServiceAnnouncement` object; null input -> null. */
export function decodeChatSourcePublicServiceAnnouncement(json: TdJsonRaw): ChatSourcePublicServiceAnnouncement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatSourcePublicServiceAnnouncement = new ChatSourcePublicServiceAnnouncement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = asString(record['type']);
  out.text = asString(record['text']);
  return out;
}

/** Encode a `chatSourcePublicServiceAnnouncement` into TDLib JSON wire shape. */
export function encodeChatSourcePublicServiceAnnouncement(value: ChatSourcePublicServiceAnnouncement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatSourcePublicServiceAnnouncement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_;
  out['text'] = value.text;
  return out;
}

/** chatStatisticsAdministratorActionsInfo = ChatStatisticsAdministratorActionsInfo — Contains statistics about administrator actions done by a user */
export class ChatStatisticsAdministratorActionsInfo {
  type: 'chatStatisticsAdministratorActionsInfo' = 'chatStatisticsAdministratorActionsInfo';
  extra?: string;
  /** user_id: int53 — Administrator user identifier */
  user_id: number = 0;
  /** deleted_message_count: int32 — Number of messages deleted by the administrator */
  deleted_message_count: number = 0;
  /** banned_user_count: int32 — Number of users banned by the administrator */
  banned_user_count: number = 0;
  /** restricted_user_count: int32 — Number of users restricted by the administrator */
  restricted_user_count: number = 0;
}

/** Decode a TDLib `chatStatisticsAdministratorActionsInfo` object; null input -> null. */
export function decodeChatStatisticsAdministratorActionsInfo(json: TdJsonRaw): ChatStatisticsAdministratorActionsInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatStatisticsAdministratorActionsInfo = new ChatStatisticsAdministratorActionsInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.deleted_message_count = asNumber(record['deleted_message_count']);
  out.banned_user_count = asNumber(record['banned_user_count']);
  out.restricted_user_count = asNumber(record['restricted_user_count']);
  return out;
}

/** Encode a `chatStatisticsAdministratorActionsInfo` into TDLib JSON wire shape. */
export function encodeChatStatisticsAdministratorActionsInfo(value: ChatStatisticsAdministratorActionsInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatStatisticsAdministratorActionsInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['deleted_message_count'] = value.deleted_message_count;
  out['banned_user_count'] = value.banned_user_count;
  out['restricted_user_count'] = value.restricted_user_count;
  return out;
}

/** chatStatisticsChannel = ChatStatistics — A detailed statistics about a channel chat */
export class ChatStatisticsChannel {
  type: 'chatStatisticsChannel' = 'chatStatisticsChannel';
  extra?: string;
  /** period: dateRange — A period to which the statistics applies */
  period: DateRange | null = null;
  /** member_count: statisticalValue — Number of members in the chat */
  member_count: StatisticalValue | null = null;
  /** mean_message_view_count: statisticalValue — Mean number of times the recently sent messages were viewed */
  mean_message_view_count: StatisticalValue | null = null;
  /** mean_message_share_count: statisticalValue — Mean number of times the recently sent messages were shared */
  mean_message_share_count: StatisticalValue | null = null;
  /** mean_message_reaction_count: statisticalValue — Mean number of times reactions were added to the recently sent messages */
  mean_message_reaction_count: StatisticalValue | null = null;
  /** mean_story_view_count: statisticalValue — Mean number of times the recently posted stories were viewed */
  mean_story_view_count: StatisticalValue | null = null;
  /** mean_story_share_count: statisticalValue — Mean number of times the recently posted stories were shared */
  mean_story_share_count: StatisticalValue | null = null;
  /** mean_story_reaction_count: statisticalValue — Mean number of times reactions were added to the recently posted stories */
  mean_story_reaction_count: StatisticalValue | null = null;
  /** enabled_notifications_percentage: double — A percentage of users with enabled notifications for the chat; 0-100 */
  enabled_notifications_percentage: number = 0;
  /** member_count_graph: StatisticalGraph — A graph containing number of members in the chat */
  member_count_graph: TdStatisticalGraph | null = null;
  /** join_graph: StatisticalGraph — A graph containing number of members joined and left the chat */
  join_graph: TdStatisticalGraph | null = null;
  /** mute_graph: StatisticalGraph — A graph containing number of members muted and unmuted the chat */
  mute_graph: TdStatisticalGraph | null = null;
  /** view_count_by_hour_graph: StatisticalGraph — A graph containing number of message views in a given hour in the last two weeks */
  view_count_by_hour_graph: TdStatisticalGraph | null = null;
  /** view_count_by_source_graph: StatisticalGraph — A graph containing number of message views per source */
  view_count_by_source_graph: TdStatisticalGraph | null = null;
  /** join_by_source_graph: StatisticalGraph — A graph containing number of new member joins per source */
  join_by_source_graph: TdStatisticalGraph | null = null;
  /** language_graph: StatisticalGraph — A graph containing number of users viewed chat messages per language */
  language_graph: TdStatisticalGraph | null = null;
  /** message_interaction_graph: StatisticalGraph — A graph containing number of chat message views and shares */
  message_interaction_graph: TdStatisticalGraph | null = null;
  /** message_reaction_graph: StatisticalGraph — A graph containing number of reactions on messages */
  message_reaction_graph: TdStatisticalGraph | null = null;
  /** story_interaction_graph: StatisticalGraph — A graph containing number of story views and shares */
  story_interaction_graph: TdStatisticalGraph | null = null;
  /** story_reaction_graph: StatisticalGraph — A graph containing number of reactions on stories */
  story_reaction_graph: TdStatisticalGraph | null = null;
  /** instant_view_interaction_graph: StatisticalGraph — A graph containing number of views of associated with the chat instant views */
  instant_view_interaction_graph: TdStatisticalGraph | null = null;
  /** recent_interactions: vector<chatStatisticsInteractionInfo> — Detailed statistics about number of views and shares of recently sent messages and posted stories */
  recent_interactions: ChatStatisticsInteractionInfo[] = [];
}

/** Decode a TDLib `chatStatisticsChannel` object; null input -> null. */
export function decodeChatStatisticsChannel(json: TdJsonRaw): ChatStatisticsChannel | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatStatisticsChannel = new ChatStatisticsChannel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.period = decodeDateRange(record['period']);
  out.member_count = decodeStatisticalValue(record['member_count']);
  out.mean_message_view_count = decodeStatisticalValue(record['mean_message_view_count']);
  out.mean_message_share_count = decodeStatisticalValue(record['mean_message_share_count']);
  out.mean_message_reaction_count = decodeStatisticalValue(record['mean_message_reaction_count']);
  out.mean_story_view_count = decodeStatisticalValue(record['mean_story_view_count']);
  out.mean_story_share_count = decodeStatisticalValue(record['mean_story_share_count']);
  out.mean_story_reaction_count = decodeStatisticalValue(record['mean_story_reaction_count']);
  out.enabled_notifications_percentage = asNumber(record['enabled_notifications_percentage']);
  out.member_count_graph = decodeTdStatisticalGraph(record['member_count_graph']);
  out.join_graph = decodeTdStatisticalGraph(record['join_graph']);
  out.mute_graph = decodeTdStatisticalGraph(record['mute_graph']);
  out.view_count_by_hour_graph = decodeTdStatisticalGraph(record['view_count_by_hour_graph']);
  out.view_count_by_source_graph = decodeTdStatisticalGraph(record['view_count_by_source_graph']);
  out.join_by_source_graph = decodeTdStatisticalGraph(record['join_by_source_graph']);
  out.language_graph = decodeTdStatisticalGraph(record['language_graph']);
  out.message_interaction_graph = decodeTdStatisticalGraph(record['message_interaction_graph']);
  out.message_reaction_graph = decodeTdStatisticalGraph(record['message_reaction_graph']);
  out.story_interaction_graph = decodeTdStatisticalGraph(record['story_interaction_graph']);
  out.story_reaction_graph = decodeTdStatisticalGraph(record['story_reaction_graph']);
  out.instant_view_interaction_graph = decodeTdStatisticalGraph(record['instant_view_interaction_graph']);
  out.recent_interactions = decodeVector(record['recent_interactions'], (item: TdJsonRaw): ChatStatisticsInteractionInfo => decodeChatStatisticsInteractionInfo(item) as ChatStatisticsInteractionInfo);
  return out;
}

/** Encode a `chatStatisticsChannel` into TDLib JSON wire shape. */
export function encodeChatStatisticsChannel(value: ChatStatisticsChannel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatStatisticsChannel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['period'] = value.period === null ? null : encodeDateRange(value.period);
  out['member_count'] = value.member_count === null ? null : encodeStatisticalValue(value.member_count);
  out['mean_message_view_count'] = value.mean_message_view_count === null ? null : encodeStatisticalValue(value.mean_message_view_count);
  out['mean_message_share_count'] = value.mean_message_share_count === null ? null : encodeStatisticalValue(value.mean_message_share_count);
  out['mean_message_reaction_count'] = value.mean_message_reaction_count === null ? null : encodeStatisticalValue(value.mean_message_reaction_count);
  out['mean_story_view_count'] = value.mean_story_view_count === null ? null : encodeStatisticalValue(value.mean_story_view_count);
  out['mean_story_share_count'] = value.mean_story_share_count === null ? null : encodeStatisticalValue(value.mean_story_share_count);
  out['mean_story_reaction_count'] = value.mean_story_reaction_count === null ? null : encodeStatisticalValue(value.mean_story_reaction_count);
  out['enabled_notifications_percentage'] = value.enabled_notifications_percentage;
  out['member_count_graph'] = value.member_count_graph === null ? null : encodeTdStatisticalGraph(value.member_count_graph);
  out['join_graph'] = value.join_graph === null ? null : encodeTdStatisticalGraph(value.join_graph);
  out['mute_graph'] = value.mute_graph === null ? null : encodeTdStatisticalGraph(value.mute_graph);
  out['view_count_by_hour_graph'] = value.view_count_by_hour_graph === null ? null : encodeTdStatisticalGraph(value.view_count_by_hour_graph);
  out['view_count_by_source_graph'] = value.view_count_by_source_graph === null ? null : encodeTdStatisticalGraph(value.view_count_by_source_graph);
  out['join_by_source_graph'] = value.join_by_source_graph === null ? null : encodeTdStatisticalGraph(value.join_by_source_graph);
  out['language_graph'] = value.language_graph === null ? null : encodeTdStatisticalGraph(value.language_graph);
  out['message_interaction_graph'] = value.message_interaction_graph === null ? null : encodeTdStatisticalGraph(value.message_interaction_graph);
  out['message_reaction_graph'] = value.message_reaction_graph === null ? null : encodeTdStatisticalGraph(value.message_reaction_graph);
  out['story_interaction_graph'] = value.story_interaction_graph === null ? null : encodeTdStatisticalGraph(value.story_interaction_graph);
  out['story_reaction_graph'] = value.story_reaction_graph === null ? null : encodeTdStatisticalGraph(value.story_reaction_graph);
  out['instant_view_interaction_graph'] = value.instant_view_interaction_graph === null ? null : encodeTdStatisticalGraph(value.instant_view_interaction_graph);
  out['recent_interactions'] = encodeVector(value.recent_interactions, (item: ChatStatisticsInteractionInfo | null): TdJsonRaw => item === null ? null : encodeChatStatisticsInteractionInfo(item));
  return out;
}

/** chatStatisticsInteractionInfo = ChatStatisticsInteractionInfo — Contains statistics about interactions with a message sent in the chat or a story posted on behalf of the chat */
export class ChatStatisticsInteractionInfo {
  type: 'chatStatisticsInteractionInfo' = 'chatStatisticsInteractionInfo';
  extra?: string;
  /** object_type: ChatStatisticsObjectType — Type of the object */
  object_type: TdChatStatisticsObjectType | null = null;
  /** view_count: int32 — Number of times the object was viewed */
  view_count: number = 0;
  /** forward_count: int32 — Number of times the object was forwarded */
  forward_count: number = 0;
  /** reaction_count: int32 — Number of times reactions were added to the object */
  reaction_count: number = 0;
}

/** Decode a TDLib `chatStatisticsInteractionInfo` object; null input -> null. */
export function decodeChatStatisticsInteractionInfo(json: TdJsonRaw): ChatStatisticsInteractionInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatStatisticsInteractionInfo = new ChatStatisticsInteractionInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.object_type = decodeTdChatStatisticsObjectType(record['object_type']);
  out.view_count = asNumber(record['view_count']);
  out.forward_count = asNumber(record['forward_count']);
  out.reaction_count = asNumber(record['reaction_count']);
  return out;
}

/** Encode a `chatStatisticsInteractionInfo` into TDLib JSON wire shape. */
export function encodeChatStatisticsInteractionInfo(value: ChatStatisticsInteractionInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatStatisticsInteractionInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['object_type'] = value.object_type === null ? null : encodeTdChatStatisticsObjectType(value.object_type);
  out['view_count'] = value.view_count;
  out['forward_count'] = value.forward_count;
  out['reaction_count'] = value.reaction_count;
  return out;
}

/** chatStatisticsInviterInfo = ChatStatisticsInviterInfo — Contains statistics about number of new members invited by a user */
export class ChatStatisticsInviterInfo {
  type: 'chatStatisticsInviterInfo' = 'chatStatisticsInviterInfo';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** added_member_count: int32 — Number of new members invited by the user */
  added_member_count: number = 0;
}

/** Decode a TDLib `chatStatisticsInviterInfo` object; null input -> null. */
export function decodeChatStatisticsInviterInfo(json: TdJsonRaw): ChatStatisticsInviterInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatStatisticsInviterInfo = new ChatStatisticsInviterInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.added_member_count = asNumber(record['added_member_count']);
  return out;
}

/** Encode a `chatStatisticsInviterInfo` into TDLib JSON wire shape. */
export function encodeChatStatisticsInviterInfo(value: ChatStatisticsInviterInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatStatisticsInviterInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['added_member_count'] = value.added_member_count;
  return out;
}

/** chatStatisticsMessageSenderInfo = ChatStatisticsMessageSenderInfo — Contains statistics about messages sent by a user */
export class ChatStatisticsMessageSenderInfo {
  type: 'chatStatisticsMessageSenderInfo' = 'chatStatisticsMessageSenderInfo';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** sent_message_count: int32 — Number of sent messages */
  sent_message_count: number = 0;
  /** average_character_count: int32 — Average number of characters in sent messages; 0 if unknown */
  average_character_count: number = 0;
}

/** Decode a TDLib `chatStatisticsMessageSenderInfo` object; null input -> null. */
export function decodeChatStatisticsMessageSenderInfo(json: TdJsonRaw): ChatStatisticsMessageSenderInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatStatisticsMessageSenderInfo = new ChatStatisticsMessageSenderInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.sent_message_count = asNumber(record['sent_message_count']);
  out.average_character_count = asNumber(record['average_character_count']);
  return out;
}

/** Encode a `chatStatisticsMessageSenderInfo` into TDLib JSON wire shape. */
export function encodeChatStatisticsMessageSenderInfo(value: ChatStatisticsMessageSenderInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatStatisticsMessageSenderInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['sent_message_count'] = value.sent_message_count;
  out['average_character_count'] = value.average_character_count;
  return out;
}

/** chatStatisticsObjectTypeMessage = ChatStatisticsObjectType — Describes a message sent in the chat */
export class ChatStatisticsObjectTypeMessage {
  type: 'chatStatisticsObjectTypeMessage' = 'chatStatisticsObjectTypeMessage';
  extra?: string;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
}

/** Decode a TDLib `chatStatisticsObjectTypeMessage` object; null input -> null. */
export function decodeChatStatisticsObjectTypeMessage(json: TdJsonRaw): ChatStatisticsObjectTypeMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatStatisticsObjectTypeMessage = new ChatStatisticsObjectTypeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `chatStatisticsObjectTypeMessage` into TDLib JSON wire shape. */
export function encodeChatStatisticsObjectTypeMessage(value: ChatStatisticsObjectTypeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatStatisticsObjectTypeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_id'] = value.message_id;
  return out;
}

/** chatStatisticsObjectTypeStory = ChatStatisticsObjectType — Describes a story posted on behalf of the chat */
export class ChatStatisticsObjectTypeStory {
  type: 'chatStatisticsObjectTypeStory' = 'chatStatisticsObjectTypeStory';
  extra?: string;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
}

/** Decode a TDLib `chatStatisticsObjectTypeStory` object; null input -> null. */
export function decodeChatStatisticsObjectTypeStory(json: TdJsonRaw): ChatStatisticsObjectTypeStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatStatisticsObjectTypeStory = new ChatStatisticsObjectTypeStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `chatStatisticsObjectTypeStory` into TDLib JSON wire shape. */
export function encodeChatStatisticsObjectTypeStory(value: ChatStatisticsObjectTypeStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatStatisticsObjectTypeStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_id'] = value.story_id;
  return out;
}

/** chatStatisticsSupergroup = ChatStatistics — A detailed statistics about a supergroup chat */
export class ChatStatisticsSupergroup {
  type: 'chatStatisticsSupergroup' = 'chatStatisticsSupergroup';
  extra?: string;
  /** period: dateRange — A period to which the statistics applies */
  period: DateRange | null = null;
  /** member_count: statisticalValue — Number of members in the chat */
  member_count: StatisticalValue | null = null;
  /** message_count: statisticalValue — Number of messages sent to the chat */
  message_count: StatisticalValue | null = null;
  /** viewer_count: statisticalValue — Number of users who viewed messages in the chat */
  viewer_count: StatisticalValue | null = null;
  /** sender_count: statisticalValue — Number of users who sent messages to the chat */
  sender_count: StatisticalValue | null = null;
  /** member_count_graph: StatisticalGraph — A graph containing number of members in the chat */
  member_count_graph: TdStatisticalGraph | null = null;
  /** join_graph: StatisticalGraph — A graph containing number of members joined and left the chat */
  join_graph: TdStatisticalGraph | null = null;
  /** join_by_source_graph: StatisticalGraph — A graph containing number of new member joins per source */
  join_by_source_graph: TdStatisticalGraph | null = null;
  /** language_graph: StatisticalGraph — A graph containing distribution of active users per language */
  language_graph: TdStatisticalGraph | null = null;
  /** message_content_graph: StatisticalGraph — A graph containing distribution of sent messages by content type */
  message_content_graph: TdStatisticalGraph | null = null;
  /** action_graph: StatisticalGraph — A graph containing number of different actions in the chat */
  action_graph: TdStatisticalGraph | null = null;
  /** day_graph: StatisticalGraph — A graph containing distribution of message views per hour */
  day_graph: TdStatisticalGraph | null = null;
  /** week_graph: StatisticalGraph — A graph containing distribution of message views per day of week */
  week_graph: TdStatisticalGraph | null = null;
  /** top_senders: vector<chatStatisticsMessageSenderInfo> — List of users sent most messages in the last week */
  top_senders: ChatStatisticsMessageSenderInfo[] = [];
  /** top_administrators: vector<chatStatisticsAdministratorActionsInfo> — List of most active administrators in the last week */
  top_administrators: ChatStatisticsAdministratorActionsInfo[] = [];
  /** top_inviters: vector<chatStatisticsInviterInfo> — List of most active inviters of new members in the last week */
  top_inviters: ChatStatisticsInviterInfo[] = [];
}

/** Decode a TDLib `chatStatisticsSupergroup` object; null input -> null. */
export function decodeChatStatisticsSupergroup(json: TdJsonRaw): ChatStatisticsSupergroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatStatisticsSupergroup = new ChatStatisticsSupergroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.period = decodeDateRange(record['period']);
  out.member_count = decodeStatisticalValue(record['member_count']);
  out.message_count = decodeStatisticalValue(record['message_count']);
  out.viewer_count = decodeStatisticalValue(record['viewer_count']);
  out.sender_count = decodeStatisticalValue(record['sender_count']);
  out.member_count_graph = decodeTdStatisticalGraph(record['member_count_graph']);
  out.join_graph = decodeTdStatisticalGraph(record['join_graph']);
  out.join_by_source_graph = decodeTdStatisticalGraph(record['join_by_source_graph']);
  out.language_graph = decodeTdStatisticalGraph(record['language_graph']);
  out.message_content_graph = decodeTdStatisticalGraph(record['message_content_graph']);
  out.action_graph = decodeTdStatisticalGraph(record['action_graph']);
  out.day_graph = decodeTdStatisticalGraph(record['day_graph']);
  out.week_graph = decodeTdStatisticalGraph(record['week_graph']);
  out.top_senders = decodeVector(record['top_senders'], (item: TdJsonRaw): ChatStatisticsMessageSenderInfo => decodeChatStatisticsMessageSenderInfo(item) as ChatStatisticsMessageSenderInfo);
  out.top_administrators = decodeVector(record['top_administrators'], (item: TdJsonRaw): ChatStatisticsAdministratorActionsInfo => decodeChatStatisticsAdministratorActionsInfo(item) as ChatStatisticsAdministratorActionsInfo);
  out.top_inviters = decodeVector(record['top_inviters'], (item: TdJsonRaw): ChatStatisticsInviterInfo => decodeChatStatisticsInviterInfo(item) as ChatStatisticsInviterInfo);
  return out;
}

/** Encode a `chatStatisticsSupergroup` into TDLib JSON wire shape. */
export function encodeChatStatisticsSupergroup(value: ChatStatisticsSupergroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatStatisticsSupergroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['period'] = value.period === null ? null : encodeDateRange(value.period);
  out['member_count'] = value.member_count === null ? null : encodeStatisticalValue(value.member_count);
  out['message_count'] = value.message_count === null ? null : encodeStatisticalValue(value.message_count);
  out['viewer_count'] = value.viewer_count === null ? null : encodeStatisticalValue(value.viewer_count);
  out['sender_count'] = value.sender_count === null ? null : encodeStatisticalValue(value.sender_count);
  out['member_count_graph'] = value.member_count_graph === null ? null : encodeTdStatisticalGraph(value.member_count_graph);
  out['join_graph'] = value.join_graph === null ? null : encodeTdStatisticalGraph(value.join_graph);
  out['join_by_source_graph'] = value.join_by_source_graph === null ? null : encodeTdStatisticalGraph(value.join_by_source_graph);
  out['language_graph'] = value.language_graph === null ? null : encodeTdStatisticalGraph(value.language_graph);
  out['message_content_graph'] = value.message_content_graph === null ? null : encodeTdStatisticalGraph(value.message_content_graph);
  out['action_graph'] = value.action_graph === null ? null : encodeTdStatisticalGraph(value.action_graph);
  out['day_graph'] = value.day_graph === null ? null : encodeTdStatisticalGraph(value.day_graph);
  out['week_graph'] = value.week_graph === null ? null : encodeTdStatisticalGraph(value.week_graph);
  out['top_senders'] = encodeVector(value.top_senders, (item: ChatStatisticsMessageSenderInfo | null): TdJsonRaw => item === null ? null : encodeChatStatisticsMessageSenderInfo(item));
  out['top_administrators'] = encodeVector(value.top_administrators, (item: ChatStatisticsAdministratorActionsInfo | null): TdJsonRaw => item === null ? null : encodeChatStatisticsAdministratorActionsInfo(item));
  out['top_inviters'] = encodeVector(value.top_inviters, (item: ChatStatisticsInviterInfo | null): TdJsonRaw => item === null ? null : encodeChatStatisticsInviterInfo(item));
  return out;
}

/** chatThemeEmoji = ChatTheme — A chat theme based on an emoji */
export class ChatThemeEmoji {
  type: 'chatThemeEmoji' = 'chatThemeEmoji';
  extra?: string;
  /** name: string — Name of the theme; full theme description is received through updateEmojiChatThemes */
  name: string = '';
}

/** Decode a TDLib `chatThemeEmoji` object; null input -> null. */
export function decodeChatThemeEmoji(json: TdJsonRaw): ChatThemeEmoji | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatThemeEmoji = new ChatThemeEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `chatThemeEmoji` into TDLib JSON wire shape. */
export function encodeChatThemeEmoji(value: ChatThemeEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatThemeEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** chatThemeGift = ChatTheme — A chat theme based on an upgraded gift */
export class ChatThemeGift {
  type: 'chatThemeGift' = 'chatThemeGift';
  extra?: string;
  /** gift_theme: giftChatTheme — The chat theme */
  gift_theme: GiftChatTheme | null = null;
}

/** Decode a TDLib `chatThemeGift` object; null input -> null. */
export function decodeChatThemeGift(json: TdJsonRaw): ChatThemeGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatThemeGift = new ChatThemeGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_theme = decodeGiftChatTheme(record['gift_theme']);
  return out;
}

/** Encode a `chatThemeGift` into TDLib JSON wire shape. */
export function encodeChatThemeGift(value: ChatThemeGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatThemeGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_theme'] = value.gift_theme === null ? null : encodeGiftChatTheme(value.gift_theme);
  return out;
}

/** chatTypeBasicGroup = ChatType — A basic group (a chat with 0-200 other users) */
export class ChatTypeBasicGroup {
  type: 'chatTypeBasicGroup' = 'chatTypeBasicGroup';
  extra?: string;
  /** basic_group_id: int53 — Basic group identifier */
  basic_group_id: number = 0;
}

/** Decode a TDLib `chatTypeBasicGroup` object; null input -> null. */
export function decodeChatTypeBasicGroup(json: TdJsonRaw): ChatTypeBasicGroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatTypeBasicGroup = new ChatTypeBasicGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.basic_group_id = asNumber(record['basic_group_id']);
  return out;
}

/** Encode a `chatTypeBasicGroup` into TDLib JSON wire shape. */
export function encodeChatTypeBasicGroup(value: ChatTypeBasicGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatTypeBasicGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['basic_group_id'] = value.basic_group_id;
  return out;
}

/** chatTypePrivate = ChatType — An ordinary chat with a user */
export class ChatTypePrivate {
  type: 'chatTypePrivate' = 'chatTypePrivate';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
}

/** Decode a TDLib `chatTypePrivate` object; null input -> null. */
export function decodeChatTypePrivate(json: TdJsonRaw): ChatTypePrivate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatTypePrivate = new ChatTypePrivate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `chatTypePrivate` into TDLib JSON wire shape. */
export function encodeChatTypePrivate(value: ChatTypePrivate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatTypePrivate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** chatTypeSecret = ChatType — A secret chat with a user */
export class ChatTypeSecret {
  type: 'chatTypeSecret' = 'chatTypeSecret';
  extra?: string;
  /** secret_chat_id: int32 — Secret chat identifier */
  secret_chat_id: number = 0;
  /** user_id: int53 — User identifier of the other user in the secret chat */
  user_id: number = 0;
}

/** Decode a TDLib `chatTypeSecret` object; null input -> null. */
export function decodeChatTypeSecret(json: TdJsonRaw): ChatTypeSecret | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatTypeSecret = new ChatTypeSecret();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.secret_chat_id = asNumber(record['secret_chat_id']);
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `chatTypeSecret` into TDLib JSON wire shape. */
export function encodeChatTypeSecret(value: ChatTypeSecret): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatTypeSecret';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['secret_chat_id'] = value.secret_chat_id;
  out['user_id'] = value.user_id;
  return out;
}

/** chatTypeSupergroup = ChatType — A supergroup or channel (with unlimited members) */
export class ChatTypeSupergroup {
  type: 'chatTypeSupergroup' = 'chatTypeSupergroup';
  extra?: string;
  /** supergroup_id: int53 — Supergroup or channel identifier */
  supergroup_id: number = 0;
  /** is_channel: Bool — True, if the supergroup is a channel */
  is_channel: boolean = false;
}

/** Decode a TDLib `chatTypeSupergroup` object; null input -> null. */
export function decodeChatTypeSupergroup(json: TdJsonRaw): ChatTypeSupergroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChatTypeSupergroup = new ChatTypeSupergroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.is_channel = asBool(record['is_channel']);
  return out;
}

/** Encode a `chatTypeSupergroup` into TDLib JSON wire shape. */
export function encodeChatTypeSupergroup(value: ChatTypeSupergroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chatTypeSupergroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['is_channel'] = value.is_channel;
  return out;
}

/** chats = Chats — Represents a list of chats */
export class Chats {
  type: 'chats' = 'chats';
  extra?: string;
  /** total_count: int32 — Approximate total number of chats found */
  total_count: number = 0;
  /** chat_ids: vector<int53> — List of chat identifiers */
  chat_ids: number[] = [];
}

/** Decode a TDLib `chats` object; null input -> null. */
export function decodeChats(json: TdJsonRaw): Chats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Chats = new Chats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `chats` into TDLib JSON wire shape. */
export function encodeChats(value: Chats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'chats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** checkAuthenticationBotToken = Ok — Checks the authentication token of a bot; to log in as a bot. Works only when the current authorization state is authorizationStateWaitPhoneNumber. Can be used instead of setAuthenticationPhoneNumber an... */
export class CheckAuthenticationBotToken {
  type: 'checkAuthenticationBotToken' = 'checkAuthenticationBotToken';
  extra?: string;
  /** token: string — The bot token */
  token: string = '';
}

/** Decode a TDLib `checkAuthenticationBotToken` object; null input -> null. */
export function decodeCheckAuthenticationBotToken(json: TdJsonRaw): CheckAuthenticationBotToken | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckAuthenticationBotToken = new CheckAuthenticationBotToken();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `checkAuthenticationBotToken` into TDLib JSON wire shape. */
export function encodeCheckAuthenticationBotToken(value: CheckAuthenticationBotToken): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkAuthenticationBotToken';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** checkAuthenticationCode = Ok — Checks the authentication code. Works only when the current authorization state is authorizationStateWaitCode */
export class CheckAuthenticationCode {
  type: 'checkAuthenticationCode' = 'checkAuthenticationCode';
  extra?: string;
  /** code: string — Authentication code to check */
  code: string = '';
}

/** Decode a TDLib `checkAuthenticationCode` object; null input -> null. */
export function decodeCheckAuthenticationCode(json: TdJsonRaw): CheckAuthenticationCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckAuthenticationCode = new CheckAuthenticationCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asString(record['code']);
  return out;
}

/** Encode a `checkAuthenticationCode` into TDLib JSON wire shape. */
export function encodeCheckAuthenticationCode(value: CheckAuthenticationCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkAuthenticationCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  return out;
}

/** checkAuthenticationEmailCode = Ok — Checks the authentication of an email address. Works only when the current authorization state is authorizationStateWaitEmailCode */
export class CheckAuthenticationEmailCode {
  type: 'checkAuthenticationEmailCode' = 'checkAuthenticationEmailCode';
  extra?: string;
  /** code: EmailAddressAuthentication — Email address authentication to check */
  code: TdEmailAddressAuthentication | null = null;
}

/** Decode a TDLib `checkAuthenticationEmailCode` object; null input -> null. */
export function decodeCheckAuthenticationEmailCode(json: TdJsonRaw): CheckAuthenticationEmailCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckAuthenticationEmailCode = new CheckAuthenticationEmailCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = decodeTdEmailAddressAuthentication(record['code']);
  return out;
}

/** Encode a `checkAuthenticationEmailCode` into TDLib JSON wire shape. */
export function encodeCheckAuthenticationEmailCode(value: CheckAuthenticationEmailCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkAuthenticationEmailCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code === null ? null : encodeTdEmailAddressAuthentication(value.code);
  return out;
}

/** checkAuthenticationPasskey = Ok — Checks a passkey to log in to the corresponding account. Call getAuthenticationPasskeyParameters to get parameters for the passkey. Works only when the current authorization state is authorizationStateWa... */
export class CheckAuthenticationPasskey {
  type: 'checkAuthenticationPasskey' = 'checkAuthenticationPasskey';
  extra?: string;
  /** credential_id: string — Base64url-encoded identifier of the credential */
  credential_id: string = '';
  /** client_data: string — JSON-encoded client data */
  client_data: string = '';
  /** authenticator_data: bytes — Authenticator data of the application that created the credential */
  authenticator_data: string = '';
  /** signature: bytes — Cryptographic signature of the credential */
  signature: string = '';
  /** user_handle: bytes — User handle of the passkey */
  user_handle: string = '';
}

/** Decode a TDLib `checkAuthenticationPasskey` object; null input -> null. */
export function decodeCheckAuthenticationPasskey(json: TdJsonRaw): CheckAuthenticationPasskey | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckAuthenticationPasskey = new CheckAuthenticationPasskey();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.credential_id = asString(record['credential_id']);
  out.client_data = asString(record['client_data']);
  out.authenticator_data = asString(record['authenticator_data']);
  out.signature = asString(record['signature']);
  out.user_handle = asString(record['user_handle']);
  return out;
}

/** Encode a `checkAuthenticationPasskey` into TDLib JSON wire shape. */
export function encodeCheckAuthenticationPasskey(value: CheckAuthenticationPasskey): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkAuthenticationPasskey';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['credential_id'] = value.credential_id;
  out['client_data'] = value.client_data;
  out['authenticator_data'] = value.authenticator_data;
  out['signature'] = value.signature;
  out['user_handle'] = value.user_handle;
  return out;
}

/** checkAuthenticationPassword = Ok — Checks the 2-step verification password for correctness. Works only when the current authorization state is authorizationStateWaitPassword */
export class CheckAuthenticationPassword {
  type: 'checkAuthenticationPassword' = 'checkAuthenticationPassword';
  extra?: string;
  /** password: string — The 2-step verification password to check */
  password: string = '';
}

/** Decode a TDLib `checkAuthenticationPassword` object; null input -> null. */
export function decodeCheckAuthenticationPassword(json: TdJsonRaw): CheckAuthenticationPassword | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckAuthenticationPassword = new CheckAuthenticationPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.password = asString(record['password']);
  return out;
}

/** Encode a `checkAuthenticationPassword` into TDLib JSON wire shape. */
export function encodeCheckAuthenticationPassword(value: CheckAuthenticationPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkAuthenticationPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['password'] = value.password;
  return out;
}

/** checkAuthenticationPasswordRecoveryCode = Ok — Checks whether a 2-step verification password recovery code sent to an email address is valid. Works only when the current authorization state is authorizationStateWaitPassword */
export class CheckAuthenticationPasswordRecoveryCode {
  type: 'checkAuthenticationPasswordRecoveryCode' = 'checkAuthenticationPasswordRecoveryCode';
  extra?: string;
  /** recovery_code: string — Recovery code to check */
  recovery_code: string = '';
}

/** Decode a TDLib `checkAuthenticationPasswordRecoveryCode` object; null input -> null. */
export function decodeCheckAuthenticationPasswordRecoveryCode(json: TdJsonRaw): CheckAuthenticationPasswordRecoveryCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckAuthenticationPasswordRecoveryCode = new CheckAuthenticationPasswordRecoveryCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.recovery_code = asString(record['recovery_code']);
  return out;
}

/** Encode a `checkAuthenticationPasswordRecoveryCode` into TDLib JSON wire shape. */
export function encodeCheckAuthenticationPasswordRecoveryCode(value: CheckAuthenticationPasswordRecoveryCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkAuthenticationPasswordRecoveryCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['recovery_code'] = value.recovery_code;
  return out;
}

/** checkAuthenticationPremiumPurchase = Ok — Checks whether an in-store purchase of Telegram Premium is possible before authorization. Works only when the current authorization state is authorizationStateWaitPremiumPurchase */
export class CheckAuthenticationPremiumPurchase {
  type: 'checkAuthenticationPremiumPurchase' = 'checkAuthenticationPremiumPurchase';
  extra?: string;
  /** premium_day_count: int32 — The number of days for which the Telegram Premium subscription will be granted */
  premium_day_count: number = 0;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
}

/** Decode a TDLib `checkAuthenticationPremiumPurchase` object; null input -> null. */
export function decodeCheckAuthenticationPremiumPurchase(json: TdJsonRaw): CheckAuthenticationPremiumPurchase | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckAuthenticationPremiumPurchase = new CheckAuthenticationPremiumPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.premium_day_count = asNumber(record['premium_day_count']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  return out;
}

/** Encode a `checkAuthenticationPremiumPurchase` into TDLib JSON wire shape. */
export function encodeCheckAuthenticationPremiumPurchase(value: CheckAuthenticationPremiumPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkAuthenticationPremiumPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['premium_day_count'] = value.premium_day_count;
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  return out;
}

/** checkAuthenticationWebToken = Ok — Checks a web token to log in to the corresponding account; for official Telegram apps only. Works only when the current authorization state is authorizationStateWaitPhoneNumber or authorizationStateWait... */
export class CheckAuthenticationWebToken {
  type: 'checkAuthenticationWebToken' = 'checkAuthenticationWebToken';
  extra?: string;
  /** token: string — The token to check */
  token: string = '';
  /** dc_id: int32 — Identifier of the datacenter of the user */
  dc_id: number = 0;
}

/** Decode a TDLib `checkAuthenticationWebToken` object; null input -> null. */
export function decodeCheckAuthenticationWebToken(json: TdJsonRaw): CheckAuthenticationWebToken | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckAuthenticationWebToken = new CheckAuthenticationWebToken();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  out.dc_id = asNumber(record['dc_id']);
  return out;
}

/** Encode a `checkAuthenticationWebToken` into TDLib JSON wire shape. */
export function encodeCheckAuthenticationWebToken(value: CheckAuthenticationWebToken): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkAuthenticationWebToken';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  out['dc_id'] = value.dc_id;
  return out;
}

/** checkBotUsername = CheckChatUsernameResult — Checks whether a username can be set for a new bot. Use checkChatUsername to check username for other chat types */
export class CheckBotUsername {
  type: 'checkBotUsername' = 'checkBotUsername';
  extra?: string;
  /** username: string — Username to be checked */
  username: string = '';
}

/** Decode a TDLib `checkBotUsername` object; null input -> null. */
export function decodeCheckBotUsername(json: TdJsonRaw): CheckBotUsername | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckBotUsername = new CheckBotUsername();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  return out;
}

/** Encode a `checkBotUsername` into TDLib JSON wire shape. */
export function encodeCheckBotUsername(value: CheckBotUsername): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkBotUsername';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  return out;
}

/** checkChatFolderInviteLink = ChatFolderInviteLinkInfo — Checks the validity of an invite link for a chat folder and returns information about the corresponding chat folder */
export class CheckChatFolderInviteLink {
  type: 'checkChatFolderInviteLink' = 'checkChatFolderInviteLink';
  extra?: string;
  /** invite_link: string — Invite link to be checked */
  invite_link: string = '';
}

/** Decode a TDLib `checkChatFolderInviteLink` object; null input -> null. */
export function decodeCheckChatFolderInviteLink(json: TdJsonRaw): CheckChatFolderInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckChatFolderInviteLink = new CheckChatFolderInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `checkChatFolderInviteLink` into TDLib JSON wire shape. */
export function encodeCheckChatFolderInviteLink(value: CheckChatFolderInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkChatFolderInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  return out;
}

/** checkChatInviteLink = ChatInviteLinkInfo — Checks the validity of an invite link for a chat and returns information about the corresponding chat */
export class CheckChatInviteLink {
  type: 'checkChatInviteLink' = 'checkChatInviteLink';
  extra?: string;
  /** invite_link: string — Invite link to be checked */
  invite_link: string = '';
}

/** Decode a TDLib `checkChatInviteLink` object; null input -> null. */
export function decodeCheckChatInviteLink(json: TdJsonRaw): CheckChatInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckChatInviteLink = new CheckChatInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `checkChatInviteLink` into TDLib JSON wire shape. */
export function encodeCheckChatInviteLink(value: CheckChatInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkChatInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  return out;
}

/** checkChatUsername = CheckChatUsernameResult — Checks whether a username can be set for a chat */
export class CheckChatUsername {
  type: 'checkChatUsername' = 'checkChatUsername';
  extra?: string;
  /** chat_id: int53 — Chat identifier; must be identifier of a supergroup chat, or a channel chat, or a private chat with self, or 0 if the chat is being created */
  chat_id: number = 0;
  /** username: string — Username to be checked */
  username: string = '';
}

/** Decode a TDLib `checkChatUsername` object; null input -> null. */
export function decodeCheckChatUsername(json: TdJsonRaw): CheckChatUsername | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckChatUsername = new CheckChatUsername();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.username = asString(record['username']);
  return out;
}

/** Encode a `checkChatUsername` into TDLib JSON wire shape. */
export function encodeCheckChatUsername(value: CheckChatUsername): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkChatUsername';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['username'] = value.username;
  return out;
}

/** checkChatUsernameResultOk = CheckChatUsernameResult — The username can be set */
export class CheckChatUsernameResultOk {
  type: 'checkChatUsernameResultOk' = 'checkChatUsernameResultOk';
  extra?: string;
}

/** Decode a TDLib `checkChatUsernameResultOk` object; null input -> null. */
export function decodeCheckChatUsernameResultOk(json: TdJsonRaw): CheckChatUsernameResultOk | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckChatUsernameResultOk = new CheckChatUsernameResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `checkChatUsernameResultOk` into TDLib JSON wire shape. */
export function encodeCheckChatUsernameResultOk(value: CheckChatUsernameResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkChatUsernameResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** checkChatUsernameResultPublicChatsTooMany = CheckChatUsernameResult — The user has too many chats with username, one of them must be made private first */
export class CheckChatUsernameResultPublicChatsTooMany {
  type: 'checkChatUsernameResultPublicChatsTooMany' = 'checkChatUsernameResultPublicChatsTooMany';
  extra?: string;
}

/** Decode a TDLib `checkChatUsernameResultPublicChatsTooMany` object; null input -> null. */
export function decodeCheckChatUsernameResultPublicChatsTooMany(json: TdJsonRaw): CheckChatUsernameResultPublicChatsTooMany | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckChatUsernameResultPublicChatsTooMany = new CheckChatUsernameResultPublicChatsTooMany();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `checkChatUsernameResultPublicChatsTooMany` into TDLib JSON wire shape. */
export function encodeCheckChatUsernameResultPublicChatsTooMany(value: CheckChatUsernameResultPublicChatsTooMany): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkChatUsernameResultPublicChatsTooMany';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** checkChatUsernameResultPublicGroupsUnavailable = CheckChatUsernameResult — The user can't be a member of a public supergroup */
export class CheckChatUsernameResultPublicGroupsUnavailable {
  type: 'checkChatUsernameResultPublicGroupsUnavailable' = 'checkChatUsernameResultPublicGroupsUnavailable';
  extra?: string;
}

/** Decode a TDLib `checkChatUsernameResultPublicGroupsUnavailable` object; null input -> null. */
export function decodeCheckChatUsernameResultPublicGroupsUnavailable(json: TdJsonRaw): CheckChatUsernameResultPublicGroupsUnavailable | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckChatUsernameResultPublicGroupsUnavailable = new CheckChatUsernameResultPublicGroupsUnavailable();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `checkChatUsernameResultPublicGroupsUnavailable` into TDLib JSON wire shape. */
export function encodeCheckChatUsernameResultPublicGroupsUnavailable(value: CheckChatUsernameResultPublicGroupsUnavailable): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkChatUsernameResultPublicGroupsUnavailable';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** checkChatUsernameResultUsernameInvalid = CheckChatUsernameResult — The username is invalid */
export class CheckChatUsernameResultUsernameInvalid {
  type: 'checkChatUsernameResultUsernameInvalid' = 'checkChatUsernameResultUsernameInvalid';
  extra?: string;
}

/** Decode a TDLib `checkChatUsernameResultUsernameInvalid` object; null input -> null. */
export function decodeCheckChatUsernameResultUsernameInvalid(json: TdJsonRaw): CheckChatUsernameResultUsernameInvalid | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckChatUsernameResultUsernameInvalid = new CheckChatUsernameResultUsernameInvalid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `checkChatUsernameResultUsernameInvalid` into TDLib JSON wire shape. */
export function encodeCheckChatUsernameResultUsernameInvalid(value: CheckChatUsernameResultUsernameInvalid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkChatUsernameResultUsernameInvalid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** checkChatUsernameResultUsernameOccupied = CheckChatUsernameResult — The username is occupied */
export class CheckChatUsernameResultUsernameOccupied {
  type: 'checkChatUsernameResultUsernameOccupied' = 'checkChatUsernameResultUsernameOccupied';
  extra?: string;
}

/** Decode a TDLib `checkChatUsernameResultUsernameOccupied` object; null input -> null. */
export function decodeCheckChatUsernameResultUsernameOccupied(json: TdJsonRaw): CheckChatUsernameResultUsernameOccupied | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckChatUsernameResultUsernameOccupied = new CheckChatUsernameResultUsernameOccupied();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `checkChatUsernameResultUsernameOccupied` into TDLib JSON wire shape. */
export function encodeCheckChatUsernameResultUsernameOccupied(value: CheckChatUsernameResultUsernameOccupied): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkChatUsernameResultUsernameOccupied';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** checkChatUsernameResultUsernamePurchasable = CheckChatUsernameResult — The username can be purchased at https://fragment.com. Information about the username can be received using getCollectibleItemInfo */
export class CheckChatUsernameResultUsernamePurchasable {
  type: 'checkChatUsernameResultUsernamePurchasable' = 'checkChatUsernameResultUsernamePurchasable';
  extra?: string;
}

/** Decode a TDLib `checkChatUsernameResultUsernamePurchasable` object; null input -> null. */
export function decodeCheckChatUsernameResultUsernamePurchasable(json: TdJsonRaw): CheckChatUsernameResultUsernamePurchasable | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckChatUsernameResultUsernamePurchasable = new CheckChatUsernameResultUsernamePurchasable();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `checkChatUsernameResultUsernamePurchasable` into TDLib JSON wire shape. */
export function encodeCheckChatUsernameResultUsernamePurchasable(value: CheckChatUsernameResultUsernamePurchasable): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkChatUsernameResultUsernamePurchasable';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** checkCreatedPublicChatsLimit = Ok — Checks whether the maximum number of owned public chats has been reached. Returns corresponding error if the limit was reached. The limit can be increased with Telegram Premium */
export class CheckCreatedPublicChatsLimit {
  type: 'checkCreatedPublicChatsLimit' = 'checkCreatedPublicChatsLimit';
  extra?: string;
  /** type: PublicChatType — Type of the public chats, for which to check the limit */
  type_: TdPublicChatType | null = null;
}

/** Decode a TDLib `checkCreatedPublicChatsLimit` object; null input -> null. */
export function decodeCheckCreatedPublicChatsLimit(json: TdJsonRaw): CheckCreatedPublicChatsLimit | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckCreatedPublicChatsLimit = new CheckCreatedPublicChatsLimit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPublicChatType(record['type']);
  return out;
}

/** Encode a `checkCreatedPublicChatsLimit` into TDLib JSON wire shape. */
export function encodeCheckCreatedPublicChatsLimit(value: CheckCreatedPublicChatsLimit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkCreatedPublicChatsLimit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPublicChatType(value.type_);
  return out;
}

/** checkEmailAddressVerificationCode = Ok — Checks the email address verification code for Telegram Passport */
export class CheckEmailAddressVerificationCode {
  type: 'checkEmailAddressVerificationCode' = 'checkEmailAddressVerificationCode';
  extra?: string;
  /** code: string — Verification code to check */
  code: string = '';
}

/** Decode a TDLib `checkEmailAddressVerificationCode` object; null input -> null. */
export function decodeCheckEmailAddressVerificationCode(json: TdJsonRaw): CheckEmailAddressVerificationCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckEmailAddressVerificationCode = new CheckEmailAddressVerificationCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asString(record['code']);
  return out;
}

/** Encode a `checkEmailAddressVerificationCode` into TDLib JSON wire shape. */
export function encodeCheckEmailAddressVerificationCode(value: CheckEmailAddressVerificationCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkEmailAddressVerificationCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  return out;
}

/** checkLoginEmailAddressCode = Ok — Checks the login email address authentication */
export class CheckLoginEmailAddressCode {
  type: 'checkLoginEmailAddressCode' = 'checkLoginEmailAddressCode';
  extra?: string;
  /** code: EmailAddressAuthentication — Email address authentication to check */
  code: TdEmailAddressAuthentication | null = null;
}

/** Decode a TDLib `checkLoginEmailAddressCode` object; null input -> null. */
export function decodeCheckLoginEmailAddressCode(json: TdJsonRaw): CheckLoginEmailAddressCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckLoginEmailAddressCode = new CheckLoginEmailAddressCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = decodeTdEmailAddressAuthentication(record['code']);
  return out;
}

/** Encode a `checkLoginEmailAddressCode` into TDLib JSON wire shape. */
export function encodeCheckLoginEmailAddressCode(value: CheckLoginEmailAddressCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkLoginEmailAddressCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code === null ? null : encodeTdEmailAddressAuthentication(value.code);
  return out;
}

/** checkOauthRequestMatchCode = Ok — Checks a match-code for an OAuth authorization request. If fails, then the authorization request has failed. Otherwise, authorization confirmation dialog must be shown and the link must be processed usin... */
export class CheckOauthRequestMatchCode {
  type: 'checkOauthRequestMatchCode' = 'checkOauthRequestMatchCode';
  extra?: string;
  /** url: string — URL of the OAuth deep link */
  url: string = '';
  /** match_code: string — The matching code chosen by the user */
  match_code: string = '';
}

/** Decode a TDLib `checkOauthRequestMatchCode` object; null input -> null. */
export function decodeCheckOauthRequestMatchCode(json: TdJsonRaw): CheckOauthRequestMatchCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckOauthRequestMatchCode = new CheckOauthRequestMatchCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.match_code = asString(record['match_code']);
  return out;
}

/** Encode a `checkOauthRequestMatchCode` into TDLib JSON wire shape. */
export function encodeCheckOauthRequestMatchCode(value: CheckOauthRequestMatchCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkOauthRequestMatchCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['match_code'] = value.match_code;
  return out;
}

/** checkPasswordRecoveryCode = Ok — Checks whether a 2-step verification password recovery code sent to an email address is valid */
export class CheckPasswordRecoveryCode {
  type: 'checkPasswordRecoveryCode' = 'checkPasswordRecoveryCode';
  extra?: string;
  /** recovery_code: string — Recovery code to check */
  recovery_code: string = '';
}

/** Decode a TDLib `checkPasswordRecoveryCode` object; null input -> null. */
export function decodeCheckPasswordRecoveryCode(json: TdJsonRaw): CheckPasswordRecoveryCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckPasswordRecoveryCode = new CheckPasswordRecoveryCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.recovery_code = asString(record['recovery_code']);
  return out;
}

/** Encode a `checkPasswordRecoveryCode` into TDLib JSON wire shape. */
export function encodeCheckPasswordRecoveryCode(value: CheckPasswordRecoveryCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkPasswordRecoveryCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['recovery_code'] = value.recovery_code;
  return out;
}

/** checkPhoneNumberCode = Ok — Checks the authentication code and completes the request for which the code was sent if appropriate */
export class CheckPhoneNumberCode {
  type: 'checkPhoneNumberCode' = 'checkPhoneNumberCode';
  extra?: string;
  /** code: string — Authentication code to check */
  code: string = '';
}

/** Decode a TDLib `checkPhoneNumberCode` object; null input -> null. */
export function decodeCheckPhoneNumberCode(json: TdJsonRaw): CheckPhoneNumberCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckPhoneNumberCode = new CheckPhoneNumberCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asString(record['code']);
  return out;
}

/** Encode a `checkPhoneNumberCode` into TDLib JSON wire shape. */
export function encodeCheckPhoneNumberCode(value: CheckPhoneNumberCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkPhoneNumberCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  return out;
}

/** checkPremiumGiftCode = PremiumGiftCodeInfo — Returns information about a Telegram Premium gift code */
export class CheckPremiumGiftCode {
  type: 'checkPremiumGiftCode' = 'checkPremiumGiftCode';
  extra?: string;
  /** code: string — The code to check */
  code: string = '';
}

/** Decode a TDLib `checkPremiumGiftCode` object; null input -> null. */
export function decodeCheckPremiumGiftCode(json: TdJsonRaw): CheckPremiumGiftCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckPremiumGiftCode = new CheckPremiumGiftCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asString(record['code']);
  return out;
}

/** Encode a `checkPremiumGiftCode` into TDLib JSON wire shape. */
export function encodeCheckPremiumGiftCode(value: CheckPremiumGiftCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkPremiumGiftCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  return out;
}

/** checkQuickReplyShortcutName = Ok — Checks validness of a name for a quick reply shortcut. Can be called synchronously */
export class CheckQuickReplyShortcutName {
  type: 'checkQuickReplyShortcutName' = 'checkQuickReplyShortcutName';
  extra?: string;
  /** name: string — The name of the shortcut; 1-32 characters */
  name: string = '';
}

/** Decode a TDLib `checkQuickReplyShortcutName` object; null input -> null. */
export function decodeCheckQuickReplyShortcutName(json: TdJsonRaw): CheckQuickReplyShortcutName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckQuickReplyShortcutName = new CheckQuickReplyShortcutName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `checkQuickReplyShortcutName` into TDLib JSON wire shape. */
export function encodeCheckQuickReplyShortcutName(value: CheckQuickReplyShortcutName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkQuickReplyShortcutName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** checkRecoveryEmailAddressCode = PasswordState — Checks the 2-step verification recovery email address verification code */
export class CheckRecoveryEmailAddressCode {
  type: 'checkRecoveryEmailAddressCode' = 'checkRecoveryEmailAddressCode';
  extra?: string;
  /** code: string — Verification code to check */
  code: string = '';
}

/** Decode a TDLib `checkRecoveryEmailAddressCode` object; null input -> null. */
export function decodeCheckRecoveryEmailAddressCode(json: TdJsonRaw): CheckRecoveryEmailAddressCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckRecoveryEmailAddressCode = new CheckRecoveryEmailAddressCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asString(record['code']);
  return out;
}

/** Encode a `checkRecoveryEmailAddressCode` into TDLib JSON wire shape. */
export function encodeCheckRecoveryEmailAddressCode(value: CheckRecoveryEmailAddressCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkRecoveryEmailAddressCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  return out;
}

/** checkStickerSetName = CheckStickerSetNameResult — Checks whether a name can be used for a new sticker set */
export class CheckStickerSetName {
  type: 'checkStickerSetName' = 'checkStickerSetName';
  extra?: string;
  /** name: string — Name to be checked */
  name: string = '';
}

/** Decode a TDLib `checkStickerSetName` object; null input -> null. */
export function decodeCheckStickerSetName(json: TdJsonRaw): CheckStickerSetName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckStickerSetName = new CheckStickerSetName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `checkStickerSetName` into TDLib JSON wire shape. */
export function encodeCheckStickerSetName(value: CheckStickerSetName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkStickerSetName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** checkStickerSetNameResultNameInvalid = CheckStickerSetNameResult — The name is invalid */
export class CheckStickerSetNameResultNameInvalid {
  type: 'checkStickerSetNameResultNameInvalid' = 'checkStickerSetNameResultNameInvalid';
  extra?: string;
}

/** Decode a TDLib `checkStickerSetNameResultNameInvalid` object; null input -> null. */
export function decodeCheckStickerSetNameResultNameInvalid(json: TdJsonRaw): CheckStickerSetNameResultNameInvalid | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckStickerSetNameResultNameInvalid = new CheckStickerSetNameResultNameInvalid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `checkStickerSetNameResultNameInvalid` into TDLib JSON wire shape. */
export function encodeCheckStickerSetNameResultNameInvalid(value: CheckStickerSetNameResultNameInvalid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkStickerSetNameResultNameInvalid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** checkStickerSetNameResultNameOccupied = CheckStickerSetNameResult — The name is occupied */
export class CheckStickerSetNameResultNameOccupied {
  type: 'checkStickerSetNameResultNameOccupied' = 'checkStickerSetNameResultNameOccupied';
  extra?: string;
}

/** Decode a TDLib `checkStickerSetNameResultNameOccupied` object; null input -> null. */
export function decodeCheckStickerSetNameResultNameOccupied(json: TdJsonRaw): CheckStickerSetNameResultNameOccupied | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckStickerSetNameResultNameOccupied = new CheckStickerSetNameResultNameOccupied();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `checkStickerSetNameResultNameOccupied` into TDLib JSON wire shape. */
export function encodeCheckStickerSetNameResultNameOccupied(value: CheckStickerSetNameResultNameOccupied): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkStickerSetNameResultNameOccupied';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** checkStickerSetNameResultOk = CheckStickerSetNameResult — The name can be set */
export class CheckStickerSetNameResultOk {
  type: 'checkStickerSetNameResultOk' = 'checkStickerSetNameResultOk';
  extra?: string;
}

/** Decode a TDLib `checkStickerSetNameResultOk` object; null input -> null. */
export function decodeCheckStickerSetNameResultOk(json: TdJsonRaw): CheckStickerSetNameResultOk | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckStickerSetNameResultOk = new CheckStickerSetNameResultOk();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `checkStickerSetNameResultOk` into TDLib JSON wire shape. */
export function encodeCheckStickerSetNameResultOk(value: CheckStickerSetNameResultOk): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkStickerSetNameResultOk';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** checkWebAppFileDownload = Ok — Checks whether a file can be downloaded and saved locally by Web App request */
export class CheckWebAppFileDownload {
  type: 'checkWebAppFileDownload' = 'checkWebAppFileDownload';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the bot, providing the Web App */
  bot_user_id: number = 0;
  /** file_name: string — Name of the file */
  file_name: string = '';
  /** url: string — URL of the file */
  url: string = '';
}

/** Decode a TDLib `checkWebAppFileDownload` object; null input -> null. */
export function decodeCheckWebAppFileDownload(json: TdJsonRaw): CheckWebAppFileDownload | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CheckWebAppFileDownload = new CheckWebAppFileDownload();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.file_name = asString(record['file_name']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `checkWebAppFileDownload` into TDLib JSON wire shape. */
export function encodeCheckWebAppFileDownload(value: CheckWebAppFileDownload): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checkWebAppFileDownload';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['file_name'] = value.file_name;
  out['url'] = value.url;
  return out;
}

/** checklist = Checklist — Describes a checklist */
export class Checklist {
  type: 'checklist' = 'checklist';
  extra?: string;
  /** title: formattedText — Title of the checklist; may contain only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities */
  title: FormattedText | null = null;
  /** tasks: vector<checklistTask> — List of tasks in the checklist */
  tasks: ChecklistTask[] = [];
  /** others_can_add_tasks: Bool — True, if users other than creator of the list can add tasks to the list */
  others_can_add_tasks: boolean = false;
  /** can_add_tasks: Bool — True, if the current user can add tasks to the list if they have Telegram Premium subscription */
  can_add_tasks: boolean = false;
  /** others_can_mark_tasks_as_done: Bool — True, if users other than creator of the list can mark tasks as done or not done. If true, then the checklist is called "group checklist" */
  others_can_mark_tasks_as_done: boolean = false;
  /** can_mark_tasks_as_done: Bool — True, if the current user can mark tasks as done or not done if they have Telegram Premium subscription */
  can_mark_tasks_as_done: boolean = false;
}

/** Decode a TDLib `checklist` object; null input -> null. */
export function decodeChecklist(json: TdJsonRaw): Checklist | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Checklist = new Checklist();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = decodeFormattedText(record['title']);
  out.tasks = decodeVector(record['tasks'], (item: TdJsonRaw): ChecklistTask => decodeChecklistTask(item) as ChecklistTask);
  out.others_can_add_tasks = asBool(record['others_can_add_tasks']);
  out.can_add_tasks = asBool(record['can_add_tasks']);
  out.others_can_mark_tasks_as_done = asBool(record['others_can_mark_tasks_as_done']);
  out.can_mark_tasks_as_done = asBool(record['can_mark_tasks_as_done']);
  return out;
}

/** Encode a `checklist` into TDLib JSON wire shape. */
export function encodeChecklist(value: Checklist): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checklist';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title === null ? null : encodeFormattedText(value.title);
  out['tasks'] = encodeVector(value.tasks, (item: ChecklistTask | null): TdJsonRaw => item === null ? null : encodeChecklistTask(item));
  out['others_can_add_tasks'] = value.others_can_add_tasks;
  out['can_add_tasks'] = value.can_add_tasks;
  out['others_can_mark_tasks_as_done'] = value.others_can_mark_tasks_as_done;
  out['can_mark_tasks_as_done'] = value.can_mark_tasks_as_done;
  return out;
}

/** checklistTask = ChecklistTask — Describes a task in a checklist */
export class ChecklistTask {
  type: 'checklistTask' = 'checklistTask';
  extra?: string;
  /** id: int32 — Unique identifier of the task */
  id: number = 0;
  /** text: formattedText — Text of the task; may contain only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, DateTime and automatically found entities */
  text: FormattedText | null = null;
  /** completed_by: MessageSender — Identifier of the user or chat that completed the task; may be null if the task isn't completed yet */
  completed_by: TdMessageSender | null = null;
  /** completion_date: int32 — Point in time (Unix timestamp) when the task was completed; 0 if the task isn't completed */
  completion_date: number = 0;
}

/** Decode a TDLib `checklistTask` object; null input -> null. */
export function decodeChecklistTask(json: TdJsonRaw): ChecklistTask | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ChecklistTask = new ChecklistTask();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.text = decodeFormattedText(record['text']);
  out.completed_by = decodeTdMessageSender(record['completed_by']);
  out.completion_date = asNumber(record['completion_date']);
  return out;
}

/** Encode a `checklistTask` into TDLib JSON wire shape. */
export function encodeChecklistTask(value: ChecklistTask): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'checklistTask';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['completed_by'] = value.completed_by === null ? null : encodeTdMessageSender(value.completed_by);
  out['completion_date'] = value.completion_date;
  return out;
}

/** cleanFileName = Text — Removes potentially dangerous characters from the name of a file. Returns an empty string on failure. Can be called synchronously */
export class CleanFileName {
  type: 'cleanFileName' = 'cleanFileName';
  extra?: string;
  /** file_name: string — File name or path to the file */
  file_name: string = '';
}

/** Decode a TDLib `cleanFileName` object; null input -> null. */
export function decodeCleanFileName(json: TdJsonRaw): CleanFileName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CleanFileName = new CleanFileName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_name = asString(record['file_name']);
  return out;
}

/** Encode a `cleanFileName` into TDLib JSON wire shape. */
export function encodeCleanFileName(value: CleanFileName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'cleanFileName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_name'] = value.file_name;
  return out;
}

/** clearAllDraftMessages = Ok — Clears message drafts in all chats */
export class ClearAllDraftMessages {
  type: 'clearAllDraftMessages' = 'clearAllDraftMessages';
  extra?: string;
  /** exclude_secret_chats: Bool — Pass true to keep local message drafts in secret chats */
  exclude_secret_chats: boolean = false;
}

/** Decode a TDLib `clearAllDraftMessages` object; null input -> null. */
export function decodeClearAllDraftMessages(json: TdJsonRaw): ClearAllDraftMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClearAllDraftMessages = new ClearAllDraftMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.exclude_secret_chats = asBool(record['exclude_secret_chats']);
  return out;
}

/** Encode a `clearAllDraftMessages` into TDLib JSON wire shape. */
export function encodeClearAllDraftMessages(value: ClearAllDraftMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clearAllDraftMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['exclude_secret_chats'] = value.exclude_secret_chats;
  return out;
}

/** clearAutosaveSettingsExceptions = Ok — Clears the list of all autosave settings exceptions. The method is guaranteed to work only after at least one call to getAutosaveSettings */
export class ClearAutosaveSettingsExceptions {
  type: 'clearAutosaveSettingsExceptions' = 'clearAutosaveSettingsExceptions';
  extra?: string;
}

/** Decode a TDLib `clearAutosaveSettingsExceptions` object; null input -> null. */
export function decodeClearAutosaveSettingsExceptions(json: TdJsonRaw): ClearAutosaveSettingsExceptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClearAutosaveSettingsExceptions = new ClearAutosaveSettingsExceptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `clearAutosaveSettingsExceptions` into TDLib JSON wire shape. */
export function encodeClearAutosaveSettingsExceptions(value: ClearAutosaveSettingsExceptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clearAutosaveSettingsExceptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** clearImportedContacts = Ok — Clears all imported contacts, contact list remains unchanged */
export class ClearImportedContacts {
  type: 'clearImportedContacts' = 'clearImportedContacts';
  extra?: string;
}

/** Decode a TDLib `clearImportedContacts` object; null input -> null. */
export function decodeClearImportedContacts(json: TdJsonRaw): ClearImportedContacts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClearImportedContacts = new ClearImportedContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `clearImportedContacts` into TDLib JSON wire shape. */
export function encodeClearImportedContacts(value: ClearImportedContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clearImportedContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** clearRecentEmojiStatuses = Ok — Clears the list of recently used emoji statuses for self status */
export class ClearRecentEmojiStatuses {
  type: 'clearRecentEmojiStatuses' = 'clearRecentEmojiStatuses';
  extra?: string;
}

/** Decode a TDLib `clearRecentEmojiStatuses` object; null input -> null. */
export function decodeClearRecentEmojiStatuses(json: TdJsonRaw): ClearRecentEmojiStatuses | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClearRecentEmojiStatuses = new ClearRecentEmojiStatuses();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `clearRecentEmojiStatuses` into TDLib JSON wire shape. */
export function encodeClearRecentEmojiStatuses(value: ClearRecentEmojiStatuses): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clearRecentEmojiStatuses';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** clearRecentReactions = Ok — Clears the list of recently used reactions */
export class ClearRecentReactions {
  type: 'clearRecentReactions' = 'clearRecentReactions';
  extra?: string;
}

/** Decode a TDLib `clearRecentReactions` object; null input -> null. */
export function decodeClearRecentReactions(json: TdJsonRaw): ClearRecentReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClearRecentReactions = new ClearRecentReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `clearRecentReactions` into TDLib JSON wire shape. */
export function encodeClearRecentReactions(value: ClearRecentReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clearRecentReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** clearRecentStickers = Ok — Clears the list of recently used stickers */
export class ClearRecentStickers {
  type: 'clearRecentStickers' = 'clearRecentStickers';
  extra?: string;
  /** is_attached: Bool — Pass true to clear the list of stickers recently attached to photo or video files; pass false to clear the list of recently sent stickers */
  is_attached: boolean = false;
}

/** Decode a TDLib `clearRecentStickers` object; null input -> null. */
export function decodeClearRecentStickers(json: TdJsonRaw): ClearRecentStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClearRecentStickers = new ClearRecentStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_attached = asBool(record['is_attached']);
  return out;
}

/** Encode a `clearRecentStickers` into TDLib JSON wire shape. */
export function encodeClearRecentStickers(value: ClearRecentStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clearRecentStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_attached'] = value.is_attached;
  return out;
}

/** clearRecentlyFoundChats = Ok — Clears the list of recently found chats */
export class ClearRecentlyFoundChats {
  type: 'clearRecentlyFoundChats' = 'clearRecentlyFoundChats';
  extra?: string;
}

/** Decode a TDLib `clearRecentlyFoundChats` object; null input -> null. */
export function decodeClearRecentlyFoundChats(json: TdJsonRaw): ClearRecentlyFoundChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClearRecentlyFoundChats = new ClearRecentlyFoundChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `clearRecentlyFoundChats` into TDLib JSON wire shape. */
export function encodeClearRecentlyFoundChats(value: ClearRecentlyFoundChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clearRecentlyFoundChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** clearSearchedForTags = Ok — Clears the list of recently searched for hashtags or cashtags */
export class ClearSearchedForTags {
  type: 'clearSearchedForTags' = 'clearSearchedForTags';
  extra?: string;
  /** clear_cashtags: Bool — Pass true to clear the list of recently searched for cashtags; otherwise, the list of recently searched for hashtags will be cleared */
  clear_cashtags: boolean = false;
}

/** Decode a TDLib `clearSearchedForTags` object; null input -> null. */
export function decodeClearSearchedForTags(json: TdJsonRaw): ClearSearchedForTags | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClearSearchedForTags = new ClearSearchedForTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.clear_cashtags = asBool(record['clear_cashtags']);
  return out;
}

/** Encode a `clearSearchedForTags` into TDLib JSON wire shape. */
export function encodeClearSearchedForTags(value: ClearSearchedForTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clearSearchedForTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['clear_cashtags'] = value.clear_cashtags;
  return out;
}

/** clickAnimatedEmojiMessage = Sticker — Informs TDLib that a message with an animated emoji was clicked by the user. Returns a big animated sticker to be played or a 404 error if usual animation needs to be played */
export class ClickAnimatedEmojiMessage {
  type: 'clickAnimatedEmojiMessage' = 'clickAnimatedEmojiMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the clicked message */
  message_id: number = 0;
}

/** Decode a TDLib `clickAnimatedEmojiMessage` object; null input -> null. */
export function decodeClickAnimatedEmojiMessage(json: TdJsonRaw): ClickAnimatedEmojiMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClickAnimatedEmojiMessage = new ClickAnimatedEmojiMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `clickAnimatedEmojiMessage` into TDLib JSON wire shape. */
export function encodeClickAnimatedEmojiMessage(value: ClickAnimatedEmojiMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clickAnimatedEmojiMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** clickChatSponsoredMessage = Ok — Informs TDLib that the user opened the sponsored chat via the button, the name, the chat photo, a mention in the sponsored message text, or the media in the sponsored message */
export class ClickChatSponsoredMessage {
  type: 'clickChatSponsoredMessage' = 'clickChatSponsoredMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the sponsored message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the sponsored message */
  message_id: number = 0;
  /** is_media_click: Bool — Pass true if the media was clicked in the sponsored message */
  is_media_click: boolean = false;
  /** from_fullscreen: Bool — Pass true if the user expanded the video from the sponsored message fullscreen before the click */
  from_fullscreen: boolean = false;
}

/** Decode a TDLib `clickChatSponsoredMessage` object; null input -> null. */
export function decodeClickChatSponsoredMessage(json: TdJsonRaw): ClickChatSponsoredMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClickChatSponsoredMessage = new ClickChatSponsoredMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.is_media_click = asBool(record['is_media_click']);
  out.from_fullscreen = asBool(record['from_fullscreen']);
  return out;
}

/** Encode a `clickChatSponsoredMessage` into TDLib JSON wire shape. */
export function encodeClickChatSponsoredMessage(value: ClickChatSponsoredMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clickChatSponsoredMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['is_media_click'] = value.is_media_click;
  out['from_fullscreen'] = value.from_fullscreen;
  return out;
}

/** clickPremiumSubscriptionButton = Ok — Informs TDLib that the user clicked Premium subscription button on the Premium features screen */
export class ClickPremiumSubscriptionButton {
  type: 'clickPremiumSubscriptionButton' = 'clickPremiumSubscriptionButton';
  extra?: string;
}

/** Decode a TDLib `clickPremiumSubscriptionButton` object; null input -> null. */
export function decodeClickPremiumSubscriptionButton(json: TdJsonRaw): ClickPremiumSubscriptionButton | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClickPremiumSubscriptionButton = new ClickPremiumSubscriptionButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `clickPremiumSubscriptionButton` into TDLib JSON wire shape. */
export function encodeClickPremiumSubscriptionButton(value: ClickPremiumSubscriptionButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clickPremiumSubscriptionButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** clickVideoMessageAdvertisement = Ok — Informs TDLib that the user clicked a video message advertisement */
export class ClickVideoMessageAdvertisement {
  type: 'clickVideoMessageAdvertisement' = 'clickVideoMessageAdvertisement';
  extra?: string;
  /** advertisement_unique_id: int53 — Unique identifier of the advertisement */
  advertisement_unique_id: number = 0;
}

/** Decode a TDLib `clickVideoMessageAdvertisement` object; null input -> null. */
export function decodeClickVideoMessageAdvertisement(json: TdJsonRaw): ClickVideoMessageAdvertisement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClickVideoMessageAdvertisement = new ClickVideoMessageAdvertisement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.advertisement_unique_id = asNumber(record['advertisement_unique_id']);
  return out;
}

/** Encode a `clickVideoMessageAdvertisement` into TDLib JSON wire shape. */
export function encodeClickVideoMessageAdvertisement(value: ClickVideoMessageAdvertisement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'clickVideoMessageAdvertisement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['advertisement_unique_id'] = value.advertisement_unique_id;
  return out;
}

/** close = Ok — Closes the TDLib instance. All databases will be flushed to disk and properly closed. After the close completes, updateAuthorizationState with authorizationStateClosed will be sent. Can be called before initialization */
export class Close {
  type: 'close' = 'close';
  extra?: string;
}

/** Decode a TDLib `close` object; null input -> null. */
export function decodeClose(json: TdJsonRaw): Close | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Close = new Close();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `close` into TDLib JSON wire shape. */
export function encodeClose(value: Close): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'close';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** closeBirthdayUser = CloseBirthdayUser — Describes a user who had or will have a birthday soon */
export class CloseBirthdayUser {
  type: 'closeBirthdayUser' = 'closeBirthdayUser';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** birthdate: birthdate — Birthdate of the user */
  birthdate: Birthdate | null = null;
}

/** Decode a TDLib `closeBirthdayUser` object; null input -> null. */
export function decodeCloseBirthdayUser(json: TdJsonRaw): CloseBirthdayUser | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CloseBirthdayUser = new CloseBirthdayUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.birthdate = decodeBirthdate(record['birthdate']);
  return out;
}

/** Encode a `closeBirthdayUser` into TDLib JSON wire shape. */
export function encodeCloseBirthdayUser(value: CloseBirthdayUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'closeBirthdayUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['birthdate'] = value.birthdate === null ? null : encodeBirthdate(value.birthdate);
  return out;
}

/** closeChat = Ok — Informs TDLib that the chat is closed by the user. Many useful activities depend on the chat being opened or closed */
export class CloseChat {
  type: 'closeChat' = 'closeChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `closeChat` object; null input -> null. */
export function decodeCloseChat(json: TdJsonRaw): CloseChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CloseChat = new CloseChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `closeChat` into TDLib JSON wire shape. */
export function encodeCloseChat(value: CloseChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'closeChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** closeGiftAuction = Ok — Informs TDLib that a gift auction was closed by the user */
export class CloseGiftAuction {
  type: 'closeGiftAuction' = 'closeGiftAuction';
  extra?: string;
  /** gift_id: int64 — Identifier of the gift, which auction was closed */
  gift_id: string = '';
}

/** Decode a TDLib `closeGiftAuction` object; null input -> null. */
export function decodeCloseGiftAuction(json: TdJsonRaw): CloseGiftAuction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CloseGiftAuction = new CloseGiftAuction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_id = asInt64(record['gift_id']);
  return out;
}

/** Encode a `closeGiftAuction` into TDLib JSON wire shape. */
export function encodeCloseGiftAuction(value: CloseGiftAuction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'closeGiftAuction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_id'] = value.gift_id;
  return out;
}

/** closeSecretChat = Ok — Closes a secret chat, effectively transferring its state to secretChatStateClosed */
export class CloseSecretChat {
  type: 'closeSecretChat' = 'closeSecretChat';
  extra?: string;
  /** secret_chat_id: int32 — Secret chat identifier */
  secret_chat_id: number = 0;
}

/** Decode a TDLib `closeSecretChat` object; null input -> null. */
export function decodeCloseSecretChat(json: TdJsonRaw): CloseSecretChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CloseSecretChat = new CloseSecretChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.secret_chat_id = asNumber(record['secret_chat_id']);
  return out;
}

/** Encode a `closeSecretChat` into TDLib JSON wire shape. */
export function encodeCloseSecretChat(value: CloseSecretChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'closeSecretChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['secret_chat_id'] = value.secret_chat_id;
  return out;
}

/** closeStory = Ok — Informs TDLib that a story is closed by the user */
export class CloseStory {
  type: 'closeStory' = 'closeStory';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the poster of the story to close */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — The identifier of the story */
  story_id: number = 0;
}

/** Decode a TDLib `closeStory` object; null input -> null. */
export function decodeCloseStory(json: TdJsonRaw): CloseStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CloseStory = new CloseStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `closeStory` into TDLib JSON wire shape. */
export function encodeCloseStory(value: CloseStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'closeStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** closeWebApp = Ok — Informs TDLib that a previously opened Web App was closed */
export class CloseWebApp {
  type: 'closeWebApp' = 'closeWebApp';
  extra?: string;
  /** web_app_launch_id: int64 — Identifier of Web App launch, received from openWebApp */
  web_app_launch_id: string = '';
}

/** Decode a TDLib `closeWebApp` object; null input -> null. */
export function decodeCloseWebApp(json: TdJsonRaw): CloseWebApp | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CloseWebApp = new CloseWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.web_app_launch_id = asInt64(record['web_app_launch_id']);
  return out;
}

/** Encode a `closeWebApp` into TDLib JSON wire shape. */
export function encodeCloseWebApp(value: CloseWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'closeWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['web_app_launch_id'] = value.web_app_launch_id;
  return out;
}

/** closedVectorPath = ClosedVectorPath — Represents a closed vector path. The path begins at the end point of the last command. The coordinate system origin is in the upper-left corner */
export class ClosedVectorPath {
  type: 'closedVectorPath' = 'closedVectorPath';
  extra?: string;
  /** commands: vector<VectorPathCommand> — List of vector path commands */
  commands: TdVectorPathCommand[] = [];
}

/** Decode a TDLib `closedVectorPath` object; null input -> null. */
export function decodeClosedVectorPath(json: TdJsonRaw): ClosedVectorPath | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ClosedVectorPath = new ClosedVectorPath();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.commands = decodeVector(record['commands'], (item: TdJsonRaw): TdVectorPathCommand => decodeTdVectorPathCommand(item) as TdVectorPathCommand);
  return out;
}

/** Encode a `closedVectorPath` into TDLib JSON wire shape. */
export function encodeClosedVectorPath(value: ClosedVectorPath): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'closedVectorPath';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['commands'] = encodeVector(value.commands, (item: TdVectorPathCommand | null): TdJsonRaw => item === null ? null : encodeTdVectorPathCommand(item));
  return out;
}

/** collectibleItemInfo = CollectibleItemInfo — Contains information about a collectible item and its last purchase */
export class CollectibleItemInfo {
  type: 'collectibleItemInfo' = 'collectibleItemInfo';
  extra?: string;
  /** purchase_date: int32 — Point in time (Unix timestamp) when the item was purchased */
  purchase_date: number = 0;
  /** currency: string — Currency for the paid amount */
  currency: string = '';
  /** amount: int53 — The paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** cryptocurrency: string — Cryptocurrency used to pay for the item */
  cryptocurrency: string = '';
  /** cryptocurrency_amount: int64 — The paid amount, in the smallest units of the cryptocurrency */
  cryptocurrency_amount: string = '';
  /** url: string — Individual URL for the item on https://fragment.com */
  url: string = '';
}

/** Decode a TDLib `collectibleItemInfo` object; null input -> null. */
export function decodeCollectibleItemInfo(json: TdJsonRaw): CollectibleItemInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CollectibleItemInfo = new CollectibleItemInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.purchase_date = asNumber(record['purchase_date']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.cryptocurrency = asString(record['cryptocurrency']);
  out.cryptocurrency_amount = asInt64(record['cryptocurrency_amount']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `collectibleItemInfo` into TDLib JSON wire shape. */
export function encodeCollectibleItemInfo(value: CollectibleItemInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'collectibleItemInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['purchase_date'] = value.purchase_date;
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['cryptocurrency'] = value.cryptocurrency;
  out['cryptocurrency_amount'] = value.cryptocurrency_amount;
  out['url'] = value.url;
  return out;
}

/** collectibleItemTypePhoneNumber = CollectibleItemType — A phone number */
export class CollectibleItemTypePhoneNumber {
  type: 'collectibleItemTypePhoneNumber' = 'collectibleItemTypePhoneNumber';
  extra?: string;
  /** phone_number: string — The phone number */
  phone_number: string = '';
}

/** Decode a TDLib `collectibleItemTypePhoneNumber` object; null input -> null. */
export function decodeCollectibleItemTypePhoneNumber(json: TdJsonRaw): CollectibleItemTypePhoneNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CollectibleItemTypePhoneNumber = new CollectibleItemTypePhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  return out;
}

/** Encode a `collectibleItemTypePhoneNumber` into TDLib JSON wire shape. */
export function encodeCollectibleItemTypePhoneNumber(value: CollectibleItemTypePhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'collectibleItemTypePhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  return out;
}

/** collectibleItemTypeUsername = CollectibleItemType — A username */
export class CollectibleItemTypeUsername {
  type: 'collectibleItemTypeUsername' = 'collectibleItemTypeUsername';
  extra?: string;
  /** username: string — The username */
  username: string = '';
}

/** Decode a TDLib `collectibleItemTypeUsername` object; null input -> null. */
export function decodeCollectibleItemTypeUsername(json: TdJsonRaw): CollectibleItemTypeUsername | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CollectibleItemTypeUsername = new CollectibleItemTypeUsername();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  return out;
}

/** Encode a `collectibleItemTypeUsername` into TDLib JSON wire shape. */
export function encodeCollectibleItemTypeUsername(value: CollectibleItemTypeUsername): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'collectibleItemTypeUsername';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  return out;
}

/** commitPendingLiveStoryReactions = Ok — Applies all pending paid reactions in a live story group call */
export class CommitPendingLiveStoryReactions {
  type: 'commitPendingLiveStoryReactions' = 'commitPendingLiveStoryReactions';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `commitPendingLiveStoryReactions` object; null input -> null. */
export function decodeCommitPendingLiveStoryReactions(json: TdJsonRaw): CommitPendingLiveStoryReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommitPendingLiveStoryReactions = new CommitPendingLiveStoryReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `commitPendingLiveStoryReactions` into TDLib JSON wire shape. */
export function encodeCommitPendingLiveStoryReactions(value: CommitPendingLiveStoryReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'commitPendingLiveStoryReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** commitPendingPaidMessageReactions = Ok — Applies all pending paid reactions on a message */
export class CommitPendingPaidMessageReactions {
  type: 'commitPendingPaidMessageReactions' = 'commitPendingPaidMessageReactions';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
}

/** Decode a TDLib `commitPendingPaidMessageReactions` object; null input -> null. */
export function decodeCommitPendingPaidMessageReactions(json: TdJsonRaw): CommitPendingPaidMessageReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommitPendingPaidMessageReactions = new CommitPendingPaidMessageReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `commitPendingPaidMessageReactions` into TDLib JSON wire shape. */
export function encodeCommitPendingPaidMessageReactions(value: CommitPendingPaidMessageReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'commitPendingPaidMessageReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** community = Community — Represents a community consisting of supergroup chats, channel chats and chats with bots */
export class Community {
  type: 'community' = 'community';
  extra?: string;
  /** id: int53 — Community identifier */
  id: number = 0;
  /** have_access: Bool — If false, the community is inaccessible, and the only information known about the community is inside this class. Identifier of the community can't be passed to any method */
  have_access: boolean = false;
  /** name: string — Community name */
  name: string = '';
  /** photo: chatPhotoInfo — Community photo; may be null */
  photo: ChatPhotoInfo | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the community was joined, or the point in time when the community was created, in case the user is not a member of any chat in the community */
  date: number = 0;
  /** status: CommunityMemberStatus — Status of the current user in the community */
  status: TdCommunityMemberStatus | null = null;
  /** permissions: communityPermissions — Actions that non-administrator community members are allowed to take in the community */
  permissions: CommunityPermissions | null = null;
}

/** Decode a TDLib `community` object; null input -> null. */
export function decodeCommunity(json: TdJsonRaw): Community | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Community = new Community();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.have_access = asBool(record['have_access']);
  out.name = asString(record['name']);
  out.photo = decodeChatPhotoInfo(record['photo']);
  out.date = asNumber(record['date']);
  out.status = decodeTdCommunityMemberStatus(record['status']);
  out.permissions = decodeCommunityPermissions(record['permissions']);
  return out;
}

/** Encode a `community` into TDLib JSON wire shape. */
export function encodeCommunity(value: Community): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'community';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['have_access'] = value.have_access;
  out['name'] = value.name;
  out['photo'] = value.photo === null ? null : encodeChatPhotoInfo(value.photo);
  out['date'] = value.date;
  out['status'] = value.status === null ? null : encodeTdCommunityMemberStatus(value.status);
  out['permissions'] = value.permissions === null ? null : encodeCommunityPermissions(value.permissions);
  return out;
}

/** communityAdministratorRights = CommunityAdministratorRights — Describes rights of the administrator in a community */
export class CommunityAdministratorRights {
  type: 'communityAdministratorRights' = 'communityAdministratorRights';
  extra?: string;
  /** can_manage_community: Bool — True, if the user is an administrator. Implied by any other privilege */
  can_manage_community: boolean = false;
  /** can_change_info: Bool — True, if the administrator can change the community name, photo, and other settings */
  can_change_info: boolean = false;
  /** can_edit_chat_list: Bool — True, if the user can change the chats added to the community */
  can_edit_chat_list: boolean = false;
  /** can_promote_members: Bool — True, if the administrator can add new administrators with a subset of their own privileges or demote administrators that were directly or indirectly promoted by them */
  can_promote_members: boolean = false;
  /** can_ban_members: Bool — True, if the administrator can ban, or unban community members */
  can_ban_members: boolean = false;
}

/** Decode a TDLib `communityAdministratorRights` object; null input -> null. */
export function decodeCommunityAdministratorRights(json: TdJsonRaw): CommunityAdministratorRights | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityAdministratorRights = new CommunityAdministratorRights();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_manage_community = asBool(record['can_manage_community']);
  out.can_change_info = asBool(record['can_change_info']);
  out.can_edit_chat_list = asBool(record['can_edit_chat_list']);
  out.can_promote_members = asBool(record['can_promote_members']);
  out.can_ban_members = asBool(record['can_ban_members']);
  return out;
}

/** Encode a `communityAdministratorRights` into TDLib JSON wire shape. */
export function encodeCommunityAdministratorRights(value: CommunityAdministratorRights): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityAdministratorRights';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_manage_community'] = value.can_manage_community;
  out['can_change_info'] = value.can_change_info;
  out['can_edit_chat_list'] = value.can_edit_chat_list;
  out['can_promote_members'] = value.can_promote_members;
  out['can_ban_members'] = value.can_ban_members;
  return out;
}

/** communityChat = CommunityChat — Describes a chat in a community */
export class CommunityChat {
  type: 'communityChat' = 'communityChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in the community */
  chat_id: number = 0;
  /** can_view_history: Bool — True, if message history of the chat can be viewed */
  can_view_history: boolean = false;
  /** is_hidden: Bool — True, if the chat is hidden in the list of community chats; for community administrators only */
  is_hidden: boolean = false;
}

/** Decode a TDLib `communityChat` object; null input -> null. */
export function decodeCommunityChat(json: TdJsonRaw): CommunityChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityChat = new CommunityChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.can_view_history = asBool(record['can_view_history']);
  out.is_hidden = asBool(record['is_hidden']);
  return out;
}

/** Encode a `communityChat` into TDLib JSON wire shape. */
export function encodeCommunityChat(value: CommunityChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['can_view_history'] = value.can_view_history;
  out['is_hidden'] = value.is_hidden;
  return out;
}

/** communityFullInfo = CommunityFullInfo — Contains full information about a community */
export class CommunityFullInfo {
  type: 'communityFullInfo' = 'communityFullInfo';
  extra?: string;
  /** photo: chatPhoto — Photo of the community */
  photo: ChatPhoto | null = null;
  /** chats: vector<communityChat> — Chats belonging to the community */
  chats: CommunityChat[] = [];
  /** administrator_count: int32 — Number of privileged users in the community; 0 if the current user isn't an administrator of the community */
  administrator_count: number = 0;
  /** banned_count: int32 — Number of users banned from the community; 0 if the current user isn't an administrator of the community */
  banned_count: number = 0;
  /** add_chat_request_count: int32 — Number of pending requests for addition of chats to the community; 0 if the current user isn't an administrator of the community */
  add_chat_request_count: number = 0;
}

/** Decode a TDLib `communityFullInfo` object; null input -> null. */
export function decodeCommunityFullInfo(json: TdJsonRaw): CommunityFullInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityFullInfo = new CommunityFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  out.chats = decodeVector(record['chats'], (item: TdJsonRaw): CommunityChat => decodeCommunityChat(item) as CommunityChat);
  out.administrator_count = asNumber(record['administrator_count']);
  out.banned_count = asNumber(record['banned_count']);
  out.add_chat_request_count = asNumber(record['add_chat_request_count']);
  return out;
}

/** Encode a `communityFullInfo` into TDLib JSON wire shape. */
export function encodeCommunityFullInfo(value: CommunityFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  out['chats'] = encodeVector(value.chats, (item: CommunityChat | null): TdJsonRaw => item === null ? null : encodeCommunityChat(item));
  out['administrator_count'] = value.administrator_count;
  out['banned_count'] = value.banned_count;
  out['add_chat_request_count'] = value.add_chat_request_count;
  return out;
}

/** communityId = CommunityId — Contains identifier of a community */
export class CommunityId {
  type: 'communityId' = 'communityId';
  extra?: string;
  /** id: int53 — Community identifier */
  id: number = 0;
}

/** Decode a TDLib `communityId` object; null input -> null. */
export function decodeCommunityId(json: TdJsonRaw): CommunityId | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityId = new CommunityId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  return out;
}

/** Encode a `communityId` into TDLib JSON wire shape. */
export function encodeCommunityId(value: CommunityId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  return out;
}

/** communityMemberStatusAdministrator = CommunityMemberStatus — The user is a member of the community and has some additional privileges */
export class CommunityMemberStatusAdministrator {
  type: 'communityMemberStatusAdministrator' = 'communityMemberStatusAdministrator';
  extra?: string;
  /** can_be_edited: Bool — True, if the current user can edit the administrator privileges for the called user */
  can_be_edited: boolean = false;
  /** rights: communityAdministratorRights — Rights of the administrator */
  rights: CommunityAdministratorRights | null = null;
}

/** Decode a TDLib `communityMemberStatusAdministrator` object; null input -> null. */
export function decodeCommunityMemberStatusAdministrator(json: TdJsonRaw): CommunityMemberStatusAdministrator | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityMemberStatusAdministrator = new CommunityMemberStatusAdministrator();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_be_edited = asBool(record['can_be_edited']);
  out.rights = decodeCommunityAdministratorRights(record['rights']);
  return out;
}

/** Encode a `communityMemberStatusAdministrator` into TDLib JSON wire shape. */
export function encodeCommunityMemberStatusAdministrator(value: CommunityMemberStatusAdministrator): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityMemberStatusAdministrator';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_be_edited'] = value.can_be_edited;
  out['rights'] = value.rights === null ? null : encodeCommunityAdministratorRights(value.rights);
  return out;
}

/** communityMemberStatusBanned = CommunityMemberStatus — The user or the chat was banned in the community; implies ban in all chats in the community */
export class CommunityMemberStatusBanned {
  type: 'communityMemberStatusBanned' = 'communityMemberStatusBanned';
  extra?: string;
}

/** Decode a TDLib `communityMemberStatusBanned` object; null input -> null. */
export function decodeCommunityMemberStatusBanned(json: TdJsonRaw): CommunityMemberStatusBanned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityMemberStatusBanned = new CommunityMemberStatusBanned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `communityMemberStatusBanned` into TDLib JSON wire shape. */
export function encodeCommunityMemberStatusBanned(value: CommunityMemberStatusBanned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityMemberStatusBanned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** communityMemberStatusCreator = CommunityMemberStatus — The user is the owner of the community and has all the administrator privileges */
export class CommunityMemberStatusCreator {
  type: 'communityMemberStatusCreator' = 'communityMemberStatusCreator';
  extra?: string;
}

/** Decode a TDLib `communityMemberStatusCreator` object; null input -> null. */
export function decodeCommunityMemberStatusCreator(json: TdJsonRaw): CommunityMemberStatusCreator | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityMemberStatusCreator = new CommunityMemberStatusCreator();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `communityMemberStatusCreator` into TDLib JSON wire shape. */
export function encodeCommunityMemberStatusCreator(value: CommunityMemberStatusCreator): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityMemberStatusCreator';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** communityMemberStatusLeft = CommunityMemberStatus — The user or the chat is not a community member */
export class CommunityMemberStatusLeft {
  type: 'communityMemberStatusLeft' = 'communityMemberStatusLeft';
  extra?: string;
}

/** Decode a TDLib `communityMemberStatusLeft` object; null input -> null. */
export function decodeCommunityMemberStatusLeft(json: TdJsonRaw): CommunityMemberStatusLeft | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityMemberStatusLeft = new CommunityMemberStatusLeft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `communityMemberStatusLeft` into TDLib JSON wire shape. */
export function encodeCommunityMemberStatusLeft(value: CommunityMemberStatusLeft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityMemberStatusLeft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** communityMemberStatusMember = CommunityMemberStatus — The user is a member of the community, without any additional privileges or restrictions */
export class CommunityMemberStatusMember {
  type: 'communityMemberStatusMember' = 'communityMemberStatusMember';
  extra?: string;
}

/** Decode a TDLib `communityMemberStatusMember` object; null input -> null. */
export function decodeCommunityMemberStatusMember(json: TdJsonRaw): CommunityMemberStatusMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityMemberStatusMember = new CommunityMemberStatusMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `communityMemberStatusMember` into TDLib JSON wire shape. */
export function encodeCommunityMemberStatusMember(value: CommunityMemberStatusMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityMemberStatusMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** communityPermissions = CommunityPermissions — Describes actions that a user is allowed to take in a community */
export class CommunityPermissions {
  type: 'communityPermissions' = 'communityPermissions';
  extra?: string;
  /** can_edit_chat_list: Bool — True, if the user can change the chats added to the community */
  can_edit_chat_list: boolean = false;
}

/** Decode a TDLib `communityPermissions` object; null input -> null. */
export function decodeCommunityPermissions(json: TdJsonRaw): CommunityPermissions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CommunityPermissions = new CommunityPermissions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_edit_chat_list = asBool(record['can_edit_chat_list']);
  return out;
}

/** Encode a `communityPermissions` into TDLib JSON wire shape. */
export function encodeCommunityPermissions(value: CommunityPermissions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'communityPermissions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_edit_chat_list'] = value.can_edit_chat_list;
  return out;
}

/** composeRichMessageWithAi = RichMessage — Changes a rich message using an AI model. May return an error with a message "AICOMPOSE_FLOOD_PREMIUM" if Telegram Premium is required to send further requests */
export class ComposeRichMessageWithAi {
  type: 'composeRichMessageWithAi' = 'composeRichMessageWithAi';
  extra?: string;
  /** message: inputRichMessage — The original message */
  message: InputRichMessage | null = null;
  /** translate_to_language_code: string — Pass a language code to which the text will be translated; pass an empty string if translation isn't needed. See translateText.to_language_code for the list of supported values */
  translate_to_language_code: string = '';
  /** style_name: string — Name of the style of the resulted text; handle updateTextCompositionStyles to get the list of supported styles; pass an empty string to keep the current style of the text or if a custom prompt is used */
  style_name: string = '';
  /** custom_prompt: string — Custom prompt that will be used instead of style_name; 0-getOption("text_composition_style_prompt_length_max") characters */
  custom_prompt: string = '';
  /** add_emojis: Bool — Pass true to add emoji to the text */
  add_emojis: boolean = false;
}

/** Decode a TDLib `composeRichMessageWithAi` object; null input -> null. */
export function decodeComposeRichMessageWithAi(json: TdJsonRaw): ComposeRichMessageWithAi | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ComposeRichMessageWithAi = new ComposeRichMessageWithAi();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeInputRichMessage(record['message']);
  out.translate_to_language_code = asString(record['translate_to_language_code']);
  out.style_name = asString(record['style_name']);
  out.custom_prompt = asString(record['custom_prompt']);
  out.add_emojis = asBool(record['add_emojis']);
  return out;
}

/** Encode a `composeRichMessageWithAi` into TDLib JSON wire shape. */
export function encodeComposeRichMessageWithAi(value: ComposeRichMessageWithAi): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'composeRichMessageWithAi';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeInputRichMessage(value.message);
  out['translate_to_language_code'] = value.translate_to_language_code;
  out['style_name'] = value.style_name;
  out['custom_prompt'] = value.custom_prompt;
  out['add_emojis'] = value.add_emojis;
  return out;
}

/** composeTextWithAi = FormattedText — Changes text using an AI model; must not be used in secret chats. May return an error with a message "AICOMPOSE_FLOOD_PREMIUM" if Telegram Premium is required to send further requests */
export class ComposeTextWithAi {
  type: 'composeTextWithAi' = 'composeTextWithAi';
  extra?: string;
  /** text: formattedText — The original text */
  text: FormattedText | null = null;
  /** translate_to_language_code: string — Pass a language code to which the text will be translated; pass an empty string if translation isn't needed. See translateText.to_language_code for the list of supported values */
  translate_to_language_code: string = '';
  /** style_name: string — Name of the style of the resulted text; handle updateTextCompositionStyles to get the list of supported styles; pass an empty string to keep the current style of the text */
  style_name: string = '';
  /** add_emojis: Bool — Pass true to add emoji to the text */
  add_emojis: boolean = false;
}

/** Decode a TDLib `composeTextWithAi` object; null input -> null. */
export function decodeComposeTextWithAi(json: TdJsonRaw): ComposeTextWithAi | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ComposeTextWithAi = new ComposeTextWithAi();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.translate_to_language_code = asString(record['translate_to_language_code']);
  out.style_name = asString(record['style_name']);
  out.add_emojis = asBool(record['add_emojis']);
  return out;
}

/** Encode a `composeTextWithAi` into TDLib JSON wire shape. */
export function encodeComposeTextWithAi(value: ComposeTextWithAi): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'composeTextWithAi';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['translate_to_language_code'] = value.translate_to_language_code;
  out['style_name'] = value.style_name;
  out['add_emojis'] = value.add_emojis;
  return out;
}

/** confirmBusinessConnectedBot = Ok — Confirms an unconfirmed business connection of the current user from another device */
export class ConfirmBusinessConnectedBot {
  type: 'confirmBusinessConnectedBot' = 'confirmBusinessConnectedBot';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the bot */
  bot_user_id: number = 0;
}

/** Decode a TDLib `confirmBusinessConnectedBot` object; null input -> null. */
export function decodeConfirmBusinessConnectedBot(json: TdJsonRaw): ConfirmBusinessConnectedBot | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConfirmBusinessConnectedBot = new ConfirmBusinessConnectedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `confirmBusinessConnectedBot` into TDLib JSON wire shape. */
export function encodeConfirmBusinessConnectedBot(value: ConfirmBusinessConnectedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'confirmBusinessConnectedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** confirmQrCodeAuthentication = Session — Confirms QR code authentication on another device. Returns created session on success */
export class ConfirmQrCodeAuthentication {
  type: 'confirmQrCodeAuthentication' = 'confirmQrCodeAuthentication';
  extra?: string;
  /** link: string — A link from a QR code. The link must be scanned by the in-app camera */
  link: string = '';
}

/** Decode a TDLib `confirmQrCodeAuthentication` object; null input -> null. */
export function decodeConfirmQrCodeAuthentication(json: TdJsonRaw): ConfirmQrCodeAuthentication | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConfirmQrCodeAuthentication = new ConfirmQrCodeAuthentication();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  return out;
}

/** Encode a `confirmQrCodeAuthentication` into TDLib JSON wire shape. */
export function encodeConfirmQrCodeAuthentication(value: ConfirmQrCodeAuthentication): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'confirmQrCodeAuthentication';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  return out;
}

/** confirmSession = Ok — Confirms an unconfirmed session of the current user from another device */
export class ConfirmSession {
  type: 'confirmSession' = 'confirmSession';
  extra?: string;
  /** session_id: int64 — Session identifier */
  session_id: string = '';
}

/** Decode a TDLib `confirmSession` object; null input -> null. */
export function decodeConfirmSession(json: TdJsonRaw): ConfirmSession | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConfirmSession = new ConfirmSession();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.session_id = asInt64(record['session_id']);
  return out;
}

/** Encode a `confirmSession` into TDLib JSON wire shape. */
export function encodeConfirmSession(value: ConfirmSession): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'confirmSession';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['session_id'] = value.session_id;
  return out;
}

/** connectAffiliateProgram = ConnectedAffiliateProgram — Connects an affiliate program to the given affiliate. Returns information about the connected affiliate program */
export class ConnectAffiliateProgram {
  type: 'connectAffiliateProgram' = 'connectAffiliateProgram';
  extra?: string;
  /** affiliate: AffiliateType — The affiliate to which the affiliate program will be connected */
  affiliate: TdAffiliateType | null = null;
  /** bot_user_id: int53 — Identifier of the bot, which affiliate program is connected */
  bot_user_id: number = 0;
}

/** Decode a TDLib `connectAffiliateProgram` object; null input -> null. */
export function decodeConnectAffiliateProgram(json: TdJsonRaw): ConnectAffiliateProgram | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectAffiliateProgram = new ConnectAffiliateProgram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.affiliate = decodeTdAffiliateType(record['affiliate']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `connectAffiliateProgram` into TDLib JSON wire shape. */
export function encodeConnectAffiliateProgram(value: ConnectAffiliateProgram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectAffiliateProgram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['affiliate'] = value.affiliate === null ? null : encodeTdAffiliateType(value.affiliate);
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** connectedAffiliateProgram = ConnectedAffiliateProgram — Describes an affiliate program that was connected to an affiliate */
export class ConnectedAffiliateProgram {
  type: 'connectedAffiliateProgram' = 'connectedAffiliateProgram';
  extra?: string;
  /** url: string — The link that can be used to refer users if the program is still active */
  url: string = '';
  /** bot_user_id: int53 — User identifier of the bot created the program */
  bot_user_id: number = 0;
  /** parameters: affiliateProgramParameters — The parameters of the affiliate program */
  parameters: AffiliateProgramParameters | null = null;
  /** connection_date: int32 — Point in time (Unix timestamp) when the affiliate program was connected */
  connection_date: number = 0;
  /** is_disconnected: Bool — True, if the program was canceled by the bot, or disconnected by the chat owner and isn't available anymore */
  is_disconnected: boolean = false;
  /** user_count: int64 — The number of users that used the affiliate program */
  user_count: string = '';
  /** revenue_star_count: int53 — The number of Telegram Stars that were earned by the affiliate program */
  revenue_star_count: number = 0;
}

/** Decode a TDLib `connectedAffiliateProgram` object; null input -> null. */
export function decodeConnectedAffiliateProgram(json: TdJsonRaw): ConnectedAffiliateProgram | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectedAffiliateProgram = new ConnectedAffiliateProgram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.parameters = decodeAffiliateProgramParameters(record['parameters']);
  out.connection_date = asNumber(record['connection_date']);
  out.is_disconnected = asBool(record['is_disconnected']);
  out.user_count = asInt64(record['user_count']);
  out.revenue_star_count = asNumber(record['revenue_star_count']);
  return out;
}

/** Encode a `connectedAffiliateProgram` into TDLib JSON wire shape. */
export function encodeConnectedAffiliateProgram(value: ConnectedAffiliateProgram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectedAffiliateProgram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['bot_user_id'] = value.bot_user_id;
  out['parameters'] = value.parameters === null ? null : encodeAffiliateProgramParameters(value.parameters);
  out['connection_date'] = value.connection_date;
  out['is_disconnected'] = value.is_disconnected;
  out['user_count'] = value.user_count;
  out['revenue_star_count'] = value.revenue_star_count;
  return out;
}

/** connectedAffiliatePrograms = ConnectedAffiliatePrograms — Represents a list of affiliate programs that were connected to an affiliate */
export class ConnectedAffiliatePrograms {
  type: 'connectedAffiliatePrograms' = 'connectedAffiliatePrograms';
  extra?: string;
  /** total_count: int32 — The total number of affiliate programs that were connected to the affiliate */
  total_count: number = 0;
  /** programs: vector<connectedAffiliateProgram> — The list of connected affiliate programs */
  programs: ConnectedAffiliateProgram[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `connectedAffiliatePrograms` object; null input -> null. */
export function decodeConnectedAffiliatePrograms(json: TdJsonRaw): ConnectedAffiliatePrograms | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectedAffiliatePrograms = new ConnectedAffiliatePrograms();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.programs = decodeVector(record['programs'], (item: TdJsonRaw): ConnectedAffiliateProgram => decodeConnectedAffiliateProgram(item) as ConnectedAffiliateProgram);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `connectedAffiliatePrograms` into TDLib JSON wire shape. */
export function encodeConnectedAffiliatePrograms(value: ConnectedAffiliatePrograms): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectedAffiliatePrograms';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['programs'] = encodeVector(value.programs, (item: ConnectedAffiliateProgram | null): TdJsonRaw => item === null ? null : encodeConnectedAffiliateProgram(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** connectedWebsite = ConnectedWebsite — Contains information about one website the current user is logged in with Telegram */
export class ConnectedWebsite {
  type: 'connectedWebsite' = 'connectedWebsite';
  extra?: string;
  /** id: int64 — Website identifier */
  id: string = '';
  /** domain_name: string — The domain name of the website */
  domain_name: string = '';
  /** bot_user_id: int53 — User identifier of a bot linked with the website */
  bot_user_id: number = 0;
  /** browser: string — The version of a browser used to log in */
  browser: string = '';
  /** platform: string — Operating system the browser is running on */
  platform: string = '';
  /** log_in_date: int32 — Point in time (Unix timestamp) when the user was logged in */
  log_in_date: number = 0;
  /** last_active_date: int32 — Point in time (Unix timestamp) when obtained authorization was last used */
  last_active_date: number = 0;
  /** ip_address: string — IP address from which the user was logged in, in human-readable format */
  ip_address: string = '';
  /** location: string — Human-readable description of a country and a region from which the user was logged in, based on the IP address */
  location: string = '';
}

/** Decode a TDLib `connectedWebsite` object; null input -> null. */
export function decodeConnectedWebsite(json: TdJsonRaw): ConnectedWebsite | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectedWebsite = new ConnectedWebsite();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.domain_name = asString(record['domain_name']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.browser = asString(record['browser']);
  out.platform = asString(record['platform']);
  out.log_in_date = asNumber(record['log_in_date']);
  out.last_active_date = asNumber(record['last_active_date']);
  out.ip_address = asString(record['ip_address']);
  out.location = asString(record['location']);
  return out;
}

/** Encode a `connectedWebsite` into TDLib JSON wire shape. */
export function encodeConnectedWebsite(value: ConnectedWebsite): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectedWebsite';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['domain_name'] = value.domain_name;
  out['bot_user_id'] = value.bot_user_id;
  out['browser'] = value.browser;
  out['platform'] = value.platform;
  out['log_in_date'] = value.log_in_date;
  out['last_active_date'] = value.last_active_date;
  out['ip_address'] = value.ip_address;
  out['location'] = value.location;
  return out;
}

/** connectedWebsites = ConnectedWebsites — Contains a list of websites the current user is logged in with Telegram */
export class ConnectedWebsites {
  type: 'connectedWebsites' = 'connectedWebsites';
  extra?: string;
  /** websites: vector<connectedWebsite> — List of connected websites */
  websites: ConnectedWebsite[] = [];
}

/** Decode a TDLib `connectedWebsites` object; null input -> null. */
export function decodeConnectedWebsites(json: TdJsonRaw): ConnectedWebsites | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectedWebsites = new ConnectedWebsites();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.websites = decodeVector(record['websites'], (item: TdJsonRaw): ConnectedWebsite => decodeConnectedWebsite(item) as ConnectedWebsite);
  return out;
}

/** Encode a `connectedWebsites` into TDLib JSON wire shape. */
export function encodeConnectedWebsites(value: ConnectedWebsites): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectedWebsites';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['websites'] = encodeVector(value.websites, (item: ConnectedWebsite | null): TdJsonRaw => item === null ? null : encodeConnectedWebsite(item));
  return out;
}

/** connectionStateConnecting = ConnectionState — Establishing a connection to the Telegram servers */
export class ConnectionStateConnecting {
  type: 'connectionStateConnecting' = 'connectionStateConnecting';
  extra?: string;
}

/** Decode a TDLib `connectionStateConnecting` object; null input -> null. */
export function decodeConnectionStateConnecting(json: TdJsonRaw): ConnectionStateConnecting | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectionStateConnecting = new ConnectionStateConnecting();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `connectionStateConnecting` into TDLib JSON wire shape. */
export function encodeConnectionStateConnecting(value: ConnectionStateConnecting): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectionStateConnecting';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** connectionStateConnectingToProxy = ConnectionState — Establishing a connection with a proxy server */
export class ConnectionStateConnectingToProxy {
  type: 'connectionStateConnectingToProxy' = 'connectionStateConnectingToProxy';
  extra?: string;
}

/** Decode a TDLib `connectionStateConnectingToProxy` object; null input -> null. */
export function decodeConnectionStateConnectingToProxy(json: TdJsonRaw): ConnectionStateConnectingToProxy | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectionStateConnectingToProxy = new ConnectionStateConnectingToProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `connectionStateConnectingToProxy` into TDLib JSON wire shape. */
export function encodeConnectionStateConnectingToProxy(value: ConnectionStateConnectingToProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectionStateConnectingToProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** connectionStateReady = ConnectionState — There is a working connection to the Telegram servers */
export class ConnectionStateReady {
  type: 'connectionStateReady' = 'connectionStateReady';
  extra?: string;
}

/** Decode a TDLib `connectionStateReady` object; null input -> null. */
export function decodeConnectionStateReady(json: TdJsonRaw): ConnectionStateReady | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectionStateReady = new ConnectionStateReady();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `connectionStateReady` into TDLib JSON wire shape. */
export function encodeConnectionStateReady(value: ConnectionStateReady): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectionStateReady';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** connectionStateUpdating = ConnectionState — Downloading data expected to be received while the application was offline */
export class ConnectionStateUpdating {
  type: 'connectionStateUpdating' = 'connectionStateUpdating';
  extra?: string;
}

/** Decode a TDLib `connectionStateUpdating` object; null input -> null. */
export function decodeConnectionStateUpdating(json: TdJsonRaw): ConnectionStateUpdating | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectionStateUpdating = new ConnectionStateUpdating();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `connectionStateUpdating` into TDLib JSON wire shape. */
export function encodeConnectionStateUpdating(value: ConnectionStateUpdating): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectionStateUpdating';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** connectionStateWaitingForNetwork = ConnectionState — Waiting for the network to become available. Use setNetworkType to change the available network type */
export class ConnectionStateWaitingForNetwork {
  type: 'connectionStateWaitingForNetwork' = 'connectionStateWaitingForNetwork';
  extra?: string;
}

/** Decode a TDLib `connectionStateWaitingForNetwork` object; null input -> null. */
export function decodeConnectionStateWaitingForNetwork(json: TdJsonRaw): ConnectionStateWaitingForNetwork | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ConnectionStateWaitingForNetwork = new ConnectionStateWaitingForNetwork();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `connectionStateWaitingForNetwork` into TDLib JSON wire shape. */
export function encodeConnectionStateWaitingForNetwork(value: ConnectionStateWaitingForNetwork): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'connectionStateWaitingForNetwork';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** contact = Contact — Describes a contact of a user */
export class Contact {
  type: 'contact' = 'contact';
  extra?: string;
  /** phone_number: string — Phone number of the user */
  phone_number: string = '';
  /** first_name: string — First name of the user; 1-64 characters */
  first_name: string = '';
  /** last_name: string — Last name of the user; 0-64 characters */
  last_name: string = '';
  /** vcard: string — Additional data about the user in a form of vCard; 0-2048 bytes in length */
  vcard: string = '';
  /** user_id: int53 — Identifier of the user, if known; 0 otherwise */
  user_id: number = 0;
}

/** Decode a TDLib `contact` object; null input -> null. */
export function decodeContact(json: TdJsonRaw): Contact | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Contact = new Contact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  out.first_name = asString(record['first_name']);
  out.last_name = asString(record['last_name']);
  out.vcard = asString(record['vcard']);
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `contact` into TDLib JSON wire shape. */
export function encodeContact(value: Contact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'contact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  out['first_name'] = value.first_name;
  out['last_name'] = value.last_name;
  out['vcard'] = value.vcard;
  out['user_id'] = value.user_id;
  return out;
}

/** count = Count — Contains a counter */
export class Count {
  type: 'count' = 'count';
  extra?: string;
  /** count: int32 — Count */
  count: number = 0;
}

/** Decode a TDLib `count` object; null input -> null. */
export function decodeCount(json: TdJsonRaw): Count | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Count = new Count();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.count = asNumber(record['count']);
  return out;
}

/** Encode a `count` into TDLib JSON wire shape. */
export function encodeCount(value: Count): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'count';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['count'] = value.count;
  return out;
}

/** countries = Countries — Contains information about countries */
export class Countries {
  type: 'countries' = 'countries';
  extra?: string;
  /** countries: vector<countryInfo> — The list of countries */
  countries: CountryInfo[] = [];
}

/** Decode a TDLib `countries` object; null input -> null. */
export function decodeCountries(json: TdJsonRaw): Countries | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Countries = new Countries();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.countries = decodeVector(record['countries'], (item: TdJsonRaw): CountryInfo => decodeCountryInfo(item) as CountryInfo);
  return out;
}

/** Encode a `countries` into TDLib JSON wire shape. */
export function encodeCountries(value: Countries): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'countries';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['countries'] = encodeVector(value.countries, (item: CountryInfo | null): TdJsonRaw => item === null ? null : encodeCountryInfo(item));
  return out;
}

/** countryInfo = CountryInfo — Contains information about a country */
export class CountryInfo {
  type: 'countryInfo' = 'countryInfo';
  extra?: string;
  /** country_code: string — A two-letter ISO 3166-1 alpha-2 country code */
  country_code: string = '';
  /** name: string — Native name of the country */
  name: string = '';
  /** english_name: string — English name of the country */
  english_name: string = '';
  /** flag_emoji: string — An emoji for the flag of the country; may be empty if unknown */
  flag_emoji: string = '';
  /** is_hidden: Bool — True, if the country must be hidden from the list of all countries */
  is_hidden: boolean = false;
  /** calling_codes: vector<string> — List of country calling codes */
  calling_codes: string[] = [];
}

/** Decode a TDLib `countryInfo` object; null input -> null. */
export function decodeCountryInfo(json: TdJsonRaw): CountryInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CountryInfo = new CountryInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.country_code = asString(record['country_code']);
  out.name = asString(record['name']);
  out.english_name = asString(record['english_name']);
  out.flag_emoji = asString(record['flag_emoji']);
  out.is_hidden = asBool(record['is_hidden']);
  out.calling_codes = decodeVector(record['calling_codes'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `countryInfo` into TDLib JSON wire shape. */
export function encodeCountryInfo(value: CountryInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'countryInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['country_code'] = value.country_code;
  out['name'] = value.name;
  out['english_name'] = value.english_name;
  out['flag_emoji'] = value.flag_emoji;
  out['is_hidden'] = value.is_hidden;
  out['calling_codes'] = encodeVector(value.calling_codes, (item: string): TdJsonRaw => item);
  return out;
}

/** craftGift = CraftGiftResult — Crafts a new gift from other gifts that will be permanently lost Consequently, the first gift must not have been withdrawn to the TON blockchain as an NFT and must have an empty gift_address */
export class CraftGift {
  type: 'craftGift' = 'craftGift';
  extra?: string;
  /** received_gift_ids: vector<string> — Identifier of the gifts to use for crafting. In the case of a successful craft, the resulting gift will have the number of the first gift. */
  received_gift_ids: string[] = [];
}

/** Decode a TDLib `craftGift` object; null input -> null. */
export function decodeCraftGift(json: TdJsonRaw): CraftGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CraftGift = new CraftGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.received_gift_ids = decodeVector(record['received_gift_ids'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `craftGift` into TDLib JSON wire shape. */
export function encodeCraftGift(value: CraftGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'craftGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['received_gift_ids'] = encodeVector(value.received_gift_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** craftGiftResultFail = CraftGiftResult — Crafting has failed */
export class CraftGiftResultFail {
  type: 'craftGiftResultFail' = 'craftGiftResultFail';
  extra?: string;
}

/** Decode a TDLib `craftGiftResultFail` object; null input -> null. */
export function decodeCraftGiftResultFail(json: TdJsonRaw): CraftGiftResultFail | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CraftGiftResultFail = new CraftGiftResultFail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `craftGiftResultFail` into TDLib JSON wire shape. */
export function encodeCraftGiftResultFail(value: CraftGiftResultFail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'craftGiftResultFail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** craftGiftResultInvalidGift = CraftGiftResult — Crafting isn't possible because one of the gifts isn't suitable for crafting */
export class CraftGiftResultInvalidGift {
  type: 'craftGiftResultInvalidGift' = 'craftGiftResultInvalidGift';
  extra?: string;
}

/** Decode a TDLib `craftGiftResultInvalidGift` object; null input -> null. */
export function decodeCraftGiftResultInvalidGift(json: TdJsonRaw): CraftGiftResultInvalidGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CraftGiftResultInvalidGift = new CraftGiftResultInvalidGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `craftGiftResultInvalidGift` into TDLib JSON wire shape. */
export function encodeCraftGiftResultInvalidGift(value: CraftGiftResultInvalidGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'craftGiftResultInvalidGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** craftGiftResultSuccess = CraftGiftResult — Crafting was successful */
export class CraftGiftResultSuccess {
  type: 'craftGiftResultSuccess' = 'craftGiftResultSuccess';
  extra?: string;
  /** gift: upgradedGift — The created gift */
  gift: UpgradedGift | null = null;
  /** received_gift_id: string — Unique identifier of the received gift for the current user */
  received_gift_id: string = '';
}

/** Decode a TDLib `craftGiftResultSuccess` object; null input -> null. */
export function decodeCraftGiftResultSuccess(json: TdJsonRaw): CraftGiftResultSuccess | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CraftGiftResultSuccess = new CraftGiftResultSuccess();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  out.received_gift_id = asString(record['received_gift_id']);
  return out;
}

/** Encode a `craftGiftResultSuccess` into TDLib JSON wire shape. */
export function encodeCraftGiftResultSuccess(value: CraftGiftResultSuccess): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'craftGiftResultSuccess';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  out['received_gift_id'] = value.received_gift_id;
  return out;
}

/** craftGiftResultTooEarly = CraftGiftResult — Crafting isn't possible because one of the gifts can't be used for crafting yet */
export class CraftGiftResultTooEarly {
  type: 'craftGiftResultTooEarly' = 'craftGiftResultTooEarly';
  extra?: string;
  /** retry_after: int32 — Time left before the gift can be used for crafting */
  retry_after: number = 0;
}

/** Decode a TDLib `craftGiftResultTooEarly` object; null input -> null. */
export function decodeCraftGiftResultTooEarly(json: TdJsonRaw): CraftGiftResultTooEarly | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CraftGiftResultTooEarly = new CraftGiftResultTooEarly();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.retry_after = asNumber(record['retry_after']);
  return out;
}

/** Encode a `craftGiftResultTooEarly` into TDLib JSON wire shape. */
export function encodeCraftGiftResultTooEarly(value: CraftGiftResultTooEarly): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'craftGiftResultTooEarly';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['retry_after'] = value.retry_after;
  return out;
}

/** createBasicGroupChat = Chat — Returns an existing chat corresponding to a known basic group */
export class CreateBasicGroupChat {
  type: 'createBasicGroupChat' = 'createBasicGroupChat';
  extra?: string;
  /** basic_group_id: int53 — Basic group identifier */
  basic_group_id: number = 0;
  /** force: Bool — Pass true to create the chat without a network request. In this case all information about the chat except its type, title and photo can be incorrect */
  force: boolean = false;
}

/** Decode a TDLib `createBasicGroupChat` object; null input -> null. */
export function decodeCreateBasicGroupChat(json: TdJsonRaw): CreateBasicGroupChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateBasicGroupChat = new CreateBasicGroupChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.basic_group_id = asNumber(record['basic_group_id']);
  out.force = asBool(record['force']);
  return out;
}

/** Encode a `createBasicGroupChat` into TDLib JSON wire shape. */
export function encodeCreateBasicGroupChat(value: CreateBasicGroupChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createBasicGroupChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['basic_group_id'] = value.basic_group_id;
  out['force'] = value.force;
  return out;
}

/** createBot = User — Creates a bot which will be managed by another bot. Returns the created bot. May return an error with a message "BOT_CREATE_LIMIT_EXCEEDED" if the user already owns the maximum allowed number of bots as per getOption("... */
export class CreateBot {
  type: 'createBot' = 'createBot';
  extra?: string;
  /** manager_bot_user_id: int53 — Identifier of the bot that will manage the created bot */
  manager_bot_user_id: number = 0;
  /** name: string — Name of the bot; 1-64 characters */
  name: string = '';
  /** username: string — Username of the bot. The username must end with "bot". Use checkBotUsername to find whether the name is suitable */
  username: string = '';
  /** via_link: Bool — Pass true if the bot is created from an internalLinkTypeRequestManagedBot link */
  via_link: boolean = false;
}

/** Decode a TDLib `createBot` object; null input -> null. */
export function decodeCreateBot(json: TdJsonRaw): CreateBot | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateBot = new CreateBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.manager_bot_user_id = asNumber(record['manager_bot_user_id']);
  out.name = asString(record['name']);
  out.username = asString(record['username']);
  out.via_link = asBool(record['via_link']);
  return out;
}

/** Encode a `createBot` into TDLib JSON wire shape. */
export function encodeCreateBot(value: CreateBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['manager_bot_user_id'] = value.manager_bot_user_id;
  out['name'] = value.name;
  out['username'] = value.username;
  out['via_link'] = value.via_link;
  return out;
}

/** createBusinessChatLink = BusinessChatLink — Creates a business chat link for the current account. Requires Telegram Business subscription. There can be up to getOption("business_chat_link_count_max") links created. Returns the created link */
export class CreateBusinessChatLink {
  type: 'createBusinessChatLink' = 'createBusinessChatLink';
  extra?: string;
  /** link_info: inputBusinessChatLink — Information about the link to create */
  link_info: InputBusinessChatLink | null = null;
}

/** Decode a TDLib `createBusinessChatLink` object; null input -> null. */
export function decodeCreateBusinessChatLink(json: TdJsonRaw): CreateBusinessChatLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateBusinessChatLink = new CreateBusinessChatLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link_info = decodeInputBusinessChatLink(record['link_info']);
  return out;
}

/** Encode a `createBusinessChatLink` into TDLib JSON wire shape. */
export function encodeCreateBusinessChatLink(value: CreateBusinessChatLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createBusinessChatLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link_info'] = value.link_info === null ? null : encodeInputBusinessChatLink(value.link_info);
  return out;
}

/** createCall = CallId — Creates a new call */
export class CreateCall {
  type: 'createCall' = 'createCall';
  extra?: string;
  /** user_id: int53 — Identifier of the user to be called */
  user_id: number = 0;
  /** protocol: callProtocol — The call protocols supported by the application */
  protocol: CallProtocol | null = null;
  /** is_video: Bool — Pass true to create a video call */
  is_video: boolean = false;
}

/** Decode a TDLib `createCall` object; null input -> null. */
export function decodeCreateCall(json: TdJsonRaw): CreateCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateCall = new CreateCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.protocol = decodeCallProtocol(record['protocol']);
  out.is_video = asBool(record['is_video']);
  return out;
}

/** Encode a `createCall` into TDLib JSON wire shape. */
export function encodeCreateCall(value: CreateCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['protocol'] = value.protocol === null ? null : encodeCallProtocol(value.protocol);
  out['is_video'] = value.is_video;
  return out;
}

/** createChatFolder = ChatFolderInfo — Creates new chat folder. Returns information about the created chat folder. There can be up to getOption("chat_folder_count_max") chat folders, but the limit can be increased with Telegram Premium */
export class CreateChatFolder {
  type: 'createChatFolder' = 'createChatFolder';
  extra?: string;
  /** folder: chatFolder — The new chat folder */
  folder: ChatFolder | null = null;
}

/** Decode a TDLib `createChatFolder` object; null input -> null. */
export function decodeCreateChatFolder(json: TdJsonRaw): CreateChatFolder | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateChatFolder = new CreateChatFolder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.folder = decodeChatFolder(record['folder']);
  return out;
}

/** Encode a `createChatFolder` into TDLib JSON wire shape. */
export function encodeCreateChatFolder(value: CreateChatFolder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createChatFolder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['folder'] = value.folder === null ? null : encodeChatFolder(value.folder);
  return out;
}

/** createChatFolderInviteLink = ChatFolderInviteLink — Creates a new invite link for a chat folder. A link can be created for a chat folder if it has only pinned and included chats */
export class CreateChatFolderInviteLink {
  type: 'createChatFolderInviteLink' = 'createChatFolderInviteLink';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
  /** name: string — Name of the link; 0-32 characters */
  name: string = '';
  /** chat_ids: vector<int53> — Identifiers of chats to be accessible by the invite link. Use getChatsForChatFolderInviteLink to get suitable chats. Basic groups will be automatically converted to supergroups before link creation */
  chat_ids: number[] = [];
}

/** Decode a TDLib `createChatFolderInviteLink` object; null input -> null. */
export function decodeCreateChatFolderInviteLink(json: TdJsonRaw): CreateChatFolderInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateChatFolderInviteLink = new CreateChatFolderInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  out.name = asString(record['name']);
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `createChatFolderInviteLink` into TDLib JSON wire shape. */
export function encodeCreateChatFolderInviteLink(value: CreateChatFolderInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createChatFolderInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  out['name'] = value.name;
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** createChatInviteLink = ChatInviteLink — Creates a new invite link for a chat. Available for basic groups, supergroups, and channels. Requires administrator privileges and can_invite_users right in the chat */
export class CreateChatInviteLink {
  type: 'createChatInviteLink' = 'createChatInviteLink';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** name: string — Invite link name; 0-32 characters */
  name: string = '';
  /** expiration_date: int32 — Point in time (Unix timestamp) when the link will expire; pass 0 if never */
  expiration_date: number = 0;
  /** member_limit: int32 — The maximum number of chat members that can join the chat via the link simultaneously; 0-99999; pass 0 if not limited */
  member_limit: number = 0;
  /** creates_join_request: Bool — Pass true if users joining the chat via the link need to be approved by chat administrators. In this case, member_limit must be 0 */
  creates_join_request: boolean = false;
}

/** Decode a TDLib `createChatInviteLink` object; null input -> null. */
export function decodeCreateChatInviteLink(json: TdJsonRaw): CreateChatInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateChatInviteLink = new CreateChatInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.name = asString(record['name']);
  out.expiration_date = asNumber(record['expiration_date']);
  out.member_limit = asNumber(record['member_limit']);
  out.creates_join_request = asBool(record['creates_join_request']);
  return out;
}

/** Encode a `createChatInviteLink` into TDLib JSON wire shape. */
export function encodeCreateChatInviteLink(value: CreateChatInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createChatInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['name'] = value.name;
  out['expiration_date'] = value.expiration_date;
  out['member_limit'] = value.member_limit;
  out['creates_join_request'] = value.creates_join_request;
  return out;
}

/** createChatSubscriptionInviteLink = ChatInviteLink — Creates a new subscription invite link for a channel chat. Requires can_invite_users right in the chat Subscription period must be 2592000 in production environment, and 60 or 300 if Te... */
export class CreateChatSubscriptionInviteLink {
  type: 'createChatSubscriptionInviteLink' = 'createChatSubscriptionInviteLink';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** name: string — Invite link name; 0-32 characters */
  name: string = '';
  /** subscription_pricing: starSubscriptionPricing — Information about subscription plan that will be applied to the users joining the chat via the link. */
  subscription_pricing: StarSubscriptionPricing | null = null;
}

/** Decode a TDLib `createChatSubscriptionInviteLink` object; null input -> null. */
export function decodeCreateChatSubscriptionInviteLink(json: TdJsonRaw): CreateChatSubscriptionInviteLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateChatSubscriptionInviteLink = new CreateChatSubscriptionInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.name = asString(record['name']);
  out.subscription_pricing = decodeStarSubscriptionPricing(record['subscription_pricing']);
  return out;
}

/** Encode a `createChatSubscriptionInviteLink` into TDLib JSON wire shape. */
export function encodeCreateChatSubscriptionInviteLink(value: CreateChatSubscriptionInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createChatSubscriptionInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['name'] = value.name;
  out['subscription_pricing'] = value.subscription_pricing === null ? null : encodeStarSubscriptionPricing(value.subscription_pricing);
  return out;
}

/** createCommunity = CommunityId — Creates a new community for the given chat. Returns identifier of the created community basic group chats will be automatically upgraded to supergroup chats */
export class CreateCommunity {
  type: 'createCommunity' = 'createCommunity';
  extra?: string;
  /** name: string — Name of the new community */
  name: string = '';
  /** chat_id: int53 — Identifier of the chat in the community; only chats with owned bots and owned basic group, supergroup and channel chats are allowed; */
  chat_id: number = 0;
  /** is_chat_hidden: Bool — Pass true if the chat will be visible only to administrators of the community */
  is_chat_hidden: boolean = false;
}

/** Decode a TDLib `createCommunity` object; null input -> null. */
export function decodeCreateCommunity(json: TdJsonRaw): CreateCommunity | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateCommunity = new CreateCommunity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.chat_id = asNumber(record['chat_id']);
  out.is_chat_hidden = asBool(record['is_chat_hidden']);
  return out;
}

/** Encode a `createCommunity` into TDLib JSON wire shape. */
export function encodeCreateCommunity(value: CreateCommunity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createCommunity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['chat_id'] = value.chat_id;
  out['is_chat_hidden'] = value.is_chat_hidden;
  return out;
}

/** createForumTopic = ForumTopicInfo — Creates a topic in a forum supergroup chat or a chat with a bot with topics; requires can_manage_topics administrator or can_create_topics member right in the supergroup */
export class CreateForumTopic {
  type: 'createForumTopic' = 'createForumTopic';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** name: string — Name of the topic; 1-128 characters */
  name: string = '';
  /** is_name_implicit: Bool — Pass true if the name of the topic wasn't entered explicitly; for chats with bots only */
  is_name_implicit: boolean = false;
  /** icon: forumTopicIcon — Icon of the topic. Icon color must be one of 0x6FB9F0, 0xFFD67E, 0xCB86DB, 0x8EEE98, 0xFF93B2, or 0xFB6F5F. Telegram Premium users can use any custom emoji as topic icon, other users can use only a custom emoji ret... */
  icon: ForumTopicIcon | null = null;
}

/** Decode a TDLib `createForumTopic` object; null input -> null. */
export function decodeCreateForumTopic(json: TdJsonRaw): CreateForumTopic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateForumTopic = new CreateForumTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.name = asString(record['name']);
  out.is_name_implicit = asBool(record['is_name_implicit']);
  out.icon = decodeForumTopicIcon(record['icon']);
  return out;
}

/** Encode a `createForumTopic` into TDLib JSON wire shape. */
export function encodeCreateForumTopic(value: CreateForumTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createForumTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['name'] = value.name;
  out['is_name_implicit'] = value.is_name_implicit;
  out['icon'] = value.icon === null ? null : encodeForumTopicIcon(value.icon);
  return out;
}

/** createGiftCollection = GiftCollection — Creates a collection from gifts on the current user's or a channel's profile page; requires can_post_messages administrator right in the channel chat. An owner can have up to getOption("gift_collec... */
export class CreateGiftCollection {
  type: 'createGiftCollection' = 'createGiftCollection';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that received the gifts */
  owner_id: TdMessageSender | null = null;
  /** name: string — Name of the collection; 1-12 characters */
  name: string = '';
  /** received_gift_ids: vector<string> — Identifier of the gifts to add to the collection; 0-getOption("gift_collection_size_max") identifiers */
  received_gift_ids: string[] = [];
}

/** Decode a TDLib `createGiftCollection` object; null input -> null. */
export function decodeCreateGiftCollection(json: TdJsonRaw): CreateGiftCollection | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateGiftCollection = new CreateGiftCollection();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.name = asString(record['name']);
  out.received_gift_ids = decodeVector(record['received_gift_ids'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `createGiftCollection` into TDLib JSON wire shape. */
export function encodeCreateGiftCollection(value: CreateGiftCollection): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createGiftCollection';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['name'] = value.name;
  out['received_gift_ids'] = encodeVector(value.received_gift_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** createGroupCall = GroupCallInfo — Creates a new group call that isn't bound to a chat */
export class CreateGroupCall {
  type: 'createGroupCall' = 'createGroupCall';
  extra?: string;
  /** join_parameters: groupCallJoinParameters — Parameters to join the call; pass null to only create call link without joining the call */
  join_parameters: GroupCallJoinParameters | null = null;
}

/** Decode a TDLib `createGroupCall` object; null input -> null. */
export function decodeCreateGroupCall(json: TdJsonRaw): CreateGroupCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateGroupCall = new CreateGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.join_parameters = decodeGroupCallJoinParameters(record['join_parameters']);
  return out;
}

/** Encode a `createGroupCall` into TDLib JSON wire shape. */
export function encodeCreateGroupCall(value: CreateGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['join_parameters'] = value.join_parameters === null ? null : encodeGroupCallJoinParameters(value.join_parameters);
  return out;
}

/** createInvoiceLink = HttpUrl — Creates a link for the given invoice; for bots only */
export class CreateInvoiceLink {
  type: 'createInvoiceLink' = 'createInvoiceLink';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which to send the request */
  business_connection_id: string = '';
  /** invoice: InputMessageContent — Information about the invoice of the type inputMessageInvoice */
  invoice: TdInputMessageContent | null = null;
}

/** Decode a TDLib `createInvoiceLink` object; null input -> null. */
export function decodeCreateInvoiceLink(json: TdJsonRaw): CreateInvoiceLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateInvoiceLink = new CreateInvoiceLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.invoice = decodeTdInputMessageContent(record['invoice']);
  return out;
}

/** Encode a `createInvoiceLink` into TDLib JSON wire shape. */
export function encodeCreateInvoiceLink(value: CreateInvoiceLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createInvoiceLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['invoice'] = value.invoice === null ? null : encodeTdInputMessageContent(value.invoice);
  return out;
}

/** createNewBasicGroupChat = CreatedBasicGroupChat — Creates a new basic group and sends a corresponding messageBasicGroupChatCreate. Returns information about the newly created chat */
export class CreateNewBasicGroupChat {
  type: 'createNewBasicGroupChat' = 'createNewBasicGroupChat';
  extra?: string;
  /** user_ids: vector<int53> — Identifiers of users to be added to the basic group; may be empty to create a basic group without other members */
  user_ids: number[] = [];
  /** title: string — Title of the new basic group; 1-128 characters */
  title: string = '';
  /** message_auto_delete_time: int32 — Message auto-delete time value, in seconds; must be from 0 up to 365 * 86400 and be divisible by 86400. If 0, then messages aren't deleted automatically */
  message_auto_delete_time: number = 0;
}

/** Decode a TDLib `createNewBasicGroupChat` object; null input -> null. */
export function decodeCreateNewBasicGroupChat(json: TdJsonRaw): CreateNewBasicGroupChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateNewBasicGroupChat = new CreateNewBasicGroupChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.title = asString(record['title']);
  out.message_auto_delete_time = asNumber(record['message_auto_delete_time']);
  return out;
}

/** Encode a `createNewBasicGroupChat` into TDLib JSON wire shape. */
export function encodeCreateNewBasicGroupChat(value: CreateNewBasicGroupChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createNewBasicGroupChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  out['title'] = value.title;
  out['message_auto_delete_time'] = value.message_auto_delete_time;
  return out;
}

/** createNewSecretChat = Chat — Creates a new secret chat. Returns the newly created chat */
export class CreateNewSecretChat {
  type: 'createNewSecretChat' = 'createNewSecretChat';
  extra?: string;
  /** user_id: int53 — Identifier of the target user */
  user_id: number = 0;
}

/** Decode a TDLib `createNewSecretChat` object; null input -> null. */
export function decodeCreateNewSecretChat(json: TdJsonRaw): CreateNewSecretChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateNewSecretChat = new CreateNewSecretChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `createNewSecretChat` into TDLib JSON wire shape. */
export function encodeCreateNewSecretChat(value: CreateNewSecretChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createNewSecretChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** createNewStickerSet = StickerSet — Creates a new sticker set. Returns the newly created sticker set If empty, then the name returned by getSuggestedStickerSetName will be used automatically */
export class CreateNewStickerSet {
  type: 'createNewStickerSet' = 'createNewStickerSet';
  extra?: string;
  /** user_id: int53 — Sticker set owner; ignored for regular users */
  user_id: number = 0;
  /** title: string — Sticker set title; 1-64 characters */
  title: string = '';
  /** name: string — Sticker set name. Can contain only English letters, digits and underscores. Must end with *"_by_<bot username>"* (*<bot_username>* is case insensitive) for bots; 0-64 characters. */
  name: string = '';
  /** sticker_type: StickerType — Type of the stickers in the set */
  sticker_type: TdStickerType | null = null;
  /** needs_repainting: Bool — Pass true if stickers in the sticker set must be repainted; for custom emoji sticker sets only */
  needs_repainting: boolean = false;
  /** stickers: vector<newSticker> — List of stickers to be added to the set; 1-200 stickers for custom emoji sticker sets, and 1-120 stickers otherwise. For TGS stickers, uploadStickerFile must be used before the sticker is shown */
  stickers: NewSticker[] = [];
  /** source: string — Source of the sticker set; may be empty if unknown */
  source: string = '';
}

/** Decode a TDLib `createNewStickerSet` object; null input -> null. */
export function decodeCreateNewStickerSet(json: TdJsonRaw): CreateNewStickerSet | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateNewStickerSet = new CreateNewStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.title = asString(record['title']);
  out.name = asString(record['name']);
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.needs_repainting = asBool(record['needs_repainting']);
  out.stickers = decodeVector(record['stickers'], (item: TdJsonRaw): NewSticker => decodeNewSticker(item) as NewSticker);
  out.source = asString(record['source']);
  return out;
}

/** Encode a `createNewStickerSet` into TDLib JSON wire shape. */
export function encodeCreateNewStickerSet(value: CreateNewStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createNewStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['title'] = value.title;
  out['name'] = value.name;
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['needs_repainting'] = value.needs_repainting;
  out['stickers'] = encodeVector(value.stickers, (item: NewSticker | null): TdJsonRaw => item === null ? null : encodeNewSticker(item));
  out['source'] = value.source;
  return out;
}

/** createNewSupergroupChat = Chat — Creates a new supergroup or channel and sends a corresponding messageSupergroupChatCreate. Returns the newly created chat */
export class CreateNewSupergroupChat {
  type: 'createNewSupergroupChat' = 'createNewSupergroupChat';
  extra?: string;
  /** title: string — Title of the new chat; 1-128 characters */
  title: string = '';
  /** is_forum: Bool — Pass true to create a forum supergroup chat */
  is_forum: boolean = false;
  /** is_channel: Bool — Pass true to create a channel chat; ignored if a forum is created */
  is_channel: boolean = false;
  /** description: string */
  description: string = '';
  /** location: chatLocation — Chat location if a location-based supergroup is being created; pass null to create an ordinary supergroup chat */
  location: ChatLocation | null = null;
  /** message_auto_delete_time: int32 — Message auto-delete time value, in seconds; must be from 0 up to 365 * 86400 and be divisible by 86400. If 0, then messages aren't deleted automatically */
  message_auto_delete_time: number = 0;
  /** for_import: Bool — Pass true to create a supergroup for importing messages using importMessages */
  for_import: boolean = false;
}

/** Decode a TDLib `createNewSupergroupChat` object; null input -> null. */
export function decodeCreateNewSupergroupChat(json: TdJsonRaw): CreateNewSupergroupChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateNewSupergroupChat = new CreateNewSupergroupChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.is_forum = asBool(record['is_forum']);
  out.is_channel = asBool(record['is_channel']);
  out.description = asString(record['description']);
  out.location = decodeChatLocation(record['location']);
  out.message_auto_delete_time = asNumber(record['message_auto_delete_time']);
  out.for_import = asBool(record['for_import']);
  return out;
}

/** Encode a `createNewSupergroupChat` into TDLib JSON wire shape. */
export function encodeCreateNewSupergroupChat(value: CreateNewSupergroupChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createNewSupergroupChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['is_forum'] = value.is_forum;
  out['is_channel'] = value.is_channel;
  out['description'] = value.description;
  out['location'] = value.location === null ? null : encodeChatLocation(value.location);
  out['message_auto_delete_time'] = value.message_auto_delete_time;
  out['for_import'] = value.for_import;
  return out;
}

/** createPrivateChat = Chat — Returns an existing chat corresponding to a given user */
export class CreatePrivateChat {
  type: 'createPrivateChat' = 'createPrivateChat';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** force: Bool — Pass true to create the chat without a network request. In this case all information about the chat except its type, title and photo can be incorrect */
  force: boolean = false;
}

/** Decode a TDLib `createPrivateChat` object; null input -> null. */
export function decodeCreatePrivateChat(json: TdJsonRaw): CreatePrivateChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreatePrivateChat = new CreatePrivateChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.force = asBool(record['force']);
  return out;
}

/** Encode a `createPrivateChat` into TDLib JSON wire shape. */
export function encodeCreatePrivateChat(value: CreatePrivateChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createPrivateChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['force'] = value.force;
  return out;
}

/** createRichMessageWithAi = RichMessage — Creates a new rich message using an AI model. May return an error with a message "AICOMPOSE_FLOOD_PREMIUM" if Telegram Premium is required to send further requests */
export class CreateRichMessageWithAi {
  type: 'createRichMessageWithAi' = 'createRichMessageWithAi';
  extra?: string;
  /** prompt: string — Prompt that will be used to create the message; 0-getOption("text_composition_style_prompt_length_max") characters */
  prompt: string = '';
  /** language_code: string — Pass a language code in which the text will be created */
  language_code: string = '';
  /** add_emojis: Bool — Pass true to add emoji to the text */
  add_emojis: boolean = false;
}

/** Decode a TDLib `createRichMessageWithAi` object; null input -> null. */
export function decodeCreateRichMessageWithAi(json: TdJsonRaw): CreateRichMessageWithAi | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateRichMessageWithAi = new CreateRichMessageWithAi();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.prompt = asString(record['prompt']);
  out.language_code = asString(record['language_code']);
  out.add_emojis = asBool(record['add_emojis']);
  return out;
}

/** Encode a `createRichMessageWithAi` into TDLib JSON wire shape. */
export function encodeCreateRichMessageWithAi(value: CreateRichMessageWithAi): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createRichMessageWithAi';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['prompt'] = value.prompt;
  out['language_code'] = value.language_code;
  out['add_emojis'] = value.add_emojis;
  return out;
}

/** createSecretChat = Chat — Returns an existing chat corresponding to a known secret chat */
export class CreateSecretChat {
  type: 'createSecretChat' = 'createSecretChat';
  extra?: string;
  /** secret_chat_id: int32 — Secret chat identifier */
  secret_chat_id: number = 0;
}

/** Decode a TDLib `createSecretChat` object; null input -> null. */
export function decodeCreateSecretChat(json: TdJsonRaw): CreateSecretChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateSecretChat = new CreateSecretChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.secret_chat_id = asNumber(record['secret_chat_id']);
  return out;
}

/** Encode a `createSecretChat` into TDLib JSON wire shape. */
export function encodeCreateSecretChat(value: CreateSecretChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createSecretChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['secret_chat_id'] = value.secret_chat_id;
  return out;
}

/** createStoryAlbum = StoryAlbum — Creates an album of stories; requires can_edit_stories administrator right for supergroup and channel chats */
export class CreateStoryAlbum {
  type: 'createStoryAlbum' = 'createStoryAlbum';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the stories */
  story_poster_chat_id: number = 0;
  /** name: string — Name of the album; 1-12 characters */
  name: string = '';
  /** story_ids: vector<int32> — Identifiers of stories to add to the album; 0-getOption("story_album_size_max") identifiers */
  story_ids: number[] = [];
}

/** Decode a TDLib `createStoryAlbum` object; null input -> null. */
export function decodeCreateStoryAlbum(json: TdJsonRaw): CreateStoryAlbum | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateStoryAlbum = new CreateStoryAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.name = asString(record['name']);
  out.story_ids = decodeVector(record['story_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `createStoryAlbum` into TDLib JSON wire shape. */
export function encodeCreateStoryAlbum(value: CreateStoryAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createStoryAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['name'] = value.name;
  out['story_ids'] = encodeVector(value.story_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** createSupergroupChat = Chat — Returns an existing chat corresponding to a known supergroup or channel */
export class CreateSupergroupChat {
  type: 'createSupergroupChat' = 'createSupergroupChat';
  extra?: string;
  /** supergroup_id: int53 — Supergroup or channel identifier */
  supergroup_id: number = 0;
  /** force: Bool — Pass true to create the chat without a network request. In this case all information about the chat except its type, title and photo can be incorrect */
  force: boolean = false;
}

/** Decode a TDLib `createSupergroupChat` object; null input -> null. */
export function decodeCreateSupergroupChat(json: TdJsonRaw): CreateSupergroupChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateSupergroupChat = new CreateSupergroupChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.force = asBool(record['force']);
  return out;
}

/** Encode a `createSupergroupChat` into TDLib JSON wire shape. */
export function encodeCreateSupergroupChat(value: CreateSupergroupChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createSupergroupChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['force'] = value.force;
  return out;
}

/** createTemporaryPassword = TemporaryPasswordState — Creates a new temporary password for processing payments */
export class CreateTemporaryPassword {
  type: 'createTemporaryPassword' = 'createTemporaryPassword';
  extra?: string;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
  /** valid_for: int32 — Time during which the temporary password will be valid, in seconds; must be between 60 and 86400 */
  valid_for: number = 0;
}

/** Decode a TDLib `createTemporaryPassword` object; null input -> null. */
export function decodeCreateTemporaryPassword(json: TdJsonRaw): CreateTemporaryPassword | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateTemporaryPassword = new CreateTemporaryPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.password = asString(record['password']);
  out.valid_for = asNumber(record['valid_for']);
  return out;
}

/** Encode a `createTemporaryPassword` into TDLib JSON wire shape. */
export function encodeCreateTemporaryPassword(value: CreateTemporaryPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createTemporaryPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['password'] = value.password;
  out['valid_for'] = value.valid_for;
  return out;
}

/** createTextCompositionStyle = TextCompositionStyle — Creates a custom text composition style. May return an error with a message "TONES_SAVED_TOO_MANY" if the maximum number of added custom styles has been reached */
export class CreateTextCompositionStyle {
  type: 'createTextCompositionStyle' = 'createTextCompositionStyle';
  extra?: string;
  /** title: string — Title of the style; 1-getOption("text_composition_style_title_length_max") characters */
  title: string = '';
  /** custom_emoji_id: int64 — Identifier of the custom emoji corresponding to the style */
  custom_emoji_id: string = '';
  /** prompt: string — Prompt that will be used for text composition; 1-getOption("text_composition_style_prompt_length_max") characters */
  prompt: string = '';
  /** show_creator: Bool — Pass true if the current user must be shown as the creator of the style */
  show_creator: boolean = false;
}

/** Decode a TDLib `createTextCompositionStyle` object; null input -> null. */
export function decodeCreateTextCompositionStyle(json: TdJsonRaw): CreateTextCompositionStyle | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateTextCompositionStyle = new CreateTextCompositionStyle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  out.prompt = asString(record['prompt']);
  out.show_creator = asBool(record['show_creator']);
  return out;
}

/** Encode a `createTextCompositionStyle` into TDLib JSON wire shape. */
export function encodeCreateTextCompositionStyle(value: CreateTextCompositionStyle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createTextCompositionStyle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['custom_emoji_id'] = value.custom_emoji_id;
  out['prompt'] = value.prompt;
  out['show_creator'] = value.show_creator;
  return out;
}

/** createVideoChat = GroupCallId — Creates a video chat (a group call bound to a chat); for basic groups, supergroups and channels only; requires can_manage_video_chats administrator right */
export class CreateVideoChat {
  type: 'createVideoChat' = 'createVideoChat';
  extra?: string;
  /** chat_id: int53 — Identifier of a chat in which the video chat will be created */
  chat_id: number = 0;
  /** title: string — Group call title; if empty, chat title will be used */
  title: string = '';
  /** start_date: int32 — Point in time (Unix timestamp) when the group call is expected to be started by an administrator; 0 to start the video chat immediately. The date must be at least 10 seconds and at most 8 days in the future */
  start_date: number = 0;
  /** is_rtmp_stream: Bool — Pass true to create an RTMP stream instead of an ordinary video chat */
  is_rtmp_stream: boolean = false;
}

/** Decode a TDLib `createVideoChat` object; null input -> null. */
export function decodeCreateVideoChat(json: TdJsonRaw): CreateVideoChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreateVideoChat = new CreateVideoChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.title = asString(record['title']);
  out.start_date = asNumber(record['start_date']);
  out.is_rtmp_stream = asBool(record['is_rtmp_stream']);
  return out;
}

/** Encode a `createVideoChat` into TDLib JSON wire shape. */
export function encodeCreateVideoChat(value: CreateVideoChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createVideoChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['title'] = value.title;
  out['start_date'] = value.start_date;
  out['is_rtmp_stream'] = value.is_rtmp_stream;
  return out;
}

/** createdBasicGroupChat = CreatedBasicGroupChat — Contains information about a newly created basic group chat */
export class CreatedBasicGroupChat {
  type: 'createdBasicGroupChat' = 'createdBasicGroupChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** failed_to_add_members: failedToAddMembers — Information about failed to add members */
  failed_to_add_members: FailedToAddMembers | null = null;
}

/** Decode a TDLib `createdBasicGroupChat` object; null input -> null. */
export function decodeCreatedBasicGroupChat(json: TdJsonRaw): CreatedBasicGroupChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CreatedBasicGroupChat = new CreatedBasicGroupChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.failed_to_add_members = decodeFailedToAddMembers(record['failed_to_add_members']);
  return out;
}

/** Encode a `createdBasicGroupChat` into TDLib JSON wire shape. */
export function encodeCreatedBasicGroupChat(value: CreatedBasicGroupChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'createdBasicGroupChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['failed_to_add_members'] = value.failed_to_add_members === null ? null : encodeFailedToAddMembers(value.failed_to_add_members);
  return out;
}

/** currentWeather = CurrentWeather — Describes the current weather */
export class CurrentWeather {
  type: 'currentWeather' = 'currentWeather';
  extra?: string;
  /** temperature: double — Temperature, in degree Celsius */
  temperature: number = 0;
  /** emoji: string — Emoji representing the weather */
  emoji: string = '';
}

/** Decode a TDLib `currentWeather` object; null input -> null. */
export function decodeCurrentWeather(json: TdJsonRaw): CurrentWeather | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CurrentWeather = new CurrentWeather();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.temperature = asNumber(record['temperature']);
  out.emoji = asString(record['emoji']);
  return out;
}

/** Encode a `currentWeather` into TDLib JSON wire shape. */
export function encodeCurrentWeather(value: CurrentWeather): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'currentWeather';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['temperature'] = value.temperature;
  out['emoji'] = value.emoji;
  return out;
}

/** customRequestResult = CustomRequestResult — Contains the result of a custom request */
export class CustomRequestResult {
  type: 'customRequestResult' = 'customRequestResult';
  extra?: string;
  /** result: string — A JSON-serialized result */
  result: string = '';
}

/** Decode a TDLib `customRequestResult` object; null input -> null. */
export function decodeCustomRequestResult(json: TdJsonRaw): CustomRequestResult | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: CustomRequestResult = new CustomRequestResult();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.result = asString(record['result']);
  return out;
}

/** Encode a `customRequestResult` into TDLib JSON wire shape. */
export function encodeCustomRequestResult(value: CustomRequestResult): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'customRequestResult';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['result'] = value.result;
  return out;
}
