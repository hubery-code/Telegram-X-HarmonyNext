/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { AffiliateProgramInfo, Animation, decodeAffiliateProgramInfo, decodeAnimation, encodeAffiliateProgramInfo, encodeAnimation } from './TdTypes_A';
import { ChatAdministratorRights, ChatInviteLink, ChatMember, ChatPhoto, decodeChatAdministratorRights, decodeChatInviteLink, decodeChatMember, decodeChatPhoto, encodeChatAdministratorRights, encodeChatInviteLink, encodeChatMember, encodeChatPhoto } from './TdTypes_C';
import { Document, decodeDocument, encodeDocument } from './TdTypes_D';
import { FormattedText, decodeFormattedText, encodeFormattedText } from './TdTypes_F';
import { Location, decodeLocation, encodeLocation } from './TdTypes_L';
import { Message, decodeMessage, encodeMessage } from './TdTypes_M';
import { Photo, decodePhoto, encodePhoto } from './TdTypes_P';
import { Sticker, decodeSticker, encodeSticker } from './TdTypes_S';
import { WebApp, decodeWebApp, encodeWebApp } from './TdTypes_W';
import { TdBackgroundFill, TdBackgroundType, TdBusinessAwayMessageSchedule, TdBusinessFeature, TdChatMemberStatus, TdInternalLinkType, TdMessageSender, TdStoryContent, decodeTdBackgroundFill, decodeTdBackgroundType, decodeTdBusinessAwayMessageSchedule, decodeTdBusinessFeature, decodeTdChatMemberStatus, decodeTdInternalLinkType, decodeTdMessageSender, decodeTdStoryContent, encodeTdBackgroundFill, encodeTdBackgroundType, encodeTdBusinessAwayMessageSchedule, encodeTdBusinessFeature, encodeTdChatMemberStatus, encodeTdInternalLinkType, encodeTdMessageSender, encodeTdStoryContent } from './TdUnions';

/** background = Background — Describes a chat background */
export class Background {
  type: 'background' = 'background';
  extra?: string;
  /** id: int64 — Unique background identifier */
  id: string = '';
  /** is_default: Bool — True, if this is one of default backgrounds */
  is_default: boolean = false;
  /** is_dark: Bool — True, if the background is dark and is recommended to be used with dark theme */
  is_dark: boolean = false;
  /** name: string — Unique background name */
  name: string = '';
  /** document: document — Document with the background; may be null. Null only for filled and chat theme backgrounds */
  document: Document | null = null;
  /** type: BackgroundType — Type of the background */
  type_: TdBackgroundType | null = null;
}

/** Decode a TDLib `background` object; null input -> null. */
export function decodeBackground(json: TdJsonRaw): Background | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Background = new Background();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.is_default = asBool(record['is_default']);
  out.is_dark = asBool(record['is_dark']);
  out.name = asString(record['name']);
  out.document = decodeDocument(record['document']);
  out.type_ = decodeTdBackgroundType(record['type']);
  return out;
}

/** Encode a `background` into TDLib JSON wire shape. */
export function encodeBackground(value: Background): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'background';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['is_default'] = value.is_default;
  out['is_dark'] = value.is_dark;
  out['name'] = value.name;
  out['document'] = value.document === null ? null : encodeDocument(value.document);
  out['type'] = value.type_ === null ? null : encodeTdBackgroundType(value.type_);
  return out;
}

/** backgroundFillFreeformGradient = BackgroundFill — Describes a freeform gradient fill of a background */
export class BackgroundFillFreeformGradient {
  type: 'backgroundFillFreeformGradient' = 'backgroundFillFreeformGradient';
  extra?: string;
  /** colors: vector<int32> — A list of 3 or 4 colors of the freeform gradient in the RGB format */
  colors: number[] = [];
}

/** Decode a TDLib `backgroundFillFreeformGradient` object; null input -> null. */
export function decodeBackgroundFillFreeformGradient(json: TdJsonRaw): BackgroundFillFreeformGradient | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BackgroundFillFreeformGradient = new BackgroundFillFreeformGradient();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.colors = decodeVector(record['colors'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `backgroundFillFreeformGradient` into TDLib JSON wire shape. */
export function encodeBackgroundFillFreeformGradient(value: BackgroundFillFreeformGradient): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'backgroundFillFreeformGradient';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['colors'] = encodeVector(value.colors, (item: number): TdJsonRaw => item);
  return out;
}

/** backgroundFillGradient = BackgroundFill — Describes a gradient fill of a background */
export class BackgroundFillGradient {
  type: 'backgroundFillGradient' = 'backgroundFillGradient';
  extra?: string;
  /** top_color: int32 — A top color of the background in the RGB format */
  top_color: number = 0;
  /** bottom_color: int32 — A bottom color of the background in the RGB format */
  bottom_color: number = 0;
  /** rotation_angle: int32 — Clockwise rotation angle of the gradient, in degrees; 0-359. Must always be divisible by 45 */
  rotation_angle: number = 0;
}

/** Decode a TDLib `backgroundFillGradient` object; null input -> null. */
export function decodeBackgroundFillGradient(json: TdJsonRaw): BackgroundFillGradient | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BackgroundFillGradient = new BackgroundFillGradient();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.top_color = asNumber(record['top_color']);
  out.bottom_color = asNumber(record['bottom_color']);
  out.rotation_angle = asNumber(record['rotation_angle']);
  return out;
}

/** Encode a `backgroundFillGradient` into TDLib JSON wire shape. */
export function encodeBackgroundFillGradient(value: BackgroundFillGradient): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'backgroundFillGradient';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['top_color'] = value.top_color;
  out['bottom_color'] = value.bottom_color;
  out['rotation_angle'] = value.rotation_angle;
  return out;
}

/** backgroundFillSolid = BackgroundFill — Describes a solid fill of a background */
export class BackgroundFillSolid {
  type: 'backgroundFillSolid' = 'backgroundFillSolid';
  extra?: string;
  /** color: int32 — A color of the background in the RGB format */
  color: number = 0;
}

/** Decode a TDLib `backgroundFillSolid` object; null input -> null. */
export function decodeBackgroundFillSolid(json: TdJsonRaw): BackgroundFillSolid | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BackgroundFillSolid = new BackgroundFillSolid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.color = asNumber(record['color']);
  return out;
}

/** Encode a `backgroundFillSolid` into TDLib JSON wire shape. */
export function encodeBackgroundFillSolid(value: BackgroundFillSolid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'backgroundFillSolid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['color'] = value.color;
  return out;
}

/** backgroundTypeChatTheme = BackgroundType — A background from a chat theme based on an emoji; can be used only as a chat background in channels */
export class BackgroundTypeChatTheme {
  type: 'backgroundTypeChatTheme' = 'backgroundTypeChatTheme';
  extra?: string;
  /** theme_name: string — Name of the emoji chat theme */
  theme_name: string = '';
}

/** Decode a TDLib `backgroundTypeChatTheme` object; null input -> null. */
export function decodeBackgroundTypeChatTheme(json: TdJsonRaw): BackgroundTypeChatTheme | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BackgroundTypeChatTheme = new BackgroundTypeChatTheme();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.theme_name = asString(record['theme_name']);
  return out;
}

/** Encode a `backgroundTypeChatTheme` into TDLib JSON wire shape. */
export function encodeBackgroundTypeChatTheme(value: BackgroundTypeChatTheme): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'backgroundTypeChatTheme';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['theme_name'] = value.theme_name;
  return out;
}

/** backgroundTypeFill = BackgroundType — A filled background */
export class BackgroundTypeFill {
  type: 'backgroundTypeFill' = 'backgroundTypeFill';
  extra?: string;
  /** fill: BackgroundFill — The background fill */
  fill: TdBackgroundFill | null = null;
}

/** Decode a TDLib `backgroundTypeFill` object; null input -> null. */
export function decodeBackgroundTypeFill(json: TdJsonRaw): BackgroundTypeFill | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BackgroundTypeFill = new BackgroundTypeFill();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.fill = decodeTdBackgroundFill(record['fill']);
  return out;
}

/** Encode a `backgroundTypeFill` into TDLib JSON wire shape. */
export function encodeBackgroundTypeFill(value: BackgroundTypeFill): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'backgroundTypeFill';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['fill'] = value.fill === null ? null : encodeTdBackgroundFill(value.fill);
  return out;
}

/** backgroundTypePattern = BackgroundType — A PNG or TGV (gzipped subset of SVG with MIME type "application/x-tgwallpattern") pattern to be combined with the background fill chosen by the user */
export class BackgroundTypePattern {
  type: 'backgroundTypePattern' = 'backgroundTypePattern';
  extra?: string;
  /** fill: BackgroundFill — Fill of the background */
  fill: TdBackgroundFill | null = null;
  /** intensity: int32 — Intensity of the pattern when it is shown above the filled background; 0-100 */
  intensity: number = 0;
  /** is_inverted: Bool — True, if the background fill must be applied only to the pattern itself. All other pixels are black in this case. For dark themes only */
  is_inverted: boolean = false;
  /** is_moving: Bool — True, if the background needs to be slightly moved when device is tilted */
  is_moving: boolean = false;
}

/** Decode a TDLib `backgroundTypePattern` object; null input -> null. */
export function decodeBackgroundTypePattern(json: TdJsonRaw): BackgroundTypePattern | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BackgroundTypePattern = new BackgroundTypePattern();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.fill = decodeTdBackgroundFill(record['fill']);
  out.intensity = asNumber(record['intensity']);
  out.is_inverted = asBool(record['is_inverted']);
  out.is_moving = asBool(record['is_moving']);
  return out;
}

/** Encode a `backgroundTypePattern` into TDLib JSON wire shape. */
export function encodeBackgroundTypePattern(value: BackgroundTypePattern): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'backgroundTypePattern';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['fill'] = value.fill === null ? null : encodeTdBackgroundFill(value.fill);
  out['intensity'] = value.intensity;
  out['is_inverted'] = value.is_inverted;
  out['is_moving'] = value.is_moving;
  return out;
}

/** backgroundTypeWallpaper = BackgroundType — A wallpaper in JPEG format */
export class BackgroundTypeWallpaper {
  type: 'backgroundTypeWallpaper' = 'backgroundTypeWallpaper';
  extra?: string;
  /** is_blurred: Bool — True, if the wallpaper must be downscaled to fit in 450x450 square and then box-blurred with radius 12 */
  is_blurred: boolean = false;
  /** is_moving: Bool — True, if the background needs to be slightly moved when device is tilted */
  is_moving: boolean = false;
}

/** Decode a TDLib `backgroundTypeWallpaper` object; null input -> null. */
export function decodeBackgroundTypeWallpaper(json: TdJsonRaw): BackgroundTypeWallpaper | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BackgroundTypeWallpaper = new BackgroundTypeWallpaper();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_blurred = asBool(record['is_blurred']);
  out.is_moving = asBool(record['is_moving']);
  return out;
}

/** Encode a `backgroundTypeWallpaper` into TDLib JSON wire shape. */
export function encodeBackgroundTypeWallpaper(value: BackgroundTypeWallpaper): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'backgroundTypeWallpaper';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_blurred'] = value.is_blurred;
  out['is_moving'] = value.is_moving;
  return out;
}

/** backgrounds = Backgrounds — Contains a list of backgrounds */
export class Backgrounds {
  type: 'backgrounds' = 'backgrounds';
  extra?: string;
  /** backgrounds: vector<background> — A list of backgrounds */
  backgrounds: Background[] = [];
}

/** Decode a TDLib `backgrounds` object; null input -> null. */
export function decodeBackgrounds(json: TdJsonRaw): Backgrounds | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Backgrounds = new Backgrounds();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.backgrounds = decodeVector(record['backgrounds'], (item: TdJsonRaw): Background => decodeBackground(item) as Background);
  return out;
}

/** Encode a `backgrounds` into TDLib JSON wire shape. */
export function encodeBackgrounds(value: Backgrounds): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'backgrounds';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['backgrounds'] = encodeVector(value.backgrounds, (item: Background | null): TdJsonRaw => item === null ? null : encodeBackground(item));
  return out;
}

/** banChatMember = Ok — Bans a member in a chat; requires can_restrict_members administrator right. Members can't be banned in private or secret chats. In supergroups and channels, the user will not be able to return to the group on their o... */
export class BanChatMember {
  type: 'banChatMember' = 'banChatMember';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** member_id: MessageSender — Member identifier */
  member_id: TdMessageSender | null = null;
  /** banned_until_date: int32 — Point in time (Unix timestamp) when the user will be unbanned; 0 if never. If the user is banned for more than 366 days or for less than 30 seconds from the current time, the user is considered to be banned for... */
  banned_until_date: number = 0;
  /** revoke_messages: Bool — Pass true to delete all messages in the chat for the user who is being removed. Always true for supergroups and channels */
  revoke_messages: boolean = false;
}

/** Decode a TDLib `banChatMember` object; null input -> null. */
export function decodeBanChatMember(json: TdJsonRaw): BanChatMember | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BanChatMember = new BanChatMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.member_id = decodeTdMessageSender(record['member_id']);
  out.banned_until_date = asNumber(record['banned_until_date']);
  out.revoke_messages = asBool(record['revoke_messages']);
  return out;
}

/** Encode a `banChatMember` into TDLib JSON wire shape. */
export function encodeBanChatMember(value: BanChatMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'banChatMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['member_id'] = value.member_id === null ? null : encodeTdMessageSender(value.member_id);
  out['banned_until_date'] = value.banned_until_date;
  out['revoke_messages'] = value.revoke_messages;
  return out;
}

/** banGroupCallParticipants = Ok — Bans users from a group call not bound to a chat; requires groupCall.is_owned. Only the owner of the group call can invite the banned users back */
export class BanGroupCallParticipants {
  type: 'banGroupCallParticipants' = 'banGroupCallParticipants';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** user_ids: vector<int64> — Identifiers of group call participants to ban; identifiers of unknown users from the update updateGroupCallParticipants can be also passed to the method */
  user_ids: string[] = [];
}

/** Decode a TDLib `banGroupCallParticipants` object; null input -> null. */
export function decodeBanGroupCallParticipants(json: TdJsonRaw): BanGroupCallParticipants | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BanGroupCallParticipants = new BanGroupCallParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): string => asInt64(item));
  return out;
}

/** Encode a `banGroupCallParticipants` into TDLib JSON wire shape. */
export function encodeBanGroupCallParticipants(value: BanGroupCallParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'banGroupCallParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['user_ids'] = encodeVector(value.user_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** bankCardActionOpenUrl = BankCardActionOpenUrl — Describes an action associated with a bank card number */
export class BankCardActionOpenUrl {
  type: 'bankCardActionOpenUrl' = 'bankCardActionOpenUrl';
  extra?: string;
  /** text: string — Action text */
  text: string = '';
  /** url: string — The URL to be opened */
  url: string = '';
}

/** Decode a TDLib `bankCardActionOpenUrl` object; null input -> null. */
export function decodeBankCardActionOpenUrl(json: TdJsonRaw): BankCardActionOpenUrl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BankCardActionOpenUrl = new BankCardActionOpenUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `bankCardActionOpenUrl` into TDLib JSON wire shape. */
export function encodeBankCardActionOpenUrl(value: BankCardActionOpenUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'bankCardActionOpenUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['url'] = value.url;
  return out;
}

/** bankCardInfo = BankCardInfo — Information about a bank card */
export class BankCardInfo {
  type: 'bankCardInfo' = 'bankCardInfo';
  extra?: string;
  /** title: string — Title of the bank card description */
  title: string = '';
  /** actions: vector<bankCardActionOpenUrl> — Actions that can be done with the bank card number */
  actions: BankCardActionOpenUrl[] = [];
}

/** Decode a TDLib `bankCardInfo` object; null input -> null. */
export function decodeBankCardInfo(json: TdJsonRaw): BankCardInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BankCardInfo = new BankCardInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.actions = decodeVector(record['actions'], (item: TdJsonRaw): BankCardActionOpenUrl => decodeBankCardActionOpenUrl(item) as BankCardActionOpenUrl);
  return out;
}

/** Encode a `bankCardInfo` into TDLib JSON wire shape. */
export function encodeBankCardInfo(value: BankCardInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'bankCardInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['actions'] = encodeVector(value.actions, (item: BankCardActionOpenUrl | null): TdJsonRaw => item === null ? null : encodeBankCardActionOpenUrl(item));
  return out;
}

/** basicGroup = BasicGroup — Represents a basic group of 0-200 users (must be upgraded to a supergroup to accommodate more than 200 users) */
export class BasicGroup {
  type: 'basicGroup' = 'basicGroup';
  extra?: string;
  /** id: int53 — Group identifier */
  id: number = 0;
  /** member_count: int32 — Number of members in the group */
  member_count: number = 0;
  /** status: ChatMemberStatus — Status of the current user in the group */
  status: TdChatMemberStatus | null = null;
  /** is_active: Bool — True, if the group is active */
  is_active: boolean = false;
  /** upgraded_to_supergroup_id: int53 — Identifier of the supergroup to which this group was upgraded; 0 if none */
  upgraded_to_supergroup_id: number = 0;
}

/** Decode a TDLib `basicGroup` object; null input -> null. */
export function decodeBasicGroup(json: TdJsonRaw): BasicGroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BasicGroup = new BasicGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.member_count = asNumber(record['member_count']);
  out.status = decodeTdChatMemberStatus(record['status']);
  out.is_active = asBool(record['is_active']);
  out.upgraded_to_supergroup_id = asNumber(record['upgraded_to_supergroup_id']);
  return out;
}

/** Encode a `basicGroup` into TDLib JSON wire shape. */
export function encodeBasicGroup(value: BasicGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'basicGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['member_count'] = value.member_count;
  out['status'] = value.status === null ? null : encodeTdChatMemberStatus(value.status);
  out['is_active'] = value.is_active;
  out['upgraded_to_supergroup_id'] = value.upgraded_to_supergroup_id;
  return out;
}

/** basicGroupFullInfo = BasicGroupFullInfo — Contains full information about a basic group */
export class BasicGroupFullInfo {
  type: 'basicGroupFullInfo' = 'basicGroupFullInfo';
  extra?: string;
  /** photo: chatPhoto — Chat photo; may be null if empty or unknown. If non-null, then it is the same photo as in chat.photo */
  photo: ChatPhoto | null = null;
  /** description: string */
  description: string = '';
  /** creator_user_id: int53 — User identifier of the creator of the group; 0 if unknown */
  creator_user_id: number = 0;
  /** members: vector<chatMember> — Group members */
  members: ChatMember[] = [];
  /** can_hide_members: Bool — True, if non-administrators and non-bots can be hidden in responses to getSupergroupMembers and searchChatMembers for non-administrators after upgrading the basic group to a supergroup */
  can_hide_members: boolean = false;
  /** can_toggle_aggressive_anti_spam: Bool — True, if aggressive anti-spam checks can be enabled or disabled in the supergroup after upgrading the basic group to a supergroup */
  can_toggle_aggressive_anti_spam: boolean = false;
  /** invite_link: chatInviteLink — Primary invite link for this group; may be null. For chat administrators with can_invite_users right only. Updated only after the basic group is opened */
  invite_link: ChatInviteLink | null = null;
  /** bot_commands: vector<botCommands> — List of commands of bots in the group */
  bot_commands: BotCommands[] = [];
}

/** Decode a TDLib `basicGroupFullInfo` object; null input -> null. */
export function decodeBasicGroupFullInfo(json: TdJsonRaw): BasicGroupFullInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BasicGroupFullInfo = new BasicGroupFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  out.description = asString(record['description']);
  out.creator_user_id = asNumber(record['creator_user_id']);
  out.members = decodeVector(record['members'], (item: TdJsonRaw): ChatMember => decodeChatMember(item) as ChatMember);
  out.can_hide_members = asBool(record['can_hide_members']);
  out.can_toggle_aggressive_anti_spam = asBool(record['can_toggle_aggressive_anti_spam']);
  out.invite_link = decodeChatInviteLink(record['invite_link']);
  out.bot_commands = decodeVector(record['bot_commands'], (item: TdJsonRaw): BotCommands => decodeBotCommands(item) as BotCommands);
  return out;
}

/** Encode a `basicGroupFullInfo` into TDLib JSON wire shape. */
export function encodeBasicGroupFullInfo(value: BasicGroupFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'basicGroupFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  out['description'] = value.description;
  out['creator_user_id'] = value.creator_user_id;
  out['members'] = encodeVector(value.members, (item: ChatMember | null): TdJsonRaw => item === null ? null : encodeChatMember(item));
  out['can_hide_members'] = value.can_hide_members;
  out['can_toggle_aggressive_anti_spam'] = value.can_toggle_aggressive_anti_spam;
  out['invite_link'] = value.invite_link === null ? null : encodeChatInviteLink(value.invite_link);
  out['bot_commands'] = encodeVector(value.bot_commands, (item: BotCommands | null): TdJsonRaw => item === null ? null : encodeBotCommands(item));
  return out;
}

/** birthdate = Birthdate — Represents a birthdate of a user */
export class Birthdate {
  type: 'birthdate' = 'birthdate';
  extra?: string;
  /** day: int32 — Day of the month; 1-31 */
  day: number = 0;
  /** month: int32 — Month of the year; 1-12 */
  month: number = 0;
  /** year: int32 — Birth year; 0 if unknown */
  year: number = 0;
}

/** Decode a TDLib `birthdate` object; null input -> null. */
export function decodeBirthdate(json: TdJsonRaw): Birthdate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Birthdate = new Birthdate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.day = asNumber(record['day']);
  out.month = asNumber(record['month']);
  out.year = asNumber(record['year']);
  return out;
}

/** Encode a `birthdate` into TDLib JSON wire shape. */
export function encodeBirthdate(value: Birthdate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'birthdate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['day'] = value.day;
  out['month'] = value.month;
  out['year'] = value.year;
  return out;
}

/** blockListMain = BlockList — The main block list that disallows writing messages to the current user, receiving their status and photo, viewing of stories, and some other actions */
export class BlockListMain {
  type: 'blockListMain' = 'blockListMain';
  extra?: string;
}

/** Decode a TDLib `blockListMain` object; null input -> null. */
export function decodeBlockListMain(json: TdJsonRaw): BlockListMain | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BlockListMain = new BlockListMain();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `blockListMain` into TDLib JSON wire shape. */
export function encodeBlockListMain(value: BlockListMain): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'blockListMain';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** blockListStories = BlockList — The block list that disallows viewing of stories of the current user */
export class BlockListStories {
  type: 'blockListStories' = 'blockListStories';
  extra?: string;
}

/** Decode a TDLib `blockListStories` object; null input -> null. */
export function decodeBlockListStories(json: TdJsonRaw): BlockListStories | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BlockListStories = new BlockListStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `blockListStories` into TDLib JSON wire shape. */
export function encodeBlockListStories(value: BlockListStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'blockListStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** blockMessageSenderFromReplies = Ok — Blocks an original sender of a message in the Replies chat */
export class BlockMessageSenderFromReplies {
  type: 'blockMessageSenderFromReplies' = 'blockMessageSenderFromReplies';
  extra?: string;
  /** message_id: int53 — The identifier of an incoming message in the Replies chat */
  message_id: number = 0;
  /** delete_message: Bool — Pass true to delete the message */
  delete_message: boolean = false;
  /** delete_all_messages: Bool — Pass true to delete all messages from the same sender */
  delete_all_messages: boolean = false;
  /** report_spam: Bool — Pass true to report the sender to the Telegram moderators */
  report_spam: boolean = false;
}

/** Decode a TDLib `blockMessageSenderFromReplies` object; null input -> null. */
export function decodeBlockMessageSenderFromReplies(json: TdJsonRaw): BlockMessageSenderFromReplies | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BlockMessageSenderFromReplies = new BlockMessageSenderFromReplies();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_id = asNumber(record['message_id']);
  out.delete_message = asBool(record['delete_message']);
  out.delete_all_messages = asBool(record['delete_all_messages']);
  out.report_spam = asBool(record['report_spam']);
  return out;
}

/** Encode a `blockMessageSenderFromReplies` into TDLib JSON wire shape. */
export function encodeBlockMessageSenderFromReplies(value: BlockMessageSenderFromReplies): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'blockMessageSenderFromReplies';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_id'] = value.message_id;
  out['delete_message'] = value.delete_message;
  out['delete_all_messages'] = value.delete_all_messages;
  out['report_spam'] = value.report_spam;
  return out;
}

/** boostChat = ChatBoostSlots — Boosts a chat and returns the list of available chat boost slots for the current user after the boost */
export class BoostChat {
  type: 'boostChat' = 'boostChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** slot_ids: vector<int32> — Identifiers of boost slots of the current user from which to apply boosts to the chat */
  slot_ids: number[] = [];
}

/** Decode a TDLib `boostChat` object; null input -> null. */
export function decodeBoostChat(json: TdJsonRaw): BoostChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BoostChat = new BoostChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.slot_ids = decodeVector(record['slot_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `boostChat` into TDLib JSON wire shape. */
export function encodeBoostChat(value: BoostChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'boostChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['slot_ids'] = encodeVector(value.slot_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** botAccessSettings = BotAccessSettings — Describes users that have access to a bot */
export class BotAccessSettings {
  type: 'botAccessSettings' = 'botAccessSettings';
  extra?: string;
  /** is_restricted: Bool — True, if access to the bot is restricted to its owner and selected users */
  is_restricted: boolean = false;
  /** added_user_ids: vector<int53> — Identifiers of the users who can use the bot additionally to the owner of the bot */
  added_user_ids: number[] = [];
}

/** Decode a TDLib `botAccessSettings` object; null input -> null. */
export function decodeBotAccessSettings(json: TdJsonRaw): BotAccessSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotAccessSettings = new BotAccessSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_restricted = asBool(record['is_restricted']);
  out.added_user_ids = decodeVector(record['added_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `botAccessSettings` into TDLib JSON wire shape. */
export function encodeBotAccessSettings(value: BotAccessSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botAccessSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_restricted'] = value.is_restricted;
  out['added_user_ids'] = encodeVector(value.added_user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** botCommand = BotCommand — Represents a command supported by a bot */
export class BotCommand {
  type: 'botCommand' = 'botCommand';
  extra?: string;
  /** command: string — Text of the bot command */
  command: string = '';
  /** description: string */
  description: string = '';
  /** is_ephemeral: Bool — True, if the command must send an ephemeral message instead of a regular one */
  is_ephemeral: boolean = false;
}

/** Decode a TDLib `botCommand` object; null input -> null. */
export function decodeBotCommand(json: TdJsonRaw): BotCommand | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotCommand = new BotCommand();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.command = asString(record['command']);
  out.description = asString(record['description']);
  out.is_ephemeral = asBool(record['is_ephemeral']);
  return out;
}

/** Encode a `botCommand` into TDLib JSON wire shape. */
export function encodeBotCommand(value: BotCommand): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botCommand';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['command'] = value.command;
  out['description'] = value.description;
  out['is_ephemeral'] = value.is_ephemeral;
  return out;
}

/** botCommandScopeAllChatAdministrators = BotCommandScope — A scope covering all group and supergroup chat administrators */
export class BotCommandScopeAllChatAdministrators {
  type: 'botCommandScopeAllChatAdministrators' = 'botCommandScopeAllChatAdministrators';
  extra?: string;
}

/** Decode a TDLib `botCommandScopeAllChatAdministrators` object; null input -> null. */
export function decodeBotCommandScopeAllChatAdministrators(json: TdJsonRaw): BotCommandScopeAllChatAdministrators | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotCommandScopeAllChatAdministrators = new BotCommandScopeAllChatAdministrators();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `botCommandScopeAllChatAdministrators` into TDLib JSON wire shape. */
export function encodeBotCommandScopeAllChatAdministrators(value: BotCommandScopeAllChatAdministrators): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botCommandScopeAllChatAdministrators';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** botCommandScopeAllGroupChats = BotCommandScope — A scope covering all group and supergroup chats */
export class BotCommandScopeAllGroupChats {
  type: 'botCommandScopeAllGroupChats' = 'botCommandScopeAllGroupChats';
  extra?: string;
}

/** Decode a TDLib `botCommandScopeAllGroupChats` object; null input -> null. */
export function decodeBotCommandScopeAllGroupChats(json: TdJsonRaw): BotCommandScopeAllGroupChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotCommandScopeAllGroupChats = new BotCommandScopeAllGroupChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `botCommandScopeAllGroupChats` into TDLib JSON wire shape. */
export function encodeBotCommandScopeAllGroupChats(value: BotCommandScopeAllGroupChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botCommandScopeAllGroupChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** botCommandScopeAllPrivateChats = BotCommandScope — A scope covering all private chats */
export class BotCommandScopeAllPrivateChats {
  type: 'botCommandScopeAllPrivateChats' = 'botCommandScopeAllPrivateChats';
  extra?: string;
}

/** Decode a TDLib `botCommandScopeAllPrivateChats` object; null input -> null. */
export function decodeBotCommandScopeAllPrivateChats(json: TdJsonRaw): BotCommandScopeAllPrivateChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotCommandScopeAllPrivateChats = new BotCommandScopeAllPrivateChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `botCommandScopeAllPrivateChats` into TDLib JSON wire shape. */
export function encodeBotCommandScopeAllPrivateChats(value: BotCommandScopeAllPrivateChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botCommandScopeAllPrivateChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** botCommandScopeChat = BotCommandScope — A scope covering all members of a chat */
export class BotCommandScopeChat {
  type: 'botCommandScopeChat' = 'botCommandScopeChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `botCommandScopeChat` object; null input -> null. */
export function decodeBotCommandScopeChat(json: TdJsonRaw): BotCommandScopeChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotCommandScopeChat = new BotCommandScopeChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `botCommandScopeChat` into TDLib JSON wire shape. */
export function encodeBotCommandScopeChat(value: BotCommandScopeChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botCommandScopeChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** botCommandScopeChatAdministrators = BotCommandScope — A scope covering all administrators of a chat */
export class BotCommandScopeChatAdministrators {
  type: 'botCommandScopeChatAdministrators' = 'botCommandScopeChatAdministrators';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `botCommandScopeChatAdministrators` object; null input -> null. */
export function decodeBotCommandScopeChatAdministrators(json: TdJsonRaw): BotCommandScopeChatAdministrators | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotCommandScopeChatAdministrators = new BotCommandScopeChatAdministrators();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `botCommandScopeChatAdministrators` into TDLib JSON wire shape. */
export function encodeBotCommandScopeChatAdministrators(value: BotCommandScopeChatAdministrators): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botCommandScopeChatAdministrators';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** botCommandScopeChatMember = BotCommandScope — A scope covering a member of a chat */
export class BotCommandScopeChatMember {
  type: 'botCommandScopeChatMember' = 'botCommandScopeChatMember';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
}

/** Decode a TDLib `botCommandScopeChatMember` object; null input -> null. */
export function decodeBotCommandScopeChatMember(json: TdJsonRaw): BotCommandScopeChatMember | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotCommandScopeChatMember = new BotCommandScopeChatMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `botCommandScopeChatMember` into TDLib JSON wire shape. */
export function encodeBotCommandScopeChatMember(value: BotCommandScopeChatMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botCommandScopeChatMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['user_id'] = value.user_id;
  return out;
}

/** botCommandScopeDefault = BotCommandScope — A scope covering all users */
export class BotCommandScopeDefault {
  type: 'botCommandScopeDefault' = 'botCommandScopeDefault';
  extra?: string;
}

/** Decode a TDLib `botCommandScopeDefault` object; null input -> null. */
export function decodeBotCommandScopeDefault(json: TdJsonRaw): BotCommandScopeDefault | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotCommandScopeDefault = new BotCommandScopeDefault();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `botCommandScopeDefault` into TDLib JSON wire shape. */
export function encodeBotCommandScopeDefault(value: BotCommandScopeDefault): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botCommandScopeDefault';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** botCommands = BotCommands — Contains a list of bot commands */
export class BotCommands {
  type: 'botCommands' = 'botCommands';
  extra?: string;
  /** bot_user_id: int53 — Bot's user identifier */
  bot_user_id: number = 0;
  /** commands: vector<botCommand> — List of bot commands */
  commands: BotCommand[] = [];
}

/** Decode a TDLib `botCommands` object; null input -> null. */
export function decodeBotCommands(json: TdJsonRaw): BotCommands | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotCommands = new BotCommands();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.commands = decodeVector(record['commands'], (item: TdJsonRaw): BotCommand => decodeBotCommand(item) as BotCommand);
  return out;
}

/** Encode a `botCommands` into TDLib JSON wire shape. */
export function encodeBotCommands(value: BotCommands): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botCommands';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['commands'] = encodeVector(value.commands, (item: BotCommand | null): TdJsonRaw => item === null ? null : encodeBotCommand(item));
  return out;
}

/** botInfo = BotInfo — Contains information about a bot */
export class BotInfo {
  type: 'botInfo' = 'botInfo';
  extra?: string;
  /** short_description: string — The text that is shown on the bot's profile page and is sent together with the link when users share the bot */
  short_description: string = '';
  /** description: string */
  description: string = '';
  /** photo: photo — Photo shown in the chat with the bot if the chat is empty; may be null */
  photo: Photo | null = null;
  /** animation: animation — Animation shown in the chat with the bot if the chat is empty; may be null */
  animation: Animation | null = null;
  /** manager_bot_user_id: int53 — Identifier of the bot, which manages the bot; 0 if none or unknown; for owner of the bot only */
  manager_bot_user_id: number = 0;
  /** menu_button: botMenuButton — Information about a button to show instead of the bot commands menu button; may be null if ordinary bot commands menu must be shown */
  menu_button: BotMenuButton | null = null;
  /** commands: vector<botCommand> — List of the bot commands */
  commands: BotCommand[] = [];
  /** privacy_policy_url: string — The HTTP link to the privacy policy of the bot. If empty, then /privacy command must be used if supported by the bot. If the command isn't supported, then https://telegram.org/privacy-tpa must be opened */
  privacy_policy_url: string = '';
  /** default_group_administrator_rights: chatAdministratorRights — Default administrator rights for adding the bot to basic group and supergroup chats; may be null */
  default_group_administrator_rights: ChatAdministratorRights | null = null;
  /** default_channel_administrator_rights: chatAdministratorRights — Default administrator rights for adding the bot to channels; may be null */
  default_channel_administrator_rights: ChatAdministratorRights | null = null;
  /** affiliate_program: affiliateProgramInfo — Information about the affiliate program of the bot; may be null if none */
  affiliate_program: AffiliateProgramInfo | null = null;
  /** web_app_background_light_color: int32 — Default light background color for bot Web Apps; -1 if not specified */
  web_app_background_light_color: number = 0;
  /** web_app_background_dark_color: int32 — Default dark background color for bot Web Apps; -1 if not specified */
  web_app_background_dark_color: number = 0;
  /** web_app_header_light_color: int32 — Default light header color for bot Web Apps; -1 if not specified */
  web_app_header_light_color: number = 0;
  /** web_app_header_dark_color: int32 — Default dark header color for bot Web Apps; -1 if not specified */
  web_app_header_dark_color: number = 0;
  /** verification_parameters: botVerificationParameters — Parameters of the verification that can be provided by the bot; may be null if none or the current user isn't the owner of the bot */
  verification_parameters: BotVerificationParameters | null = null;
  /** can_get_revenue_statistics: Bool — True, if the bot's revenue statistics are available to the current user */
  can_get_revenue_statistics: boolean = false;
  /** can_manage_emoji_status: Bool — True, if the bot can manage emoji status of the current user */
  can_manage_emoji_status: boolean = false;
  /** has_media_previews: Bool — True, if the bot has media previews */
  has_media_previews: boolean = false;
  /** edit_commands_link: InternalLinkType — The internal link, which can be used to edit bot commands; may be null */
  edit_commands_link: TdInternalLinkType | null = null;
  /** edit_description_link: InternalLinkType — The internal link, which can be used to edit bot description; may be null */
  edit_description_link: TdInternalLinkType | null = null;
  /** edit_description_media_link: InternalLinkType — The internal link, which can be used to edit the photo or animation shown in the chat with the bot if the chat is empty; may be null */
  edit_description_media_link: TdInternalLinkType | null = null;
  /** edit_settings_link: InternalLinkType — The internal link, which can be used to edit bot settings; may be null */
  edit_settings_link: TdInternalLinkType | null = null;
}

/** Decode a TDLib `botInfo` object; null input -> null. */
export function decodeBotInfo(json: TdJsonRaw): BotInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotInfo = new BotInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.short_description = asString(record['short_description']);
  out.description = asString(record['description']);
  out.photo = decodePhoto(record['photo']);
  out.animation = decodeAnimation(record['animation']);
  out.manager_bot_user_id = asNumber(record['manager_bot_user_id']);
  out.menu_button = decodeBotMenuButton(record['menu_button']);
  out.commands = decodeVector(record['commands'], (item: TdJsonRaw): BotCommand => decodeBotCommand(item) as BotCommand);
  out.privacy_policy_url = asString(record['privacy_policy_url']);
  out.default_group_administrator_rights = decodeChatAdministratorRights(record['default_group_administrator_rights']);
  out.default_channel_administrator_rights = decodeChatAdministratorRights(record['default_channel_administrator_rights']);
  out.affiliate_program = decodeAffiliateProgramInfo(record['affiliate_program']);
  out.web_app_background_light_color = asNumber(record['web_app_background_light_color']);
  out.web_app_background_dark_color = asNumber(record['web_app_background_dark_color']);
  out.web_app_header_light_color = asNumber(record['web_app_header_light_color']);
  out.web_app_header_dark_color = asNumber(record['web_app_header_dark_color']);
  out.verification_parameters = decodeBotVerificationParameters(record['verification_parameters']);
  out.can_get_revenue_statistics = asBool(record['can_get_revenue_statistics']);
  out.can_manage_emoji_status = asBool(record['can_manage_emoji_status']);
  out.has_media_previews = asBool(record['has_media_previews']);
  out.edit_commands_link = decodeTdInternalLinkType(record['edit_commands_link']);
  out.edit_description_link = decodeTdInternalLinkType(record['edit_description_link']);
  out.edit_description_media_link = decodeTdInternalLinkType(record['edit_description_media_link']);
  out.edit_settings_link = decodeTdInternalLinkType(record['edit_settings_link']);
  return out;
}

/** Encode a `botInfo` into TDLib JSON wire shape. */
export function encodeBotInfo(value: BotInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['short_description'] = value.short_description;
  out['description'] = value.description;
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  out['manager_bot_user_id'] = value.manager_bot_user_id;
  out['menu_button'] = value.menu_button === null ? null : encodeBotMenuButton(value.menu_button);
  out['commands'] = encodeVector(value.commands, (item: BotCommand | null): TdJsonRaw => item === null ? null : encodeBotCommand(item));
  out['privacy_policy_url'] = value.privacy_policy_url;
  out['default_group_administrator_rights'] = value.default_group_administrator_rights === null ? null : encodeChatAdministratorRights(value.default_group_administrator_rights);
  out['default_channel_administrator_rights'] = value.default_channel_administrator_rights === null ? null : encodeChatAdministratorRights(value.default_channel_administrator_rights);
  out['affiliate_program'] = value.affiliate_program === null ? null : encodeAffiliateProgramInfo(value.affiliate_program);
  out['web_app_background_light_color'] = value.web_app_background_light_color;
  out['web_app_background_dark_color'] = value.web_app_background_dark_color;
  out['web_app_header_light_color'] = value.web_app_header_light_color;
  out['web_app_header_dark_color'] = value.web_app_header_dark_color;
  out['verification_parameters'] = value.verification_parameters === null ? null : encodeBotVerificationParameters(value.verification_parameters);
  out['can_get_revenue_statistics'] = value.can_get_revenue_statistics;
  out['can_manage_emoji_status'] = value.can_manage_emoji_status;
  out['has_media_previews'] = value.has_media_previews;
  out['edit_commands_link'] = value.edit_commands_link === null ? null : encodeTdInternalLinkType(value.edit_commands_link);
  out['edit_description_link'] = value.edit_description_link === null ? null : encodeTdInternalLinkType(value.edit_description_link);
  out['edit_description_media_link'] = value.edit_description_media_link === null ? null : encodeTdInternalLinkType(value.edit_description_media_link);
  out['edit_settings_link'] = value.edit_settings_link === null ? null : encodeTdInternalLinkType(value.edit_settings_link);
  return out;
}

/** botMediaPreview = BotMediaPreview — Describes media previews of a bot */
export class BotMediaPreview {
  type: 'botMediaPreview' = 'botMediaPreview';
  extra?: string;
  /** date: int32 — Point in time (Unix timestamp) when the preview was added or changed last time */
  date: number = 0;
  /** content: StoryContent — Content of the preview; may only be of the types storyContentPhoto, storyContentVideo, or storyContentUnsupported */
  content: TdStoryContent | null = null;
}

/** Decode a TDLib `botMediaPreview` object; null input -> null. */
export function decodeBotMediaPreview(json: TdJsonRaw): BotMediaPreview | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotMediaPreview = new BotMediaPreview();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.date = asNumber(record['date']);
  out.content = decodeTdStoryContent(record['content']);
  return out;
}

/** Encode a `botMediaPreview` into TDLib JSON wire shape. */
export function encodeBotMediaPreview(value: BotMediaPreview): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botMediaPreview';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['date'] = value.date;
  out['content'] = value.content === null ? null : encodeTdStoryContent(value.content);
  return out;
}

/** botMediaPreviewInfo = BotMediaPreviewInfo — Contains a list of media previews of a bot for the given language and the list of languages for which the bot has dedicated previews */
export class BotMediaPreviewInfo {
  type: 'botMediaPreviewInfo' = 'botMediaPreviewInfo';
  extra?: string;
  /** previews: vector<botMediaPreview> — List of media previews */
  previews: BotMediaPreview[] = [];
  /** language_codes: vector<string> — List of language codes for which the bot has dedicated previews */
  language_codes: string[] = [];
}

/** Decode a TDLib `botMediaPreviewInfo` object; null input -> null. */
export function decodeBotMediaPreviewInfo(json: TdJsonRaw): BotMediaPreviewInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotMediaPreviewInfo = new BotMediaPreviewInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.previews = decodeVector(record['previews'], (item: TdJsonRaw): BotMediaPreview => decodeBotMediaPreview(item) as BotMediaPreview);
  out.language_codes = decodeVector(record['language_codes'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `botMediaPreviewInfo` into TDLib JSON wire shape. */
export function encodeBotMediaPreviewInfo(value: BotMediaPreviewInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botMediaPreviewInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['previews'] = encodeVector(value.previews, (item: BotMediaPreview | null): TdJsonRaw => item === null ? null : encodeBotMediaPreview(item));
  out['language_codes'] = encodeVector(value.language_codes, (item: string): TdJsonRaw => item);
  return out;
}

/** botMediaPreviews = BotMediaPreviews — Contains a list of media previews of a bot */
export class BotMediaPreviews {
  type: 'botMediaPreviews' = 'botMediaPreviews';
  extra?: string;
  /** previews: vector<botMediaPreview> — List of media previews */
  previews: BotMediaPreview[] = [];
}

/** Decode a TDLib `botMediaPreviews` object; null input -> null. */
export function decodeBotMediaPreviews(json: TdJsonRaw): BotMediaPreviews | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotMediaPreviews = new BotMediaPreviews();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.previews = decodeVector(record['previews'], (item: TdJsonRaw): BotMediaPreview => decodeBotMediaPreview(item) as BotMediaPreview);
  return out;
}

/** Encode a `botMediaPreviews` into TDLib JSON wire shape. */
export function encodeBotMediaPreviews(value: BotMediaPreviews): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botMediaPreviews';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['previews'] = encodeVector(value.previews, (item: BotMediaPreview | null): TdJsonRaw => item === null ? null : encodeBotMediaPreview(item));
  return out;
}

/** botMenuButton = BotMenuButton — Describes a button to be shown instead of bot commands menu button */
export class BotMenuButton {
  type: 'botMenuButton' = 'botMenuButton';
  extra?: string;
  /** text: string — Text of the button */
  text: string = '';
  /** url: string — URL of a Web App to open when the button is pressed. If the link is of the type internalLinkTypeWebApp, then it must be processed accordingly. Otherwise, the link must be passed to openWebApp */
  url: string = '';
}

/** Decode a TDLib `botMenuButton` object; null input -> null. */
export function decodeBotMenuButton(json: TdJsonRaw): BotMenuButton | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotMenuButton = new BotMenuButton();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `botMenuButton` into TDLib JSON wire shape. */
export function encodeBotMenuButton(value: BotMenuButton): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botMenuButton';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['url'] = value.url;
  return out;
}

/** botVerification = BotVerification — Describes verification status provided by a bot */
export class BotVerification {
  type: 'botVerification' = 'botVerification';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the bot that provided the verification */
  bot_user_id: number = 0;
  /** icon_custom_emoji_id: int64 — Identifier of the custom emoji that is used as the verification sign */
  icon_custom_emoji_id: string = '';
  /** custom_description: formattedText — Custom description of verification reason set by the bot. Can contain only Mention, Hashtag, Cashtag, PhoneNumber, BankCardNumber, Url, and EmailAddress entities */
  custom_description: FormattedText | null = null;
}

/** Decode a TDLib `botVerification` object; null input -> null. */
export function decodeBotVerification(json: TdJsonRaw): BotVerification | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotVerification = new BotVerification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.icon_custom_emoji_id = asInt64(record['icon_custom_emoji_id']);
  out.custom_description = decodeFormattedText(record['custom_description']);
  return out;
}

/** Encode a `botVerification` into TDLib JSON wire shape. */
export function encodeBotVerification(value: BotVerification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botVerification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['icon_custom_emoji_id'] = value.icon_custom_emoji_id;
  out['custom_description'] = value.custom_description === null ? null : encodeFormattedText(value.custom_description);
  return out;
}

/** botVerificationParameters = BotVerificationParameters — Describes parameters of verification that is provided by a bot */
export class BotVerificationParameters {
  type: 'botVerificationParameters' = 'botVerificationParameters';
  extra?: string;
  /** icon_custom_emoji_id: int64 — Identifier of the custom emoji that is used as the verification sign */
  icon_custom_emoji_id: string = '';
  /** organization_name: string — Name of the organization that provides verification */
  organization_name: string = '';
  /** default_custom_description: formattedText — Default custom description of verification reason to be used as placeholder in setMessageSenderBotVerification; may be null if none */
  default_custom_description: FormattedText | null = null;
  /** can_set_custom_description: Bool — True, if the bot is allowed to provide custom description for verified entities */
  can_set_custom_description: boolean = false;
}

/** Decode a TDLib `botVerificationParameters` object; null input -> null. */
export function decodeBotVerificationParameters(json: TdJsonRaw): BotVerificationParameters | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotVerificationParameters = new BotVerificationParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.icon_custom_emoji_id = asInt64(record['icon_custom_emoji_id']);
  out.organization_name = asString(record['organization_name']);
  out.default_custom_description = decodeFormattedText(record['default_custom_description']);
  out.can_set_custom_description = asBool(record['can_set_custom_description']);
  return out;
}

/** Encode a `botVerificationParameters` into TDLib JSON wire shape. */
export function encodeBotVerificationParameters(value: BotVerificationParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botVerificationParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['icon_custom_emoji_id'] = value.icon_custom_emoji_id;
  out['organization_name'] = value.organization_name;
  out['default_custom_description'] = value.default_custom_description === null ? null : encodeFormattedText(value.default_custom_description);
  out['can_set_custom_description'] = value.can_set_custom_description;
  return out;
}

/** botWriteAccessAllowReasonAcceptedRequest = BotWriteAccessAllowReason — The user accepted bot's request to send messages with allowBotToSendMessages */
export class BotWriteAccessAllowReasonAcceptedRequest {
  type: 'botWriteAccessAllowReasonAcceptedRequest' = 'botWriteAccessAllowReasonAcceptedRequest';
  extra?: string;
}

/** Decode a TDLib `botWriteAccessAllowReasonAcceptedRequest` object; null input -> null. */
export function decodeBotWriteAccessAllowReasonAcceptedRequest(json: TdJsonRaw): BotWriteAccessAllowReasonAcceptedRequest | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotWriteAccessAllowReasonAcceptedRequest = new BotWriteAccessAllowReasonAcceptedRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `botWriteAccessAllowReasonAcceptedRequest` into TDLib JSON wire shape. */
export function encodeBotWriteAccessAllowReasonAcceptedRequest(value: BotWriteAccessAllowReasonAcceptedRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botWriteAccessAllowReasonAcceptedRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** botWriteAccessAllowReasonAddedToAttachmentMenu = BotWriteAccessAllowReason — The user added the bot to attachment or side menu using toggleBotIsAddedToAttachmentMenu */
export class BotWriteAccessAllowReasonAddedToAttachmentMenu {
  type: 'botWriteAccessAllowReasonAddedToAttachmentMenu' = 'botWriteAccessAllowReasonAddedToAttachmentMenu';
  extra?: string;
}

/** Decode a TDLib `botWriteAccessAllowReasonAddedToAttachmentMenu` object; null input -> null. */
export function decodeBotWriteAccessAllowReasonAddedToAttachmentMenu(json: TdJsonRaw): BotWriteAccessAllowReasonAddedToAttachmentMenu | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotWriteAccessAllowReasonAddedToAttachmentMenu = new BotWriteAccessAllowReasonAddedToAttachmentMenu();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `botWriteAccessAllowReasonAddedToAttachmentMenu` into TDLib JSON wire shape. */
export function encodeBotWriteAccessAllowReasonAddedToAttachmentMenu(value: BotWriteAccessAllowReasonAddedToAttachmentMenu): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botWriteAccessAllowReasonAddedToAttachmentMenu';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** botWriteAccessAllowReasonConnectedWebsite = BotWriteAccessAllowReason — The user connected a website by logging in using Telegram Login Widget on it */
export class BotWriteAccessAllowReasonConnectedWebsite {
  type: 'botWriteAccessAllowReasonConnectedWebsite' = 'botWriteAccessAllowReasonConnectedWebsite';
  extra?: string;
  /** domain_name: string — Domain name of the connected website */
  domain_name: string = '';
}

/** Decode a TDLib `botWriteAccessAllowReasonConnectedWebsite` object; null input -> null. */
export function decodeBotWriteAccessAllowReasonConnectedWebsite(json: TdJsonRaw): BotWriteAccessAllowReasonConnectedWebsite | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotWriteAccessAllowReasonConnectedWebsite = new BotWriteAccessAllowReasonConnectedWebsite();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.domain_name = asString(record['domain_name']);
  return out;
}

/** Encode a `botWriteAccessAllowReasonConnectedWebsite` into TDLib JSON wire shape. */
export function encodeBotWriteAccessAllowReasonConnectedWebsite(value: BotWriteAccessAllowReasonConnectedWebsite): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botWriteAccessAllowReasonConnectedWebsite';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['domain_name'] = value.domain_name;
  return out;
}

/** botWriteAccessAllowReasonLaunchedWebApp = BotWriteAccessAllowReason — The user launched a Web App using getWebAppLinkUrl */
export class BotWriteAccessAllowReasonLaunchedWebApp {
  type: 'botWriteAccessAllowReasonLaunchedWebApp' = 'botWriteAccessAllowReasonLaunchedWebApp';
  extra?: string;
  /** web_app: webApp — Information about the Web App */
  web_app: WebApp | null = null;
}

/** Decode a TDLib `botWriteAccessAllowReasonLaunchedWebApp` object; null input -> null. */
export function decodeBotWriteAccessAllowReasonLaunchedWebApp(json: TdJsonRaw): BotWriteAccessAllowReasonLaunchedWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BotWriteAccessAllowReasonLaunchedWebApp = new BotWriteAccessAllowReasonLaunchedWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.web_app = decodeWebApp(record['web_app']);
  return out;
}

/** Encode a `botWriteAccessAllowReasonLaunchedWebApp` into TDLib JSON wire shape. */
export function encodeBotWriteAccessAllowReasonLaunchedWebApp(value: BotWriteAccessAllowReasonLaunchedWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'botWriteAccessAllowReasonLaunchedWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['web_app'] = value.web_app === null ? null : encodeWebApp(value.web_app);
  return out;
}

/** builtInThemeArctic = BuiltInTheme — Arctic light theme */
export class BuiltInThemeArctic {
  type: 'builtInThemeArctic' = 'builtInThemeArctic';
  extra?: string;
}

/** Decode a TDLib `builtInThemeArctic` object; null input -> null. */
export function decodeBuiltInThemeArctic(json: TdJsonRaw): BuiltInThemeArctic | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BuiltInThemeArctic = new BuiltInThemeArctic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `builtInThemeArctic` into TDLib JSON wire shape. */
export function encodeBuiltInThemeArctic(value: BuiltInThemeArctic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'builtInThemeArctic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** builtInThemeClassic = BuiltInTheme — Classic light theme */
export class BuiltInThemeClassic {
  type: 'builtInThemeClassic' = 'builtInThemeClassic';
  extra?: string;
}

/** Decode a TDLib `builtInThemeClassic` object; null input -> null. */
export function decodeBuiltInThemeClassic(json: TdJsonRaw): BuiltInThemeClassic | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BuiltInThemeClassic = new BuiltInThemeClassic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `builtInThemeClassic` into TDLib JSON wire shape. */
export function encodeBuiltInThemeClassic(value: BuiltInThemeClassic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'builtInThemeClassic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** builtInThemeDay = BuiltInTheme — Regular light theme */
export class BuiltInThemeDay {
  type: 'builtInThemeDay' = 'builtInThemeDay';
  extra?: string;
}

/** Decode a TDLib `builtInThemeDay` object; null input -> null. */
export function decodeBuiltInThemeDay(json: TdJsonRaw): BuiltInThemeDay | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BuiltInThemeDay = new BuiltInThemeDay();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `builtInThemeDay` into TDLib JSON wire shape. */
export function encodeBuiltInThemeDay(value: BuiltInThemeDay): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'builtInThemeDay';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** builtInThemeNight = BuiltInTheme — Regular dark theme */
export class BuiltInThemeNight {
  type: 'builtInThemeNight' = 'builtInThemeNight';
  extra?: string;
}

/** Decode a TDLib `builtInThemeNight` object; null input -> null. */
export function decodeBuiltInThemeNight(json: TdJsonRaw): BuiltInThemeNight | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BuiltInThemeNight = new BuiltInThemeNight();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `builtInThemeNight` into TDLib JSON wire shape. */
export function encodeBuiltInThemeNight(value: BuiltInThemeNight): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'builtInThemeNight';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** builtInThemeTinted = BuiltInTheme — Tinted dark theme */
export class BuiltInThemeTinted {
  type: 'builtInThemeTinted' = 'builtInThemeTinted';
  extra?: string;
}

/** Decode a TDLib `builtInThemeTinted` object; null input -> null. */
export function decodeBuiltInThemeTinted(json: TdJsonRaw): BuiltInThemeTinted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BuiltInThemeTinted = new BuiltInThemeTinted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `builtInThemeTinted` into TDLib JSON wire shape. */
export function encodeBuiltInThemeTinted(value: BuiltInThemeTinted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'builtInThemeTinted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessAwayMessageScheduleAlways = BusinessAwayMessageSchedule — Send away messages always */
export class BusinessAwayMessageScheduleAlways {
  type: 'businessAwayMessageScheduleAlways' = 'businessAwayMessageScheduleAlways';
  extra?: string;
}

/** Decode a TDLib `businessAwayMessageScheduleAlways` object; null input -> null. */
export function decodeBusinessAwayMessageScheduleAlways(json: TdJsonRaw): BusinessAwayMessageScheduleAlways | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessAwayMessageScheduleAlways = new BusinessAwayMessageScheduleAlways();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessAwayMessageScheduleAlways` into TDLib JSON wire shape. */
export function encodeBusinessAwayMessageScheduleAlways(value: BusinessAwayMessageScheduleAlways): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessAwayMessageScheduleAlways';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessAwayMessageScheduleCustom = BusinessAwayMessageSchedule — Send away messages only in the specified time span */
export class BusinessAwayMessageScheduleCustom {
  type: 'businessAwayMessageScheduleCustom' = 'businessAwayMessageScheduleCustom';
  extra?: string;
  /** start_date: int32 — Point in time (Unix timestamp) when the away messages will start to be sent */
  start_date: number = 0;
  /** end_date: int32 — Point in time (Unix timestamp) when the away messages will stop to be sent */
  end_date: number = 0;
}

/** Decode a TDLib `businessAwayMessageScheduleCustom` object; null input -> null. */
export function decodeBusinessAwayMessageScheduleCustom(json: TdJsonRaw): BusinessAwayMessageScheduleCustom | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessAwayMessageScheduleCustom = new BusinessAwayMessageScheduleCustom();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.start_date = asNumber(record['start_date']);
  out.end_date = asNumber(record['end_date']);
  return out;
}

/** Encode a `businessAwayMessageScheduleCustom` into TDLib JSON wire shape. */
export function encodeBusinessAwayMessageScheduleCustom(value: BusinessAwayMessageScheduleCustom): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessAwayMessageScheduleCustom';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['start_date'] = value.start_date;
  out['end_date'] = value.end_date;
  return out;
}

/** businessAwayMessageScheduleOutsideOfOpeningHours = BusinessAwayMessageSchedule — Send away messages outside of the business opening hours */
export class BusinessAwayMessageScheduleOutsideOfOpeningHours {
  type: 'businessAwayMessageScheduleOutsideOfOpeningHours' = 'businessAwayMessageScheduleOutsideOfOpeningHours';
  extra?: string;
}

/** Decode a TDLib `businessAwayMessageScheduleOutsideOfOpeningHours` object; null input -> null. */
export function decodeBusinessAwayMessageScheduleOutsideOfOpeningHours(json: TdJsonRaw): BusinessAwayMessageScheduleOutsideOfOpeningHours | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessAwayMessageScheduleOutsideOfOpeningHours = new BusinessAwayMessageScheduleOutsideOfOpeningHours();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessAwayMessageScheduleOutsideOfOpeningHours` into TDLib JSON wire shape. */
export function encodeBusinessAwayMessageScheduleOutsideOfOpeningHours(value: BusinessAwayMessageScheduleOutsideOfOpeningHours): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessAwayMessageScheduleOutsideOfOpeningHours';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessAwayMessageSettings = BusinessAwayMessageSettings — Describes settings for messages that are automatically sent by a Telegram Business account when it is away */
export class BusinessAwayMessageSettings {
  type: 'businessAwayMessageSettings' = 'businessAwayMessageSettings';
  extra?: string;
  /** shortcut_id: int32 — Unique quick reply shortcut identifier for the away messages */
  shortcut_id: number = 0;
  /** recipients: businessRecipients — Chosen recipients of the away messages */
  recipients: BusinessRecipients | null = null;
  /** schedule: BusinessAwayMessageSchedule — Settings used to check whether the current user is away */
  schedule: TdBusinessAwayMessageSchedule | null = null;
  /** offline_only: Bool — True, if the messages must not be sent if the account was online in the last 10 minutes */
  offline_only: boolean = false;
}

/** Decode a TDLib `businessAwayMessageSettings` object; null input -> null. */
export function decodeBusinessAwayMessageSettings(json: TdJsonRaw): BusinessAwayMessageSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessAwayMessageSettings = new BusinessAwayMessageSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_id = asNumber(record['shortcut_id']);
  out.recipients = decodeBusinessRecipients(record['recipients']);
  out.schedule = decodeTdBusinessAwayMessageSchedule(record['schedule']);
  out.offline_only = asBool(record['offline_only']);
  return out;
}

/** Encode a `businessAwayMessageSettings` into TDLib JSON wire shape. */
export function encodeBusinessAwayMessageSettings(value: BusinessAwayMessageSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessAwayMessageSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_id'] = value.shortcut_id;
  out['recipients'] = value.recipients === null ? null : encodeBusinessRecipients(value.recipients);
  out['schedule'] = value.schedule === null ? null : encodeTdBusinessAwayMessageSchedule(value.schedule);
  out['offline_only'] = value.offline_only;
  return out;
}

/** businessBotManageBar = BusinessBotManageBar — Contains information about a business bot that manages the chat */
export class BusinessBotManageBar {
  type: 'businessBotManageBar' = 'businessBotManageBar';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the bot */
  bot_user_id: number = 0;
  /** manage_url: string — URL to be opened to manage the bot */
  manage_url: string = '';
  /** is_bot_paused: Bool — True, if the bot is paused. Use toggleBusinessConnectedBotChatIsPaused to change the value of the field */
  is_bot_paused: boolean = false;
  /** can_bot_reply: Bool — True, if the bot can reply */
  can_bot_reply: boolean = false;
}

/** Decode a TDLib `businessBotManageBar` object; null input -> null. */
export function decodeBusinessBotManageBar(json: TdJsonRaw): BusinessBotManageBar | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessBotManageBar = new BusinessBotManageBar();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.manage_url = asString(record['manage_url']);
  out.is_bot_paused = asBool(record['is_bot_paused']);
  out.can_bot_reply = asBool(record['can_bot_reply']);
  return out;
}

/** Encode a `businessBotManageBar` into TDLib JSON wire shape. */
export function encodeBusinessBotManageBar(value: BusinessBotManageBar): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessBotManageBar';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['manage_url'] = value.manage_url;
  out['is_bot_paused'] = value.is_bot_paused;
  out['can_bot_reply'] = value.can_bot_reply;
  return out;
}

/** businessBotRights = BusinessBotRights — Describes rights of a business bot */
export class BusinessBotRights {
  type: 'businessBotRights' = 'businessBotRights';
  extra?: string;
  /** can_reply: Bool — True, if the bot can send and edit messages in the private chats that had incoming messages in the last 24 hours */
  can_reply: boolean = false;
  /** can_read_messages: Bool — True, if the bot can mark incoming private messages as read */
  can_read_messages: boolean = false;
  /** can_delete_sent_messages: Bool — True, if the bot can delete sent messages */
  can_delete_sent_messages: boolean = false;
  /** can_delete_all_messages: Bool — True, if the bot can delete any message */
  can_delete_all_messages: boolean = false;
  /** can_edit_name: Bool — True, if the bot can edit name of the business account */
  can_edit_name: boolean = false;
  /** can_edit_bio: Bool — True, if the bot can edit bio of the business account */
  can_edit_bio: boolean = false;
  /** can_edit_profile_photo: Bool — True, if the bot can edit profile photo of the business account */
  can_edit_profile_photo: boolean = false;
  /** can_edit_username: Bool — True, if the bot can edit username of the business account */
  can_edit_username: boolean = false;
  /** can_view_gifts_and_stars: Bool — True, if the bot can view gifts and Telegram Star amount owned by the business account */
  can_view_gifts_and_stars: boolean = false;
  /** can_sell_gifts: Bool — True, if the bot can sell regular gifts received by the business account */
  can_sell_gifts: boolean = false;
  /** can_change_gift_settings: Bool — True, if the bot can change gift receiving settings of the business account */
  can_change_gift_settings: boolean = false;
  /** can_transfer_and_upgrade_gifts: Bool — True, if the bot can transfer and upgrade gifts received by the business account */
  can_transfer_and_upgrade_gifts: boolean = false;
  /** can_transfer_stars: Bool — True, if the bot can transfer Telegram Stars received by the business account to account of the bot, or use them to upgrade and transfer gifts */
  can_transfer_stars: boolean = false;
  /** can_manage_stories: Bool — True, if the bot can post, edit and delete stories */
  can_manage_stories: boolean = false;
}

/** Decode a TDLib `businessBotRights` object; null input -> null. */
export function decodeBusinessBotRights(json: TdJsonRaw): BusinessBotRights | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessBotRights = new BusinessBotRights();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_reply = asBool(record['can_reply']);
  out.can_read_messages = asBool(record['can_read_messages']);
  out.can_delete_sent_messages = asBool(record['can_delete_sent_messages']);
  out.can_delete_all_messages = asBool(record['can_delete_all_messages']);
  out.can_edit_name = asBool(record['can_edit_name']);
  out.can_edit_bio = asBool(record['can_edit_bio']);
  out.can_edit_profile_photo = asBool(record['can_edit_profile_photo']);
  out.can_edit_username = asBool(record['can_edit_username']);
  out.can_view_gifts_and_stars = asBool(record['can_view_gifts_and_stars']);
  out.can_sell_gifts = asBool(record['can_sell_gifts']);
  out.can_change_gift_settings = asBool(record['can_change_gift_settings']);
  out.can_transfer_and_upgrade_gifts = asBool(record['can_transfer_and_upgrade_gifts']);
  out.can_transfer_stars = asBool(record['can_transfer_stars']);
  out.can_manage_stories = asBool(record['can_manage_stories']);
  return out;
}

/** Encode a `businessBotRights` into TDLib JSON wire shape. */
export function encodeBusinessBotRights(value: BusinessBotRights): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessBotRights';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_reply'] = value.can_reply;
  out['can_read_messages'] = value.can_read_messages;
  out['can_delete_sent_messages'] = value.can_delete_sent_messages;
  out['can_delete_all_messages'] = value.can_delete_all_messages;
  out['can_edit_name'] = value.can_edit_name;
  out['can_edit_bio'] = value.can_edit_bio;
  out['can_edit_profile_photo'] = value.can_edit_profile_photo;
  out['can_edit_username'] = value.can_edit_username;
  out['can_view_gifts_and_stars'] = value.can_view_gifts_and_stars;
  out['can_sell_gifts'] = value.can_sell_gifts;
  out['can_change_gift_settings'] = value.can_change_gift_settings;
  out['can_transfer_and_upgrade_gifts'] = value.can_transfer_and_upgrade_gifts;
  out['can_transfer_stars'] = value.can_transfer_stars;
  out['can_manage_stories'] = value.can_manage_stories;
  return out;
}

/** businessChatLink = BusinessChatLink — Contains information about a business chat link */
export class BusinessChatLink {
  type: 'businessChatLink' = 'businessChatLink';
  extra?: string;
  /** link: string — The HTTPS link */
  link: string = '';
  /** text: formattedText — Message draft text that will be added to the input field */
  text: FormattedText | null = null;
  /** title: string — Link title */
  title: string = '';
  /** view_count: int32 — Number of times the link was used */
  view_count: number = 0;
}

/** Decode a TDLib `businessChatLink` object; null input -> null. */
export function decodeBusinessChatLink(json: TdJsonRaw): BusinessChatLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessChatLink = new BusinessChatLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  out.text = decodeFormattedText(record['text']);
  out.title = asString(record['title']);
  out.view_count = asNumber(record['view_count']);
  return out;
}

/** Encode a `businessChatLink` into TDLib JSON wire shape. */
export function encodeBusinessChatLink(value: BusinessChatLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessChatLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['title'] = value.title;
  out['view_count'] = value.view_count;
  return out;
}

/** businessChatLinkInfo = BusinessChatLinkInfo — Contains information about a business chat link */
export class BusinessChatLinkInfo {
  type: 'businessChatLinkInfo' = 'businessChatLinkInfo';
  extra?: string;
  /** chat_id: int53 — Identifier of the private chat that created the link */
  chat_id: number = 0;
  /** text: formattedText — Message draft text that must be added to the input field */
  text: FormattedText | null = null;
}

/** Decode a TDLib `businessChatLinkInfo` object; null input -> null. */
export function decodeBusinessChatLinkInfo(json: TdJsonRaw): BusinessChatLinkInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessChatLinkInfo = new BusinessChatLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `businessChatLinkInfo` into TDLib JSON wire shape. */
export function encodeBusinessChatLinkInfo(value: BusinessChatLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessChatLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** businessChatLinks = BusinessChatLinks — Contains a list of business chat links created by the user */
export class BusinessChatLinks {
  type: 'businessChatLinks' = 'businessChatLinks';
  extra?: string;
  /** links: vector<businessChatLink> — List of links */
  links: BusinessChatLink[] = [];
}

/** Decode a TDLib `businessChatLinks` object; null input -> null. */
export function decodeBusinessChatLinks(json: TdJsonRaw): BusinessChatLinks | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessChatLinks = new BusinessChatLinks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.links = decodeVector(record['links'], (item: TdJsonRaw): BusinessChatLink => decodeBusinessChatLink(item) as BusinessChatLink);
  return out;
}

/** Encode a `businessChatLinks` into TDLib JSON wire shape. */
export function encodeBusinessChatLinks(value: BusinessChatLinks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessChatLinks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['links'] = encodeVector(value.links, (item: BusinessChatLink | null): TdJsonRaw => item === null ? null : encodeBusinessChatLink(item));
  return out;
}

/** businessConnectedBot = BusinessConnectedBot — Describes a business bot connected to an account */
export class BusinessConnectedBot {
  type: 'businessConnectedBot' = 'businessConnectedBot';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the bot */
  bot_user_id: number = 0;
  /** recipients: businessRecipients — Private chats that will be accessible to the bot */
  recipients: BusinessRecipients | null = null;
  /** rights: businessBotRights — Rights of the bot */
  rights: BusinessBotRights | null = null;
}

/** Decode a TDLib `businessConnectedBot` object; null input -> null. */
export function decodeBusinessConnectedBot(json: TdJsonRaw): BusinessConnectedBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessConnectedBot = new BusinessConnectedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.recipients = decodeBusinessRecipients(record['recipients']);
  out.rights = decodeBusinessBotRights(record['rights']);
  return out;
}

/** Encode a `businessConnectedBot` into TDLib JSON wire shape. */
export function encodeBusinessConnectedBot(value: BusinessConnectedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessConnectedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['recipients'] = value.recipients === null ? null : encodeBusinessRecipients(value.recipients);
  out['rights'] = value.rights === null ? null : encodeBusinessBotRights(value.rights);
  return out;
}

/** businessConnectedBotInfo = BusinessConnectedBotInfo — Describes a connection of a bot to an account */
export class BusinessConnectedBotInfo {
  type: 'businessConnectedBotInfo' = 'businessConnectedBotInfo';
  extra?: string;
  /** bot: businessConnectedBot — Information about the bot */
  bot: BusinessConnectedBot | null = null;
  /** connection_date: int32 — Point in time (Unix timestamp) when the bot was added; may be 0 if unknown */
  connection_date: number = 0;
  /** device_model: string — Model of the device that was used for the bot connection, as provided by the application; may be empty if unknown */
  device_model: string = '';
  /** location: string — A human-readable description of the location from which the bot was connected, based on the IP address; may be empty if unknown */
  location: string = '';
}

/** Decode a TDLib `businessConnectedBotInfo` object; null input -> null. */
export function decodeBusinessConnectedBotInfo(json: TdJsonRaw): BusinessConnectedBotInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessConnectedBotInfo = new BusinessConnectedBotInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot = decodeBusinessConnectedBot(record['bot']);
  out.connection_date = asNumber(record['connection_date']);
  out.device_model = asString(record['device_model']);
  out.location = asString(record['location']);
  return out;
}

/** Encode a `businessConnectedBotInfo` into TDLib JSON wire shape. */
export function encodeBusinessConnectedBotInfo(value: BusinessConnectedBotInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessConnectedBotInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot'] = value.bot === null ? null : encodeBusinessConnectedBot(value.bot);
  out['connection_date'] = value.connection_date;
  out['device_model'] = value.device_model;
  out['location'] = value.location;
  return out;
}

/** businessConnection = BusinessConnection — Describes a connection of the bot with a business account */
export class BusinessConnection {
  type: 'businessConnection' = 'businessConnection';
  extra?: string;
  /** id: string — Unique identifier of the connection */
  id: string = '';
  /** user_id: int53 — Identifier of the business user who created the connection */
  user_id: number = 0;
  /** user_chat_id: int53 — Chat identifier of the private chat with the user */
  user_chat_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) when the connection was established */
  date: number = 0;
  /** rights: businessBotRights — Rights of the bot; may be null if the connection was disabled */
  rights: BusinessBotRights | null = null;
  /** is_enabled: Bool — True, if the connection is enabled; false otherwise */
  is_enabled: boolean = false;
}

/** Decode a TDLib `businessConnection` object; null input -> null. */
export function decodeBusinessConnection(json: TdJsonRaw): BusinessConnection | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessConnection = new BusinessConnection();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.user_id = asNumber(record['user_id']);
  out.user_chat_id = asNumber(record['user_chat_id']);
  out.date = asNumber(record['date']);
  out.rights = decodeBusinessBotRights(record['rights']);
  out.is_enabled = asBool(record['is_enabled']);
  return out;
}

/** Encode a `businessConnection` into TDLib JSON wire shape. */
export function encodeBusinessConnection(value: BusinessConnection): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessConnection';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['user_id'] = value.user_id;
  out['user_chat_id'] = value.user_chat_id;
  out['date'] = value.date;
  out['rights'] = value.rights === null ? null : encodeBusinessBotRights(value.rights);
  out['is_enabled'] = value.is_enabled;
  return out;
}

/** businessFeatureAccountLinks = BusinessFeature — The ability to create links to the business account with predefined message text */
export class BusinessFeatureAccountLinks {
  type: 'businessFeatureAccountLinks' = 'businessFeatureAccountLinks';
  extra?: string;
}

/** Decode a TDLib `businessFeatureAccountLinks` object; null input -> null. */
export function decodeBusinessFeatureAccountLinks(json: TdJsonRaw): BusinessFeatureAccountLinks | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureAccountLinks = new BusinessFeatureAccountLinks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureAccountLinks` into TDLib JSON wire shape. */
export function encodeBusinessFeatureAccountLinks(value: BusinessFeatureAccountLinks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureAccountLinks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatureAwayMessage = BusinessFeature — The ability to set up an away message */
export class BusinessFeatureAwayMessage {
  type: 'businessFeatureAwayMessage' = 'businessFeatureAwayMessage';
  extra?: string;
}

/** Decode a TDLib `businessFeatureAwayMessage` object; null input -> null. */
export function decodeBusinessFeatureAwayMessage(json: TdJsonRaw): BusinessFeatureAwayMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureAwayMessage = new BusinessFeatureAwayMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureAwayMessage` into TDLib JSON wire shape. */
export function encodeBusinessFeatureAwayMessage(value: BusinessFeatureAwayMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureAwayMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatureBots = BusinessFeature — The ability to connect a bot to the account */
export class BusinessFeatureBots {
  type: 'businessFeatureBots' = 'businessFeatureBots';
  extra?: string;
}

/** Decode a TDLib `businessFeatureBots` object; null input -> null. */
export function decodeBusinessFeatureBots(json: TdJsonRaw): BusinessFeatureBots | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureBots = new BusinessFeatureBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureBots` into TDLib JSON wire shape. */
export function encodeBusinessFeatureBots(value: BusinessFeatureBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatureChatFolderTags = BusinessFeature — The ability to display folder names for each chat in the chat list */
export class BusinessFeatureChatFolderTags {
  type: 'businessFeatureChatFolderTags' = 'businessFeatureChatFolderTags';
  extra?: string;
}

/** Decode a TDLib `businessFeatureChatFolderTags` object; null input -> null. */
export function decodeBusinessFeatureChatFolderTags(json: TdJsonRaw): BusinessFeatureChatFolderTags | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureChatFolderTags = new BusinessFeatureChatFolderTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureChatFolderTags` into TDLib JSON wire shape. */
export function encodeBusinessFeatureChatFolderTags(value: BusinessFeatureChatFolderTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureChatFolderTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatureEmojiStatus = BusinessFeature — The ability to show an emoji status along with the business name */
export class BusinessFeatureEmojiStatus {
  type: 'businessFeatureEmojiStatus' = 'businessFeatureEmojiStatus';
  extra?: string;
}

/** Decode a TDLib `businessFeatureEmojiStatus` object; null input -> null. */
export function decodeBusinessFeatureEmojiStatus(json: TdJsonRaw): BusinessFeatureEmojiStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureEmojiStatus = new BusinessFeatureEmojiStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureEmojiStatus` into TDLib JSON wire shape. */
export function encodeBusinessFeatureEmojiStatus(value: BusinessFeatureEmojiStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureEmojiStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatureGreetingMessage = BusinessFeature — The ability to set up a greeting message */
export class BusinessFeatureGreetingMessage {
  type: 'businessFeatureGreetingMessage' = 'businessFeatureGreetingMessage';
  extra?: string;
}

/** Decode a TDLib `businessFeatureGreetingMessage` object; null input -> null. */
export function decodeBusinessFeatureGreetingMessage(json: TdJsonRaw): BusinessFeatureGreetingMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureGreetingMessage = new BusinessFeatureGreetingMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureGreetingMessage` into TDLib JSON wire shape. */
export function encodeBusinessFeatureGreetingMessage(value: BusinessFeatureGreetingMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureGreetingMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatureLocation = BusinessFeature — The ability to set location */
export class BusinessFeatureLocation {
  type: 'businessFeatureLocation' = 'businessFeatureLocation';
  extra?: string;
}

/** Decode a TDLib `businessFeatureLocation` object; null input -> null. */
export function decodeBusinessFeatureLocation(json: TdJsonRaw): BusinessFeatureLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureLocation = new BusinessFeatureLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureLocation` into TDLib JSON wire shape. */
export function encodeBusinessFeatureLocation(value: BusinessFeatureLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatureOpeningHours = BusinessFeature — The ability to set opening hours */
export class BusinessFeatureOpeningHours {
  type: 'businessFeatureOpeningHours' = 'businessFeatureOpeningHours';
  extra?: string;
}

/** Decode a TDLib `businessFeatureOpeningHours` object; null input -> null. */
export function decodeBusinessFeatureOpeningHours(json: TdJsonRaw): BusinessFeatureOpeningHours | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureOpeningHours = new BusinessFeatureOpeningHours();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureOpeningHours` into TDLib JSON wire shape. */
export function encodeBusinessFeatureOpeningHours(value: BusinessFeatureOpeningHours): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureOpeningHours';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeaturePromotionAnimation = BusinessFeaturePromotionAnimation — Describes a promotion animation for a Business feature */
export class BusinessFeaturePromotionAnimation {
  type: 'businessFeaturePromotionAnimation' = 'businessFeaturePromotionAnimation';
  extra?: string;
  /** feature: BusinessFeature — Business feature */
  feature: TdBusinessFeature | null = null;
  /** animation: animation — Promotion animation for the feature */
  animation: Animation | null = null;
}

/** Decode a TDLib `businessFeaturePromotionAnimation` object; null input -> null. */
export function decodeBusinessFeaturePromotionAnimation(json: TdJsonRaw): BusinessFeaturePromotionAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeaturePromotionAnimation = new BusinessFeaturePromotionAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.feature = decodeTdBusinessFeature(record['feature']);
  out.animation = decodeAnimation(record['animation']);
  return out;
}

/** Encode a `businessFeaturePromotionAnimation` into TDLib JSON wire shape. */
export function encodeBusinessFeaturePromotionAnimation(value: BusinessFeaturePromotionAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeaturePromotionAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['feature'] = value.feature === null ? null : encodeTdBusinessFeature(value.feature);
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  return out;
}

/** businessFeatureQuickReplies = BusinessFeature — The ability to use quick replies */
export class BusinessFeatureQuickReplies {
  type: 'businessFeatureQuickReplies' = 'businessFeatureQuickReplies';
  extra?: string;
}

/** Decode a TDLib `businessFeatureQuickReplies` object; null input -> null. */
export function decodeBusinessFeatureQuickReplies(json: TdJsonRaw): BusinessFeatureQuickReplies | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureQuickReplies = new BusinessFeatureQuickReplies();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureQuickReplies` into TDLib JSON wire shape. */
export function encodeBusinessFeatureQuickReplies(value: BusinessFeatureQuickReplies): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureQuickReplies';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatureStartPage = BusinessFeature — The ability to customize start page */
export class BusinessFeatureStartPage {
  type: 'businessFeatureStartPage' = 'businessFeatureStartPage';
  extra?: string;
}

/** Decode a TDLib `businessFeatureStartPage` object; null input -> null. */
export function decodeBusinessFeatureStartPage(json: TdJsonRaw): BusinessFeatureStartPage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureStartPage = new BusinessFeatureStartPage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureStartPage` into TDLib JSON wire shape. */
export function encodeBusinessFeatureStartPage(value: BusinessFeatureStartPage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureStartPage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatureUpgradedStories = BusinessFeature — Allowed to use many additional features for stories */
export class BusinessFeatureUpgradedStories {
  type: 'businessFeatureUpgradedStories' = 'businessFeatureUpgradedStories';
  extra?: string;
}

/** Decode a TDLib `businessFeatureUpgradedStories` object; null input -> null. */
export function decodeBusinessFeatureUpgradedStories(json: TdJsonRaw): BusinessFeatureUpgradedStories | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatureUpgradedStories = new BusinessFeatureUpgradedStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `businessFeatureUpgradedStories` into TDLib JSON wire shape. */
export function encodeBusinessFeatureUpgradedStories(value: BusinessFeatureUpgradedStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatureUpgradedStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** businessFeatures = BusinessFeatures — Contains information about features, available to Business user accounts */
export class BusinessFeatures {
  type: 'businessFeatures' = 'businessFeatures';
  extra?: string;
  /** features: vector<BusinessFeature> — The list of available business features */
  features: TdBusinessFeature[] = [];
}

/** Decode a TDLib `businessFeatures` object; null input -> null. */
export function decodeBusinessFeatures(json: TdJsonRaw): BusinessFeatures | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessFeatures = new BusinessFeatures();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.features = decodeVector(record['features'], (item: TdJsonRaw): TdBusinessFeature => decodeTdBusinessFeature(item) as TdBusinessFeature);
  return out;
}

/** Encode a `businessFeatures` into TDLib JSON wire shape. */
export function encodeBusinessFeatures(value: BusinessFeatures): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessFeatures';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['features'] = encodeVector(value.features, (item: TdBusinessFeature | null): TdJsonRaw => item === null ? null : encodeTdBusinessFeature(item));
  return out;
}

/** businessGreetingMessageSettings = BusinessGreetingMessageSettings — Describes settings for greeting messages that are automatically sent by a Telegram Business account as response to incoming messages in an inactive private chat */
export class BusinessGreetingMessageSettings {
  type: 'businessGreetingMessageSettings' = 'businessGreetingMessageSettings';
  extra?: string;
  /** shortcut_id: int32 — Unique quick reply shortcut identifier for the greeting messages */
  shortcut_id: number = 0;
  /** recipients: businessRecipients — Chosen recipients of the greeting messages */
  recipients: BusinessRecipients | null = null;
  /** inactivity_days: int32 — The number of days after which a chat will be considered as inactive; currently, must be one of 7, 14, 21, or 28 */
  inactivity_days: number = 0;
}

/** Decode a TDLib `businessGreetingMessageSettings` object; null input -> null. */
export function decodeBusinessGreetingMessageSettings(json: TdJsonRaw): BusinessGreetingMessageSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessGreetingMessageSettings = new BusinessGreetingMessageSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_id = asNumber(record['shortcut_id']);
  out.recipients = decodeBusinessRecipients(record['recipients']);
  out.inactivity_days = asNumber(record['inactivity_days']);
  return out;
}

/** Encode a `businessGreetingMessageSettings` into TDLib JSON wire shape. */
export function encodeBusinessGreetingMessageSettings(value: BusinessGreetingMessageSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessGreetingMessageSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_id'] = value.shortcut_id;
  out['recipients'] = value.recipients === null ? null : encodeBusinessRecipients(value.recipients);
  out['inactivity_days'] = value.inactivity_days;
  return out;
}

/** businessInfo = BusinessInfo — Contains information about a Telegram Business account Local time zone identifier will be empty. An updateUserFullInfo update is not triggered when value of this field changes */
export class BusinessInfo {
  type: 'businessInfo' = 'businessInfo';
  extra?: string;
  /** location: businessLocation — Location of the business; may be null if none */
  location: BusinessLocation | null = null;
  /** opening_hours: businessOpeningHours — Opening hours of the business; may be null if none. The hours are guaranteed to be valid and have already been split by week days */
  opening_hours: BusinessOpeningHours | null = null;
  /** local_opening_hours: businessOpeningHours — Opening hours of the business in the local time; may be null if none. The hours are guaranteed to be valid and have already been split by week days. */
  local_opening_hours: BusinessOpeningHours | null = null;
  /** next_open_in: int32 — Time left before the business will open the next time, in seconds; 0 if unknown. An updateUserFullInfo update is not triggered when value of this field changes */
  next_open_in: number = 0;
  /** next_close_in: int32 — Time left before the business will close the next time, in seconds; 0 if unknown. An updateUserFullInfo update is not triggered when value of this field changes */
  next_close_in: number = 0;
  /** greeting_message_settings: businessGreetingMessageSettings — The greeting message; may be null if none or the Business account is not of the current user */
  greeting_message_settings: BusinessGreetingMessageSettings | null = null;
  /** away_message_settings: businessAwayMessageSettings — The away message; may be null if none or the Business account is not of the current user */
  away_message_settings: BusinessAwayMessageSettings | null = null;
  /** start_page: businessStartPage — Information about start page of the account; may be null if none */
  start_page: BusinessStartPage | null = null;
}

/** Decode a TDLib `businessInfo` object; null input -> null. */
export function decodeBusinessInfo(json: TdJsonRaw): BusinessInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessInfo = new BusinessInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeBusinessLocation(record['location']);
  out.opening_hours = decodeBusinessOpeningHours(record['opening_hours']);
  out.local_opening_hours = decodeBusinessOpeningHours(record['local_opening_hours']);
  out.next_open_in = asNumber(record['next_open_in']);
  out.next_close_in = asNumber(record['next_close_in']);
  out.greeting_message_settings = decodeBusinessGreetingMessageSettings(record['greeting_message_settings']);
  out.away_message_settings = decodeBusinessAwayMessageSettings(record['away_message_settings']);
  out.start_page = decodeBusinessStartPage(record['start_page']);
  return out;
}

/** Encode a `businessInfo` into TDLib JSON wire shape. */
export function encodeBusinessInfo(value: BusinessInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeBusinessLocation(value.location);
  out['opening_hours'] = value.opening_hours === null ? null : encodeBusinessOpeningHours(value.opening_hours);
  out['local_opening_hours'] = value.local_opening_hours === null ? null : encodeBusinessOpeningHours(value.local_opening_hours);
  out['next_open_in'] = value.next_open_in;
  out['next_close_in'] = value.next_close_in;
  out['greeting_message_settings'] = value.greeting_message_settings === null ? null : encodeBusinessGreetingMessageSettings(value.greeting_message_settings);
  out['away_message_settings'] = value.away_message_settings === null ? null : encodeBusinessAwayMessageSettings(value.away_message_settings);
  out['start_page'] = value.start_page === null ? null : encodeBusinessStartPage(value.start_page);
  return out;
}

/** businessLocation = BusinessLocation — Represents a location of a business */
export class BusinessLocation {
  type: 'businessLocation' = 'businessLocation';
  extra?: string;
  /** location: location — The location; may be null if not specified */
  location: Location | null = null;
  /** address: string — Location address; 1-96 characters */
  address: string = '';
}

/** Decode a TDLib `businessLocation` object; null input -> null. */
export function decodeBusinessLocation(json: TdJsonRaw): BusinessLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessLocation = new BusinessLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  out.address = asString(record['address']);
  return out;
}

/** Encode a `businessLocation` into TDLib JSON wire shape. */
export function encodeBusinessLocation(value: BusinessLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['address'] = value.address;
  return out;
}

/** businessMessage = BusinessMessage — Describes a message from a business account as received by a bot */
export class BusinessMessage {
  type: 'businessMessage' = 'businessMessage';
  extra?: string;
  /** message: message — The message */
  message: Message | null = null;
  /** reply_to_message: message — Message that is replied by the message in the same chat; may be null if none */
  reply_to_message: Message | null = null;
}

/** Decode a TDLib `businessMessage` object; null input -> null. */
export function decodeBusinessMessage(json: TdJsonRaw): BusinessMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessMessage = new BusinessMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  out.reply_to_message = decodeMessage(record['reply_to_message']);
  return out;
}

/** Encode a `businessMessage` into TDLib JSON wire shape. */
export function encodeBusinessMessage(value: BusinessMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  out['reply_to_message'] = value.reply_to_message === null ? null : encodeMessage(value.reply_to_message);
  return out;
}

/** businessMessages = BusinessMessages — Contains a list of messages from a business account as received by a bot */
export class BusinessMessages {
  type: 'businessMessages' = 'businessMessages';
  extra?: string;
  /** messages: vector<businessMessage> — List of business messages */
  messages: BusinessMessage[] = [];
}

/** Decode a TDLib `businessMessages` object; null input -> null. */
export function decodeBusinessMessages(json: TdJsonRaw): BusinessMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessMessages = new BusinessMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): BusinessMessage => decodeBusinessMessage(item) as BusinessMessage);
  return out;
}

/** Encode a `businessMessages` into TDLib JSON wire shape. */
export function encodeBusinessMessages(value: BusinessMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['messages'] = encodeVector(value.messages, (item: BusinessMessage | null): TdJsonRaw => item === null ? null : encodeBusinessMessage(item));
  return out;
}

/** businessOpeningHours = BusinessOpeningHours — Describes opening hours of a business */
export class BusinessOpeningHours {
  type: 'businessOpeningHours' = 'businessOpeningHours';
  extra?: string;
  /** time_zone_id: string — Unique time zone identifier */
  time_zone_id: string = '';
  /** opening_hours: vector<businessOpeningHoursInterval> — Intervals of the time when the business is open */
  opening_hours: BusinessOpeningHoursInterval[] = [];
}

/** Decode a TDLib `businessOpeningHours` object; null input -> null. */
export function decodeBusinessOpeningHours(json: TdJsonRaw): BusinessOpeningHours | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessOpeningHours = new BusinessOpeningHours();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.time_zone_id = asString(record['time_zone_id']);
  out.opening_hours = decodeVector(record['opening_hours'], (item: TdJsonRaw): BusinessOpeningHoursInterval => decodeBusinessOpeningHoursInterval(item) as BusinessOpeningHoursInterval);
  return out;
}

/** Encode a `businessOpeningHours` into TDLib JSON wire shape. */
export function encodeBusinessOpeningHours(value: BusinessOpeningHours): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessOpeningHours';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['time_zone_id'] = value.time_zone_id;
  out['opening_hours'] = encodeVector(value.opening_hours, (item: BusinessOpeningHoursInterval | null): TdJsonRaw => item === null ? null : encodeBusinessOpeningHoursInterval(item));
  return out;
}

/** businessOpeningHoursInterval = BusinessOpeningHoursInterval — Describes an interval of time when the business is open */
export class BusinessOpeningHoursInterval {
  type: 'businessOpeningHoursInterval' = 'businessOpeningHoursInterval';
  extra?: string;
  /** start_minute: int32 — The minute's sequence number in a week, starting on Monday, marking the start of the time interval during which the business is open; 0-7*24*60 */
  start_minute: number = 0;
  /** end_minute: int32 — The minute's sequence number in a week, starting on Monday, marking the end of the time interval during which the business is open; 1-8*24*60 */
  end_minute: number = 0;
}

/** Decode a TDLib `businessOpeningHoursInterval` object; null input -> null. */
export function decodeBusinessOpeningHoursInterval(json: TdJsonRaw): BusinessOpeningHoursInterval | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessOpeningHoursInterval = new BusinessOpeningHoursInterval();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.start_minute = asNumber(record['start_minute']);
  out.end_minute = asNumber(record['end_minute']);
  return out;
}

/** Encode a `businessOpeningHoursInterval` into TDLib JSON wire shape. */
export function encodeBusinessOpeningHoursInterval(value: BusinessOpeningHoursInterval): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessOpeningHoursInterval';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['start_minute'] = value.start_minute;
  out['end_minute'] = value.end_minute;
  return out;
}

/** businessRecipients = BusinessRecipients — Describes private chats chosen for automatic interaction with a business */
export class BusinessRecipients {
  type: 'businessRecipients' = 'businessRecipients';
  extra?: string;
  /** chat_ids: vector<int53> — Identifiers of selected private chats */
  chat_ids: number[] = [];
  /** excluded_chat_ids: vector<int53> — Identifiers of private chats that are always excluded; for businessConnectedBot only */
  excluded_chat_ids: number[] = [];
  /** select_existing_chats: Bool — True, if all existing private chats are selected */
  select_existing_chats: boolean = false;
  /** select_new_chats: Bool — True, if all new private chats are selected */
  select_new_chats: boolean = false;
  /** select_contacts: Bool — True, if all private chats with contacts are selected */
  select_contacts: boolean = false;
  /** select_non_contacts: Bool — True, if all private chats with non-contacts are selected */
  select_non_contacts: boolean = false;
  /** exclude_selected: Bool — If true, then all private chats except the selected are chosen. Otherwise, only the selected chats are chosen */
  exclude_selected: boolean = false;
}

/** Decode a TDLib `businessRecipients` object; null input -> null. */
export function decodeBusinessRecipients(json: TdJsonRaw): BusinessRecipients | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessRecipients = new BusinessRecipients();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.excluded_chat_ids = decodeVector(record['excluded_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.select_existing_chats = asBool(record['select_existing_chats']);
  out.select_new_chats = asBool(record['select_new_chats']);
  out.select_contacts = asBool(record['select_contacts']);
  out.select_non_contacts = asBool(record['select_non_contacts']);
  out.exclude_selected = asBool(record['exclude_selected']);
  return out;
}

/** Encode a `businessRecipients` into TDLib JSON wire shape. */
export function encodeBusinessRecipients(value: BusinessRecipients): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessRecipients';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  out['excluded_chat_ids'] = encodeVector(value.excluded_chat_ids, (item: number): TdJsonRaw => item);
  out['select_existing_chats'] = value.select_existing_chats;
  out['select_new_chats'] = value.select_new_chats;
  out['select_contacts'] = value.select_contacts;
  out['select_non_contacts'] = value.select_non_contacts;
  out['exclude_selected'] = value.exclude_selected;
  return out;
}

/** businessStartPage = BusinessStartPage — Describes settings for a business account start page */
export class BusinessStartPage {
  type: 'businessStartPage' = 'businessStartPage';
  extra?: string;
  /** title: string — Title text of the start page */
  title: string = '';
  /** message: string — Message text of the start page */
  message: string = '';
  /** sticker: sticker — Greeting sticker of the start page; may be null if none */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `businessStartPage` object; null input -> null. */
export function decodeBusinessStartPage(json: TdJsonRaw): BusinessStartPage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BusinessStartPage = new BusinessStartPage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.message = asString(record['message']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `businessStartPage` into TDLib JSON wire shape. */
export function encodeBusinessStartPage(value: BusinessStartPage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'businessStartPage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['message'] = value.message;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** buttonStyleDanger = ButtonStyle — The button has red color */
export class ButtonStyleDanger {
  type: 'buttonStyleDanger' = 'buttonStyleDanger';
  extra?: string;
}

/** Decode a TDLib `buttonStyleDanger` object; null input -> null. */
export function decodeButtonStyleDanger(json: TdJsonRaw): ButtonStyleDanger | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ButtonStyleDanger = new ButtonStyleDanger();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `buttonStyleDanger` into TDLib JSON wire shape. */
export function encodeButtonStyleDanger(value: ButtonStyleDanger): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'buttonStyleDanger';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** buttonStyleDefault = ButtonStyle — The button has default style */
export class ButtonStyleDefault {
  type: 'buttonStyleDefault' = 'buttonStyleDefault';
  extra?: string;
}

/** Decode a TDLib `buttonStyleDefault` object; null input -> null. */
export function decodeButtonStyleDefault(json: TdJsonRaw): ButtonStyleDefault | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ButtonStyleDefault = new ButtonStyleDefault();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `buttonStyleDefault` into TDLib JSON wire shape. */
export function encodeButtonStyleDefault(value: ButtonStyleDefault): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'buttonStyleDefault';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** buttonStyleLink = ButtonStyle — The button must be shown as a link. The style is allowed only for callback buttons in inlineButton */
export class ButtonStyleLink {
  type: 'buttonStyleLink' = 'buttonStyleLink';
  extra?: string;
}

/** Decode a TDLib `buttonStyleLink` object; null input -> null. */
export function decodeButtonStyleLink(json: TdJsonRaw): ButtonStyleLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ButtonStyleLink = new ButtonStyleLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `buttonStyleLink` into TDLib JSON wire shape. */
export function encodeButtonStyleLink(value: ButtonStyleLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'buttonStyleLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** buttonStylePrimary = ButtonStyle — The button has dark blue color */
export class ButtonStylePrimary {
  type: 'buttonStylePrimary' = 'buttonStylePrimary';
  extra?: string;
}

/** Decode a TDLib `buttonStylePrimary` object; null input -> null. */
export function decodeButtonStylePrimary(json: TdJsonRaw): ButtonStylePrimary | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ButtonStylePrimary = new ButtonStylePrimary();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `buttonStylePrimary` into TDLib JSON wire shape. */
export function encodeButtonStylePrimary(value: ButtonStylePrimary): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'buttonStylePrimary';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** buttonStyleSuccess = ButtonStyle — The button has green color */
export class ButtonStyleSuccess {
  type: 'buttonStyleSuccess' = 'buttonStyleSuccess';
  extra?: string;
}

/** Decode a TDLib `buttonStyleSuccess` object; null input -> null. */
export function decodeButtonStyleSuccess(json: TdJsonRaw): ButtonStyleSuccess | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ButtonStyleSuccess = new ButtonStyleSuccess();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `buttonStyleSuccess` into TDLib JSON wire shape. */
export function encodeButtonStyleSuccess(value: ButtonStyleSuccess): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'buttonStyleSuccess';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** buyGiftUpgrade = Ok — Pays for upgrade of a regular gift that is owned by another user or channel chat */
export class BuyGiftUpgrade {
  type: 'buyGiftUpgrade' = 'buyGiftUpgrade';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that owns the gift */
  owner_id: TdMessageSender | null = null;
  /** prepaid_upgrade_hash: string — Prepaid upgrade hash as received along with the gift */
  prepaid_upgrade_hash: string = '';
  /** star_count: int53 — The Telegram Star amount the user agreed to pay for the upgrade; must be equal to gift.upgrade_star_count */
  star_count: number = 0;
}

/** Decode a TDLib `buyGiftUpgrade` object; null input -> null. */
export function decodeBuyGiftUpgrade(json: TdJsonRaw): BuyGiftUpgrade | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: BuyGiftUpgrade = new BuyGiftUpgrade();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.prepaid_upgrade_hash = asString(record['prepaid_upgrade_hash']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `buyGiftUpgrade` into TDLib JSON wire shape. */
export function encodeBuyGiftUpgrade(value: BuyGiftUpgrade): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'buyGiftUpgrade';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['prepaid_upgrade_hash'] = value.prepaid_upgrade_hash;
  out['star_count'] = value.star_count;
  return out;
}
