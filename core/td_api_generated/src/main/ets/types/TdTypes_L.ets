/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { Animation, Audio, decodeAnimation, decodeAudio, encodeAnimation, encodeAudio } from './TdTypes_A';
import { ChatPhoto, decodeChatPhoto, encodeChatPhoto } from './TdTypes_C';
import { Document, decodeDocument, encodeDocument } from './TdTypes_D';
import { FormattedText, decodeFormattedText, encodeFormattedText } from './TdTypes_F';
import { Gift, GiveawayParameters, decodeGift, decodeGiveawayParameters, encodeGift, encodeGiveawayParameters } from './TdTypes_G';
import { PaidReactor, Photo, decodePaidReactor, decodePhoto, encodePaidReactor, encodePhoto } from './TdTypes_P';
import { Sticker, decodeSticker, encodeSticker } from './TdTypes_S';
import { ThemeSettings, decodeThemeSettings, encodeThemeSettings } from './TdTypes_T';
import { UpgradedGift, decodeUpgradedGift, encodeUpgradedGift } from './TdTypes_U';
import { Video, VideoNote, VoiceNote, decodeVideo, decodeVideoNote, decodeVoiceNote, encodeVideo, encodeVideoNote, encodeVoiceNote } from './TdTypes_V';
import { TdBackgroundType, TdChatList, TdInviteLinkChatType, TdLanguagePackStringValue, TdLinkPreviewAlbumMedia, TdLinkPreviewType, TdStoryList, decodeTdBackgroundType, decodeTdChatList, decodeTdInviteLinkChatType, decodeTdLanguagePackStringValue, decodeTdLinkPreviewAlbumMedia, decodeTdLinkPreviewType, decodeTdStoryList, encodeTdBackgroundType, encodeTdChatList, encodeTdInviteLinkChatType, encodeTdLanguagePackStringValue, encodeTdLinkPreviewAlbumMedia, encodeTdLinkPreviewType, encodeTdStoryList } from './TdUnions';

/** labeledPricePart = LabeledPricePart — Portion of the price of a product (e.g., "delivery cost", "tax amount") */
export class LabeledPricePart {
  type: 'labeledPricePart' = 'labeledPricePart';
  extra?: string;
  /** label: string — Label for this portion of the product price */
  label: string = '';
  /** amount: int53 — Currency amount in the smallest units of the currency */
  amount: number = 0;
}

/** Decode a TDLib `labeledPricePart` object; null input -> null. */
export function decodeLabeledPricePart(json: TdJsonRaw): LabeledPricePart | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LabeledPricePart = new LabeledPricePart();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.label = asString(record['label']);
  out.amount = asNumber(record['amount']);
  return out;
}

/** Encode a `labeledPricePart` into TDLib JSON wire shape. */
export function encodeLabeledPricePart(value: LabeledPricePart): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'labeledPricePart';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['label'] = value.label;
  out['amount'] = value.amount;
  return out;
}

/** languagePackInfo = LanguagePackInfo — Contains information about a language pack */
export class LanguagePackInfo {
  type: 'languagePackInfo' = 'languagePackInfo';
  extra?: string;
  /** id: string — Unique language pack identifier */
  id: string = '';
  /** base_language_pack_id: string — Identifier of a base language pack; may be empty. If a string is missed in the language pack, then it must be fetched from base language pack. Unsupported in custom language packs */
  base_language_pack_id: string = '';
  /** name: string — Language name */
  name: string = '';
  /** native_name: string — Name of the language in that language */
  native_name: string = '';
  /** plural_code: string — A language code to be used to apply plural forms. See https://www.unicode.org/cldr/charts/latest/supplemental/language_plural_rules.html for more information */
  plural_code: string = '';
  /** is_official: Bool — True, if the language pack is official */
  is_official: boolean = false;
  /** is_rtl: Bool — True, if the language pack strings are RTL */
  is_rtl: boolean = false;
  /** is_beta: Bool — True, if the language pack is a beta language pack */
  is_beta: boolean = false;
  /** is_installed: Bool — True, if the language pack is installed by the current user */
  is_installed: boolean = false;
  /** total_string_count: int32 — Total number of non-deleted strings from the language pack */
  total_string_count: number = 0;
  /** translated_string_count: int32 — Total number of translated strings from the language pack */
  translated_string_count: number = 0;
  /** local_string_count: int32 — Total number of non-deleted strings from the language pack available locally */
  local_string_count: number = 0;
  /** translation_url: string — Link to language translation interface; empty for custom local language packs */
  translation_url: string = '';
}

/** Decode a TDLib `languagePackInfo` object; null input -> null. */
export function decodeLanguagePackInfo(json: TdJsonRaw): LanguagePackInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LanguagePackInfo = new LanguagePackInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.base_language_pack_id = asString(record['base_language_pack_id']);
  out.name = asString(record['name']);
  out.native_name = asString(record['native_name']);
  out.plural_code = asString(record['plural_code']);
  out.is_official = asBool(record['is_official']);
  out.is_rtl = asBool(record['is_rtl']);
  out.is_beta = asBool(record['is_beta']);
  out.is_installed = asBool(record['is_installed']);
  out.total_string_count = asNumber(record['total_string_count']);
  out.translated_string_count = asNumber(record['translated_string_count']);
  out.local_string_count = asNumber(record['local_string_count']);
  out.translation_url = asString(record['translation_url']);
  return out;
}

/** Encode a `languagePackInfo` into TDLib JSON wire shape. */
export function encodeLanguagePackInfo(value: LanguagePackInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'languagePackInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['base_language_pack_id'] = value.base_language_pack_id;
  out['name'] = value.name;
  out['native_name'] = value.native_name;
  out['plural_code'] = value.plural_code;
  out['is_official'] = value.is_official;
  out['is_rtl'] = value.is_rtl;
  out['is_beta'] = value.is_beta;
  out['is_installed'] = value.is_installed;
  out['total_string_count'] = value.total_string_count;
  out['translated_string_count'] = value.translated_string_count;
  out['local_string_count'] = value.local_string_count;
  out['translation_url'] = value.translation_url;
  return out;
}

/** languagePackString = LanguagePackString — Represents one language pack string */
export class LanguagePackString {
  type: 'languagePackString' = 'languagePackString';
  extra?: string;
  /** key: string — String key */
  key: string = '';
  /** value: LanguagePackStringValue — String value; pass null if the string needs to be taken from the built-in English language pack */
  value: TdLanguagePackStringValue | null = null;
}

/** Decode a TDLib `languagePackString` object; null input -> null. */
export function decodeLanguagePackString(json: TdJsonRaw): LanguagePackString | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LanguagePackString = new LanguagePackString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.key = asString(record['key']);
  out.value = decodeTdLanguagePackStringValue(record['value']);
  return out;
}

/** Encode a `languagePackString` into TDLib JSON wire shape. */
export function encodeLanguagePackString(value: LanguagePackString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'languagePackString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['key'] = value.key;
  out['value'] = value.value === null ? null : encodeTdLanguagePackStringValue(value.value);
  return out;
}

/** languagePackStringValueDeleted = LanguagePackStringValue — A deleted language pack string, the value must be taken from the built-in English language pack */
export class LanguagePackStringValueDeleted {
  type: 'languagePackStringValueDeleted' = 'languagePackStringValueDeleted';
  extra?: string;
}

/** Decode a TDLib `languagePackStringValueDeleted` object; null input -> null. */
export function decodeLanguagePackStringValueDeleted(json: TdJsonRaw): LanguagePackStringValueDeleted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LanguagePackStringValueDeleted = new LanguagePackStringValueDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `languagePackStringValueDeleted` into TDLib JSON wire shape. */
export function encodeLanguagePackStringValueDeleted(value: LanguagePackStringValueDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'languagePackStringValueDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** languagePackStringValueOrdinary = LanguagePackStringValue — An ordinary language pack string */
export class LanguagePackStringValueOrdinary {
  type: 'languagePackStringValueOrdinary' = 'languagePackStringValueOrdinary';
  extra?: string;
  /** value: string — String value */
  value: string = '';
}

/** Decode a TDLib `languagePackStringValueOrdinary` object; null input -> null. */
export function decodeLanguagePackStringValueOrdinary(json: TdJsonRaw): LanguagePackStringValueOrdinary | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LanguagePackStringValueOrdinary = new LanguagePackStringValueOrdinary();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asString(record['value']);
  return out;
}

/** Encode a `languagePackStringValueOrdinary` into TDLib JSON wire shape. */
export function encodeLanguagePackStringValueOrdinary(value: LanguagePackStringValueOrdinary): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'languagePackStringValueOrdinary';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}

/** languagePackStringValuePluralized = LanguagePackStringValue — A language pack string which has different forms based on the number of some object it mentions. See https://www.unicode.org/cldr/charts/latest/supplemental/language_plural_ru... */
export class LanguagePackStringValuePluralized {
  type: 'languagePackStringValuePluralized' = 'languagePackStringValuePluralized';
  extra?: string;
  /** zero_value: string — Value for zero objects */
  zero_value: string = '';
  /** one_value: string — Value for one object */
  one_value: string = '';
  /** two_value: string — Value for two objects */
  two_value: string = '';
  /** few_value: string — Value for few objects */
  few_value: string = '';
  /** many_value: string — Value for many objects */
  many_value: string = '';
  /** other_value: string — Default value */
  other_value: string = '';
}

/** Decode a TDLib `languagePackStringValuePluralized` object; null input -> null. */
export function decodeLanguagePackStringValuePluralized(json: TdJsonRaw): LanguagePackStringValuePluralized | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LanguagePackStringValuePluralized = new LanguagePackStringValuePluralized();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.zero_value = asString(record['zero_value']);
  out.one_value = asString(record['one_value']);
  out.two_value = asString(record['two_value']);
  out.few_value = asString(record['few_value']);
  out.many_value = asString(record['many_value']);
  out.other_value = asString(record['other_value']);
  return out;
}

/** Encode a `languagePackStringValuePluralized` into TDLib JSON wire shape. */
export function encodeLanguagePackStringValuePluralized(value: LanguagePackStringValuePluralized): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'languagePackStringValuePluralized';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['zero_value'] = value.zero_value;
  out['one_value'] = value.one_value;
  out['two_value'] = value.two_value;
  out['few_value'] = value.few_value;
  out['many_value'] = value.many_value;
  out['other_value'] = value.other_value;
  return out;
}

/** languagePackStrings = LanguagePackStrings — Contains a list of language pack strings */
export class LanguagePackStrings {
  type: 'languagePackStrings' = 'languagePackStrings';
  extra?: string;
  /** strings: vector<languagePackString> — A list of language pack strings */
  strings: LanguagePackString[] = [];
}

/** Decode a TDLib `languagePackStrings` object; null input -> null. */
export function decodeLanguagePackStrings(json: TdJsonRaw): LanguagePackStrings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LanguagePackStrings = new LanguagePackStrings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.strings = decodeVector(record['strings'], (item: TdJsonRaw): LanguagePackString => decodeLanguagePackString(item) as LanguagePackString);
  return out;
}

/** Encode a `languagePackStrings` into TDLib JSON wire shape. */
export function encodeLanguagePackStrings(value: LanguagePackStrings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'languagePackStrings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['strings'] = encodeVector(value.strings, (item: LanguagePackString | null): TdJsonRaw => item === null ? null : encodeLanguagePackString(item));
  return out;
}

/** launchPrepaidGiveaway = Ok — Launches a prepaid giveaway */
export class LaunchPrepaidGiveaway {
  type: 'launchPrepaidGiveaway' = 'launchPrepaidGiveaway';
  extra?: string;
  /** giveaway_id: int64 — Unique identifier of the prepaid giveaway */
  giveaway_id: string = '';
  /** parameters: giveawayParameters — Giveaway parameters */
  parameters: GiveawayParameters | null = null;
  /** winner_count: int32 — The number of users to receive giveaway prize */
  winner_count: number = 0;
  /** star_count: int53 — The number of Telegram Stars to be distributed through the giveaway; pass 0 for Telegram Premium giveaways */
  star_count: number = 0;
}

/** Decode a TDLib `launchPrepaidGiveaway` object; null input -> null. */
export function decodeLaunchPrepaidGiveaway(json: TdJsonRaw): LaunchPrepaidGiveaway | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LaunchPrepaidGiveaway = new LaunchPrepaidGiveaway();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.giveaway_id = asInt64(record['giveaway_id']);
  out.parameters = decodeGiveawayParameters(record['parameters']);
  out.winner_count = asNumber(record['winner_count']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `launchPrepaidGiveaway` into TDLib JSON wire shape. */
export function encodeLaunchPrepaidGiveaway(value: LaunchPrepaidGiveaway): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'launchPrepaidGiveaway';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['giveaway_id'] = value.giveaway_id;
  out['parameters'] = value.parameters === null ? null : encodeGiveawayParameters(value.parameters);
  out['winner_count'] = value.winner_count;
  out['star_count'] = value.star_count;
  return out;
}

/** leaveChat = Ok — Removes the current user from chat members. Private and secret chats can't be left using this method */
export class LeaveChat {
  type: 'leaveChat' = 'leaveChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `leaveChat` object; null input -> null. */
export function decodeLeaveChat(json: TdJsonRaw): LeaveChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LeaveChat = new LeaveChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `leaveChat` into TDLib JSON wire shape. */
export function encodeLeaveChat(value: LeaveChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'leaveChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** leaveGroupCall = Ok — Leaves a group call */
export class LeaveGroupCall {
  type: 'leaveGroupCall' = 'leaveGroupCall';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `leaveGroupCall` object; null input -> null. */
export function decodeLeaveGroupCall(json: TdJsonRaw): LeaveGroupCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LeaveGroupCall = new LeaveGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `leaveGroupCall` into TDLib JSON wire shape. */
export function encodeLeaveGroupCall(value: LeaveGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'leaveGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** linkPreview = LinkPreview — Describes a link preview */
export class LinkPreview {
  type: 'linkPreview' = 'linkPreview';
  extra?: string;
  /** url: string — Original URL of the link */
  url: string = '';
  /** display_url: string — URL to display */
  display_url: string = '';
  /** site_name: string — Short name of the website (e.g., Google Docs, App Store) */
  site_name: string = '';
  /** title: string — Title of the content */
  title: string = '';
  /** description: formattedText */
  description: FormattedText | null = null;
  /** author: string — Author of the content */
  author: string = '';
  /** type: LinkPreviewType — Type of the link preview */
  type_: TdLinkPreviewType | null = null;
  /** has_large_media: Bool — True, if size of media in the preview can be changed */
  has_large_media: boolean = false;
  /** show_large_media: Bool — True, if large media preview must be shown; otherwise, the media preview must be shown small and only the first frame must be shown for videos */
  show_large_media: boolean = false;
  /** show_media_above_description: Bool — True, if media must be shown above link preview description; otherwise, the media must be shown below the description */
  show_media_above_description: boolean = false;
  /** skip_confirmation: Bool — True, if there is no need to show an ordinary open URL confirmation, when opening the URL from the preview, because the URL is shown in the message text in clear */
  skip_confirmation: boolean = false;
  /** show_above_text: Bool — True, if the link preview must be shown above message text; otherwise, the link preview must be shown below the message text */
  show_above_text: boolean = false;
  /** instant_view_version: int32 — Version of instant view (currently, can be 1 or 2) for the web page; 0 if none */
  instant_view_version: number = 0;
}

/** Decode a TDLib `linkPreview` object; null input -> null. */
export function decodeLinkPreview(json: TdJsonRaw): LinkPreview | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreview = new LinkPreview();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.display_url = asString(record['display_url']);
  out.site_name = asString(record['site_name']);
  out.title = asString(record['title']);
  out.description = decodeFormattedText(record['description']);
  out.author = asString(record['author']);
  out.type_ = decodeTdLinkPreviewType(record['type']);
  out.has_large_media = asBool(record['has_large_media']);
  out.show_large_media = asBool(record['show_large_media']);
  out.show_media_above_description = asBool(record['show_media_above_description']);
  out.skip_confirmation = asBool(record['skip_confirmation']);
  out.show_above_text = asBool(record['show_above_text']);
  out.instant_view_version = asNumber(record['instant_view_version']);
  return out;
}

/** Encode a `linkPreview` into TDLib JSON wire shape. */
export function encodeLinkPreview(value: LinkPreview): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreview';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['display_url'] = value.display_url;
  out['site_name'] = value.site_name;
  out['title'] = value.title;
  out['description'] = value.description === null ? null : encodeFormattedText(value.description);
  out['author'] = value.author;
  out['type'] = value.type_ === null ? null : encodeTdLinkPreviewType(value.type_);
  out['has_large_media'] = value.has_large_media;
  out['show_large_media'] = value.show_large_media;
  out['show_media_above_description'] = value.show_media_above_description;
  out['skip_confirmation'] = value.skip_confirmation;
  out['show_above_text'] = value.show_above_text;
  out['instant_view_version'] = value.instant_view_version;
  return out;
}

/** linkPreviewAlbumMediaPhoto = LinkPreviewAlbumMedia — The media is a photo */
export class LinkPreviewAlbumMediaPhoto {
  type: 'linkPreviewAlbumMediaPhoto' = 'linkPreviewAlbumMediaPhoto';
  extra?: string;
  /** photo: photo — Photo description */
  photo: Photo | null = null;
}

/** Decode a TDLib `linkPreviewAlbumMediaPhoto` object; null input -> null. */
export function decodeLinkPreviewAlbumMediaPhoto(json: TdJsonRaw): LinkPreviewAlbumMediaPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewAlbumMediaPhoto = new LinkPreviewAlbumMediaPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  return out;
}

/** Encode a `linkPreviewAlbumMediaPhoto` into TDLib JSON wire shape. */
export function encodeLinkPreviewAlbumMediaPhoto(value: LinkPreviewAlbumMediaPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewAlbumMediaPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  return out;
}

/** linkPreviewAlbumMediaVideo = LinkPreviewAlbumMedia — The media is a video */
export class LinkPreviewAlbumMediaVideo {
  type: 'linkPreviewAlbumMediaVideo' = 'linkPreviewAlbumMediaVideo';
  extra?: string;
  /** video: video — Video description */
  video: Video | null = null;
}

/** Decode a TDLib `linkPreviewAlbumMediaVideo` object; null input -> null. */
export function decodeLinkPreviewAlbumMediaVideo(json: TdJsonRaw): LinkPreviewAlbumMediaVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewAlbumMediaVideo = new LinkPreviewAlbumMediaVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeVideo(record['video']);
  return out;
}

/** Encode a `linkPreviewAlbumMediaVideo` into TDLib JSON wire shape. */
export function encodeLinkPreviewAlbumMediaVideo(value: LinkPreviewAlbumMediaVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewAlbumMediaVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  return out;
}

/** linkPreviewOptions = LinkPreviewOptions — Options to be used for generation of a link preview */
export class LinkPreviewOptions {
  type: 'linkPreviewOptions' = 'linkPreviewOptions';
  extra?: string;
  /** is_disabled: Bool — True, if link preview must be disabled */
  is_disabled: boolean = false;
  /** url: string — URL to use for link preview. If empty, then the first URL found in the message text will be used */
  url: string = '';
  /** force_small_media: Bool — True, if shown media preview must be small; ignored in secret chats or if the URL isn't explicitly specified */
  force_small_media: boolean = false;
  /** force_large_media: Bool — True, if shown media preview must be large; ignored in secret chats or if the URL isn't explicitly specified */
  force_large_media: boolean = false;
  /** show_above_text: Bool — True, if link preview must be shown above message text; otherwise, the link preview will be shown below the message text; ignored in secret chats */
  show_above_text: boolean = false;
}

/** Decode a TDLib `linkPreviewOptions` object; null input -> null. */
export function decodeLinkPreviewOptions(json: TdJsonRaw): LinkPreviewOptions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewOptions = new LinkPreviewOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_disabled = asBool(record['is_disabled']);
  out.url = asString(record['url']);
  out.force_small_media = asBool(record['force_small_media']);
  out.force_large_media = asBool(record['force_large_media']);
  out.show_above_text = asBool(record['show_above_text']);
  return out;
}

/** Encode a `linkPreviewOptions` into TDLib JSON wire shape. */
export function encodeLinkPreviewOptions(value: LinkPreviewOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_disabled'] = value.is_disabled;
  out['url'] = value.url;
  out['force_small_media'] = value.force_small_media;
  out['force_large_media'] = value.force_large_media;
  out['show_above_text'] = value.show_above_text;
  return out;
}

/** linkPreviewTypeAlbum = LinkPreviewType — The link is a link to a media album consisting of photos and videos */
export class LinkPreviewTypeAlbum {
  type: 'linkPreviewTypeAlbum' = 'linkPreviewTypeAlbum';
  extra?: string;
  /** media: vector<LinkPreviewAlbumMedia> — The list of album media */
  media: TdLinkPreviewAlbumMedia[] = [];
  /** caption: string — Album caption */
  caption: string = '';
}

/** Decode a TDLib `linkPreviewTypeAlbum` object; null input -> null. */
export function decodeLinkPreviewTypeAlbum(json: TdJsonRaw): LinkPreviewTypeAlbum | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeAlbum = new LinkPreviewTypeAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.media = decodeVector(record['media'], (item: TdJsonRaw): TdLinkPreviewAlbumMedia => decodeTdLinkPreviewAlbumMedia(item) as TdLinkPreviewAlbumMedia);
  out.caption = asString(record['caption']);
  return out;
}

/** Encode a `linkPreviewTypeAlbum` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeAlbum(value: LinkPreviewTypeAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['media'] = encodeVector(value.media, (item: TdLinkPreviewAlbumMedia | null): TdJsonRaw => item === null ? null : encodeTdLinkPreviewAlbumMedia(item));
  out['caption'] = value.caption;
  return out;
}

/** linkPreviewTypeAnimation = LinkPreviewType — The link is a link to an animation */
export class LinkPreviewTypeAnimation {
  type: 'linkPreviewTypeAnimation' = 'linkPreviewTypeAnimation';
  extra?: string;
  /** animation: animation — The animation */
  animation: Animation | null = null;
}

/** Decode a TDLib `linkPreviewTypeAnimation` object; null input -> null. */
export function decodeLinkPreviewTypeAnimation(json: TdJsonRaw): LinkPreviewTypeAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeAnimation = new LinkPreviewTypeAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeAnimation(record['animation']);
  return out;
}

/** Encode a `linkPreviewTypeAnimation` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeAnimation(value: LinkPreviewTypeAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  return out;
}

/** linkPreviewTypeApp = LinkPreviewType — The link is a link to an app at App Store or Google Play */
export class LinkPreviewTypeApp {
  type: 'linkPreviewTypeApp' = 'linkPreviewTypeApp';
  extra?: string;
  /** photo: photo — Photo for the app */
  photo: Photo | null = null;
}

/** Decode a TDLib `linkPreviewTypeApp` object; null input -> null. */
export function decodeLinkPreviewTypeApp(json: TdJsonRaw): LinkPreviewTypeApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeApp = new LinkPreviewTypeApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  return out;
}

/** Encode a `linkPreviewTypeApp` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeApp(value: LinkPreviewTypeApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  return out;
}

/** linkPreviewTypeArticle = LinkPreviewType — The link is a link to a website */
export class LinkPreviewTypeArticle {
  type: 'linkPreviewTypeArticle' = 'linkPreviewTypeArticle';
  extra?: string;
  /** photo: photo — Article's main photo; may be null */
  photo: Photo | null = null;
}

/** Decode a TDLib `linkPreviewTypeArticle` object; null input -> null. */
export function decodeLinkPreviewTypeArticle(json: TdJsonRaw): LinkPreviewTypeArticle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeArticle = new LinkPreviewTypeArticle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  return out;
}

/** Encode a `linkPreviewTypeArticle` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeArticle(value: LinkPreviewTypeArticle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeArticle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  return out;
}

/** linkPreviewTypeAudio = LinkPreviewType — The link is a link to an audio */
export class LinkPreviewTypeAudio {
  type: 'linkPreviewTypeAudio' = 'linkPreviewTypeAudio';
  extra?: string;
  /** audio: audio — The audio description */
  audio: Audio | null = null;
}

/** Decode a TDLib `linkPreviewTypeAudio` object; null input -> null. */
export function decodeLinkPreviewTypeAudio(json: TdJsonRaw): LinkPreviewTypeAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeAudio = new LinkPreviewTypeAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeAudio(record['audio']);
  return out;
}

/** Encode a `linkPreviewTypeAudio` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeAudio(value: LinkPreviewTypeAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeAudio(value.audio);
  return out;
}

/** linkPreviewTypeBackground = LinkPreviewType — The link is a link to a background. Link preview title and description are available only for filled backgrounds */
export class LinkPreviewTypeBackground {
  type: 'linkPreviewTypeBackground' = 'linkPreviewTypeBackground';
  extra?: string;
  /** document: document — Document with the background; may be null for filled backgrounds */
  document: Document | null = null;
  /** background_type: BackgroundType — Type of the background; may be null if unknown */
  background_type: TdBackgroundType | null = null;
}

/** Decode a TDLib `linkPreviewTypeBackground` object; null input -> null. */
export function decodeLinkPreviewTypeBackground(json: TdJsonRaw): LinkPreviewTypeBackground | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeBackground = new LinkPreviewTypeBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeDocument(record['document']);
  out.background_type = decodeTdBackgroundType(record['background_type']);
  return out;
}

/** Encode a `linkPreviewTypeBackground` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeBackground(value: LinkPreviewTypeBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeDocument(value.document);
  out['background_type'] = value.background_type === null ? null : encodeTdBackgroundType(value.background_type);
  return out;
}

/** linkPreviewTypeChannelBoost = LinkPreviewType — The link is a link to boost a channel chat */
export class LinkPreviewTypeChannelBoost {
  type: 'linkPreviewTypeChannelBoost' = 'linkPreviewTypeChannelBoost';
  extra?: string;
  /** photo: chatPhoto — Photo of the chat; may be null */
  photo: ChatPhoto | null = null;
}

/** Decode a TDLib `linkPreviewTypeChannelBoost` object; null input -> null. */
export function decodeLinkPreviewTypeChannelBoost(json: TdJsonRaw): LinkPreviewTypeChannelBoost | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeChannelBoost = new LinkPreviewTypeChannelBoost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  return out;
}

/** Encode a `linkPreviewTypeChannelBoost` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeChannelBoost(value: LinkPreviewTypeChannelBoost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeChannelBoost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  return out;
}

/** linkPreviewTypeChat = LinkPreviewType — The link is a link to a chat */
export class LinkPreviewTypeChat {
  type: 'linkPreviewTypeChat' = 'linkPreviewTypeChat';
  extra?: string;
  /** type: InviteLinkChatType — Type of the chat */
  type_: TdInviteLinkChatType | null = null;
  /** photo: chatPhoto — Photo of the chat; may be null */
  photo: ChatPhoto | null = null;
  /** creates_join_request: Bool — True, if the link only creates join request */
  creates_join_request: boolean = false;
}

/** Decode a TDLib `linkPreviewTypeChat` object; null input -> null. */
export function decodeLinkPreviewTypeChat(json: TdJsonRaw): LinkPreviewTypeChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeChat = new LinkPreviewTypeChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdInviteLinkChatType(record['type']);
  out.photo = decodeChatPhoto(record['photo']);
  out.creates_join_request = asBool(record['creates_join_request']);
  return out;
}

/** Encode a `linkPreviewTypeChat` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeChat(value: LinkPreviewTypeChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdInviteLinkChatType(value.type_);
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  out['creates_join_request'] = value.creates_join_request;
  return out;
}

/** linkPreviewTypeDirectMessagesChat = LinkPreviewType — The link is a link to a direct messages chat of a channel */
export class LinkPreviewTypeDirectMessagesChat {
  type: 'linkPreviewTypeDirectMessagesChat' = 'linkPreviewTypeDirectMessagesChat';
  extra?: string;
  /** photo: chatPhoto — Photo of the channel chat; may be null */
  photo: ChatPhoto | null = null;
}

/** Decode a TDLib `linkPreviewTypeDirectMessagesChat` object; null input -> null. */
export function decodeLinkPreviewTypeDirectMessagesChat(json: TdJsonRaw): LinkPreviewTypeDirectMessagesChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeDirectMessagesChat = new LinkPreviewTypeDirectMessagesChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  return out;
}

/** Encode a `linkPreviewTypeDirectMessagesChat` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeDirectMessagesChat(value: LinkPreviewTypeDirectMessagesChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeDirectMessagesChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  return out;
}

/** linkPreviewTypeDocument = LinkPreviewType — The link is a link to a general file */
export class LinkPreviewTypeDocument {
  type: 'linkPreviewTypeDocument' = 'linkPreviewTypeDocument';
  extra?: string;
  /** document: document — The document description */
  document: Document | null = null;
}

/** Decode a TDLib `linkPreviewTypeDocument` object; null input -> null. */
export function decodeLinkPreviewTypeDocument(json: TdJsonRaw): LinkPreviewTypeDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeDocument = new LinkPreviewTypeDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeDocument(record['document']);
  return out;
}

/** Encode a `linkPreviewTypeDocument` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeDocument(value: LinkPreviewTypeDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeDocument(value.document);
  return out;
}

/** linkPreviewTypeEmbeddedAnimationPlayer = LinkPreviewType — The link is a link to an animation player */
export class LinkPreviewTypeEmbeddedAnimationPlayer {
  type: 'linkPreviewTypeEmbeddedAnimationPlayer' = 'linkPreviewTypeEmbeddedAnimationPlayer';
  extra?: string;
  /** url: string — URL of the external animation player */
  url: string = '';
  /** animation: animation — The cached animation; may be null if unknown */
  animation: Animation | null = null;
  /** thumbnail: photo — Thumbnail of the animation; may be null if unknown */
  thumbnail: Photo | null = null;
  /** duration: int32 — Duration of the animation, in seconds */
  duration: number = 0;
  /** width: int32 — Expected width of the embedded player */
  width: number = 0;
  /** height: int32 — Expected height of the embedded player */
  height: number = 0;
}

/** Decode a TDLib `linkPreviewTypeEmbeddedAnimationPlayer` object; null input -> null. */
export function decodeLinkPreviewTypeEmbeddedAnimationPlayer(json: TdJsonRaw): LinkPreviewTypeEmbeddedAnimationPlayer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeEmbeddedAnimationPlayer = new LinkPreviewTypeEmbeddedAnimationPlayer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.animation = decodeAnimation(record['animation']);
  out.thumbnail = decodePhoto(record['thumbnail']);
  out.duration = asNumber(record['duration']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  return out;
}

/** Encode a `linkPreviewTypeEmbeddedAnimationPlayer` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeEmbeddedAnimationPlayer(value: LinkPreviewTypeEmbeddedAnimationPlayer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeEmbeddedAnimationPlayer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  out['thumbnail'] = value.thumbnail === null ? null : encodePhoto(value.thumbnail);
  out['duration'] = value.duration;
  out['width'] = value.width;
  out['height'] = value.height;
  return out;
}

/** linkPreviewTypeEmbeddedAudioPlayer = LinkPreviewType — The link is a link to an audio player */
export class LinkPreviewTypeEmbeddedAudioPlayer {
  type: 'linkPreviewTypeEmbeddedAudioPlayer' = 'linkPreviewTypeEmbeddedAudioPlayer';
  extra?: string;
  /** url: string — URL of the external audio player */
  url: string = '';
  /** audio: audio — The cached audio; may be null if unknown */
  audio: Audio | null = null;
  /** thumbnail: photo — Thumbnail of the audio; may be null if unknown */
  thumbnail: Photo | null = null;
  /** duration: int32 — Duration of the audio, in seconds */
  duration: number = 0;
  /** width: int32 — Expected width of the embedded player */
  width: number = 0;
  /** height: int32 — Expected height of the embedded player */
  height: number = 0;
}

/** Decode a TDLib `linkPreviewTypeEmbeddedAudioPlayer` object; null input -> null. */
export function decodeLinkPreviewTypeEmbeddedAudioPlayer(json: TdJsonRaw): LinkPreviewTypeEmbeddedAudioPlayer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeEmbeddedAudioPlayer = new LinkPreviewTypeEmbeddedAudioPlayer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.audio = decodeAudio(record['audio']);
  out.thumbnail = decodePhoto(record['thumbnail']);
  out.duration = asNumber(record['duration']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  return out;
}

/** Encode a `linkPreviewTypeEmbeddedAudioPlayer` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeEmbeddedAudioPlayer(value: LinkPreviewTypeEmbeddedAudioPlayer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeEmbeddedAudioPlayer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['audio'] = value.audio === null ? null : encodeAudio(value.audio);
  out['thumbnail'] = value.thumbnail === null ? null : encodePhoto(value.thumbnail);
  out['duration'] = value.duration;
  out['width'] = value.width;
  out['height'] = value.height;
  return out;
}

/** linkPreviewTypeEmbeddedVideoPlayer = LinkPreviewType — The link is a link to a video player */
export class LinkPreviewTypeEmbeddedVideoPlayer {
  type: 'linkPreviewTypeEmbeddedVideoPlayer' = 'linkPreviewTypeEmbeddedVideoPlayer';
  extra?: string;
  /** url: string — URL of the external video player */
  url: string = '';
  /** video: video — The cached video; may be null if unknown */
  video: Video | null = null;
  /** thumbnail: photo — Thumbnail of the video; may be null if unknown */
  thumbnail: Photo | null = null;
  /** duration: int32 — Duration of the video, in seconds */
  duration: number = 0;
  /** width: int32 — Expected width of the embedded player */
  width: number = 0;
  /** height: int32 — Expected height of the embedded player */
  height: number = 0;
}

/** Decode a TDLib `linkPreviewTypeEmbeddedVideoPlayer` object; null input -> null. */
export function decodeLinkPreviewTypeEmbeddedVideoPlayer(json: TdJsonRaw): LinkPreviewTypeEmbeddedVideoPlayer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeEmbeddedVideoPlayer = new LinkPreviewTypeEmbeddedVideoPlayer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.video = decodeVideo(record['video']);
  out.thumbnail = decodePhoto(record['thumbnail']);
  out.duration = asNumber(record['duration']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  return out;
}

/** Encode a `linkPreviewTypeEmbeddedVideoPlayer` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeEmbeddedVideoPlayer(value: LinkPreviewTypeEmbeddedVideoPlayer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeEmbeddedVideoPlayer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  out['thumbnail'] = value.thumbnail === null ? null : encodePhoto(value.thumbnail);
  out['duration'] = value.duration;
  out['width'] = value.width;
  out['height'] = value.height;
  return out;
}

/** linkPreviewTypeExternalAudio = LinkPreviewType — The link is a link to an audio file */
export class LinkPreviewTypeExternalAudio {
  type: 'linkPreviewTypeExternalAudio' = 'linkPreviewTypeExternalAudio';
  extra?: string;
  /** url: string — URL of the audio file */
  url: string = '';
  /** mime_type: string — MIME type of the audio file */
  mime_type: string = '';
  /** duration: int32 — Duration of the audio, in seconds; 0 if unknown */
  duration: number = 0;
}

/** Decode a TDLib `linkPreviewTypeExternalAudio` object; null input -> null. */
export function decodeLinkPreviewTypeExternalAudio(json: TdJsonRaw): LinkPreviewTypeExternalAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeExternalAudio = new LinkPreviewTypeExternalAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.mime_type = asString(record['mime_type']);
  out.duration = asNumber(record['duration']);
  return out;
}

/** Encode a `linkPreviewTypeExternalAudio` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeExternalAudio(value: LinkPreviewTypeExternalAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeExternalAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['mime_type'] = value.mime_type;
  out['duration'] = value.duration;
  return out;
}

/** linkPreviewTypeExternalVideo = LinkPreviewType — The link is a link to a video file */
export class LinkPreviewTypeExternalVideo {
  type: 'linkPreviewTypeExternalVideo' = 'linkPreviewTypeExternalVideo';
  extra?: string;
  /** url: string — URL of the video file */
  url: string = '';
  /** mime_type: string — MIME type of the video file */
  mime_type: string = '';
  /** width: int32 — Expected width of the video preview; 0 if unknown */
  width: number = 0;
  /** height: int32 — Expected height of the video preview; 0 if unknown */
  height: number = 0;
  /** duration: int32 — Duration of the video, in seconds; 0 if unknown */
  duration: number = 0;
}

/** Decode a TDLib `linkPreviewTypeExternalVideo` object; null input -> null. */
export function decodeLinkPreviewTypeExternalVideo(json: TdJsonRaw): LinkPreviewTypeExternalVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeExternalVideo = new LinkPreviewTypeExternalVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.mime_type = asString(record['mime_type']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.duration = asNumber(record['duration']);
  return out;
}

/** Encode a `linkPreviewTypeExternalVideo` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeExternalVideo(value: LinkPreviewTypeExternalVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeExternalVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['mime_type'] = value.mime_type;
  out['width'] = value.width;
  out['height'] = value.height;
  out['duration'] = value.duration;
  return out;
}

/** linkPreviewTypeGiftAuction = LinkPreviewType — The link is a link to a gift auction */
export class LinkPreviewTypeGiftAuction {
  type: 'linkPreviewTypeGiftAuction' = 'linkPreviewTypeGiftAuction';
  extra?: string;
  /** gift: gift — The gift */
  gift: Gift | null = null;
  /** auction_end_date: int32 — Point in time (Unix timestamp) when the auction will be ended */
  auction_end_date: number = 0;
}

/** Decode a TDLib `linkPreviewTypeGiftAuction` object; null input -> null. */
export function decodeLinkPreviewTypeGiftAuction(json: TdJsonRaw): LinkPreviewTypeGiftAuction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeGiftAuction = new LinkPreviewTypeGiftAuction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeGift(record['gift']);
  out.auction_end_date = asNumber(record['auction_end_date']);
  return out;
}

/** Encode a `linkPreviewTypeGiftAuction` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeGiftAuction(value: LinkPreviewTypeGiftAuction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeGiftAuction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  out['auction_end_date'] = value.auction_end_date;
  return out;
}

/** linkPreviewTypeGiftCollection = LinkPreviewType — The link is a link to a gift collection */
export class LinkPreviewTypeGiftCollection {
  type: 'linkPreviewTypeGiftCollection' = 'linkPreviewTypeGiftCollection';
  extra?: string;
  /** icons: vector<sticker> — Icons for some gifts from the collection; may be empty */
  icons: Sticker[] = [];
}

/** Decode a TDLib `linkPreviewTypeGiftCollection` object; null input -> null. */
export function decodeLinkPreviewTypeGiftCollection(json: TdJsonRaw): LinkPreviewTypeGiftCollection | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeGiftCollection = new LinkPreviewTypeGiftCollection();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.icons = decodeVector(record['icons'], (item: TdJsonRaw): Sticker => decodeSticker(item) as Sticker);
  return out;
}

/** Encode a `linkPreviewTypeGiftCollection` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeGiftCollection(value: LinkPreviewTypeGiftCollection): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeGiftCollection';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['icons'] = encodeVector(value.icons, (item: Sticker | null): TdJsonRaw => item === null ? null : encodeSticker(item));
  return out;
}

/** linkPreviewTypeGroupCall = LinkPreviewType — The link is a link to a group call that isn't bound to a chat */
export class LinkPreviewTypeGroupCall {
  type: 'linkPreviewTypeGroupCall' = 'linkPreviewTypeGroupCall';
  extra?: string;
}

/** Decode a TDLib `linkPreviewTypeGroupCall` object; null input -> null. */
export function decodeLinkPreviewTypeGroupCall(json: TdJsonRaw): LinkPreviewTypeGroupCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeGroupCall = new LinkPreviewTypeGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `linkPreviewTypeGroupCall` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeGroupCall(value: LinkPreviewTypeGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** linkPreviewTypeInvoice = LinkPreviewType — The link is a link to an invoice */
export class LinkPreviewTypeInvoice {
  type: 'linkPreviewTypeInvoice' = 'linkPreviewTypeInvoice';
  extra?: string;
}

/** Decode a TDLib `linkPreviewTypeInvoice` object; null input -> null. */
export function decodeLinkPreviewTypeInvoice(json: TdJsonRaw): LinkPreviewTypeInvoice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeInvoice = new LinkPreviewTypeInvoice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `linkPreviewTypeInvoice` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeInvoice(value: LinkPreviewTypeInvoice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeInvoice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** linkPreviewTypeLiveStory = LinkPreviewType — The link is a link to a live story group call */
export class LinkPreviewTypeLiveStory {
  type: 'linkPreviewTypeLiveStory' = 'linkPreviewTypeLiveStory';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
}

/** Decode a TDLib `linkPreviewTypeLiveStory` object; null input -> null. */
export function decodeLinkPreviewTypeLiveStory(json: TdJsonRaw): LinkPreviewTypeLiveStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeLiveStory = new LinkPreviewTypeLiveStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `linkPreviewTypeLiveStory` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeLiveStory(value: LinkPreviewTypeLiveStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeLiveStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** linkPreviewTypeMessage = LinkPreviewType — The link is a link to a text or a poll Telegram message */
export class LinkPreviewTypeMessage {
  type: 'linkPreviewTypeMessage' = 'linkPreviewTypeMessage';
  extra?: string;
}

/** Decode a TDLib `linkPreviewTypeMessage` object; null input -> null. */
export function decodeLinkPreviewTypeMessage(json: TdJsonRaw): LinkPreviewTypeMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeMessage = new LinkPreviewTypeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `linkPreviewTypeMessage` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeMessage(value: LinkPreviewTypeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** linkPreviewTypePhoto = LinkPreviewType — The link is a link to a photo */
export class LinkPreviewTypePhoto {
  type: 'linkPreviewTypePhoto' = 'linkPreviewTypePhoto';
  extra?: string;
  /** photo: photo — The photo */
  photo: Photo | null = null;
}

/** Decode a TDLib `linkPreviewTypePhoto` object; null input -> null. */
export function decodeLinkPreviewTypePhoto(json: TdJsonRaw): LinkPreviewTypePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypePhoto = new LinkPreviewTypePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  return out;
}

/** Encode a `linkPreviewTypePhoto` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypePhoto(value: LinkPreviewTypePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  return out;
}

/** linkPreviewTypePremiumGiftCode = LinkPreviewType — The link is a link to a Telegram Premium gift code */
export class LinkPreviewTypePremiumGiftCode {
  type: 'linkPreviewTypePremiumGiftCode' = 'linkPreviewTypePremiumGiftCode';
  extra?: string;
}

/** Decode a TDLib `linkPreviewTypePremiumGiftCode` object; null input -> null. */
export function decodeLinkPreviewTypePremiumGiftCode(json: TdJsonRaw): LinkPreviewTypePremiumGiftCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypePremiumGiftCode = new LinkPreviewTypePremiumGiftCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `linkPreviewTypePremiumGiftCode` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypePremiumGiftCode(value: LinkPreviewTypePremiumGiftCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypePremiumGiftCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** linkPreviewTypeRequestManagedBot = LinkPreviewType — The link is a link to a dialog for creating of a managed bot */
export class LinkPreviewTypeRequestManagedBot {
  type: 'linkPreviewTypeRequestManagedBot' = 'linkPreviewTypeRequestManagedBot';
  extra?: string;
}

/** Decode a TDLib `linkPreviewTypeRequestManagedBot` object; null input -> null. */
export function decodeLinkPreviewTypeRequestManagedBot(json: TdJsonRaw): LinkPreviewTypeRequestManagedBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeRequestManagedBot = new LinkPreviewTypeRequestManagedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `linkPreviewTypeRequestManagedBot` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeRequestManagedBot(value: LinkPreviewTypeRequestManagedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeRequestManagedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** linkPreviewTypeShareableChatFolder = LinkPreviewType — The link is a link to a shareable chat folder */
export class LinkPreviewTypeShareableChatFolder {
  type: 'linkPreviewTypeShareableChatFolder' = 'linkPreviewTypeShareableChatFolder';
  extra?: string;
}

/** Decode a TDLib `linkPreviewTypeShareableChatFolder` object; null input -> null. */
export function decodeLinkPreviewTypeShareableChatFolder(json: TdJsonRaw): LinkPreviewTypeShareableChatFolder | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeShareableChatFolder = new LinkPreviewTypeShareableChatFolder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `linkPreviewTypeShareableChatFolder` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeShareableChatFolder(value: LinkPreviewTypeShareableChatFolder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeShareableChatFolder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** linkPreviewTypeSticker = LinkPreviewType — The link is a link to a sticker */
export class LinkPreviewTypeSticker {
  type: 'linkPreviewTypeSticker' = 'linkPreviewTypeSticker';
  extra?: string;
  /** sticker: sticker — The sticker. It can be an arbitrary WEBP image and can have dimensions bigger than 512 */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `linkPreviewTypeSticker` object; null input -> null. */
export function decodeLinkPreviewTypeSticker(json: TdJsonRaw): LinkPreviewTypeSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeSticker = new LinkPreviewTypeSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `linkPreviewTypeSticker` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeSticker(value: LinkPreviewTypeSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** linkPreviewTypeStickerSet = LinkPreviewType — The link is a link to a sticker set */
export class LinkPreviewTypeStickerSet {
  type: 'linkPreviewTypeStickerSet' = 'linkPreviewTypeStickerSet';
  extra?: string;
  /** stickers: vector<sticker> — Up to 4 stickers from the sticker set */
  stickers: Sticker[] = [];
}

/** Decode a TDLib `linkPreviewTypeStickerSet` object; null input -> null. */
export function decodeLinkPreviewTypeStickerSet(json: TdJsonRaw): LinkPreviewTypeStickerSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeStickerSet = new LinkPreviewTypeStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.stickers = decodeVector(record['stickers'], (item: TdJsonRaw): Sticker => decodeSticker(item) as Sticker);
  return out;
}

/** Encode a `linkPreviewTypeStickerSet` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeStickerSet(value: LinkPreviewTypeStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['stickers'] = encodeVector(value.stickers, (item: Sticker | null): TdJsonRaw => item === null ? null : encodeSticker(item));
  return out;
}

/** linkPreviewTypeStory = LinkPreviewType — The link is a link to a story. Link preview description is unavailable */
export class LinkPreviewTypeStory {
  type: 'linkPreviewTypeStory' = 'linkPreviewTypeStory';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
}

/** Decode a TDLib `linkPreviewTypeStory` object; null input -> null. */
export function decodeLinkPreviewTypeStory(json: TdJsonRaw): LinkPreviewTypeStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeStory = new LinkPreviewTypeStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `linkPreviewTypeStory` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeStory(value: LinkPreviewTypeStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** linkPreviewTypeStoryAlbum = LinkPreviewType — The link is a link to an album of stories */
export class LinkPreviewTypeStoryAlbum {
  type: 'linkPreviewTypeStoryAlbum' = 'linkPreviewTypeStoryAlbum';
  extra?: string;
  /** photo_icon: photo — Icon of the album; may be null if none */
  photo_icon: Photo | null = null;
  /** video_icon: video — Video icon of the album; may be null if none */
  video_icon: Video | null = null;
}

/** Decode a TDLib `linkPreviewTypeStoryAlbum` object; null input -> null. */
export function decodeLinkPreviewTypeStoryAlbum(json: TdJsonRaw): LinkPreviewTypeStoryAlbum | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeStoryAlbum = new LinkPreviewTypeStoryAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo_icon = decodePhoto(record['photo_icon']);
  out.video_icon = decodeVideo(record['video_icon']);
  return out;
}

/** Encode a `linkPreviewTypeStoryAlbum` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeStoryAlbum(value: LinkPreviewTypeStoryAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeStoryAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo_icon'] = value.photo_icon === null ? null : encodePhoto(value.photo_icon);
  out['video_icon'] = value.video_icon === null ? null : encodeVideo(value.video_icon);
  return out;
}

/** linkPreviewTypeSupergroupBoost = LinkPreviewType — The link is a link to boost a supergroup chat */
export class LinkPreviewTypeSupergroupBoost {
  type: 'linkPreviewTypeSupergroupBoost' = 'linkPreviewTypeSupergroupBoost';
  extra?: string;
  /** photo: chatPhoto — Photo of the chat; may be null */
  photo: ChatPhoto | null = null;
}

/** Decode a TDLib `linkPreviewTypeSupergroupBoost` object; null input -> null. */
export function decodeLinkPreviewTypeSupergroupBoost(json: TdJsonRaw): LinkPreviewTypeSupergroupBoost | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeSupergroupBoost = new LinkPreviewTypeSupergroupBoost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  return out;
}

/** Encode a `linkPreviewTypeSupergroupBoost` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeSupergroupBoost(value: LinkPreviewTypeSupergroupBoost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeSupergroupBoost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  return out;
}

/** linkPreviewTypeTextCompositionStyle = LinkPreviewType — The link is a link to a text composition style */
export class LinkPreviewTypeTextCompositionStyle {
  type: 'linkPreviewTypeTextCompositionStyle' = 'linkPreviewTypeTextCompositionStyle';
  extra?: string;
  /** custom_emoji_id: int64 — Identifier of the custom emoji corresponding to the style; 0 if none */
  custom_emoji_id: string = '';
}

/** Decode a TDLib `linkPreviewTypeTextCompositionStyle` object; null input -> null. */
export function decodeLinkPreviewTypeTextCompositionStyle(json: TdJsonRaw): LinkPreviewTypeTextCompositionStyle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeTextCompositionStyle = new LinkPreviewTypeTextCompositionStyle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  return out;
}

/** Encode a `linkPreviewTypeTextCompositionStyle` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeTextCompositionStyle(value: LinkPreviewTypeTextCompositionStyle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeTextCompositionStyle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_emoji_id'] = value.custom_emoji_id;
  return out;
}

/** linkPreviewTypeTheme = LinkPreviewType — The link is a link to a cloud theme. TDLib has no theme support yet */
export class LinkPreviewTypeTheme {
  type: 'linkPreviewTypeTheme' = 'linkPreviewTypeTheme';
  extra?: string;
  /** documents: vector<document> — The list of files with theme description */
  documents: Document[] = [];
  /** settings: themeSettings — Settings for the cloud theme; may be null if unknown */
  settings: ThemeSettings | null = null;
}

/** Decode a TDLib `linkPreviewTypeTheme` object; null input -> null. */
export function decodeLinkPreviewTypeTheme(json: TdJsonRaw): LinkPreviewTypeTheme | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeTheme = new LinkPreviewTypeTheme();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.documents = decodeVector(record['documents'], (item: TdJsonRaw): Document => decodeDocument(item) as Document);
  out.settings = decodeThemeSettings(record['settings']);
  return out;
}

/** Encode a `linkPreviewTypeTheme` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeTheme(value: LinkPreviewTypeTheme): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeTheme';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['documents'] = encodeVector(value.documents, (item: Document | null): TdJsonRaw => item === null ? null : encodeDocument(item));
  out['settings'] = value.settings === null ? null : encodeThemeSettings(value.settings);
  return out;
}

/** linkPreviewTypeUnsupported = LinkPreviewType — The link preview type is unsupported yet */
export class LinkPreviewTypeUnsupported {
  type: 'linkPreviewTypeUnsupported' = 'linkPreviewTypeUnsupported';
  extra?: string;
}

/** Decode a TDLib `linkPreviewTypeUnsupported` object; null input -> null. */
export function decodeLinkPreviewTypeUnsupported(json: TdJsonRaw): LinkPreviewTypeUnsupported | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeUnsupported = new LinkPreviewTypeUnsupported();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `linkPreviewTypeUnsupported` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeUnsupported(value: LinkPreviewTypeUnsupported): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeUnsupported';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** linkPreviewTypeUpgradedGift = LinkPreviewType — The link is a link to an upgraded gift */
export class LinkPreviewTypeUpgradedGift {
  type: 'linkPreviewTypeUpgradedGift' = 'linkPreviewTypeUpgradedGift';
  extra?: string;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
}

/** Decode a TDLib `linkPreviewTypeUpgradedGift` object; null input -> null. */
export function decodeLinkPreviewTypeUpgradedGift(json: TdJsonRaw): LinkPreviewTypeUpgradedGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeUpgradedGift = new LinkPreviewTypeUpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  return out;
}

/** Encode a `linkPreviewTypeUpgradedGift` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeUpgradedGift(value: LinkPreviewTypeUpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeUpgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  return out;
}

/** linkPreviewTypeUser = LinkPreviewType — The link is a link to a user */
export class LinkPreviewTypeUser {
  type: 'linkPreviewTypeUser' = 'linkPreviewTypeUser';
  extra?: string;
  /** photo: chatPhoto — Photo of the user; may be null if none */
  photo: ChatPhoto | null = null;
  /** is_bot: Bool — True, if the user is a bot */
  is_bot: boolean = false;
}

/** Decode a TDLib `linkPreviewTypeUser` object; null input -> null. */
export function decodeLinkPreviewTypeUser(json: TdJsonRaw): LinkPreviewTypeUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeUser = new LinkPreviewTypeUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  out.is_bot = asBool(record['is_bot']);
  return out;
}

/** Encode a `linkPreviewTypeUser` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeUser(value: LinkPreviewTypeUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  out['is_bot'] = value.is_bot;
  return out;
}

/** linkPreviewTypeVideo = LinkPreviewType — The link is a link to a video */
export class LinkPreviewTypeVideo {
  type: 'linkPreviewTypeVideo' = 'linkPreviewTypeVideo';
  extra?: string;
  /** video: video — The video description */
  video: Video | null = null;
  /** cover: photo — Cover of the video; may be null if none */
  cover: Photo | null = null;
  /** start_timestamp: int32 — Timestamp from which the video playing must start, in seconds */
  start_timestamp: number = 0;
}

/** Decode a TDLib `linkPreviewTypeVideo` object; null input -> null. */
export function decodeLinkPreviewTypeVideo(json: TdJsonRaw): LinkPreviewTypeVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeVideo = new LinkPreviewTypeVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeVideo(record['video']);
  out.cover = decodePhoto(record['cover']);
  out.start_timestamp = asNumber(record['start_timestamp']);
  return out;
}

/** Encode a `linkPreviewTypeVideo` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeVideo(value: LinkPreviewTypeVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  out['cover'] = value.cover === null ? null : encodePhoto(value.cover);
  out['start_timestamp'] = value.start_timestamp;
  return out;
}

/** linkPreviewTypeVideoChat = LinkPreviewType — The link is a link to a video chat */
export class LinkPreviewTypeVideoChat {
  type: 'linkPreviewTypeVideoChat' = 'linkPreviewTypeVideoChat';
  extra?: string;
  /** photo: chatPhoto — Photo of the chat with the video chat; may be null if none */
  photo: ChatPhoto | null = null;
  /** is_live_stream: Bool — True, if the video chat is expected to be a live stream in a channel or a broadcast group */
  is_live_stream: boolean = false;
  /** joins_as_speaker: Bool — True, if the user can use the link to join the video chat without being muted by administrators */
  joins_as_speaker: boolean = false;
}

/** Decode a TDLib `linkPreviewTypeVideoChat` object; null input -> null. */
export function decodeLinkPreviewTypeVideoChat(json: TdJsonRaw): LinkPreviewTypeVideoChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeVideoChat = new LinkPreviewTypeVideoChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  out.is_live_stream = asBool(record['is_live_stream']);
  out.joins_as_speaker = asBool(record['joins_as_speaker']);
  return out;
}

/** Encode a `linkPreviewTypeVideoChat` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeVideoChat(value: LinkPreviewTypeVideoChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeVideoChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  out['is_live_stream'] = value.is_live_stream;
  out['joins_as_speaker'] = value.joins_as_speaker;
  return out;
}

/** linkPreviewTypeVideoNote = LinkPreviewType — The link is a link to a video note message */
export class LinkPreviewTypeVideoNote {
  type: 'linkPreviewTypeVideoNote' = 'linkPreviewTypeVideoNote';
  extra?: string;
  /** video_note: videoNote — The video note */
  video_note: VideoNote | null = null;
}

/** Decode a TDLib `linkPreviewTypeVideoNote` object; null input -> null. */
export function decodeLinkPreviewTypeVideoNote(json: TdJsonRaw): LinkPreviewTypeVideoNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeVideoNote = new LinkPreviewTypeVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video_note = decodeVideoNote(record['video_note']);
  return out;
}

/** Encode a `linkPreviewTypeVideoNote` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeVideoNote(value: LinkPreviewTypeVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video_note'] = value.video_note === null ? null : encodeVideoNote(value.video_note);
  return out;
}

/** linkPreviewTypeVoiceNote = LinkPreviewType — The link is a link to a voice note message */
export class LinkPreviewTypeVoiceNote {
  type: 'linkPreviewTypeVoiceNote' = 'linkPreviewTypeVoiceNote';
  extra?: string;
  /** voice_note: voiceNote — The voice note */
  voice_note: VoiceNote | null = null;
}

/** Decode a TDLib `linkPreviewTypeVoiceNote` object; null input -> null. */
export function decodeLinkPreviewTypeVoiceNote(json: TdJsonRaw): LinkPreviewTypeVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeVoiceNote = new LinkPreviewTypeVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.voice_note = decodeVoiceNote(record['voice_note']);
  return out;
}

/** Encode a `linkPreviewTypeVoiceNote` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeVoiceNote(value: LinkPreviewTypeVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['voice_note'] = value.voice_note === null ? null : encodeVoiceNote(value.voice_note);
  return out;
}

/** linkPreviewTypeWebApp = LinkPreviewType — The link is a link to a Web App */
export class LinkPreviewTypeWebApp {
  type: 'linkPreviewTypeWebApp' = 'linkPreviewTypeWebApp';
  extra?: string;
  /** photo: photo — Web App photo; may be null if none */
  photo: Photo | null = null;
}

/** Decode a TDLib `linkPreviewTypeWebApp` object; null input -> null. */
export function decodeLinkPreviewTypeWebApp(json: TdJsonRaw): LinkPreviewTypeWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LinkPreviewTypeWebApp = new LinkPreviewTypeWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  return out;
}

/** Encode a `linkPreviewTypeWebApp` into TDLib JSON wire shape. */
export function encodeLinkPreviewTypeWebApp(value: LinkPreviewTypeWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'linkPreviewTypeWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  return out;
}

/** listenToAudio = Ok — Informs TDLib that an audio was listened by the user */
export class ListenToAudio {
  type: 'listenToAudio' = 'listenToAudio';
  extra?: string;
  /** audio_file_id: int32 — Identifier of the file with an audio */
  audio_file_id: number = 0;
  /** duration: int32 — Duration of the listening to the audio, in seconds */
  duration: number = 0;
}

/** Decode a TDLib `listenToAudio` object; null input -> null. */
export function decodeListenToAudio(json: TdJsonRaw): ListenToAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ListenToAudio = new ListenToAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio_file_id = asNumber(record['audio_file_id']);
  out.duration = asNumber(record['duration']);
  return out;
}

/** Encode a `listenToAudio` into TDLib JSON wire shape. */
export function encodeListenToAudio(value: ListenToAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'listenToAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio_file_id'] = value.audio_file_id;
  out['duration'] = value.duration;
  return out;
}

/** liveLocation = LiveLocation — A live location Can't be enabled in direct messages chats, channels and Saved Messages. Available only to the message sender */
export class LiveLocation {
  type: 'liveLocation' = 'liveLocation';
  extra?: string;
  /** location: location — The current location */
  location: Location | null = null;
  /** live_period: int32 — Time relative to the message send date, for which the location can be updated, in seconds; if 0x7FFFFFFF, then location can be updated forever */
  live_period: number = 0;
  /** heading: int32 — The direction in which the location moves, in degrees; 1-360; 0 if unknown */
  heading: number = 0;
  /** proximity_alert_radius: int32 — The maximum distance to another chat member for proximity alerts, in meters (0-100000). 0 if the notification is disabled. */
  proximity_alert_radius: number = 0;
}

/** Decode a TDLib `liveLocation` object; null input -> null. */
export function decodeLiveLocation(json: TdJsonRaw): LiveLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LiveLocation = new LiveLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  out.live_period = asNumber(record['live_period']);
  out.heading = asNumber(record['heading']);
  out.proximity_alert_radius = asNumber(record['proximity_alert_radius']);
  return out;
}

/** Encode a `liveLocation` into TDLib JSON wire shape. */
export function encodeLiveLocation(value: LiveLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'liveLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['live_period'] = value.live_period;
  out['heading'] = value.heading;
  out['proximity_alert_radius'] = value.proximity_alert_radius;
  return out;
}

/** liveStoryDonors = LiveStoryDonors — Contains a list of users and chats that spend most money on paid messages and reactions in a live story */
export class LiveStoryDonors {
  type: 'liveStoryDonors' = 'liveStoryDonors';
  extra?: string;
  /** total_star_count: int53 — Total amount of spend Telegram Stars */
  total_star_count: number = 0;
  /** top_donors: vector<paidReactor> — List of top donors in the live story */
  top_donors: PaidReactor[] = [];
}

/** Decode a TDLib `liveStoryDonors` object; null input -> null. */
export function decodeLiveStoryDonors(json: TdJsonRaw): LiveStoryDonors | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LiveStoryDonors = new LiveStoryDonors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_star_count = asNumber(record['total_star_count']);
  out.top_donors = decodeVector(record['top_donors'], (item: TdJsonRaw): PaidReactor => decodePaidReactor(item) as PaidReactor);
  return out;
}

/** Encode a `liveStoryDonors` into TDLib JSON wire shape. */
export function encodeLiveStoryDonors(value: LiveStoryDonors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'liveStoryDonors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_star_count'] = value.total_star_count;
  out['top_donors'] = encodeVector(value.top_donors, (item: PaidReactor | null): TdJsonRaw => item === null ? null : encodePaidReactor(item));
  return out;
}

/** loadActiveStories = Ok — Loads more active stories from a story list. The loaded stories will be sent through updates. Active stories are sorted by the pair (active_stories.order, active_stories.story_poster_chat_id) in descending order.... */
export class LoadActiveStories {
  type: 'loadActiveStories' = 'loadActiveStories';
  extra?: string;
  /** story_list: StoryList — The story list in which to load active stories */
  story_list: TdStoryList | null = null;
}

/** Decode a TDLib `loadActiveStories` object; null input -> null. */
export function decodeLoadActiveStories(json: TdJsonRaw): LoadActiveStories | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoadActiveStories = new LoadActiveStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_list = decodeTdStoryList(record['story_list']);
  return out;
}

/** Encode a `loadActiveStories` into TDLib JSON wire shape. */
export function encodeLoadActiveStories(value: LoadActiveStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loadActiveStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_list'] = value.story_list === null ? null : encodeTdStoryList(value.story_list);
  return out;
}

/** loadChatWelcomeMessages = Ok — Loads welcome messages of a chat; requires can_send_welcome_messages administrator right in the chat. The loaded messages will be sent through updateChatWelcomeMessages */
export class LoadChatWelcomeMessages {
  type: 'loadChatWelcomeMessages' = 'loadChatWelcomeMessages';
  extra?: string;
  /** chat_id: int53 — The identifier of the chat */
  chat_id: number = 0;
}

/** Decode a TDLib `loadChatWelcomeMessages` object; null input -> null. */
export function decodeLoadChatWelcomeMessages(json: TdJsonRaw): LoadChatWelcomeMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoadChatWelcomeMessages = new LoadChatWelcomeMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `loadChatWelcomeMessages` into TDLib JSON wire shape. */
export function encodeLoadChatWelcomeMessages(value: LoadChatWelcomeMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loadChatWelcomeMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** loadChats = Ok — Loads more chats from a chat list. The loaded chats and their positions in the chat list will be sent through updates. Chats are sorted by the pair (chat.position.order, chat.id) in descending order. Returns a 404 error ... */
export class LoadChats {
  type: 'loadChats' = 'loadChats';
  extra?: string;
  /** chat_list: ChatList — The chat list in which to load chats; pass null to load chats from the main chat list */
  chat_list: TdChatList | null = null;
  /** limit: int32 — The maximum number of chats to be loaded. For optimal performance, the number of loaded chats is chosen by TDLib and can be smaller than the specified limit, even if the end of the list is not reached */
  limit: number = 0;
}

/** Decode a TDLib `loadChats` object; null input -> null. */
export function decodeLoadChats(json: TdJsonRaw): LoadChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoadChats = new LoadChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_list = decodeTdChatList(record['chat_list']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `loadChats` into TDLib JSON wire shape. */
export function encodeLoadChats(value: LoadChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loadChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  out['limit'] = value.limit;
  return out;
}

/** loadCommunityFullInfo = Ok — Returns full information about a community. The data will be sent through update. */
export class LoadCommunityFullInfo {
  type: 'loadCommunityFullInfo' = 'loadCommunityFullInfo';
  extra?: string;
  /** community_id: int53 — Community identifier */
  community_id: number = 0;
}

/** Decode a TDLib `loadCommunityFullInfo` object; null input -> null. */
export function decodeLoadCommunityFullInfo(json: TdJsonRaw): LoadCommunityFullInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoadCommunityFullInfo = new LoadCommunityFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.community_id = asNumber(record['community_id']);
  return out;
}

/** Encode a `loadCommunityFullInfo` into TDLib JSON wire shape. */
export function encodeLoadCommunityFullInfo(value: LoadCommunityFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loadCommunityFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['community_id'] = value.community_id;
  return out;
}

/** loadDirectMessagesChatTopics = Ok — Loads more topics in a channel direct messages chat administered by the current user. The loaded topics will be sent through updateDirectMessagesChatTopic. Topics are sorted by their topic.order in des... */
export class LoadDirectMessagesChatTopics {
  type: 'loadDirectMessagesChatTopics' = 'loadDirectMessagesChatTopics';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** limit: int32 — The maximum number of topics to be loaded. For optimal performance, the number of loaded topics is chosen by TDLib and can be smaller than the specified limit, even if the end of the list is not reached */
  limit: number = 0;
}

/** Decode a TDLib `loadDirectMessagesChatTopics` object; null input -> null. */
export function decodeLoadDirectMessagesChatTopics(json: TdJsonRaw): LoadDirectMessagesChatTopics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoadDirectMessagesChatTopics = new LoadDirectMessagesChatTopics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `loadDirectMessagesChatTopics` into TDLib JSON wire shape. */
export function encodeLoadDirectMessagesChatTopics(value: LoadDirectMessagesChatTopics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loadDirectMessagesChatTopics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['limit'] = value.limit;
  return out;
}

/** loadGroupCallParticipants = Ok — Loads more participants of a group call; not supported in live stories. The loaded participants will be received through updates. Use the field groupCall.loaded_all_participants to check whether all parti... */
export class LoadGroupCallParticipants {
  type: 'loadGroupCallParticipants' = 'loadGroupCallParticipants';
  extra?: string;
  /** group_call_id: int32 — Group call identifier. The group call must be previously received through getGroupCall and must be joined or being joined */
  group_call_id: number = 0;
  /** limit: int32 — The maximum number of participants to load; up to 100 */
  limit: number = 0;
}

/** Decode a TDLib `loadGroupCallParticipants` object; null input -> null. */
export function decodeLoadGroupCallParticipants(json: TdJsonRaw): LoadGroupCallParticipants | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoadGroupCallParticipants = new LoadGroupCallParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `loadGroupCallParticipants` into TDLib JSON wire shape. */
export function encodeLoadGroupCallParticipants(value: LoadGroupCallParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loadGroupCallParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['limit'] = value.limit;
  return out;
}

/** loadQuickReplyShortcutMessages = Ok — Loads quick reply messages that can be sent by a given quick reply shortcut. The loaded messages will be sent through updateQuickReplyShortcutMessages */
export class LoadQuickReplyShortcutMessages {
  type: 'loadQuickReplyShortcutMessages' = 'loadQuickReplyShortcutMessages';
  extra?: string;
  /** shortcut_id: int32 — Unique identifier of the quick reply shortcut */
  shortcut_id: number = 0;
}

/** Decode a TDLib `loadQuickReplyShortcutMessages` object; null input -> null. */
export function decodeLoadQuickReplyShortcutMessages(json: TdJsonRaw): LoadQuickReplyShortcutMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoadQuickReplyShortcutMessages = new LoadQuickReplyShortcutMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_id = asNumber(record['shortcut_id']);
  return out;
}

/** Encode a `loadQuickReplyShortcutMessages` into TDLib JSON wire shape. */
export function encodeLoadQuickReplyShortcutMessages(value: LoadQuickReplyShortcutMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loadQuickReplyShortcutMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_id'] = value.shortcut_id;
  return out;
}

/** loadQuickReplyShortcuts = Ok — Loads quick reply shortcuts created by the current user. The loaded data will be sent through updateQuickReplyShortcut and updateQuickReplyShortcuts */
export class LoadQuickReplyShortcuts {
  type: 'loadQuickReplyShortcuts' = 'loadQuickReplyShortcuts';
  extra?: string;
}

/** Decode a TDLib `loadQuickReplyShortcuts` object; null input -> null. */
export function decodeLoadQuickReplyShortcuts(json: TdJsonRaw): LoadQuickReplyShortcuts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoadQuickReplyShortcuts = new LoadQuickReplyShortcuts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `loadQuickReplyShortcuts` into TDLib JSON wire shape. */
export function encodeLoadQuickReplyShortcuts(value: LoadQuickReplyShortcuts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loadQuickReplyShortcuts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** loadSavedMessagesTopics = Ok — Loads more Saved Messages topics. The loaded topics will be sent through updateSavedMessagesTopic. Topics are sorted by their topic.order in descending order. Returns a 404 error if all topics have been loaded */
export class LoadSavedMessagesTopics {
  type: 'loadSavedMessagesTopics' = 'loadSavedMessagesTopics';
  extra?: string;
  /** limit: int32 — The maximum number of topics to be loaded. For optimal performance, the number of loaded topics is chosen by TDLib and can be smaller than the specified limit, even if the end of the list is not reached */
  limit: number = 0;
}

/** Decode a TDLib `loadSavedMessagesTopics` object; null input -> null. */
export function decodeLoadSavedMessagesTopics(json: TdJsonRaw): LoadSavedMessagesTopics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoadSavedMessagesTopics = new LoadSavedMessagesTopics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.limit = asNumber(record['limit']);
  return out;
}

/** Encode a `loadSavedMessagesTopics` into TDLib JSON wire shape. */
export function encodeLoadSavedMessagesTopics(value: LoadSavedMessagesTopics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loadSavedMessagesTopics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['limit'] = value.limit;
  return out;
}

/** localFile = LocalFile — Represents a local file */
export class LocalFile {
  type: 'localFile' = 'localFile';
  extra?: string;
  /** path: string — Local path to the locally available file part; may be empty */
  path: string = '';
  /** can_be_downloaded: Bool — True, if it is possible to download or generate the file */
  can_be_downloaded: boolean = false;
  /** can_be_deleted: Bool — True, if the file can be deleted */
  can_be_deleted: boolean = false;
  /** is_downloading_active: Bool — True, if the file is currently being downloaded (or a local copy is being generated by some other means) */
  is_downloading_active: boolean = false;
  /** is_downloading_completed: Bool — True, if the local copy is fully available */
  is_downloading_completed: boolean = false;
  /** download_offset: int53 — Download will be started from this offset. downloaded_prefix_size is calculated from this offset */
  download_offset: number = 0;
  /** downloaded_prefix_size: int53 — If is_downloading_completed is false, then only some prefix of the file starting from download_offset is ready to be read. downloaded_prefix_size is the size of that prefix in bytes */
  downloaded_prefix_size: number = 0;
  /** downloaded_size: int53 — Total downloaded file size, in bytes. Can be used only for calculating download progress. The actual file size may be bigger, and some parts of it may contain garbage */
  downloaded_size: number = 0;
}

/** Decode a TDLib `localFile` object; null input -> null. */
export function decodeLocalFile(json: TdJsonRaw): LocalFile | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LocalFile = new LocalFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.path = asString(record['path']);
  out.can_be_downloaded = asBool(record['can_be_downloaded']);
  out.can_be_deleted = asBool(record['can_be_deleted']);
  out.is_downloading_active = asBool(record['is_downloading_active']);
  out.is_downloading_completed = asBool(record['is_downloading_completed']);
  out.download_offset = asNumber(record['download_offset']);
  out.downloaded_prefix_size = asNumber(record['downloaded_prefix_size']);
  out.downloaded_size = asNumber(record['downloaded_size']);
  return out;
}

/** Encode a `localFile` into TDLib JSON wire shape. */
export function encodeLocalFile(value: LocalFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'localFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['path'] = value.path;
  out['can_be_downloaded'] = value.can_be_downloaded;
  out['can_be_deleted'] = value.can_be_deleted;
  out['is_downloading_active'] = value.is_downloading_active;
  out['is_downloading_completed'] = value.is_downloading_completed;
  out['download_offset'] = value.download_offset;
  out['downloaded_prefix_size'] = value.downloaded_prefix_size;
  out['downloaded_size'] = value.downloaded_size;
  return out;
}

/** localizationTargetInfo = LocalizationTargetInfo — Contains information about the current localization target */
export class LocalizationTargetInfo {
  type: 'localizationTargetInfo' = 'localizationTargetInfo';
  extra?: string;
  /** language_packs: vector<languagePackInfo> — List of available language packs for this application */
  language_packs: LanguagePackInfo[] = [];
}

/** Decode a TDLib `localizationTargetInfo` object; null input -> null. */
export function decodeLocalizationTargetInfo(json: TdJsonRaw): LocalizationTargetInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LocalizationTargetInfo = new LocalizationTargetInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_packs = decodeVector(record['language_packs'], (item: TdJsonRaw): LanguagePackInfo => decodeLanguagePackInfo(item) as LanguagePackInfo);
  return out;
}

/** Encode a `localizationTargetInfo` into TDLib JSON wire shape. */
export function encodeLocalizationTargetInfo(value: LocalizationTargetInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'localizationTargetInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_packs'] = encodeVector(value.language_packs, (item: LanguagePackInfo | null): TdJsonRaw => item === null ? null : encodeLanguagePackInfo(item));
  return out;
}

/** location = Location — Describes a location on planet Earth */
export class Location {
  type: 'location' = 'location';
  extra?: string;
  /** latitude: double — Latitude of the location in degrees; as defined by the sender */
  latitude: number = 0;
  /** longitude: double — Longitude of the location, in degrees; as defined by the sender */
  longitude: number = 0;
  /** horizontal_accuracy: double — The estimated horizontal accuracy of the location, in meters; as defined by the sender. 0 if unknown */
  horizontal_accuracy: number = 0;
}

/** Decode a TDLib `location` object; null input -> null. */
export function decodeLocation(json: TdJsonRaw): Location | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Location = new Location();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.latitude = asNumber(record['latitude']);
  out.longitude = asNumber(record['longitude']);
  out.horizontal_accuracy = asNumber(record['horizontal_accuracy']);
  return out;
}

/** Encode a `location` into TDLib JSON wire shape. */
export function encodeLocation(value: Location): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'location';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['latitude'] = value.latitude;
  out['longitude'] = value.longitude;
  out['horizontal_accuracy'] = value.horizontal_accuracy;
  return out;
}

/** locationAddress = LocationAddress — Describes an address of a location */
export class LocationAddress {
  type: 'locationAddress' = 'locationAddress';
  extra?: string;
  /** country_code: string — A two-letter ISO 3166-1 alpha-2 country code */
  country_code: string = '';
  /** state: string — State, if applicable; empty if unknown */
  state: string = '';
  /** city: string — City; empty if unknown */
  city: string = '';
  /** street: string — The address; empty if unknown */
  street: string = '';
}

/** Decode a TDLib `locationAddress` object; null input -> null. */
export function decodeLocationAddress(json: TdJsonRaw): LocationAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LocationAddress = new LocationAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.country_code = asString(record['country_code']);
  out.state = asString(record['state']);
  out.city = asString(record['city']);
  out.street = asString(record['street']);
  return out;
}

/** Encode a `locationAddress` into TDLib JSON wire shape. */
export function encodeLocationAddress(value: LocationAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'locationAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['country_code'] = value.country_code;
  out['state'] = value.state;
  out['city'] = value.city;
  out['street'] = value.street;
  return out;
}

/** logOut = Ok — Closes the TDLib instance after a proper logout. Requires an available network connection. All local data will be destroyed. After the logout completes, updateAuthorizationState with authorizationStateClosed will be sent */
export class LogOut {
  type: 'logOut' = 'logOut';
  extra?: string;
}

/** Decode a TDLib `logOut` object; null input -> null. */
export function decodeLogOut(json: TdJsonRaw): LogOut | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LogOut = new LogOut();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `logOut` into TDLib JSON wire shape. */
export function encodeLogOut(value: LogOut): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'logOut';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** logStreamDefault = LogStream — The log is written to stderr or an OS specific log */
export class LogStreamDefault {
  type: 'logStreamDefault' = 'logStreamDefault';
  extra?: string;
}

/** Decode a TDLib `logStreamDefault` object; null input -> null. */
export function decodeLogStreamDefault(json: TdJsonRaw): LogStreamDefault | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LogStreamDefault = new LogStreamDefault();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `logStreamDefault` into TDLib JSON wire shape. */
export function encodeLogStreamDefault(value: LogStreamDefault): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'logStreamDefault';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** logStreamEmpty = LogStream — The log is written nowhere */
export class LogStreamEmpty {
  type: 'logStreamEmpty' = 'logStreamEmpty';
  extra?: string;
}

/** Decode a TDLib `logStreamEmpty` object; null input -> null. */
export function decodeLogStreamEmpty(json: TdJsonRaw): LogStreamEmpty | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LogStreamEmpty = new LogStreamEmpty();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `logStreamEmpty` into TDLib JSON wire shape. */
export function encodeLogStreamEmpty(value: LogStreamEmpty): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'logStreamEmpty';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** logStreamFile = LogStream — The log is written to a file */
export class LogStreamFile {
  type: 'logStreamFile' = 'logStreamFile';
  extra?: string;
  /** path: string — Path to the file to where the internal TDLib log will be written */
  path: string = '';
  /** max_file_size: int53 — The maximum size of the file to where the internal TDLib log is written before the file will automatically be rotated, in bytes */
  max_file_size: number = 0;
  /** redirect_stderr: Bool — Pass true to additionally redirect stderr to the log file. Ignored on Windows */
  redirect_stderr: boolean = false;
}

/** Decode a TDLib `logStreamFile` object; null input -> null. */
export function decodeLogStreamFile(json: TdJsonRaw): LogStreamFile | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LogStreamFile = new LogStreamFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.path = asString(record['path']);
  out.max_file_size = asNumber(record['max_file_size']);
  out.redirect_stderr = asBool(record['redirect_stderr']);
  return out;
}

/** Encode a `logStreamFile` into TDLib JSON wire shape. */
export function encodeLogStreamFile(value: LogStreamFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'logStreamFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['path'] = value.path;
  out['max_file_size'] = value.max_file_size;
  out['redirect_stderr'] = value.redirect_stderr;
  return out;
}

/** logTags = LogTags — Contains a list of available TDLib internal log tags */
export class LogTags {
  type: 'logTags' = 'logTags';
  extra?: string;
  /** tags: vector<string> — List of log tags */
  tags: string[] = [];
}

/** Decode a TDLib `logTags` object; null input -> null. */
export function decodeLogTags(json: TdJsonRaw): LogTags | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LogTags = new LogTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.tags = decodeVector(record['tags'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `logTags` into TDLib JSON wire shape. */
export function encodeLogTags(value: LogTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'logTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['tags'] = encodeVector(value.tags, (item: string): TdJsonRaw => item);
  return out;
}

/** logVerbosityLevel = LogVerbosityLevel — Contains a TDLib internal log verbosity level */
export class LogVerbosityLevel {
  type: 'logVerbosityLevel' = 'logVerbosityLevel';
  extra?: string;
  /** verbosity_level: int32 — Log verbosity level */
  verbosity_level: number = 0;
}

/** Decode a TDLib `logVerbosityLevel` object; null input -> null. */
export function decodeLogVerbosityLevel(json: TdJsonRaw): LogVerbosityLevel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LogVerbosityLevel = new LogVerbosityLevel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.verbosity_level = asNumber(record['verbosity_level']);
  return out;
}

/** Encode a `logVerbosityLevel` into TDLib JSON wire shape. */
export function encodeLogVerbosityLevel(value: LogVerbosityLevel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'logVerbosityLevel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['verbosity_level'] = value.verbosity_level;
  return out;
}

/** loginUrlInfoOpen = LoginUrlInfo — An HTTP URL needs to be open */
export class LoginUrlInfoOpen {
  type: 'loginUrlInfoOpen' = 'loginUrlInfoOpen';
  extra?: string;
  /** url: string — The URL to open */
  url: string = '';
  /** skip_confirmation: Bool — True, if there is no need to show an ordinary open URL confirmation */
  skip_confirmation: boolean = false;
}

/** Decode a TDLib `loginUrlInfoOpen` object; null input -> null. */
export function decodeLoginUrlInfoOpen(json: TdJsonRaw): LoginUrlInfoOpen | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoginUrlInfoOpen = new LoginUrlInfoOpen();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.skip_confirmation = asBool(record['skip_confirmation']);
  return out;
}

/** Encode a `loginUrlInfoOpen` into TDLib JSON wire shape. */
export function encodeLoginUrlInfoOpen(value: LoginUrlInfoOpen): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loginUrlInfoOpen';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['skip_confirmation'] = value.skip_confirmation;
  return out;
}

/** loginUrlInfoRequestConfirmation = LoginUrlInfo — An authorization confirmation dialog needs to be shown to the user */
export class LoginUrlInfoRequestConfirmation {
  type: 'loginUrlInfoRequestConfirmation' = 'loginUrlInfoRequestConfirmation';
  extra?: string;
  /** url: string — An HTTP URL to be opened */
  url: string = '';
  /** domain: string — A domain of the URL */
  domain: string = '';
  /** bot_user_id: int53 — User identifier of a bot linked with the website */
  bot_user_id: number = 0;
  /** request_write_access: Bool — True, if the user must be asked for the permission to the bot to send them messages */
  request_write_access: boolean = false;
}

/** Decode a TDLib `loginUrlInfoRequestConfirmation` object; null input -> null. */
export function decodeLoginUrlInfoRequestConfirmation(json: TdJsonRaw): LoginUrlInfoRequestConfirmation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: LoginUrlInfoRequestConfirmation = new LoginUrlInfoRequestConfirmation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.domain = asString(record['domain']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.request_write_access = asBool(record['request_write_access']);
  return out;
}

/** Encode a `loginUrlInfoRequestConfirmation` into TDLib JSON wire shape. */
export function encodeLoginUrlInfoRequestConfirmation(value: LoginUrlInfoRequestConfirmation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'loginUrlInfoRequestConfirmation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['domain'] = value.domain;
  out['bot_user_id'] = value.bot_user_id;
  out['request_write_access'] = value.request_write_access;
  return out;
}
