/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { AlternativeVideo, AnimatedEmoji, Animation, Audio, decodeAlternativeVideo, decodeAnimatedEmoji, decodeAnimation, decodeAudio, encodeAlternativeVideo, encodeAnimatedEmoji, encodeAnimation, encodeAudio } from './TdTypes_A';
import { Birthdate, decodeBirthdate, encodeBirthdate } from './TdTypes_B';
import { ChatBackground, ChatPhoto, Checklist, ChecklistTask, Contact, decodeChatBackground, decodeChatPhoto, decodeChecklist, decodeChecklistTask, decodeContact, encodeChatBackground, encodeChatPhoto, encodeChecklist, encodeChecklistTask, encodeContact } from './TdTypes_C';
import { Document, DraftMessage, decodeDocument, decodeDraftMessage, encodeDocument, encodeDraftMessage } from './TdTypes_D';
import { EncryptedCredentials, EncryptedPassportElement, EphemeralMessageContent, ErrorValue, decodeEncryptedCredentials, decodeEncryptedPassportElement, decodeEphemeralMessageContent, decodeErrorValue, encodeEncryptedCredentials, encodeEncryptedPassportElement, encodeEphemeralMessageContent, encodeErrorValue } from './TdTypes_E';
import { FactCheck, FormattedText, ForumTopicIcon, ForwardSource, decodeFactCheck, decodeFormattedText, decodeForumTopicIcon, decodeForwardSource, encodeFactCheck, encodeFormattedText, encodeForumTopicIcon, encodeForwardSource } from './TdTypes_F';
import { Game, Gift, GiveawayParameters, decodeGame, decodeGift, decodeGiveawayParameters, encodeGame, encodeGift, encodeGiveawayParameters } from './TdTypes_G';
import { InputSuggestedPostInfo, decodeInputSuggestedPostInfo, encodeInputSuggestedPostInfo } from './TdTypes_I';
import { LinkPreview, LinkPreviewOptions, LiveLocation, Location, decodeLinkPreview, decodeLinkPreviewOptions, decodeLiveLocation, decodeLocation, encodeLinkPreview, encodeLinkPreviewOptions, encodeLiveLocation, encodeLocation } from './TdTypes_L';
import { OrderInfo, decodeOrderInfo, encodeOrderInfo } from './TdTypes_O';
import { PaidReactor, Photo, Poll, ProductInfo, decodePaidReactor, decodePhoto, decodePoll, decodeProductInfo, encodePaidReactor, encodePhoto, encodePoll, encodeProductInfo } from './TdTypes_P';
import { RestrictionInfo, RichMessage, decodeRestrictionInfo, decodeRichMessage, encodeRestrictionInfo, encodeRichMessage } from './TdTypes_R';
import { SharedChat, SharedUser, StarAmount, Sticker, SuggestedPostInfo, decodeSharedChat, decodeSharedUser, decodeStarAmount, decodeSticker, decodeSuggestedPostInfo, encodeSharedChat, encodeSharedUser, encodeStarAmount, encodeSticker, encodeSuggestedPostInfo } from './TdTypes_S';
import { TextQuote, decodeTextQuote, encodeTextQuote } from './TdTypes_T';
import { UnreadReaction, UpgradedGift, decodeUnreadReaction, decodeUpgradedGift, encodeUnreadReaction, encodeUpgradedGift } from './TdTypes_U';
import { Venue, Video, VideoNote, VideoStoryboard, VoiceNote, decodeVenue, decodeVideo, decodeVideoNote, decodeVideoStoryboard, decodeVoiceNote, encodeVenue, encodeVideo, encodeVideoNote, encodeVideoStoryboard, encodeVoiceNote } from './TdTypes_V';
import { WebAppUrl, decodeWebAppUrl, encodeWebAppUrl } from './TdTypes_W';
import { TdBotWriteAccessAllowReason, TdCallDiscardReason, TdChatTheme, TdDiceStickers, TdGiftPurchaseOfferState, TdGiftResalePrice, TdGiveawayPrize, TdMaskPoint, TdMessageContent, TdMessageEffectType, TdMessageOrigin, TdMessageReplyTo, TdMessageSchedulingState, TdMessageSelfDestructType, TdMessageSender, TdMessageSendingState, TdMessageTopic, TdPaidMedia, TdPassportElementType, TdPollMedia, TdReactionType, TdReplyMarkup, TdStatisticalGraph, TdSuggestedPostPrice, TdSuggestedPostRefundReason, TdUpgradedGiftOrigin, TdWebAppOpenMode, decodeTdBotWriteAccessAllowReason, decodeTdCallDiscardReason, decodeTdChatTheme, decodeTdDiceStickers, decodeTdGiftPurchaseOfferState, decodeTdGiftResalePrice, decodeTdGiveawayPrize, decodeTdMaskPoint, decodeTdMessageContent, decodeTdMessageEffectType, decodeTdMessageOrigin, decodeTdMessageReplyTo, decodeTdMessageSchedulingState, decodeTdMessageSelfDestructType, decodeTdMessageSender, decodeTdMessageSendingState, decodeTdMessageTopic, decodeTdPaidMedia, decodeTdPassportElementType, decodeTdPollMedia, decodeTdReactionType, decodeTdReplyMarkup, decodeTdStatisticalGraph, decodeTdSuggestedPostPrice, decodeTdSuggestedPostRefundReason, decodeTdUpgradedGiftOrigin, decodeTdWebAppOpenMode, encodeTdBotWriteAccessAllowReason, encodeTdCallDiscardReason, encodeTdChatTheme, encodeTdDiceStickers, encodeTdGiftPurchaseOfferState, encodeTdGiftResalePrice, encodeTdGiveawayPrize, encodeTdMaskPoint, encodeTdMessageContent, encodeTdMessageEffectType, encodeTdMessageOrigin, encodeTdMessageReplyTo, encodeTdMessageSchedulingState, encodeTdMessageSelfDestructType, encodeTdMessageSender, encodeTdMessageSendingState, encodeTdMessageTopic, encodeTdPaidMedia, encodeTdPassportElementType, encodeTdPollMedia, encodeTdReactionType, encodeTdReplyMarkup, encodeTdStatisticalGraph, encodeTdSuggestedPostPrice, encodeTdSuggestedPostRefundReason, encodeTdUpgradedGiftOrigin, encodeTdWebAppOpenMode } from './TdUnions';

/** mainWebApp = MainWebApp — Contains information about the main Web App of a bot */
export class MainWebApp {
  type: 'mainWebApp' = 'mainWebApp';
  extra?: string;
  /** url: webAppUrl — URL of the Web App to open */
  url: WebAppUrl | null = null;
  /** mode: WebAppOpenMode — The mode in which the Web App must be opened */
  mode: TdWebAppOpenMode | null = null;
}

/** Decode a TDLib `mainWebApp` object; null input -> null. */
export function decodeMainWebApp(json: TdJsonRaw): MainWebApp | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MainWebApp = new MainWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = decodeWebAppUrl(record['url']);
  out.mode = decodeTdWebAppOpenMode(record['mode']);
  return out;
}

/** Encode a `mainWebApp` into TDLib JSON wire shape. */
export function encodeMainWebApp(value: MainWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'mainWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url === null ? null : encodeWebAppUrl(value.url);
  out['mode'] = value.mode === null ? null : encodeTdWebAppOpenMode(value.mode);
  return out;
}

/** markChecklistTasksAsDone = Ok — Adds tasks of a checklist in a message as done or not done */
export class MarkChecklistTasksAsDone {
  type: 'markChecklistTasksAsDone' = 'markChecklistTasksAsDone';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the checklist. Use messageProperties.can_mark_tasks_as_done to check whether the tasks can be marked as done or not done */
  message_id: number = 0;
  /** marked_as_done_task_ids: vector<int32> — Identifiers of tasks that were marked as done */
  marked_as_done_task_ids: number[] = [];
  /** marked_as_not_done_task_ids: vector<int32> — Identifiers of tasks that were marked as not done */
  marked_as_not_done_task_ids: number[] = [];
}

/** Decode a TDLib `markChecklistTasksAsDone` object; null input -> null. */
export function decodeMarkChecklistTasksAsDone(json: TdJsonRaw): MarkChecklistTasksAsDone | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MarkChecklistTasksAsDone = new MarkChecklistTasksAsDone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.marked_as_done_task_ids = decodeVector(record['marked_as_done_task_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.marked_as_not_done_task_ids = decodeVector(record['marked_as_not_done_task_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `markChecklistTasksAsDone` into TDLib JSON wire shape. */
export function encodeMarkChecklistTasksAsDone(value: MarkChecklistTasksAsDone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'markChecklistTasksAsDone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['marked_as_done_task_ids'] = encodeVector(value.marked_as_done_task_ids, (item: number): TdJsonRaw => item);
  out['marked_as_not_done_task_ids'] = encodeVector(value.marked_as_not_done_task_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** maskPointChin = MaskPoint — The mask is placed relatively to the chin */
export class MaskPointChin {
  type: 'maskPointChin' = 'maskPointChin';
  extra?: string;
}

/** Decode a TDLib `maskPointChin` object; null input -> null. */
export function decodeMaskPointChin(json: TdJsonRaw): MaskPointChin | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MaskPointChin = new MaskPointChin();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `maskPointChin` into TDLib JSON wire shape. */
export function encodeMaskPointChin(value: MaskPointChin): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'maskPointChin';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** maskPointEyes = MaskPoint — The mask is placed relatively to the eyes */
export class MaskPointEyes {
  type: 'maskPointEyes' = 'maskPointEyes';
  extra?: string;
}

/** Decode a TDLib `maskPointEyes` object; null input -> null. */
export function decodeMaskPointEyes(json: TdJsonRaw): MaskPointEyes | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MaskPointEyes = new MaskPointEyes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `maskPointEyes` into TDLib JSON wire shape. */
export function encodeMaskPointEyes(value: MaskPointEyes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'maskPointEyes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** maskPointForehead = MaskPoint — The mask is placed relatively to the forehead */
export class MaskPointForehead {
  type: 'maskPointForehead' = 'maskPointForehead';
  extra?: string;
}

/** Decode a TDLib `maskPointForehead` object; null input -> null. */
export function decodeMaskPointForehead(json: TdJsonRaw): MaskPointForehead | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MaskPointForehead = new MaskPointForehead();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `maskPointForehead` into TDLib JSON wire shape. */
export function encodeMaskPointForehead(value: MaskPointForehead): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'maskPointForehead';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** maskPointMouth = MaskPoint — The mask is placed relatively to the mouth */
export class MaskPointMouth {
  type: 'maskPointMouth' = 'maskPointMouth';
  extra?: string;
}

/** Decode a TDLib `maskPointMouth` object; null input -> null. */
export function decodeMaskPointMouth(json: TdJsonRaw): MaskPointMouth | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MaskPointMouth = new MaskPointMouth();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `maskPointMouth` into TDLib JSON wire shape. */
export function encodeMaskPointMouth(value: MaskPointMouth): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'maskPointMouth';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** maskPosition = MaskPosition — Position on a photo where a mask is placed */
export class MaskPosition {
  type: 'maskPosition' = 'maskPosition';
  extra?: string;
  /** point: MaskPoint — Part of the face, relative to which the mask is placed */
  point: TdMaskPoint | null = null;
  /** x_shift: double — Shift by X-axis measured in widths of the mask scaled to the face size, from left to right. (For example, -1.0 will place the mask just to the left of the default mask position) */
  x_shift: number = 0;
  /** y_shift: double — Shift by Y-axis measured in heights of the mask scaled to the face size, from top to bottom. (For example, 1.0 will place the mask just below the default mask position) */
  y_shift: number = 0;
  /** scale: double — Mask scaling coefficient. (For example, 2.0 means a doubled size) */
  scale: number = 0;
}

/** Decode a TDLib `maskPosition` object; null input -> null. */
export function decodeMaskPosition(json: TdJsonRaw): MaskPosition | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MaskPosition = new MaskPosition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.point = decodeTdMaskPoint(record['point']);
  out.x_shift = asNumber(record['x_shift']);
  out.y_shift = asNumber(record['y_shift']);
  out.scale = asNumber(record['scale']);
  return out;
}

/** Encode a `maskPosition` into TDLib JSON wire shape. */
export function encodeMaskPosition(value: MaskPosition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'maskPosition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['point'] = value.point === null ? null : encodeTdMaskPoint(value.point);
  out['x_shift'] = value.x_shift;
  out['y_shift'] = value.y_shift;
  out['scale'] = value.scale;
  return out;
}

/** message = Message — Describes a message then the date must be shown along with the message instead of the date when the message was sent */
export class Message {
  type: 'message' = 'message';
  extra?: string;
  /** id: int53 — Message identifier; unique for the chat to which the message belongs */
  id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of the message */
  sender_id: TdMessageSender | null = null;
  /** receiver_id: MessageSender — Identifier of the user or the chat which received the ephemeral message; may be null. Always null for non-ephemeral messages */
  receiver_id: TdMessageSender | null = null;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** sending_state: MessageSendingState — The sending state of the message; may be null if the message isn't being sent and didn't fail to be sent */
  sending_state: TdMessageSendingState | null = null;
  /** scheduling_state: MessageSchedulingState — The scheduling state of the message; may be null if the message isn't scheduled */
  scheduling_state: TdMessageSchedulingState | null = null;
  /** is_outgoing: Bool — True, if the message is outgoing */
  is_outgoing: boolean = false;
  /** is_pinned: Bool — True, if the message is pinned */
  is_pinned: boolean = false;
  /** is_from_offline: Bool — True, if the message was sent because of a scheduled action by the message sender, for example, as away, or greeting service message */
  is_from_offline: boolean = false;
  /** can_be_saved: Bool — True, if content of the message can be saved locally */
  can_be_saved: boolean = false;
  /** has_timestamped_media: Bool — True, if media timestamp entities refers to a media in this message as opposed to a media in the replied message */
  has_timestamped_media: boolean = false;
  /** is_channel_post: Bool — True, if the message is a channel post. All messages to channels are channel posts, all other messages are not channel posts */
  is_channel_post: boolean = false;
  /** is_paid_star_suggested_post: Bool — True, if the message is a suggested channel post which was paid in Telegram Stars; a warning must be shown if the message is deleted in less than getOption("suggested_post_lifetime_min") seconds after ... */
  is_paid_star_suggested_post: boolean = false;
  /** is_paid_gram_suggested_post: Bool — True, if the message is a suggested channel post which was paid in TON Grams; a warning must be shown if the message is deleted in less than getOption("suggested_post_lifetime_min") seconds after sending */
  is_paid_gram_suggested_post: boolean = false;
  /** contains_unread_mention: Bool — True, if the message contains an unread mention for the current user */
  contains_unread_mention: boolean = false;
  /** contains_unread_poll_votes: Bool — True, if the message is a poll message with unread votes */
  contains_unread_poll_votes: boolean = false;
  /** date: int32 — Point in time (Unix timestamp) when the message was sent; 0 for scheduled messages */
  date: number = 0;
  /** edit_date: int32 — Point in time (Unix timestamp) when the message was last edited; 0 for scheduled messages. If getOption("show_message_edit_date_by_default") is true, */
  edit_date: number = 0;
  /** forward_info: messageForwardInfo — Information about the initial message sender; may be null if none or unknown */
  forward_info: MessageForwardInfo | null = null;
  /** import_info: messageImportInfo — Information about the initial message for messages created with importMessages; may be null if the message isn't imported */
  import_info: MessageImportInfo | null = null;
  /** interaction_info: messageInteractionInfo — Information about interactions with the message; may be null if none */
  interaction_info: MessageInteractionInfo | null = null;
  /** unread_reactions: vector<unreadReaction> — Information about unread reactions added to the message */
  unread_reactions: UnreadReaction[] = [];
  /** fact_check: factCheck — Information about fact-check added to the message; may be null if none */
  fact_check: FactCheck | null = null;
  /** suggested_post_info: suggestedPostInfo — Information about the suggested post; may be null if the message isn't a suggested post */
  suggested_post_info: SuggestedPostInfo | null = null;
  /** reply_to: MessageReplyTo — Information about the message or the story this message is replying to; may be null if none */
  reply_to: TdMessageReplyTo | null = null;
  /** topic_id: MessageTopic — Identifier of the topic within the chat to which the message belongs; may be null if none; may change when the chat is converted to a forum or back */
  topic_id: TdMessageTopic | null = null;
  /** self_destruct_type: MessageSelfDestructType — The message's self-destruct type; may be null if none */
  self_destruct_type: TdMessageSelfDestructType | null = null;
  /** self_destruct_in: double — Time left before the message self-destruct timer expires, in seconds; 0 if self-destruction isn't scheduled yet */
  self_destruct_in: number = 0;
  /** auto_delete_in: double — Time left before the message will be automatically deleted by message_auto_delete_time setting of the chat, in seconds; 0 if never */
  auto_delete_in: number = 0;
  /** via_bot_user_id: int53 — If non-zero, the user identifier of the inline bot through which this message was sent */
  via_bot_user_id: number = 0;
  /** guest_bot_caller_id: MessageSender — The identifier of the user or chat which used a guest bot to send the message; may be null if none */
  guest_bot_caller_id: TdMessageSender | null = null;
  /** sender_business_bot_user_id: int53 — If non-zero, the user identifier of the business bot that sent this message */
  sender_business_bot_user_id: number = 0;
  /** sender_boost_count: int32 — Number of times the sender of the message boosted the supergroup at the time the message was sent; 0 if none or unknown. For messages sent by the current user, supergroupFullInfo.my_boost_count must be used in... */
  sender_boost_count: number = 0;
  /** sender_tag: string — Tag of the sender of the message in the supergroup at the time the message was sent; may be empty if none or unknown. For messages sent in basic groups or supergroup administrators, the current custom title or tag mu... */
  sender_tag: string = '';
  /** paid_message_star_count: int53 — The number of Telegram Stars the sender paid to send the message */
  paid_message_star_count: number = 0;
  /** author_signature: string — For channel posts and anonymous group messages, optional author signature */
  author_signature: string = '';
  /** media_album_id: int64 — Unique identifier of an album this message belongs to; 0 if none. Only audios, documents, photos and videos can be grouped together in albums */
  media_album_id: string = '';
  /** effect_id: int64 — Unique identifier of the effect added to the message; 0 if none */
  effect_id: string = '';
  /** restriction_info: restrictionInfo — Information about the restrictions that must be applied to the message content; may be null if none */
  restriction_info: RestrictionInfo | null = null;
  /** summary_language_code: string — IETF language tag of the message language on which it can be summarized; empty if summary isn't available for the message */
  summary_language_code: string = '';
  /** content: MessageContent — Content of the message */
  content: TdMessageContent | null = null;
  /** ephemeral_content: ephemeralMessageContent — Content of the message, which is visible only to the current user and must be shown instead of the regular content; may be null if none */
  ephemeral_content: EphemeralMessageContent | null = null;
  /** reply_markup: ReplyMarkup — Reply markup for the message; may be null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** ephemeral_message_id: int32 — Unique identifier of the ephemeral message if the message is ephemeral; for bots only */
  ephemeral_message_id: number = 0;
  /** chat_instance: int64 — Identifier that uniquely corresponds to the chat to which the message was sent; for bots only */
  chat_instance: string = '';
}

/** Decode a TDLib `message` object; null input -> null. */
export function decodeMessage(json: TdJsonRaw): Message | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Message = new Message();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.receiver_id = decodeTdMessageSender(record['receiver_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.sending_state = decodeTdMessageSendingState(record['sending_state']);
  out.scheduling_state = decodeTdMessageSchedulingState(record['scheduling_state']);
  out.is_outgoing = asBool(record['is_outgoing']);
  out.is_pinned = asBool(record['is_pinned']);
  out.is_from_offline = asBool(record['is_from_offline']);
  out.can_be_saved = asBool(record['can_be_saved']);
  out.has_timestamped_media = asBool(record['has_timestamped_media']);
  out.is_channel_post = asBool(record['is_channel_post']);
  out.is_paid_star_suggested_post = asBool(record['is_paid_star_suggested_post']);
  out.is_paid_gram_suggested_post = asBool(record['is_paid_gram_suggested_post']);
  out.contains_unread_mention = asBool(record['contains_unread_mention']);
  out.contains_unread_poll_votes = asBool(record['contains_unread_poll_votes']);
  out.date = asNumber(record['date']);
  out.edit_date = asNumber(record['edit_date']);
  out.forward_info = decodeMessageForwardInfo(record['forward_info']);
  out.import_info = decodeMessageImportInfo(record['import_info']);
  out.interaction_info = decodeMessageInteractionInfo(record['interaction_info']);
  out.unread_reactions = decodeVector(record['unread_reactions'], (item: TdJsonRaw): UnreadReaction => decodeUnreadReaction(item) as UnreadReaction);
  out.fact_check = decodeFactCheck(record['fact_check']);
  out.suggested_post_info = decodeSuggestedPostInfo(record['suggested_post_info']);
  out.reply_to = decodeTdMessageReplyTo(record['reply_to']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.self_destruct_type = decodeTdMessageSelfDestructType(record['self_destruct_type']);
  out.self_destruct_in = asNumber(record['self_destruct_in']);
  out.auto_delete_in = asNumber(record['auto_delete_in']);
  out.via_bot_user_id = asNumber(record['via_bot_user_id']);
  out.guest_bot_caller_id = decodeTdMessageSender(record['guest_bot_caller_id']);
  out.sender_business_bot_user_id = asNumber(record['sender_business_bot_user_id']);
  out.sender_boost_count = asNumber(record['sender_boost_count']);
  out.sender_tag = asString(record['sender_tag']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  out.author_signature = asString(record['author_signature']);
  out.media_album_id = asInt64(record['media_album_id']);
  out.effect_id = asInt64(record['effect_id']);
  out.restriction_info = decodeRestrictionInfo(record['restriction_info']);
  out.summary_language_code = asString(record['summary_language_code']);
  out.content = decodeTdMessageContent(record['content']);
  out.ephemeral_content = decodeEphemeralMessageContent(record['ephemeral_content']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.ephemeral_message_id = asNumber(record['ephemeral_message_id']);
  out.chat_instance = asInt64(record['chat_instance']);
  return out;
}

/** Encode a `message` into TDLib JSON wire shape. */
export function encodeMessage(value: Message): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'message';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['receiver_id'] = value.receiver_id === null ? null : encodeTdMessageSender(value.receiver_id);
  out['chat_id'] = value.chat_id;
  out['sending_state'] = value.sending_state === null ? null : encodeTdMessageSendingState(value.sending_state);
  out['scheduling_state'] = value.scheduling_state === null ? null : encodeTdMessageSchedulingState(value.scheduling_state);
  out['is_outgoing'] = value.is_outgoing;
  out['is_pinned'] = value.is_pinned;
  out['is_from_offline'] = value.is_from_offline;
  out['can_be_saved'] = value.can_be_saved;
  out['has_timestamped_media'] = value.has_timestamped_media;
  out['is_channel_post'] = value.is_channel_post;
  out['is_paid_star_suggested_post'] = value.is_paid_star_suggested_post;
  out['is_paid_gram_suggested_post'] = value.is_paid_gram_suggested_post;
  out['contains_unread_mention'] = value.contains_unread_mention;
  out['contains_unread_poll_votes'] = value.contains_unread_poll_votes;
  out['date'] = value.date;
  out['edit_date'] = value.edit_date;
  out['forward_info'] = value.forward_info === null ? null : encodeMessageForwardInfo(value.forward_info);
  out['import_info'] = value.import_info === null ? null : encodeMessageImportInfo(value.import_info);
  out['interaction_info'] = value.interaction_info === null ? null : encodeMessageInteractionInfo(value.interaction_info);
  out['unread_reactions'] = encodeVector(value.unread_reactions, (item: UnreadReaction | null): TdJsonRaw => item === null ? null : encodeUnreadReaction(item));
  out['fact_check'] = value.fact_check === null ? null : encodeFactCheck(value.fact_check);
  out['suggested_post_info'] = value.suggested_post_info === null ? null : encodeSuggestedPostInfo(value.suggested_post_info);
  out['reply_to'] = value.reply_to === null ? null : encodeTdMessageReplyTo(value.reply_to);
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['self_destruct_type'] = value.self_destruct_type === null ? null : encodeTdMessageSelfDestructType(value.self_destruct_type);
  out['self_destruct_in'] = value.self_destruct_in;
  out['auto_delete_in'] = value.auto_delete_in;
  out['via_bot_user_id'] = value.via_bot_user_id;
  out['guest_bot_caller_id'] = value.guest_bot_caller_id === null ? null : encodeTdMessageSender(value.guest_bot_caller_id);
  out['sender_business_bot_user_id'] = value.sender_business_bot_user_id;
  out['sender_boost_count'] = value.sender_boost_count;
  out['sender_tag'] = value.sender_tag;
  out['paid_message_star_count'] = value.paid_message_star_count;
  out['author_signature'] = value.author_signature;
  out['media_album_id'] = value.media_album_id;
  out['effect_id'] = value.effect_id;
  out['restriction_info'] = value.restriction_info === null ? null : encodeRestrictionInfo(value.restriction_info);
  out['summary_language_code'] = value.summary_language_code;
  out['content'] = value.content === null ? null : encodeTdMessageContent(value.content);
  out['ephemeral_content'] = value.ephemeral_content === null ? null : encodeEphemeralMessageContent(value.ephemeral_content);
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['ephemeral_message_id'] = value.ephemeral_message_id;
  out['chat_instance'] = value.chat_instance;
  return out;
}

/** messageAnimatedEmoji = MessageContent — A message with an animated emoji */
export class MessageAnimatedEmoji {
  type: 'messageAnimatedEmoji' = 'messageAnimatedEmoji';
  extra?: string;
  /** animated_emoji: animatedEmoji — The animated emoji */
  animated_emoji: AnimatedEmoji | null = null;
  /** emoji: string — The corresponding emoji */
  emoji: string = '';
}

/** Decode a TDLib `messageAnimatedEmoji` object; null input -> null. */
export function decodeMessageAnimatedEmoji(json: TdJsonRaw): MessageAnimatedEmoji | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageAnimatedEmoji = new MessageAnimatedEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animated_emoji = decodeAnimatedEmoji(record['animated_emoji']);
  out.emoji = asString(record['emoji']);
  return out;
}

/** Encode a `messageAnimatedEmoji` into TDLib JSON wire shape. */
export function encodeMessageAnimatedEmoji(value: MessageAnimatedEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageAnimatedEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animated_emoji'] = value.animated_emoji === null ? null : encodeAnimatedEmoji(value.animated_emoji);
  out['emoji'] = value.emoji;
  return out;
}

/** messageAnimation = MessageContent — An animation message (GIF-style). */
export class MessageAnimation {
  type: 'messageAnimation' = 'messageAnimation';
  extra?: string;
  /** animation: animation — The animation description */
  animation: Animation | null = null;
  /** caption: formattedText — Animation caption */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — True, if the caption must be shown above the animation; otherwise, the caption must be shown below the animation */
  show_caption_above_media: boolean = false;
  /** has_spoiler: Bool — True, if the animation preview must be covered by a spoiler animation */
  has_spoiler: boolean = false;
  /** is_secret: Bool — True, if the animation thumbnail must be blurred and the animation must be shown only while tapped */
  is_secret: boolean = false;
}

/** Decode a TDLib `messageAnimation` object; null input -> null. */
export function decodeMessageAnimation(json: TdJsonRaw): MessageAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageAnimation = new MessageAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeAnimation(record['animation']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  out.has_spoiler = asBool(record['has_spoiler']);
  out.is_secret = asBool(record['is_secret']);
  return out;
}

/** Encode a `messageAnimation` into TDLib JSON wire shape. */
export function encodeMessageAnimation(value: MessageAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  out['has_spoiler'] = value.has_spoiler;
  out['is_secret'] = value.is_secret;
  return out;
}

/** messageAudio = MessageContent — An audio message */
export class MessageAudio {
  type: 'messageAudio' = 'messageAudio';
  extra?: string;
  /** audio: audio — The audio description */
  audio: Audio | null = null;
  /** caption: formattedText — Audio caption */
  caption: FormattedText | null = null;
}

/** Decode a TDLib `messageAudio` object; null input -> null. */
export function decodeMessageAudio(json: TdJsonRaw): MessageAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageAudio = new MessageAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeAudio(record['audio']);
  out.caption = decodeFormattedText(record['caption']);
  return out;
}

/** Encode a `messageAudio` into TDLib JSON wire shape. */
export function encodeMessageAudio(value: MessageAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeAudio(value.audio);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  return out;
}

/** messageAutoDeleteTime = MessageAutoDeleteTime — Contains default auto-delete timer setting for new chats */
export class MessageAutoDeleteTime {
  type: 'messageAutoDeleteTime' = 'messageAutoDeleteTime';
  extra?: string;
  /** time: int32 — Message auto-delete time, in seconds. If 0, then messages aren't deleted automatically */
  time: number = 0;
}

/** Decode a TDLib `messageAutoDeleteTime` object; null input -> null. */
export function decodeMessageAutoDeleteTime(json: TdJsonRaw): MessageAutoDeleteTime | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageAutoDeleteTime = new MessageAutoDeleteTime();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.time = asNumber(record['time']);
  return out;
}

/** Encode a `messageAutoDeleteTime` into TDLib JSON wire shape. */
export function encodeMessageAutoDeleteTime(value: MessageAutoDeleteTime): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageAutoDeleteTime';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['time'] = value.time;
  return out;
}

/** messageBasicGroupChatCreate = MessageContent — A newly created basic group */
export class MessageBasicGroupChatCreate {
  type: 'messageBasicGroupChatCreate' = 'messageBasicGroupChatCreate';
  extra?: string;
  /** title: string — Title of the basic group */
  title: string = '';
  /** member_user_ids: vector<int53> — User identifiers of members in the basic group */
  member_user_ids: number[] = [];
}

/** Decode a TDLib `messageBasicGroupChatCreate` object; null input -> null. */
export function decodeMessageBasicGroupChatCreate(json: TdJsonRaw): MessageBasicGroupChatCreate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageBasicGroupChatCreate = new MessageBasicGroupChatCreate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.member_user_ids = decodeVector(record['member_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `messageBasicGroupChatCreate` into TDLib JSON wire shape. */
export function encodeMessageBasicGroupChatCreate(value: MessageBasicGroupChatCreate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageBasicGroupChatCreate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['member_user_ids'] = encodeVector(value.member_user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** messageBotWriteAccessAllowed = MessageContent — The user allowed the bot to send messages */
export class MessageBotWriteAccessAllowed {
  type: 'messageBotWriteAccessAllowed' = 'messageBotWriteAccessAllowed';
  extra?: string;
  /** reason: BotWriteAccessAllowReason — The reason why the bot was allowed to write messages */
  reason: TdBotWriteAccessAllowReason | null = null;
}

/** Decode a TDLib `messageBotWriteAccessAllowed` object; null input -> null. */
export function decodeMessageBotWriteAccessAllowed(json: TdJsonRaw): MessageBotWriteAccessAllowed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageBotWriteAccessAllowed = new MessageBotWriteAccessAllowed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reason = decodeTdBotWriteAccessAllowReason(record['reason']);
  return out;
}

/** Encode a `messageBotWriteAccessAllowed` into TDLib JSON wire shape. */
export function encodeMessageBotWriteAccessAllowed(value: MessageBotWriteAccessAllowed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageBotWriteAccessAllowed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reason'] = value.reason === null ? null : encodeTdBotWriteAccessAllowReason(value.reason);
  return out;
}

/** messageCalendar = MessageCalendar — Contains information about found messages, split by days according to the option "utc_time_offset" */
export class MessageCalendar {
  type: 'messageCalendar' = 'messageCalendar';
  extra?: string;
  /** total_count: int32 — Total number of found messages */
  total_count: number = 0;
  /** days: vector<messageCalendarDay> — Information about messages sent */
  days: MessageCalendarDay[] = [];
}

/** Decode a TDLib `messageCalendar` object; null input -> null. */
export function decodeMessageCalendar(json: TdJsonRaw): MessageCalendar | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageCalendar = new MessageCalendar();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.days = decodeVector(record['days'], (item: TdJsonRaw): MessageCalendarDay => decodeMessageCalendarDay(item) as MessageCalendarDay);
  return out;
}

/** Encode a `messageCalendar` into TDLib JSON wire shape. */
export function encodeMessageCalendar(value: MessageCalendar): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageCalendar';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['days'] = encodeVector(value.days, (item: MessageCalendarDay | null): TdJsonRaw => item === null ? null : encodeMessageCalendarDay(item));
  return out;
}

/** messageCalendarDay = MessageCalendarDay — Contains information about found messages sent on a specific day */
export class MessageCalendarDay {
  type: 'messageCalendarDay' = 'messageCalendarDay';
  extra?: string;
  /** total_count: int32 — Total number of found messages sent on the day */
  total_count: number = 0;
  /** message: message — First message sent on the day */
  message: Message | null = null;
}

/** Decode a TDLib `messageCalendarDay` object; null input -> null. */
export function decodeMessageCalendarDay(json: TdJsonRaw): MessageCalendarDay | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageCalendarDay = new MessageCalendarDay();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.message = decodeMessage(record['message']);
  return out;
}

/** Encode a `messageCalendarDay` into TDLib JSON wire shape. */
export function encodeMessageCalendarDay(value: MessageCalendarDay): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageCalendarDay';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  return out;
}

/** messageCall = MessageContent — A message with information about an ended call */
export class MessageCall {
  type: 'messageCall' = 'messageCall';
  extra?: string;
  /** unique_id: int64 — Persistent unique call identifier; 0 for calls from other devices, which can't be passed as inputCallFromMessage */
  unique_id: string = '';
  /** is_video: Bool — True, if the call was a video call */
  is_video: boolean = false;
  /** discard_reason: CallDiscardReason — Reason why the call was discarded */
  discard_reason: TdCallDiscardReason | null = null;
  /** duration: int32 — Call duration, in seconds */
  duration: number = 0;
}

/** Decode a TDLib `messageCall` object; null input -> null. */
export function decodeMessageCall(json: TdJsonRaw): MessageCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageCall = new MessageCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.unique_id = asInt64(record['unique_id']);
  out.is_video = asBool(record['is_video']);
  out.discard_reason = decodeTdCallDiscardReason(record['discard_reason']);
  out.duration = asNumber(record['duration']);
  return out;
}

/** Encode a `messageCall` into TDLib JSON wire shape. */
export function encodeMessageCall(value: MessageCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['unique_id'] = value.unique_id;
  out['is_video'] = value.is_video;
  out['discard_reason'] = value.discard_reason === null ? null : encodeTdCallDiscardReason(value.discard_reason);
  out['duration'] = value.duration;
  return out;
}

/** messageChatAddMembers = MessageContent — New chat members were added */
export class MessageChatAddMembers {
  type: 'messageChatAddMembers' = 'messageChatAddMembers';
  extra?: string;
  /** member_user_ids: vector<int53> — User identifiers of the new members */
  member_user_ids: number[] = [];
}

/** Decode a TDLib `messageChatAddMembers` object; null input -> null. */
export function decodeMessageChatAddMembers(json: TdJsonRaw): MessageChatAddMembers | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatAddMembers = new MessageChatAddMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.member_user_ids = decodeVector(record['member_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `messageChatAddMembers` into TDLib JSON wire shape. */
export function encodeMessageChatAddMembers(value: MessageChatAddMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatAddMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['member_user_ids'] = encodeVector(value.member_user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** messageChatAddedToCommunity = MessageContent — The chat was added to a community */
export class MessageChatAddedToCommunity {
  type: 'messageChatAddedToCommunity' = 'messageChatAddedToCommunity';
  extra?: string;
  /** community_id: int53 — Identifier of the community to which the chat was added */
  community_id: number = 0;
}

/** Decode a TDLib `messageChatAddedToCommunity` object; null input -> null. */
export function decodeMessageChatAddedToCommunity(json: TdJsonRaw): MessageChatAddedToCommunity | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatAddedToCommunity = new MessageChatAddedToCommunity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.community_id = asNumber(record['community_id']);
  return out;
}

/** Encode a `messageChatAddedToCommunity` into TDLib JSON wire shape. */
export function encodeMessageChatAddedToCommunity(value: MessageChatAddedToCommunity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatAddedToCommunity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['community_id'] = value.community_id;
  return out;
}

/** messageChatBoost = MessageContent — The chat was boosted by the sender of the message */
export class MessageChatBoost {
  type: 'messageChatBoost' = 'messageChatBoost';
  extra?: string;
  /** boost_count: int32 — Number of times the chat was boosted */
  boost_count: number = 0;
}

/** Decode a TDLib `messageChatBoost` object; null input -> null. */
export function decodeMessageChatBoost(json: TdJsonRaw): MessageChatBoost | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatBoost = new MessageChatBoost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.boost_count = asNumber(record['boost_count']);
  return out;
}

/** Encode a `messageChatBoost` into TDLib JSON wire shape. */
export function encodeMessageChatBoost(value: MessageChatBoost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatBoost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['boost_count'] = value.boost_count;
  return out;
}

/** messageChatChangePhoto = MessageContent — An updated chat photo */
export class MessageChatChangePhoto {
  type: 'messageChatChangePhoto' = 'messageChatChangePhoto';
  extra?: string;
  /** photo: chatPhoto — New chat photo */
  photo: ChatPhoto | null = null;
}

/** Decode a TDLib `messageChatChangePhoto` object; null input -> null. */
export function decodeMessageChatChangePhoto(json: TdJsonRaw): MessageChatChangePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatChangePhoto = new MessageChatChangePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  return out;
}

/** Encode a `messageChatChangePhoto` into TDLib JSON wire shape. */
export function encodeMessageChatChangePhoto(value: MessageChatChangePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatChangePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  return out;
}

/** messageChatChangeTitle = MessageContent — An updated chat title */
export class MessageChatChangeTitle {
  type: 'messageChatChangeTitle' = 'messageChatChangeTitle';
  extra?: string;
  /** title: string — New chat title */
  title: string = '';
}

/** Decode a TDLib `messageChatChangeTitle` object; null input -> null. */
export function decodeMessageChatChangeTitle(json: TdJsonRaw): MessageChatChangeTitle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatChangeTitle = new MessageChatChangeTitle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  return out;
}

/** Encode a `messageChatChangeTitle` into TDLib JSON wire shape. */
export function encodeMessageChatChangeTitle(value: MessageChatChangeTitle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatChangeTitle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  return out;
}

/** messageChatDeleteMember = MessageContent — A chat member was deleted */
export class MessageChatDeleteMember {
  type: 'messageChatDeleteMember' = 'messageChatDeleteMember';
  extra?: string;
  /** user_id: int53 — User identifier of the deleted chat member */
  user_id: number = 0;
}

/** Decode a TDLib `messageChatDeleteMember` object; null input -> null. */
export function decodeMessageChatDeleteMember(json: TdJsonRaw): MessageChatDeleteMember | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatDeleteMember = new MessageChatDeleteMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `messageChatDeleteMember` into TDLib JSON wire shape. */
export function encodeMessageChatDeleteMember(value: MessageChatDeleteMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatDeleteMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** messageChatDeletePhoto = MessageContent — A deleted chat photo */
export class MessageChatDeletePhoto {
  type: 'messageChatDeletePhoto' = 'messageChatDeletePhoto';
  extra?: string;
}

/** Decode a TDLib `messageChatDeletePhoto` object; null input -> null. */
export function decodeMessageChatDeletePhoto(json: TdJsonRaw): MessageChatDeletePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatDeletePhoto = new MessageChatDeletePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageChatDeletePhoto` into TDLib JSON wire shape. */
export function encodeMessageChatDeletePhoto(value: MessageChatDeletePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatDeletePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageChatHasProtectedContentDisableRequested = MessageContent — Chat has_protected_content setting was requested to be disabled */
export class MessageChatHasProtectedContentDisableRequested {
  type: 'messageChatHasProtectedContentDisableRequested' = 'messageChatHasProtectedContentDisableRequested';
  extra?: string;
  /** is_expired: Bool — True, if the request has expired */
  is_expired: boolean = false;
}

/** Decode a TDLib `messageChatHasProtectedContentDisableRequested` object; null input -> null. */
export function decodeMessageChatHasProtectedContentDisableRequested(json: TdJsonRaw): MessageChatHasProtectedContentDisableRequested | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatHasProtectedContentDisableRequested = new MessageChatHasProtectedContentDisableRequested();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_expired = asBool(record['is_expired']);
  return out;
}

/** Encode a `messageChatHasProtectedContentDisableRequested` into TDLib JSON wire shape. */
export function encodeMessageChatHasProtectedContentDisableRequested(value: MessageChatHasProtectedContentDisableRequested): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatHasProtectedContentDisableRequested';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_expired'] = value.is_expired;
  return out;
}

/** messageChatHasProtectedContentToggled = MessageContent — Chat has_protected_content setting was changed or request to change it was rejected */
export class MessageChatHasProtectedContentToggled {
  type: 'messageChatHasProtectedContentToggled' = 'messageChatHasProtectedContentToggled';
  extra?: string;
  /** request_message_id: int53 — Identifier of the message with the request to change the setting; can be an identifier of a deleted message or 0 */
  request_message_id: number = 0;
  /** old_has_protected_content: Bool — Previous value of the setting */
  old_has_protected_content: boolean = false;
  /** new_has_protected_content: Bool — New value of the setting */
  new_has_protected_content: boolean = false;
}

/** Decode a TDLib `messageChatHasProtectedContentToggled` object; null input -> null. */
export function decodeMessageChatHasProtectedContentToggled(json: TdJsonRaw): MessageChatHasProtectedContentToggled | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatHasProtectedContentToggled = new MessageChatHasProtectedContentToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.request_message_id = asNumber(record['request_message_id']);
  out.old_has_protected_content = asBool(record['old_has_protected_content']);
  out.new_has_protected_content = asBool(record['new_has_protected_content']);
  return out;
}

/** Encode a `messageChatHasProtectedContentToggled` into TDLib JSON wire shape. */
export function encodeMessageChatHasProtectedContentToggled(value: MessageChatHasProtectedContentToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatHasProtectedContentToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['request_message_id'] = value.request_message_id;
  out['old_has_protected_content'] = value.old_has_protected_content;
  out['new_has_protected_content'] = value.new_has_protected_content;
  return out;
}

/** messageChatJoinByLink = MessageContent — A new member joined the chat via an invite link */
export class MessageChatJoinByLink {
  type: 'messageChatJoinByLink' = 'messageChatJoinByLink';
  extra?: string;
}

/** Decode a TDLib `messageChatJoinByLink` object; null input -> null. */
export function decodeMessageChatJoinByLink(json: TdJsonRaw): MessageChatJoinByLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatJoinByLink = new MessageChatJoinByLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageChatJoinByLink` into TDLib JSON wire shape. */
export function encodeMessageChatJoinByLink(value: MessageChatJoinByLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatJoinByLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageChatJoinByRequest = MessageContent — A new member was accepted to the chat by an administrator */
export class MessageChatJoinByRequest {
  type: 'messageChatJoinByRequest' = 'messageChatJoinByRequest';
  extra?: string;
}

/** Decode a TDLib `messageChatJoinByRequest` object; null input -> null. */
export function decodeMessageChatJoinByRequest(json: TdJsonRaw): MessageChatJoinByRequest | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatJoinByRequest = new MessageChatJoinByRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageChatJoinByRequest` into TDLib JSON wire shape. */
export function encodeMessageChatJoinByRequest(value: MessageChatJoinByRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatJoinByRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageChatJoinFromCommunity = MessageContent — A new member joined the chat from a community */
export class MessageChatJoinFromCommunity {
  type: 'messageChatJoinFromCommunity' = 'messageChatJoinFromCommunity';
  extra?: string;
  /** community_id: int53 — Identifier of the community from which the user joined the chat */
  community_id: number = 0;
}

/** Decode a TDLib `messageChatJoinFromCommunity` object; null input -> null. */
export function decodeMessageChatJoinFromCommunity(json: TdJsonRaw): MessageChatJoinFromCommunity | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatJoinFromCommunity = new MessageChatJoinFromCommunity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.community_id = asNumber(record['community_id']);
  return out;
}

/** Encode a `messageChatJoinFromCommunity` into TDLib JSON wire shape. */
export function encodeMessageChatJoinFromCommunity(value: MessageChatJoinFromCommunity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatJoinFromCommunity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['community_id'] = value.community_id;
  return out;
}

/** messageChatOwnerChanged = MessageContent — The owner of the chat has changed */
export class MessageChatOwnerChanged {
  type: 'messageChatOwnerChanged' = 'messageChatOwnerChanged';
  extra?: string;
  /** new_owner_user_id: int53 — Identifier of the user who is the new owner of the chat */
  new_owner_user_id: number = 0;
}

/** Decode a TDLib `messageChatOwnerChanged` object; null input -> null. */
export function decodeMessageChatOwnerChanged(json: TdJsonRaw): MessageChatOwnerChanged | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatOwnerChanged = new MessageChatOwnerChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.new_owner_user_id = asNumber(record['new_owner_user_id']);
  return out;
}

/** Encode a `messageChatOwnerChanged` into TDLib JSON wire shape. */
export function encodeMessageChatOwnerChanged(value: MessageChatOwnerChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatOwnerChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['new_owner_user_id'] = value.new_owner_user_id;
  return out;
}

/** messageChatOwnerLeft = MessageContent — The owner of the chat has left */
export class MessageChatOwnerLeft {
  type: 'messageChatOwnerLeft' = 'messageChatOwnerLeft';
  extra?: string;
  /** new_owner_user_id: int53 — Identifier of the user who will become the new owner of the chat if the previous owner isn't return; 0 if none */
  new_owner_user_id: number = 0;
}

/** Decode a TDLib `messageChatOwnerLeft` object; null input -> null. */
export function decodeMessageChatOwnerLeft(json: TdJsonRaw): MessageChatOwnerLeft | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatOwnerLeft = new MessageChatOwnerLeft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.new_owner_user_id = asNumber(record['new_owner_user_id']);
  return out;
}

/** Encode a `messageChatOwnerLeft` into TDLib JSON wire shape. */
export function encodeMessageChatOwnerLeft(value: MessageChatOwnerLeft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatOwnerLeft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['new_owner_user_id'] = value.new_owner_user_id;
  return out;
}

/** messageChatRemovedFromCommunity = MessageContent — The chat was removed from a community */
export class MessageChatRemovedFromCommunity {
  type: 'messageChatRemovedFromCommunity' = 'messageChatRemovedFromCommunity';
  extra?: string;
}

/** Decode a TDLib `messageChatRemovedFromCommunity` object; null input -> null. */
export function decodeMessageChatRemovedFromCommunity(json: TdJsonRaw): MessageChatRemovedFromCommunity | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatRemovedFromCommunity = new MessageChatRemovedFromCommunity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageChatRemovedFromCommunity` into TDLib JSON wire shape. */
export function encodeMessageChatRemovedFromCommunity(value: MessageChatRemovedFromCommunity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatRemovedFromCommunity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageChatSetBackground = MessageContent — A new background was set in the chat */
export class MessageChatSetBackground {
  type: 'messageChatSetBackground' = 'messageChatSetBackground';
  extra?: string;
  /** old_background_message_id: int53 — Identifier of the message with a previously set same background; 0 if none. Can be an identifier of a deleted message */
  old_background_message_id: number = 0;
  /** background: chatBackground — The new background */
  background: ChatBackground | null = null;
  /** only_for_self: Bool — True, if the background was set only for self */
  only_for_self: boolean = false;
}

/** Decode a TDLib `messageChatSetBackground` object; null input -> null. */
export function decodeMessageChatSetBackground(json: TdJsonRaw): MessageChatSetBackground | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatSetBackground = new MessageChatSetBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.old_background_message_id = asNumber(record['old_background_message_id']);
  out.background = decodeChatBackground(record['background']);
  out.only_for_self = asBool(record['only_for_self']);
  return out;
}

/** Encode a `messageChatSetBackground` into TDLib JSON wire shape. */
export function encodeMessageChatSetBackground(value: MessageChatSetBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatSetBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['old_background_message_id'] = value.old_background_message_id;
  out['background'] = value.background === null ? null : encodeChatBackground(value.background);
  out['only_for_self'] = value.only_for_self;
  return out;
}

/** messageChatSetMessageAutoDeleteTime = MessageContent — The auto-delete or self-destruct timer for messages in the chat has been changed */
export class MessageChatSetMessageAutoDeleteTime {
  type: 'messageChatSetMessageAutoDeleteTime' = 'messageChatSetMessageAutoDeleteTime';
  extra?: string;
  /** message_auto_delete_time: int32 — New value auto-delete or self-destruct time, in seconds; 0 if disabled */
  message_auto_delete_time: number = 0;
  /** from_user_id: int53 — If not 0, a user identifier, which default setting was automatically applied */
  from_user_id: number = 0;
}

/** Decode a TDLib `messageChatSetMessageAutoDeleteTime` object; null input -> null. */
export function decodeMessageChatSetMessageAutoDeleteTime(json: TdJsonRaw): MessageChatSetMessageAutoDeleteTime | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatSetMessageAutoDeleteTime = new MessageChatSetMessageAutoDeleteTime();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_auto_delete_time = asNumber(record['message_auto_delete_time']);
  out.from_user_id = asNumber(record['from_user_id']);
  return out;
}

/** Encode a `messageChatSetMessageAutoDeleteTime` into TDLib JSON wire shape. */
export function encodeMessageChatSetMessageAutoDeleteTime(value: MessageChatSetMessageAutoDeleteTime): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatSetMessageAutoDeleteTime';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_auto_delete_time'] = value.message_auto_delete_time;
  out['from_user_id'] = value.from_user_id;
  return out;
}

/** messageChatSetTheme = MessageContent — A theme in the chat has been changed */
export class MessageChatSetTheme {
  type: 'messageChatSetTheme' = 'messageChatSetTheme';
  extra?: string;
  /** theme: ChatTheme — New theme for the chat; may be null if chat theme was reset to the default one */
  theme: TdChatTheme | null = null;
}

/** Decode a TDLib `messageChatSetTheme` object; null input -> null. */
export function decodeMessageChatSetTheme(json: TdJsonRaw): MessageChatSetTheme | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatSetTheme = new MessageChatSetTheme();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.theme = decodeTdChatTheme(record['theme']);
  return out;
}

/** Encode a `messageChatSetTheme` into TDLib JSON wire shape. */
export function encodeMessageChatSetTheme(value: MessageChatSetTheme): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatSetTheme';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['theme'] = value.theme === null ? null : encodeTdChatTheme(value.theme);
  return out;
}

/** messageChatShared = MessageContent — The current user shared a chat, which was requested by the bot */
export class MessageChatShared {
  type: 'messageChatShared' = 'messageChatShared';
  extra?: string;
  /** chat: sharedChat — The shared chat */
  chat: SharedChat | null = null;
  /** button_id: int32 — Identifier of the keyboard button with the request */
  button_id: number = 0;
}

/** Decode a TDLib `messageChatShared` object; null input -> null. */
export function decodeMessageChatShared(json: TdJsonRaw): MessageChatShared | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatShared = new MessageChatShared();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat = decodeSharedChat(record['chat']);
  out.button_id = asNumber(record['button_id']);
  return out;
}

/** Encode a `messageChatShared` into TDLib JSON wire shape. */
export function encodeMessageChatShared(value: MessageChatShared): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatShared';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat'] = value.chat === null ? null : encodeSharedChat(value.chat);
  out['button_id'] = value.button_id;
  return out;
}

/** messageChatUpgradeFrom = MessageContent — A supergroup has been created from a basic group */
export class MessageChatUpgradeFrom {
  type: 'messageChatUpgradeFrom' = 'messageChatUpgradeFrom';
  extra?: string;
  /** title: string — Title of the newly created supergroup */
  title: string = '';
  /** basic_group_id: int53 — The identifier of the original basic group */
  basic_group_id: number = 0;
}

/** Decode a TDLib `messageChatUpgradeFrom` object; null input -> null. */
export function decodeMessageChatUpgradeFrom(json: TdJsonRaw): MessageChatUpgradeFrom | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatUpgradeFrom = new MessageChatUpgradeFrom();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.basic_group_id = asNumber(record['basic_group_id']);
  return out;
}

/** Encode a `messageChatUpgradeFrom` into TDLib JSON wire shape. */
export function encodeMessageChatUpgradeFrom(value: MessageChatUpgradeFrom): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatUpgradeFrom';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['basic_group_id'] = value.basic_group_id;
  return out;
}

/** messageChatUpgradeTo = MessageContent — A basic group was upgraded to a supergroup and was deactivated as the result */
export class MessageChatUpgradeTo {
  type: 'messageChatUpgradeTo' = 'messageChatUpgradeTo';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup to which the basic group was upgraded */
  supergroup_id: number = 0;
}

/** Decode a TDLib `messageChatUpgradeTo` object; null input -> null. */
export function decodeMessageChatUpgradeTo(json: TdJsonRaw): MessageChatUpgradeTo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChatUpgradeTo = new MessageChatUpgradeTo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  return out;
}

/** Encode a `messageChatUpgradeTo` into TDLib JSON wire shape. */
export function encodeMessageChatUpgradeTo(value: MessageChatUpgradeTo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChatUpgradeTo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  return out;
}

/** messageChecklist = MessageContent — A message with a checklist */
export class MessageChecklist {
  type: 'messageChecklist' = 'messageChecklist';
  extra?: string;
  /** list: checklist — The checklist description */
  list: Checklist | null = null;
}

/** Decode a TDLib `messageChecklist` object; null input -> null. */
export function decodeMessageChecklist(json: TdJsonRaw): MessageChecklist | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChecklist = new MessageChecklist();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.list = decodeChecklist(record['list']);
  return out;
}

/** Encode a `messageChecklist` into TDLib JSON wire shape. */
export function encodeMessageChecklist(value: MessageChecklist): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChecklist';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['list'] = value.list === null ? null : encodeChecklist(value.list);
  return out;
}

/** messageChecklistTasksAdded = MessageContent — Some tasks were added to a checklist */
export class MessageChecklistTasksAdded {
  type: 'messageChecklistTasksAdded' = 'messageChecklistTasksAdded';
  extra?: string;
  /** checklist_message_id: int53 — Identifier of the message with the checklist; may be 0 or an identifier of a deleted message */
  checklist_message_id: number = 0;
  /** tasks: vector<checklistTask> — List of tasks added to the checklist */
  tasks: ChecklistTask[] = [];
}

/** Decode a TDLib `messageChecklistTasksAdded` object; null input -> null. */
export function decodeMessageChecklistTasksAdded(json: TdJsonRaw): MessageChecklistTasksAdded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChecklistTasksAdded = new MessageChecklistTasksAdded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.checklist_message_id = asNumber(record['checklist_message_id']);
  out.tasks = decodeVector(record['tasks'], (item: TdJsonRaw): ChecklistTask => decodeChecklistTask(item) as ChecklistTask);
  return out;
}

/** Encode a `messageChecklistTasksAdded` into TDLib JSON wire shape. */
export function encodeMessageChecklistTasksAdded(value: MessageChecklistTasksAdded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChecklistTasksAdded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['checklist_message_id'] = value.checklist_message_id;
  out['tasks'] = encodeVector(value.tasks, (item: ChecklistTask | null): TdJsonRaw => item === null ? null : encodeChecklistTask(item));
  return out;
}

/** messageChecklistTasksDone = MessageContent — Some tasks from a checklist were marked as done or not done */
export class MessageChecklistTasksDone {
  type: 'messageChecklistTasksDone' = 'messageChecklistTasksDone';
  extra?: string;
  /** checklist_message_id: int53 — Identifier of the message with the checklist; may be 0 or an identifier of a deleted message */
  checklist_message_id: number = 0;
  /** marked_as_done_task_ids: vector<int32> — Identifiers of tasks that were marked as done */
  marked_as_done_task_ids: number[] = [];
  /** marked_as_not_done_task_ids: vector<int32> — Identifiers of tasks that were marked as not done */
  marked_as_not_done_task_ids: number[] = [];
}

/** Decode a TDLib `messageChecklistTasksDone` object; null input -> null. */
export function decodeMessageChecklistTasksDone(json: TdJsonRaw): MessageChecklistTasksDone | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageChecklistTasksDone = new MessageChecklistTasksDone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.checklist_message_id = asNumber(record['checklist_message_id']);
  out.marked_as_done_task_ids = decodeVector(record['marked_as_done_task_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.marked_as_not_done_task_ids = decodeVector(record['marked_as_not_done_task_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `messageChecklistTasksDone` into TDLib JSON wire shape. */
export function encodeMessageChecklistTasksDone(value: MessageChecklistTasksDone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageChecklistTasksDone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['checklist_message_id'] = value.checklist_message_id;
  out['marked_as_done_task_ids'] = encodeVector(value.marked_as_done_task_ids, (item: number): TdJsonRaw => item);
  out['marked_as_not_done_task_ids'] = encodeVector(value.marked_as_not_done_task_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** messageContact = MessageContent — A message with a user contact */
export class MessageContact {
  type: 'messageContact' = 'messageContact';
  extra?: string;
  /** contact: contact — The contact description */
  contact: Contact | null = null;
}

/** Decode a TDLib `messageContact` object; null input -> null. */
export function decodeMessageContact(json: TdJsonRaw): MessageContact | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageContact = new MessageContact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.contact = decodeContact(record['contact']);
  return out;
}

/** Encode a `messageContact` into TDLib JSON wire shape. */
export function encodeMessageContact(value: MessageContact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageContact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['contact'] = value.contact === null ? null : encodeContact(value.contact);
  return out;
}

/** messageContactRegistered = MessageContent — A contact has registered with Telegram */
export class MessageContactRegistered {
  type: 'messageContactRegistered' = 'messageContactRegistered';
  extra?: string;
}

/** Decode a TDLib `messageContactRegistered` object; null input -> null. */
export function decodeMessageContactRegistered(json: TdJsonRaw): MessageContactRegistered | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageContactRegistered = new MessageContactRegistered();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageContactRegistered` into TDLib JSON wire shape. */
export function encodeMessageContactRegistered(value: MessageContactRegistered): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageContactRegistered';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageCopyOptions = MessageCopyOptions — Options to be used when a message content is copied without reference to the original sender. Service messages, messages with messageInvoice, messagePaidMedia, messageGiveaway, or messageGiveaway... */
export class MessageCopyOptions {
  type: 'messageCopyOptions' = 'messageCopyOptions';
  extra?: string;
  /** send_copy: Bool — True, if content of the message needs to be copied without reference to the original sender. Always true if the message is forwarded to a secret chat or is local. */
  send_copy: boolean = false;
  /** replace_caption: Bool — True, if media caption of the message copy needs to be replaced. Ignored if send_copy is false */
  replace_caption: boolean = false;
  /** new_caption: formattedText — New message caption; pass null to copy message without caption. Ignored if replace_caption is false */
  new_caption: FormattedText | null = null;
  /** new_show_caption_above_media: Bool — True, if new caption must be shown above the media; otherwise, new caption must be shown below the media; not supported in secret chats. Ignored if replace_caption is false */
  new_show_caption_above_media: boolean = false;
}

/** Decode a TDLib `messageCopyOptions` object; null input -> null. */
export function decodeMessageCopyOptions(json: TdJsonRaw): MessageCopyOptions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageCopyOptions = new MessageCopyOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.send_copy = asBool(record['send_copy']);
  out.replace_caption = asBool(record['replace_caption']);
  out.new_caption = decodeFormattedText(record['new_caption']);
  out.new_show_caption_above_media = asBool(record['new_show_caption_above_media']);
  return out;
}

/** Encode a `messageCopyOptions` into TDLib JSON wire shape. */
export function encodeMessageCopyOptions(value: MessageCopyOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageCopyOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['send_copy'] = value.send_copy;
  out['replace_caption'] = value.replace_caption;
  out['new_caption'] = value.new_caption === null ? null : encodeFormattedText(value.new_caption);
  out['new_show_caption_above_media'] = value.new_show_caption_above_media;
  return out;
}

/** messageCustomServiceAction = MessageContent — A non-standard action has happened in the chat */
export class MessageCustomServiceAction {
  type: 'messageCustomServiceAction' = 'messageCustomServiceAction';
  extra?: string;
  /** text: string — Message text to be shown in the chat */
  text: string = '';
}

/** Decode a TDLib `messageCustomServiceAction` object; null input -> null. */
export function decodeMessageCustomServiceAction(json: TdJsonRaw): MessageCustomServiceAction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageCustomServiceAction = new MessageCustomServiceAction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  return out;
}

/** Encode a `messageCustomServiceAction` into TDLib JSON wire shape. */
export function encodeMessageCustomServiceAction(value: MessageCustomServiceAction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageCustomServiceAction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  return out;
}

/** messageDice = MessageContent — A dice message. The dice value is randomly generated by the server */
export class MessageDice {
  type: 'messageDice' = 'messageDice';
  extra?: string;
  /** initial_state: DiceStickers — The animated stickers with the initial dice animation; may be null if unknown. The update updateMessageContent will be sent when the sticker became known */
  initial_state: TdDiceStickers | null = null;
  /** final_state: DiceStickers — The animated stickers with the final dice animation; may be null if unknown. The update updateMessageContent will be sent when the sticker became known */
  final_state: TdDiceStickers | null = null;
  /** emoji: string — Emoji on which the dice throw animation is based */
  emoji: string = '';
  /** value: int32 — The dice value. If the value is 0, then the dice don't have final state yet */
  value: number = 0;
  /** success_animation_frame_number: int32 — Number of frame after which a success animation like a shower of confetti needs to be shown on updateMessageSendSucceeded */
  success_animation_frame_number: number = 0;
}

/** Decode a TDLib `messageDice` object; null input -> null. */
export function decodeMessageDice(json: TdJsonRaw): MessageDice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageDice = new MessageDice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.initial_state = decodeTdDiceStickers(record['initial_state']);
  out.final_state = decodeTdDiceStickers(record['final_state']);
  out.emoji = asString(record['emoji']);
  out.value = asNumber(record['value']);
  out.success_animation_frame_number = asNumber(record['success_animation_frame_number']);
  return out;
}

/** Encode a `messageDice` into TDLib JSON wire shape. */
export function encodeMessageDice(value: MessageDice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageDice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['initial_state'] = value.initial_state === null ? null : encodeTdDiceStickers(value.initial_state);
  out['final_state'] = value.final_state === null ? null : encodeTdDiceStickers(value.final_state);
  out['emoji'] = value.emoji;
  out['value'] = value.value;
  out['success_animation_frame_number'] = value.success_animation_frame_number;
  return out;
}

/** messageDirectMessagePriceChanged = MessageContent — A price for direct messages was changed in the channel chat 0 if the direct messages group was disabled or the messages are free */
export class MessageDirectMessagePriceChanged {
  type: 'messageDirectMessagePriceChanged' = 'messageDirectMessagePriceChanged';
  extra?: string;
  /** is_enabled: Bool — True, if direct messages group was enabled for the channel; false otherwise */
  is_enabled: boolean = false;
  /** paid_message_star_count: int53 — The new number of Telegram Stars that must be paid by non-administrator users of the channel chat for each message sent to the direct messages group; */
  paid_message_star_count: number = 0;
}

/** Decode a TDLib `messageDirectMessagePriceChanged` object; null input -> null. */
export function decodeMessageDirectMessagePriceChanged(json: TdJsonRaw): MessageDirectMessagePriceChanged | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageDirectMessagePriceChanged = new MessageDirectMessagePriceChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_enabled = asBool(record['is_enabled']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  return out;
}

/** Encode a `messageDirectMessagePriceChanged` into TDLib JSON wire shape. */
export function encodeMessageDirectMessagePriceChanged(value: MessageDirectMessagePriceChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageDirectMessagePriceChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_enabled'] = value.is_enabled;
  out['paid_message_star_count'] = value.paid_message_star_count;
  return out;
}

/** messageDocument = MessageContent — A document message (general file) */
export class MessageDocument {
  type: 'messageDocument' = 'messageDocument';
  extra?: string;
  /** document: document — The document description */
  document: Document | null = null;
  /** caption: formattedText — Document caption */
  caption: FormattedText | null = null;
}

/** Decode a TDLib `messageDocument` object; null input -> null. */
export function decodeMessageDocument(json: TdJsonRaw): MessageDocument | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageDocument = new MessageDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeDocument(record['document']);
  out.caption = decodeFormattedText(record['caption']);
  return out;
}

/** Encode a `messageDocument` into TDLib JSON wire shape. */
export function encodeMessageDocument(value: MessageDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeDocument(value.document);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  return out;
}

/** messageEffect = MessageEffect — Contains information about an effect added to a message */
export class MessageEffect {
  type: 'messageEffect' = 'messageEffect';
  extra?: string;
  /** id: int64 — Unique identifier of the effect */
  id: string = '';
  /** static_icon: sticker — Static icon for the effect in WEBP format; may be null if none */
  static_icon: Sticker | null = null;
  /** emoji: string — Emoji corresponding to the effect that can be used if static icon isn't available */
  emoji: string = '';
  /** is_premium: Bool — True, if Telegram Premium subscription is required to use the effect */
  is_premium: boolean = false;
  /** type: MessageEffectType — Type of the effect */
  type_: TdMessageEffectType | null = null;
}

/** Decode a TDLib `messageEffect` object; null input -> null. */
export function decodeMessageEffect(json: TdJsonRaw): MessageEffect | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageEffect = new MessageEffect();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.static_icon = decodeSticker(record['static_icon']);
  out.emoji = asString(record['emoji']);
  out.is_premium = asBool(record['is_premium']);
  out.type_ = decodeTdMessageEffectType(record['type']);
  return out;
}

/** Encode a `messageEffect` into TDLib JSON wire shape. */
export function encodeMessageEffect(value: MessageEffect): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageEffect';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['static_icon'] = value.static_icon === null ? null : encodeSticker(value.static_icon);
  out['emoji'] = value.emoji;
  out['is_premium'] = value.is_premium;
  out['type'] = value.type_ === null ? null : encodeTdMessageEffectType(value.type_);
  return out;
}

/** messageEffectTypeEmojiReaction = MessageEffectType — An effect from an emoji reaction */
export class MessageEffectTypeEmojiReaction {
  type: 'messageEffectTypeEmojiReaction' = 'messageEffectTypeEmojiReaction';
  extra?: string;
  /** select_animation: sticker — Select animation for the effect in TGS format */
  select_animation: Sticker | null = null;
  /** effect_animation: sticker — Effect animation for the effect in TGS format */
  effect_animation: Sticker | null = null;
}

/** Decode a TDLib `messageEffectTypeEmojiReaction` object; null input -> null. */
export function decodeMessageEffectTypeEmojiReaction(json: TdJsonRaw): MessageEffectTypeEmojiReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageEffectTypeEmojiReaction = new MessageEffectTypeEmojiReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.select_animation = decodeSticker(record['select_animation']);
  out.effect_animation = decodeSticker(record['effect_animation']);
  return out;
}

/** Encode a `messageEffectTypeEmojiReaction` into TDLib JSON wire shape. */
export function encodeMessageEffectTypeEmojiReaction(value: MessageEffectTypeEmojiReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageEffectTypeEmojiReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['select_animation'] = value.select_animation === null ? null : encodeSticker(value.select_animation);
  out['effect_animation'] = value.effect_animation === null ? null : encodeSticker(value.effect_animation);
  return out;
}

/** messageEffectTypePremiumSticker = MessageEffectType — An effect from a premium sticker */
export class MessageEffectTypePremiumSticker {
  type: 'messageEffectTypePremiumSticker' = 'messageEffectTypePremiumSticker';
  extra?: string;
  /** sticker: sticker — The premium sticker. The effect can be found at sticker.full_type.premium_animation */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `messageEffectTypePremiumSticker` object; null input -> null. */
export function decodeMessageEffectTypePremiumSticker(json: TdJsonRaw): MessageEffectTypePremiumSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageEffectTypePremiumSticker = new MessageEffectTypePremiumSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `messageEffectTypePremiumSticker` into TDLib JSON wire shape. */
export function encodeMessageEffectTypePremiumSticker(value: MessageEffectTypePremiumSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageEffectTypePremiumSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** messageExpiredPhoto = MessageContent — A self-destructed photo message */
export class MessageExpiredPhoto {
  type: 'messageExpiredPhoto' = 'messageExpiredPhoto';
  extra?: string;
}

/** Decode a TDLib `messageExpiredPhoto` object; null input -> null. */
export function decodeMessageExpiredPhoto(json: TdJsonRaw): MessageExpiredPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageExpiredPhoto = new MessageExpiredPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageExpiredPhoto` into TDLib JSON wire shape. */
export function encodeMessageExpiredPhoto(value: MessageExpiredPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageExpiredPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageExpiredVideo = MessageContent — A self-destructed video message */
export class MessageExpiredVideo {
  type: 'messageExpiredVideo' = 'messageExpiredVideo';
  extra?: string;
}

/** Decode a TDLib `messageExpiredVideo` object; null input -> null. */
export function decodeMessageExpiredVideo(json: TdJsonRaw): MessageExpiredVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageExpiredVideo = new MessageExpiredVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageExpiredVideo` into TDLib JSON wire shape. */
export function encodeMessageExpiredVideo(value: MessageExpiredVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageExpiredVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageExpiredVideoNote = MessageContent — A self-destructed video note message */
export class MessageExpiredVideoNote {
  type: 'messageExpiredVideoNote' = 'messageExpiredVideoNote';
  extra?: string;
}

/** Decode a TDLib `messageExpiredVideoNote` object; null input -> null. */
export function decodeMessageExpiredVideoNote(json: TdJsonRaw): MessageExpiredVideoNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageExpiredVideoNote = new MessageExpiredVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageExpiredVideoNote` into TDLib JSON wire shape. */
export function encodeMessageExpiredVideoNote(value: MessageExpiredVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageExpiredVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageExpiredVoiceNote = MessageContent — A self-destructed voice note message */
export class MessageExpiredVoiceNote {
  type: 'messageExpiredVoiceNote' = 'messageExpiredVoiceNote';
  extra?: string;
}

/** Decode a TDLib `messageExpiredVoiceNote` object; null input -> null. */
export function decodeMessageExpiredVoiceNote(json: TdJsonRaw): MessageExpiredVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageExpiredVoiceNote = new MessageExpiredVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageExpiredVoiceNote` into TDLib JSON wire shape. */
export function encodeMessageExpiredVoiceNote(value: MessageExpiredVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageExpiredVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageFileTypeGroup = MessageFileType — The messages were exported from a group chat */
export class MessageFileTypeGroup {
  type: 'messageFileTypeGroup' = 'messageFileTypeGroup';
  extra?: string;
  /** title: string — Title of the group chat; may be empty if unrecognized */
  title: string = '';
}

/** Decode a TDLib `messageFileTypeGroup` object; null input -> null. */
export function decodeMessageFileTypeGroup(json: TdJsonRaw): MessageFileTypeGroup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageFileTypeGroup = new MessageFileTypeGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  return out;
}

/** Encode a `messageFileTypeGroup` into TDLib JSON wire shape. */
export function encodeMessageFileTypeGroup(value: MessageFileTypeGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageFileTypeGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  return out;
}

/** messageFileTypePrivate = MessageFileType — The messages were exported from a private chat */
export class MessageFileTypePrivate {
  type: 'messageFileTypePrivate' = 'messageFileTypePrivate';
  extra?: string;
  /** name: string — Name of the other party; may be empty if unrecognized */
  name: string = '';
}

/** Decode a TDLib `messageFileTypePrivate` object; null input -> null. */
export function decodeMessageFileTypePrivate(json: TdJsonRaw): MessageFileTypePrivate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageFileTypePrivate = new MessageFileTypePrivate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `messageFileTypePrivate` into TDLib JSON wire shape. */
export function encodeMessageFileTypePrivate(value: MessageFileTypePrivate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageFileTypePrivate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** messageFileTypeUnknown = MessageFileType — The messages were exported from a chat of unknown type */
export class MessageFileTypeUnknown {
  type: 'messageFileTypeUnknown' = 'messageFileTypeUnknown';
  extra?: string;
}

/** Decode a TDLib `messageFileTypeUnknown` object; null input -> null. */
export function decodeMessageFileTypeUnknown(json: TdJsonRaw): MessageFileTypeUnknown | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageFileTypeUnknown = new MessageFileTypeUnknown();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageFileTypeUnknown` into TDLib JSON wire shape. */
export function encodeMessageFileTypeUnknown(value: MessageFileTypeUnknown): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageFileTypeUnknown';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageForumTopicCreated = MessageContent — A forum topic has been created */
export class MessageForumTopicCreated {
  type: 'messageForumTopicCreated' = 'messageForumTopicCreated';
  extra?: string;
  /** name: string — Name of the topic */
  name: string = '';
  /** is_name_implicit: Bool — True, if the name of the topic wasn't added explicitly */
  is_name_implicit: boolean = false;
  /** icon: forumTopicIcon — Icon of the topic */
  icon: ForumTopicIcon | null = null;
}

/** Decode a TDLib `messageForumTopicCreated` object; null input -> null. */
export function decodeMessageForumTopicCreated(json: TdJsonRaw): MessageForumTopicCreated | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageForumTopicCreated = new MessageForumTopicCreated();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.is_name_implicit = asBool(record['is_name_implicit']);
  out.icon = decodeForumTopicIcon(record['icon']);
  return out;
}

/** Encode a `messageForumTopicCreated` into TDLib JSON wire shape. */
export function encodeMessageForumTopicCreated(value: MessageForumTopicCreated): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageForumTopicCreated';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['is_name_implicit'] = value.is_name_implicit;
  out['icon'] = value.icon === null ? null : encodeForumTopicIcon(value.icon);
  return out;
}

/** messageForumTopicEdited = MessageContent — A forum topic has been edited */
export class MessageForumTopicEdited {
  type: 'messageForumTopicEdited' = 'messageForumTopicEdited';
  extra?: string;
  /** name: string — If non-empty, the new name of the topic */
  name: string = '';
  /** edit_icon_custom_emoji_id: Bool — True, if icon's custom_emoji_id is changed */
  edit_icon_custom_emoji_id: boolean = false;
  /** icon_custom_emoji_id: int64 — New unique identifier of the custom emoji shown on the topic icon; 0 if none. Must be ignored if edit_icon_custom_emoji_id is false */
  icon_custom_emoji_id: string = '';
}

/** Decode a TDLib `messageForumTopicEdited` object; null input -> null. */
export function decodeMessageForumTopicEdited(json: TdJsonRaw): MessageForumTopicEdited | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageForumTopicEdited = new MessageForumTopicEdited();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.edit_icon_custom_emoji_id = asBool(record['edit_icon_custom_emoji_id']);
  out.icon_custom_emoji_id = asInt64(record['icon_custom_emoji_id']);
  return out;
}

/** Encode a `messageForumTopicEdited` into TDLib JSON wire shape. */
export function encodeMessageForumTopicEdited(value: MessageForumTopicEdited): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageForumTopicEdited';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['edit_icon_custom_emoji_id'] = value.edit_icon_custom_emoji_id;
  out['icon_custom_emoji_id'] = value.icon_custom_emoji_id;
  return out;
}

/** messageForumTopicIsClosedToggled = MessageContent — A forum topic has been closed or opened */
export class MessageForumTopicIsClosedToggled {
  type: 'messageForumTopicIsClosedToggled' = 'messageForumTopicIsClosedToggled';
  extra?: string;
  /** is_closed: Bool — True, if the topic was closed; otherwise, the topic was reopened */
  is_closed: boolean = false;
}

/** Decode a TDLib `messageForumTopicIsClosedToggled` object; null input -> null. */
export function decodeMessageForumTopicIsClosedToggled(json: TdJsonRaw): MessageForumTopicIsClosedToggled | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageForumTopicIsClosedToggled = new MessageForumTopicIsClosedToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_closed = asBool(record['is_closed']);
  return out;
}

/** Encode a `messageForumTopicIsClosedToggled` into TDLib JSON wire shape. */
export function encodeMessageForumTopicIsClosedToggled(value: MessageForumTopicIsClosedToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageForumTopicIsClosedToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_closed'] = value.is_closed;
  return out;
}

/** messageForumTopicIsHiddenToggled = MessageContent — A General forum topic has been hidden or unhidden */
export class MessageForumTopicIsHiddenToggled {
  type: 'messageForumTopicIsHiddenToggled' = 'messageForumTopicIsHiddenToggled';
  extra?: string;
  /** is_hidden: Bool — True, if the topic was hidden; otherwise, the topic was unhidden */
  is_hidden: boolean = false;
}

/** Decode a TDLib `messageForumTopicIsHiddenToggled` object; null input -> null. */
export function decodeMessageForumTopicIsHiddenToggled(json: TdJsonRaw): MessageForumTopicIsHiddenToggled | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageForumTopicIsHiddenToggled = new MessageForumTopicIsHiddenToggled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_hidden = asBool(record['is_hidden']);
  return out;
}

/** Encode a `messageForumTopicIsHiddenToggled` into TDLib JSON wire shape. */
export function encodeMessageForumTopicIsHiddenToggled(value: MessageForumTopicIsHiddenToggled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageForumTopicIsHiddenToggled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_hidden'] = value.is_hidden;
  return out;
}

/** messageForwardInfo = MessageForwardInfo — Contains information about a forwarded message */
export class MessageForwardInfo {
  type: 'messageForwardInfo' = 'messageForwardInfo';
  extra?: string;
  /** origin: MessageOrigin — Origin of the forwarded message */
  origin: TdMessageOrigin | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the message was originally sent */
  date: number = 0;
  /** source: forwardSource — For messages forwarded to the chat with the current user (Saved Messages), to the Replies bot chat, or to the channel's discussion group, information about the source message from which the message was forwarded l... */
  source: ForwardSource | null = null;
  /** public_service_announcement_type: string — The type of public service announcement for the forwarded message */
  public_service_announcement_type: string = '';
}

/** Decode a TDLib `messageForwardInfo` object; null input -> null. */
export function decodeMessageForwardInfo(json: TdJsonRaw): MessageForwardInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageForwardInfo = new MessageForwardInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.origin = decodeTdMessageOrigin(record['origin']);
  out.date = asNumber(record['date']);
  out.source = decodeForwardSource(record['source']);
  out.public_service_announcement_type = asString(record['public_service_announcement_type']);
  return out;
}

/** Encode a `messageForwardInfo` into TDLib JSON wire shape. */
export function encodeMessageForwardInfo(value: MessageForwardInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageForwardInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['origin'] = value.origin === null ? null : encodeTdMessageOrigin(value.origin);
  out['date'] = value.date;
  out['source'] = value.source === null ? null : encodeForwardSource(value.source);
  out['public_service_announcement_type'] = value.public_service_announcement_type;
  return out;
}

/** messageGame = MessageContent — A message with a game */
export class MessageGame {
  type: 'messageGame' = 'messageGame';
  extra?: string;
  /** game: game — The game description */
  game: Game | null = null;
}

/** Decode a TDLib `messageGame` object; null input -> null. */
export function decodeMessageGame(json: TdJsonRaw): MessageGame | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGame = new MessageGame();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.game = decodeGame(record['game']);
  return out;
}

/** Encode a `messageGame` into TDLib JSON wire shape. */
export function encodeMessageGame(value: MessageGame): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGame';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['game'] = value.game === null ? null : encodeGame(value.game);
  return out;
}

/** messageGameScore = MessageContent — A new high score was achieved in a game */
export class MessageGameScore {
  type: 'messageGameScore' = 'messageGameScore';
  extra?: string;
  /** game_message_id: int53 — Identifier of the message with the game, can be an identifier of a deleted message */
  game_message_id: number = 0;
  /** game_id: int64 — Identifier of the game; may be different from the games presented in the message with the game */
  game_id: string = '';
  /** score: int32 — New score */
  score: number = 0;
}

/** Decode a TDLib `messageGameScore` object; null input -> null. */
export function decodeMessageGameScore(json: TdJsonRaw): MessageGameScore | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGameScore = new MessageGameScore();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.game_message_id = asNumber(record['game_message_id']);
  out.game_id = asInt64(record['game_id']);
  out.score = asNumber(record['score']);
  return out;
}

/** Encode a `messageGameScore` into TDLib JSON wire shape. */
export function encodeMessageGameScore(value: MessageGameScore): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGameScore';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['game_message_id'] = value.game_message_id;
  out['game_id'] = value.game_id;
  out['score'] = value.score;
  return out;
}

/** messageGift = MessageContent — A regular gift was received or sent by the current user, or the current user was notified about a channel gift */
export class MessageGift {
  type: 'messageGift' = 'messageGift';
  extra?: string;
  /** gift: gift — The gift */
  gift: Gift | null = null;
  /** sender_id: MessageSender — Sender of the gift; may be null for outgoing messages about prepaid upgrade of gifts from unknown users */
  sender_id: TdMessageSender | null = null;
  /** receiver_id: MessageSender — Receiver of the gift */
  receiver_id: TdMessageSender | null = null;
  /** received_gift_id: string — Unique identifier of the received gift for the current user; only for the receiver of the gift */
  received_gift_id: string = '';
  /** text: formattedText — Message added to the gift */
  text: FormattedText | null = null;
  /** unique_gift_number: int32 — Unique number of the gift among gifts upgraded from the same gift after upgrade; 0 if yet unassigned */
  unique_gift_number: number = 0;
  /** sell_star_count: int53 — Number of Telegram Stars that can be claimed by the receiver instead of the regular gift; 0 if the gift can't be sold by the receiver */
  sell_star_count: number = 0;
  /** prepaid_upgrade_star_count: int53 — Number of Telegram Stars that were paid by the sender for the ability to upgrade the gift */
  prepaid_upgrade_star_count: number = 0;
  /** is_upgrade_separate: Bool — True, if the upgrade was bought after the gift was sent. In this case, prepaid upgrade cost must not be added to the gift cost */
  is_upgrade_separate: boolean = false;
  /** is_from_auction: Bool — True, if the message is a notification about a gift won on an auction */
  is_from_auction: boolean = false;
  /** is_private: Bool — True, if the sender and gift text are shown only to the gift receiver; otherwise, everyone will be able to see them */
  is_private: boolean = false;
  /** is_saved: Bool — True, if the gift is displayed on the user's or the channel's profile page; only for the receiver of the gift */
  is_saved: boolean = false;
  /** is_prepaid_upgrade: Bool — True, if the message is about prepaid upgrade of the gift by another user */
  is_prepaid_upgrade: boolean = false;
  /** can_be_upgraded: Bool — True, if the gift can be upgraded to a unique gift; only for the receiver of the gift */
  can_be_upgraded: boolean = false;
  /** was_converted: Bool — True, if the gift was converted to Telegram Stars; only for the receiver of the gift */
  was_converted: boolean = false;
  /** was_upgraded: Bool — True, if the gift was upgraded to a unique gift */
  was_upgraded: boolean = false;
  /** was_refunded: Bool — True, if the gift was refunded and isn't available anymore */
  was_refunded: boolean = false;
  /** upgraded_received_gift_id: string — Identifier of the corresponding upgraded gift; may be empty if unknown. Use getReceivedGift to get information about the gift */
  upgraded_received_gift_id: string = '';
  /** prepaid_upgrade_hash: string — If non-empty, then the user can pay for an upgrade of the gift using buyGiftUpgrade */
  prepaid_upgrade_hash: string = '';
}

/** Decode a TDLib `messageGift` object; null input -> null. */
export function decodeMessageGift(json: TdJsonRaw): MessageGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGift = new MessageGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeGift(record['gift']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.receiver_id = decodeTdMessageSender(record['receiver_id']);
  out.received_gift_id = asString(record['received_gift_id']);
  out.text = decodeFormattedText(record['text']);
  out.unique_gift_number = asNumber(record['unique_gift_number']);
  out.sell_star_count = asNumber(record['sell_star_count']);
  out.prepaid_upgrade_star_count = asNumber(record['prepaid_upgrade_star_count']);
  out.is_upgrade_separate = asBool(record['is_upgrade_separate']);
  out.is_from_auction = asBool(record['is_from_auction']);
  out.is_private = asBool(record['is_private']);
  out.is_saved = asBool(record['is_saved']);
  out.is_prepaid_upgrade = asBool(record['is_prepaid_upgrade']);
  out.can_be_upgraded = asBool(record['can_be_upgraded']);
  out.was_converted = asBool(record['was_converted']);
  out.was_upgraded = asBool(record['was_upgraded']);
  out.was_refunded = asBool(record['was_refunded']);
  out.upgraded_received_gift_id = asString(record['upgraded_received_gift_id']);
  out.prepaid_upgrade_hash = asString(record['prepaid_upgrade_hash']);
  return out;
}

/** Encode a `messageGift` into TDLib JSON wire shape. */
export function encodeMessageGift(value: MessageGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['receiver_id'] = value.receiver_id === null ? null : encodeTdMessageSender(value.receiver_id);
  out['received_gift_id'] = value.received_gift_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['unique_gift_number'] = value.unique_gift_number;
  out['sell_star_count'] = value.sell_star_count;
  out['prepaid_upgrade_star_count'] = value.prepaid_upgrade_star_count;
  out['is_upgrade_separate'] = value.is_upgrade_separate;
  out['is_from_auction'] = value.is_from_auction;
  out['is_private'] = value.is_private;
  out['is_saved'] = value.is_saved;
  out['is_prepaid_upgrade'] = value.is_prepaid_upgrade;
  out['can_be_upgraded'] = value.can_be_upgraded;
  out['was_converted'] = value.was_converted;
  out['was_upgraded'] = value.was_upgraded;
  out['was_refunded'] = value.was_refunded;
  out['upgraded_received_gift_id'] = value.upgraded_received_gift_id;
  out['prepaid_upgrade_hash'] = value.prepaid_upgrade_hash;
  return out;
}

/** messageGiftedGrams = MessageContent — TON Grams were gifted to a user */
export class MessageGiftedGrams {
  type: 'messageGiftedGrams' = 'messageGiftedGrams';
  extra?: string;
  /** gifter_user_id: int53 — The identifier of a user who gifted Grams; 0 if the gift was anonymous or is outgoing */
  gifter_user_id: number = 0;
  /** receiver_user_id: int53 — The identifier of a user who received Grams; 0 if the gift is incoming */
  receiver_user_id: number = 0;
  /** gram_amount: int53 — The received Gram amount, in the smallest units of the cryptocurrency */
  gram_amount: number = 0;
  /** transaction_id: string — Identifier of the transaction for Gram credit; for receiver only */
  transaction_id: string = '';
  /** sticker: sticker — A sticker to be shown in the message; may be null if unknown */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `messageGiftedGrams` object; null input -> null. */
export function decodeMessageGiftedGrams(json: TdJsonRaw): MessageGiftedGrams | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGiftedGrams = new MessageGiftedGrams();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gifter_user_id = asNumber(record['gifter_user_id']);
  out.receiver_user_id = asNumber(record['receiver_user_id']);
  out.gram_amount = asNumber(record['gram_amount']);
  out.transaction_id = asString(record['transaction_id']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `messageGiftedGrams` into TDLib JSON wire shape. */
export function encodeMessageGiftedGrams(value: MessageGiftedGrams): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGiftedGrams';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gifter_user_id'] = value.gifter_user_id;
  out['receiver_user_id'] = value.receiver_user_id;
  out['gram_amount'] = value.gram_amount;
  out['transaction_id'] = value.transaction_id;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** messageGiftedPremium = MessageContent — Telegram Premium was gifted to a user */
export class MessageGiftedPremium {
  type: 'messageGiftedPremium' = 'messageGiftedPremium';
  extra?: string;
  /** gifter_user_id: int53 — The identifier of a user who gifted Telegram Premium; 0 if the gift was anonymous or is outgoing */
  gifter_user_id: number = 0;
  /** receiver_user_id: int53 — The identifier of a user who received Telegram Premium; 0 if the gift is incoming */
  receiver_user_id: number = 0;
  /** text: formattedText — Message added to the gifted Telegram Premium by the sender */
  text: FormattedText | null = null;
  /** currency: string — Currency for the paid amount */
  currency: string = '';
  /** amount: int53 — The paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** cryptocurrency: string — Cryptocurrency used to pay for the gift; may be empty if none */
  cryptocurrency: string = '';
  /** cryptocurrency_amount: int64 — The paid amount, in the smallest units of the cryptocurrency; 0 if none */
  cryptocurrency_amount: string = '';
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active after code activation; 0 if the number of months isn't integer */
  month_count: number = 0;
  /** day_count: int32 — Number of days the Telegram Premium subscription will be active */
  day_count: number = 0;
  /** sticker: sticker — A sticker to be shown in the message; may be null if unknown */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `messageGiftedPremium` object; null input -> null. */
export function decodeMessageGiftedPremium(json: TdJsonRaw): MessageGiftedPremium | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGiftedPremium = new MessageGiftedPremium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gifter_user_id = asNumber(record['gifter_user_id']);
  out.receiver_user_id = asNumber(record['receiver_user_id']);
  out.text = decodeFormattedText(record['text']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.cryptocurrency = asString(record['cryptocurrency']);
  out.cryptocurrency_amount = asInt64(record['cryptocurrency_amount']);
  out.month_count = asNumber(record['month_count']);
  out.day_count = asNumber(record['day_count']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `messageGiftedPremium` into TDLib JSON wire shape. */
export function encodeMessageGiftedPremium(value: MessageGiftedPremium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGiftedPremium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gifter_user_id'] = value.gifter_user_id;
  out['receiver_user_id'] = value.receiver_user_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['cryptocurrency'] = value.cryptocurrency;
  out['cryptocurrency_amount'] = value.cryptocurrency_amount;
  out['month_count'] = value.month_count;
  out['day_count'] = value.day_count;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** messageGiftedStars = MessageContent — Telegram Stars were gifted to a user */
export class MessageGiftedStars {
  type: 'messageGiftedStars' = 'messageGiftedStars';
  extra?: string;
  /** gifter_user_id: int53 — The identifier of a user who gifted Telegram Stars; 0 if the gift was anonymous or is outgoing */
  gifter_user_id: number = 0;
  /** receiver_user_id: int53 — The identifier of a user who received Telegram Stars; 0 if the gift is incoming */
  receiver_user_id: number = 0;
  /** currency: string — Currency for the paid amount */
  currency: string = '';
  /** amount: int53 — The paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** cryptocurrency: string — Cryptocurrency used to pay for the gift; may be empty if none */
  cryptocurrency: string = '';
  /** cryptocurrency_amount: int64 — The paid amount, in the smallest units of the cryptocurrency; 0 if none */
  cryptocurrency_amount: string = '';
  /** star_count: int53 — Number of Telegram Stars that were gifted */
  star_count: number = 0;
  /** transaction_id: string — Identifier of the transaction for Telegram Stars purchase; for receiver only */
  transaction_id: string = '';
  /** sticker: sticker — A sticker to be shown in the message; may be null if unknown */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `messageGiftedStars` object; null input -> null. */
export function decodeMessageGiftedStars(json: TdJsonRaw): MessageGiftedStars | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGiftedStars = new MessageGiftedStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gifter_user_id = asNumber(record['gifter_user_id']);
  out.receiver_user_id = asNumber(record['receiver_user_id']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.cryptocurrency = asString(record['cryptocurrency']);
  out.cryptocurrency_amount = asInt64(record['cryptocurrency_amount']);
  out.star_count = asNumber(record['star_count']);
  out.transaction_id = asString(record['transaction_id']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `messageGiftedStars` into TDLib JSON wire shape. */
export function encodeMessageGiftedStars(value: MessageGiftedStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGiftedStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gifter_user_id'] = value.gifter_user_id;
  out['receiver_user_id'] = value.receiver_user_id;
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['cryptocurrency'] = value.cryptocurrency;
  out['cryptocurrency_amount'] = value.cryptocurrency_amount;
  out['star_count'] = value.star_count;
  out['transaction_id'] = value.transaction_id;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** messageGiveaway = MessageContent — A giveaway */
export class MessageGiveaway {
  type: 'messageGiveaway' = 'messageGiveaway';
  extra?: string;
  /** parameters: giveawayParameters — Giveaway parameters */
  parameters: GiveawayParameters | null = null;
  /** winner_count: int32 — Number of users who will receive Telegram Premium subscription gift codes */
  winner_count: number = 0;
  /** prize: GiveawayPrize — Prize of the giveaway */
  prize: TdGiveawayPrize | null = null;
  /** sticker: sticker — A sticker to be shown in the message; may be null if unknown */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `messageGiveaway` object; null input -> null. */
export function decodeMessageGiveaway(json: TdJsonRaw): MessageGiveaway | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGiveaway = new MessageGiveaway();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.parameters = decodeGiveawayParameters(record['parameters']);
  out.winner_count = asNumber(record['winner_count']);
  out.prize = decodeTdGiveawayPrize(record['prize']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `messageGiveaway` into TDLib JSON wire shape. */
export function encodeMessageGiveaway(value: MessageGiveaway): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGiveaway';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['parameters'] = value.parameters === null ? null : encodeGiveawayParameters(value.parameters);
  out['winner_count'] = value.winner_count;
  out['prize'] = value.prize === null ? null : encodeTdGiveawayPrize(value.prize);
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** messageGiveawayCompleted = MessageContent — A giveaway without public winners has been completed for the chat */
export class MessageGiveawayCompleted {
  type: 'messageGiveawayCompleted' = 'messageGiveawayCompleted';
  extra?: string;
  /** giveaway_message_id: int53 — Identifier of the message with the giveaway; may be 0 or an identifier of a deleted message */
  giveaway_message_id: number = 0;
  /** winner_count: int32 — Number of winners in the giveaway */
  winner_count: number = 0;
  /** is_star_giveaway: Bool — True, if the giveaway is a Telegram Star giveaway */
  is_star_giveaway: boolean = false;
  /** unclaimed_prize_count: int32 — Number of undistributed prizes; for Telegram Premium giveaways only */
  unclaimed_prize_count: number = 0;
}

/** Decode a TDLib `messageGiveawayCompleted` object; null input -> null. */
export function decodeMessageGiveawayCompleted(json: TdJsonRaw): MessageGiveawayCompleted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGiveawayCompleted = new MessageGiveawayCompleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.giveaway_message_id = asNumber(record['giveaway_message_id']);
  out.winner_count = asNumber(record['winner_count']);
  out.is_star_giveaway = asBool(record['is_star_giveaway']);
  out.unclaimed_prize_count = asNumber(record['unclaimed_prize_count']);
  return out;
}

/** Encode a `messageGiveawayCompleted` into TDLib JSON wire shape. */
export function encodeMessageGiveawayCompleted(value: MessageGiveawayCompleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGiveawayCompleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['giveaway_message_id'] = value.giveaway_message_id;
  out['winner_count'] = value.winner_count;
  out['is_star_giveaway'] = value.is_star_giveaway;
  out['unclaimed_prize_count'] = value.unclaimed_prize_count;
  return out;
}

/** messageGiveawayCreated = MessageContent — A giveaway was created for the chat. Use telegramPaymentPurposePremiumGiveaway, storePaymentPurposePremiumGiveaway, telegramPaymentPurposeStarGiveaway, or storePaymentPurposeStarGiveaway to creat... */
export class MessageGiveawayCreated {
  type: 'messageGiveawayCreated' = 'messageGiveawayCreated';
  extra?: string;
  /** star_count: int53 — Number of Telegram Stars that will be shared by winners of the giveaway; 0 for Telegram Premium giveaways */
  star_count: number = 0;
}

/** Decode a TDLib `messageGiveawayCreated` object; null input -> null. */
export function decodeMessageGiveawayCreated(json: TdJsonRaw): MessageGiveawayCreated | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGiveawayCreated = new MessageGiveawayCreated();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `messageGiveawayCreated` into TDLib JSON wire shape. */
export function encodeMessageGiveawayCreated(value: MessageGiveawayCreated): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGiveawayCreated';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  return out;
}

/** messageGiveawayPrizeStars = MessageContent — Telegram Stars were received by the current user from a giveaway */
export class MessageGiveawayPrizeStars {
  type: 'messageGiveawayPrizeStars' = 'messageGiveawayPrizeStars';
  extra?: string;
  /** star_count: int53 — Number of Telegram Stars that were received */
  star_count: number = 0;
  /** transaction_id: string — Identifier of the transaction for Telegram Stars credit */
  transaction_id: string = '';
  /** boosted_chat_id: int53 — Identifier of the supergroup or channel chat, which was automatically boosted by the winners of the giveaway */
  boosted_chat_id: number = 0;
  /** giveaway_message_id: int53 — Identifier of the message with the giveaway in the boosted chat; may be 0 or an identifier of a deleted message */
  giveaway_message_id: number = 0;
  /** is_unclaimed: Bool — True, if the corresponding winner wasn't chosen and the Telegram Stars were received by the owner of the boosted chat */
  is_unclaimed: boolean = false;
  /** sticker: sticker — A sticker to be shown in the message; may be null if unknown */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `messageGiveawayPrizeStars` object; null input -> null. */
export function decodeMessageGiveawayPrizeStars(json: TdJsonRaw): MessageGiveawayPrizeStars | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGiveawayPrizeStars = new MessageGiveawayPrizeStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.transaction_id = asString(record['transaction_id']);
  out.boosted_chat_id = asNumber(record['boosted_chat_id']);
  out.giveaway_message_id = asNumber(record['giveaway_message_id']);
  out.is_unclaimed = asBool(record['is_unclaimed']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `messageGiveawayPrizeStars` into TDLib JSON wire shape. */
export function encodeMessageGiveawayPrizeStars(value: MessageGiveawayPrizeStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGiveawayPrizeStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['transaction_id'] = value.transaction_id;
  out['boosted_chat_id'] = value.boosted_chat_id;
  out['giveaway_message_id'] = value.giveaway_message_id;
  out['is_unclaimed'] = value.is_unclaimed;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** messageGiveawayWinners = MessageContent — A giveaway with public winners has been completed for the chat */
export class MessageGiveawayWinners {
  type: 'messageGiveawayWinners' = 'messageGiveawayWinners';
  extra?: string;
  /** boosted_chat_id: int53 — Identifier of the supergroup or channel chat, which was automatically boosted by the winners of the giveaway */
  boosted_chat_id: number = 0;
  /** giveaway_message_id: int53 — Identifier of the message with the giveaway in the boosted chat */
  giveaway_message_id: number = 0;
  /** additional_chat_count: int32 — Number of other chats that participated in the giveaway */
  additional_chat_count: number = 0;
  /** actual_winners_selection_date: int32 — Point in time (Unix timestamp) when the winners were selected. May be bigger than winners selection date specified in parameters of the giveaway */
  actual_winners_selection_date: number = 0;
  /** only_new_members: Bool — True, if only new members of the chats were eligible for the giveaway */
  only_new_members: boolean = false;
  /** was_refunded: Bool — True, if the giveaway was canceled and was fully refunded */
  was_refunded: boolean = false;
  /** prize: GiveawayPrize — Prize of the giveaway */
  prize: TdGiveawayPrize | null = null;
  /** prize_description: string — Additional description of the giveaway prize */
  prize_description: string = '';
  /** winner_count: int32 — Total number of winners in the giveaway */
  winner_count: number = 0;
  /** winner_user_ids: vector<int53> — Up to 100 user identifiers of the winners of the giveaway */
  winner_user_ids: number[] = [];
  /** unclaimed_prize_count: int32 — Number of undistributed prizes; for Telegram Premium giveaways only */
  unclaimed_prize_count: number = 0;
}

/** Decode a TDLib `messageGiveawayWinners` object; null input -> null. */
export function decodeMessageGiveawayWinners(json: TdJsonRaw): MessageGiveawayWinners | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGiveawayWinners = new MessageGiveawayWinners();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.boosted_chat_id = asNumber(record['boosted_chat_id']);
  out.giveaway_message_id = asNumber(record['giveaway_message_id']);
  out.additional_chat_count = asNumber(record['additional_chat_count']);
  out.actual_winners_selection_date = asNumber(record['actual_winners_selection_date']);
  out.only_new_members = asBool(record['only_new_members']);
  out.was_refunded = asBool(record['was_refunded']);
  out.prize = decodeTdGiveawayPrize(record['prize']);
  out.prize_description = asString(record['prize_description']);
  out.winner_count = asNumber(record['winner_count']);
  out.winner_user_ids = decodeVector(record['winner_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.unclaimed_prize_count = asNumber(record['unclaimed_prize_count']);
  return out;
}

/** Encode a `messageGiveawayWinners` into TDLib JSON wire shape. */
export function encodeMessageGiveawayWinners(value: MessageGiveawayWinners): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGiveawayWinners';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['boosted_chat_id'] = value.boosted_chat_id;
  out['giveaway_message_id'] = value.giveaway_message_id;
  out['additional_chat_count'] = value.additional_chat_count;
  out['actual_winners_selection_date'] = value.actual_winners_selection_date;
  out['only_new_members'] = value.only_new_members;
  out['was_refunded'] = value.was_refunded;
  out['prize'] = value.prize === null ? null : encodeTdGiveawayPrize(value.prize);
  out['prize_description'] = value.prize_description;
  out['winner_count'] = value.winner_count;
  out['winner_user_ids'] = encodeVector(value.winner_user_ids, (item: number): TdJsonRaw => item);
  out['unclaimed_prize_count'] = value.unclaimed_prize_count;
  return out;
}

/** messageGroupCall = MessageContent — A message with information about a group call not bound to a chat. If the message is incoming, the call isn't active, isn't missed, and has no duration, and getOption("can_accept_calls") is true, then ... */
export class MessageGroupCall {
  type: 'messageGroupCall' = 'messageGroupCall';
  extra?: string;
  /** unique_id: int64 — Persistent unique group call identifier */
  unique_id: string = '';
  /** is_active: Bool — True, if the call is active, i.e. the called user joined the call */
  is_active: boolean = false;
  /** was_missed: Bool — True, if the called user missed or declined the call */
  was_missed: boolean = false;
  /** is_video: Bool — True, if the call is a video call */
  is_video: boolean = false;
  /** duration: int32 — Call duration, in seconds; for left calls only */
  duration: number = 0;
  /** other_participant_ids: vector<MessageSender> — Identifiers of some other call participants */
  other_participant_ids: TdMessageSender[] = [];
}

/** Decode a TDLib `messageGroupCall` object; null input -> null. */
export function decodeMessageGroupCall(json: TdJsonRaw): MessageGroupCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageGroupCall = new MessageGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.unique_id = asInt64(record['unique_id']);
  out.is_active = asBool(record['is_active']);
  out.was_missed = asBool(record['was_missed']);
  out.is_video = asBool(record['is_video']);
  out.duration = asNumber(record['duration']);
  out.other_participant_ids = decodeVector(record['other_participant_ids'], (item: TdJsonRaw): TdMessageSender => decodeTdMessageSender(item) as TdMessageSender);
  return out;
}

/** Encode a `messageGroupCall` into TDLib JSON wire shape. */
export function encodeMessageGroupCall(value: MessageGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['unique_id'] = value.unique_id;
  out['is_active'] = value.is_active;
  out['was_missed'] = value.was_missed;
  out['is_video'] = value.is_video;
  out['duration'] = value.duration;
  out['other_participant_ids'] = encodeVector(value.other_participant_ids, (item: TdMessageSender | null): TdJsonRaw => item === null ? null : encodeTdMessageSender(item));
  return out;
}

/** messageImportInfo = MessageImportInfo — Contains information about a message created with importMessages */
export class MessageImportInfo {
  type: 'messageImportInfo' = 'messageImportInfo';
  extra?: string;
  /** sender_name: string — Name of the original sender */
  sender_name: string = '';
  /** date: int32 — Point in time (Unix timestamp) when the message was originally sent */
  date: number = 0;
}

/** Decode a TDLib `messageImportInfo` object; null input -> null. */
export function decodeMessageImportInfo(json: TdJsonRaw): MessageImportInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageImportInfo = new MessageImportInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_name = asString(record['sender_name']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `messageImportInfo` into TDLib JSON wire shape. */
export function encodeMessageImportInfo(value: MessageImportInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageImportInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_name'] = value.sender_name;
  out['date'] = value.date;
  return out;
}

/** messageInteractionInfo = MessageInteractionInfo — Contains information about interactions with a message */
export class MessageInteractionInfo {
  type: 'messageInteractionInfo' = 'messageInteractionInfo';
  extra?: string;
  /** view_count: int32 — Number of times the message was viewed */
  view_count: number = 0;
  /** forward_count: int32 — Number of times the message was forwarded */
  forward_count: number = 0;
  /** reply_info: messageReplyInfo — Information about direct or indirect replies to the message; may be null. Currently, available only in channels with a discussion supergroup and discussion supergroups for messages, which are not replies it... */
  reply_info: MessageReplyInfo | null = null;
  /** reactions: messageReactions — The list of reactions or tags added to the message; may be null */
  reactions: MessageReactions | null = null;
}

/** Decode a TDLib `messageInteractionInfo` object; null input -> null. */
export function decodeMessageInteractionInfo(json: TdJsonRaw): MessageInteractionInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageInteractionInfo = new MessageInteractionInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.view_count = asNumber(record['view_count']);
  out.forward_count = asNumber(record['forward_count']);
  out.reply_info = decodeMessageReplyInfo(record['reply_info']);
  out.reactions = decodeMessageReactions(record['reactions']);
  return out;
}

/** Encode a `messageInteractionInfo` into TDLib JSON wire shape. */
export function encodeMessageInteractionInfo(value: MessageInteractionInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageInteractionInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['view_count'] = value.view_count;
  out['forward_count'] = value.forward_count;
  out['reply_info'] = value.reply_info === null ? null : encodeMessageReplyInfo(value.reply_info);
  out['reactions'] = value.reactions === null ? null : encodeMessageReactions(value.reactions);
  return out;
}

/** messageInviteVideoChatParticipants = MessageContent — A message with information about an invitation to a video chat */
export class MessageInviteVideoChatParticipants {
  type: 'messageInviteVideoChatParticipants' = 'messageInviteVideoChatParticipants';
  extra?: string;
  /** group_call_id: int32 — Identifier of the video chat. The video chat can be received through the method getGroupCall */
  group_call_id: number = 0;
  /** user_ids: vector<int53> — Invited user identifiers */
  user_ids: number[] = [];
}

/** Decode a TDLib `messageInviteVideoChatParticipants` object; null input -> null. */
export function decodeMessageInviteVideoChatParticipants(json: TdJsonRaw): MessageInviteVideoChatParticipants | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageInviteVideoChatParticipants = new MessageInviteVideoChatParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `messageInviteVideoChatParticipants` into TDLib JSON wire shape. */
export function encodeMessageInviteVideoChatParticipants(value: MessageInviteVideoChatParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageInviteVideoChatParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** messageInvoice = MessageContent — A message with an invoice from a bot. Use getInternalLink with internalLinkTypeBotStart to share the invoice */
export class MessageInvoice {
  type: 'messageInvoice' = 'messageInvoice';
  extra?: string;
  /** product_info: productInfo — Information about the product */
  product_info: ProductInfo | null = null;
  /** currency: string — Currency for the product price */
  currency: string = '';
  /** total_amount: int53 — Product total price in the smallest units of the currency */
  total_amount: number = 0;
  /** start_parameter: string — Unique invoice bot start_parameter to be passed to getInternalLink */
  start_parameter: string = '';
  /** is_test: Bool — True, if the invoice is a test invoice */
  is_test: boolean = false;
  /** need_shipping_address: Bool — True, if the shipping address must be specified */
  need_shipping_address: boolean = false;
  /** receipt_message_id: int53 — The identifier of the message with the receipt, after the product has been purchased */
  receipt_message_id: number = 0;
  /** paid_media: PaidMedia — Extended media attached to the invoice; may be null if none */
  paid_media: TdPaidMedia | null = null;
  /** paid_media_caption: formattedText — Extended media caption; may be null if none */
  paid_media_caption: FormattedText | null = null;
}

/** Decode a TDLib `messageInvoice` object; null input -> null. */
export function decodeMessageInvoice(json: TdJsonRaw): MessageInvoice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageInvoice = new MessageInvoice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.product_info = decodeProductInfo(record['product_info']);
  out.currency = asString(record['currency']);
  out.total_amount = asNumber(record['total_amount']);
  out.start_parameter = asString(record['start_parameter']);
  out.is_test = asBool(record['is_test']);
  out.need_shipping_address = asBool(record['need_shipping_address']);
  out.receipt_message_id = asNumber(record['receipt_message_id']);
  out.paid_media = decodeTdPaidMedia(record['paid_media']);
  out.paid_media_caption = decodeFormattedText(record['paid_media_caption']);
  return out;
}

/** Encode a `messageInvoice` into TDLib JSON wire shape. */
export function encodeMessageInvoice(value: MessageInvoice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageInvoice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['product_info'] = value.product_info === null ? null : encodeProductInfo(value.product_info);
  out['currency'] = value.currency;
  out['total_amount'] = value.total_amount;
  out['start_parameter'] = value.start_parameter;
  out['is_test'] = value.is_test;
  out['need_shipping_address'] = value.need_shipping_address;
  out['receipt_message_id'] = value.receipt_message_id;
  out['paid_media'] = value.paid_media === null ? null : encodeTdPaidMedia(value.paid_media);
  out['paid_media_caption'] = value.paid_media_caption === null ? null : encodeFormattedText(value.paid_media_caption);
  return out;
}

/** messageLink = MessageLink — Contains an HTTPS link to a message in a supergroup or channel, or a forum topic */
export class MessageLink {
  type: 'messageLink' = 'messageLink';
  extra?: string;
  /** link: string — The link */
  link: string = '';
  /** is_public: Bool — True, if the link will work for non-members of the chat */
  is_public: boolean = false;
}

/** Decode a TDLib `messageLink` object; null input -> null. */
export function decodeMessageLink(json: TdJsonRaw): MessageLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageLink = new MessageLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  out.is_public = asBool(record['is_public']);
  return out;
}

/** Encode a `messageLink` into TDLib JSON wire shape. */
export function encodeMessageLink(value: MessageLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  out['is_public'] = value.is_public;
  return out;
}

/** messageLinkInfo = MessageLinkInfo — Contains information about a link to a message or a forum topic in a chat */
export class MessageLinkInfo {
  type: 'messageLinkInfo' = 'messageLinkInfo';
  extra?: string;
  /** is_public: Bool — True, if the link is a public link for a message or a forum topic in a chat */
  is_public: boolean = false;
  /** chat_id: int53 — If found, identifier of the chat to which the link points, 0 otherwise */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Identifier of the specific topic in which the message must be opened, or a topic to open if the message is missing; may be null if none */
  topic_id: TdMessageTopic | null = null;
  /** message: message — If found, the linked message; may be null */
  message: Message | null = null;
  /** media_timestamp: int32 — Timestamp from which the video/audio/video note/voice note/story playing must start, in seconds; 0 if not specified. The media can be in the message content or in its link preview */
  media_timestamp: number = 0;
  /** checklist_task_id: int32 — Identifier of the checklist task that is linked; 0 if none */
  checklist_task_id: number = 0;
  /** poll_option_id: string — Identifier of the poll option that is linked; empty if none */
  poll_option_id: string = '';
  /** for_album: Bool — True, if the whole media album to which the message belongs is linked */
  for_album: boolean = false;
}

/** Decode a TDLib `messageLinkInfo` object; null input -> null. */
export function decodeMessageLinkInfo(json: TdJsonRaw): MessageLinkInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageLinkInfo = new MessageLinkInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_public = asBool(record['is_public']);
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.message = decodeMessage(record['message']);
  out.media_timestamp = asNumber(record['media_timestamp']);
  out.checklist_task_id = asNumber(record['checklist_task_id']);
  out.poll_option_id = asString(record['poll_option_id']);
  out.for_album = asBool(record['for_album']);
  return out;
}

/** Encode a `messageLinkInfo` into TDLib JSON wire shape. */
export function encodeMessageLinkInfo(value: MessageLinkInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageLinkInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_public'] = value.is_public;
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  out['media_timestamp'] = value.media_timestamp;
  out['checklist_task_id'] = value.checklist_task_id;
  out['poll_option_id'] = value.poll_option_id;
  out['for_album'] = value.for_album;
  return out;
}

/** messageLiveLocation = MessageContent — A message with a live location */
export class MessageLiveLocation {
  type: 'messageLiveLocation' = 'messageLiveLocation';
  extra?: string;
  /** location: liveLocation — The current location */
  location: LiveLocation | null = null;
  /** expires_in: int32 — Left time for which the location can be updated, in seconds. If 0, then the location can't be updated anymore. The update updateMessageContent is not sent when this field changes */
  expires_in: number = 0;
}

/** Decode a TDLib `messageLiveLocation` object; null input -> null. */
export function decodeMessageLiveLocation(json: TdJsonRaw): MessageLiveLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageLiveLocation = new MessageLiveLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLiveLocation(record['location']);
  out.expires_in = asNumber(record['expires_in']);
  return out;
}

/** Encode a `messageLiveLocation` into TDLib JSON wire shape. */
export function encodeMessageLiveLocation(value: MessageLiveLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageLiveLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLiveLocation(value.location);
  out['expires_in'] = value.expires_in;
  return out;
}

/** messageLocation = MessageContent — A message with a location */
export class MessageLocation {
  type: 'messageLocation' = 'messageLocation';
  extra?: string;
  /** location: location — The location */
  location: Location | null = null;
}

/** Decode a TDLib `messageLocation` object; null input -> null. */
export function decodeMessageLocation(json: TdJsonRaw): MessageLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageLocation = new MessageLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  return out;
}

/** Encode a `messageLocation` into TDLib JSON wire shape. */
export function encodeMessageLocation(value: MessageLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  return out;
}

/** messageManagedBotCreated = MessageContent — A bot managed by another bot was created by the user */
export class MessageManagedBotCreated {
  type: 'messageManagedBotCreated' = 'messageManagedBotCreated';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the created bot */
  bot_user_id: number = 0;
  /** manager_bot_user_id: int53 — Identifier of the bot which will manage the new bot */
  manager_bot_user_id: number = 0;
}

/** Decode a TDLib `messageManagedBotCreated` object; null input -> null. */
export function decodeMessageManagedBotCreated(json: TdJsonRaw): MessageManagedBotCreated | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageManagedBotCreated = new MessageManagedBotCreated();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.manager_bot_user_id = asNumber(record['manager_bot_user_id']);
  return out;
}

/** Encode a `messageManagedBotCreated` into TDLib JSON wire shape. */
export function encodeMessageManagedBotCreated(value: MessageManagedBotCreated): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageManagedBotCreated';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['manager_bot_user_id'] = value.manager_bot_user_id;
  return out;
}

/** messageOriginChannel = MessageOrigin — The message was originally a post in a channel */
export class MessageOriginChannel {
  type: 'messageOriginChannel' = 'messageOriginChannel';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat to which the message was originally sent */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier of the original message */
  message_id: number = 0;
  /** author_signature: string — Original post author signature */
  author_signature: string = '';
}

/** Decode a TDLib `messageOriginChannel` object; null input -> null. */
export function decodeMessageOriginChannel(json: TdJsonRaw): MessageOriginChannel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageOriginChannel = new MessageOriginChannel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.author_signature = asString(record['author_signature']);
  return out;
}

/** Encode a `messageOriginChannel` into TDLib JSON wire shape. */
export function encodeMessageOriginChannel(value: MessageOriginChannel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageOriginChannel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['author_signature'] = value.author_signature;
  return out;
}

/** messageOriginChat = MessageOrigin — The message was originally sent on behalf of a chat */
export class MessageOriginChat {
  type: 'messageOriginChat' = 'messageOriginChat';
  extra?: string;
  /** sender_chat_id: int53 — Identifier of the chat that originally sent the message */
  sender_chat_id: number = 0;
  /** author_signature: string — For messages originally sent by an anonymous chat administrator, original message author signature */
  author_signature: string = '';
}

/** Decode a TDLib `messageOriginChat` object; null input -> null. */
export function decodeMessageOriginChat(json: TdJsonRaw): MessageOriginChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageOriginChat = new MessageOriginChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_chat_id = asNumber(record['sender_chat_id']);
  out.author_signature = asString(record['author_signature']);
  return out;
}

/** Encode a `messageOriginChat` into TDLib JSON wire shape. */
export function encodeMessageOriginChat(value: MessageOriginChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageOriginChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_chat_id'] = value.sender_chat_id;
  out['author_signature'] = value.author_signature;
  return out;
}

/** messageOriginHiddenUser = MessageOrigin — The message was originally sent by a user who is hidden by their privacy settings */
export class MessageOriginHiddenUser {
  type: 'messageOriginHiddenUser' = 'messageOriginHiddenUser';
  extra?: string;
  /** sender_name: string — Name of the sender */
  sender_name: string = '';
}

/** Decode a TDLib `messageOriginHiddenUser` object; null input -> null. */
export function decodeMessageOriginHiddenUser(json: TdJsonRaw): MessageOriginHiddenUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageOriginHiddenUser = new MessageOriginHiddenUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_name = asString(record['sender_name']);
  return out;
}

/** Encode a `messageOriginHiddenUser` into TDLib JSON wire shape. */
export function encodeMessageOriginHiddenUser(value: MessageOriginHiddenUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageOriginHiddenUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_name'] = value.sender_name;
  return out;
}

/** messageOriginUser = MessageOrigin — The message was originally sent by a known user */
export class MessageOriginUser {
  type: 'messageOriginUser' = 'messageOriginUser';
  extra?: string;
  /** sender_user_id: int53 — Identifier of the user who originally sent the message */
  sender_user_id: number = 0;
}

/** Decode a TDLib `messageOriginUser` object; null input -> null. */
export function decodeMessageOriginUser(json: TdJsonRaw): MessageOriginUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageOriginUser = new MessageOriginUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_user_id = asNumber(record['sender_user_id']);
  return out;
}

/** Encode a `messageOriginUser` into TDLib JSON wire shape. */
export function encodeMessageOriginUser(value: MessageOriginUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageOriginUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_user_id'] = value.sender_user_id;
  return out;
}

/** messagePaidMedia = MessageContent — A message with paid media */
export class MessagePaidMedia {
  type: 'messagePaidMedia' = 'messagePaidMedia';
  extra?: string;
  /** star_count: int53 — Number of Telegram Stars needed to buy access to the media in the message */
  star_count: number = 0;
  /** media: vector<PaidMedia> — Information about the media */
  media: TdPaidMedia[] = [];
  /** caption: formattedText — Media caption */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — True, if the caption must be shown above the media; otherwise, the caption must be shown below the media */
  show_caption_above_media: boolean = false;
}

/** Decode a TDLib `messagePaidMedia` object; null input -> null. */
export function decodeMessagePaidMedia(json: TdJsonRaw): MessagePaidMedia | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePaidMedia = new MessagePaidMedia();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.media = decodeVector(record['media'], (item: TdJsonRaw): TdPaidMedia => decodeTdPaidMedia(item) as TdPaidMedia);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  return out;
}

/** Encode a `messagePaidMedia` into TDLib JSON wire shape. */
export function encodeMessagePaidMedia(value: MessagePaidMedia): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePaidMedia';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['media'] = encodeVector(value.media, (item: TdPaidMedia | null): TdJsonRaw => item === null ? null : encodeTdPaidMedia(item));
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  return out;
}

/** messagePaidMessagePriceChanged = MessageContent — A price for paid messages was changed in the supergroup chat */
export class MessagePaidMessagePriceChanged {
  type: 'messagePaidMessagePriceChanged' = 'messagePaidMessagePriceChanged';
  extra?: string;
  /** paid_message_star_count: int53 — The new number of Telegram Stars that must be paid by non-administrator users of the supergroup chat for each sent message */
  paid_message_star_count: number = 0;
}

/** Decode a TDLib `messagePaidMessagePriceChanged` object; null input -> null. */
export function decodeMessagePaidMessagePriceChanged(json: TdJsonRaw): MessagePaidMessagePriceChanged | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePaidMessagePriceChanged = new MessagePaidMessagePriceChanged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  return out;
}

/** Encode a `messagePaidMessagePriceChanged` into TDLib JSON wire shape. */
export function encodeMessagePaidMessagePriceChanged(value: MessagePaidMessagePriceChanged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePaidMessagePriceChanged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['paid_message_star_count'] = value.paid_message_star_count;
  return out;
}

/** messagePaidMessagesRefunded = MessageContent — Paid messages were refunded */
export class MessagePaidMessagesRefunded {
  type: 'messagePaidMessagesRefunded' = 'messagePaidMessagesRefunded';
  extra?: string;
  /** message_count: int32 — The number of refunded messages */
  message_count: number = 0;
  /** star_count: int53 — The number of refunded Telegram Stars */
  star_count: number = 0;
}

/** Decode a TDLib `messagePaidMessagesRefunded` object; null input -> null. */
export function decodeMessagePaidMessagesRefunded(json: TdJsonRaw): MessagePaidMessagesRefunded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePaidMessagesRefunded = new MessagePaidMessagesRefunded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_count = asNumber(record['message_count']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `messagePaidMessagesRefunded` into TDLib JSON wire shape. */
export function encodeMessagePaidMessagesRefunded(value: MessagePaidMessagesRefunded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePaidMessagesRefunded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_count'] = value.message_count;
  out['star_count'] = value.star_count;
  return out;
}

/** messagePassportDataReceived = MessageContent — Telegram Passport data has been received; for bots only */
export class MessagePassportDataReceived {
  type: 'messagePassportDataReceived' = 'messagePassportDataReceived';
  extra?: string;
  /** elements: vector<encryptedPassportElement> — List of received Telegram Passport elements */
  elements: EncryptedPassportElement[] = [];
  /** credentials: encryptedCredentials — Encrypted data credentials */
  credentials: EncryptedCredentials | null = null;
}

/** Decode a TDLib `messagePassportDataReceived` object; null input -> null. */
export function decodeMessagePassportDataReceived(json: TdJsonRaw): MessagePassportDataReceived | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePassportDataReceived = new MessagePassportDataReceived();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.elements = decodeVector(record['elements'], (item: TdJsonRaw): EncryptedPassportElement => decodeEncryptedPassportElement(item) as EncryptedPassportElement);
  out.credentials = decodeEncryptedCredentials(record['credentials']);
  return out;
}

/** Encode a `messagePassportDataReceived` into TDLib JSON wire shape. */
export function encodeMessagePassportDataReceived(value: MessagePassportDataReceived): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePassportDataReceived';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['elements'] = encodeVector(value.elements, (item: EncryptedPassportElement | null): TdJsonRaw => item === null ? null : encodeEncryptedPassportElement(item));
  out['credentials'] = value.credentials === null ? null : encodeEncryptedCredentials(value.credentials);
  return out;
}

/** messagePassportDataSent = MessageContent — Telegram Passport data has been sent to a bot */
export class MessagePassportDataSent {
  type: 'messagePassportDataSent' = 'messagePassportDataSent';
  extra?: string;
  /** types: vector<PassportElementType> — List of Telegram Passport element types sent */
  types: TdPassportElementType[] = [];
}

/** Decode a TDLib `messagePassportDataSent` object; null input -> null. */
export function decodeMessagePassportDataSent(json: TdJsonRaw): MessagePassportDataSent | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePassportDataSent = new MessagePassportDataSent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.types = decodeVector(record['types'], (item: TdJsonRaw): TdPassportElementType => decodeTdPassportElementType(item) as TdPassportElementType);
  return out;
}

/** Encode a `messagePassportDataSent` into TDLib JSON wire shape. */
export function encodeMessagePassportDataSent(value: MessagePassportDataSent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePassportDataSent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['types'] = encodeVector(value.types, (item: TdPassportElementType | null): TdJsonRaw => item === null ? null : encodeTdPassportElementType(item));
  return out;
}

/** messagePaymentRefunded = MessageContent — A payment has been refunded */
export class MessagePaymentRefunded {
  type: 'messagePaymentRefunded' = 'messagePaymentRefunded';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the previous owner of the Telegram Stars that refunds them */
  owner_id: TdMessageSender | null = null;
  /** currency: string — Currency for the price of the product */
  currency: string = '';
  /** total_amount: int53 — Total price for the product, in the smallest units of the currency */
  total_amount: number = 0;
  /** invoice_payload: bytes — Invoice payload; only for bots */
  invoice_payload: string = '';
  /** telegram_payment_charge_id: string — Telegram payment identifier */
  telegram_payment_charge_id: string = '';
  /** provider_payment_charge_id: string — Provider payment identifier */
  provider_payment_charge_id: string = '';
}

/** Decode a TDLib `messagePaymentRefunded` object; null input -> null. */
export function decodeMessagePaymentRefunded(json: TdJsonRaw): MessagePaymentRefunded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePaymentRefunded = new MessagePaymentRefunded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.currency = asString(record['currency']);
  out.total_amount = asNumber(record['total_amount']);
  out.invoice_payload = asString(record['invoice_payload']);
  out.telegram_payment_charge_id = asString(record['telegram_payment_charge_id']);
  out.provider_payment_charge_id = asString(record['provider_payment_charge_id']);
  return out;
}

/** Encode a `messagePaymentRefunded` into TDLib JSON wire shape. */
export function encodeMessagePaymentRefunded(value: MessagePaymentRefunded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePaymentRefunded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['currency'] = value.currency;
  out['total_amount'] = value.total_amount;
  out['invoice_payload'] = value.invoice_payload;
  out['telegram_payment_charge_id'] = value.telegram_payment_charge_id;
  out['provider_payment_charge_id'] = value.provider_payment_charge_id;
  return out;
}

/** messagePaymentSuccessful = MessageContent — A payment has been sent to a bot or a business account */
export class MessagePaymentSuccessful {
  type: 'messagePaymentSuccessful' = 'messagePaymentSuccessful';
  extra?: string;
  /** invoice_chat_id: int53 — Identifier of the chat, containing the corresponding invoice message */
  invoice_chat_id: number = 0;
  /** invoice_message_id: int53 — Identifier of the message with the corresponding invoice; may be 0 or an identifier of a deleted message */
  invoice_message_id: number = 0;
  /** currency: string — Currency for the price of the product */
  currency: string = '';
  /** total_amount: int53 — Total price for the product, in the smallest units of the currency */
  total_amount: number = 0;
  /** subscription_until_date: int32 — Point in time (Unix timestamp) when the subscription will expire; 0 if unknown or the payment isn't recurring */
  subscription_until_date: number = 0;
  /** is_recurring: Bool — True, if this is a recurring payment */
  is_recurring: boolean = false;
  /** is_first_recurring: Bool — True, if this is the first recurring payment */
  is_first_recurring: boolean = false;
  /** invoice_name: string — Name of the invoice; may be empty if unknown */
  invoice_name: string = '';
}

/** Decode a TDLib `messagePaymentSuccessful` object; null input -> null. */
export function decodeMessagePaymentSuccessful(json: TdJsonRaw): MessagePaymentSuccessful | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePaymentSuccessful = new MessagePaymentSuccessful();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invoice_chat_id = asNumber(record['invoice_chat_id']);
  out.invoice_message_id = asNumber(record['invoice_message_id']);
  out.currency = asString(record['currency']);
  out.total_amount = asNumber(record['total_amount']);
  out.subscription_until_date = asNumber(record['subscription_until_date']);
  out.is_recurring = asBool(record['is_recurring']);
  out.is_first_recurring = asBool(record['is_first_recurring']);
  out.invoice_name = asString(record['invoice_name']);
  return out;
}

/** Encode a `messagePaymentSuccessful` into TDLib JSON wire shape. */
export function encodeMessagePaymentSuccessful(value: MessagePaymentSuccessful): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePaymentSuccessful';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invoice_chat_id'] = value.invoice_chat_id;
  out['invoice_message_id'] = value.invoice_message_id;
  out['currency'] = value.currency;
  out['total_amount'] = value.total_amount;
  out['subscription_until_date'] = value.subscription_until_date;
  out['is_recurring'] = value.is_recurring;
  out['is_first_recurring'] = value.is_first_recurring;
  out['invoice_name'] = value.invoice_name;
  return out;
}

/** messagePaymentSuccessfulBot = MessageContent — A payment has been received by the bot or the business account */
export class MessagePaymentSuccessfulBot {
  type: 'messagePaymentSuccessfulBot' = 'messagePaymentSuccessfulBot';
  extra?: string;
  /** currency: string — Currency for price of the product */
  currency: string = '';
  /** total_amount: int53 — Total price for the product, in the smallest units of the currency */
  total_amount: number = 0;
  /** subscription_until_date: int32 — Point in time (Unix timestamp) when the subscription will expire; 0 if unknown or the payment isn't recurring */
  subscription_until_date: number = 0;
  /** is_recurring: Bool — True, if this is a recurring payment */
  is_recurring: boolean = false;
  /** is_first_recurring: Bool — True, if this is the first recurring payment */
  is_first_recurring: boolean = false;
  /** invoice_payload: bytes — Invoice payload */
  invoice_payload: string = '';
  /** shipping_option_id: string — Identifier of the shipping option chosen by the user; may be empty if not applicable; for bots only */
  shipping_option_id: string = '';
  /** order_info: orderInfo — Information about the order; may be null; for bots only */
  order_info: OrderInfo | null = null;
  /** telegram_payment_charge_id: string — Telegram payment identifier */
  telegram_payment_charge_id: string = '';
  /** provider_payment_charge_id: string — Provider payment identifier */
  provider_payment_charge_id: string = '';
}

/** Decode a TDLib `messagePaymentSuccessfulBot` object; null input -> null. */
export function decodeMessagePaymentSuccessfulBot(json: TdJsonRaw): MessagePaymentSuccessfulBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePaymentSuccessfulBot = new MessagePaymentSuccessfulBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.total_amount = asNumber(record['total_amount']);
  out.subscription_until_date = asNumber(record['subscription_until_date']);
  out.is_recurring = asBool(record['is_recurring']);
  out.is_first_recurring = asBool(record['is_first_recurring']);
  out.invoice_payload = asString(record['invoice_payload']);
  out.shipping_option_id = asString(record['shipping_option_id']);
  out.order_info = decodeOrderInfo(record['order_info']);
  out.telegram_payment_charge_id = asString(record['telegram_payment_charge_id']);
  out.provider_payment_charge_id = asString(record['provider_payment_charge_id']);
  return out;
}

/** Encode a `messagePaymentSuccessfulBot` into TDLib JSON wire shape. */
export function encodeMessagePaymentSuccessfulBot(value: MessagePaymentSuccessfulBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePaymentSuccessfulBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['total_amount'] = value.total_amount;
  out['subscription_until_date'] = value.subscription_until_date;
  out['is_recurring'] = value.is_recurring;
  out['is_first_recurring'] = value.is_first_recurring;
  out['invoice_payload'] = value.invoice_payload;
  out['shipping_option_id'] = value.shipping_option_id;
  out['order_info'] = value.order_info === null ? null : encodeOrderInfo(value.order_info);
  out['telegram_payment_charge_id'] = value.telegram_payment_charge_id;
  out['provider_payment_charge_id'] = value.provider_payment_charge_id;
  return out;
}

/** messagePhoto = MessageContent — A photo message */
export class MessagePhoto {
  type: 'messagePhoto' = 'messagePhoto';
  extra?: string;
  /** photo: photo — The photo */
  photo: Photo | null = null;
  /** video: video — The video representing the live photo; may be null if the photo is static */
  video: Video | null = null;
  /** caption: formattedText — Photo caption */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — True, if the caption must be shown above the photo; otherwise, the caption must be shown below the photo */
  show_caption_above_media: boolean = false;
  /** has_spoiler: Bool — True, if the photo preview must be covered by a spoiler animation */
  has_spoiler: boolean = false;
  /** is_secret: Bool — True, if the photo must be blurred and must be shown only while tapped */
  is_secret: boolean = false;
}

/** Decode a TDLib `messagePhoto` object; null input -> null. */
export function decodeMessagePhoto(json: TdJsonRaw): MessagePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePhoto = new MessagePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  out.video = decodeVideo(record['video']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  out.has_spoiler = asBool(record['has_spoiler']);
  out.is_secret = asBool(record['is_secret']);
  return out;
}

/** Encode a `messagePhoto` into TDLib JSON wire shape. */
export function encodeMessagePhoto(value: MessagePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  out['has_spoiler'] = value.has_spoiler;
  out['is_secret'] = value.is_secret;
  return out;
}

/** messagePinMessage = MessageContent — A message has been pinned */
export class MessagePinMessage {
  type: 'messagePinMessage' = 'messagePinMessage';
  extra?: string;
  /** message_id: int53 — Identifier of the pinned message, can be an identifier of a deleted message or 0 */
  message_id: number = 0;
}

/** Decode a TDLib `messagePinMessage` object; null input -> null. */
export function decodeMessagePinMessage(json: TdJsonRaw): MessagePinMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePinMessage = new MessagePinMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `messagePinMessage` into TDLib JSON wire shape. */
export function encodeMessagePinMessage(value: MessagePinMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePinMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_id'] = value.message_id;
  return out;
}

/** messagePoll = MessageContent — A message with a poll */
export class MessagePoll {
  type: 'messagePoll' = 'messagePoll';
  extra?: string;
  /** poll: poll — Information about the poll */
  poll: Poll | null = null;
  /** description: formattedText */
  description: FormattedText | null = null;
  /** media: PollMedia — Media attached to the poll; may be null if none. If present, currently, can be only of the types pollMediaAnimation, pollMediaAudio, pollMediaDocument, pollMediaLocation, pollMediaPhoto, pollMediaVenue, or pollMediaVideo */
  media: TdPollMedia | null = null;
  /** can_add_option: Bool — True, if an option can be added to the poll using addPollOption */
  can_add_option: boolean = false;
}

/** Decode a TDLib `messagePoll` object; null input -> null. */
export function decodeMessagePoll(json: TdJsonRaw): MessagePoll | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePoll = new MessagePoll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.poll = decodePoll(record['poll']);
  out.description = decodeFormattedText(record['description']);
  out.media = decodeTdPollMedia(record['media']);
  out.can_add_option = asBool(record['can_add_option']);
  return out;
}

/** Encode a `messagePoll` into TDLib JSON wire shape. */
export function encodeMessagePoll(value: MessagePoll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePoll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['poll'] = value.poll === null ? null : encodePoll(value.poll);
  out['description'] = value.description === null ? null : encodeFormattedText(value.description);
  out['media'] = value.media === null ? null : encodeTdPollMedia(value.media);
  out['can_add_option'] = value.can_add_option;
  return out;
}

/** messagePollOptionAdded = MessageContent — A message with information about an added poll option */
export class MessagePollOptionAdded {
  type: 'messagePollOptionAdded' = 'messagePollOptionAdded';
  extra?: string;
  /** poll_message_id: int53 — Identifier of the message with the poll; can be an identifier of a deleted message or 0 */
  poll_message_id: number = 0;
  /** option_id: string — Identifier of the added option in the poll */
  option_id: string = '';
  /** text: formattedText — Text of the option; 1-100 characters; may contain only custom emoji entities */
  text: FormattedText | null = null;
}

/** Decode a TDLib `messagePollOptionAdded` object; null input -> null. */
export function decodeMessagePollOptionAdded(json: TdJsonRaw): MessagePollOptionAdded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePollOptionAdded = new MessagePollOptionAdded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.poll_message_id = asNumber(record['poll_message_id']);
  out.option_id = asString(record['option_id']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `messagePollOptionAdded` into TDLib JSON wire shape. */
export function encodeMessagePollOptionAdded(value: MessagePollOptionAdded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePollOptionAdded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['poll_message_id'] = value.poll_message_id;
  out['option_id'] = value.option_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** messagePollOptionDeleted = MessageContent — A message with information about a deleted poll option */
export class MessagePollOptionDeleted {
  type: 'messagePollOptionDeleted' = 'messagePollOptionDeleted';
  extra?: string;
  /** poll_message_id: int53 — Identifier of the message with the poll; can be an identifier of a deleted message or 0 */
  poll_message_id: number = 0;
  /** option_id: string — Identifier of the deleted option in the poll */
  option_id: string = '';
  /** text: formattedText — Text of the option; 1-100 characters; may contain only custom emoji entities */
  text: FormattedText | null = null;
}

/** Decode a TDLib `messagePollOptionDeleted` object; null input -> null. */
export function decodeMessagePollOptionDeleted(json: TdJsonRaw): MessagePollOptionDeleted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePollOptionDeleted = new MessagePollOptionDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.poll_message_id = asNumber(record['poll_message_id']);
  out.option_id = asString(record['option_id']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `messagePollOptionDeleted` into TDLib JSON wire shape. */
export function encodeMessagePollOptionDeleted(value: MessagePollOptionDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePollOptionDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['poll_message_id'] = value.poll_message_id;
  out['option_id'] = value.option_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** messagePosition = MessagePosition — Contains information about a message in a specific position */
export class MessagePosition {
  type: 'messagePosition' = 'messagePosition';
  extra?: string;
  /** position: int32 — 0-based message position in the full list of suitable messages */
  position: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) when the message was sent */
  date: number = 0;
}

/** Decode a TDLib `messagePosition` object; null input -> null. */
export function decodeMessagePosition(json: TdJsonRaw): MessagePosition | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePosition = new MessagePosition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.position = asNumber(record['position']);
  out.message_id = asNumber(record['message_id']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `messagePosition` into TDLib JSON wire shape. */
export function encodeMessagePosition(value: MessagePosition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePosition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['position'] = value.position;
  out['message_id'] = value.message_id;
  out['date'] = value.date;
  return out;
}

/** messagePositions = MessagePositions — Contains a list of message positions */
export class MessagePositions {
  type: 'messagePositions' = 'messagePositions';
  extra?: string;
  /** total_count: int32 — Total number of messages found */
  total_count: number = 0;
  /** positions: vector<messagePosition> — List of message positions */
  positions: MessagePosition[] = [];
}

/** Decode a TDLib `messagePositions` object; null input -> null. */
export function decodeMessagePositions(json: TdJsonRaw): MessagePositions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePositions = new MessagePositions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.positions = decodeVector(record['positions'], (item: TdJsonRaw): MessagePosition => decodeMessagePosition(item) as MessagePosition);
  return out;
}

/** Encode a `messagePositions` into TDLib JSON wire shape. */
export function encodeMessagePositions(value: MessagePositions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePositions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['positions'] = encodeVector(value.positions, (item: MessagePosition | null): TdJsonRaw => item === null ? null : encodeMessagePosition(item));
  return out;
}

/** messagePremiumGiftCode = MessageContent — A Telegram Premium gift code was created for the user */
export class MessagePremiumGiftCode {
  type: 'messagePremiumGiftCode' = 'messagePremiumGiftCode';
  extra?: string;
  /** creator_id: MessageSender — Identifier of a chat or a user who created the gift code; may be null if unknown */
  creator_id: TdMessageSender | null = null;
  /** text: formattedText — Message added to the gift */
  text: FormattedText | null = null;
  /** is_from_giveaway: Bool — True, if the gift code was created for a giveaway */
  is_from_giveaway: boolean = false;
  /** is_unclaimed: Bool — True, if the winner for the corresponding Telegram Premium subscription wasn't chosen */
  is_unclaimed: boolean = false;
  /** currency: string — Currency for the paid amount; empty if unknown */
  currency: string = '';
  /** amount: int53 — The paid amount, in the smallest units of the currency; 0 if unknown */
  amount: number = 0;
  /** cryptocurrency: string — Cryptocurrency used to pay for the gift; may be empty if none or unknown */
  cryptocurrency: string = '';
  /** cryptocurrency_amount: int64 — The paid amount, in the smallest units of the cryptocurrency; 0 if unknown */
  cryptocurrency_amount: string = '';
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active after code activation; 0 if the number of months isn't integer */
  month_count: number = 0;
  /** day_count: int32 — Number of days the Telegram Premium subscription will be active after code activation */
  day_count: number = 0;
  /** sticker: sticker — A sticker to be shown in the message; may be null if unknown */
  sticker: Sticker | null = null;
  /** code: string — The gift code */
  code: string = '';
}

/** Decode a TDLib `messagePremiumGiftCode` object; null input -> null. */
export function decodeMessagePremiumGiftCode(json: TdJsonRaw): MessagePremiumGiftCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessagePremiumGiftCode = new MessagePremiumGiftCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.creator_id = decodeTdMessageSender(record['creator_id']);
  out.text = decodeFormattedText(record['text']);
  out.is_from_giveaway = asBool(record['is_from_giveaway']);
  out.is_unclaimed = asBool(record['is_unclaimed']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.cryptocurrency = asString(record['cryptocurrency']);
  out.cryptocurrency_amount = asInt64(record['cryptocurrency_amount']);
  out.month_count = asNumber(record['month_count']);
  out.day_count = asNumber(record['day_count']);
  out.sticker = decodeSticker(record['sticker']);
  out.code = asString(record['code']);
  return out;
}

/** Encode a `messagePremiumGiftCode` into TDLib JSON wire shape. */
export function encodeMessagePremiumGiftCode(value: MessagePremiumGiftCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messagePremiumGiftCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['creator_id'] = value.creator_id === null ? null : encodeTdMessageSender(value.creator_id);
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['is_from_giveaway'] = value.is_from_giveaway;
  out['is_unclaimed'] = value.is_unclaimed;
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['cryptocurrency'] = value.cryptocurrency;
  out['cryptocurrency_amount'] = value.cryptocurrency_amount;
  out['month_count'] = value.month_count;
  out['day_count'] = value.day_count;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  out['code'] = value.code;
  return out;
}

/** messageProperties = MessageProperties — Contains properties of a message and describes actions that can be done with the message right now For live location, poll, and checklist messages this fields shows whether editMessageLiveLocation,... */
export class MessageProperties {
  type: 'messageProperties' = 'messageProperties';
  extra?: string;
  /** can_add_offer: Bool — True, if an offer can be added to the message using addOffer */
  can_add_offer: boolean = false;
  /** can_add_tasks: Bool — True, if tasks can be added to the message's checklist using addChecklistTasks if the current user has Telegram Premium subscription */
  can_add_tasks: boolean = false;
  /** can_be_approved: Bool — True, if the message is a suggested post that can be approved by the user using approveSuggestedPost */
  can_be_approved: boolean = false;
  /** can_be_copied: Bool — True, if content of the message can be copied using inputMessageForwarded or forwardMessages with copy options */
  can_be_copied: boolean = false;
  /** can_be_copied_to_secret_chat: Bool — True, if content of the message can be copied to a secret chat using inputMessageForwarded or forwardMessages with copy options */
  can_be_copied_to_secret_chat: boolean = false;
  /** can_be_declined: Bool — True, if the message is a suggested post that can be declined by the user using declineSuggestedPost */
  can_be_declined: boolean = false;
  /** can_be_deleted_only_for_self: Bool — True, if the message can be deleted only for the current user while other users will continue to see it using the method deleteMessages with revoke == false */
  can_be_deleted_only_for_self: boolean = false;
  /** can_be_deleted_for_all_users: Bool — True, if the message can be deleted for all users using the method deleteMessages with revoke == true */
  can_be_deleted_for_all_users: boolean = false;
  /** can_be_edited: Bool — True, if the message can be edited using the methods editMessageText, editMessageCaption, or editMessageReplyMarkup. */
  can_be_edited: boolean = false;
  /** can_be_forwarded: Bool — True, if the message can be forwarded using inputMessageForwarded or forwardMessages without copy options */
  can_be_forwarded: boolean = false;
  /** can_be_paid: Bool — True, if the message can be paid using inputInvoiceMessage */
  can_be_paid: boolean = false;
  /** can_be_pinned: Bool — True, if the message can be pinned or unpinned in the chat using pinChatMessage or unpinChatMessage */
  can_be_pinned: boolean = false;
  /** can_be_replied: Bool — True, if the message can be replied in the same chat and forum topic using inputMessageReplyToMessage. Ephemeral messages can be replied only by other ephemeral messages */
  can_be_replied: boolean = false;
  /** can_be_replied_in_another_chat: Bool — True, if the message can be replied in another chat or forum topic using inputMessageReplyToExternalMessage */
  can_be_replied_in_another_chat: boolean = false;
  /** can_be_saved: Bool — True, if content of the message can be saved locally */
  can_be_saved: boolean = false;
  /** can_be_shared_in_story: Bool — True, if the message can be shared in a story using inputStoryAreaTypeMessage */
  can_be_shared_in_story: boolean = false;
  /** can_delete_reactions: Bool — True, if the user can delete reactions of other users in the message using the method deleteMessageReactionsFromSender */
  can_delete_reactions: boolean = false;
  /** can_edit_media: Bool — True, if the message can be edited using the method editMessageMedia */
  can_edit_media: boolean = false;
  /** can_edit_scheduling_state: Bool — True, if scheduling state of the message can be edited */
  can_edit_scheduling_state: boolean = false;
  /** can_edit_suggested_post_info: Bool — True, if another price or post send time can be suggested using addOffer */
  can_edit_suggested_post_info: boolean = false;
  /** can_get_author: Bool — True, if author of the message sent on behalf of a chat can be received through getMessageAuthor */
  can_get_author: boolean = false;
  /** can_get_embedding_code: Bool — True, if code for message embedding can be received using getMessageEmbeddingCode */
  can_get_embedding_code: boolean = false;
  /** can_get_link: Bool — True, if a link can be generated for the message using getMessageLink */
  can_get_link: boolean = false;
  /** can_get_media_timestamp_links: Bool — True, if media timestamp links can be generated for media timestamp entities in the message text, caption or link preview description using getMessageLink */
  can_get_media_timestamp_links: boolean = false;
  /** can_get_message_thread: Bool — True, if information about the message thread is available through getMessageThread and getMessageThreadHistory */
  can_get_message_thread: boolean = false;
  /** can_get_poll_vote_statistics: Bool — True, if the message is a poll and vote statistics are available through getPollVoteStatistics */
  can_get_poll_vote_statistics: boolean = false;
  /** can_get_read_date: Bool — True, if read date of the message can be received through getMessageReadDate */
  can_get_read_date: boolean = false;
  /** can_get_statistics: Bool — True, if message statistics are available through getMessageStatistics and message forwards can be received using getMessagePublicForwards */
  can_get_statistics: boolean = false;
  /** can_get_video_advertisements: Bool — True, if advertisements for video of the message can be received through getVideoMessageAdvertisements */
  can_get_video_advertisements: boolean = false;
  /** can_get_viewers: Bool — True, if chat members already viewed the message can be received through getMessageViewers */
  can_get_viewers: boolean = false;
  /** can_mark_tasks_as_done: Bool — True, if tasks can be marked as done or not done in the message's checklist using markChecklistTasksAsDone if the current user has Telegram Premium subscription */
  can_mark_tasks_as_done: boolean = false;
  /** can_recognize_speech: Bool — True, if speech can be recognized for the message through recognizeSpeech */
  can_recognize_speech: boolean = false;
  /** can_report_chat: Bool — True, if the message can be reported using reportChat */
  can_report_chat: boolean = false;
  /** can_report_reactions: Bool — True, if reactions on the message can be reported through reportMessageReactions */
  can_report_reactions: boolean = false;
  /** can_report_supergroup_spam: Bool — True, if the message can be reported using reportSupergroupSpam */
  can_report_supergroup_spam: boolean = false;
  /** can_set_fact_check: Bool — True, if fact check for the message can be changed through setMessageFactCheck */
  can_set_fact_check: boolean = false;
  /** has_protected_content_by_current_user: Bool — True, if content of the message can't be saved locally, because it is protected by the current user; if true, then can_be_saved is false */
  has_protected_content_by_current_user: boolean = false;
  /** has_protected_content_by_other_user: Bool — True, if content of the message can't be saved locally, because it is protected by the other user; if true, then can_be_saved is false */
  has_protected_content_by_other_user: boolean = false;
  /** need_show_statistics: Bool — True, if message statistics must be available from context menu of the message */
  need_show_statistics: boolean = false;
}

/** Decode a TDLib `messageProperties` object; null input -> null. */
export function decodeMessageProperties(json: TdJsonRaw): MessageProperties | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageProperties = new MessageProperties();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_add_offer = asBool(record['can_add_offer']);
  out.can_add_tasks = asBool(record['can_add_tasks']);
  out.can_be_approved = asBool(record['can_be_approved']);
  out.can_be_copied = asBool(record['can_be_copied']);
  out.can_be_copied_to_secret_chat = asBool(record['can_be_copied_to_secret_chat']);
  out.can_be_declined = asBool(record['can_be_declined']);
  out.can_be_deleted_only_for_self = asBool(record['can_be_deleted_only_for_self']);
  out.can_be_deleted_for_all_users = asBool(record['can_be_deleted_for_all_users']);
  out.can_be_edited = asBool(record['can_be_edited']);
  out.can_be_forwarded = asBool(record['can_be_forwarded']);
  out.can_be_paid = asBool(record['can_be_paid']);
  out.can_be_pinned = asBool(record['can_be_pinned']);
  out.can_be_replied = asBool(record['can_be_replied']);
  out.can_be_replied_in_another_chat = asBool(record['can_be_replied_in_another_chat']);
  out.can_be_saved = asBool(record['can_be_saved']);
  out.can_be_shared_in_story = asBool(record['can_be_shared_in_story']);
  out.can_delete_reactions = asBool(record['can_delete_reactions']);
  out.can_edit_media = asBool(record['can_edit_media']);
  out.can_edit_scheduling_state = asBool(record['can_edit_scheduling_state']);
  out.can_edit_suggested_post_info = asBool(record['can_edit_suggested_post_info']);
  out.can_get_author = asBool(record['can_get_author']);
  out.can_get_embedding_code = asBool(record['can_get_embedding_code']);
  out.can_get_link = asBool(record['can_get_link']);
  out.can_get_media_timestamp_links = asBool(record['can_get_media_timestamp_links']);
  out.can_get_message_thread = asBool(record['can_get_message_thread']);
  out.can_get_poll_vote_statistics = asBool(record['can_get_poll_vote_statistics']);
  out.can_get_read_date = asBool(record['can_get_read_date']);
  out.can_get_statistics = asBool(record['can_get_statistics']);
  out.can_get_video_advertisements = asBool(record['can_get_video_advertisements']);
  out.can_get_viewers = asBool(record['can_get_viewers']);
  out.can_mark_tasks_as_done = asBool(record['can_mark_tasks_as_done']);
  out.can_recognize_speech = asBool(record['can_recognize_speech']);
  out.can_report_chat = asBool(record['can_report_chat']);
  out.can_report_reactions = asBool(record['can_report_reactions']);
  out.can_report_supergroup_spam = asBool(record['can_report_supergroup_spam']);
  out.can_set_fact_check = asBool(record['can_set_fact_check']);
  out.has_protected_content_by_current_user = asBool(record['has_protected_content_by_current_user']);
  out.has_protected_content_by_other_user = asBool(record['has_protected_content_by_other_user']);
  out.need_show_statistics = asBool(record['need_show_statistics']);
  return out;
}

/** Encode a `messageProperties` into TDLib JSON wire shape. */
export function encodeMessageProperties(value: MessageProperties): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageProperties';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_add_offer'] = value.can_add_offer;
  out['can_add_tasks'] = value.can_add_tasks;
  out['can_be_approved'] = value.can_be_approved;
  out['can_be_copied'] = value.can_be_copied;
  out['can_be_copied_to_secret_chat'] = value.can_be_copied_to_secret_chat;
  out['can_be_declined'] = value.can_be_declined;
  out['can_be_deleted_only_for_self'] = value.can_be_deleted_only_for_self;
  out['can_be_deleted_for_all_users'] = value.can_be_deleted_for_all_users;
  out['can_be_edited'] = value.can_be_edited;
  out['can_be_forwarded'] = value.can_be_forwarded;
  out['can_be_paid'] = value.can_be_paid;
  out['can_be_pinned'] = value.can_be_pinned;
  out['can_be_replied'] = value.can_be_replied;
  out['can_be_replied_in_another_chat'] = value.can_be_replied_in_another_chat;
  out['can_be_saved'] = value.can_be_saved;
  out['can_be_shared_in_story'] = value.can_be_shared_in_story;
  out['can_delete_reactions'] = value.can_delete_reactions;
  out['can_edit_media'] = value.can_edit_media;
  out['can_edit_scheduling_state'] = value.can_edit_scheduling_state;
  out['can_edit_suggested_post_info'] = value.can_edit_suggested_post_info;
  out['can_get_author'] = value.can_get_author;
  out['can_get_embedding_code'] = value.can_get_embedding_code;
  out['can_get_link'] = value.can_get_link;
  out['can_get_media_timestamp_links'] = value.can_get_media_timestamp_links;
  out['can_get_message_thread'] = value.can_get_message_thread;
  out['can_get_poll_vote_statistics'] = value.can_get_poll_vote_statistics;
  out['can_get_read_date'] = value.can_get_read_date;
  out['can_get_statistics'] = value.can_get_statistics;
  out['can_get_video_advertisements'] = value.can_get_video_advertisements;
  out['can_get_viewers'] = value.can_get_viewers;
  out['can_mark_tasks_as_done'] = value.can_mark_tasks_as_done;
  out['can_recognize_speech'] = value.can_recognize_speech;
  out['can_report_chat'] = value.can_report_chat;
  out['can_report_reactions'] = value.can_report_reactions;
  out['can_report_supergroup_spam'] = value.can_report_supergroup_spam;
  out['can_set_fact_check'] = value.can_set_fact_check;
  out['has_protected_content_by_current_user'] = value.has_protected_content_by_current_user;
  out['has_protected_content_by_other_user'] = value.has_protected_content_by_other_user;
  out['need_show_statistics'] = value.need_show_statistics;
  return out;
}

/** messageProximityAlertTriggered = MessageContent — A user in the chat came within proximity alert range */
export class MessageProximityAlertTriggered {
  type: 'messageProximityAlertTriggered' = 'messageProximityAlertTriggered';
  extra?: string;
  /** traveler_id: MessageSender — The identifier of a user or chat that triggered the proximity alert */
  traveler_id: TdMessageSender | null = null;
  /** watcher_id: MessageSender — The identifier of a user or chat that subscribed for the proximity alert */
  watcher_id: TdMessageSender | null = null;
  /** distance: int32 — The distance between the users */
  distance: number = 0;
}

/** Decode a TDLib `messageProximityAlertTriggered` object; null input -> null. */
export function decodeMessageProximityAlertTriggered(json: TdJsonRaw): MessageProximityAlertTriggered | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageProximityAlertTriggered = new MessageProximityAlertTriggered();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.traveler_id = decodeTdMessageSender(record['traveler_id']);
  out.watcher_id = decodeTdMessageSender(record['watcher_id']);
  out.distance = asNumber(record['distance']);
  return out;
}

/** Encode a `messageProximityAlertTriggered` into TDLib JSON wire shape. */
export function encodeMessageProximityAlertTriggered(value: MessageProximityAlertTriggered): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageProximityAlertTriggered';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['traveler_id'] = value.traveler_id === null ? null : encodeTdMessageSender(value.traveler_id);
  out['watcher_id'] = value.watcher_id === null ? null : encodeTdMessageSender(value.watcher_id);
  out['distance'] = value.distance;
  return out;
}

/** messageReaction = MessageReaction — Contains information about a reaction to a message */
export class MessageReaction {
  type: 'messageReaction' = 'messageReaction';
  extra?: string;
  /** type: ReactionType — Type of the reaction */
  type_: TdReactionType | null = null;
  /** total_count: int32 — Number of times the reaction was added */
  total_count: number = 0;
  /** is_chosen: Bool — True, if the reaction is chosen by the current user */
  is_chosen: boolean = false;
  /** used_sender_id: MessageSender — Identifier of the message sender used by the current user to add the reaction; may be null if unknown or the reaction isn't chosen */
  used_sender_id: TdMessageSender | null = null;
  /** recent_sender_ids: vector<MessageSender> — Identifiers of at most 3 recent message senders, added the reaction; available in private, basic group and supergroup chats */
  recent_sender_ids: TdMessageSender[] = [];
}

/** Decode a TDLib `messageReaction` object; null input -> null. */
export function decodeMessageReaction(json: TdJsonRaw): MessageReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReaction = new MessageReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdReactionType(record['type']);
  out.total_count = asNumber(record['total_count']);
  out.is_chosen = asBool(record['is_chosen']);
  out.used_sender_id = decodeTdMessageSender(record['used_sender_id']);
  out.recent_sender_ids = decodeVector(record['recent_sender_ids'], (item: TdJsonRaw): TdMessageSender => decodeTdMessageSender(item) as TdMessageSender);
  return out;
}

/** Encode a `messageReaction` into TDLib JSON wire shape. */
export function encodeMessageReaction(value: MessageReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdReactionType(value.type_);
  out['total_count'] = value.total_count;
  out['is_chosen'] = value.is_chosen;
  out['used_sender_id'] = value.used_sender_id === null ? null : encodeTdMessageSender(value.used_sender_id);
  out['recent_sender_ids'] = encodeVector(value.recent_sender_ids, (item: TdMessageSender | null): TdJsonRaw => item === null ? null : encodeTdMessageSender(item));
  return out;
}

/** messageReactions = MessageReactions — Contains a list of reactions added to a message */
export class MessageReactions {
  type: 'messageReactions' = 'messageReactions';
  extra?: string;
  /** reactions: vector<messageReaction> — List of added reactions */
  reactions: MessageReaction[] = [];
  /** are_tags: Bool — True, if the reactions are tags and Telegram Premium users can filter messages by them */
  are_tags: boolean = false;
  /** paid_reactors: vector<paidReactor> — Information about top users that added the paid reaction */
  paid_reactors: PaidReactor[] = [];
  /** can_get_added_reactions: Bool — True, if the list of added reactions is available using getMessageAddedReactions */
  can_get_added_reactions: boolean = false;
}

/** Decode a TDLib `messageReactions` object; null input -> null. */
export function decodeMessageReactions(json: TdJsonRaw): MessageReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReactions = new MessageReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reactions = decodeVector(record['reactions'], (item: TdJsonRaw): MessageReaction => decodeMessageReaction(item) as MessageReaction);
  out.are_tags = asBool(record['are_tags']);
  out.paid_reactors = decodeVector(record['paid_reactors'], (item: TdJsonRaw): PaidReactor => decodePaidReactor(item) as PaidReactor);
  out.can_get_added_reactions = asBool(record['can_get_added_reactions']);
  return out;
}

/** Encode a `messageReactions` into TDLib JSON wire shape. */
export function encodeMessageReactions(value: MessageReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reactions'] = encodeVector(value.reactions, (item: MessageReaction | null): TdJsonRaw => item === null ? null : encodeMessageReaction(item));
  out['are_tags'] = value.are_tags;
  out['paid_reactors'] = encodeVector(value.paid_reactors, (item: PaidReactor | null): TdJsonRaw => item === null ? null : encodePaidReactor(item));
  out['can_get_added_reactions'] = value.can_get_added_reactions;
  return out;
}

/** messageReadDateMyPrivacyRestricted = MessageReadDate — The read date is unknown due to privacy settings of the current user, but will be known if the user subscribes to Telegram Premium */
export class MessageReadDateMyPrivacyRestricted {
  type: 'messageReadDateMyPrivacyRestricted' = 'messageReadDateMyPrivacyRestricted';
  extra?: string;
}

/** Decode a TDLib `messageReadDateMyPrivacyRestricted` object; null input -> null. */
export function decodeMessageReadDateMyPrivacyRestricted(json: TdJsonRaw): MessageReadDateMyPrivacyRestricted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReadDateMyPrivacyRestricted = new MessageReadDateMyPrivacyRestricted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageReadDateMyPrivacyRestricted` into TDLib JSON wire shape. */
export function encodeMessageReadDateMyPrivacyRestricted(value: MessageReadDateMyPrivacyRestricted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReadDateMyPrivacyRestricted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageReadDateRead = MessageReadDate — Contains read date of the message */
export class MessageReadDateRead {
  type: 'messageReadDateRead' = 'messageReadDateRead';
  extra?: string;
  /** read_date: int32 — Point in time (Unix timestamp) when the message was read by the other user */
  read_date: number = 0;
}

/** Decode a TDLib `messageReadDateRead` object; null input -> null. */
export function decodeMessageReadDateRead(json: TdJsonRaw): MessageReadDateRead | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReadDateRead = new MessageReadDateRead();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.read_date = asNumber(record['read_date']);
  return out;
}

/** Encode a `messageReadDateRead` into TDLib JSON wire shape. */
export function encodeMessageReadDateRead(value: MessageReadDateRead): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReadDateRead';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['read_date'] = value.read_date;
  return out;
}

/** messageReadDateTooOld = MessageReadDate — The message is too old to get read date */
export class MessageReadDateTooOld {
  type: 'messageReadDateTooOld' = 'messageReadDateTooOld';
  extra?: string;
}

/** Decode a TDLib `messageReadDateTooOld` object; null input -> null. */
export function decodeMessageReadDateTooOld(json: TdJsonRaw): MessageReadDateTooOld | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReadDateTooOld = new MessageReadDateTooOld();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageReadDateTooOld` into TDLib JSON wire shape. */
export function encodeMessageReadDateTooOld(value: MessageReadDateTooOld): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReadDateTooOld';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageReadDateUnread = MessageReadDate — The message is unread yet */
export class MessageReadDateUnread {
  type: 'messageReadDateUnread' = 'messageReadDateUnread';
  extra?: string;
}

/** Decode a TDLib `messageReadDateUnread` object; null input -> null. */
export function decodeMessageReadDateUnread(json: TdJsonRaw): MessageReadDateUnread | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReadDateUnread = new MessageReadDateUnread();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageReadDateUnread` into TDLib JSON wire shape. */
export function encodeMessageReadDateUnread(value: MessageReadDateUnread): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReadDateUnread';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageReadDateUserPrivacyRestricted = MessageReadDate — The read date is unknown due to privacy settings of the other user */
export class MessageReadDateUserPrivacyRestricted {
  type: 'messageReadDateUserPrivacyRestricted' = 'messageReadDateUserPrivacyRestricted';
  extra?: string;
}

/** Decode a TDLib `messageReadDateUserPrivacyRestricted` object; null input -> null. */
export function decodeMessageReadDateUserPrivacyRestricted(json: TdJsonRaw): MessageReadDateUserPrivacyRestricted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReadDateUserPrivacyRestricted = new MessageReadDateUserPrivacyRestricted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageReadDateUserPrivacyRestricted` into TDLib JSON wire shape. */
export function encodeMessageReadDateUserPrivacyRestricted(value: MessageReadDateUserPrivacyRestricted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReadDateUserPrivacyRestricted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageRefundedUpgradedGift = MessageContent — A gift which purchase, upgrade or transfer were refunded */
export class MessageRefundedUpgradedGift {
  type: 'messageRefundedUpgradedGift' = 'messageRefundedUpgradedGift';
  extra?: string;
  /** gift: gift — The gift */
  gift: Gift | null = null;
  /** sender_id: MessageSender — Sender of the gift */
  sender_id: TdMessageSender | null = null;
  /** receiver_id: MessageSender — Receiver of the gift */
  receiver_id: TdMessageSender | null = null;
  /** origin: UpgradedGiftOrigin — Origin of the upgraded gift */
  origin: TdUpgradedGiftOrigin | null = null;
}

/** Decode a TDLib `messageRefundedUpgradedGift` object; null input -> null. */
export function decodeMessageRefundedUpgradedGift(json: TdJsonRaw): MessageRefundedUpgradedGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageRefundedUpgradedGift = new MessageRefundedUpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeGift(record['gift']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.receiver_id = decodeTdMessageSender(record['receiver_id']);
  out.origin = decodeTdUpgradedGiftOrigin(record['origin']);
  return out;
}

/** Encode a `messageRefundedUpgradedGift` into TDLib JSON wire shape. */
export function encodeMessageRefundedUpgradedGift(value: MessageRefundedUpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageRefundedUpgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['receiver_id'] = value.receiver_id === null ? null : encodeTdMessageSender(value.receiver_id);
  out['origin'] = value.origin === null ? null : encodeTdUpgradedGiftOrigin(value.origin);
  return out;
}

/** messageReplyInfo = MessageReplyInfo — Contains information about replies to a message */
export class MessageReplyInfo {
  type: 'messageReplyInfo' = 'messageReplyInfo';
  extra?: string;
  /** reply_count: int32 — Number of times the message was directly or indirectly replied */
  reply_count: number = 0;
  /** recent_replier_ids: vector<MessageSender> — Identifiers of at most 3 recent repliers to the message; available in channels with a discussion supergroup. The users and chats are expected to be inaccessible: only their photo and name will ... */
  recent_replier_ids: TdMessageSender[] = [];
  /** last_read_inbox_message_id: int53 — Identifier of the last read incoming reply to the message */
  last_read_inbox_message_id: number = 0;
  /** last_read_outbox_message_id: int53 — Identifier of the last read outgoing reply to the message */
  last_read_outbox_message_id: number = 0;
  /** last_message_id: int53 — Identifier of the last reply to the message */
  last_message_id: number = 0;
}

/** Decode a TDLib `messageReplyInfo` object; null input -> null. */
export function decodeMessageReplyInfo(json: TdJsonRaw): MessageReplyInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReplyInfo = new MessageReplyInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reply_count = asNumber(record['reply_count']);
  out.recent_replier_ids = decodeVector(record['recent_replier_ids'], (item: TdJsonRaw): TdMessageSender => decodeTdMessageSender(item) as TdMessageSender);
  out.last_read_inbox_message_id = asNumber(record['last_read_inbox_message_id']);
  out.last_read_outbox_message_id = asNumber(record['last_read_outbox_message_id']);
  out.last_message_id = asNumber(record['last_message_id']);
  return out;
}

/** Encode a `messageReplyInfo` into TDLib JSON wire shape. */
export function encodeMessageReplyInfo(value: MessageReplyInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReplyInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reply_count'] = value.reply_count;
  out['recent_replier_ids'] = encodeVector(value.recent_replier_ids, (item: TdMessageSender | null): TdJsonRaw => item === null ? null : encodeTdMessageSender(item));
  out['last_read_inbox_message_id'] = value.last_read_inbox_message_id;
  out['last_read_outbox_message_id'] = value.last_read_outbox_message_id;
  out['last_message_id'] = value.last_message_id;
  return out;
}

/** messageReplyToMessage = MessageReplyTo — Describes a message replied by a given message Can be only one of the following types: messageAnimation, messageAudio, messageChecklist, messageContact, messageDice, messageDocument, messageGame, ... */
export class MessageReplyToMessage {
  type: 'messageReplyToMessage' = 'messageReplyToMessage';
  extra?: string;
  /** chat_id: int53 — The identifier of the chat to which the message belongs; may be 0 if the replied message is in unknown chat */
  chat_id: number = 0;
  /** message_id: int53 — The identifier of the message; may be 0 if the replied message is in unknown chat */
  message_id: number = 0;
  /** quote: textQuote — Chosen quote from the replied message; may be null if none */
  quote: TextQuote | null = null;
  /** checklist_task_id: int32 — Identifier of the checklist task in the original message that was replied; 0 if none */
  checklist_task_id: number = 0;
  /** poll_option_id: string — Identifier of the poll option in the original message that was replied; empty if none */
  poll_option_id: string = '';
  /** origin: MessageOrigin — Information about origin of the message if the message was from another chat or topic; may be null for messages from the same chat */
  origin: TdMessageOrigin | null = null;
  /** origin_send_date: int32 — Point in time (Unix timestamp) when the message was sent if the message was from another chat or topic; 0 for messages from the same chat */
  origin_send_date: number = 0;
  /** content: MessageContent — Media content of the message if the message was from another chat or topic; may be null for messages from the same chat and messages without media. */
  content: TdMessageContent | null = null;
}

/** Decode a TDLib `messageReplyToMessage` object; null input -> null. */
export function decodeMessageReplyToMessage(json: TdJsonRaw): MessageReplyToMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReplyToMessage = new MessageReplyToMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.quote = decodeTextQuote(record['quote']);
  out.checklist_task_id = asNumber(record['checklist_task_id']);
  out.poll_option_id = asString(record['poll_option_id']);
  out.origin = decodeTdMessageOrigin(record['origin']);
  out.origin_send_date = asNumber(record['origin_send_date']);
  out.content = decodeTdMessageContent(record['content']);
  return out;
}

/** Encode a `messageReplyToMessage` into TDLib JSON wire shape. */
export function encodeMessageReplyToMessage(value: MessageReplyToMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReplyToMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['quote'] = value.quote === null ? null : encodeTextQuote(value.quote);
  out['checklist_task_id'] = value.checklist_task_id;
  out['poll_option_id'] = value.poll_option_id;
  out['origin'] = value.origin === null ? null : encodeTdMessageOrigin(value.origin);
  out['origin_send_date'] = value.origin_send_date;
  out['content'] = value.content === null ? null : encodeTdMessageContent(value.content);
  return out;
}

/** messageReplyToStory = MessageReplyTo — Describes a story replied by a given message */
export class MessageReplyToStory {
  type: 'messageReplyToStory' = 'messageReplyToStory';
  extra?: string;
  /** story_poster_chat_id: int53 — The identifier of the poster of the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — The identifier of the story */
  story_id: number = 0;
}

/** Decode a TDLib `messageReplyToStory` object; null input -> null. */
export function decodeMessageReplyToStory(json: TdJsonRaw): MessageReplyToStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageReplyToStory = new MessageReplyToStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `messageReplyToStory` into TDLib JSON wire shape. */
export function encodeMessageReplyToStory(value: MessageReplyToStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageReplyToStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** messageRichMessage = MessageContent — A rich message; the message can have multiple media of the same type, all of which must be shown in the corresponding profile tab */
export class MessageRichMessage {
  type: 'messageRichMessage' = 'messageRichMessage';
  extra?: string;
  /** message: richMessage — The rich message */
  message: RichMessage | null = null;
}

/** Decode a TDLib `messageRichMessage` object; null input -> null. */
export function decodeMessageRichMessage(json: TdJsonRaw): MessageRichMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageRichMessage = new MessageRichMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeRichMessage(record['message']);
  return out;
}

/** Encode a `messageRichMessage` into TDLib JSON wire shape. */
export function encodeMessageRichMessage(value: MessageRichMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageRichMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeRichMessage(value.message);
  return out;
}

/** messageSchedulingStateSendAtDate = MessageSchedulingState — The message will be sent at the specified date must be one of 0, 86400, 7 * 86400, 14 * 86400, 30 * 86400, 91 * 86400, 182 * 86400, 365 * 86400, or additionally 60, or 300 in th... */
export class MessageSchedulingStateSendAtDate {
  type: 'messageSchedulingStateSendAtDate' = 'messageSchedulingStateSendAtDate';
  extra?: string;
  /** send_date: int32 — Point in time (Unix timestamp) when the message will be sent. The date must be within 367 days in the future */
  send_date: number = 0;
  /** repeat_period: int32 — Period after which the message will be sent again; in seconds; 0 if never; for Telegram Premium users only; may be non-zero only in sendMessage and forwardMessages with one message requests; */
  repeat_period: number = 0;
}

/** Decode a TDLib `messageSchedulingStateSendAtDate` object; null input -> null. */
export function decodeMessageSchedulingStateSendAtDate(json: TdJsonRaw): MessageSchedulingStateSendAtDate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSchedulingStateSendAtDate = new MessageSchedulingStateSendAtDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.send_date = asNumber(record['send_date']);
  out.repeat_period = asNumber(record['repeat_period']);
  return out;
}

/** Encode a `messageSchedulingStateSendAtDate` into TDLib JSON wire shape. */
export function encodeMessageSchedulingStateSendAtDate(value: MessageSchedulingStateSendAtDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSchedulingStateSendAtDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['send_date'] = value.send_date;
  out['repeat_period'] = value.repeat_period;
  return out;
}

/** messageSchedulingStateSendWhenOnline = MessageSchedulingState — The message will be sent when the other user is online. Applicable to private chats only and when the exact online status of the other user is known */
export class MessageSchedulingStateSendWhenOnline {
  type: 'messageSchedulingStateSendWhenOnline' = 'messageSchedulingStateSendWhenOnline';
  extra?: string;
}

/** Decode a TDLib `messageSchedulingStateSendWhenOnline` object; null input -> null. */
export function decodeMessageSchedulingStateSendWhenOnline(json: TdJsonRaw): MessageSchedulingStateSendWhenOnline | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSchedulingStateSendWhenOnline = new MessageSchedulingStateSendWhenOnline();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSchedulingStateSendWhenOnline` into TDLib JSON wire shape. */
export function encodeMessageSchedulingStateSendWhenOnline(value: MessageSchedulingStateSendWhenOnline): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSchedulingStateSendWhenOnline';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSchedulingStateSendWhenVideoProcessed = MessageSchedulingState — The message will be sent when the video in the message is converted and optimized; can be used only by the server */
export class MessageSchedulingStateSendWhenVideoProcessed {
  type: 'messageSchedulingStateSendWhenVideoProcessed' = 'messageSchedulingStateSendWhenVideoProcessed';
  extra?: string;
  /** send_date: int32 — Approximate point in time (Unix timestamp) when the message is expected to be sent */
  send_date: number = 0;
}

/** Decode a TDLib `messageSchedulingStateSendWhenVideoProcessed` object; null input -> null. */
export function decodeMessageSchedulingStateSendWhenVideoProcessed(json: TdJsonRaw): MessageSchedulingStateSendWhenVideoProcessed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSchedulingStateSendWhenVideoProcessed = new MessageSchedulingStateSendWhenVideoProcessed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.send_date = asNumber(record['send_date']);
  return out;
}

/** Encode a `messageSchedulingStateSendWhenVideoProcessed` into TDLib JSON wire shape. */
export function encodeMessageSchedulingStateSendWhenVideoProcessed(value: MessageSchedulingStateSendWhenVideoProcessed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSchedulingStateSendWhenVideoProcessed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['send_date'] = value.send_date;
  return out;
}

/** messageScreenshotTaken = MessageContent — A screenshot of a message in the chat has been taken */
export class MessageScreenshotTaken {
  type: 'messageScreenshotTaken' = 'messageScreenshotTaken';
  extra?: string;
}

/** Decode a TDLib `messageScreenshotTaken` object; null input -> null. */
export function decodeMessageScreenshotTaken(json: TdJsonRaw): MessageScreenshotTaken | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageScreenshotTaken = new MessageScreenshotTaken();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageScreenshotTaken` into TDLib JSON wire shape. */
export function encodeMessageScreenshotTaken(value: MessageScreenshotTaken): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageScreenshotTaken';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSelfDestructTypeImmediately = MessageSelfDestructType — The message can be opened only once and will be self-destructed once closed */
export class MessageSelfDestructTypeImmediately {
  type: 'messageSelfDestructTypeImmediately' = 'messageSelfDestructTypeImmediately';
  extra?: string;
}

/** Decode a TDLib `messageSelfDestructTypeImmediately` object; null input -> null. */
export function decodeMessageSelfDestructTypeImmediately(json: TdJsonRaw): MessageSelfDestructTypeImmediately | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSelfDestructTypeImmediately = new MessageSelfDestructTypeImmediately();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSelfDestructTypeImmediately` into TDLib JSON wire shape. */
export function encodeMessageSelfDestructTypeImmediately(value: MessageSelfDestructTypeImmediately): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSelfDestructTypeImmediately';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSelfDestructTypeTimer = MessageSelfDestructType — The message will be self-destructed in the specified time after its content was opened */
export class MessageSelfDestructTypeTimer {
  type: 'messageSelfDestructTypeTimer' = 'messageSelfDestructTypeTimer';
  extra?: string;
  /** self_destruct_time: int32 — The message's self-destruct time, in seconds; must be between 0 and 60 in private chats */
  self_destruct_time: number = 0;
}

/** Decode a TDLib `messageSelfDestructTypeTimer` object; null input -> null. */
export function decodeMessageSelfDestructTypeTimer(json: TdJsonRaw): MessageSelfDestructTypeTimer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSelfDestructTypeTimer = new MessageSelfDestructTypeTimer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.self_destruct_time = asNumber(record['self_destruct_time']);
  return out;
}

/** Encode a `messageSelfDestructTypeTimer` into TDLib JSON wire shape. */
export function encodeMessageSelfDestructTypeTimer(value: MessageSelfDestructTypeTimer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSelfDestructTypeTimer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['self_destruct_time'] = value.self_destruct_time;
  return out;
}

/** messageSendOptions = MessageSendOptions — Options to be used when a message is sent live location messages and self-destructing messages can't be scheduled */
export class MessageSendOptions {
  type: 'messageSendOptions' = 'messageSendOptions';
  extra?: string;
  /** suggested_post_info: inputSuggestedPostInfo — Information about the suggested post; pass null if none. For messages to channel direct messages chat only. Applicable only to sendMessage and addOffer */
  suggested_post_info: InputSuggestedPostInfo | null = null;
  /** disable_notification: Bool — Pass true to disable notification for the message */
  disable_notification: boolean = false;
  /** from_background: Bool — Pass true if the message is sent from the background */
  from_background: boolean = false;
  /** protect_content: Bool — Pass true if the content of the message must be protected from forwarding and saving; for bots only */
  protect_content: boolean = false;
  /** allow_paid_broadcast: Bool — Pass true to allow the message to ignore regular broadcast limits for a small fee; for bots only */
  allow_paid_broadcast: boolean = false;
  /** paid_message_star_count: int53 — The number of Telegram Stars the user agreed to pay to send the messages */
  paid_message_star_count: number = 0;
  /** update_order_of_installed_sticker_sets: Bool — Pass true if the user explicitly chosen a sticker or a custom emoji from an installed sticker set; applicable only to sendMessage and sendMessageAlbum */
  update_order_of_installed_sticker_sets: boolean = false;
  /** scheduling_state: MessageSchedulingState — Message scheduling state; pass null to send message immediately. Messages sent to a secret chat, to a chat with paid messages, to a channel direct messages chat, */
  scheduling_state: TdMessageSchedulingState | null = null;
  /** effect_id: int64 — Identifier of the effect to apply to the message; pass 0 if none; applicable only to sendMessage, sendMessageAlbum in private chats and forwardMessages with one message to private chats */
  effect_id: string = '';
  /** sending_id: int32 — Non-persistent identifier, which will be returned back in messageSendingStatePending object and can be used to match sent messages and corresponding updateNewMessage updates */
  sending_id: number = 0;
  /** only_preview: Bool — Pass true to get a fake message instead of actually sending them */
  only_preview: boolean = false;
}

/** Decode a TDLib `messageSendOptions` object; null input -> null. */
export function decodeMessageSendOptions(json: TdJsonRaw): MessageSendOptions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSendOptions = new MessageSendOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.suggested_post_info = decodeInputSuggestedPostInfo(record['suggested_post_info']);
  out.disable_notification = asBool(record['disable_notification']);
  out.from_background = asBool(record['from_background']);
  out.protect_content = asBool(record['protect_content']);
  out.allow_paid_broadcast = asBool(record['allow_paid_broadcast']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  out.update_order_of_installed_sticker_sets = asBool(record['update_order_of_installed_sticker_sets']);
  out.scheduling_state = decodeTdMessageSchedulingState(record['scheduling_state']);
  out.effect_id = asInt64(record['effect_id']);
  out.sending_id = asNumber(record['sending_id']);
  out.only_preview = asBool(record['only_preview']);
  return out;
}

/** Encode a `messageSendOptions` into TDLib JSON wire shape. */
export function encodeMessageSendOptions(value: MessageSendOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSendOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['suggested_post_info'] = value.suggested_post_info === null ? null : encodeInputSuggestedPostInfo(value.suggested_post_info);
  out['disable_notification'] = value.disable_notification;
  out['from_background'] = value.from_background;
  out['protect_content'] = value.protect_content;
  out['allow_paid_broadcast'] = value.allow_paid_broadcast;
  out['paid_message_star_count'] = value.paid_message_star_count;
  out['update_order_of_installed_sticker_sets'] = value.update_order_of_installed_sticker_sets;
  out['scheduling_state'] = value.scheduling_state === null ? null : encodeTdMessageSchedulingState(value.scheduling_state);
  out['effect_id'] = value.effect_id;
  out['sending_id'] = value.sending_id;
  out['only_preview'] = value.only_preview;
  return out;
}

/** messageSenderChat = MessageSender — The message was sent on behalf of a chat */
export class MessageSenderChat {
  type: 'messageSenderChat' = 'messageSenderChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that sent the message */
  chat_id: number = 0;
}

/** Decode a TDLib `messageSenderChat` object; null input -> null. */
export function decodeMessageSenderChat(json: TdJsonRaw): MessageSenderChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSenderChat = new MessageSenderChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `messageSenderChat` into TDLib JSON wire shape. */
export function encodeMessageSenderChat(value: MessageSenderChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSenderChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** messageSenderUser = MessageSender — The message was sent by a known user */
export class MessageSenderUser {
  type: 'messageSenderUser' = 'messageSenderUser';
  extra?: string;
  /** user_id: int53 — Identifier of the user who sent the message */
  user_id: number = 0;
}

/** Decode a TDLib `messageSenderUser` object; null input -> null. */
export function decodeMessageSenderUser(json: TdJsonRaw): MessageSenderUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSenderUser = new MessageSenderUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `messageSenderUser` into TDLib JSON wire shape. */
export function encodeMessageSenderUser(value: MessageSenderUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSenderUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** messageSenders = MessageSenders — Represents a list of message senders */
export class MessageSenders {
  type: 'messageSenders' = 'messageSenders';
  extra?: string;
  /** total_count: int32 — Approximate total number of message senders found */
  total_count: number = 0;
  /** senders: vector<MessageSender> — List of message senders */
  senders: TdMessageSender[] = [];
}

/** Decode a TDLib `messageSenders` object; null input -> null. */
export function decodeMessageSenders(json: TdJsonRaw): MessageSenders | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSenders = new MessageSenders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.senders = decodeVector(record['senders'], (item: TdJsonRaw): TdMessageSender => decodeTdMessageSender(item) as TdMessageSender);
  return out;
}

/** Encode a `messageSenders` into TDLib JSON wire shape. */
export function encodeMessageSenders(value: MessageSenders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSenders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['senders'] = encodeVector(value.senders, (item: TdMessageSender | null): TdJsonRaw => item === null ? null : encodeTdMessageSender(item));
  return out;
}

/** messageSendingStateFailed = MessageSendingState — The message failed to be sent */
export class MessageSendingStateFailed {
  type: 'messageSendingStateFailed' = 'messageSendingStateFailed';
  extra?: string;
  /** error: error — The cause of the message sending failure */
  error: ErrorValue | null = null;
  /** can_retry: Bool — True, if the message can be re-sent using resendMessages or readdQuickReplyShortcutMessages */
  can_retry: boolean = false;
  /** need_another_sender: Bool — True, if the message can be re-sent only on behalf of a different sender */
  need_another_sender: boolean = false;
  /** need_another_reply_quote: Bool — True, if the message can be re-sent only if another quote is chosen in the message that is replied by the given message */
  need_another_reply_quote: boolean = false;
  /** need_drop_reply: Bool — True, if the message can be re-sent only if the message to be replied is removed. This will be done automatically by resendMessages */
  need_drop_reply: boolean = false;
  /** required_paid_message_star_count: int53 — The number of Telegram Stars that must be paid to send the message; 0 if the current amount is correct */
  required_paid_message_star_count: number = 0;
  /** retry_after: double — Time left before the message can be re-sent, in seconds. No update is sent when this field changes */
  retry_after: number = 0;
}

/** Decode a TDLib `messageSendingStateFailed` object; null input -> null. */
export function decodeMessageSendingStateFailed(json: TdJsonRaw): MessageSendingStateFailed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSendingStateFailed = new MessageSendingStateFailed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.error = decodeErrorValue(record['error']);
  out.can_retry = asBool(record['can_retry']);
  out.need_another_sender = asBool(record['need_another_sender']);
  out.need_another_reply_quote = asBool(record['need_another_reply_quote']);
  out.need_drop_reply = asBool(record['need_drop_reply']);
  out.required_paid_message_star_count = asNumber(record['required_paid_message_star_count']);
  out.retry_after = asNumber(record['retry_after']);
  return out;
}

/** Encode a `messageSendingStateFailed` into TDLib JSON wire shape. */
export function encodeMessageSendingStateFailed(value: MessageSendingStateFailed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSendingStateFailed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['error'] = value.error === null ? null : encodeErrorValue(value.error);
  out['can_retry'] = value.can_retry;
  out['need_another_sender'] = value.need_another_sender;
  out['need_another_reply_quote'] = value.need_another_reply_quote;
  out['need_drop_reply'] = value.need_drop_reply;
  out['required_paid_message_star_count'] = value.required_paid_message_star_count;
  out['retry_after'] = value.retry_after;
  return out;
}

/** messageSendingStatePending = MessageSendingState — The message is being sent now, but has not yet been delivered to the server */
export class MessageSendingStatePending {
  type: 'messageSendingStatePending' = 'messageSendingStatePending';
  extra?: string;
  /** sending_id: int32 — Non-persistent message sending identifier, specified by the application */
  sending_id: number = 0;
}

/** Decode a TDLib `messageSendingStatePending` object; null input -> null. */
export function decodeMessageSendingStatePending(json: TdJsonRaw): MessageSendingStatePending | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSendingStatePending = new MessageSendingStatePending();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sending_id = asNumber(record['sending_id']);
  return out;
}

/** Encode a `messageSendingStatePending` into TDLib JSON wire shape. */
export function encodeMessageSendingStatePending(value: MessageSendingStatePending): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSendingStatePending';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sending_id'] = value.sending_id;
  return out;
}

/** messageSourceChatEventLog = MessageSource — The message is from a chat event log */
export class MessageSourceChatEventLog {
  type: 'messageSourceChatEventLog' = 'messageSourceChatEventLog';
  extra?: string;
}

/** Decode a TDLib `messageSourceChatEventLog` object; null input -> null. */
export function decodeMessageSourceChatEventLog(json: TdJsonRaw): MessageSourceChatEventLog | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceChatEventLog = new MessageSourceChatEventLog();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceChatEventLog` into TDLib JSON wire shape. */
export function encodeMessageSourceChatEventLog(value: MessageSourceChatEventLog): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceChatEventLog';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceChatHistory = MessageSource — The message is from a chat history */
export class MessageSourceChatHistory {
  type: 'messageSourceChatHistory' = 'messageSourceChatHistory';
  extra?: string;
}

/** Decode a TDLib `messageSourceChatHistory` object; null input -> null. */
export function decodeMessageSourceChatHistory(json: TdJsonRaw): MessageSourceChatHistory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceChatHistory = new MessageSourceChatHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceChatHistory` into TDLib JSON wire shape. */
export function encodeMessageSourceChatHistory(value: MessageSourceChatHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceChatHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceChatList = MessageSource — The message is from a chat list or a forum topic list */
export class MessageSourceChatList {
  type: 'messageSourceChatList' = 'messageSourceChatList';
  extra?: string;
}

/** Decode a TDLib `messageSourceChatList` object; null input -> null. */
export function decodeMessageSourceChatList(json: TdJsonRaw): MessageSourceChatList | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceChatList = new MessageSourceChatList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceChatList` into TDLib JSON wire shape. */
export function encodeMessageSourceChatList(value: MessageSourceChatList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceChatList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceDirectMessagesChatTopicHistory = MessageSource — The message is from history of a topic in a channel direct messages chat administered by the current user */
export class MessageSourceDirectMessagesChatTopicHistory {
  type: 'messageSourceDirectMessagesChatTopicHistory' = 'messageSourceDirectMessagesChatTopicHistory';
  extra?: string;
}

/** Decode a TDLib `messageSourceDirectMessagesChatTopicHistory` object; null input -> null. */
export function decodeMessageSourceDirectMessagesChatTopicHistory(json: TdJsonRaw): MessageSourceDirectMessagesChatTopicHistory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceDirectMessagesChatTopicHistory = new MessageSourceDirectMessagesChatTopicHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceDirectMessagesChatTopicHistory` into TDLib JSON wire shape. */
export function encodeMessageSourceDirectMessagesChatTopicHistory(value: MessageSourceDirectMessagesChatTopicHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceDirectMessagesChatTopicHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceForumTopicHistory = MessageSource — The message is from history of a forum topic */
export class MessageSourceForumTopicHistory {
  type: 'messageSourceForumTopicHistory' = 'messageSourceForumTopicHistory';
  extra?: string;
}

/** Decode a TDLib `messageSourceForumTopicHistory` object; null input -> null. */
export function decodeMessageSourceForumTopicHistory(json: TdJsonRaw): MessageSourceForumTopicHistory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceForumTopicHistory = new MessageSourceForumTopicHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceForumTopicHistory` into TDLib JSON wire shape. */
export function encodeMessageSourceForumTopicHistory(value: MessageSourceForumTopicHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceForumTopicHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceHistoryPreview = MessageSource — The message is from chat, message thread or forum topic history preview */
export class MessageSourceHistoryPreview {
  type: 'messageSourceHistoryPreview' = 'messageSourceHistoryPreview';
  extra?: string;
}

/** Decode a TDLib `messageSourceHistoryPreview` object; null input -> null. */
export function decodeMessageSourceHistoryPreview(json: TdJsonRaw): MessageSourceHistoryPreview | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceHistoryPreview = new MessageSourceHistoryPreview();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceHistoryPreview` into TDLib JSON wire shape. */
export function encodeMessageSourceHistoryPreview(value: MessageSourceHistoryPreview): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceHistoryPreview';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceMessageThreadHistory = MessageSource — The message is from history of a message thread */
export class MessageSourceMessageThreadHistory {
  type: 'messageSourceMessageThreadHistory' = 'messageSourceMessageThreadHistory';
  extra?: string;
}

/** Decode a TDLib `messageSourceMessageThreadHistory` object; null input -> null. */
export function decodeMessageSourceMessageThreadHistory(json: TdJsonRaw): MessageSourceMessageThreadHistory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceMessageThreadHistory = new MessageSourceMessageThreadHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceMessageThreadHistory` into TDLib JSON wire shape. */
export function encodeMessageSourceMessageThreadHistory(value: MessageSourceMessageThreadHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceMessageThreadHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceNotification = MessageSource — The message is from a notification */
export class MessageSourceNotification {
  type: 'messageSourceNotification' = 'messageSourceNotification';
  extra?: string;
}

/** Decode a TDLib `messageSourceNotification` object; null input -> null. */
export function decodeMessageSourceNotification(json: TdJsonRaw): MessageSourceNotification | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceNotification = new MessageSourceNotification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceNotification` into TDLib JSON wire shape. */
export function encodeMessageSourceNotification(value: MessageSourceNotification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceNotification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceOther = MessageSource — The message is from some other source */
export class MessageSourceOther {
  type: 'messageSourceOther' = 'messageSourceOther';
  extra?: string;
}

/** Decode a TDLib `messageSourceOther` object; null input -> null. */
export function decodeMessageSourceOther(json: TdJsonRaw): MessageSourceOther | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceOther = new MessageSourceOther();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceOther` into TDLib JSON wire shape. */
export function encodeMessageSourceOther(value: MessageSourceOther): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceOther';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceScreenshot = MessageSource — The message was screenshotted; the source must be used only if the message content was visible during the screenshot */
export class MessageSourceScreenshot {
  type: 'messageSourceScreenshot' = 'messageSourceScreenshot';
  extra?: string;
}

/** Decode a TDLib `messageSourceScreenshot` object; null input -> null. */
export function decodeMessageSourceScreenshot(json: TdJsonRaw): MessageSourceScreenshot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceScreenshot = new MessageSourceScreenshot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceScreenshot` into TDLib JSON wire shape. */
export function encodeMessageSourceScreenshot(value: MessageSourceScreenshot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceScreenshot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageSourceSearch = MessageSource — The message is from search results, including file downloads, local file list, outgoing document messages, calendar */
export class MessageSourceSearch {
  type: 'messageSourceSearch' = 'messageSourceSearch';
  extra?: string;
}

/** Decode a TDLib `messageSourceSearch` object; null input -> null. */
export function decodeMessageSourceSearch(json: TdJsonRaw): MessageSourceSearch | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSourceSearch = new MessageSourceSearch();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageSourceSearch` into TDLib JSON wire shape. */
export function encodeMessageSourceSearch(value: MessageSourceSearch): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSourceSearch';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageStakeDice = MessageContent — A stake dice message. The dice value is randomly generated by the server */
export class MessageStakeDice {
  type: 'messageStakeDice' = 'messageStakeDice';
  extra?: string;
  /** initial_state: DiceStickers — The animated stickers with the initial dice animation; may be null if unknown. The update updateMessageContent will be sent when the sticker became known */
  initial_state: TdDiceStickers | null = null;
  /** final_state: DiceStickers — The animated stickers with the final dice animation; may be null if unknown. The update updateMessageContent will be sent when the sticker became known */
  final_state: TdDiceStickers | null = null;
  /** value: int32 — The dice value. If the value is 0, then the dice don't have final state yet */
  value: number = 0;
  /** stake_gram_amount: int53 — The TON Gram amount that was staked; in the smallest units of the currency */
  stake_gram_amount: number = 0;
  /** prize_gram_amount: int53 — The TON Gram amount that was gained from the roll; in the smallest units of the currency; -1 if the dice don't have final state yet */
  prize_gram_amount: number = 0;
}

/** Decode a TDLib `messageStakeDice` object; null input -> null. */
export function decodeMessageStakeDice(json: TdJsonRaw): MessageStakeDice | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageStakeDice = new MessageStakeDice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.initial_state = decodeTdDiceStickers(record['initial_state']);
  out.final_state = decodeTdDiceStickers(record['final_state']);
  out.value = asNumber(record['value']);
  out.stake_gram_amount = asNumber(record['stake_gram_amount']);
  out.prize_gram_amount = asNumber(record['prize_gram_amount']);
  return out;
}

/** Encode a `messageStakeDice` into TDLib JSON wire shape. */
export function encodeMessageStakeDice(value: MessageStakeDice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageStakeDice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['initial_state'] = value.initial_state === null ? null : encodeTdDiceStickers(value.initial_state);
  out['final_state'] = value.final_state === null ? null : encodeTdDiceStickers(value.final_state);
  out['value'] = value.value;
  out['stake_gram_amount'] = value.stake_gram_amount;
  out['prize_gram_amount'] = value.prize_gram_amount;
  return out;
}

/** messageStatistics = MessageStatistics — A detailed statistics about a message */
export class MessageStatistics {
  type: 'messageStatistics' = 'messageStatistics';
  extra?: string;
  /** message_interaction_graph: StatisticalGraph — A graph containing number of message views and shares */
  message_interaction_graph: TdStatisticalGraph | null = null;
  /** message_reaction_graph: StatisticalGraph — A graph containing number of message reactions */
  message_reaction_graph: TdStatisticalGraph | null = null;
}

/** Decode a TDLib `messageStatistics` object; null input -> null. */
export function decodeMessageStatistics(json: TdJsonRaw): MessageStatistics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageStatistics = new MessageStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_interaction_graph = decodeTdStatisticalGraph(record['message_interaction_graph']);
  out.message_reaction_graph = decodeTdStatisticalGraph(record['message_reaction_graph']);
  return out;
}

/** Encode a `messageStatistics` into TDLib JSON wire shape. */
export function encodeMessageStatistics(value: MessageStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_interaction_graph'] = value.message_interaction_graph === null ? null : encodeTdStatisticalGraph(value.message_interaction_graph);
  out['message_reaction_graph'] = value.message_reaction_graph === null ? null : encodeTdStatisticalGraph(value.message_reaction_graph);
  return out;
}

/** messageSticker = MessageContent — A sticker message */
export class MessageSticker {
  type: 'messageSticker' = 'messageSticker';
  extra?: string;
  /** sticker: sticker — The sticker description */
  sticker: Sticker | null = null;
  /** is_premium: Bool — True, if premium animation of the sticker must be played */
  is_premium: boolean = false;
}

/** Decode a TDLib `messageSticker` object; null input -> null. */
export function decodeMessageSticker(json: TdJsonRaw): MessageSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSticker = new MessageSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeSticker(record['sticker']);
  out.is_premium = asBool(record['is_premium']);
  return out;
}

/** Encode a `messageSticker` into TDLib JSON wire shape. */
export function encodeMessageSticker(value: MessageSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  out['is_premium'] = value.is_premium;
  return out;
}

/** messageStory = MessageContent — A message with a forwarded story */
export class MessageStory {
  type: 'messageStory' = 'messageStory';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
  /** via_mention: Bool — True, if the story was automatically forwarded because of a mention of the user */
  via_mention: boolean = false;
}

/** Decode a TDLib `messageStory` object; null input -> null. */
export function decodeMessageStory(json: TdJsonRaw): MessageStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageStory = new MessageStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.via_mention = asBool(record['via_mention']);
  return out;
}

/** Encode a `messageStory` into TDLib JSON wire shape. */
export function encodeMessageStory(value: MessageStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['via_mention'] = value.via_mention;
  return out;
}

/** messageSuggestBirthdate = MessageContent — A birthdate was suggested to be set */
export class MessageSuggestBirthdate {
  type: 'messageSuggestBirthdate' = 'messageSuggestBirthdate';
  extra?: string;
  /** birthdate: birthdate — The suggested birthdate. Use the method setBirthdate to apply the birthdate */
  birthdate: Birthdate | null = null;
}

/** Decode a TDLib `messageSuggestBirthdate` object; null input -> null. */
export function decodeMessageSuggestBirthdate(json: TdJsonRaw): MessageSuggestBirthdate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSuggestBirthdate = new MessageSuggestBirthdate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.birthdate = decodeBirthdate(record['birthdate']);
  return out;
}

/** Encode a `messageSuggestBirthdate` into TDLib JSON wire shape. */
export function encodeMessageSuggestBirthdate(value: MessageSuggestBirthdate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSuggestBirthdate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['birthdate'] = value.birthdate === null ? null : encodeBirthdate(value.birthdate);
  return out;
}

/** messageSuggestProfilePhoto = MessageContent — A profile photo was suggested to a user in a private chat */
export class MessageSuggestProfilePhoto {
  type: 'messageSuggestProfilePhoto' = 'messageSuggestProfilePhoto';
  extra?: string;
  /** photo: chatPhoto — The suggested chat photo. Use the method setProfilePhoto with inputChatPhotoPrevious to apply the photo */
  photo: ChatPhoto | null = null;
}

/** Decode a TDLib `messageSuggestProfilePhoto` object; null input -> null. */
export function decodeMessageSuggestProfilePhoto(json: TdJsonRaw): MessageSuggestProfilePhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSuggestProfilePhoto = new MessageSuggestProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodeChatPhoto(record['photo']);
  return out;
}

/** Encode a `messageSuggestProfilePhoto` into TDLib JSON wire shape. */
export function encodeMessageSuggestProfilePhoto(value: MessageSuggestProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSuggestProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  return out;
}

/** messageSuggestedPostApprovalFailed = MessageContent — Approval of suggested post has failed, because the user who proposed the post didn't have enough funds */
export class MessageSuggestedPostApprovalFailed {
  type: 'messageSuggestedPostApprovalFailed' = 'messageSuggestedPostApprovalFailed';
  extra?: string;
  /** suggested_post_message_id: int53 — Identifier of the message with the suggested post; may be 0 or an identifier of a deleted message */
  suggested_post_message_id: number = 0;
  /** price: SuggestedPostPrice — Price of the suggested post */
  price: TdSuggestedPostPrice | null = null;
}

/** Decode a TDLib `messageSuggestedPostApprovalFailed` object; null input -> null. */
export function decodeMessageSuggestedPostApprovalFailed(json: TdJsonRaw): MessageSuggestedPostApprovalFailed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSuggestedPostApprovalFailed = new MessageSuggestedPostApprovalFailed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.suggested_post_message_id = asNumber(record['suggested_post_message_id']);
  out.price = decodeTdSuggestedPostPrice(record['price']);
  return out;
}

/** Encode a `messageSuggestedPostApprovalFailed` into TDLib JSON wire shape. */
export function encodeMessageSuggestedPostApprovalFailed(value: MessageSuggestedPostApprovalFailed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSuggestedPostApprovalFailed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['suggested_post_message_id'] = value.suggested_post_message_id;
  out['price'] = value.price === null ? null : encodeTdSuggestedPostPrice(value.price);
  return out;
}

/** messageSuggestedPostApproved = MessageContent — A suggested post was approved */
export class MessageSuggestedPostApproved {
  type: 'messageSuggestedPostApproved' = 'messageSuggestedPostApproved';
  extra?: string;
  /** suggested_post_message_id: int53 — Identifier of the message with the suggested post; may be 0 or an identifier of a deleted message */
  suggested_post_message_id: number = 0;
  /** price: SuggestedPostPrice — Price of the suggested post; may be null if the post is non-paid */
  price: TdSuggestedPostPrice | null = null;
  /** send_date: int32 — Point in time (Unix timestamp) when the post is expected to be published */
  send_date: number = 0;
}

/** Decode a TDLib `messageSuggestedPostApproved` object; null input -> null. */
export function decodeMessageSuggestedPostApproved(json: TdJsonRaw): MessageSuggestedPostApproved | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSuggestedPostApproved = new MessageSuggestedPostApproved();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.suggested_post_message_id = asNumber(record['suggested_post_message_id']);
  out.price = decodeTdSuggestedPostPrice(record['price']);
  out.send_date = asNumber(record['send_date']);
  return out;
}

/** Encode a `messageSuggestedPostApproved` into TDLib JSON wire shape. */
export function encodeMessageSuggestedPostApproved(value: MessageSuggestedPostApproved): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSuggestedPostApproved';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['suggested_post_message_id'] = value.suggested_post_message_id;
  out['price'] = value.price === null ? null : encodeTdSuggestedPostPrice(value.price);
  out['send_date'] = value.send_date;
  return out;
}

/** messageSuggestedPostDeclined = MessageContent — A suggested post was declined */
export class MessageSuggestedPostDeclined {
  type: 'messageSuggestedPostDeclined' = 'messageSuggestedPostDeclined';
  extra?: string;
  /** suggested_post_message_id: int53 — Identifier of the message with the suggested post; may be 0 or an identifier of a deleted message */
  suggested_post_message_id: number = 0;
  /** comment: string — Comment added by administrator of the channel when the post was declined */
  comment: string = '';
}

/** Decode a TDLib `messageSuggestedPostDeclined` object; null input -> null. */
export function decodeMessageSuggestedPostDeclined(json: TdJsonRaw): MessageSuggestedPostDeclined | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSuggestedPostDeclined = new MessageSuggestedPostDeclined();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.suggested_post_message_id = asNumber(record['suggested_post_message_id']);
  out.comment = asString(record['comment']);
  return out;
}

/** Encode a `messageSuggestedPostDeclined` into TDLib JSON wire shape. */
export function encodeMessageSuggestedPostDeclined(value: MessageSuggestedPostDeclined): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSuggestedPostDeclined';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['suggested_post_message_id'] = value.suggested_post_message_id;
  out['comment'] = value.comment;
  return out;
}

/** messageSuggestedPostPaid = MessageContent — A suggested post was published for getOption("suggested_post_lifetime_min") seconds and payment for the post was received */
export class MessageSuggestedPostPaid {
  type: 'messageSuggestedPostPaid' = 'messageSuggestedPostPaid';
  extra?: string;
  /** suggested_post_message_id: int53 — Identifier of the message with the suggested post; may be 0 or an identifier of a deleted message */
  suggested_post_message_id: number = 0;
  /** star_amount: starAmount — The amount of received Telegram Stars */
  star_amount: StarAmount | null = null;
  /** gram_amount: int53 — The amount of received TON Grams; in the smallest units of the cryptocurrency */
  gram_amount: number = 0;
}

/** Decode a TDLib `messageSuggestedPostPaid` object; null input -> null. */
export function decodeMessageSuggestedPostPaid(json: TdJsonRaw): MessageSuggestedPostPaid | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSuggestedPostPaid = new MessageSuggestedPostPaid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.suggested_post_message_id = asNumber(record['suggested_post_message_id']);
  out.star_amount = decodeStarAmount(record['star_amount']);
  out.gram_amount = asNumber(record['gram_amount']);
  return out;
}

/** Encode a `messageSuggestedPostPaid` into TDLib JSON wire shape. */
export function encodeMessageSuggestedPostPaid(value: MessageSuggestedPostPaid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSuggestedPostPaid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['suggested_post_message_id'] = value.suggested_post_message_id;
  out['star_amount'] = value.star_amount === null ? null : encodeStarAmount(value.star_amount);
  out['gram_amount'] = value.gram_amount;
  return out;
}

/** messageSuggestedPostRefunded = MessageContent — A suggested post was refunded */
export class MessageSuggestedPostRefunded {
  type: 'messageSuggestedPostRefunded' = 'messageSuggestedPostRefunded';
  extra?: string;
  /** suggested_post_message_id: int53 — Identifier of the message with the suggested post; may be 0 or an identifier of a deleted message */
  suggested_post_message_id: number = 0;
  /** reason: SuggestedPostRefundReason — Reason of the refund */
  reason: TdSuggestedPostRefundReason | null = null;
}

/** Decode a TDLib `messageSuggestedPostRefunded` object; null input -> null. */
export function decodeMessageSuggestedPostRefunded(json: TdJsonRaw): MessageSuggestedPostRefunded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSuggestedPostRefunded = new MessageSuggestedPostRefunded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.suggested_post_message_id = asNumber(record['suggested_post_message_id']);
  out.reason = decodeTdSuggestedPostRefundReason(record['reason']);
  return out;
}

/** Encode a `messageSuggestedPostRefunded` into TDLib JSON wire shape. */
export function encodeMessageSuggestedPostRefunded(value: MessageSuggestedPostRefunded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSuggestedPostRefunded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['suggested_post_message_id'] = value.suggested_post_message_id;
  out['reason'] = value.reason === null ? null : encodeTdSuggestedPostRefundReason(value.reason);
  return out;
}

/** messageSupergroupChatCreate = MessageContent — A newly created supergroup or channel */
export class MessageSupergroupChatCreate {
  type: 'messageSupergroupChatCreate' = 'messageSupergroupChatCreate';
  extra?: string;
  /** title: string — Title of the supergroup or channel */
  title: string = '';
}

/** Decode a TDLib `messageSupergroupChatCreate` object; null input -> null. */
export function decodeMessageSupergroupChatCreate(json: TdJsonRaw): MessageSupergroupChatCreate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageSupergroupChatCreate = new MessageSupergroupChatCreate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  return out;
}

/** Encode a `messageSupergroupChatCreate` into TDLib JSON wire shape. */
export function encodeMessageSupergroupChatCreate(value: MessageSupergroupChatCreate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageSupergroupChatCreate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  return out;
}

/** messageText = MessageContent — A text message */
export class MessageText {
  type: 'messageText' = 'messageText';
  extra?: string;
  /** text: formattedText — Text of the message */
  text: FormattedText | null = null;
  /** link_preview: linkPreview — A link preview attached to the message; may be null */
  link_preview: LinkPreview | null = null;
  /** link_preview_options: linkPreviewOptions — Options which were used for generation of the link preview; may be null if default options were used */
  link_preview_options: LinkPreviewOptions | null = null;
}

/** Decode a TDLib `messageText` object; null input -> null. */
export function decodeMessageText(json: TdJsonRaw): MessageText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageText = new MessageText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.link_preview = decodeLinkPreview(record['link_preview']);
  out.link_preview_options = decodeLinkPreviewOptions(record['link_preview_options']);
  return out;
}

/** Encode a `messageText` into TDLib JSON wire shape. */
export function encodeMessageText(value: MessageText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['link_preview'] = value.link_preview === null ? null : encodeLinkPreview(value.link_preview);
  out['link_preview_options'] = value.link_preview_options === null ? null : encodeLinkPreviewOptions(value.link_preview_options);
  return out;
}

/** messageThreadInfo = MessageThreadInfo — Contains information about a message thread */
export class MessageThreadInfo {
  type: 'messageThreadInfo' = 'messageThreadInfo';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message thread belongs */
  chat_id: number = 0;
  /** message_thread_id: int53 — Message thread identifier, unique within the chat */
  message_thread_id: number = 0;
  /** reply_info: messageReplyInfo — Information about the message thread; may be null for forum topic threads */
  reply_info: MessageReplyInfo | null = null;
  /** unread_message_count: int32 — Approximate number of unread messages in the message thread */
  unread_message_count: number = 0;
  /** messages: vector<message> — The messages from which the thread starts. The messages are returned in reverse chronological order (i.e., in order of decreasing message_id) */
  messages: Message[] = [];
  /** draft_message: draftMessage — A draft of a message in the message thread; may be null if none */
  draft_message: DraftMessage | null = null;
}

/** Decode a TDLib `messageThreadInfo` object; null input -> null. */
export function decodeMessageThreadInfo(json: TdJsonRaw): MessageThreadInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageThreadInfo = new MessageThreadInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_thread_id = asNumber(record['message_thread_id']);
  out.reply_info = decodeMessageReplyInfo(record['reply_info']);
  out.unread_message_count = asNumber(record['unread_message_count']);
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): Message => decodeMessage(item) as Message);
  out.draft_message = decodeDraftMessage(record['draft_message']);
  return out;
}

/** Encode a `messageThreadInfo` into TDLib JSON wire shape. */
export function encodeMessageThreadInfo(value: MessageThreadInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageThreadInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_thread_id'] = value.message_thread_id;
  out['reply_info'] = value.reply_info === null ? null : encodeMessageReplyInfo(value.reply_info);
  out['unread_message_count'] = value.unread_message_count;
  out['messages'] = encodeVector(value.messages, (item: Message | null): TdJsonRaw => item === null ? null : encodeMessage(item));
  out['draft_message'] = value.draft_message === null ? null : encodeDraftMessage(value.draft_message);
  return out;
}

/** messageTopicDirectMessages = MessageTopic — A topic in a channel direct messages chat administered by the current user */
export class MessageTopicDirectMessages {
  type: 'messageTopicDirectMessages' = 'messageTopicDirectMessages';
  extra?: string;
  /** direct_messages_chat_topic_id: int53 — Unique identifier of the topic */
  direct_messages_chat_topic_id: number = 0;
}

/** Decode a TDLib `messageTopicDirectMessages` object; null input -> null. */
export function decodeMessageTopicDirectMessages(json: TdJsonRaw): MessageTopicDirectMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageTopicDirectMessages = new MessageTopicDirectMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.direct_messages_chat_topic_id = asNumber(record['direct_messages_chat_topic_id']);
  return out;
}

/** Encode a `messageTopicDirectMessages` into TDLib JSON wire shape. */
export function encodeMessageTopicDirectMessages(value: MessageTopicDirectMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageTopicDirectMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['direct_messages_chat_topic_id'] = value.direct_messages_chat_topic_id;
  return out;
}

/** messageTopicForum = MessageTopic — A topic in a forum supergroup chat or a chat with a bot */
export class MessageTopicForum {
  type: 'messageTopicForum' = 'messageTopicForum';
  extra?: string;
  /** forum_topic_id: int32 — Unique identifier of the forum topic */
  forum_topic_id: number = 0;
}

/** Decode a TDLib `messageTopicForum` object; null input -> null. */
export function decodeMessageTopicForum(json: TdJsonRaw): MessageTopicForum | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageTopicForum = new MessageTopicForum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  return out;
}

/** Encode a `messageTopicForum` into TDLib JSON wire shape. */
export function encodeMessageTopicForum(value: MessageTopicForum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageTopicForum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['forum_topic_id'] = value.forum_topic_id;
  return out;
}

/** messageTopicSavedMessages = MessageTopic — A topic in Saved Messages chat */
export class MessageTopicSavedMessages {
  type: 'messageTopicSavedMessages' = 'messageTopicSavedMessages';
  extra?: string;
  /** saved_messages_topic_id: int53 — Unique identifier of the Saved Messages topic */
  saved_messages_topic_id: number = 0;
}

/** Decode a TDLib `messageTopicSavedMessages` object; null input -> null. */
export function decodeMessageTopicSavedMessages(json: TdJsonRaw): MessageTopicSavedMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageTopicSavedMessages = new MessageTopicSavedMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  return out;
}

/** Encode a `messageTopicSavedMessages` into TDLib JSON wire shape. */
export function encodeMessageTopicSavedMessages(value: MessageTopicSavedMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageTopicSavedMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  return out;
}

/** messageTopicThread = MessageTopic — A topic in a non-forum supergroup chat */
export class MessageTopicThread {
  type: 'messageTopicThread' = 'messageTopicThread';
  extra?: string;
  /** message_thread_id: int53 — Unique identifier of the message thread */
  message_thread_id: number = 0;
}

/** Decode a TDLib `messageTopicThread` object; null input -> null. */
export function decodeMessageTopicThread(json: TdJsonRaw): MessageTopicThread | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageTopicThread = new MessageTopicThread();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_thread_id = asNumber(record['message_thread_id']);
  return out;
}

/** Encode a `messageTopicThread` into TDLib JSON wire shape. */
export function encodeMessageTopicThread(value: MessageTopicThread): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageTopicThread';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_thread_id'] = value.message_thread_id;
  return out;
}

/** messageUnsupported = MessageContent — A message content that is not supported in the current TDLib version */
export class MessageUnsupported {
  type: 'messageUnsupported' = 'messageUnsupported';
  extra?: string;
}

/** Decode a TDLib `messageUnsupported` object; null input -> null. */
export function decodeMessageUnsupported(json: TdJsonRaw): MessageUnsupported | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageUnsupported = new MessageUnsupported();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `messageUnsupported` into TDLib JSON wire shape. */
export function encodeMessageUnsupported(value: MessageUnsupported): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageUnsupported';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** messageUpgradedGift = MessageContent — An upgraded gift was received or sent by the current user, or the current user was notified about a channel gift */
export class MessageUpgradedGift {
  type: 'messageUpgradedGift' = 'messageUpgradedGift';
  extra?: string;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
  /** sender_id: MessageSender — Sender of the gift; may be null for anonymous gifts */
  sender_id: TdMessageSender | null = null;
  /** receiver_id: MessageSender — Receiver of the gift */
  receiver_id: TdMessageSender | null = null;
  /** origin: UpgradedGiftOrigin — Origin of the upgraded gift */
  origin: TdUpgradedGiftOrigin | null = null;
  /** received_gift_id: string — Unique identifier of the received gift for the current user; only for the receiver of the gift */
  received_gift_id: string = '';
  /** text: formattedText — Message added to the gift */
  text: FormattedText | null = null;
  /** is_private: Bool — True, if the sender and gift text are shown only to the gift receiver; otherwise, everyone will be able to see them */
  is_private: boolean = false;
  /** is_saved: Bool — True, if the gift is displayed on the user's or the channel's profile page; only for the receiver of the gift */
  is_saved: boolean = false;
  /** can_be_transferred: Bool — True, if the gift can be transferred to another owner; only for the receiver of the gift */
  can_be_transferred: boolean = false;
  /** was_transferred: Bool — True, if the gift has already been transferred to another owner; only for the receiver of the gift */
  was_transferred: boolean = false;
  /** transfer_star_count: int53 — Number of Telegram Stars that must be paid to transfer the upgraded gift; only for the receiver of the gift */
  transfer_star_count: number = 0;
  /** drop_original_details_star_count: int53 — Number of Telegram Stars that must be paid to drop original details of the upgraded gift; 0 if not available; only for the receiver of the gift */
  drop_original_details_star_count: number = 0;
  /** next_transfer_date: int32 — Point in time (Unix timestamp) when the gift can be transferred to another owner; can be in the past; 0 if the gift can be transferred immediately or transfer isn't possible; only for the receiver of the gift */
  next_transfer_date: number = 0;
  /** next_resale_date: int32 — Point in time (Unix timestamp) when the gift can be resold to another user; can be in the past; 0 if the gift can't be resold; only for the receiver of the gift */
  next_resale_date: number = 0;
  /** export_date: int32 — Point in time (Unix timestamp) when the gift can be transferred to the TON blockchain as an NFT; can be in the past; 0 if NFT export isn't possible; only for the receiver of the gift */
  export_date: number = 0;
  /** craft_date: int32 — Point in time (Unix timestamp) when the gift can be used to craft another gift; can be in the past; only for the receiver of the gift */
  craft_date: number = 0;
}

/** Decode a TDLib `messageUpgradedGift` object; null input -> null. */
export function decodeMessageUpgradedGift(json: TdJsonRaw): MessageUpgradedGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageUpgradedGift = new MessageUpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.receiver_id = decodeTdMessageSender(record['receiver_id']);
  out.origin = decodeTdUpgradedGiftOrigin(record['origin']);
  out.received_gift_id = asString(record['received_gift_id']);
  out.text = decodeFormattedText(record['text']);
  out.is_private = asBool(record['is_private']);
  out.is_saved = asBool(record['is_saved']);
  out.can_be_transferred = asBool(record['can_be_transferred']);
  out.was_transferred = asBool(record['was_transferred']);
  out.transfer_star_count = asNumber(record['transfer_star_count']);
  out.drop_original_details_star_count = asNumber(record['drop_original_details_star_count']);
  out.next_transfer_date = asNumber(record['next_transfer_date']);
  out.next_resale_date = asNumber(record['next_resale_date']);
  out.export_date = asNumber(record['export_date']);
  out.craft_date = asNumber(record['craft_date']);
  return out;
}

/** Encode a `messageUpgradedGift` into TDLib JSON wire shape. */
export function encodeMessageUpgradedGift(value: MessageUpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageUpgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['receiver_id'] = value.receiver_id === null ? null : encodeTdMessageSender(value.receiver_id);
  out['origin'] = value.origin === null ? null : encodeTdUpgradedGiftOrigin(value.origin);
  out['received_gift_id'] = value.received_gift_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['is_private'] = value.is_private;
  out['is_saved'] = value.is_saved;
  out['can_be_transferred'] = value.can_be_transferred;
  out['was_transferred'] = value.was_transferred;
  out['transfer_star_count'] = value.transfer_star_count;
  out['drop_original_details_star_count'] = value.drop_original_details_star_count;
  out['next_transfer_date'] = value.next_transfer_date;
  out['next_resale_date'] = value.next_resale_date;
  out['export_date'] = value.export_date;
  out['craft_date'] = value.craft_date;
  return out;
}

/** messageUpgradedGiftPurchaseOffer = MessageContent — An offer to purchase an upgraded gift was sent or received */
export class MessageUpgradedGiftPurchaseOffer {
  type: 'messageUpgradedGiftPurchaseOffer' = 'messageUpgradedGiftPurchaseOffer';
  extra?: string;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
  /** state: GiftPurchaseOfferState — State of the offer */
  state: TdGiftPurchaseOfferState | null = null;
  /** price: GiftResalePrice — The proposed price */
  price: TdGiftResalePrice | null = null;
  /** expiration_date: int32 — Point in time (Unix timestamp) when the offer will expire or has expired */
  expiration_date: number = 0;
}

/** Decode a TDLib `messageUpgradedGiftPurchaseOffer` object; null input -> null. */
export function decodeMessageUpgradedGiftPurchaseOffer(json: TdJsonRaw): MessageUpgradedGiftPurchaseOffer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageUpgradedGiftPurchaseOffer = new MessageUpgradedGiftPurchaseOffer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  out.state = decodeTdGiftPurchaseOfferState(record['state']);
  out.price = decodeTdGiftResalePrice(record['price']);
  out.expiration_date = asNumber(record['expiration_date']);
  return out;
}

/** Encode a `messageUpgradedGiftPurchaseOffer` into TDLib JSON wire shape. */
export function encodeMessageUpgradedGiftPurchaseOffer(value: MessageUpgradedGiftPurchaseOffer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageUpgradedGiftPurchaseOffer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  out['state'] = value.state === null ? null : encodeTdGiftPurchaseOfferState(value.state);
  out['price'] = value.price === null ? null : encodeTdGiftResalePrice(value.price);
  out['expiration_date'] = value.expiration_date;
  return out;
}

/** messageUpgradedGiftPurchaseOfferRejected = MessageContent — An offer to purchase a gift was rejected or expired */
export class MessageUpgradedGiftPurchaseOfferRejected {
  type: 'messageUpgradedGiftPurchaseOfferRejected' = 'messageUpgradedGiftPurchaseOfferRejected';
  extra?: string;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
  /** price: GiftResalePrice — The proposed price */
  price: TdGiftResalePrice | null = null;
  /** offer_message_id: int53 — Identifier of the message with purchase offer which was rejected or expired; may be 0 or an identifier of a deleted message */
  offer_message_id: number = 0;
  /** was_expired: Bool — True, if the offer has expired; otherwise, the offer was explicitly rejected */
  was_expired: boolean = false;
}

/** Decode a TDLib `messageUpgradedGiftPurchaseOfferRejected` object; null input -> null. */
export function decodeMessageUpgradedGiftPurchaseOfferRejected(json: TdJsonRaw): MessageUpgradedGiftPurchaseOfferRejected | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageUpgradedGiftPurchaseOfferRejected = new MessageUpgradedGiftPurchaseOfferRejected();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  out.price = decodeTdGiftResalePrice(record['price']);
  out.offer_message_id = asNumber(record['offer_message_id']);
  out.was_expired = asBool(record['was_expired']);
  return out;
}

/** Encode a `messageUpgradedGiftPurchaseOfferRejected` into TDLib JSON wire shape. */
export function encodeMessageUpgradedGiftPurchaseOfferRejected(value: MessageUpgradedGiftPurchaseOfferRejected): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageUpgradedGiftPurchaseOfferRejected';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  out['price'] = value.price === null ? null : encodeTdGiftResalePrice(value.price);
  out['offer_message_id'] = value.offer_message_id;
  out['was_expired'] = value.was_expired;
  return out;
}

/** messageUsersShared = MessageContent — The current user shared users who were requested by the bot */
export class MessageUsersShared {
  type: 'messageUsersShared' = 'messageUsersShared';
  extra?: string;
  /** users: vector<sharedUser> — The shared users */
  users: SharedUser[] = [];
  /** button_id: int32 — Identifier of the keyboard button with the request */
  button_id: number = 0;
}

/** Decode a TDLib `messageUsersShared` object; null input -> null. */
export function decodeMessageUsersShared(json: TdJsonRaw): MessageUsersShared | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageUsersShared = new MessageUsersShared();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.users = decodeVector(record['users'], (item: TdJsonRaw): SharedUser => decodeSharedUser(item) as SharedUser);
  out.button_id = asNumber(record['button_id']);
  return out;
}

/** Encode a `messageUsersShared` into TDLib JSON wire shape. */
export function encodeMessageUsersShared(value: MessageUsersShared): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageUsersShared';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['users'] = encodeVector(value.users, (item: SharedUser | null): TdJsonRaw => item === null ? null : encodeSharedUser(item));
  out['button_id'] = value.button_id;
  return out;
}

/** messageVenue = MessageContent — A message with information about a venue */
export class MessageVenue {
  type: 'messageVenue' = 'messageVenue';
  extra?: string;
  /** venue: venue — The venue description */
  venue: Venue | null = null;
}

/** Decode a TDLib `messageVenue` object; null input -> null. */
export function decodeMessageVenue(json: TdJsonRaw): MessageVenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageVenue = new MessageVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.venue = decodeVenue(record['venue']);
  return out;
}

/** Encode a `messageVenue` into TDLib JSON wire shape. */
export function encodeMessageVenue(value: MessageVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['venue'] = value.venue === null ? null : encodeVenue(value.venue);
  return out;
}

/** messageVideo = MessageContent — A video message */
export class MessageVideo {
  type: 'messageVideo' = 'messageVideo';
  extra?: string;
  /** video: video — The video description */
  video: Video | null = null;
  /** alternative_videos: vector<alternativeVideo> — Alternative qualities of the video */
  alternative_videos: AlternativeVideo[] = [];
  /** storyboards: vector<videoStoryboard> — Available storyboards for the video */
  storyboards: VideoStoryboard[] = [];
  /** cover: photo — Cover of the video; may be null if none */
  cover: Photo | null = null;
  /** start_timestamp: int32 — Timestamp from which the video playing must start, in seconds */
  start_timestamp: number = 0;
  /** caption: formattedText — Video caption */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — True, if the caption must be shown above the video; otherwise, the caption must be shown below the video */
  show_caption_above_media: boolean = false;
  /** has_spoiler: Bool — True, if the video preview must be covered by a spoiler animation */
  has_spoiler: boolean = false;
  /** is_secret: Bool — True, if the video thumbnail must be blurred and the video must be shown only while tapped */
  is_secret: boolean = false;
}

/** Decode a TDLib `messageVideo` object; null input -> null. */
export function decodeMessageVideo(json: TdJsonRaw): MessageVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageVideo = new MessageVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeVideo(record['video']);
  out.alternative_videos = decodeVector(record['alternative_videos'], (item: TdJsonRaw): AlternativeVideo => decodeAlternativeVideo(item) as AlternativeVideo);
  out.storyboards = decodeVector(record['storyboards'], (item: TdJsonRaw): VideoStoryboard => decodeVideoStoryboard(item) as VideoStoryboard);
  out.cover = decodePhoto(record['cover']);
  out.start_timestamp = asNumber(record['start_timestamp']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  out.has_spoiler = asBool(record['has_spoiler']);
  out.is_secret = asBool(record['is_secret']);
  return out;
}

/** Encode a `messageVideo` into TDLib JSON wire shape. */
export function encodeMessageVideo(value: MessageVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  out['alternative_videos'] = encodeVector(value.alternative_videos, (item: AlternativeVideo | null): TdJsonRaw => item === null ? null : encodeAlternativeVideo(item));
  out['storyboards'] = encodeVector(value.storyboards, (item: VideoStoryboard | null): TdJsonRaw => item === null ? null : encodeVideoStoryboard(item));
  out['cover'] = value.cover === null ? null : encodePhoto(value.cover);
  out['start_timestamp'] = value.start_timestamp;
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  out['has_spoiler'] = value.has_spoiler;
  out['is_secret'] = value.is_secret;
  return out;
}

/** messageVideoChatEnded = MessageContent — A message with information about an ended video chat */
export class MessageVideoChatEnded {
  type: 'messageVideoChatEnded' = 'messageVideoChatEnded';
  extra?: string;
  /** duration: int32 — Call duration, in seconds */
  duration: number = 0;
}

/** Decode a TDLib `messageVideoChatEnded` object; null input -> null. */
export function decodeMessageVideoChatEnded(json: TdJsonRaw): MessageVideoChatEnded | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageVideoChatEnded = new MessageVideoChatEnded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.duration = asNumber(record['duration']);
  return out;
}

/** Encode a `messageVideoChatEnded` into TDLib JSON wire shape. */
export function encodeMessageVideoChatEnded(value: MessageVideoChatEnded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageVideoChatEnded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['duration'] = value.duration;
  return out;
}

/** messageVideoChatScheduled = MessageContent — A new video chat was scheduled */
export class MessageVideoChatScheduled {
  type: 'messageVideoChatScheduled' = 'messageVideoChatScheduled';
  extra?: string;
  /** group_call_id: int32 — Identifier of the video chat. The video chat can be received through the method getGroupCall */
  group_call_id: number = 0;
  /** start_date: int32 — Point in time (Unix timestamp) when the group call is expected to be started by an administrator */
  start_date: number = 0;
}

/** Decode a TDLib `messageVideoChatScheduled` object; null input -> null. */
export function decodeMessageVideoChatScheduled(json: TdJsonRaw): MessageVideoChatScheduled | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageVideoChatScheduled = new MessageVideoChatScheduled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.start_date = asNumber(record['start_date']);
  return out;
}

/** Encode a `messageVideoChatScheduled` into TDLib JSON wire shape. */
export function encodeMessageVideoChatScheduled(value: MessageVideoChatScheduled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageVideoChatScheduled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['start_date'] = value.start_date;
  return out;
}

/** messageVideoChatStarted = MessageContent — A newly created video chat */
export class MessageVideoChatStarted {
  type: 'messageVideoChatStarted' = 'messageVideoChatStarted';
  extra?: string;
  /** group_call_id: int32 — Identifier of the video chat. The video chat can be received through the method getGroupCall */
  group_call_id: number = 0;
}

/** Decode a TDLib `messageVideoChatStarted` object; null input -> null. */
export function decodeMessageVideoChatStarted(json: TdJsonRaw): MessageVideoChatStarted | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageVideoChatStarted = new MessageVideoChatStarted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `messageVideoChatStarted` into TDLib JSON wire shape. */
export function encodeMessageVideoChatStarted(value: MessageVideoChatStarted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageVideoChatStarted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** messageVideoNote = MessageContent — A video note message */
export class MessageVideoNote {
  type: 'messageVideoNote' = 'messageVideoNote';
  extra?: string;
  /** video_note: videoNote — The video note description */
  video_note: VideoNote | null = null;
  /** is_viewed: Bool — True, if at least one of the recipients has viewed the video note */
  is_viewed: boolean = false;
  /** is_secret: Bool — True, if the video note thumbnail must be blurred and the video note must be shown only while tapped */
  is_secret: boolean = false;
}

/** Decode a TDLib `messageVideoNote` object; null input -> null. */
export function decodeMessageVideoNote(json: TdJsonRaw): MessageVideoNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageVideoNote = new MessageVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video_note = decodeVideoNote(record['video_note']);
  out.is_viewed = asBool(record['is_viewed']);
  out.is_secret = asBool(record['is_secret']);
  return out;
}

/** Encode a `messageVideoNote` into TDLib JSON wire shape. */
export function encodeMessageVideoNote(value: MessageVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video_note'] = value.video_note === null ? null : encodeVideoNote(value.video_note);
  out['is_viewed'] = value.is_viewed;
  out['is_secret'] = value.is_secret;
  return out;
}

/** messageViewer = MessageViewer — Represents a viewer of a message */
export class MessageViewer {
  type: 'messageViewer' = 'messageViewer';
  extra?: string;
  /** user_id: int53 — User identifier of the viewer */
  user_id: number = 0;
  /** view_date: int32 — Approximate point in time (Unix timestamp) when the message was viewed */
  view_date: number = 0;
}

/** Decode a TDLib `messageViewer` object; null input -> null. */
export function decodeMessageViewer(json: TdJsonRaw): MessageViewer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageViewer = new MessageViewer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.view_date = asNumber(record['view_date']);
  return out;
}

/** Encode a `messageViewer` into TDLib JSON wire shape. */
export function encodeMessageViewer(value: MessageViewer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageViewer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['view_date'] = value.view_date;
  return out;
}

/** messageViewers = MessageViewers — Represents a list of message viewers */
export class MessageViewers {
  type: 'messageViewers' = 'messageViewers';
  extra?: string;
  /** viewers: vector<messageViewer> — List of message viewers */
  viewers: MessageViewer[] = [];
}

/** Decode a TDLib `messageViewers` object; null input -> null. */
export function decodeMessageViewers(json: TdJsonRaw): MessageViewers | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageViewers = new MessageViewers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.viewers = decodeVector(record['viewers'], (item: TdJsonRaw): MessageViewer => decodeMessageViewer(item) as MessageViewer);
  return out;
}

/** Encode a `messageViewers` into TDLib JSON wire shape. */
export function encodeMessageViewers(value: MessageViewers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageViewers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['viewers'] = encodeVector(value.viewers, (item: MessageViewer | null): TdJsonRaw => item === null ? null : encodeMessageViewer(item));
  return out;
}

/** messageVoiceNote = MessageContent — A voice note message */
export class MessageVoiceNote {
  type: 'messageVoiceNote' = 'messageVoiceNote';
  extra?: string;
  /** voice_note: voiceNote — The voice note description */
  voice_note: VoiceNote | null = null;
  /** caption: formattedText — Voice note caption */
  caption: FormattedText | null = null;
  /** is_listened: Bool — True, if at least one of the recipients has listened to the voice note */
  is_listened: boolean = false;
}

/** Decode a TDLib `messageVoiceNote` object; null input -> null. */
export function decodeMessageVoiceNote(json: TdJsonRaw): MessageVoiceNote | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageVoiceNote = new MessageVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.voice_note = decodeVoiceNote(record['voice_note']);
  out.caption = decodeFormattedText(record['caption']);
  out.is_listened = asBool(record['is_listened']);
  return out;
}

/** Encode a `messageVoiceNote` into TDLib JSON wire shape. */
export function encodeMessageVoiceNote(value: MessageVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['voice_note'] = value.voice_note === null ? null : encodeVoiceNote(value.voice_note);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['is_listened'] = value.is_listened;
  return out;
}

/** messageWebAppDataReceived = MessageContent — Data from a Web App has been received; for bots only */
export class MessageWebAppDataReceived {
  type: 'messageWebAppDataReceived' = 'messageWebAppDataReceived';
  extra?: string;
  /** button_text: string — Text of the keyboardButtonTypeWebApp button, which opened the Web App */
  button_text: string = '';
  /** data: string — The data */
  data: string = '';
}

/** Decode a TDLib `messageWebAppDataReceived` object; null input -> null. */
export function decodeMessageWebAppDataReceived(json: TdJsonRaw): MessageWebAppDataReceived | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageWebAppDataReceived = new MessageWebAppDataReceived();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.button_text = asString(record['button_text']);
  out.data = asString(record['data']);
  return out;
}

/** Encode a `messageWebAppDataReceived` into TDLib JSON wire shape. */
export function encodeMessageWebAppDataReceived(value: MessageWebAppDataReceived): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageWebAppDataReceived';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['button_text'] = value.button_text;
  out['data'] = value.data;
  return out;
}

/** messageWebAppDataSent = MessageContent — Data from a Web App has been sent to a bot */
export class MessageWebAppDataSent {
  type: 'messageWebAppDataSent' = 'messageWebAppDataSent';
  extra?: string;
  /** button_text: string — Text of the keyboardButtonTypeWebApp button, which opened the Web App */
  button_text: string = '';
}

/** Decode a TDLib `messageWebAppDataSent` object; null input -> null. */
export function decodeMessageWebAppDataSent(json: TdJsonRaw): MessageWebAppDataSent | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: MessageWebAppDataSent = new MessageWebAppDataSent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.button_text = asString(record['button_text']);
  return out;
}

/** Encode a `messageWebAppDataSent` into TDLib JSON wire shape. */
export function encodeMessageWebAppDataSent(value: MessageWebAppDataSent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messageWebAppDataSent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['button_text'] = value.button_text;
  return out;
}

/** messages = Messages — Contains a list of messages */
export class Messages {
  type: 'messages' = 'messages';
  extra?: string;
  /** total_count: int32 — Approximate total number of messages found */
  total_count: number = 0;
  /** messages: vector<message> — List of messages; messages may be null */
  messages: Message[] = [];
}

/** Decode a TDLib `messages` object; null input -> null. */
export function decodeMessages(json: TdJsonRaw): Messages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Messages = new Messages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): Message => decodeMessage(item) as Message);
  return out;
}

/** Encode a `messages` into TDLib JSON wire shape. */
export function encodeMessages(value: Messages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'messages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['messages'] = encodeVector(value.messages, (item: Message | null): TdJsonRaw => item === null ? null : encodeMessage(item));
  return out;
}

/** minithumbnail = Minithumbnail — Thumbnail image of a very poor quality and low resolution */
export class Minithumbnail {
  type: 'minithumbnail' = 'minithumbnail';
  extra?: string;
  /** width: int32 — Thumbnail width, usually doesn't exceed 40 */
  width: number = 0;
  /** height: int32 — Thumbnail height, usually doesn't exceed 40 */
  height: number = 0;
  /** data: bytes — The thumbnail in JPEG format */
  data: string = '';
}

/** Decode a TDLib `minithumbnail` object; null input -> null. */
export function decodeMinithumbnail(json: TdJsonRaw): Minithumbnail | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Minithumbnail = new Minithumbnail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.data = asString(record['data']);
  return out;
}

/** Encode a `minithumbnail` into TDLib JSON wire shape. */
export function encodeMinithumbnail(value: Minithumbnail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'minithumbnail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['width'] = value.width;
  out['height'] = value.height;
  out['data'] = value.data;
  return out;
}
