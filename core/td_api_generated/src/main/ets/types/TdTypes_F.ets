/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { AffiliateProgramInfo, decodeAffiliateProgramInfo, encodeAffiliateProgramInfo } from './TdTypes_A';
import { ChatBoost, ChatNotificationSettings, decodeChatBoost, decodeChatNotificationSettings, encodeChatBoost, encodeChatNotificationSettings } from './TdTypes_C';
import { DiffText, DownloadedFileCounts, DraftMessage, decodeDiffText, decodeDownloadedFileCounts, decodeDraftMessage, encodeDiffText, encodeDownloadedFileCounts, encodeDraftMessage } from './TdTypes_D';
import { ErrorValue, decodeErrorValue, encodeErrorValue } from './TdTypes_E';
import { InputRichMessage, decodeInputRichMessage, encodeInputRichMessage } from './TdTypes_I';
import { LocalFile, decodeLocalFile, encodeLocalFile } from './TdTypes_L';
import { Message, MessageSendOptions, decodeMessage, decodeMessageSendOptions, encodeMessage, encodeMessageSendOptions } from './TdTypes_M';
import { PublicPostSearchLimits, decodePublicPostSearchLimits, encodePublicPostSearchLimits } from './TdTypes_P';
import { RemoteFile, decodeRemoteFile, encodeRemoteFile } from './TdTypes_R';
import { Story, decodeStory, encodeStory } from './TdTypes_S';
import { TextEntity, decodeTextEntity, encodeTextEntity } from './TdTypes_T';
import { WebApp, decodeWebApp, encodeWebApp } from './TdTypes_W';
import { TdMessageSender, TdMessageTopic, decodeTdMessageSender, decodeTdMessageTopic, encodeTdMessageSender, encodeTdMessageTopic } from './TdUnions_M';

/** factCheck = FactCheck — Describes a fact-check added to the message by an independent checker */
export class FactCheck {
  type: 'factCheck' = 'factCheck';
  extra?: string;
  /** text: formattedText — Text of the fact-check */
  text: FormattedText | null = null;
  /** country_code: string — A two-letter ISO 3166-1 alpha-2 country code of the country for which the fact-check is shown */
  country_code: string = '';
}

/** Decode a TDLib `factCheck` object; null input -> null. */
export function decodeFactCheck(json: TdJsonRaw): FactCheck | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FactCheck = new FactCheck();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.country_code = asString(record['country_code']);
  return out;
}

/** Encode a `factCheck` into TDLib JSON wire shape. */
export function encodeFactCheck(value: FactCheck): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'factCheck';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['country_code'] = value.country_code;
  return out;
}

/** failedToAddMember = FailedToAddMember — Contains information about a user who has failed to be added to a chat */
export class FailedToAddMember {
  type: 'failedToAddMember' = 'failedToAddMember';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** premium_would_allow_invite: Bool — True, if subscription to Telegram Premium would have allowed to add the user to the chat */
  premium_would_allow_invite: boolean = false;
  /** premium_required_to_send_messages: Bool — True, if subscription to Telegram Premium is required to send the user chat invite link */
  premium_required_to_send_messages: boolean = false;
}

/** Decode a TDLib `failedToAddMember` object; null input -> null. */
export function decodeFailedToAddMember(json: TdJsonRaw): FailedToAddMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FailedToAddMember = new FailedToAddMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.premium_would_allow_invite = asBool(record['premium_would_allow_invite']);
  out.premium_required_to_send_messages = asBool(record['premium_required_to_send_messages']);
  return out;
}

/** Encode a `failedToAddMember` into TDLib JSON wire shape. */
export function encodeFailedToAddMember(value: FailedToAddMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'failedToAddMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['premium_would_allow_invite'] = value.premium_would_allow_invite;
  out['premium_required_to_send_messages'] = value.premium_required_to_send_messages;
  return out;
}

/** failedToAddMembers = FailedToAddMembers — Represents a list of users that has failed to be added to a chat */
export class FailedToAddMembers {
  type: 'failedToAddMembers' = 'failedToAddMembers';
  extra?: string;
  /** failed_to_add_members: vector<failedToAddMember> — Information about users that weren't added to the chat */
  failed_to_add_members: FailedToAddMember[] = [];
}

/** Decode a TDLib `failedToAddMembers` object; null input -> null. */
export function decodeFailedToAddMembers(json: TdJsonRaw): FailedToAddMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FailedToAddMembers = new FailedToAddMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.failed_to_add_members = decodeVector(record['failed_to_add_members'], (item: TdJsonRaw): FailedToAddMember => decodeFailedToAddMember(item) as FailedToAddMember);
  return out;
}

/** Encode a `failedToAddMembers` into TDLib JSON wire shape. */
export function encodeFailedToAddMembers(value: FailedToAddMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'failedToAddMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['failed_to_add_members'] = encodeVector(value.failed_to_add_members, (item: FailedToAddMember | null): TdJsonRaw => item === null ? null : encodeFailedToAddMember(item));
  return out;
}

/** file = File — Represents a file */
export class File {
  type: 'file' = 'file';
  extra?: string;
  /** id: int32 — Unique file identifier */
  id: number = 0;
  /** size: int53 — File size, in bytes; 0 if unknown */
  size: number = 0;
  /** expected_size: int53 — Approximate file size in bytes in case the exact file size is unknown. Can be used to show download/upload progress */
  expected_size: number = 0;
  /** local: localFile — Information about the local copy of the file */
  local: LocalFile | null = null;
  /** remote: remoteFile — Information about the remote copy of the file */
  remote: RemoteFile | null = null;
}

/** Decode a TDLib `file` object; null input -> null. */
export function decodeFile(json: TdJsonRaw): File | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: File = new File();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.size = asNumber(record['size']);
  out.expected_size = asNumber(record['expected_size']);
  out.local = decodeLocalFile(record['local']);
  out.remote = decodeRemoteFile(record['remote']);
  return out;
}

/** Encode a `file` into TDLib JSON wire shape. */
export function encodeFile(value: File): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'file';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['size'] = value.size;
  out['expected_size'] = value.expected_size;
  out['local'] = value.local === null ? null : encodeLocalFile(value.local);
  out['remote'] = value.remote === null ? null : encodeRemoteFile(value.remote);
  return out;
}

/** fileDownload = FileDownload — Describes a file added to file download list */
export class FileDownload {
  type: 'fileDownload' = 'fileDownload';
  extra?: string;
  /** file_id: int32 — File identifier */
  file_id: number = 0;
  /** message: message — The message with the file */
  message: Message | null = null;
  /** add_date: int32 — Point in time (Unix timestamp) when the file was added to the download list */
  add_date: number = 0;
  /** complete_date: int32 — Point in time (Unix timestamp) when the file downloading was completed; 0 if the file downloading isn't completed */
  complete_date: number = 0;
  /** is_paused: Bool — True, if downloading of the file is paused */
  is_paused: boolean = false;
}

/** Decode a TDLib `fileDownload` object; null input -> null. */
export function decodeFileDownload(json: TdJsonRaw): FileDownload | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileDownload = new FileDownload();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.message = decodeMessage(record['message']);
  out.add_date = asNumber(record['add_date']);
  out.complete_date = asNumber(record['complete_date']);
  out.is_paused = asBool(record['is_paused']);
  return out;
}

/** Encode a `fileDownload` into TDLib JSON wire shape. */
export function encodeFileDownload(value: FileDownload): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileDownload';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  out['add_date'] = value.add_date;
  out['complete_date'] = value.complete_date;
  out['is_paused'] = value.is_paused;
  return out;
}

/** fileDownloadedPrefixSize = FileDownloadedPrefixSize — Contains size of downloaded prefix of a file */
export class FileDownloadedPrefixSize {
  type: 'fileDownloadedPrefixSize' = 'fileDownloadedPrefixSize';
  extra?: string;
  /** size: int53 — The prefix size, in bytes */
  size: number = 0;
}

/** Decode a TDLib `fileDownloadedPrefixSize` object; null input -> null. */
export function decodeFileDownloadedPrefixSize(json: TdJsonRaw): FileDownloadedPrefixSize | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileDownloadedPrefixSize = new FileDownloadedPrefixSize();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.size = asNumber(record['size']);
  return out;
}

/** Encode a `fileDownloadedPrefixSize` into TDLib JSON wire shape. */
export function encodeFileDownloadedPrefixSize(value: FileDownloadedPrefixSize): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileDownloadedPrefixSize';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['size'] = value.size;
  return out;
}

/** fileTypeAnimation = FileType — The file is an animation */
export class FileTypeAnimation {
  type: 'fileTypeAnimation' = 'fileTypeAnimation';
  extra?: string;
}

/** Decode a TDLib `fileTypeAnimation` object; null input -> null. */
export function decodeFileTypeAnimation(json: TdJsonRaw): FileTypeAnimation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeAnimation = new FileTypeAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeAnimation` into TDLib JSON wire shape. */
export function encodeFileTypeAnimation(value: FileTypeAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeAudio = FileType — The file is an audio file */
export class FileTypeAudio {
  type: 'fileTypeAudio' = 'fileTypeAudio';
  extra?: string;
}

/** Decode a TDLib `fileTypeAudio` object; null input -> null. */
export function decodeFileTypeAudio(json: TdJsonRaw): FileTypeAudio | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeAudio = new FileTypeAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeAudio` into TDLib JSON wire shape. */
export function encodeFileTypeAudio(value: FileTypeAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeDocument = FileType — The file is a document */
export class FileTypeDocument {
  type: 'fileTypeDocument' = 'fileTypeDocument';
  extra?: string;
}

/** Decode a TDLib `fileTypeDocument` object; null input -> null. */
export function decodeFileTypeDocument(json: TdJsonRaw): FileTypeDocument | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeDocument = new FileTypeDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeDocument` into TDLib JSON wire shape. */
export function encodeFileTypeDocument(value: FileTypeDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeLivePhotoVideo = FileType — The file is a video for a live photo */
export class FileTypeLivePhotoVideo {
  type: 'fileTypeLivePhotoVideo' = 'fileTypeLivePhotoVideo';
  extra?: string;
}

/** Decode a TDLib `fileTypeLivePhotoVideo` object; null input -> null. */
export function decodeFileTypeLivePhotoVideo(json: TdJsonRaw): FileTypeLivePhotoVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeLivePhotoVideo = new FileTypeLivePhotoVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeLivePhotoVideo` into TDLib JSON wire shape. */
export function encodeFileTypeLivePhotoVideo(value: FileTypeLivePhotoVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeLivePhotoVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeNone = FileType — The data is not a file */
export class FileTypeNone {
  type: 'fileTypeNone' = 'fileTypeNone';
  extra?: string;
}

/** Decode a TDLib `fileTypeNone` object; null input -> null. */
export function decodeFileTypeNone(json: TdJsonRaw): FileTypeNone | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeNone = new FileTypeNone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeNone` into TDLib JSON wire shape. */
export function encodeFileTypeNone(value: FileTypeNone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeNone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeNotificationSound = FileType — The file is a notification sound */
export class FileTypeNotificationSound {
  type: 'fileTypeNotificationSound' = 'fileTypeNotificationSound';
  extra?: string;
}

/** Decode a TDLib `fileTypeNotificationSound` object; null input -> null. */
export function decodeFileTypeNotificationSound(json: TdJsonRaw): FileTypeNotificationSound | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeNotificationSound = new FileTypeNotificationSound();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeNotificationSound` into TDLib JSON wire shape. */
export function encodeFileTypeNotificationSound(value: FileTypeNotificationSound): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeNotificationSound';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypePhoto = FileType — The file is a photo */
export class FileTypePhoto {
  type: 'fileTypePhoto' = 'fileTypePhoto';
  extra?: string;
}

/** Decode a TDLib `fileTypePhoto` object; null input -> null. */
export function decodeFileTypePhoto(json: TdJsonRaw): FileTypePhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypePhoto = new FileTypePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypePhoto` into TDLib JSON wire shape. */
export function encodeFileTypePhoto(value: FileTypePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypePhotoStory = FileType — The file is a photo published as a story */
export class FileTypePhotoStory {
  type: 'fileTypePhotoStory' = 'fileTypePhotoStory';
  extra?: string;
}

/** Decode a TDLib `fileTypePhotoStory` object; null input -> null. */
export function decodeFileTypePhotoStory(json: TdJsonRaw): FileTypePhotoStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypePhotoStory = new FileTypePhotoStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypePhotoStory` into TDLib JSON wire shape. */
export function encodeFileTypePhotoStory(value: FileTypePhotoStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypePhotoStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeProfilePhoto = FileType — The file is a profile photo */
export class FileTypeProfilePhoto {
  type: 'fileTypeProfilePhoto' = 'fileTypeProfilePhoto';
  extra?: string;
}

/** Decode a TDLib `fileTypeProfilePhoto` object; null input -> null. */
export function decodeFileTypeProfilePhoto(json: TdJsonRaw): FileTypeProfilePhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeProfilePhoto = new FileTypeProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeProfilePhoto` into TDLib JSON wire shape. */
export function encodeFileTypeProfilePhoto(value: FileTypeProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeSecret = FileType — The file was sent to a secret chat (the file type is not known to the server) */
export class FileTypeSecret {
  type: 'fileTypeSecret' = 'fileTypeSecret';
  extra?: string;
}

/** Decode a TDLib `fileTypeSecret` object; null input -> null. */
export function decodeFileTypeSecret(json: TdJsonRaw): FileTypeSecret | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeSecret = new FileTypeSecret();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeSecret` into TDLib JSON wire shape. */
export function encodeFileTypeSecret(value: FileTypeSecret): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeSecret';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeSecretThumbnail = FileType — The file is a thumbnail of a file from a secret chat */
export class FileTypeSecretThumbnail {
  type: 'fileTypeSecretThumbnail' = 'fileTypeSecretThumbnail';
  extra?: string;
}

/** Decode a TDLib `fileTypeSecretThumbnail` object; null input -> null. */
export function decodeFileTypeSecretThumbnail(json: TdJsonRaw): FileTypeSecretThumbnail | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeSecretThumbnail = new FileTypeSecretThumbnail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeSecretThumbnail` into TDLib JSON wire shape. */
export function encodeFileTypeSecretThumbnail(value: FileTypeSecretThumbnail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeSecretThumbnail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeSecure = FileType — The file is a file from Secure storage used for storing Telegram Passport files */
export class FileTypeSecure {
  type: 'fileTypeSecure' = 'fileTypeSecure';
  extra?: string;
}

/** Decode a TDLib `fileTypeSecure` object; null input -> null. */
export function decodeFileTypeSecure(json: TdJsonRaw): FileTypeSecure | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeSecure = new FileTypeSecure();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeSecure` into TDLib JSON wire shape. */
export function encodeFileTypeSecure(value: FileTypeSecure): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeSecure';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeSelfDestructingLivePhotoVideo = FileType — The file is a self-destructing video for a live photo in a private chat */
export class FileTypeSelfDestructingLivePhotoVideo {
  type: 'fileTypeSelfDestructingLivePhotoVideo' = 'fileTypeSelfDestructingLivePhotoVideo';
  extra?: string;
}

/** Decode a TDLib `fileTypeSelfDestructingLivePhotoVideo` object; null input -> null. */
export function decodeFileTypeSelfDestructingLivePhotoVideo(json: TdJsonRaw): FileTypeSelfDestructingLivePhotoVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeSelfDestructingLivePhotoVideo = new FileTypeSelfDestructingLivePhotoVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeSelfDestructingLivePhotoVideo` into TDLib JSON wire shape. */
export function encodeFileTypeSelfDestructingLivePhotoVideo(value: FileTypeSelfDestructingLivePhotoVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeSelfDestructingLivePhotoVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeSelfDestructingPhoto = FileType — The file is a self-destructing photo in a private chat */
export class FileTypeSelfDestructingPhoto {
  type: 'fileTypeSelfDestructingPhoto' = 'fileTypeSelfDestructingPhoto';
  extra?: string;
}

/** Decode a TDLib `fileTypeSelfDestructingPhoto` object; null input -> null. */
export function decodeFileTypeSelfDestructingPhoto(json: TdJsonRaw): FileTypeSelfDestructingPhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeSelfDestructingPhoto = new FileTypeSelfDestructingPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeSelfDestructingPhoto` into TDLib JSON wire shape. */
export function encodeFileTypeSelfDestructingPhoto(value: FileTypeSelfDestructingPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeSelfDestructingPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeSelfDestructingVideo = FileType — The file is a self-destructing video in a private chat */
export class FileTypeSelfDestructingVideo {
  type: 'fileTypeSelfDestructingVideo' = 'fileTypeSelfDestructingVideo';
  extra?: string;
}

/** Decode a TDLib `fileTypeSelfDestructingVideo` object; null input -> null. */
export function decodeFileTypeSelfDestructingVideo(json: TdJsonRaw): FileTypeSelfDestructingVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeSelfDestructingVideo = new FileTypeSelfDestructingVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeSelfDestructingVideo` into TDLib JSON wire shape. */
export function encodeFileTypeSelfDestructingVideo(value: FileTypeSelfDestructingVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeSelfDestructingVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeSelfDestructingVideoNote = FileType — The file is a self-destructing video note in a private chat */
export class FileTypeSelfDestructingVideoNote {
  type: 'fileTypeSelfDestructingVideoNote' = 'fileTypeSelfDestructingVideoNote';
  extra?: string;
}

/** Decode a TDLib `fileTypeSelfDestructingVideoNote` object; null input -> null. */
export function decodeFileTypeSelfDestructingVideoNote(json: TdJsonRaw): FileTypeSelfDestructingVideoNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeSelfDestructingVideoNote = new FileTypeSelfDestructingVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeSelfDestructingVideoNote` into TDLib JSON wire shape. */
export function encodeFileTypeSelfDestructingVideoNote(value: FileTypeSelfDestructingVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeSelfDestructingVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeSelfDestructingVoiceNote = FileType — The file is a self-destructing voice note in a private chat */
export class FileTypeSelfDestructingVoiceNote {
  type: 'fileTypeSelfDestructingVoiceNote' = 'fileTypeSelfDestructingVoiceNote';
  extra?: string;
}

/** Decode a TDLib `fileTypeSelfDestructingVoiceNote` object; null input -> null. */
export function decodeFileTypeSelfDestructingVoiceNote(json: TdJsonRaw): FileTypeSelfDestructingVoiceNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeSelfDestructingVoiceNote = new FileTypeSelfDestructingVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeSelfDestructingVoiceNote` into TDLib JSON wire shape. */
export function encodeFileTypeSelfDestructingVoiceNote(value: FileTypeSelfDestructingVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeSelfDestructingVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeSticker = FileType — The file is a sticker */
export class FileTypeSticker {
  type: 'fileTypeSticker' = 'fileTypeSticker';
  extra?: string;
}

/** Decode a TDLib `fileTypeSticker` object; null input -> null. */
export function decodeFileTypeSticker(json: TdJsonRaw): FileTypeSticker | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeSticker = new FileTypeSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeSticker` into TDLib JSON wire shape. */
export function encodeFileTypeSticker(value: FileTypeSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeThumbnail = FileType — The file is a thumbnail of another file */
export class FileTypeThumbnail {
  type: 'fileTypeThumbnail' = 'fileTypeThumbnail';
  extra?: string;
}

/** Decode a TDLib `fileTypeThumbnail` object; null input -> null. */
export function decodeFileTypeThumbnail(json: TdJsonRaw): FileTypeThumbnail | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeThumbnail = new FileTypeThumbnail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeThumbnail` into TDLib JSON wire shape. */
export function encodeFileTypeThumbnail(value: FileTypeThumbnail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeThumbnail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeUnknown = FileType — The file type is not yet known */
export class FileTypeUnknown {
  type: 'fileTypeUnknown' = 'fileTypeUnknown';
  extra?: string;
}

/** Decode a TDLib `fileTypeUnknown` object; null input -> null. */
export function decodeFileTypeUnknown(json: TdJsonRaw): FileTypeUnknown | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeUnknown = new FileTypeUnknown();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeUnknown` into TDLib JSON wire shape. */
export function encodeFileTypeUnknown(value: FileTypeUnknown): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeUnknown';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeVideo = FileType — The file is a video */
export class FileTypeVideo {
  type: 'fileTypeVideo' = 'fileTypeVideo';
  extra?: string;
}

/** Decode a TDLib `fileTypeVideo` object; null input -> null. */
export function decodeFileTypeVideo(json: TdJsonRaw): FileTypeVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeVideo = new FileTypeVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeVideo` into TDLib JSON wire shape. */
export function encodeFileTypeVideo(value: FileTypeVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeVideoNote = FileType — The file is a video note */
export class FileTypeVideoNote {
  type: 'fileTypeVideoNote' = 'fileTypeVideoNote';
  extra?: string;
}

/** Decode a TDLib `fileTypeVideoNote` object; null input -> null. */
export function decodeFileTypeVideoNote(json: TdJsonRaw): FileTypeVideoNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeVideoNote = new FileTypeVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeVideoNote` into TDLib JSON wire shape. */
export function encodeFileTypeVideoNote(value: FileTypeVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeVideoStory = FileType — The file is a video published as a story */
export class FileTypeVideoStory {
  type: 'fileTypeVideoStory' = 'fileTypeVideoStory';
  extra?: string;
}

/** Decode a TDLib `fileTypeVideoStory` object; null input -> null. */
export function decodeFileTypeVideoStory(json: TdJsonRaw): FileTypeVideoStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeVideoStory = new FileTypeVideoStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeVideoStory` into TDLib JSON wire shape. */
export function encodeFileTypeVideoStory(value: FileTypeVideoStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeVideoStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeVoiceNote = FileType — The file is a voice note */
export class FileTypeVoiceNote {
  type: 'fileTypeVoiceNote' = 'fileTypeVoiceNote';
  extra?: string;
}

/** Decode a TDLib `fileTypeVoiceNote` object; null input -> null. */
export function decodeFileTypeVoiceNote(json: TdJsonRaw): FileTypeVoiceNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeVoiceNote = new FileTypeVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeVoiceNote` into TDLib JSON wire shape. */
export function encodeFileTypeVoiceNote(value: FileTypeVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** fileTypeWallpaper = FileType — The file is a wallpaper or a background pattern */
export class FileTypeWallpaper {
  type: 'fileTypeWallpaper' = 'fileTypeWallpaper';
  extra?: string;
}

/** Decode a TDLib `fileTypeWallpaper` object; null input -> null. */
export function decodeFileTypeWallpaper(json: TdJsonRaw): FileTypeWallpaper | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FileTypeWallpaper = new FileTypeWallpaper();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `fileTypeWallpaper` into TDLib JSON wire shape. */
export function encodeFileTypeWallpaper(value: FileTypeWallpaper): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fileTypeWallpaper';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** finishFileGeneration = Ok — Finishes the file generation */
export class FinishFileGeneration {
  type: 'finishFileGeneration' = 'finishFileGeneration';
  extra?: string;
  /** generation_id: int64 — The identifier of the generation process */
  generation_id: string = '';
  /** error: error — If passed, the file generation has failed and must be terminated; pass null if the file generation succeeded */
  error: ErrorValue | null = null;
}

/** Decode a TDLib `finishFileGeneration` object; null input -> null. */
export function decodeFinishFileGeneration(json: TdJsonRaw): FinishFileGeneration | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FinishFileGeneration = new FinishFileGeneration();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.generation_id = asInt64(record['generation_id']);
  out.error = decodeErrorValue(record['error']);
  return out;
}

/** Encode a `finishFileGeneration` into TDLib JSON wire shape. */
export function encodeFinishFileGeneration(value: FinishFileGeneration): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'finishFileGeneration';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['generation_id'] = value.generation_id;
  out['error'] = value.error === null ? null : encodeErrorValue(value.error);
  return out;
}

/** firebaseAuthenticationSettingsAndroid = FirebaseAuthenticationSettings — Settings for Firebase Authentication in the official Android application */
export class FirebaseAuthenticationSettingsAndroid {
  type: 'firebaseAuthenticationSettingsAndroid' = 'firebaseAuthenticationSettingsAndroid';
  extra?: string;
}

/** Decode a TDLib `firebaseAuthenticationSettingsAndroid` object; null input -> null. */
export function decodeFirebaseAuthenticationSettingsAndroid(json: TdJsonRaw): FirebaseAuthenticationSettingsAndroid | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FirebaseAuthenticationSettingsAndroid = new FirebaseAuthenticationSettingsAndroid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `firebaseAuthenticationSettingsAndroid` into TDLib JSON wire shape. */
export function encodeFirebaseAuthenticationSettingsAndroid(value: FirebaseAuthenticationSettingsAndroid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'firebaseAuthenticationSettingsAndroid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** firebaseAuthenticationSettingsIos = FirebaseAuthenticationSettings — Settings for Firebase Authentication in the official iOS application */
export class FirebaseAuthenticationSettingsIos {
  type: 'firebaseAuthenticationSettingsIos' = 'firebaseAuthenticationSettingsIos';
  extra?: string;
  /** device_token: string — Device token from Apple Push Notification service */
  device_token: string = '';
  /** is_app_sandbox: Bool — True, if App Sandbox is enabled */
  is_app_sandbox: boolean = false;
}

/** Decode a TDLib `firebaseAuthenticationSettingsIos` object; null input -> null. */
export function decodeFirebaseAuthenticationSettingsIos(json: TdJsonRaw): FirebaseAuthenticationSettingsIos | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FirebaseAuthenticationSettingsIos = new FirebaseAuthenticationSettingsIos();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.device_token = asString(record['device_token']);
  out.is_app_sandbox = asBool(record['is_app_sandbox']);
  return out;
}

/** Encode a `firebaseAuthenticationSettingsIos` into TDLib JSON wire shape. */
export function encodeFirebaseAuthenticationSettingsIos(value: FirebaseAuthenticationSettingsIos): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'firebaseAuthenticationSettingsIos';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['device_token'] = value.device_token;
  out['is_app_sandbox'] = value.is_app_sandbox;
  return out;
}

/** firebaseDeviceVerificationParametersPlayIntegrity = FirebaseDeviceVerificationParameters — Device verification must be performed with the classic Play Integrity verification (https://developer.android.com/google/play/integrity/classic) */
export class FirebaseDeviceVerificationParametersPlayIntegrity {
  type: 'firebaseDeviceVerificationParametersPlayIntegrity' = 'firebaseDeviceVerificationParametersPlayIntegrity';
  extra?: string;
  /** nonce: string — Base64url-encoded nonce to pass to the Play Integrity API */
  nonce: string = '';
  /** cloud_project_number: int64 — Cloud project number to pass to the Play Integrity API */
  cloud_project_number: string = '';
}

/** Decode a TDLib `firebaseDeviceVerificationParametersPlayIntegrity` object; null input -> null. */
export function decodeFirebaseDeviceVerificationParametersPlayIntegrity(json: TdJsonRaw): FirebaseDeviceVerificationParametersPlayIntegrity | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FirebaseDeviceVerificationParametersPlayIntegrity = new FirebaseDeviceVerificationParametersPlayIntegrity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.nonce = asString(record['nonce']);
  out.cloud_project_number = asInt64(record['cloud_project_number']);
  return out;
}

/** Encode a `firebaseDeviceVerificationParametersPlayIntegrity` into TDLib JSON wire shape. */
export function encodeFirebaseDeviceVerificationParametersPlayIntegrity(value: FirebaseDeviceVerificationParametersPlayIntegrity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'firebaseDeviceVerificationParametersPlayIntegrity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['nonce'] = value.nonce;
  out['cloud_project_number'] = value.cloud_project_number;
  return out;
}

/** firebaseDeviceVerificationParametersSafetyNet = FirebaseDeviceVerificationParameters — Device verification must be performed with the SafetyNet Attestation API */
export class FirebaseDeviceVerificationParametersSafetyNet {
  type: 'firebaseDeviceVerificationParametersSafetyNet' = 'firebaseDeviceVerificationParametersSafetyNet';
  extra?: string;
  /** nonce: bytes — Nonce to pass to the SafetyNet Attestation API */
  nonce: string = '';
}

/** Decode a TDLib `firebaseDeviceVerificationParametersSafetyNet` object; null input -> null. */
export function decodeFirebaseDeviceVerificationParametersSafetyNet(json: TdJsonRaw): FirebaseDeviceVerificationParametersSafetyNet | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FirebaseDeviceVerificationParametersSafetyNet = new FirebaseDeviceVerificationParametersSafetyNet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.nonce = asString(record['nonce']);
  return out;
}

/** Encode a `firebaseDeviceVerificationParametersSafetyNet` into TDLib JSON wire shape. */
export function encodeFirebaseDeviceVerificationParametersSafetyNet(value: FirebaseDeviceVerificationParametersSafetyNet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'firebaseDeviceVerificationParametersSafetyNet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['nonce'] = value.nonce;
  return out;
}

/** fixRichMessageWithAi = RichMessage — Fixes a rich message using an AI model. May return an error with a message "AICOMPOSE_FLOOD_PREMIUM" if Telegram Premium is required to send further requests */
export class FixRichMessageWithAi {
  type: 'fixRichMessageWithAi' = 'fixRichMessageWithAi';
  extra?: string;
  /** message: inputRichMessage — The original message */
  message: InputRichMessage | null = null;
}

/** Decode a TDLib `fixRichMessageWithAi` object; null input -> null. */
export function decodeFixRichMessageWithAi(json: TdJsonRaw): FixRichMessageWithAi | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FixRichMessageWithAi = new FixRichMessageWithAi();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeInputRichMessage(record['message']);
  return out;
}

/** Encode a `fixRichMessageWithAi` into TDLib JSON wire shape. */
export function encodeFixRichMessageWithAi(value: FixRichMessageWithAi): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fixRichMessageWithAi';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeInputRichMessage(value.message);
  return out;
}

/** fixTextWithAi = FixedText — Fixes text using an AI model; must not be used in secret chats. May return an error with a message "AICOMPOSE_FLOOD_PREMIUM" if Telegram Premium is required to send further requests */
export class FixTextWithAi {
  type: 'fixTextWithAi' = 'fixTextWithAi';
  extra?: string;
  /** text: formattedText — The original text */
  text: FormattedText | null = null;
}

/** Decode a TDLib `fixTextWithAi` object; null input -> null. */
export function decodeFixTextWithAi(json: TdJsonRaw): FixTextWithAi | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FixTextWithAi = new FixTextWithAi();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `fixTextWithAi` into TDLib JSON wire shape. */
export function encodeFixTextWithAi(value: FixTextWithAi): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fixTextWithAi';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** fixedText = FixedText — A text fixed using fixTextWithAi */
export class FixedText {
  type: 'fixedText' = 'fixedText';
  extra?: string;
  /** text: formattedText — The resulting text */
  text: FormattedText | null = null;
  /** diff_text: diffText — Changes made to the original text */
  diff_text: DiffText | null = null;
}

/** Decode a TDLib `fixedText` object; null input -> null. */
export function decodeFixedText(json: TdJsonRaw): FixedText | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FixedText = new FixedText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.diff_text = decodeDiffText(record['diff_text']);
  return out;
}

/** Encode a `fixedText` into TDLib JSON wire shape. */
export function encodeFixedText(value: FixedText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'fixedText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['diff_text'] = value.diff_text === null ? null : encodeDiffText(value.diff_text);
  return out;
}

/** formattedText = FormattedText — A text with some entities Pre, Code, PreCode, and DateTime entities can't contain other entities. BlockQuote entities can't contain other BlockQuote entities. Bold, Italic, Underline, Strikethrough, and Sp... */
export class FormattedText {
  type: 'formattedText' = 'formattedText';
  extra?: string;
  /** text: string — The text */
  text: string = '';
  /** entities: vector<textEntity> — Entities contained in the text. Entities can be nested, but must not mutually intersect with each other. */
  entities: TextEntity[] = [];
}

/** Decode a TDLib `formattedText` object; null input -> null. */
export function decodeFormattedText(json: TdJsonRaw): FormattedText | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FormattedText = new FormattedText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.entities = decodeVector(record['entities'], (item: TdJsonRaw): TextEntity => decodeTextEntity(item) as TextEntity);
  return out;
}

/** Encode a `formattedText` into TDLib JSON wire shape. */
export function encodeFormattedText(value: FormattedText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'formattedText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['entities'] = encodeVector(value.entities, (item: TextEntity | null): TdJsonRaw => item === null ? null : encodeTextEntity(item));
  return out;
}

/** forumTopic = ForumTopic — Describes a forum topic */
export class ForumTopic {
  type: 'forumTopic' = 'forumTopic';
  extra?: string;
  /** info: forumTopicInfo — Basic information about the topic */
  info: ForumTopicInfo | null = null;
  /** last_message: message — Last message in the topic; may be null if unknown */
  last_message: Message | null = null;
  /** order: int64 — A parameter used to determine order of the topic in the topic list. Topics must be sorted by the order in descending order */
  order: string = '';
  /** is_pinned: Bool — True, if the topic is pinned in the topic list */
  is_pinned: boolean = false;
  /** unread_count: int32 — Number of unread messages in the topic */
  unread_count: number = 0;
  /** last_read_inbox_message_id: int53 — Identifier of the last read incoming message */
  last_read_inbox_message_id: number = 0;
  /** last_read_outbox_message_id: int53 — Identifier of the last read outgoing message */
  last_read_outbox_message_id: number = 0;
  /** unread_mention_count: int32 — Number of unread messages with a mention/reply in the topic */
  unread_mention_count: number = 0;
  /** unread_reaction_count: int32 — Number of messages with unread reactions in the topic */
  unread_reaction_count: number = 0;
  /** unread_poll_vote_count: int32 — Number of messages with unread poll votes in the topic */
  unread_poll_vote_count: number = 0;
  /** notification_settings: chatNotificationSettings — Notification settings for the topic */
  notification_settings: ChatNotificationSettings | null = null;
  /** draft_message: draftMessage — A draft of a message in the topic; may be null if none */
  draft_message: DraftMessage | null = null;
}

/** Decode a TDLib `forumTopic` object; null input -> null. */
export function decodeForumTopic(json: TdJsonRaw): ForumTopic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ForumTopic = new ForumTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.info = decodeForumTopicInfo(record['info']);
  out.last_message = decodeMessage(record['last_message']);
  out.order = asInt64(record['order']);
  out.is_pinned = asBool(record['is_pinned']);
  out.unread_count = asNumber(record['unread_count']);
  out.last_read_inbox_message_id = asNumber(record['last_read_inbox_message_id']);
  out.last_read_outbox_message_id = asNumber(record['last_read_outbox_message_id']);
  out.unread_mention_count = asNumber(record['unread_mention_count']);
  out.unread_reaction_count = asNumber(record['unread_reaction_count']);
  out.unread_poll_vote_count = asNumber(record['unread_poll_vote_count']);
  out.notification_settings = decodeChatNotificationSettings(record['notification_settings']);
  out.draft_message = decodeDraftMessage(record['draft_message']);
  return out;
}

/** Encode a `forumTopic` into TDLib JSON wire shape. */
export function encodeForumTopic(value: ForumTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'forumTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['info'] = value.info === null ? null : encodeForumTopicInfo(value.info);
  out['last_message'] = value.last_message === null ? null : encodeMessage(value.last_message);
  out['order'] = value.order;
  out['is_pinned'] = value.is_pinned;
  out['unread_count'] = value.unread_count;
  out['last_read_inbox_message_id'] = value.last_read_inbox_message_id;
  out['last_read_outbox_message_id'] = value.last_read_outbox_message_id;
  out['unread_mention_count'] = value.unread_mention_count;
  out['unread_reaction_count'] = value.unread_reaction_count;
  out['unread_poll_vote_count'] = value.unread_poll_vote_count;
  out['notification_settings'] = value.notification_settings === null ? null : encodeChatNotificationSettings(value.notification_settings);
  out['draft_message'] = value.draft_message === null ? null : encodeDraftMessage(value.draft_message);
  return out;
}

/** forumTopicIcon = ForumTopicIcon — Describes a forum topic icon */
export class ForumTopicIcon {
  type: 'forumTopicIcon' = 'forumTopicIcon';
  extra?: string;
  /** color: int32 — Color of the topic icon in RGB format */
  color: number = 0;
  /** custom_emoji_id: int64 — Unique identifier of the custom emoji shown on the topic icon; 0 if none */
  custom_emoji_id: string = '';
}

/** Decode a TDLib `forumTopicIcon` object; null input -> null. */
export function decodeForumTopicIcon(json: TdJsonRaw): ForumTopicIcon | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ForumTopicIcon = new ForumTopicIcon();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.color = asNumber(record['color']);
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  return out;
}

/** Encode a `forumTopicIcon` into TDLib JSON wire shape. */
export function encodeForumTopicIcon(value: ForumTopicIcon): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'forumTopicIcon';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['color'] = value.color;
  out['custom_emoji_id'] = value.custom_emoji_id;
  return out;
}

/** forumTopicInfo = ForumTopicInfo — Contains basic information about a forum topic */
export class ForumTopicInfo {
  type: 'forumTopicInfo' = 'forumTopicInfo';
  extra?: string;
  /** chat_id: int53 — Identifier of a forum supergroup chat or a chat with a bot to which the topic belongs */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier of the topic */
  forum_topic_id: number = 0;
  /** name: string — Name of the topic */
  name: string = '';
  /** icon: forumTopicIcon — Icon of the topic */
  icon: ForumTopicIcon | null = null;
  /** creation_date: int32 — Point in time (Unix timestamp) when the topic was created */
  creation_date: number = 0;
  /** creator_id: MessageSender — Identifier of the creator of the topic */
  creator_id: TdMessageSender | null = null;
  /** is_general: Bool — True, if the topic is the General topic */
  is_general: boolean = false;
  /** is_outgoing: Bool — True, if the topic was created by the current user */
  is_outgoing: boolean = false;
  /** is_closed: Bool — True, if the topic is closed. If the topic is closed, then the user must have can_manage_topics administrator right in the supergroup or must be the creator of the topic to send messages there */
  is_closed: boolean = false;
  /** is_hidden: Bool — True, if the topic is hidden above the topic list and closed; for General topic only */
  is_hidden: boolean = false;
  /** is_name_implicit: Bool — True, if the name of the topic wasn't added explicitly */
  is_name_implicit: boolean = false;
}

/** Decode a TDLib `forumTopicInfo` object; null input -> null. */
export function decodeForumTopicInfo(json: TdJsonRaw): ForumTopicInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ForumTopicInfo = new ForumTopicInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.name = asString(record['name']);
  out.icon = decodeForumTopicIcon(record['icon']);
  out.creation_date = asNumber(record['creation_date']);
  out.creator_id = decodeTdMessageSender(record['creator_id']);
  out.is_general = asBool(record['is_general']);
  out.is_outgoing = asBool(record['is_outgoing']);
  out.is_closed = asBool(record['is_closed']);
  out.is_hidden = asBool(record['is_hidden']);
  out.is_name_implicit = asBool(record['is_name_implicit']);
  return out;
}

/** Encode a `forumTopicInfo` into TDLib JSON wire shape. */
export function encodeForumTopicInfo(value: ForumTopicInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'forumTopicInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['name'] = value.name;
  out['icon'] = value.icon === null ? null : encodeForumTopicIcon(value.icon);
  out['creation_date'] = value.creation_date;
  out['creator_id'] = value.creator_id === null ? null : encodeTdMessageSender(value.creator_id);
  out['is_general'] = value.is_general;
  out['is_outgoing'] = value.is_outgoing;
  out['is_closed'] = value.is_closed;
  out['is_hidden'] = value.is_hidden;
  out['is_name_implicit'] = value.is_name_implicit;
  return out;
}

/** forumTopics = ForumTopics — Describes a list of forum topics */
export class ForumTopics {
  type: 'forumTopics' = 'forumTopics';
  extra?: string;
  /** total_count: int32 — Approximate total number of forum topics found */
  total_count: number = 0;
  /** topics: vector<forumTopic> — List of forum topics */
  topics: ForumTopic[] = [];
  /** next_offset_date: int32 — Offset date for the next getForumTopics request */
  next_offset_date: number = 0;
  /** next_offset_message_id: int53 — Offset message identifier for the next getForumTopics request */
  next_offset_message_id: number = 0;
  /** next_offset_forum_topic_id: int32 — Offset forum topic identifier for the next getForumTopics request */
  next_offset_forum_topic_id: number = 0;
}

/** Decode a TDLib `forumTopics` object; null input -> null. */
export function decodeForumTopics(json: TdJsonRaw): ForumTopics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ForumTopics = new ForumTopics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.topics = decodeVector(record['topics'], (item: TdJsonRaw): ForumTopic => decodeForumTopic(item) as ForumTopic);
  out.next_offset_date = asNumber(record['next_offset_date']);
  out.next_offset_message_id = asNumber(record['next_offset_message_id']);
  out.next_offset_forum_topic_id = asNumber(record['next_offset_forum_topic_id']);
  return out;
}

/** Encode a `forumTopics` into TDLib JSON wire shape. */
export function encodeForumTopics(value: ForumTopics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'forumTopics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['topics'] = encodeVector(value.topics, (item: ForumTopic | null): TdJsonRaw => item === null ? null : encodeForumTopic(item));
  out['next_offset_date'] = value.next_offset_date;
  out['next_offset_message_id'] = value.next_offset_message_id;
  out['next_offset_forum_topic_id'] = value.next_offset_forum_topic_id;
  return out;
}

/** forwardMessages = Messages — Forwards previously sent messages. Returns the forwarded messages in the same order as the message identifiers passed in message_ids. If a message can't be forwarded, null will be returned instead of the mess... */
export class ForwardMessages {
  type: 'forwardMessages' = 'forwardMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which to forward messages */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Topic in which the messages will be forwarded; message threads aren't supported; pass null if none */
  topic_id: TdMessageTopic | null = null;
  /** from_chat_id: int53 — Identifier of the chat from which to forward messages */
  from_chat_id: number = 0;
  /** message_ids: vector<int53> — Identifiers of the messages to forward. Message identifiers must be in a strictly increasing order. At most 100 messages can be forwarded simultaneously. A message can be forwarded only if messageProperties.c... */
  message_ids: number[] = [];
  /** options: messageSendOptions — Options to be used to send the messages; pass null to use default options */
  options: MessageSendOptions | null = null;
  /** send_copy: Bool — Pass true to copy content of the messages without reference to the original sender. Always true if the messages are forwarded to a secret chat or are local. */
  send_copy: boolean = false;
  /** remove_caption: Bool — Pass true to remove media captions of message copies. Ignored if send_copy is false */
  remove_caption: boolean = false;
}

/** Decode a TDLib `forwardMessages` object; null input -> null. */
export function decodeForwardMessages(json: TdJsonRaw): ForwardMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ForwardMessages = new ForwardMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.from_chat_id = asNumber(record['from_chat_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.options = decodeMessageSendOptions(record['options']);
  out.send_copy = asBool(record['send_copy']);
  out.remove_caption = asBool(record['remove_caption']);
  return out;
}

/** Encode a `forwardMessages` into TDLib JSON wire shape. */
export function encodeForwardMessages(value: ForwardMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'forwardMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['from_chat_id'] = value.from_chat_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  out['options'] = value.options === null ? null : encodeMessageSendOptions(value.options);
  out['send_copy'] = value.send_copy;
  out['remove_caption'] = value.remove_caption;
  return out;
}

/** forwardSource = ForwardSource — Contains information about the last message from which a new message was forwarded last time */
export class ForwardSource {
  type: 'forwardSource' = 'forwardSource';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message that was forwarded belonged; may be 0 if unknown */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message; may be 0 if unknown */
  message_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of the message; may be null if unknown or the new message was forwarded not to Saved Messages */
  sender_id: TdMessageSender | null = null;
  /** sender_name: string — Name of the sender of the message if the sender is hidden by their privacy settings */
  sender_name: string = '';
  /** date: int32 — Point in time (Unix timestamp) when the message is sent; 0 if unknown */
  date: number = 0;
  /** is_outgoing: Bool — True, if the message that was forwarded is outgoing; always false if sender is unknown */
  is_outgoing: boolean = false;
}

/** Decode a TDLib `forwardSource` object; null input -> null. */
export function decodeForwardSource(json: TdJsonRaw): ForwardSource | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ForwardSource = new ForwardSource();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.sender_name = asString(record['sender_name']);
  out.date = asNumber(record['date']);
  out.is_outgoing = asBool(record['is_outgoing']);
  return out;
}

/** Encode a `forwardSource` into TDLib JSON wire shape. */
export function encodeForwardSource(value: ForwardSource): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'forwardSource';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['sender_name'] = value.sender_name;
  out['date'] = value.date;
  out['is_outgoing'] = value.is_outgoing;
  return out;
}

/** foundAffiliateProgram = FoundAffiliateProgram — Describes a found affiliate program */
export class FoundAffiliateProgram {
  type: 'foundAffiliateProgram' = 'foundAffiliateProgram';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the bot created the program */
  bot_user_id: number = 0;
  /** info: affiliateProgramInfo — Information about the affiliate program */
  info: AffiliateProgramInfo | null = null;
}

/** Decode a TDLib `foundAffiliateProgram` object; null input -> null. */
export function decodeFoundAffiliateProgram(json: TdJsonRaw): FoundAffiliateProgram | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundAffiliateProgram = new FoundAffiliateProgram();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.info = decodeAffiliateProgramInfo(record['info']);
  return out;
}

/** Encode a `foundAffiliateProgram` into TDLib JSON wire shape. */
export function encodeFoundAffiliateProgram(value: FoundAffiliateProgram): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundAffiliateProgram';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['info'] = value.info === null ? null : encodeAffiliateProgramInfo(value.info);
  return out;
}

/** foundAffiliatePrograms = FoundAffiliatePrograms — Represents a list of found affiliate programs */
export class FoundAffiliatePrograms {
  type: 'foundAffiliatePrograms' = 'foundAffiliatePrograms';
  extra?: string;
  /** total_count: int32 — The total number of found affiliate programs */
  total_count: number = 0;
  /** programs: vector<foundAffiliateProgram> — The list of affiliate programs */
  programs: FoundAffiliateProgram[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `foundAffiliatePrograms` object; null input -> null. */
export function decodeFoundAffiliatePrograms(json: TdJsonRaw): FoundAffiliatePrograms | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundAffiliatePrograms = new FoundAffiliatePrograms();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.programs = decodeVector(record['programs'], (item: TdJsonRaw): FoundAffiliateProgram => decodeFoundAffiliateProgram(item) as FoundAffiliateProgram);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `foundAffiliatePrograms` into TDLib JSON wire shape. */
export function encodeFoundAffiliatePrograms(value: FoundAffiliatePrograms): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundAffiliatePrograms';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['programs'] = encodeVector(value.programs, (item: FoundAffiliateProgram | null): TdJsonRaw => item === null ? null : encodeFoundAffiliateProgram(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** foundChatBoosts = FoundChatBoosts — Contains a list of boosts applied to a chat */
export class FoundChatBoosts {
  type: 'foundChatBoosts' = 'foundChatBoosts';
  extra?: string;
  /** total_count: int32 — Total number of boosts applied to the chat */
  total_count: number = 0;
  /** boosts: vector<chatBoost> — List of boosts */
  boosts: ChatBoost[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `foundChatBoosts` object; null input -> null. */
export function decodeFoundChatBoosts(json: TdJsonRaw): FoundChatBoosts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundChatBoosts = new FoundChatBoosts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.boosts = decodeVector(record['boosts'], (item: TdJsonRaw): ChatBoost => decodeChatBoost(item) as ChatBoost);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `foundChatBoosts` into TDLib JSON wire shape. */
export function encodeFoundChatBoosts(value: FoundChatBoosts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundChatBoosts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['boosts'] = encodeVector(value.boosts, (item: ChatBoost | null): TdJsonRaw => item === null ? null : encodeChatBoost(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** foundChatMessages = FoundChatMessages — Contains a list of messages found by a search in a given chat */
export class FoundChatMessages {
  type: 'foundChatMessages' = 'foundChatMessages';
  extra?: string;
  /** total_count: int32 — Approximate total number of messages found; -1 if unknown */
  total_count: number = 0;
  /** messages: vector<message> — List of messages */
  messages: Message[] = [];
  /** next_from_message_id: int53 — The offset for the next request. If 0, there are no more results */
  next_from_message_id: number = 0;
}

/** Decode a TDLib `foundChatMessages` object; null input -> null. */
export function decodeFoundChatMessages(json: TdJsonRaw): FoundChatMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundChatMessages = new FoundChatMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): Message => decodeMessage(item) as Message);
  out.next_from_message_id = asNumber(record['next_from_message_id']);
  return out;
}

/** Encode a `foundChatMessages` into TDLib JSON wire shape. */
export function encodeFoundChatMessages(value: FoundChatMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundChatMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['messages'] = encodeVector(value.messages, (item: Message | null): TdJsonRaw => item === null ? null : encodeMessage(item));
  out['next_from_message_id'] = value.next_from_message_id;
  return out;
}

/** foundFileDownloads = FoundFileDownloads — Contains a list of downloaded files, found by a search */
export class FoundFileDownloads {
  type: 'foundFileDownloads' = 'foundFileDownloads';
  extra?: string;
  /** total_counts: downloadedFileCounts — Total number of suitable files, ignoring offset */
  total_counts: DownloadedFileCounts | null = null;
  /** files: vector<fileDownload> — The list of files */
  files: FileDownload[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `foundFileDownloads` object; null input -> null. */
export function decodeFoundFileDownloads(json: TdJsonRaw): FoundFileDownloads | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundFileDownloads = new FoundFileDownloads();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_counts = decodeDownloadedFileCounts(record['total_counts']);
  out.files = decodeVector(record['files'], (item: TdJsonRaw): FileDownload => decodeFileDownload(item) as FileDownload);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `foundFileDownloads` into TDLib JSON wire shape. */
export function encodeFoundFileDownloads(value: FoundFileDownloads): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundFileDownloads';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_counts'] = value.total_counts === null ? null : encodeDownloadedFileCounts(value.total_counts);
  out['files'] = encodeVector(value.files, (item: FileDownload | null): TdJsonRaw => item === null ? null : encodeFileDownload(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** foundMessages = FoundMessages — Contains a list of messages found by a search */
export class FoundMessages {
  type: 'foundMessages' = 'foundMessages';
  extra?: string;
  /** total_count: int32 — Approximate total number of messages found; -1 if unknown */
  total_count: number = 0;
  /** messages: vector<message> — List of messages */
  messages: Message[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `foundMessages` object; null input -> null. */
export function decodeFoundMessages(json: TdJsonRaw): FoundMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundMessages = new FoundMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): Message => decodeMessage(item) as Message);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `foundMessages` into TDLib JSON wire shape. */
export function encodeFoundMessages(value: FoundMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['messages'] = encodeVector(value.messages, (item: Message | null): TdJsonRaw => item === null ? null : encodeMessage(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** foundPosition = FoundPosition — Contains 0-based match position */
export class FoundPosition {
  type: 'foundPosition' = 'foundPosition';
  extra?: string;
  /** position: int32 — The position of the match */
  position: number = 0;
}

/** Decode a TDLib `foundPosition` object; null input -> null. */
export function decodeFoundPosition(json: TdJsonRaw): FoundPosition | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundPosition = new FoundPosition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.position = asNumber(record['position']);
  return out;
}

/** Encode a `foundPosition` into TDLib JSON wire shape. */
export function encodeFoundPosition(value: FoundPosition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundPosition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['position'] = value.position;
  return out;
}

/** foundPositions = FoundPositions — Contains 0-based positions of matched objects */
export class FoundPositions {
  type: 'foundPositions' = 'foundPositions';
  extra?: string;
  /** total_count: int32 — Total number of matched objects */
  total_count: number = 0;
  /** positions: vector<int32> — The positions of the matched objects */
  positions: number[] = [];
}

/** Decode a TDLib `foundPositions` object; null input -> null. */
export function decodeFoundPositions(json: TdJsonRaw): FoundPositions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundPositions = new FoundPositions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.positions = decodeVector(record['positions'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `foundPositions` into TDLib JSON wire shape. */
export function encodeFoundPositions(value: FoundPositions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundPositions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['positions'] = encodeVector(value.positions, (item: number): TdJsonRaw => item);
  return out;
}

/** foundPublicPosts = FoundPublicPosts — Contains a list of messages found by a public post search */
export class FoundPublicPosts {
  type: 'foundPublicPosts' = 'foundPublicPosts';
  extra?: string;
  /** messages: vector<message> — List of found public posts */
  messages: Message[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
  /** search_limits: publicPostSearchLimits — Updated public post search limits after the query; repeated requests with the same query will be free; may be null if they didn't change */
  search_limits: PublicPostSearchLimits | null = null;
  /** are_limits_exceeded: Bool — True, if the query has failed because search limits are exceeded. In this case search_limits.daily_free_query_count will be equal to 0 */
  are_limits_exceeded: boolean = false;
}

/** Decode a TDLib `foundPublicPosts` object; null input -> null. */
export function decodeFoundPublicPosts(json: TdJsonRaw): FoundPublicPosts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundPublicPosts = new FoundPublicPosts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): Message => decodeMessage(item) as Message);
  out.next_offset = asString(record['next_offset']);
  out.search_limits = decodePublicPostSearchLimits(record['search_limits']);
  out.are_limits_exceeded = asBool(record['are_limits_exceeded']);
  return out;
}

/** Encode a `foundPublicPosts` into TDLib JSON wire shape. */
export function encodeFoundPublicPosts(value: FoundPublicPosts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundPublicPosts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['messages'] = encodeVector(value.messages, (item: Message | null): TdJsonRaw => item === null ? null : encodeMessage(item));
  out['next_offset'] = value.next_offset;
  out['search_limits'] = value.search_limits === null ? null : encodePublicPostSearchLimits(value.search_limits);
  out['are_limits_exceeded'] = value.are_limits_exceeded;
  return out;
}

/** foundStories = FoundStories — Contains a list of stories found by a search */
export class FoundStories {
  type: 'foundStories' = 'foundStories';
  extra?: string;
  /** total_count: int32 — Approximate total number of stories found */
  total_count: number = 0;
  /** stories: vector<story> — List of stories */
  stories: Story[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `foundStories` object; null input -> null. */
export function decodeFoundStories(json: TdJsonRaw): FoundStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundStories = new FoundStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.stories = decodeVector(record['stories'], (item: TdJsonRaw): Story => decodeStory(item) as Story);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `foundStories` into TDLib JSON wire shape. */
export function encodeFoundStories(value: FoundStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['stories'] = encodeVector(value.stories, (item: Story | null): TdJsonRaw => item === null ? null : encodeStory(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** foundUsers = FoundUsers — Represents a list of found users */
export class FoundUsers {
  type: 'foundUsers' = 'foundUsers';
  extra?: string;
  /** user_ids: vector<int53> — Identifiers of the found users */
  user_ids: number[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `foundUsers` object; null input -> null. */
export function decodeFoundUsers(json: TdJsonRaw): FoundUsers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundUsers = new FoundUsers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `foundUsers` into TDLib JSON wire shape. */
export function encodeFoundUsers(value: FoundUsers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundUsers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  out['next_offset'] = value.next_offset;
  return out;
}

/** foundWebApp = FoundWebApp — Contains information about a Web App found by its short name */
export class FoundWebApp {
  type: 'foundWebApp' = 'foundWebApp';
  extra?: string;
  /** web_app: webApp — The Web App */
  web_app: WebApp | null = null;
  /** request_write_access: Bool — True, if the user must be asked for the permission to the bot to send them messages */
  request_write_access: boolean = false;
  /** skip_confirmation: Bool — True, if there is no need to show an ordinary open URL confirmation before opening the Web App. The field must be ignored and confirmation must be shown anyway if the Web App link was hidden */
  skip_confirmation: boolean = false;
}

/** Decode a TDLib `foundWebApp` object; null input -> null. */
export function decodeFoundWebApp(json: TdJsonRaw): FoundWebApp | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: FoundWebApp = new FoundWebApp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.web_app = decodeWebApp(record['web_app']);
  out.request_write_access = asBool(record['request_write_access']);
  out.skip_confirmation = asBool(record['skip_confirmation']);
  return out;
}

/** Encode a `foundWebApp` into TDLib JSON wire shape. */
export function encodeFoundWebApp(value: FoundWebApp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'foundWebApp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['web_app'] = value.web_app === null ? null : encodeWebApp(value.web_app);
  out['request_write_access'] = value.request_write_access;
  out['skip_confirmation'] = value.skip_confirmation;
  return out;
}
