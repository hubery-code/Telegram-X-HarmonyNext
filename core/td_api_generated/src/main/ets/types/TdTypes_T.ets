/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { Background, decodeBackground, encodeBackground } from './TdTypes_B';
import { ChatInviteLinkInfo, decodeChatInviteLinkInfo, encodeChatInviteLinkInfo } from './TdTypes_C';
import { ErrorValue, decodeErrorValue, encodeErrorValue } from './TdTypes_E';
import { File, FormattedText, decodeFile, decodeFormattedText, encodeFile, encodeFormattedText } from './TdTypes_F';
import { GiveawayParameters, decodeGiveawayParameters, encodeGiveawayParameters } from './TdTypes_G';
import { InputRichMessage, decodeInputRichMessage, encodeInputRichMessage } from './TdTypes_I';
import { ProxyValue, decodeProxyValue, encodeProxyValue } from './TdTypes_P';
import { Sticker, StickerSetInfo, decodeSticker, decodeStickerSetInfo, encodeSticker, encodeStickerSetInfo } from './TdTypes_S';
import { UpgradedGift, decodeUpgradedGift, encodeUpgradedGift } from './TdTypes_U';
import { TdBackgroundFill, TdBuiltInTheme, decodeTdBackgroundFill, decodeTdBuiltInTheme, encodeTdBackgroundFill, encodeTdBuiltInTheme } from './TdUnions_B';
import { TdChatList, decodeTdChatList, encodeTdChatList } from './TdUnions_C';
import { TdDateTimeFormattingType, decodeTdDateTimeFormattingType, encodeTdDateTimeFormattingType } from './TdUnions_D';
import { TdInternalLinkType, decodeTdInternalLinkType, encodeTdInternalLinkType } from './TdUnions_I';
import { TdMessageSender, decodeTdMessageSender, encodeTdMessageSender } from './TdUnions_M';
import { TdRevenueWithdrawalState, decodeTdRevenueWithdrawalState, encodeTdRevenueWithdrawalState } from './TdUnions_R';
import { TdTMeUrlType, TdTextEntityType, TdThumbnailFormat, TdTonTransactionType, decodeTdTMeUrlType, decodeTdTextEntityType, decodeTdThumbnailFormat, decodeTdTonTransactionType, encodeTdTMeUrlType, encodeTdTextEntityType, encodeTdThumbnailFormat, encodeTdTonTransactionType } from './TdUnions_T';

/** tMeUrl = TMeUrl — Represents a URL linking to an internal Telegram entity */
export class TMeUrl {
  type: 'tMeUrl' = 'tMeUrl';
  extra?: string;
  /** url: string — URL */
  url: string = '';
  /** type: TMeUrlType — Type of the URL */
  type_: TdTMeUrlType | null = null;
}

/** Decode a TDLib `tMeUrl` object; null input -> null. */
export function decodeTMeUrl(json: TdJsonRaw): TMeUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TMeUrl = new TMeUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.type_ = decodeTdTMeUrlType(record['type']);
  return out;
}

/** Encode a `tMeUrl` into TDLib JSON wire shape. */
export function encodeTMeUrl(value: TMeUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tMeUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['type'] = value.type_ === null ? null : encodeTdTMeUrlType(value.type_);
  return out;
}

/** tMeUrlTypeChatInvite = TMeUrlType — A chat invite link */
export class TMeUrlTypeChatInvite {
  type: 'tMeUrlTypeChatInvite' = 'tMeUrlTypeChatInvite';
  extra?: string;
  /** info: chatInviteLinkInfo — Information about the chat invite link */
  info: ChatInviteLinkInfo | null = null;
}

/** Decode a TDLib `tMeUrlTypeChatInvite` object; null input -> null. */
export function decodeTMeUrlTypeChatInvite(json: TdJsonRaw): TMeUrlTypeChatInvite | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TMeUrlTypeChatInvite = new TMeUrlTypeChatInvite();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.info = decodeChatInviteLinkInfo(record['info']);
  return out;
}

/** Encode a `tMeUrlTypeChatInvite` into TDLib JSON wire shape. */
export function encodeTMeUrlTypeChatInvite(value: TMeUrlTypeChatInvite): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tMeUrlTypeChatInvite';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['info'] = value.info === null ? null : encodeChatInviteLinkInfo(value.info);
  return out;
}

/** tMeUrlTypeStickerSet = TMeUrlType — A URL linking to a sticker set */
export class TMeUrlTypeStickerSet {
  type: 'tMeUrlTypeStickerSet' = 'tMeUrlTypeStickerSet';
  extra?: string;
  /** sticker_set_id: int64 — Identifier of the sticker set */
  sticker_set_id: string = '';
}

/** Decode a TDLib `tMeUrlTypeStickerSet` object; null input -> null. */
export function decodeTMeUrlTypeStickerSet(json: TdJsonRaw): TMeUrlTypeStickerSet | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TMeUrlTypeStickerSet = new TMeUrlTypeStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_set_id = asInt64(record['sticker_set_id']);
  return out;
}

/** Encode a `tMeUrlTypeStickerSet` into TDLib JSON wire shape. */
export function encodeTMeUrlTypeStickerSet(value: TMeUrlTypeStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tMeUrlTypeStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_set_id'] = value.sticker_set_id;
  return out;
}

/** tMeUrlTypeSupergroup = TMeUrlType — A URL linking to a public supergroup or channel */
export class TMeUrlTypeSupergroup {
  type: 'tMeUrlTypeSupergroup' = 'tMeUrlTypeSupergroup';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup or channel */
  supergroup_id: number = 0;
}

/** Decode a TDLib `tMeUrlTypeSupergroup` object; null input -> null. */
export function decodeTMeUrlTypeSupergroup(json: TdJsonRaw): TMeUrlTypeSupergroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TMeUrlTypeSupergroup = new TMeUrlTypeSupergroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  return out;
}

/** Encode a `tMeUrlTypeSupergroup` into TDLib JSON wire shape. */
export function encodeTMeUrlTypeSupergroup(value: TMeUrlTypeSupergroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tMeUrlTypeSupergroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  return out;
}

/** tMeUrlTypeUser = TMeUrlType — A URL linking to a user */
export class TMeUrlTypeUser {
  type: 'tMeUrlTypeUser' = 'tMeUrlTypeUser';
  extra?: string;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
}

/** Decode a TDLib `tMeUrlTypeUser` object; null input -> null. */
export function decodeTMeUrlTypeUser(json: TdJsonRaw): TMeUrlTypeUser | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TMeUrlTypeUser = new TMeUrlTypeUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `tMeUrlTypeUser` into TDLib JSON wire shape. */
export function encodeTMeUrlTypeUser(value: TMeUrlTypeUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tMeUrlTypeUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** tMeUrls = TMeUrls — Contains a list of t.me URLs */
export class TMeUrls {
  type: 'tMeUrls' = 'tMeUrls';
  extra?: string;
  /** urls: vector<tMeUrl> — List of URLs */
  urls: TMeUrl[] = [];
}

/** Decode a TDLib `tMeUrls` object; null input -> null. */
export function decodeTMeUrls(json: TdJsonRaw): TMeUrls | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TMeUrls = new TMeUrls();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.urls = decodeVector(record['urls'], (item: TdJsonRaw): TMeUrl => decodeTMeUrl(item) as TMeUrl);
  return out;
}

/** Encode a `tMeUrls` into TDLib JSON wire shape. */
export function encodeTMeUrls(value: TMeUrls): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tMeUrls';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['urls'] = encodeVector(value.urls, (item: TMeUrl | null): TdJsonRaw => item === null ? null : encodeTMeUrl(item));
  return out;
}

/** targetChatChosen = TargetChat — The chat needs to be chosen by the user among chats of the specified types */
export class TargetChatChosen {
  type: 'targetChatChosen' = 'targetChatChosen';
  extra?: string;
  /** types: targetChatTypes — Allowed types for the chat */
  types: TargetChatTypes | null = null;
}

/** Decode a TDLib `targetChatChosen` object; null input -> null. */
export function decodeTargetChatChosen(json: TdJsonRaw): TargetChatChosen | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TargetChatChosen = new TargetChatChosen();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.types = decodeTargetChatTypes(record['types']);
  return out;
}

/** Encode a `targetChatChosen` into TDLib JSON wire shape. */
export function encodeTargetChatChosen(value: TargetChatChosen): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'targetChatChosen';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['types'] = value.types === null ? null : encodeTargetChatTypes(value.types);
  return out;
}

/** targetChatCurrent = TargetChat — The currently opened chat and forum topic must be kept */
export class TargetChatCurrent {
  type: 'targetChatCurrent' = 'targetChatCurrent';
  extra?: string;
}

/** Decode a TDLib `targetChatCurrent` object; null input -> null. */
export function decodeTargetChatCurrent(json: TdJsonRaw): TargetChatCurrent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TargetChatCurrent = new TargetChatCurrent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `targetChatCurrent` into TDLib JSON wire shape. */
export function encodeTargetChatCurrent(value: TargetChatCurrent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'targetChatCurrent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** targetChatInternalLink = TargetChat — The chat needs to be open with the provided internal link */
export class TargetChatInternalLink {
  type: 'targetChatInternalLink' = 'targetChatInternalLink';
  extra?: string;
  /** link: InternalLinkType — An internal link pointing to the chat */
  link: TdInternalLinkType | null = null;
}

/** Decode a TDLib `targetChatInternalLink` object; null input -> null. */
export function decodeTargetChatInternalLink(json: TdJsonRaw): TargetChatInternalLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TargetChatInternalLink = new TargetChatInternalLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = decodeTdInternalLinkType(record['link']);
  return out;
}

/** Encode a `targetChatInternalLink` into TDLib JSON wire shape. */
export function encodeTargetChatInternalLink(value: TargetChatInternalLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'targetChatInternalLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link === null ? null : encodeTdInternalLinkType(value.link);
  return out;
}

/** targetChatTypes = TargetChatTypes — Describes allowed types for the target chat */
export class TargetChatTypes {
  type: 'targetChatTypes' = 'targetChatTypes';
  extra?: string;
  /** allow_user_chats: Bool — True, if private chats with ordinary users are allowed */
  allow_user_chats: boolean = false;
  /** allow_bot_chats: Bool — True, if private chats with other bots are allowed */
  allow_bot_chats: boolean = false;
  /** allow_group_chats: Bool — True, if basic group and supergroup chats are allowed */
  allow_group_chats: boolean = false;
  /** allow_channel_chats: Bool — True, if channel chats are allowed */
  allow_channel_chats: boolean = false;
}

/** Decode a TDLib `targetChatTypes` object; null input -> null. */
export function decodeTargetChatTypes(json: TdJsonRaw): TargetChatTypes | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TargetChatTypes = new TargetChatTypes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.allow_user_chats = asBool(record['allow_user_chats']);
  out.allow_bot_chats = asBool(record['allow_bot_chats']);
  out.allow_group_chats = asBool(record['allow_group_chats']);
  out.allow_channel_chats = asBool(record['allow_channel_chats']);
  return out;
}

/** Encode a `targetChatTypes` into TDLib JSON wire shape. */
export function encodeTargetChatTypes(value: TargetChatTypes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'targetChatTypes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['allow_user_chats'] = value.allow_user_chats;
  out['allow_bot_chats'] = value.allow_bot_chats;
  out['allow_group_chats'] = value.allow_group_chats;
  out['allow_channel_chats'] = value.allow_channel_chats;
  return out;
}

/** telegramPaymentPurposeGiftedStars = TelegramPaymentPurpose — The user buying Telegram Stars for other users */
export class TelegramPaymentPurposeGiftedStars {
  type: 'telegramPaymentPurposeGiftedStars' = 'telegramPaymentPurposeGiftedStars';
  extra?: string;
  /** user_id: int53 — Identifier of the user to which Telegram Stars are gifted */
  user_id: number = 0;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** star_count: int53 — Number of bought Telegram Stars */
  star_count: number = 0;
}

/** Decode a TDLib `telegramPaymentPurposeGiftedStars` object; null input -> null. */
export function decodeTelegramPaymentPurposeGiftedStars(json: TdJsonRaw): TelegramPaymentPurposeGiftedStars | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TelegramPaymentPurposeGiftedStars = new TelegramPaymentPurposeGiftedStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `telegramPaymentPurposeGiftedStars` into TDLib JSON wire shape. */
export function encodeTelegramPaymentPurposeGiftedStars(value: TelegramPaymentPurposeGiftedStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'telegramPaymentPurposeGiftedStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['star_count'] = value.star_count;
  return out;
}

/** telegramPaymentPurposeJoinChat = TelegramPaymentPurpose — The user joins a chat and subscribes to regular payments in Telegram Stars */
export class TelegramPaymentPurposeJoinChat {
  type: 'telegramPaymentPurposeJoinChat' = 'telegramPaymentPurposeJoinChat';
  extra?: string;
  /** invite_link: string — Invite link to use */
  invite_link: string = '';
}

/** Decode a TDLib `telegramPaymentPurposeJoinChat` object; null input -> null. */
export function decodeTelegramPaymentPurposeJoinChat(json: TdJsonRaw): TelegramPaymentPurposeJoinChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TelegramPaymentPurposeJoinChat = new TelegramPaymentPurposeJoinChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  return out;
}

/** Encode a `telegramPaymentPurposeJoinChat` into TDLib JSON wire shape. */
export function encodeTelegramPaymentPurposeJoinChat(value: TelegramPaymentPurposeJoinChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'telegramPaymentPurposeJoinChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  return out;
}

/** telegramPaymentPurposePremiumGift = TelegramPaymentPurpose — The user gifting Telegram Premium to another user */
export class TelegramPaymentPurposePremiumGift {
  type: 'telegramPaymentPurposePremiumGift' = 'telegramPaymentPurposePremiumGift';
  extra?: string;
  /** currency: string — ISO 4217 currency code of the payment currency, or "XTR" for payments in Telegram Stars */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** user_id: int53 — Identifier of the user who will receive Telegram Premium */
  user_id: number = 0;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active for the user */
  month_count: number = 0;
  /** text: formattedText — Text to show to the user receiving Telegram Premium; 0-getOption("gift_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed */
  text: FormattedText | null = null;
}

/** Decode a TDLib `telegramPaymentPurposePremiumGift` object; null input -> null. */
export function decodeTelegramPaymentPurposePremiumGift(json: TdJsonRaw): TelegramPaymentPurposePremiumGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TelegramPaymentPurposePremiumGift = new TelegramPaymentPurposePremiumGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.user_id = asNumber(record['user_id']);
  out.month_count = asNumber(record['month_count']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `telegramPaymentPurposePremiumGift` into TDLib JSON wire shape. */
export function encodeTelegramPaymentPurposePremiumGift(value: TelegramPaymentPurposePremiumGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'telegramPaymentPurposePremiumGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['user_id'] = value.user_id;
  out['month_count'] = value.month_count;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** telegramPaymentPurposePremiumGiftCodes = TelegramPaymentPurpose — The user boosting a chat by creating Telegram Premium gift codes for other users */
export class TelegramPaymentPurposePremiumGiftCodes {
  type: 'telegramPaymentPurposePremiumGiftCodes' = 'telegramPaymentPurposePremiumGiftCodes';
  extra?: string;
  /** boosted_chat_id: int53 — Identifier of the supergroup or channel chat, which will be automatically boosted by the users for duration of the Premium subscription and which is administered by the user */
  boosted_chat_id: number = 0;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** user_ids: vector<int53> — Identifiers of the users who can activate the gift codes */
  user_ids: number[] = [];
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active for the users */
  month_count: number = 0;
  /** text: formattedText — Text to show along with the gift codes; 0-getOption("gift_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed */
  text: FormattedText | null = null;
}

/** Decode a TDLib `telegramPaymentPurposePremiumGiftCodes` object; null input -> null. */
export function decodeTelegramPaymentPurposePremiumGiftCodes(json: TdJsonRaw): TelegramPaymentPurposePremiumGiftCodes | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TelegramPaymentPurposePremiumGiftCodes = new TelegramPaymentPurposePremiumGiftCodes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.boosted_chat_id = asNumber(record['boosted_chat_id']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.month_count = asNumber(record['month_count']);
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `telegramPaymentPurposePremiumGiftCodes` into TDLib JSON wire shape. */
export function encodeTelegramPaymentPurposePremiumGiftCodes(value: TelegramPaymentPurposePremiumGiftCodes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'telegramPaymentPurposePremiumGiftCodes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['boosted_chat_id'] = value.boosted_chat_id;
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  out['month_count'] = value.month_count;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** telegramPaymentPurposePremiumGiveaway = TelegramPaymentPurpose — The user creating a Telegram Premium giveaway */
export class TelegramPaymentPurposePremiumGiveaway {
  type: 'telegramPaymentPurposePremiumGiveaway' = 'telegramPaymentPurposePremiumGiveaway';
  extra?: string;
  /** parameters: giveawayParameters — Giveaway parameters */
  parameters: GiveawayParameters | null = null;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** winner_count: int32 — Number of users who will be able to activate the gift codes */
  winner_count: number = 0;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active for the users */
  month_count: number = 0;
}

/** Decode a TDLib `telegramPaymentPurposePremiumGiveaway` object; null input -> null. */
export function decodeTelegramPaymentPurposePremiumGiveaway(json: TdJsonRaw): TelegramPaymentPurposePremiumGiveaway | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TelegramPaymentPurposePremiumGiveaway = new TelegramPaymentPurposePremiumGiveaway();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.parameters = decodeGiveawayParameters(record['parameters']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.winner_count = asNumber(record['winner_count']);
  out.month_count = asNumber(record['month_count']);
  return out;
}

/** Encode a `telegramPaymentPurposePremiumGiveaway` into TDLib JSON wire shape. */
export function encodeTelegramPaymentPurposePremiumGiveaway(value: TelegramPaymentPurposePremiumGiveaway): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'telegramPaymentPurposePremiumGiveaway';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['parameters'] = value.parameters === null ? null : encodeGiveawayParameters(value.parameters);
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['winner_count'] = value.winner_count;
  out['month_count'] = value.month_count;
  return out;
}

/** telegramPaymentPurposeStarGiveaway = TelegramPaymentPurpose — The user creating a Telegram Star giveaway */
export class TelegramPaymentPurposeStarGiveaway {
  type: 'telegramPaymentPurposeStarGiveaway' = 'telegramPaymentPurposeStarGiveaway';
  extra?: string;
  /** parameters: giveawayParameters — Giveaway parameters */
  parameters: GiveawayParameters | null = null;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** winner_count: int32 — The number of users to receive Telegram Stars */
  winner_count: number = 0;
  /** star_count: int53 — The number of Telegram Stars to be distributed through the giveaway */
  star_count: number = 0;
}

/** Decode a TDLib `telegramPaymentPurposeStarGiveaway` object; null input -> null. */
export function decodeTelegramPaymentPurposeStarGiveaway(json: TdJsonRaw): TelegramPaymentPurposeStarGiveaway | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TelegramPaymentPurposeStarGiveaway = new TelegramPaymentPurposeStarGiveaway();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.parameters = decodeGiveawayParameters(record['parameters']);
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.winner_count = asNumber(record['winner_count']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `telegramPaymentPurposeStarGiveaway` into TDLib JSON wire shape. */
export function encodeTelegramPaymentPurposeStarGiveaway(value: TelegramPaymentPurposeStarGiveaway): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'telegramPaymentPurposeStarGiveaway';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['parameters'] = value.parameters === null ? null : encodeGiveawayParameters(value.parameters);
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['winner_count'] = value.winner_count;
  out['star_count'] = value.star_count;
  return out;
}

/** telegramPaymentPurposeStars = TelegramPaymentPurpose — The user buying Telegram Stars */
export class TelegramPaymentPurposeStars {
  type: 'telegramPaymentPurposeStars' = 'telegramPaymentPurposeStars';
  extra?: string;
  /** currency: string — ISO 4217 currency code of the payment currency */
  currency: string = '';
  /** amount: int53 — Paid amount, in the smallest units of the currency */
  amount: number = 0;
  /** star_count: int53 — Number of bought Telegram Stars */
  star_count: number = 0;
  /** chat_id: int53 — Identifier of the chat that is supposed to receive the Telegram Stars; pass 0 if none */
  chat_id: number = 0;
}

/** Decode a TDLib `telegramPaymentPurposeStars` object; null input -> null. */
export function decodeTelegramPaymentPurposeStars(json: TdJsonRaw): TelegramPaymentPurposeStars | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TelegramPaymentPurposeStars = new TelegramPaymentPurposeStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.star_count = asNumber(record['star_count']);
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `telegramPaymentPurposeStars` into TDLib JSON wire shape. */
export function encodeTelegramPaymentPurposeStars(value: TelegramPaymentPurposeStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'telegramPaymentPurposeStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['star_count'] = value.star_count;
  out['chat_id'] = value.chat_id;
  return out;
}

/** temporaryPasswordState = TemporaryPasswordState — Returns information about the availability of a temporary password, which can be used for payments */
export class TemporaryPasswordState {
  type: 'temporaryPasswordState' = 'temporaryPasswordState';
  extra?: string;
  /** has_password: Bool — True, if a temporary password is available */
  has_password: boolean = false;
  /** valid_for: int32 — Time left before the temporary password expires, in seconds */
  valid_for: number = 0;
}

/** Decode a TDLib `temporaryPasswordState` object; null input -> null. */
export function decodeTemporaryPasswordState(json: TdJsonRaw): TemporaryPasswordState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TemporaryPasswordState = new TemporaryPasswordState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.has_password = asBool(record['has_password']);
  out.valid_for = asNumber(record['valid_for']);
  return out;
}

/** Encode a `temporaryPasswordState` into TDLib JSON wire shape. */
export function encodeTemporaryPasswordState(value: TemporaryPasswordState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'temporaryPasswordState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['has_password'] = value.has_password;
  out['valid_for'] = value.valid_for;
  return out;
}

/** terminateAllOtherSessions = Ok — Terminates all other sessions of the current user. Additionally, the user must be suggested to delete the connected business bot using deleteBusinessConnectedBot if there is any */
export class TerminateAllOtherSessions {
  type: 'terminateAllOtherSessions' = 'terminateAllOtherSessions';
  extra?: string;
}

/** Decode a TDLib `terminateAllOtherSessions` object; null input -> null. */
export function decodeTerminateAllOtherSessions(json: TdJsonRaw): TerminateAllOtherSessions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TerminateAllOtherSessions = new TerminateAllOtherSessions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `terminateAllOtherSessions` into TDLib JSON wire shape. */
export function encodeTerminateAllOtherSessions(value: TerminateAllOtherSessions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'terminateAllOtherSessions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** terminateSession = Ok — Terminates a session of the current user */
export class TerminateSession {
  type: 'terminateSession' = 'terminateSession';
  extra?: string;
  /** session_id: int64 — Session identifier */
  session_id: string = '';
}

/** Decode a TDLib `terminateSession` object; null input -> null. */
export function decodeTerminateSession(json: TdJsonRaw): TerminateSession | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TerminateSession = new TerminateSession();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.session_id = asInt64(record['session_id']);
  return out;
}

/** Encode a `terminateSession` into TDLib JSON wire shape. */
export function encodeTerminateSession(value: TerminateSession): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'terminateSession';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['session_id'] = value.session_id;
  return out;
}

/** termsOfService = TermsOfService — Contains Telegram terms of service */
export class TermsOfService {
  type: 'termsOfService' = 'termsOfService';
  extra?: string;
  /** text: formattedText — Text of the terms of service */
  text: FormattedText | null = null;
  /** min_user_age: int32 — The minimum age of a user to be able to accept the terms; 0 if age isn't restricted */
  min_user_age: number = 0;
  /** show_popup: Bool — True, if a blocking popup with terms of service must be shown to the user */
  show_popup: boolean = false;
}

/** Decode a TDLib `termsOfService` object; null input -> null. */
export function decodeTermsOfService(json: TdJsonRaw): TermsOfService | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TermsOfService = new TermsOfService();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.min_user_age = asNumber(record['min_user_age']);
  out.show_popup = asBool(record['show_popup']);
  return out;
}

/** Encode a `termsOfService` into TDLib JSON wire shape. */
export function encodeTermsOfService(value: TermsOfService): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'termsOfService';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['min_user_age'] = value.min_user_age;
  out['show_popup'] = value.show_popup;
  return out;
}

/** testBytes = TestBytes — A simple object containing a sequence of bytes; for testing only */
export class TestBytes {
  type: 'testBytes' = 'testBytes';
  extra?: string;
  /** value: bytes — Bytes */
  value: string = '';
}

/** Decode a TDLib `testBytes` object; null input -> null. */
export function decodeTestBytes(json: TdJsonRaw): TestBytes | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestBytes = new TestBytes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asString(record['value']);
  return out;
}

/** Encode a `testBytes` into TDLib JSON wire shape. */
export function encodeTestBytes(value: TestBytes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testBytes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}

/** testCallBytes = TestBytes — Returns the received bytes; for testing only. This is an offline method. Can be called before authorization */
export class TestCallBytes {
  type: 'testCallBytes' = 'testCallBytes';
  extra?: string;
  /** x: bytes — Bytes to return */
  x: string = '';
}

/** Decode a TDLib `testCallBytes` object; null input -> null. */
export function decodeTestCallBytes(json: TdJsonRaw): TestCallBytes | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestCallBytes = new TestCallBytes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.x = asString(record['x']);
  return out;
}

/** Encode a `testCallBytes` into TDLib JSON wire shape. */
export function encodeTestCallBytes(value: TestCallBytes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testCallBytes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['x'] = value.x;
  return out;
}

/** testCallEmpty = Ok — Does nothing; for testing only. This is an offline method. Can be called before authorization */
export class TestCallEmpty {
  type: 'testCallEmpty' = 'testCallEmpty';
  extra?: string;
}

/** Decode a TDLib `testCallEmpty` object; null input -> null. */
export function decodeTestCallEmpty(json: TdJsonRaw): TestCallEmpty | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestCallEmpty = new TestCallEmpty();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `testCallEmpty` into TDLib JSON wire shape. */
export function encodeTestCallEmpty(value: TestCallEmpty): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testCallEmpty';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** testCallString = TestString — Returns the received string; for testing only. This is an offline method. Can be called before authorization */
export class TestCallString {
  type: 'testCallString' = 'testCallString';
  extra?: string;
  /** x: string — String to return */
  x: string = '';
}

/** Decode a TDLib `testCallString` object; null input -> null. */
export function decodeTestCallString(json: TdJsonRaw): TestCallString | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestCallString = new TestCallString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.x = asString(record['x']);
  return out;
}

/** Encode a `testCallString` into TDLib JSON wire shape. */
export function encodeTestCallString(value: TestCallString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testCallString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['x'] = value.x;
  return out;
}

/** testCallVectorInt = TestVectorInt — Returns the received vector of numbers; for testing only. This is an offline method. Can be called before authorization */
export class TestCallVectorInt {
  type: 'testCallVectorInt' = 'testCallVectorInt';
  extra?: string;
  /** x: vector<int32> — Vector of numbers to return */
  x: number[] = [];
}

/** Decode a TDLib `testCallVectorInt` object; null input -> null. */
export function decodeTestCallVectorInt(json: TdJsonRaw): TestCallVectorInt | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestCallVectorInt = new TestCallVectorInt();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.x = decodeVector(record['x'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `testCallVectorInt` into TDLib JSON wire shape. */
export function encodeTestCallVectorInt(value: TestCallVectorInt): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testCallVectorInt';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['x'] = encodeVector(value.x, (item: number): TdJsonRaw => item);
  return out;
}

/** testCallVectorIntObject = TestVectorIntObject — Returns the received vector of objects containing a number; for testing only. This is an offline method. Can be called before authorization */
export class TestCallVectorIntObject {
  type: 'testCallVectorIntObject' = 'testCallVectorIntObject';
  extra?: string;
  /** x: vector<testInt> — Vector of objects to return */
  x: TestInt[] = [];
}

/** Decode a TDLib `testCallVectorIntObject` object; null input -> null. */
export function decodeTestCallVectorIntObject(json: TdJsonRaw): TestCallVectorIntObject | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestCallVectorIntObject = new TestCallVectorIntObject();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.x = decodeVector(record['x'], (item: TdJsonRaw): TestInt => decodeTestInt(item) as TestInt);
  return out;
}

/** Encode a `testCallVectorIntObject` into TDLib JSON wire shape. */
export function encodeTestCallVectorIntObject(value: TestCallVectorIntObject): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testCallVectorIntObject';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['x'] = encodeVector(value.x, (item: TestInt | null): TdJsonRaw => item === null ? null : encodeTestInt(item));
  return out;
}

/** testCallVectorString = TestVectorString — Returns the received vector of strings; for testing only. This is an offline method. Can be called before authorization */
export class TestCallVectorString {
  type: 'testCallVectorString' = 'testCallVectorString';
  extra?: string;
  /** x: vector<string> — Vector of strings to return */
  x: string[] = [];
}

/** Decode a TDLib `testCallVectorString` object; null input -> null. */
export function decodeTestCallVectorString(json: TdJsonRaw): TestCallVectorString | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestCallVectorString = new TestCallVectorString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.x = decodeVector(record['x'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `testCallVectorString` into TDLib JSON wire shape. */
export function encodeTestCallVectorString(value: TestCallVectorString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testCallVectorString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['x'] = encodeVector(value.x, (item: string): TdJsonRaw => item);
  return out;
}

/** testCallVectorStringObject = TestVectorStringObject — Returns the received vector of objects containing a string; for testing only. This is an offline method. Can be called before authorization */
export class TestCallVectorStringObject {
  type: 'testCallVectorStringObject' = 'testCallVectorStringObject';
  extra?: string;
  /** x: vector<testString> — Vector of objects to return */
  x: TestString[] = [];
}

/** Decode a TDLib `testCallVectorStringObject` object; null input -> null. */
export function decodeTestCallVectorStringObject(json: TdJsonRaw): TestCallVectorStringObject | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestCallVectorStringObject = new TestCallVectorStringObject();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.x = decodeVector(record['x'], (item: TdJsonRaw): TestString => decodeTestString(item) as TestString);
  return out;
}

/** Encode a `testCallVectorStringObject` into TDLib JSON wire shape. */
export function encodeTestCallVectorStringObject(value: TestCallVectorStringObject): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testCallVectorStringObject';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['x'] = encodeVector(value.x, (item: TestString | null): TdJsonRaw => item === null ? null : encodeTestString(item));
  return out;
}

/** testGetDifference = Ok — Forces an updates.getDifference call to the Telegram servers; for testing only */
export class TestGetDifference {
  type: 'testGetDifference' = 'testGetDifference';
  extra?: string;
}

/** Decode a TDLib `testGetDifference` object; null input -> null. */
export function decodeTestGetDifference(json: TdJsonRaw): TestGetDifference | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestGetDifference = new TestGetDifference();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `testGetDifference` into TDLib JSON wire shape. */
export function encodeTestGetDifference(value: TestGetDifference): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testGetDifference';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** testInt = TestInt — A simple object containing a number; for testing only */
export class TestInt {
  type: 'testInt' = 'testInt';
  extra?: string;
  /** value: int32 — Number */
  value: number = 0;
}

/** Decode a TDLib `testInt` object; null input -> null. */
export function decodeTestInt(json: TdJsonRaw): TestInt | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestInt = new TestInt();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asNumber(record['value']);
  return out;
}

/** Encode a `testInt` into TDLib JSON wire shape. */
export function encodeTestInt(value: TestInt): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testInt';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}

/** testNetwork = Ok — Sends a simple network request to the Telegram servers; for testing only. Can be called before authorization */
export class TestNetwork {
  type: 'testNetwork' = 'testNetwork';
  extra?: string;
}

/** Decode a TDLib `testNetwork` object; null input -> null. */
export function decodeTestNetwork(json: TdJsonRaw): TestNetwork | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestNetwork = new TestNetwork();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `testNetwork` into TDLib JSON wire shape. */
export function encodeTestNetwork(value: TestNetwork): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testNetwork';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** testProxy = Ok — Sends a simple network request to the Telegram servers via proxy; for testing only. Can be called before authorization */
export class TestProxy {
  type: 'testProxy' = 'testProxy';
  extra?: string;
  /** proxy: proxy — The proxy to test */
  proxy: ProxyValue | null = null;
  /** dc_id: int32 — Identifier of a datacenter with which to test connection */
  dc_id: number = 0;
  /** timeout: double — The maximum overall timeout for the request */
  timeout: number = 0;
}

/** Decode a TDLib `testProxy` object; null input -> null. */
export function decodeTestProxy(json: TdJsonRaw): TestProxy | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestProxy = new TestProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.proxy = decodeProxyValue(record['proxy']);
  out.dc_id = asNumber(record['dc_id']);
  out.timeout = asNumber(record['timeout']);
  return out;
}

/** Encode a `testProxy` into TDLib JSON wire shape. */
export function encodeTestProxy(value: TestProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['proxy'] = value.proxy === null ? null : encodeProxyValue(value.proxy);
  out['dc_id'] = value.dc_id;
  out['timeout'] = value.timeout;
  return out;
}

/** testReturnError = Error — Returns the specified error and ensures that the Error object is used; for testing only. Can be called synchronously */
export class TestReturnError {
  type: 'testReturnError' = 'testReturnError';
  extra?: string;
  /** error: error — The error to be returned */
  error: ErrorValue | null = null;
}

/** Decode a TDLib `testReturnError` object; null input -> null. */
export function decodeTestReturnError(json: TdJsonRaw): TestReturnError | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestReturnError = new TestReturnError();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.error = decodeErrorValue(record['error']);
  return out;
}

/** Encode a `testReturnError` into TDLib JSON wire shape. */
export function encodeTestReturnError(value: TestReturnError): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testReturnError';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['error'] = value.error === null ? null : encodeErrorValue(value.error);
  return out;
}

/** testSquareInt = TestInt — Returns the squared received number; for testing only. This is an offline method. Can be called before authorization */
export class TestSquareInt {
  type: 'testSquareInt' = 'testSquareInt';
  extra?: string;
  /** x: int32 — Number to square */
  x: number = 0;
}

/** Decode a TDLib `testSquareInt` object; null input -> null. */
export function decodeTestSquareInt(json: TdJsonRaw): TestSquareInt | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestSquareInt = new TestSquareInt();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.x = asNumber(record['x']);
  return out;
}

/** Encode a `testSquareInt` into TDLib JSON wire shape. */
export function encodeTestSquareInt(value: TestSquareInt): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testSquareInt';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['x'] = value.x;
  return out;
}

/** testString = TestString — A simple object containing a string; for testing only */
export class TestString {
  type: 'testString' = 'testString';
  extra?: string;
  /** value: string — String */
  value: string = '';
}

/** Decode a TDLib `testString` object; null input -> null. */
export function decodeTestString(json: TdJsonRaw): TestString | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestString = new TestString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = asString(record['value']);
  return out;
}

/** Encode a `testString` into TDLib JSON wire shape. */
export function encodeTestString(value: TestString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = value.value;
  return out;
}

/** testUseUpdate = Update — Does nothing and ensures that the Update object is used; for testing only. This is an offline method. Can be called before authorization */
export class TestUseUpdate {
  type: 'testUseUpdate' = 'testUseUpdate';
  extra?: string;
}

/** Decode a TDLib `testUseUpdate` object; null input -> null. */
export function decodeTestUseUpdate(json: TdJsonRaw): TestUseUpdate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestUseUpdate = new TestUseUpdate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `testUseUpdate` into TDLib JSON wire shape. */
export function encodeTestUseUpdate(value: TestUseUpdate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testUseUpdate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** testVectorInt = TestVectorInt — A simple object containing a vector of numbers; for testing only */
export class TestVectorInt {
  type: 'testVectorInt' = 'testVectorInt';
  extra?: string;
  /** value: vector<int32> — Vector of numbers */
  value: number[] = [];
}

/** Decode a TDLib `testVectorInt` object; null input -> null. */
export function decodeTestVectorInt(json: TdJsonRaw): TestVectorInt | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestVectorInt = new TestVectorInt();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = decodeVector(record['value'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `testVectorInt` into TDLib JSON wire shape. */
export function encodeTestVectorInt(value: TestVectorInt): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testVectorInt';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = encodeVector(value.value, (item: number): TdJsonRaw => item);
  return out;
}

/** testVectorIntObject = TestVectorIntObject — A simple object containing a vector of objects that hold a number; for testing only */
export class TestVectorIntObject {
  type: 'testVectorIntObject' = 'testVectorIntObject';
  extra?: string;
  /** value: vector<testInt> — Vector of objects */
  value: TestInt[] = [];
}

/** Decode a TDLib `testVectorIntObject` object; null input -> null. */
export function decodeTestVectorIntObject(json: TdJsonRaw): TestVectorIntObject | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestVectorIntObject = new TestVectorIntObject();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = decodeVector(record['value'], (item: TdJsonRaw): TestInt => decodeTestInt(item) as TestInt);
  return out;
}

/** Encode a `testVectorIntObject` into TDLib JSON wire shape. */
export function encodeTestVectorIntObject(value: TestVectorIntObject): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testVectorIntObject';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = encodeVector(value.value, (item: TestInt | null): TdJsonRaw => item === null ? null : encodeTestInt(item));
  return out;
}

/** testVectorString = TestVectorString — A simple object containing a vector of strings; for testing only */
export class TestVectorString {
  type: 'testVectorString' = 'testVectorString';
  extra?: string;
  /** value: vector<string> — Vector of strings */
  value: string[] = [];
}

/** Decode a TDLib `testVectorString` object; null input -> null. */
export function decodeTestVectorString(json: TdJsonRaw): TestVectorString | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestVectorString = new TestVectorString();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = decodeVector(record['value'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `testVectorString` into TDLib JSON wire shape. */
export function encodeTestVectorString(value: TestVectorString): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testVectorString';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = encodeVector(value.value, (item: string): TdJsonRaw => item);
  return out;
}

/** testVectorStringObject = TestVectorStringObject — A simple object containing a vector of objects that hold a string; for testing only */
export class TestVectorStringObject {
  type: 'testVectorStringObject' = 'testVectorStringObject';
  extra?: string;
  /** value: vector<testString> — Vector of objects */
  value: TestString[] = [];
}

/** Decode a TDLib `testVectorStringObject` object; null input -> null. */
export function decodeTestVectorStringObject(json: TdJsonRaw): TestVectorStringObject | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TestVectorStringObject = new TestVectorStringObject();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.value = decodeVector(record['value'], (item: TdJsonRaw): TestString => decodeTestString(item) as TestString);
  return out;
}

/** Encode a `testVectorStringObject` into TDLib JSON wire shape. */
export function encodeTestVectorStringObject(value: TestVectorStringObject): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'testVectorStringObject';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['value'] = encodeVector(value.value, (item: TestString | null): TdJsonRaw => item === null ? null : encodeTestString(item));
  return out;
}

/** text = Text — Contains some text */
export class Text {
  type: 'text' = 'text';
  extra?: string;
  /** text: string — Text */
  text: string = '';
}

/** Decode a TDLib `text` object; null input -> null. */
export function decodeText(json: TdJsonRaw): Text | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Text = new Text();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  return out;
}

/** Encode a `text` into TDLib JSON wire shape. */
export function encodeText(value: Text): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'text';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  return out;
}

/** textCompositionStyle = TextCompositionStyle — Describes a style that can be used to compose a text */
export class TextCompositionStyle {
  type: 'textCompositionStyle' = 'textCompositionStyle';
  extra?: string;
  /** name: string — Name of the style */
  name: string = '';
  /** custom_emoji_id: int64 — Identifier of the custom emoji corresponding to the style; 0 if none */
  custom_emoji_id: string = '';
  /** title: string — Title of the style in the user application's language */
  title: string = '';
  /** is_custom: Bool — True, if the style is created by a user */
  is_custom: boolean = false;
  /** is_creator: Bool — True, if the user is creator of the style */
  is_creator: boolean = false;
  /** install_count: int32 — Number of users that installed the style; for created custom styles only; 0 if unknown */
  install_count: number = 0;
  /** prompt: string — Prompt of the style; for created custom styles only */
  prompt: string = '';
  /** creator_user_id: int53 — User identifier of the creator of the style; 0 if none or unknown */
  creator_user_id: number = 0;
  /** english_example: textCompositionStyleExample — Example of the style usage in English; may be null if unknown */
  english_example: TextCompositionStyleExample | null = null;
}

/** Decode a TDLib `textCompositionStyle` object; null input -> null. */
export function decodeTextCompositionStyle(json: TdJsonRaw): TextCompositionStyle | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextCompositionStyle = new TextCompositionStyle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  out.title = asString(record['title']);
  out.is_custom = asBool(record['is_custom']);
  out.is_creator = asBool(record['is_creator']);
  out.install_count = asNumber(record['install_count']);
  out.prompt = asString(record['prompt']);
  out.creator_user_id = asNumber(record['creator_user_id']);
  out.english_example = decodeTextCompositionStyleExample(record['english_example']);
  return out;
}

/** Encode a `textCompositionStyle` into TDLib JSON wire shape. */
export function encodeTextCompositionStyle(value: TextCompositionStyle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textCompositionStyle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['custom_emoji_id'] = value.custom_emoji_id;
  out['title'] = value.title;
  out['is_custom'] = value.is_custom;
  out['is_creator'] = value.is_creator;
  out['install_count'] = value.install_count;
  out['prompt'] = value.prompt;
  out['creator_user_id'] = value.creator_user_id;
  out['english_example'] = value.english_example === null ? null : encodeTextCompositionStyleExample(value.english_example);
  return out;
}

/** textCompositionStyleExample = TextCompositionStyleExample — Contains an example of text composition style usage */
export class TextCompositionStyleExample {
  type: 'textCompositionStyleExample' = 'textCompositionStyleExample';
  extra?: string;
  /** source_text: formattedText — Source text */
  source_text: FormattedText | null = null;
  /** result_text: formattedText — The text after the style was applied to the source text */
  result_text: FormattedText | null = null;
}

/** Decode a TDLib `textCompositionStyleExample` object; null input -> null. */
export function decodeTextCompositionStyleExample(json: TdJsonRaw): TextCompositionStyleExample | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextCompositionStyleExample = new TextCompositionStyleExample();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.source_text = decodeFormattedText(record['source_text']);
  out.result_text = decodeFormattedText(record['result_text']);
  return out;
}

/** Encode a `textCompositionStyleExample` into TDLib JSON wire shape. */
export function encodeTextCompositionStyleExample(value: TextCompositionStyleExample): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textCompositionStyleExample';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['source_text'] = value.source_text === null ? null : encodeFormattedText(value.source_text);
  out['result_text'] = value.result_text === null ? null : encodeFormattedText(value.result_text);
  return out;
}

/** textEntities = TextEntities — Contains a list of text entities */
export class TextEntities {
  type: 'textEntities' = 'textEntities';
  extra?: string;
  /** entities: vector<textEntity> — List of text entities */
  entities: TextEntity[] = [];
}

/** Decode a TDLib `textEntities` object; null input -> null. */
export function decodeTextEntities(json: TdJsonRaw): TextEntities | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntities = new TextEntities();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.entities = decodeVector(record['entities'], (item: TdJsonRaw): TextEntity => decodeTextEntity(item) as TextEntity);
  return out;
}

/** Encode a `textEntities` into TDLib JSON wire shape. */
export function encodeTextEntities(value: TextEntities): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntities';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['entities'] = encodeVector(value.entities, (item: TextEntity | null): TdJsonRaw => item === null ? null : encodeTextEntity(item));
  return out;
}

/** textEntity = TextEntity — Represents a part of the text that needs to be formatted in some unusual way */
export class TextEntity {
  type: 'textEntity' = 'textEntity';
  extra?: string;
  /** offset: int32 — Offset of the entity, in UTF-16 code units */
  offset: number = 0;
  /** length: int32 — Length of the entity, in UTF-16 code units */
  length: number = 0;
  /** type: TextEntityType — Type of the entity */
  type_: TdTextEntityType | null = null;
}

/** Decode a TDLib `textEntity` object; null input -> null. */
export function decodeTextEntity(json: TdJsonRaw): TextEntity | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntity = new TextEntity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.offset = asNumber(record['offset']);
  out.length = asNumber(record['length']);
  out.type_ = decodeTdTextEntityType(record['type']);
  return out;
}

/** Encode a `textEntity` into TDLib JSON wire shape. */
export function encodeTextEntity(value: TextEntity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['offset'] = value.offset;
  out['length'] = value.length;
  out['type'] = value.type_ === null ? null : encodeTdTextEntityType(value.type_);
  return out;
}

/** textEntityTypeBankCardNumber = TextEntityType — A bank card number. The getBankCardInfo method can be used to get information about the bank card */
export class TextEntityTypeBankCardNumber {
  type: 'textEntityTypeBankCardNumber' = 'textEntityTypeBankCardNumber';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeBankCardNumber` object; null input -> null. */
export function decodeTextEntityTypeBankCardNumber(json: TdJsonRaw): TextEntityTypeBankCardNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeBankCardNumber = new TextEntityTypeBankCardNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeBankCardNumber` into TDLib JSON wire shape. */
export function encodeTextEntityTypeBankCardNumber(value: TextEntityTypeBankCardNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeBankCardNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeBlockQuote = TextEntityType — Text that must be formatted as if inside a blockquote HTML tag; not supported in secret chats */
export class TextEntityTypeBlockQuote {
  type: 'textEntityTypeBlockQuote' = 'textEntityTypeBlockQuote';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeBlockQuote` object; null input -> null. */
export function decodeTextEntityTypeBlockQuote(json: TdJsonRaw): TextEntityTypeBlockQuote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeBlockQuote = new TextEntityTypeBlockQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeBlockQuote` into TDLib JSON wire shape. */
export function encodeTextEntityTypeBlockQuote(value: TextEntityTypeBlockQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeBlockQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeBold = TextEntityType — A bold text */
export class TextEntityTypeBold {
  type: 'textEntityTypeBold' = 'textEntityTypeBold';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeBold` object; null input -> null. */
export function decodeTextEntityTypeBold(json: TdJsonRaw): TextEntityTypeBold | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeBold = new TextEntityTypeBold();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeBold` into TDLib JSON wire shape. */
export function encodeTextEntityTypeBold(value: TextEntityTypeBold): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeBold';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeBotCommand = TextEntityType — A bot command, beginning with "/" */
export class TextEntityTypeBotCommand {
  type: 'textEntityTypeBotCommand' = 'textEntityTypeBotCommand';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeBotCommand` object; null input -> null. */
export function decodeTextEntityTypeBotCommand(json: TdJsonRaw): TextEntityTypeBotCommand | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeBotCommand = new TextEntityTypeBotCommand();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeBotCommand` into TDLib JSON wire shape. */
export function encodeTextEntityTypeBotCommand(value: TextEntityTypeBotCommand): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeBotCommand';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeCashtag = TextEntityType — A cashtag text, beginning with "$", consisting of capital English letters (e.g., "$USD"), and optionally containing a chat username at the end */
export class TextEntityTypeCashtag {
  type: 'textEntityTypeCashtag' = 'textEntityTypeCashtag';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeCashtag` object; null input -> null. */
export function decodeTextEntityTypeCashtag(json: TdJsonRaw): TextEntityTypeCashtag | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeCashtag = new TextEntityTypeCashtag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeCashtag` into TDLib JSON wire shape. */
export function encodeTextEntityTypeCashtag(value: TextEntityTypeCashtag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeCashtag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeCode = TextEntityType — Text that must be formatted as if inside a code HTML tag */
export class TextEntityTypeCode {
  type: 'textEntityTypeCode' = 'textEntityTypeCode';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeCode` object; null input -> null. */
export function decodeTextEntityTypeCode(json: TdJsonRaw): TextEntityTypeCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeCode = new TextEntityTypeCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeCode` into TDLib JSON wire shape. */
export function encodeTextEntityTypeCode(value: TextEntityTypeCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeCustomEmoji = TextEntityType — A custom emoji. The text behind a custom emoji must be an emoji. Only premium users can use premium custom emoji */
export class TextEntityTypeCustomEmoji {
  type: 'textEntityTypeCustomEmoji' = 'textEntityTypeCustomEmoji';
  extra?: string;
  /** custom_emoji_id: int64 — Unique identifier of the custom emoji */
  custom_emoji_id: string = '';
}

/** Decode a TDLib `textEntityTypeCustomEmoji` object; null input -> null. */
export function decodeTextEntityTypeCustomEmoji(json: TdJsonRaw): TextEntityTypeCustomEmoji | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeCustomEmoji = new TextEntityTypeCustomEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  return out;
}

/** Encode a `textEntityTypeCustomEmoji` into TDLib JSON wire shape. */
export function encodeTextEntityTypeCustomEmoji(value: TextEntityTypeCustomEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeCustomEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_emoji_id'] = value.custom_emoji_id;
  return out;
}

/** textEntityTypeDateTime = TextEntityType — A date and time */
export class TextEntityTypeDateTime {
  type: 'textEntityTypeDateTime' = 'textEntityTypeDateTime';
  extra?: string;
  /** unix_time: int32 — Point in time (Unix timestamp) representing the date and time */
  unix_time: number = 0;
  /** formatting_type: DateTimeFormattingType — Date and time formatting type; may be null if none and the original text must not be changed */
  formatting_type: TdDateTimeFormattingType | null = null;
}

/** Decode a TDLib `textEntityTypeDateTime` object; null input -> null. */
export function decodeTextEntityTypeDateTime(json: TdJsonRaw): TextEntityTypeDateTime | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeDateTime = new TextEntityTypeDateTime();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.unix_time = asNumber(record['unix_time']);
  out.formatting_type = decodeTdDateTimeFormattingType(record['formatting_type']);
  return out;
}

/** Encode a `textEntityTypeDateTime` into TDLib JSON wire shape. */
export function encodeTextEntityTypeDateTime(value: TextEntityTypeDateTime): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeDateTime';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['unix_time'] = value.unix_time;
  out['formatting_type'] = value.formatting_type === null ? null : encodeTdDateTimeFormattingType(value.formatting_type);
  return out;
}

/** textEntityTypeEmailAddress = TextEntityType — An email address */
export class TextEntityTypeEmailAddress {
  type: 'textEntityTypeEmailAddress' = 'textEntityTypeEmailAddress';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeEmailAddress` object; null input -> null. */
export function decodeTextEntityTypeEmailAddress(json: TdJsonRaw): TextEntityTypeEmailAddress | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeEmailAddress = new TextEntityTypeEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeEmailAddress` into TDLib JSON wire shape. */
export function encodeTextEntityTypeEmailAddress(value: TextEntityTypeEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeExpandableBlockQuote = TextEntityType — Text that must be formatted as if inside a blockquote HTML tag and collapsed by default to 3 lines with the ability to show full text; not supported in secret chats */
export class TextEntityTypeExpandableBlockQuote {
  type: 'textEntityTypeExpandableBlockQuote' = 'textEntityTypeExpandableBlockQuote';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeExpandableBlockQuote` object; null input -> null. */
export function decodeTextEntityTypeExpandableBlockQuote(json: TdJsonRaw): TextEntityTypeExpandableBlockQuote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeExpandableBlockQuote = new TextEntityTypeExpandableBlockQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeExpandableBlockQuote` into TDLib JSON wire shape. */
export function encodeTextEntityTypeExpandableBlockQuote(value: TextEntityTypeExpandableBlockQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeExpandableBlockQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeHashtag = TextEntityType — A hashtag text, beginning with "#" and optionally containing a chat username at the end */
export class TextEntityTypeHashtag {
  type: 'textEntityTypeHashtag' = 'textEntityTypeHashtag';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeHashtag` object; null input -> null. */
export function decodeTextEntityTypeHashtag(json: TdJsonRaw): TextEntityTypeHashtag | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeHashtag = new TextEntityTypeHashtag();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeHashtag` into TDLib JSON wire shape. */
export function encodeTextEntityTypeHashtag(value: TextEntityTypeHashtag): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeHashtag';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeItalic = TextEntityType — An italic text */
export class TextEntityTypeItalic {
  type: 'textEntityTypeItalic' = 'textEntityTypeItalic';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeItalic` object; null input -> null. */
export function decodeTextEntityTypeItalic(json: TdJsonRaw): TextEntityTypeItalic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeItalic = new TextEntityTypeItalic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeItalic` into TDLib JSON wire shape. */
export function encodeTextEntityTypeItalic(value: TextEntityTypeItalic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeItalic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeMediaTimestamp = TextEntityType — A media timestamp */
export class TextEntityTypeMediaTimestamp {
  type: 'textEntityTypeMediaTimestamp' = 'textEntityTypeMediaTimestamp';
  extra?: string;
  /** media_timestamp: int32 — Timestamp from which a video/audio/video note/voice note/story playing must start, in seconds. The media can be in the content or the link preview of the current message, or in the same places in the replied message */
  media_timestamp: number = 0;
}

/** Decode a TDLib `textEntityTypeMediaTimestamp` object; null input -> null. */
export function decodeTextEntityTypeMediaTimestamp(json: TdJsonRaw): TextEntityTypeMediaTimestamp | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeMediaTimestamp = new TextEntityTypeMediaTimestamp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.media_timestamp = asNumber(record['media_timestamp']);
  return out;
}

/** Encode a `textEntityTypeMediaTimestamp` into TDLib JSON wire shape. */
export function encodeTextEntityTypeMediaTimestamp(value: TextEntityTypeMediaTimestamp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeMediaTimestamp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['media_timestamp'] = value.media_timestamp;
  return out;
}

/** textEntityTypeMention = TextEntityType — A mention of a user, a supergroup, or a channel by their username */
export class TextEntityTypeMention {
  type: 'textEntityTypeMention' = 'textEntityTypeMention';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeMention` object; null input -> null. */
export function decodeTextEntityTypeMention(json: TdJsonRaw): TextEntityTypeMention | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeMention = new TextEntityTypeMention();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeMention` into TDLib JSON wire shape. */
export function encodeTextEntityTypeMention(value: TextEntityTypeMention): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeMention';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeMentionName = TextEntityType — A text shows instead of a raw mention of the user (e.g., when the user has no username) */
export class TextEntityTypeMentionName {
  type: 'textEntityTypeMentionName' = 'textEntityTypeMentionName';
  extra?: string;
  /** user_id: int53 — Identifier of the mentioned user */
  user_id: number = 0;
}

/** Decode a TDLib `textEntityTypeMentionName` object; null input -> null. */
export function decodeTextEntityTypeMentionName(json: TdJsonRaw): TextEntityTypeMentionName | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeMentionName = new TextEntityTypeMentionName();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `textEntityTypeMentionName` into TDLib JSON wire shape. */
export function encodeTextEntityTypeMentionName(value: TextEntityTypeMentionName): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeMentionName';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** textEntityTypePhoneNumber = TextEntityType — A phone number */
export class TextEntityTypePhoneNumber {
  type: 'textEntityTypePhoneNumber' = 'textEntityTypePhoneNumber';
  extra?: string;
}

/** Decode a TDLib `textEntityTypePhoneNumber` object; null input -> null. */
export function decodeTextEntityTypePhoneNumber(json: TdJsonRaw): TextEntityTypePhoneNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypePhoneNumber = new TextEntityTypePhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypePhoneNumber` into TDLib JSON wire shape. */
export function encodeTextEntityTypePhoneNumber(value: TextEntityTypePhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypePhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypePre = TextEntityType — Text that must be formatted as if inside a pre HTML tag */
export class TextEntityTypePre {
  type: 'textEntityTypePre' = 'textEntityTypePre';
  extra?: string;
}

/** Decode a TDLib `textEntityTypePre` object; null input -> null. */
export function decodeTextEntityTypePre(json: TdJsonRaw): TextEntityTypePre | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypePre = new TextEntityTypePre();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypePre` into TDLib JSON wire shape. */
export function encodeTextEntityTypePre(value: TextEntityTypePre): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypePre';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypePreCode = TextEntityType — Text that must be formatted as if inside pre, and code HTML tags */
export class TextEntityTypePreCode {
  type: 'textEntityTypePreCode' = 'textEntityTypePreCode';
  extra?: string;
  /** language: string — Programming language of the code; as defined by the sender */
  language: string = '';
}

/** Decode a TDLib `textEntityTypePreCode` object; null input -> null. */
export function decodeTextEntityTypePreCode(json: TdJsonRaw): TextEntityTypePreCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypePreCode = new TextEntityTypePreCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language = asString(record['language']);
  return out;
}

/** Encode a `textEntityTypePreCode` into TDLib JSON wire shape. */
export function encodeTextEntityTypePreCode(value: TextEntityTypePreCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypePreCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language'] = value.language;
  return out;
}

/** textEntityTypeSpoiler = TextEntityType — A spoiler text */
export class TextEntityTypeSpoiler {
  type: 'textEntityTypeSpoiler' = 'textEntityTypeSpoiler';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeSpoiler` object; null input -> null. */
export function decodeTextEntityTypeSpoiler(json: TdJsonRaw): TextEntityTypeSpoiler | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeSpoiler = new TextEntityTypeSpoiler();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeSpoiler` into TDLib JSON wire shape. */
export function encodeTextEntityTypeSpoiler(value: TextEntityTypeSpoiler): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeSpoiler';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeStrikethrough = TextEntityType — A strikethrough text */
export class TextEntityTypeStrikethrough {
  type: 'textEntityTypeStrikethrough' = 'textEntityTypeStrikethrough';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeStrikethrough` object; null input -> null. */
export function decodeTextEntityTypeStrikethrough(json: TdJsonRaw): TextEntityTypeStrikethrough | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeStrikethrough = new TextEntityTypeStrikethrough();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeStrikethrough` into TDLib JSON wire shape. */
export function encodeTextEntityTypeStrikethrough(value: TextEntityTypeStrikethrough): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeStrikethrough';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeTextUrl = TextEntityType — A text description shown instead of a raw URL */
export class TextEntityTypeTextUrl {
  type: 'textEntityTypeTextUrl' = 'textEntityTypeTextUrl';
  extra?: string;
  /** url: string — HTTP or tg:// URL to be opened when the link is clicked */
  url: string = '';
}

/** Decode a TDLib `textEntityTypeTextUrl` object; null input -> null. */
export function decodeTextEntityTypeTextUrl(json: TdJsonRaw): TextEntityTypeTextUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeTextUrl = new TextEntityTypeTextUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `textEntityTypeTextUrl` into TDLib JSON wire shape. */
export function encodeTextEntityTypeTextUrl(value: TextEntityTypeTextUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeTextUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** textEntityTypeUnderline = TextEntityType — An underlined text */
export class TextEntityTypeUnderline {
  type: 'textEntityTypeUnderline' = 'textEntityTypeUnderline';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeUnderline` object; null input -> null. */
export function decodeTextEntityTypeUnderline(json: TdJsonRaw): TextEntityTypeUnderline | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeUnderline = new TextEntityTypeUnderline();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeUnderline` into TDLib JSON wire shape. */
export function encodeTextEntityTypeUnderline(value: TextEntityTypeUnderline): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeUnderline';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textEntityTypeUrl = TextEntityType — An HTTP URL */
export class TextEntityTypeUrl {
  type: 'textEntityTypeUrl' = 'textEntityTypeUrl';
  extra?: string;
}

/** Decode a TDLib `textEntityTypeUrl` object; null input -> null. */
export function decodeTextEntityTypeUrl(json: TdJsonRaw): TextEntityTypeUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextEntityTypeUrl = new TextEntityTypeUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textEntityTypeUrl` into TDLib JSON wire shape. */
export function encodeTextEntityTypeUrl(value: TextEntityTypeUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textEntityTypeUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textParseModeHTML = TextParseMode — The text uses HTML-style formatting. The same as Telegram Bot API "HTML" parse mode */
export class TextParseModeHTML {
  type: 'textParseModeHTML' = 'textParseModeHTML';
  extra?: string;
}

/** Decode a TDLib `textParseModeHTML` object; null input -> null. */
export function decodeTextParseModeHTML(json: TdJsonRaw): TextParseModeHTML | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextParseModeHTML = new TextParseModeHTML();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `textParseModeHTML` into TDLib JSON wire shape. */
export function encodeTextParseModeHTML(value: TextParseModeHTML): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textParseModeHTML';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** textParseModeMarkdown = TextParseMode — The text uses Markdown-style formatting */
export class TextParseModeMarkdown {
  type: 'textParseModeMarkdown' = 'textParseModeMarkdown';
  extra?: string;
  /** version: int32 — Version of the parser: 0 or 1 - Telegram Bot API "Markdown" parse mode, 2 - Telegram Bot API "MarkdownV2" parse mode */
  version: number = 0;
}

/** Decode a TDLib `textParseModeMarkdown` object; null input -> null. */
export function decodeTextParseModeMarkdown(json: TdJsonRaw): TextParseModeMarkdown | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextParseModeMarkdown = new TextParseModeMarkdown();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.version = asNumber(record['version']);
  return out;
}

/** Encode a `textParseModeMarkdown` into TDLib JSON wire shape. */
export function encodeTextParseModeMarkdown(value: TextParseModeMarkdown): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textParseModeMarkdown';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['version'] = value.version;
  return out;
}

/** textQuote = TextQuote — Describes manually or automatically chosen quote from another message */
export class TextQuote {
  type: 'textQuote' = 'textQuote';
  extra?: string;
  /** text: formattedText — Text of the quote. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities can be present in the text */
  text: FormattedText | null = null;
  /** position: int32 — Approximate quote position in the original message in UTF-16 code units as specified by the message sender */
  position: number = 0;
  /** is_manual: Bool — True, if the quote was manually chosen by the message sender */
  is_manual: boolean = false;
}

/** Decode a TDLib `textQuote` object; null input -> null. */
export function decodeTextQuote(json: TdJsonRaw): TextQuote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TextQuote = new TextQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.position = asNumber(record['position']);
  out.is_manual = asBool(record['is_manual']);
  return out;
}

/** Encode a `textQuote` into TDLib JSON wire shape. */
export function encodeTextQuote(value: TextQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'textQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['position'] = value.position;
  out['is_manual'] = value.is_manual;
  return out;
}

/** themeParameters = ThemeParameters — Contains parameters of the application theme */
export class ThemeParameters {
  type: 'themeParameters' = 'themeParameters';
  extra?: string;
  /** background_color: int32 — A color of the background in the RGB format */
  background_color: number = 0;
  /** secondary_background_color: int32 — A secondary color for the background in the RGB format */
  secondary_background_color: number = 0;
  /** header_background_color: int32 — A color of the header background in the RGB format */
  header_background_color: number = 0;
  /** bottom_bar_background_color: int32 — A color of the bottom bar background in the RGB format */
  bottom_bar_background_color: number = 0;
  /** section_background_color: int32 — A color of the section background in the RGB format */
  section_background_color: number = 0;
  /** section_separator_color: int32 — A color of the section separator in the RGB format */
  section_separator_color: number = 0;
  /** text_color: int32 — A color of text in the RGB format */
  text_color: number = 0;
  /** accent_text_color: int32 — An accent color of the text in the RGB format */
  accent_text_color: number = 0;
  /** section_header_text_color: int32 — A color of text on the section headers in the RGB format */
  section_header_text_color: number = 0;
  /** subtitle_text_color: int32 — A color of the subtitle text in the RGB format */
  subtitle_text_color: number = 0;
  /** destructive_text_color: int32 — A color of the text for destructive actions in the RGB format */
  destructive_text_color: number = 0;
  /** hint_color: int32 — A color of hints in the RGB format */
  hint_color: number = 0;
  /** link_color: int32 — A color of links in the RGB format */
  link_color: number = 0;
  /** button_color: int32 — A color of the buttons in the RGB format */
  button_color: number = 0;
  /** button_text_color: int32 — A color of text on the buttons in the RGB format */
  button_text_color: number = 0;
}

/** Decode a TDLib `themeParameters` object; null input -> null. */
export function decodeThemeParameters(json: TdJsonRaw): ThemeParameters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ThemeParameters = new ThemeParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.background_color = asNumber(record['background_color']);
  out.secondary_background_color = asNumber(record['secondary_background_color']);
  out.header_background_color = asNumber(record['header_background_color']);
  out.bottom_bar_background_color = asNumber(record['bottom_bar_background_color']);
  out.section_background_color = asNumber(record['section_background_color']);
  out.section_separator_color = asNumber(record['section_separator_color']);
  out.text_color = asNumber(record['text_color']);
  out.accent_text_color = asNumber(record['accent_text_color']);
  out.section_header_text_color = asNumber(record['section_header_text_color']);
  out.subtitle_text_color = asNumber(record['subtitle_text_color']);
  out.destructive_text_color = asNumber(record['destructive_text_color']);
  out.hint_color = asNumber(record['hint_color']);
  out.link_color = asNumber(record['link_color']);
  out.button_color = asNumber(record['button_color']);
  out.button_text_color = asNumber(record['button_text_color']);
  return out;
}

/** Encode a `themeParameters` into TDLib JSON wire shape. */
export function encodeThemeParameters(value: ThemeParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'themeParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['background_color'] = value.background_color;
  out['secondary_background_color'] = value.secondary_background_color;
  out['header_background_color'] = value.header_background_color;
  out['bottom_bar_background_color'] = value.bottom_bar_background_color;
  out['section_background_color'] = value.section_background_color;
  out['section_separator_color'] = value.section_separator_color;
  out['text_color'] = value.text_color;
  out['accent_text_color'] = value.accent_text_color;
  out['section_header_text_color'] = value.section_header_text_color;
  out['subtitle_text_color'] = value.subtitle_text_color;
  out['destructive_text_color'] = value.destructive_text_color;
  out['hint_color'] = value.hint_color;
  out['link_color'] = value.link_color;
  out['button_color'] = value.button_color;
  out['button_text_color'] = value.button_text_color;
  return out;
}

/** themeSettings = ThemeSettings — Describes theme settings */
export class ThemeSettings {
  type: 'themeSettings' = 'themeSettings';
  extra?: string;
  /** base_theme: BuiltInTheme — Base theme for this theme */
  base_theme: TdBuiltInTheme | null = null;
  /** accent_color: int32 — Theme accent color in ARGB format */
  accent_color: number = 0;
  /** background: background — The background to be used in chats; may be null */
  background: Background | null = null;
  /** outgoing_message_fill: BackgroundFill — The fill to be used as a background for outgoing messages; may be null if the fill from the base theme must be used instead */
  outgoing_message_fill: TdBackgroundFill | null = null;
  /** animate_outgoing_message_fill: Bool — If true, the freeform gradient fill needs to be animated on every sent message */
  animate_outgoing_message_fill: boolean = false;
  /** outgoing_message_accent_color: int32 — Accent color of outgoing messages in ARGB format */
  outgoing_message_accent_color: number = 0;
}

/** Decode a TDLib `themeSettings` object; null input -> null. */
export function decodeThemeSettings(json: TdJsonRaw): ThemeSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ThemeSettings = new ThemeSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.base_theme = decodeTdBuiltInTheme(record['base_theme']);
  out.accent_color = asNumber(record['accent_color']);
  out.background = decodeBackground(record['background']);
  out.outgoing_message_fill = decodeTdBackgroundFill(record['outgoing_message_fill']);
  out.animate_outgoing_message_fill = asBool(record['animate_outgoing_message_fill']);
  out.outgoing_message_accent_color = asNumber(record['outgoing_message_accent_color']);
  return out;
}

/** Encode a `themeSettings` into TDLib JSON wire shape. */
export function encodeThemeSettings(value: ThemeSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'themeSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['base_theme'] = value.base_theme === null ? null : encodeTdBuiltInTheme(value.base_theme);
  out['accent_color'] = value.accent_color;
  out['background'] = value.background === null ? null : encodeBackground(value.background);
  out['outgoing_message_fill'] = value.outgoing_message_fill === null ? null : encodeTdBackgroundFill(value.outgoing_message_fill);
  out['animate_outgoing_message_fill'] = value.animate_outgoing_message_fill;
  out['outgoing_message_accent_color'] = value.outgoing_message_accent_color;
  return out;
}

/** thumbnail = Thumbnail — Represents a thumbnail */
export class Thumbnail {
  type: 'thumbnail' = 'thumbnail';
  extra?: string;
  /** format: ThumbnailFormat — Thumbnail format */
  format: TdThumbnailFormat | null = null;
  /** width: int32 — Thumbnail width */
  width: number = 0;
  /** height: int32 — Thumbnail height */
  height: number = 0;
  /** file: file — The thumbnail */
  file: File | null = null;
}

/** Decode a TDLib `thumbnail` object; null input -> null. */
export function decodeThumbnail(json: TdJsonRaw): Thumbnail | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Thumbnail = new Thumbnail();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.format = decodeTdThumbnailFormat(record['format']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.file = decodeFile(record['file']);
  return out;
}

/** Encode a `thumbnail` into TDLib JSON wire shape. */
export function encodeThumbnail(value: Thumbnail): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'thumbnail';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['format'] = value.format === null ? null : encodeTdThumbnailFormat(value.format);
  out['width'] = value.width;
  out['height'] = value.height;
  out['file'] = value.file === null ? null : encodeFile(value.file);
  return out;
}

/** thumbnailFormatGif = ThumbnailFormat — The thumbnail is in static GIF format. It will be used only for some bot inline query results */
export class ThumbnailFormatGif {
  type: 'thumbnailFormatGif' = 'thumbnailFormatGif';
  extra?: string;
}

/** Decode a TDLib `thumbnailFormatGif` object; null input -> null. */
export function decodeThumbnailFormatGif(json: TdJsonRaw): ThumbnailFormatGif | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ThumbnailFormatGif = new ThumbnailFormatGif();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `thumbnailFormatGif` into TDLib JSON wire shape. */
export function encodeThumbnailFormatGif(value: ThumbnailFormatGif): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'thumbnailFormatGif';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** thumbnailFormatJpeg = ThumbnailFormat — The thumbnail is in JPEG format */
export class ThumbnailFormatJpeg {
  type: 'thumbnailFormatJpeg' = 'thumbnailFormatJpeg';
  extra?: string;
}

/** Decode a TDLib `thumbnailFormatJpeg` object; null input -> null. */
export function decodeThumbnailFormatJpeg(json: TdJsonRaw): ThumbnailFormatJpeg | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ThumbnailFormatJpeg = new ThumbnailFormatJpeg();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `thumbnailFormatJpeg` into TDLib JSON wire shape. */
export function encodeThumbnailFormatJpeg(value: ThumbnailFormatJpeg): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'thumbnailFormatJpeg';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** thumbnailFormatMpeg4 = ThumbnailFormat — The thumbnail is in MPEG4 format. It will be used only for some animations and videos */
export class ThumbnailFormatMpeg4 {
  type: 'thumbnailFormatMpeg4' = 'thumbnailFormatMpeg4';
  extra?: string;
}

/** Decode a TDLib `thumbnailFormatMpeg4` object; null input -> null. */
export function decodeThumbnailFormatMpeg4(json: TdJsonRaw): ThumbnailFormatMpeg4 | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ThumbnailFormatMpeg4 = new ThumbnailFormatMpeg4();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `thumbnailFormatMpeg4` into TDLib JSON wire shape. */
export function encodeThumbnailFormatMpeg4(value: ThumbnailFormatMpeg4): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'thumbnailFormatMpeg4';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** thumbnailFormatPng = ThumbnailFormat — The thumbnail is in PNG format. It will be used only for background patterns */
export class ThumbnailFormatPng {
  type: 'thumbnailFormatPng' = 'thumbnailFormatPng';
  extra?: string;
}

/** Decode a TDLib `thumbnailFormatPng` object; null input -> null. */
export function decodeThumbnailFormatPng(json: TdJsonRaw): ThumbnailFormatPng | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ThumbnailFormatPng = new ThumbnailFormatPng();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `thumbnailFormatPng` into TDLib JSON wire shape. */
export function encodeThumbnailFormatPng(value: ThumbnailFormatPng): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'thumbnailFormatPng';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** thumbnailFormatTgs = ThumbnailFormat — The thumbnail is in TGS format. It will be used only for sticker sets */
export class ThumbnailFormatTgs {
  type: 'thumbnailFormatTgs' = 'thumbnailFormatTgs';
  extra?: string;
}

/** Decode a TDLib `thumbnailFormatTgs` object; null input -> null. */
export function decodeThumbnailFormatTgs(json: TdJsonRaw): ThumbnailFormatTgs | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ThumbnailFormatTgs = new ThumbnailFormatTgs();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `thumbnailFormatTgs` into TDLib JSON wire shape. */
export function encodeThumbnailFormatTgs(value: ThumbnailFormatTgs): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'thumbnailFormatTgs';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** thumbnailFormatWebm = ThumbnailFormat — The thumbnail is in WEBM format. It will be used only for sticker sets */
export class ThumbnailFormatWebm {
  type: 'thumbnailFormatWebm' = 'thumbnailFormatWebm';
  extra?: string;
}

/** Decode a TDLib `thumbnailFormatWebm` object; null input -> null. */
export function decodeThumbnailFormatWebm(json: TdJsonRaw): ThumbnailFormatWebm | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ThumbnailFormatWebm = new ThumbnailFormatWebm();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `thumbnailFormatWebm` into TDLib JSON wire shape. */
export function encodeThumbnailFormatWebm(value: ThumbnailFormatWebm): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'thumbnailFormatWebm';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** thumbnailFormatWebp = ThumbnailFormat — The thumbnail is in WEBP format. It will be used only for some stickers and sticker sets */
export class ThumbnailFormatWebp {
  type: 'thumbnailFormatWebp' = 'thumbnailFormatWebp';
  extra?: string;
}

/** Decode a TDLib `thumbnailFormatWebp` object; null input -> null. */
export function decodeThumbnailFormatWebp(json: TdJsonRaw): ThumbnailFormatWebp | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ThumbnailFormatWebp = new ThumbnailFormatWebp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `thumbnailFormatWebp` into TDLib JSON wire shape. */
export function encodeThumbnailFormatWebp(value: ThumbnailFormatWebp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'thumbnailFormatWebp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** timeZone = TimeZone — Describes a time zone */
export class TimeZone {
  type: 'timeZone' = 'timeZone';
  extra?: string;
  /** id: string — Unique time zone identifier */
  id: string = '';
  /** name: string — Time zone name */
  name: string = '';
  /** utc_time_offset: int32 — Current UTC time offset for the time zone */
  utc_time_offset: number = 0;
}

/** Decode a TDLib `timeZone` object; null input -> null. */
export function decodeTimeZone(json: TdJsonRaw): TimeZone | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TimeZone = new TimeZone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.name = asString(record['name']);
  out.utc_time_offset = asNumber(record['utc_time_offset']);
  return out;
}

/** Encode a `timeZone` into TDLib JSON wire shape. */
export function encodeTimeZone(value: TimeZone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'timeZone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['name'] = value.name;
  out['utc_time_offset'] = value.utc_time_offset;
  return out;
}

/** timeZones = TimeZones — Contains a list of time zones */
export class TimeZones {
  type: 'timeZones' = 'timeZones';
  extra?: string;
  /** time_zones: vector<timeZone> — A list of time zones */
  time_zones: TimeZone[] = [];
}

/** Decode a TDLib `timeZones` object; null input -> null. */
export function decodeTimeZones(json: TdJsonRaw): TimeZones | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TimeZones = new TimeZones();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.time_zones = decodeVector(record['time_zones'], (item: TdJsonRaw): TimeZone => decodeTimeZone(item) as TimeZone);
  return out;
}

/** Encode a `timeZones` into TDLib JSON wire shape. */
export function encodeTimeZones(value: TimeZones): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'timeZones';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['time_zones'] = encodeVector(value.time_zones, (item: TimeZone | null): TdJsonRaw => item === null ? null : encodeTimeZone(item));
  return out;
}

/** toggleAllDownloadsArePaused = Ok — Changes pause state of all files in the file download list */
export class ToggleAllDownloadsArePaused {
  type: 'toggleAllDownloadsArePaused' = 'toggleAllDownloadsArePaused';
  extra?: string;
  /** are_paused: Bool — Pass true to pause all downloads; pass false to unpause them */
  are_paused: boolean = false;
}

/** Decode a TDLib `toggleAllDownloadsArePaused` object; null input -> null. */
export function decodeToggleAllDownloadsArePaused(json: TdJsonRaw): ToggleAllDownloadsArePaused | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleAllDownloadsArePaused = new ToggleAllDownloadsArePaused();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.are_paused = asBool(record['are_paused']);
  return out;
}

/** Encode a `toggleAllDownloadsArePaused` into TDLib JSON wire shape. */
export function encodeToggleAllDownloadsArePaused(value: ToggleAllDownloadsArePaused): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleAllDownloadsArePaused';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['are_paused'] = value.are_paused;
  return out;
}

/** toggleBotCanManageEmojiStatus = Ok — Toggles whether the bot can manage emoji status of the current user */
export class ToggleBotCanManageEmojiStatus {
  type: 'toggleBotCanManageEmojiStatus' = 'toggleBotCanManageEmojiStatus';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the bot */
  bot_user_id: number = 0;
  /** can_manage_emoji_status: Bool — Pass true if the bot is allowed to change emoji status of the user; pass false otherwise */
  can_manage_emoji_status: boolean = false;
}

/** Decode a TDLib `toggleBotCanManageEmojiStatus` object; null input -> null. */
export function decodeToggleBotCanManageEmojiStatus(json: TdJsonRaw): ToggleBotCanManageEmojiStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleBotCanManageEmojiStatus = new ToggleBotCanManageEmojiStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.can_manage_emoji_status = asBool(record['can_manage_emoji_status']);
  return out;
}

/** Encode a `toggleBotCanManageEmojiStatus` into TDLib JSON wire shape. */
export function encodeToggleBotCanManageEmojiStatus(value: ToggleBotCanManageEmojiStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleBotCanManageEmojiStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['can_manage_emoji_status'] = value.can_manage_emoji_status;
  return out;
}

/** toggleBotIsAddedToAttachmentMenu = Ok — Adds or removes a bot to attachment and side menu. Bot can be added to the menu, only if userTypeBot.can_be_added_to_attachment_menu == true */
export class ToggleBotIsAddedToAttachmentMenu {
  type: 'toggleBotIsAddedToAttachmentMenu' = 'toggleBotIsAddedToAttachmentMenu';
  extra?: string;
  /** bot_user_id: int53 — Bot's user identifier */
  bot_user_id: number = 0;
  /** is_added: Bool — Pass true to add the bot to attachment menu; pass false to remove the bot from attachment menu */
  is_added: boolean = false;
  /** allow_write_access: Bool — Pass true if the current user allowed the bot to send them messages. Ignored if is_added is false */
  allow_write_access: boolean = false;
}

/** Decode a TDLib `toggleBotIsAddedToAttachmentMenu` object; null input -> null. */
export function decodeToggleBotIsAddedToAttachmentMenu(json: TdJsonRaw): ToggleBotIsAddedToAttachmentMenu | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleBotIsAddedToAttachmentMenu = new ToggleBotIsAddedToAttachmentMenu();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.is_added = asBool(record['is_added']);
  out.allow_write_access = asBool(record['allow_write_access']);
  return out;
}

/** Encode a `toggleBotIsAddedToAttachmentMenu` into TDLib JSON wire shape. */
export function encodeToggleBotIsAddedToAttachmentMenu(value: ToggleBotIsAddedToAttachmentMenu): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleBotIsAddedToAttachmentMenu';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['is_added'] = value.is_added;
  out['allow_write_access'] = value.allow_write_access;
  return out;
}

/** toggleBotUsernameIsActive = Ok — Changes active state for a username of a bot. The editable username can be disabled only if there are other active usernames. May return an error with a message "USERNAMES_ACTIVE_TOO_MUCH" if the maximum ... */
export class ToggleBotUsernameIsActive {
  type: 'toggleBotUsernameIsActive' = 'toggleBotUsernameIsActive';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
  /** username: string — The username to change */
  username: string = '';
  /** is_active: Bool — Pass true to activate the username; pass false to disable it */
  is_active: boolean = false;
}

/** Decode a TDLib `toggleBotUsernameIsActive` object; null input -> null. */
export function decodeToggleBotUsernameIsActive(json: TdJsonRaw): ToggleBotUsernameIsActive | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleBotUsernameIsActive = new ToggleBotUsernameIsActive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.username = asString(record['username']);
  out.is_active = asBool(record['is_active']);
  return out;
}

/** Encode a `toggleBotUsernameIsActive` into TDLib JSON wire shape. */
export function encodeToggleBotUsernameIsActive(value: ToggleBotUsernameIsActive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleBotUsernameIsActive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['username'] = value.username;
  out['is_active'] = value.is_active;
  return out;
}

/** toggleBusinessConnectedBotChatIsPaused = Ok — Pauses or resumes the connected business bot in a specific chat */
export class ToggleBusinessConnectedBotChatIsPaused {
  type: 'toggleBusinessConnectedBotChatIsPaused' = 'toggleBusinessConnectedBotChatIsPaused';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** is_paused: Bool — Pass true to pause the connected bot in the chat; pass false to resume the bot */
  is_paused: boolean = false;
}

/** Decode a TDLib `toggleBusinessConnectedBotChatIsPaused` object; null input -> null. */
export function decodeToggleBusinessConnectedBotChatIsPaused(json: TdJsonRaw): ToggleBusinessConnectedBotChatIsPaused | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleBusinessConnectedBotChatIsPaused = new ToggleBusinessConnectedBotChatIsPaused();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.is_paused = asBool(record['is_paused']);
  return out;
}

/** Encode a `toggleBusinessConnectedBotChatIsPaused` into TDLib JSON wire shape. */
export function encodeToggleBusinessConnectedBotChatIsPaused(value: ToggleBusinessConnectedBotChatIsPaused): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleBusinessConnectedBotChatIsPaused';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['is_paused'] = value.is_paused;
  return out;
}

/** toggleChatDefaultDisableNotification = Ok — Changes the value of the default disable_notification parameter, used when a message is sent to a chat */
export class ToggleChatDefaultDisableNotification {
  type: 'toggleChatDefaultDisableNotification' = 'toggleChatDefaultDisableNotification';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** default_disable_notification: Bool — New value of default_disable_notification */
  default_disable_notification: boolean = false;
}

/** Decode a TDLib `toggleChatDefaultDisableNotification` object; null input -> null. */
export function decodeToggleChatDefaultDisableNotification(json: TdJsonRaw): ToggleChatDefaultDisableNotification | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleChatDefaultDisableNotification = new ToggleChatDefaultDisableNotification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.default_disable_notification = asBool(record['default_disable_notification']);
  return out;
}

/** Encode a `toggleChatDefaultDisableNotification` into TDLib JSON wire shape. */
export function encodeToggleChatDefaultDisableNotification(value: ToggleChatDefaultDisableNotification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleChatDefaultDisableNotification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['default_disable_notification'] = value.default_disable_notification;
  return out;
}

/** toggleChatFolderTags = Ok — Toggles whether chat folder tags are enabled */
export class ToggleChatFolderTags {
  type: 'toggleChatFolderTags' = 'toggleChatFolderTags';
  extra?: string;
  /** are_tags_enabled: Bool — Pass true to enable folder tags; pass false to disable them */
  are_tags_enabled: boolean = false;
}

/** Decode a TDLib `toggleChatFolderTags` object; null input -> null. */
export function decodeToggleChatFolderTags(json: TdJsonRaw): ToggleChatFolderTags | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleChatFolderTags = new ToggleChatFolderTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.are_tags_enabled = asBool(record['are_tags_enabled']);
  return out;
}

/** Encode a `toggleChatFolderTags` into TDLib JSON wire shape. */
export function encodeToggleChatFolderTags(value: ToggleChatFolderTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleChatFolderTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['are_tags_enabled'] = value.are_tags_enabled;
  return out;
}

/** toggleChatGiftNotifications = Ok — Toggles whether notifications for new gifts received by a channel chat are sent to the current user; requires can_post_messages administrator right in the chat */
export class ToggleChatGiftNotifications {
  type: 'toggleChatGiftNotifications' = 'toggleChatGiftNotifications';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat */
  chat_id: number = 0;
  /** are_enabled: Bool — Pass true to enable notifications about new gifts owned by the channel chat; pass false to disable the notifications */
  are_enabled: boolean = false;
}

/** Decode a TDLib `toggleChatGiftNotifications` object; null input -> null. */
export function decodeToggleChatGiftNotifications(json: TdJsonRaw): ToggleChatGiftNotifications | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleChatGiftNotifications = new ToggleChatGiftNotifications();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.are_enabled = asBool(record['are_enabled']);
  return out;
}

/** Encode a `toggleChatGiftNotifications` into TDLib JSON wire shape. */
export function encodeToggleChatGiftNotifications(value: ToggleChatGiftNotifications): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleChatGiftNotifications';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['are_enabled'] = value.are_enabled;
  return out;
}

/** toggleChatHasProtectedContent = Ok — Changes the ability of users to save, forward, or copy chat content. Requires owner privileges in basic groups, supergroups and channels. Requires Telegram Premium to enable protected content in priva... */
export class ToggleChatHasProtectedContent {
  type: 'toggleChatHasProtectedContent' = 'toggleChatHasProtectedContent';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** has_protected_content: Bool — New value of has_protected_content */
  has_protected_content: boolean = false;
}

/** Decode a TDLib `toggleChatHasProtectedContent` object; null input -> null. */
export function decodeToggleChatHasProtectedContent(json: TdJsonRaw): ToggleChatHasProtectedContent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleChatHasProtectedContent = new ToggleChatHasProtectedContent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.has_protected_content = asBool(record['has_protected_content']);
  return out;
}

/** Encode a `toggleChatHasProtectedContent` into TDLib JSON wire shape. */
export function encodeToggleChatHasProtectedContent(value: ToggleChatHasProtectedContent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleChatHasProtectedContent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['has_protected_content'] = value.has_protected_content;
  return out;
}

/** toggleChatIsMarkedAsUnread = Ok — Changes the marked as unread state of a chat */
export class ToggleChatIsMarkedAsUnread {
  type: 'toggleChatIsMarkedAsUnread' = 'toggleChatIsMarkedAsUnread';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** is_marked_as_unread: Bool — New value of is_marked_as_unread */
  is_marked_as_unread: boolean = false;
}

/** Decode a TDLib `toggleChatIsMarkedAsUnread` object; null input -> null. */
export function decodeToggleChatIsMarkedAsUnread(json: TdJsonRaw): ToggleChatIsMarkedAsUnread | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleChatIsMarkedAsUnread = new ToggleChatIsMarkedAsUnread();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.is_marked_as_unread = asBool(record['is_marked_as_unread']);
  return out;
}

/** Encode a `toggleChatIsMarkedAsUnread` into TDLib JSON wire shape. */
export function encodeToggleChatIsMarkedAsUnread(value: ToggleChatIsMarkedAsUnread): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleChatIsMarkedAsUnread';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['is_marked_as_unread'] = value.is_marked_as_unread;
  return out;
}

/** toggleChatIsPinned = Ok — Changes the pinned state of a chat. There can be up to getOption("pinned_chat_count_max")/getOption("pinned_archived_chat_count_max") pinned non-secret chats and the same number of secret chats in the main/archi... */
export class ToggleChatIsPinned {
  type: 'toggleChatIsPinned' = 'toggleChatIsPinned';
  extra?: string;
  /** chat_list: ChatList — Chat list in which to change the pinned state of the chat */
  chat_list: TdChatList | null = null;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** is_pinned: Bool — Pass true to pin the chat; pass false to unpin it */
  is_pinned: boolean = false;
}

/** Decode a TDLib `toggleChatIsPinned` object; null input -> null. */
export function decodeToggleChatIsPinned(json: TdJsonRaw): ToggleChatIsPinned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleChatIsPinned = new ToggleChatIsPinned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_list = decodeTdChatList(record['chat_list']);
  out.chat_id = asNumber(record['chat_id']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `toggleChatIsPinned` into TDLib JSON wire shape. */
export function encodeToggleChatIsPinned(value: ToggleChatIsPinned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleChatIsPinned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  out['chat_id'] = value.chat_id;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** toggleChatIsTranslatable = Ok — Changes the translatable state of a chat */
export class ToggleChatIsTranslatable {
  type: 'toggleChatIsTranslatable' = 'toggleChatIsTranslatable';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** is_translatable: Bool — New value of is_translatable */
  is_translatable: boolean = false;
}

/** Decode a TDLib `toggleChatIsTranslatable` object; null input -> null. */
export function decodeToggleChatIsTranslatable(json: TdJsonRaw): ToggleChatIsTranslatable | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleChatIsTranslatable = new ToggleChatIsTranslatable();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.is_translatable = asBool(record['is_translatable']);
  return out;
}

/** Encode a `toggleChatIsTranslatable` into TDLib JSON wire shape. */
export function encodeToggleChatIsTranslatable(value: ToggleChatIsTranslatable): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleChatIsTranslatable';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['is_translatable'] = value.is_translatable;
  return out;
}

/** toggleChatViewAsTopics = Ok — Changes the view_as_topics setting of a forum chat or Saved Messages */
export class ToggleChatViewAsTopics {
  type: 'toggleChatViewAsTopics' = 'toggleChatViewAsTopics';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** view_as_topics: Bool — New value of view_as_topics */
  view_as_topics: boolean = false;
}

/** Decode a TDLib `toggleChatViewAsTopics` object; null input -> null. */
export function decodeToggleChatViewAsTopics(json: TdJsonRaw): ToggleChatViewAsTopics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleChatViewAsTopics = new ToggleChatViewAsTopics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.view_as_topics = asBool(record['view_as_topics']);
  return out;
}

/** Encode a `toggleChatViewAsTopics` into TDLib JSON wire shape. */
export function encodeToggleChatViewAsTopics(value: ToggleChatViewAsTopics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleChatViewAsTopics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['view_as_topics'] = value.view_as_topics;
  return out;
}

/** toggleDirectMessagesChatTopicCanSendUnpaidMessages = Ok — Allows to send unpaid messages to the given topic of the channel direct messages chat administered by the current user */
export class ToggleDirectMessagesChatTopicCanSendUnpaidMessages {
  type: 'toggleDirectMessagesChatTopicCanSendUnpaidMessages' = 'toggleDirectMessagesChatTopicCanSendUnpaidMessages';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** topic_id: int53 — Identifier of the topic */
  topic_id: number = 0;
  /** can_send_unpaid_messages: Bool — Pass true to allow unpaid messages; pass false to disallow unpaid messages */
  can_send_unpaid_messages: boolean = false;
  /** refund_payments: Bool — Pass true to refund the user previously paid messages */
  refund_payments: boolean = false;
}

/** Decode a TDLib `toggleDirectMessagesChatTopicCanSendUnpaidMessages` object; null input -> null. */
export function decodeToggleDirectMessagesChatTopicCanSendUnpaidMessages(json: TdJsonRaw): ToggleDirectMessagesChatTopicCanSendUnpaidMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleDirectMessagesChatTopicCanSendUnpaidMessages = new ToggleDirectMessagesChatTopicCanSendUnpaidMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  out.can_send_unpaid_messages = asBool(record['can_send_unpaid_messages']);
  out.refund_payments = asBool(record['refund_payments']);
  return out;
}

/** Encode a `toggleDirectMessagesChatTopicCanSendUnpaidMessages` into TDLib JSON wire shape. */
export function encodeToggleDirectMessagesChatTopicCanSendUnpaidMessages(value: ToggleDirectMessagesChatTopicCanSendUnpaidMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleDirectMessagesChatTopicCanSendUnpaidMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  out['can_send_unpaid_messages'] = value.can_send_unpaid_messages;
  out['refund_payments'] = value.refund_payments;
  return out;
}

/** toggleDownloadIsPaused = Ok — Changes pause state of a file in the file download list */
export class ToggleDownloadIsPaused {
  type: 'toggleDownloadIsPaused' = 'toggleDownloadIsPaused';
  extra?: string;
  /** file_id: int32 — Identifier of the downloaded file */
  file_id: number = 0;
  /** is_paused: Bool — Pass true if the download is paused */
  is_paused: boolean = false;
}

/** Decode a TDLib `toggleDownloadIsPaused` object; null input -> null. */
export function decodeToggleDownloadIsPaused(json: TdJsonRaw): ToggleDownloadIsPaused | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleDownloadIsPaused = new ToggleDownloadIsPaused();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.is_paused = asBool(record['is_paused']);
  return out;
}

/** Encode a `toggleDownloadIsPaused` into TDLib JSON wire shape. */
export function encodeToggleDownloadIsPaused(value: ToggleDownloadIsPaused): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleDownloadIsPaused';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['is_paused'] = value.is_paused;
  return out;
}

/** toggleForumTopicIsClosed = Ok — Toggles whether a topic is closed in a forum supergroup chat; requires can_manage_topics administrator right in the supergroup unless the user is creator of the topic */
export class ToggleForumTopicIsClosed {
  type: 'toggleForumTopicIsClosed' = 'toggleForumTopicIsClosed';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier */
  forum_topic_id: number = 0;
  /** is_closed: Bool — Pass true to close the topic; pass false to reopen it */
  is_closed: boolean = false;
}

/** Decode a TDLib `toggleForumTopicIsClosed` object; null input -> null. */
export function decodeToggleForumTopicIsClosed(json: TdJsonRaw): ToggleForumTopicIsClosed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleForumTopicIsClosed = new ToggleForumTopicIsClosed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.is_closed = asBool(record['is_closed']);
  return out;
}

/** Encode a `toggleForumTopicIsClosed` into TDLib JSON wire shape. */
export function encodeToggleForumTopicIsClosed(value: ToggleForumTopicIsClosed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleForumTopicIsClosed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['is_closed'] = value.is_closed;
  return out;
}

/** toggleForumTopicIsPinned = Ok — Changes the pinned state of a topic in a forum supergroup chat or a chat with a bot with topics; requires can_manage_topics administrator right in the supergroup. There can be up to getOption("pinned_forum... */
export class ToggleForumTopicIsPinned {
  type: 'toggleForumTopicIsPinned' = 'toggleForumTopicIsPinned';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier */
  forum_topic_id: number = 0;
  /** is_pinned: Bool — Pass true to pin the topic; pass false to unpin it */
  is_pinned: boolean = false;
}

/** Decode a TDLib `toggleForumTopicIsPinned` object; null input -> null. */
export function decodeToggleForumTopicIsPinned(json: TdJsonRaw): ToggleForumTopicIsPinned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleForumTopicIsPinned = new ToggleForumTopicIsPinned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `toggleForumTopicIsPinned` into TDLib JSON wire shape. */
export function encodeToggleForumTopicIsPinned(value: ToggleForumTopicIsPinned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleForumTopicIsPinned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** toggleGeneralForumTopicIsHidden = Ok — Toggles whether a General topic is hidden in a forum supergroup chat; requires can_manage_topics administrator right in the supergroup */
export class ToggleGeneralForumTopicIsHidden {
  type: 'toggleGeneralForumTopicIsHidden' = 'toggleGeneralForumTopicIsHidden';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** is_hidden: Bool — Pass true to hide and close the General topic; pass false to unhide it */
  is_hidden: boolean = false;
}

/** Decode a TDLib `toggleGeneralForumTopicIsHidden` object; null input -> null. */
export function decodeToggleGeneralForumTopicIsHidden(json: TdJsonRaw): ToggleGeneralForumTopicIsHidden | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleGeneralForumTopicIsHidden = new ToggleGeneralForumTopicIsHidden();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.is_hidden = asBool(record['is_hidden']);
  return out;
}

/** Encode a `toggleGeneralForumTopicIsHidden` into TDLib JSON wire shape. */
export function encodeToggleGeneralForumTopicIsHidden(value: ToggleGeneralForumTopicIsHidden): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleGeneralForumTopicIsHidden';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['is_hidden'] = value.is_hidden;
  return out;
}

/** toggleGiftIsSaved = Ok — Toggles whether a gift is shown on the current user's or the channel's profile page; requires can_post_messages administrator right in the channel chat */
export class ToggleGiftIsSaved {
  type: 'toggleGiftIsSaved' = 'toggleGiftIsSaved';
  extra?: string;
  /** received_gift_id: string — Identifier of the gift */
  received_gift_id: string = '';
  /** is_saved: Bool — Pass true to display the gift on the user's or the channel's profile page; pass false to remove it from the profile page */
  is_saved: boolean = false;
}

/** Decode a TDLib `toggleGiftIsSaved` object; null input -> null. */
export function decodeToggleGiftIsSaved(json: TdJsonRaw): ToggleGiftIsSaved | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleGiftIsSaved = new ToggleGiftIsSaved();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.received_gift_id = asString(record['received_gift_id']);
  out.is_saved = asBool(record['is_saved']);
  return out;
}

/** Encode a `toggleGiftIsSaved` into TDLib JSON wire shape. */
export function encodeToggleGiftIsSaved(value: ToggleGiftIsSaved): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleGiftIsSaved';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['received_gift_id'] = value.received_gift_id;
  out['is_saved'] = value.is_saved;
  return out;
}

/** toggleGroupCallAreMessagesAllowed = Ok — Toggles whether participants of a group call can send messages there. Requires groupCall.can_toggle_are_messages_allowed right */
export class ToggleGroupCallAreMessagesAllowed {
  type: 'toggleGroupCallAreMessagesAllowed' = 'toggleGroupCallAreMessagesAllowed';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** are_messages_allowed: Bool — New value of the are_messages_allowed setting */
  are_messages_allowed: boolean = false;
}

/** Decode a TDLib `toggleGroupCallAreMessagesAllowed` object; null input -> null. */
export function decodeToggleGroupCallAreMessagesAllowed(json: TdJsonRaw): ToggleGroupCallAreMessagesAllowed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleGroupCallAreMessagesAllowed = new ToggleGroupCallAreMessagesAllowed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.are_messages_allowed = asBool(record['are_messages_allowed']);
  return out;
}

/** Encode a `toggleGroupCallAreMessagesAllowed` into TDLib JSON wire shape. */
export function encodeToggleGroupCallAreMessagesAllowed(value: ToggleGroupCallAreMessagesAllowed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleGroupCallAreMessagesAllowed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['are_messages_allowed'] = value.are_messages_allowed;
  return out;
}

/** toggleGroupCallIsMyVideoEnabled = Ok — Toggles whether current user's video is enabled */
export class ToggleGroupCallIsMyVideoEnabled {
  type: 'toggleGroupCallIsMyVideoEnabled' = 'toggleGroupCallIsMyVideoEnabled';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** is_my_video_enabled: Bool — Pass true if the current user's video is enabled */
  is_my_video_enabled: boolean = false;
}

/** Decode a TDLib `toggleGroupCallIsMyVideoEnabled` object; null input -> null. */
export function decodeToggleGroupCallIsMyVideoEnabled(json: TdJsonRaw): ToggleGroupCallIsMyVideoEnabled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleGroupCallIsMyVideoEnabled = new ToggleGroupCallIsMyVideoEnabled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.is_my_video_enabled = asBool(record['is_my_video_enabled']);
  return out;
}

/** Encode a `toggleGroupCallIsMyVideoEnabled` into TDLib JSON wire shape. */
export function encodeToggleGroupCallIsMyVideoEnabled(value: ToggleGroupCallIsMyVideoEnabled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleGroupCallIsMyVideoEnabled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['is_my_video_enabled'] = value.is_my_video_enabled;
  return out;
}

/** toggleGroupCallIsMyVideoPaused = Ok — Toggles whether current user's video is paused */
export class ToggleGroupCallIsMyVideoPaused {
  type: 'toggleGroupCallIsMyVideoPaused' = 'toggleGroupCallIsMyVideoPaused';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** is_my_video_paused: Bool — Pass true if the current user's video is paused */
  is_my_video_paused: boolean = false;
}

/** Decode a TDLib `toggleGroupCallIsMyVideoPaused` object; null input -> null. */
export function decodeToggleGroupCallIsMyVideoPaused(json: TdJsonRaw): ToggleGroupCallIsMyVideoPaused | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleGroupCallIsMyVideoPaused = new ToggleGroupCallIsMyVideoPaused();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.is_my_video_paused = asBool(record['is_my_video_paused']);
  return out;
}

/** Encode a `toggleGroupCallIsMyVideoPaused` into TDLib JSON wire shape. */
export function encodeToggleGroupCallIsMyVideoPaused(value: ToggleGroupCallIsMyVideoPaused): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleGroupCallIsMyVideoPaused';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['is_my_video_paused'] = value.is_my_video_paused;
  return out;
}

/** toggleGroupCallParticipantIsHandRaised = Ok — Toggles whether a group call participant hand is rased; for video chats only */
export class ToggleGroupCallParticipantIsHandRaised {
  type: 'toggleGroupCallParticipantIsHandRaised' = 'toggleGroupCallParticipantIsHandRaised';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** participant_id: MessageSender — Participant identifier */
  participant_id: TdMessageSender | null = null;
  /** is_hand_raised: Bool — Pass true if the user's hand needs to be raised. Only self hand can be raised. Requires groupCall.can_be_managed right to lower other's hand */
  is_hand_raised: boolean = false;
}

/** Decode a TDLib `toggleGroupCallParticipantIsHandRaised` object; null input -> null. */
export function decodeToggleGroupCallParticipantIsHandRaised(json: TdJsonRaw): ToggleGroupCallParticipantIsHandRaised | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleGroupCallParticipantIsHandRaised = new ToggleGroupCallParticipantIsHandRaised();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.participant_id = decodeTdMessageSender(record['participant_id']);
  out.is_hand_raised = asBool(record['is_hand_raised']);
  return out;
}

/** Encode a `toggleGroupCallParticipantIsHandRaised` into TDLib JSON wire shape. */
export function encodeToggleGroupCallParticipantIsHandRaised(value: ToggleGroupCallParticipantIsHandRaised): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleGroupCallParticipantIsHandRaised';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['participant_id'] = value.participant_id === null ? null : encodeTdMessageSender(value.participant_id);
  out['is_hand_raised'] = value.is_hand_raised;
  return out;
}

/** toggleGroupCallParticipantIsMuted = Ok — Toggles whether a participant of an active group call is muted, unmuted, or allowed to unmute themselves; not supported for live stories */
export class ToggleGroupCallParticipantIsMuted {
  type: 'toggleGroupCallParticipantIsMuted' = 'toggleGroupCallParticipantIsMuted';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** participant_id: MessageSender — Participant identifier */
  participant_id: TdMessageSender | null = null;
  /** is_muted: Bool — Pass true to mute the user; pass false to unmute them */
  is_muted: boolean = false;
}

/** Decode a TDLib `toggleGroupCallParticipantIsMuted` object; null input -> null. */
export function decodeToggleGroupCallParticipantIsMuted(json: TdJsonRaw): ToggleGroupCallParticipantIsMuted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleGroupCallParticipantIsMuted = new ToggleGroupCallParticipantIsMuted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.participant_id = decodeTdMessageSender(record['participant_id']);
  out.is_muted = asBool(record['is_muted']);
  return out;
}

/** Encode a `toggleGroupCallParticipantIsMuted` into TDLib JSON wire shape. */
export function encodeToggleGroupCallParticipantIsMuted(value: ToggleGroupCallParticipantIsMuted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleGroupCallParticipantIsMuted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['participant_id'] = value.participant_id === null ? null : encodeTdMessageSender(value.participant_id);
  out['is_muted'] = value.is_muted;
  return out;
}

/** toggleGroupCallScreenSharingIsPaused = Ok — Pauses or unpauses screen sharing in a joined group call; not supported in live stories */
export class ToggleGroupCallScreenSharingIsPaused {
  type: 'toggleGroupCallScreenSharingIsPaused' = 'toggleGroupCallScreenSharingIsPaused';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** is_paused: Bool — Pass true to pause screen sharing; pass false to unpause it */
  is_paused: boolean = false;
}

/** Decode a TDLib `toggleGroupCallScreenSharingIsPaused` object; null input -> null. */
export function decodeToggleGroupCallScreenSharingIsPaused(json: TdJsonRaw): ToggleGroupCallScreenSharingIsPaused | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleGroupCallScreenSharingIsPaused = new ToggleGroupCallScreenSharingIsPaused();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.is_paused = asBool(record['is_paused']);
  return out;
}

/** Encode a `toggleGroupCallScreenSharingIsPaused` into TDLib JSON wire shape. */
export function encodeToggleGroupCallScreenSharingIsPaused(value: ToggleGroupCallScreenSharingIsPaused): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleGroupCallScreenSharingIsPaused';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['is_paused'] = value.is_paused;
  return out;
}

/** toggleHasSponsoredMessagesEnabled = Ok — Toggles whether the current user has sponsored messages enabled. The setting has no effect for users without Telegram Premium for which sponsored messages are always enabled */
export class ToggleHasSponsoredMessagesEnabled {
  type: 'toggleHasSponsoredMessagesEnabled' = 'toggleHasSponsoredMessagesEnabled';
  extra?: string;
  /** has_sponsored_messages_enabled: Bool — Pass true to enable sponsored messages for the current user; false to disable them */
  has_sponsored_messages_enabled: boolean = false;
}

/** Decode a TDLib `toggleHasSponsoredMessagesEnabled` object; null input -> null. */
export function decodeToggleHasSponsoredMessagesEnabled(json: TdJsonRaw): ToggleHasSponsoredMessagesEnabled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleHasSponsoredMessagesEnabled = new ToggleHasSponsoredMessagesEnabled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.has_sponsored_messages_enabled = asBool(record['has_sponsored_messages_enabled']);
  return out;
}

/** Encode a `toggleHasSponsoredMessagesEnabled` into TDLib JSON wire shape. */
export function encodeToggleHasSponsoredMessagesEnabled(value: ToggleHasSponsoredMessagesEnabled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleHasSponsoredMessagesEnabled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['has_sponsored_messages_enabled'] = value.has_sponsored_messages_enabled;
  return out;
}

/** toggleSavedMessagesTopicIsPinned = Ok — Changes the pinned state of a Saved Messages topic. There can be up to getOption("pinned_saved_messages_topic_count_max") pinned topics. The limit can be increased with Telegram Premium */
export class ToggleSavedMessagesTopicIsPinned {
  type: 'toggleSavedMessagesTopicIsPinned' = 'toggleSavedMessagesTopicIsPinned';
  extra?: string;
  /** saved_messages_topic_id: int53 — Identifier of Saved Messages topic to pin or unpin */
  saved_messages_topic_id: number = 0;
  /** is_pinned: Bool — Pass true to pin the topic; pass false to unpin it */
  is_pinned: boolean = false;
}

/** Decode a TDLib `toggleSavedMessagesTopicIsPinned` object; null input -> null. */
export function decodeToggleSavedMessagesTopicIsPinned(json: TdJsonRaw): ToggleSavedMessagesTopicIsPinned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSavedMessagesTopicIsPinned = new ToggleSavedMessagesTopicIsPinned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `toggleSavedMessagesTopicIsPinned` into TDLib JSON wire shape. */
export function encodeToggleSavedMessagesTopicIsPinned(value: ToggleSavedMessagesTopicIsPinned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSavedMessagesTopicIsPinned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** toggleSessionCanAcceptCalls = Ok — Toggles whether a session can accept incoming calls */
export class ToggleSessionCanAcceptCalls {
  type: 'toggleSessionCanAcceptCalls' = 'toggleSessionCanAcceptCalls';
  extra?: string;
  /** session_id: int64 — Session identifier */
  session_id: string = '';
  /** can_accept_calls: Bool — Pass true to allow accepting incoming calls by the session; pass false otherwise */
  can_accept_calls: boolean = false;
}

/** Decode a TDLib `toggleSessionCanAcceptCalls` object; null input -> null. */
export function decodeToggleSessionCanAcceptCalls(json: TdJsonRaw): ToggleSessionCanAcceptCalls | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSessionCanAcceptCalls = new ToggleSessionCanAcceptCalls();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.session_id = asInt64(record['session_id']);
  out.can_accept_calls = asBool(record['can_accept_calls']);
  return out;
}

/** Encode a `toggleSessionCanAcceptCalls` into TDLib JSON wire shape. */
export function encodeToggleSessionCanAcceptCalls(value: ToggleSessionCanAcceptCalls): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSessionCanAcceptCalls';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['session_id'] = value.session_id;
  out['can_accept_calls'] = value.can_accept_calls;
  return out;
}

/** toggleSessionCanAcceptSecretChats = Ok — Toggles whether a session can accept incoming secret chats */
export class ToggleSessionCanAcceptSecretChats {
  type: 'toggleSessionCanAcceptSecretChats' = 'toggleSessionCanAcceptSecretChats';
  extra?: string;
  /** session_id: int64 — Session identifier */
  session_id: string = '';
  /** can_accept_secret_chats: Bool — Pass true to allow accepting secret chats by the session; pass false otherwise */
  can_accept_secret_chats: boolean = false;
}

/** Decode a TDLib `toggleSessionCanAcceptSecretChats` object; null input -> null. */
export function decodeToggleSessionCanAcceptSecretChats(json: TdJsonRaw): ToggleSessionCanAcceptSecretChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSessionCanAcceptSecretChats = new ToggleSessionCanAcceptSecretChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.session_id = asInt64(record['session_id']);
  out.can_accept_secret_chats = asBool(record['can_accept_secret_chats']);
  return out;
}

/** Encode a `toggleSessionCanAcceptSecretChats` into TDLib JSON wire shape. */
export function encodeToggleSessionCanAcceptSecretChats(value: ToggleSessionCanAcceptSecretChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSessionCanAcceptSecretChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['session_id'] = value.session_id;
  out['can_accept_secret_chats'] = value.can_accept_secret_chats;
  return out;
}

/** toggleStoryIsPostedToChatPage = Ok — Toggles whether a story is accessible after expiration. Can be called only if story.can_toggle_is_posted_to_chat_page == true */
export class ToggleStoryIsPostedToChatPage {
  type: 'toggleStoryIsPostedToChatPage' = 'toggleStoryIsPostedToChatPage';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Identifier of the story */
  story_id: number = 0;
  /** is_posted_to_chat_page: Bool — Pass true to make the story accessible after expiration; pass false to make it private */
  is_posted_to_chat_page: boolean = false;
}

/** Decode a TDLib `toggleStoryIsPostedToChatPage` object; null input -> null. */
export function decodeToggleStoryIsPostedToChatPage(json: TdJsonRaw): ToggleStoryIsPostedToChatPage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleStoryIsPostedToChatPage = new ToggleStoryIsPostedToChatPage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.is_posted_to_chat_page = asBool(record['is_posted_to_chat_page']);
  return out;
}

/** Encode a `toggleStoryIsPostedToChatPage` into TDLib JSON wire shape. */
export function encodeToggleStoryIsPostedToChatPage(value: ToggleStoryIsPostedToChatPage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleStoryIsPostedToChatPage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['is_posted_to_chat_page'] = value.is_posted_to_chat_page;
  return out;
}

/** toggleSupergroupCanHaveSponsoredMessages = Ok — Toggles whether sponsored messages are shown in the channel chat; requires owner privileges in the channel. The chat must have at least chatBoostFeatures.min_sponsored_message_disable_boost... */
export class ToggleSupergroupCanHaveSponsoredMessages {
  type: 'toggleSupergroupCanHaveSponsoredMessages' = 'toggleSupergroupCanHaveSponsoredMessages';
  extra?: string;
  /** supergroup_id: int53 — The identifier of the channel */
  supergroup_id: number = 0;
  /** can_have_sponsored_messages: Bool — The new value of can_have_sponsored_messages */
  can_have_sponsored_messages: boolean = false;
}

/** Decode a TDLib `toggleSupergroupCanHaveSponsoredMessages` object; null input -> null. */
export function decodeToggleSupergroupCanHaveSponsoredMessages(json: TdJsonRaw): ToggleSupergroupCanHaveSponsoredMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupCanHaveSponsoredMessages = new ToggleSupergroupCanHaveSponsoredMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.can_have_sponsored_messages = asBool(record['can_have_sponsored_messages']);
  return out;
}

/** Encode a `toggleSupergroupCanHaveSponsoredMessages` into TDLib JSON wire shape. */
export function encodeToggleSupergroupCanHaveSponsoredMessages(value: ToggleSupergroupCanHaveSponsoredMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupCanHaveSponsoredMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['can_have_sponsored_messages'] = value.can_have_sponsored_messages;
  return out;
}

/** toggleSupergroupHasAggressiveAntiSpamEnabled = Ok — Toggles whether aggressive anti-spam checks are enabled in the supergroup. Can be called only if supergroupFullInfo.can_toggle_aggressive_anti_spam == true */
export class ToggleSupergroupHasAggressiveAntiSpamEnabled {
  type: 'toggleSupergroupHasAggressiveAntiSpamEnabled' = 'toggleSupergroupHasAggressiveAntiSpamEnabled';
  extra?: string;
  /** supergroup_id: int53 — The identifier of the supergroup, which isn't a broadcast group */
  supergroup_id: number = 0;
  /** has_aggressive_anti_spam_enabled: Bool — The new value of has_aggressive_anti_spam_enabled */
  has_aggressive_anti_spam_enabled: boolean = false;
}

/** Decode a TDLib `toggleSupergroupHasAggressiveAntiSpamEnabled` object; null input -> null. */
export function decodeToggleSupergroupHasAggressiveAntiSpamEnabled(json: TdJsonRaw): ToggleSupergroupHasAggressiveAntiSpamEnabled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupHasAggressiveAntiSpamEnabled = new ToggleSupergroupHasAggressiveAntiSpamEnabled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.has_aggressive_anti_spam_enabled = asBool(record['has_aggressive_anti_spam_enabled']);
  return out;
}

/** Encode a `toggleSupergroupHasAggressiveAntiSpamEnabled` into TDLib JSON wire shape. */
export function encodeToggleSupergroupHasAggressiveAntiSpamEnabled(value: ToggleSupergroupHasAggressiveAntiSpamEnabled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupHasAggressiveAntiSpamEnabled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['has_aggressive_anti_spam_enabled'] = value.has_aggressive_anti_spam_enabled;
  return out;
}

/** toggleSupergroupHasAutomaticTranslation = Ok — Toggles whether messages are automatically translated in the channel chat; requires can_change_info administrator right in the channel. The chat must have at least chatBoostFeatures.min_auto... */
export class ToggleSupergroupHasAutomaticTranslation {
  type: 'toggleSupergroupHasAutomaticTranslation' = 'toggleSupergroupHasAutomaticTranslation';
  extra?: string;
  /** supergroup_id: int53 — The identifier of the channel */
  supergroup_id: number = 0;
  /** has_automatic_translation: Bool — The new value of has_automatic_translation */
  has_automatic_translation: boolean = false;
}

/** Decode a TDLib `toggleSupergroupHasAutomaticTranslation` object; null input -> null. */
export function decodeToggleSupergroupHasAutomaticTranslation(json: TdJsonRaw): ToggleSupergroupHasAutomaticTranslation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupHasAutomaticTranslation = new ToggleSupergroupHasAutomaticTranslation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.has_automatic_translation = asBool(record['has_automatic_translation']);
  return out;
}

/** Encode a `toggleSupergroupHasAutomaticTranslation` into TDLib JSON wire shape. */
export function encodeToggleSupergroupHasAutomaticTranslation(value: ToggleSupergroupHasAutomaticTranslation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupHasAutomaticTranslation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['has_automatic_translation'] = value.has_automatic_translation;
  return out;
}

/** toggleSupergroupHasHiddenMembers = Ok — Toggles whether non-administrators can receive only administrators and bots using getSupergroupMembers or searchChatMembers. Can be called only if supergroupFullInfo.can_hide_members == true */
export class ToggleSupergroupHasHiddenMembers {
  type: 'toggleSupergroupHasHiddenMembers' = 'toggleSupergroupHasHiddenMembers';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup */
  supergroup_id: number = 0;
  /** has_hidden_members: Bool — New value of has_hidden_members */
  has_hidden_members: boolean = false;
}

/** Decode a TDLib `toggleSupergroupHasHiddenMembers` object; null input -> null. */
export function decodeToggleSupergroupHasHiddenMembers(json: TdJsonRaw): ToggleSupergroupHasHiddenMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupHasHiddenMembers = new ToggleSupergroupHasHiddenMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.has_hidden_members = asBool(record['has_hidden_members']);
  return out;
}

/** Encode a `toggleSupergroupHasHiddenMembers` into TDLib JSON wire shape. */
export function encodeToggleSupergroupHasHiddenMembers(value: ToggleSupergroupHasHiddenMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupHasHiddenMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['has_hidden_members'] = value.has_hidden_members;
  return out;
}

/** toggleSupergroupIsAllHistoryAvailable = Ok — Toggles whether the message history of a supergroup is available to new members; requires can_change_info member right */
export class ToggleSupergroupIsAllHistoryAvailable {
  type: 'toggleSupergroupIsAllHistoryAvailable' = 'toggleSupergroupIsAllHistoryAvailable';
  extra?: string;
  /** supergroup_id: int53 — The identifier of the supergroup */
  supergroup_id: number = 0;
  /** is_all_history_available: Bool — The new value of is_all_history_available */
  is_all_history_available: boolean = false;
}

/** Decode a TDLib `toggleSupergroupIsAllHistoryAvailable` object; null input -> null. */
export function decodeToggleSupergroupIsAllHistoryAvailable(json: TdJsonRaw): ToggleSupergroupIsAllHistoryAvailable | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupIsAllHistoryAvailable = new ToggleSupergroupIsAllHistoryAvailable();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.is_all_history_available = asBool(record['is_all_history_available']);
  return out;
}

/** Encode a `toggleSupergroupIsAllHistoryAvailable` into TDLib JSON wire shape. */
export function encodeToggleSupergroupIsAllHistoryAvailable(value: ToggleSupergroupIsAllHistoryAvailable): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupIsAllHistoryAvailable';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['is_all_history_available'] = value.is_all_history_available;
  return out;
}

/** toggleSupergroupIsBroadcastGroup = Ok — Upgrades supergroup to a broadcast group; requires owner privileges in the supergroup */
export class ToggleSupergroupIsBroadcastGroup {
  type: 'toggleSupergroupIsBroadcastGroup' = 'toggleSupergroupIsBroadcastGroup';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup */
  supergroup_id: number = 0;
}

/** Decode a TDLib `toggleSupergroupIsBroadcastGroup` object; null input -> null. */
export function decodeToggleSupergroupIsBroadcastGroup(json: TdJsonRaw): ToggleSupergroupIsBroadcastGroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupIsBroadcastGroup = new ToggleSupergroupIsBroadcastGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  return out;
}

/** Encode a `toggleSupergroupIsBroadcastGroup` into TDLib JSON wire shape. */
export function encodeToggleSupergroupIsBroadcastGroup(value: ToggleSupergroupIsBroadcastGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupIsBroadcastGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  return out;
}

/** toggleSupergroupIsForum = Ok — Toggles whether the supergroup is a forum; requires owner privileges in the supergroup. Discussion supergroups can't be converted to forums */
export class ToggleSupergroupIsForum {
  type: 'toggleSupergroupIsForum' = 'toggleSupergroupIsForum';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup */
  supergroup_id: number = 0;
  /** is_forum: Bool — New value of is_forum */
  is_forum: boolean = false;
  /** has_forum_tabs: Bool — New value of has_forum_tabs; ignored if is_forum is false */
  has_forum_tabs: boolean = false;
}

/** Decode a TDLib `toggleSupergroupIsForum` object; null input -> null. */
export function decodeToggleSupergroupIsForum(json: TdJsonRaw): ToggleSupergroupIsForum | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupIsForum = new ToggleSupergroupIsForum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.is_forum = asBool(record['is_forum']);
  out.has_forum_tabs = asBool(record['has_forum_tabs']);
  return out;
}

/** Encode a `toggleSupergroupIsForum` into TDLib JSON wire shape. */
export function encodeToggleSupergroupIsForum(value: ToggleSupergroupIsForum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupIsForum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['is_forum'] = value.is_forum;
  out['has_forum_tabs'] = value.has_forum_tabs;
  return out;
}

/** toggleSupergroupJoinByRequest = Ok — Toggles whether all users directly joining the supergroup need to be approved by supergroup administrators; requires can_restrict_members administrator right The bot must have administrator privileges... */
export class ToggleSupergroupJoinByRequest {
  type: 'toggleSupergroupJoinByRequest' = 'toggleSupergroupJoinByRequest';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup that isn't a broadcast group and isn't a channel direct message group */
  supergroup_id: number = 0;
  /** join_by_request: Bool — New value of join_by_request */
  join_by_request: boolean = false;
  /** guard_bot_user_id: int53 — Identifier of the bot which will be the guard bot in the group; pass 0 if none; ignored if join_by_request == false. */
  guard_bot_user_id: number = 0;
  /** apply_to_invite_links: Bool — Pass true to apply the change to the existing invite links, including primary links */
  apply_to_invite_links: boolean = false;
}

/** Decode a TDLib `toggleSupergroupJoinByRequest` object; null input -> null. */
export function decodeToggleSupergroupJoinByRequest(json: TdJsonRaw): ToggleSupergroupJoinByRequest | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupJoinByRequest = new ToggleSupergroupJoinByRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.join_by_request = asBool(record['join_by_request']);
  out.guard_bot_user_id = asNumber(record['guard_bot_user_id']);
  out.apply_to_invite_links = asBool(record['apply_to_invite_links']);
  return out;
}

/** Encode a `toggleSupergroupJoinByRequest` into TDLib JSON wire shape. */
export function encodeToggleSupergroupJoinByRequest(value: ToggleSupergroupJoinByRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupJoinByRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['join_by_request'] = value.join_by_request;
  out['guard_bot_user_id'] = value.guard_bot_user_id;
  out['apply_to_invite_links'] = value.apply_to_invite_links;
  return out;
}

/** toggleSupergroupJoinToSendMessages = Ok — Toggles whether joining is mandatory to send messages to a discussion supergroup; requires can_restrict_members administrator right */
export class ToggleSupergroupJoinToSendMessages {
  type: 'toggleSupergroupJoinToSendMessages' = 'toggleSupergroupJoinToSendMessages';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup that isn't a broadcast group */
  supergroup_id: number = 0;
  /** join_to_send_messages: Bool — New value of join_to_send_messages */
  join_to_send_messages: boolean = false;
}

/** Decode a TDLib `toggleSupergroupJoinToSendMessages` object; null input -> null. */
export function decodeToggleSupergroupJoinToSendMessages(json: TdJsonRaw): ToggleSupergroupJoinToSendMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupJoinToSendMessages = new ToggleSupergroupJoinToSendMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.join_to_send_messages = asBool(record['join_to_send_messages']);
  return out;
}

/** Encode a `toggleSupergroupJoinToSendMessages` into TDLib JSON wire shape. */
export function encodeToggleSupergroupJoinToSendMessages(value: ToggleSupergroupJoinToSendMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupJoinToSendMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['join_to_send_messages'] = value.join_to_send_messages;
  return out;
}

/** toggleSupergroupSignMessages = Ok — Toggles whether sender signature or link to the account is added to sent messages in a channel; requires can_change_info member right */
export class ToggleSupergroupSignMessages {
  type: 'toggleSupergroupSignMessages' = 'toggleSupergroupSignMessages';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the channel */
  supergroup_id: number = 0;
  /** sign_messages: Bool — New value of sign_messages */
  sign_messages: boolean = false;
  /** show_message_sender: Bool — New value of show_message_sender */
  show_message_sender: boolean = false;
}

/** Decode a TDLib `toggleSupergroupSignMessages` object; null input -> null. */
export function decodeToggleSupergroupSignMessages(json: TdJsonRaw): ToggleSupergroupSignMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupSignMessages = new ToggleSupergroupSignMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.sign_messages = asBool(record['sign_messages']);
  out.show_message_sender = asBool(record['show_message_sender']);
  return out;
}

/** Encode a `toggleSupergroupSignMessages` into TDLib JSON wire shape. */
export function encodeToggleSupergroupSignMessages(value: ToggleSupergroupSignMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupSignMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['sign_messages'] = value.sign_messages;
  out['show_message_sender'] = value.show_message_sender;
  return out;
}

/** toggleSupergroupUsernameIsActive = Ok — Changes active state for a username of a supergroup or channel, requires owner privileges in the supergroup or channel. The editable username can't be disabled. May return an error with a message "... */
export class ToggleSupergroupUsernameIsActive {
  type: 'toggleSupergroupUsernameIsActive' = 'toggleSupergroupUsernameIsActive';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup or channel */
  supergroup_id: number = 0;
  /** username: string — The username to change */
  username: string = '';
  /** is_active: Bool — Pass true to activate the username; pass false to disable it */
  is_active: boolean = false;
}

/** Decode a TDLib `toggleSupergroupUsernameIsActive` object; null input -> null. */
export function decodeToggleSupergroupUsernameIsActive(json: TdJsonRaw): ToggleSupergroupUsernameIsActive | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleSupergroupUsernameIsActive = new ToggleSupergroupUsernameIsActive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.username = asString(record['username']);
  out.is_active = asBool(record['is_active']);
  return out;
}

/** Encode a `toggleSupergroupUsernameIsActive` into TDLib JSON wire shape. */
export function encodeToggleSupergroupUsernameIsActive(value: ToggleSupergroupUsernameIsActive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleSupergroupUsernameIsActive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['username'] = value.username;
  out['is_active'] = value.is_active;
  return out;
}

/** toggleUsernameIsActive = Ok — Changes active state for a username of the current user. The editable username can't be disabled. May return an error with a message "USERNAMES_ACTIVE_TOO_MUCH" if the maximum number of active usernames has ... */
export class ToggleUsernameIsActive {
  type: 'toggleUsernameIsActive' = 'toggleUsernameIsActive';
  extra?: string;
  /** username: string — The username to change */
  username: string = '';
  /** is_active: Bool — Pass true to activate the username; pass false to disable it */
  is_active: boolean = false;
}

/** Decode a TDLib `toggleUsernameIsActive` object; null input -> null. */
export function decodeToggleUsernameIsActive(json: TdJsonRaw): ToggleUsernameIsActive | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleUsernameIsActive = new ToggleUsernameIsActive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  out.is_active = asBool(record['is_active']);
  return out;
}

/** Encode a `toggleUsernameIsActive` into TDLib JSON wire shape. */
export function encodeToggleUsernameIsActive(value: ToggleUsernameIsActive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleUsernameIsActive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  out['is_active'] = value.is_active;
  return out;
}

/** toggleVideoChatEnabledStartNotification = Ok — Toggles whether the current user will receive a notification when the video chat starts; for scheduled video chats only */
export class ToggleVideoChatEnabledStartNotification {
  type: 'toggleVideoChatEnabledStartNotification' = 'toggleVideoChatEnabledStartNotification';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** enabled_start_notification: Bool — New value of the enabled_start_notification setting */
  enabled_start_notification: boolean = false;
}

/** Decode a TDLib `toggleVideoChatEnabledStartNotification` object; null input -> null. */
export function decodeToggleVideoChatEnabledStartNotification(json: TdJsonRaw): ToggleVideoChatEnabledStartNotification | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleVideoChatEnabledStartNotification = new ToggleVideoChatEnabledStartNotification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.enabled_start_notification = asBool(record['enabled_start_notification']);
  return out;
}

/** Encode a `toggleVideoChatEnabledStartNotification` into TDLib JSON wire shape. */
export function encodeToggleVideoChatEnabledStartNotification(value: ToggleVideoChatEnabledStartNotification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleVideoChatEnabledStartNotification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['enabled_start_notification'] = value.enabled_start_notification;
  return out;
}

/** toggleVideoChatMuteNewParticipants = Ok — Toggles whether new participants of a video chat can be unmuted only by administrators of the video chat. Requires groupCall.can_toggle_mute_new_participants right */
export class ToggleVideoChatMuteNewParticipants {
  type: 'toggleVideoChatMuteNewParticipants' = 'toggleVideoChatMuteNewParticipants';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** mute_new_participants: Bool — New value of the mute_new_participants setting */
  mute_new_participants: boolean = false;
}

/** Decode a TDLib `toggleVideoChatMuteNewParticipants` object; null input -> null. */
export function decodeToggleVideoChatMuteNewParticipants(json: TdJsonRaw): ToggleVideoChatMuteNewParticipants | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ToggleVideoChatMuteNewParticipants = new ToggleVideoChatMuteNewParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.mute_new_participants = asBool(record['mute_new_participants']);
  return out;
}

/** Encode a `toggleVideoChatMuteNewParticipants` into TDLib JSON wire shape. */
export function encodeToggleVideoChatMuteNewParticipants(value: ToggleVideoChatMuteNewParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'toggleVideoChatMuteNewParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['mute_new_participants'] = value.mute_new_participants;
  return out;
}

/** tonTransaction = TonTransaction — Represents a transaction changing the amount of owned TON Grams */
export class TonTransaction {
  type: 'tonTransaction' = 'tonTransaction';
  extra?: string;
  /** id: string — Unique identifier of the transaction */
  id: string = '';
  /** gram_amount: int53 — The amount of added owned Grams, in the smallest units of the cryptocurrency; negative for outgoing transactions */
  gram_amount: number = 0;
  /** is_refund: Bool — True, if the transaction is a refund of a previous transaction */
  is_refund: boolean = false;
  /** date: int32 — Point in time (Unix timestamp) when the transaction was completed */
  date: number = 0;
  /** type: TonTransactionType — Type of the transaction */
  type_: TdTonTransactionType | null = null;
}

/** Decode a TDLib `tonTransaction` object; null input -> null. */
export function decodeTonTransaction(json: TdJsonRaw): TonTransaction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransaction = new TonTransaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.gram_amount = asNumber(record['gram_amount']);
  out.is_refund = asBool(record['is_refund']);
  out.date = asNumber(record['date']);
  out.type_ = decodeTdTonTransactionType(record['type']);
  return out;
}

/** Encode a `tonTransaction` into TDLib JSON wire shape. */
export function encodeTonTransaction(value: TonTransaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['gram_amount'] = value.gram_amount;
  out['is_refund'] = value.is_refund;
  out['date'] = value.date;
  out['type'] = value.type_ === null ? null : encodeTdTonTransactionType(value.type_);
  return out;
}

/** tonTransactionTypeFragmentDeposit = TonTransactionType — The transaction is a deposit of Grams from Fragment */
export class TonTransactionTypeFragmentDeposit {
  type: 'tonTransactionTypeFragmentDeposit' = 'tonTransactionTypeFragmentDeposit';
  extra?: string;
  /** is_gift: Bool — True, if the transaction is a gift from another user */
  is_gift: boolean = false;
  /** sticker: sticker — The sticker to be shown in the transaction information; may be null if unknown */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `tonTransactionTypeFragmentDeposit` object; null input -> null. */
export function decodeTonTransactionTypeFragmentDeposit(json: TdJsonRaw): TonTransactionTypeFragmentDeposit | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactionTypeFragmentDeposit = new TonTransactionTypeFragmentDeposit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_gift = asBool(record['is_gift']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `tonTransactionTypeFragmentDeposit` into TDLib JSON wire shape. */
export function encodeTonTransactionTypeFragmentDeposit(value: TonTransactionTypeFragmentDeposit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactionTypeFragmentDeposit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_gift'] = value.is_gift;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** tonTransactionTypeFragmentWithdrawal = TonTransactionType — The transaction is a withdrawal of earned Grams to Fragment */
export class TonTransactionTypeFragmentWithdrawal {
  type: 'tonTransactionTypeFragmentWithdrawal' = 'tonTransactionTypeFragmentWithdrawal';
  extra?: string;
  /** withdrawal_state: RevenueWithdrawalState — State of the withdrawal; may be null for refunds from Fragment */
  withdrawal_state: TdRevenueWithdrawalState | null = null;
}

/** Decode a TDLib `tonTransactionTypeFragmentWithdrawal` object; null input -> null. */
export function decodeTonTransactionTypeFragmentWithdrawal(json: TdJsonRaw): TonTransactionTypeFragmentWithdrawal | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactionTypeFragmentWithdrawal = new TonTransactionTypeFragmentWithdrawal();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.withdrawal_state = decodeTdRevenueWithdrawalState(record['withdrawal_state']);
  return out;
}

/** Encode a `tonTransactionTypeFragmentWithdrawal` into TDLib JSON wire shape. */
export function encodeTonTransactionTypeFragmentWithdrawal(value: TonTransactionTypeFragmentWithdrawal): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactionTypeFragmentWithdrawal';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['withdrawal_state'] = value.withdrawal_state === null ? null : encodeTdRevenueWithdrawalState(value.withdrawal_state);
  return out;
}

/** tonTransactionTypeGiftPurchaseOffer = TonTransactionType — The transaction is an offer of gift purchase */
export class TonTransactionTypeGiftPurchaseOffer {
  type: 'tonTransactionTypeGiftPurchaseOffer' = 'tonTransactionTypeGiftPurchaseOffer';
  extra?: string;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
}

/** Decode a TDLib `tonTransactionTypeGiftPurchaseOffer` object; null input -> null. */
export function decodeTonTransactionTypeGiftPurchaseOffer(json: TdJsonRaw): TonTransactionTypeGiftPurchaseOffer | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactionTypeGiftPurchaseOffer = new TonTransactionTypeGiftPurchaseOffer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  return out;
}

/** Encode a `tonTransactionTypeGiftPurchaseOffer` into TDLib JSON wire shape. */
export function encodeTonTransactionTypeGiftPurchaseOffer(value: TonTransactionTypeGiftPurchaseOffer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactionTypeGiftPurchaseOffer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  return out;
}

/** tonTransactionTypeStakeDicePayout = TonTransactionType — The transaction is a payment for successful stake dice throw */
export class TonTransactionTypeStakeDicePayout {
  type: 'tonTransactionTypeStakeDicePayout' = 'tonTransactionTypeStakeDicePayout';
  extra?: string;
}

/** Decode a TDLib `tonTransactionTypeStakeDicePayout` object; null input -> null. */
export function decodeTonTransactionTypeStakeDicePayout(json: TdJsonRaw): TonTransactionTypeStakeDicePayout | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactionTypeStakeDicePayout = new TonTransactionTypeStakeDicePayout();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `tonTransactionTypeStakeDicePayout` into TDLib JSON wire shape. */
export function encodeTonTransactionTypeStakeDicePayout(value: TonTransactionTypeStakeDicePayout): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactionTypeStakeDicePayout';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** tonTransactionTypeStakeDiceStake = TonTransactionType — The transaction is a payment for stake dice throw */
export class TonTransactionTypeStakeDiceStake {
  type: 'tonTransactionTypeStakeDiceStake' = 'tonTransactionTypeStakeDiceStake';
  extra?: string;
}

/** Decode a TDLib `tonTransactionTypeStakeDiceStake` object; null input -> null. */
export function decodeTonTransactionTypeStakeDiceStake(json: TdJsonRaw): TonTransactionTypeStakeDiceStake | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactionTypeStakeDiceStake = new TonTransactionTypeStakeDiceStake();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `tonTransactionTypeStakeDiceStake` into TDLib JSON wire shape. */
export function encodeTonTransactionTypeStakeDiceStake(value: TonTransactionTypeStakeDiceStake): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactionTypeStakeDiceStake';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** tonTransactionTypeSuggestedPostPayment = TonTransactionType — The transaction is a payment for a suggested post */
export class TonTransactionTypeSuggestedPostPayment {
  type: 'tonTransactionTypeSuggestedPostPayment' = 'tonTransactionTypeSuggestedPostPayment';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat that posted the post */
  chat_id: number = 0;
}

/** Decode a TDLib `tonTransactionTypeSuggestedPostPayment` object; null input -> null. */
export function decodeTonTransactionTypeSuggestedPostPayment(json: TdJsonRaw): TonTransactionTypeSuggestedPostPayment | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactionTypeSuggestedPostPayment = new TonTransactionTypeSuggestedPostPayment();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `tonTransactionTypeSuggestedPostPayment` into TDLib JSON wire shape. */
export function encodeTonTransactionTypeSuggestedPostPayment(value: TonTransactionTypeSuggestedPostPayment): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactionTypeSuggestedPostPayment';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** tonTransactionTypeUnsupported = TonTransactionType — The transaction is a transaction of an unsupported type */
export class TonTransactionTypeUnsupported {
  type: 'tonTransactionTypeUnsupported' = 'tonTransactionTypeUnsupported';
  extra?: string;
}

/** Decode a TDLib `tonTransactionTypeUnsupported` object; null input -> null. */
export function decodeTonTransactionTypeUnsupported(json: TdJsonRaw): TonTransactionTypeUnsupported | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactionTypeUnsupported = new TonTransactionTypeUnsupported();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `tonTransactionTypeUnsupported` into TDLib JSON wire shape. */
export function encodeTonTransactionTypeUnsupported(value: TonTransactionTypeUnsupported): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactionTypeUnsupported';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** tonTransactionTypeUpgradedGiftPurchase = TonTransactionType — The transaction is a purchase of an upgraded gift for some user or channel */
export class TonTransactionTypeUpgradedGiftPurchase {
  type: 'tonTransactionTypeUpgradedGiftPurchase' = 'tonTransactionTypeUpgradedGiftPurchase';
  extra?: string;
  /** user_id: int53 — Identifier of the user who sold the gift */
  user_id: number = 0;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
}

/** Decode a TDLib `tonTransactionTypeUpgradedGiftPurchase` object; null input -> null. */
export function decodeTonTransactionTypeUpgradedGiftPurchase(json: TdJsonRaw): TonTransactionTypeUpgradedGiftPurchase | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactionTypeUpgradedGiftPurchase = new TonTransactionTypeUpgradedGiftPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.gift = decodeUpgradedGift(record['gift']);
  return out;
}

/** Encode a `tonTransactionTypeUpgradedGiftPurchase` into TDLib JSON wire shape. */
export function encodeTonTransactionTypeUpgradedGiftPurchase(value: TonTransactionTypeUpgradedGiftPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactionTypeUpgradedGiftPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  return out;
}

/** tonTransactionTypeUpgradedGiftSale = TonTransactionType — The transaction is a sale of an upgraded gift */
export class TonTransactionTypeUpgradedGiftSale {
  type: 'tonTransactionTypeUpgradedGiftSale' = 'tonTransactionTypeUpgradedGiftSale';
  extra?: string;
  /** user_id: int53 — Identifier of the user who bought the gift */
  user_id: number = 0;
  /** gift: upgradedGift — The gift */
  gift: UpgradedGift | null = null;
  /** commission_per_mille: int32 — The number of Grams received by the Telegram for each 1000 Grams received by the seller of the gift */
  commission_per_mille: number = 0;
  /** commission_gram_amount: int53 — The Gram amount that was received by the Telegram; in the smallest units of the currency */
  commission_gram_amount: number = 0;
  /** via_offer: Bool — True, if the gift was sold through a purchase offer */
  via_offer: boolean = false;
}

/** Decode a TDLib `tonTransactionTypeUpgradedGiftSale` object; null input -> null. */
export function decodeTonTransactionTypeUpgradedGiftSale(json: TdJsonRaw): TonTransactionTypeUpgradedGiftSale | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactionTypeUpgradedGiftSale = new TonTransactionTypeUpgradedGiftSale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.gift = decodeUpgradedGift(record['gift']);
  out.commission_per_mille = asNumber(record['commission_per_mille']);
  out.commission_gram_amount = asNumber(record['commission_gram_amount']);
  out.via_offer = asBool(record['via_offer']);
  return out;
}

/** Encode a `tonTransactionTypeUpgradedGiftSale` into TDLib JSON wire shape. */
export function encodeTonTransactionTypeUpgradedGiftSale(value: TonTransactionTypeUpgradedGiftSale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactionTypeUpgradedGiftSale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  out['commission_per_mille'] = value.commission_per_mille;
  out['commission_gram_amount'] = value.commission_gram_amount;
  out['via_offer'] = value.via_offer;
  return out;
}

/** tonTransactions = TonTransactions — Represents a list of TON Gram transactions */
export class TonTransactions {
  type: 'tonTransactions' = 'tonTransactions';
  extra?: string;
  /** gram_amount: int53 — The total amount of owned Grams, in the smallest units of the cryptocurrency */
  gram_amount: number = 0;
  /** transactions: vector<tonTransaction> — List of Gram transactions */
  transactions: TonTransaction[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `tonTransactions` object; null input -> null. */
export function decodeTonTransactions(json: TdJsonRaw): TonTransactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TonTransactions = new TonTransactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gram_amount = asNumber(record['gram_amount']);
  out.transactions = decodeVector(record['transactions'], (item: TdJsonRaw): TonTransaction => decodeTonTransaction(item) as TonTransaction);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `tonTransactions` into TDLib JSON wire shape. */
export function encodeTonTransactions(value: TonTransactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'tonTransactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gram_amount'] = value.gram_amount;
  out['transactions'] = encodeVector(value.transactions, (item: TonTransaction | null): TdJsonRaw => item === null ? null : encodeTonTransaction(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** topChatCategoryBots = TopChatCategory — A category containing frequently used private chats with bot users */
export class TopChatCategoryBots {
  type: 'topChatCategoryBots' = 'topChatCategoryBots';
  extra?: string;
}

/** Decode a TDLib `topChatCategoryBots` object; null input -> null. */
export function decodeTopChatCategoryBots(json: TdJsonRaw): TopChatCategoryBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TopChatCategoryBots = new TopChatCategoryBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `topChatCategoryBots` into TDLib JSON wire shape. */
export function encodeTopChatCategoryBots(value: TopChatCategoryBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'topChatCategoryBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** topChatCategoryCalls = TopChatCategory — A category containing frequently used chats used for calls */
export class TopChatCategoryCalls {
  type: 'topChatCategoryCalls' = 'topChatCategoryCalls';
  extra?: string;
}

/** Decode a TDLib `topChatCategoryCalls` object; null input -> null. */
export function decodeTopChatCategoryCalls(json: TdJsonRaw): TopChatCategoryCalls | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TopChatCategoryCalls = new TopChatCategoryCalls();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `topChatCategoryCalls` into TDLib JSON wire shape. */
export function encodeTopChatCategoryCalls(value: TopChatCategoryCalls): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'topChatCategoryCalls';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** topChatCategoryChannels = TopChatCategory — A category containing frequently used channels */
export class TopChatCategoryChannels {
  type: 'topChatCategoryChannels' = 'topChatCategoryChannels';
  extra?: string;
}

/** Decode a TDLib `topChatCategoryChannels` object; null input -> null. */
export function decodeTopChatCategoryChannels(json: TdJsonRaw): TopChatCategoryChannels | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TopChatCategoryChannels = new TopChatCategoryChannels();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `topChatCategoryChannels` into TDLib JSON wire shape. */
export function encodeTopChatCategoryChannels(value: TopChatCategoryChannels): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'topChatCategoryChannels';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** topChatCategoryForwardChats = TopChatCategory — A category containing frequently used chats used to forward messages */
export class TopChatCategoryForwardChats {
  type: 'topChatCategoryForwardChats' = 'topChatCategoryForwardChats';
  extra?: string;
}

/** Decode a TDLib `topChatCategoryForwardChats` object; null input -> null. */
export function decodeTopChatCategoryForwardChats(json: TdJsonRaw): TopChatCategoryForwardChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TopChatCategoryForwardChats = new TopChatCategoryForwardChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `topChatCategoryForwardChats` into TDLib JSON wire shape. */
export function encodeTopChatCategoryForwardChats(value: TopChatCategoryForwardChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'topChatCategoryForwardChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** topChatCategoryGroups = TopChatCategory — A category containing frequently used basic groups and supergroups */
export class TopChatCategoryGroups {
  type: 'topChatCategoryGroups' = 'topChatCategoryGroups';
  extra?: string;
}

/** Decode a TDLib `topChatCategoryGroups` object; null input -> null. */
export function decodeTopChatCategoryGroups(json: TdJsonRaw): TopChatCategoryGroups | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TopChatCategoryGroups = new TopChatCategoryGroups();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `topChatCategoryGroups` into TDLib JSON wire shape. */
export function encodeTopChatCategoryGroups(value: TopChatCategoryGroups): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'topChatCategoryGroups';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** topChatCategoryGuestBots = TopChatCategory — A category containing frequently used chats with bots, which were used as guest bots */
export class TopChatCategoryGuestBots {
  type: 'topChatCategoryGuestBots' = 'topChatCategoryGuestBots';
  extra?: string;
}

/** Decode a TDLib `topChatCategoryGuestBots` object; null input -> null. */
export function decodeTopChatCategoryGuestBots(json: TdJsonRaw): TopChatCategoryGuestBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TopChatCategoryGuestBots = new TopChatCategoryGuestBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `topChatCategoryGuestBots` into TDLib JSON wire shape. */
export function encodeTopChatCategoryGuestBots(value: TopChatCategoryGuestBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'topChatCategoryGuestBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** topChatCategoryInlineBots = TopChatCategory — A category containing frequently used chats with inline bots sorted by their usage in inline mode */
export class TopChatCategoryInlineBots {
  type: 'topChatCategoryInlineBots' = 'topChatCategoryInlineBots';
  extra?: string;
}

/** Decode a TDLib `topChatCategoryInlineBots` object; null input -> null. */
export function decodeTopChatCategoryInlineBots(json: TdJsonRaw): TopChatCategoryInlineBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TopChatCategoryInlineBots = new TopChatCategoryInlineBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `topChatCategoryInlineBots` into TDLib JSON wire shape. */
export function encodeTopChatCategoryInlineBots(value: TopChatCategoryInlineBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'topChatCategoryInlineBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** topChatCategoryUsers = TopChatCategory — A category containing frequently used private chats with non-bot users */
export class TopChatCategoryUsers {
  type: 'topChatCategoryUsers' = 'topChatCategoryUsers';
  extra?: string;
}

/** Decode a TDLib `topChatCategoryUsers` object; null input -> null. */
export function decodeTopChatCategoryUsers(json: TdJsonRaw): TopChatCategoryUsers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TopChatCategoryUsers = new TopChatCategoryUsers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `topChatCategoryUsers` into TDLib JSON wire shape. */
export function encodeTopChatCategoryUsers(value: TopChatCategoryUsers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'topChatCategoryUsers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** topChatCategoryWebAppBots = TopChatCategory — A category containing frequently used chats with bots, which Web Apps were opened */
export class TopChatCategoryWebAppBots {
  type: 'topChatCategoryWebAppBots' = 'topChatCategoryWebAppBots';
  extra?: string;
}

/** Decode a TDLib `topChatCategoryWebAppBots` object; null input -> null. */
export function decodeTopChatCategoryWebAppBots(json: TdJsonRaw): TopChatCategoryWebAppBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TopChatCategoryWebAppBots = new TopChatCategoryWebAppBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `topChatCategoryWebAppBots` into TDLib JSON wire shape. */
export function encodeTopChatCategoryWebAppBots(value: TopChatCategoryWebAppBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'topChatCategoryWebAppBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** transactionDirectionIncoming = TransactionDirection — The transaction is incoming and increases the amount of owned currency */
export class TransactionDirectionIncoming {
  type: 'transactionDirectionIncoming' = 'transactionDirectionIncoming';
  extra?: string;
}

/** Decode a TDLib `transactionDirectionIncoming` object; null input -> null. */
export function decodeTransactionDirectionIncoming(json: TdJsonRaw): TransactionDirectionIncoming | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TransactionDirectionIncoming = new TransactionDirectionIncoming();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `transactionDirectionIncoming` into TDLib JSON wire shape. */
export function encodeTransactionDirectionIncoming(value: TransactionDirectionIncoming): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'transactionDirectionIncoming';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** transactionDirectionOutgoing = TransactionDirection — The transaction is outgoing and decreases the amount of owned currency */
export class TransactionDirectionOutgoing {
  type: 'transactionDirectionOutgoing' = 'transactionDirectionOutgoing';
  extra?: string;
}

/** Decode a TDLib `transactionDirectionOutgoing` object; null input -> null. */
export function decodeTransactionDirectionOutgoing(json: TdJsonRaw): TransactionDirectionOutgoing | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TransactionDirectionOutgoing = new TransactionDirectionOutgoing();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `transactionDirectionOutgoing` into TDLib JSON wire shape. */
export function encodeTransactionDirectionOutgoing(value: TransactionDirectionOutgoing): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'transactionDirectionOutgoing';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** transferBusinessAccountStars = Ok — Transfers Telegram Stars from the business account to the business bot; for bots only */
export class TransferBusinessAccountStars {
  type: 'transferBusinessAccountStars' = 'transferBusinessAccountStars';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection */
  business_connection_id: string = '';
  /** star_count: int53 — Number of Telegram Stars to transfer */
  star_count: number = 0;
}

/** Decode a TDLib `transferBusinessAccountStars` object; null input -> null. */
export function decodeTransferBusinessAccountStars(json: TdJsonRaw): TransferBusinessAccountStars | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TransferBusinessAccountStars = new TransferBusinessAccountStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `transferBusinessAccountStars` into TDLib JSON wire shape. */
export function encodeTransferBusinessAccountStars(value: TransferBusinessAccountStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'transferBusinessAccountStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['star_count'] = value.star_count;
  return out;
}

/** transferChatOwnership = Ok — Changes the owner of a chat; for basic groups, supergroups and channel chats only; requires owner privileges in the chat. Use the method canTransferOwnership to check whether the ownership can be transferred ... */
export class TransferChatOwnership {
  type: 'transferChatOwnership' = 'transferChatOwnership';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** user_id: int53 — Identifier of the user to which transfer the ownership. The ownership can't be transferred to a bot or to a deleted user */
  user_id: number = 0;
  /** password: string — The 2-step verification password of the current user */
  password: string = '';
}

/** Decode a TDLib `transferChatOwnership` object; null input -> null. */
export function decodeTransferChatOwnership(json: TdJsonRaw): TransferChatOwnership | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TransferChatOwnership = new TransferChatOwnership();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.user_id = asNumber(record['user_id']);
  out.password = asString(record['password']);
  return out;
}

/** Encode a `transferChatOwnership` into TDLib JSON wire shape. */
export function encodeTransferChatOwnership(value: TransferChatOwnership): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'transferChatOwnership';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['user_id'] = value.user_id;
  out['password'] = value.password;
  return out;
}

/** transferGift = Ok — Sends an upgraded gift to another user or channel chat */
export class TransferGift {
  type: 'transferGift' = 'transferGift';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which to send the request; for bots only */
  business_connection_id: string = '';
  /** received_gift_id: string — Identifier of the gift */
  received_gift_id: string = '';
  /** new_owner_id: MessageSender — Identifier of the user or the channel chat that will receive the gift */
  new_owner_id: TdMessageSender | null = null;
  /** star_count: int53 — The Telegram Star amount required to pay for the transfer */
  star_count: number = 0;
}

/** Decode a TDLib `transferGift` object; null input -> null. */
export function decodeTransferGift(json: TdJsonRaw): TransferGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TransferGift = new TransferGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.received_gift_id = asString(record['received_gift_id']);
  out.new_owner_id = decodeTdMessageSender(record['new_owner_id']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `transferGift` into TDLib JSON wire shape. */
export function encodeTransferGift(value: TransferGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'transferGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['received_gift_id'] = value.received_gift_id;
  out['new_owner_id'] = value.new_owner_id === null ? null : encodeTdMessageSender(value.new_owner_id);
  out['star_count'] = value.star_count;
  return out;
}

/** translateMessageRichMessage = RichMessage — Extracts rich message of the given message and translates it to the given language */
export class TranslateMessageRichMessage {
  type: 'translateMessageRichMessage' = 'translateMessageRichMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** to_language_code: string — Language code of the language to which the message is translated. See translateText.to_language_code for the list of supported values */
  to_language_code: string = '';
  /** tone: string — Tone of the translation; see translateText.tone for the list of supported values */
  tone: string = '';
}

/** Decode a TDLib `translateMessageRichMessage` object; null input -> null. */
export function decodeTranslateMessageRichMessage(json: TdJsonRaw): TranslateMessageRichMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TranslateMessageRichMessage = new TranslateMessageRichMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.to_language_code = asString(record['to_language_code']);
  out.tone = asString(record['tone']);
  return out;
}

/** Encode a `translateMessageRichMessage` into TDLib JSON wire shape. */
export function encodeTranslateMessageRichMessage(value: TranslateMessageRichMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'translateMessageRichMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['to_language_code'] = value.to_language_code;
  out['tone'] = value.tone;
  return out;
}

/** translateMessageText = FormattedText — Extracts text or caption of the given message and translates it to the given language; must not be used in secret chats. If the current user is a Telegram Premium user, then text formatting is prese... */
export class TranslateMessageText {
  type: 'translateMessageText' = 'translateMessageText';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** to_language_code: string — Language code of the language to which the message is translated. See translateText.to_language_code for the list of supported values */
  to_language_code: string = '';
  /** tone: string — Tone of the translation; see translateText.tone for the list of supported values */
  tone: string = '';
}

/** Decode a TDLib `translateMessageText` object; null input -> null. */
export function decodeTranslateMessageText(json: TdJsonRaw): TranslateMessageText | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TranslateMessageText = new TranslateMessageText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.to_language_code = asString(record['to_language_code']);
  out.tone = asString(record['tone']);
  return out;
}

/** Encode a `translateMessageText` into TDLib JSON wire shape. */
export function encodeTranslateMessageText(value: TranslateMessageText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'translateMessageText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['to_language_code'] = value.to_language_code;
  out['tone'] = value.tone;
  return out;
}

/** translateRichMessage = RichMessage — Translates a rich message to the given language */
export class TranslateRichMessage {
  type: 'translateRichMessage' = 'translateRichMessage';
  extra?: string;
  /** message: inputRichMessage — Rich message to translate */
  message: InputRichMessage | null = null;
  /** to_language_code: string — Language code of the language to which the message is translated. See translateText.to_language_code for the list of supported values */
  to_language_code: string = '';
  /** tone: string — Tone of the translation; see translateText.tone for the list of supported values */
  tone: string = '';
}

/** Decode a TDLib `translateRichMessage` object; null input -> null. */
export function decodeTranslateRichMessage(json: TdJsonRaw): TranslateRichMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TranslateRichMessage = new TranslateRichMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeInputRichMessage(record['message']);
  out.to_language_code = asString(record['to_language_code']);
  out.tone = asString(record['tone']);
  return out;
}

/** Encode a `translateRichMessage` into TDLib JSON wire shape. */
export function encodeTranslateRichMessage(value: TranslateRichMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'translateRichMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeInputRichMessage(value.message);
  out['to_language_code'] = value.to_language_code;
  out['tone'] = value.tone;
  return out;
}

/** translateText = FormattedText — Translates a text to the given language; must not be used in secret chats. If the current user is a Telegram Premium user, then text formatting is preserved "af", "sq", "am", "ar", "hy", "az", "eu", "be", ... */
export class TranslateText {
  type: 'translateText' = 'translateText';
  extra?: string;
  /** text: formattedText — Text to translate */
  text: FormattedText | null = null;
  /** to_language_code: string — Language code of the language to which the message is translated. Must be one of */
  to_language_code: string = '';
  /** tone: string — Tone of the translation; must be one of "", "formal", "neutral", "casual"; defaults to "neutral" */
  tone: string = '';
}

/** Decode a TDLib `translateText` object; null input -> null. */
export function decodeTranslateText(json: TdJsonRaw): TranslateText | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TranslateText = new TranslateText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  out.to_language_code = asString(record['to_language_code']);
  out.tone = asString(record['tone']);
  return out;
}

/** Encode a `translateText` into TDLib JSON wire shape. */
export function encodeTranslateText(value: TranslateText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'translateText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['to_language_code'] = value.to_language_code;
  out['tone'] = value.tone;
  return out;
}

/** trendingStickerSets = TrendingStickerSets — Represents a list of trending sticker sets */
export class TrendingStickerSets {
  type: 'trendingStickerSets' = 'trendingStickerSets';
  extra?: string;
  /** total_count: int32 — Approximate total number of trending sticker sets */
  total_count: number = 0;
  /** sets: vector<stickerSetInfo> — List of trending sticker sets */
  sets: StickerSetInfo[] = [];
  /** is_premium: Bool — True, if the list contains sticker sets with premium stickers */
  is_premium: boolean = false;
}

/** Decode a TDLib `trendingStickerSets` object; null input -> null. */
export function decodeTrendingStickerSets(json: TdJsonRaw): TrendingStickerSets | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: TrendingStickerSets = new TrendingStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.sets = decodeVector(record['sets'], (item: TdJsonRaw): StickerSetInfo => decodeStickerSetInfo(item) as StickerSetInfo);
  out.is_premium = asBool(record['is_premium']);
  return out;
}

/** Encode a `trendingStickerSets` into TDLib JSON wire shape. */
export function encodeTrendingStickerSets(value: TrendingStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'trendingStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['sets'] = encodeVector(value.sets, (item: StickerSetInfo | null): TdJsonRaw => item === null ? null : encodeStickerSetInfo(item));
  out['is_premium'] = value.is_premium;
  return out;
}
