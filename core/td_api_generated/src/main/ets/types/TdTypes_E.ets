/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { ChatFolder, decodeChatFolder, encodeChatFolder } from './TdTypes_C';
import { DatedFile, decodeDatedFile, encodeDatedFile } from './TdTypes_D';
import { FormattedText, decodeFormattedText, encodeFormattedText } from './TdTypes_F';
import { InputBusinessChatLink, InputChecklist, InputStoryAreas, decodeInputBusinessChatLink, decodeInputChecklist, decodeInputStoryAreas, encodeInputBusinessChatLink, encodeInputChecklist, encodeInputStoryAreas } from './TdTypes_I';
import { LanguagePackInfo, LiveLocation, decodeLanguagePackInfo, decodeLiveLocation, encodeLanguagePackInfo, encodeLiveLocation } from './TdTypes_L';
import { ProxyValue, decodeProxyValue, encodeProxyValue } from './TdTypes_P';
import { Sticker, decodeSticker, encodeSticker } from './TdTypes_S';
import { ThemeSettings, decodeThemeSettings, encodeThemeSettings } from './TdTypes_T';
import { UpgradedGiftBackdropColors, decodeUpgradedGiftBackdropColors, encodeUpgradedGiftBackdropColors } from './TdTypes_U';
import { TdEmojiCategorySource, TdEmojiStatusType, TdGroupCallDataChannel, TdInputMessageContent, TdInputStoryContent, TdMessageContent, TdMessageSchedulingState, TdPassportElementType, TdReplyMarkup, TdStoryPrivacySettings, decodeTdEmojiCategorySource, decodeTdEmojiStatusType, decodeTdGroupCallDataChannel, decodeTdInputMessageContent, decodeTdInputStoryContent, decodeTdMessageContent, decodeTdMessageSchedulingState, decodeTdPassportElementType, decodeTdReplyMarkup, decodeTdStoryPrivacySettings, encodeTdEmojiCategorySource, encodeTdEmojiStatusType, encodeTdGroupCallDataChannel, encodeTdInputMessageContent, encodeTdInputStoryContent, encodeTdMessageContent, encodeTdMessageSchedulingState, encodeTdPassportElementType, encodeTdReplyMarkup, encodeTdStoryPrivacySettings } from './TdUnions';

/** editBotMediaPreview = BotMediaPreview — Replaces media preview in the list of media previews of a bot. Returns the new preview after edit is completed server-side */
export class EditBotMediaPreview {
  type: 'editBotMediaPreview' = 'editBotMediaPreview';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot. The bot must be owned and must have the main Web App */
  bot_user_id: number = 0;
  /** language_code: string — Language code of the media preview to edit */
  language_code: string = '';
  /** file_id: int32 — File identifier of the media to replace */
  file_id: number = 0;
  /** content: InputStoryContent — Content of the new preview */
  content: TdInputStoryContent | null = null;
}

/** Decode a TDLib `editBotMediaPreview` object; null input -> null. */
export function decodeEditBotMediaPreview(json: TdJsonRaw): EditBotMediaPreview | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditBotMediaPreview = new EditBotMediaPreview();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  out.file_id = asNumber(record['file_id']);
  out.content = decodeTdInputStoryContent(record['content']);
  return out;
}

/** Encode a `editBotMediaPreview` into TDLib JSON wire shape. */
export function encodeEditBotMediaPreview(value: EditBotMediaPreview): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editBotMediaPreview';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  out['file_id'] = value.file_id;
  out['content'] = value.content === null ? null : encodeTdInputStoryContent(value.content);
  return out;
}

/** editBusinessChatLink = BusinessChatLink — Edits a business chat link of the current account. Requires Telegram Business subscription. Returns the edited link */
export class EditBusinessChatLink {
  type: 'editBusinessChatLink' = 'editBusinessChatLink';
  extra?: string;
  /** link: string — The link to edit */
  link: string = '';
  /** link_info: inputBusinessChatLink — New description of the link */
  link_info: InputBusinessChatLink | null = null;
}

/** Decode a TDLib `editBusinessChatLink` object; null input -> null. */
export function decodeEditBusinessChatLink(json: TdJsonRaw): EditBusinessChatLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditBusinessChatLink = new EditBusinessChatLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  out.link_info = decodeInputBusinessChatLink(record['link_info']);
  return out;
}

/** Encode a `editBusinessChatLink` into TDLib JSON wire shape. */
export function encodeEditBusinessChatLink(value: EditBusinessChatLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editBusinessChatLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  out['link_info'] = value.link_info === null ? null : encodeInputBusinessChatLink(value.link_info);
  return out;
}

/** editBusinessMessageCaption = BusinessMessage — Edits the caption of a message sent on behalf of a business account; for bots only */
export class EditBusinessMessageCaption {
  type: 'editBusinessMessageCaption' = 'editBusinessMessageCaption';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which the message was sent */
  business_connection_id: string = '';
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** caption: formattedText — New message content caption; pass null to remove caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — Pass true to show the caption above the media; otherwise, the caption will be shown below the media. May be true only for animation, photo, and video messages */
  show_caption_above_media: boolean = false;
}

/** Decode a TDLib `editBusinessMessageCaption` object; null input -> null. */
export function decodeEditBusinessMessageCaption(json: TdJsonRaw): EditBusinessMessageCaption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditBusinessMessageCaption = new EditBusinessMessageCaption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  return out;
}

/** Encode a `editBusinessMessageCaption` into TDLib JSON wire shape. */
export function encodeEditBusinessMessageCaption(value: EditBusinessMessageCaption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editBusinessMessageCaption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  return out;
}

/** editBusinessMessageChecklist = BusinessMessage — Edits the content of a checklist in a message sent on behalf of a business account; for bots only */
export class EditBusinessMessageChecklist {
  type: 'editBusinessMessageChecklist' = 'editBusinessMessageChecklist';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which the message was sent */
  business_connection_id: string = '';
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** checklist: inputChecklist — The new checklist. If some tasks were completed, this information will be kept */
  checklist: InputChecklist | null = null;
}

/** Decode a TDLib `editBusinessMessageChecklist` object; null input -> null. */
export function decodeEditBusinessMessageChecklist(json: TdJsonRaw): EditBusinessMessageChecklist | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditBusinessMessageChecklist = new EditBusinessMessageChecklist();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.checklist = decodeInputChecklist(record['checklist']);
  return out;
}

/** Encode a `editBusinessMessageChecklist` into TDLib JSON wire shape. */
export function encodeEditBusinessMessageChecklist(value: EditBusinessMessageChecklist): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editBusinessMessageChecklist';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['checklist'] = value.checklist === null ? null : encodeInputChecklist(value.checklist);
  return out;
}

/** editBusinessMessageLiveLocation = BusinessMessage — Edits the content of a live location in a message sent on behalf of a business account; for bots only then it must not exceed the current live_period by more than a day, and the live lo... */
export class EditBusinessMessageLiveLocation {
  type: 'editBusinessMessageLiveLocation' = 'editBusinessMessageLiveLocation';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which the message was sent */
  business_connection_id: string = '';
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** location: liveLocation — New live location of the message; pass null to stop sharing the live location. If the new live_period isn't set to 0x7FFFFFFF, */
  location: LiveLocation | null = null;
}

/** Decode a TDLib `editBusinessMessageLiveLocation` object; null input -> null. */
export function decodeEditBusinessMessageLiveLocation(json: TdJsonRaw): EditBusinessMessageLiveLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditBusinessMessageLiveLocation = new EditBusinessMessageLiveLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.location = decodeLiveLocation(record['location']);
  return out;
}

/** Encode a `editBusinessMessageLiveLocation` into TDLib JSON wire shape. */
export function encodeEditBusinessMessageLiveLocation(value: EditBusinessMessageLiveLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editBusinessMessageLiveLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['location'] = value.location === null ? null : encodeLiveLocation(value.location);
  return out;
}

/** editBusinessMessageMedia = BusinessMessage — Edits the media content of a message with a text, an animation, an audio, a document, a photo or a video in a message sent on behalf of a business account; for bots only */
export class EditBusinessMessageMedia {
  type: 'editBusinessMessageMedia' = 'editBusinessMessageMedia';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which the message was sent */
  business_connection_id: string = '';
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — New content of the message. Must be one of the following types: inputMessageAnimation, inputMessageAudio, inputMessageDocument, inputMessagePhoto or inputMessageVideo */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editBusinessMessageMedia` object; null input -> null. */
export function decodeEditBusinessMessageMedia(json: TdJsonRaw): EditBusinessMessageMedia | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditBusinessMessageMedia = new EditBusinessMessageMedia();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editBusinessMessageMedia` into TDLib JSON wire shape. */
export function encodeEditBusinessMessageMedia(value: EditBusinessMessageMedia): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editBusinessMessageMedia';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editBusinessMessageReplyMarkup = BusinessMessage — Edits the reply markup of a message sent on behalf of a business account; for bots only */
export class EditBusinessMessageReplyMarkup {
  type: 'editBusinessMessageReplyMarkup' = 'editBusinessMessageReplyMarkup';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which the message was sent */
  business_connection_id: string = '';
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
}

/** Decode a TDLib `editBusinessMessageReplyMarkup` object; null input -> null. */
export function decodeEditBusinessMessageReplyMarkup(json: TdJsonRaw): EditBusinessMessageReplyMarkup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditBusinessMessageReplyMarkup = new EditBusinessMessageReplyMarkup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  return out;
}

/** Encode a `editBusinessMessageReplyMarkup` into TDLib JSON wire shape. */
export function encodeEditBusinessMessageReplyMarkup(value: EditBusinessMessageReplyMarkup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editBusinessMessageReplyMarkup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  return out;
}

/** editBusinessMessageText = BusinessMessage — Edits the text of a text or game message sent on behalf of a business account; for bots only */
export class EditBusinessMessageText {
  type: 'editBusinessMessageText' = 'editBusinessMessageText';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which the message was sent */
  business_connection_id: string = '';
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — New text content of the message. Must be of type inputMessageText or inputMessageRichMessage */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editBusinessMessageText` object; null input -> null. */
export function decodeEditBusinessMessageText(json: TdJsonRaw): EditBusinessMessageText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditBusinessMessageText = new EditBusinessMessageText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editBusinessMessageText` into TDLib JSON wire shape. */
export function encodeEditBusinessMessageText(value: EditBusinessMessageText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editBusinessMessageText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editBusinessStory = Story — Changes a story posted by the bot on behalf of a business account; for bots only */
export class EditBusinessStory {
  type: 'editBusinessStory' = 'editBusinessStory';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Identifier of the story to edit */
  story_id: number = 0;
  /** content: InputStoryContent — New content of the story */
  content: TdInputStoryContent | null = null;
  /** areas: inputStoryAreas — New clickable rectangle areas to be shown on the story media */
  areas: InputStoryAreas | null = null;
  /** caption: formattedText — New story caption */
  caption: FormattedText | null = null;
  /** privacy_settings: StoryPrivacySettings — The new privacy settings for the story */
  privacy_settings: TdStoryPrivacySettings | null = null;
}

/** Decode a TDLib `editBusinessStory` object; null input -> null. */
export function decodeEditBusinessStory(json: TdJsonRaw): EditBusinessStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditBusinessStory = new EditBusinessStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.content = decodeTdInputStoryContent(record['content']);
  out.areas = decodeInputStoryAreas(record['areas']);
  out.caption = decodeFormattedText(record['caption']);
  out.privacy_settings = decodeTdStoryPrivacySettings(record['privacy_settings']);
  return out;
}

/** Encode a `editBusinessStory` into TDLib JSON wire shape. */
export function encodeEditBusinessStory(value: EditBusinessStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editBusinessStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['content'] = value.content === null ? null : encodeTdInputStoryContent(value.content);
  out['areas'] = value.areas === null ? null : encodeInputStoryAreas(value.areas);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['privacy_settings'] = value.privacy_settings === null ? null : encodeTdStoryPrivacySettings(value.privacy_settings);
  return out;
}

/** editCallbackQueryMessage = Ok — Edits the message from which a callback query has originated with an ephemeral message; for bots only inputMessageAudio, inputMessageDocument, inputMessagePhoto, inputMessageRichMessage, inputMessageSticke... */
export class EditCallbackQueryMessage {
  type: 'editCallbackQueryMessage' = 'editCallbackQueryMessage';
  extra?: string;
  /** callback_query_id: int64 — Identifier of the callback query */
  callback_query_id: string = '';
  /** protect_content: Bool — Pass true if the content of the message must be protected from forwarding and saving */
  protect_content: boolean = false;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — New content of the message. Must be one of the following types: inputMessageText, inputMessageAnimation, */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editCallbackQueryMessage` object; null input -> null. */
export function decodeEditCallbackQueryMessage(json: TdJsonRaw): EditCallbackQueryMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditCallbackQueryMessage = new EditCallbackQueryMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.callback_query_id = asInt64(record['callback_query_id']);
  out.protect_content = asBool(record['protect_content']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editCallbackQueryMessage` into TDLib JSON wire shape. */
export function encodeEditCallbackQueryMessage(value: EditCallbackQueryMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editCallbackQueryMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['callback_query_id'] = value.callback_query_id;
  out['protect_content'] = value.protect_content;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editChatFolder = ChatFolderInfo — Edits existing chat folder. Returns information about the edited chat folder */
export class EditChatFolder {
  type: 'editChatFolder' = 'editChatFolder';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
  /** folder: chatFolder — The edited chat folder */
  folder: ChatFolder | null = null;
}

/** Decode a TDLib `editChatFolder` object; null input -> null. */
export function decodeEditChatFolder(json: TdJsonRaw): EditChatFolder | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditChatFolder = new EditChatFolder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  out.folder = decodeChatFolder(record['folder']);
  return out;
}

/** Encode a `editChatFolder` into TDLib JSON wire shape. */
export function encodeEditChatFolder(value: EditChatFolder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editChatFolder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  out['folder'] = value.folder === null ? null : encodeChatFolder(value.folder);
  return out;
}

/** editChatFolderInviteLink = ChatFolderInviteLink — Edits an invite link for a chat folder */
export class EditChatFolderInviteLink {
  type: 'editChatFolderInviteLink' = 'editChatFolderInviteLink';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
  /** invite_link: string — Invite link to be edited */
  invite_link: string = '';
  /** name: string — New name of the link; 0-32 characters */
  name: string = '';
  /** chat_ids: vector<int53> — New identifiers of chats to be accessible by the invite link. Use getChatsForChatFolderInviteLink to get suitable chats. Basic groups will be automatically converted to supergroups before link editing */
  chat_ids: number[] = [];
}

/** Decode a TDLib `editChatFolderInviteLink` object; null input -> null. */
export function decodeEditChatFolderInviteLink(json: TdJsonRaw): EditChatFolderInviteLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditChatFolderInviteLink = new EditChatFolderInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  out.invite_link = asString(record['invite_link']);
  out.name = asString(record['name']);
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `editChatFolderInviteLink` into TDLib JSON wire shape. */
export function encodeEditChatFolderInviteLink(value: EditChatFolderInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editChatFolderInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  out['invite_link'] = value.invite_link;
  out['name'] = value.name;
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** editChatInviteLink = ChatInviteLink — Edits a non-primary invite link for a chat. Available in basic groups, supergroups, and channels. If the link creates a subscription, then expiration_date, member_limit and creates_join_request must ... */
export class EditChatInviteLink {
  type: 'editChatInviteLink' = 'editChatInviteLink';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** invite_link: string — Invite link to be edited */
  invite_link: string = '';
  /** name: string — Invite link name; 0-32 characters */
  name: string = '';
  /** expiration_date: int32 — Point in time (Unix timestamp) when the link will expire; pass 0 if never */
  expiration_date: number = 0;
  /** member_limit: int32 — The maximum number of chat members that can join the chat via the link simultaneously; 0-99999; pass 0 if not limited */
  member_limit: number = 0;
  /** creates_join_request: Bool — Pass true if users joining the chat via the link need to be approved by chat administrators. In this case, member_limit must be 0 */
  creates_join_request: boolean = false;
}

/** Decode a TDLib `editChatInviteLink` object; null input -> null. */
export function decodeEditChatInviteLink(json: TdJsonRaw): EditChatInviteLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditChatInviteLink = new EditChatInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.invite_link = asString(record['invite_link']);
  out.name = asString(record['name']);
  out.expiration_date = asNumber(record['expiration_date']);
  out.member_limit = asNumber(record['member_limit']);
  out.creates_join_request = asBool(record['creates_join_request']);
  return out;
}

/** Encode a `editChatInviteLink` into TDLib JSON wire shape. */
export function encodeEditChatInviteLink(value: EditChatInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editChatInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['invite_link'] = value.invite_link;
  out['name'] = value.name;
  out['expiration_date'] = value.expiration_date;
  out['member_limit'] = value.member_limit;
  out['creates_join_request'] = value.creates_join_request;
  return out;
}

/** editChatSubscriptionInviteLink = ChatInviteLink — Edits a subscription invite link for a channel chat. Requires can_invite_users right in the chat for own links and owner privileges for other links */
export class EditChatSubscriptionInviteLink {
  type: 'editChatSubscriptionInviteLink' = 'editChatSubscriptionInviteLink';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** invite_link: string — Invite link to be edited */
  invite_link: string = '';
  /** name: string — Invite link name; 0-32 characters */
  name: string = '';
}

/** Decode a TDLib `editChatSubscriptionInviteLink` object; null input -> null. */
export function decodeEditChatSubscriptionInviteLink(json: TdJsonRaw): EditChatSubscriptionInviteLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditChatSubscriptionInviteLink = new EditChatSubscriptionInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.invite_link = asString(record['invite_link']);
  out.name = asString(record['name']);
  return out;
}

/** Encode a `editChatSubscriptionInviteLink` into TDLib JSON wire shape. */
export function encodeEditChatSubscriptionInviteLink(value: EditChatSubscriptionInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editChatSubscriptionInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['invite_link'] = value.invite_link;
  out['name'] = value.name;
  return out;
}

/** editChatWelcomeMessage = Ok — Edits a welcome message of a chat; requires can_send_welcome_messages administrator right in the chat inputMessageAudio, inputMessageDocument, inputMessagePhoto, inputMessageRichMessage, inputMessageSticker,... */
export class EditChatWelcomeMessage {
  type: 'editChatWelcomeMessage' = 'editChatWelcomeMessage';
  extra?: string;
  /** chat_id: int53 — The identifier of the chat */
  chat_id: number = 0;
  /** welcome_message_id: int32 — The identifier of the welcome message */
  welcome_message_id: number = 0;
  /** input_message_content: InputMessageContent — New content of the message. Must be one of the following types: inputMessageText, inputMessageAnimation, */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editChatWelcomeMessage` object; null input -> null. */
export function decodeEditChatWelcomeMessage(json: TdJsonRaw): EditChatWelcomeMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditChatWelcomeMessage = new EditChatWelcomeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.welcome_message_id = asNumber(record['welcome_message_id']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editChatWelcomeMessage` into TDLib JSON wire shape. */
export function encodeEditChatWelcomeMessage(value: EditChatWelcomeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editChatWelcomeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['welcome_message_id'] = value.welcome_message_id;
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editCustomLanguagePackInfo = Ok — Edits information about a custom local language pack in the current localization target. Can be called before authorization */
export class EditCustomLanguagePackInfo {
  type: 'editCustomLanguagePackInfo' = 'editCustomLanguagePackInfo';
  extra?: string;
  /** info: languagePackInfo — New information about the custom local language pack */
  info: LanguagePackInfo | null = null;
}

/** Decode a TDLib `editCustomLanguagePackInfo` object; null input -> null. */
export function decodeEditCustomLanguagePackInfo(json: TdJsonRaw): EditCustomLanguagePackInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditCustomLanguagePackInfo = new EditCustomLanguagePackInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.info = decodeLanguagePackInfo(record['info']);
  return out;
}

/** Encode a `editCustomLanguagePackInfo` into TDLib JSON wire shape. */
export function encodeEditCustomLanguagePackInfo(value: EditCustomLanguagePackInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editCustomLanguagePackInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['info'] = value.info === null ? null : encodeLanguagePackInfo(value.info);
  return out;
}

/** editEphemeralMessage = Ok — Edits the text, media, or reply markup of an ephemeral message sent by the bot; for bots only inputMessageAudio, inputMessageDocument, inputMessagePhoto, inputMessageRichMessage, inputMessageSticker, inputMess... */
export class EditEphemeralMessage {
  type: 'editEphemeralMessage' = 'editEphemeralMessage';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** receiver_user_id: int53 — Identifier of the user who received the message */
  receiver_user_id: number = 0;
  /** ephemeral_message_id: int32 — Identifier of the ephemeral message */
  ephemeral_message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — New content of the message; pass null to edit only reply markup. Must be one of the following types: inputMessageText, inputMessageAnimation, */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editEphemeralMessage` object; null input -> null. */
export function decodeEditEphemeralMessage(json: TdJsonRaw): EditEphemeralMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditEphemeralMessage = new EditEphemeralMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.receiver_user_id = asNumber(record['receiver_user_id']);
  out.ephemeral_message_id = asNumber(record['ephemeral_message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editEphemeralMessage` into TDLib JSON wire shape. */
export function encodeEditEphemeralMessage(value: EditEphemeralMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editEphemeralMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['receiver_user_id'] = value.receiver_user_id;
  out['ephemeral_message_id'] = value.ephemeral_message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editEphemeralMessageCaption = Ok — Edits the caption and reply markup of an ephemeral message sent by the bot; for bots only */
export class EditEphemeralMessageCaption {
  type: 'editEphemeralMessageCaption' = 'editEphemeralMessageCaption';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** receiver_user_id: int53 — Identifier of the user who received the message */
  receiver_user_id: number = 0;
  /** ephemeral_message_id: int32 — Identifier of the ephemeral message */
  ephemeral_message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** caption: formattedText — New message content caption; pass null to remove caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — Pass true to show the caption above the media; otherwise, the caption will be shown below the media. May be true only for animation, photo, and video messages */
  show_caption_above_media: boolean = false;
}

/** Decode a TDLib `editEphemeralMessageCaption` object; null input -> null. */
export function decodeEditEphemeralMessageCaption(json: TdJsonRaw): EditEphemeralMessageCaption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditEphemeralMessageCaption = new EditEphemeralMessageCaption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.receiver_user_id = asNumber(record['receiver_user_id']);
  out.ephemeral_message_id = asNumber(record['ephemeral_message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  return out;
}

/** Encode a `editEphemeralMessageCaption` into TDLib JSON wire shape. */
export function encodeEditEphemeralMessageCaption(value: EditEphemeralMessageCaption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editEphemeralMessageCaption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['receiver_user_id'] = value.receiver_user_id;
  out['ephemeral_message_id'] = value.ephemeral_message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  return out;
}

/** editForumTopic = Ok — Edits title and icon of a topic in a forum supergroup chat or a chat with a bot with topics; for supergroup chats requires can_manage_topics administrator right unless the user is creator of the topic */
export class EditForumTopic {
  type: 'editForumTopic' = 'editForumTopic';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier */
  forum_topic_id: number = 0;
  /** name: string — New name of the topic; 0-128 characters. If empty, the previous topic name is kept */
  name: string = '';
  /** edit_icon_custom_emoji: Bool — Pass true to edit the icon of the topic. Icon of the General topic can't be edited */
  edit_icon_custom_emoji: boolean = false;
  /** icon_custom_emoji_id: int64 — Identifier of the new custom emoji for topic icon; pass 0 to remove the custom emoji. Ignored if edit_icon_custom_emoji is false. Telegram Premium users can use any custom emoji, other users can use only a c... */
  icon_custom_emoji_id: string = '';
}

/** Decode a TDLib `editForumTopic` object; null input -> null. */
export function decodeEditForumTopic(json: TdJsonRaw): EditForumTopic | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditForumTopic = new EditForumTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.name = asString(record['name']);
  out.edit_icon_custom_emoji = asBool(record['edit_icon_custom_emoji']);
  out.icon_custom_emoji_id = asInt64(record['icon_custom_emoji_id']);
  return out;
}

/** Encode a `editForumTopic` into TDLib JSON wire shape. */
export function encodeEditForumTopic(value: EditForumTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editForumTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['name'] = value.name;
  out['edit_icon_custom_emoji'] = value.edit_icon_custom_emoji;
  out['icon_custom_emoji_id'] = value.icon_custom_emoji_id;
  return out;
}

/** editInlineMessageCaption = Ok — Edits the caption of an inline message sent via a bot; for bots only */
export class EditInlineMessageCaption {
  type: 'editInlineMessageCaption' = 'editInlineMessageCaption';
  extra?: string;
  /** inline_message_id: string — Inline message identifier */
  inline_message_id: string = '';
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** caption: formattedText — New message content caption; pass null to remove caption; 0-getOption("message_caption_length_max") characters */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — Pass true to show the caption above the media; otherwise, the caption will be shown below the media. May be true only for animation, photo, and video messages */
  show_caption_above_media: boolean = false;
}

/** Decode a TDLib `editInlineMessageCaption` object; null input -> null. */
export function decodeEditInlineMessageCaption(json: TdJsonRaw): EditInlineMessageCaption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditInlineMessageCaption = new EditInlineMessageCaption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_message_id = asString(record['inline_message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  return out;
}

/** Encode a `editInlineMessageCaption` into TDLib JSON wire shape. */
export function encodeEditInlineMessageCaption(value: EditInlineMessageCaption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editInlineMessageCaption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_message_id'] = value.inline_message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  return out;
}

/** editInlineMessageLiveLocation = Ok — Edits the content of a live location in an inline message sent via a bot; for bots only then it must not exceed the current live_period by more than a day, and the live location expiration date must r... */
export class EditInlineMessageLiveLocation {
  type: 'editInlineMessageLiveLocation' = 'editInlineMessageLiveLocation';
  extra?: string;
  /** inline_message_id: string — Inline message identifier */
  inline_message_id: string = '';
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** location: liveLocation — New live location of the message; pass null to stop sharing the live location. If the new live_period isn't set to 0x7FFFFFFF, */
  location: LiveLocation | null = null;
}

/** Decode a TDLib `editInlineMessageLiveLocation` object; null input -> null. */
export function decodeEditInlineMessageLiveLocation(json: TdJsonRaw): EditInlineMessageLiveLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditInlineMessageLiveLocation = new EditInlineMessageLiveLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_message_id = asString(record['inline_message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.location = decodeLiveLocation(record['location']);
  return out;
}

/** Encode a `editInlineMessageLiveLocation` into TDLib JSON wire shape. */
export function encodeEditInlineMessageLiveLocation(value: EditInlineMessageLiveLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editInlineMessageLiveLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_message_id'] = value.inline_message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['location'] = value.location === null ? null : encodeLiveLocation(value.location);
  return out;
}

/** editInlineMessageMedia = Ok — Edits the media content of a message with a text, an animation, an audio, a document, a photo or a video in an inline message sent via a bot; for bots only */
export class EditInlineMessageMedia {
  type: 'editInlineMessageMedia' = 'editInlineMessageMedia';
  extra?: string;
  /** inline_message_id: string — Inline message identifier */
  inline_message_id: string = '';
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — New content of the message. Must be one of the following types: inputMessageAnimation, inputMessageAudio, inputMessageDocument, inputMessagePhoto or inputMessageVideo */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editInlineMessageMedia` object; null input -> null. */
export function decodeEditInlineMessageMedia(json: TdJsonRaw): EditInlineMessageMedia | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditInlineMessageMedia = new EditInlineMessageMedia();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_message_id = asString(record['inline_message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editInlineMessageMedia` into TDLib JSON wire shape. */
export function encodeEditInlineMessageMedia(value: EditInlineMessageMedia): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editInlineMessageMedia';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_message_id'] = value.inline_message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editInlineMessageReplyMarkup = Ok — Edits the reply markup of an inline message sent via a bot; for bots only */
export class EditInlineMessageReplyMarkup {
  type: 'editInlineMessageReplyMarkup' = 'editInlineMessageReplyMarkup';
  extra?: string;
  /** inline_message_id: string — Inline message identifier */
  inline_message_id: string = '';
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
}

/** Decode a TDLib `editInlineMessageReplyMarkup` object; null input -> null. */
export function decodeEditInlineMessageReplyMarkup(json: TdJsonRaw): EditInlineMessageReplyMarkup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditInlineMessageReplyMarkup = new EditInlineMessageReplyMarkup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_message_id = asString(record['inline_message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  return out;
}

/** Encode a `editInlineMessageReplyMarkup` into TDLib JSON wire shape. */
export function encodeEditInlineMessageReplyMarkup(value: EditInlineMessageReplyMarkup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editInlineMessageReplyMarkup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_message_id'] = value.inline_message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  return out;
}

/** editInlineMessageText = Ok — Edits the text of an inline text or game message sent via the bot; for bots only */
export class EditInlineMessageText {
  type: 'editInlineMessageText' = 'editInlineMessageText';
  extra?: string;
  /** inline_message_id: string — Inline message identifier */
  inline_message_id: string = '';
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — New text content of the message. Must be of type inputMessageText or inputMessageRichMessage; file upload isn't supported */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editInlineMessageText` object; null input -> null. */
export function decodeEditInlineMessageText(json: TdJsonRaw): EditInlineMessageText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditInlineMessageText = new EditInlineMessageText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_message_id = asString(record['inline_message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editInlineMessageText` into TDLib JSON wire shape. */
export function encodeEditInlineMessageText(value: EditInlineMessageText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editInlineMessageText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_message_id'] = value.inline_message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editMessageCaption = Message — Edits the message content caption. Returns the edited message after the edit is completed on the server side */
export class EditMessageCaption {
  type: 'editMessageCaption' = 'editMessageCaption';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use messageProperties.can_be_edited to check whether the message can be edited */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
  /** caption: formattedText — New message content caption; 0-getOption("message_caption_length_max") characters; pass null to remove caption */
  caption: FormattedText | null = null;
  /** show_caption_above_media: Bool — Pass true to show the caption above the media; otherwise, the caption will be shown below the media. May be true only for animation, photo, and video messages */
  show_caption_above_media: boolean = false;
}

/** Decode a TDLib `editMessageCaption` object; null input -> null. */
export function decodeEditMessageCaption(json: TdJsonRaw): EditMessageCaption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditMessageCaption = new EditMessageCaption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.caption = decodeFormattedText(record['caption']);
  out.show_caption_above_media = asBool(record['show_caption_above_media']);
  return out;
}

/** Encode a `editMessageCaption` into TDLib JSON wire shape. */
export function encodeEditMessageCaption(value: EditMessageCaption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editMessageCaption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['show_caption_above_media'] = value.show_caption_above_media;
  return out;
}

/** editMessageChecklist = Message — Edits the message content of a checklist. Returns the edited message after the edit is completed on the server side */
export class EditMessageChecklist {
  type: 'editMessageChecklist' = 'editMessageChecklist';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use messageProperties.can_be_edited to check whether the message can be edited */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
  /** checklist: inputChecklist — The new checklist. If some tasks were completed, this information will be kept */
  checklist: InputChecklist | null = null;
}

/** Decode a TDLib `editMessageChecklist` object; null input -> null. */
export function decodeEditMessageChecklist(json: TdJsonRaw): EditMessageChecklist | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditMessageChecklist = new EditMessageChecklist();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.checklist = decodeInputChecklist(record['checklist']);
  return out;
}

/** Encode a `editMessageChecklist` into TDLib JSON wire shape. */
export function encodeEditMessageChecklist(value: EditMessageChecklist): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editMessageChecklist';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['checklist'] = value.checklist === null ? null : encodeInputChecklist(value.checklist);
  return out;
}

/** editMessageLiveLocation = Message — Edits the message content of a live location. Messages can be edited for a limited period of time specified in the live location. Returns the edited message after the edit is completed on the server si... */
export class EditMessageLiveLocation {
  type: 'editMessageLiveLocation' = 'editMessageLiveLocation';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use messageProperties.can_be_edited to check whether the message can be edited */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
  /** location: liveLocation — New live location of the message; pass null to stop sharing the live location. If the new live_period isn't set to 0x7FFFFFFF, */
  location: LiveLocation | null = null;
}

/** Decode a TDLib `editMessageLiveLocation` object; null input -> null. */
export function decodeEditMessageLiveLocation(json: TdJsonRaw): EditMessageLiveLocation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditMessageLiveLocation = new EditMessageLiveLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.location = decodeLiveLocation(record['location']);
  return out;
}

/** Encode a `editMessageLiveLocation` into TDLib JSON wire shape. */
export function encodeEditMessageLiveLocation(value: EditMessageLiveLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editMessageLiveLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['location'] = value.location === null ? null : encodeLiveLocation(value.location);
  return out;
}

/** editMessageMedia = Message — Edits the media content of a message, including message caption. If only the caption needs to be edited, use editMessageCaption instead. The type of message content in an album can't be changed with exception... */
export class EditMessageMedia {
  type: 'editMessageMedia' = 'editMessageMedia';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use messageProperties.can_edit_media to check whether the message can be edited */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — New content of the message. Must be one of the following types: inputMessageAnimation, inputMessageAudio, inputMessageDocument, inputMessagePhoto or inputMessageVideo */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editMessageMedia` object; null input -> null. */
export function decodeEditMessageMedia(json: TdJsonRaw): EditMessageMedia | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditMessageMedia = new EditMessageMedia();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editMessageMedia` into TDLib JSON wire shape. */
export function encodeEditMessageMedia(value: EditMessageMedia): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editMessageMedia';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editMessageReplyMarkup = Message — Edits the message reply markup; for bots only. Returns the edited message after the edit is completed on the server side */
export class EditMessageReplyMarkup {
  type: 'editMessageReplyMarkup' = 'editMessageReplyMarkup';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use messageProperties.can_be_edited to check whether the message can be edited */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none */
  reply_markup: TdReplyMarkup | null = null;
}

/** Decode a TDLib `editMessageReplyMarkup` object; null input -> null. */
export function decodeEditMessageReplyMarkup(json: TdJsonRaw): EditMessageReplyMarkup | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditMessageReplyMarkup = new EditMessageReplyMarkup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  return out;
}

/** Encode a `editMessageReplyMarkup` into TDLib JSON wire shape. */
export function encodeEditMessageReplyMarkup(value: EditMessageReplyMarkup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editMessageReplyMarkup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  return out;
}

/** editMessageSchedulingState = Ok — Edits the time when a scheduled message will be sent. Scheduling state of all messages in the same album or forwarded together with the message will be also changed */
export class EditMessageSchedulingState {
  type: 'editMessageSchedulingState' = 'editMessageSchedulingState';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use messageProperties.can_edit_scheduling_state to check whether the message is suitable */
  message_id: number = 0;
  /** scheduling_state: MessageSchedulingState — The new message scheduling state; pass null to send the message immediately. Must be null for messages in the state messageSchedulingStateSendWhenVideoProcessed */
  scheduling_state: TdMessageSchedulingState | null = null;
}

/** Decode a TDLib `editMessageSchedulingState` object; null input -> null. */
export function decodeEditMessageSchedulingState(json: TdJsonRaw): EditMessageSchedulingState | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditMessageSchedulingState = new EditMessageSchedulingState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.scheduling_state = decodeTdMessageSchedulingState(record['scheduling_state']);
  return out;
}

/** Encode a `editMessageSchedulingState` into TDLib JSON wire shape. */
export function encodeEditMessageSchedulingState(value: EditMessageSchedulingState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editMessageSchedulingState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['scheduling_state'] = value.scheduling_state === null ? null : encodeTdMessageSchedulingState(value.scheduling_state);
  return out;
}

/** editMessageText = Message — Edits the text of a message (or a text of a game message). Returns the edited message after the edit is completed on the server side */
export class EditMessageText {
  type: 'editMessageText' = 'editMessageText';
  extra?: string;
  /** chat_id: int53 — The chat the message belongs to */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message. Use messageProperties.can_be_edited to check whether the message can be edited */
  message_id: number = 0;
  /** reply_markup: ReplyMarkup — The new message reply markup; pass null if none; for bots only */
  reply_markup: TdReplyMarkup | null = null;
  /** input_message_content: InputMessageContent — New text content of the message. Must be of type inputMessageText or inputMessageRichMessage */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editMessageText` object; null input -> null. */
export function decodeEditMessageText(json: TdJsonRaw): EditMessageText | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditMessageText = new EditMessageText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editMessageText` into TDLib JSON wire shape. */
export function encodeEditMessageText(value: EditMessageText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editMessageText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editProxy = AddedProxy — Edits an existing proxy server for network requests. Can be called before authorization */
export class EditProxy {
  type: 'editProxy' = 'editProxy';
  extra?: string;
  /** proxy_id: int32 — Proxy identifier */
  proxy_id: number = 0;
  /** proxy: proxy — The new information about the proxy */
  proxy: ProxyValue | null = null;
  /** enable: Bool — Pass true to immediately enable the proxy */
  enable: boolean = false;
  /** comment: string — New comment for the proxy */
  comment: string = '';
}

/** Decode a TDLib `editProxy` object; null input -> null. */
export function decodeEditProxy(json: TdJsonRaw): EditProxy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditProxy = new EditProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.proxy_id = asNumber(record['proxy_id']);
  out.proxy = decodeProxyValue(record['proxy']);
  out.enable = asBool(record['enable']);
  out.comment = asString(record['comment']);
  return out;
}

/** Encode a `editProxy` into TDLib JSON wire shape. */
export function encodeEditProxy(value: EditProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['proxy_id'] = value.proxy_id;
  out['proxy'] = value.proxy === null ? null : encodeProxyValue(value.proxy);
  out['enable'] = value.enable;
  out['comment'] = value.comment;
  return out;
}

/** editQuickReplyMessage = Ok — Asynchronously edits the text, media or caption of a quick reply message. Use quickReplyMessage.can_be_edited to check whether a message can be edited. Media message can be edited only to a media message. Che... */
export class EditQuickReplyMessage {
  type: 'editQuickReplyMessage' = 'editQuickReplyMessage';
  extra?: string;
  /** shortcut_id: int32 — Unique identifier of the quick reply shortcut with the message */
  shortcut_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** input_message_content: InputMessageContent — New content of the message. Must be one of the following types: inputMessageAnimation, inputMessageAudio, inputMessageChecklist, inputMessageDocument, inputMessagePhoto, inputMessageRichMessag... */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `editQuickReplyMessage` object; null input -> null. */
export function decodeEditQuickReplyMessage(json: TdJsonRaw): EditQuickReplyMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditQuickReplyMessage = new EditQuickReplyMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_id = asNumber(record['shortcut_id']);
  out.message_id = asNumber(record['message_id']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `editQuickReplyMessage` into TDLib JSON wire shape. */
export function encodeEditQuickReplyMessage(value: EditQuickReplyMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editQuickReplyMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_id'] = value.shortcut_id;
  out['message_id'] = value.message_id;
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** editStarSubscription = Ok — Cancels or re-enables Telegram Star subscription */
export class EditStarSubscription {
  type: 'editStarSubscription' = 'editStarSubscription';
  extra?: string;
  /** subscription_id: string — Identifier of the subscription to change */
  subscription_id: string = '';
  /** is_canceled: Bool — New value of is_canceled */
  is_canceled: boolean = false;
}

/** Decode a TDLib `editStarSubscription` object; null input -> null. */
export function decodeEditStarSubscription(json: TdJsonRaw): EditStarSubscription | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditStarSubscription = new EditStarSubscription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subscription_id = asString(record['subscription_id']);
  out.is_canceled = asBool(record['is_canceled']);
  return out;
}

/** Encode a `editStarSubscription` into TDLib JSON wire shape. */
export function encodeEditStarSubscription(value: EditStarSubscription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editStarSubscription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subscription_id'] = value.subscription_id;
  out['is_canceled'] = value.is_canceled;
  return out;
}

/** editStory = Ok — Changes content and caption of a story. Can be called only if story.can_be_edited == true */
export class EditStory {
  type: 'editStory' = 'editStory';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Identifier of the story to edit */
  story_id: number = 0;
  /** content: InputStoryContent — New content of the story; pass null to keep the current content */
  content: TdInputStoryContent | null = null;
  /** areas: inputStoryAreas — New clickable rectangle areas to be shown on the story media; pass null to keep the current areas. Areas can't be edited if story content isn't changed */
  areas: InputStoryAreas | null = null;
  /** caption: formattedText — New story caption; pass null to keep the current caption */
  caption: FormattedText | null = null;
}

/** Decode a TDLib `editStory` object; null input -> null. */
export function decodeEditStory(json: TdJsonRaw): EditStory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditStory = new EditStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.content = decodeTdInputStoryContent(record['content']);
  out.areas = decodeInputStoryAreas(record['areas']);
  out.caption = decodeFormattedText(record['caption']);
  return out;
}

/** Encode a `editStory` into TDLib JSON wire shape. */
export function encodeEditStory(value: EditStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['content'] = value.content === null ? null : encodeTdInputStoryContent(value.content);
  out['areas'] = value.areas === null ? null : encodeInputStoryAreas(value.areas);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  return out;
}

/** editStoryCover = Ok — Changes cover of a video story. Can be called only if story.can_be_edited == true and the story isn't being edited now */
export class EditStoryCover {
  type: 'editStoryCover' = 'editStoryCover';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Identifier of the story to edit */
  story_id: number = 0;
  /** cover_frame_timestamp: double — New timestamp of the frame, which will be used as video thumbnail */
  cover_frame_timestamp: number = 0;
}

/** Decode a TDLib `editStoryCover` object; null input -> null. */
export function decodeEditStoryCover(json: TdJsonRaw): EditStoryCover | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditStoryCover = new EditStoryCover();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  out.cover_frame_timestamp = asNumber(record['cover_frame_timestamp']);
  return out;
}

/** Encode a `editStoryCover` into TDLib JSON wire shape. */
export function encodeEditStoryCover(value: EditStoryCover): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editStoryCover';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  out['cover_frame_timestamp'] = value.cover_frame_timestamp;
  return out;
}

/** editTextCompositionStyle = TextCompositionStyle — Edits a custom text composition style that was created by the current user */
export class EditTextCompositionStyle {
  type: 'editTextCompositionStyle' = 'editTextCompositionStyle';
  extra?: string;
  /** name: string — Name of the style */
  name: string = '';
  /** title: string — Title of the style; 1-getOption("text_composition_style_title_length_max") characters */
  title: string = '';
  /** custom_emoji_id: int64 — Identifier of the custom emoji corresponding to the style */
  custom_emoji_id: string = '';
  /** prompt: string — Prompt that will be used for text composition; 1-getOption("text_composition_style_prompt_length_max") characters */
  prompt: string = '';
  /** show_creator: Bool — Pass true if the current user must be shown as the creator of the style */
  show_creator: boolean = false;
}

/** Decode a TDLib `editTextCompositionStyle` object; null input -> null. */
export function decodeEditTextCompositionStyle(json: TdJsonRaw): EditTextCompositionStyle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditTextCompositionStyle = new EditTextCompositionStyle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.title = asString(record['title']);
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  out.prompt = asString(record['prompt']);
  out.show_creator = asBool(record['show_creator']);
  return out;
}

/** Encode a `editTextCompositionStyle` into TDLib JSON wire shape. */
export function encodeEditTextCompositionStyle(value: EditTextCompositionStyle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editTextCompositionStyle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['title'] = value.title;
  out['custom_emoji_id'] = value.custom_emoji_id;
  out['prompt'] = value.prompt;
  out['show_creator'] = value.show_creator;
  return out;
}

/** editUserStarSubscription = Ok — Cancels or re-enables Telegram Star subscription for a user; for bots only */
export class EditUserStarSubscription {
  type: 'editUserStarSubscription' = 'editUserStarSubscription';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** telegram_payment_charge_id: string — Telegram payment identifier of the subscription */
  telegram_payment_charge_id: string = '';
  /** is_canceled: Bool — Pass true to cancel the subscription; pass false to allow the user to enable it */
  is_canceled: boolean = false;
}

/** Decode a TDLib `editUserStarSubscription` object; null input -> null. */
export function decodeEditUserStarSubscription(json: TdJsonRaw): EditUserStarSubscription | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EditUserStarSubscription = new EditUserStarSubscription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.telegram_payment_charge_id = asString(record['telegram_payment_charge_id']);
  out.is_canceled = asBool(record['is_canceled']);
  return out;
}

/** Encode a `editUserStarSubscription` into TDLib JSON wire shape. */
export function encodeEditUserStarSubscription(value: EditUserStarSubscription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'editUserStarSubscription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['telegram_payment_charge_id'] = value.telegram_payment_charge_id;
  out['is_canceled'] = value.is_canceled;
  return out;
}

/** emailAddressAuthenticationAppleId = EmailAddressAuthentication — An authentication token received through Apple ID */
export class EmailAddressAuthenticationAppleId {
  type: 'emailAddressAuthenticationAppleId' = 'emailAddressAuthenticationAppleId';
  extra?: string;
  /** token: string — The token */
  token: string = '';
}

/** Decode a TDLib `emailAddressAuthenticationAppleId` object; null input -> null. */
export function decodeEmailAddressAuthenticationAppleId(json: TdJsonRaw): EmailAddressAuthenticationAppleId | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmailAddressAuthenticationAppleId = new EmailAddressAuthenticationAppleId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `emailAddressAuthenticationAppleId` into TDLib JSON wire shape. */
export function encodeEmailAddressAuthenticationAppleId(value: EmailAddressAuthenticationAppleId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emailAddressAuthenticationAppleId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** emailAddressAuthenticationCode = EmailAddressAuthentication — An authentication code delivered to a user's email address */
export class EmailAddressAuthenticationCode {
  type: 'emailAddressAuthenticationCode' = 'emailAddressAuthenticationCode';
  extra?: string;
  /** code: string — The code */
  code: string = '';
}

/** Decode a TDLib `emailAddressAuthenticationCode` object; null input -> null. */
export function decodeEmailAddressAuthenticationCode(json: TdJsonRaw): EmailAddressAuthenticationCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmailAddressAuthenticationCode = new EmailAddressAuthenticationCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asString(record['code']);
  return out;
}

/** Encode a `emailAddressAuthenticationCode` into TDLib JSON wire shape. */
export function encodeEmailAddressAuthenticationCode(value: EmailAddressAuthenticationCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emailAddressAuthenticationCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  return out;
}

/** emailAddressAuthenticationCodeInfo = EmailAddressAuthenticationCodeInfo — Information about the email address authentication code that was sent */
export class EmailAddressAuthenticationCodeInfo {
  type: 'emailAddressAuthenticationCodeInfo' = 'emailAddressAuthenticationCodeInfo';
  extra?: string;
  /** email_address_pattern: string — Pattern of the email address to which an authentication code was sent */
  email_address_pattern: string = '';
  /** length: int32 — Length of the code; 0 if unknown */
  length: number = 0;
}

/** Decode a TDLib `emailAddressAuthenticationCodeInfo` object; null input -> null. */
export function decodeEmailAddressAuthenticationCodeInfo(json: TdJsonRaw): EmailAddressAuthenticationCodeInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmailAddressAuthenticationCodeInfo = new EmailAddressAuthenticationCodeInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.email_address_pattern = asString(record['email_address_pattern']);
  out.length = asNumber(record['length']);
  return out;
}

/** Encode a `emailAddressAuthenticationCodeInfo` into TDLib JSON wire shape. */
export function encodeEmailAddressAuthenticationCodeInfo(value: EmailAddressAuthenticationCodeInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emailAddressAuthenticationCodeInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['email_address_pattern'] = value.email_address_pattern;
  out['length'] = value.length;
  return out;
}

/** emailAddressAuthenticationGoogleId = EmailAddressAuthentication — An authentication token received through Google ID */
export class EmailAddressAuthenticationGoogleId {
  type: 'emailAddressAuthenticationGoogleId' = 'emailAddressAuthenticationGoogleId';
  extra?: string;
  /** token: string — The token */
  token: string = '';
}

/** Decode a TDLib `emailAddressAuthenticationGoogleId` object; null input -> null. */
export function decodeEmailAddressAuthenticationGoogleId(json: TdJsonRaw): EmailAddressAuthenticationGoogleId | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmailAddressAuthenticationGoogleId = new EmailAddressAuthenticationGoogleId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.token = asString(record['token']);
  return out;
}

/** Encode a `emailAddressAuthenticationGoogleId` into TDLib JSON wire shape. */
export function encodeEmailAddressAuthenticationGoogleId(value: EmailAddressAuthenticationGoogleId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emailAddressAuthenticationGoogleId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['token'] = value.token;
  return out;
}

/** emailAddressResetStateAvailable = EmailAddressResetState — Email address can be reset after the given period. Call resetAuthenticationEmailAddress to reset it and allow the user to authorize with a code sent to the user's phone number */
export class EmailAddressResetStateAvailable {
  type: 'emailAddressResetStateAvailable' = 'emailAddressResetStateAvailable';
  extra?: string;
  /** wait_period: int32 — Time required to wait before the email address can be reset; 0 if the user is subscribed to Telegram Premium */
  wait_period: number = 0;
}

/** Decode a TDLib `emailAddressResetStateAvailable` object; null input -> null. */
export function decodeEmailAddressResetStateAvailable(json: TdJsonRaw): EmailAddressResetStateAvailable | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmailAddressResetStateAvailable = new EmailAddressResetStateAvailable();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.wait_period = asNumber(record['wait_period']);
  return out;
}

/** Encode a `emailAddressResetStateAvailable` into TDLib JSON wire shape. */
export function encodeEmailAddressResetStateAvailable(value: EmailAddressResetStateAvailable): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emailAddressResetStateAvailable';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['wait_period'] = value.wait_period;
  return out;
}

/** emailAddressResetStatePending = EmailAddressResetState — Email address reset has already been requested. Call resetAuthenticationEmailAddress to check whether immediate reset is possible */
export class EmailAddressResetStatePending {
  type: 'emailAddressResetStatePending' = 'emailAddressResetStatePending';
  extra?: string;
  /** reset_in: int32 — Left time before the email address will be reset, in seconds. updateAuthorizationState is not sent when this field changes */
  reset_in: number = 0;
}

/** Decode a TDLib `emailAddressResetStatePending` object; null input -> null. */
export function decodeEmailAddressResetStatePending(json: TdJsonRaw): EmailAddressResetStatePending | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmailAddressResetStatePending = new EmailAddressResetStatePending();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reset_in = asNumber(record['reset_in']);
  return out;
}

/** Encode a `emailAddressResetStatePending` into TDLib JSON wire shape. */
export function encodeEmailAddressResetStatePending(value: EmailAddressResetStatePending): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emailAddressResetStatePending';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reset_in'] = value.reset_in;
  return out;
}

/** emojiCategories = EmojiCategories — Represents a list of emoji categories */
export class EmojiCategories {
  type: 'emojiCategories' = 'emojiCategories';
  extra?: string;
  /** categories: vector<emojiCategory> — List of categories */
  categories: EmojiCategory[] = [];
}

/** Decode a TDLib `emojiCategories` object; null input -> null. */
export function decodeEmojiCategories(json: TdJsonRaw): EmojiCategories | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiCategories = new EmojiCategories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.categories = decodeVector(record['categories'], (item: TdJsonRaw): EmojiCategory => decodeEmojiCategory(item) as EmojiCategory);
  return out;
}

/** Encode a `emojiCategories` into TDLib JSON wire shape. */
export function encodeEmojiCategories(value: EmojiCategories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiCategories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['categories'] = encodeVector(value.categories, (item: EmojiCategory | null): TdJsonRaw => item === null ? null : encodeEmojiCategory(item));
  return out;
}

/** emojiCategory = EmojiCategory — Describes an emoji category */
export class EmojiCategory {
  type: 'emojiCategory' = 'emojiCategory';
  extra?: string;
  /** name: string — Name of the category */
  name: string = '';
  /** icon: sticker — Custom emoji sticker, which represents icon of the category */
  icon: Sticker | null = null;
  /** source: EmojiCategorySource — Source of stickers for the emoji category */
  source: TdEmojiCategorySource | null = null;
  /** is_greeting: Bool — True, if the category must be shown first when choosing a sticker for the start page */
  is_greeting: boolean = false;
}

/** Decode a TDLib `emojiCategory` object; null input -> null. */
export function decodeEmojiCategory(json: TdJsonRaw): EmojiCategory | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiCategory = new EmojiCategory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.icon = decodeSticker(record['icon']);
  out.source = decodeTdEmojiCategorySource(record['source']);
  out.is_greeting = asBool(record['is_greeting']);
  return out;
}

/** Encode a `emojiCategory` into TDLib JSON wire shape. */
export function encodeEmojiCategory(value: EmojiCategory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiCategory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['icon'] = value.icon === null ? null : encodeSticker(value.icon);
  out['source'] = value.source === null ? null : encodeTdEmojiCategorySource(value.source);
  out['is_greeting'] = value.is_greeting;
  return out;
}

/** emojiCategorySourcePremium = EmojiCategorySource — The category contains premium stickers that must be found by getPremiumStickers */
export class EmojiCategorySourcePremium {
  type: 'emojiCategorySourcePremium' = 'emojiCategorySourcePremium';
  extra?: string;
}

/** Decode a TDLib `emojiCategorySourcePremium` object; null input -> null. */
export function decodeEmojiCategorySourcePremium(json: TdJsonRaw): EmojiCategorySourcePremium | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiCategorySourcePremium = new EmojiCategorySourcePremium();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `emojiCategorySourcePremium` into TDLib JSON wire shape. */
export function encodeEmojiCategorySourcePremium(value: EmojiCategorySourcePremium): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiCategorySourcePremium';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** emojiCategorySourceSearch = EmojiCategorySource — The category contains a list of similar emoji to search for in getStickers and searchStickers for stickers, or getInlineQueryResults with the bot getOption("animation_search_bot_username"... */
export class EmojiCategorySourceSearch {
  type: 'emojiCategorySourceSearch' = 'emojiCategorySourceSearch';
  extra?: string;
  /** emojis: vector<string> — List of emojis to search for */
  emojis: string[] = [];
}

/** Decode a TDLib `emojiCategorySourceSearch` object; null input -> null. */
export function decodeEmojiCategorySourceSearch(json: TdJsonRaw): EmojiCategorySourceSearch | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiCategorySourceSearch = new EmojiCategorySourceSearch();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emojis = decodeVector(record['emojis'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `emojiCategorySourceSearch` into TDLib JSON wire shape. */
export function encodeEmojiCategorySourceSearch(value: EmojiCategorySourceSearch): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiCategorySourceSearch';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emojis'] = encodeVector(value.emojis, (item: string): TdJsonRaw => item);
  return out;
}

/** emojiCategoryTypeChatPhoto = EmojiCategoryType — The category must be used for chat photo emoji selection */
export class EmojiCategoryTypeChatPhoto {
  type: 'emojiCategoryTypeChatPhoto' = 'emojiCategoryTypeChatPhoto';
  extra?: string;
}

/** Decode a TDLib `emojiCategoryTypeChatPhoto` object; null input -> null. */
export function decodeEmojiCategoryTypeChatPhoto(json: TdJsonRaw): EmojiCategoryTypeChatPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiCategoryTypeChatPhoto = new EmojiCategoryTypeChatPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `emojiCategoryTypeChatPhoto` into TDLib JSON wire shape. */
export function encodeEmojiCategoryTypeChatPhoto(value: EmojiCategoryTypeChatPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiCategoryTypeChatPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** emojiCategoryTypeDefault = EmojiCategoryType — The category must be used by default (e.g., for custom emoji or animation search) */
export class EmojiCategoryTypeDefault {
  type: 'emojiCategoryTypeDefault' = 'emojiCategoryTypeDefault';
  extra?: string;
}

/** Decode a TDLib `emojiCategoryTypeDefault` object; null input -> null. */
export function decodeEmojiCategoryTypeDefault(json: TdJsonRaw): EmojiCategoryTypeDefault | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiCategoryTypeDefault = new EmojiCategoryTypeDefault();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `emojiCategoryTypeDefault` into TDLib JSON wire shape. */
export function encodeEmojiCategoryTypeDefault(value: EmojiCategoryTypeDefault): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiCategoryTypeDefault';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** emojiCategoryTypeEmojiStatus = EmojiCategoryType — The category must be used for emoji status selection */
export class EmojiCategoryTypeEmojiStatus {
  type: 'emojiCategoryTypeEmojiStatus' = 'emojiCategoryTypeEmojiStatus';
  extra?: string;
}

/** Decode a TDLib `emojiCategoryTypeEmojiStatus` object; null input -> null. */
export function decodeEmojiCategoryTypeEmojiStatus(json: TdJsonRaw): EmojiCategoryTypeEmojiStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiCategoryTypeEmojiStatus = new EmojiCategoryTypeEmojiStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `emojiCategoryTypeEmojiStatus` into TDLib JSON wire shape. */
export function encodeEmojiCategoryTypeEmojiStatus(value: EmojiCategoryTypeEmojiStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiCategoryTypeEmojiStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** emojiCategoryTypeRegularStickers = EmojiCategoryType — The category must be used by default for regular sticker selection. It may contain greeting emoji category and premium stickers */
export class EmojiCategoryTypeRegularStickers {
  type: 'emojiCategoryTypeRegularStickers' = 'emojiCategoryTypeRegularStickers';
  extra?: string;
}

/** Decode a TDLib `emojiCategoryTypeRegularStickers` object; null input -> null. */
export function decodeEmojiCategoryTypeRegularStickers(json: TdJsonRaw): EmojiCategoryTypeRegularStickers | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiCategoryTypeRegularStickers = new EmojiCategoryTypeRegularStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `emojiCategoryTypeRegularStickers` into TDLib JSON wire shape. */
export function encodeEmojiCategoryTypeRegularStickers(value: EmojiCategoryTypeRegularStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiCategoryTypeRegularStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** emojiChatTheme = EmojiChatTheme — Describes a chat theme based on an emoji */
export class EmojiChatTheme {
  type: 'emojiChatTheme' = 'emojiChatTheme';
  extra?: string;
  /** name: string — Theme name */
  name: string = '';
  /** light_settings: themeSettings — Theme settings for a light chat theme */
  light_settings: ThemeSettings | null = null;
  /** dark_settings: themeSettings — Theme settings for a dark chat theme */
  dark_settings: ThemeSettings | null = null;
}

/** Decode a TDLib `emojiChatTheme` object; null input -> null. */
export function decodeEmojiChatTheme(json: TdJsonRaw): EmojiChatTheme | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiChatTheme = new EmojiChatTheme();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.light_settings = decodeThemeSettings(record['light_settings']);
  out.dark_settings = decodeThemeSettings(record['dark_settings']);
  return out;
}

/** Encode a `emojiChatTheme` into TDLib JSON wire shape. */
export function encodeEmojiChatTheme(value: EmojiChatTheme): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiChatTheme';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['light_settings'] = value.light_settings === null ? null : encodeThemeSettings(value.light_settings);
  out['dark_settings'] = value.dark_settings === null ? null : encodeThemeSettings(value.dark_settings);
  return out;
}

/** emojiKeyword = EmojiKeyword — Represents an emoji with its keyword */
export class EmojiKeyword {
  type: 'emojiKeyword' = 'emojiKeyword';
  extra?: string;
  /** emoji: string — The emoji */
  emoji: string = '';
  /** keyword: string — The keyword */
  keyword: string = '';
}

/** Decode a TDLib `emojiKeyword` object; null input -> null. */
export function decodeEmojiKeyword(json: TdJsonRaw): EmojiKeyword | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiKeyword = new EmojiKeyword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji = asString(record['emoji']);
  out.keyword = asString(record['keyword']);
  return out;
}

/** Encode a `emojiKeyword` into TDLib JSON wire shape. */
export function encodeEmojiKeyword(value: EmojiKeyword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiKeyword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji'] = value.emoji;
  out['keyword'] = value.keyword;
  return out;
}

/** emojiKeywords = EmojiKeywords — Represents a list of emojis with their keywords */
export class EmojiKeywords {
  type: 'emojiKeywords' = 'emojiKeywords';
  extra?: string;
  /** emoji_keywords: vector<emojiKeyword> — List of emojis with their keywords */
  emoji_keywords: EmojiKeyword[] = [];
}

/** Decode a TDLib `emojiKeywords` object; null input -> null. */
export function decodeEmojiKeywords(json: TdJsonRaw): EmojiKeywords | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiKeywords = new EmojiKeywords();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji_keywords = decodeVector(record['emoji_keywords'], (item: TdJsonRaw): EmojiKeyword => decodeEmojiKeyword(item) as EmojiKeyword);
  return out;
}

/** Encode a `emojiKeywords` into TDLib JSON wire shape. */
export function encodeEmojiKeywords(value: EmojiKeywords): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiKeywords';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji_keywords'] = encodeVector(value.emoji_keywords, (item: EmojiKeyword | null): TdJsonRaw => item === null ? null : encodeEmojiKeyword(item));
  return out;
}

/** emojiReaction = EmojiReaction — Contains information about an emoji reaction */
export class EmojiReaction {
  type: 'emojiReaction' = 'emojiReaction';
  extra?: string;
  /** emoji: string — Text representation of the reaction */
  emoji: string = '';
  /** title: string — Reaction title */
  title: string = '';
  /** is_active: Bool — True, if the reaction can be added to new messages and enabled in chats */
  is_active: boolean = false;
  /** static_icon: sticker — Static icon for the reaction */
  static_icon: Sticker | null = null;
  /** appear_animation: sticker — Appear animation for the reaction */
  appear_animation: Sticker | null = null;
  /** select_animation: sticker — Select animation for the reaction */
  select_animation: Sticker | null = null;
  /** activate_animation: sticker — Activate animation for the reaction */
  activate_animation: Sticker | null = null;
  /** effect_animation: sticker — Effect animation for the reaction */
  effect_animation: Sticker | null = null;
  /** around_animation: sticker — Around animation for the reaction; may be null */
  around_animation: Sticker | null = null;
  /** center_animation: sticker — Center animation for the reaction; may be null */
  center_animation: Sticker | null = null;
}

/** Decode a TDLib `emojiReaction` object; null input -> null. */
export function decodeEmojiReaction(json: TdJsonRaw): EmojiReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiReaction = new EmojiReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji = asString(record['emoji']);
  out.title = asString(record['title']);
  out.is_active = asBool(record['is_active']);
  out.static_icon = decodeSticker(record['static_icon']);
  out.appear_animation = decodeSticker(record['appear_animation']);
  out.select_animation = decodeSticker(record['select_animation']);
  out.activate_animation = decodeSticker(record['activate_animation']);
  out.effect_animation = decodeSticker(record['effect_animation']);
  out.around_animation = decodeSticker(record['around_animation']);
  out.center_animation = decodeSticker(record['center_animation']);
  return out;
}

/** Encode a `emojiReaction` into TDLib JSON wire shape. */
export function encodeEmojiReaction(value: EmojiReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji'] = value.emoji;
  out['title'] = value.title;
  out['is_active'] = value.is_active;
  out['static_icon'] = value.static_icon === null ? null : encodeSticker(value.static_icon);
  out['appear_animation'] = value.appear_animation === null ? null : encodeSticker(value.appear_animation);
  out['select_animation'] = value.select_animation === null ? null : encodeSticker(value.select_animation);
  out['activate_animation'] = value.activate_animation === null ? null : encodeSticker(value.activate_animation);
  out['effect_animation'] = value.effect_animation === null ? null : encodeSticker(value.effect_animation);
  out['around_animation'] = value.around_animation === null ? null : encodeSticker(value.around_animation);
  out['center_animation'] = value.center_animation === null ? null : encodeSticker(value.center_animation);
  return out;
}

/** emojiStatus = EmojiStatus — Describes an emoji to be shown instead of the Telegram Premium badge */
export class EmojiStatus {
  type: 'emojiStatus' = 'emojiStatus';
  extra?: string;
  /** type: EmojiStatusType — Type of the emoji status */
  type_: TdEmojiStatusType | null = null;
  /** expiration_date: int32 — Point in time (Unix timestamp) when the status will expire; 0 if never */
  expiration_date: number = 0;
}

/** Decode a TDLib `emojiStatus` object; null input -> null. */
export function decodeEmojiStatus(json: TdJsonRaw): EmojiStatus | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiStatus = new EmojiStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdEmojiStatusType(record['type']);
  out.expiration_date = asNumber(record['expiration_date']);
  return out;
}

/** Encode a `emojiStatus` into TDLib JSON wire shape. */
export function encodeEmojiStatus(value: EmojiStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdEmojiStatusType(value.type_);
  out['expiration_date'] = value.expiration_date;
  return out;
}

/** emojiStatusCustomEmojis = EmojiStatusCustomEmojis — Contains a list of custom emoji identifiers for emoji statuses */
export class EmojiStatusCustomEmojis {
  type: 'emojiStatusCustomEmojis' = 'emojiStatusCustomEmojis';
  extra?: string;
  /** custom_emoji_ids: vector<int64> — The list of custom emoji identifiers */
  custom_emoji_ids: string[] = [];
}

/** Decode a TDLib `emojiStatusCustomEmojis` object; null input -> null. */
export function decodeEmojiStatusCustomEmojis(json: TdJsonRaw): EmojiStatusCustomEmojis | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiStatusCustomEmojis = new EmojiStatusCustomEmojis();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_emoji_ids = decodeVector(record['custom_emoji_ids'], (item: TdJsonRaw): string => asInt64(item));
  return out;
}

/** Encode a `emojiStatusCustomEmojis` into TDLib JSON wire shape. */
export function encodeEmojiStatusCustomEmojis(value: EmojiStatusCustomEmojis): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiStatusCustomEmojis';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_emoji_ids'] = encodeVector(value.custom_emoji_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** emojiStatusTypeCustomEmoji = EmojiStatusType — A custom emoji set as emoji status */
export class EmojiStatusTypeCustomEmoji {
  type: 'emojiStatusTypeCustomEmoji' = 'emojiStatusTypeCustomEmoji';
  extra?: string;
  /** custom_emoji_id: int64 — Identifier of the custom emoji in stickerFormatTgs format */
  custom_emoji_id: string = '';
}

/** Decode a TDLib `emojiStatusTypeCustomEmoji` object; null input -> null. */
export function decodeEmojiStatusTypeCustomEmoji(json: TdJsonRaw): EmojiStatusTypeCustomEmoji | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiStatusTypeCustomEmoji = new EmojiStatusTypeCustomEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_emoji_id = asInt64(record['custom_emoji_id']);
  return out;
}

/** Encode a `emojiStatusTypeCustomEmoji` into TDLib JSON wire shape. */
export function encodeEmojiStatusTypeCustomEmoji(value: EmojiStatusTypeCustomEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiStatusTypeCustomEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_emoji_id'] = value.custom_emoji_id;
  return out;
}

/** emojiStatusTypeUpgradedGift = EmojiStatusType — An upgraded gift set as emoji status */
export class EmojiStatusTypeUpgradedGift {
  type: 'emojiStatusTypeUpgradedGift' = 'emojiStatusTypeUpgradedGift';
  extra?: string;
  /** upgraded_gift_id: int64 — Identifier of the upgraded gift */
  upgraded_gift_id: string = '';
  /** gift_title: string — The title of the upgraded gift */
  gift_title: string = '';
  /** gift_name: string — Unique name of the upgraded gift that can be used with internalLinkTypeUpgradedGift */
  gift_name: string = '';
  /** model_custom_emoji_id: int64 — Custom emoji identifier of the model of the upgraded gift */
  model_custom_emoji_id: string = '';
  /** symbol_custom_emoji_id: int64 — Custom emoji identifier of the symbol of the upgraded gift */
  symbol_custom_emoji_id: string = '';
  /** backdrop_colors: upgradedGiftBackdropColors — Colors of the backdrop of the upgraded gift */
  backdrop_colors: UpgradedGiftBackdropColors | null = null;
}

/** Decode a TDLib `emojiStatusTypeUpgradedGift` object; null input -> null. */
export function decodeEmojiStatusTypeUpgradedGift(json: TdJsonRaw): EmojiStatusTypeUpgradedGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiStatusTypeUpgradedGift = new EmojiStatusTypeUpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.upgraded_gift_id = asInt64(record['upgraded_gift_id']);
  out.gift_title = asString(record['gift_title']);
  out.gift_name = asString(record['gift_name']);
  out.model_custom_emoji_id = asInt64(record['model_custom_emoji_id']);
  out.symbol_custom_emoji_id = asInt64(record['symbol_custom_emoji_id']);
  out.backdrop_colors = decodeUpgradedGiftBackdropColors(record['backdrop_colors']);
  return out;
}

/** Encode a `emojiStatusTypeUpgradedGift` into TDLib JSON wire shape. */
export function encodeEmojiStatusTypeUpgradedGift(value: EmojiStatusTypeUpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiStatusTypeUpgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['upgraded_gift_id'] = value.upgraded_gift_id;
  out['gift_title'] = value.gift_title;
  out['gift_name'] = value.gift_name;
  out['model_custom_emoji_id'] = value.model_custom_emoji_id;
  out['symbol_custom_emoji_id'] = value.symbol_custom_emoji_id;
  out['backdrop_colors'] = value.backdrop_colors === null ? null : encodeUpgradedGiftBackdropColors(value.backdrop_colors);
  return out;
}

/** emojiStatuses = EmojiStatuses — Contains a list of emoji statuses */
export class EmojiStatuses {
  type: 'emojiStatuses' = 'emojiStatuses';
  extra?: string;
  /** emoji_statuses: vector<emojiStatus> — The list of emoji statuses identifiers */
  emoji_statuses: EmojiStatus[] = [];
}

/** Decode a TDLib `emojiStatuses` object; null input -> null. */
export function decodeEmojiStatuses(json: TdJsonRaw): EmojiStatuses | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EmojiStatuses = new EmojiStatuses();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emoji_statuses = decodeVector(record['emoji_statuses'], (item: TdJsonRaw): EmojiStatus => decodeEmojiStatus(item) as EmojiStatus);
  return out;
}

/** Encode a `emojiStatuses` into TDLib JSON wire shape. */
export function encodeEmojiStatuses(value: EmojiStatuses): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojiStatuses';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emoji_statuses'] = encodeVector(value.emoji_statuses, (item: EmojiStatus | null): TdJsonRaw => item === null ? null : encodeEmojiStatus(item));
  return out;
}

/** emojis = Emojis — Represents a list of emojis */
export class Emojis {
  type: 'emojis' = 'emojis';
  extra?: string;
  /** emojis: vector<string> — List of emojis */
  emojis: string[] = [];
}

/** Decode a TDLib `emojis` object; null input -> null. */
export function decodeEmojis(json: TdJsonRaw): Emojis | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Emojis = new Emojis();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emojis = decodeVector(record['emojis'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `emojis` into TDLib JSON wire shape. */
export function encodeEmojis(value: Emojis): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'emojis';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emojis'] = encodeVector(value.emojis, (item: string): TdJsonRaw => item);
  return out;
}

/** enableProxy = Ok — Enables a proxy. Only one proxy can be enabled at a time. Can be called before authorization */
export class EnableProxy {
  type: 'enableProxy' = 'enableProxy';
  extra?: string;
  /** proxy_id: int32 — Proxy identifier */
  proxy_id: number = 0;
}

/** Decode a TDLib `enableProxy` object; null input -> null. */
export function decodeEnableProxy(json: TdJsonRaw): EnableProxy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EnableProxy = new EnableProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.proxy_id = asNumber(record['proxy_id']);
  return out;
}

/** Encode a `enableProxy` into TDLib JSON wire shape. */
export function encodeEnableProxy(value: EnableProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'enableProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['proxy_id'] = value.proxy_id;
  return out;
}

/** encryptGroupCallData = Data — Encrypts group call data before sending them over network using tgcalls */
export class EncryptGroupCallData {
  type: 'encryptGroupCallData' = 'encryptGroupCallData';
  extra?: string;
  /** group_call_id: int32 — Group call identifier. The call must not be a video chat */
  group_call_id: number = 0;
  /** data_channel: GroupCallDataChannel — Data channel for which data is encrypted */
  data_channel: TdGroupCallDataChannel | null = null;
  /** data: bytes — Data to encrypt */
  data: string = '';
  /** unencrypted_prefix_size: int32 — Size of data prefix that must be kept unencrypted */
  unencrypted_prefix_size: number = 0;
}

/** Decode a TDLib `encryptGroupCallData` object; null input -> null. */
export function decodeEncryptGroupCallData(json: TdJsonRaw): EncryptGroupCallData | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EncryptGroupCallData = new EncryptGroupCallData();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.data_channel = decodeTdGroupCallDataChannel(record['data_channel']);
  out.data = asString(record['data']);
  out.unencrypted_prefix_size = asNumber(record['unencrypted_prefix_size']);
  return out;
}

/** Encode a `encryptGroupCallData` into TDLib JSON wire shape. */
export function encodeEncryptGroupCallData(value: EncryptGroupCallData): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'encryptGroupCallData';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['data_channel'] = value.data_channel === null ? null : encodeTdGroupCallDataChannel(value.data_channel);
  out['data'] = value.data;
  out['unencrypted_prefix_size'] = value.unencrypted_prefix_size;
  return out;
}

/** encryptedCredentials = EncryptedCredentials — Contains encrypted Telegram Passport data credentials */
export class EncryptedCredentials {
  type: 'encryptedCredentials' = 'encryptedCredentials';
  extra?: string;
  /** data: bytes — The encrypted credentials */
  data: string = '';
  /** hash: bytes — The decrypted data hash */
  hash: string = '';
  /** secret: bytes — Secret for data decryption, encrypted with the service's public key */
  secret: string = '';
}

/** Decode a TDLib `encryptedCredentials` object; null input -> null. */
export function decodeEncryptedCredentials(json: TdJsonRaw): EncryptedCredentials | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EncryptedCredentials = new EncryptedCredentials();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.data = asString(record['data']);
  out.hash = asString(record['hash']);
  out.secret = asString(record['secret']);
  return out;
}

/** Encode a `encryptedCredentials` into TDLib JSON wire shape. */
export function encodeEncryptedCredentials(value: EncryptedCredentials): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'encryptedCredentials';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['data'] = value.data;
  out['hash'] = value.hash;
  out['secret'] = value.secret;
  return out;
}

/** encryptedPassportElement = EncryptedPassportElement — Contains information about an encrypted Telegram Passport element; for bots only */
export class EncryptedPassportElement {
  type: 'encryptedPassportElement' = 'encryptedPassportElement';
  extra?: string;
  /** type: PassportElementType — Type of Telegram Passport element */
  type_: TdPassportElementType | null = null;
  /** data: bytes — Encrypted JSON-encoded data about the user */
  data: string = '';
  /** front_side: datedFile — The front side of an identity document */
  front_side: DatedFile | null = null;
  /** reverse_side: datedFile — The reverse side of an identity document; may be null */
  reverse_side: DatedFile | null = null;
  /** selfie: datedFile — Selfie with the document; may be null */
  selfie: DatedFile | null = null;
  /** translation: vector<datedFile> — List of files containing a certified English translation of the document */
  translation: DatedFile[] = [];
  /** files: vector<datedFile> — List of attached files */
  files: DatedFile[] = [];
  /** value: string — Unencrypted data, phone number or email address */
  value: string = '';
  /** hash: string — Hash of the entire element */
  hash: string = '';
}

/** Decode a TDLib `encryptedPassportElement` object; null input -> null. */
export function decodeEncryptedPassportElement(json: TdJsonRaw): EncryptedPassportElement | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EncryptedPassportElement = new EncryptedPassportElement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPassportElementType(record['type']);
  out.data = asString(record['data']);
  out.front_side = decodeDatedFile(record['front_side']);
  out.reverse_side = decodeDatedFile(record['reverse_side']);
  out.selfie = decodeDatedFile(record['selfie']);
  out.translation = decodeVector(record['translation'], (item: TdJsonRaw): DatedFile => decodeDatedFile(item) as DatedFile);
  out.files = decodeVector(record['files'], (item: TdJsonRaw): DatedFile => decodeDatedFile(item) as DatedFile);
  out.value = asString(record['value']);
  out.hash = asString(record['hash']);
  return out;
}

/** Encode a `encryptedPassportElement` into TDLib JSON wire shape. */
export function encodeEncryptedPassportElement(value: EncryptedPassportElement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'encryptedPassportElement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPassportElementType(value.type_);
  out['data'] = value.data;
  out['front_side'] = value.front_side === null ? null : encodeDatedFile(value.front_side);
  out['reverse_side'] = value.reverse_side === null ? null : encodeDatedFile(value.reverse_side);
  out['selfie'] = value.selfie === null ? null : encodeDatedFile(value.selfie);
  out['translation'] = encodeVector(value.translation, (item: DatedFile | null): TdJsonRaw => item === null ? null : encodeDatedFile(item));
  out['files'] = encodeVector(value.files, (item: DatedFile | null): TdJsonRaw => item === null ? null : encodeDatedFile(item));
  out['value'] = value.value;
  out['hash'] = value.hash;
  return out;
}

/** endGroupCall = Ok — Ends a group call. Requires groupCall.can_be_managed right for video chats and live stories or groupCall.is_owned otherwise */
export class EndGroupCall {
  type: 'endGroupCall' = 'endGroupCall';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `endGroupCall` object; null input -> null. */
export function decodeEndGroupCall(json: TdJsonRaw): EndGroupCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EndGroupCall = new EndGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `endGroupCall` into TDLib JSON wire shape. */
export function encodeEndGroupCall(value: EndGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'endGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** endGroupCallRecording = Ok — Ends recording of an active group call; for video chats only. Requires groupCall.can_be_managed right */
export class EndGroupCallRecording {
  type: 'endGroupCallRecording' = 'endGroupCallRecording';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `endGroupCallRecording` object; null input -> null. */
export function decodeEndGroupCallRecording(json: TdJsonRaw): EndGroupCallRecording | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EndGroupCallRecording = new EndGroupCallRecording();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `endGroupCallRecording` into TDLib JSON wire shape. */
export function encodeEndGroupCallRecording(value: EndGroupCallRecording): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'endGroupCallRecording';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** endGroupCallScreenSharing = Ok — Ends screen sharing in a joined group call; not supported in live stories */
export class EndGroupCallScreenSharing {
  type: 'endGroupCallScreenSharing' = 'endGroupCallScreenSharing';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
}

/** Decode a TDLib `endGroupCallScreenSharing` object; null input -> null. */
export function decodeEndGroupCallScreenSharing(json: TdJsonRaw): EndGroupCallScreenSharing | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EndGroupCallScreenSharing = new EndGroupCallScreenSharing();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  return out;
}

/** Encode a `endGroupCallScreenSharing` into TDLib JSON wire shape. */
export function encodeEndGroupCallScreenSharing(value: EndGroupCallScreenSharing): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'endGroupCallScreenSharing';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  return out;
}

/** ephemeralMessageContent = EphemeralMessageContent — Describes an ephemeral content of a regular message, which must be shown instead of the regular content */
export class EphemeralMessageContent {
  type: 'ephemeralMessageContent' = 'ephemeralMessageContent';
  extra?: string;
  /** can_be_saved: Bool — True, if content of the message can be saved locally */
  can_be_saved: boolean = false;
  /** has_timestamped_media: Bool — True, if media timestamp entities refers to a media in this message as opposed to a media in the replied message */
  has_timestamped_media: boolean = false;
  /** content: MessageContent — Content of the message */
  content: TdMessageContent | null = null;
  /** reply_markup: ReplyMarkup — Reply markup for the message; may be null if none */
  reply_markup: TdReplyMarkup | null = null;
}

/** Decode a TDLib `ephemeralMessageContent` object; null input -> null. */
export function decodeEphemeralMessageContent(json: TdJsonRaw): EphemeralMessageContent | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: EphemeralMessageContent = new EphemeralMessageContent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_be_saved = asBool(record['can_be_saved']);
  out.has_timestamped_media = asBool(record['has_timestamped_media']);
  out.content = decodeTdMessageContent(record['content']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  return out;
}

/** Encode a `ephemeralMessageContent` into TDLib JSON wire shape. */
export function encodeEphemeralMessageContent(value: EphemeralMessageContent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'ephemeralMessageContent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_be_saved'] = value.can_be_saved;
  out['has_timestamped_media'] = value.has_timestamped_media;
  out['content'] = value.content === null ? null : encodeTdMessageContent(value.content);
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  return out;
}

/** error = Error — An object of this type can be returned on every function call, in case of an error */
export class ErrorValue {
  type: 'error' = 'error';
  extra?: string;
  /** code: int32 — Error code; subject to future changes. If the error code is 406, the error message must not be processed in any way and must not be displayed to the user */
  code: number = 0;
  /** message: string — Error message; subject to future changes */
  message: string = '';
}

/** Decode a TDLib `error` object; null input -> null. */
export function decodeErrorValue(json: TdJsonRaw): ErrorValue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ErrorValue = new ErrorValue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asNumber(record['code']);
  out.message = asString(record['message']);
  return out;
}

/** Encode a `error` into TDLib JSON wire shape. */
export function encodeErrorValue(value: ErrorValue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'error';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  out['message'] = value.message;
  return out;
}
