/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { AccentColor, Address, AgeVerificationParameters, AttachmentMenuBot, Audio, decodeAccentColor, decodeAddress, decodeAgeVerificationParameters, decodeAttachmentMenuBot, decodeAudio, encodeAccentColor, encodeAddress, encodeAgeVerificationParameters, encodeAttachmentMenuBot, encodeAudio } from './TdTypes_A';
import { Background, BasicGroup, BasicGroupFullInfo, Birthdate, BotInfo, BotVerification, BusinessBotManageBar, BusinessConnection, BusinessInfo, BusinessMessage, decodeBackground, decodeBasicGroup, decodeBasicGroupFullInfo, decodeBirthdate, decodeBotInfo, decodeBotVerification, decodeBusinessBotManageBar, decodeBusinessConnection, decodeBusinessInfo, decodeBusinessMessage, encodeBackground, encodeBasicGroup, encodeBasicGroupFullInfo, encodeBirthdate, encodeBotInfo, encodeBotVerification, encodeBusinessBotManageBar, encodeBusinessConnection, encodeBusinessInfo, encodeBusinessMessage } from './TdTypes_B';
import { Call, Chat, ChatActiveStories, ChatBackground, ChatBoost, ChatFolderInfo, ChatInviteLink, ChatJoinRequest, ChatJoinRequestsInfo, ChatMember, ChatNotificationSettings, ChatPermissions, ChatPhoto, ChatPhotoInfo, ChatPosition, ChatRevenueAmount, CloseBirthdayUser, Community, CommunityFullInfo, decodeCall, decodeChat, decodeChatActiveStories, decodeChatBackground, decodeChatBoost, decodeChatFolderInfo, decodeChatInviteLink, decodeChatJoinRequest, decodeChatJoinRequestsInfo, decodeChatMember, decodeChatNotificationSettings, decodeChatPermissions, decodeChatPhoto, decodeChatPhotoInfo, decodeChatPosition, decodeChatRevenueAmount, decodeCloseBirthdayUser, decodeCommunity, decodeCommunityFullInfo, encodeCall, encodeChat, encodeChatActiveStories, encodeChatBackground, encodeChatBoost, encodeChatFolderInfo, encodeChatInviteLink, encodeChatJoinRequest, encodeChatJoinRequestsInfo, encodeChatMember, encodeChatNotificationSettings, encodeChatPermissions, encodeChatPhoto, encodeChatPhotoInfo, encodeChatPosition, encodeChatRevenueAmount, encodeCloseBirthdayUser, encodeCommunity, encodeCommunityFullInfo } from './TdTypes_C';
import { DirectMessagesChatTopic, DownloadedFileCounts, DraftMessage, decodeDirectMessagesChatTopic, decodeDownloadedFileCounts, decodeDraftMessage, encodeDirectMessagesChatTopic, encodeDownloadedFileCounts, encodeDraftMessage } from './TdTypes_D';
import { EmojiChatTheme, EmojiStatus, EphemeralMessageContent, ErrorValue, decodeEmojiChatTheme, decodeEmojiStatus, decodeEphemeralMessageContent, decodeErrorValue, encodeEmojiChatTheme, encodeEmojiStatus, encodeEphemeralMessageContent, encodeErrorValue } from './TdTypes_E';
import { FactCheck, File, FileDownload, FormattedText, ForumTopicInfo, decodeFactCheck, decodeFile, decodeFileDownload, decodeFormattedText, decodeForumTopicInfo, encodeFactCheck, encodeFile, encodeFileDownload, encodeFormattedText, encodeForumTopicInfo } from './TdTypes_F';
import { GiftAuctionState, GiftResaleParameters, GiftSettings, GramRevenueStatus, GroupCall, GroupCallMessage, GroupCallMessageLevel, GroupCallParticipant, decodeGiftAuctionState, decodeGiftResaleParameters, decodeGiftSettings, decodeGramRevenueStatus, decodeGroupCall, decodeGroupCallMessage, decodeGroupCallMessageLevel, decodeGroupCallParticipant, encodeGiftAuctionState, encodeGiftResaleParameters, encodeGiftSettings, encodeGramRevenueStatus, encodeGroupCall, encodeGroupCallMessage, encodeGroupCallMessageLevel, encodeGroupCallParticipant } from './TdTypes_G';
import { LanguagePackString, LiveStoryDonors, Location, decodeLanguagePackString, decodeLiveStoryDonors, decodeLocation, encodeLanguagePackString, encodeLiveStoryDonors, encodeLocation } from './TdTypes_L';
import { Message, MessageInteractionInfo, MessageReaction, decodeMessage, decodeMessageInteractionInfo, decodeMessageReaction, encodeMessage, encodeMessageInteractionInfo, encodeMessageReaction } from './TdTypes_M';
import { Notification, NotificationGroup, decodeNotification, decodeNotificationGroup, encodeNotification, encodeNotificationGroup } from './TdTypes_N';
import { OrderInfo, decodeOrderInfo, encodeOrderInfo } from './TdTypes_O';
import { Poll, ProfileAccentColor, ProfilePhoto, decodePoll, decodeProfileAccentColor, decodeProfilePhoto, encodePoll, encodeProfileAccentColor, encodeProfilePhoto } from './TdTypes_P';
import { QuickReplyMessage, QuickReplyShortcut, decodeQuickReplyMessage, decodeQuickReplyShortcut, encodeQuickReplyMessage, encodeQuickReplyShortcut } from './TdTypes_Q';
import { ReactionNotificationSettings, RestrictionInfo, decodeReactionNotificationSettings, decodeRestrictionInfo, encodeReactionNotificationSettings, encodeRestrictionInfo } from './TdTypes_R';
import { SavedMessagesTags, SavedMessagesTopic, ScopeAutosaveSettings, ScopeNotificationSettings, SecretChat, StakeDiceState, StarAmount, StarRevenueStatus, Sticker, StickerSet, Story, SuggestedPostInfo, Supergroup, SupergroupFullInfo, decodeSavedMessagesTags, decodeSavedMessagesTopic, decodeScopeAutosaveSettings, decodeScopeNotificationSettings, decodeSecretChat, decodeStakeDiceState, decodeStarAmount, decodeStarRevenueStatus, decodeSticker, decodeStickerSet, decodeStory, decodeSuggestedPostInfo, decodeSupergroup, decodeSupergroupFullInfo, encodeSavedMessagesTags, encodeSavedMessagesTopic, encodeScopeAutosaveSettings, encodeScopeNotificationSettings, encodeSecretChat, encodeStakeDiceState, encodeStarAmount, encodeStarRevenueStatus, encodeSticker, encodeStickerSet, encodeStory, encodeSuggestedPostInfo, encodeSupergroup, encodeSupergroupFullInfo } from './TdTypes_S';
import { TermsOfService, TextCompositionStyle, TrendingStickerSets, decodeTermsOfService, decodeTextCompositionStyle, decodeTrendingStickerSets, encodeTermsOfService, encodeTextCompositionStyle, encodeTrendingStickerSets } from './TdTypes_T';
import { VerificationStatus, VideoChat, decodeVerificationStatus, decodeVideoChat, encodeVerificationStatus, encodeVideoChat } from './TdTypes_V';
import { WebBrowserSettings, WelcomeMessage, decodeWebBrowserSettings, decodeWelcomeMessage, encodeWebBrowserSettings, encodeWelcomeMessage } from './TdTypes_W';
import { TdActiveStoryState, TdAuthorizationState, TdAutosaveSettingsScope, decodeTdActiveStoryState, decodeTdAuthorizationState, decodeTdAutosaveSettingsScope, encodeTdActiveStoryState, encodeTdAuthorizationState, encodeTdAutosaveSettingsScope } from './TdUnions_A';
import { TdBlockList, decodeTdBlockList, encodeTdBlockList } from './TdUnions_B';
import { TdCallbackQueryPayload, TdCanPostStoryResult, TdChatAction, TdChatActionBar, TdChatAvailableReactions, TdChatJoinRequestResult, TdChatList, TdChatTheme, TdChatType, TdConnectionState, decodeTdCallbackQueryPayload, decodeTdCanPostStoryResult, decodeTdChatAction, decodeTdChatActionBar, decodeTdChatAvailableReactions, decodeTdChatJoinRequestResult, decodeTdChatList, decodeTdChatTheme, decodeTdChatType, decodeTdConnectionState, encodeTdCallbackQueryPayload, encodeTdCanPostStoryResult, encodeTdChatAction, encodeTdChatActionBar, encodeTdChatAvailableReactions, encodeTdChatJoinRequestResult, encodeTdChatList, encodeTdChatTheme, encodeTdChatType, encodeTdConnectionState } from './TdUnions_C';
import { TdGiftResalePrice, decodeTdGiftResalePrice, encodeTdGiftResalePrice } from './TdUnions_G';
import { TdInputFile, decodeTdInputFile, encodeTdInputFile } from './TdUnions_I';
import { TdMessageContent, TdMessageSender, TdMessageTopic, decodeTdMessageContent, decodeTdMessageSender, decodeTdMessageTopic, encodeTdMessageContent, encodeTdMessageSender, encodeTdMessageTopic } from './TdUnions_M';
import { TdNotificationGroupType, TdNotificationSettingsScope, decodeTdNotificationGroupType, decodeTdNotificationSettingsScope, encodeTdNotificationGroupType, encodeTdNotificationSettingsScope } from './TdUnions_N';
import { TdOptionValue, decodeTdOptionValue, encodeTdOptionValue } from './TdUnions_O';
import { TdPaidReactionType, TdProfileTab, decodeTdPaidReactionType, decodeTdProfileTab, encodeTdPaidReactionType, encodeTdProfileTab } from './TdUnions_P';
import { TdReactionType, TdReplyMarkup, decodeTdReactionType, decodeTdReplyMarkup, encodeTdReactionType, encodeTdReplyMarkup } from './TdUnions_R';
import { TdSessionType, TdStickerFormat, TdStickerType, TdStoryList, TdSuggestedAction, decodeTdSessionType, decodeTdStickerFormat, decodeTdStickerType, decodeTdStoryList, decodeTdSuggestedAction, encodeTdSessionType, encodeTdStickerFormat, encodeTdStickerType, encodeTdStoryList, encodeTdSuggestedAction } from './TdUnions_S';
import { TdUpdate, TdUpgradedGiftAttributeRarity, TdUserPrivacySetting, TdUserPrivacySettingRule, TdUserStatus, TdUserType, decodeTdUpdate, decodeTdUpgradedGiftAttributeRarity, decodeTdUserPrivacySetting, decodeTdUserPrivacySettingRule, decodeTdUserStatus, decodeTdUserType, encodeTdUpdate, encodeTdUpgradedGiftAttributeRarity, encodeTdUserPrivacySetting, encodeTdUserPrivacySettingRule, encodeTdUserStatus, encodeTdUserType } from './TdUnions_U';

/** unconfirmedSession = UnconfirmedSession — Contains information about an unconfirmed session */
export class UnconfirmedSession {
  type: 'unconfirmedSession' = 'unconfirmedSession';
  extra?: string;
  /** type: SessionType — Session type */
  type_: TdSessionType | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the user has logged in or the business bot was connected */
  date: number = 0;
  /** device_model: string — Model of the device that was used for the session creation, as provided by the application */
  device_model: string = '';
  /** location: string — A human-readable description of the location from which the session was created, based on the IP address */
  location: string = '';
}

/** Decode a TDLib `unconfirmedSession` object; null input -> null. */
export function decodeUnconfirmedSession(json: TdJsonRaw): UnconfirmedSession | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UnconfirmedSession = new UnconfirmedSession();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdSessionType(record['type']);
  out.date = asNumber(record['date']);
  out.device_model = asString(record['device_model']);
  out.location = asString(record['location']);
  return out;
}

/** Encode a `unconfirmedSession` into TDLib JSON wire shape. */
export function encodeUnconfirmedSession(value: UnconfirmedSession): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'unconfirmedSession';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdSessionType(value.type_);
  out['date'] = value.date;
  out['device_model'] = value.device_model;
  out['location'] = value.location;
  return out;
}

/** unpinAllChatMessages = Ok — Removes all pinned messages from a chat; requires can_pin_messages member right if the chat is a basic group or supergroup, or can_edit_messages administrator right if the chat is a channel */
export class UnpinAllChatMessages {
  type: 'unpinAllChatMessages' = 'unpinAllChatMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
}

/** Decode a TDLib `unpinAllChatMessages` object; null input -> null. */
export function decodeUnpinAllChatMessages(json: TdJsonRaw): UnpinAllChatMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UnpinAllChatMessages = new UnpinAllChatMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `unpinAllChatMessages` into TDLib JSON wire shape. */
export function encodeUnpinAllChatMessages(value: UnpinAllChatMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'unpinAllChatMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** unpinAllDirectMessagesChatTopicMessages = Ok — Removes all pinned messages from the topic in a channel direct messages chat administered by the current user */
export class UnpinAllDirectMessagesChatTopicMessages {
  type: 'unpinAllDirectMessagesChatTopicMessages' = 'unpinAllDirectMessagesChatTopicMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** topic_id: int53 — Topic identifier */
  topic_id: number = 0;
}

/** Decode a TDLib `unpinAllDirectMessagesChatTopicMessages` object; null input -> null. */
export function decodeUnpinAllDirectMessagesChatTopicMessages(json: TdJsonRaw): UnpinAllDirectMessagesChatTopicMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UnpinAllDirectMessagesChatTopicMessages = new UnpinAllDirectMessagesChatTopicMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = asNumber(record['topic_id']);
  return out;
}

/** Encode a `unpinAllDirectMessagesChatTopicMessages` into TDLib JSON wire shape. */
export function encodeUnpinAllDirectMessagesChatTopicMessages(value: UnpinAllDirectMessagesChatTopicMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'unpinAllDirectMessagesChatTopicMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id;
  return out;
}

/** unpinAllForumTopicMessages = Ok — Removes all pinned messages from a topic in a forum supergroup chat or a chat with a bot with topics; requires can_pin_messages member right in the supergroup */
export class UnpinAllForumTopicMessages {
  type: 'unpinAllForumTopicMessages' = 'unpinAllForumTopicMessages';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier in which messages will be unpinned */
  forum_topic_id: number = 0;
}

/** Decode a TDLib `unpinAllForumTopicMessages` object; null input -> null. */
export function decodeUnpinAllForumTopicMessages(json: TdJsonRaw): UnpinAllForumTopicMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UnpinAllForumTopicMessages = new UnpinAllForumTopicMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  return out;
}

/** Encode a `unpinAllForumTopicMessages` into TDLib JSON wire shape. */
export function encodeUnpinAllForumTopicMessages(value: UnpinAllForumTopicMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'unpinAllForumTopicMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  return out;
}

/** unpinChatMessage = Ok — Removes a pinned message from a chat; requires can_pin_messages member right if the chat is a basic group or supergroup, or can_edit_messages administrator right if the chat is a channel */
export class UnpinChatMessage {
  type: 'unpinChatMessage' = 'unpinChatMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the removed pinned message */
  message_id: number = 0;
}

/** Decode a TDLib `unpinChatMessage` object; null input -> null. */
export function decodeUnpinChatMessage(json: TdJsonRaw): UnpinChatMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UnpinChatMessage = new UnpinChatMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `unpinChatMessage` into TDLib JSON wire shape. */
export function encodeUnpinChatMessage(value: UnpinChatMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'unpinChatMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** unreadReaction = UnreadReaction — Contains information about an unread reaction to a message */
export class UnreadReaction {
  type: 'unreadReaction' = 'unreadReaction';
  extra?: string;
  /** type: ReactionType — Type of the reaction */
  type_: TdReactionType | null = null;
  /** sender_id: MessageSender — Identifier of the sender, added the reaction */
  sender_id: TdMessageSender | null = null;
  /** is_big: Bool — True, if the reaction was added with a big animation */
  is_big: boolean = false;
}

/** Decode a TDLib `unreadReaction` object; null input -> null. */
export function decodeUnreadReaction(json: TdJsonRaw): UnreadReaction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UnreadReaction = new UnreadReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdReactionType(record['type']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.is_big = asBool(record['is_big']);
  return out;
}

/** Encode a `unreadReaction` into TDLib JSON wire shape. */
export function encodeUnreadReaction(value: UnreadReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'unreadReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdReactionType(value.type_);
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['is_big'] = value.is_big;
  return out;
}

/** updateAccentColors = Update — The list of supported accent colors has changed and aren't included in the list. The exact colors for the accent colors with identifiers 0-6 must be taken from the application theme */
export class UpdateAccentColors {
  type: 'updateAccentColors' = 'updateAccentColors';
  extra?: string;
  /** colors: vector<accentColor> — Information about supported colors; colors with identifiers 0 (red), 1 (orange), 2 (purple/violet), 3 (green), 4 (cyan), 5 (blue), 6 (pink) must always be supported */
  colors: AccentColor[] = [];
  /** available_accent_color_ids: vector<int32> — The list of accent color identifiers, which can be set through setAccentColor and setChatAccentColor. The colors must be shown in the specified order */
  available_accent_color_ids: number[] = [];
}

/** Decode a TDLib `updateAccentColors` object; null input -> null. */
export function decodeUpdateAccentColors(json: TdJsonRaw): UpdateAccentColors | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateAccentColors = new UpdateAccentColors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.colors = decodeVector(record['colors'], (item: TdJsonRaw): AccentColor => decodeAccentColor(item) as AccentColor);
  out.available_accent_color_ids = decodeVector(record['available_accent_color_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateAccentColors` into TDLib JSON wire shape. */
export function encodeUpdateAccentColors(value: UpdateAccentColors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateAccentColors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['colors'] = encodeVector(value.colors, (item: AccentColor | null): TdJsonRaw => item === null ? null : encodeAccentColor(item));
  out['available_accent_color_ids'] = encodeVector(value.available_accent_color_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateActiveEmojiReactions = Update — The list of active emoji reactions has changed */
export class UpdateActiveEmojiReactions {
  type: 'updateActiveEmojiReactions' = 'updateActiveEmojiReactions';
  extra?: string;
  /** emojis: vector<string> — The new list of active emoji reactions */
  emojis: string[] = [];
}

/** Decode a TDLib `updateActiveEmojiReactions` object; null input -> null. */
export function decodeUpdateActiveEmojiReactions(json: TdJsonRaw): UpdateActiveEmojiReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateActiveEmojiReactions = new UpdateActiveEmojiReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emojis = decodeVector(record['emojis'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `updateActiveEmojiReactions` into TDLib JSON wire shape. */
export function encodeUpdateActiveEmojiReactions(value: UpdateActiveEmojiReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateActiveEmojiReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emojis'] = encodeVector(value.emojis, (item: string): TdJsonRaw => item);
  return out;
}

/** updateActiveGiftAuctions = Update — The list of auctions in which the current user participates has changed */
export class UpdateActiveGiftAuctions {
  type: 'updateActiveGiftAuctions' = 'updateActiveGiftAuctions';
  extra?: string;
  /** states: vector<giftAuctionState> — New states of the auctions */
  states: GiftAuctionState[] = [];
}

/** Decode a TDLib `updateActiveGiftAuctions` object; null input -> null. */
export function decodeUpdateActiveGiftAuctions(json: TdJsonRaw): UpdateActiveGiftAuctions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateActiveGiftAuctions = new UpdateActiveGiftAuctions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.states = decodeVector(record['states'], (item: TdJsonRaw): GiftAuctionState => decodeGiftAuctionState(item) as GiftAuctionState);
  return out;
}

/** Encode a `updateActiveGiftAuctions` into TDLib JSON wire shape. */
export function encodeUpdateActiveGiftAuctions(value: UpdateActiveGiftAuctions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateActiveGiftAuctions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['states'] = encodeVector(value.states, (item: GiftAuctionState | null): TdJsonRaw => item === null ? null : encodeGiftAuctionState(item));
  return out;
}

/** updateActiveLiveLocationMessages = Update — The list of messages with active live location that need to be updated by the application has changed. The list is persistent across application restarts only if the message database is used */
export class UpdateActiveLiveLocationMessages {
  type: 'updateActiveLiveLocationMessages' = 'updateActiveLiveLocationMessages';
  extra?: string;
  /** messages: vector<message> — The list of messages with active live locations */
  messages: Message[] = [];
}

/** Decode a TDLib `updateActiveLiveLocationMessages` object; null input -> null. */
export function decodeUpdateActiveLiveLocationMessages(json: TdJsonRaw): UpdateActiveLiveLocationMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateActiveLiveLocationMessages = new UpdateActiveLiveLocationMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): Message => decodeMessage(item) as Message);
  return out;
}

/** Encode a `updateActiveLiveLocationMessages` into TDLib JSON wire shape. */
export function encodeUpdateActiveLiveLocationMessages(value: UpdateActiveLiveLocationMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateActiveLiveLocationMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['messages'] = encodeVector(value.messages, (item: Message | null): TdJsonRaw => item === null ? null : encodeMessage(item));
  return out;
}

/** updateActiveNotifications = Update — Contains active notifications that were shown on previous application launches. This update is sent only if the message database is used. In that case it comes once before any updateNotification and u... */
export class UpdateActiveNotifications {
  type: 'updateActiveNotifications' = 'updateActiveNotifications';
  extra?: string;
  /** groups: vector<notificationGroup> — Lists of active notification groups */
  groups: NotificationGroup[] = [];
}

/** Decode a TDLib `updateActiveNotifications` object; null input -> null. */
export function decodeUpdateActiveNotifications(json: TdJsonRaw): UpdateActiveNotifications | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateActiveNotifications = new UpdateActiveNotifications();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.groups = decodeVector(record['groups'], (item: TdJsonRaw): NotificationGroup => decodeNotificationGroup(item) as NotificationGroup);
  return out;
}

/** Encode a `updateActiveNotifications` into TDLib JSON wire shape. */
export function encodeUpdateActiveNotifications(value: UpdateActiveNotifications): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateActiveNotifications';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['groups'] = encodeVector(value.groups, (item: NotificationGroup | null): TdJsonRaw => item === null ? null : encodeNotificationGroup(item));
  return out;
}

/** updateAgeVerificationParameters = Update — The parameters for age verification of the current user's account have changed */
export class UpdateAgeVerificationParameters {
  type: 'updateAgeVerificationParameters' = 'updateAgeVerificationParameters';
  extra?: string;
  /** parameters: ageVerificationParameters — Parameters for the age verification; may be null if age verification isn't needed */
  parameters: AgeVerificationParameters | null = null;
}

/** Decode a TDLib `updateAgeVerificationParameters` object; null input -> null. */
export function decodeUpdateAgeVerificationParameters(json: TdJsonRaw): UpdateAgeVerificationParameters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateAgeVerificationParameters = new UpdateAgeVerificationParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.parameters = decodeAgeVerificationParameters(record['parameters']);
  return out;
}

/** Encode a `updateAgeVerificationParameters` into TDLib JSON wire shape. */
export function encodeUpdateAgeVerificationParameters(value: UpdateAgeVerificationParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateAgeVerificationParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['parameters'] = value.parameters === null ? null : encodeAgeVerificationParameters(value.parameters);
  return out;
}

/** updateAnimatedEmojiMessageClicked = Update — Some animated emoji message was clicked and a big animated sticker must be played if the message is visible on the screen. chatActionWatchingAnimations with the text of the message needs to be... */
export class UpdateAnimatedEmojiMessageClicked {
  type: 'updateAnimatedEmojiMessageClicked' = 'updateAnimatedEmojiMessageClicked';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** sticker: sticker — The animated sticker to be played */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `updateAnimatedEmojiMessageClicked` object; null input -> null. */
export function decodeUpdateAnimatedEmojiMessageClicked(json: TdJsonRaw): UpdateAnimatedEmojiMessageClicked | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateAnimatedEmojiMessageClicked = new UpdateAnimatedEmojiMessageClicked();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `updateAnimatedEmojiMessageClicked` into TDLib JSON wire shape. */
export function encodeUpdateAnimatedEmojiMessageClicked(value: UpdateAnimatedEmojiMessageClicked): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateAnimatedEmojiMessageClicked';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** updateAnimationSearchParameters = Update — The parameters of animation search through getOption("animation_search_bot_username") bot have changed */
export class UpdateAnimationSearchParameters {
  type: 'updateAnimationSearchParameters' = 'updateAnimationSearchParameters';
  extra?: string;
  /** provider: string — Name of the animation search provider */
  provider: string = '';
  /** emojis: vector<string> — The new list of emojis suggested for searching */
  emojis: string[] = [];
}

/** Decode a TDLib `updateAnimationSearchParameters` object; null input -> null. */
export function decodeUpdateAnimationSearchParameters(json: TdJsonRaw): UpdateAnimationSearchParameters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateAnimationSearchParameters = new UpdateAnimationSearchParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.provider = asString(record['provider']);
  out.emojis = decodeVector(record['emojis'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `updateAnimationSearchParameters` into TDLib JSON wire shape. */
export function encodeUpdateAnimationSearchParameters(value: UpdateAnimationSearchParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateAnimationSearchParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['provider'] = value.provider;
  out['emojis'] = encodeVector(value.emojis, (item: string): TdJsonRaw => item);
  return out;
}

/** updateApplicationRecaptchaVerificationRequired = Update — A request can't be completed unless reCAPTCHA verification is performed; for official mobile applications only. The method setApplicationVerificationToken must be called once the ... */
export class UpdateApplicationRecaptchaVerificationRequired {
  type: 'updateApplicationRecaptchaVerificationRequired' = 'updateApplicationRecaptchaVerificationRequired';
  extra?: string;
  /** verification_id: int53 — Unique identifier for the verification process */
  verification_id: number = 0;
  /** action: string — The action for the check */
  action: string = '';
  /** recaptcha_key_id: string — Identifier of the reCAPTCHA key */
  recaptcha_key_id: string = '';
}

/** Decode a TDLib `updateApplicationRecaptchaVerificationRequired` object; null input -> null. */
export function decodeUpdateApplicationRecaptchaVerificationRequired(json: TdJsonRaw): UpdateApplicationRecaptchaVerificationRequired | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateApplicationRecaptchaVerificationRequired = new UpdateApplicationRecaptchaVerificationRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.verification_id = asNumber(record['verification_id']);
  out.action = asString(record['action']);
  out.recaptcha_key_id = asString(record['recaptcha_key_id']);
  return out;
}

/** Encode a `updateApplicationRecaptchaVerificationRequired` into TDLib JSON wire shape. */
export function encodeUpdateApplicationRecaptchaVerificationRequired(value: UpdateApplicationRecaptchaVerificationRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateApplicationRecaptchaVerificationRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['verification_id'] = value.verification_id;
  out['action'] = value.action;
  out['recaptcha_key_id'] = value.recaptcha_key_id;
  return out;
}

/** updateApplicationVerificationRequired = Update — A request can't be completed unless application verification is performed; for official mobile applications only. The method setApplicationVerificationToken must be called once the verific... */
export class UpdateApplicationVerificationRequired {
  type: 'updateApplicationVerificationRequired' = 'updateApplicationVerificationRequired';
  extra?: string;
  /** verification_id: int53 — Unique identifier for the verification process */
  verification_id: number = 0;
  /** nonce: string — Unique base64url-encoded nonce for the classic Play Integrity verification (https://developer.android.com/google/play/integrity/classic) for Android, */
  nonce: string = '';
  /** cloud_project_number: int64 — Cloud project number to pass to the Play Integrity API on Android */
  cloud_project_number: string = '';
}

/** Decode a TDLib `updateApplicationVerificationRequired` object; null input -> null. */
export function decodeUpdateApplicationVerificationRequired(json: TdJsonRaw): UpdateApplicationVerificationRequired | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateApplicationVerificationRequired = new UpdateApplicationVerificationRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.verification_id = asNumber(record['verification_id']);
  out.nonce = asString(record['nonce']);
  out.cloud_project_number = asInt64(record['cloud_project_number']);
  return out;
}

/** Encode a `updateApplicationVerificationRequired` into TDLib JSON wire shape. */
export function encodeUpdateApplicationVerificationRequired(value: UpdateApplicationVerificationRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateApplicationVerificationRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['verification_id'] = value.verification_id;
  out['nonce'] = value.nonce;
  out['cloud_project_number'] = value.cloud_project_number;
  return out;
}

/** updateAttachmentMenuBots = Update — The list of bots added to attachment or side menu has changed */
export class UpdateAttachmentMenuBots {
  type: 'updateAttachmentMenuBots' = 'updateAttachmentMenuBots';
  extra?: string;
  /** bots: vector<attachmentMenuBot> — The new list of bots. The bots must not be shown on scheduled messages screen */
  bots: AttachmentMenuBot[] = [];
}

/** Decode a TDLib `updateAttachmentMenuBots` object; null input -> null. */
export function decodeUpdateAttachmentMenuBots(json: TdJsonRaw): UpdateAttachmentMenuBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateAttachmentMenuBots = new UpdateAttachmentMenuBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bots = decodeVector(record['bots'], (item: TdJsonRaw): AttachmentMenuBot => decodeAttachmentMenuBot(item) as AttachmentMenuBot);
  return out;
}

/** Encode a `updateAttachmentMenuBots` into TDLib JSON wire shape. */
export function encodeUpdateAttachmentMenuBots(value: UpdateAttachmentMenuBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateAttachmentMenuBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bots'] = encodeVector(value.bots, (item: AttachmentMenuBot | null): TdJsonRaw => item === null ? null : encodeAttachmentMenuBot(item));
  return out;
}

/** updateAuthorizationState = Update — The user authorization state has changed */
export class UpdateAuthorizationState {
  type: 'updateAuthorizationState' = 'updateAuthorizationState';
  extra?: string;
  /** authorization_state: AuthorizationState — New authorization state */
  authorization_state: TdAuthorizationState | null = null;
}

/** Decode a TDLib `updateAuthorizationState` object; null input -> null. */
export function decodeUpdateAuthorizationState(json: TdJsonRaw): UpdateAuthorizationState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateAuthorizationState = new UpdateAuthorizationState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.authorization_state = decodeTdAuthorizationState(record['authorization_state']);
  return out;
}

/** Encode a `updateAuthorizationState` into TDLib JSON wire shape. */
export function encodeUpdateAuthorizationState(value: UpdateAuthorizationState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateAuthorizationState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['authorization_state'] = value.authorization_state === null ? null : encodeTdAuthorizationState(value.authorization_state);
  return out;
}

/** updateAutosaveSettings = Update — Autosave settings for some type of chats were updated */
export class UpdateAutosaveSettings {
  type: 'updateAutosaveSettings' = 'updateAutosaveSettings';
  extra?: string;
  /** scope: AutosaveSettingsScope — Type of chats for which autosave settings were updated */
  scope: TdAutosaveSettingsScope | null = null;
  /** settings: scopeAutosaveSettings — The new autosave settings; may be null if the settings are reset to default */
  settings: ScopeAutosaveSettings | null = null;
}

/** Decode a TDLib `updateAutosaveSettings` object; null input -> null. */
export function decodeUpdateAutosaveSettings(json: TdJsonRaw): UpdateAutosaveSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateAutosaveSettings = new UpdateAutosaveSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scope = decodeTdAutosaveSettingsScope(record['scope']);
  out.settings = decodeScopeAutosaveSettings(record['settings']);
  return out;
}

/** Encode a `updateAutosaveSettings` into TDLib JSON wire shape. */
export function encodeUpdateAutosaveSettings(value: UpdateAutosaveSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateAutosaveSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scope'] = value.scope === null ? null : encodeTdAutosaveSettingsScope(value.scope);
  out['settings'] = value.settings === null ? null : encodeScopeAutosaveSettings(value.settings);
  return out;
}

/** updateAvailableMessageEffects = Update — The list of available message effects has changed */
export class UpdateAvailableMessageEffects {
  type: 'updateAvailableMessageEffects' = 'updateAvailableMessageEffects';
  extra?: string;
  /** reaction_effect_ids: vector<int64> — The new list of available message effects from emoji reactions */
  reaction_effect_ids: string[] = [];
  /** sticker_effect_ids: vector<int64> — The new list of available message effects from Premium stickers */
  sticker_effect_ids: string[] = [];
}

/** Decode a TDLib `updateAvailableMessageEffects` object; null input -> null. */
export function decodeUpdateAvailableMessageEffects(json: TdJsonRaw): UpdateAvailableMessageEffects | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateAvailableMessageEffects = new UpdateAvailableMessageEffects();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reaction_effect_ids = decodeVector(record['reaction_effect_ids'], (item: TdJsonRaw): string => asInt64(item));
  out.sticker_effect_ids = decodeVector(record['sticker_effect_ids'], (item: TdJsonRaw): string => asInt64(item));
  return out;
}

/** Encode a `updateAvailableMessageEffects` into TDLib JSON wire shape. */
export function encodeUpdateAvailableMessageEffects(value: UpdateAvailableMessageEffects): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateAvailableMessageEffects';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reaction_effect_ids'] = encodeVector(value.reaction_effect_ids, (item: string): TdJsonRaw => item);
  out['sticker_effect_ids'] = encodeVector(value.sticker_effect_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** updateBasicGroup = Update — Some data of a basic group has changed. This update is guaranteed to come before the basic group identifier is returned to the application */
export class UpdateBasicGroup {
  type: 'updateBasicGroup' = 'updateBasicGroup';
  extra?: string;
  /** basic_group: basicGroup — New data about the group */
  basic_group: BasicGroup | null = null;
}

/** Decode a TDLib `updateBasicGroup` object; null input -> null. */
export function decodeUpdateBasicGroup(json: TdJsonRaw): UpdateBasicGroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateBasicGroup = new UpdateBasicGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.basic_group = decodeBasicGroup(record['basic_group']);
  return out;
}

/** Encode a `updateBasicGroup` into TDLib JSON wire shape. */
export function encodeUpdateBasicGroup(value: UpdateBasicGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateBasicGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['basic_group'] = value.basic_group === null ? null : encodeBasicGroup(value.basic_group);
  return out;
}

/** updateBasicGroupFullInfo = Update — Some data in basicGroupFullInfo has been changed */
export class UpdateBasicGroupFullInfo {
  type: 'updateBasicGroupFullInfo' = 'updateBasicGroupFullInfo';
  extra?: string;
  /** basic_group_id: int53 — Identifier of a basic group */
  basic_group_id: number = 0;
  /** basic_group_full_info: basicGroupFullInfo — New full information about the group */
  basic_group_full_info: BasicGroupFullInfo | null = null;
}

/** Decode a TDLib `updateBasicGroupFullInfo` object; null input -> null. */
export function decodeUpdateBasicGroupFullInfo(json: TdJsonRaw): UpdateBasicGroupFullInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateBasicGroupFullInfo = new UpdateBasicGroupFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.basic_group_id = asNumber(record['basic_group_id']);
  out.basic_group_full_info = decodeBasicGroupFullInfo(record['basic_group_full_info']);
  return out;
}

/** Encode a `updateBasicGroupFullInfo` into TDLib JSON wire shape. */
export function encodeUpdateBasicGroupFullInfo(value: UpdateBasicGroupFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateBasicGroupFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['basic_group_id'] = value.basic_group_id;
  out['basic_group_full_info'] = value.basic_group_full_info === null ? null : encodeBasicGroupFullInfo(value.basic_group_full_info);
  return out;
}

/** updateBusinessConnection = Update — A business connection has changed; for bots only */
export class UpdateBusinessConnection {
  type: 'updateBusinessConnection' = 'updateBusinessConnection';
  extra?: string;
  /** connection: businessConnection — New data about the connection */
  connection: BusinessConnection | null = null;
}

/** Decode a TDLib `updateBusinessConnection` object; null input -> null. */
export function decodeUpdateBusinessConnection(json: TdJsonRaw): UpdateBusinessConnection | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateBusinessConnection = new UpdateBusinessConnection();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.connection = decodeBusinessConnection(record['connection']);
  return out;
}

/** Encode a `updateBusinessConnection` into TDLib JSON wire shape. */
export function encodeUpdateBusinessConnection(value: UpdateBusinessConnection): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateBusinessConnection';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['connection'] = value.connection === null ? null : encodeBusinessConnection(value.connection);
  return out;
}

/** updateBusinessMessageEdited = Update — A message in a business account was edited; for bots only */
export class UpdateBusinessMessageEdited {
  type: 'updateBusinessMessageEdited' = 'updateBusinessMessageEdited';
  extra?: string;
  /** connection_id: string — Unique identifier of the business connection */
  connection_id: string = '';
  /** message: businessMessage — The edited message */
  message: BusinessMessage | null = null;
}

/** Decode a TDLib `updateBusinessMessageEdited` object; null input -> null. */
export function decodeUpdateBusinessMessageEdited(json: TdJsonRaw): UpdateBusinessMessageEdited | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateBusinessMessageEdited = new UpdateBusinessMessageEdited();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.connection_id = asString(record['connection_id']);
  out.message = decodeBusinessMessage(record['message']);
  return out;
}

/** Encode a `updateBusinessMessageEdited` into TDLib JSON wire shape. */
export function encodeUpdateBusinessMessageEdited(value: UpdateBusinessMessageEdited): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateBusinessMessageEdited';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['connection_id'] = value.connection_id;
  out['message'] = value.message === null ? null : encodeBusinessMessage(value.message);
  return out;
}

/** updateBusinessMessagesDeleted = Update — Messages in a business account were deleted; for bots only */
export class UpdateBusinessMessagesDeleted {
  type: 'updateBusinessMessagesDeleted' = 'updateBusinessMessagesDeleted';
  extra?: string;
  /** connection_id: string — Unique identifier of the business connection */
  connection_id: string = '';
  /** chat_id: int53 — Identifier of a chat in the business account in which messages were deleted */
  chat_id: number = 0;
  /** message_ids: vector<int53> — Unique message identifiers of the deleted messages */
  message_ids: number[] = [];
}

/** Decode a TDLib `updateBusinessMessagesDeleted` object; null input -> null. */
export function decodeUpdateBusinessMessagesDeleted(json: TdJsonRaw): UpdateBusinessMessagesDeleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateBusinessMessagesDeleted = new UpdateBusinessMessagesDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.connection_id = asString(record['connection_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateBusinessMessagesDeleted` into TDLib JSON wire shape. */
export function encodeUpdateBusinessMessagesDeleted(value: UpdateBusinessMessagesDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateBusinessMessagesDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['connection_id'] = value.connection_id;
  out['chat_id'] = value.chat_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateCall = Update — New call was created or information about a call was updated */
export class UpdateCall {
  type: 'updateCall' = 'updateCall';
  extra?: string;
  /** call: call — New data about a call */
  call: Call | null = null;
}

/** Decode a TDLib `updateCall` object; null input -> null. */
export function decodeUpdateCall(json: TdJsonRaw): UpdateCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateCall = new UpdateCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call = decodeCall(record['call']);
  return out;
}

/** Encode a `updateCall` into TDLib JSON wire shape. */
export function encodeUpdateCall(value: UpdateCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call'] = value.call === null ? null : encodeCall(value.call);
  return out;
}

/** updateChatAccentColors = Update — Chat accent colors have changed */
export class UpdateChatAccentColors {
  type: 'updateChatAccentColors' = 'updateChatAccentColors';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** accent_color_id: int32 — The new chat accent color identifier */
  accent_color_id: number = 0;
  /** background_custom_emoji_id: int64 — The new identifier of a custom emoji to be shown on the reply header and link preview background; 0 if none */
  background_custom_emoji_id: string = '';
  /** upgraded_gift_colors: upgradedGiftColors — Color scheme based on an upgraded gift to be used for the chat instead of accent_color_id and background_custom_emoji_id; may be null if none */
  upgraded_gift_colors: UpgradedGiftColors | null = null;
  /** profile_accent_color_id: int32 — The new chat profile accent color identifier; -1 if none */
  profile_accent_color_id: number = 0;
  /** profile_background_custom_emoji_id: int64 — The new identifier of a custom emoji to be shown on the profile background; 0 if none */
  profile_background_custom_emoji_id: string = '';
}

/** Decode a TDLib `updateChatAccentColors` object; null input -> null. */
export function decodeUpdateChatAccentColors(json: TdJsonRaw): UpdateChatAccentColors | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatAccentColors = new UpdateChatAccentColors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.accent_color_id = asNumber(record['accent_color_id']);
  out.background_custom_emoji_id = asInt64(record['background_custom_emoji_id']);
  out.upgraded_gift_colors = decodeUpgradedGiftColors(record['upgraded_gift_colors']);
  out.profile_accent_color_id = asNumber(record['profile_accent_color_id']);
  out.profile_background_custom_emoji_id = asInt64(record['profile_background_custom_emoji_id']);
  return out;
}

/** Encode a `updateChatAccentColors` into TDLib JSON wire shape. */
export function encodeUpdateChatAccentColors(value: UpdateChatAccentColors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatAccentColors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['accent_color_id'] = value.accent_color_id;
  out['background_custom_emoji_id'] = value.background_custom_emoji_id;
  out['upgraded_gift_colors'] = value.upgraded_gift_colors === null ? null : encodeUpgradedGiftColors(value.upgraded_gift_colors);
  out['profile_accent_color_id'] = value.profile_accent_color_id;
  out['profile_background_custom_emoji_id'] = value.profile_background_custom_emoji_id;
  return out;
}

/** updateChatAction = Update — A message sender activity in the chat has changed */
export class UpdateChatAction {
  type: 'updateChatAction' = 'updateChatAction';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Identifier of the specific topic in which the action was performed; may be null if none */
  topic_id: TdMessageTopic | null = null;
  /** sender_id: MessageSender — Identifier of a message sender performing the action */
  sender_id: TdMessageSender | null = null;
  /** action: ChatAction — The action */
  action: TdChatAction | null = null;
}

/** Decode a TDLib `updateChatAction` object; null input -> null. */
export function decodeUpdateChatAction(json: TdJsonRaw): UpdateChatAction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatAction = new UpdateChatAction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.action = decodeTdChatAction(record['action']);
  return out;
}

/** Encode a `updateChatAction` into TDLib JSON wire shape. */
export function encodeUpdateChatAction(value: UpdateChatAction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatAction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['action'] = value.action === null ? null : encodeTdChatAction(value.action);
  return out;
}

/** updateChatActionBar = Update — The chat action bar was changed */
export class UpdateChatActionBar {
  type: 'updateChatActionBar' = 'updateChatActionBar';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** action_bar: ChatActionBar — The new value of the action bar; may be null */
  action_bar: TdChatActionBar | null = null;
}

/** Decode a TDLib `updateChatActionBar` object; null input -> null. */
export function decodeUpdateChatActionBar(json: TdJsonRaw): UpdateChatActionBar | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatActionBar = new UpdateChatActionBar();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.action_bar = decodeTdChatActionBar(record['action_bar']);
  return out;
}

/** Encode a `updateChatActionBar` into TDLib JSON wire shape. */
export function encodeUpdateChatActionBar(value: UpdateChatActionBar): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatActionBar';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['action_bar'] = value.action_bar === null ? null : encodeTdChatActionBar(value.action_bar);
  return out;
}

/** updateChatActiveStories = Update — The list of active stories posted by a specific chat has changed */
export class UpdateChatActiveStories {
  type: 'updateChatActiveStories' = 'updateChatActiveStories';
  extra?: string;
  /** active_stories: chatActiveStories — The new list of active stories */
  active_stories: ChatActiveStories | null = null;
}

/** Decode a TDLib `updateChatActiveStories` object; null input -> null. */
export function decodeUpdateChatActiveStories(json: TdJsonRaw): UpdateChatActiveStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatActiveStories = new UpdateChatActiveStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.active_stories = decodeChatActiveStories(record['active_stories']);
  return out;
}

/** Encode a `updateChatActiveStories` into TDLib JSON wire shape. */
export function encodeUpdateChatActiveStories(value: UpdateChatActiveStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatActiveStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['active_stories'] = value.active_stories === null ? null : encodeChatActiveStories(value.active_stories);
  return out;
}

/** updateChatAddedToList = Update — A chat was added to a chat list */
export class UpdateChatAddedToList {
  type: 'updateChatAddedToList' = 'updateChatAddedToList';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** chat_list: ChatList — The chat list to which the chat was added */
  chat_list: TdChatList | null = null;
}

/** Decode a TDLib `updateChatAddedToList` object; null input -> null. */
export function decodeUpdateChatAddedToList(json: TdJsonRaw): UpdateChatAddedToList | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatAddedToList = new UpdateChatAddedToList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.chat_list = decodeTdChatList(record['chat_list']);
  return out;
}

/** Encode a `updateChatAddedToList` into TDLib JSON wire shape. */
export function encodeUpdateChatAddedToList(value: UpdateChatAddedToList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatAddedToList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  return out;
}

/** updateChatAvailableReactions = Update — The chat available reactions were changed */
export class UpdateChatAvailableReactions {
  type: 'updateChatAvailableReactions' = 'updateChatAvailableReactions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** available_reactions: ChatAvailableReactions — The new reactions, available in the chat */
  available_reactions: TdChatAvailableReactions | null = null;
}

/** Decode a TDLib `updateChatAvailableReactions` object; null input -> null. */
export function decodeUpdateChatAvailableReactions(json: TdJsonRaw): UpdateChatAvailableReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatAvailableReactions = new UpdateChatAvailableReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.available_reactions = decodeTdChatAvailableReactions(record['available_reactions']);
  return out;
}

/** Encode a `updateChatAvailableReactions` into TDLib JSON wire shape. */
export function encodeUpdateChatAvailableReactions(value: UpdateChatAvailableReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatAvailableReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['available_reactions'] = value.available_reactions === null ? null : encodeTdChatAvailableReactions(value.available_reactions);
  return out;
}

/** updateChatBackground = Update — The chat background was changed */
export class UpdateChatBackground {
  type: 'updateChatBackground' = 'updateChatBackground';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** background: chatBackground — The new chat background; may be null if background was reset to default */
  background: ChatBackground | null = null;
}

/** Decode a TDLib `updateChatBackground` object; null input -> null. */
export function decodeUpdateChatBackground(json: TdJsonRaw): UpdateChatBackground | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatBackground = new UpdateChatBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.background = decodeChatBackground(record['background']);
  return out;
}

/** Encode a `updateChatBackground` into TDLib JSON wire shape. */
export function encodeUpdateChatBackground(value: UpdateChatBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['background'] = value.background === null ? null : encodeChatBackground(value.background);
  return out;
}

/** updateChatBlockList = Update — A chat was blocked or unblocked */
export class UpdateChatBlockList {
  type: 'updateChatBlockList' = 'updateChatBlockList';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** block_list: BlockList — Block list to which the chat is added; may be null if none */
  block_list: TdBlockList | null = null;
}

/** Decode a TDLib `updateChatBlockList` object; null input -> null. */
export function decodeUpdateChatBlockList(json: TdJsonRaw): UpdateChatBlockList | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatBlockList = new UpdateChatBlockList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.block_list = decodeTdBlockList(record['block_list']);
  return out;
}

/** Encode a `updateChatBlockList` into TDLib JSON wire shape. */
export function encodeUpdateChatBlockList(value: UpdateChatBlockList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatBlockList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['block_list'] = value.block_list === null ? null : encodeTdBlockList(value.block_list);
  return out;
}

/** updateChatBoost = Update — A chat boost has changed; for bots only */
export class UpdateChatBoost {
  type: 'updateChatBoost' = 'updateChatBoost';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** boost: chatBoost — New information about the boost */
  boost: ChatBoost | null = null;
}

/** Decode a TDLib `updateChatBoost` object; null input -> null. */
export function decodeUpdateChatBoost(json: TdJsonRaw): UpdateChatBoost | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatBoost = new UpdateChatBoost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.boost = decodeChatBoost(record['boost']);
  return out;
}

/** Encode a `updateChatBoost` into TDLib JSON wire shape. */
export function encodeUpdateChatBoost(value: UpdateChatBoost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatBoost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['boost'] = value.boost === null ? null : encodeChatBoost(value.boost);
  return out;
}

/** updateChatBusinessBotManageBar = Update — The bar for managing business bot was changed in a chat */
export class UpdateChatBusinessBotManageBar {
  type: 'updateChatBusinessBotManageBar' = 'updateChatBusinessBotManageBar';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** business_bot_manage_bar: businessBotManageBar — The new value of the business bot manage bar; may be null */
  business_bot_manage_bar: BusinessBotManageBar | null = null;
}

/** Decode a TDLib `updateChatBusinessBotManageBar` object; null input -> null. */
export function decodeUpdateChatBusinessBotManageBar(json: TdJsonRaw): UpdateChatBusinessBotManageBar | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatBusinessBotManageBar = new UpdateChatBusinessBotManageBar();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.business_bot_manage_bar = decodeBusinessBotManageBar(record['business_bot_manage_bar']);
  return out;
}

/** Encode a `updateChatBusinessBotManageBar` into TDLib JSON wire shape. */
export function encodeUpdateChatBusinessBotManageBar(value: UpdateChatBusinessBotManageBar): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatBusinessBotManageBar';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['business_bot_manage_bar'] = value.business_bot_manage_bar === null ? null : encodeBusinessBotManageBar(value.business_bot_manage_bar);
  return out;
}

/** updateChatDefaultDisableNotification = Update — The value of the default disable_notification parameter, used when a message is sent to the chat, was changed */
export class UpdateChatDefaultDisableNotification {
  type: 'updateChatDefaultDisableNotification' = 'updateChatDefaultDisableNotification';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** default_disable_notification: Bool — The new default_disable_notification value */
  default_disable_notification: boolean = false;
}

/** Decode a TDLib `updateChatDefaultDisableNotification` object; null input -> null. */
export function decodeUpdateChatDefaultDisableNotification(json: TdJsonRaw): UpdateChatDefaultDisableNotification | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatDefaultDisableNotification = new UpdateChatDefaultDisableNotification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.default_disable_notification = asBool(record['default_disable_notification']);
  return out;
}

/** Encode a `updateChatDefaultDisableNotification` into TDLib JSON wire shape. */
export function encodeUpdateChatDefaultDisableNotification(value: UpdateChatDefaultDisableNotification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatDefaultDisableNotification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['default_disable_notification'] = value.default_disable_notification;
  return out;
}

/** updateChatDraftMessage = Update — A chat draft has changed. Be aware that the update may come in the currently opened chat but with old content of the draft. If the user has changed the content of the draft, this update mustn't be applied */
export class UpdateChatDraftMessage {
  type: 'updateChatDraftMessage' = 'updateChatDraftMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** draft_message: draftMessage — The new draft message; may be null if none */
  draft_message: DraftMessage | null = null;
  /** positions: vector<chatPosition> — The new chat positions in the chat lists */
  positions: ChatPosition[] = [];
}

/** Decode a TDLib `updateChatDraftMessage` object; null input -> null. */
export function decodeUpdateChatDraftMessage(json: TdJsonRaw): UpdateChatDraftMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatDraftMessage = new UpdateChatDraftMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.draft_message = decodeDraftMessage(record['draft_message']);
  out.positions = decodeVector(record['positions'], (item: TdJsonRaw): ChatPosition => decodeChatPosition(item) as ChatPosition);
  return out;
}

/** Encode a `updateChatDraftMessage` into TDLib JSON wire shape. */
export function encodeUpdateChatDraftMessage(value: UpdateChatDraftMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatDraftMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['draft_message'] = value.draft_message === null ? null : encodeDraftMessage(value.draft_message);
  out['positions'] = encodeVector(value.positions, (item: ChatPosition | null): TdJsonRaw => item === null ? null : encodeChatPosition(item));
  return out;
}

/** updateChatEmojiStatus = Update — Chat emoji status has changed */
export class UpdateChatEmojiStatus {
  type: 'updateChatEmojiStatus' = 'updateChatEmojiStatus';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** emoji_status: emojiStatus — The new chat emoji status; may be null */
  emoji_status: EmojiStatus | null = null;
}

/** Decode a TDLib `updateChatEmojiStatus` object; null input -> null. */
export function decodeUpdateChatEmojiStatus(json: TdJsonRaw): UpdateChatEmojiStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatEmojiStatus = new UpdateChatEmojiStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.emoji_status = decodeEmojiStatus(record['emoji_status']);
  return out;
}

/** Encode a `updateChatEmojiStatus` into TDLib JSON wire shape. */
export function encodeUpdateChatEmojiStatus(value: UpdateChatEmojiStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatEmojiStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['emoji_status'] = value.emoji_status === null ? null : encodeEmojiStatus(value.emoji_status);
  return out;
}

/** updateChatFolders = Update — The list of chat folders or a chat folder has changed */
export class UpdateChatFolders {
  type: 'updateChatFolders' = 'updateChatFolders';
  extra?: string;
  /** chat_folders: vector<chatFolderInfo> — The new list of chat folders */
  chat_folders: ChatFolderInfo[] = [];
  /** main_chat_list_position: int32 — Position of the main chat list among chat folders, 0-based */
  main_chat_list_position: number = 0;
  /** are_tags_enabled: Bool — True, if folder tags are enabled */
  are_tags_enabled: boolean = false;
}

/** Decode a TDLib `updateChatFolders` object; null input -> null. */
export function decodeUpdateChatFolders(json: TdJsonRaw): UpdateChatFolders | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatFolders = new UpdateChatFolders();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folders = decodeVector(record['chat_folders'], (item: TdJsonRaw): ChatFolderInfo => decodeChatFolderInfo(item) as ChatFolderInfo);
  out.main_chat_list_position = asNumber(record['main_chat_list_position']);
  out.are_tags_enabled = asBool(record['are_tags_enabled']);
  return out;
}

/** Encode a `updateChatFolders` into TDLib JSON wire shape. */
export function encodeUpdateChatFolders(value: UpdateChatFolders): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatFolders';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folders'] = encodeVector(value.chat_folders, (item: ChatFolderInfo | null): TdJsonRaw => item === null ? null : encodeChatFolderInfo(item));
  out['main_chat_list_position'] = value.main_chat_list_position;
  out['are_tags_enabled'] = value.are_tags_enabled;
  return out;
}

/** updateChatHasProtectedContent = Update — A chat content was allowed or restricted for saving */
export class UpdateChatHasProtectedContent {
  type: 'updateChatHasProtectedContent' = 'updateChatHasProtectedContent';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** has_protected_content: Bool — New value of has_protected_content */
  has_protected_content: boolean = false;
}

/** Decode a TDLib `updateChatHasProtectedContent` object; null input -> null. */
export function decodeUpdateChatHasProtectedContent(json: TdJsonRaw): UpdateChatHasProtectedContent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatHasProtectedContent = new UpdateChatHasProtectedContent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.has_protected_content = asBool(record['has_protected_content']);
  return out;
}

/** Encode a `updateChatHasProtectedContent` into TDLib JSON wire shape. */
export function encodeUpdateChatHasProtectedContent(value: UpdateChatHasProtectedContent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatHasProtectedContent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['has_protected_content'] = value.has_protected_content;
  return out;
}

/** updateChatHasScheduledMessages = Update — A chat's has_scheduled_messages field has changed */
export class UpdateChatHasScheduledMessages {
  type: 'updateChatHasScheduledMessages' = 'updateChatHasScheduledMessages';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** has_scheduled_messages: Bool — New value of has_scheduled_messages */
  has_scheduled_messages: boolean = false;
}

/** Decode a TDLib `updateChatHasScheduledMessages` object; null input -> null. */
export function decodeUpdateChatHasScheduledMessages(json: TdJsonRaw): UpdateChatHasScheduledMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatHasScheduledMessages = new UpdateChatHasScheduledMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.has_scheduled_messages = asBool(record['has_scheduled_messages']);
  return out;
}

/** Encode a `updateChatHasScheduledMessages` into TDLib JSON wire shape. */
export function encodeUpdateChatHasScheduledMessages(value: UpdateChatHasScheduledMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatHasScheduledMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['has_scheduled_messages'] = value.has_scheduled_messages;
  return out;
}

/** updateChatHasWelcomeMessages = Update — A chat's has_welcome_messages field has changed */
export class UpdateChatHasWelcomeMessages {
  type: 'updateChatHasWelcomeMessages' = 'updateChatHasWelcomeMessages';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** has_welcome_messages: Bool — New value of has_welcome_messages */
  has_welcome_messages: boolean = false;
}

/** Decode a TDLib `updateChatHasWelcomeMessages` object; null input -> null. */
export function decodeUpdateChatHasWelcomeMessages(json: TdJsonRaw): UpdateChatHasWelcomeMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatHasWelcomeMessages = new UpdateChatHasWelcomeMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.has_welcome_messages = asBool(record['has_welcome_messages']);
  return out;
}

/** Encode a `updateChatHasWelcomeMessages` into TDLib JSON wire shape. */
export function encodeUpdateChatHasWelcomeMessages(value: UpdateChatHasWelcomeMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatHasWelcomeMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['has_welcome_messages'] = value.has_welcome_messages;
  return out;
}

/** updateChatIsMarkedAsUnread = Update — A chat was marked as unread or was read */
export class UpdateChatIsMarkedAsUnread {
  type: 'updateChatIsMarkedAsUnread' = 'updateChatIsMarkedAsUnread';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** is_marked_as_unread: Bool — New value of is_marked_as_unread */
  is_marked_as_unread: boolean = false;
}

/** Decode a TDLib `updateChatIsMarkedAsUnread` object; null input -> null. */
export function decodeUpdateChatIsMarkedAsUnread(json: TdJsonRaw): UpdateChatIsMarkedAsUnread | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatIsMarkedAsUnread = new UpdateChatIsMarkedAsUnread();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.is_marked_as_unread = asBool(record['is_marked_as_unread']);
  return out;
}

/** Encode a `updateChatIsMarkedAsUnread` into TDLib JSON wire shape. */
export function encodeUpdateChatIsMarkedAsUnread(value: UpdateChatIsMarkedAsUnread): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatIsMarkedAsUnread';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['is_marked_as_unread'] = value.is_marked_as_unread;
  return out;
}

/** updateChatIsTranslatable = Update — Translation of chat messages was enabled or disabled */
export class UpdateChatIsTranslatable {
  type: 'updateChatIsTranslatable' = 'updateChatIsTranslatable';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** is_translatable: Bool — New value of is_translatable */
  is_translatable: boolean = false;
}

/** Decode a TDLib `updateChatIsTranslatable` object; null input -> null. */
export function decodeUpdateChatIsTranslatable(json: TdJsonRaw): UpdateChatIsTranslatable | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatIsTranslatable = new UpdateChatIsTranslatable();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.is_translatable = asBool(record['is_translatable']);
  return out;
}

/** Encode a `updateChatIsTranslatable` into TDLib JSON wire shape. */
export function encodeUpdateChatIsTranslatable(value: UpdateChatIsTranslatable): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatIsTranslatable';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['is_translatable'] = value.is_translatable;
  return out;
}

/** updateChatJoinResult = Update — A join request from the user was completed */
export class UpdateChatJoinResult {
  type: 'updateChatJoinResult' = 'updateChatJoinResult';
  extra?: string;
  /** query_id: int64 — Identifier of the join request query as received in chatJoinResultGuardBotApprovalRequired. If the corresponding Web App is still open, then it must be closed */
  query_id: string = '';
  /** chat_id: int53 — Identifier of the joined chat, or 0 if the request wasn't approved */
  chat_id: number = 0;
  /** result: ChatJoinRequestResult — Result of the join */
  result: TdChatJoinRequestResult | null = null;
}

/** Decode a TDLib `updateChatJoinResult` object; null input -> null. */
export function decodeUpdateChatJoinResult(json: TdJsonRaw): UpdateChatJoinResult | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatJoinResult = new UpdateChatJoinResult();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query_id = asInt64(record['query_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.result = decodeTdChatJoinRequestResult(record['result']);
  return out;
}

/** Encode a `updateChatJoinResult` into TDLib JSON wire shape. */
export function encodeUpdateChatJoinResult(value: UpdateChatJoinResult): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatJoinResult';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query_id'] = value.query_id;
  out['chat_id'] = value.chat_id;
  out['result'] = value.result === null ? null : encodeTdChatJoinRequestResult(value.result);
  return out;
}

/** updateChatLastMessage = Update — The last message of a chat was changed */
export class UpdateChatLastMessage {
  type: 'updateChatLastMessage' = 'updateChatLastMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** last_message: message — The new last message in the chat; may be null if the last message became unknown. While the last message is unknown, new messages can be added to the chat without corresponding updateNewMessage update */
  last_message: Message | null = null;
  /** positions: vector<chatPosition> — The new chat positions in the chat lists */
  positions: ChatPosition[] = [];
}

/** Decode a TDLib `updateChatLastMessage` object; null input -> null. */
export function decodeUpdateChatLastMessage(json: TdJsonRaw): UpdateChatLastMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatLastMessage = new UpdateChatLastMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.last_message = decodeMessage(record['last_message']);
  out.positions = decodeVector(record['positions'], (item: TdJsonRaw): ChatPosition => decodeChatPosition(item) as ChatPosition);
  return out;
}

/** Encode a `updateChatLastMessage` into TDLib JSON wire shape. */
export function encodeUpdateChatLastMessage(value: UpdateChatLastMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatLastMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['last_message'] = value.last_message === null ? null : encodeMessage(value.last_message);
  out['positions'] = encodeVector(value.positions, (item: ChatPosition | null): TdJsonRaw => item === null ? null : encodeChatPosition(item));
  return out;
}

/** updateChatMember = Update — User rights changed in a chat; for bots only */
export class UpdateChatMember {
  type: 'updateChatMember' = 'updateChatMember';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** actor_user_id: int53 — Identifier of the user, changing the rights */
  actor_user_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) when the user rights were changed */
  date: number = 0;
  /** invite_link: chatInviteLink — If user has joined the chat using an invite link, the invite link; may be null */
  invite_link: ChatInviteLink | null = null;
  /** via_join_request: Bool — True, if the user has joined the chat after sending a join request and being approved by an administrator */
  via_join_request: boolean = false;
  /** via_chat_folder_invite_link: Bool — True, if the user has joined the chat using an invite link for a chat folder */
  via_chat_folder_invite_link: boolean = false;
  /** old_chat_member: chatMember — Previous chat member */
  old_chat_member: ChatMember | null = null;
  /** new_chat_member: chatMember — New chat member */
  new_chat_member: ChatMember | null = null;
}

/** Decode a TDLib `updateChatMember` object; null input -> null. */
export function decodeUpdateChatMember(json: TdJsonRaw): UpdateChatMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatMember = new UpdateChatMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.actor_user_id = asNumber(record['actor_user_id']);
  out.date = asNumber(record['date']);
  out.invite_link = decodeChatInviteLink(record['invite_link']);
  out.via_join_request = asBool(record['via_join_request']);
  out.via_chat_folder_invite_link = asBool(record['via_chat_folder_invite_link']);
  out.old_chat_member = decodeChatMember(record['old_chat_member']);
  out.new_chat_member = decodeChatMember(record['new_chat_member']);
  return out;
}

/** Encode a `updateChatMember` into TDLib JSON wire shape. */
export function encodeUpdateChatMember(value: UpdateChatMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['actor_user_id'] = value.actor_user_id;
  out['date'] = value.date;
  out['invite_link'] = value.invite_link === null ? null : encodeChatInviteLink(value.invite_link);
  out['via_join_request'] = value.via_join_request;
  out['via_chat_folder_invite_link'] = value.via_chat_folder_invite_link;
  out['old_chat_member'] = value.old_chat_member === null ? null : encodeChatMember(value.old_chat_member);
  out['new_chat_member'] = value.new_chat_member === null ? null : encodeChatMember(value.new_chat_member);
  return out;
}

/** updateChatMessageAutoDeleteTime = Update — The message auto-delete or self-destruct timer setting for a chat was changed */
export class UpdateChatMessageAutoDeleteTime {
  type: 'updateChatMessageAutoDeleteTime' = 'updateChatMessageAutoDeleteTime';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_auto_delete_time: int32 — New value of message_auto_delete_time */
  message_auto_delete_time: number = 0;
}

/** Decode a TDLib `updateChatMessageAutoDeleteTime` object; null input -> null. */
export function decodeUpdateChatMessageAutoDeleteTime(json: TdJsonRaw): UpdateChatMessageAutoDeleteTime | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatMessageAutoDeleteTime = new UpdateChatMessageAutoDeleteTime();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_auto_delete_time = asNumber(record['message_auto_delete_time']);
  return out;
}

/** Encode a `updateChatMessageAutoDeleteTime` into TDLib JSON wire shape. */
export function encodeUpdateChatMessageAutoDeleteTime(value: UpdateChatMessageAutoDeleteTime): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatMessageAutoDeleteTime';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_auto_delete_time'] = value.message_auto_delete_time;
  return out;
}

/** updateChatMessageSender = Update — The message sender that is selected to send messages in a chat has changed */
export class UpdateChatMessageSender {
  type: 'updateChatMessageSender' = 'updateChatMessageSender';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_sender_id: MessageSender — New value of message_sender_id; may be null if the user can't change message sender */
  message_sender_id: TdMessageSender | null = null;
}

/** Decode a TDLib `updateChatMessageSender` object; null input -> null. */
export function decodeUpdateChatMessageSender(json: TdJsonRaw): UpdateChatMessageSender | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatMessageSender = new UpdateChatMessageSender();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_sender_id = decodeTdMessageSender(record['message_sender_id']);
  return out;
}

/** Encode a `updateChatMessageSender` into TDLib JSON wire shape. */
export function encodeUpdateChatMessageSender(value: UpdateChatMessageSender): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatMessageSender';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_sender_id'] = value.message_sender_id === null ? null : encodeTdMessageSender(value.message_sender_id);
  return out;
}

/** updateChatNotificationSettings = Update — Notification settings for a chat were changed */
export class UpdateChatNotificationSettings {
  type: 'updateChatNotificationSettings' = 'updateChatNotificationSettings';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** notification_settings: chatNotificationSettings — The new notification settings */
  notification_settings: ChatNotificationSettings | null = null;
}

/** Decode a TDLib `updateChatNotificationSettings` object; null input -> null. */
export function decodeUpdateChatNotificationSettings(json: TdJsonRaw): UpdateChatNotificationSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatNotificationSettings = new UpdateChatNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.notification_settings = decodeChatNotificationSettings(record['notification_settings']);
  return out;
}

/** Encode a `updateChatNotificationSettings` into TDLib JSON wire shape. */
export function encodeUpdateChatNotificationSettings(value: UpdateChatNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['notification_settings'] = value.notification_settings === null ? null : encodeChatNotificationSettings(value.notification_settings);
  return out;
}

/** updateChatOnlineMemberCount = Update — The number of online group members has changed. This update with non-zero number of online group members is sent only for currently opened chats. There is no guarantee that it is sent just after the... */
export class UpdateChatOnlineMemberCount {
  type: 'updateChatOnlineMemberCount' = 'updateChatOnlineMemberCount';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** online_member_count: int32 — New number of online members in the chat, or 0 if unknown */
  online_member_count: number = 0;
}

/** Decode a TDLib `updateChatOnlineMemberCount` object; null input -> null. */
export function decodeUpdateChatOnlineMemberCount(json: TdJsonRaw): UpdateChatOnlineMemberCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatOnlineMemberCount = new UpdateChatOnlineMemberCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.online_member_count = asNumber(record['online_member_count']);
  return out;
}

/** Encode a `updateChatOnlineMemberCount` into TDLib JSON wire shape. */
export function encodeUpdateChatOnlineMemberCount(value: UpdateChatOnlineMemberCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatOnlineMemberCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['online_member_count'] = value.online_member_count;
  return out;
}

/** updateChatPendingJoinRequests = Update — The chat pending join requests were changed */
export class UpdateChatPendingJoinRequests {
  type: 'updateChatPendingJoinRequests' = 'updateChatPendingJoinRequests';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** pending_join_requests: chatJoinRequestsInfo — The new data about pending join requests; may be null */
  pending_join_requests: ChatJoinRequestsInfo | null = null;
}

/** Decode a TDLib `updateChatPendingJoinRequests` object; null input -> null. */
export function decodeUpdateChatPendingJoinRequests(json: TdJsonRaw): UpdateChatPendingJoinRequests | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatPendingJoinRequests = new UpdateChatPendingJoinRequests();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.pending_join_requests = decodeChatJoinRequestsInfo(record['pending_join_requests']);
  return out;
}

/** Encode a `updateChatPendingJoinRequests` into TDLib JSON wire shape. */
export function encodeUpdateChatPendingJoinRequests(value: UpdateChatPendingJoinRequests): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatPendingJoinRequests';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['pending_join_requests'] = value.pending_join_requests === null ? null : encodeChatJoinRequestsInfo(value.pending_join_requests);
  return out;
}

/** updateChatPermissions = Update — Chat permissions were changed */
export class UpdateChatPermissions {
  type: 'updateChatPermissions' = 'updateChatPermissions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** permissions: chatPermissions — The new chat permissions */
  permissions: ChatPermissions | null = null;
}

/** Decode a TDLib `updateChatPermissions` object; null input -> null. */
export function decodeUpdateChatPermissions(json: TdJsonRaw): UpdateChatPermissions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatPermissions = new UpdateChatPermissions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.permissions = decodeChatPermissions(record['permissions']);
  return out;
}

/** Encode a `updateChatPermissions` into TDLib JSON wire shape. */
export function encodeUpdateChatPermissions(value: UpdateChatPermissions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatPermissions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['permissions'] = value.permissions === null ? null : encodeChatPermissions(value.permissions);
  return out;
}

/** updateChatPhoto = Update — A chat photo was changed */
export class UpdateChatPhoto {
  type: 'updateChatPhoto' = 'updateChatPhoto';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** photo: chatPhotoInfo — The new chat photo; may be null */
  photo: ChatPhotoInfo | null = null;
}

/** Decode a TDLib `updateChatPhoto` object; null input -> null. */
export function decodeUpdateChatPhoto(json: TdJsonRaw): UpdateChatPhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatPhoto = new UpdateChatPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.photo = decodeChatPhotoInfo(record['photo']);
  return out;
}

/** Encode a `updateChatPhoto` into TDLib JSON wire shape. */
export function encodeUpdateChatPhoto(value: UpdateChatPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['photo'] = value.photo === null ? null : encodeChatPhotoInfo(value.photo);
  return out;
}

/** updateChatPosition = Update — The position of a chat in a chat list has changed. An updateChatLastMessage or updateChatDraftMessage update might be sent instead of the update */
export class UpdateChatPosition {
  type: 'updateChatPosition' = 'updateChatPosition';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** position: chatPosition — New chat position. If new order is 0, then the chat needs to be removed from the list */
  position: ChatPosition | null = null;
}

/** Decode a TDLib `updateChatPosition` object; null input -> null. */
export function decodeUpdateChatPosition(json: TdJsonRaw): UpdateChatPosition | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatPosition = new UpdateChatPosition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.position = decodeChatPosition(record['position']);
  return out;
}

/** Encode a `updateChatPosition` into TDLib JSON wire shape. */
export function encodeUpdateChatPosition(value: UpdateChatPosition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatPosition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['position'] = value.position === null ? null : encodeChatPosition(value.position);
  return out;
}

/** updateChatReadInbox = Update — Incoming messages were read or the number of unread messages has been changed */
export class UpdateChatReadInbox {
  type: 'updateChatReadInbox' = 'updateChatReadInbox';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** last_read_inbox_message_id: int53 — Identifier of the last read incoming message */
  last_read_inbox_message_id: number = 0;
  /** unread_count: int32 — The number of unread messages left in the chat */
  unread_count: number = 0;
}

/** Decode a TDLib `updateChatReadInbox` object; null input -> null. */
export function decodeUpdateChatReadInbox(json: TdJsonRaw): UpdateChatReadInbox | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatReadInbox = new UpdateChatReadInbox();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.last_read_inbox_message_id = asNumber(record['last_read_inbox_message_id']);
  out.unread_count = asNumber(record['unread_count']);
  return out;
}

/** Encode a `updateChatReadInbox` into TDLib JSON wire shape. */
export function encodeUpdateChatReadInbox(value: UpdateChatReadInbox): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatReadInbox';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['last_read_inbox_message_id'] = value.last_read_inbox_message_id;
  out['unread_count'] = value.unread_count;
  return out;
}

/** updateChatReadOutbox = Update — Outgoing messages were read */
export class UpdateChatReadOutbox {
  type: 'updateChatReadOutbox' = 'updateChatReadOutbox';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** last_read_outbox_message_id: int53 — Identifier of last read outgoing message */
  last_read_outbox_message_id: number = 0;
}

/** Decode a TDLib `updateChatReadOutbox` object; null input -> null. */
export function decodeUpdateChatReadOutbox(json: TdJsonRaw): UpdateChatReadOutbox | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatReadOutbox = new UpdateChatReadOutbox();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.last_read_outbox_message_id = asNumber(record['last_read_outbox_message_id']);
  return out;
}

/** Encode a `updateChatReadOutbox` into TDLib JSON wire shape. */
export function encodeUpdateChatReadOutbox(value: UpdateChatReadOutbox): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatReadOutbox';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['last_read_outbox_message_id'] = value.last_read_outbox_message_id;
  return out;
}

/** updateChatRemovedFromList = Update — A chat was removed from a chat list */
export class UpdateChatRemovedFromList {
  type: 'updateChatRemovedFromList' = 'updateChatRemovedFromList';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** chat_list: ChatList — The chat list from which the chat was removed */
  chat_list: TdChatList | null = null;
}

/** Decode a TDLib `updateChatRemovedFromList` object; null input -> null. */
export function decodeUpdateChatRemovedFromList(json: TdJsonRaw): UpdateChatRemovedFromList | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatRemovedFromList = new UpdateChatRemovedFromList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.chat_list = decodeTdChatList(record['chat_list']);
  return out;
}

/** Encode a `updateChatRemovedFromList` into TDLib JSON wire shape. */
export function encodeUpdateChatRemovedFromList(value: UpdateChatRemovedFromList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatRemovedFromList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  return out;
}

/** updateChatReplyMarkup = Update — The chat reply markup was changed */
export class UpdateChatReplyMarkup {
  type: 'updateChatReplyMarkup' = 'updateChatReplyMarkup';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** reply_markup_message: message — The message from which the reply markup must be used; may be null if there is no default reply markup in the chat */
  reply_markup_message: Message | null = null;
}

/** Decode a TDLib `updateChatReplyMarkup` object; null input -> null. */
export function decodeUpdateChatReplyMarkup(json: TdJsonRaw): UpdateChatReplyMarkup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatReplyMarkup = new UpdateChatReplyMarkup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.reply_markup_message = decodeMessage(record['reply_markup_message']);
  return out;
}

/** Encode a `updateChatReplyMarkup` into TDLib JSON wire shape. */
export function encodeUpdateChatReplyMarkup(value: UpdateChatReplyMarkup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatReplyMarkup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['reply_markup_message'] = value.reply_markup_message === null ? null : encodeMessage(value.reply_markup_message);
  return out;
}

/** updateChatRevenueAmount = Update — The revenue earned from sponsored messages in a chat has changed. If chat revenue screen is opened, then getChatRevenueTransactions may be called to fetch new transactions */
export class UpdateChatRevenueAmount {
  type: 'updateChatRevenueAmount' = 'updateChatRevenueAmount';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** revenue_amount: chatRevenueAmount — New amount of earned revenue */
  revenue_amount: ChatRevenueAmount | null = null;
}

/** Decode a TDLib `updateChatRevenueAmount` object; null input -> null. */
export function decodeUpdateChatRevenueAmount(json: TdJsonRaw): UpdateChatRevenueAmount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatRevenueAmount = new UpdateChatRevenueAmount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.revenue_amount = decodeChatRevenueAmount(record['revenue_amount']);
  return out;
}

/** Encode a `updateChatRevenueAmount` into TDLib JSON wire shape. */
export function encodeUpdateChatRevenueAmount(value: UpdateChatRevenueAmount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatRevenueAmount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['revenue_amount'] = value.revenue_amount === null ? null : encodeChatRevenueAmount(value.revenue_amount);
  return out;
}

/** updateChatTheme = Update — The chat theme was changed */
export class UpdateChatTheme {
  type: 'updateChatTheme' = 'updateChatTheme';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** theme: ChatTheme — The new theme of the chat; may be null if theme was reset to default */
  theme: TdChatTheme | null = null;
}

/** Decode a TDLib `updateChatTheme` object; null input -> null. */
export function decodeUpdateChatTheme(json: TdJsonRaw): UpdateChatTheme | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatTheme = new UpdateChatTheme();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.theme = decodeTdChatTheme(record['theme']);
  return out;
}

/** Encode a `updateChatTheme` into TDLib JSON wire shape. */
export function encodeUpdateChatTheme(value: UpdateChatTheme): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatTheme';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['theme'] = value.theme === null ? null : encodeTdChatTheme(value.theme);
  return out;
}

/** updateChatTitle = Update — The title of a chat was changed */
export class UpdateChatTitle {
  type: 'updateChatTitle' = 'updateChatTitle';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** title: string — The new chat title */
  title: string = '';
}

/** Decode a TDLib `updateChatTitle` object; null input -> null. */
export function decodeUpdateChatTitle(json: TdJsonRaw): UpdateChatTitle | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatTitle = new UpdateChatTitle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.title = asString(record['title']);
  return out;
}

/** Encode a `updateChatTitle` into TDLib JSON wire shape. */
export function encodeUpdateChatTitle(value: UpdateChatTitle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatTitle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['title'] = value.title;
  return out;
}

/** updateChatUnreadMentionCount = Update — The chat unread_mention_count has changed */
export class UpdateChatUnreadMentionCount {
  type: 'updateChatUnreadMentionCount' = 'updateChatUnreadMentionCount';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** unread_mention_count: int32 — The number of unread mention messages left in the chat */
  unread_mention_count: number = 0;
}

/** Decode a TDLib `updateChatUnreadMentionCount` object; null input -> null. */
export function decodeUpdateChatUnreadMentionCount(json: TdJsonRaw): UpdateChatUnreadMentionCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatUnreadMentionCount = new UpdateChatUnreadMentionCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.unread_mention_count = asNumber(record['unread_mention_count']);
  return out;
}

/** Encode a `updateChatUnreadMentionCount` into TDLib JSON wire shape. */
export function encodeUpdateChatUnreadMentionCount(value: UpdateChatUnreadMentionCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatUnreadMentionCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['unread_mention_count'] = value.unread_mention_count;
  return out;
}

/** updateChatUnreadPollVoteCount = Update — The chat unread_poll_vote_count has changed */
export class UpdateChatUnreadPollVoteCount {
  type: 'updateChatUnreadPollVoteCount' = 'updateChatUnreadPollVoteCount';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** unread_poll_vote_count: int32 — The number of messages with unread poll votes left in the chat */
  unread_poll_vote_count: number = 0;
}

/** Decode a TDLib `updateChatUnreadPollVoteCount` object; null input -> null. */
export function decodeUpdateChatUnreadPollVoteCount(json: TdJsonRaw): UpdateChatUnreadPollVoteCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatUnreadPollVoteCount = new UpdateChatUnreadPollVoteCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.unread_poll_vote_count = asNumber(record['unread_poll_vote_count']);
  return out;
}

/** Encode a `updateChatUnreadPollVoteCount` into TDLib JSON wire shape. */
export function encodeUpdateChatUnreadPollVoteCount(value: UpdateChatUnreadPollVoteCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatUnreadPollVoteCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['unread_poll_vote_count'] = value.unread_poll_vote_count;
  return out;
}

/** updateChatUnreadReactionCount = Update — The chat unread_reaction_count has changed */
export class UpdateChatUnreadReactionCount {
  type: 'updateChatUnreadReactionCount' = 'updateChatUnreadReactionCount';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** unread_reaction_count: int32 — The number of messages with unread reactions left in the chat */
  unread_reaction_count: number = 0;
}

/** Decode a TDLib `updateChatUnreadReactionCount` object; null input -> null. */
export function decodeUpdateChatUnreadReactionCount(json: TdJsonRaw): UpdateChatUnreadReactionCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatUnreadReactionCount = new UpdateChatUnreadReactionCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.unread_reaction_count = asNumber(record['unread_reaction_count']);
  return out;
}

/** Encode a `updateChatUnreadReactionCount` into TDLib JSON wire shape. */
export function encodeUpdateChatUnreadReactionCount(value: UpdateChatUnreadReactionCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatUnreadReactionCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['unread_reaction_count'] = value.unread_reaction_count;
  return out;
}

/** updateChatVideoChat = Update — A chat video chat state has changed */
export class UpdateChatVideoChat {
  type: 'updateChatVideoChat' = 'updateChatVideoChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** video_chat: videoChat — New value of video_chat */
  video_chat: VideoChat | null = null;
}

/** Decode a TDLib `updateChatVideoChat` object; null input -> null. */
export function decodeUpdateChatVideoChat(json: TdJsonRaw): UpdateChatVideoChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatVideoChat = new UpdateChatVideoChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.video_chat = decodeVideoChat(record['video_chat']);
  return out;
}

/** Encode a `updateChatVideoChat` into TDLib JSON wire shape. */
export function encodeUpdateChatVideoChat(value: UpdateChatVideoChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatVideoChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['video_chat'] = value.video_chat === null ? null : encodeVideoChat(value.video_chat);
  return out;
}

/** updateChatViewAsTopics = Update — A chat default appearance has changed */
export class UpdateChatViewAsTopics {
  type: 'updateChatViewAsTopics' = 'updateChatViewAsTopics';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** view_as_topics: Bool — New value of view_as_topics */
  view_as_topics: boolean = false;
}

/** Decode a TDLib `updateChatViewAsTopics` object; null input -> null. */
export function decodeUpdateChatViewAsTopics(json: TdJsonRaw): UpdateChatViewAsTopics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatViewAsTopics = new UpdateChatViewAsTopics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.view_as_topics = asBool(record['view_as_topics']);
  return out;
}

/** Encode a `updateChatViewAsTopics` into TDLib JSON wire shape. */
export function encodeUpdateChatViewAsTopics(value: UpdateChatViewAsTopics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatViewAsTopics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['view_as_topics'] = value.view_as_topics;
  return out;
}

/** updateChatWelcomeMessages = Update — The list of welcome messages of a chat has changed */
export class UpdateChatWelcomeMessages {
  type: 'updateChatWelcomeMessages' = 'updateChatWelcomeMessages';
  extra?: string;
  /** chat_id: int53 — The identifier of the chat */
  chat_id: number = 0;
  /** messages: vector<welcomeMessage> — The new list of welcome messages of the chat in the order from the first to the last sent */
  messages: WelcomeMessage[] = [];
}

/** Decode a TDLib `updateChatWelcomeMessages` object; null input -> null. */
export function decodeUpdateChatWelcomeMessages(json: TdJsonRaw): UpdateChatWelcomeMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateChatWelcomeMessages = new UpdateChatWelcomeMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): WelcomeMessage => decodeWelcomeMessage(item) as WelcomeMessage);
  return out;
}

/** Encode a `updateChatWelcomeMessages` into TDLib JSON wire shape. */
export function encodeUpdateChatWelcomeMessages(value: UpdateChatWelcomeMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateChatWelcomeMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['messages'] = encodeVector(value.messages, (item: WelcomeMessage | null): TdJsonRaw => item === null ? null : encodeWelcomeMessage(item));
  return out;
}

/** updateCommunity = Update — Some data of a community has changed. This update is guaranteed to come before the community identifier is returned to the application */
export class UpdateCommunity {
  type: 'updateCommunity' = 'updateCommunity';
  extra?: string;
  /** community: community — New data about the community */
  community: Community | null = null;
}

/** Decode a TDLib `updateCommunity` object; null input -> null. */
export function decodeUpdateCommunity(json: TdJsonRaw): UpdateCommunity | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateCommunity = new UpdateCommunity();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.community = decodeCommunity(record['community']);
  return out;
}

/** Encode a `updateCommunity` into TDLib JSON wire shape. */
export function encodeUpdateCommunity(value: UpdateCommunity): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateCommunity';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['community'] = value.community === null ? null : encodeCommunity(value.community);
  return out;
}

/** updateCommunityFullInfo = Update — Some data in communityFullInfo has been changed */
export class UpdateCommunityFullInfo {
  type: 'updateCommunityFullInfo' = 'updateCommunityFullInfo';
  extra?: string;
  /** community_id: int53 — Identifier of the community */
  community_id: number = 0;
  /** community_full_info: communityFullInfo — New full information about the community */
  community_full_info: CommunityFullInfo | null = null;
}

/** Decode a TDLib `updateCommunityFullInfo` object; null input -> null. */
export function decodeUpdateCommunityFullInfo(json: TdJsonRaw): UpdateCommunityFullInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateCommunityFullInfo = new UpdateCommunityFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.community_id = asNumber(record['community_id']);
  out.community_full_info = decodeCommunityFullInfo(record['community_full_info']);
  return out;
}

/** Encode a `updateCommunityFullInfo` into TDLib JSON wire shape. */
export function encodeUpdateCommunityFullInfo(value: UpdateCommunityFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateCommunityFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['community_id'] = value.community_id;
  out['community_full_info'] = value.community_full_info === null ? null : encodeCommunityFullInfo(value.community_full_info);
  return out;
}

/** updateConnectionState = Update — The connection state has changed. This update must be used only to show a human-readable description of the connection state */
export class UpdateConnectionState {
  type: 'updateConnectionState' = 'updateConnectionState';
  extra?: string;
  /** state: ConnectionState — The new connection state */
  state: TdConnectionState | null = null;
}

/** Decode a TDLib `updateConnectionState` object; null input -> null. */
export function decodeUpdateConnectionState(json: TdJsonRaw): UpdateConnectionState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateConnectionState = new UpdateConnectionState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.state = decodeTdConnectionState(record['state']);
  return out;
}

/** Encode a `updateConnectionState` into TDLib JSON wire shape. */
export function encodeUpdateConnectionState(value: UpdateConnectionState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateConnectionState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['state'] = value.state === null ? null : encodeTdConnectionState(value.state);
  return out;
}

/** updateContactCloseBirthdays = Update — The list of contacts that had birthdays recently or will have birthday soon has changed */
export class UpdateContactCloseBirthdays {
  type: 'updateContactCloseBirthdays' = 'updateContactCloseBirthdays';
  extra?: string;
  /** close_birthday_users: vector<closeBirthdayUser> — List of contact users with close birthday */
  close_birthday_users: CloseBirthdayUser[] = [];
}

/** Decode a TDLib `updateContactCloseBirthdays` object; null input -> null. */
export function decodeUpdateContactCloseBirthdays(json: TdJsonRaw): UpdateContactCloseBirthdays | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateContactCloseBirthdays = new UpdateContactCloseBirthdays();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.close_birthday_users = decodeVector(record['close_birthday_users'], (item: TdJsonRaw): CloseBirthdayUser => decodeCloseBirthdayUser(item) as CloseBirthdayUser);
  return out;
}

/** Encode a `updateContactCloseBirthdays` into TDLib JSON wire shape. */
export function encodeUpdateContactCloseBirthdays(value: UpdateContactCloseBirthdays): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateContactCloseBirthdays';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['close_birthday_users'] = encodeVector(value.close_birthday_users, (item: CloseBirthdayUser | null): TdJsonRaw => item === null ? null : encodeCloseBirthdayUser(item));
  return out;
}

/** updateDefaultBackground = Update — The default background has changed */
export class UpdateDefaultBackground {
  type: 'updateDefaultBackground' = 'updateDefaultBackground';
  extra?: string;
  /** for_dark_theme: Bool — True, if default background for dark theme has changed */
  for_dark_theme: boolean = false;
  /** background: background — The new default background; may be null */
  background: Background | null = null;
}

/** Decode a TDLib `updateDefaultBackground` object; null input -> null. */
export function decodeUpdateDefaultBackground(json: TdJsonRaw): UpdateDefaultBackground | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateDefaultBackground = new UpdateDefaultBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.for_dark_theme = asBool(record['for_dark_theme']);
  out.background = decodeBackground(record['background']);
  return out;
}

/** Encode a `updateDefaultBackground` into TDLib JSON wire shape. */
export function encodeUpdateDefaultBackground(value: UpdateDefaultBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateDefaultBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['for_dark_theme'] = value.for_dark_theme;
  out['background'] = value.background === null ? null : encodeBackground(value.background);
  return out;
}

/** updateDefaultPaidReactionType = Update — The type of default paid reaction has changed */
export class UpdateDefaultPaidReactionType {
  type: 'updateDefaultPaidReactionType' = 'updateDefaultPaidReactionType';
  extra?: string;
  /** type: PaidReactionType — The new type of the default paid reaction */
  type_: TdPaidReactionType | null = null;
}

/** Decode a TDLib `updateDefaultPaidReactionType` object; null input -> null. */
export function decodeUpdateDefaultPaidReactionType(json: TdJsonRaw): UpdateDefaultPaidReactionType | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateDefaultPaidReactionType = new UpdateDefaultPaidReactionType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPaidReactionType(record['type']);
  return out;
}

/** Encode a `updateDefaultPaidReactionType` into TDLib JSON wire shape. */
export function encodeUpdateDefaultPaidReactionType(value: UpdateDefaultPaidReactionType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateDefaultPaidReactionType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPaidReactionType(value.type_);
  return out;
}

/** updateDefaultReactionType = Update — The type of default reaction has changed */
export class UpdateDefaultReactionType {
  type: 'updateDefaultReactionType' = 'updateDefaultReactionType';
  extra?: string;
  /** reaction_type: ReactionType — The new type of the default reaction */
  reaction_type: TdReactionType | null = null;
}

/** Decode a TDLib `updateDefaultReactionType` object; null input -> null. */
export function decodeUpdateDefaultReactionType(json: TdJsonRaw): UpdateDefaultReactionType | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateDefaultReactionType = new UpdateDefaultReactionType();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.reaction_type = decodeTdReactionType(record['reaction_type']);
  return out;
}

/** Encode a `updateDefaultReactionType` into TDLib JSON wire shape. */
export function encodeUpdateDefaultReactionType(value: UpdateDefaultReactionType): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateDefaultReactionType';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['reaction_type'] = value.reaction_type === null ? null : encodeTdReactionType(value.reaction_type);
  return out;
}

/** updateDeleteMessages = Update — Some messages were deleted */
export class UpdateDeleteMessages {
  type: 'updateDeleteMessages' = 'updateDeleteMessages';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_ids: vector<int53> — Identifiers of the deleted messages */
  message_ids: number[] = [];
  /** is_permanent: Bool — True, if the messages are permanently deleted by a user (as opposed to just becoming inaccessible) */
  is_permanent: boolean = false;
  /** from_cache: Bool — True, if the messages are deleted only from the cache and can possibly be retrieved again in the future */
  from_cache: boolean = false;
}

/** Decode a TDLib `updateDeleteMessages` object; null input -> null. */
export function decodeUpdateDeleteMessages(json: TdJsonRaw): UpdateDeleteMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateDeleteMessages = new UpdateDeleteMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.is_permanent = asBool(record['is_permanent']);
  out.from_cache = asBool(record['from_cache']);
  return out;
}

/** Encode a `updateDeleteMessages` into TDLib JSON wire shape. */
export function encodeUpdateDeleteMessages(value: UpdateDeleteMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateDeleteMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  out['is_permanent'] = value.is_permanent;
  out['from_cache'] = value.from_cache;
  return out;
}

/** updateDiceEmojis = Update — The list of supported dice emojis has changed */
export class UpdateDiceEmojis {
  type: 'updateDiceEmojis' = 'updateDiceEmojis';
  extra?: string;
  /** emojis: vector<string> — The new list of supported dice emojis */
  emojis: string[] = [];
}

/** Decode a TDLib `updateDiceEmojis` object; null input -> null. */
export function decodeUpdateDiceEmojis(json: TdJsonRaw): UpdateDiceEmojis | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateDiceEmojis = new UpdateDiceEmojis();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.emojis = decodeVector(record['emojis'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `updateDiceEmojis` into TDLib JSON wire shape. */
export function encodeUpdateDiceEmojis(value: UpdateDiceEmojis): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateDiceEmojis';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['emojis'] = encodeVector(value.emojis, (item: string): TdJsonRaw => item);
  return out;
}

/** updateDirectMessagesChatTopic = Update — Basic information about a topic in a channel direct messages chat administered by the current user has changed. This update is guaranteed to come before the topic identifier is returned to the app... */
export class UpdateDirectMessagesChatTopic {
  type: 'updateDirectMessagesChatTopic' = 'updateDirectMessagesChatTopic';
  extra?: string;
  /** topic: directMessagesChatTopic — New data about the topic */
  topic: DirectMessagesChatTopic | null = null;
}

/** Decode a TDLib `updateDirectMessagesChatTopic` object; null input -> null. */
export function decodeUpdateDirectMessagesChatTopic(json: TdJsonRaw): UpdateDirectMessagesChatTopic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateDirectMessagesChatTopic = new UpdateDirectMessagesChatTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.topic = decodeDirectMessagesChatTopic(record['topic']);
  return out;
}

/** Encode a `updateDirectMessagesChatTopic` into TDLib JSON wire shape. */
export function encodeUpdateDirectMessagesChatTopic(value: UpdateDirectMessagesChatTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateDirectMessagesChatTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['topic'] = value.topic === null ? null : encodeDirectMessagesChatTopic(value.topic);
  return out;
}

/** updateEmojiChatThemes = Update — The list of available emoji chat themes has changed */
export class UpdateEmojiChatThemes {
  type: 'updateEmojiChatThemes' = 'updateEmojiChatThemes';
  extra?: string;
  /** chat_themes: vector<emojiChatTheme> — The new list of emoji chat themes */
  chat_themes: EmojiChatTheme[] = [];
}

/** Decode a TDLib `updateEmojiChatThemes` object; null input -> null. */
export function decodeUpdateEmojiChatThemes(json: TdJsonRaw): UpdateEmojiChatThemes | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateEmojiChatThemes = new UpdateEmojiChatThemes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_themes = decodeVector(record['chat_themes'], (item: TdJsonRaw): EmojiChatTheme => decodeEmojiChatTheme(item) as EmojiChatTheme);
  return out;
}

/** Encode a `updateEmojiChatThemes` into TDLib JSON wire shape. */
export function encodeUpdateEmojiChatThemes(value: UpdateEmojiChatThemes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateEmojiChatThemes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_themes'] = encodeVector(value.chat_themes, (item: EmojiChatTheme | null): TdJsonRaw => item === null ? null : encodeEmojiChatTheme(item));
  return out;
}

/** updateFavoriteStickers = Update — The list of favorite stickers was updated */
export class UpdateFavoriteStickers {
  type: 'updateFavoriteStickers' = 'updateFavoriteStickers';
  extra?: string;
  /** sticker_ids: vector<int32> — The new list of file identifiers of favorite stickers */
  sticker_ids: number[] = [];
}

/** Decode a TDLib `updateFavoriteStickers` object; null input -> null. */
export function decodeUpdateFavoriteStickers(json: TdJsonRaw): UpdateFavoriteStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateFavoriteStickers = new UpdateFavoriteStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_ids = decodeVector(record['sticker_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateFavoriteStickers` into TDLib JSON wire shape. */
export function encodeUpdateFavoriteStickers(value: UpdateFavoriteStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateFavoriteStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_ids'] = encodeVector(value.sticker_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateFile = Update — Information about a file was updated */
export class UpdateFile {
  type: 'updateFile' = 'updateFile';
  extra?: string;
  /** file: file — New data about the file */
  file: File | null = null;
}

/** Decode a TDLib `updateFile` object; null input -> null. */
export function decodeUpdateFile(json: TdJsonRaw): UpdateFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateFile = new UpdateFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file = decodeFile(record['file']);
  return out;
}

/** Encode a `updateFile` into TDLib JSON wire shape. */
export function encodeUpdateFile(value: UpdateFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file'] = value.file === null ? null : encodeFile(value.file);
  return out;
}

/** updateFileAddedToDownloads = Update — A file was added to the file download list. This update is sent only after file download list is loaded for the first time */
export class UpdateFileAddedToDownloads {
  type: 'updateFileAddedToDownloads' = 'updateFileAddedToDownloads';
  extra?: string;
  /** file_download: fileDownload — The added file download */
  file_download: FileDownload | null = null;
  /** counts: downloadedFileCounts — New number of being downloaded and recently downloaded files found */
  counts: DownloadedFileCounts | null = null;
}

/** Decode a TDLib `updateFileAddedToDownloads` object; null input -> null. */
export function decodeUpdateFileAddedToDownloads(json: TdJsonRaw): UpdateFileAddedToDownloads | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateFileAddedToDownloads = new UpdateFileAddedToDownloads();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_download = decodeFileDownload(record['file_download']);
  out.counts = decodeDownloadedFileCounts(record['counts']);
  return out;
}

/** Encode a `updateFileAddedToDownloads` into TDLib JSON wire shape. */
export function encodeUpdateFileAddedToDownloads(value: UpdateFileAddedToDownloads): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateFileAddedToDownloads';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_download'] = value.file_download === null ? null : encodeFileDownload(value.file_download);
  out['counts'] = value.counts === null ? null : encodeDownloadedFileCounts(value.counts);
  return out;
}

/** updateFileDownload = Update — A file download was changed. This update is sent only after file download list is loaded for the first time */
export class UpdateFileDownload {
  type: 'updateFileDownload' = 'updateFileDownload';
  extra?: string;
  /** file_id: int32 — File identifier */
  file_id: number = 0;
  /** complete_date: int32 — Point in time (Unix timestamp) when the file downloading was completed; 0 if the file downloading isn't completed */
  complete_date: number = 0;
  /** is_paused: Bool — True, if downloading of the file is paused */
  is_paused: boolean = false;
  /** counts: downloadedFileCounts — New number of being downloaded and recently downloaded files found */
  counts: DownloadedFileCounts | null = null;
}

/** Decode a TDLib `updateFileDownload` object; null input -> null. */
export function decodeUpdateFileDownload(json: TdJsonRaw): UpdateFileDownload | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateFileDownload = new UpdateFileDownload();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.complete_date = asNumber(record['complete_date']);
  out.is_paused = asBool(record['is_paused']);
  out.counts = decodeDownloadedFileCounts(record['counts']);
  return out;
}

/** Encode a `updateFileDownload` into TDLib JSON wire shape. */
export function encodeUpdateFileDownload(value: UpdateFileDownload): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateFileDownload';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['complete_date'] = value.complete_date;
  out['is_paused'] = value.is_paused;
  out['counts'] = value.counts === null ? null : encodeDownloadedFileCounts(value.counts);
  return out;
}

/** updateFileDownloads = Update — The state of the file download list has changed */
export class UpdateFileDownloads {
  type: 'updateFileDownloads' = 'updateFileDownloads';
  extra?: string;
  /** total_size: int53 — Total size of files in the file download list, in bytes */
  total_size: number = 0;
  /** total_count: int32 — Total number of files in the file download list */
  total_count: number = 0;
  /** downloaded_size: int53 — Total downloaded size of files in the file download list, in bytes */
  downloaded_size: number = 0;
}

/** Decode a TDLib `updateFileDownloads` object; null input -> null. */
export function decodeUpdateFileDownloads(json: TdJsonRaw): UpdateFileDownloads | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateFileDownloads = new UpdateFileDownloads();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_size = asNumber(record['total_size']);
  out.total_count = asNumber(record['total_count']);
  out.downloaded_size = asNumber(record['downloaded_size']);
  return out;
}

/** Encode a `updateFileDownloads` into TDLib JSON wire shape. */
export function encodeUpdateFileDownloads(value: UpdateFileDownloads): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateFileDownloads';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_size'] = value.total_size;
  out['total_count'] = value.total_count;
  out['downloaded_size'] = value.downloaded_size;
  return out;
}

/** updateFileGenerationStart = Update — The file generation process needs to be started by the application. Use setFileGenerationProgress and finishFileGeneration to generate the file If the application has no access to the path, it can use... */
export class UpdateFileGenerationStart {
  type: 'updateFileGenerationStart' = 'updateFileGenerationStart';
  extra?: string;
  /** generation_id: int64 — Unique identifier for the generation process */
  generation_id: string = '';
  /** original_path: string — The original path specified by the application in inputFileGenerated */
  original_path: string = '';
  /** destination_path: string — The path to a file that must be created and where the new file must be generated by the application. */
  destination_path: string = '';
  /** conversion: string — If the conversion is "#url#", then original_path contains an HTTP/HTTPS URL of a file that must be downloaded by the application. */
  conversion: string = '';
}

/** Decode a TDLib `updateFileGenerationStart` object; null input -> null. */
export function decodeUpdateFileGenerationStart(json: TdJsonRaw): UpdateFileGenerationStart | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateFileGenerationStart = new UpdateFileGenerationStart();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.generation_id = asInt64(record['generation_id']);
  out.original_path = asString(record['original_path']);
  out.destination_path = asString(record['destination_path']);
  out.conversion = asString(record['conversion']);
  return out;
}

/** Encode a `updateFileGenerationStart` into TDLib JSON wire shape. */
export function encodeUpdateFileGenerationStart(value: UpdateFileGenerationStart): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateFileGenerationStart';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['generation_id'] = value.generation_id;
  out['original_path'] = value.original_path;
  out['destination_path'] = value.destination_path;
  out['conversion'] = value.conversion;
  return out;
}

/** updateFileGenerationStop = Update — File generation is no longer needed */
export class UpdateFileGenerationStop {
  type: 'updateFileGenerationStop' = 'updateFileGenerationStop';
  extra?: string;
  /** generation_id: int64 — Unique identifier for the generation process */
  generation_id: string = '';
}

/** Decode a TDLib `updateFileGenerationStop` object; null input -> null. */
export function decodeUpdateFileGenerationStop(json: TdJsonRaw): UpdateFileGenerationStop | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateFileGenerationStop = new UpdateFileGenerationStop();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.generation_id = asInt64(record['generation_id']);
  return out;
}

/** Encode a `updateFileGenerationStop` into TDLib JSON wire shape. */
export function encodeUpdateFileGenerationStop(value: UpdateFileGenerationStop): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateFileGenerationStop';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['generation_id'] = value.generation_id;
  return out;
}

/** updateFileRemovedFromDownloads = Update — A file was removed from the file download list. This update is sent only after file download list is loaded for the first time */
export class UpdateFileRemovedFromDownloads {
  type: 'updateFileRemovedFromDownloads' = 'updateFileRemovedFromDownloads';
  extra?: string;
  /** file_id: int32 — File identifier */
  file_id: number = 0;
  /** counts: downloadedFileCounts — New number of being downloaded and recently downloaded files found */
  counts: DownloadedFileCounts | null = null;
}

/** Decode a TDLib `updateFileRemovedFromDownloads` object; null input -> null. */
export function decodeUpdateFileRemovedFromDownloads(json: TdJsonRaw): UpdateFileRemovedFromDownloads | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateFileRemovedFromDownloads = new UpdateFileRemovedFromDownloads();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.counts = decodeDownloadedFileCounts(record['counts']);
  return out;
}

/** Encode a `updateFileRemovedFromDownloads` into TDLib JSON wire shape. */
export function encodeUpdateFileRemovedFromDownloads(value: UpdateFileRemovedFromDownloads): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateFileRemovedFromDownloads';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['counts'] = value.counts === null ? null : encodeDownloadedFileCounts(value.counts);
  return out;
}

/** updateForumTopic = Update — Information about a topic in a forum chat was changed */
export class UpdateForumTopic {
  type: 'updateForumTopic' = 'updateForumTopic';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — Forum topic identifier of the topic */
  forum_topic_id: number = 0;
  /** is_pinned: Bool — True, if the topic is pinned in the topic list */
  is_pinned: boolean = false;
  /** last_read_inbox_message_id: int53 — Identifier of the last read incoming message */
  last_read_inbox_message_id: number = 0;
  /** last_read_outbox_message_id: int53 — Identifier of the last read outgoing message */
  last_read_outbox_message_id: number = 0;
  /** unread_mention_count: int32 — Number of unread messages with a mention/reply in the topic */
  unread_mention_count: number = 0;
  /** unread_reaction_count: int32 — Number of messages with unread reactions in the topic */
  unread_reaction_count: number = 0;
  /** unread_poll_vote_count: int32 — Number of messages with unread poll votes in the topic */
  unread_poll_vote_count: number = 0;
  /** notification_settings: chatNotificationSettings — Notification settings for the topic */
  notification_settings: ChatNotificationSettings | null = null;
  /** draft_message: draftMessage — A draft of a message in the topic; may be null if none */
  draft_message: DraftMessage | null = null;
}

/** Decode a TDLib `updateForumTopic` object; null input -> null. */
export function decodeUpdateForumTopic(json: TdJsonRaw): UpdateForumTopic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateForumTopic = new UpdateForumTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.is_pinned = asBool(record['is_pinned']);
  out.last_read_inbox_message_id = asNumber(record['last_read_inbox_message_id']);
  out.last_read_outbox_message_id = asNumber(record['last_read_outbox_message_id']);
  out.unread_mention_count = asNumber(record['unread_mention_count']);
  out.unread_reaction_count = asNumber(record['unread_reaction_count']);
  out.unread_poll_vote_count = asNumber(record['unread_poll_vote_count']);
  out.notification_settings = decodeChatNotificationSettings(record['notification_settings']);
  out.draft_message = decodeDraftMessage(record['draft_message']);
  return out;
}

/** Encode a `updateForumTopic` into TDLib JSON wire shape. */
export function encodeUpdateForumTopic(value: UpdateForumTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateForumTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['is_pinned'] = value.is_pinned;
  out['last_read_inbox_message_id'] = value.last_read_inbox_message_id;
  out['last_read_outbox_message_id'] = value.last_read_outbox_message_id;
  out['unread_mention_count'] = value.unread_mention_count;
  out['unread_reaction_count'] = value.unread_reaction_count;
  out['unread_poll_vote_count'] = value.unread_poll_vote_count;
  out['notification_settings'] = value.notification_settings === null ? null : encodeChatNotificationSettings(value.notification_settings);
  out['draft_message'] = value.draft_message === null ? null : encodeDraftMessage(value.draft_message);
  return out;
}

/** updateForumTopicInfo = Update — Basic information about a topic in a forum chat was changed */
export class UpdateForumTopicInfo {
  type: 'updateForumTopicInfo' = 'updateForumTopicInfo';
  extra?: string;
  /** info: forumTopicInfo — New information about the topic */
  info: ForumTopicInfo | null = null;
}

/** Decode a TDLib `updateForumTopicInfo` object; null input -> null. */
export function decodeUpdateForumTopicInfo(json: TdJsonRaw): UpdateForumTopicInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateForumTopicInfo = new UpdateForumTopicInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.info = decodeForumTopicInfo(record['info']);
  return out;
}

/** Encode a `updateForumTopicInfo` into TDLib JSON wire shape. */
export function encodeUpdateForumTopicInfo(value: UpdateForumTopicInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateForumTopicInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['info'] = value.info === null ? null : encodeForumTopicInfo(value.info);
  return out;
}

/** updateFreezeState = Update — The freeze state of the current user's account has changed */
export class UpdateFreezeState {
  type: 'updateFreezeState' = 'updateFreezeState';
  extra?: string;
  /** is_frozen: Bool — True, if the account is frozen */
  is_frozen: boolean = false;
  /** freezing_date: int32 — Point in time (Unix timestamp) when the account was frozen; 0 if the account isn't frozen */
  freezing_date: number = 0;
  /** deletion_date: int32 — Point in time (Unix timestamp) when the account will be deleted and can't be unfrozen; 0 if the account isn't frozen */
  deletion_date: number = 0;
  /** appeal_link: string — The link to open to send an appeal to unfreeze the account */
  appeal_link: string = '';
}

/** Decode a TDLib `updateFreezeState` object; null input -> null. */
export function decodeUpdateFreezeState(json: TdJsonRaw): UpdateFreezeState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateFreezeState = new UpdateFreezeState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_frozen = asBool(record['is_frozen']);
  out.freezing_date = asNumber(record['freezing_date']);
  out.deletion_date = asNumber(record['deletion_date']);
  out.appeal_link = asString(record['appeal_link']);
  return out;
}

/** Encode a `updateFreezeState` into TDLib JSON wire shape. */
export function encodeUpdateFreezeState(value: UpdateFreezeState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateFreezeState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_frozen'] = value.is_frozen;
  out['freezing_date'] = value.freezing_date;
  out['deletion_date'] = value.deletion_date;
  out['appeal_link'] = value.appeal_link;
  return out;
}

/** updateGiftAuctionState = Update — State of a gift auction was updated */
export class UpdateGiftAuctionState {
  type: 'updateGiftAuctionState' = 'updateGiftAuctionState';
  extra?: string;
  /** state: giftAuctionState — New state of the auction */
  state: GiftAuctionState | null = null;
}

/** Decode a TDLib `updateGiftAuctionState` object; null input -> null. */
export function decodeUpdateGiftAuctionState(json: TdJsonRaw): UpdateGiftAuctionState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateGiftAuctionState = new UpdateGiftAuctionState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.state = decodeGiftAuctionState(record['state']);
  return out;
}

/** Encode a `updateGiftAuctionState` into TDLib JSON wire shape. */
export function encodeUpdateGiftAuctionState(value: UpdateGiftAuctionState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateGiftAuctionState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['state'] = value.state === null ? null : encodeGiftAuctionState(value.state);
  return out;
}

/** updateGramRevenueStatus = Update — The TON Gram revenue earned by the current user has changed. If Gram transaction screen of the chat is opened, then getTonTransactions may be called to fetch new transactions */
export class UpdateGramRevenueStatus {
  type: 'updateGramRevenueStatus' = 'updateGramRevenueStatus';
  extra?: string;
  /** status: gramRevenueStatus — New Gram revenue status */
  status: GramRevenueStatus | null = null;
}

/** Decode a TDLib `updateGramRevenueStatus` object; null input -> null. */
export function decodeUpdateGramRevenueStatus(json: TdJsonRaw): UpdateGramRevenueStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateGramRevenueStatus = new UpdateGramRevenueStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.status = decodeGramRevenueStatus(record['status']);
  return out;
}

/** Encode a `updateGramRevenueStatus` into TDLib JSON wire shape. */
export function encodeUpdateGramRevenueStatus(value: UpdateGramRevenueStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateGramRevenueStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['status'] = value.status === null ? null : encodeGramRevenueStatus(value.status);
  return out;
}

/** updateGroupCall = Update — Information about a group call was updated */
export class UpdateGroupCall {
  type: 'updateGroupCall' = 'updateGroupCall';
  extra?: string;
  /** group_call: groupCall — New data about the group call */
  group_call: GroupCall | null = null;
}

/** Decode a TDLib `updateGroupCall` object; null input -> null. */
export function decodeUpdateGroupCall(json: TdJsonRaw): UpdateGroupCall | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateGroupCall = new UpdateGroupCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call = decodeGroupCall(record['group_call']);
  return out;
}

/** Encode a `updateGroupCall` into TDLib JSON wire shape. */
export function encodeUpdateGroupCall(value: UpdateGroupCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateGroupCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call'] = value.group_call === null ? null : encodeGroupCall(value.group_call);
  return out;
}

/** updateGroupCallMessageLevels = Update — The levels of live story group call messages have changed */
export class UpdateGroupCallMessageLevels {
  type: 'updateGroupCallMessageLevels' = 'updateGroupCallMessageLevels';
  extra?: string;
  /** levels: vector<groupCallMessageLevel> — New description of the levels in decreasing order of groupCallMessageLevel.min_star_count */
  levels: GroupCallMessageLevel[] = [];
}

/** Decode a TDLib `updateGroupCallMessageLevels` object; null input -> null. */
export function decodeUpdateGroupCallMessageLevels(json: TdJsonRaw): UpdateGroupCallMessageLevels | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateGroupCallMessageLevels = new UpdateGroupCallMessageLevels();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.levels = decodeVector(record['levels'], (item: TdJsonRaw): GroupCallMessageLevel => decodeGroupCallMessageLevel(item) as GroupCallMessageLevel);
  return out;
}

/** Encode a `updateGroupCallMessageLevels` into TDLib JSON wire shape. */
export function encodeUpdateGroupCallMessageLevels(value: UpdateGroupCallMessageLevels): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateGroupCallMessageLevels';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['levels'] = encodeVector(value.levels, (item: GroupCallMessageLevel | null): TdJsonRaw => item === null ? null : encodeGroupCallMessageLevel(item));
  return out;
}

/** updateGroupCallMessageSendFailed = Update — A group call message failed to send */
export class UpdateGroupCallMessageSendFailed {
  type: 'updateGroupCallMessageSendFailed' = 'updateGroupCallMessageSendFailed';
  extra?: string;
  /** group_call_id: int32 — Identifier of the group call */
  group_call_id: number = 0;
  /** message_id: int32 — Message identifier */
  message_id: number = 0;
  /** error: error — The cause of the message sending failure */
  error: ErrorValue | null = null;
}

/** Decode a TDLib `updateGroupCallMessageSendFailed` object; null input -> null. */
export function decodeUpdateGroupCallMessageSendFailed(json: TdJsonRaw): UpdateGroupCallMessageSendFailed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateGroupCallMessageSendFailed = new UpdateGroupCallMessageSendFailed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.message_id = asNumber(record['message_id']);
  out.error = decodeErrorValue(record['error']);
  return out;
}

/** Encode a `updateGroupCallMessageSendFailed` into TDLib JSON wire shape. */
export function encodeUpdateGroupCallMessageSendFailed(value: UpdateGroupCallMessageSendFailed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateGroupCallMessageSendFailed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['message_id'] = value.message_id;
  out['error'] = value.error === null ? null : encodeErrorValue(value.error);
  return out;
}

/** updateGroupCallMessagesDeleted = Update — Some group call messages were deleted */
export class UpdateGroupCallMessagesDeleted {
  type: 'updateGroupCallMessagesDeleted' = 'updateGroupCallMessagesDeleted';
  extra?: string;
  /** group_call_id: int32 — Identifier of the group call */
  group_call_id: number = 0;
  /** message_ids: vector<int32> — Identifiers of the deleted messages */
  message_ids: number[] = [];
}

/** Decode a TDLib `updateGroupCallMessagesDeleted` object; null input -> null. */
export function decodeUpdateGroupCallMessagesDeleted(json: TdJsonRaw): UpdateGroupCallMessagesDeleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateGroupCallMessagesDeleted = new UpdateGroupCallMessagesDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.message_ids = decodeVector(record['message_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateGroupCallMessagesDeleted` into TDLib JSON wire shape. */
export function encodeUpdateGroupCallMessagesDeleted(value: UpdateGroupCallMessagesDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateGroupCallMessagesDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['message_ids'] = encodeVector(value.message_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateGroupCallParticipant = Update — Information about a group call participant was changed. The updates are sent only after the group call is received through getGroupCall and only if the call is joined or being joined */
export class UpdateGroupCallParticipant {
  type: 'updateGroupCallParticipant' = 'updateGroupCallParticipant';
  extra?: string;
  /** group_call_id: int32 — Identifier of the group call */
  group_call_id: number = 0;
  /** participant: groupCallParticipant — New data about the participant */
  participant: GroupCallParticipant | null = null;
}

/** Decode a TDLib `updateGroupCallParticipant` object; null input -> null. */
export function decodeUpdateGroupCallParticipant(json: TdJsonRaw): UpdateGroupCallParticipant | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateGroupCallParticipant = new UpdateGroupCallParticipant();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.participant = decodeGroupCallParticipant(record['participant']);
  return out;
}

/** Encode a `updateGroupCallParticipant` into TDLib JSON wire shape. */
export function encodeUpdateGroupCallParticipant(value: UpdateGroupCallParticipant): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateGroupCallParticipant';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['participant'] = value.participant === null ? null : encodeGroupCallParticipant(value.participant);
  return out;
}

/** updateGroupCallParticipants = Update — The list of group call participants that can send and receive encrypted call data has changed; for group calls not bound to a chat only The participants must be shown in the list of group call parti... */
export class UpdateGroupCallParticipants {
  type: 'updateGroupCallParticipants' = 'updateGroupCallParticipants';
  extra?: string;
  /** group_call_id: int32 — Identifier of the group call */
  group_call_id: number = 0;
  /** participant_user_ids: vector<int64> — New list of group call participant user identifiers. The identifiers may be invalid or the corresponding users may be unknown. */
  participant_user_ids: string[] = [];
}

/** Decode a TDLib `updateGroupCallParticipants` object; null input -> null. */
export function decodeUpdateGroupCallParticipants(json: TdJsonRaw): UpdateGroupCallParticipants | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateGroupCallParticipants = new UpdateGroupCallParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.participant_user_ids = decodeVector(record['participant_user_ids'], (item: TdJsonRaw): string => asInt64(item));
  return out;
}

/** Encode a `updateGroupCallParticipants` into TDLib JSON wire shape. */
export function encodeUpdateGroupCallParticipants(value: UpdateGroupCallParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateGroupCallParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['participant_user_ids'] = encodeVector(value.participant_user_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** updateGroupCallVerificationState = Update — The verification state of an encrypted group call has changed; for group calls not bound to a chat only */
export class UpdateGroupCallVerificationState {
  type: 'updateGroupCallVerificationState' = 'updateGroupCallVerificationState';
  extra?: string;
  /** group_call_id: int32 — Identifier of the group call */
  group_call_id: number = 0;
  /** generation: int32 — The call state generation to which the emoji corresponds. If generation is different for two users, then their emoji may be also different */
  generation: number = 0;
  /** emojis: vector<string> — Group call state fingerprint represented as 4 emoji; may be empty if the state isn't verified yet */
  emojis: string[] = [];
}

/** Decode a TDLib `updateGroupCallVerificationState` object; null input -> null. */
export function decodeUpdateGroupCallVerificationState(json: TdJsonRaw): UpdateGroupCallVerificationState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateGroupCallVerificationState = new UpdateGroupCallVerificationState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.generation = asNumber(record['generation']);
  out.emojis = decodeVector(record['emojis'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `updateGroupCallVerificationState` into TDLib JSON wire shape. */
export function encodeUpdateGroupCallVerificationState(value: UpdateGroupCallVerificationState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateGroupCallVerificationState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['generation'] = value.generation;
  out['emojis'] = encodeVector(value.emojis, (item: string): TdJsonRaw => item);
  return out;
}

/** updateHavePendingNotifications = Update — Describes whether there are some pending notification updates. Can be used to prevent application from killing, while there are some pending notifications */
export class UpdateHavePendingNotifications {
  type: 'updateHavePendingNotifications' = 'updateHavePendingNotifications';
  extra?: string;
  /** have_delayed_notifications: Bool — True, if there are some delayed notification updates, which will be sent soon */
  have_delayed_notifications: boolean = false;
  /** have_unreceived_notifications: Bool — True, if there can be some yet unreceived notifications, which are being fetched from the server */
  have_unreceived_notifications: boolean = false;
}

/** Decode a TDLib `updateHavePendingNotifications` object; null input -> null. */
export function decodeUpdateHavePendingNotifications(json: TdJsonRaw): UpdateHavePendingNotifications | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateHavePendingNotifications = new UpdateHavePendingNotifications();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.have_delayed_notifications = asBool(record['have_delayed_notifications']);
  out.have_unreceived_notifications = asBool(record['have_unreceived_notifications']);
  return out;
}

/** Encode a `updateHavePendingNotifications` into TDLib JSON wire shape. */
export function encodeUpdateHavePendingNotifications(value: UpdateHavePendingNotifications): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateHavePendingNotifications';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['have_delayed_notifications'] = value.have_delayed_notifications;
  out['have_unreceived_notifications'] = value.have_unreceived_notifications;
  return out;
}

/** updateInstalledStickerSets = Update — The list of installed sticker sets was updated */
export class UpdateInstalledStickerSets {
  type: 'updateInstalledStickerSets' = 'updateInstalledStickerSets';
  extra?: string;
  /** sticker_type: StickerType — Type of the affected stickers */
  sticker_type: TdStickerType | null = null;
  /** sticker_set_ids: vector<int64> — The new list of installed ordinary sticker sets */
  sticker_set_ids: string[] = [];
}

/** Decode a TDLib `updateInstalledStickerSets` object; null input -> null. */
export function decodeUpdateInstalledStickerSets(json: TdJsonRaw): UpdateInstalledStickerSets | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateInstalledStickerSets = new UpdateInstalledStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.sticker_set_ids = decodeVector(record['sticker_set_ids'], (item: TdJsonRaw): string => asInt64(item));
  return out;
}

/** Encode a `updateInstalledStickerSets` into TDLib JSON wire shape. */
export function encodeUpdateInstalledStickerSets(value: UpdateInstalledStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateInstalledStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['sticker_set_ids'] = encodeVector(value.sticker_set_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** updateLanguagePackStrings = Update — Some language pack strings have been updated */
export class UpdateLanguagePackStrings {
  type: 'updateLanguagePackStrings' = 'updateLanguagePackStrings';
  extra?: string;
  /** localization_target: string — Localization target to which the language pack belongs */
  localization_target: string = '';
  /** language_pack_id: string — Identifier of the updated language pack */
  language_pack_id: string = '';
  /** strings: vector<languagePackString> — List of changed language pack strings; empty if all strings have changed */
  strings: LanguagePackString[] = [];
}

/** Decode a TDLib `updateLanguagePackStrings` object; null input -> null. */
export function decodeUpdateLanguagePackStrings(json: TdJsonRaw): UpdateLanguagePackStrings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateLanguagePackStrings = new UpdateLanguagePackStrings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.localization_target = asString(record['localization_target']);
  out.language_pack_id = asString(record['language_pack_id']);
  out.strings = decodeVector(record['strings'], (item: TdJsonRaw): LanguagePackString => decodeLanguagePackString(item) as LanguagePackString);
  return out;
}

/** Encode a `updateLanguagePackStrings` into TDLib JSON wire shape. */
export function encodeUpdateLanguagePackStrings(value: UpdateLanguagePackStrings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateLanguagePackStrings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['localization_target'] = value.localization_target;
  out['language_pack_id'] = value.language_pack_id;
  out['strings'] = encodeVector(value.strings, (item: LanguagePackString | null): TdJsonRaw => item === null ? null : encodeLanguagePackString(item));
  return out;
}

/** updateLiveStoryTopDonors = Update — The list of top donors in live story group call has changed */
export class UpdateLiveStoryTopDonors {
  type: 'updateLiveStoryTopDonors' = 'updateLiveStoryTopDonors';
  extra?: string;
  /** group_call_id: int32 — Identifier of the group call */
  group_call_id: number = 0;
  /** donors: liveStoryDonors — New list of live story donors */
  donors: LiveStoryDonors | null = null;
}

/** Decode a TDLib `updateLiveStoryTopDonors` object; null input -> null. */
export function decodeUpdateLiveStoryTopDonors(json: TdJsonRaw): UpdateLiveStoryTopDonors | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateLiveStoryTopDonors = new UpdateLiveStoryTopDonors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.donors = decodeLiveStoryDonors(record['donors']);
  return out;
}

/** Encode a `updateLiveStoryTopDonors` into TDLib JSON wire shape. */
export function encodeUpdateLiveStoryTopDonors(value: UpdateLiveStoryTopDonors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateLiveStoryTopDonors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['donors'] = value.donors === null ? null : encodeLiveStoryDonors(value.donors);
  return out;
}

/** updateManagedBot = Update — A bot that can be managed by the current bot was created or updated; for bots only */
export class UpdateManagedBot {
  type: 'updateManagedBot' = 'updateManagedBot';
  extra?: string;
  /** user_id: int53 — Identifier of the user who created the bot */
  user_id: number = 0;
  /** bot_user_id: int53 — Identifier of the created managed bot */
  bot_user_id: number = 0;
}

/** Decode a TDLib `updateManagedBot` object; null input -> null. */
export function decodeUpdateManagedBot(json: TdJsonRaw): UpdateManagedBot | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateManagedBot = new UpdateManagedBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `updateManagedBot` into TDLib JSON wire shape. */
export function encodeUpdateManagedBot(value: UpdateManagedBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateManagedBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** updateMessageContainsUnreadPollVotes = Update — Unread votes were added or removed from a poll message */
export class UpdateMessageContainsUnreadPollVotes {
  type: 'updateMessageContainsUnreadPollVotes' = 'updateMessageContainsUnreadPollVotes';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** contains_unread_poll_votes: Bool — True, if the message is a poll message with unread votes */
  contains_unread_poll_votes: boolean = false;
  /** unread_poll_vote_count: int32 — The new number of messages with unread poll votes in the chat */
  unread_poll_vote_count: number = 0;
}

/** Decode a TDLib `updateMessageContainsUnreadPollVotes` object; null input -> null. */
export function decodeUpdateMessageContainsUnreadPollVotes(json: TdJsonRaw): UpdateMessageContainsUnreadPollVotes | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageContainsUnreadPollVotes = new UpdateMessageContainsUnreadPollVotes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.contains_unread_poll_votes = asBool(record['contains_unread_poll_votes']);
  out.unread_poll_vote_count = asNumber(record['unread_poll_vote_count']);
  return out;
}

/** Encode a `updateMessageContainsUnreadPollVotes` into TDLib JSON wire shape. */
export function encodeUpdateMessageContainsUnreadPollVotes(value: UpdateMessageContainsUnreadPollVotes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageContainsUnreadPollVotes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['contains_unread_poll_votes'] = value.contains_unread_poll_votes;
  out['unread_poll_vote_count'] = value.unread_poll_vote_count;
  return out;
}

/** updateMessageContent = Update — The message content has changed */
export class UpdateMessageContent {
  type: 'updateMessageContent' = 'updateMessageContent';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** new_content: MessageContent — New message content */
  new_content: TdMessageContent | null = null;
}

/** Decode a TDLib `updateMessageContent` object; null input -> null. */
export function decodeUpdateMessageContent(json: TdJsonRaw): UpdateMessageContent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageContent = new UpdateMessageContent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.new_content = decodeTdMessageContent(record['new_content']);
  return out;
}

/** Encode a `updateMessageContent` into TDLib JSON wire shape. */
export function encodeUpdateMessageContent(value: UpdateMessageContent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageContent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['new_content'] = value.new_content === null ? null : encodeTdMessageContent(value.new_content);
  return out;
}

/** updateMessageContentOpened = Update — The message content was opened. Updates voice note messages to "listened", video note messages to "viewed" and starts the self-destruct timer */
export class UpdateMessageContentOpened {
  type: 'updateMessageContentOpened' = 'updateMessageContentOpened';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
}

/** Decode a TDLib `updateMessageContentOpened` object; null input -> null. */
export function decodeUpdateMessageContentOpened(json: TdJsonRaw): UpdateMessageContentOpened | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageContentOpened = new UpdateMessageContentOpened();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `updateMessageContentOpened` into TDLib JSON wire shape. */
export function encodeUpdateMessageContentOpened(value: UpdateMessageContentOpened): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageContentOpened';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** updateMessageEdited = Update — A message was edited. Changes in the message content will come in a separate updateMessageContent */
export class UpdateMessageEdited {
  type: 'updateMessageEdited' = 'updateMessageEdited';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** edit_date: int32 — Point in time (Unix timestamp) when the message was edited */
  edit_date: number = 0;
  /** reply_markup: ReplyMarkup — New message reply markup; may be null */
  reply_markup: TdReplyMarkup | null = null;
}

/** Decode a TDLib `updateMessageEdited` object; null input -> null. */
export function decodeUpdateMessageEdited(json: TdJsonRaw): UpdateMessageEdited | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageEdited = new UpdateMessageEdited();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.edit_date = asNumber(record['edit_date']);
  out.reply_markup = decodeTdReplyMarkup(record['reply_markup']);
  return out;
}

/** Encode a `updateMessageEdited` into TDLib JSON wire shape. */
export function encodeUpdateMessageEdited(value: UpdateMessageEdited): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageEdited';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['edit_date'] = value.edit_date;
  out['reply_markup'] = value.reply_markup === null ? null : encodeTdReplyMarkup(value.reply_markup);
  return out;
}

/** updateMessageEphemeralContent = Update — The message ephemeral content has changed */
export class UpdateMessageEphemeralContent {
  type: 'updateMessageEphemeralContent' = 'updateMessageEphemeralContent';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** ephemeral_content: ephemeralMessageContent — New ephemeral content of the message; may be null if none */
  ephemeral_content: EphemeralMessageContent | null = null;
}

/** Decode a TDLib `updateMessageEphemeralContent` object; null input -> null. */
export function decodeUpdateMessageEphemeralContent(json: TdJsonRaw): UpdateMessageEphemeralContent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageEphemeralContent = new UpdateMessageEphemeralContent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.ephemeral_content = decodeEphemeralMessageContent(record['ephemeral_content']);
  return out;
}

/** Encode a `updateMessageEphemeralContent` into TDLib JSON wire shape. */
export function encodeUpdateMessageEphemeralContent(value: UpdateMessageEphemeralContent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageEphemeralContent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['ephemeral_content'] = value.ephemeral_content === null ? null : encodeEphemeralMessageContent(value.ephemeral_content);
  return out;
}

/** updateMessageFactCheck = Update — A fact-check added to a message was changed */
export class UpdateMessageFactCheck {
  type: 'updateMessageFactCheck' = 'updateMessageFactCheck';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** fact_check: factCheck — The new fact-check */
  fact_check: FactCheck | null = null;
}

/** Decode a TDLib `updateMessageFactCheck` object; null input -> null. */
export function decodeUpdateMessageFactCheck(json: TdJsonRaw): UpdateMessageFactCheck | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageFactCheck = new UpdateMessageFactCheck();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.fact_check = decodeFactCheck(record['fact_check']);
  return out;
}

/** Encode a `updateMessageFactCheck` into TDLib JSON wire shape. */
export function encodeUpdateMessageFactCheck(value: UpdateMessageFactCheck): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageFactCheck';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['fact_check'] = value.fact_check === null ? null : encodeFactCheck(value.fact_check);
  return out;
}

/** updateMessageInteractionInfo = Update — The information about interactions with a message has changed */
export class UpdateMessageInteractionInfo {
  type: 'updateMessageInteractionInfo' = 'updateMessageInteractionInfo';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** interaction_info: messageInteractionInfo — New information about interactions with the message; may be null */
  interaction_info: MessageInteractionInfo | null = null;
}

/** Decode a TDLib `updateMessageInteractionInfo` object; null input -> null. */
export function decodeUpdateMessageInteractionInfo(json: TdJsonRaw): UpdateMessageInteractionInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageInteractionInfo = new UpdateMessageInteractionInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.interaction_info = decodeMessageInteractionInfo(record['interaction_info']);
  return out;
}

/** Encode a `updateMessageInteractionInfo` into TDLib JSON wire shape. */
export function encodeUpdateMessageInteractionInfo(value: UpdateMessageInteractionInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageInteractionInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['interaction_info'] = value.interaction_info === null ? null : encodeMessageInteractionInfo(value.interaction_info);
  return out;
}

/** updateMessageIsPinned = Update — The message pinned state was changed */
export class UpdateMessageIsPinned {
  type: 'updateMessageIsPinned' = 'updateMessageIsPinned';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — The message identifier */
  message_id: number = 0;
  /** is_pinned: Bool — True, if the message is pinned */
  is_pinned: boolean = false;
}

/** Decode a TDLib `updateMessageIsPinned` object; null input -> null. */
export function decodeUpdateMessageIsPinned(json: TdJsonRaw): UpdateMessageIsPinned | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageIsPinned = new UpdateMessageIsPinned();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `updateMessageIsPinned` into TDLib JSON wire shape. */
export function encodeUpdateMessageIsPinned(value: UpdateMessageIsPinned): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageIsPinned';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** updateMessageLiveLocationViewed = Update — A message with a live location was viewed. When the update is received, the application is expected to update the live location */
export class UpdateMessageLiveLocationViewed {
  type: 'updateMessageLiveLocationViewed' = 'updateMessageLiveLocationViewed';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the live location message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message with live location */
  message_id: number = 0;
}

/** Decode a TDLib `updateMessageLiveLocationViewed` object; null input -> null. */
export function decodeUpdateMessageLiveLocationViewed(json: TdJsonRaw): UpdateMessageLiveLocationViewed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageLiveLocationViewed = new UpdateMessageLiveLocationViewed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `updateMessageLiveLocationViewed` into TDLib JSON wire shape. */
export function encodeUpdateMessageLiveLocationViewed(value: UpdateMessageLiveLocationViewed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageLiveLocationViewed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** updateMessageMentionRead = Update — A message with an unread mention was read */
export class UpdateMessageMentionRead {
  type: 'updateMessageMentionRead' = 'updateMessageMentionRead';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** unread_mention_count: int32 — The new number of unread mention messages left in the chat */
  unread_mention_count: number = 0;
}

/** Decode a TDLib `updateMessageMentionRead` object; null input -> null. */
export function decodeUpdateMessageMentionRead(json: TdJsonRaw): UpdateMessageMentionRead | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageMentionRead = new UpdateMessageMentionRead();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.unread_mention_count = asNumber(record['unread_mention_count']);
  return out;
}

/** Encode a `updateMessageMentionRead` into TDLib JSON wire shape. */
export function encodeUpdateMessageMentionRead(value: UpdateMessageMentionRead): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageMentionRead';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['unread_mention_count'] = value.unread_mention_count;
  return out;
}

/** updateMessageReaction = Update — User changed its reactions on a message with public reactions; for bots only */
export class UpdateMessageReaction {
  type: 'updateMessageReaction' = 'updateMessageReaction';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** actor_id: MessageSender — Identifier of the user or chat that changed reactions */
  actor_id: TdMessageSender | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the reactions were changed */
  date: number = 0;
  /** old_reaction_types: vector<ReactionType> — Old list of chosen reactions */
  old_reaction_types: TdReactionType[] = [];
  /** new_reaction_types: vector<ReactionType> — New list of chosen reactions */
  new_reaction_types: TdReactionType[] = [];
}

/** Decode a TDLib `updateMessageReaction` object; null input -> null. */
export function decodeUpdateMessageReaction(json: TdJsonRaw): UpdateMessageReaction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageReaction = new UpdateMessageReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.actor_id = decodeTdMessageSender(record['actor_id']);
  out.date = asNumber(record['date']);
  out.old_reaction_types = decodeVector(record['old_reaction_types'], (item: TdJsonRaw): TdReactionType => decodeTdReactionType(item) as TdReactionType);
  out.new_reaction_types = decodeVector(record['new_reaction_types'], (item: TdJsonRaw): TdReactionType => decodeTdReactionType(item) as TdReactionType);
  return out;
}

/** Encode a `updateMessageReaction` into TDLib JSON wire shape. */
export function encodeUpdateMessageReaction(value: UpdateMessageReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['actor_id'] = value.actor_id === null ? null : encodeTdMessageSender(value.actor_id);
  out['date'] = value.date;
  out['old_reaction_types'] = encodeVector(value.old_reaction_types, (item: TdReactionType | null): TdJsonRaw => item === null ? null : encodeTdReactionType(item));
  out['new_reaction_types'] = encodeVector(value.new_reaction_types, (item: TdReactionType | null): TdJsonRaw => item === null ? null : encodeTdReactionType(item));
  return out;
}

/** updateMessageReactions = Update — Reactions added to a message with anonymous reactions have changed; for bots only */
export class UpdateMessageReactions {
  type: 'updateMessageReactions' = 'updateMessageReactions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** date: int32 — Point in time (Unix timestamp) when the reactions were changed */
  date: number = 0;
  /** reactions: vector<messageReaction> — The list of reactions added to the message */
  reactions: MessageReaction[] = [];
}

/** Decode a TDLib `updateMessageReactions` object; null input -> null. */
export function decodeUpdateMessageReactions(json: TdJsonRaw): UpdateMessageReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageReactions = new UpdateMessageReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.date = asNumber(record['date']);
  out.reactions = decodeVector(record['reactions'], (item: TdJsonRaw): MessageReaction => decodeMessageReaction(item) as MessageReaction);
  return out;
}

/** Encode a `updateMessageReactions` into TDLib JSON wire shape. */
export function encodeUpdateMessageReactions(value: UpdateMessageReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['date'] = value.date;
  out['reactions'] = encodeVector(value.reactions, (item: MessageReaction | null): TdJsonRaw => item === null ? null : encodeMessageReaction(item));
  return out;
}

/** updateMessageSendAcknowledged = Update — A request to send a message has reached the Telegram server. This doesn't mean that the message will be sent successfully. This update is sent only if the option "use_quick_ack" is set to true. Th... */
export class UpdateMessageSendAcknowledged {
  type: 'updateMessageSendAcknowledged' = 'updateMessageSendAcknowledged';
  extra?: string;
  /** chat_id: int53 — The chat identifier of the sent message */
  chat_id: number = 0;
  /** message_id: int53 — A temporary message identifier */
  message_id: number = 0;
}

/** Decode a TDLib `updateMessageSendAcknowledged` object; null input -> null. */
export function decodeUpdateMessageSendAcknowledged(json: TdJsonRaw): UpdateMessageSendAcknowledged | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageSendAcknowledged = new UpdateMessageSendAcknowledged();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `updateMessageSendAcknowledged` into TDLib JSON wire shape. */
export function encodeUpdateMessageSendAcknowledged(value: UpdateMessageSendAcknowledged): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageSendAcknowledged';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** updateMessageSendFailed = Update — A message failed to send. Be aware that some messages being sent can be irrecoverably deleted, in which case updateDeleteMessages will be received instead of this update */
export class UpdateMessageSendFailed {
  type: 'updateMessageSendFailed' = 'updateMessageSendFailed';
  extra?: string;
  /** message: message — The failed to send message */
  message: Message | null = null;
  /** old_message_id: int53 — The previous temporary message identifier */
  old_message_id: number = 0;
  /** error: error — The cause of the message sending failure */
  error: ErrorValue | null = null;
}

/** Decode a TDLib `updateMessageSendFailed` object; null input -> null. */
export function decodeUpdateMessageSendFailed(json: TdJsonRaw): UpdateMessageSendFailed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageSendFailed = new UpdateMessageSendFailed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  out.old_message_id = asNumber(record['old_message_id']);
  out.error = decodeErrorValue(record['error']);
  return out;
}

/** Encode a `updateMessageSendFailed` into TDLib JSON wire shape. */
export function encodeUpdateMessageSendFailed(value: UpdateMessageSendFailed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageSendFailed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  out['old_message_id'] = value.old_message_id;
  out['error'] = value.error === null ? null : encodeErrorValue(value.error);
  return out;
}

/** updateMessageSendSucceeded = Update — A message has been successfully sent For example, the field scheduling_state may change, making the message scheduled, or non-scheduled */
export class UpdateMessageSendSucceeded {
  type: 'updateMessageSendSucceeded' = 'updateMessageSendSucceeded';
  extra?: string;
  /** message: message — The sent message. Almost any field of the new message can be different from the corresponding field of the original message. */
  message: Message | null = null;
  /** old_message_id: int53 — The previous temporary message identifier */
  old_message_id: number = 0;
}

/** Decode a TDLib `updateMessageSendSucceeded` object; null input -> null. */
export function decodeUpdateMessageSendSucceeded(json: TdJsonRaw): UpdateMessageSendSucceeded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageSendSucceeded = new UpdateMessageSendSucceeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  out.old_message_id = asNumber(record['old_message_id']);
  return out;
}

/** Encode a `updateMessageSendSucceeded` into TDLib JSON wire shape. */
export function encodeUpdateMessageSendSucceeded(value: UpdateMessageSendSucceeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageSendSucceeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  out['old_message_id'] = value.old_message_id;
  return out;
}

/** updateMessageSuggestedPostInfo = Update — Information about suggested post of a message was changed */
export class UpdateMessageSuggestedPostInfo {
  type: 'updateMessageSuggestedPostInfo' = 'updateMessageSuggestedPostInfo';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** suggested_post_info: suggestedPostInfo — The new information about the suggested post */
  suggested_post_info: SuggestedPostInfo | null = null;
}

/** Decode a TDLib `updateMessageSuggestedPostInfo` object; null input -> null. */
export function decodeUpdateMessageSuggestedPostInfo(json: TdJsonRaw): UpdateMessageSuggestedPostInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageSuggestedPostInfo = new UpdateMessageSuggestedPostInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.suggested_post_info = decodeSuggestedPostInfo(record['suggested_post_info']);
  return out;
}

/** Encode a `updateMessageSuggestedPostInfo` into TDLib JSON wire shape. */
export function encodeUpdateMessageSuggestedPostInfo(value: UpdateMessageSuggestedPostInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageSuggestedPostInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['suggested_post_info'] = value.suggested_post_info === null ? null : encodeSuggestedPostInfo(value.suggested_post_info);
  return out;
}

/** updateMessageUnreadReactions = Update — The list of unread reactions added to a message was changed */
export class UpdateMessageUnreadReactions {
  type: 'updateMessageUnreadReactions' = 'updateMessageUnreadReactions';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** unread_reactions: vector<unreadReaction> — The new list of unread reactions */
  unread_reactions: UnreadReaction[] = [];
  /** unread_reaction_count: int32 — The new number of messages with unread reactions in the chat */
  unread_reaction_count: number = 0;
}

/** Decode a TDLib `updateMessageUnreadReactions` object; null input -> null. */
export function decodeUpdateMessageUnreadReactions(json: TdJsonRaw): UpdateMessageUnreadReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateMessageUnreadReactions = new UpdateMessageUnreadReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.unread_reactions = decodeVector(record['unread_reactions'], (item: TdJsonRaw): UnreadReaction => decodeUnreadReaction(item) as UnreadReaction);
  out.unread_reaction_count = asNumber(record['unread_reaction_count']);
  return out;
}

/** Encode a `updateMessageUnreadReactions` into TDLib JSON wire shape. */
export function encodeUpdateMessageUnreadReactions(value: UpdateMessageUnreadReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateMessageUnreadReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['unread_reactions'] = encodeVector(value.unread_reactions, (item: UnreadReaction | null): TdJsonRaw => item === null ? null : encodeUnreadReaction(item));
  out['unread_reaction_count'] = value.unread_reaction_count;
  return out;
}

/** updateNewBusinessCallbackQuery = Update — A new incoming callback query from a business message; for bots only */
export class UpdateNewBusinessCallbackQuery {
  type: 'updateNewBusinessCallbackQuery' = 'updateNewBusinessCallbackQuery';
  extra?: string;
  /** id: int64 — Unique query identifier */
  id: string = '';
  /** sender_user_id: int53 — Identifier of the user who sent the query */
  sender_user_id: number = 0;
  /** connection_id: string — Unique identifier of the business connection */
  connection_id: string = '';
  /** message: businessMessage — The message from the business account from which the query originated */
  message: BusinessMessage | null = null;
  /** chat_instance: int64 — An identifier uniquely corresponding to the chat a message was sent to */
  chat_instance: string = '';
  /** payload: CallbackQueryPayload — Query payload */
  payload: TdCallbackQueryPayload | null = null;
}

/** Decode a TDLib `updateNewBusinessCallbackQuery` object; null input -> null. */
export function decodeUpdateNewBusinessCallbackQuery(json: TdJsonRaw): UpdateNewBusinessCallbackQuery | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewBusinessCallbackQuery = new UpdateNewBusinessCallbackQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.sender_user_id = asNumber(record['sender_user_id']);
  out.connection_id = asString(record['connection_id']);
  out.message = decodeBusinessMessage(record['message']);
  out.chat_instance = asInt64(record['chat_instance']);
  out.payload = decodeTdCallbackQueryPayload(record['payload']);
  return out;
}

/** Encode a `updateNewBusinessCallbackQuery` into TDLib JSON wire shape. */
export function encodeUpdateNewBusinessCallbackQuery(value: UpdateNewBusinessCallbackQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewBusinessCallbackQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['sender_user_id'] = value.sender_user_id;
  out['connection_id'] = value.connection_id;
  out['message'] = value.message === null ? null : encodeBusinessMessage(value.message);
  out['chat_instance'] = value.chat_instance;
  out['payload'] = value.payload === null ? null : encodeTdCallbackQueryPayload(value.payload);
  return out;
}

/** updateNewBusinessMessage = Update — A new message was added to a business account; for bots only */
export class UpdateNewBusinessMessage {
  type: 'updateNewBusinessMessage' = 'updateNewBusinessMessage';
  extra?: string;
  /** connection_id: string — Unique identifier of the business connection */
  connection_id: string = '';
  /** message: businessMessage — The new message */
  message: BusinessMessage | null = null;
}

/** Decode a TDLib `updateNewBusinessMessage` object; null input -> null. */
export function decodeUpdateNewBusinessMessage(json: TdJsonRaw): UpdateNewBusinessMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewBusinessMessage = new UpdateNewBusinessMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.connection_id = asString(record['connection_id']);
  out.message = decodeBusinessMessage(record['message']);
  return out;
}

/** Encode a `updateNewBusinessMessage` into TDLib JSON wire shape. */
export function encodeUpdateNewBusinessMessage(value: UpdateNewBusinessMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewBusinessMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['connection_id'] = value.connection_id;
  out['message'] = value.message === null ? null : encodeBusinessMessage(value.message);
  return out;
}

/** updateNewCallSignalingData = Update — New call signaling data arrived */
export class UpdateNewCallSignalingData {
  type: 'updateNewCallSignalingData' = 'updateNewCallSignalingData';
  extra?: string;
  /** call_id: int32 — The call identifier */
  call_id: number = 0;
  /** data: bytes — The data */
  data: string = '';
}

/** Decode a TDLib `updateNewCallSignalingData` object; null input -> null. */
export function decodeUpdateNewCallSignalingData(json: TdJsonRaw): UpdateNewCallSignalingData | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewCallSignalingData = new UpdateNewCallSignalingData();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call_id = asNumber(record['call_id']);
  out.data = asString(record['data']);
  return out;
}

/** Encode a `updateNewCallSignalingData` into TDLib JSON wire shape. */
export function encodeUpdateNewCallSignalingData(value: UpdateNewCallSignalingData): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewCallSignalingData';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call_id'] = value.call_id;
  out['data'] = value.data;
  return out;
}

/** updateNewCallbackQuery = Update — A new incoming callback query; for bots only */
export class UpdateNewCallbackQuery {
  type: 'updateNewCallbackQuery' = 'updateNewCallbackQuery';
  extra?: string;
  /** id: int64 — Unique query identifier */
  id: string = '';
  /** sender_user_id: int53 — Identifier of the user who sent the query */
  sender_user_id: number = 0;
  /** chat_id: int53 — Identifier of the chat where the query was sent */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message from which the query originated */
  message_id: number = 0;
  /** chat_instance: int64 — Identifier that uniquely corresponds to the chat to which the message was sent */
  chat_instance: string = '';
  /** payload: CallbackQueryPayload — Query payload */
  payload: TdCallbackQueryPayload | null = null;
}

/** Decode a TDLib `updateNewCallbackQuery` object; null input -> null. */
export function decodeUpdateNewCallbackQuery(json: TdJsonRaw): UpdateNewCallbackQuery | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewCallbackQuery = new UpdateNewCallbackQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.sender_user_id = asNumber(record['sender_user_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.chat_instance = asInt64(record['chat_instance']);
  out.payload = decodeTdCallbackQueryPayload(record['payload']);
  return out;
}

/** Encode a `updateNewCallbackQuery` into TDLib JSON wire shape. */
export function encodeUpdateNewCallbackQuery(value: UpdateNewCallbackQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewCallbackQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['sender_user_id'] = value.sender_user_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['chat_instance'] = value.chat_instance;
  out['payload'] = value.payload === null ? null : encodeTdCallbackQueryPayload(value.payload);
  return out;
}

/** updateNewChat = Update — A new chat has been loaded/created. This update is guaranteed to come before the chat identifier is returned to the application. The chat field changes will be reported through separate updates */
export class UpdateNewChat {
  type: 'updateNewChat' = 'updateNewChat';
  extra?: string;
  /** chat: chat — The chat */
  chat: Chat | null = null;
}

/** Decode a TDLib `updateNewChat` object; null input -> null. */
export function decodeUpdateNewChat(json: TdJsonRaw): UpdateNewChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewChat = new UpdateNewChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat = decodeChat(record['chat']);
  return out;
}

/** Encode a `updateNewChat` into TDLib JSON wire shape. */
export function encodeUpdateNewChat(value: UpdateNewChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat'] = value.chat === null ? null : encodeChat(value.chat);
  return out;
}

/** updateNewChatJoinRequest = Update — A user sent a join request to a chat; for bots only */
export class UpdateNewChatJoinRequest {
  type: 'updateNewChatJoinRequest' = 'updateNewChatJoinRequest';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** request: chatJoinRequest — Join request */
  request: ChatJoinRequest | null = null;
  /** user_chat_id: int53 — Chat identifier of the private chat with the user */
  user_chat_id: number = 0;
  /** invite_link: chatInviteLink — The invite link, which was used to send join request; may be null */
  invite_link: ChatInviteLink | null = null;
  /** query_id: int64 — Identifier of the join request query, which can be used in answerChatJoinRequestQuery; 0 if none */
  query_id: string = '';
}

/** Decode a TDLib `updateNewChatJoinRequest` object; null input -> null. */
export function decodeUpdateNewChatJoinRequest(json: TdJsonRaw): UpdateNewChatJoinRequest | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewChatJoinRequest = new UpdateNewChatJoinRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.request = decodeChatJoinRequest(record['request']);
  out.user_chat_id = asNumber(record['user_chat_id']);
  out.invite_link = decodeChatInviteLink(record['invite_link']);
  out.query_id = asInt64(record['query_id']);
  return out;
}

/** Encode a `updateNewChatJoinRequest` into TDLib JSON wire shape. */
export function encodeUpdateNewChatJoinRequest(value: UpdateNewChatJoinRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewChatJoinRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['request'] = value.request === null ? null : encodeChatJoinRequest(value.request);
  out['user_chat_id'] = value.user_chat_id;
  out['invite_link'] = value.invite_link === null ? null : encodeChatInviteLink(value.invite_link);
  out['query_id'] = value.query_id;
  return out;
}

/** updateNewChosenInlineResult = Update — The user has chosen a result of an inline query; for bots only */
export class UpdateNewChosenInlineResult {
  type: 'updateNewChosenInlineResult' = 'updateNewChosenInlineResult';
  extra?: string;
  /** sender_user_id: int53 — Identifier of the user who sent the query */
  sender_user_id: number = 0;
  /** user_location: location — User location; may be null */
  user_location: Location | null = null;
  /** query: string — Text of the query */
  query: string = '';
  /** result_id: string — Identifier of the chosen result */
  result_id: string = '';
  /** inline_message_id: string — Identifier of the sent inline message, if known */
  inline_message_id: string = '';
}

/** Decode a TDLib `updateNewChosenInlineResult` object; null input -> null. */
export function decodeUpdateNewChosenInlineResult(json: TdJsonRaw): UpdateNewChosenInlineResult | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewChosenInlineResult = new UpdateNewChosenInlineResult();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_user_id = asNumber(record['sender_user_id']);
  out.user_location = decodeLocation(record['user_location']);
  out.query = asString(record['query']);
  out.result_id = asString(record['result_id']);
  out.inline_message_id = asString(record['inline_message_id']);
  return out;
}

/** Encode a `updateNewChosenInlineResult` into TDLib JSON wire shape. */
export function encodeUpdateNewChosenInlineResult(value: UpdateNewChosenInlineResult): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewChosenInlineResult';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_user_id'] = value.sender_user_id;
  out['user_location'] = value.user_location === null ? null : encodeLocation(value.user_location);
  out['query'] = value.query;
  out['result_id'] = value.result_id;
  out['inline_message_id'] = value.inline_message_id;
  return out;
}

/** updateNewCustomEvent = Update — A new incoming event; for bots only */
export class UpdateNewCustomEvent {
  type: 'updateNewCustomEvent' = 'updateNewCustomEvent';
  extra?: string;
  /** event: string — A JSON-serialized event */
  event: string = '';
}

/** Decode a TDLib `updateNewCustomEvent` object; null input -> null. */
export function decodeUpdateNewCustomEvent(json: TdJsonRaw): UpdateNewCustomEvent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewCustomEvent = new UpdateNewCustomEvent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.event = asString(record['event']);
  return out;
}

/** Encode a `updateNewCustomEvent` into TDLib JSON wire shape. */
export function encodeUpdateNewCustomEvent(value: UpdateNewCustomEvent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewCustomEvent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['event'] = value.event;
  return out;
}

/** updateNewCustomQuery = Update — A new incoming query; for bots only */
export class UpdateNewCustomQuery {
  type: 'updateNewCustomQuery' = 'updateNewCustomQuery';
  extra?: string;
  /** id: int64 — The query identifier */
  id: string = '';
  /** data: string — JSON-serialized query data */
  data: string = '';
  /** timeout: int32 — Query timeout */
  timeout: number = 0;
}

/** Decode a TDLib `updateNewCustomQuery` object; null input -> null. */
export function decodeUpdateNewCustomQuery(json: TdJsonRaw): UpdateNewCustomQuery | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewCustomQuery = new UpdateNewCustomQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.data = asString(record['data']);
  out.timeout = asNumber(record['timeout']);
  return out;
}

/** Encode a `updateNewCustomQuery` into TDLib JSON wire shape. */
export function encodeUpdateNewCustomQuery(value: UpdateNewCustomQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewCustomQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['data'] = value.data;
  out['timeout'] = value.timeout;
  return out;
}

/** updateNewGroupCallMessage = Update — A new message was received in a group call */
export class UpdateNewGroupCallMessage {
  type: 'updateNewGroupCallMessage' = 'updateNewGroupCallMessage';
  extra?: string;
  /** group_call_id: int32 — Identifier of the group call */
  group_call_id: number = 0;
  /** message: groupCallMessage — The message */
  message: GroupCallMessage | null = null;
}

/** Decode a TDLib `updateNewGroupCallMessage` object; null input -> null. */
export function decodeUpdateNewGroupCallMessage(json: TdJsonRaw): UpdateNewGroupCallMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewGroupCallMessage = new UpdateNewGroupCallMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.message = decodeGroupCallMessage(record['message']);
  return out;
}

/** Encode a `updateNewGroupCallMessage` into TDLib JSON wire shape. */
export function encodeUpdateNewGroupCallMessage(value: UpdateNewGroupCallMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewGroupCallMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['message'] = value.message === null ? null : encodeGroupCallMessage(value.message);
  return out;
}

/** updateNewGroupCallPaidReaction = Update — A new paid reaction was received in a live story group call */
export class UpdateNewGroupCallPaidReaction {
  type: 'updateNewGroupCallPaidReaction' = 'updateNewGroupCallPaidReaction';
  extra?: string;
  /** group_call_id: int32 — Identifier of the group call */
  group_call_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of the reaction */
  sender_id: TdMessageSender | null = null;
  /** star_count: int53 — The number of Telegram Stars that were paid to send the reaction */
  star_count: number = 0;
}

/** Decode a TDLib `updateNewGroupCallPaidReaction` object; null input -> null. */
export function decodeUpdateNewGroupCallPaidReaction(json: TdJsonRaw): UpdateNewGroupCallPaidReaction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewGroupCallPaidReaction = new UpdateNewGroupCallPaidReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `updateNewGroupCallPaidReaction` into TDLib JSON wire shape. */
export function encodeUpdateNewGroupCallPaidReaction(value: UpdateNewGroupCallPaidReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewGroupCallPaidReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['star_count'] = value.star_count;
  return out;
}

/** updateNewGuestQuery = Update — A new incoming guest query; for bots only */
export class UpdateNewGuestQuery {
  type: 'updateNewGuestQuery' = 'updateNewGuestQuery';
  extra?: string;
  /** id: int64 — Unique query identifier */
  id: string = '';
  /** message: message — The message with the query */
  message: Message | null = null;
  /** reference_messages: vector<message> — The list of reference messages */
  reference_messages: Message[] = [];
}

/** Decode a TDLib `updateNewGuestQuery` object; null input -> null. */
export function decodeUpdateNewGuestQuery(json: TdJsonRaw): UpdateNewGuestQuery | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewGuestQuery = new UpdateNewGuestQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.message = decodeMessage(record['message']);
  out.reference_messages = decodeVector(record['reference_messages'], (item: TdJsonRaw): Message => decodeMessage(item) as Message);
  return out;
}

/** Encode a `updateNewGuestQuery` into TDLib JSON wire shape. */
export function encodeUpdateNewGuestQuery(value: UpdateNewGuestQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewGuestQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  out['reference_messages'] = encodeVector(value.reference_messages, (item: Message | null): TdJsonRaw => item === null ? null : encodeMessage(item));
  return out;
}

/** updateNewInlineCallbackQuery = Update — A new incoming callback query from a message sent via a bot; for bots only */
export class UpdateNewInlineCallbackQuery {
  type: 'updateNewInlineCallbackQuery' = 'updateNewInlineCallbackQuery';
  extra?: string;
  /** id: int64 — Unique query identifier */
  id: string = '';
  /** sender_user_id: int53 — Identifier of the user who sent the query */
  sender_user_id: number = 0;
  /** inline_message_id: string — Identifier of the inline message from which the query originated */
  inline_message_id: string = '';
  /** chat_instance: int64 — An identifier uniquely corresponding to the chat a message was sent to */
  chat_instance: string = '';
  /** payload: CallbackQueryPayload — Query payload */
  payload: TdCallbackQueryPayload | null = null;
}

/** Decode a TDLib `updateNewInlineCallbackQuery` object; null input -> null. */
export function decodeUpdateNewInlineCallbackQuery(json: TdJsonRaw): UpdateNewInlineCallbackQuery | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewInlineCallbackQuery = new UpdateNewInlineCallbackQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.sender_user_id = asNumber(record['sender_user_id']);
  out.inline_message_id = asString(record['inline_message_id']);
  out.chat_instance = asInt64(record['chat_instance']);
  out.payload = decodeTdCallbackQueryPayload(record['payload']);
  return out;
}

/** Encode a `updateNewInlineCallbackQuery` into TDLib JSON wire shape. */
export function encodeUpdateNewInlineCallbackQuery(value: UpdateNewInlineCallbackQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewInlineCallbackQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['sender_user_id'] = value.sender_user_id;
  out['inline_message_id'] = value.inline_message_id;
  out['chat_instance'] = value.chat_instance;
  out['payload'] = value.payload === null ? null : encodeTdCallbackQueryPayload(value.payload);
  return out;
}

/** updateNewInlineQuery = Update — A new incoming inline query; for bots only */
export class UpdateNewInlineQuery {
  type: 'updateNewInlineQuery' = 'updateNewInlineQuery';
  extra?: string;
  /** id: int64 — Unique query identifier */
  id: string = '';
  /** sender_user_id: int53 — Identifier of the user who sent the query */
  sender_user_id: number = 0;
  /** user_location: location — User location; may be null */
  user_location: Location | null = null;
  /** chat_type: ChatType — The type of the chat from which the query originated; may be null if unknown */
  chat_type: TdChatType | null = null;
  /** query: string — Text of the query */
  query: string = '';
  /** offset: string — Offset of the first entry to return */
  offset: string = '';
}

/** Decode a TDLib `updateNewInlineQuery` object; null input -> null. */
export function decodeUpdateNewInlineQuery(json: TdJsonRaw): UpdateNewInlineQuery | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewInlineQuery = new UpdateNewInlineQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.sender_user_id = asNumber(record['sender_user_id']);
  out.user_location = decodeLocation(record['user_location']);
  out.chat_type = decodeTdChatType(record['chat_type']);
  out.query = asString(record['query']);
  out.offset = asString(record['offset']);
  return out;
}

/** Encode a `updateNewInlineQuery` into TDLib JSON wire shape. */
export function encodeUpdateNewInlineQuery(value: UpdateNewInlineQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewInlineQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['sender_user_id'] = value.sender_user_id;
  out['user_location'] = value.user_location === null ? null : encodeLocation(value.user_location);
  out['chat_type'] = value.chat_type === null ? null : encodeTdChatType(value.chat_type);
  out['query'] = value.query;
  out['offset'] = value.offset;
  return out;
}

/** updateNewMessage = Update — A new message was received; can also be an outgoing message */
export class UpdateNewMessage {
  type: 'updateNewMessage' = 'updateNewMessage';
  extra?: string;
  /** message: message — The new message */
  message: Message | null = null;
}

/** Decode a TDLib `updateNewMessage` object; null input -> null. */
export function decodeUpdateNewMessage(json: TdJsonRaw): UpdateNewMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewMessage = new UpdateNewMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  return out;
}

/** Encode a `updateNewMessage` into TDLib JSON wire shape. */
export function encodeUpdateNewMessage(value: UpdateNewMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  return out;
}

/** updateNewOauthRequest = Update — An OAuth authorization request was received */
export class UpdateNewOauthRequest {
  type: 'updateNewOauthRequest' = 'updateNewOauthRequest';
  extra?: string;
  /** domain: string — A domain of the URL where the user authorizes */
  domain: string = '';
  /** location: string — Human-readable description of a country and a region from which the authorization is performed, based on the IP address */
  location: string = '';
  /** url: string — The URL to pass to getOauthLinkInfo; the link is valid for 60 seconds */
  url: string = '';
}

/** Decode a TDLib `updateNewOauthRequest` object; null input -> null. */
export function decodeUpdateNewOauthRequest(json: TdJsonRaw): UpdateNewOauthRequest | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewOauthRequest = new UpdateNewOauthRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.domain = asString(record['domain']);
  out.location = asString(record['location']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `updateNewOauthRequest` into TDLib JSON wire shape. */
export function encodeUpdateNewOauthRequest(value: UpdateNewOauthRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewOauthRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['domain'] = value.domain;
  out['location'] = value.location;
  out['url'] = value.url;
  return out;
}

/** updateNewPreCheckoutQuery = Update — A new incoming pre-checkout query; for bots only. Contains full information about a checkout */
export class UpdateNewPreCheckoutQuery {
  type: 'updateNewPreCheckoutQuery' = 'updateNewPreCheckoutQuery';
  extra?: string;
  /** id: int64 — Unique query identifier */
  id: string = '';
  /** sender_user_id: int53 — Identifier of the user who sent the query */
  sender_user_id: number = 0;
  /** currency: string — Currency for the product price */
  currency: string = '';
  /** total_amount: int53 — Total price for the product, in the smallest units of the currency */
  total_amount: number = 0;
  /** invoice_payload: bytes — Invoice payload */
  invoice_payload: string = '';
  /** shipping_option_id: string — Identifier of a shipping option chosen by the user; may be empty if not applicable */
  shipping_option_id: string = '';
  /** order_info: orderInfo — Information about the order; may be null */
  order_info: OrderInfo | null = null;
}

/** Decode a TDLib `updateNewPreCheckoutQuery` object; null input -> null. */
export function decodeUpdateNewPreCheckoutQuery(json: TdJsonRaw): UpdateNewPreCheckoutQuery | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewPreCheckoutQuery = new UpdateNewPreCheckoutQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.sender_user_id = asNumber(record['sender_user_id']);
  out.currency = asString(record['currency']);
  out.total_amount = asNumber(record['total_amount']);
  out.invoice_payload = asString(record['invoice_payload']);
  out.shipping_option_id = asString(record['shipping_option_id']);
  out.order_info = decodeOrderInfo(record['order_info']);
  return out;
}

/** Encode a `updateNewPreCheckoutQuery` into TDLib JSON wire shape. */
export function encodeUpdateNewPreCheckoutQuery(value: UpdateNewPreCheckoutQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewPreCheckoutQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['sender_user_id'] = value.sender_user_id;
  out['currency'] = value.currency;
  out['total_amount'] = value.total_amount;
  out['invoice_payload'] = value.invoice_payload;
  out['shipping_option_id'] = value.shipping_option_id;
  out['order_info'] = value.order_info === null ? null : encodeOrderInfo(value.order_info);
  return out;
}

/** updateNewShippingQuery = Update — A new incoming shipping query; for bots only. Only for invoices with flexible price */
export class UpdateNewShippingQuery {
  type: 'updateNewShippingQuery' = 'updateNewShippingQuery';
  extra?: string;
  /** id: int64 — Unique query identifier */
  id: string = '';
  /** sender_user_id: int53 — Identifier of the user who sent the query */
  sender_user_id: number = 0;
  /** invoice_payload: string — Invoice payload */
  invoice_payload: string = '';
  /** shipping_address: address — User shipping address */
  shipping_address: Address | null = null;
}

/** Decode a TDLib `updateNewShippingQuery` object; null input -> null. */
export function decodeUpdateNewShippingQuery(json: TdJsonRaw): UpdateNewShippingQuery | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNewShippingQuery = new UpdateNewShippingQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.sender_user_id = asNumber(record['sender_user_id']);
  out.invoice_payload = asString(record['invoice_payload']);
  out.shipping_address = decodeAddress(record['shipping_address']);
  return out;
}

/** Encode a `updateNewShippingQuery` into TDLib JSON wire shape. */
export function encodeUpdateNewShippingQuery(value: UpdateNewShippingQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNewShippingQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['sender_user_id'] = value.sender_user_id;
  out['invoice_payload'] = value.invoice_payload;
  out['shipping_address'] = value.shipping_address === null ? null : encodeAddress(value.shipping_address);
  return out;
}

/** updateNotification = Update — A notification was changed */
export class UpdateNotification {
  type: 'updateNotification' = 'updateNotification';
  extra?: string;
  /** notification_group_id: int32 — Unique notification group identifier */
  notification_group_id: number = 0;
  /** notification: notification — Changed notification */
  notification: Notification | null = null;
}

/** Decode a TDLib `updateNotification` object; null input -> null. */
export function decodeUpdateNotification(json: TdJsonRaw): UpdateNotification | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNotification = new UpdateNotification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_group_id = asNumber(record['notification_group_id']);
  out.notification = decodeNotification(record['notification']);
  return out;
}

/** Encode a `updateNotification` into TDLib JSON wire shape. */
export function encodeUpdateNotification(value: UpdateNotification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNotification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_group_id'] = value.notification_group_id;
  out['notification'] = value.notification === null ? null : encodeNotification(value.notification);
  return out;
}

/** updateNotificationGroup = Update — A list of active notifications in a notification group has changed */
export class UpdateNotificationGroup {
  type: 'updateNotificationGroup' = 'updateNotificationGroup';
  extra?: string;
  /** notification_group_id: int32 — Unique notification group identifier */
  notification_group_id: number = 0;
  /** type: NotificationGroupType — New type of the notification group */
  type_: TdNotificationGroupType | null = null;
  /** chat_id: int53 — Identifier of a chat to which all notifications in the group belong */
  chat_id: number = 0;
  /** notification_settings_chat_id: int53 — Chat identifier, which notification settings must be applied to the added notifications */
  notification_settings_chat_id: number = 0;
  /** notification_sound_id: int64 — Identifier of the notification sound to be played; 0 if sound is disabled */
  notification_sound_id: string = '';
  /** total_count: int32 — Total number of unread notifications in the group, can be bigger than number of active notifications */
  total_count: number = 0;
  /** added_notifications: vector<notification> — List of added group notifications, sorted by notification identifier */
  added_notifications: Notification[] = [];
  /** removed_notification_ids: vector<int32> — Identifiers of removed group notifications, sorted by notification identifier */
  removed_notification_ids: number[] = [];
}

/** Decode a TDLib `updateNotificationGroup` object; null input -> null. */
export function decodeUpdateNotificationGroup(json: TdJsonRaw): UpdateNotificationGroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateNotificationGroup = new UpdateNotificationGroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_group_id = asNumber(record['notification_group_id']);
  out.type_ = decodeTdNotificationGroupType(record['type']);
  out.chat_id = asNumber(record['chat_id']);
  out.notification_settings_chat_id = asNumber(record['notification_settings_chat_id']);
  out.notification_sound_id = asInt64(record['notification_sound_id']);
  out.total_count = asNumber(record['total_count']);
  out.added_notifications = decodeVector(record['added_notifications'], (item: TdJsonRaw): Notification => decodeNotification(item) as Notification);
  out.removed_notification_ids = decodeVector(record['removed_notification_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateNotificationGroup` into TDLib JSON wire shape. */
export function encodeUpdateNotificationGroup(value: UpdateNotificationGroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateNotificationGroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_group_id'] = value.notification_group_id;
  out['type'] = value.type_ === null ? null : encodeTdNotificationGroupType(value.type_);
  out['chat_id'] = value.chat_id;
  out['notification_settings_chat_id'] = value.notification_settings_chat_id;
  out['notification_sound_id'] = value.notification_sound_id;
  out['total_count'] = value.total_count;
  out['added_notifications'] = encodeVector(value.added_notifications, (item: Notification | null): TdJsonRaw => item === null ? null : encodeNotification(item));
  out['removed_notification_ids'] = encodeVector(value.removed_notification_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateOption = Update — An option changed its value */
export class UpdateOption {
  type: 'updateOption' = 'updateOption';
  extra?: string;
  /** name: string — The option name */
  name: string = '';
  /** value: OptionValue — The new option value */
  value: TdOptionValue | null = null;
}

/** Decode a TDLib `updateOption` object; null input -> null. */
export function decodeUpdateOption(json: TdJsonRaw): UpdateOption | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateOption = new UpdateOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.value = decodeTdOptionValue(record['value']);
  return out;
}

/** Encode a `updateOption` into TDLib JSON wire shape. */
export function encodeUpdateOption(value: UpdateOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['value'] = value.value === null ? null : encodeTdOptionValue(value.value);
  return out;
}

/** updateOwnedGramCount = Update — The number of TON Grams owned by the current user has changed */
export class UpdateOwnedGramCount {
  type: 'updateOwnedGramCount' = 'updateOwnedGramCount';
  extra?: string;
  /** gram_amount: int53 — The new amount of owned Grams; in the smallest units of the cryptocurrency */
  gram_amount: number = 0;
}

/** Decode a TDLib `updateOwnedGramCount` object; null input -> null. */
export function decodeUpdateOwnedGramCount(json: TdJsonRaw): UpdateOwnedGramCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateOwnedGramCount = new UpdateOwnedGramCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gram_amount = asNumber(record['gram_amount']);
  return out;
}

/** Encode a `updateOwnedGramCount` into TDLib JSON wire shape. */
export function encodeUpdateOwnedGramCount(value: UpdateOwnedGramCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateOwnedGramCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gram_amount'] = value.gram_amount;
  return out;
}

/** updateOwnedStarCount = Update — The number of Telegram Stars owned by the current user has changed */
export class UpdateOwnedStarCount {
  type: 'updateOwnedStarCount' = 'updateOwnedStarCount';
  extra?: string;
  /** star_amount: starAmount — The new amount of owned Telegram Stars */
  star_amount: StarAmount | null = null;
}

/** Decode a TDLib `updateOwnedStarCount` object; null input -> null. */
export function decodeUpdateOwnedStarCount(json: TdJsonRaw): UpdateOwnedStarCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateOwnedStarCount = new UpdateOwnedStarCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_amount = decodeStarAmount(record['star_amount']);
  return out;
}

/** Encode a `updateOwnedStarCount` into TDLib JSON wire shape. */
export function encodeUpdateOwnedStarCount(value: UpdateOwnedStarCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateOwnedStarCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_amount'] = value.star_amount === null ? null : encodeStarAmount(value.star_amount);
  return out;
}

/** updatePaidMediaPurchased = Update — Paid media were purchased by a user; for bots only */
export class UpdatePaidMediaPurchased {
  type: 'updatePaidMediaPurchased' = 'updatePaidMediaPurchased';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** payload: string — Bot-specified payload for the paid media */
  payload: string = '';
}

/** Decode a TDLib `updatePaidMediaPurchased` object; null input -> null. */
export function decodeUpdatePaidMediaPurchased(json: TdJsonRaw): UpdatePaidMediaPurchased | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdatePaidMediaPurchased = new UpdatePaidMediaPurchased();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.payload = asString(record['payload']);
  return out;
}

/** Encode a `updatePaidMediaPurchased` into TDLib JSON wire shape. */
export function encodeUpdatePaidMediaPurchased(value: UpdatePaidMediaPurchased): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updatePaidMediaPurchased';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['payload'] = value.payload;
  return out;
}

/** updatePendingMessage = Update — A new pending text or rich message was received in a chat with a bot. The message must be shown in the chat for at most getOption("pending_text_message_period") seconds, replace any other pending message w... */
export class UpdatePendingMessage {
  type: 'updatePendingMessage' = 'updatePendingMessage';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — The forum topic identifier in which the message will be sent; 0 if none */
  forum_topic_id: number = 0;
  /** draft_id: int64 — Unique identifier of the message draft within the message thread */
  draft_id: string = '';
  /** can_stop: Bool — True, if a button that calls stopPendingMessage to stop further message generation must be shown */
  can_stop: boolean = false;
  /** keep_on_stop: Bool — True, if the pending message must not be automatically deleted when the user presses the Stop button */
  keep_on_stop: boolean = false;
  /** content: MessageContent — Content of the message; always of the type messageText or messageRichMessage */
  content: TdMessageContent | null = null;
}

/** Decode a TDLib `updatePendingMessage` object; null input -> null. */
export function decodeUpdatePendingMessage(json: TdJsonRaw): UpdatePendingMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdatePendingMessage = new UpdatePendingMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.draft_id = asInt64(record['draft_id']);
  out.can_stop = asBool(record['can_stop']);
  out.keep_on_stop = asBool(record['keep_on_stop']);
  out.content = decodeTdMessageContent(record['content']);
  return out;
}

/** Encode a `updatePendingMessage` into TDLib JSON wire shape. */
export function encodeUpdatePendingMessage(value: UpdatePendingMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updatePendingMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['draft_id'] = value.draft_id;
  out['can_stop'] = value.can_stop;
  out['keep_on_stop'] = value.keep_on_stop;
  out['content'] = value.content === null ? null : encodeTdMessageContent(value.content);
  return out;
}

/** updatePoll = Update — A poll was updated; for bots only */
export class UpdatePoll {
  type: 'updatePoll' = 'updatePoll';
  extra?: string;
  /** poll: poll — New data about the poll */
  poll: Poll | null = null;
}

/** Decode a TDLib `updatePoll` object; null input -> null. */
export function decodeUpdatePoll(json: TdJsonRaw): UpdatePoll | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdatePoll = new UpdatePoll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.poll = decodePoll(record['poll']);
  return out;
}

/** Encode a `updatePoll` into TDLib JSON wire shape. */
export function encodeUpdatePoll(value: UpdatePoll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updatePoll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['poll'] = value.poll === null ? null : encodePoll(value.poll);
  return out;
}

/** updatePollAnswer = Update — A user changed the answer to a poll; for bots only */
export class UpdatePollAnswer {
  type: 'updatePollAnswer' = 'updatePollAnswer';
  extra?: string;
  /** poll_id: int64 — Unique poll identifier */
  poll_id: string = '';
  /** voter_id: MessageSender — Identifier of the message sender that changed the answer to the poll */
  voter_id: TdMessageSender | null = null;
  /** option_ids: vector<string> — Unique identifiers of answer options, that were chosen by the user */
  option_ids: string[] = [];
  /** option_positions: vector<int32> — 0-based identifiers of answer options, that were chosen by the user */
  option_positions: number[] = [];
}

/** Decode a TDLib `updatePollAnswer` object; null input -> null. */
export function decodeUpdatePollAnswer(json: TdJsonRaw): UpdatePollAnswer | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdatePollAnswer = new UpdatePollAnswer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.poll_id = asInt64(record['poll_id']);
  out.voter_id = decodeTdMessageSender(record['voter_id']);
  out.option_ids = decodeVector(record['option_ids'], (item: TdJsonRaw): string => asString(item));
  out.option_positions = decodeVector(record['option_positions'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updatePollAnswer` into TDLib JSON wire shape. */
export function encodeUpdatePollAnswer(value: UpdatePollAnswer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updatePollAnswer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['poll_id'] = value.poll_id;
  out['voter_id'] = value.voter_id === null ? null : encodeTdMessageSender(value.voter_id);
  out['option_ids'] = encodeVector(value.option_ids, (item: string): TdJsonRaw => item);
  out['option_positions'] = encodeVector(value.option_positions, (item: number): TdJsonRaw => item);
  return out;
}

/** updateProfileAccentColors = Update — The list of supported accent colors for user profiles has changed */
export class UpdateProfileAccentColors {
  type: 'updateProfileAccentColors' = 'updateProfileAccentColors';
  extra?: string;
  /** colors: vector<profileAccentColor> — Information about supported colors */
  colors: ProfileAccentColor[] = [];
  /** available_accent_color_ids: vector<int32> — The list of accent color identifiers, which can be set through setProfileAccentColor and setChatProfileAccentColor. The colors must be shown in the specified order */
  available_accent_color_ids: number[] = [];
}

/** Decode a TDLib `updateProfileAccentColors` object; null input -> null. */
export function decodeUpdateProfileAccentColors(json: TdJsonRaw): UpdateProfileAccentColors | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateProfileAccentColors = new UpdateProfileAccentColors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.colors = decodeVector(record['colors'], (item: TdJsonRaw): ProfileAccentColor => decodeProfileAccentColor(item) as ProfileAccentColor);
  out.available_accent_color_ids = decodeVector(record['available_accent_color_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateProfileAccentColors` into TDLib JSON wire shape. */
export function encodeUpdateProfileAccentColors(value: UpdateProfileAccentColors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateProfileAccentColors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['colors'] = encodeVector(value.colors, (item: ProfileAccentColor | null): TdJsonRaw => item === null ? null : encodeProfileAccentColor(item));
  out['available_accent_color_ids'] = encodeVector(value.available_accent_color_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateQuickReplyShortcut = Update — Basic information about a quick reply shortcut has changed. This update is guaranteed to come before the quick shortcut name is returned to the application */
export class UpdateQuickReplyShortcut {
  type: 'updateQuickReplyShortcut' = 'updateQuickReplyShortcut';
  extra?: string;
  /** shortcut: quickReplyShortcut — New data about the shortcut */
  shortcut: QuickReplyShortcut | null = null;
}

/** Decode a TDLib `updateQuickReplyShortcut` object; null input -> null. */
export function decodeUpdateQuickReplyShortcut(json: TdJsonRaw): UpdateQuickReplyShortcut | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateQuickReplyShortcut = new UpdateQuickReplyShortcut();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut = decodeQuickReplyShortcut(record['shortcut']);
  return out;
}

/** Encode a `updateQuickReplyShortcut` into TDLib JSON wire shape. */
export function encodeUpdateQuickReplyShortcut(value: UpdateQuickReplyShortcut): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateQuickReplyShortcut';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut'] = value.shortcut === null ? null : encodeQuickReplyShortcut(value.shortcut);
  return out;
}

/** updateQuickReplyShortcutDeleted = Update — A quick reply shortcut and all its messages were deleted */
export class UpdateQuickReplyShortcutDeleted {
  type: 'updateQuickReplyShortcutDeleted' = 'updateQuickReplyShortcutDeleted';
  extra?: string;
  /** shortcut_id: int32 — The identifier of the deleted shortcut */
  shortcut_id: number = 0;
}

/** Decode a TDLib `updateQuickReplyShortcutDeleted` object; null input -> null. */
export function decodeUpdateQuickReplyShortcutDeleted(json: TdJsonRaw): UpdateQuickReplyShortcutDeleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateQuickReplyShortcutDeleted = new UpdateQuickReplyShortcutDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_id = asNumber(record['shortcut_id']);
  return out;
}

/** Encode a `updateQuickReplyShortcutDeleted` into TDLib JSON wire shape. */
export function encodeUpdateQuickReplyShortcutDeleted(value: UpdateQuickReplyShortcutDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateQuickReplyShortcutDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_id'] = value.shortcut_id;
  return out;
}

/** updateQuickReplyShortcutMessages = Update — The list of quick reply shortcut messages has changed */
export class UpdateQuickReplyShortcutMessages {
  type: 'updateQuickReplyShortcutMessages' = 'updateQuickReplyShortcutMessages';
  extra?: string;
  /** shortcut_id: int32 — The identifier of the shortcut */
  shortcut_id: number = 0;
  /** messages: vector<quickReplyMessage> — The new list of quick reply messages for the shortcut in order from the first to the last sent */
  messages: QuickReplyMessage[] = [];
}

/** Decode a TDLib `updateQuickReplyShortcutMessages` object; null input -> null. */
export function decodeUpdateQuickReplyShortcutMessages(json: TdJsonRaw): UpdateQuickReplyShortcutMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateQuickReplyShortcutMessages = new UpdateQuickReplyShortcutMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_id = asNumber(record['shortcut_id']);
  out.messages = decodeVector(record['messages'], (item: TdJsonRaw): QuickReplyMessage => decodeQuickReplyMessage(item) as QuickReplyMessage);
  return out;
}

/** Encode a `updateQuickReplyShortcutMessages` into TDLib JSON wire shape. */
export function encodeUpdateQuickReplyShortcutMessages(value: UpdateQuickReplyShortcutMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateQuickReplyShortcutMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_id'] = value.shortcut_id;
  out['messages'] = encodeVector(value.messages, (item: QuickReplyMessage | null): TdJsonRaw => item === null ? null : encodeQuickReplyMessage(item));
  return out;
}

/** updateQuickReplyShortcuts = Update — The list of quick reply shortcuts has changed */
export class UpdateQuickReplyShortcuts {
  type: 'updateQuickReplyShortcuts' = 'updateQuickReplyShortcuts';
  extra?: string;
  /** shortcut_ids: vector<int32> — The new list of identifiers of quick reply shortcuts */
  shortcut_ids: number[] = [];
}

/** Decode a TDLib `updateQuickReplyShortcuts` object; null input -> null. */
export function decodeUpdateQuickReplyShortcuts(json: TdJsonRaw): UpdateQuickReplyShortcuts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateQuickReplyShortcuts = new UpdateQuickReplyShortcuts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_ids = decodeVector(record['shortcut_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateQuickReplyShortcuts` into TDLib JSON wire shape. */
export function encodeUpdateQuickReplyShortcuts(value: UpdateQuickReplyShortcuts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateQuickReplyShortcuts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_ids'] = encodeVector(value.shortcut_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateReactionNotificationSettings = Update — Notification settings for reactions were updated */
export class UpdateReactionNotificationSettings {
  type: 'updateReactionNotificationSettings' = 'updateReactionNotificationSettings';
  extra?: string;
  /** notification_settings: reactionNotificationSettings — The new notification settings */
  notification_settings: ReactionNotificationSettings | null = null;
}

/** Decode a TDLib `updateReactionNotificationSettings` object; null input -> null. */
export function decodeUpdateReactionNotificationSettings(json: TdJsonRaw): UpdateReactionNotificationSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateReactionNotificationSettings = new UpdateReactionNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_settings = decodeReactionNotificationSettings(record['notification_settings']);
  return out;
}

/** Encode a `updateReactionNotificationSettings` into TDLib JSON wire shape. */
export function encodeUpdateReactionNotificationSettings(value: UpdateReactionNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateReactionNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_settings'] = value.notification_settings === null ? null : encodeReactionNotificationSettings(value.notification_settings);
  return out;
}

/** updateRecentStickers = Update — The list of recently used stickers was updated */
export class UpdateRecentStickers {
  type: 'updateRecentStickers' = 'updateRecentStickers';
  extra?: string;
  /** is_attached: Bool — True, if the list of stickers attached to photo or video files was updated; otherwise, the list of sent stickers is updated */
  is_attached: boolean = false;
  /** sticker_ids: vector<int32> — The new list of file identifiers of recently used stickers */
  sticker_ids: number[] = [];
}

/** Decode a TDLib `updateRecentStickers` object; null input -> null. */
export function decodeUpdateRecentStickers(json: TdJsonRaw): UpdateRecentStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateRecentStickers = new UpdateRecentStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_attached = asBool(record['is_attached']);
  out.sticker_ids = decodeVector(record['sticker_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateRecentStickers` into TDLib JSON wire shape. */
export function encodeUpdateRecentStickers(value: UpdateRecentStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateRecentStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_attached'] = value.is_attached;
  out['sticker_ids'] = encodeVector(value.sticker_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateSavedAnimations = Update — The list of saved animations was updated */
export class UpdateSavedAnimations {
  type: 'updateSavedAnimations' = 'updateSavedAnimations';
  extra?: string;
  /** animation_ids: vector<int32> — The new list of file identifiers of saved animations */
  animation_ids: number[] = [];
}

/** Decode a TDLib `updateSavedAnimations` object; null input -> null. */
export function decodeUpdateSavedAnimations(json: TdJsonRaw): UpdateSavedAnimations | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSavedAnimations = new UpdateSavedAnimations();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation_ids = decodeVector(record['animation_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateSavedAnimations` into TDLib JSON wire shape. */
export function encodeUpdateSavedAnimations(value: UpdateSavedAnimations): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSavedAnimations';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation_ids'] = encodeVector(value.animation_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateSavedMessagesTags = Update — Tags used in Saved Messages or a Saved Messages topic have changed */
export class UpdateSavedMessagesTags {
  type: 'updateSavedMessagesTags' = 'updateSavedMessagesTags';
  extra?: string;
  /** saved_messages_topic_id: int53 — Identifier of Saved Messages topic which tags were changed; 0 if tags for the whole chat has changed */
  saved_messages_topic_id: number = 0;
  /** tags: savedMessagesTags — The new tags */
  tags: SavedMessagesTags | null = null;
}

/** Decode a TDLib `updateSavedMessagesTags` object; null input -> null. */
export function decodeUpdateSavedMessagesTags(json: TdJsonRaw): UpdateSavedMessagesTags | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSavedMessagesTags = new UpdateSavedMessagesTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.saved_messages_topic_id = asNumber(record['saved_messages_topic_id']);
  out.tags = decodeSavedMessagesTags(record['tags']);
  return out;
}

/** Encode a `updateSavedMessagesTags` into TDLib JSON wire shape. */
export function encodeUpdateSavedMessagesTags(value: UpdateSavedMessagesTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSavedMessagesTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['saved_messages_topic_id'] = value.saved_messages_topic_id;
  out['tags'] = value.tags === null ? null : encodeSavedMessagesTags(value.tags);
  return out;
}

/** updateSavedMessagesTopic = Update — Basic information about a Saved Messages topic has changed. This update is guaranteed to come before the topic identifier is returned to the application */
export class UpdateSavedMessagesTopic {
  type: 'updateSavedMessagesTopic' = 'updateSavedMessagesTopic';
  extra?: string;
  /** topic: savedMessagesTopic — New data about the topic */
  topic: SavedMessagesTopic | null = null;
}

/** Decode a TDLib `updateSavedMessagesTopic` object; null input -> null. */
export function decodeUpdateSavedMessagesTopic(json: TdJsonRaw): UpdateSavedMessagesTopic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSavedMessagesTopic = new UpdateSavedMessagesTopic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.topic = decodeSavedMessagesTopic(record['topic']);
  return out;
}

/** Encode a `updateSavedMessagesTopic` into TDLib JSON wire shape. */
export function encodeUpdateSavedMessagesTopic(value: UpdateSavedMessagesTopic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSavedMessagesTopic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['topic'] = value.topic === null ? null : encodeSavedMessagesTopic(value.topic);
  return out;
}

/** updateSavedMessagesTopicCount = Update — Number of Saved Messages topics has changed */
export class UpdateSavedMessagesTopicCount {
  type: 'updateSavedMessagesTopicCount' = 'updateSavedMessagesTopicCount';
  extra?: string;
  /** topic_count: int32 — Approximate total number of Saved Messages topics */
  topic_count: number = 0;
}

/** Decode a TDLib `updateSavedMessagesTopicCount` object; null input -> null. */
export function decodeUpdateSavedMessagesTopicCount(json: TdJsonRaw): UpdateSavedMessagesTopicCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSavedMessagesTopicCount = new UpdateSavedMessagesTopicCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.topic_count = asNumber(record['topic_count']);
  return out;
}

/** Encode a `updateSavedMessagesTopicCount` into TDLib JSON wire shape. */
export function encodeUpdateSavedMessagesTopicCount(value: UpdateSavedMessagesTopicCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSavedMessagesTopicCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['topic_count'] = value.topic_count;
  return out;
}

/** updateSavedNotificationSounds = Update — The list of saved notification sounds was updated. This update may not be sent until information about a notification sound was requested for the first time */
export class UpdateSavedNotificationSounds {
  type: 'updateSavedNotificationSounds' = 'updateSavedNotificationSounds';
  extra?: string;
  /** notification_sound_ids: vector<int64> — The new list of identifiers of saved notification sounds */
  notification_sound_ids: string[] = [];
}

/** Decode a TDLib `updateSavedNotificationSounds` object; null input -> null. */
export function decodeUpdateSavedNotificationSounds(json: TdJsonRaw): UpdateSavedNotificationSounds | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSavedNotificationSounds = new UpdateSavedNotificationSounds();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.notification_sound_ids = decodeVector(record['notification_sound_ids'], (item: TdJsonRaw): string => asInt64(item));
  return out;
}

/** Encode a `updateSavedNotificationSounds` into TDLib JSON wire shape. */
export function encodeUpdateSavedNotificationSounds(value: UpdateSavedNotificationSounds): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSavedNotificationSounds';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['notification_sound_ids'] = encodeVector(value.notification_sound_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** updateScopeNotificationSettings = Update — Notification settings for some type of chats were updated */
export class UpdateScopeNotificationSettings {
  type: 'updateScopeNotificationSettings' = 'updateScopeNotificationSettings';
  extra?: string;
  /** scope: NotificationSettingsScope — Types of chats for which notification settings were updated */
  scope: TdNotificationSettingsScope | null = null;
  /** notification_settings: scopeNotificationSettings — The new notification settings */
  notification_settings: ScopeNotificationSettings | null = null;
}

/** Decode a TDLib `updateScopeNotificationSettings` object; null input -> null. */
export function decodeUpdateScopeNotificationSettings(json: TdJsonRaw): UpdateScopeNotificationSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateScopeNotificationSettings = new UpdateScopeNotificationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.scope = decodeTdNotificationSettingsScope(record['scope']);
  out.notification_settings = decodeScopeNotificationSettings(record['notification_settings']);
  return out;
}

/** Encode a `updateScopeNotificationSettings` into TDLib JSON wire shape. */
export function encodeUpdateScopeNotificationSettings(value: UpdateScopeNotificationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateScopeNotificationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['scope'] = value.scope === null ? null : encodeTdNotificationSettingsScope(value.scope);
  out['notification_settings'] = value.notification_settings === null ? null : encodeScopeNotificationSettings(value.notification_settings);
  return out;
}

/** updateSecretChat = Update — Some data of a secret chat has changed. This update is guaranteed to come before the secret chat identifier is returned to the application */
export class UpdateSecretChat {
  type: 'updateSecretChat' = 'updateSecretChat';
  extra?: string;
  /** secret_chat: secretChat — New data about the secret chat */
  secret_chat: SecretChat | null = null;
}

/** Decode a TDLib `updateSecretChat` object; null input -> null. */
export function decodeUpdateSecretChat(json: TdJsonRaw): UpdateSecretChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSecretChat = new UpdateSecretChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.secret_chat = decodeSecretChat(record['secret_chat']);
  return out;
}

/** Encode a `updateSecretChat` into TDLib JSON wire shape. */
export function encodeUpdateSecretChat(value: UpdateSecretChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSecretChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['secret_chat'] = value.secret_chat === null ? null : encodeSecretChat(value.secret_chat);
  return out;
}

/** updateServiceNotification = Update — A service notification from the server was received. Upon receiving this the application must show a popup with the content of the notification */
export class UpdateServiceNotification {
  type: 'updateServiceNotification' = 'updateServiceNotification';
  extra?: string;
  /** type: string — Notification type. If type begins with "AUTH_KEY_DROP_", then two buttons "Cancel" and "Log out" must be shown under notification; if user presses the second, all local data must be destroyed using Destroy method */
  type_: string = '';
  /** content: MessageContent — Notification content */
  content: TdMessageContent | null = null;
}

/** Decode a TDLib `updateServiceNotification` object; null input -> null. */
export function decodeUpdateServiceNotification(json: TdJsonRaw): UpdateServiceNotification | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateServiceNotification = new UpdateServiceNotification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = asString(record['type']);
  out.content = decodeTdMessageContent(record['content']);
  return out;
}

/** Encode a `updateServiceNotification` into TDLib JSON wire shape. */
export function encodeUpdateServiceNotification(value: UpdateServiceNotification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateServiceNotification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_;
  out['content'] = value.content === null ? null : encodeTdMessageContent(value.content);
  return out;
}

/** updateSpeechRecognitionTrial = Update — The parameters of speech recognition without Telegram Premium subscription have changed */
export class UpdateSpeechRecognitionTrial {
  type: 'updateSpeechRecognitionTrial' = 'updateSpeechRecognitionTrial';
  extra?: string;
  /** max_media_duration: int32 — The maximum allowed duration of media for speech recognition without Telegram Premium subscription, in seconds */
  max_media_duration: number = 0;
  /** weekly_count: int32 — The total number of allowed speech recognitions per week; 0 if none */
  weekly_count: number = 0;
  /** left_count: int32 — Number of left speech recognition attempts this week */
  left_count: number = 0;
  /** next_reset_date: int32 — Point in time (Unix timestamp) when the weekly number of tries will reset; 0 if unknown */
  next_reset_date: number = 0;
}

/** Decode a TDLib `updateSpeechRecognitionTrial` object; null input -> null. */
export function decodeUpdateSpeechRecognitionTrial(json: TdJsonRaw): UpdateSpeechRecognitionTrial | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSpeechRecognitionTrial = new UpdateSpeechRecognitionTrial();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.max_media_duration = asNumber(record['max_media_duration']);
  out.weekly_count = asNumber(record['weekly_count']);
  out.left_count = asNumber(record['left_count']);
  out.next_reset_date = asNumber(record['next_reset_date']);
  return out;
}

/** Encode a `updateSpeechRecognitionTrial` into TDLib JSON wire shape. */
export function encodeUpdateSpeechRecognitionTrial(value: UpdateSpeechRecognitionTrial): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSpeechRecognitionTrial';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['max_media_duration'] = value.max_media_duration;
  out['weekly_count'] = value.weekly_count;
  out['left_count'] = value.left_count;
  out['next_reset_date'] = value.next_reset_date;
  return out;
}

/** updateSpeedLimitNotification = Update — Download or upload file speed for the user was limited, but it can be restored by subscription to Telegram Premium. The notification can be postponed until a file being downloaded or uploaded is vi... */
export class UpdateSpeedLimitNotification {
  type: 'updateSpeedLimitNotification' = 'updateSpeedLimitNotification';
  extra?: string;
  /** is_upload: Bool — True, if upload speed was limited; false, if download speed was limited */
  is_upload: boolean = false;
}

/** Decode a TDLib `updateSpeedLimitNotification` object; null input -> null. */
export function decodeUpdateSpeedLimitNotification(json: TdJsonRaw): UpdateSpeedLimitNotification | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSpeedLimitNotification = new UpdateSpeedLimitNotification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_upload = asBool(record['is_upload']);
  return out;
}

/** Encode a `updateSpeedLimitNotification` into TDLib JSON wire shape. */
export function encodeUpdateSpeedLimitNotification(value: UpdateSpeedLimitNotification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSpeedLimitNotification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_upload'] = value.is_upload;
  return out;
}

/** updateStakeDiceState = Update — The stake dice state has changed */
export class UpdateStakeDiceState {
  type: 'updateStakeDiceState' = 'updateStakeDiceState';
  extra?: string;
  /** state: stakeDiceState — The new state. The state can be used only if it was received recently enough. Otherwise, a new state must be requested using getStakeDiceState */
  state: StakeDiceState | null = null;
}

/** Decode a TDLib `updateStakeDiceState` object; null input -> null. */
export function decodeUpdateStakeDiceState(json: TdJsonRaw): UpdateStakeDiceState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStakeDiceState = new UpdateStakeDiceState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.state = decodeStakeDiceState(record['state']);
  return out;
}

/** Encode a `updateStakeDiceState` into TDLib JSON wire shape. */
export function encodeUpdateStakeDiceState(value: UpdateStakeDiceState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStakeDiceState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['state'] = value.state === null ? null : encodeStakeDiceState(value.state);
  return out;
}

/** updateStarRevenueStatus = Update — The Telegram Star revenue earned by a user or a chat has changed. If Telegram Star transaction screen of the chat is opened, then getStarTransactions may be called to fetch new transactions */
export class UpdateStarRevenueStatus {
  type: 'updateStarRevenueStatus' = 'updateStarRevenueStatus';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the owner of the Telegram Stars */
  owner_id: TdMessageSender | null = null;
  /** status: starRevenueStatus — New Telegram Star revenue status */
  status: StarRevenueStatus | null = null;
}

/** Decode a TDLib `updateStarRevenueStatus` object; null input -> null. */
export function decodeUpdateStarRevenueStatus(json: TdJsonRaw): UpdateStarRevenueStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStarRevenueStatus = new UpdateStarRevenueStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.status = decodeStarRevenueStatus(record['status']);
  return out;
}

/** Encode a `updateStarRevenueStatus` into TDLib JSON wire shape. */
export function encodeUpdateStarRevenueStatus(value: UpdateStarRevenueStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStarRevenueStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['status'] = value.status === null ? null : encodeStarRevenueStatus(value.status);
  return out;
}

/** updateStickerSet = Update — A sticker set has changed */
export class UpdateStickerSet {
  type: 'updateStickerSet' = 'updateStickerSet';
  extra?: string;
  /** sticker_set: stickerSet — The sticker set */
  sticker_set: StickerSet | null = null;
}

/** Decode a TDLib `updateStickerSet` object; null input -> null. */
export function decodeUpdateStickerSet(json: TdJsonRaw): UpdateStickerSet | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStickerSet = new UpdateStickerSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_set = decodeStickerSet(record['sticker_set']);
  return out;
}

/** Encode a `updateStickerSet` into TDLib JSON wire shape. */
export function encodeUpdateStickerSet(value: UpdateStickerSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStickerSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_set'] = value.sticker_set === null ? null : encodeStickerSet(value.sticker_set);
  return out;
}

/** updateStopMessageDraft = Update — A message draft generation was stopped by the user */
export class UpdateStopMessageDraft {
  type: 'updateStopMessageDraft' = 'updateStopMessageDraft';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** forum_topic_id: int32 — The forum topic identifier of the message draft */
  forum_topic_id: number = 0;
  /** draft_id: int64 — Identifier of the message draft within the message thread */
  draft_id: string = '';
}

/** Decode a TDLib `updateStopMessageDraft` object; null input -> null. */
export function decodeUpdateStopMessageDraft(json: TdJsonRaw): UpdateStopMessageDraft | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStopMessageDraft = new UpdateStopMessageDraft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.forum_topic_id = asNumber(record['forum_topic_id']);
  out.draft_id = asInt64(record['draft_id']);
  return out;
}

/** Encode a `updateStopMessageDraft` into TDLib JSON wire shape. */
export function encodeUpdateStopMessageDraft(value: UpdateStopMessageDraft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStopMessageDraft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['forum_topic_id'] = value.forum_topic_id;
  out['draft_id'] = value.draft_id;
  return out;
}

/** updateStory = Update — A story was changed */
export class UpdateStory {
  type: 'updateStory' = 'updateStory';
  extra?: string;
  /** story: story — The new information about the story */
  story: Story | null = null;
}

/** Decode a TDLib `updateStory` object; null input -> null. */
export function decodeUpdateStory(json: TdJsonRaw): UpdateStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStory = new UpdateStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story = decodeStory(record['story']);
  return out;
}

/** Encode a `updateStory` into TDLib JSON wire shape. */
export function encodeUpdateStory(value: UpdateStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story'] = value.story === null ? null : encodeStory(value.story);
  return out;
}

/** updateStoryDeleted = Update — A story became inaccessible */
export class UpdateStoryDeleted {
  type: 'updateStoryDeleted' = 'updateStoryDeleted';
  extra?: string;
  /** story_poster_chat_id: int53 — Identifier of the chat that posted the story */
  story_poster_chat_id: number = 0;
  /** story_id: int32 — Story identifier */
  story_id: number = 0;
}

/** Decode a TDLib `updateStoryDeleted` object; null input -> null. */
export function decodeUpdateStoryDeleted(json: TdJsonRaw): UpdateStoryDeleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStoryDeleted = new UpdateStoryDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_poster_chat_id = asNumber(record['story_poster_chat_id']);
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `updateStoryDeleted` into TDLib JSON wire shape. */
export function encodeUpdateStoryDeleted(value: UpdateStoryDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStoryDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_poster_chat_id'] = value.story_poster_chat_id;
  out['story_id'] = value.story_id;
  return out;
}

/** updateStoryListChatCount = Update — Number of chats in a story list has changed */
export class UpdateStoryListChatCount {
  type: 'updateStoryListChatCount' = 'updateStoryListChatCount';
  extra?: string;
  /** story_list: StoryList — The story list */
  story_list: TdStoryList | null = null;
  /** chat_count: int32 — Approximate total number of chats with active stories in the list */
  chat_count: number = 0;
}

/** Decode a TDLib `updateStoryListChatCount` object; null input -> null. */
export function decodeUpdateStoryListChatCount(json: TdJsonRaw): UpdateStoryListChatCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStoryListChatCount = new UpdateStoryListChatCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_list = decodeTdStoryList(record['story_list']);
  out.chat_count = asNumber(record['chat_count']);
  return out;
}

/** Encode a `updateStoryListChatCount` into TDLib JSON wire shape. */
export function encodeUpdateStoryListChatCount(value: UpdateStoryListChatCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStoryListChatCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_list'] = value.story_list === null ? null : encodeTdStoryList(value.story_list);
  out['chat_count'] = value.chat_count;
  return out;
}

/** updateStoryPostFailed = Update — A story failed to post. If the story posting is canceled, then updateStoryDeleted will be received instead of this update */
export class UpdateStoryPostFailed {
  type: 'updateStoryPostFailed' = 'updateStoryPostFailed';
  extra?: string;
  /** story: story — The failed to post story */
  story: Story | null = null;
  /** error: error — The cause of the story posting failure */
  error: ErrorValue | null = null;
  /** error_type: CanPostStoryResult — Type of the error; may be null if unknown */
  error_type: TdCanPostStoryResult | null = null;
}

/** Decode a TDLib `updateStoryPostFailed` object; null input -> null. */
export function decodeUpdateStoryPostFailed(json: TdJsonRaw): UpdateStoryPostFailed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStoryPostFailed = new UpdateStoryPostFailed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story = decodeStory(record['story']);
  out.error = decodeErrorValue(record['error']);
  out.error_type = decodeTdCanPostStoryResult(record['error_type']);
  return out;
}

/** Encode a `updateStoryPostFailed` into TDLib JSON wire shape. */
export function encodeUpdateStoryPostFailed(value: UpdateStoryPostFailed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStoryPostFailed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story'] = value.story === null ? null : encodeStory(value.story);
  out['error'] = value.error === null ? null : encodeErrorValue(value.error);
  out['error_type'] = value.error_type === null ? null : encodeTdCanPostStoryResult(value.error_type);
  return out;
}

/** updateStoryPostSucceeded = Update — A story has been successfully posted */
export class UpdateStoryPostSucceeded {
  type: 'updateStoryPostSucceeded' = 'updateStoryPostSucceeded';
  extra?: string;
  /** story: story — The posted story */
  story: Story | null = null;
  /** old_story_id: int32 — The previous temporary story identifier */
  old_story_id: number = 0;
}

/** Decode a TDLib `updateStoryPostSucceeded` object; null input -> null. */
export function decodeUpdateStoryPostSucceeded(json: TdJsonRaw): UpdateStoryPostSucceeded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStoryPostSucceeded = new UpdateStoryPostSucceeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story = decodeStory(record['story']);
  out.old_story_id = asNumber(record['old_story_id']);
  return out;
}

/** Encode a `updateStoryPostSucceeded` into TDLib JSON wire shape. */
export function encodeUpdateStoryPostSucceeded(value: UpdateStoryPostSucceeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStoryPostSucceeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story'] = value.story === null ? null : encodeStory(value.story);
  out['old_story_id'] = value.old_story_id;
  return out;
}

/** updateStoryStealthMode = Update — Story stealth mode settings have changed */
export class UpdateStoryStealthMode {
  type: 'updateStoryStealthMode' = 'updateStoryStealthMode';
  extra?: string;
  /** active_until_date: int32 — Point in time (Unix timestamp) until stealth mode is active; 0 if it is disabled */
  active_until_date: number = 0;
  /** cooldown_until_date: int32 — Point in time (Unix timestamp) when stealth mode can be enabled again; 0 if there is no active cooldown */
  cooldown_until_date: number = 0;
}

/** Decode a TDLib `updateStoryStealthMode` object; null input -> null. */
export function decodeUpdateStoryStealthMode(json: TdJsonRaw): UpdateStoryStealthMode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateStoryStealthMode = new UpdateStoryStealthMode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.active_until_date = asNumber(record['active_until_date']);
  out.cooldown_until_date = asNumber(record['cooldown_until_date']);
  return out;
}

/** Encode a `updateStoryStealthMode` into TDLib JSON wire shape. */
export function encodeUpdateStoryStealthMode(value: UpdateStoryStealthMode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateStoryStealthMode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['active_until_date'] = value.active_until_date;
  out['cooldown_until_date'] = value.cooldown_until_date;
  return out;
}

/** updateSuggestedActions = Update — The list of suggested to the user actions has changed */
export class UpdateSuggestedActions {
  type: 'updateSuggestedActions' = 'updateSuggestedActions';
  extra?: string;
  /** added_actions: vector<SuggestedAction> — Added suggested actions */
  added_actions: TdSuggestedAction[] = [];
  /** removed_actions: vector<SuggestedAction> — Removed suggested actions */
  removed_actions: TdSuggestedAction[] = [];
}

/** Decode a TDLib `updateSuggestedActions` object; null input -> null. */
export function decodeUpdateSuggestedActions(json: TdJsonRaw): UpdateSuggestedActions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSuggestedActions = new UpdateSuggestedActions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.added_actions = decodeVector(record['added_actions'], (item: TdJsonRaw): TdSuggestedAction => decodeTdSuggestedAction(item) as TdSuggestedAction);
  out.removed_actions = decodeVector(record['removed_actions'], (item: TdJsonRaw): TdSuggestedAction => decodeTdSuggestedAction(item) as TdSuggestedAction);
  return out;
}

/** Encode a `updateSuggestedActions` into TDLib JSON wire shape. */
export function encodeUpdateSuggestedActions(value: UpdateSuggestedActions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSuggestedActions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['added_actions'] = encodeVector(value.added_actions, (item: TdSuggestedAction | null): TdJsonRaw => item === null ? null : encodeTdSuggestedAction(item));
  out['removed_actions'] = encodeVector(value.removed_actions, (item: TdSuggestedAction | null): TdJsonRaw => item === null ? null : encodeTdSuggestedAction(item));
  return out;
}

/** updateSupergroup = Update — Some data of a supergroup or a channel has changed. This update is guaranteed to come before the supergroup identifier is returned to the application */
export class UpdateSupergroup {
  type: 'updateSupergroup' = 'updateSupergroup';
  extra?: string;
  /** supergroup: supergroup — New data about the supergroup */
  supergroup: Supergroup | null = null;
}

/** Decode a TDLib `updateSupergroup` object; null input -> null. */
export function decodeUpdateSupergroup(json: TdJsonRaw): UpdateSupergroup | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSupergroup = new UpdateSupergroup();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup = decodeSupergroup(record['supergroup']);
  return out;
}

/** Encode a `updateSupergroup` into TDLib JSON wire shape. */
export function encodeUpdateSupergroup(value: UpdateSupergroup): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSupergroup';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup'] = value.supergroup === null ? null : encodeSupergroup(value.supergroup);
  return out;
}

/** updateSupergroupFullInfo = Update — Some data in supergroupFullInfo has been changed */
export class UpdateSupergroupFullInfo {
  type: 'updateSupergroupFullInfo' = 'updateSupergroupFullInfo';
  extra?: string;
  /** supergroup_id: int53 — Identifier of the supergroup or channel */
  supergroup_id: number = 0;
  /** supergroup_full_info: supergroupFullInfo — New full information about the supergroup */
  supergroup_full_info: SupergroupFullInfo | null = null;
}

/** Decode a TDLib `updateSupergroupFullInfo` object; null input -> null. */
export function decodeUpdateSupergroupFullInfo(json: TdJsonRaw): UpdateSupergroupFullInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateSupergroupFullInfo = new UpdateSupergroupFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.supergroup_id = asNumber(record['supergroup_id']);
  out.supergroup_full_info = decodeSupergroupFullInfo(record['supergroup_full_info']);
  return out;
}

/** Encode a `updateSupergroupFullInfo` into TDLib JSON wire shape. */
export function encodeUpdateSupergroupFullInfo(value: UpdateSupergroupFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateSupergroupFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['supergroup_id'] = value.supergroup_id;
  out['supergroup_full_info'] = value.supergroup_full_info === null ? null : encodeSupergroupFullInfo(value.supergroup_full_info);
  return out;
}

/** updateTermsOfService = Update — New terms of service must be accepted by the user. If the terms of service are declined, then the deleteAccount method must be called with the reason "Decline ToS update" */
export class UpdateTermsOfService {
  type: 'updateTermsOfService' = 'updateTermsOfService';
  extra?: string;
  /** terms_of_service_id: string — Identifier of the terms of service */
  terms_of_service_id: string = '';
  /** terms_of_service: termsOfService — The new terms of service */
  terms_of_service: TermsOfService | null = null;
}

/** Decode a TDLib `updateTermsOfService` object; null input -> null. */
export function decodeUpdateTermsOfService(json: TdJsonRaw): UpdateTermsOfService | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateTermsOfService = new UpdateTermsOfService();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.terms_of_service_id = asString(record['terms_of_service_id']);
  out.terms_of_service = decodeTermsOfService(record['terms_of_service']);
  return out;
}

/** Encode a `updateTermsOfService` into TDLib JSON wire shape. */
export function encodeUpdateTermsOfService(value: UpdateTermsOfService): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateTermsOfService';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['terms_of_service_id'] = value.terms_of_service_id;
  out['terms_of_service'] = value.terms_of_service === null ? null : encodeTermsOfService(value.terms_of_service);
  return out;
}

/** updateTextCompositionStyles = Update — The styles supported for text composition have changed */
export class UpdateTextCompositionStyles {
  type: 'updateTextCompositionStyles' = 'updateTextCompositionStyles';
  extra?: string;
  /** styles: vector<textCompositionStyle> — The new list of supported styles */
  styles: TextCompositionStyle[] = [];
}

/** Decode a TDLib `updateTextCompositionStyles` object; null input -> null. */
export function decodeUpdateTextCompositionStyles(json: TdJsonRaw): UpdateTextCompositionStyles | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateTextCompositionStyles = new UpdateTextCompositionStyles();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.styles = decodeVector(record['styles'], (item: TdJsonRaw): TextCompositionStyle => decodeTextCompositionStyle(item) as TextCompositionStyle);
  return out;
}

/** Encode a `updateTextCompositionStyles` into TDLib JSON wire shape. */
export function encodeUpdateTextCompositionStyles(value: UpdateTextCompositionStyles): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateTextCompositionStyles';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['styles'] = encodeVector(value.styles, (item: TextCompositionStyle | null): TdJsonRaw => item === null ? null : encodeTextCompositionStyle(item));
  return out;
}

/** updateTopicMessageCount = Update — Number of messages in a topic has changed; for Saved Messages and channel direct messages chat topics only */
export class UpdateTopicMessageCount {
  type: 'updateTopicMessageCount' = 'updateTopicMessageCount';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat in topic of which the number of messages has changed */
  chat_id: number = 0;
  /** topic_id: MessageTopic — Identifier of the topic */
  topic_id: TdMessageTopic | null = null;
  /** message_count: int32 — Approximate number of messages in the topic */
  message_count: number = 0;
}

/** Decode a TDLib `updateTopicMessageCount` object; null input -> null. */
export function decodeUpdateTopicMessageCount(json: TdJsonRaw): UpdateTopicMessageCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateTopicMessageCount = new UpdateTopicMessageCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.topic_id = decodeTdMessageTopic(record['topic_id']);
  out.message_count = asNumber(record['message_count']);
  return out;
}

/** Encode a `updateTopicMessageCount` into TDLib JSON wire shape. */
export function encodeUpdateTopicMessageCount(value: UpdateTopicMessageCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateTopicMessageCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['topic_id'] = value.topic_id === null ? null : encodeTdMessageTopic(value.topic_id);
  out['message_count'] = value.message_count;
  return out;
}

/** updateTrendingStickerSets = Update — The list of trending sticker sets was updated or some of them were viewed */
export class UpdateTrendingStickerSets {
  type: 'updateTrendingStickerSets' = 'updateTrendingStickerSets';
  extra?: string;
  /** sticker_type: StickerType — Type of the affected stickers */
  sticker_type: TdStickerType | null = null;
  /** sticker_sets: trendingStickerSets — The prefix of the list of trending sticker sets with the newest trending sticker sets */
  sticker_sets: TrendingStickerSets | null = null;
}

/** Decode a TDLib `updateTrendingStickerSets` object; null input -> null. */
export function decodeUpdateTrendingStickerSets(json: TdJsonRaw): UpdateTrendingStickerSets | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateTrendingStickerSets = new UpdateTrendingStickerSets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_type = decodeTdStickerType(record['sticker_type']);
  out.sticker_sets = decodeTrendingStickerSets(record['sticker_sets']);
  return out;
}

/** Encode a `updateTrendingStickerSets` into TDLib JSON wire shape. */
export function encodeUpdateTrendingStickerSets(value: UpdateTrendingStickerSets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateTrendingStickerSets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_type'] = value.sticker_type === null ? null : encodeTdStickerType(value.sticker_type);
  out['sticker_sets'] = value.sticker_sets === null ? null : encodeTrendingStickerSets(value.sticker_sets);
  return out;
}

/** updateTrustedMiniAppBots = Update — Lists of bots which Mini Apps must be allowed to read text from clipboard and must be opened without a warning */
export class UpdateTrustedMiniAppBots {
  type: 'updateTrustedMiniAppBots' = 'updateTrustedMiniAppBots';
  extra?: string;
  /** bot_user_ids: vector<int53> — List of user identifiers of the bots; the corresponding users may not be sent using updateUser updates and may not be accessible */
  bot_user_ids: number[] = [];
}

/** Decode a TDLib `updateTrustedMiniAppBots` object; null input -> null. */
export function decodeUpdateTrustedMiniAppBots(json: TdJsonRaw): UpdateTrustedMiniAppBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateTrustedMiniAppBots = new UpdateTrustedMiniAppBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_ids = decodeVector(record['bot_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `updateTrustedMiniAppBots` into TDLib JSON wire shape. */
export function encodeUpdateTrustedMiniAppBots(value: UpdateTrustedMiniAppBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateTrustedMiniAppBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_ids'] = encodeVector(value.bot_user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** updateUnconfirmedSession = Update — The first unconfirmed session has changed */
export class UpdateUnconfirmedSession {
  type: 'updateUnconfirmedSession' = 'updateUnconfirmedSession';
  extra?: string;
  /** session: unconfirmedSession — The unconfirmed session; may be null if none */
  session: UnconfirmedSession | null = null;
  /** unconfirmed_session_count: int32 — The total number of unconfirmed sessions */
  unconfirmed_session_count: number = 0;
}

/** Decode a TDLib `updateUnconfirmedSession` object; null input -> null. */
export function decodeUpdateUnconfirmedSession(json: TdJsonRaw): UpdateUnconfirmedSession | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateUnconfirmedSession = new UpdateUnconfirmedSession();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.session = decodeUnconfirmedSession(record['session']);
  out.unconfirmed_session_count = asNumber(record['unconfirmed_session_count']);
  return out;
}

/** Encode a `updateUnconfirmedSession` into TDLib JSON wire shape. */
export function encodeUpdateUnconfirmedSession(value: UpdateUnconfirmedSession): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateUnconfirmedSession';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['session'] = value.session === null ? null : encodeUnconfirmedSession(value.session);
  out['unconfirmed_session_count'] = value.unconfirmed_session_count;
  return out;
}

/** updateUnreadChatCount = Update — Number of unread chats, i.e. with unread messages or marked as unread, has changed. This update is sent only if the message database is used */
export class UpdateUnreadChatCount {
  type: 'updateUnreadChatCount' = 'updateUnreadChatCount';
  extra?: string;
  /** chat_list: ChatList — The chat list with changed number of unread messages */
  chat_list: TdChatList | null = null;
  /** total_count: int32 — Approximate total number of chats in the chat list */
  total_count: number = 0;
  /** unread_count: int32 — Total number of unread chats */
  unread_count: number = 0;
  /** unread_unmuted_count: int32 — Total number of unread unmuted chats */
  unread_unmuted_count: number = 0;
  /** marked_as_unread_count: int32 — Total number of chats marked as unread */
  marked_as_unread_count: number = 0;
  /** marked_as_unread_unmuted_count: int32 — Total number of unmuted chats marked as unread */
  marked_as_unread_unmuted_count: number = 0;
}

/** Decode a TDLib `updateUnreadChatCount` object; null input -> null. */
export function decodeUpdateUnreadChatCount(json: TdJsonRaw): UpdateUnreadChatCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateUnreadChatCount = new UpdateUnreadChatCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_list = decodeTdChatList(record['chat_list']);
  out.total_count = asNumber(record['total_count']);
  out.unread_count = asNumber(record['unread_count']);
  out.unread_unmuted_count = asNumber(record['unread_unmuted_count']);
  out.marked_as_unread_count = asNumber(record['marked_as_unread_count']);
  out.marked_as_unread_unmuted_count = asNumber(record['marked_as_unread_unmuted_count']);
  return out;
}

/** Encode a `updateUnreadChatCount` into TDLib JSON wire shape. */
export function encodeUpdateUnreadChatCount(value: UpdateUnreadChatCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateUnreadChatCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  out['total_count'] = value.total_count;
  out['unread_count'] = value.unread_count;
  out['unread_unmuted_count'] = value.unread_unmuted_count;
  out['marked_as_unread_count'] = value.marked_as_unread_count;
  out['marked_as_unread_unmuted_count'] = value.marked_as_unread_unmuted_count;
  return out;
}

/** updateUnreadMessageCount = Update — Number of unread messages in a chat list has changed. This update is sent only if the message database is used */
export class UpdateUnreadMessageCount {
  type: 'updateUnreadMessageCount' = 'updateUnreadMessageCount';
  extra?: string;
  /** chat_list: ChatList — The chat list with changed number of unread messages */
  chat_list: TdChatList | null = null;
  /** unread_count: int32 — Total number of unread messages */
  unread_count: number = 0;
  /** unread_unmuted_count: int32 — Total number of unread messages in unmuted chats */
  unread_unmuted_count: number = 0;
}

/** Decode a TDLib `updateUnreadMessageCount` object; null input -> null. */
export function decodeUpdateUnreadMessageCount(json: TdJsonRaw): UpdateUnreadMessageCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateUnreadMessageCount = new UpdateUnreadMessageCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_list = decodeTdChatList(record['chat_list']);
  out.unread_count = asNumber(record['unread_count']);
  out.unread_unmuted_count = asNumber(record['unread_unmuted_count']);
  return out;
}

/** Encode a `updateUnreadMessageCount` into TDLib JSON wire shape. */
export function encodeUpdateUnreadMessageCount(value: UpdateUnreadMessageCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateUnreadMessageCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  out['unread_count'] = value.unread_count;
  out['unread_unmuted_count'] = value.unread_unmuted_count;
  return out;
}

/** updateUser = Update — Some data of a user has changed. This update is guaranteed to come before the user identifier is returned to the application */
export class UpdateUser {
  type: 'updateUser' = 'updateUser';
  extra?: string;
  /** user: user — New data about the user */
  user: User | null = null;
}

/** Decode a TDLib `updateUser` object; null input -> null. */
export function decodeUpdateUser(json: TdJsonRaw): UpdateUser | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateUser = new UpdateUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user = decodeUser(record['user']);
  return out;
}

/** Encode a `updateUser` into TDLib JSON wire shape. */
export function encodeUpdateUser(value: UpdateUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user'] = value.user === null ? null : encodeUser(value.user);
  return out;
}

/** updateUserFullInfo = Update — Some data in userFullInfo has been changed */
export class UpdateUserFullInfo {
  type: 'updateUserFullInfo' = 'updateUserFullInfo';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** user_full_info: userFullInfo — New full information about the user */
  user_full_info: UserFullInfo | null = null;
}

/** Decode a TDLib `updateUserFullInfo` object; null input -> null. */
export function decodeUpdateUserFullInfo(json: TdJsonRaw): UpdateUserFullInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateUserFullInfo = new UpdateUserFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.user_full_info = decodeUserFullInfo(record['user_full_info']);
  return out;
}

/** Encode a `updateUserFullInfo` into TDLib JSON wire shape. */
export function encodeUpdateUserFullInfo(value: UpdateUserFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateUserFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['user_full_info'] = value.user_full_info === null ? null : encodeUserFullInfo(value.user_full_info);
  return out;
}

/** updateUserPrivacySettingRules = Update — Some privacy setting rules have been changed */
export class UpdateUserPrivacySettingRules {
  type: 'updateUserPrivacySettingRules' = 'updateUserPrivacySettingRules';
  extra?: string;
  /** setting: UserPrivacySetting — The privacy setting */
  setting: TdUserPrivacySetting | null = null;
  /** rules: userPrivacySettingRules — New privacy rules */
  rules: UserPrivacySettingRules | null = null;
}

/** Decode a TDLib `updateUserPrivacySettingRules` object; null input -> null. */
export function decodeUpdateUserPrivacySettingRules(json: TdJsonRaw): UpdateUserPrivacySettingRules | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateUserPrivacySettingRules = new UpdateUserPrivacySettingRules();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.setting = decodeTdUserPrivacySetting(record['setting']);
  out.rules = decodeUserPrivacySettingRules(record['rules']);
  return out;
}

/** Encode a `updateUserPrivacySettingRules` into TDLib JSON wire shape. */
export function encodeUpdateUserPrivacySettingRules(value: UpdateUserPrivacySettingRules): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateUserPrivacySettingRules';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['setting'] = value.setting === null ? null : encodeTdUserPrivacySetting(value.setting);
  out['rules'] = value.rules === null ? null : encodeUserPrivacySettingRules(value.rules);
  return out;
}

/** updateUserStatus = Update — The user went online or offline */
export class UpdateUserStatus {
  type: 'updateUserStatus' = 'updateUserStatus';
  extra?: string;
  /** user_id: int53 — User identifier */
  user_id: number = 0;
  /** status: UserStatus — New status of the user */
  status: TdUserStatus | null = null;
}

/** Decode a TDLib `updateUserStatus` object; null input -> null. */
export function decodeUpdateUserStatus(json: TdJsonRaw): UpdateUserStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateUserStatus = new UpdateUserStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.status = decodeTdUserStatus(record['status']);
  return out;
}

/** Encode a `updateUserStatus` into TDLib JSON wire shape. */
export function encodeUpdateUserStatus(value: UpdateUserStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateUserStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['status'] = value.status === null ? null : encodeTdUserStatus(value.status);
  return out;
}

/** updateUserSubscription = Update — Subscription of a user to the bot was changed; for bots only */
export class UpdateUserSubscription {
  type: 'updateUserSubscription' = 'updateUserSubscription';
  extra?: string;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
  /** payload: string — Bot-specified subscription invoice payload */
  payload: string = '';
  /** is_canceled: Bool — True, if the subscription was canceled */
  is_canceled: boolean = false;
  /** is_restored: Bool — True, if the subscription was restored */
  is_restored: boolean = false;
  /** is_payment_failed: Bool — True, if the payment for the subscription has failed */
  is_payment_failed: boolean = false;
}

/** Decode a TDLib `updateUserSubscription` object; null input -> null. */
export function decodeUpdateUserSubscription(json: TdJsonRaw): UpdateUserSubscription | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateUserSubscription = new UpdateUserSubscription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.payload = asString(record['payload']);
  out.is_canceled = asBool(record['is_canceled']);
  out.is_restored = asBool(record['is_restored']);
  out.is_payment_failed = asBool(record['is_payment_failed']);
  return out;
}

/** Encode a `updateUserSubscription` into TDLib JSON wire shape. */
export function encodeUpdateUserSubscription(value: UpdateUserSubscription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateUserSubscription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['payload'] = value.payload;
  out['is_canceled'] = value.is_canceled;
  out['is_restored'] = value.is_restored;
  out['is_payment_failed'] = value.is_payment_failed;
  return out;
}

/** updateVideoPublished = Update — An automatically scheduled message with video has been successfully sent after conversion */
export class UpdateVideoPublished {
  type: 'updateVideoPublished' = 'updateVideoPublished';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the sent message */
  message_id: number = 0;
}

/** Decode a TDLib `updateVideoPublished` object; null input -> null. */
export function decodeUpdateVideoPublished(json: TdJsonRaw): UpdateVideoPublished | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateVideoPublished = new UpdateVideoPublished();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  return out;
}

/** Encode a `updateVideoPublished` into TDLib JSON wire shape. */
export function encodeUpdateVideoPublished(value: UpdateVideoPublished): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateVideoPublished';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  return out;
}

/** updateWebAppMessageSent = Update — A message was sent by an opened Web App, so the Web App needs to be closed */
export class UpdateWebAppMessageSent {
  type: 'updateWebAppMessageSent' = 'updateWebAppMessageSent';
  extra?: string;
  /** web_app_launch_id: int64 — Identifier of Web App launch */
  web_app_launch_id: string = '';
}

/** Decode a TDLib `updateWebAppMessageSent` object; null input -> null. */
export function decodeUpdateWebAppMessageSent(json: TdJsonRaw): UpdateWebAppMessageSent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateWebAppMessageSent = new UpdateWebAppMessageSent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.web_app_launch_id = asInt64(record['web_app_launch_id']);
  return out;
}

/** Encode a `updateWebAppMessageSent` into TDLib JSON wire shape. */
export function encodeUpdateWebAppMessageSent(value: UpdateWebAppMessageSent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateWebAppMessageSent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['web_app_launch_id'] = value.web_app_launch_id;
  return out;
}

/** updateWebBrowserSettings = Update — Web browser settings have been updated */
export class UpdateWebBrowserSettings {
  type: 'updateWebBrowserSettings' = 'updateWebBrowserSettings';
  extra?: string;
  /** settings: webBrowserSettings — New settings */
  settings: WebBrowserSettings | null = null;
}

/** Decode a TDLib `updateWebBrowserSettings` object; null input -> null. */
export function decodeUpdateWebBrowserSettings(json: TdJsonRaw): UpdateWebBrowserSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpdateWebBrowserSettings = new UpdateWebBrowserSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.settings = decodeWebBrowserSettings(record['settings']);
  return out;
}

/** Encode a `updateWebBrowserSettings` into TDLib JSON wire shape. */
export function encodeUpdateWebBrowserSettings(value: UpdateWebBrowserSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updateWebBrowserSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['settings'] = value.settings === null ? null : encodeWebBrowserSettings(value.settings);
  return out;
}

/** updates = Updates — Contains a list of updates */
export class Updates {
  type: 'updates' = 'updates';
  extra?: string;
  /** updates: vector<Update> — List of updates */
  updates: TdUpdate[] = [];
}

/** Decode a TDLib `updates` object; null input -> null. */
export function decodeUpdates(json: TdJsonRaw): Updates | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Updates = new Updates();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.updates = decodeVector(record['updates'], (item: TdJsonRaw): TdUpdate => decodeTdUpdate(item) as TdUpdate);
  return out;
}

/** Encode a `updates` into TDLib JSON wire shape. */
export function encodeUpdates(value: Updates): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'updates';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['updates'] = encodeVector(value.updates, (item: TdUpdate | null): TdJsonRaw => item === null ? null : encodeTdUpdate(item));
  return out;
}

/** upgradeBasicGroupChatToSupergroupChat = Chat — Creates a new supergroup from an existing basic group and sends a corresponding messageChatUpgradeTo and messageChatUpgradeFrom; requires owner privileges. Deactivates the original basic group */
export class UpgradeBasicGroupChatToSupergroupChat {
  type: 'upgradeBasicGroupChatToSupergroupChat' = 'upgradeBasicGroupChatToSupergroupChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to upgrade */
  chat_id: number = 0;
}

/** Decode a TDLib `upgradeBasicGroupChatToSupergroupChat` object; null input -> null. */
export function decodeUpgradeBasicGroupChatToSupergroupChat(json: TdJsonRaw): UpgradeBasicGroupChatToSupergroupChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradeBasicGroupChatToSupergroupChat = new UpgradeBasicGroupChatToSupergroupChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `upgradeBasicGroupChatToSupergroupChat` into TDLib JSON wire shape. */
export function encodeUpgradeBasicGroupChatToSupergroupChat(value: UpgradeBasicGroupChatToSupergroupChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradeBasicGroupChatToSupergroupChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** upgradeGift = UpgradeGiftResult — Upgrades a regular gift */
export class UpgradeGift {
  type: 'upgradeGift' = 'upgradeGift';
  extra?: string;
  /** business_connection_id: string — Unique identifier of business connection on behalf of which to send the request; for bots only */
  business_connection_id: string = '';
  /** received_gift_id: string — Identifier of the gift */
  received_gift_id: string = '';
  /** keep_original_details: Bool — Pass true to keep the original gift text, sender and receiver in the upgraded gift */
  keep_original_details: boolean = false;
  /** star_count: int53 — The Telegram Star amount required to pay for the upgrade. If the gift has prepaid_upgrade_star_count > 0, then pass 0, otherwise, pass gift.upgrade_star_count */
  star_count: number = 0;
}

/** Decode a TDLib `upgradeGift` object; null input -> null. */
export function decodeUpgradeGift(json: TdJsonRaw): UpgradeGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradeGift = new UpgradeGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.business_connection_id = asString(record['business_connection_id']);
  out.received_gift_id = asString(record['received_gift_id']);
  out.keep_original_details = asBool(record['keep_original_details']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `upgradeGift` into TDLib JSON wire shape. */
export function encodeUpgradeGift(value: UpgradeGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradeGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['business_connection_id'] = value.business_connection_id;
  out['received_gift_id'] = value.received_gift_id;
  out['keep_original_details'] = value.keep_original_details;
  out['star_count'] = value.star_count;
  return out;
}

/** upgradeGiftResult = UpgradeGiftResult — Contains result of gift upgrading */
export class UpgradeGiftResult {
  type: 'upgradeGiftResult' = 'upgradeGiftResult';
  extra?: string;
  /** gift: upgradedGift — The upgraded gift */
  gift: UpgradedGift | null = null;
  /** received_gift_id: string — Unique identifier of the received gift for the current user */
  received_gift_id: string = '';
  /** is_saved: Bool — True, if the gift is displayed on the user's or the channel's profile page */
  is_saved: boolean = false;
  /** can_be_transferred: Bool — True, if the gift can be transferred to another owner */
  can_be_transferred: boolean = false;
  /** transfer_star_count: int53 — Number of Telegram Stars that must be paid to transfer the upgraded gift */
  transfer_star_count: number = 0;
  /** drop_original_details_star_count: int53 — Number of Telegram Stars that must be paid to drop original details of the upgraded gift; 0 if not available */
  drop_original_details_star_count: number = 0;
  /** next_transfer_date: int32 — Point in time (Unix timestamp) when the gift can be transferred to another owner; can be in the past; 0 if the gift can be transferred immediately or transfer isn't possible */
  next_transfer_date: number = 0;
  /** next_resale_date: int32 — Point in time (Unix timestamp) when the gift can be resold to another user; can be in the past; 0 if the gift can't be resold; only for the receiver of the gift */
  next_resale_date: number = 0;
  /** export_date: int32 — Point in time (Unix timestamp) when the gift can be transferred to the TON blockchain as an NFT; can be in the past */
  export_date: number = 0;
}

/** Decode a TDLib `upgradeGiftResult` object; null input -> null. */
export function decodeUpgradeGiftResult(json: TdJsonRaw): UpgradeGiftResult | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradeGiftResult = new UpgradeGiftResult();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeUpgradedGift(record['gift']);
  out.received_gift_id = asString(record['received_gift_id']);
  out.is_saved = asBool(record['is_saved']);
  out.can_be_transferred = asBool(record['can_be_transferred']);
  out.transfer_star_count = asNumber(record['transfer_star_count']);
  out.drop_original_details_star_count = asNumber(record['drop_original_details_star_count']);
  out.next_transfer_date = asNumber(record['next_transfer_date']);
  out.next_resale_date = asNumber(record['next_resale_date']);
  out.export_date = asNumber(record['export_date']);
  return out;
}

/** Encode a `upgradeGiftResult` into TDLib JSON wire shape. */
export function encodeUpgradeGiftResult(value: UpgradeGiftResult): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradeGiftResult';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeUpgradedGift(value.gift);
  out['received_gift_id'] = value.received_gift_id;
  out['is_saved'] = value.is_saved;
  out['can_be_transferred'] = value.can_be_transferred;
  out['transfer_star_count'] = value.transfer_star_count;
  out['drop_original_details_star_count'] = value.drop_original_details_star_count;
  out['next_transfer_date'] = value.next_transfer_date;
  out['next_resale_date'] = value.next_resale_date;
  out['export_date'] = value.export_date;
  return out;
}

/** upgradedGift = UpgradedGift — Describes an upgraded gift that can be transferred to another owner or transferred to the TON blockchain as an NFT */
export class UpgradedGift {
  type: 'upgradedGift' = 'upgradedGift';
  extra?: string;
  /** id: int64 — Unique identifier of the gift */
  id: string = '';
  /** regular_gift_id: int64 — Unique identifier of the regular gift from which the gift was upgraded; may be 0 for short period of time for old gifts from database */
  regular_gift_id: string = '';
  /** publisher_chat_id: int53 — Identifier of the chat that published the gift; 0 if none */
  publisher_chat_id: number = 0;
  /** title: string — The title of the upgraded gift */
  title: string = '';
  /** name: string — Unique name of the upgraded gift that can be used with internalLinkTypeUpgradedGift or sendResoldGift */
  name: string = '';
  /** number: int32 — Unique number of the upgraded gift among gifts upgraded from the same gift */
  number: number = 0;
  /** total_upgraded_count: int32 — Total number of gifts that were upgraded from the same gift */
  total_upgraded_count: number = 0;
  /** max_upgraded_count: int32 — The maximum number of gifts that can be upgraded from the same gift */
  max_upgraded_count: number = 0;
  /** is_burned: Bool — True, if the gift was used to craft another gift */
  is_burned: boolean = false;
  /** is_crafted: Bool — True, if the gift was crafted from other gifts */
  is_crafted: boolean = false;
  /** is_premium: Bool — True, if the original gift could have been bought only by Telegram Premium subscribers */
  is_premium: boolean = false;
  /** is_theme_available: Bool — True, if the gift can be used to set a theme in a chat */
  is_theme_available: boolean = false;
  /** used_theme_chat_id: int53 — Identifier of the chat for which the gift is used to set a theme; 0 if none or the gift isn't owned by the current user */
  used_theme_chat_id: number = 0;
  /** host_id: MessageSender — Identifier of the user or the chat to which the upgraded gift was assigned from blockchain; may be null if none or unknown */
  host_id: TdMessageSender | null = null;
  /** owner_id: MessageSender — Identifier of the user or the chat that owns the upgraded gift; may be null if none or unknown */
  owner_id: TdMessageSender | null = null;
  /** owner_address: string — Address of the gift NFT owner in TON blockchain; may be empty if none. Append the address to getOption("ton_blockchain_explorer_url") to get a link with information about the address */
  owner_address: string = '';
  /** owner_name: string — Name of the owner for the case when owner identifier and address aren't known */
  owner_name: string = '';
  /** gift_address: string — Address of the gift NFT in TON blockchain; may be empty if none. Append the address to getOption("ton_blockchain_explorer_url") to get a link with information about the address */
  gift_address: string = '';
  /** model: upgradedGiftModel — Model of the upgraded gift */
  model: UpgradedGiftModel | null = null;
  /** symbol: upgradedGiftSymbol — Symbol of the upgraded gift */
  symbol: UpgradedGiftSymbol | null = null;
  /** backdrop: upgradedGiftBackdrop — Backdrop of the upgraded gift */
  backdrop: UpgradedGiftBackdrop | null = null;
  /** original_details: upgradedGiftOriginalDetails — Information about the originally sent gift; may be null if unknown */
  original_details: UpgradedGiftOriginalDetails | null = null;
  /** colors: upgradedGiftColors — Colors that can be set for user's name, background of empty chat photo, replies to messages and link previews; may be null if none or unknown */
  colors: UpgradedGiftColors | null = null;
  /** resale_parameters: giftResaleParameters — Resale parameters of the gift; may be null if resale isn't possible */
  resale_parameters: GiftResaleParameters | null = null;
  /** can_send_purchase_offer: Bool — True, if an offer to purchase the gift can be sent using sendGiftPurchaseOffer */
  can_send_purchase_offer: boolean = false;
  /** craft_probability_per_mille: int32 — Probability that the gift adds to the chance of successful crafting of a new gift; 0 if the gift can't be used for crafting */
  craft_probability_per_mille: number = 0;
  /** value_currency: string — ISO 4217 currency code of the currency in which value of the gift is represented; may be empty if unavailable */
  value_currency: string = '';
  /** value_amount: int53 — Estimated value of the gift; in the smallest units of the currency; 0 if unavailable */
  value_amount: number = 0;
  /** value_usd_amount: int53 — Estimated value of the gift in USD; in USD cents; 0 if unavailable */
  value_usd_amount: number = 0;
}

/** Decode a TDLib `upgradedGift` object; null input -> null. */
export function decodeUpgradedGift(json: TdJsonRaw): UpgradedGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGift = new UpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.regular_gift_id = asInt64(record['regular_gift_id']);
  out.publisher_chat_id = asNumber(record['publisher_chat_id']);
  out.title = asString(record['title']);
  out.name = asString(record['name']);
  out.number = asNumber(record['number']);
  out.total_upgraded_count = asNumber(record['total_upgraded_count']);
  out.max_upgraded_count = asNumber(record['max_upgraded_count']);
  out.is_burned = asBool(record['is_burned']);
  out.is_crafted = asBool(record['is_crafted']);
  out.is_premium = asBool(record['is_premium']);
  out.is_theme_available = asBool(record['is_theme_available']);
  out.used_theme_chat_id = asNumber(record['used_theme_chat_id']);
  out.host_id = decodeTdMessageSender(record['host_id']);
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.owner_address = asString(record['owner_address']);
  out.owner_name = asString(record['owner_name']);
  out.gift_address = asString(record['gift_address']);
  out.model = decodeUpgradedGiftModel(record['model']);
  out.symbol = decodeUpgradedGiftSymbol(record['symbol']);
  out.backdrop = decodeUpgradedGiftBackdrop(record['backdrop']);
  out.original_details = decodeUpgradedGiftOriginalDetails(record['original_details']);
  out.colors = decodeUpgradedGiftColors(record['colors']);
  out.resale_parameters = decodeGiftResaleParameters(record['resale_parameters']);
  out.can_send_purchase_offer = asBool(record['can_send_purchase_offer']);
  out.craft_probability_per_mille = asNumber(record['craft_probability_per_mille']);
  out.value_currency = asString(record['value_currency']);
  out.value_amount = asNumber(record['value_amount']);
  out.value_usd_amount = asNumber(record['value_usd_amount']);
  return out;
}

/** Encode a `upgradedGift` into TDLib JSON wire shape. */
export function encodeUpgradedGift(value: UpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['regular_gift_id'] = value.regular_gift_id;
  out['publisher_chat_id'] = value.publisher_chat_id;
  out['title'] = value.title;
  out['name'] = value.name;
  out['number'] = value.number;
  out['total_upgraded_count'] = value.total_upgraded_count;
  out['max_upgraded_count'] = value.max_upgraded_count;
  out['is_burned'] = value.is_burned;
  out['is_crafted'] = value.is_crafted;
  out['is_premium'] = value.is_premium;
  out['is_theme_available'] = value.is_theme_available;
  out['used_theme_chat_id'] = value.used_theme_chat_id;
  out['host_id'] = value.host_id === null ? null : encodeTdMessageSender(value.host_id);
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['owner_address'] = value.owner_address;
  out['owner_name'] = value.owner_name;
  out['gift_address'] = value.gift_address;
  out['model'] = value.model === null ? null : encodeUpgradedGiftModel(value.model);
  out['symbol'] = value.symbol === null ? null : encodeUpgradedGiftSymbol(value.symbol);
  out['backdrop'] = value.backdrop === null ? null : encodeUpgradedGiftBackdrop(value.backdrop);
  out['original_details'] = value.original_details === null ? null : encodeUpgradedGiftOriginalDetails(value.original_details);
  out['colors'] = value.colors === null ? null : encodeUpgradedGiftColors(value.colors);
  out['resale_parameters'] = value.resale_parameters === null ? null : encodeGiftResaleParameters(value.resale_parameters);
  out['can_send_purchase_offer'] = value.can_send_purchase_offer;
  out['craft_probability_per_mille'] = value.craft_probability_per_mille;
  out['value_currency'] = value.value_currency;
  out['value_amount'] = value.value_amount;
  out['value_usd_amount'] = value.value_usd_amount;
  return out;
}

/** upgradedGiftAttributeIdBackdrop = UpgradedGiftAttributeId — Identifier of a gift backdrop */
export class UpgradedGiftAttributeIdBackdrop {
  type: 'upgradedGiftAttributeIdBackdrop' = 'upgradedGiftAttributeIdBackdrop';
  extra?: string;
  /** backdrop_id: int32 — Identifier of the backdrop */
  backdrop_id: number = 0;
}

/** Decode a TDLib `upgradedGiftAttributeIdBackdrop` object; null input -> null. */
export function decodeUpgradedGiftAttributeIdBackdrop(json: TdJsonRaw): UpgradedGiftAttributeIdBackdrop | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftAttributeIdBackdrop = new UpgradedGiftAttributeIdBackdrop();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.backdrop_id = asNumber(record['backdrop_id']);
  return out;
}

/** Encode a `upgradedGiftAttributeIdBackdrop` into TDLib JSON wire shape. */
export function encodeUpgradedGiftAttributeIdBackdrop(value: UpgradedGiftAttributeIdBackdrop): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftAttributeIdBackdrop';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['backdrop_id'] = value.backdrop_id;
  return out;
}

/** upgradedGiftAttributeIdModel = UpgradedGiftAttributeId — Identifier of a gift model */
export class UpgradedGiftAttributeIdModel {
  type: 'upgradedGiftAttributeIdModel' = 'upgradedGiftAttributeIdModel';
  extra?: string;
  /** sticker_id: int64 — Identifier of the sticker representing the model */
  sticker_id: string = '';
}

/** Decode a TDLib `upgradedGiftAttributeIdModel` object; null input -> null. */
export function decodeUpgradedGiftAttributeIdModel(json: TdJsonRaw): UpgradedGiftAttributeIdModel | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftAttributeIdModel = new UpgradedGiftAttributeIdModel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_id = asInt64(record['sticker_id']);
  return out;
}

/** Encode a `upgradedGiftAttributeIdModel` into TDLib JSON wire shape. */
export function encodeUpgradedGiftAttributeIdModel(value: UpgradedGiftAttributeIdModel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftAttributeIdModel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_id'] = value.sticker_id;
  return out;
}

/** upgradedGiftAttributeIdSymbol = UpgradedGiftAttributeId — Identifier of a gift symbol */
export class UpgradedGiftAttributeIdSymbol {
  type: 'upgradedGiftAttributeIdSymbol' = 'upgradedGiftAttributeIdSymbol';
  extra?: string;
  /** sticker_id: int64 — Identifier of the sticker representing the symbol */
  sticker_id: string = '';
}

/** Decode a TDLib `upgradedGiftAttributeIdSymbol` object; null input -> null. */
export function decodeUpgradedGiftAttributeIdSymbol(json: TdJsonRaw): UpgradedGiftAttributeIdSymbol | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftAttributeIdSymbol = new UpgradedGiftAttributeIdSymbol();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker_id = asInt64(record['sticker_id']);
  return out;
}

/** Encode a `upgradedGiftAttributeIdSymbol` into TDLib JSON wire shape. */
export function encodeUpgradedGiftAttributeIdSymbol(value: UpgradedGiftAttributeIdSymbol): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftAttributeIdSymbol';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker_id'] = value.sticker_id;
  return out;
}

/** upgradedGiftAttributeRarityEpic = UpgradedGiftAttributeRarity — The attribute is epic */
export class UpgradedGiftAttributeRarityEpic {
  type: 'upgradedGiftAttributeRarityEpic' = 'upgradedGiftAttributeRarityEpic';
  extra?: string;
}

/** Decode a TDLib `upgradedGiftAttributeRarityEpic` object; null input -> null. */
export function decodeUpgradedGiftAttributeRarityEpic(json: TdJsonRaw): UpgradedGiftAttributeRarityEpic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftAttributeRarityEpic = new UpgradedGiftAttributeRarityEpic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `upgradedGiftAttributeRarityEpic` into TDLib JSON wire shape. */
export function encodeUpgradedGiftAttributeRarityEpic(value: UpgradedGiftAttributeRarityEpic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftAttributeRarityEpic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** upgradedGiftAttributeRarityLegendary = UpgradedGiftAttributeRarity — The attribute is legendary */
export class UpgradedGiftAttributeRarityLegendary {
  type: 'upgradedGiftAttributeRarityLegendary' = 'upgradedGiftAttributeRarityLegendary';
  extra?: string;
}

/** Decode a TDLib `upgradedGiftAttributeRarityLegendary` object; null input -> null. */
export function decodeUpgradedGiftAttributeRarityLegendary(json: TdJsonRaw): UpgradedGiftAttributeRarityLegendary | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftAttributeRarityLegendary = new UpgradedGiftAttributeRarityLegendary();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `upgradedGiftAttributeRarityLegendary` into TDLib JSON wire shape. */
export function encodeUpgradedGiftAttributeRarityLegendary(value: UpgradedGiftAttributeRarityLegendary): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftAttributeRarityLegendary';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** upgradedGiftAttributeRarityPerMille = UpgradedGiftAttributeRarity — The rarity is represented as the numeric frequency of the model */
export class UpgradedGiftAttributeRarityPerMille {
  type: 'upgradedGiftAttributeRarityPerMille' = 'upgradedGiftAttributeRarityPerMille';
  extra?: string;
  /** per_mille: int32 — The number of upgraded gifts that receive this attribute for each 1000 gifts upgraded; if 0, then it can be shown as "<0.1%" */
  per_mille: number = 0;
}

/** Decode a TDLib `upgradedGiftAttributeRarityPerMille` object; null input -> null. */
export function decodeUpgradedGiftAttributeRarityPerMille(json: TdJsonRaw): UpgradedGiftAttributeRarityPerMille | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftAttributeRarityPerMille = new UpgradedGiftAttributeRarityPerMille();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.per_mille = asNumber(record['per_mille']);
  return out;
}

/** Encode a `upgradedGiftAttributeRarityPerMille` into TDLib JSON wire shape. */
export function encodeUpgradedGiftAttributeRarityPerMille(value: UpgradedGiftAttributeRarityPerMille): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftAttributeRarityPerMille';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['per_mille'] = value.per_mille;
  return out;
}

/** upgradedGiftAttributeRarityRare = UpgradedGiftAttributeRarity — The attribute is rare */
export class UpgradedGiftAttributeRarityRare {
  type: 'upgradedGiftAttributeRarityRare' = 'upgradedGiftAttributeRarityRare';
  extra?: string;
}

/** Decode a TDLib `upgradedGiftAttributeRarityRare` object; null input -> null. */
export function decodeUpgradedGiftAttributeRarityRare(json: TdJsonRaw): UpgradedGiftAttributeRarityRare | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftAttributeRarityRare = new UpgradedGiftAttributeRarityRare();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `upgradedGiftAttributeRarityRare` into TDLib JSON wire shape. */
export function encodeUpgradedGiftAttributeRarityRare(value: UpgradedGiftAttributeRarityRare): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftAttributeRarityRare';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** upgradedGiftAttributeRarityUncommon = UpgradedGiftAttributeRarity — The attribute is uncommon */
export class UpgradedGiftAttributeRarityUncommon {
  type: 'upgradedGiftAttributeRarityUncommon' = 'upgradedGiftAttributeRarityUncommon';
  extra?: string;
}

/** Decode a TDLib `upgradedGiftAttributeRarityUncommon` object; null input -> null. */
export function decodeUpgradedGiftAttributeRarityUncommon(json: TdJsonRaw): UpgradedGiftAttributeRarityUncommon | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftAttributeRarityUncommon = new UpgradedGiftAttributeRarityUncommon();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `upgradedGiftAttributeRarityUncommon` into TDLib JSON wire shape. */
export function encodeUpgradedGiftAttributeRarityUncommon(value: UpgradedGiftAttributeRarityUncommon): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftAttributeRarityUncommon';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** upgradedGiftBackdrop = UpgradedGiftBackdrop — Describes a backdrop of an upgraded gift */
export class UpgradedGiftBackdrop {
  type: 'upgradedGiftBackdrop' = 'upgradedGiftBackdrop';
  extra?: string;
  /** id: int32 — Unique identifier of the backdrop */
  id: number = 0;
  /** name: string — Name of the backdrop */
  name: string = '';
  /** colors: upgradedGiftBackdropColors — Colors of the backdrop */
  colors: UpgradedGiftBackdropColors | null = null;
  /** rarity: UpgradedGiftAttributeRarity — The rarity of the backdrop */
  rarity: TdUpgradedGiftAttributeRarity | null = null;
}

/** Decode a TDLib `upgradedGiftBackdrop` object; null input -> null. */
export function decodeUpgradedGiftBackdrop(json: TdJsonRaw): UpgradedGiftBackdrop | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftBackdrop = new UpgradedGiftBackdrop();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.name = asString(record['name']);
  out.colors = decodeUpgradedGiftBackdropColors(record['colors']);
  out.rarity = decodeTdUpgradedGiftAttributeRarity(record['rarity']);
  return out;
}

/** Encode a `upgradedGiftBackdrop` into TDLib JSON wire shape. */
export function encodeUpgradedGiftBackdrop(value: UpgradedGiftBackdrop): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftBackdrop';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['name'] = value.name;
  out['colors'] = value.colors === null ? null : encodeUpgradedGiftBackdropColors(value.colors);
  out['rarity'] = value.rarity === null ? null : encodeTdUpgradedGiftAttributeRarity(value.rarity);
  return out;
}

/** upgradedGiftBackdropColors = UpgradedGiftBackdropColors — Describes colors of a backdrop of an upgraded gift */
export class UpgradedGiftBackdropColors {
  type: 'upgradedGiftBackdropColors' = 'upgradedGiftBackdropColors';
  extra?: string;
  /** center_color: int32 — A color in the center of the backdrop in the RGB format */
  center_color: number = 0;
  /** edge_color: int32 — A color on the edges of the backdrop in the RGB format */
  edge_color: number = 0;
  /** symbol_color: int32 — A color to be applied for the symbol in the RGB format */
  symbol_color: number = 0;
  /** text_color: int32 — A color for the text on the backdrop in the RGB format */
  text_color: number = 0;
}

/** Decode a TDLib `upgradedGiftBackdropColors` object; null input -> null. */
export function decodeUpgradedGiftBackdropColors(json: TdJsonRaw): UpgradedGiftBackdropColors | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftBackdropColors = new UpgradedGiftBackdropColors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.center_color = asNumber(record['center_color']);
  out.edge_color = asNumber(record['edge_color']);
  out.symbol_color = asNumber(record['symbol_color']);
  out.text_color = asNumber(record['text_color']);
  return out;
}

/** Encode a `upgradedGiftBackdropColors` into TDLib JSON wire shape. */
export function encodeUpgradedGiftBackdropColors(value: UpgradedGiftBackdropColors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftBackdropColors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['center_color'] = value.center_color;
  out['edge_color'] = value.edge_color;
  out['symbol_color'] = value.symbol_color;
  out['text_color'] = value.text_color;
  return out;
}

/** upgradedGiftBackdropCount = UpgradedGiftBackdropCount — Describes a backdrop of an upgraded gift */
export class UpgradedGiftBackdropCount {
  type: 'upgradedGiftBackdropCount' = 'upgradedGiftBackdropCount';
  extra?: string;
  /** backdrop: upgradedGiftBackdrop — The backdrop */
  backdrop: UpgradedGiftBackdrop | null = null;
  /** total_count: int32 — Total number of gifts with the symbol */
  total_count: number = 0;
}

/** Decode a TDLib `upgradedGiftBackdropCount` object; null input -> null. */
export function decodeUpgradedGiftBackdropCount(json: TdJsonRaw): UpgradedGiftBackdropCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftBackdropCount = new UpgradedGiftBackdropCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.backdrop = decodeUpgradedGiftBackdrop(record['backdrop']);
  out.total_count = asNumber(record['total_count']);
  return out;
}

/** Encode a `upgradedGiftBackdropCount` into TDLib JSON wire shape. */
export function encodeUpgradedGiftBackdropCount(value: UpgradedGiftBackdropCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftBackdropCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['backdrop'] = value.backdrop === null ? null : encodeUpgradedGiftBackdrop(value.backdrop);
  out['total_count'] = value.total_count;
  return out;
}

/** upgradedGiftColors = UpgradedGiftColors — Contains information about color scheme for user's name, background of empty chat photo, replies to messages and link previews */
export class UpgradedGiftColors {
  type: 'upgradedGiftColors' = 'upgradedGiftColors';
  extra?: string;
  /** id: int64 — Unique identifier of the upgraded gift colors */
  id: string = '';
  /** model_custom_emoji_id: int64 — Custom emoji identifier of the model of the upgraded gift */
  model_custom_emoji_id: string = '';
  /** symbol_custom_emoji_id: int64 — Custom emoji identifier of the symbol of the upgraded gift */
  symbol_custom_emoji_id: string = '';
  /** light_theme_accent_color: int32 — Accent color to use in light themes in RGB format */
  light_theme_accent_color: number = 0;
  /** light_theme_colors: vector<int32> — The list of 1-3 colors in RGB format, describing the accent color, as expected to be shown in light themes */
  light_theme_colors: number[] = [];
  /** dark_theme_accent_color: int32 — Accent color to use in dark themes in RGB format */
  dark_theme_accent_color: number = 0;
  /** dark_theme_colors: vector<int32> — The list of 1-3 colors in RGB format, describing the accent color, as expected to be shown in dark themes */
  dark_theme_colors: number[] = [];
}

/** Decode a TDLib `upgradedGiftColors` object; null input -> null. */
export function decodeUpgradedGiftColors(json: TdJsonRaw): UpgradedGiftColors | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftColors = new UpgradedGiftColors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.model_custom_emoji_id = asInt64(record['model_custom_emoji_id']);
  out.symbol_custom_emoji_id = asInt64(record['symbol_custom_emoji_id']);
  out.light_theme_accent_color = asNumber(record['light_theme_accent_color']);
  out.light_theme_colors = decodeVector(record['light_theme_colors'], (item: TdJsonRaw): number => asNumber(item));
  out.dark_theme_accent_color = asNumber(record['dark_theme_accent_color']);
  out.dark_theme_colors = decodeVector(record['dark_theme_colors'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `upgradedGiftColors` into TDLib JSON wire shape. */
export function encodeUpgradedGiftColors(value: UpgradedGiftColors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftColors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['model_custom_emoji_id'] = value.model_custom_emoji_id;
  out['symbol_custom_emoji_id'] = value.symbol_custom_emoji_id;
  out['light_theme_accent_color'] = value.light_theme_accent_color;
  out['light_theme_colors'] = encodeVector(value.light_theme_colors, (item: number): TdJsonRaw => item);
  out['dark_theme_accent_color'] = value.dark_theme_accent_color;
  out['dark_theme_colors'] = encodeVector(value.dark_theme_colors, (item: number): TdJsonRaw => item);
  return out;
}

/** upgradedGiftModel = UpgradedGiftModel — Describes a model of an upgraded gift */
export class UpgradedGiftModel {
  type: 'upgradedGiftModel' = 'upgradedGiftModel';
  extra?: string;
  /** name: string — Name of the model */
  name: string = '';
  /** sticker: sticker — The sticker representing the upgraded gift */
  sticker: Sticker | null = null;
  /** rarity: UpgradedGiftAttributeRarity — The rarity of the model */
  rarity: TdUpgradedGiftAttributeRarity | null = null;
  /** is_crafted: Bool — True, if the model can be obtained only through gift crafting */
  is_crafted: boolean = false;
}

/** Decode a TDLib `upgradedGiftModel` object; null input -> null. */
export function decodeUpgradedGiftModel(json: TdJsonRaw): UpgradedGiftModel | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftModel = new UpgradedGiftModel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.sticker = decodeSticker(record['sticker']);
  out.rarity = decodeTdUpgradedGiftAttributeRarity(record['rarity']);
  out.is_crafted = asBool(record['is_crafted']);
  return out;
}

/** Encode a `upgradedGiftModel` into TDLib JSON wire shape. */
export function encodeUpgradedGiftModel(value: UpgradedGiftModel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftModel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  out['rarity'] = value.rarity === null ? null : encodeTdUpgradedGiftAttributeRarity(value.rarity);
  out['is_crafted'] = value.is_crafted;
  return out;
}

/** upgradedGiftModelCount = UpgradedGiftModelCount — Describes a model of an upgraded gift with the number of gifts found */
export class UpgradedGiftModelCount {
  type: 'upgradedGiftModelCount' = 'upgradedGiftModelCount';
  extra?: string;
  /** model: upgradedGiftModel — The model */
  model: UpgradedGiftModel | null = null;
  /** total_count: int32 — Total number of gifts with the model */
  total_count: number = 0;
}

/** Decode a TDLib `upgradedGiftModelCount` object; null input -> null. */
export function decodeUpgradedGiftModelCount(json: TdJsonRaw): UpgradedGiftModelCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftModelCount = new UpgradedGiftModelCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.model = decodeUpgradedGiftModel(record['model']);
  out.total_count = asNumber(record['total_count']);
  return out;
}

/** Encode a `upgradedGiftModelCount` into TDLib JSON wire shape. */
export function encodeUpgradedGiftModelCount(value: UpgradedGiftModelCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftModelCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['model'] = value.model === null ? null : encodeUpgradedGiftModel(value.model);
  out['total_count'] = value.total_count;
  return out;
}

/** upgradedGiftOriginBlockchain = UpgradedGiftOrigin — The gift was assigned from blockchain and isn't owned by the current user. The gift can't be transferred, resold or withdrawn to blockchain */
export class UpgradedGiftOriginBlockchain {
  type: 'upgradedGiftOriginBlockchain' = 'upgradedGiftOriginBlockchain';
  extra?: string;
}

/** Decode a TDLib `upgradedGiftOriginBlockchain` object; null input -> null. */
export function decodeUpgradedGiftOriginBlockchain(json: TdJsonRaw): UpgradedGiftOriginBlockchain | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftOriginBlockchain = new UpgradedGiftOriginBlockchain();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `upgradedGiftOriginBlockchain` into TDLib JSON wire shape. */
export function encodeUpgradedGiftOriginBlockchain(value: UpgradedGiftOriginBlockchain): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftOriginBlockchain';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** upgradedGiftOriginCraft = UpgradedGiftOrigin — The gift was crafted from other gifts */
export class UpgradedGiftOriginCraft {
  type: 'upgradedGiftOriginCraft' = 'upgradedGiftOriginCraft';
  extra?: string;
}

/** Decode a TDLib `upgradedGiftOriginCraft` object; null input -> null. */
export function decodeUpgradedGiftOriginCraft(json: TdJsonRaw): UpgradedGiftOriginCraft | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftOriginCraft = new UpgradedGiftOriginCraft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `upgradedGiftOriginCraft` into TDLib JSON wire shape. */
export function encodeUpgradedGiftOriginCraft(value: UpgradedGiftOriginCraft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftOriginCraft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** upgradedGiftOriginOffer = UpgradedGiftOrigin — The gift was bought through an offer */
export class UpgradedGiftOriginOffer {
  type: 'upgradedGiftOriginOffer' = 'upgradedGiftOriginOffer';
  extra?: string;
  /** price: GiftResalePrice — Price paid for the gift */
  price: TdGiftResalePrice | null = null;
}

/** Decode a TDLib `upgradedGiftOriginOffer` object; null input -> null. */
export function decodeUpgradedGiftOriginOffer(json: TdJsonRaw): UpgradedGiftOriginOffer | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftOriginOffer = new UpgradedGiftOriginOffer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.price = decodeTdGiftResalePrice(record['price']);
  return out;
}

/** Encode a `upgradedGiftOriginOffer` into TDLib JSON wire shape. */
export function encodeUpgradedGiftOriginOffer(value: UpgradedGiftOriginOffer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftOriginOffer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['price'] = value.price === null ? null : encodeTdGiftResalePrice(value.price);
  return out;
}

/** upgradedGiftOriginPrepaidUpgrade = UpgradedGiftOrigin — The sender or receiver of the message has paid for upgrade of the gift, which has been completed */
export class UpgradedGiftOriginPrepaidUpgrade {
  type: 'upgradedGiftOriginPrepaidUpgrade' = 'upgradedGiftOriginPrepaidUpgrade';
  extra?: string;
}

/** Decode a TDLib `upgradedGiftOriginPrepaidUpgrade` object; null input -> null. */
export function decodeUpgradedGiftOriginPrepaidUpgrade(json: TdJsonRaw): UpgradedGiftOriginPrepaidUpgrade | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftOriginPrepaidUpgrade = new UpgradedGiftOriginPrepaidUpgrade();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `upgradedGiftOriginPrepaidUpgrade` into TDLib JSON wire shape. */
export function encodeUpgradedGiftOriginPrepaidUpgrade(value: UpgradedGiftOriginPrepaidUpgrade): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftOriginPrepaidUpgrade';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** upgradedGiftOriginResale = UpgradedGiftOrigin — The gift was bought from another user */
export class UpgradedGiftOriginResale {
  type: 'upgradedGiftOriginResale' = 'upgradedGiftOriginResale';
  extra?: string;
  /** price: GiftResalePrice — Price paid for the gift */
  price: TdGiftResalePrice | null = null;
}

/** Decode a TDLib `upgradedGiftOriginResale` object; null input -> null. */
export function decodeUpgradedGiftOriginResale(json: TdJsonRaw): UpgradedGiftOriginResale | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftOriginResale = new UpgradedGiftOriginResale();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.price = decodeTdGiftResalePrice(record['price']);
  return out;
}

/** Encode a `upgradedGiftOriginResale` into TDLib JSON wire shape. */
export function encodeUpgradedGiftOriginResale(value: UpgradedGiftOriginResale): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftOriginResale';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['price'] = value.price === null ? null : encodeTdGiftResalePrice(value.price);
  return out;
}

/** upgradedGiftOriginTransfer = UpgradedGiftOrigin — The gift was transferred from another owner */
export class UpgradedGiftOriginTransfer {
  type: 'upgradedGiftOriginTransfer' = 'upgradedGiftOriginTransfer';
  extra?: string;
}

/** Decode a TDLib `upgradedGiftOriginTransfer` object; null input -> null. */
export function decodeUpgradedGiftOriginTransfer(json: TdJsonRaw): UpgradedGiftOriginTransfer | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftOriginTransfer = new UpgradedGiftOriginTransfer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `upgradedGiftOriginTransfer` into TDLib JSON wire shape. */
export function encodeUpgradedGiftOriginTransfer(value: UpgradedGiftOriginTransfer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftOriginTransfer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** upgradedGiftOriginUpgrade = UpgradedGiftOrigin — The gift was obtained by upgrading of a previously received gift */
export class UpgradedGiftOriginUpgrade {
  type: 'upgradedGiftOriginUpgrade' = 'upgradedGiftOriginUpgrade';
  extra?: string;
  /** gift_message_id: int53 — Identifier of the message with the regular gift that was upgraded; may be 0 or an identifier of a deleted message */
  gift_message_id: number = 0;
}

/** Decode a TDLib `upgradedGiftOriginUpgrade` object; null input -> null. */
export function decodeUpgradedGiftOriginUpgrade(json: TdJsonRaw): UpgradedGiftOriginUpgrade | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftOriginUpgrade = new UpgradedGiftOriginUpgrade();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_message_id = asNumber(record['gift_message_id']);
  return out;
}

/** Encode a `upgradedGiftOriginUpgrade` into TDLib JSON wire shape. */
export function encodeUpgradedGiftOriginUpgrade(value: UpgradedGiftOriginUpgrade): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftOriginUpgrade';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_message_id'] = value.gift_message_id;
  return out;
}

/** upgradedGiftOriginalDetails = UpgradedGiftOriginalDetails — Describes the original details about the gift */
export class UpgradedGiftOriginalDetails {
  type: 'upgradedGiftOriginalDetails' = 'upgradedGiftOriginalDetails';
  extra?: string;
  /** sender_id: MessageSender — Identifier of the user or the chat that sent the gift; may be null if the gift was private */
  sender_id: TdMessageSender | null = null;
  /** receiver_id: MessageSender — Identifier of the user or the chat that received the gift */
  receiver_id: TdMessageSender | null = null;
  /** text: formattedText — Message added to the gift */
  text: FormattedText | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the gift was sent */
  date: number = 0;
}

/** Decode a TDLib `upgradedGiftOriginalDetails` object; null input -> null. */
export function decodeUpgradedGiftOriginalDetails(json: TdJsonRaw): UpgradedGiftOriginalDetails | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftOriginalDetails = new UpgradedGiftOriginalDetails();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.receiver_id = decodeTdMessageSender(record['receiver_id']);
  out.text = decodeFormattedText(record['text']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `upgradedGiftOriginalDetails` into TDLib JSON wire shape. */
export function encodeUpgradedGiftOriginalDetails(value: UpgradedGiftOriginalDetails): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftOriginalDetails';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['receiver_id'] = value.receiver_id === null ? null : encodeTdMessageSender(value.receiver_id);
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['date'] = value.date;
  return out;
}

/** upgradedGiftSymbol = UpgradedGiftSymbol — Describes a symbol shown on the pattern of an upgraded gift */
export class UpgradedGiftSymbol {
  type: 'upgradedGiftSymbol' = 'upgradedGiftSymbol';
  extra?: string;
  /** name: string — Name of the symbol */
  name: string = '';
  /** sticker: sticker — The sticker representing the symbol */
  sticker: Sticker | null = null;
  /** rarity: UpgradedGiftAttributeRarity — The rarity of the symbol */
  rarity: TdUpgradedGiftAttributeRarity | null = null;
}

/** Decode a TDLib `upgradedGiftSymbol` object; null input -> null. */
export function decodeUpgradedGiftSymbol(json: TdJsonRaw): UpgradedGiftSymbol | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftSymbol = new UpgradedGiftSymbol();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.sticker = decodeSticker(record['sticker']);
  out.rarity = decodeTdUpgradedGiftAttributeRarity(record['rarity']);
  return out;
}

/** Encode a `upgradedGiftSymbol` into TDLib JSON wire shape. */
export function encodeUpgradedGiftSymbol(value: UpgradedGiftSymbol): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftSymbol';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  out['rarity'] = value.rarity === null ? null : encodeTdUpgradedGiftAttributeRarity(value.rarity);
  return out;
}

/** upgradedGiftSymbolCount = UpgradedGiftSymbolCount — Describes a symbol shown on the pattern of an upgraded gift */
export class UpgradedGiftSymbolCount {
  type: 'upgradedGiftSymbolCount' = 'upgradedGiftSymbolCount';
  extra?: string;
  /** symbol: upgradedGiftSymbol — The symbol */
  symbol: UpgradedGiftSymbol | null = null;
  /** total_count: int32 — Total number of gifts with the symbol */
  total_count: number = 0;
}

/** Decode a TDLib `upgradedGiftSymbolCount` object; null input -> null. */
export function decodeUpgradedGiftSymbolCount(json: TdJsonRaw): UpgradedGiftSymbolCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftSymbolCount = new UpgradedGiftSymbolCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.symbol = decodeUpgradedGiftSymbol(record['symbol']);
  out.total_count = asNumber(record['total_count']);
  return out;
}

/** Encode a `upgradedGiftSymbolCount` into TDLib JSON wire shape. */
export function encodeUpgradedGiftSymbolCount(value: UpgradedGiftSymbolCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftSymbolCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['symbol'] = value.symbol === null ? null : encodeUpgradedGiftSymbol(value.symbol);
  out['total_count'] = value.total_count;
  return out;
}

/** upgradedGiftValueInfo = UpgradedGiftValueInfo — Contains information about value of an upgraded gift */
export class UpgradedGiftValueInfo {
  type: 'upgradedGiftValueInfo' = 'upgradedGiftValueInfo';
  extra?: string;
  /** currency: string — ISO 4217 currency code of the currency in which the prices are represented */
  currency: string = '';
  /** value: int53 — Estimated value of the gift; in the smallest units of the currency */
  value: number = 0;
  /** is_value_average: Bool — True, if the value is calculated as average value of similar sold gifts. Otherwise, it is based on the sale price of the gift */
  is_value_average: boolean = false;
  /** initial_sale_date: int32 — Point in time (Unix timestamp) when the corresponding regular gift was originally purchased */
  initial_sale_date: number = 0;
  /** initial_sale_star_count: int53 — The Telegram Star amount that was paid for the gift */
  initial_sale_star_count: number = 0;
  /** initial_sale_price: int53 — Initial price of the gift; in the smallest units of the currency */
  initial_sale_price: number = 0;
  /** last_sale_date: int32 — Point in time (Unix timestamp) when the upgraded gift was purchased last time; 0 if never */
  last_sale_date: number = 0;
  /** last_sale_price: int53 — Last purchase price of the gift; in the smallest units of the currency; 0 if the gift has never been resold */
  last_sale_price: number = 0;
  /** is_last_sale_on_fragment: Bool — True, if the last sale was completed on Fragment */
  is_last_sale_on_fragment: boolean = false;
  /** minimum_price: int53 — The current minimum price of gifts upgraded from the same gift; in the smallest units of the currency; 0 if there are no such gifts */
  minimum_price: number = 0;
  /** average_sale_price: int53 — The average sale price in the last month of gifts upgraded from the same gift; in the smallest units of the currency; 0 if there were no such sales */
  average_sale_price: number = 0;
  /** telegram_listed_gift_count: int32 — Number of gifts upgraded from the same gift being resold on Telegram */
  telegram_listed_gift_count: number = 0;
  /** fragment_listed_gift_count: int32 — Number of gifts upgraded from the same gift being resold on Fragment */
  fragment_listed_gift_count: number = 0;
  /** fragment_url: string — The HTTPS link to the Fragment for the gift; may be empty if there are no such gifts being sold on Fragment */
  fragment_url: string = '';
}

/** Decode a TDLib `upgradedGiftValueInfo` object; null input -> null. */
export function decodeUpgradedGiftValueInfo(json: TdJsonRaw): UpgradedGiftValueInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UpgradedGiftValueInfo = new UpgradedGiftValueInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.value = asNumber(record['value']);
  out.is_value_average = asBool(record['is_value_average']);
  out.initial_sale_date = asNumber(record['initial_sale_date']);
  out.initial_sale_star_count = asNumber(record['initial_sale_star_count']);
  out.initial_sale_price = asNumber(record['initial_sale_price']);
  out.last_sale_date = asNumber(record['last_sale_date']);
  out.last_sale_price = asNumber(record['last_sale_price']);
  out.is_last_sale_on_fragment = asBool(record['is_last_sale_on_fragment']);
  out.minimum_price = asNumber(record['minimum_price']);
  out.average_sale_price = asNumber(record['average_sale_price']);
  out.telegram_listed_gift_count = asNumber(record['telegram_listed_gift_count']);
  out.fragment_listed_gift_count = asNumber(record['fragment_listed_gift_count']);
  out.fragment_url = asString(record['fragment_url']);
  return out;
}

/** Encode a `upgradedGiftValueInfo` into TDLib JSON wire shape. */
export function encodeUpgradedGiftValueInfo(value: UpgradedGiftValueInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'upgradedGiftValueInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['value'] = value.value;
  out['is_value_average'] = value.is_value_average;
  out['initial_sale_date'] = value.initial_sale_date;
  out['initial_sale_star_count'] = value.initial_sale_star_count;
  out['initial_sale_price'] = value.initial_sale_price;
  out['last_sale_date'] = value.last_sale_date;
  out['last_sale_price'] = value.last_sale_price;
  out['is_last_sale_on_fragment'] = value.is_last_sale_on_fragment;
  out['minimum_price'] = value.minimum_price;
  out['average_sale_price'] = value.average_sale_price;
  out['telegram_listed_gift_count'] = value.telegram_listed_gift_count;
  out['fragment_listed_gift_count'] = value.fragment_listed_gift_count;
  out['fragment_url'] = value.fragment_url;
  return out;
}

/** uploadStickerFile = File — Uploads a file with a sticker; returns the uploaded file See https://core.telegram.org/animated_stickers#technical-requirements for technical requirements */
export class UploadStickerFile {
  type: 'uploadStickerFile' = 'uploadStickerFile';
  extra?: string;
  /** user_id: int53 — Sticker file owner; ignored for regular users */
  user_id: number = 0;
  /** sticker_format: StickerFormat — Sticker format */
  sticker_format: TdStickerFormat | null = null;
  /** sticker: InputFile — File to upload; must fit in a 512x512 square. For WEBP stickers the file must be in WEBP or PNG format, which will be converted to WEBP server-side. */
  sticker: TdInputFile | null = null;
}

/** Decode a TDLib `uploadStickerFile` object; null input -> null. */
export function decodeUploadStickerFile(json: TdJsonRaw): UploadStickerFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UploadStickerFile = new UploadStickerFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.sticker_format = decodeTdStickerFormat(record['sticker_format']);
  out.sticker = decodeTdInputFile(record['sticker']);
  return out;
}

/** Encode a `uploadStickerFile` into TDLib JSON wire shape. */
export function encodeUploadStickerFile(value: UploadStickerFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'uploadStickerFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['sticker_format'] = value.sticker_format === null ? null : encodeTdStickerFormat(value.sticker_format);
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  return out;
}

/** user = User — Represents a user */
export class User {
  type: 'user' = 'user';
  extra?: string;
  /** id: int53 — User identifier */
  id: number = 0;
  /** first_name: string — First name of the user */
  first_name: string = '';
  /** last_name: string — Last name of the user */
  last_name: string = '';
  /** usernames: usernames — Usernames of the user; may be null */
  usernames: Usernames | null = null;
  /** phone_number: string — Phone number of the user */
  phone_number: string = '';
  /** status: UserStatus — Current online status of the user */
  status: TdUserStatus | null = null;
  /** profile_photo: profilePhoto — Profile photo of the user; may be null */
  profile_photo: ProfilePhoto | null = null;
  /** accent_color_id: int32 — Identifier of the accent color for name, and backgrounds of profile photo, reply header, and link preview */
  accent_color_id: number = 0;
  /** background_custom_emoji_id: int64 — Identifier of a custom emoji to be shown on the reply header and link preview background; 0 if none */
  background_custom_emoji_id: string = '';
  /** upgraded_gift_colors: upgradedGiftColors — Color scheme based on an upgraded gift to be used for the user instead of accent_color_id and background_custom_emoji_id; may be null if none */
  upgraded_gift_colors: UpgradedGiftColors | null = null;
  /** profile_accent_color_id: int32 — Identifier of the accent color for the user's profile; -1 if none */
  profile_accent_color_id: number = 0;
  /** profile_background_custom_emoji_id: int64 — Identifier of a custom emoji to be shown on the background of the user's profile; 0 if none */
  profile_background_custom_emoji_id: string = '';
  /** emoji_status: emojiStatus — Emoji status to be shown instead of the default Telegram Premium badge; may be null */
  emoji_status: EmojiStatus | null = null;
  /** is_contact: Bool — The user is a contact of the current user */
  is_contact: boolean = false;
  /** is_mutual_contact: Bool — The user is a contact of the current user and the current user is a contact of the user */
  is_mutual_contact: boolean = false;
  /** is_close_friend: Bool — The user is a close friend of the current user; implies that the user is a contact */
  is_close_friend: boolean = false;
  /** verification_status: verificationStatus — Information about verification status of the user; may be null if none */
  verification_status: VerificationStatus | null = null;
  /** is_premium: Bool — True, if the user is a Telegram Premium user */
  is_premium: boolean = false;
  /** is_support: Bool — True, if the user is Telegram support account */
  is_support: boolean = false;
  /** restriction_info: restrictionInfo — Information about restrictions that must be applied to the corresponding private chat; may be null if none */
  restriction_info: RestrictionInfo | null = null;
  /** active_story_state: ActiveStoryState — State of active stories of the user; may be null if the user has no active stories */
  active_story_state: TdActiveStoryState | null = null;
  /** restricts_new_chats: Bool — True, if the user may restrict new chats with non-contacts. Use canSendMessageToUser to check whether the current user can message the user or try to create a chat with them */
  restricts_new_chats: boolean = false;
  /** paid_message_star_count: int53 — Number of Telegram Stars that must be paid by general user for each sent message to the user. If positive and userFullInfo is unknown, use canSendMessageToUser to check whether the current user must pay */
  paid_message_star_count: number = 0;
  /** have_access: Bool — If false, the user is inaccessible, and the only information known about the user is inside this class. Identifier of the user can't be passed to any method */
  have_access: boolean = false;
  /** type: UserType — Type of the user */
  type_: TdUserType | null = null;
  /** language_code: string — IETF language tag of the user's language; only available to bots */
  language_code: string = '';
  /** added_to_attachment_menu: Bool — True, if the user added the current bot to attachment menu; only available to bots */
  added_to_attachment_menu: boolean = false;
}

/** Decode a TDLib `user` object; null input -> null. */
export function decodeUser(json: TdJsonRaw): User | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: User = new User();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.first_name = asString(record['first_name']);
  out.last_name = asString(record['last_name']);
  out.usernames = decodeUsernames(record['usernames']);
  out.phone_number = asString(record['phone_number']);
  out.status = decodeTdUserStatus(record['status']);
  out.profile_photo = decodeProfilePhoto(record['profile_photo']);
  out.accent_color_id = asNumber(record['accent_color_id']);
  out.background_custom_emoji_id = asInt64(record['background_custom_emoji_id']);
  out.upgraded_gift_colors = decodeUpgradedGiftColors(record['upgraded_gift_colors']);
  out.profile_accent_color_id = asNumber(record['profile_accent_color_id']);
  out.profile_background_custom_emoji_id = asInt64(record['profile_background_custom_emoji_id']);
  out.emoji_status = decodeEmojiStatus(record['emoji_status']);
  out.is_contact = asBool(record['is_contact']);
  out.is_mutual_contact = asBool(record['is_mutual_contact']);
  out.is_close_friend = asBool(record['is_close_friend']);
  out.verification_status = decodeVerificationStatus(record['verification_status']);
  out.is_premium = asBool(record['is_premium']);
  out.is_support = asBool(record['is_support']);
  out.restriction_info = decodeRestrictionInfo(record['restriction_info']);
  out.active_story_state = decodeTdActiveStoryState(record['active_story_state']);
  out.restricts_new_chats = asBool(record['restricts_new_chats']);
  out.paid_message_star_count = asNumber(record['paid_message_star_count']);
  out.have_access = asBool(record['have_access']);
  out.type_ = decodeTdUserType(record['type']);
  out.language_code = asString(record['language_code']);
  out.added_to_attachment_menu = asBool(record['added_to_attachment_menu']);
  return out;
}

/** Encode a `user` into TDLib JSON wire shape. */
export function encodeUser(value: User): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'user';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['first_name'] = value.first_name;
  out['last_name'] = value.last_name;
  out['usernames'] = value.usernames === null ? null : encodeUsernames(value.usernames);
  out['phone_number'] = value.phone_number;
  out['status'] = value.status === null ? null : encodeTdUserStatus(value.status);
  out['profile_photo'] = value.profile_photo === null ? null : encodeProfilePhoto(value.profile_photo);
  out['accent_color_id'] = value.accent_color_id;
  out['background_custom_emoji_id'] = value.background_custom_emoji_id;
  out['upgraded_gift_colors'] = value.upgraded_gift_colors === null ? null : encodeUpgradedGiftColors(value.upgraded_gift_colors);
  out['profile_accent_color_id'] = value.profile_accent_color_id;
  out['profile_background_custom_emoji_id'] = value.profile_background_custom_emoji_id;
  out['emoji_status'] = value.emoji_status === null ? null : encodeEmojiStatus(value.emoji_status);
  out['is_contact'] = value.is_contact;
  out['is_mutual_contact'] = value.is_mutual_contact;
  out['is_close_friend'] = value.is_close_friend;
  out['verification_status'] = value.verification_status === null ? null : encodeVerificationStatus(value.verification_status);
  out['is_premium'] = value.is_premium;
  out['is_support'] = value.is_support;
  out['restriction_info'] = value.restriction_info === null ? null : encodeRestrictionInfo(value.restriction_info);
  out['active_story_state'] = value.active_story_state === null ? null : encodeTdActiveStoryState(value.active_story_state);
  out['restricts_new_chats'] = value.restricts_new_chats;
  out['paid_message_star_count'] = value.paid_message_star_count;
  out['have_access'] = value.have_access;
  out['type'] = value.type_ === null ? null : encodeTdUserType(value.type_);
  out['language_code'] = value.language_code;
  out['added_to_attachment_menu'] = value.added_to_attachment_menu;
  return out;
}

/** userAuctionBid = UserAuctionBid — Describes a bid of the current user in an auction */
export class UserAuctionBid {
  type: 'userAuctionBid' = 'userAuctionBid';
  extra?: string;
  /** star_count: int53 — The number of Telegram Stars that were put in the bid */
  star_count: number = 0;
  /** bid_date: int32 — Point in time (Unix timestamp) when the bid was made */
  bid_date: number = 0;
  /** next_bid_star_count: int53 — The minimum number of Telegram Stars that can be put for the next bid */
  next_bid_star_count: number = 0;
  /** owner_id: MessageSender — Identifier of the user or the chat that will receive the auctioned item. If the auction is opened in context of another user or chat, then a warning is supposed to be shown to the current user */
  owner_id: TdMessageSender | null = null;
  /** was_returned: Bool — True, if the bid was returned to the user, because it was outbid and can't win anymore */
  was_returned: boolean = false;
}

/** Decode a TDLib `userAuctionBid` object; null input -> null. */
export function decodeUserAuctionBid(json: TdJsonRaw): UserAuctionBid | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserAuctionBid = new UserAuctionBid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.bid_date = asNumber(record['bid_date']);
  out.next_bid_star_count = asNumber(record['next_bid_star_count']);
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.was_returned = asBool(record['was_returned']);
  return out;
}

/** Encode a `userAuctionBid` into TDLib JSON wire shape. */
export function encodeUserAuctionBid(value: UserAuctionBid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userAuctionBid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['bid_date'] = value.bid_date;
  out['next_bid_star_count'] = value.next_bid_star_count;
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['was_returned'] = value.was_returned;
  return out;
}

/** userFullInfo = UserFullInfo — Contains full information about a user If non-null, then it is the same photo as in user.profile_photo and chat.photo. This photo isn't returned in the list of user photos If non-null and personal_photo is n... */
export class UserFullInfo {
  type: 'userFullInfo' = 'userFullInfo';
  extra?: string;
  /** personal_photo: chatPhoto — User profile photo set by the current user for the contact; may be null. If null and user.profile_photo is null, then the photo is empty; otherwise, it is unknown. */
  personal_photo: ChatPhoto | null = null;
  /** photo: chatPhoto — User profile photo; may be null. If null and user.profile_photo is null, then the photo is empty; otherwise, it is unknown. */
  photo: ChatPhoto | null = null;
  /** public_photo: chatPhoto — User profile photo visible if the main photo is hidden by privacy settings; may be null. If null and user.profile_photo is null, then the photo is empty; otherwise, it is unknown. */
  public_photo: ChatPhoto | null = null;
  /** community_id: int53 — Identifier of the community to which chat with the bot was added; for bots only */
  community_id: number = 0;
  /** block_list: BlockList — Block list to which the user is added; may be null if none */
  block_list: TdBlockList | null = null;
  /** can_be_called: Bool — True, if the user can be called */
  can_be_called: boolean = false;
  /** supports_video_calls: Bool — True, if a video call can be created with the user */
  supports_video_calls: boolean = false;
  /** has_private_calls: Bool — True, if the user can't be called due to their privacy settings */
  has_private_calls: boolean = false;
  /** has_private_forwards: Bool — True, if the user can't be linked in forwarded messages due to their privacy settings */
  has_private_forwards: boolean = false;
  /** has_restricted_voice_and_video_note_messages: Bool — True, if voice and video notes can't be sent or forwarded to the user */
  has_restricted_voice_and_video_note_messages: boolean = false;
  /** has_posted_to_profile_stories: Bool — True, if the user has posted to profile stories */
  has_posted_to_profile_stories: boolean = false;
  /** has_sponsored_messages_enabled: Bool — True, if the user always enabled sponsored messages; known only for the current user */
  has_sponsored_messages_enabled: boolean = false;
  /** need_phone_number_privacy_exception: Bool — True, if the current user needs to explicitly allow to share their phone number with the user when the method addContact is used */
  need_phone_number_privacy_exception: boolean = false;
  /** set_chat_background: Bool — True, if the user set chat background for both chat users and it wasn't reverted yet */
  set_chat_background: boolean = false;
  /** uses_unofficial_app: Bool — True, if the user uses an unofficial application that poses a security risk */
  uses_unofficial_app: boolean = false;
  /** bio: formattedText — A short user bio; may be null for bots */
  bio: FormattedText | null = null;
  /** birthdate: birthdate — Birthdate of the user; may be null if unknown */
  birthdate: Birthdate | null = null;
  /** personal_chat_id: int53 — Identifier of the personal chat of the user; 0 if none */
  personal_chat_id: number = 0;
  /** gift_count: int32 — Number of saved to profile gifts for other users or the total number of received gifts for the current user */
  gift_count: number = 0;
  /** group_in_common_count: int32 — Number of group chats where both the other user and the current user are a member; 0 for the current user */
  group_in_common_count: number = 0;
  /** incoming_paid_message_star_count: int53 — Number of Telegram Stars that must be paid by the user for each sent message to the current user */
  incoming_paid_message_star_count: number = 0;
  /** outgoing_paid_message_star_count: int53 — Number of Telegram Stars that must be paid by the current user for each sent message to the user */
  outgoing_paid_message_star_count: number = 0;
  /** gift_settings: giftSettings — Settings for gift receiving for the user */
  gift_settings: GiftSettings | null = null;
  /** bot_verification: botVerification — Information about verification status of the user provided by a bot; may be null if none or unknown */
  bot_verification: BotVerification | null = null;
  /** main_profile_tab: ProfileTab — The main tab chosen by the user; may be null if not chosen manually */
  main_profile_tab: TdProfileTab | null = null;
  /** first_profile_audio: audio — The first audio file added to the user's profile; may be null if none */
  first_profile_audio: Audio | null = null;
  /** rating: userRating — The current rating of the user; may be null if none */
  rating: UserRating | null = null;
  /** pending_rating: userRating — The rating of the user after the next change; may be null if the user isn't the current user or there are no pending rating changes */
  pending_rating: UserRating | null = null;
  /** pending_rating_date: int32 — Unix timestamp when rating of the user will change to pending_rating; 0 if the user isn't the current user or there are no pending rating changes */
  pending_rating_date: number = 0;
  /** note: formattedText — Note added to the user's contact; may be null if none */
  note: FormattedText | null = null;
  /** business_info: businessInfo — Information about business settings for Telegram Business accounts; may be null if none */
  business_info: BusinessInfo | null = null;
  /** bot_info: botInfo — For bots, information about the bot; may be null if the user isn't a bot */
  bot_info: BotInfo | null = null;
}

/** Decode a TDLib `userFullInfo` object; null input -> null. */
export function decodeUserFullInfo(json: TdJsonRaw): UserFullInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserFullInfo = new UserFullInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.personal_photo = decodeChatPhoto(record['personal_photo']);
  out.photo = decodeChatPhoto(record['photo']);
  out.public_photo = decodeChatPhoto(record['public_photo']);
  out.community_id = asNumber(record['community_id']);
  out.block_list = decodeTdBlockList(record['block_list']);
  out.can_be_called = asBool(record['can_be_called']);
  out.supports_video_calls = asBool(record['supports_video_calls']);
  out.has_private_calls = asBool(record['has_private_calls']);
  out.has_private_forwards = asBool(record['has_private_forwards']);
  out.has_restricted_voice_and_video_note_messages = asBool(record['has_restricted_voice_and_video_note_messages']);
  out.has_posted_to_profile_stories = asBool(record['has_posted_to_profile_stories']);
  out.has_sponsored_messages_enabled = asBool(record['has_sponsored_messages_enabled']);
  out.need_phone_number_privacy_exception = asBool(record['need_phone_number_privacy_exception']);
  out.set_chat_background = asBool(record['set_chat_background']);
  out.uses_unofficial_app = asBool(record['uses_unofficial_app']);
  out.bio = decodeFormattedText(record['bio']);
  out.birthdate = decodeBirthdate(record['birthdate']);
  out.personal_chat_id = asNumber(record['personal_chat_id']);
  out.gift_count = asNumber(record['gift_count']);
  out.group_in_common_count = asNumber(record['group_in_common_count']);
  out.incoming_paid_message_star_count = asNumber(record['incoming_paid_message_star_count']);
  out.outgoing_paid_message_star_count = asNumber(record['outgoing_paid_message_star_count']);
  out.gift_settings = decodeGiftSettings(record['gift_settings']);
  out.bot_verification = decodeBotVerification(record['bot_verification']);
  out.main_profile_tab = decodeTdProfileTab(record['main_profile_tab']);
  out.first_profile_audio = decodeAudio(record['first_profile_audio']);
  out.rating = decodeUserRating(record['rating']);
  out.pending_rating = decodeUserRating(record['pending_rating']);
  out.pending_rating_date = asNumber(record['pending_rating_date']);
  out.note = decodeFormattedText(record['note']);
  out.business_info = decodeBusinessInfo(record['business_info']);
  out.bot_info = decodeBotInfo(record['bot_info']);
  return out;
}

/** Encode a `userFullInfo` into TDLib JSON wire shape. */
export function encodeUserFullInfo(value: UserFullInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userFullInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['personal_photo'] = value.personal_photo === null ? null : encodeChatPhoto(value.personal_photo);
  out['photo'] = value.photo === null ? null : encodeChatPhoto(value.photo);
  out['public_photo'] = value.public_photo === null ? null : encodeChatPhoto(value.public_photo);
  out['community_id'] = value.community_id;
  out['block_list'] = value.block_list === null ? null : encodeTdBlockList(value.block_list);
  out['can_be_called'] = value.can_be_called;
  out['supports_video_calls'] = value.supports_video_calls;
  out['has_private_calls'] = value.has_private_calls;
  out['has_private_forwards'] = value.has_private_forwards;
  out['has_restricted_voice_and_video_note_messages'] = value.has_restricted_voice_and_video_note_messages;
  out['has_posted_to_profile_stories'] = value.has_posted_to_profile_stories;
  out['has_sponsored_messages_enabled'] = value.has_sponsored_messages_enabled;
  out['need_phone_number_privacy_exception'] = value.need_phone_number_privacy_exception;
  out['set_chat_background'] = value.set_chat_background;
  out['uses_unofficial_app'] = value.uses_unofficial_app;
  out['bio'] = value.bio === null ? null : encodeFormattedText(value.bio);
  out['birthdate'] = value.birthdate === null ? null : encodeBirthdate(value.birthdate);
  out['personal_chat_id'] = value.personal_chat_id;
  out['gift_count'] = value.gift_count;
  out['group_in_common_count'] = value.group_in_common_count;
  out['incoming_paid_message_star_count'] = value.incoming_paid_message_star_count;
  out['outgoing_paid_message_star_count'] = value.outgoing_paid_message_star_count;
  out['gift_settings'] = value.gift_settings === null ? null : encodeGiftSettings(value.gift_settings);
  out['bot_verification'] = value.bot_verification === null ? null : encodeBotVerification(value.bot_verification);
  out['main_profile_tab'] = value.main_profile_tab === null ? null : encodeTdProfileTab(value.main_profile_tab);
  out['first_profile_audio'] = value.first_profile_audio === null ? null : encodeAudio(value.first_profile_audio);
  out['rating'] = value.rating === null ? null : encodeUserRating(value.rating);
  out['pending_rating'] = value.pending_rating === null ? null : encodeUserRating(value.pending_rating);
  out['pending_rating_date'] = value.pending_rating_date;
  out['note'] = value.note === null ? null : encodeFormattedText(value.note);
  out['business_info'] = value.business_info === null ? null : encodeBusinessInfo(value.business_info);
  out['bot_info'] = value.bot_info === null ? null : encodeBotInfo(value.bot_info);
  return out;
}

/** userLink = UserLink — Contains an HTTPS URL, which can be used to get information about a user */
export class UserLink {
  type: 'userLink' = 'userLink';
  extra?: string;
  /** url: string — The URL */
  url: string = '';
  /** expires_in: int32 — Left time for which the link is valid, in seconds; 0 if the link is a public username link */
  expires_in: number = 0;
}

/** Decode a TDLib `userLink` object; null input -> null. */
export function decodeUserLink(json: TdJsonRaw): UserLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserLink = new UserLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.expires_in = asNumber(record['expires_in']);
  return out;
}

/** Encode a `userLink` into TDLib JSON wire shape. */
export function encodeUserLink(value: UserLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['expires_in'] = value.expires_in;
  return out;
}

/** userPrivacySettingAllowCalls = UserPrivacySetting — A privacy setting for managing whether the user can be called */
export class UserPrivacySettingAllowCalls {
  type: 'userPrivacySettingAllowCalls' = 'userPrivacySettingAllowCalls';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingAllowCalls` object; null input -> null. */
export function decodeUserPrivacySettingAllowCalls(json: TdJsonRaw): UserPrivacySettingAllowCalls | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingAllowCalls = new UserPrivacySettingAllowCalls();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingAllowCalls` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingAllowCalls(value: UserPrivacySettingAllowCalls): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingAllowCalls';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingAllowChatInvites = UserPrivacySetting — A privacy setting for managing whether the user can be invited to chats */
export class UserPrivacySettingAllowChatInvites {
  type: 'userPrivacySettingAllowChatInvites' = 'userPrivacySettingAllowChatInvites';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingAllowChatInvites` object; null input -> null. */
export function decodeUserPrivacySettingAllowChatInvites(json: TdJsonRaw): UserPrivacySettingAllowChatInvites | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingAllowChatInvites = new UserPrivacySettingAllowChatInvites();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingAllowChatInvites` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingAllowChatInvites(value: UserPrivacySettingAllowChatInvites): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingAllowChatInvites';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingAllowFindingByPhoneNumber = UserPrivacySetting — A privacy setting for managing whether the user can be found by their phone number. Checked only if the phone number is not known to the other user. Can be set only to "A... */
export class UserPrivacySettingAllowFindingByPhoneNumber {
  type: 'userPrivacySettingAllowFindingByPhoneNumber' = 'userPrivacySettingAllowFindingByPhoneNumber';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingAllowFindingByPhoneNumber` object; null input -> null. */
export function decodeUserPrivacySettingAllowFindingByPhoneNumber(json: TdJsonRaw): UserPrivacySettingAllowFindingByPhoneNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingAllowFindingByPhoneNumber = new UserPrivacySettingAllowFindingByPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingAllowFindingByPhoneNumber` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingAllowFindingByPhoneNumber(value: UserPrivacySettingAllowFindingByPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingAllowFindingByPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingAllowPeerToPeerCalls = UserPrivacySetting — A privacy setting for managing whether peer-to-peer connections can be used for calls */
export class UserPrivacySettingAllowPeerToPeerCalls {
  type: 'userPrivacySettingAllowPeerToPeerCalls' = 'userPrivacySettingAllowPeerToPeerCalls';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingAllowPeerToPeerCalls` object; null input -> null. */
export function decodeUserPrivacySettingAllowPeerToPeerCalls(json: TdJsonRaw): UserPrivacySettingAllowPeerToPeerCalls | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingAllowPeerToPeerCalls = new UserPrivacySettingAllowPeerToPeerCalls();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingAllowPeerToPeerCalls` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingAllowPeerToPeerCalls(value: UserPrivacySettingAllowPeerToPeerCalls): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingAllowPeerToPeerCalls';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingAllowPrivateVoiceAndVideoNoteMessages = UserPrivacySetting — A privacy setting for managing whether the user can receive voice and video messages in private chats; for Telegram Premium users only */
export class UserPrivacySettingAllowPrivateVoiceAndVideoNoteMessages {
  type: 'userPrivacySettingAllowPrivateVoiceAndVideoNoteMessages' = 'userPrivacySettingAllowPrivateVoiceAndVideoNoteMessages';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingAllowPrivateVoiceAndVideoNoteMessages` object; null input -> null. */
export function decodeUserPrivacySettingAllowPrivateVoiceAndVideoNoteMessages(json: TdJsonRaw): UserPrivacySettingAllowPrivateVoiceAndVideoNoteMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingAllowPrivateVoiceAndVideoNoteMessages = new UserPrivacySettingAllowPrivateVoiceAndVideoNoteMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingAllowPrivateVoiceAndVideoNoteMessages` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingAllowPrivateVoiceAndVideoNoteMessages(value: UserPrivacySettingAllowPrivateVoiceAndVideoNoteMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingAllowPrivateVoiceAndVideoNoteMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingAllowUnpaidMessages = UserPrivacySetting — A privacy setting for managing whether the user can receive messages without additional payment */
export class UserPrivacySettingAllowUnpaidMessages {
  type: 'userPrivacySettingAllowUnpaidMessages' = 'userPrivacySettingAllowUnpaidMessages';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingAllowUnpaidMessages` object; null input -> null. */
export function decodeUserPrivacySettingAllowUnpaidMessages(json: TdJsonRaw): UserPrivacySettingAllowUnpaidMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingAllowUnpaidMessages = new UserPrivacySettingAllowUnpaidMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingAllowUnpaidMessages` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingAllowUnpaidMessages(value: UserPrivacySettingAllowUnpaidMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingAllowUnpaidMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingAutosaveGifts = UserPrivacySetting — A privacy setting for managing whether received gifts are automatically shown on the user's profile page */
export class UserPrivacySettingAutosaveGifts {
  type: 'userPrivacySettingAutosaveGifts' = 'userPrivacySettingAutosaveGifts';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingAutosaveGifts` object; null input -> null. */
export function decodeUserPrivacySettingAutosaveGifts(json: TdJsonRaw): UserPrivacySettingAutosaveGifts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingAutosaveGifts = new UserPrivacySettingAutosaveGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingAutosaveGifts` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingAutosaveGifts(value: UserPrivacySettingAutosaveGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingAutosaveGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingRuleAllowAll = UserPrivacySettingRule — A rule to allow all users to do something */
export class UserPrivacySettingRuleAllowAll {
  type: 'userPrivacySettingRuleAllowAll' = 'userPrivacySettingRuleAllowAll';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingRuleAllowAll` object; null input -> null. */
export function decodeUserPrivacySettingRuleAllowAll(json: TdJsonRaw): UserPrivacySettingRuleAllowAll | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleAllowAll = new UserPrivacySettingRuleAllowAll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingRuleAllowAll` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleAllowAll(value: UserPrivacySettingRuleAllowAll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleAllowAll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingRuleAllowBots = UserPrivacySettingRule — A rule to allow all bots to do something */
export class UserPrivacySettingRuleAllowBots {
  type: 'userPrivacySettingRuleAllowBots' = 'userPrivacySettingRuleAllowBots';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingRuleAllowBots` object; null input -> null. */
export function decodeUserPrivacySettingRuleAllowBots(json: TdJsonRaw): UserPrivacySettingRuleAllowBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleAllowBots = new UserPrivacySettingRuleAllowBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingRuleAllowBots` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleAllowBots(value: UserPrivacySettingRuleAllowBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleAllowBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingRuleAllowChatMembers = UserPrivacySettingRule — A rule to allow all members of certain specified basic groups and supergroups to doing something */
export class UserPrivacySettingRuleAllowChatMembers {
  type: 'userPrivacySettingRuleAllowChatMembers' = 'userPrivacySettingRuleAllowChatMembers';
  extra?: string;
  /** chat_ids: vector<int53> — The chat identifiers, total number of chats in all rules must not exceed 20 */
  chat_ids: number[] = [];
}

/** Decode a TDLib `userPrivacySettingRuleAllowChatMembers` object; null input -> null. */
export function decodeUserPrivacySettingRuleAllowChatMembers(json: TdJsonRaw): UserPrivacySettingRuleAllowChatMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleAllowChatMembers = new UserPrivacySettingRuleAllowChatMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `userPrivacySettingRuleAllowChatMembers` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleAllowChatMembers(value: UserPrivacySettingRuleAllowChatMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleAllowChatMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** userPrivacySettingRuleAllowContacts = UserPrivacySettingRule — A rule to allow all contacts of the user to do something */
export class UserPrivacySettingRuleAllowContacts {
  type: 'userPrivacySettingRuleAllowContacts' = 'userPrivacySettingRuleAllowContacts';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingRuleAllowContacts` object; null input -> null. */
export function decodeUserPrivacySettingRuleAllowContacts(json: TdJsonRaw): UserPrivacySettingRuleAllowContacts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleAllowContacts = new UserPrivacySettingRuleAllowContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingRuleAllowContacts` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleAllowContacts(value: UserPrivacySettingRuleAllowContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleAllowContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingRuleAllowPremiumUsers = UserPrivacySettingRule — A rule to allow all Premium Users to do something; currently, allowed only for userPrivacySettingAllowChatInvites */
export class UserPrivacySettingRuleAllowPremiumUsers {
  type: 'userPrivacySettingRuleAllowPremiumUsers' = 'userPrivacySettingRuleAllowPremiumUsers';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingRuleAllowPremiumUsers` object; null input -> null. */
export function decodeUserPrivacySettingRuleAllowPremiumUsers(json: TdJsonRaw): UserPrivacySettingRuleAllowPremiumUsers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleAllowPremiumUsers = new UserPrivacySettingRuleAllowPremiumUsers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingRuleAllowPremiumUsers` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleAllowPremiumUsers(value: UserPrivacySettingRuleAllowPremiumUsers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleAllowPremiumUsers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingRuleAllowUsers = UserPrivacySettingRule — A rule to allow certain specified users to do something */
export class UserPrivacySettingRuleAllowUsers {
  type: 'userPrivacySettingRuleAllowUsers' = 'userPrivacySettingRuleAllowUsers';
  extra?: string;
  /** user_ids: vector<int53> — The user identifiers, total number of users in all rules must not exceed 1000 */
  user_ids: number[] = [];
}

/** Decode a TDLib `userPrivacySettingRuleAllowUsers` object; null input -> null. */
export function decodeUserPrivacySettingRuleAllowUsers(json: TdJsonRaw): UserPrivacySettingRuleAllowUsers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleAllowUsers = new UserPrivacySettingRuleAllowUsers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `userPrivacySettingRuleAllowUsers` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleAllowUsers(value: UserPrivacySettingRuleAllowUsers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleAllowUsers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** userPrivacySettingRuleRestrictAll = UserPrivacySettingRule — A rule to restrict all users from doing something */
export class UserPrivacySettingRuleRestrictAll {
  type: 'userPrivacySettingRuleRestrictAll' = 'userPrivacySettingRuleRestrictAll';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingRuleRestrictAll` object; null input -> null. */
export function decodeUserPrivacySettingRuleRestrictAll(json: TdJsonRaw): UserPrivacySettingRuleRestrictAll | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleRestrictAll = new UserPrivacySettingRuleRestrictAll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingRuleRestrictAll` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleRestrictAll(value: UserPrivacySettingRuleRestrictAll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleRestrictAll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingRuleRestrictBots = UserPrivacySettingRule — A rule to restrict all bots from doing something */
export class UserPrivacySettingRuleRestrictBots {
  type: 'userPrivacySettingRuleRestrictBots' = 'userPrivacySettingRuleRestrictBots';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingRuleRestrictBots` object; null input -> null. */
export function decodeUserPrivacySettingRuleRestrictBots(json: TdJsonRaw): UserPrivacySettingRuleRestrictBots | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleRestrictBots = new UserPrivacySettingRuleRestrictBots();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingRuleRestrictBots` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleRestrictBots(value: UserPrivacySettingRuleRestrictBots): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleRestrictBots';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingRuleRestrictChatMembers = UserPrivacySettingRule — A rule to restrict all members of specified basic groups and supergroups from doing something */
export class UserPrivacySettingRuleRestrictChatMembers {
  type: 'userPrivacySettingRuleRestrictChatMembers' = 'userPrivacySettingRuleRestrictChatMembers';
  extra?: string;
  /** chat_ids: vector<int53> — The chat identifiers, total number of chats in all rules must not exceed 20 */
  chat_ids: number[] = [];
}

/** Decode a TDLib `userPrivacySettingRuleRestrictChatMembers` object; null input -> null. */
export function decodeUserPrivacySettingRuleRestrictChatMembers(json: TdJsonRaw): UserPrivacySettingRuleRestrictChatMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleRestrictChatMembers = new UserPrivacySettingRuleRestrictChatMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `userPrivacySettingRuleRestrictChatMembers` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleRestrictChatMembers(value: UserPrivacySettingRuleRestrictChatMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleRestrictChatMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** userPrivacySettingRuleRestrictContacts = UserPrivacySettingRule — A rule to restrict all contacts of the user from doing something */
export class UserPrivacySettingRuleRestrictContacts {
  type: 'userPrivacySettingRuleRestrictContacts' = 'userPrivacySettingRuleRestrictContacts';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingRuleRestrictContacts` object; null input -> null. */
export function decodeUserPrivacySettingRuleRestrictContacts(json: TdJsonRaw): UserPrivacySettingRuleRestrictContacts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleRestrictContacts = new UserPrivacySettingRuleRestrictContacts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingRuleRestrictContacts` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleRestrictContacts(value: UserPrivacySettingRuleRestrictContacts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleRestrictContacts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingRuleRestrictUsers = UserPrivacySettingRule — A rule to restrict all specified users from doing something */
export class UserPrivacySettingRuleRestrictUsers {
  type: 'userPrivacySettingRuleRestrictUsers' = 'userPrivacySettingRuleRestrictUsers';
  extra?: string;
  /** user_ids: vector<int53> — The user identifiers, total number of users in all rules must not exceed 1000 */
  user_ids: number[] = [];
}

/** Decode a TDLib `userPrivacySettingRuleRestrictUsers` object; null input -> null. */
export function decodeUserPrivacySettingRuleRestrictUsers(json: TdJsonRaw): UserPrivacySettingRuleRestrictUsers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRuleRestrictUsers = new UserPrivacySettingRuleRestrictUsers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `userPrivacySettingRuleRestrictUsers` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRuleRestrictUsers(value: UserPrivacySettingRuleRestrictUsers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRuleRestrictUsers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** userPrivacySettingRules = UserPrivacySettingRules — A list of privacy rules. Rules are matched in the specified order. The first matched rule defines the privacy setting for a given user. If no rule matches, the action is not allowed */
export class UserPrivacySettingRules {
  type: 'userPrivacySettingRules' = 'userPrivacySettingRules';
  extra?: string;
  /** rules: vector<UserPrivacySettingRule> — A list of rules */
  rules: TdUserPrivacySettingRule[] = [];
}

/** Decode a TDLib `userPrivacySettingRules` object; null input -> null. */
export function decodeUserPrivacySettingRules(json: TdJsonRaw): UserPrivacySettingRules | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingRules = new UserPrivacySettingRules();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.rules = decodeVector(record['rules'], (item: TdJsonRaw): TdUserPrivacySettingRule => decodeTdUserPrivacySettingRule(item) as TdUserPrivacySettingRule);
  return out;
}

/** Encode a `userPrivacySettingRules` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingRules(value: UserPrivacySettingRules): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingRules';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['rules'] = encodeVector(value.rules, (item: TdUserPrivacySettingRule | null): TdJsonRaw => item === null ? null : encodeTdUserPrivacySettingRule(item));
  return out;
}

/** userPrivacySettingShowBio = UserPrivacySetting — A privacy setting for managing whether the user's bio is visible */
export class UserPrivacySettingShowBio {
  type: 'userPrivacySettingShowBio' = 'userPrivacySettingShowBio';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingShowBio` object; null input -> null. */
export function decodeUserPrivacySettingShowBio(json: TdJsonRaw): UserPrivacySettingShowBio | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingShowBio = new UserPrivacySettingShowBio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingShowBio` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingShowBio(value: UserPrivacySettingShowBio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingShowBio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingShowBirthdate = UserPrivacySetting — A privacy setting for managing whether the user's birthdate is visible */
export class UserPrivacySettingShowBirthdate {
  type: 'userPrivacySettingShowBirthdate' = 'userPrivacySettingShowBirthdate';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingShowBirthdate` object; null input -> null. */
export function decodeUserPrivacySettingShowBirthdate(json: TdJsonRaw): UserPrivacySettingShowBirthdate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingShowBirthdate = new UserPrivacySettingShowBirthdate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingShowBirthdate` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingShowBirthdate(value: UserPrivacySettingShowBirthdate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingShowBirthdate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingShowLinkInForwardedMessages = UserPrivacySetting — A privacy setting for managing whether a link to the user's account is included in forwarded messages */
export class UserPrivacySettingShowLinkInForwardedMessages {
  type: 'userPrivacySettingShowLinkInForwardedMessages' = 'userPrivacySettingShowLinkInForwardedMessages';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingShowLinkInForwardedMessages` object; null input -> null. */
export function decodeUserPrivacySettingShowLinkInForwardedMessages(json: TdJsonRaw): UserPrivacySettingShowLinkInForwardedMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingShowLinkInForwardedMessages = new UserPrivacySettingShowLinkInForwardedMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingShowLinkInForwardedMessages` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingShowLinkInForwardedMessages(value: UserPrivacySettingShowLinkInForwardedMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingShowLinkInForwardedMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingShowPhoneNumber = UserPrivacySetting — A privacy setting for managing whether the user's phone number is visible */
export class UserPrivacySettingShowPhoneNumber {
  type: 'userPrivacySettingShowPhoneNumber' = 'userPrivacySettingShowPhoneNumber';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingShowPhoneNumber` object; null input -> null. */
export function decodeUserPrivacySettingShowPhoneNumber(json: TdJsonRaw): UserPrivacySettingShowPhoneNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingShowPhoneNumber = new UserPrivacySettingShowPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingShowPhoneNumber` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingShowPhoneNumber(value: UserPrivacySettingShowPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingShowPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingShowProfileAudio = UserPrivacySetting — A privacy setting for managing whether the user's profile audio files are visible */
export class UserPrivacySettingShowProfileAudio {
  type: 'userPrivacySettingShowProfileAudio' = 'userPrivacySettingShowProfileAudio';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingShowProfileAudio` object; null input -> null. */
export function decodeUserPrivacySettingShowProfileAudio(json: TdJsonRaw): UserPrivacySettingShowProfileAudio | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingShowProfileAudio = new UserPrivacySettingShowProfileAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingShowProfileAudio` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingShowProfileAudio(value: UserPrivacySettingShowProfileAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingShowProfileAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingShowProfilePhoto = UserPrivacySetting — A privacy setting for managing whether the user's profile photo is visible */
export class UserPrivacySettingShowProfilePhoto {
  type: 'userPrivacySettingShowProfilePhoto' = 'userPrivacySettingShowProfilePhoto';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingShowProfilePhoto` object; null input -> null. */
export function decodeUserPrivacySettingShowProfilePhoto(json: TdJsonRaw): UserPrivacySettingShowProfilePhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingShowProfilePhoto = new UserPrivacySettingShowProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingShowProfilePhoto` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingShowProfilePhoto(value: UserPrivacySettingShowProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingShowProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userPrivacySettingShowStatus = UserPrivacySetting — A privacy setting for managing whether the user's online status is visible */
export class UserPrivacySettingShowStatus {
  type: 'userPrivacySettingShowStatus' = 'userPrivacySettingShowStatus';
  extra?: string;
}

/** Decode a TDLib `userPrivacySettingShowStatus` object; null input -> null. */
export function decodeUserPrivacySettingShowStatus(json: TdJsonRaw): UserPrivacySettingShowStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserPrivacySettingShowStatus = new UserPrivacySettingShowStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userPrivacySettingShowStatus` into TDLib JSON wire shape. */
export function encodeUserPrivacySettingShowStatus(value: UserPrivacySettingShowStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userPrivacySettingShowStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userRating = UserRating — Contains description of user rating */
export class UserRating {
  type: 'userRating' = 'userRating';
  extra?: string;
  /** level: int32 — The level of the user; may be negative */
  level: number = 0;
  /** is_maximum_level_reached: Bool — True, if the maximum level is reached */
  is_maximum_level_reached: boolean = false;
  /** rating: int53 — Numerical value of the rating */
  rating: number = 0;
  /** current_level_rating: int53 — The rating required for the current level */
  current_level_rating: number = 0;
  /** next_level_rating: int53 — The rating required for the next level; 0 if the maximum level is reached */
  next_level_rating: number = 0;
}

/** Decode a TDLib `userRating` object; null input -> null. */
export function decodeUserRating(json: TdJsonRaw): UserRating | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserRating = new UserRating();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.level = asNumber(record['level']);
  out.is_maximum_level_reached = asBool(record['is_maximum_level_reached']);
  out.rating = asNumber(record['rating']);
  out.current_level_rating = asNumber(record['current_level_rating']);
  out.next_level_rating = asNumber(record['next_level_rating']);
  return out;
}

/** Encode a `userRating` into TDLib JSON wire shape. */
export function encodeUserRating(value: UserRating): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userRating';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['level'] = value.level;
  out['is_maximum_level_reached'] = value.is_maximum_level_reached;
  out['rating'] = value.rating;
  out['current_level_rating'] = value.current_level_rating;
  out['next_level_rating'] = value.next_level_rating;
  return out;
}

/** userStatusEmpty = UserStatus — The user's status has never been changed */
export class UserStatusEmpty {
  type: 'userStatusEmpty' = 'userStatusEmpty';
  extra?: string;
}

/** Decode a TDLib `userStatusEmpty` object; null input -> null. */
export function decodeUserStatusEmpty(json: TdJsonRaw): UserStatusEmpty | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserStatusEmpty = new UserStatusEmpty();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userStatusEmpty` into TDLib JSON wire shape. */
export function encodeUserStatusEmpty(value: UserStatusEmpty): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userStatusEmpty';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userStatusLastMonth = UserStatus — The user is offline, but was online last month */
export class UserStatusLastMonth {
  type: 'userStatusLastMonth' = 'userStatusLastMonth';
  extra?: string;
  /** by_my_privacy_settings: Bool — Exact user's status is hidden because the current user enabled userPrivacySettingShowStatus privacy setting for the user and has no Telegram Premium */
  by_my_privacy_settings: boolean = false;
}

/** Decode a TDLib `userStatusLastMonth` object; null input -> null. */
export function decodeUserStatusLastMonth(json: TdJsonRaw): UserStatusLastMonth | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserStatusLastMonth = new UserStatusLastMonth();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.by_my_privacy_settings = asBool(record['by_my_privacy_settings']);
  return out;
}

/** Encode a `userStatusLastMonth` into TDLib JSON wire shape. */
export function encodeUserStatusLastMonth(value: UserStatusLastMonth): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userStatusLastMonth';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['by_my_privacy_settings'] = value.by_my_privacy_settings;
  return out;
}

/** userStatusLastWeek = UserStatus — The user is offline, but was online last week */
export class UserStatusLastWeek {
  type: 'userStatusLastWeek' = 'userStatusLastWeek';
  extra?: string;
  /** by_my_privacy_settings: Bool — Exact user's status is hidden because the current user enabled userPrivacySettingShowStatus privacy setting for the user and has no Telegram Premium */
  by_my_privacy_settings: boolean = false;
}

/** Decode a TDLib `userStatusLastWeek` object; null input -> null. */
export function decodeUserStatusLastWeek(json: TdJsonRaw): UserStatusLastWeek | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserStatusLastWeek = new UserStatusLastWeek();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.by_my_privacy_settings = asBool(record['by_my_privacy_settings']);
  return out;
}

/** Encode a `userStatusLastWeek` into TDLib JSON wire shape. */
export function encodeUserStatusLastWeek(value: UserStatusLastWeek): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userStatusLastWeek';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['by_my_privacy_settings'] = value.by_my_privacy_settings;
  return out;
}

/** userStatusOffline = UserStatus — The user is offline */
export class UserStatusOffline {
  type: 'userStatusOffline' = 'userStatusOffline';
  extra?: string;
  /** was_online: int32 — Point in time (Unix timestamp) when the user was last online */
  was_online: number = 0;
}

/** Decode a TDLib `userStatusOffline` object; null input -> null. */
export function decodeUserStatusOffline(json: TdJsonRaw): UserStatusOffline | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserStatusOffline = new UserStatusOffline();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.was_online = asNumber(record['was_online']);
  return out;
}

/** Encode a `userStatusOffline` into TDLib JSON wire shape. */
export function encodeUserStatusOffline(value: UserStatusOffline): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userStatusOffline';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['was_online'] = value.was_online;
  return out;
}

/** userStatusOnline = UserStatus — The user is online */
export class UserStatusOnline {
  type: 'userStatusOnline' = 'userStatusOnline';
  extra?: string;
  /** expires: int32 — Point in time (Unix timestamp) when the user's online status will expire */
  expires: number = 0;
}

/** Decode a TDLib `userStatusOnline` object; null input -> null. */
export function decodeUserStatusOnline(json: TdJsonRaw): UserStatusOnline | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserStatusOnline = new UserStatusOnline();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.expires = asNumber(record['expires']);
  return out;
}

/** Encode a `userStatusOnline` into TDLib JSON wire shape. */
export function encodeUserStatusOnline(value: UserStatusOnline): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userStatusOnline';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['expires'] = value.expires;
  return out;
}

/** userStatusRecently = UserStatus — The user was online recently */
export class UserStatusRecently {
  type: 'userStatusRecently' = 'userStatusRecently';
  extra?: string;
  /** by_my_privacy_settings: Bool — Exact user's status is hidden because the current user enabled userPrivacySettingShowStatus privacy setting for the user and has no Telegram Premium */
  by_my_privacy_settings: boolean = false;
}

/** Decode a TDLib `userStatusRecently` object; null input -> null. */
export function decodeUserStatusRecently(json: TdJsonRaw): UserStatusRecently | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserStatusRecently = new UserStatusRecently();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.by_my_privacy_settings = asBool(record['by_my_privacy_settings']);
  return out;
}

/** Encode a `userStatusRecently` into TDLib JSON wire shape. */
export function encodeUserStatusRecently(value: UserStatusRecently): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userStatusRecently';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['by_my_privacy_settings'] = value.by_my_privacy_settings;
  return out;
}

/** userSupportInfo = UserSupportInfo — Contains custom information about the user */
export class UserSupportInfo {
  type: 'userSupportInfo' = 'userSupportInfo';
  extra?: string;
  /** message: formattedText — Information message */
  message: FormattedText | null = null;
  /** author: string — Information author */
  author: string = '';
  /** date: int32 — Information change date */
  date: number = 0;
}

/** Decode a TDLib `userSupportInfo` object; null input -> null. */
export function decodeUserSupportInfo(json: TdJsonRaw): UserSupportInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserSupportInfo = new UserSupportInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeFormattedText(record['message']);
  out.author = asString(record['author']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `userSupportInfo` into TDLib JSON wire shape. */
export function encodeUserSupportInfo(value: UserSupportInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userSupportInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeFormattedText(value.message);
  out['author'] = value.author;
  out['date'] = value.date;
  return out;
}

/** userTypeBot = UserType — A bot (see https://core.telegram.org/bots) */
export class UserTypeBot {
  type: 'userTypeBot' = 'userTypeBot';
  extra?: string;
  /** can_be_edited: Bool — True, if the bot is owned by the current user and can be edited using the methods toggleBotUsernameIsActive, reorderBotActiveUsernames, setBotProfilePhoto, setBotName, setBotInfoDescription, and setBotInfoShortDescr... */
  can_be_edited: boolean = false;
  /** can_join_groups: Bool — True, if the bot can be invited to basic group and supergroup chats */
  can_join_groups: boolean = false;
  /** can_read_all_group_messages: Bool — True, if the bot can read all messages in basic group or supergroup chats and not just those addressed to the bot. In private and channel chats a bot can always read all messages */
  can_read_all_group_messages: boolean = false;
  /** has_main_web_app: Bool — True, if the bot has the main Web App */
  has_main_web_app: boolean = false;
  /** has_topics: Bool — True, if the bot has topics */
  has_topics: boolean = false;
  /** allows_users_to_create_topics: Bool — True, if users can create and delete topics in the chat with the bot */
  allows_users_to_create_topics: boolean = false;
  /** can_manage_bots: Bool — True, if the bot can manage other bots */
  can_manage_bots: boolean = false;
  /** is_inline: Bool — True, if the bot supports inline queries */
  is_inline: boolean = false;
  /** inline_query_placeholder: string — Placeholder for inline queries (displayed on the application input field) */
  inline_query_placeholder: string = '';
  /** supports_guest_queries: Bool — True, if the bot can be queried by username from any non-secret chat */
  supports_guest_queries: boolean = false;
  /** is_guard: Bool — True, if the bot can be set as a guard bot in supergroup chats */
  is_guard: boolean = false;
  /** need_location: Bool — True, if the location of the user is expected to be sent with every inline query to this bot */
  need_location: boolean = false;
  /** can_connect_to_business: Bool — True, if the bot supports connection to user accounts for chat automation */
  can_connect_to_business: boolean = false;
  /** can_be_added_to_attachment_menu: Bool — True, if the bot can be added to attachment or side menu */
  can_be_added_to_attachment_menu: boolean = false;
  /** active_user_count: int32 — The number of recently active users of the bot */
  active_user_count: number = 0;
}

/** Decode a TDLib `userTypeBot` object; null input -> null. */
export function decodeUserTypeBot(json: TdJsonRaw): UserTypeBot | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserTypeBot = new UserTypeBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_be_edited = asBool(record['can_be_edited']);
  out.can_join_groups = asBool(record['can_join_groups']);
  out.can_read_all_group_messages = asBool(record['can_read_all_group_messages']);
  out.has_main_web_app = asBool(record['has_main_web_app']);
  out.has_topics = asBool(record['has_topics']);
  out.allows_users_to_create_topics = asBool(record['allows_users_to_create_topics']);
  out.can_manage_bots = asBool(record['can_manage_bots']);
  out.is_inline = asBool(record['is_inline']);
  out.inline_query_placeholder = asString(record['inline_query_placeholder']);
  out.supports_guest_queries = asBool(record['supports_guest_queries']);
  out.is_guard = asBool(record['is_guard']);
  out.need_location = asBool(record['need_location']);
  out.can_connect_to_business = asBool(record['can_connect_to_business']);
  out.can_be_added_to_attachment_menu = asBool(record['can_be_added_to_attachment_menu']);
  out.active_user_count = asNumber(record['active_user_count']);
  return out;
}

/** Encode a `userTypeBot` into TDLib JSON wire shape. */
export function encodeUserTypeBot(value: UserTypeBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userTypeBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_be_edited'] = value.can_be_edited;
  out['can_join_groups'] = value.can_join_groups;
  out['can_read_all_group_messages'] = value.can_read_all_group_messages;
  out['has_main_web_app'] = value.has_main_web_app;
  out['has_topics'] = value.has_topics;
  out['allows_users_to_create_topics'] = value.allows_users_to_create_topics;
  out['can_manage_bots'] = value.can_manage_bots;
  out['is_inline'] = value.is_inline;
  out['inline_query_placeholder'] = value.inline_query_placeholder;
  out['supports_guest_queries'] = value.supports_guest_queries;
  out['is_guard'] = value.is_guard;
  out['need_location'] = value.need_location;
  out['can_connect_to_business'] = value.can_connect_to_business;
  out['can_be_added_to_attachment_menu'] = value.can_be_added_to_attachment_menu;
  out['active_user_count'] = value.active_user_count;
  return out;
}

/** userTypeDeleted = UserType — A deleted user or deleted bot. No information on the user besides the user identifier is available. It is not possible to perform any active actions on this type of user */
export class UserTypeDeleted {
  type: 'userTypeDeleted' = 'userTypeDeleted';
  extra?: string;
}

/** Decode a TDLib `userTypeDeleted` object; null input -> null. */
export function decodeUserTypeDeleted(json: TdJsonRaw): UserTypeDeleted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserTypeDeleted = new UserTypeDeleted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userTypeDeleted` into TDLib JSON wire shape. */
export function encodeUserTypeDeleted(value: UserTypeDeleted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userTypeDeleted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userTypeRegular = UserType — A regular user */
export class UserTypeRegular {
  type: 'userTypeRegular' = 'userTypeRegular';
  extra?: string;
}

/** Decode a TDLib `userTypeRegular` object; null input -> null. */
export function decodeUserTypeRegular(json: TdJsonRaw): UserTypeRegular | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserTypeRegular = new UserTypeRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userTypeRegular` into TDLib JSON wire shape. */
export function encodeUserTypeRegular(value: UserTypeRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userTypeRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** userTypeUnknown = UserType — No information on the user besides the user identifier is available, yet this user has not been deleted. This object is extremely rare and must be handled like a deleted user. It is not possible to perform an... */
export class UserTypeUnknown {
  type: 'userTypeUnknown' = 'userTypeUnknown';
  extra?: string;
}

/** Decode a TDLib `userTypeUnknown` object; null input -> null. */
export function decodeUserTypeUnknown(json: TdJsonRaw): UserTypeUnknown | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: UserTypeUnknown = new UserTypeUnknown();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `userTypeUnknown` into TDLib JSON wire shape. */
export function encodeUserTypeUnknown(value: UserTypeUnknown): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'userTypeUnknown';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** usernames = Usernames — Describes usernames assigned to a user, a supergroup, or a channel */
export class Usernames {
  type: 'usernames' = 'usernames';
  extra?: string;
  /** active_usernames: vector<string> — List of active usernames; the first one must be shown as the primary username. The order of active usernames can be changed with reorderActiveUsernames, reorderBotActiveUsernames or reorderSupergroupAct... */
  active_usernames: string[] = [];
  /** disabled_usernames: vector<string> — List of currently disabled usernames; the username can be activated with toggleUsernameIsActive, toggleBotUsernameIsActive, or toggleSupergroupUsernameIsActive */
  disabled_usernames: string[] = [];
  /** editable_username: string — Active or disabled username, which may be changed with setUsername or setSupergroupUsername */
  editable_username: string = '';
  /** collectible_usernames: vector<string> — Collectible usernames that were purchased at https://fragment.com and can be passed to getCollectibleItemInfo for more details */
  collectible_usernames: string[] = [];
}

/** Decode a TDLib `usernames` object; null input -> null. */
export function decodeUsernames(json: TdJsonRaw): Usernames | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Usernames = new Usernames();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.active_usernames = decodeVector(record['active_usernames'], (item: TdJsonRaw): string => asString(item));
  out.disabled_usernames = decodeVector(record['disabled_usernames'], (item: TdJsonRaw): string => asString(item));
  out.editable_username = asString(record['editable_username']);
  out.collectible_usernames = decodeVector(record['collectible_usernames'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `usernames` into TDLib JSON wire shape. */
export function encodeUsernames(value: Usernames): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'usernames';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['active_usernames'] = encodeVector(value.active_usernames, (item: string): TdJsonRaw => item);
  out['disabled_usernames'] = encodeVector(value.disabled_usernames, (item: string): TdJsonRaw => item);
  out['editable_username'] = value.editable_username;
  out['collectible_usernames'] = encodeVector(value.collectible_usernames, (item: string): TdJsonRaw => item);
  return out;
}

/** users = Users — Represents a list of users */
export class Users {
  type: 'users' = 'users';
  extra?: string;
  /** total_count: int32 — Approximate total number of users found */
  total_count: number = 0;
  /** user_ids: vector<int53> — A list of user identifiers */
  user_ids: number[] = [];
}

/** Decode a TDLib `users` object; null input -> null. */
export function decodeUsers(json: TdJsonRaw): Users | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Users = new Users();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `users` into TDLib JSON wire shape. */
export function encodeUsers(value: Users): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'users';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}
