/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { CallProtocol, decodeCallProtocol, encodeCallProtocol } from './TdTypes_C';
import { EmailAddressAuthenticationCodeInfo, decodeEmailAddressAuthenticationCodeInfo, encodeEmailAddressAuthenticationCodeInfo } from './TdTypes_E';
import { File, decodeFile, encodeFile } from './TdTypes_F';
import { Gift, decodeGift, encodeGift } from './TdTypes_G';
import { ImportedContact, InlineQueryResultsButton, InputAudio, InputChecklistTask, InputPollOption, decodeImportedContact, decodeInlineQueryResultsButton, decodeInputAudio, decodeInputChecklistTask, decodeInputPollOption, encodeImportedContact, encodeInlineQueryResultsButton, encodeInputAudio, encodeInputChecklistTask, encodeInputPollOption } from './TdTypes_I';
import { MessageSendOptions, Minithumbnail, decodeMessageSendOptions, decodeMinithumbnail, encodeMessageSendOptions, encodeMinithumbnail } from './TdTypes_M';
import { NewSticker, decodeNewSticker, encodeNewSticker } from './TdTypes_N';
import { Photo, ProxyValue, decodePhoto, decodeProxyValue, encodePhoto, encodeProxyValue } from './TdTypes_P';
import { ScopeAutosaveSettings, ShippingOption, StarAmount, Sticker, decodeScopeAutosaveSettings, decodeShippingOption, decodeStarAmount, decodeSticker, encodeScopeAutosaveSettings, encodeShippingOption, encodeStarAmount, encodeSticker } from './TdTypes_S';
import { TermsOfService, Thumbnail, decodeTermsOfService, decodeThumbnail, encodeTermsOfService, encodeThumbnail } from './TdTypes_T';
import { UserAuctionBid, decodeUserAuctionBid, encodeUserAuctionBid } from './TdTypes_U';
import { TdAuthenticationCodeType, TdChatJoinRequestResult, TdChatList, TdEmailAddressResetState, TdFirebaseDeviceVerificationParameters, TdInputFile, TdInputInlineQueryResult, TdInputMessageContent, TdInputMessageReplyTo, TdInputStoryContent, TdMessageSender, TdNetworkStatisticsEntry, TdPaidReactionType, TdReactionType, TdReactionUnavailabilityReason, TdStorePaymentPurpose, TdStoreTransaction, decodeTdAuthenticationCodeType, decodeTdChatJoinRequestResult, decodeTdChatList, decodeTdEmailAddressResetState, decodeTdFirebaseDeviceVerificationParameters, decodeTdInputFile, decodeTdInputInlineQueryResult, decodeTdInputMessageContent, decodeTdInputMessageReplyTo, decodeTdInputStoryContent, decodeTdMessageSender, decodeTdNetworkStatisticsEntry, decodeTdPaidReactionType, decodeTdReactionType, decodeTdReactionUnavailabilityReason, decodeTdStorePaymentPurpose, decodeTdStoreTransaction, encodeTdAuthenticationCodeType, encodeTdChatJoinRequestResult, encodeTdChatList, encodeTdEmailAddressResetState, encodeTdFirebaseDeviceVerificationParameters, encodeTdInputFile, encodeTdInputInlineQueryResult, encodeTdInputMessageContent, encodeTdInputMessageReplyTo, encodeTdInputStoryContent, encodeTdMessageSender, encodeTdNetworkStatisticsEntry, encodeTdPaidReactionType, encodeTdReactionType, encodeTdReactionUnavailabilityReason, encodeTdStorePaymentPurpose, encodeTdStoreTransaction } from './TdUnions';

/** accentColor = AccentColor — Contains information about supported accent color for user/chat name, background of empty chat photo, replies to messages and link previews */
export class AccentColor {
  type: 'accentColor' = 'accentColor';
  extra?: string;
  /** id: int32 — Accent color identifier */
  id: number = 0;
  /** built_in_accent_color_id: int32 — Identifier of a built-in color to use in places, where only one color is needed; 0-6 */
  built_in_accent_color_id: number = 0;
  /** light_theme_colors: vector<int32> — The list of 1-3 colors in RGB format, describing the accent color, as expected to be shown in light themes */
  light_theme_colors: number[] = [];
  /** dark_theme_colors: vector<int32> — The list of 1-3 colors in RGB format, describing the accent color, as expected to be shown in dark themes */
  dark_theme_colors: number[] = [];
  /** min_channel_chat_boost_level: int32 — The minimum chat boost level required to use the color in a channel chat */
  min_channel_chat_boost_level: number = 0;
}

/** Decode a TDLib `accentColor` object; null input -> null. */
export function decodeAccentColor(json: TdJsonRaw): AccentColor | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AccentColor = new AccentColor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.built_in_accent_color_id = asNumber(record['built_in_accent_color_id']);
  out.light_theme_colors = decodeVector(record['light_theme_colors'], (item: TdJsonRaw): number => asNumber(item));
  out.dark_theme_colors = decodeVector(record['dark_theme_colors'], (item: TdJsonRaw): number => asNumber(item));
  out.min_channel_chat_boost_level = asNumber(record['min_channel_chat_boost_level']);
  return out;
}

/** Encode a `accentColor` into TDLib JSON wire shape. */
export function encodeAccentColor(value: AccentColor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'accentColor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['built_in_accent_color_id'] = value.built_in_accent_color_id;
  out['light_theme_colors'] = encodeVector(value.light_theme_colors, (item: number): TdJsonRaw => item);
  out['dark_theme_colors'] = encodeVector(value.dark_theme_colors, (item: number): TdJsonRaw => item);
  out['min_channel_chat_boost_level'] = value.min_channel_chat_boost_level;
  return out;
}

/** acceptCall = Ok — Accepts an incoming call */
export class AcceptCall {
  type: 'acceptCall' = 'acceptCall';
  extra?: string;
  /** call_id: int32 — Call identifier */
  call_id: number = 0;
  /** protocol: callProtocol — The call protocols supported by the application */
  protocol: CallProtocol | null = null;
}

/** Decode a TDLib `acceptCall` object; null input -> null. */
export function decodeAcceptCall(json: TdJsonRaw): AcceptCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AcceptCall = new AcceptCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.call_id = asNumber(record['call_id']);
  out.protocol = decodeCallProtocol(record['protocol']);
  return out;
}

/** Encode a `acceptCall` into TDLib JSON wire shape. */
export function encodeAcceptCall(value: AcceptCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'acceptCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['call_id'] = value.call_id;
  out['protocol'] = value.protocol === null ? null : encodeCallProtocol(value.protocol);
  return out;
}

/** acceptOauthRequest = HttpUrl — Accepts an OAuth authorization request. Returns an HTTP URL to open after successful authorization. May return an empty link if just a toast about successful login has to be shown */
export class AcceptOauthRequest {
  type: 'acceptOauthRequest' = 'acceptOauthRequest';
  extra?: string;
  /** url: string — URL of the OAuth deep link */
  url: string = '';
  /** match_code: string — The matching code chosen by the user */
  match_code: string = '';
  /** allow_write_access: Bool — Pass true if the current user allowed the bot that was returned in getOauthLinkInfo, to send them messages */
  allow_write_access: boolean = false;
  /** allow_phone_number_access: Bool — Pass true if the current user allowed the bot that was returned in getOauthLinkInfo, to access their phone number */
  allow_phone_number_access: boolean = false;
}

/** Decode a TDLib `acceptOauthRequest` object; null input -> null. */
export function decodeAcceptOauthRequest(json: TdJsonRaw): AcceptOauthRequest | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AcceptOauthRequest = new AcceptOauthRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.match_code = asString(record['match_code']);
  out.allow_write_access = asBool(record['allow_write_access']);
  out.allow_phone_number_access = asBool(record['allow_phone_number_access']);
  return out;
}

/** Encode a `acceptOauthRequest` into TDLib JSON wire shape. */
export function encodeAcceptOauthRequest(value: AcceptOauthRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'acceptOauthRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['match_code'] = value.match_code;
  out['allow_write_access'] = value.allow_write_access;
  out['allow_phone_number_access'] = value.allow_phone_number_access;
  return out;
}

/** acceptTermsOfService = Ok — Accepts Telegram terms of service */
export class AcceptTermsOfService {
  type: 'acceptTermsOfService' = 'acceptTermsOfService';
  extra?: string;
  /** terms_of_service_id: string — Terms of service identifier */
  terms_of_service_id: string = '';
}

/** Decode a TDLib `acceptTermsOfService` object; null input -> null. */
export function decodeAcceptTermsOfService(json: TdJsonRaw): AcceptTermsOfService | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AcceptTermsOfService = new AcceptTermsOfService();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.terms_of_service_id = asString(record['terms_of_service_id']);
  return out;
}

/** Encode a `acceptTermsOfService` into TDLib JSON wire shape. */
export function encodeAcceptTermsOfService(value: AcceptTermsOfService): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'acceptTermsOfService';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['terms_of_service_id'] = value.terms_of_service_id;
  return out;
}

/** acceptedGiftTypes = AcceptedGiftTypes — Describes gift types that are accepted by a user */
export class AcceptedGiftTypes {
  type: 'acceptedGiftTypes' = 'acceptedGiftTypes';
  extra?: string;
  /** unlimited_gifts: Bool — True, if unlimited regular gifts are accepted */
  unlimited_gifts: boolean = false;
  /** limited_gifts: Bool — True, if limited regular gifts are accepted */
  limited_gifts: boolean = false;
  /** upgraded_gifts: Bool — True, if upgraded gifts and regular gifts that can be upgraded for free are accepted */
  upgraded_gifts: boolean = false;
  /** gifts_from_channels: Bool — True, if gifts from channels are accepted subject to other restrictions */
  gifts_from_channels: boolean = false;
  /** premium_subscription: Bool — True, if Telegram Premium subscription is accepted */
  premium_subscription: boolean = false;
}

/** Decode a TDLib `acceptedGiftTypes` object; null input -> null. */
export function decodeAcceptedGiftTypes(json: TdJsonRaw): AcceptedGiftTypes | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AcceptedGiftTypes = new AcceptedGiftTypes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.unlimited_gifts = asBool(record['unlimited_gifts']);
  out.limited_gifts = asBool(record['limited_gifts']);
  out.upgraded_gifts = asBool(record['upgraded_gifts']);
  out.gifts_from_channels = asBool(record['gifts_from_channels']);
  out.premium_subscription = asBool(record['premium_subscription']);
  return out;
}

/** Encode a `acceptedGiftTypes` into TDLib JSON wire shape. */
export function encodeAcceptedGiftTypes(value: AcceptedGiftTypes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'acceptedGiftTypes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['unlimited_gifts'] = value.unlimited_gifts;
  out['limited_gifts'] = value.limited_gifts;
  out['upgraded_gifts'] = value.upgraded_gifts;
  out['gifts_from_channels'] = value.gifts_from_channels;
  out['premium_subscription'] = value.premium_subscription;
  return out;
}

/** accountInfo = AccountInfo — Contains basic information about another user who started a chat with the current user */
export class AccountInfo {
  type: 'accountInfo' = 'accountInfo';
  extra?: string;
  /** registration_month: int32 — Month when the user was registered in Telegram; 0-12; may be 0 if unknown */
  registration_month: number = 0;
  /** registration_year: int32 — Year when the user was registered in Telegram; 0-9999; may be 0 if unknown */
  registration_year: number = 0;
  /** phone_number_country_code: string — A two-letter ISO 3166-1 alpha-2 country code based on the phone number of the user; may be empty if unknown */
  phone_number_country_code: string = '';
  /** last_name_change_date: int32 — Point in time (Unix timestamp) when the user changed name last time; 0 if unknown */
  last_name_change_date: number = 0;
  /** last_photo_change_date: int32 — Point in time (Unix timestamp) when the user changed photo last time; 0 if unknown */
  last_photo_change_date: number = 0;
}

/** Decode a TDLib `accountInfo` object; null input -> null. */
export function decodeAccountInfo(json: TdJsonRaw): AccountInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AccountInfo = new AccountInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.registration_month = asNumber(record['registration_month']);
  out.registration_year = asNumber(record['registration_year']);
  out.phone_number_country_code = asString(record['phone_number_country_code']);
  out.last_name_change_date = asNumber(record['last_name_change_date']);
  out.last_photo_change_date = asNumber(record['last_photo_change_date']);
  return out;
}

/** Encode a `accountInfo` into TDLib JSON wire shape. */
export function encodeAccountInfo(value: AccountInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'accountInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['registration_month'] = value.registration_month;
  out['registration_year'] = value.registration_year;
  out['phone_number_country_code'] = value.phone_number_country_code;
  out['last_name_change_date'] = value.last_name_change_date;
  out['last_photo_change_date'] = value.last_photo_change_date;
  return out;
}

/** accountTtl = AccountTtl — Contains information about the period of inactivity after which the current user's account will automatically be deleted */
export class AccountTtl {
  type: 'accountTtl' = 'accountTtl';
  extra?: string;
  /** days: int32 — Number of days of inactivity before the account will be flagged for deletion; 30-730 days */
  days: number = 0;
}

/** Decode a TDLib `accountTtl` object; null input -> null. */
export function decodeAccountTtl(json: TdJsonRaw): AccountTtl | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AccountTtl = new AccountTtl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.days = asNumber(record['days']);
  return out;
}

/** Encode a `accountTtl` into TDLib JSON wire shape. */
export function encodeAccountTtl(value: AccountTtl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'accountTtl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['days'] = value.days;
  return out;
}

/** activateStoryStealthMode = Ok — Activates stealth mode for stories, which hides all views of stories from the current user in the last "story_stealth_mode_past_period" seconds and for the next "story_stealth_mode_future_period" seconds; ... */
export class ActivateStoryStealthMode {
  type: 'activateStoryStealthMode' = 'activateStoryStealthMode';
  extra?: string;
}

/** Decode a TDLib `activateStoryStealthMode` object; null input -> null. */
export function decodeActivateStoryStealthMode(json: TdJsonRaw): ActivateStoryStealthMode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ActivateStoryStealthMode = new ActivateStoryStealthMode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `activateStoryStealthMode` into TDLib JSON wire shape. */
export function encodeActivateStoryStealthMode(value: ActivateStoryStealthMode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'activateStoryStealthMode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** activeStoryStateLive = ActiveStoryState — The chat has an active live story */
export class ActiveStoryStateLive {
  type: 'activeStoryStateLive' = 'activeStoryStateLive';
  extra?: string;
  /** story_id: int32 — Identifier of the active live story */
  story_id: number = 0;
}

/** Decode a TDLib `activeStoryStateLive` object; null input -> null. */
export function decodeActiveStoryStateLive(json: TdJsonRaw): ActiveStoryStateLive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ActiveStoryStateLive = new ActiveStoryStateLive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story_id = asNumber(record['story_id']);
  return out;
}

/** Encode a `activeStoryStateLive` into TDLib JSON wire shape. */
export function encodeActiveStoryStateLive(value: ActiveStoryStateLive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'activeStoryStateLive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story_id'] = value.story_id;
  return out;
}

/** activeStoryStateRead = ActiveStoryState — The chat has active stories, all of which were read */
export class ActiveStoryStateRead {
  type: 'activeStoryStateRead' = 'activeStoryStateRead';
  extra?: string;
}

/** Decode a TDLib `activeStoryStateRead` object; null input -> null. */
export function decodeActiveStoryStateRead(json: TdJsonRaw): ActiveStoryStateRead | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ActiveStoryStateRead = new ActiveStoryStateRead();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `activeStoryStateRead` into TDLib JSON wire shape. */
export function encodeActiveStoryStateRead(value: ActiveStoryStateRead): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'activeStoryStateRead';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** activeStoryStateUnread = ActiveStoryState — The chat has some unread active stories */
export class ActiveStoryStateUnread {
  type: 'activeStoryStateUnread' = 'activeStoryStateUnread';
  extra?: string;
}

/** Decode a TDLib `activeStoryStateUnread` object; null input -> null. */
export function decodeActiveStoryStateUnread(json: TdJsonRaw): ActiveStoryStateUnread | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ActiveStoryStateUnread = new ActiveStoryStateUnread();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `activeStoryStateUnread` into TDLib JSON wire shape. */
export function encodeActiveStoryStateUnread(value: ActiveStoryStateUnread): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'activeStoryStateUnread';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** addBotMediaPreview = BotMediaPreview — Adds a new media preview to the beginning of the list of media previews of a bot. Returns the added preview after addition is completed server-side. The total number of previews must not exceed getO... */
export class AddBotMediaPreview {
  type: 'addBotMediaPreview' = 'addBotMediaPreview';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot. The bot must be owned and must have the main Web App */
  bot_user_id: number = 0;
  /** language_code: string — A two-letter ISO 639-1 language code for which preview is added. If empty, then the preview will be shown to all users for whose languages there are no dedicated previews. */
  language_code: string = '';
  /** content: InputStoryContent — Content of the added preview */
  content: TdInputStoryContent | null = null;
}

/** Decode a TDLib `addBotMediaPreview` object; null input -> null. */
export function decodeAddBotMediaPreview(json: TdJsonRaw): AddBotMediaPreview | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddBotMediaPreview = new AddBotMediaPreview();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.language_code = asString(record['language_code']);
  out.content = decodeTdInputStoryContent(record['content']);
  return out;
}

/** Encode a `addBotMediaPreview` into TDLib JSON wire shape. */
export function encodeAddBotMediaPreview(value: AddBotMediaPreview): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addBotMediaPreview';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['language_code'] = value.language_code;
  out['content'] = value.content === null ? null : encodeTdInputStoryContent(value.content);
  return out;
}

/** addChatFolderByInviteLink = Ok — Adds a chat folder by an invite link */
export class AddChatFolderByInviteLink {
  type: 'addChatFolderByInviteLink' = 'addChatFolderByInviteLink';
  extra?: string;
  /** invite_link: string — Invite link for the chat folder */
  invite_link: string = '';
  /** chat_ids: vector<int53> — Identifiers of the chats added to the chat folder. The chats are automatically joined if they aren't joined yet */
  chat_ids: number[] = [];
}

/** Decode a TDLib `addChatFolderByInviteLink` object; null input -> null. */
export function decodeAddChatFolderByInviteLink(json: TdJsonRaw): AddChatFolderByInviteLink | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddChatFolderByInviteLink = new AddChatFolderByInviteLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invite_link = asString(record['invite_link']);
  out.chat_ids = decodeVector(record['chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `addChatFolderByInviteLink` into TDLib JSON wire shape. */
export function encodeAddChatFolderByInviteLink(value: AddChatFolderByInviteLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addChatFolderByInviteLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invite_link'] = value.invite_link;
  out['chat_ids'] = encodeVector(value.chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** addChatMember = FailedToAddMembers — Adds a new member to a chat; requires can_invite_users member right. Members can't be added to private or secret chats. Returns information about members that weren't added */
export class AddChatMember {
  type: 'addChatMember' = 'addChatMember';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
  /** forward_limit: int32 — The number of earlier messages from the chat to be forwarded to the new member; up to 100. Ignored for supergroups and channels, or if the added user is a bot */
  forward_limit: number = 0;
}

/** Decode a TDLib `addChatMember` object; null input -> null. */
export function decodeAddChatMember(json: TdJsonRaw): AddChatMember | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddChatMember = new AddChatMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.user_id = asNumber(record['user_id']);
  out.forward_limit = asNumber(record['forward_limit']);
  return out;
}

/** Encode a `addChatMember` into TDLib JSON wire shape. */
export function encodeAddChatMember(value: AddChatMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addChatMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['user_id'] = value.user_id;
  out['forward_limit'] = value.forward_limit;
  return out;
}

/** addChatMembers = FailedToAddMembers — Adds multiple new members to a chat; requires can_invite_users member right. Currently, this method is available only in supergroups and channels. This method can't be used to join a chat. Members ca... */
export class AddChatMembers {
  type: 'addChatMembers' = 'addChatMembers';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** user_ids: vector<int53> — Identifiers of the users to be added to the chat. The maximum number of added users is 20 for supergroups and 100 for channels */
  user_ids: number[] = [];
}

/** Decode a TDLib `addChatMembers` object; null input -> null. */
export function decodeAddChatMembers(json: TdJsonRaw): AddChatMembers | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddChatMembers = new AddChatMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.user_ids = decodeVector(record['user_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `addChatMembers` into TDLib JSON wire shape. */
export function encodeAddChatMembers(value: AddChatMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addChatMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['user_ids'] = encodeVector(value.user_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** addChatToList = Ok — Adds a chat to a chat list. A chat can't be simultaneously in Main and Archive chat lists, so it is automatically removed from another one if needed */
export class AddChatToList {
  type: 'addChatToList' = 'addChatToList';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** chat_list: ChatList — The chat list. Use getChatListsToAddChat to get suitable chat lists */
  chat_list: TdChatList | null = null;
}

/** Decode a TDLib `addChatToList` object; null input -> null. */
export function decodeAddChatToList(json: TdJsonRaw): AddChatToList | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddChatToList = new AddChatToList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.chat_list = decodeTdChatList(record['chat_list']);
  return out;
}

/** Encode a `addChatToList` into TDLib JSON wire shape. */
export function encodeAddChatToList(value: AddChatToList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addChatToList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['chat_list'] = value.chat_list === null ? null : encodeTdChatList(value.chat_list);
  return out;
}

/** addChatWelcomeMessage = Ok — Adds a message to the list of welcome messages of a chat; requires can_send_welcome_messages administrator right in the chat. There can be up to getOption("welcome_message_count_max") welcome messages in a ch... */
export class AddChatWelcomeMessage {
  type: 'addChatWelcomeMessage' = 'addChatWelcomeMessage';
  extra?: string;
  /** chat_id: int53 — The identifier of the chat */
  chat_id: number = 0;
  /** input_message_content: InputMessageContent — The content of the message to be sent. Must be one of the following types: inputMessageText, inputMessageAnimation, */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `addChatWelcomeMessage` object; null input -> null. */
export function decodeAddChatWelcomeMessage(json: TdJsonRaw): AddChatWelcomeMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddChatWelcomeMessage = new AddChatWelcomeMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `addChatWelcomeMessage` into TDLib JSON wire shape. */
export function encodeAddChatWelcomeMessage(value: AddChatWelcomeMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addChatWelcomeMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** addChecklistTasks = Ok — Adds tasks to a checklist in a message */
export class AddChecklistTasks {
  type: 'addChecklistTasks' = 'addChecklistTasks';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat with the message */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the checklist. Use messageProperties.can_add_tasks to check whether the tasks can be added */
  message_id: number = 0;
  /** tasks: vector<inputChecklistTask> — List of added tasks */
  tasks: InputChecklistTask[] = [];
}

/** Decode a TDLib `addChecklistTasks` object; null input -> null. */
export function decodeAddChecklistTasks(json: TdJsonRaw): AddChecklistTasks | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddChecklistTasks = new AddChecklistTasks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.tasks = decodeVector(record['tasks'], (item: TdJsonRaw): InputChecklistTask => decodeInputChecklistTask(item) as InputChecklistTask);
  return out;
}

/** Encode a `addChecklistTasks` into TDLib JSON wire shape. */
export function encodeAddChecklistTasks(value: AddChecklistTasks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addChecklistTasks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['tasks'] = encodeVector(value.tasks, (item: InputChecklistTask | null): TdJsonRaw => item === null ? null : encodeInputChecklistTask(item));
  return out;
}

/** addContact = Ok — Adds a user to the contact list or edits an existing contact by their user identifier Use the field userFullInfo.need_phone_number_privacy_exception to check whether the current user needs to be asked to share their pho... */
export class AddContact {
  type: 'addContact' = 'addContact';
  extra?: string;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
  /** contact: importedContact — The contact to add or edit; phone number may be empty and needs to be specified only if known */
  contact: ImportedContact | null = null;
  /** share_phone_number: Bool — Pass true to share the current user's phone number with the new contact. A corresponding rule to userPrivacySettingShowPhoneNumber will be added if needed. */
  share_phone_number: boolean = false;
}

/** Decode a TDLib `addContact` object; null input -> null. */
export function decodeAddContact(json: TdJsonRaw): AddContact | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddContact = new AddContact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.contact = decodeImportedContact(record['contact']);
  out.share_phone_number = asBool(record['share_phone_number']);
  return out;
}

/** Encode a `addContact` into TDLib JSON wire shape. */
export function encodeAddContact(value: AddContact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addContact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['contact'] = value.contact === null ? null : encodeImportedContact(value.contact);
  out['share_phone_number'] = value.share_phone_number;
  return out;
}

/** addCustomServerLanguagePack = Ok — Adds a custom server language pack to the list of installed language packs in current localization target. Can be called before authorization */
export class AddCustomServerLanguagePack {
  type: 'addCustomServerLanguagePack' = 'addCustomServerLanguagePack';
  extra?: string;
  /** language_pack_id: string — Identifier of a language pack to be added */
  language_pack_id: string = '';
}

/** Decode a TDLib `addCustomServerLanguagePack` object; null input -> null. */
export function decodeAddCustomServerLanguagePack(json: TdJsonRaw): AddCustomServerLanguagePack | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddCustomServerLanguagePack = new AddCustomServerLanguagePack();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.language_pack_id = asString(record['language_pack_id']);
  return out;
}

/** Encode a `addCustomServerLanguagePack` into TDLib JSON wire shape. */
export function encodeAddCustomServerLanguagePack(value: AddCustomServerLanguagePack): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addCustomServerLanguagePack';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['language_pack_id'] = value.language_pack_id;
  return out;
}

/** addFavoriteSticker = Ok — Adds a new sticker to the list of favorite stickers. The new sticker is added to the top of the list. If the sticker was already in the list, it is removed from the list first. Only stickers belonging to a stick... */
export class AddFavoriteSticker {
  type: 'addFavoriteSticker' = 'addFavoriteSticker';
  extra?: string;
  /** sticker: InputFile — Sticker file to add */
  sticker: TdInputFile | null = null;
}

/** Decode a TDLib `addFavoriteSticker` object; null input -> null. */
export function decodeAddFavoriteSticker(json: TdJsonRaw): AddFavoriteSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddFavoriteSticker = new AddFavoriteSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeTdInputFile(record['sticker']);
  return out;
}

/** Encode a `addFavoriteSticker` into TDLib JSON wire shape. */
export function encodeAddFavoriteSticker(value: AddFavoriteSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addFavoriteSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  return out;
}

/** addFileToDownloads = File — Adds a file from a message to the list of file downloads. Download progress and completion of the download will be notified through updateFile updates. If message database is used, the list of file downloads i... */
export class AddFileToDownloads {
  type: 'addFileToDownloads' = 'addFileToDownloads';
  extra?: string;
  /** file_id: int32 — Identifier of the file to download */
  file_id: number = 0;
  /** chat_id: int53 — Chat identifier of the message with the file */
  chat_id: number = 0;
  /** message_id: int53 — Message identifier */
  message_id: number = 0;
  /** priority: int32 — Priority of the download (1-32). The higher the priority, the earlier the file will be downloaded. If the priorities of two files are equal, then the last one for which downloadFile/addFileToDownloads was called will be... */
  priority: number = 0;
}

/** Decode a TDLib `addFileToDownloads` object; null input -> null. */
export function decodeAddFileToDownloads(json: TdJsonRaw): AddFileToDownloads | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddFileToDownloads = new AddFileToDownloads();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_id = asNumber(record['file_id']);
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.priority = asNumber(record['priority']);
  return out;
}

/** Encode a `addFileToDownloads` into TDLib JSON wire shape. */
export function encodeAddFileToDownloads(value: AddFileToDownloads): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addFileToDownloads';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_id'] = value.file_id;
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['priority'] = value.priority;
  return out;
}

/** addGiftCollectionGifts = GiftCollection — Adds gifts to the beginning of a previously created collection. If the collection is owned by a channel chat, then requires can_post_messages administrator right in the channel chat. Returns the ... */
export class AddGiftCollectionGifts {
  type: 'addGiftCollectionGifts' = 'addGiftCollectionGifts';
  extra?: string;
  /** owner_id: MessageSender — Identifier of the user or the channel chat that owns the collection */
  owner_id: TdMessageSender | null = null;
  /** collection_id: int32 — Identifier of the gift collection */
  collection_id: number = 0;
  /** received_gift_ids: vector<string> — Identifier of the gifts to add to the collection; 1-getOption("gift_collection_size_max") identifiers. */
  received_gift_ids: string[] = [];
}

/** Decode a TDLib `addGiftCollectionGifts` object; null input -> null. */
export function decodeAddGiftCollectionGifts(json: TdJsonRaw): AddGiftCollectionGifts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddGiftCollectionGifts = new AddGiftCollectionGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.owner_id = decodeTdMessageSender(record['owner_id']);
  out.collection_id = asNumber(record['collection_id']);
  out.received_gift_ids = decodeVector(record['received_gift_ids'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `addGiftCollectionGifts` into TDLib JSON wire shape. */
export function encodeAddGiftCollectionGifts(value: AddGiftCollectionGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addGiftCollectionGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['owner_id'] = value.owner_id === null ? null : encodeTdMessageSender(value.owner_id);
  out['collection_id'] = value.collection_id;
  out['received_gift_ids'] = encodeVector(value.received_gift_ids, (item: string): TdJsonRaw => item);
  return out;
}

/** addLocalMessage = Message — Adds a local message to a chat. The message is persistent across application restarts only if the message database is used. Returns the added message */
export class AddLocalMessage {
  type: 'addLocalMessage' = 'addLocalMessage';
  extra?: string;
  /** chat_id: int53 — Target chat; channel direct messages chats aren't supported */
  chat_id: number = 0;
  /** sender_id: MessageSender — Identifier of the sender of the message */
  sender_id: TdMessageSender | null = null;
  /** reply_to: InputMessageReplyTo — Information about the message or story to be replied; pass null if none */
  reply_to: TdInputMessageReplyTo | null = null;
  /** disable_notification: Bool — Pass true to disable notification for the message */
  disable_notification: boolean = false;
  /** input_message_content: InputMessageContent — The content of the message to be added */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `addLocalMessage` object; null input -> null. */
export function decodeAddLocalMessage(json: TdJsonRaw): AddLocalMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddLocalMessage = new AddLocalMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.reply_to = decodeTdInputMessageReplyTo(record['reply_to']);
  out.disable_notification = asBool(record['disable_notification']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `addLocalMessage` into TDLib JSON wire shape. */
export function encodeAddLocalMessage(value: AddLocalMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addLocalMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['reply_to'] = value.reply_to === null ? null : encodeTdInputMessageReplyTo(value.reply_to);
  out['disable_notification'] = value.disable_notification;
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** addLogMessage = Ok — Adds a message to TDLib internal log. Can be called synchronously */
export class AddLogMessage {
  type: 'addLogMessage' = 'addLogMessage';
  extra?: string;
  /** verbosity_level: int32 — The minimum verbosity level needed for the message to be logged; 0-1023 */
  verbosity_level: number = 0;
  /** text: string — Text of a message to log */
  text: string = '';
}

/** Decode a TDLib `addLogMessage` object; null input -> null. */
export function decodeAddLogMessage(json: TdJsonRaw): AddLogMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddLogMessage = new AddLogMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.verbosity_level = asNumber(record['verbosity_level']);
  out.text = asString(record['text']);
  return out;
}

/** Encode a `addLogMessage` into TDLib JSON wire shape. */
export function encodeAddLogMessage(value: AddLogMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addLogMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['verbosity_level'] = value.verbosity_level;
  out['text'] = value.text;
  return out;
}

/** addLoginPasskey = Passkey — Adds a passkey allowed to be used for the login by the current user and returns the added passkey. Call getPasskeyParameters to get parameters for creating of the passkey */
export class AddLoginPasskey {
  type: 'addLoginPasskey' = 'addLoginPasskey';
  extra?: string;
  /** client_data: string — JSON-encoded client data */
  client_data: string = '';
  /** attestation_object: bytes — Passkey attestation object */
  attestation_object: string = '';
}

/** Decode a TDLib `addLoginPasskey` object; null input -> null. */
export function decodeAddLoginPasskey(json: TdJsonRaw): AddLoginPasskey | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddLoginPasskey = new AddLoginPasskey();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.client_data = asString(record['client_data']);
  out.attestation_object = asString(record['attestation_object']);
  return out;
}

/** Encode a `addLoginPasskey` into TDLib JSON wire shape. */
export function encodeAddLoginPasskey(value: AddLoginPasskey): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addLoginPasskey';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['client_data'] = value.client_data;
  out['attestation_object'] = value.attestation_object;
  return out;
}

/** addMessageReaction = Ok — Adds a reaction or a tag to a message. Use getMessageAvailableReactions to receive the list of available reactions for the message */
export class AddMessageReaction {
  type: 'addMessageReaction' = 'addMessageReaction';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** reaction_type: ReactionType — Type of the reaction to add. Use addPendingPaidMessageReaction instead to add the paid reaction */
  reaction_type: TdReactionType | null = null;
  /** is_big: Bool — Pass true if the reaction is added with a big animation */
  is_big: boolean = false;
  /** update_recent_reactions: Bool — Pass true if the reaction needs to be added to recent reactions; tags are never added to the list of recent reactions */
  update_recent_reactions: boolean = false;
}

/** Decode a TDLib `addMessageReaction` object; null input -> null. */
export function decodeAddMessageReaction(json: TdJsonRaw): AddMessageReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddMessageReaction = new AddMessageReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.reaction_type = decodeTdReactionType(record['reaction_type']);
  out.is_big = asBool(record['is_big']);
  out.update_recent_reactions = asBool(record['update_recent_reactions']);
  return out;
}

/** Encode a `addMessageReaction` into TDLib JSON wire shape. */
export function encodeAddMessageReaction(value: AddMessageReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addMessageReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['reaction_type'] = value.reaction_type === null ? null : encodeTdReactionType(value.reaction_type);
  out['is_big'] = value.is_big;
  out['update_recent_reactions'] = value.update_recent_reactions;
  return out;
}

/** addNetworkStatistics = Ok — Adds the specified data to data usage statistics. Can be called before authorization */
export class AddNetworkStatistics {
  type: 'addNetworkStatistics' = 'addNetworkStatistics';
  extra?: string;
  /** entry: NetworkStatisticsEntry — The network statistics entry with the data to be added to statistics */
  entry: TdNetworkStatisticsEntry | null = null;
}

/** Decode a TDLib `addNetworkStatistics` object; null input -> null. */
export function decodeAddNetworkStatistics(json: TdJsonRaw): AddNetworkStatistics | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddNetworkStatistics = new AddNetworkStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.entry = decodeTdNetworkStatisticsEntry(record['entry']);
  return out;
}

/** Encode a `addNetworkStatistics` into TDLib JSON wire shape. */
export function encodeAddNetworkStatistics(value: AddNetworkStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addNetworkStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['entry'] = value.entry === null ? null : encodeTdNetworkStatisticsEntry(value.entry);
  return out;
}

/** addOffer = Message — Sends a suggested post based on a previously sent message in a channel direct messages chat. Can be also used to suggest price or time change for an existing suggested post. Returns the sent message or messagePropert... */
export class AddOffer {
  type: 'addOffer' = 'addOffer';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message in the chat which will be sent as suggested post. Use messageProperties.can_add_offer to check whether an offer can be added */
  message_id: number = 0;
  /** options: messageSendOptions — Options to be used to send the message. New information about the suggested post must always be specified */
  options: MessageSendOptions | null = null;
}

/** Decode a TDLib `addOffer` object; null input -> null. */
export function decodeAddOffer(json: TdJsonRaw): AddOffer | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddOffer = new AddOffer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.options = decodeMessageSendOptions(record['options']);
  return out;
}

/** Encode a `addOffer` into TDLib JSON wire shape. */
export function encodeAddOffer(value: AddOffer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addOffer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['options'] = value.options === null ? null : encodeMessageSendOptions(value.options);
  return out;
}

/** addPendingLiveStoryReaction = Ok — Adds pending paid reaction in a live story group call. Can't be used in live stories posted by the current user. Call commitPendingLiveStoryReactions or removePendingLiveStoryReactions to actually send ... */
export class AddPendingLiveStoryReaction {
  type: 'addPendingLiveStoryReaction' = 'addPendingLiveStoryReaction';
  extra?: string;
  /** group_call_id: int32 — Group call identifier */
  group_call_id: number = 0;
  /** star_count: int53 — Number of Telegram Stars to be used for the reaction. The total number of pending paid reactions must not exceed getOption("paid_group_call_message_star_count_max") */
  star_count: number = 0;
}

/** Decode a TDLib `addPendingLiveStoryReaction` object; null input -> null. */
export function decodeAddPendingLiveStoryReaction(json: TdJsonRaw): AddPendingLiveStoryReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddPendingLiveStoryReaction = new AddPendingLiveStoryReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.group_call_id = asNumber(record['group_call_id']);
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `addPendingLiveStoryReaction` into TDLib JSON wire shape. */
export function encodeAddPendingLiveStoryReaction(value: AddPendingLiveStoryReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addPendingLiveStoryReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['group_call_id'] = value.group_call_id;
  out['star_count'] = value.star_count;
  return out;
}

/** addPendingPaidMessageReaction = Ok — Adds the paid message reaction to a message. Use getMessageAvailableReactions to check whether the reaction is available for the message */
export class AddPendingPaidMessageReaction {
  type: 'addPendingPaidMessageReaction' = 'addPendingPaidMessageReaction';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the message belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message */
  message_id: number = 0;
  /** star_count: int53 — Number of Telegram Stars to be used for the reaction. The total number of pending paid reactions must not exceed getOption("paid_reaction_star_count_max") */
  star_count: number = 0;
  /** type: PaidReactionType — Type of the paid reaction; pass null if the user didn't choose reaction type explicitly, for example, the reaction is set from the message bubble */
  type_: TdPaidReactionType | null = null;
}

/** Decode a TDLib `addPendingPaidMessageReaction` object; null input -> null. */
export function decodeAddPendingPaidMessageReaction(json: TdJsonRaw): AddPendingPaidMessageReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddPendingPaidMessageReaction = new AddPendingPaidMessageReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.star_count = asNumber(record['star_count']);
  out.type_ = decodeTdPaidReactionType(record['type']);
  return out;
}

/** Encode a `addPendingPaidMessageReaction` into TDLib JSON wire shape. */
export function encodeAddPendingPaidMessageReaction(value: AddPendingPaidMessageReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addPendingPaidMessageReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['star_count'] = value.star_count;
  out['type'] = value.type_ === null ? null : encodeTdPaidReactionType(value.type_);
  return out;
}

/** addPollOption = Ok — Adds an option to a poll */
export class AddPollOption {
  type: 'addPollOption' = 'addPollOption';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to which the poll belongs */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message containing the poll. Use messagePoll.can_add_option to check whether an option can be added */
  message_id: number = 0;
  /** option: inputPollOption — The new option */
  option: InputPollOption | null = null;
}

/** Decode a TDLib `addPollOption` object; null input -> null. */
export function decodeAddPollOption(json: TdJsonRaw): AddPollOption | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddPollOption = new AddPollOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.option = decodeInputPollOption(record['option']);
  return out;
}

/** Encode a `addPollOption` into TDLib JSON wire shape. */
export function encodeAddPollOption(value: AddPollOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addPollOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['option'] = value.option === null ? null : encodeInputPollOption(value.option);
  return out;
}

/** addProfileAudio = Ok — Adds an audio file to the beginning of the profile audio files of the current user */
export class AddProfileAudio {
  type: 'addProfileAudio' = 'addProfileAudio';
  extra?: string;
  /** audio: inputAudio — The audio to add */
  audio: InputAudio | null = null;
}

/** Decode a TDLib `addProfileAudio` object; null input -> null. */
export function decodeAddProfileAudio(json: TdJsonRaw): AddProfileAudio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddProfileAudio = new AddProfileAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeInputAudio(record['audio']);
  return out;
}

/** Encode a `addProfileAudio` into TDLib JSON wire shape. */
export function encodeAddProfileAudio(value: AddProfileAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addProfileAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeInputAudio(value.audio);
  return out;
}

/** addProxy = AddedProxy — Adds a proxy server for network requests. Can be called before authorization */
export class AddProxy {
  type: 'addProxy' = 'addProxy';
  extra?: string;
  /** proxy: proxy — The proxy to add */
  proxy: ProxyValue | null = null;
  /** enable: Bool — Pass true to immediately enable the proxy */
  enable: boolean = false;
  /** comment: string — Comment to set for the proxy */
  comment: string = '';
}

/** Decode a TDLib `addProxy` object; null input -> null. */
export function decodeAddProxy(json: TdJsonRaw): AddProxy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddProxy = new AddProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.proxy = decodeProxyValue(record['proxy']);
  out.enable = asBool(record['enable']);
  out.comment = asString(record['comment']);
  return out;
}

/** Encode a `addProxy` into TDLib JSON wire shape. */
export function encodeAddProxy(value: AddProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['proxy'] = value.proxy === null ? null : encodeProxyValue(value.proxy);
  out['enable'] = value.enable;
  out['comment'] = value.comment;
  return out;
}

/** addQuickReplyShortcutInlineQueryResultMessage = QuickReplyMessage — Adds a message to a quick reply shortcut via inline bot. If shortcut doesn't exist and there are less than getOption("quick_reply_shortcut_count_max") shortcuts, then a ... */
export class AddQuickReplyShortcutInlineQueryResultMessage {
  type: 'addQuickReplyShortcutInlineQueryResultMessage' = 'addQuickReplyShortcutInlineQueryResultMessage';
  extra?: string;
  /** shortcut_name: string — Name of the target shortcut */
  shortcut_name: string = '';
  /** reply_to_message_id: int53 — Identifier of a quick reply message in the same shortcut to be replied; pass 0 if none */
  reply_to_message_id: number = 0;
  /** query_id: int64 — Identifier of the inline query */
  query_id: string = '';
  /** result_id: string — Identifier of the inline query result */
  result_id: string = '';
  /** hide_via_bot: Bool — Pass true to hide the bot, via which the message is sent. Can be used only for bots getOption("animation_search_bot_username"), getOption("photo_search_bot_username"), and getOption("venue_search_bot_username") */
  hide_via_bot: boolean = false;
}

/** Decode a TDLib `addQuickReplyShortcutInlineQueryResultMessage` object; null input -> null. */
export function decodeAddQuickReplyShortcutInlineQueryResultMessage(json: TdJsonRaw): AddQuickReplyShortcutInlineQueryResultMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddQuickReplyShortcutInlineQueryResultMessage = new AddQuickReplyShortcutInlineQueryResultMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_name = asString(record['shortcut_name']);
  out.reply_to_message_id = asNumber(record['reply_to_message_id']);
  out.query_id = asInt64(record['query_id']);
  out.result_id = asString(record['result_id']);
  out.hide_via_bot = asBool(record['hide_via_bot']);
  return out;
}

/** Encode a `addQuickReplyShortcutInlineQueryResultMessage` into TDLib JSON wire shape. */
export function encodeAddQuickReplyShortcutInlineQueryResultMessage(value: AddQuickReplyShortcutInlineQueryResultMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addQuickReplyShortcutInlineQueryResultMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_name'] = value.shortcut_name;
  out['reply_to_message_id'] = value.reply_to_message_id;
  out['query_id'] = value.query_id;
  out['result_id'] = value.result_id;
  out['hide_via_bot'] = value.hide_via_bot;
  return out;
}

/** addQuickReplyShortcutMessage = QuickReplyMessage — Adds a message to a quick reply shortcut. If shortcut doesn't exist and there are less than getOption("quick_reply_shortcut_count_max") shortcuts, then a new shortcut is created. The sho... */
export class AddQuickReplyShortcutMessage {
  type: 'addQuickReplyShortcutMessage' = 'addQuickReplyShortcutMessage';
  extra?: string;
  /** shortcut_name: string — Name of the target shortcut */
  shortcut_name: string = '';
  /** reply_to_message_id: int53 — Identifier of a quick reply message in the same shortcut to be replied; pass 0 if none */
  reply_to_message_id: number = 0;
  /** input_message_content: InputMessageContent — The content of the message to be added; inputMessagePaidMedia, inputMessageForwarded and inputMessageLiveLocation */
  input_message_content: TdInputMessageContent | null = null;
}

/** Decode a TDLib `addQuickReplyShortcutMessage` object; null input -> null. */
export function decodeAddQuickReplyShortcutMessage(json: TdJsonRaw): AddQuickReplyShortcutMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddQuickReplyShortcutMessage = new AddQuickReplyShortcutMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_name = asString(record['shortcut_name']);
  out.reply_to_message_id = asNumber(record['reply_to_message_id']);
  out.input_message_content = decodeTdInputMessageContent(record['input_message_content']);
  return out;
}

/** Encode a `addQuickReplyShortcutMessage` into TDLib JSON wire shape. */
export function encodeAddQuickReplyShortcutMessage(value: AddQuickReplyShortcutMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addQuickReplyShortcutMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_name'] = value.shortcut_name;
  out['reply_to_message_id'] = value.reply_to_message_id;
  out['input_message_content'] = value.input_message_content === null ? null : encodeTdInputMessageContent(value.input_message_content);
  return out;
}

/** addQuickReplyShortcutMessageAlbum = QuickReplyMessages — Adds 2-10 messages grouped together into an album to a quick reply shortcut. Currently, only audio, document, photo and video messages can be grouped into an album. Documents and a... */
export class AddQuickReplyShortcutMessageAlbum {
  type: 'addQuickReplyShortcutMessageAlbum' = 'addQuickReplyShortcutMessageAlbum';
  extra?: string;
  /** shortcut_name: string — Name of the target shortcut */
  shortcut_name: string = '';
  /** reply_to_message_id: int53 — Identifier of a quick reply message in the same shortcut to be replied; pass 0 if none */
  reply_to_message_id: number = 0;
  /** input_message_contents: vector<InputMessageContent> — Contents of messages to be sent. At most 10 messages can be added to an album. All messages must have the same value of show_caption_above_media */
  input_message_contents: TdInputMessageContent[] = [];
}

/** Decode a TDLib `addQuickReplyShortcutMessageAlbum` object; null input -> null. */
export function decodeAddQuickReplyShortcutMessageAlbum(json: TdJsonRaw): AddQuickReplyShortcutMessageAlbum | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddQuickReplyShortcutMessageAlbum = new AddQuickReplyShortcutMessageAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shortcut_name = asString(record['shortcut_name']);
  out.reply_to_message_id = asNumber(record['reply_to_message_id']);
  out.input_message_contents = decodeVector(record['input_message_contents'], (item: TdJsonRaw): TdInputMessageContent => decodeTdInputMessageContent(item) as TdInputMessageContent);
  return out;
}

/** Encode a `addQuickReplyShortcutMessageAlbum` into TDLib JSON wire shape. */
export function encodeAddQuickReplyShortcutMessageAlbum(value: AddQuickReplyShortcutMessageAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addQuickReplyShortcutMessageAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shortcut_name'] = value.shortcut_name;
  out['reply_to_message_id'] = value.reply_to_message_id;
  out['input_message_contents'] = encodeVector(value.input_message_contents, (item: TdInputMessageContent | null): TdJsonRaw => item === null ? null : encodeTdInputMessageContent(item));
  return out;
}

/** addRecentSticker = Stickers — Manually adds a new sticker to the list of recently used stickers. The new sticker is added to the top of the list. If the sticker was already in the list, it is removed from the list first. Only stickers be... */
export class AddRecentSticker {
  type: 'addRecentSticker' = 'addRecentSticker';
  extra?: string;
  /** is_attached: Bool — Pass true to add the sticker to the list of stickers recently attached to photo or video files; pass false to add the sticker to the list of recently sent stickers */
  is_attached: boolean = false;
  /** sticker: InputFile — Sticker file to add */
  sticker: TdInputFile | null = null;
}

/** Decode a TDLib `addRecentSticker` object; null input -> null. */
export function decodeAddRecentSticker(json: TdJsonRaw): AddRecentSticker | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddRecentSticker = new AddRecentSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_attached = asBool(record['is_attached']);
  out.sticker = decodeTdInputFile(record['sticker']);
  return out;
}

/** Encode a `addRecentSticker` into TDLib JSON wire shape. */
export function encodeAddRecentSticker(value: AddRecentSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addRecentSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_attached'] = value.is_attached;
  out['sticker'] = value.sticker === null ? null : encodeTdInputFile(value.sticker);
  return out;
}

/** addRecentlyFoundChat = Ok — Adds a chat to the list of recently found chats. The chat is added to the beginning of the list. If the chat is already in the list, it will be removed from the list first */
export class AddRecentlyFoundChat {
  type: 'addRecentlyFoundChat' = 'addRecentlyFoundChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat to add */
  chat_id: number = 0;
}

/** Decode a TDLib `addRecentlyFoundChat` object; null input -> null. */
export function decodeAddRecentlyFoundChat(json: TdJsonRaw): AddRecentlyFoundChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddRecentlyFoundChat = new AddRecentlyFoundChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `addRecentlyFoundChat` into TDLib JSON wire shape. */
export function encodeAddRecentlyFoundChat(value: AddRecentlyFoundChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addRecentlyFoundChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** addSavedAnimation = Ok — Manually adds a new animation to the list of saved animations. The new animation is added to the beginning of the list. If the animation was already in the list, it is removed first. Only non-secret video animati... */
export class AddSavedAnimation {
  type: 'addSavedAnimation' = 'addSavedAnimation';
  extra?: string;
  /** animation: InputFile — The animation file to be added. Only animations known to the server (i.e., successfully sent via a message) can be added to the list */
  animation: TdInputFile | null = null;
}

/** Decode a TDLib `addSavedAnimation` object; null input -> null. */
export function decodeAddSavedAnimation(json: TdJsonRaw): AddSavedAnimation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddSavedAnimation = new AddSavedAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeTdInputFile(record['animation']);
  return out;
}

/** Encode a `addSavedAnimation` into TDLib JSON wire shape. */
export function encodeAddSavedAnimation(value: AddSavedAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addSavedAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeTdInputFile(value.animation);
  return out;
}

/** addSavedNotificationSound = NotificationSound — Adds a new notification sound to the list of saved notification sounds. The new notification sound is added to the top of the list. If it is already in the list, its position isn't changed */
export class AddSavedNotificationSound {
  type: 'addSavedNotificationSound' = 'addSavedNotificationSound';
  extra?: string;
  /** sound: InputFile — Notification sound file to add */
  sound: TdInputFile | null = null;
}

/** Decode a TDLib `addSavedNotificationSound` object; null input -> null. */
export function decodeAddSavedNotificationSound(json: TdJsonRaw): AddSavedNotificationSound | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddSavedNotificationSound = new AddSavedNotificationSound();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sound = decodeTdInputFile(record['sound']);
  return out;
}

/** Encode a `addSavedNotificationSound` into TDLib JSON wire shape. */
export function encodeAddSavedNotificationSound(value: AddSavedNotificationSound): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addSavedNotificationSound';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sound'] = value.sound === null ? null : encodeTdInputFile(value.sound);
  return out;
}

/** addStickerToSet = Ok — Adds a new sticker to a set */
export class AddStickerToSet {
  type: 'addStickerToSet' = 'addStickerToSet';
  extra?: string;
  /** user_id: int53 — Sticker set owner; ignored for regular users */
  user_id: number = 0;
  /** name: string — Sticker set name. The sticker set must be owned by the current user, and contain less than 200 stickers for custom emoji sticker sets and less than 120 otherwise */
  name: string = '';
  /** sticker: newSticker — Sticker to add to the set */
  sticker: NewSticker | null = null;
}

/** Decode a TDLib `addStickerToSet` object; null input -> null. */
export function decodeAddStickerToSet(json: TdJsonRaw): AddStickerToSet | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddStickerToSet = new AddStickerToSet();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.name = asString(record['name']);
  out.sticker = decodeNewSticker(record['sticker']);
  return out;
}

/** Encode a `addStickerToSet` into TDLib JSON wire shape. */
export function encodeAddStickerToSet(value: AddStickerToSet): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addStickerToSet';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['name'] = value.name;
  out['sticker'] = value.sticker === null ? null : encodeNewSticker(value.sticker);
  return out;
}

/** addStoryAlbumStories = StoryAlbum — Adds stories to the beginning of a previously created story album. If the album is owned by a supergroup or a channel chat, then requires can_edit_stories administrator right in the chat. Returns the c... */
export class AddStoryAlbumStories {
  type: 'addStoryAlbumStories' = 'addStoryAlbumStories';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that owns the stories */
  chat_id: number = 0;
  /** story_album_id: int32 — Identifier of the story album */
  story_album_id: number = 0;
  /** story_ids: vector<int32> — Identifier of the stories to add to the album; 1-getOption("story_album_size_max") identifiers. */
  story_ids: number[] = [];
}

/** Decode a TDLib `addStoryAlbumStories` object; null input -> null. */
export function decodeAddStoryAlbumStories(json: TdJsonRaw): AddStoryAlbumStories | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddStoryAlbumStories = new AddStoryAlbumStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.story_album_id = asNumber(record['story_album_id']);
  out.story_ids = decodeVector(record['story_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `addStoryAlbumStories` into TDLib JSON wire shape. */
export function encodeAddStoryAlbumStories(value: AddStoryAlbumStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addStoryAlbumStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['story_album_id'] = value.story_album_id;
  out['story_ids'] = encodeVector(value.story_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** addTextCompositionStyle = Ok — Adds a custom text composition style to the list of used by the user styles. May return an error with a message "TONES_SAVED_TOO_MANY" if the maximum number of added custom styles getOption("added_text_comp... */
export class AddTextCompositionStyle {
  type: 'addTextCompositionStyle' = 'addTextCompositionStyle';
  extra?: string;
  /** name: string — Name of the style */
  name: string = '';
}

/** Decode a TDLib `addTextCompositionStyle` object; null input -> null. */
export function decodeAddTextCompositionStyle(json: TdJsonRaw): AddTextCompositionStyle | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddTextCompositionStyle = new AddTextCompositionStyle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `addTextCompositionStyle` into TDLib JSON wire shape. */
export function encodeAddTextCompositionStyle(value: AddTextCompositionStyle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addTextCompositionStyle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** addWebBrowserSettingsException = Ok — Adds a special handling for the opening of the specified URL */
export class AddWebBrowserSettingsException {
  type: 'addWebBrowserSettingsException' = 'addWebBrowserSettingsException';
  extra?: string;
  /** open_external_browser: Bool — Pass true if the specified website must be opened in an external browser; pass false to open it in the in-app browser. There can be at most 100 exceptions in each list of the exceptions */
  open_external_browser: boolean = false;
  /** url: string — URL of the website */
  url: string = '';
}

/** Decode a TDLib `addWebBrowserSettingsException` object; null input -> null. */
export function decodeAddWebBrowserSettingsException(json: TdJsonRaw): AddWebBrowserSettingsException | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddWebBrowserSettingsException = new AddWebBrowserSettingsException();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.open_external_browser = asBool(record['open_external_browser']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `addWebBrowserSettingsException` into TDLib JSON wire shape. */
export function encodeAddWebBrowserSettingsException(value: AddWebBrowserSettingsException): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addWebBrowserSettingsException';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['open_external_browser'] = value.open_external_browser;
  out['url'] = value.url;
  return out;
}

/** addedProxies = AddedProxies — Represents a list of added proxy servers */
export class AddedProxies {
  type: 'addedProxies' = 'addedProxies';
  extra?: string;
  /** proxies: vector<addedProxy> — List of proxy servers */
  proxies: AddedProxy[] = [];
}

/** Decode a TDLib `addedProxies` object; null input -> null. */
export function decodeAddedProxies(json: TdJsonRaw): AddedProxies | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddedProxies = new AddedProxies();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.proxies = decodeVector(record['proxies'], (item: TdJsonRaw): AddedProxy => decodeAddedProxy(item) as AddedProxy);
  return out;
}

/** Encode a `addedProxies` into TDLib JSON wire shape. */
export function encodeAddedProxies(value: AddedProxies): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addedProxies';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['proxies'] = encodeVector(value.proxies, (item: AddedProxy | null): TdJsonRaw => item === null ? null : encodeAddedProxy(item));
  return out;
}

/** addedProxy = AddedProxy — Contains information about a proxy server added to the list of proxies */
export class AddedProxy {
  type: 'addedProxy' = 'addedProxy';
  extra?: string;
  /** id: int32 — Unique identifier of the proxy */
  id: number = 0;
  /** last_used_date: int32 — Point in time (Unix timestamp) when the proxy was last used; 0 if never */
  last_used_date: number = 0;
  /** is_enabled: Bool — True, if the proxy is enabled now */
  is_enabled: boolean = false;
  /** comment: string — Comment for the proxy added by the user */
  comment: string = '';
  /** proxy: proxy — The proxy */
  proxy: ProxyValue | null = null;
}

/** Decode a TDLib `addedProxy` object; null input -> null. */
export function decodeAddedProxy(json: TdJsonRaw): AddedProxy | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddedProxy = new AddedProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.last_used_date = asNumber(record['last_used_date']);
  out.is_enabled = asBool(record['is_enabled']);
  out.comment = asString(record['comment']);
  out.proxy = decodeProxyValue(record['proxy']);
  return out;
}

/** Encode a `addedProxy` into TDLib JSON wire shape. */
export function encodeAddedProxy(value: AddedProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addedProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['last_used_date'] = value.last_used_date;
  out['is_enabled'] = value.is_enabled;
  out['comment'] = value.comment;
  out['proxy'] = value.proxy === null ? null : encodeProxyValue(value.proxy);
  return out;
}

/** addedReaction = AddedReaction — Represents a reaction applied to a message */
export class AddedReaction {
  type: 'addedReaction' = 'addedReaction';
  extra?: string;
  /** type: ReactionType — Type of the reaction */
  type_: TdReactionType | null = null;
  /** sender_id: MessageSender — Identifier of the chat member, applied the reaction */
  sender_id: TdMessageSender | null = null;
  /** is_outgoing: Bool — True, if the reaction was added by the current user */
  is_outgoing: boolean = false;
  /** date: int32 — Point in time (Unix timestamp) when the reaction was added */
  date: number = 0;
}

/** Decode a TDLib `addedReaction` object; null input -> null. */
export function decodeAddedReaction(json: TdJsonRaw): AddedReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddedReaction = new AddedReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdReactionType(record['type']);
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.is_outgoing = asBool(record['is_outgoing']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `addedReaction` into TDLib JSON wire shape. */
export function encodeAddedReaction(value: AddedReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addedReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdReactionType(value.type_);
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['is_outgoing'] = value.is_outgoing;
  out['date'] = value.date;
  return out;
}

/** addedReactions = AddedReactions — Represents a list of reactions added to a message */
export class AddedReactions {
  type: 'addedReactions' = 'addedReactions';
  extra?: string;
  /** total_count: int32 — The total number of found reactions */
  total_count: number = 0;
  /** reactions: vector<addedReaction> — The list of added reactions */
  reactions: AddedReaction[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `addedReactions` object; null input -> null. */
export function decodeAddedReactions(json: TdJsonRaw): AddedReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AddedReactions = new AddedReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.reactions = decodeVector(record['reactions'], (item: TdJsonRaw): AddedReaction => decodeAddedReaction(item) as AddedReaction);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `addedReactions` into TDLib JSON wire shape. */
export function encodeAddedReactions(value: AddedReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'addedReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['reactions'] = encodeVector(value.reactions, (item: AddedReaction | null): TdJsonRaw => item === null ? null : encodeAddedReaction(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** address = Address — Describes an address */
export class Address {
  type: 'address' = 'address';
  extra?: string;
  /** country_code: string — A two-letter ISO 3166-1 alpha-2 country code */
  country_code: string = '';
  /** state: string — State, if applicable */
  state: string = '';
  /** city: string — City */
  city: string = '';
  /** street_line1: string — First line of the address */
  street_line1: string = '';
  /** street_line2: string — Second line of the address */
  street_line2: string = '';
  /** postal_code: string — Address postal code */
  postal_code: string = '';
}

/** Decode a TDLib `address` object; null input -> null. */
export function decodeAddress(json: TdJsonRaw): Address | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Address = new Address();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.country_code = asString(record['country_code']);
  out.state = asString(record['state']);
  out.city = asString(record['city']);
  out.street_line1 = asString(record['street_line1']);
  out.street_line2 = asString(record['street_line2']);
  out.postal_code = asString(record['postal_code']);
  return out;
}

/** Encode a `address` into TDLib JSON wire shape. */
export function encodeAddress(value: Address): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'address';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['country_code'] = value.country_code;
  out['state'] = value.state;
  out['city'] = value.city;
  out['street_line1'] = value.street_line1;
  out['street_line2'] = value.street_line2;
  out['postal_code'] = value.postal_code;
  return out;
}

/** advertisementSponsor = AdvertisementSponsor — Information about the sponsor of an advertisement */
export class AdvertisementSponsor {
  type: 'advertisementSponsor' = 'advertisementSponsor';
  extra?: string;
  /** url: string — URL of the sponsor to be opened when the advertisement is clicked */
  url: string = '';
  /** photo: photo — Photo of the sponsor; may be null if must not be shown */
  photo: Photo | null = null;
  /** info: string — Additional optional information about the sponsor to be shown along with the advertisement */
  info: string = '';
}

/** Decode a TDLib `advertisementSponsor` object; null input -> null. */
export function decodeAdvertisementSponsor(json: TdJsonRaw): AdvertisementSponsor | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AdvertisementSponsor = new AdvertisementSponsor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.photo = decodePhoto(record['photo']);
  out.info = asString(record['info']);
  return out;
}

/** Encode a `advertisementSponsor` into TDLib JSON wire shape. */
export function encodeAdvertisementSponsor(value: AdvertisementSponsor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'advertisementSponsor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['info'] = value.info;
  return out;
}

/** affiliateInfo = AffiliateInfo — Contains information about an affiliate that received commission from a Telegram Star transaction */
export class AffiliateInfo {
  type: 'affiliateInfo' = 'affiliateInfo';
  extra?: string;
  /** commission_per_mille: int32 — The number of Telegram Stars received by the affiliate for each 1000 Telegram Stars received by the program owner */
  commission_per_mille: number = 0;
  /** affiliate_chat_id: int53 — Identifier of the chat which received the commission */
  affiliate_chat_id: number = 0;
  /** star_amount: starAmount — The Telegram Star amount that was received by the affiliate; can be negative for refunds */
  star_amount: StarAmount | null = null;
}

/** Decode a TDLib `affiliateInfo` object; null input -> null. */
export function decodeAffiliateInfo(json: TdJsonRaw): AffiliateInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AffiliateInfo = new AffiliateInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.commission_per_mille = asNumber(record['commission_per_mille']);
  out.affiliate_chat_id = asNumber(record['affiliate_chat_id']);
  out.star_amount = decodeStarAmount(record['star_amount']);
  return out;
}

/** Encode a `affiliateInfo` into TDLib JSON wire shape. */
export function encodeAffiliateInfo(value: AffiliateInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'affiliateInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['commission_per_mille'] = value.commission_per_mille;
  out['affiliate_chat_id'] = value.affiliate_chat_id;
  out['star_amount'] = value.star_amount === null ? null : encodeStarAmount(value.star_amount);
  return out;
}

/** affiliateProgramInfo = AffiliateProgramInfo — Contains information about an active affiliate program If positive, then the program can't be connected using connectAffiliateProgram, but active connections will work until the date */
export class AffiliateProgramInfo {
  type: 'affiliateProgramInfo' = 'affiliateProgramInfo';
  extra?: string;
  /** parameters: affiliateProgramParameters — Parameters of the affiliate program */
  parameters: AffiliateProgramParameters | null = null;
  /** end_date: int32 — Point in time (Unix timestamp) when the affiliate program will be closed; 0 if the affiliate program isn't scheduled to be closed. */
  end_date: number = 0;
  /** daily_revenue_per_user_amount: starAmount — The amount of daily revenue per user in Telegram Stars of the bot that created the affiliate program */
  daily_revenue_per_user_amount: StarAmount | null = null;
}

/** Decode a TDLib `affiliateProgramInfo` object; null input -> null. */
export function decodeAffiliateProgramInfo(json: TdJsonRaw): AffiliateProgramInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AffiliateProgramInfo = new AffiliateProgramInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.parameters = decodeAffiliateProgramParameters(record['parameters']);
  out.end_date = asNumber(record['end_date']);
  out.daily_revenue_per_user_amount = decodeStarAmount(record['daily_revenue_per_user_amount']);
  return out;
}

/** Encode a `affiliateProgramInfo` into TDLib JSON wire shape. */
export function encodeAffiliateProgramInfo(value: AffiliateProgramInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'affiliateProgramInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['parameters'] = value.parameters === null ? null : encodeAffiliateProgramParameters(value.parameters);
  out['end_date'] = value.end_date;
  out['daily_revenue_per_user_amount'] = value.daily_revenue_per_user_amount === null ? null : encodeStarAmount(value.daily_revenue_per_user_amount);
  return out;
}

/** affiliateProgramParameters = AffiliateProgramParameters — Describes parameters of an affiliate program getOption("affiliate_program_commission_per_mille_min")-getOption("affiliate_program_commission_per_mille_max") */
export class AffiliateProgramParameters {
  type: 'affiliateProgramParameters' = 'affiliateProgramParameters';
  extra?: string;
  /** commission_per_mille: int32 — The number of Telegram Stars received by the affiliate for each 1000 Telegram Stars received by the program owner; */
  commission_per_mille: number = 0;
  /** month_count: int32 — Number of months the program will be active; 0-36. If 0, then the program is eternal */
  month_count: number = 0;
}

/** Decode a TDLib `affiliateProgramParameters` object; null input -> null. */
export function decodeAffiliateProgramParameters(json: TdJsonRaw): AffiliateProgramParameters | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AffiliateProgramParameters = new AffiliateProgramParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.commission_per_mille = asNumber(record['commission_per_mille']);
  out.month_count = asNumber(record['month_count']);
  return out;
}

/** Encode a `affiliateProgramParameters` into TDLib JSON wire shape. */
export function encodeAffiliateProgramParameters(value: AffiliateProgramParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'affiliateProgramParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['commission_per_mille'] = value.commission_per_mille;
  out['month_count'] = value.month_count;
  return out;
}

/** affiliateProgramSortOrderCreationDate = AffiliateProgramSortOrder — The affiliate programs must be sorted by creation date */
export class AffiliateProgramSortOrderCreationDate {
  type: 'affiliateProgramSortOrderCreationDate' = 'affiliateProgramSortOrderCreationDate';
  extra?: string;
}

/** Decode a TDLib `affiliateProgramSortOrderCreationDate` object; null input -> null. */
export function decodeAffiliateProgramSortOrderCreationDate(json: TdJsonRaw): AffiliateProgramSortOrderCreationDate | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AffiliateProgramSortOrderCreationDate = new AffiliateProgramSortOrderCreationDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `affiliateProgramSortOrderCreationDate` into TDLib JSON wire shape. */
export function encodeAffiliateProgramSortOrderCreationDate(value: AffiliateProgramSortOrderCreationDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'affiliateProgramSortOrderCreationDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** affiliateProgramSortOrderProfitability = AffiliateProgramSortOrder — The affiliate programs must be sorted by the profitability */
export class AffiliateProgramSortOrderProfitability {
  type: 'affiliateProgramSortOrderProfitability' = 'affiliateProgramSortOrderProfitability';
  extra?: string;
}

/** Decode a TDLib `affiliateProgramSortOrderProfitability` object; null input -> null. */
export function decodeAffiliateProgramSortOrderProfitability(json: TdJsonRaw): AffiliateProgramSortOrderProfitability | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AffiliateProgramSortOrderProfitability = new AffiliateProgramSortOrderProfitability();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `affiliateProgramSortOrderProfitability` into TDLib JSON wire shape. */
export function encodeAffiliateProgramSortOrderProfitability(value: AffiliateProgramSortOrderProfitability): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'affiliateProgramSortOrderProfitability';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** affiliateProgramSortOrderRevenue = AffiliateProgramSortOrder — The affiliate programs must be sorted by the expected revenue */
export class AffiliateProgramSortOrderRevenue {
  type: 'affiliateProgramSortOrderRevenue' = 'affiliateProgramSortOrderRevenue';
  extra?: string;
}

/** Decode a TDLib `affiliateProgramSortOrderRevenue` object; null input -> null. */
export function decodeAffiliateProgramSortOrderRevenue(json: TdJsonRaw): AffiliateProgramSortOrderRevenue | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AffiliateProgramSortOrderRevenue = new AffiliateProgramSortOrderRevenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `affiliateProgramSortOrderRevenue` into TDLib JSON wire shape. */
export function encodeAffiliateProgramSortOrderRevenue(value: AffiliateProgramSortOrderRevenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'affiliateProgramSortOrderRevenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** affiliateTypeBot = AffiliateType — The affiliate is a bot owned by the current user */
export class AffiliateTypeBot {
  type: 'affiliateTypeBot' = 'affiliateTypeBot';
  extra?: string;
  /** user_id: int53 — User identifier of the bot */
  user_id: number = 0;
}

/** Decode a TDLib `affiliateTypeBot` object; null input -> null. */
export function decodeAffiliateTypeBot(json: TdJsonRaw): AffiliateTypeBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AffiliateTypeBot = new AffiliateTypeBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  return out;
}

/** Encode a `affiliateTypeBot` into TDLib JSON wire shape. */
export function encodeAffiliateTypeBot(value: AffiliateTypeBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'affiliateTypeBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  return out;
}

/** affiliateTypeChannel = AffiliateType — The affiliate is a channel chat where the current user has can_post_messages administrator right */
export class AffiliateTypeChannel {
  type: 'affiliateTypeChannel' = 'affiliateTypeChannel';
  extra?: string;
  /** chat_id: int53 — Identifier of the channel chat */
  chat_id: number = 0;
}

/** Decode a TDLib `affiliateTypeChannel` object; null input -> null. */
export function decodeAffiliateTypeChannel(json: TdJsonRaw): AffiliateTypeChannel | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AffiliateTypeChannel = new AffiliateTypeChannel();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `affiliateTypeChannel` into TDLib JSON wire shape. */
export function encodeAffiliateTypeChannel(value: AffiliateTypeChannel): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'affiliateTypeChannel';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** affiliateTypeCurrentUser = AffiliateType — The affiliate is the current user */
export class AffiliateTypeCurrentUser {
  type: 'affiliateTypeCurrentUser' = 'affiliateTypeCurrentUser';
  extra?: string;
}

/** Decode a TDLib `affiliateTypeCurrentUser` object; null input -> null. */
export function decodeAffiliateTypeCurrentUser(json: TdJsonRaw): AffiliateTypeCurrentUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AffiliateTypeCurrentUser = new AffiliateTypeCurrentUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `affiliateTypeCurrentUser` into TDLib JSON wire shape. */
export function encodeAffiliateTypeCurrentUser(value: AffiliateTypeCurrentUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'affiliateTypeCurrentUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** ageVerificationParameters = AgeVerificationParameters — Describes parameters for age verification of the current user */
export class AgeVerificationParameters {
  type: 'ageVerificationParameters' = 'ageVerificationParameters';
  extra?: string;
  /** min_age: int32 — The minimum age required to view restricted content */
  min_age: number = 0;
  /** verification_bot_username: string — Username of the bot which main Web App may be used to verify age of the user */
  verification_bot_username: string = '';
  /** country: string — Unique name for the country or region, which legislation required age verification. May be used to get the corresponding localization key */
  country: string = '';
}

/** Decode a TDLib `ageVerificationParameters` object; null input -> null. */
export function decodeAgeVerificationParameters(json: TdJsonRaw): AgeVerificationParameters | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AgeVerificationParameters = new AgeVerificationParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.min_age = asNumber(record['min_age']);
  out.verification_bot_username = asString(record['verification_bot_username']);
  out.country = asString(record['country']);
  return out;
}

/** Encode a `ageVerificationParameters` into TDLib JSON wire shape. */
export function encodeAgeVerificationParameters(value: AgeVerificationParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'ageVerificationParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['min_age'] = value.min_age;
  out['verification_bot_username'] = value.verification_bot_username;
  out['country'] = value.country;
  return out;
}

/** allowBotToSendMessages = Ok — Allows the specified bot to send messages to the user */
export class AllowBotToSendMessages {
  type: 'allowBotToSendMessages' = 'allowBotToSendMessages';
  extra?: string;
  /** bot_user_id: int53 — Identifier of the target bot */
  bot_user_id: number = 0;
}

/** Decode a TDLib `allowBotToSendMessages` object; null input -> null. */
export function decodeAllowBotToSendMessages(json: TdJsonRaw): AllowBotToSendMessages | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AllowBotToSendMessages = new AllowBotToSendMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  return out;
}

/** Encode a `allowBotToSendMessages` into TDLib JSON wire shape. */
export function encodeAllowBotToSendMessages(value: AllowBotToSendMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'allowBotToSendMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  return out;
}

/** allowUnpaidMessagesFromUser = Ok — Allows the specified user to send unpaid private messages to the current user by adding a rule to userPrivacySettingAllowUnpaidMessages */
export class AllowUnpaidMessagesFromUser {
  type: 'allowUnpaidMessagesFromUser' = 'allowUnpaidMessagesFromUser';
  extra?: string;
  /** user_id: int53 — Identifier of the user */
  user_id: number = 0;
  /** refund_payments: Bool — Pass true to refund the user previously paid messages */
  refund_payments: boolean = false;
}

/** Decode a TDLib `allowUnpaidMessagesFromUser` object; null input -> null. */
export function decodeAllowUnpaidMessagesFromUser(json: TdJsonRaw): AllowUnpaidMessagesFromUser | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AllowUnpaidMessagesFromUser = new AllowUnpaidMessagesFromUser();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.user_id = asNumber(record['user_id']);
  out.refund_payments = asBool(record['refund_payments']);
  return out;
}

/** Encode a `allowUnpaidMessagesFromUser` into TDLib JSON wire shape. */
export function encodeAllowUnpaidMessagesFromUser(value: AllowUnpaidMessagesFromUser): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'allowUnpaidMessagesFromUser';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['user_id'] = value.user_id;
  out['refund_payments'] = value.refund_payments;
  return out;
}

/** alternativeVideo = AlternativeVideo — Describes an alternative re-encoded quality of a video file */
export class AlternativeVideo {
  type: 'alternativeVideo' = 'alternativeVideo';
  extra?: string;
  /** id: int64 — Unique identifier of the alternative video, which is used in the HLS file */
  id: string = '';
  /** width: int32 — Video width */
  width: number = 0;
  /** height: int32 — Video height */
  height: number = 0;
  /** codec: string — Codec used for video file encoding, for example, "h264", "h265", "av1", or "av01" */
  codec: string = '';
  /** hls_file: file — HLS file describing the video */
  hls_file: File | null = null;
  /** video: file — File containing the video */
  video: File | null = null;
}

/** Decode a TDLib `alternativeVideo` object; null input -> null. */
export function decodeAlternativeVideo(json: TdJsonRaw): AlternativeVideo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AlternativeVideo = new AlternativeVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.codec = asString(record['codec']);
  out.hls_file = decodeFile(record['hls_file']);
  out.video = decodeFile(record['video']);
  return out;
}

/** Encode a `alternativeVideo` into TDLib JSON wire shape. */
export function encodeAlternativeVideo(value: AlternativeVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'alternativeVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['width'] = value.width;
  out['height'] = value.height;
  out['codec'] = value.codec;
  out['hls_file'] = value.hls_file === null ? null : encodeFile(value.hls_file);
  out['video'] = value.video === null ? null : encodeFile(value.video);
  return out;
}

/** animatedChatPhoto = AnimatedChatPhoto — Animated variant of a chat photo in MPEG4 format */
export class AnimatedChatPhoto {
  type: 'animatedChatPhoto' = 'animatedChatPhoto';
  extra?: string;
  /** length: int32 — Animation width and height */
  length: number = 0;
  /** file: file — Information about the animation file */
  file: File | null = null;
  /** main_frame_timestamp: double — Timestamp of the frame, used as a static chat photo */
  main_frame_timestamp: number = 0;
}

/** Decode a TDLib `animatedChatPhoto` object; null input -> null. */
export function decodeAnimatedChatPhoto(json: TdJsonRaw): AnimatedChatPhoto | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnimatedChatPhoto = new AnimatedChatPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.length = asNumber(record['length']);
  out.file = decodeFile(record['file']);
  out.main_frame_timestamp = asNumber(record['main_frame_timestamp']);
  return out;
}

/** Encode a `animatedChatPhoto` into TDLib JSON wire shape. */
export function encodeAnimatedChatPhoto(value: AnimatedChatPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'animatedChatPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['length'] = value.length;
  out['file'] = value.file === null ? null : encodeFile(value.file);
  out['main_frame_timestamp'] = value.main_frame_timestamp;
  return out;
}

/** animatedEmoji = AnimatedEmoji — Describes an animated or custom representation of an emoji */
export class AnimatedEmoji {
  type: 'animatedEmoji' = 'animatedEmoji';
  extra?: string;
  /** sticker: sticker — Sticker for the emoji; may be null if yet unknown for a custom emoji. If the sticker is a custom emoji, then it can have arbitrary format */
  sticker: Sticker | null = null;
  /** sticker_width: int32 — Expected width of the sticker, which can be used if the sticker is null */
  sticker_width: number = 0;
  /** sticker_height: int32 — Expected height of the sticker, which can be used if the sticker is null */
  sticker_height: number = 0;
  /** fitzpatrick_type: int32 — Emoji modifier fitzpatrick type; 0-6; 0 if none */
  fitzpatrick_type: number = 0;
  /** sound: file — File containing the sound to be played when the sticker is clicked; may be null. The sound is encoded with the Opus codec, and stored inside an OGG container */
  sound: File | null = null;
}

/** Decode a TDLib `animatedEmoji` object; null input -> null. */
export function decodeAnimatedEmoji(json: TdJsonRaw): AnimatedEmoji | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnimatedEmoji = new AnimatedEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeSticker(record['sticker']);
  out.sticker_width = asNumber(record['sticker_width']);
  out.sticker_height = asNumber(record['sticker_height']);
  out.fitzpatrick_type = asNumber(record['fitzpatrick_type']);
  out.sound = decodeFile(record['sound']);
  return out;
}

/** Encode a `animatedEmoji` into TDLib JSON wire shape. */
export function encodeAnimatedEmoji(value: AnimatedEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'animatedEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  out['sticker_width'] = value.sticker_width;
  out['sticker_height'] = value.sticker_height;
  out['fitzpatrick_type'] = value.fitzpatrick_type;
  out['sound'] = value.sound === null ? null : encodeFile(value.sound);
  return out;
}

/** animation = Animation — Describes an animation file. The animation must be encoded in GIF or MPEG4 format */
export class Animation {
  type: 'animation' = 'animation';
  extra?: string;
  /** duration: int32 — Duration of the animation, in seconds; as defined by the sender */
  duration: number = 0;
  /** width: int32 — Width of the animation */
  width: number = 0;
  /** height: int32 — Height of the animation */
  height: number = 0;
  /** file_name: string — Original name of the file; as defined by the sender */
  file_name: string = '';
  /** mime_type: string — MIME type of the file, usually "image/gif" or "video/mp4" */
  mime_type: string = '';
  /** has_stickers: Bool — True, if stickers were added to the animation. The list of corresponding sticker sets can be received using getAttachedStickerSets */
  has_stickers: boolean = false;
  /** minithumbnail: minithumbnail — Animation minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
  /** thumbnail: thumbnail — Animation thumbnail in JPEG or MPEG4 format; may be null */
  thumbnail: Thumbnail | null = null;
  /** animation: file — File containing the animation */
  animation: File | null = null;
}

/** Decode a TDLib `animation` object; null input -> null. */
export function decodeAnimation(json: TdJsonRaw): Animation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Animation = new Animation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.duration = asNumber(record['duration']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.file_name = asString(record['file_name']);
  out.mime_type = asString(record['mime_type']);
  out.has_stickers = asBool(record['has_stickers']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  out.thumbnail = decodeThumbnail(record['thumbnail']);
  out.animation = decodeFile(record['animation']);
  return out;
}

/** Encode a `animation` into TDLib JSON wire shape. */
export function encodeAnimation(value: Animation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'animation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['duration'] = value.duration;
  out['width'] = value.width;
  out['height'] = value.height;
  out['file_name'] = value.file_name;
  out['mime_type'] = value.mime_type;
  out['has_stickers'] = value.has_stickers;
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  out['thumbnail'] = value.thumbnail === null ? null : encodeThumbnail(value.thumbnail);
  out['animation'] = value.animation === null ? null : encodeFile(value.animation);
  return out;
}

/** animations = Animations — Represents a list of animations */
export class Animations {
  type: 'animations' = 'animations';
  extra?: string;
  /** animations: vector<animation> — List of animations */
  animations: Animation[] = [];
}

/** Decode a TDLib `animations` object; null input -> null. */
export function decodeAnimations(json: TdJsonRaw): Animations | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Animations = new Animations();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animations = decodeVector(record['animations'], (item: TdJsonRaw): Animation => decodeAnimation(item) as Animation);
  return out;
}

/** Encode a `animations` into TDLib JSON wire shape. */
export function encodeAnimations(value: Animations): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'animations';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animations'] = encodeVector(value.animations, (item: Animation | null): TdJsonRaw => item === null ? null : encodeAnimation(item));
  return out;
}

/** answerCallbackQuery = Ok — Sets the result of a callback query; for bots only */
export class AnswerCallbackQuery {
  type: 'answerCallbackQuery' = 'answerCallbackQuery';
  extra?: string;
  /** callback_query_id: int64 — Identifier of the callback query */
  callback_query_id: string = '';
  /** text: string — Text of the answer */
  text: string = '';
  /** show_alert: Bool — Pass true to show an alert to the user instead of a toast notification */
  show_alert: boolean = false;
  /** url: string — URL to be opened */
  url: string = '';
  /** cache_time: int32 — Time during which the result of the query can be cached, in seconds */
  cache_time: number = 0;
}

/** Decode a TDLib `answerCallbackQuery` object; null input -> null. */
export function decodeAnswerCallbackQuery(json: TdJsonRaw): AnswerCallbackQuery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnswerCallbackQuery = new AnswerCallbackQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.callback_query_id = asInt64(record['callback_query_id']);
  out.text = asString(record['text']);
  out.show_alert = asBool(record['show_alert']);
  out.url = asString(record['url']);
  out.cache_time = asNumber(record['cache_time']);
  return out;
}

/** Encode a `answerCallbackQuery` into TDLib JSON wire shape. */
export function encodeAnswerCallbackQuery(value: AnswerCallbackQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'answerCallbackQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['callback_query_id'] = value.callback_query_id;
  out['text'] = value.text;
  out['show_alert'] = value.show_alert;
  out['url'] = value.url;
  out['cache_time'] = value.cache_time;
  return out;
}

/** answerChatJoinRequestQuery = Ok — Sets the result of a chat join query; for bots only */
export class AnswerChatJoinRequestQuery {
  type: 'answerChatJoinRequestQuery' = 'answerChatJoinRequestQuery';
  extra?: string;
  /** query_id: int64 — Identifier of the query */
  query_id: string = '';
  /** result: ChatJoinRequestResult — The result */
  result: TdChatJoinRequestResult | null = null;
  /** url: string — URL of the Web App to open */
  url: string = '';
}

/** Decode a TDLib `answerChatJoinRequestQuery` object; null input -> null. */
export function decodeAnswerChatJoinRequestQuery(json: TdJsonRaw): AnswerChatJoinRequestQuery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnswerChatJoinRequestQuery = new AnswerChatJoinRequestQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.query_id = asInt64(record['query_id']);
  out.result = decodeTdChatJoinRequestResult(record['result']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `answerChatJoinRequestQuery` into TDLib JSON wire shape. */
export function encodeAnswerChatJoinRequestQuery(value: AnswerChatJoinRequestQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'answerChatJoinRequestQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['query_id'] = value.query_id;
  out['result'] = value.result === null ? null : encodeTdChatJoinRequestResult(value.result);
  out['url'] = value.url;
  return out;
}

/** answerCustomQuery = Ok — Answers a custom query; for bots only */
export class AnswerCustomQuery {
  type: 'answerCustomQuery' = 'answerCustomQuery';
  extra?: string;
  /** custom_query_id: int64 — Identifier of a custom query */
  custom_query_id: string = '';
  /** data: string — JSON-serialized answer to the query */
  data: string = '';
}

/** Decode a TDLib `answerCustomQuery` object; null input -> null. */
export function decodeAnswerCustomQuery(json: TdJsonRaw): AnswerCustomQuery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnswerCustomQuery = new AnswerCustomQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.custom_query_id = asInt64(record['custom_query_id']);
  out.data = asString(record['data']);
  return out;
}

/** Encode a `answerCustomQuery` into TDLib JSON wire shape. */
export function encodeAnswerCustomQuery(value: AnswerCustomQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'answerCustomQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['custom_query_id'] = value.custom_query_id;
  out['data'] = value.data;
  return out;
}

/** answerGuestQuery = InlineMessageId — Sets the result of a guest query; for bots only */
export class AnswerGuestQuery {
  type: 'answerGuestQuery' = 'answerGuestQuery';
  extra?: string;
  /** guest_query_id: int64 — Identifier of the guest query */
  guest_query_id: string = '';
  /** result: InputInlineQueryResult — The result of the query */
  result: TdInputInlineQueryResult | null = null;
}

/** Decode a TDLib `answerGuestQuery` object; null input -> null. */
export function decodeAnswerGuestQuery(json: TdJsonRaw): AnswerGuestQuery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnswerGuestQuery = new AnswerGuestQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.guest_query_id = asInt64(record['guest_query_id']);
  out.result = decodeTdInputInlineQueryResult(record['result']);
  return out;
}

/** Encode a `answerGuestQuery` into TDLib JSON wire shape. */
export function encodeAnswerGuestQuery(value: AnswerGuestQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'answerGuestQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['guest_query_id'] = value.guest_query_id;
  out['result'] = value.result === null ? null : encodeTdInputInlineQueryResult(value.result);
  return out;
}

/** answerInlineQuery = Ok — Sets the result of an inline query; for bots only */
export class AnswerInlineQuery {
  type: 'answerInlineQuery' = 'answerInlineQuery';
  extra?: string;
  /** inline_query_id: int64 — Identifier of the inline query */
  inline_query_id: string = '';
  /** is_personal: Bool — Pass true if results may be cached and returned only for the user who sent the query. By default, results may be returned to any user who sends the same query */
  is_personal: boolean = false;
  /** button: inlineQueryResultsButton — Button to be shown above inline query results; pass null if none */
  button: InlineQueryResultsButton | null = null;
  /** results: vector<InputInlineQueryResult> — The results of the query */
  results: TdInputInlineQueryResult[] = [];
  /** cache_time: int32 — Allowed time to cache the results of the query, in seconds */
  cache_time: number = 0;
  /** next_offset: string — Offset for the next inline query; pass an empty string if there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `answerInlineQuery` object; null input -> null. */
export function decodeAnswerInlineQuery(json: TdJsonRaw): AnswerInlineQuery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnswerInlineQuery = new AnswerInlineQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_query_id = asInt64(record['inline_query_id']);
  out.is_personal = asBool(record['is_personal']);
  out.button = decodeInlineQueryResultsButton(record['button']);
  out.results = decodeVector(record['results'], (item: TdJsonRaw): TdInputInlineQueryResult => decodeTdInputInlineQueryResult(item) as TdInputInlineQueryResult);
  out.cache_time = asNumber(record['cache_time']);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `answerInlineQuery` into TDLib JSON wire shape. */
export function encodeAnswerInlineQuery(value: AnswerInlineQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'answerInlineQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_query_id'] = value.inline_query_id;
  out['is_personal'] = value.is_personal;
  out['button'] = value.button === null ? null : encodeInlineQueryResultsButton(value.button);
  out['results'] = encodeVector(value.results, (item: TdInputInlineQueryResult | null): TdJsonRaw => item === null ? null : encodeTdInputInlineQueryResult(item));
  out['cache_time'] = value.cache_time;
  out['next_offset'] = value.next_offset;
  return out;
}

/** answerPreCheckoutQuery = Ok — Sets the result of a pre-checkout query; for bots only */
export class AnswerPreCheckoutQuery {
  type: 'answerPreCheckoutQuery' = 'answerPreCheckoutQuery';
  extra?: string;
  /** pre_checkout_query_id: int64 — Identifier of the pre-checkout query */
  pre_checkout_query_id: string = '';
  /** error_message: string — An error message, empty on success */
  error_message: string = '';
}

/** Decode a TDLib `answerPreCheckoutQuery` object; null input -> null. */
export function decodeAnswerPreCheckoutQuery(json: TdJsonRaw): AnswerPreCheckoutQuery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnswerPreCheckoutQuery = new AnswerPreCheckoutQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.pre_checkout_query_id = asInt64(record['pre_checkout_query_id']);
  out.error_message = asString(record['error_message']);
  return out;
}

/** Encode a `answerPreCheckoutQuery` into TDLib JSON wire shape. */
export function encodeAnswerPreCheckoutQuery(value: AnswerPreCheckoutQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'answerPreCheckoutQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['pre_checkout_query_id'] = value.pre_checkout_query_id;
  out['error_message'] = value.error_message;
  return out;
}

/** answerShippingQuery = Ok — Sets the result of a shipping query; for bots only */
export class AnswerShippingQuery {
  type: 'answerShippingQuery' = 'answerShippingQuery';
  extra?: string;
  /** shipping_query_id: int64 — Identifier of the shipping query */
  shipping_query_id: string = '';
  /** shipping_options: vector<shippingOption> — Available shipping options */
  shipping_options: ShippingOption[] = [];
  /** error_message: string — An error message, empty on success */
  error_message: string = '';
}

/** Decode a TDLib `answerShippingQuery` object; null input -> null. */
export function decodeAnswerShippingQuery(json: TdJsonRaw): AnswerShippingQuery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnswerShippingQuery = new AnswerShippingQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.shipping_query_id = asInt64(record['shipping_query_id']);
  out.shipping_options = decodeVector(record['shipping_options'], (item: TdJsonRaw): ShippingOption => decodeShippingOption(item) as ShippingOption);
  out.error_message = asString(record['error_message']);
  return out;
}

/** Encode a `answerShippingQuery` into TDLib JSON wire shape. */
export function encodeAnswerShippingQuery(value: AnswerShippingQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'answerShippingQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['shipping_query_id'] = value.shipping_query_id;
  out['shipping_options'] = encodeVector(value.shipping_options, (item: ShippingOption | null): TdJsonRaw => item === null ? null : encodeShippingOption(item));
  out['error_message'] = value.error_message;
  return out;
}

/** answerWebAppQuery = InlineMessageId — Sets the result of interaction with a Web App and sends corresponding message on behalf of the user to the chat from which the query originated; for bots only */
export class AnswerWebAppQuery {
  type: 'answerWebAppQuery' = 'answerWebAppQuery';
  extra?: string;
  /** web_app_query_id: string — Identifier of the Web App query */
  web_app_query_id: string = '';
  /** result: InputInlineQueryResult — The result of the query */
  result: TdInputInlineQueryResult | null = null;
}

/** Decode a TDLib `answerWebAppQuery` object; null input -> null. */
export function decodeAnswerWebAppQuery(json: TdJsonRaw): AnswerWebAppQuery | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AnswerWebAppQuery = new AnswerWebAppQuery();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.web_app_query_id = asString(record['web_app_query_id']);
  out.result = decodeTdInputInlineQueryResult(record['result']);
  return out;
}

/** Encode a `answerWebAppQuery` into TDLib JSON wire shape. */
export function encodeAnswerWebAppQuery(value: AnswerWebAppQuery): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'answerWebAppQuery';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['web_app_query_id'] = value.web_app_query_id;
  out['result'] = value.result === null ? null : encodeTdInputInlineQueryResult(value.result);
  return out;
}

/** applyPremiumGiftCode = Ok — Applies a Telegram Premium gift code */
export class ApplyPremiumGiftCode {
  type: 'applyPremiumGiftCode' = 'applyPremiumGiftCode';
  extra?: string;
  /** code: string — The code to apply */
  code: string = '';
}

/** Decode a TDLib `applyPremiumGiftCode` object; null input -> null. */
export function decodeApplyPremiumGiftCode(json: TdJsonRaw): ApplyPremiumGiftCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ApplyPremiumGiftCode = new ApplyPremiumGiftCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code = asString(record['code']);
  return out;
}

/** Encode a `applyPremiumGiftCode` into TDLib JSON wire shape. */
export function encodeApplyPremiumGiftCode(value: ApplyPremiumGiftCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'applyPremiumGiftCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code'] = value.code;
  return out;
}

/** approveSuggestedPost = Ok — Approves a suggested post in a channel direct messages chat then the date must be in the future, but at most getOption("suggested_post_send_delay_max") seconds in the future */
export class ApproveSuggestedPost {
  type: 'approveSuggestedPost' = 'approveSuggestedPost';
  extra?: string;
  /** chat_id: int53 — Chat identifier of the channel direct messages chat */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the message with the suggested post. Use messageProperties.can_be_approved to check whether the suggested post can be approved */
  message_id: number = 0;
  /** send_date: int32 — Point in time (Unix timestamp) when the post is expected to be published; pass 0 if the date has already been chosen. If specified, */
  send_date: number = 0;
}

/** Decode a TDLib `approveSuggestedPost` object; null input -> null. */
export function decodeApproveSuggestedPost(json: TdJsonRaw): ApproveSuggestedPost | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ApproveSuggestedPost = new ApproveSuggestedPost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.send_date = asNumber(record['send_date']);
  return out;
}

/** Encode a `approveSuggestedPost` into TDLib JSON wire shape. */
export function encodeApproveSuggestedPost(value: ApproveSuggestedPost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'approveSuggestedPost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['send_date'] = value.send_date;
  return out;
}

/** archiveChatListSettings = ArchiveChatListSettings — Contains settings for automatic moving of chats to and from the Archive chat lists */
export class ArchiveChatListSettings {
  type: 'archiveChatListSettings' = 'archiveChatListSettings';
  extra?: string;
  /** archive_and_mute_new_chats_from_unknown_users: Bool — True, if new chats from non-contacts will be automatically archived and muted. Can be set to true only if the option "can_archive_and_mute_new_chats_from_unknown_users" is true */
  archive_and_mute_new_chats_from_unknown_users: boolean = false;
  /** keep_unmuted_chats_archived: Bool — True, if unmuted chats will be kept in the Archive chat list when they get a new message */
  keep_unmuted_chats_archived: boolean = false;
  /** keep_chats_from_folders_archived: Bool — True, if unmuted chats, that are always included or pinned in a folder, will be kept in the Archive chat list when they get a new message. Ignored if keep_unmuted_chats_archived == true */
  keep_chats_from_folders_archived: boolean = false;
}

/** Decode a TDLib `archiveChatListSettings` object; null input -> null. */
export function decodeArchiveChatListSettings(json: TdJsonRaw): ArchiveChatListSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ArchiveChatListSettings = new ArchiveChatListSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.archive_and_mute_new_chats_from_unknown_users = asBool(record['archive_and_mute_new_chats_from_unknown_users']);
  out.keep_unmuted_chats_archived = asBool(record['keep_unmuted_chats_archived']);
  out.keep_chats_from_folders_archived = asBool(record['keep_chats_from_folders_archived']);
  return out;
}

/** Encode a `archiveChatListSettings` into TDLib JSON wire shape. */
export function encodeArchiveChatListSettings(value: ArchiveChatListSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'archiveChatListSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['archive_and_mute_new_chats_from_unknown_users'] = value.archive_and_mute_new_chats_from_unknown_users;
  out['keep_unmuted_chats_archived'] = value.keep_unmuted_chats_archived;
  out['keep_chats_from_folders_archived'] = value.keep_chats_from_folders_archived;
  return out;
}

/** assignStoreTransaction = Ok — Informs server about an in-store purchase. For official applications only */
export class AssignStoreTransaction {
  type: 'assignStoreTransaction' = 'assignStoreTransaction';
  extra?: string;
  /** transaction: StoreTransaction — Information about the transaction */
  transaction: TdStoreTransaction | null = null;
  /** purpose: StorePaymentPurpose — Transaction purpose */
  purpose: TdStorePaymentPurpose | null = null;
}

/** Decode a TDLib `assignStoreTransaction` object; null input -> null. */
export function decodeAssignStoreTransaction(json: TdJsonRaw): AssignStoreTransaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AssignStoreTransaction = new AssignStoreTransaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.transaction = decodeTdStoreTransaction(record['transaction']);
  out.purpose = decodeTdStorePaymentPurpose(record['purpose']);
  return out;
}

/** Encode a `assignStoreTransaction` into TDLib JSON wire shape. */
export function encodeAssignStoreTransaction(value: AssignStoreTransaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'assignStoreTransaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['transaction'] = value.transaction === null ? null : encodeTdStoreTransaction(value.transaction);
  out['purpose'] = value.purpose === null ? null : encodeTdStorePaymentPurpose(value.purpose);
  return out;
}

/** attachmentMenuBot = AttachmentMenuBot — Represents a bot, which can be added to attachment or side menu */
export class AttachmentMenuBot {
  type: 'attachmentMenuBot' = 'attachmentMenuBot';
  extra?: string;
  /** bot_user_id: int53 — User identifier of the bot */
  bot_user_id: number = 0;
  /** supports_self_chat: Bool — True, if the bot supports opening from attachment menu in the chat with the bot */
  supports_self_chat: boolean = false;
  /** supports_user_chats: Bool — True, if the bot supports opening from attachment menu in private chats with ordinary users */
  supports_user_chats: boolean = false;
  /** supports_bot_chats: Bool — True, if the bot supports opening from attachment menu in private chats with other bots */
  supports_bot_chats: boolean = false;
  /** supports_group_chats: Bool — True, if the bot supports opening from attachment menu in basic group and supergroup chats */
  supports_group_chats: boolean = false;
  /** supports_channel_chats: Bool — True, if the bot supports opening from attachment menu in channel chats */
  supports_channel_chats: boolean = false;
  /** request_write_access: Bool — True, if the user must be asked for the permission to send messages to the bot */
  request_write_access: boolean = false;
  /** is_added: Bool — True, if the bot was explicitly added by the user. If the bot isn't added, then on the first bot launch toggleBotIsAddedToAttachmentMenu must be called and the bot must be added or removed */
  is_added: boolean = false;
  /** show_in_attachment_menu: Bool — True, if the bot must be shown in the attachment menu */
  show_in_attachment_menu: boolean = false;
  /** show_in_side_menu: Bool — True, if the bot must be shown in the side menu */
  show_in_side_menu: boolean = false;
  /** show_disclaimer_in_side_menu: Bool — True, if a disclaimer, why the bot is shown in the side menu, is needed */
  show_disclaimer_in_side_menu: boolean = false;
  /** name: string — Name for the bot in attachment menu */
  name: string = '';
  /** name_color: attachmentMenuBotColor — Color to highlight selected name of the bot if appropriate; may be null */
  name_color: AttachmentMenuBotColor | null = null;
  /** default_icon: file — Default icon for the bot in SVG format; may be null */
  default_icon: File | null = null;
  /** ios_static_icon: file — Icon for the bot in SVG format for the official iOS app; may be null */
  ios_static_icon: File | null = null;
  /** ios_animated_icon: file — Icon for the bot in TGS format for the official iOS app; may be null */
  ios_animated_icon: File | null = null;
  /** ios_side_menu_icon: file — Icon for the bot in PNG format for the official iOS app side menu; may be null */
  ios_side_menu_icon: File | null = null;
  /** android_icon: file — Icon for the bot in TGS format for the official Android app; may be null */
  android_icon: File | null = null;
  /** android_side_menu_icon: file — Icon for the bot in SVG format for the official Android app side menu; may be null */
  android_side_menu_icon: File | null = null;
  /** macos_icon: file — Icon for the bot in TGS format for the official native macOS app; may be null */
  macos_icon: File | null = null;
  /** macos_side_menu_icon: file — Icon for the bot in PNG format for the official macOS app side menu; may be null */
  macos_side_menu_icon: File | null = null;
  /** icon_color: attachmentMenuBotColor — Color to highlight selected icon of the bot if appropriate; may be null */
  icon_color: AttachmentMenuBotColor | null = null;
  /** web_app_placeholder: file — Default placeholder for opened Web Apps in SVG format; may be null */
  web_app_placeholder: File | null = null;
}

/** Decode a TDLib `attachmentMenuBot` object; null input -> null. */
export function decodeAttachmentMenuBot(json: TdJsonRaw): AttachmentMenuBot | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AttachmentMenuBot = new AttachmentMenuBot();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bot_user_id = asNumber(record['bot_user_id']);
  out.supports_self_chat = asBool(record['supports_self_chat']);
  out.supports_user_chats = asBool(record['supports_user_chats']);
  out.supports_bot_chats = asBool(record['supports_bot_chats']);
  out.supports_group_chats = asBool(record['supports_group_chats']);
  out.supports_channel_chats = asBool(record['supports_channel_chats']);
  out.request_write_access = asBool(record['request_write_access']);
  out.is_added = asBool(record['is_added']);
  out.show_in_attachment_menu = asBool(record['show_in_attachment_menu']);
  out.show_in_side_menu = asBool(record['show_in_side_menu']);
  out.show_disclaimer_in_side_menu = asBool(record['show_disclaimer_in_side_menu']);
  out.name = asString(record['name']);
  out.name_color = decodeAttachmentMenuBotColor(record['name_color']);
  out.default_icon = decodeFile(record['default_icon']);
  out.ios_static_icon = decodeFile(record['ios_static_icon']);
  out.ios_animated_icon = decodeFile(record['ios_animated_icon']);
  out.ios_side_menu_icon = decodeFile(record['ios_side_menu_icon']);
  out.android_icon = decodeFile(record['android_icon']);
  out.android_side_menu_icon = decodeFile(record['android_side_menu_icon']);
  out.macos_icon = decodeFile(record['macos_icon']);
  out.macos_side_menu_icon = decodeFile(record['macos_side_menu_icon']);
  out.icon_color = decodeAttachmentMenuBotColor(record['icon_color']);
  out.web_app_placeholder = decodeFile(record['web_app_placeholder']);
  return out;
}

/** Encode a `attachmentMenuBot` into TDLib JSON wire shape. */
export function encodeAttachmentMenuBot(value: AttachmentMenuBot): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'attachmentMenuBot';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bot_user_id'] = value.bot_user_id;
  out['supports_self_chat'] = value.supports_self_chat;
  out['supports_user_chats'] = value.supports_user_chats;
  out['supports_bot_chats'] = value.supports_bot_chats;
  out['supports_group_chats'] = value.supports_group_chats;
  out['supports_channel_chats'] = value.supports_channel_chats;
  out['request_write_access'] = value.request_write_access;
  out['is_added'] = value.is_added;
  out['show_in_attachment_menu'] = value.show_in_attachment_menu;
  out['show_in_side_menu'] = value.show_in_side_menu;
  out['show_disclaimer_in_side_menu'] = value.show_disclaimer_in_side_menu;
  out['name'] = value.name;
  out['name_color'] = value.name_color === null ? null : encodeAttachmentMenuBotColor(value.name_color);
  out['default_icon'] = value.default_icon === null ? null : encodeFile(value.default_icon);
  out['ios_static_icon'] = value.ios_static_icon === null ? null : encodeFile(value.ios_static_icon);
  out['ios_animated_icon'] = value.ios_animated_icon === null ? null : encodeFile(value.ios_animated_icon);
  out['ios_side_menu_icon'] = value.ios_side_menu_icon === null ? null : encodeFile(value.ios_side_menu_icon);
  out['android_icon'] = value.android_icon === null ? null : encodeFile(value.android_icon);
  out['android_side_menu_icon'] = value.android_side_menu_icon === null ? null : encodeFile(value.android_side_menu_icon);
  out['macos_icon'] = value.macos_icon === null ? null : encodeFile(value.macos_icon);
  out['macos_side_menu_icon'] = value.macos_side_menu_icon === null ? null : encodeFile(value.macos_side_menu_icon);
  out['icon_color'] = value.icon_color === null ? null : encodeAttachmentMenuBotColor(value.icon_color);
  out['web_app_placeholder'] = value.web_app_placeholder === null ? null : encodeFile(value.web_app_placeholder);
  return out;
}

/** attachmentMenuBotColor = AttachmentMenuBotColor — Describes a color to highlight a bot added to attachment menu */
export class AttachmentMenuBotColor {
  type: 'attachmentMenuBotColor' = 'attachmentMenuBotColor';
  extra?: string;
  /** light_color: int32 — Color in the RGB format for light themes */
  light_color: number = 0;
  /** dark_color: int32 — Color in the RGB format for dark themes */
  dark_color: number = 0;
}

/** Decode a TDLib `attachmentMenuBotColor` object; null input -> null. */
export function decodeAttachmentMenuBotColor(json: TdJsonRaw): AttachmentMenuBotColor | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AttachmentMenuBotColor = new AttachmentMenuBotColor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.light_color = asNumber(record['light_color']);
  out.dark_color = asNumber(record['dark_color']);
  return out;
}

/** Encode a `attachmentMenuBotColor` into TDLib JSON wire shape. */
export function encodeAttachmentMenuBotColor(value: AttachmentMenuBotColor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'attachmentMenuBotColor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['light_color'] = value.light_color;
  out['dark_color'] = value.dark_color;
  return out;
}

/** attributeCraftPersistenceProbability = AttributeCraftPersistenceProbability — Describes chance of the crafted gift to have the backdrop or symbol of one of the original gifts if 1, 2, 3, or 4 gifts with the attribute are used in the craf... */
export class AttributeCraftPersistenceProbability {
  type: 'attributeCraftPersistenceProbability' = 'attributeCraftPersistenceProbability';
  extra?: string;
  /** persistence_chance_per_mille: vector<int32> — The 4 numbers that describe probability of the craft result to have the same attribute as one of the original gifts */
  persistence_chance_per_mille: number[] = [];
}

/** Decode a TDLib `attributeCraftPersistenceProbability` object; null input -> null. */
export function decodeAttributeCraftPersistenceProbability(json: TdJsonRaw): AttributeCraftPersistenceProbability | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AttributeCraftPersistenceProbability = new AttributeCraftPersistenceProbability();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.persistence_chance_per_mille = decodeVector(record['persistence_chance_per_mille'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `attributeCraftPersistenceProbability` into TDLib JSON wire shape. */
export function encodeAttributeCraftPersistenceProbability(value: AttributeCraftPersistenceProbability): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'attributeCraftPersistenceProbability';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['persistence_chance_per_mille'] = encodeVector(value.persistence_chance_per_mille, (item: number): TdJsonRaw => item);
  return out;
}

/** auctionBid = AuctionBid — Describes a bid in an auction */
export class AuctionBid {
  type: 'auctionBid' = 'auctionBid';
  extra?: string;
  /** star_count: int53 — The number of Telegram Stars that were put in the bid */
  star_count: number = 0;
  /** bid_date: int32 — Point in time (Unix timestamp) when the bid was made */
  bid_date: number = 0;
  /** position: int32 — Position of the bid in the list of all bids */
  position: number = 0;
}

/** Decode a TDLib `auctionBid` object; null input -> null. */
export function decodeAuctionBid(json: TdJsonRaw): AuctionBid | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuctionBid = new AuctionBid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.bid_date = asNumber(record['bid_date']);
  out.position = asNumber(record['position']);
  return out;
}

/** Encode a `auctionBid` into TDLib JSON wire shape. */
export function encodeAuctionBid(value: AuctionBid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'auctionBid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['bid_date'] = value.bid_date;
  out['position'] = value.position;
  return out;
}

/** auctionRound = AuctionRound — Describes a round of an auction */
export class AuctionRound {
  type: 'auctionRound' = 'auctionRound';
  extra?: string;
  /** number: int32 — 1-based number of the round */
  number: number = 0;
  /** duration: int32 — Duration of the round, in seconds */
  duration: number = 0;
  /** extend_time: int32 — The number of seconds for which the round will be extended if there are changes in the top winners */
  extend_time: number = 0;
  /** top_winner_count: int32 — The number of top winners who trigger round extension if changed */
  top_winner_count: number = 0;
}

/** Decode a TDLib `auctionRound` object; null input -> null. */
export function decodeAuctionRound(json: TdJsonRaw): AuctionRound | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuctionRound = new AuctionRound();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.number = asNumber(record['number']);
  out.duration = asNumber(record['duration']);
  out.extend_time = asNumber(record['extend_time']);
  out.top_winner_count = asNumber(record['top_winner_count']);
  return out;
}

/** Encode a `auctionRound` into TDLib JSON wire shape. */
export function encodeAuctionRound(value: AuctionRound): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'auctionRound';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['number'] = value.number;
  out['duration'] = value.duration;
  out['extend_time'] = value.extend_time;
  out['top_winner_count'] = value.top_winner_count;
  return out;
}

/** auctionStateActive = AuctionState — Contains information about an ongoing or scheduled auction */
export class AuctionStateActive {
  type: 'auctionStateActive' = 'auctionStateActive';
  extra?: string;
  /** start_date: int32 — Point in time (Unix timestamp) when the auction started or will start */
  start_date: number = 0;
  /** end_date: int32 — Point in time (Unix timestamp) when the auction will be ended */
  end_date: number = 0;
  /** min_bid: int53 — The minimum possible bid in the auction in Telegram Stars */
  min_bid: number = 0;
  /** bid_levels: vector<auctionBid> — A sparse list of bids that were made in the auction */
  bid_levels: AuctionBid[] = [];
  /** top_bidder_user_ids: vector<int53> — User identifiers of at most 3 users with the biggest bids */
  top_bidder_user_ids: number[] = [];
  /** rounds: vector<auctionRound> — Rounds of the auction in which their duration or extension rules are changed */
  rounds: AuctionRound[] = [];
  /** current_round_end_date: int32 — Point in time (Unix timestamp) when the current round will end */
  current_round_end_date: number = 0;
  /** current_round_number: int32 — 1-based number of the current round */
  current_round_number: number = 0;
  /** total_round_count: int32 — The total number of rounds */
  total_round_count: number = 0;
  /** distributed_item_count: int32 — The number of items that were purchased on the auction by all users */
  distributed_item_count: number = 0;
  /** left_item_count: int32 — The number of items that have to be distributed on the auction */
  left_item_count: number = 0;
  /** acquired_item_count: int32 — The number of items that were purchased by the current user on the auction */
  acquired_item_count: number = 0;
  /** user_bid: userAuctionBid — Bid of the current user in the auction; may be null if none */
  user_bid: UserAuctionBid | null = null;
}

/** Decode a TDLib `auctionStateActive` object; null input -> null. */
export function decodeAuctionStateActive(json: TdJsonRaw): AuctionStateActive | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuctionStateActive = new AuctionStateActive();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.start_date = asNumber(record['start_date']);
  out.end_date = asNumber(record['end_date']);
  out.min_bid = asNumber(record['min_bid']);
  out.bid_levels = decodeVector(record['bid_levels'], (item: TdJsonRaw): AuctionBid => decodeAuctionBid(item) as AuctionBid);
  out.top_bidder_user_ids = decodeVector(record['top_bidder_user_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.rounds = decodeVector(record['rounds'], (item: TdJsonRaw): AuctionRound => decodeAuctionRound(item) as AuctionRound);
  out.current_round_end_date = asNumber(record['current_round_end_date']);
  out.current_round_number = asNumber(record['current_round_number']);
  out.total_round_count = asNumber(record['total_round_count']);
  out.distributed_item_count = asNumber(record['distributed_item_count']);
  out.left_item_count = asNumber(record['left_item_count']);
  out.acquired_item_count = asNumber(record['acquired_item_count']);
  out.user_bid = decodeUserAuctionBid(record['user_bid']);
  return out;
}

/** Encode a `auctionStateActive` into TDLib JSON wire shape. */
export function encodeAuctionStateActive(value: AuctionStateActive): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'auctionStateActive';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['start_date'] = value.start_date;
  out['end_date'] = value.end_date;
  out['min_bid'] = value.min_bid;
  out['bid_levels'] = encodeVector(value.bid_levels, (item: AuctionBid | null): TdJsonRaw => item === null ? null : encodeAuctionBid(item));
  out['top_bidder_user_ids'] = encodeVector(value.top_bidder_user_ids, (item: number): TdJsonRaw => item);
  out['rounds'] = encodeVector(value.rounds, (item: AuctionRound | null): TdJsonRaw => item === null ? null : encodeAuctionRound(item));
  out['current_round_end_date'] = value.current_round_end_date;
  out['current_round_number'] = value.current_round_number;
  out['total_round_count'] = value.total_round_count;
  out['distributed_item_count'] = value.distributed_item_count;
  out['left_item_count'] = value.left_item_count;
  out['acquired_item_count'] = value.acquired_item_count;
  out['user_bid'] = value.user_bid === null ? null : encodeUserAuctionBid(value.user_bid);
  return out;
}

/** auctionStateFinished = AuctionState — Contains information about a finished auction */
export class AuctionStateFinished {
  type: 'auctionStateFinished' = 'auctionStateFinished';
  extra?: string;
  /** start_date: int32 — Point in time (Unix timestamp) when the auction started */
  start_date: number = 0;
  /** end_date: int32 — Point in time (Unix timestamp) when the auction will be ended */
  end_date: number = 0;
  /** average_price: int53 — Average price of bought items in Telegram Stars */
  average_price: number = 0;
  /** acquired_item_count: int32 — The number of items that were purchased by the current user on the auction */
  acquired_item_count: number = 0;
  /** telegram_listed_item_count: int32 — Number of items from the auction being resold on Telegram */
  telegram_listed_item_count: number = 0;
  /** fragment_listed_item_count: int32 — Number of items from the auction being resold on Fragment */
  fragment_listed_item_count: number = 0;
  /** fragment_url: string — The HTTPS link to the Fragment for the resold items; may be empty if there are no such items being sold on Fragment */
  fragment_url: string = '';
}

/** Decode a TDLib `auctionStateFinished` object; null input -> null. */
export function decodeAuctionStateFinished(json: TdJsonRaw): AuctionStateFinished | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuctionStateFinished = new AuctionStateFinished();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.start_date = asNumber(record['start_date']);
  out.end_date = asNumber(record['end_date']);
  out.average_price = asNumber(record['average_price']);
  out.acquired_item_count = asNumber(record['acquired_item_count']);
  out.telegram_listed_item_count = asNumber(record['telegram_listed_item_count']);
  out.fragment_listed_item_count = asNumber(record['fragment_listed_item_count']);
  out.fragment_url = asString(record['fragment_url']);
  return out;
}

/** Encode a `auctionStateFinished` into TDLib JSON wire shape. */
export function encodeAuctionStateFinished(value: AuctionStateFinished): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'auctionStateFinished';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['start_date'] = value.start_date;
  out['end_date'] = value.end_date;
  out['average_price'] = value.average_price;
  out['acquired_item_count'] = value.acquired_item_count;
  out['telegram_listed_item_count'] = value.telegram_listed_item_count;
  out['fragment_listed_item_count'] = value.fragment_listed_item_count;
  out['fragment_url'] = value.fragment_url;
  return out;
}

/** audio = Audio — Describes an audio file. Audio is usually in MP3 or M4A format */
export class Audio {
  type: 'audio' = 'audio';
  extra?: string;
  /** duration: int32 — Duration of the audio, in seconds; as defined by the sender */
  duration: number = 0;
  /** title: string — Title of the audio; as defined by the sender */
  title: string = '';
  /** performer: string — Performer of the audio; as defined by the sender */
  performer: string = '';
  /** file_name: string — Original name of the file; as defined by the sender */
  file_name: string = '';
  /** mime_type: string — The MIME type of the file; as defined by the sender */
  mime_type: string = '';
  /** album_cover_minithumbnail: minithumbnail — The minithumbnail of the album cover; may be null */
  album_cover_minithumbnail: Minithumbnail | null = null;
  /** album_cover_thumbnail: thumbnail — The thumbnail of the album cover in JPEG format; as defined by the sender. The full size thumbnail is expected to be extracted from the downloaded audio file; may be null */
  album_cover_thumbnail: Thumbnail | null = null;
  /** external_album_covers: vector<thumbnail> — Album cover variants to use if the downloaded audio file contains no album cover. Provided thumbnail dimensions are approximate */
  external_album_covers: Thumbnail[] = [];
  /** audio: file — File containing the audio */
  audio: File | null = null;
}

/** Decode a TDLib `audio` object; null input -> null. */
export function decodeAudio(json: TdJsonRaw): Audio | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Audio = new Audio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.duration = asNumber(record['duration']);
  out.title = asString(record['title']);
  out.performer = asString(record['performer']);
  out.file_name = asString(record['file_name']);
  out.mime_type = asString(record['mime_type']);
  out.album_cover_minithumbnail = decodeMinithumbnail(record['album_cover_minithumbnail']);
  out.album_cover_thumbnail = decodeThumbnail(record['album_cover_thumbnail']);
  out.external_album_covers = decodeVector(record['external_album_covers'], (item: TdJsonRaw): Thumbnail => decodeThumbnail(item) as Thumbnail);
  out.audio = decodeFile(record['audio']);
  return out;
}

/** Encode a `audio` into TDLib JSON wire shape. */
export function encodeAudio(value: Audio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'audio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['duration'] = value.duration;
  out['title'] = value.title;
  out['performer'] = value.performer;
  out['file_name'] = value.file_name;
  out['mime_type'] = value.mime_type;
  out['album_cover_minithumbnail'] = value.album_cover_minithumbnail === null ? null : encodeMinithumbnail(value.album_cover_minithumbnail);
  out['album_cover_thumbnail'] = value.album_cover_thumbnail === null ? null : encodeThumbnail(value.album_cover_thumbnail);
  out['external_album_covers'] = encodeVector(value.external_album_covers, (item: Thumbnail | null): TdJsonRaw => item === null ? null : encodeThumbnail(item));
  out['audio'] = value.audio === null ? null : encodeFile(value.audio);
  return out;
}

/** audios = Audios — Contains a list of audio files */
export class Audios {
  type: 'audios' = 'audios';
  extra?: string;
  /** total_count: int32 — Approximate total number of audio files found */
  total_count: number = 0;
  /** audios: vector<audio> — List of audio files */
  audios: Audio[] = [];
}

/** Decode a TDLib `audios` object; null input -> null. */
export function decodeAudios(json: TdJsonRaw): Audios | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Audios = new Audios();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.audios = decodeVector(record['audios'], (item: TdJsonRaw): Audio => decodeAudio(item) as Audio);
  return out;
}

/** Encode a `audios` into TDLib JSON wire shape. */
export function encodeAudios(value: Audios): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'audios';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['audios'] = encodeVector(value.audios, (item: Audio | null): TdJsonRaw => item === null ? null : encodeAudio(item));
  return out;
}

/** authenticationCodeInfo = AuthenticationCodeInfo — Information about the authentication code that was sent */
export class AuthenticationCodeInfo {
  type: 'authenticationCodeInfo' = 'authenticationCodeInfo';
  extra?: string;
  /** phone_number: string — A phone number that is being authenticated */
  phone_number: string = '';
  /** type: AuthenticationCodeType — The way the code was sent to the user */
  type_: TdAuthenticationCodeType | null = null;
  /** next_type: AuthenticationCodeType — The way the next code will be sent to the user; may be null */
  next_type: TdAuthenticationCodeType | null = null;
  /** timeout: int32 — Timeout before the code can be re-sent, in seconds */
  timeout: number = 0;
}

/** Decode a TDLib `authenticationCodeInfo` object; null input -> null. */
export function decodeAuthenticationCodeInfo(json: TdJsonRaw): AuthenticationCodeInfo | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeInfo = new AuthenticationCodeInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  out.type_ = decodeTdAuthenticationCodeType(record['type']);
  out.next_type = decodeTdAuthenticationCodeType(record['next_type']);
  out.timeout = asNumber(record['timeout']);
  return out;
}

/** Encode a `authenticationCodeInfo` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeInfo(value: AuthenticationCodeInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  out['type'] = value.type_ === null ? null : encodeTdAuthenticationCodeType(value.type_);
  out['next_type'] = value.next_type === null ? null : encodeTdAuthenticationCodeType(value.next_type);
  out['timeout'] = value.timeout;
  return out;
}

/** authenticationCodeTypeCall = AuthenticationCodeType — A digit-only authentication code is delivered via a phone call to the specified phone number */
export class AuthenticationCodeTypeCall {
  type: 'authenticationCodeTypeCall' = 'authenticationCodeTypeCall';
  extra?: string;
  /** length: int32 — Length of the code */
  length: number = 0;
}

/** Decode a TDLib `authenticationCodeTypeCall` object; null input -> null. */
export function decodeAuthenticationCodeTypeCall(json: TdJsonRaw): AuthenticationCodeTypeCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeCall = new AuthenticationCodeTypeCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.length = asNumber(record['length']);
  return out;
}

/** Encode a `authenticationCodeTypeCall` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeCall(value: AuthenticationCodeTypeCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['length'] = value.length;
  return out;
}

/** authenticationCodeTypeFirebaseAndroid = AuthenticationCodeType — A digit-only authentication code is delivered via Firebase Authentication to the official Android application */
export class AuthenticationCodeTypeFirebaseAndroid {
  type: 'authenticationCodeTypeFirebaseAndroid' = 'authenticationCodeTypeFirebaseAndroid';
  extra?: string;
  /** device_verification_parameters: FirebaseDeviceVerificationParameters — Parameters to be used for device verification */
  device_verification_parameters: TdFirebaseDeviceVerificationParameters | null = null;
  /** length: int32 — Length of the code */
  length: number = 0;
}

/** Decode a TDLib `authenticationCodeTypeFirebaseAndroid` object; null input -> null. */
export function decodeAuthenticationCodeTypeFirebaseAndroid(json: TdJsonRaw): AuthenticationCodeTypeFirebaseAndroid | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeFirebaseAndroid = new AuthenticationCodeTypeFirebaseAndroid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.device_verification_parameters = decodeTdFirebaseDeviceVerificationParameters(record['device_verification_parameters']);
  out.length = asNumber(record['length']);
  return out;
}

/** Encode a `authenticationCodeTypeFirebaseAndroid` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeFirebaseAndroid(value: AuthenticationCodeTypeFirebaseAndroid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeFirebaseAndroid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['device_verification_parameters'] = value.device_verification_parameters === null ? null : encodeTdFirebaseDeviceVerificationParameters(value.device_verification_parameters);
  out['length'] = value.length;
  return out;
}

/** authenticationCodeTypeFirebaseIos = AuthenticationCodeType — A digit-only authentication code is delivered via Firebase Authentication to the official iOS application */
export class AuthenticationCodeTypeFirebaseIos {
  type: 'authenticationCodeTypeFirebaseIos' = 'authenticationCodeTypeFirebaseIos';
  extra?: string;
  /** receipt: string — Receipt of successful application token validation to compare with receipt from push notification */
  receipt: string = '';
  /** push_timeout: int32 — Time after the next authentication method is expected to be used if verification push notification isn't received, in seconds */
  push_timeout: number = 0;
  /** length: int32 — Length of the code */
  length: number = 0;
}

/** Decode a TDLib `authenticationCodeTypeFirebaseIos` object; null input -> null. */
export function decodeAuthenticationCodeTypeFirebaseIos(json: TdJsonRaw): AuthenticationCodeTypeFirebaseIos | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeFirebaseIos = new AuthenticationCodeTypeFirebaseIos();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.receipt = asString(record['receipt']);
  out.push_timeout = asNumber(record['push_timeout']);
  out.length = asNumber(record['length']);
  return out;
}

/** Encode a `authenticationCodeTypeFirebaseIos` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeFirebaseIos(value: AuthenticationCodeTypeFirebaseIos): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeFirebaseIos';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['receipt'] = value.receipt;
  out['push_timeout'] = value.push_timeout;
  out['length'] = value.length;
  return out;
}

/** authenticationCodeTypeFlashCall = AuthenticationCodeType — An authentication code is delivered by an immediately canceled call to the specified phone number. The phone number that calls is the code that must be entered automatically */
export class AuthenticationCodeTypeFlashCall {
  type: 'authenticationCodeTypeFlashCall' = 'authenticationCodeTypeFlashCall';
  extra?: string;
  /** pattern: string — Pattern of the phone number from which the call will be made */
  pattern: string = '';
}

/** Decode a TDLib `authenticationCodeTypeFlashCall` object; null input -> null. */
export function decodeAuthenticationCodeTypeFlashCall(json: TdJsonRaw): AuthenticationCodeTypeFlashCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeFlashCall = new AuthenticationCodeTypeFlashCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.pattern = asString(record['pattern']);
  return out;
}

/** Encode a `authenticationCodeTypeFlashCall` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeFlashCall(value: AuthenticationCodeTypeFlashCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeFlashCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['pattern'] = value.pattern;
  return out;
}

/** authenticationCodeTypeFragment = AuthenticationCodeType — A digit-only authentication code is delivered to https://fragment.com. The user must be logged in there via a wallet owning the phone number's NFT */
export class AuthenticationCodeTypeFragment {
  type: 'authenticationCodeTypeFragment' = 'authenticationCodeTypeFragment';
  extra?: string;
  /** url: string — URL to open to receive the code */
  url: string = '';
  /** length: int32 — Length of the code */
  length: number = 0;
}

/** Decode a TDLib `authenticationCodeTypeFragment` object; null input -> null. */
export function decodeAuthenticationCodeTypeFragment(json: TdJsonRaw): AuthenticationCodeTypeFragment | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeFragment = new AuthenticationCodeTypeFragment();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.length = asNumber(record['length']);
  return out;
}

/** Encode a `authenticationCodeTypeFragment` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeFragment(value: AuthenticationCodeTypeFragment): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeFragment';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['length'] = value.length;
  return out;
}

/** authenticationCodeTypeMissedCall = AuthenticationCodeType — An authentication code is delivered by an immediately canceled call to the specified phone number. The last digits of the phone number that calls are the code that must be enter... */
export class AuthenticationCodeTypeMissedCall {
  type: 'authenticationCodeTypeMissedCall' = 'authenticationCodeTypeMissedCall';
  extra?: string;
  /** phone_number_prefix: string — Prefix of the phone number from which the call will be made */
  phone_number_prefix: string = '';
  /** length: int32 — Number of digits in the code, excluding the prefix */
  length: number = 0;
}

/** Decode a TDLib `authenticationCodeTypeMissedCall` object; null input -> null. */
export function decodeAuthenticationCodeTypeMissedCall(json: TdJsonRaw): AuthenticationCodeTypeMissedCall | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeMissedCall = new AuthenticationCodeTypeMissedCall();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number_prefix = asString(record['phone_number_prefix']);
  out.length = asNumber(record['length']);
  return out;
}

/** Encode a `authenticationCodeTypeMissedCall` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeMissedCall(value: AuthenticationCodeTypeMissedCall): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeMissedCall';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number_prefix'] = value.phone_number_prefix;
  out['length'] = value.length;
  return out;
}

/** authenticationCodeTypeSms = AuthenticationCodeType — A digit-only authentication code is delivered via an SMS message to the specified phone number; non-official applications may not receive this type of code */
export class AuthenticationCodeTypeSms {
  type: 'authenticationCodeTypeSms' = 'authenticationCodeTypeSms';
  extra?: string;
  /** length: int32 — Length of the code */
  length: number = 0;
}

/** Decode a TDLib `authenticationCodeTypeSms` object; null input -> null. */
export function decodeAuthenticationCodeTypeSms(json: TdJsonRaw): AuthenticationCodeTypeSms | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeSms = new AuthenticationCodeTypeSms();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.length = asNumber(record['length']);
  return out;
}

/** Encode a `authenticationCodeTypeSms` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeSms(value: AuthenticationCodeTypeSms): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeSms';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['length'] = value.length;
  return out;
}

/** authenticationCodeTypeSmsPhrase = AuthenticationCodeType — An authentication code is a phrase from multiple words delivered via an SMS message to the specified phone number; non-official applications may not receive this type of code */
export class AuthenticationCodeTypeSmsPhrase {
  type: 'authenticationCodeTypeSmsPhrase' = 'authenticationCodeTypeSmsPhrase';
  extra?: string;
  /** first_word: string — The first word of the phrase if known */
  first_word: string = '';
}

/** Decode a TDLib `authenticationCodeTypeSmsPhrase` object; null input -> null. */
export function decodeAuthenticationCodeTypeSmsPhrase(json: TdJsonRaw): AuthenticationCodeTypeSmsPhrase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeSmsPhrase = new AuthenticationCodeTypeSmsPhrase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.first_word = asString(record['first_word']);
  return out;
}

/** Encode a `authenticationCodeTypeSmsPhrase` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeSmsPhrase(value: AuthenticationCodeTypeSmsPhrase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeSmsPhrase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['first_word'] = value.first_word;
  return out;
}

/** authenticationCodeTypeSmsWord = AuthenticationCodeType — An authentication code is a word delivered via an SMS message to the specified phone number; non-official applications may not receive this type of code */
export class AuthenticationCodeTypeSmsWord {
  type: 'authenticationCodeTypeSmsWord' = 'authenticationCodeTypeSmsWord';
  extra?: string;
  /** first_letter: string — The first letters of the word if known */
  first_letter: string = '';
}

/** Decode a TDLib `authenticationCodeTypeSmsWord` object; null input -> null. */
export function decodeAuthenticationCodeTypeSmsWord(json: TdJsonRaw): AuthenticationCodeTypeSmsWord | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeSmsWord = new AuthenticationCodeTypeSmsWord();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.first_letter = asString(record['first_letter']);
  return out;
}

/** Encode a `authenticationCodeTypeSmsWord` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeSmsWord(value: AuthenticationCodeTypeSmsWord): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeSmsWord';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['first_letter'] = value.first_letter;
  return out;
}

/** authenticationCodeTypeTelegramMessage = AuthenticationCodeType — A digit-only authentication code is delivered via a private Telegram message, which can be viewed from another active session */
export class AuthenticationCodeTypeTelegramMessage {
  type: 'authenticationCodeTypeTelegramMessage' = 'authenticationCodeTypeTelegramMessage';
  extra?: string;
  /** length: int32 — Length of the code */
  length: number = 0;
}

/** Decode a TDLib `authenticationCodeTypeTelegramMessage` object; null input -> null. */
export function decodeAuthenticationCodeTypeTelegramMessage(json: TdJsonRaw): AuthenticationCodeTypeTelegramMessage | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthenticationCodeTypeTelegramMessage = new AuthenticationCodeTypeTelegramMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.length = asNumber(record['length']);
  return out;
}

/** Encode a `authenticationCodeTypeTelegramMessage` into TDLib JSON wire shape. */
export function encodeAuthenticationCodeTypeTelegramMessage(value: AuthenticationCodeTypeTelegramMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authenticationCodeTypeTelegramMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['length'] = value.length;
  return out;
}

/** authorizationStateClosed = AuthorizationState — TDLib client is in its final state. All databases are closed and all resources are released. No other updates will be received after this. All queries will be responded to with error code 5... */
export class AuthorizationStateClosed {
  type: 'authorizationStateClosed' = 'authorizationStateClosed';
  extra?: string;
}

/** Decode a TDLib `authorizationStateClosed` object; null input -> null. */
export function decodeAuthorizationStateClosed(json: TdJsonRaw): AuthorizationStateClosed | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateClosed = new AuthorizationStateClosed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `authorizationStateClosed` into TDLib JSON wire shape. */
export function encodeAuthorizationStateClosed(value: AuthorizationStateClosed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateClosed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** authorizationStateClosing = AuthorizationState — TDLib is closing, all subsequent queries will be answered with the error 500. Note that closing TDLib can take a while. All resources will be freed only after authorizationStateClosed has ... */
export class AuthorizationStateClosing {
  type: 'authorizationStateClosing' = 'authorizationStateClosing';
  extra?: string;
}

/** Decode a TDLib `authorizationStateClosing` object; null input -> null. */
export function decodeAuthorizationStateClosing(json: TdJsonRaw): AuthorizationStateClosing | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateClosing = new AuthorizationStateClosing();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `authorizationStateClosing` into TDLib JSON wire shape. */
export function encodeAuthorizationStateClosing(value: AuthorizationStateClosing): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateClosing';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** authorizationStateLoggingOut = AuthorizationState — The user is currently logging out */
export class AuthorizationStateLoggingOut {
  type: 'authorizationStateLoggingOut' = 'authorizationStateLoggingOut';
  extra?: string;
}

/** Decode a TDLib `authorizationStateLoggingOut` object; null input -> null. */
export function decodeAuthorizationStateLoggingOut(json: TdJsonRaw): AuthorizationStateLoggingOut | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateLoggingOut = new AuthorizationStateLoggingOut();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `authorizationStateLoggingOut` into TDLib JSON wire shape. */
export function encodeAuthorizationStateLoggingOut(value: AuthorizationStateLoggingOut): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateLoggingOut';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** authorizationStateReady = AuthorizationState — The user has been successfully authorized. TDLib is now ready to answer general requests */
export class AuthorizationStateReady {
  type: 'authorizationStateReady' = 'authorizationStateReady';
  extra?: string;
}

/** Decode a TDLib `authorizationStateReady` object; null input -> null. */
export function decodeAuthorizationStateReady(json: TdJsonRaw): AuthorizationStateReady | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateReady = new AuthorizationStateReady();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `authorizationStateReady` into TDLib JSON wire shape. */
export function encodeAuthorizationStateReady(value: AuthorizationStateReady): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateReady';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** authorizationStateWaitCode = AuthorizationState — TDLib needs the user's authentication code to authorize. Call checkAuthenticationCode to check the code */
export class AuthorizationStateWaitCode {
  type: 'authorizationStateWaitCode' = 'authorizationStateWaitCode';
  extra?: string;
  /** code_info: authenticationCodeInfo — Information about the authorization code that was sent */
  code_info: AuthenticationCodeInfo | null = null;
}

/** Decode a TDLib `authorizationStateWaitCode` object; null input -> null. */
export function decodeAuthorizationStateWaitCode(json: TdJsonRaw): AuthorizationStateWaitCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateWaitCode = new AuthorizationStateWaitCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.code_info = decodeAuthenticationCodeInfo(record['code_info']);
  return out;
}

/** Encode a `authorizationStateWaitCode` into TDLib JSON wire shape. */
export function encodeAuthorizationStateWaitCode(value: AuthorizationStateWaitCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateWaitCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['code_info'] = value.code_info === null ? null : encodeAuthenticationCodeInfo(value.code_info);
  return out;
}

/** authorizationStateWaitEmailAddress = AuthorizationState — TDLib needs the user's email address to authorize. Call setAuthenticationEmailAddress to provide the email address, or directly call checkAuthenticationEmailCode with Apple ID/Goo... */
export class AuthorizationStateWaitEmailAddress {
  type: 'authorizationStateWaitEmailAddress' = 'authorizationStateWaitEmailAddress';
  extra?: string;
  /** allow_apple_id: Bool — True, if authorization through Apple ID is allowed */
  allow_apple_id: boolean = false;
  /** allow_google_id: Bool — True, if authorization through Google ID is allowed */
  allow_google_id: boolean = false;
}

/** Decode a TDLib `authorizationStateWaitEmailAddress` object; null input -> null. */
export function decodeAuthorizationStateWaitEmailAddress(json: TdJsonRaw): AuthorizationStateWaitEmailAddress | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateWaitEmailAddress = new AuthorizationStateWaitEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.allow_apple_id = asBool(record['allow_apple_id']);
  out.allow_google_id = asBool(record['allow_google_id']);
  return out;
}

/** Encode a `authorizationStateWaitEmailAddress` into TDLib JSON wire shape. */
export function encodeAuthorizationStateWaitEmailAddress(value: AuthorizationStateWaitEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateWaitEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['allow_apple_id'] = value.allow_apple_id;
  out['allow_google_id'] = value.allow_google_id;
  return out;
}

/** authorizationStateWaitEmailCode = AuthorizationState — TDLib needs the user's authentication code sent to an email address to authorize. Call checkAuthenticationEmailCode to provide the code */
export class AuthorizationStateWaitEmailCode {
  type: 'authorizationStateWaitEmailCode' = 'authorizationStateWaitEmailCode';
  extra?: string;
  /** allow_apple_id: Bool — True, if authorization through Apple ID is allowed */
  allow_apple_id: boolean = false;
  /** allow_google_id: Bool — True, if authorization through Google ID is allowed */
  allow_google_id: boolean = false;
  /** code_info: emailAddressAuthenticationCodeInfo — Information about the sent authentication code */
  code_info: EmailAddressAuthenticationCodeInfo | null = null;
  /** email_address_reset_state: EmailAddressResetState — Reset state of the email address; may be null if the email address can't be reset */
  email_address_reset_state: TdEmailAddressResetState | null = null;
}

/** Decode a TDLib `authorizationStateWaitEmailCode` object; null input -> null. */
export function decodeAuthorizationStateWaitEmailCode(json: TdJsonRaw): AuthorizationStateWaitEmailCode | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateWaitEmailCode = new AuthorizationStateWaitEmailCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.allow_apple_id = asBool(record['allow_apple_id']);
  out.allow_google_id = asBool(record['allow_google_id']);
  out.code_info = decodeEmailAddressAuthenticationCodeInfo(record['code_info']);
  out.email_address_reset_state = decodeTdEmailAddressResetState(record['email_address_reset_state']);
  return out;
}

/** Encode a `authorizationStateWaitEmailCode` into TDLib JSON wire shape. */
export function encodeAuthorizationStateWaitEmailCode(value: AuthorizationStateWaitEmailCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateWaitEmailCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['allow_apple_id'] = value.allow_apple_id;
  out['allow_google_id'] = value.allow_google_id;
  out['code_info'] = value.code_info === null ? null : encodeEmailAddressAuthenticationCodeInfo(value.code_info);
  out['email_address_reset_state'] = value.email_address_reset_state === null ? null : encodeTdEmailAddressResetState(value.email_address_reset_state);
  return out;
}

/** authorizationStateWaitOtherDeviceConfirmation = AuthorizationState — The user needs to confirm authorization on another logged in device by scanning a QR code with the provided link */
export class AuthorizationStateWaitOtherDeviceConfirmation {
  type: 'authorizationStateWaitOtherDeviceConfirmation' = 'authorizationStateWaitOtherDeviceConfirmation';
  extra?: string;
  /** link: string — A tg:// URL for the QR code. The link will be updated frequently */
  link: string = '';
}

/** Decode a TDLib `authorizationStateWaitOtherDeviceConfirmation` object; null input -> null. */
export function decodeAuthorizationStateWaitOtherDeviceConfirmation(json: TdJsonRaw): AuthorizationStateWaitOtherDeviceConfirmation | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateWaitOtherDeviceConfirmation = new AuthorizationStateWaitOtherDeviceConfirmation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.link = asString(record['link']);
  return out;
}

/** Encode a `authorizationStateWaitOtherDeviceConfirmation` into TDLib JSON wire shape. */
export function encodeAuthorizationStateWaitOtherDeviceConfirmation(value: AuthorizationStateWaitOtherDeviceConfirmation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateWaitOtherDeviceConfirmation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['link'] = value.link;
  return out;
}

/** authorizationStateWaitPassword = AuthorizationState — The user has been authorized, but needs to enter a 2-step verification password to start using the application. Call checkAuthenticationPassword to provide the password, or requestAut... */
export class AuthorizationStateWaitPassword {
  type: 'authorizationStateWaitPassword' = 'authorizationStateWaitPassword';
  extra?: string;
  /** password_hint: string — Hint for the password; may be empty */
  password_hint: string = '';
  /** has_recovery_email_address: Bool — True, if a recovery email address has been set up */
  has_recovery_email_address: boolean = false;
  /** has_passport_data: Bool — True, if some Telegram Passport elements were saved */
  has_passport_data: boolean = false;
  /** recovery_email_address_pattern: string — Pattern of the email address to which the recovery email was sent; empty until a recovery email has been sent */
  recovery_email_address_pattern: string = '';
}

/** Decode a TDLib `authorizationStateWaitPassword` object; null input -> null. */
export function decodeAuthorizationStateWaitPassword(json: TdJsonRaw): AuthorizationStateWaitPassword | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateWaitPassword = new AuthorizationStateWaitPassword();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.password_hint = asString(record['password_hint']);
  out.has_recovery_email_address = asBool(record['has_recovery_email_address']);
  out.has_passport_data = asBool(record['has_passport_data']);
  out.recovery_email_address_pattern = asString(record['recovery_email_address_pattern']);
  return out;
}

/** Encode a `authorizationStateWaitPassword` into TDLib JSON wire shape. */
export function encodeAuthorizationStateWaitPassword(value: AuthorizationStateWaitPassword): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateWaitPassword';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['password_hint'] = value.password_hint;
  out['has_recovery_email_address'] = value.has_recovery_email_address;
  out['has_passport_data'] = value.has_passport_data;
  out['recovery_email_address_pattern'] = value.recovery_email_address_pattern;
  return out;
}

/** authorizationStateWaitPhoneNumber = AuthorizationState — TDLib needs the user's phone number to authorize. Call setAuthenticationPhoneNumber to provide the phone number, or use requestQrCodeAuthentication, getAuthenticationPasskeyParamet... */
export class AuthorizationStateWaitPhoneNumber {
  type: 'authorizationStateWaitPhoneNumber' = 'authorizationStateWaitPhoneNumber';
  extra?: string;
}

/** Decode a TDLib `authorizationStateWaitPhoneNumber` object; null input -> null. */
export function decodeAuthorizationStateWaitPhoneNumber(json: TdJsonRaw): AuthorizationStateWaitPhoneNumber | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateWaitPhoneNumber = new AuthorizationStateWaitPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `authorizationStateWaitPhoneNumber` into TDLib JSON wire shape. */
export function encodeAuthorizationStateWaitPhoneNumber(value: AuthorizationStateWaitPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateWaitPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** authorizationStateWaitPremiumPurchase = AuthorizationState — The user must buy Telegram Premium as an in-store purchase to log in. Call checkAuthenticationPremiumPurchase and then setAuthenticationPremiumPurchaseTransaction */
export class AuthorizationStateWaitPremiumPurchase {
  type: 'authorizationStateWaitPremiumPurchase' = 'authorizationStateWaitPremiumPurchase';
  extra?: string;
  /** store_product_id: string — Identifier of the store product that must be bought */
  store_product_id: string = '';
  /** premium_day_count: int32 — Duration of the Telegram Premium subscription after the purchase; may be 0 if Telegram Premium subscription will not be granted */
  premium_day_count: number = 0;
  /** support_email_address: string — Email address to use for support if the user has issues with Telegram Premium purchase */
  support_email_address: string = '';
  /** support_email_subject: string — Subject for the email sent to the support email address */
  support_email_subject: string = '';
}

/** Decode a TDLib `authorizationStateWaitPremiumPurchase` object; null input -> null. */
export function decodeAuthorizationStateWaitPremiumPurchase(json: TdJsonRaw): AuthorizationStateWaitPremiumPurchase | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateWaitPremiumPurchase = new AuthorizationStateWaitPremiumPurchase();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.store_product_id = asString(record['store_product_id']);
  out.premium_day_count = asNumber(record['premium_day_count']);
  out.support_email_address = asString(record['support_email_address']);
  out.support_email_subject = asString(record['support_email_subject']);
  return out;
}

/** Encode a `authorizationStateWaitPremiumPurchase` into TDLib JSON wire shape. */
export function encodeAuthorizationStateWaitPremiumPurchase(value: AuthorizationStateWaitPremiumPurchase): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateWaitPremiumPurchase';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['store_product_id'] = value.store_product_id;
  out['premium_day_count'] = value.premium_day_count;
  out['support_email_address'] = value.support_email_address;
  out['support_email_subject'] = value.support_email_subject;
  return out;
}

/** authorizationStateWaitRegistration = AuthorizationState — The user is unregistered and needs to accept terms of service and enter their first name and last name to finish registration. Call registerUser to accept the terms of service and... */
export class AuthorizationStateWaitRegistration {
  type: 'authorizationStateWaitRegistration' = 'authorizationStateWaitRegistration';
  extra?: string;
  /** terms_of_service: termsOfService — Telegram terms of service */
  terms_of_service: TermsOfService | null = null;
}

/** Decode a TDLib `authorizationStateWaitRegistration` object; null input -> null. */
export function decodeAuthorizationStateWaitRegistration(json: TdJsonRaw): AuthorizationStateWaitRegistration | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateWaitRegistration = new AuthorizationStateWaitRegistration();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.terms_of_service = decodeTermsOfService(record['terms_of_service']);
  return out;
}

/** Encode a `authorizationStateWaitRegistration` into TDLib JSON wire shape. */
export function encodeAuthorizationStateWaitRegistration(value: AuthorizationStateWaitRegistration): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateWaitRegistration';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['terms_of_service'] = value.terms_of_service === null ? null : encodeTermsOfService(value.terms_of_service);
  return out;
}

/** authorizationStateWaitTdlibParameters = AuthorizationState — Initialization parameters are needed. Call setTdlibParameters to provide them */
export class AuthorizationStateWaitTdlibParameters {
  type: 'authorizationStateWaitTdlibParameters' = 'authorizationStateWaitTdlibParameters';
  extra?: string;
}

/** Decode a TDLib `authorizationStateWaitTdlibParameters` object; null input -> null. */
export function decodeAuthorizationStateWaitTdlibParameters(json: TdJsonRaw): AuthorizationStateWaitTdlibParameters | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AuthorizationStateWaitTdlibParameters = new AuthorizationStateWaitTdlibParameters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `authorizationStateWaitTdlibParameters` into TDLib JSON wire shape. */
export function encodeAuthorizationStateWaitTdlibParameters(value: AuthorizationStateWaitTdlibParameters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'authorizationStateWaitTdlibParameters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** autoDownloadSettings = AutoDownloadSettings — Contains auto-download settings */
export class AutoDownloadSettings {
  type: 'autoDownloadSettings' = 'autoDownloadSettings';
  extra?: string;
  /** is_auto_download_enabled: Bool — True, if the auto-download is enabled */
  is_auto_download_enabled: boolean = false;
  /** max_photo_file_size: int32 — The maximum size of a photo file to be auto-downloaded, in bytes */
  max_photo_file_size: number = 0;
  /** max_video_file_size: int53 — The maximum size of a video file to be auto-downloaded, in bytes */
  max_video_file_size: number = 0;
  /** max_other_file_size: int53 — The maximum size of other file types to be auto-downloaded, in bytes */
  max_other_file_size: number = 0;
  /** video_upload_bitrate: int32 — The maximum suggested bitrate for uploaded videos, in kbit/s */
  video_upload_bitrate: number = 0;
  /** preload_large_videos: Bool — True, if the beginning of video files needs to be preloaded for instant playback */
  preload_large_videos: boolean = false;
  /** preload_next_audio: Bool — True, if the next audio track needs to be preloaded while the user is listening to an audio file */
  preload_next_audio: boolean = false;
  /** preload_stories: Bool — True, if stories need to be preloaded */
  preload_stories: boolean = false;
  /** use_less_data_for_calls: Bool — True, if "use less data for calls" option needs to be enabled */
  use_less_data_for_calls: boolean = false;
}

/** Decode a TDLib `autoDownloadSettings` object; null input -> null. */
export function decodeAutoDownloadSettings(json: TdJsonRaw): AutoDownloadSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AutoDownloadSettings = new AutoDownloadSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_auto_download_enabled = asBool(record['is_auto_download_enabled']);
  out.max_photo_file_size = asNumber(record['max_photo_file_size']);
  out.max_video_file_size = asNumber(record['max_video_file_size']);
  out.max_other_file_size = asNumber(record['max_other_file_size']);
  out.video_upload_bitrate = asNumber(record['video_upload_bitrate']);
  out.preload_large_videos = asBool(record['preload_large_videos']);
  out.preload_next_audio = asBool(record['preload_next_audio']);
  out.preload_stories = asBool(record['preload_stories']);
  out.use_less_data_for_calls = asBool(record['use_less_data_for_calls']);
  return out;
}

/** Encode a `autoDownloadSettings` into TDLib JSON wire shape. */
export function encodeAutoDownloadSettings(value: AutoDownloadSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'autoDownloadSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_auto_download_enabled'] = value.is_auto_download_enabled;
  out['max_photo_file_size'] = value.max_photo_file_size;
  out['max_video_file_size'] = value.max_video_file_size;
  out['max_other_file_size'] = value.max_other_file_size;
  out['video_upload_bitrate'] = value.video_upload_bitrate;
  out['preload_large_videos'] = value.preload_large_videos;
  out['preload_next_audio'] = value.preload_next_audio;
  out['preload_stories'] = value.preload_stories;
  out['use_less_data_for_calls'] = value.use_less_data_for_calls;
  return out;
}

/** autoDownloadSettingsPresets = AutoDownloadSettingsPresets — Contains auto-download settings presets for the current user */
export class AutoDownloadSettingsPresets {
  type: 'autoDownloadSettingsPresets' = 'autoDownloadSettingsPresets';
  extra?: string;
  /** low: autoDownloadSettings — Preset with lowest settings; expected to be used by default when roaming */
  low: AutoDownloadSettings | null = null;
  /** medium: autoDownloadSettings — Preset with medium settings; expected to be used by default when using mobile data */
  medium: AutoDownloadSettings | null = null;
  /** high: autoDownloadSettings — Preset with highest settings; expected to be used by default when connected on Wi-Fi */
  high: AutoDownloadSettings | null = null;
}

/** Decode a TDLib `autoDownloadSettingsPresets` object; null input -> null. */
export function decodeAutoDownloadSettingsPresets(json: TdJsonRaw): AutoDownloadSettingsPresets | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AutoDownloadSettingsPresets = new AutoDownloadSettingsPresets();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.low = decodeAutoDownloadSettings(record['low']);
  out.medium = decodeAutoDownloadSettings(record['medium']);
  out.high = decodeAutoDownloadSettings(record['high']);
  return out;
}

/** Encode a `autoDownloadSettingsPresets` into TDLib JSON wire shape. */
export function encodeAutoDownloadSettingsPresets(value: AutoDownloadSettingsPresets): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'autoDownloadSettingsPresets';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['low'] = value.low === null ? null : encodeAutoDownloadSettings(value.low);
  out['medium'] = value.medium === null ? null : encodeAutoDownloadSettings(value.medium);
  out['high'] = value.high === null ? null : encodeAutoDownloadSettings(value.high);
  return out;
}

/** autosaveSettings = AutosaveSettings — Describes autosave settings */
export class AutosaveSettings {
  type: 'autosaveSettings' = 'autosaveSettings';
  extra?: string;
  /** private_chat_settings: scopeAutosaveSettings — Default autosave settings for private chats */
  private_chat_settings: ScopeAutosaveSettings | null = null;
  /** group_settings: scopeAutosaveSettings — Default autosave settings for basic group and supergroup chats */
  group_settings: ScopeAutosaveSettings | null = null;
  /** channel_settings: scopeAutosaveSettings — Default autosave settings for channel chats */
  channel_settings: ScopeAutosaveSettings | null = null;
  /** exceptions: vector<autosaveSettingsException> — Autosave settings for specific chats */
  exceptions: AutosaveSettingsException[] = [];
}

/** Decode a TDLib `autosaveSettings` object; null input -> null. */
export function decodeAutosaveSettings(json: TdJsonRaw): AutosaveSettings | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AutosaveSettings = new AutosaveSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.private_chat_settings = decodeScopeAutosaveSettings(record['private_chat_settings']);
  out.group_settings = decodeScopeAutosaveSettings(record['group_settings']);
  out.channel_settings = decodeScopeAutosaveSettings(record['channel_settings']);
  out.exceptions = decodeVector(record['exceptions'], (item: TdJsonRaw): AutosaveSettingsException => decodeAutosaveSettingsException(item) as AutosaveSettingsException);
  return out;
}

/** Encode a `autosaveSettings` into TDLib JSON wire shape. */
export function encodeAutosaveSettings(value: AutosaveSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'autosaveSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['private_chat_settings'] = value.private_chat_settings === null ? null : encodeScopeAutosaveSettings(value.private_chat_settings);
  out['group_settings'] = value.group_settings === null ? null : encodeScopeAutosaveSettings(value.group_settings);
  out['channel_settings'] = value.channel_settings === null ? null : encodeScopeAutosaveSettings(value.channel_settings);
  out['exceptions'] = encodeVector(value.exceptions, (item: AutosaveSettingsException | null): TdJsonRaw => item === null ? null : encodeAutosaveSettingsException(item));
  return out;
}

/** autosaveSettingsException = AutosaveSettingsException — Contains autosave settings for a chat, which overrides default settings for the corresponding scope */
export class AutosaveSettingsException {
  type: 'autosaveSettingsException' = 'autosaveSettingsException';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** settings: scopeAutosaveSettings — Autosave settings for the chat */
  settings: ScopeAutosaveSettings | null = null;
}

/** Decode a TDLib `autosaveSettingsException` object; null input -> null. */
export function decodeAutosaveSettingsException(json: TdJsonRaw): AutosaveSettingsException | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AutosaveSettingsException = new AutosaveSettingsException();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.settings = decodeScopeAutosaveSettings(record['settings']);
  return out;
}

/** Encode a `autosaveSettingsException` into TDLib JSON wire shape. */
export function encodeAutosaveSettingsException(value: AutosaveSettingsException): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'autosaveSettingsException';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['settings'] = value.settings === null ? null : encodeScopeAutosaveSettings(value.settings);
  return out;
}

/** autosaveSettingsScopeChannelChats = AutosaveSettingsScope — Autosave settings applied to all channel chats without chat-specific settings */
export class AutosaveSettingsScopeChannelChats {
  type: 'autosaveSettingsScopeChannelChats' = 'autosaveSettingsScopeChannelChats';
  extra?: string;
}

/** Decode a TDLib `autosaveSettingsScopeChannelChats` object; null input -> null. */
export function decodeAutosaveSettingsScopeChannelChats(json: TdJsonRaw): AutosaveSettingsScopeChannelChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AutosaveSettingsScopeChannelChats = new AutosaveSettingsScopeChannelChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `autosaveSettingsScopeChannelChats` into TDLib JSON wire shape. */
export function encodeAutosaveSettingsScopeChannelChats(value: AutosaveSettingsScopeChannelChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'autosaveSettingsScopeChannelChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** autosaveSettingsScopeChat = AutosaveSettingsScope — Autosave settings applied to a chat */
export class AutosaveSettingsScopeChat {
  type: 'autosaveSettingsScopeChat' = 'autosaveSettingsScopeChat';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
}

/** Decode a TDLib `autosaveSettingsScopeChat` object; null input -> null. */
export function decodeAutosaveSettingsScopeChat(json: TdJsonRaw): AutosaveSettingsScopeChat | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AutosaveSettingsScopeChat = new AutosaveSettingsScopeChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `autosaveSettingsScopeChat` into TDLib JSON wire shape. */
export function encodeAutosaveSettingsScopeChat(value: AutosaveSettingsScopeChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'autosaveSettingsScopeChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** autosaveSettingsScopeGroupChats = AutosaveSettingsScope — Autosave settings applied to all basic group and supergroup chats without chat-specific settings */
export class AutosaveSettingsScopeGroupChats {
  type: 'autosaveSettingsScopeGroupChats' = 'autosaveSettingsScopeGroupChats';
  extra?: string;
}

/** Decode a TDLib `autosaveSettingsScopeGroupChats` object; null input -> null. */
export function decodeAutosaveSettingsScopeGroupChats(json: TdJsonRaw): AutosaveSettingsScopeGroupChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AutosaveSettingsScopeGroupChats = new AutosaveSettingsScopeGroupChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `autosaveSettingsScopeGroupChats` into TDLib JSON wire shape. */
export function encodeAutosaveSettingsScopeGroupChats(value: AutosaveSettingsScopeGroupChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'autosaveSettingsScopeGroupChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** autosaveSettingsScopePrivateChats = AutosaveSettingsScope — Autosave settings applied to all private chats without chat-specific settings */
export class AutosaveSettingsScopePrivateChats {
  type: 'autosaveSettingsScopePrivateChats' = 'autosaveSettingsScopePrivateChats';
  extra?: string;
}

/** Decode a TDLib `autosaveSettingsScopePrivateChats` object; null input -> null. */
export function decodeAutosaveSettingsScopePrivateChats(json: TdJsonRaw): AutosaveSettingsScopePrivateChats | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AutosaveSettingsScopePrivateChats = new AutosaveSettingsScopePrivateChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `autosaveSettingsScopePrivateChats` into TDLib JSON wire shape. */
export function encodeAutosaveSettingsScopePrivateChats(value: AutosaveSettingsScopePrivateChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'autosaveSettingsScopePrivateChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** availableGift = AvailableGift — Describes a gift that is available for purchase */
export class AvailableGift {
  type: 'availableGift' = 'availableGift';
  extra?: string;
  /** gift: gift — The gift */
  gift: Gift | null = null;
  /** resale_count: int32 — Number of gifts that are available for resale */
  resale_count: number = 0;
  /** min_resale_star_count: int53 — The minimum price for the gifts available for resale in Telegram Star equivalent; 0 if there are no such gifts */
  min_resale_star_count: number = 0;
  /** title: string — The title of the upgraded gift; empty if the gift isn't available for resale */
  title: string = '';
}

/** Decode a TDLib `availableGift` object; null input -> null. */
export function decodeAvailableGift(json: TdJsonRaw): AvailableGift | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AvailableGift = new AvailableGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift = decodeGift(record['gift']);
  out.resale_count = asNumber(record['resale_count']);
  out.min_resale_star_count = asNumber(record['min_resale_star_count']);
  out.title = asString(record['title']);
  return out;
}

/** Encode a `availableGift` into TDLib JSON wire shape. */
export function encodeAvailableGift(value: AvailableGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'availableGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift'] = value.gift === null ? null : encodeGift(value.gift);
  out['resale_count'] = value.resale_count;
  out['min_resale_star_count'] = value.min_resale_star_count;
  out['title'] = value.title;
  return out;
}

/** availableGifts = AvailableGifts — Contains a list of gifts that can be sent to another user or channel chat */
export class AvailableGifts {
  type: 'availableGifts' = 'availableGifts';
  extra?: string;
  /** gifts: vector<availableGift> — The list of gifts */
  gifts: AvailableGift[] = [];
}

/** Decode a TDLib `availableGifts` object; null input -> null. */
export function decodeAvailableGifts(json: TdJsonRaw): AvailableGifts | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AvailableGifts = new AvailableGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gifts = decodeVector(record['gifts'], (item: TdJsonRaw): AvailableGift => decodeAvailableGift(item) as AvailableGift);
  return out;
}

/** Encode a `availableGifts` into TDLib JSON wire shape. */
export function encodeAvailableGifts(value: AvailableGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'availableGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gifts'] = encodeVector(value.gifts, (item: AvailableGift | null): TdJsonRaw => item === null ? null : encodeAvailableGift(item));
  return out;
}

/** availableReaction = AvailableReaction — Represents an available reaction */
export class AvailableReaction {
  type: 'availableReaction' = 'availableReaction';
  extra?: string;
  /** type: ReactionType — Type of the reaction */
  type_: TdReactionType | null = null;
  /** needs_premium: Bool — True, if Telegram Premium is needed to send the reaction */
  needs_premium: boolean = false;
}

/** Decode a TDLib `availableReaction` object; null input -> null. */
export function decodeAvailableReaction(json: TdJsonRaw): AvailableReaction | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AvailableReaction = new AvailableReaction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdReactionType(record['type']);
  out.needs_premium = asBool(record['needs_premium']);
  return out;
}

/** Encode a `availableReaction` into TDLib JSON wire shape. */
export function encodeAvailableReaction(value: AvailableReaction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'availableReaction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdReactionType(value.type_);
  out['needs_premium'] = value.needs_premium;
  return out;
}

/** availableReactions = AvailableReactions — Represents a list of reactions that can be added to a message */
export class AvailableReactions {
  type: 'availableReactions' = 'availableReactions';
  extra?: string;
  /** top_reactions: vector<availableReaction> — List of reactions to be shown at the top */
  top_reactions: AvailableReaction[] = [];
  /** recent_reactions: vector<availableReaction> — List of recently used reactions */
  recent_reactions: AvailableReaction[] = [];
  /** popular_reactions: vector<availableReaction> — List of popular reactions */
  popular_reactions: AvailableReaction[] = [];
  /** allow_custom_emoji: Bool — True, if any custom emoji reaction can be added by Telegram Premium subscribers */
  allow_custom_emoji: boolean = false;
  /** are_tags: Bool — True, if the reactions will be tags and the message can be found by them */
  are_tags: boolean = false;
  /** unavailability_reason: ReactionUnavailabilityReason — The reason why the current user can't add reactions to the message, despite some other users can; may be null if none */
  unavailability_reason: TdReactionUnavailabilityReason | null = null;
}

/** Decode a TDLib `availableReactions` object; null input -> null. */
export function decodeAvailableReactions(json: TdJsonRaw): AvailableReactions | null {
  if (json === null) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: AvailableReactions = new AvailableReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.top_reactions = decodeVector(record['top_reactions'], (item: TdJsonRaw): AvailableReaction => decodeAvailableReaction(item) as AvailableReaction);
  out.recent_reactions = decodeVector(record['recent_reactions'], (item: TdJsonRaw): AvailableReaction => decodeAvailableReaction(item) as AvailableReaction);
  out.popular_reactions = decodeVector(record['popular_reactions'], (item: TdJsonRaw): AvailableReaction => decodeAvailableReaction(item) as AvailableReaction);
  out.allow_custom_emoji = asBool(record['allow_custom_emoji']);
  out.are_tags = asBool(record['are_tags']);
  out.unavailability_reason = decodeTdReactionUnavailabilityReason(record['unavailability_reason']);
  return out;
}

/** Encode a `availableReactions` into TDLib JSON wire shape. */
export function encodeAvailableReactions(value: AvailableReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'availableReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['top_reactions'] = encodeVector(value.top_reactions, (item: AvailableReaction | null): TdJsonRaw => item === null ? null : encodeAvailableReaction(item));
  out['recent_reactions'] = encodeVector(value.recent_reactions, (item: AvailableReaction | null): TdJsonRaw => item === null ? null : encodeAvailableReaction(item));
  out['popular_reactions'] = encodeVector(value.popular_reactions, (item: AvailableReaction | null): TdJsonRaw => item === null ? null : encodeAvailableReaction(item));
  out['allow_custom_emoji'] = value.allow_custom_emoji;
  out['are_tags'] = value.are_tags;
  out['unavailability_reason'] = value.unavailability_reason === null ? null : encodeTdReactionUnavailabilityReason(value.unavailability_reason);
  return out;
}
