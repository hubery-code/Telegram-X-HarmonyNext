/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { TdSuggestedAction, decodeTdSuggestedAction, encodeTdSuggestedAction } from './TdUnions_S';

/** hashtags = Hashtags — Contains a list of hashtags */
export class Hashtags {
  type: 'hashtags' = 'hashtags';
  extra?: string;
  /** hashtags: vector<string> — A list of hashtags */
  hashtags: string[] = [];
}

/** Decode a TDLib `hashtags` object; null input -> null. */
export function decodeHashtags(json: TdJsonRaw): Hashtags | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Hashtags = new Hashtags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.hashtags = decodeVector(record['hashtags'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `hashtags` into TDLib JSON wire shape. */
export function encodeHashtags(value: Hashtags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'hashtags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['hashtags'] = encodeVector(value.hashtags, (item: string): TdJsonRaw => item);
  return out;
}

/** hideContactCloseBirthdays = Ok — Hides the list of contacts that have close birthdays for 24 hours */
export class HideContactCloseBirthdays {
  type: 'hideContactCloseBirthdays' = 'hideContactCloseBirthdays';
  extra?: string;
}

/** Decode a TDLib `hideContactCloseBirthdays` object; null input -> null. */
export function decodeHideContactCloseBirthdays(json: TdJsonRaw): HideContactCloseBirthdays | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: HideContactCloseBirthdays = new HideContactCloseBirthdays();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `hideContactCloseBirthdays` into TDLib JSON wire shape. */
export function encodeHideContactCloseBirthdays(value: HideContactCloseBirthdays): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'hideContactCloseBirthdays';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** hideSuggestedAction = Ok — Hides a suggested action */
export class HideSuggestedAction {
  type: 'hideSuggestedAction' = 'hideSuggestedAction';
  extra?: string;
  /** action: SuggestedAction — Suggested action to hide */
  action: TdSuggestedAction | null = null;
}

/** Decode a TDLib `hideSuggestedAction` object; null input -> null. */
export function decodeHideSuggestedAction(json: TdJsonRaw): HideSuggestedAction | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: HideSuggestedAction = new HideSuggestedAction();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.action = decodeTdSuggestedAction(record['action']);
  return out;
}

/** Encode a `hideSuggestedAction` into TDLib JSON wire shape. */
export function encodeHideSuggestedAction(value: HideSuggestedAction): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'hideSuggestedAction';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['action'] = value.action === null ? null : encodeTdSuggestedAction(value.action);
  return out;
}

/** httpUrl = HttpUrl — Contains an HTTP URL */
export class HttpUrl {
  type: 'httpUrl' = 'httpUrl';
  extra?: string;
  /** url: string — The URL */
  url: string = '';
}

/** Decode a TDLib `httpUrl` object; null input -> null. */
export function decodeHttpUrl(json: TdJsonRaw): HttpUrl | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: HttpUrl = new HttpUrl();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `httpUrl` into TDLib JSON wire shape. */
export function encodeHttpUrl(value: HttpUrl): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'httpUrl';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}
