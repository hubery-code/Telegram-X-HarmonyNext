/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, TdUnknownObject, asRecord, asString, encodeUnknownObject } from '../runtime/TdJson';
import { ValidatedOrderInfo, VectorPathCommandCubicBezierCurve, VectorPathCommandLine, Venue, VerificationStatus, Video, VideoChat, VideoMessageAdvertisement, VideoMessageAdvertisements, VideoNote, VideoStoryboard, VoiceNote, decodeValidatedOrderInfo, decodeVectorPathCommandCubicBezierCurve, decodeVectorPathCommandLine, decodeVenue, decodeVerificationStatus, decodeVideo, decodeVideoChat, decodeVideoMessageAdvertisement, decodeVideoMessageAdvertisements, decodeVideoNote, decodeVideoStoryboard, decodeVoiceNote, encodeValidatedOrderInfo, encodeVectorPathCommandCubicBezierCurve, encodeVectorPathCommandLine, encodeVenue, encodeVerificationStatus, encodeVideo, encodeVideoChat, encodeVideoMessageAdvertisement, encodeVideoMessageAdvertisements, encodeVideoNote, encodeVideoStoryboard, encodeVoiceNote } from './TdTypes_V';

/** TDLib type ValidatedOrderInfo */
export type TdValidatedOrderInfo =
  | ValidatedOrderInfo
  | TdUnknownObject
  ;

/** Decode any member of TDLib type ValidatedOrderInfo; unknown @type -> TdUnknownObject. */
export function decodeTdValidatedOrderInfo(json: TdJsonRaw): TdValidatedOrderInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'validatedOrderInfo':
      return decodeValidatedOrderInfo(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type ValidatedOrderInfo. */
export function encodeTdValidatedOrderInfo(value: TdValidatedOrderInfo): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'validatedOrderInfo':
      return encodeValidatedOrderInfo(value as ValidatedOrderInfo);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type VectorPathCommand — Represents a vector path command */
export type TdVectorPathCommand =
  | VectorPathCommandCubicBezierCurve
  | VectorPathCommandLine
  | TdUnknownObject
  ;

/** Decode any member of TDLib type VectorPathCommand; unknown @type -> TdUnknownObject. */
export function decodeTdVectorPathCommand(json: TdJsonRaw): TdVectorPathCommand | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'vectorPathCommandCubicBezierCurve':
      return decodeVectorPathCommandCubicBezierCurve(json);
    case 'vectorPathCommandLine':
      return decodeVectorPathCommandLine(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type VectorPathCommand. */
export function encodeTdVectorPathCommand(value: TdVectorPathCommand): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'vectorPathCommandCubicBezierCurve':
      return encodeVectorPathCommandCubicBezierCurve(value as VectorPathCommandCubicBezierCurve);
    case 'vectorPathCommandLine':
      return encodeVectorPathCommandLine(value as VectorPathCommandLine);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type Venue */
export type TdVenue =
  | Venue
  | TdUnknownObject
  ;

/** Decode any member of TDLib type Venue; unknown @type -> TdUnknownObject. */
export function decodeTdVenue(json: TdJsonRaw): TdVenue | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'venue':
      return decodeVenue(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type Venue. */
export function encodeTdVenue(value: TdVenue): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'venue':
      return encodeVenue(value as Venue);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type VerificationStatus */
export type TdVerificationStatus =
  | VerificationStatus
  | TdUnknownObject
  ;

/** Decode any member of TDLib type VerificationStatus; unknown @type -> TdUnknownObject. */
export function decodeTdVerificationStatus(json: TdJsonRaw): TdVerificationStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'verificationStatus':
      return decodeVerificationStatus(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type VerificationStatus. */
export function encodeTdVerificationStatus(value: TdVerificationStatus): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'verificationStatus':
      return encodeVerificationStatus(value as VerificationStatus);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type Video */
export type TdVideo =
  | Video
  | TdUnknownObject
  ;

/** Decode any member of TDLib type Video; unknown @type -> TdUnknownObject. */
export function decodeTdVideo(json: TdJsonRaw): TdVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'video':
      return decodeVideo(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type Video. */
export function encodeTdVideo(value: TdVideo): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'video':
      return encodeVideo(value as Video);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type VideoChat */
export type TdVideoChat =
  | VideoChat
  | TdUnknownObject
  ;

/** Decode any member of TDLib type VideoChat; unknown @type -> TdUnknownObject. */
export function decodeTdVideoChat(json: TdJsonRaw): TdVideoChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'videoChat':
      return decodeVideoChat(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type VideoChat. */
export function encodeTdVideoChat(value: TdVideoChat): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'videoChat':
      return encodeVideoChat(value as VideoChat);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type VideoMessageAdvertisement */
export type TdVideoMessageAdvertisement =
  | VideoMessageAdvertisement
  | TdUnknownObject
  ;

/** Decode any member of TDLib type VideoMessageAdvertisement; unknown @type -> TdUnknownObject. */
export function decodeTdVideoMessageAdvertisement(json: TdJsonRaw): TdVideoMessageAdvertisement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'videoMessageAdvertisement':
      return decodeVideoMessageAdvertisement(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type VideoMessageAdvertisement. */
export function encodeTdVideoMessageAdvertisement(value: TdVideoMessageAdvertisement): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'videoMessageAdvertisement':
      return encodeVideoMessageAdvertisement(value as VideoMessageAdvertisement);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type VideoMessageAdvertisements */
export type TdVideoMessageAdvertisements =
  | VideoMessageAdvertisements
  | TdUnknownObject
  ;

/** Decode any member of TDLib type VideoMessageAdvertisements; unknown @type -> TdUnknownObject. */
export function decodeTdVideoMessageAdvertisements(json: TdJsonRaw): TdVideoMessageAdvertisements | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'videoMessageAdvertisements':
      return decodeVideoMessageAdvertisements(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type VideoMessageAdvertisements. */
export function encodeTdVideoMessageAdvertisements(value: TdVideoMessageAdvertisements): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'videoMessageAdvertisements':
      return encodeVideoMessageAdvertisements(value as VideoMessageAdvertisements);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type VideoNote */
export type TdVideoNote =
  | VideoNote
  | TdUnknownObject
  ;

/** Decode any member of TDLib type VideoNote; unknown @type -> TdUnknownObject. */
export function decodeTdVideoNote(json: TdJsonRaw): TdVideoNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'videoNote':
      return decodeVideoNote(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type VideoNote. */
export function encodeTdVideoNote(value: TdVideoNote): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'videoNote':
      return encodeVideoNote(value as VideoNote);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type VideoStoryboard */
export type TdVideoStoryboard =
  | VideoStoryboard
  | TdUnknownObject
  ;

/** Decode any member of TDLib type VideoStoryboard; unknown @type -> TdUnknownObject. */
export function decodeTdVideoStoryboard(json: TdJsonRaw): TdVideoStoryboard | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'videoStoryboard':
      return decodeVideoStoryboard(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type VideoStoryboard. */
export function encodeTdVideoStoryboard(value: TdVideoStoryboard): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'videoStoryboard':
      return encodeVideoStoryboard(value as VideoStoryboard);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

/** TDLib type VoiceNote */
export type TdVoiceNote =
  | VoiceNote
  | TdUnknownObject
  ;

/** Decode any member of TDLib type VoiceNote; unknown @type -> TdUnknownObject. */
export function decodeTdVoiceNote(json: TdJsonRaw): TdVoiceNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const typeName: string = asString(record['@type'], '');
  switch (typeName) {
    case 'voiceNote':
      return decodeVoiceNote(json);
    default:
      return new TdUnknownObject(typeName, record);
  }
}

/** Encode any member of TDLib type VoiceNote. */
export function encodeTdVoiceNote(value: TdVoiceNote): Record<string, TdJsonRaw> {
  switch (value.type) {
    case 'voiceNote':
      return encodeVoiceNote(value as VoiceNote);
    default:
      return encodeUnknownObject(value as TdUnknownObject);
  }
}

