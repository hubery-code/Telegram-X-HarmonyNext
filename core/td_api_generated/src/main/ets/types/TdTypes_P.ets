/**
 * GENERATED FILE — DO NOT EDIT.
 * Generated by tools/td_api_codegen/td_api_arkts.py (GEN-002) from
 * core/td_api_generated/schema.ir.json (GEN-001).
 * Regenerate: python3 tools/td_api_codegen/td_api_arkts.py generate
 */

import { TdJsonRaw, asBool, asInt64, asNumber, asRecord, asString, decodeVector, encodeVector } from '../runtime/TdJson';
import { Address, AlternativeVideo, Animation, Audio, decodeAddress, decodeAlternativeVideo, decodeAnimation, decodeAudio, encodeAddress, encodeAlternativeVideo, encodeAnimation, encodeAudio } from './TdTypes_A';
import { BusinessFeaturePromotionAnimation, decodeBusinessFeaturePromotionAnimation, encodeBusinessFeaturePromotionAnimation } from './TdTypes_B';
import { ChatPhotoInfo, CountryInfo, decodeChatPhotoInfo, decodeCountryInfo, encodeChatPhotoInfo, encodeCountryInfo } from './TdTypes_C';
import { DateValue, DatedFile, Document, decodeDateValue, decodeDatedFile, decodeDocument, encodeDateValue, encodeDatedFile, encodeDocument } from './TdTypes_D';
import { EmailAddressAuthenticationCodeInfo, decodeEmailAddressAuthenticationCodeInfo, encodeEmailAddressAuthenticationCodeInfo } from './TdTypes_E';
import { File, FormattedText, decodeFile, decodeFormattedText, encodeFile, encodeFormattedText } from './TdTypes_F';
import { IdentityDocument, InlineButton, InputStoryAreas, Invoice, decodeIdentityDocument, decodeInlineButton, decodeInputStoryAreas, decodeInvoice, encodeIdentityDocument, encodeInlineButton, encodeInputStoryAreas, encodeInvoice } from './TdTypes_I';
import { LinkPreview, Location, decodeLinkPreview, decodeLocation, encodeLinkPreview, encodeLocation } from './TdTypes_L';
import { Message, Minithumbnail, decodeMessage, decodeMinithumbnail, encodeMessage, encodeMinithumbnail } from './TdTypes_M';
import { OrderInfo, decodeOrderInfo, encodeOrderInfo } from './TdTypes_O';
import { SavedCredentials, ShippingOption, StarSubscriptionPricing, Sticker, Story, StoryFullId, decodeSavedCredentials, decodeShippingOption, decodeStarSubscriptionPricing, decodeSticker, decodeStory, decodeStoryFullId, encodeSavedCredentials, encodeShippingOption, encodeStarSubscriptionPricing, encodeSticker, encodeStory, encodeStoryFullId } from './TdTypes_S';
import { TargetChatTypes, decodeTargetChatTypes, encodeTargetChatTypes } from './TdTypes_T';
import { Venue, Video, VideoNote, VideoStoryboard, VoiceNote, decodeVenue, decodeVideo, decodeVideoNote, decodeVideoStoryboard, decodeVoiceNote, encodeVenue, encodeVideo, encodeVideoNote, encodeVideoStoryboard, encodeVoiceNote } from './TdTypes_V';
import { TdBusinessFeature, decodeTdBusinessFeature, encodeTdBusinessFeature } from './TdUnions_B';
import { TdFileType, TdFirebaseAuthenticationSettings, decodeTdFileType, decodeTdFirebaseAuthenticationSettings, encodeTdFileType, encodeTdFirebaseAuthenticationSettings } from './TdUnions_F';
import { TdGiveawayPrize, decodeTdGiveawayPrize, encodeTdGiveawayPrize } from './TdUnions_G';
import { TdInlineQueryResult, TdInputFile, TdInputStoryContent, TdInternalLinkType, decodeTdInlineQueryResult, decodeTdInputFile, decodeTdInputStoryContent, decodeTdInternalLinkType, encodeTdInlineQueryResult, encodeTdInputFile, encodeTdInputStoryContent, encodeTdInternalLinkType } from './TdUnions_I';
import { TdMessageSender, decodeTdMessageSender, encodeTdMessageSender } from './TdUnions_M';
import { TdPageBlock, TdPageBlockHorizontalAlignment, TdPageBlockVerticalAlignment, TdPassportElement, TdPassportElementErrorSource, TdPassportElementType, TdPaymentFormType, TdPaymentProvider, TdPaymentReceiptType, TdPollMedia, TdPollType, TdPollVoteRestrictionReason, TdPremiumFeature, TdPremiumLimitType, TdPremiumStoryFeature, TdProxyType, TdPublicForward, decodeTdPageBlock, decodeTdPageBlockHorizontalAlignment, decodeTdPageBlockVerticalAlignment, decodeTdPassportElement, decodeTdPassportElementErrorSource, decodeTdPassportElementType, decodeTdPaymentFormType, decodeTdPaymentProvider, decodeTdPaymentReceiptType, decodeTdPollMedia, decodeTdPollType, decodeTdPollVoteRestrictionReason, decodeTdPremiumFeature, decodeTdPremiumLimitType, decodeTdPremiumStoryFeature, decodeTdProxyType, decodeTdPublicForward, encodeTdPageBlock, encodeTdPageBlockHorizontalAlignment, encodeTdPageBlockVerticalAlignment, encodeTdPassportElement, encodeTdPassportElementErrorSource, encodeTdPassportElementType, encodeTdPaymentFormType, encodeTdPaymentProvider, encodeTdPaymentReceiptType, encodeTdPollMedia, encodeTdPollType, encodeTdPollVoteRestrictionReason, encodeTdPremiumFeature, encodeTdPremiumLimitType, encodeTdPremiumStoryFeature, encodeTdProxyType, encodeTdPublicForward } from './TdUnions_P';
import { TdRichText, decodeTdRichText, encodeTdRichText } from './TdUnions_R';
import { TdStatisticalGraph, TdStoryPrivacySettings, decodeTdStatisticalGraph, decodeTdStoryPrivacySettings, encodeTdStatisticalGraph, encodeTdStoryPrivacySettings } from './TdUnions_S';
import { TdTextParseMode, decodeTdTextParseMode, encodeTdTextParseMode } from './TdUnions_T';

/** pageBlockAnchor = PageBlock — An invisible anchor on a page, which can be used in a URL to open the page from the specified anchor */
export class PageBlockAnchor {
  type: 'pageBlockAnchor' = 'pageBlockAnchor';
  extra?: string;
  /** name: string — Name of the anchor */
  name: string = '';
}

/** Decode a TDLib `pageBlockAnchor` object; null input -> null. */
export function decodePageBlockAnchor(json: TdJsonRaw): PageBlockAnchor | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockAnchor = new PageBlockAnchor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `pageBlockAnchor` into TDLib JSON wire shape. */
export function encodePageBlockAnchor(value: PageBlockAnchor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockAnchor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** pageBlockAnimation = PageBlock — An animation */
export class PageBlockAnimation {
  type: 'pageBlockAnimation' = 'pageBlockAnimation';
  extra?: string;
  /** animation: animation — Animation file; may be null */
  animation: Animation | null = null;
  /** caption: pageBlockCaption — Animation caption; may be null if none */
  caption: PageBlockCaption | null = null;
  /** need_autoplay: Bool — True, if the animation must be played automatically */
  need_autoplay: boolean = false;
  /** has_spoiler: Bool — True, if the animation preview must be covered by a spoiler animation */
  has_spoiler: boolean = false;
}

/** Decode a TDLib `pageBlockAnimation` object; null input -> null. */
export function decodePageBlockAnimation(json: TdJsonRaw): PageBlockAnimation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockAnimation = new PageBlockAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeAnimation(record['animation']);
  out.caption = decodePageBlockCaption(record['caption']);
  out.need_autoplay = asBool(record['need_autoplay']);
  out.has_spoiler = asBool(record['has_spoiler']);
  return out;
}

/** Encode a `pageBlockAnimation` into TDLib JSON wire shape. */
export function encodePageBlockAnimation(value: PageBlockAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  out['need_autoplay'] = value.need_autoplay;
  out['has_spoiler'] = value.has_spoiler;
  return out;
}

/** pageBlockAudio = PageBlock — An audio file */
export class PageBlockAudio {
  type: 'pageBlockAudio' = 'pageBlockAudio';
  extra?: string;
  /** audio: audio — Audio file */
  audio: Audio | null = null;
  /** caption: pageBlockCaption — Audio file caption; may be null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `pageBlockAudio` object; null input -> null. */
export function decodePageBlockAudio(json: TdJsonRaw): PageBlockAudio | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockAudio = new PageBlockAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeAudio(record['audio']);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `pageBlockAudio` into TDLib JSON wire shape. */
export function encodePageBlockAudio(value: PageBlockAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeAudio(value.audio);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** pageBlockAuthorDate = PageBlock — The author and publishing date of a page; instant view only */
export class PageBlockAuthorDate {
  type: 'pageBlockAuthorDate' = 'pageBlockAuthorDate';
  extra?: string;
  /** author: RichText — Author */
  author: TdRichText | null = null;
  /** publish_date: int32 — Point in time (Unix timestamp) when the article was published; 0 if unknown */
  publish_date: number = 0;
}

/** Decode a TDLib `pageBlockAuthorDate` object; null input -> null. */
export function decodePageBlockAuthorDate(json: TdJsonRaw): PageBlockAuthorDate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockAuthorDate = new PageBlockAuthorDate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.author = decodeTdRichText(record['author']);
  out.publish_date = asNumber(record['publish_date']);
  return out;
}

/** Encode a `pageBlockAuthorDate` into TDLib JSON wire shape. */
export function encodePageBlockAuthorDate(value: PageBlockAuthorDate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockAuthorDate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['author'] = value.author === null ? null : encodeTdRichText(value.author);
  out['publish_date'] = value.publish_date;
  return out;
}

/** pageBlockBlockQuote = PageBlock — A block quote */
export class PageBlockBlockQuote {
  type: 'pageBlockBlockQuote' = 'pageBlockBlockQuote';
  extra?: string;
  /** blocks: vector<PageBlock> — Quote blocks */
  blocks: TdPageBlock[] = [];
  /** credit: RichText — Quote credit; may be null if none */
  credit: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockBlockQuote` object; null input -> null. */
export function decodePageBlockBlockQuote(json: TdJsonRaw): PageBlockBlockQuote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockBlockQuote = new PageBlockBlockQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdPageBlock => decodeTdPageBlock(item) as TdPageBlock);
  out.credit = decodeTdRichText(record['credit']);
  return out;
}

/** Encode a `pageBlockBlockQuote` into TDLib JSON wire shape. */
export function encodePageBlockBlockQuote(value: PageBlockBlockQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockBlockQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdPageBlock | null): TdJsonRaw => item === null ? null : encodeTdPageBlock(item));
  out['credit'] = value.credit === null ? null : encodeTdRichText(value.credit);
  return out;
}

/** pageBlockButtonRow = PageBlock — A list of buttons shown in a row */
export class PageBlockButtonRow {
  type: 'pageBlockButtonRow' = 'pageBlockButtonRow';
  extra?: string;
  /** buttons: vector<inlineButton> — The buttons */
  buttons: InlineButton[] = [];
  /** align: PageBlockHorizontalAlignment — Horizontal alignment of the buttons; may be null if the buttons must be shown full-width */
  align: TdPageBlockHorizontalAlignment | null = null;
}

/** Decode a TDLib `pageBlockButtonRow` object; null input -> null. */
export function decodePageBlockButtonRow(json: TdJsonRaw): PageBlockButtonRow | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockButtonRow = new PageBlockButtonRow();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.buttons = decodeVector(record['buttons'], (item: TdJsonRaw): InlineButton => decodeInlineButton(item) as InlineButton);
  out.align = decodeTdPageBlockHorizontalAlignment(record['align']);
  return out;
}

/** Encode a `pageBlockButtonRow` into TDLib JSON wire shape. */
export function encodePageBlockButtonRow(value: PageBlockButtonRow): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockButtonRow';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['buttons'] = encodeVector(value.buttons, (item: InlineButton | null): TdJsonRaw => item === null ? null : encodeInlineButton(item));
  out['align'] = value.align === null ? null : encodeTdPageBlockHorizontalAlignment(value.align);
  return out;
}

/** pageBlockCaption = PageBlockCaption — Contains a caption of another block */
export class PageBlockCaption {
  type: 'pageBlockCaption' = 'pageBlockCaption';
  extra?: string;
  /** text: RichText — Content of the caption */
  text: TdRichText | null = null;
  /** credit: RichText — Block credit (like HTML tag <cite>); may be null if none */
  credit: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockCaption` object; null input -> null. */
export function decodePageBlockCaption(json: TdJsonRaw): PageBlockCaption | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockCaption = new PageBlockCaption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.credit = decodeTdRichText(record['credit']);
  return out;
}

/** Encode a `pageBlockCaption` into TDLib JSON wire shape. */
export function encodePageBlockCaption(value: PageBlockCaption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockCaption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['credit'] = value.credit === null ? null : encodeTdRichText(value.credit);
  return out;
}

/** pageBlockChatLink = PageBlock — A link to a chat; instant view only */
export class PageBlockChatLink {
  type: 'pageBlockChatLink' = 'pageBlockChatLink';
  extra?: string;
  /** title: string — Chat title */
  title: string = '';
  /** photo: chatPhotoInfo — Chat photo; may be null */
  photo: ChatPhotoInfo | null = null;
  /** accent_color_id: int32 — Identifier of the accent color for chat title and background of chat photo */
  accent_color_id: number = 0;
  /** username: string — Chat username by which all other information about the chat can be resolved */
  username: string = '';
}

/** Decode a TDLib `pageBlockChatLink` object; null input -> null. */
export function decodePageBlockChatLink(json: TdJsonRaw): PageBlockChatLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockChatLink = new PageBlockChatLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.photo = decodeChatPhotoInfo(record['photo']);
  out.accent_color_id = asNumber(record['accent_color_id']);
  out.username = asString(record['username']);
  return out;
}

/** Encode a `pageBlockChatLink` into TDLib JSON wire shape. */
export function encodePageBlockChatLink(value: PageBlockChatLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockChatLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['photo'] = value.photo === null ? null : encodeChatPhotoInfo(value.photo);
  out['accent_color_id'] = value.accent_color_id;
  out['username'] = value.username;
  return out;
}

/** pageBlockCollage = PageBlock — A collage */
export class PageBlockCollage {
  type: 'pageBlockCollage' = 'pageBlockCollage';
  extra?: string;
  /** blocks: vector<PageBlock> — Collage item contents */
  blocks: TdPageBlock[] = [];
  /** caption: pageBlockCaption — Block caption; may be null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `pageBlockCollage` object; null input -> null. */
export function decodePageBlockCollage(json: TdJsonRaw): PageBlockCollage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockCollage = new PageBlockCollage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdPageBlock => decodeTdPageBlock(item) as TdPageBlock);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `pageBlockCollage` into TDLib JSON wire shape. */
export function encodePageBlockCollage(value: PageBlockCollage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockCollage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdPageBlock | null): TdJsonRaw => item === null ? null : encodeTdPageBlock(item));
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** pageBlockCover = PageBlock — A page cover; instant view only */
export class PageBlockCover {
  type: 'pageBlockCover' = 'pageBlockCover';
  extra?: string;
  /** cover: PageBlock — Cover */
  cover: TdPageBlock | null = null;
}

/** Decode a TDLib `pageBlockCover` object; null input -> null. */
export function decodePageBlockCover(json: TdJsonRaw): PageBlockCover | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockCover = new PageBlockCover();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.cover = decodeTdPageBlock(record['cover']);
  return out;
}

/** Encode a `pageBlockCover` into TDLib JSON wire shape. */
export function encodePageBlockCover(value: PageBlockCover): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockCover';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['cover'] = value.cover === null ? null : encodeTdPageBlock(value.cover);
  return out;
}

/** pageBlockDetails = PageBlock — A collapsible block */
export class PageBlockDetails {
  type: 'pageBlockDetails' = 'pageBlockDetails';
  extra?: string;
  /** header: RichText — Always visible heading for the block */
  header: TdRichText | null = null;
  /** blocks: vector<PageBlock> — Block contents */
  blocks: TdPageBlock[] = [];
  /** is_open: Bool — True, if the block is open by default */
  is_open: boolean = false;
}

/** Decode a TDLib `pageBlockDetails` object; null input -> null. */
export function decodePageBlockDetails(json: TdJsonRaw): PageBlockDetails | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockDetails = new PageBlockDetails();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.header = decodeTdRichText(record['header']);
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdPageBlock => decodeTdPageBlock(item) as TdPageBlock);
  out.is_open = asBool(record['is_open']);
  return out;
}

/** Encode a `pageBlockDetails` into TDLib JSON wire shape. */
export function encodePageBlockDetails(value: PageBlockDetails): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockDetails';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['header'] = value.header === null ? null : encodeTdRichText(value.header);
  out['blocks'] = encodeVector(value.blocks, (item: TdPageBlock | null): TdJsonRaw => item === null ? null : encodeTdPageBlock(item));
  out['is_open'] = value.is_open;
  return out;
}

/** pageBlockDivider = PageBlock — An empty block separating a page */
export class PageBlockDivider {
  type: 'pageBlockDivider' = 'pageBlockDivider';
  extra?: string;
}

/** Decode a TDLib `pageBlockDivider` object; null input -> null. */
export function decodePageBlockDivider(json: TdJsonRaw): PageBlockDivider | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockDivider = new PageBlockDivider();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pageBlockDivider` into TDLib JSON wire shape. */
export function encodePageBlockDivider(value: PageBlockDivider): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockDivider';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pageBlockDocument = PageBlock — A general file */
export class PageBlockDocument {
  type: 'pageBlockDocument' = 'pageBlockDocument';
  extra?: string;
  /** document: document — The file */
  document: Document | null = null;
  /** caption: pageBlockCaption — File caption; may be null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `pageBlockDocument` object; null input -> null. */
export function decodePageBlockDocument(json: TdJsonRaw): PageBlockDocument | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockDocument = new PageBlockDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeDocument(record['document']);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `pageBlockDocument` into TDLib JSON wire shape. */
export function encodePageBlockDocument(value: PageBlockDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeDocument(value.document);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** pageBlockEmbedded = PageBlock — An embedded web page; instant view only */
export class PageBlockEmbedded {
  type: 'pageBlockEmbedded' = 'pageBlockEmbedded';
  extra?: string;
  /** url: string — URL of the embedded page, if available */
  url: string = '';
  /** html: string — HTML-markup of the embedded page */
  html: string = '';
  /** poster_photo: photo — Poster photo, if available; may be null */
  poster_photo: Photo | null = null;
  /** width: int32 — Block width; 0 if unknown */
  width: number = 0;
  /** height: int32 — Block height; 0 if unknown */
  height: number = 0;
  /** caption: pageBlockCaption — Block caption; may be null if none */
  caption: PageBlockCaption | null = null;
  /** is_full_width: Bool — True, if the block must be full width */
  is_full_width: boolean = false;
  /** allow_scrolling: Bool — True, if scrolling needs to be allowed */
  allow_scrolling: boolean = false;
}

/** Decode a TDLib `pageBlockEmbedded` object; null input -> null. */
export function decodePageBlockEmbedded(json: TdJsonRaw): PageBlockEmbedded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockEmbedded = new PageBlockEmbedded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.html = asString(record['html']);
  out.poster_photo = decodePhoto(record['poster_photo']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.caption = decodePageBlockCaption(record['caption']);
  out.is_full_width = asBool(record['is_full_width']);
  out.allow_scrolling = asBool(record['allow_scrolling']);
  return out;
}

/** Encode a `pageBlockEmbedded` into TDLib JSON wire shape. */
export function encodePageBlockEmbedded(value: PageBlockEmbedded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockEmbedded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['html'] = value.html;
  out['poster_photo'] = value.poster_photo === null ? null : encodePhoto(value.poster_photo);
  out['width'] = value.width;
  out['height'] = value.height;
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  out['is_full_width'] = value.is_full_width;
  out['allow_scrolling'] = value.allow_scrolling;
  return out;
}

/** pageBlockEmbeddedPost = PageBlock — An embedded post; instant view only */
export class PageBlockEmbeddedPost {
  type: 'pageBlockEmbeddedPost' = 'pageBlockEmbeddedPost';
  extra?: string;
  /** url: string — URL of the embedded post */
  url: string = '';
  /** author: string — Post author */
  author: string = '';
  /** author_photo: photo — Post author photo; may be null */
  author_photo: Photo | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the post was created; 0 if unknown */
  date: number = 0;
  /** blocks: vector<PageBlock> — Post content */
  blocks: TdPageBlock[] = [];
  /** caption: pageBlockCaption — Post caption; may be null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `pageBlockEmbeddedPost` object; null input -> null. */
export function decodePageBlockEmbeddedPost(json: TdJsonRaw): PageBlockEmbeddedPost | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockEmbeddedPost = new PageBlockEmbeddedPost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.author = asString(record['author']);
  out.author_photo = decodePhoto(record['author_photo']);
  out.date = asNumber(record['date']);
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdPageBlock => decodeTdPageBlock(item) as TdPageBlock);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `pageBlockEmbeddedPost` into TDLib JSON wire shape. */
export function encodePageBlockEmbeddedPost(value: PageBlockEmbeddedPost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockEmbeddedPost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['author'] = value.author;
  out['author_photo'] = value.author_photo === null ? null : encodePhoto(value.author_photo);
  out['date'] = value.date;
  out['blocks'] = encodeVector(value.blocks, (item: TdPageBlock | null): TdJsonRaw => item === null ? null : encodeTdPageBlock(item));
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** pageBlockExpandableBlockQuote = PageBlock — An expandable block quote */
export class PageBlockExpandableBlockQuote {
  type: 'pageBlockExpandableBlockQuote' = 'pageBlockExpandableBlockQuote';
  extra?: string;
  /** text: RichText — Text of the quote */
  text: TdRichText | null = null;
  /** credit: RichText — Quote credit; may be null if none */
  credit: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockExpandableBlockQuote` object; null input -> null. */
export function decodePageBlockExpandableBlockQuote(json: TdJsonRaw): PageBlockExpandableBlockQuote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockExpandableBlockQuote = new PageBlockExpandableBlockQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.credit = decodeTdRichText(record['credit']);
  return out;
}

/** Encode a `pageBlockExpandableBlockQuote` into TDLib JSON wire shape. */
export function encodePageBlockExpandableBlockQuote(value: PageBlockExpandableBlockQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockExpandableBlockQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['credit'] = value.credit === null ? null : encodeTdRichText(value.credit);
  return out;
}

/** pageBlockFooter = PageBlock — The footer of a page */
export class PageBlockFooter {
  type: 'pageBlockFooter' = 'pageBlockFooter';
  extra?: string;
  /** footer: RichText — Footer */
  footer: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockFooter` object; null input -> null. */
export function decodePageBlockFooter(json: TdJsonRaw): PageBlockFooter | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockFooter = new PageBlockFooter();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.footer = decodeTdRichText(record['footer']);
  return out;
}

/** Encode a `pageBlockFooter` into TDLib JSON wire shape. */
export function encodePageBlockFooter(value: PageBlockFooter): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockFooter';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['footer'] = value.footer === null ? null : encodeTdRichText(value.footer);
  return out;
}

/** pageBlockHeader = PageBlock — A header; instant view only */
export class PageBlockHeader {
  type: 'pageBlockHeader' = 'pageBlockHeader';
  extra?: string;
  /** header: RichText — Header */
  header: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockHeader` object; null input -> null. */
export function decodePageBlockHeader(json: TdJsonRaw): PageBlockHeader | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockHeader = new PageBlockHeader();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.header = decodeTdRichText(record['header']);
  return out;
}

/** Encode a `pageBlockHeader` into TDLib JSON wire shape. */
export function encodePageBlockHeader(value: PageBlockHeader): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockHeader';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['header'] = value.header === null ? null : encodeTdRichText(value.header);
  return out;
}

/** pageBlockHorizontalAlignmentCenter = PageBlockHorizontalAlignment — The content must be center-aligned */
export class PageBlockHorizontalAlignmentCenter {
  type: 'pageBlockHorizontalAlignmentCenter' = 'pageBlockHorizontalAlignmentCenter';
  extra?: string;
}

/** Decode a TDLib `pageBlockHorizontalAlignmentCenter` object; null input -> null. */
export function decodePageBlockHorizontalAlignmentCenter(json: TdJsonRaw): PageBlockHorizontalAlignmentCenter | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockHorizontalAlignmentCenter = new PageBlockHorizontalAlignmentCenter();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pageBlockHorizontalAlignmentCenter` into TDLib JSON wire shape. */
export function encodePageBlockHorizontalAlignmentCenter(value: PageBlockHorizontalAlignmentCenter): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockHorizontalAlignmentCenter';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pageBlockHorizontalAlignmentLeft = PageBlockHorizontalAlignment — The content must be left-aligned */
export class PageBlockHorizontalAlignmentLeft {
  type: 'pageBlockHorizontalAlignmentLeft' = 'pageBlockHorizontalAlignmentLeft';
  extra?: string;
}

/** Decode a TDLib `pageBlockHorizontalAlignmentLeft` object; null input -> null. */
export function decodePageBlockHorizontalAlignmentLeft(json: TdJsonRaw): PageBlockHorizontalAlignmentLeft | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockHorizontalAlignmentLeft = new PageBlockHorizontalAlignmentLeft();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pageBlockHorizontalAlignmentLeft` into TDLib JSON wire shape. */
export function encodePageBlockHorizontalAlignmentLeft(value: PageBlockHorizontalAlignmentLeft): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockHorizontalAlignmentLeft';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pageBlockHorizontalAlignmentRight = PageBlockHorizontalAlignment — The content must be right-aligned */
export class PageBlockHorizontalAlignmentRight {
  type: 'pageBlockHorizontalAlignmentRight' = 'pageBlockHorizontalAlignmentRight';
  extra?: string;
}

/** Decode a TDLib `pageBlockHorizontalAlignmentRight` object; null input -> null. */
export function decodePageBlockHorizontalAlignmentRight(json: TdJsonRaw): PageBlockHorizontalAlignmentRight | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockHorizontalAlignmentRight = new PageBlockHorizontalAlignmentRight();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pageBlockHorizontalAlignmentRight` into TDLib JSON wire shape. */
export function encodePageBlockHorizontalAlignmentRight(value: PageBlockHorizontalAlignmentRight): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockHorizontalAlignmentRight';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pageBlockKicker = PageBlock — A kicker; instant view only */
export class PageBlockKicker {
  type: 'pageBlockKicker' = 'pageBlockKicker';
  extra?: string;
  /** kicker: RichText — Kicker */
  kicker: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockKicker` object; null input -> null. */
export function decodePageBlockKicker(json: TdJsonRaw): PageBlockKicker | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockKicker = new PageBlockKicker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.kicker = decodeTdRichText(record['kicker']);
  return out;
}

/** Encode a `pageBlockKicker` into TDLib JSON wire shape. */
export function encodePageBlockKicker(value: PageBlockKicker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockKicker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['kicker'] = value.kicker === null ? null : encodeTdRichText(value.kicker);
  return out;
}

/** pageBlockList = PageBlock — A list of data blocks */
export class PageBlockList {
  type: 'pageBlockList' = 'pageBlockList';
  extra?: string;
  /** items: vector<pageBlockListItem> — The items of the list */
  items: PageBlockListItem[] = [];
}

/** Decode a TDLib `pageBlockList` object; null input -> null. */
export function decodePageBlockList(json: TdJsonRaw): PageBlockList | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockList = new PageBlockList();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.items = decodeVector(record['items'], (item: TdJsonRaw): PageBlockListItem => decodePageBlockListItem(item) as PageBlockListItem);
  return out;
}

/** Encode a `pageBlockList` into TDLib JSON wire shape. */
export function encodePageBlockList(value: PageBlockList): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockList';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['items'] = encodeVector(value.items, (item: PageBlockListItem | null): TdJsonRaw => item === null ? null : encodePageBlockListItem(item));
  return out;
}

/** pageBlockListItem = PageBlockListItem — Describes an item of a list page block "1" for decimal numbers, or empty for unordered lists */
export class PageBlockListItem {
  type: 'pageBlockListItem' = 'pageBlockListItem';
  extra?: string;
  /** label: string — Item label */
  label: string = '';
  /** blocks: vector<PageBlock> — Item blocks */
  blocks: TdPageBlock[] = [];
  /** has_checkbox: Bool — True, if the item has a checkbox */
  has_checkbox: boolean = false;
  /** is_checked: Bool — True, if the item is checked */
  is_checked: boolean = false;
  /** value: int32 — Value of the item; 0 for unordered lists */
  value: number = 0;
  /** type: string — Type of the item numbering type; must be one of "a" for lowercase letters, "A" for uppercase letters, "i" for lowercase Roman numerals, "I" for uppercase Roman numerals, */
  type_: string = '';
}

/** Decode a TDLib `pageBlockListItem` object; null input -> null. */
export function decodePageBlockListItem(json: TdJsonRaw): PageBlockListItem | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockListItem = new PageBlockListItem();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.label = asString(record['label']);
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdPageBlock => decodeTdPageBlock(item) as TdPageBlock);
  out.has_checkbox = asBool(record['has_checkbox']);
  out.is_checked = asBool(record['is_checked']);
  out.value = asNumber(record['value']);
  out.type_ = asString(record['type']);
  return out;
}

/** Encode a `pageBlockListItem` into TDLib JSON wire shape. */
export function encodePageBlockListItem(value: PageBlockListItem): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockListItem';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['label'] = value.label;
  out['blocks'] = encodeVector(value.blocks, (item: TdPageBlock | null): TdJsonRaw => item === null ? null : encodeTdPageBlock(item));
  out['has_checkbox'] = value.has_checkbox;
  out['is_checked'] = value.is_checked;
  out['value'] = value.value;
  out['type'] = value.type_;
  return out;
}

/** pageBlockMap = PageBlock — A map */
export class PageBlockMap {
  type: 'pageBlockMap' = 'pageBlockMap';
  extra?: string;
  /** location: location — Location of the map center */
  location: Location | null = null;
  /** zoom: int32 — Map zoom level */
  zoom: number = 0;
  /** width: int32 — Map width */
  width: number = 0;
  /** height: int32 — Map height */
  height: number = 0;
  /** caption: pageBlockCaption — Block caption; may be null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `pageBlockMap` object; null input -> null. */
export function decodePageBlockMap(json: TdJsonRaw): PageBlockMap | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockMap = new PageBlockMap();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  out.zoom = asNumber(record['zoom']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `pageBlockMap` into TDLib JSON wire shape. */
export function encodePageBlockMap(value: PageBlockMap): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockMap';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  out['zoom'] = value.zoom;
  out['width'] = value.width;
  out['height'] = value.height;
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** pageBlockMathematicalExpression = PageBlock — A mathematical expression */
export class PageBlockMathematicalExpression {
  type: 'pageBlockMathematicalExpression' = 'pageBlockMathematicalExpression';
  extra?: string;
  /** expression: string — The expression in LaTeX format */
  expression: string = '';
}

/** Decode a TDLib `pageBlockMathematicalExpression` object; null input -> null. */
export function decodePageBlockMathematicalExpression(json: TdJsonRaw): PageBlockMathematicalExpression | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockMathematicalExpression = new PageBlockMathematicalExpression();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.expression = asString(record['expression']);
  return out;
}

/** Encode a `pageBlockMathematicalExpression` into TDLib JSON wire shape. */
export function encodePageBlockMathematicalExpression(value: PageBlockMathematicalExpression): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockMathematicalExpression';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['expression'] = value.expression;
  return out;
}

/** pageBlockParagraph = PageBlock — A text paragraph */
export class PageBlockParagraph {
  type: 'pageBlockParagraph' = 'pageBlockParagraph';
  extra?: string;
  /** text: RichText — Paragraph text */
  text: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockParagraph` object; null input -> null. */
export function decodePageBlockParagraph(json: TdJsonRaw): PageBlockParagraph | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockParagraph = new PageBlockParagraph();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `pageBlockParagraph` into TDLib JSON wire shape. */
export function encodePageBlockParagraph(value: PageBlockParagraph): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockParagraph';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** pageBlockPhoto = PageBlock — A photo */
export class PageBlockPhoto {
  type: 'pageBlockPhoto' = 'pageBlockPhoto';
  extra?: string;
  /** photo: photo — Photo file; may be null */
  photo: Photo | null = null;
  /** caption: pageBlockCaption — Photo caption; may be null if none */
  caption: PageBlockCaption | null = null;
  /** url: string — URL that needs to be opened when the photo is clicked; instant view only */
  url: string = '';
  /** has_spoiler: Bool — True, if the photo preview must be covered by a spoiler animation */
  has_spoiler: boolean = false;
}

/** Decode a TDLib `pageBlockPhoto` object; null input -> null. */
export function decodePageBlockPhoto(json: TdJsonRaw): PageBlockPhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockPhoto = new PageBlockPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  out.caption = decodePageBlockCaption(record['caption']);
  out.url = asString(record['url']);
  out.has_spoiler = asBool(record['has_spoiler']);
  return out;
}

/** Encode a `pageBlockPhoto` into TDLib JSON wire shape. */
export function encodePageBlockPhoto(value: PageBlockPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  out['url'] = value.url;
  out['has_spoiler'] = value.has_spoiler;
  return out;
}

/** pageBlockPreformatted = PageBlock — A preformatted text paragraph */
export class PageBlockPreformatted {
  type: 'pageBlockPreformatted' = 'pageBlockPreformatted';
  extra?: string;
  /** text: RichText — Paragraph text */
  text: TdRichText | null = null;
  /** language: string — Programming language for which the text needs to be formatted */
  language: string = '';
}

/** Decode a TDLib `pageBlockPreformatted` object; null input -> null. */
export function decodePageBlockPreformatted(json: TdJsonRaw): PageBlockPreformatted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockPreformatted = new PageBlockPreformatted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.language = asString(record['language']);
  return out;
}

/** Encode a `pageBlockPreformatted` into TDLib JSON wire shape. */
export function encodePageBlockPreformatted(value: PageBlockPreformatted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockPreformatted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['language'] = value.language;
  return out;
}

/** pageBlockPullQuote = PageBlock — A pull quote */
export class PageBlockPullQuote {
  type: 'pageBlockPullQuote' = 'pageBlockPullQuote';
  extra?: string;
  /** text: RichText — Quote text */
  text: TdRichText | null = null;
  /** credit: RichText — Quote credit; may be null if none */
  credit: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockPullQuote` object; null input -> null. */
export function decodePageBlockPullQuote(json: TdJsonRaw): PageBlockPullQuote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockPullQuote = new PageBlockPullQuote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.credit = decodeTdRichText(record['credit']);
  return out;
}

/** Encode a `pageBlockPullQuote` into TDLib JSON wire shape. */
export function encodePageBlockPullQuote(value: PageBlockPullQuote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockPullQuote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['credit'] = value.credit === null ? null : encodeTdRichText(value.credit);
  return out;
}

/** pageBlockRelatedArticle = PageBlockRelatedArticle — Contains information about a related article */
export class PageBlockRelatedArticle {
  type: 'pageBlockRelatedArticle' = 'pageBlockRelatedArticle';
  extra?: string;
  /** url: string — Related article URL */
  url: string = '';
  /** title: string — Article title; may be empty */
  title: string = '';
  /** description: string */
  description: string = '';
  /** photo: photo — Article photo; may be null */
  photo: Photo | null = null;
  /** author: string — Article author; may be empty */
  author: string = '';
  /** publish_date: int32 — Point in time (Unix timestamp) when the article was published; 0 if unknown */
  publish_date: number = 0;
}

/** Decode a TDLib `pageBlockRelatedArticle` object; null input -> null. */
export function decodePageBlockRelatedArticle(json: TdJsonRaw): PageBlockRelatedArticle | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockRelatedArticle = new PageBlockRelatedArticle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.title = asString(record['title']);
  out.description = asString(record['description']);
  out.photo = decodePhoto(record['photo']);
  out.author = asString(record['author']);
  out.publish_date = asNumber(record['publish_date']);
  return out;
}

/** Encode a `pageBlockRelatedArticle` into TDLib JSON wire shape. */
export function encodePageBlockRelatedArticle(value: PageBlockRelatedArticle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockRelatedArticle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['title'] = value.title;
  out['description'] = value.description;
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['author'] = value.author;
  out['publish_date'] = value.publish_date;
  return out;
}

/** pageBlockRelatedArticles = PageBlock — Related articles; instant view only */
export class PageBlockRelatedArticles {
  type: 'pageBlockRelatedArticles' = 'pageBlockRelatedArticles';
  extra?: string;
  /** header: RichText — Block header */
  header: TdRichText | null = null;
  /** articles: vector<pageBlockRelatedArticle> — List of related articles */
  articles: PageBlockRelatedArticle[] = [];
}

/** Decode a TDLib `pageBlockRelatedArticles` object; null input -> null. */
export function decodePageBlockRelatedArticles(json: TdJsonRaw): PageBlockRelatedArticles | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockRelatedArticles = new PageBlockRelatedArticles();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.header = decodeTdRichText(record['header']);
  out.articles = decodeVector(record['articles'], (item: TdJsonRaw): PageBlockRelatedArticle => decodePageBlockRelatedArticle(item) as PageBlockRelatedArticle);
  return out;
}

/** Encode a `pageBlockRelatedArticles` into TDLib JSON wire shape. */
export function encodePageBlockRelatedArticles(value: PageBlockRelatedArticles): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockRelatedArticles';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['header'] = value.header === null ? null : encodeTdRichText(value.header);
  out['articles'] = encodeVector(value.articles, (item: PageBlockRelatedArticle | null): TdJsonRaw => item === null ? null : encodePageBlockRelatedArticle(item));
  return out;
}

/** pageBlockSectionHeading = PageBlock — A section heading */
export class PageBlockSectionHeading {
  type: 'pageBlockSectionHeading' = 'pageBlockSectionHeading';
  extra?: string;
  /** text: RichText — Text of the section heading */
  text: TdRichText | null = null;
  /** size: int32 — Relative size of the text font; 1-6, 1 is the largest, 6 is the smallest */
  size: number = 0;
}

/** Decode a TDLib `pageBlockSectionHeading` object; null input -> null. */
export function decodePageBlockSectionHeading(json: TdJsonRaw): PageBlockSectionHeading | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockSectionHeading = new PageBlockSectionHeading();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.size = asNumber(record['size']);
  return out;
}

/** Encode a `pageBlockSectionHeading` into TDLib JSON wire shape. */
export function encodePageBlockSectionHeading(value: PageBlockSectionHeading): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockSectionHeading';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['size'] = value.size;
  return out;
}

/** pageBlockSlideshow = PageBlock — A slideshow */
export class PageBlockSlideshow {
  type: 'pageBlockSlideshow' = 'pageBlockSlideshow';
  extra?: string;
  /** blocks: vector<PageBlock> — Slideshow item contents */
  blocks: TdPageBlock[] = [];
  /** caption: pageBlockCaption — Block caption; may be null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `pageBlockSlideshow` object; null input -> null. */
export function decodePageBlockSlideshow(json: TdJsonRaw): PageBlockSlideshow | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockSlideshow = new PageBlockSlideshow();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.blocks = decodeVector(record['blocks'], (item: TdJsonRaw): TdPageBlock => decodeTdPageBlock(item) as TdPageBlock);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `pageBlockSlideshow` into TDLib JSON wire shape. */
export function encodePageBlockSlideshow(value: PageBlockSlideshow): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockSlideshow';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['blocks'] = encodeVector(value.blocks, (item: TdPageBlock | null): TdJsonRaw => item === null ? null : encodeTdPageBlock(item));
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** pageBlockSubheader = PageBlock — A subheader; instant view only */
export class PageBlockSubheader {
  type: 'pageBlockSubheader' = 'pageBlockSubheader';
  extra?: string;
  /** subheader: RichText — Subheader */
  subheader: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockSubheader` object; null input -> null. */
export function decodePageBlockSubheader(json: TdJsonRaw): PageBlockSubheader | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockSubheader = new PageBlockSubheader();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subheader = decodeTdRichText(record['subheader']);
  return out;
}

/** Encode a `pageBlockSubheader` into TDLib JSON wire shape. */
export function encodePageBlockSubheader(value: PageBlockSubheader): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockSubheader';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subheader'] = value.subheader === null ? null : encodeTdRichText(value.subheader);
  return out;
}

/** pageBlockSubtitle = PageBlock — The subtitle of a page; instant view only */
export class PageBlockSubtitle {
  type: 'pageBlockSubtitle' = 'pageBlockSubtitle';
  extra?: string;
  /** subtitle: RichText — Subtitle */
  subtitle: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockSubtitle` object; null input -> null. */
export function decodePageBlockSubtitle(json: TdJsonRaw): PageBlockSubtitle | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockSubtitle = new PageBlockSubtitle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.subtitle = decodeTdRichText(record['subtitle']);
  return out;
}

/** Encode a `pageBlockSubtitle` into TDLib JSON wire shape. */
export function encodePageBlockSubtitle(value: PageBlockSubtitle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockSubtitle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['subtitle'] = value.subtitle === null ? null : encodeTdRichText(value.subtitle);
  return out;
}

/** pageBlockTable = PageBlock — A table */
export class PageBlockTable {
  type: 'pageBlockTable' = 'pageBlockTable';
  extra?: string;
  /** caption: RichText — Table caption; may be null if none */
  caption: TdRichText | null = null;
  /** cells: vector<vector<pageBlockTableCell>> — Table cells */
  cells: PageBlockTableCell[][] = [];
  /** is_bordered: Bool — True, if the table is bordered */
  is_bordered: boolean = false;
  /** is_striped: Bool — True, if the table is striped */
  is_striped: boolean = false;
  /** is_compact: Bool — True, if table cells must have smaller indents */
  is_compact: boolean = false;
}

/** Decode a TDLib `pageBlockTable` object; null input -> null. */
export function decodePageBlockTable(json: TdJsonRaw): PageBlockTable | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockTable = new PageBlockTable();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.caption = decodeTdRichText(record['caption']);
  out.cells = decodeVector(record['cells'], (items: TdJsonRaw): (PageBlockTableCell)[] => decodeVector(items, (item: TdJsonRaw): PageBlockTableCell => decodePageBlockTableCell(item) as PageBlockTableCell));
  out.is_bordered = asBool(record['is_bordered']);
  out.is_striped = asBool(record['is_striped']);
  out.is_compact = asBool(record['is_compact']);
  return out;
}

/** Encode a `pageBlockTable` into TDLib JSON wire shape. */
export function encodePageBlockTable(value: PageBlockTable): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockTable';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['caption'] = value.caption === null ? null : encodeTdRichText(value.caption);
  out['cells'] = encodeVector(value.cells, (items: (PageBlockTableCell | null)[]): TdJsonRaw[] => encodeVector(items, (item: PageBlockTableCell | null): TdJsonRaw => item === null ? null : encodePageBlockTableCell(item)));
  out['is_bordered'] = value.is_bordered;
  out['is_striped'] = value.is_striped;
  out['is_compact'] = value.is_compact;
  return out;
}

/** pageBlockTableCell = PageBlockTableCell — Represents a cell of a table */
export class PageBlockTableCell {
  type: 'pageBlockTableCell' = 'pageBlockTableCell';
  extra?: string;
  /** text: RichText — Cell text; may be null. If the text is null, then the cell must be invisible */
  text: TdRichText | null = null;
  /** is_header: Bool — True, if it is a header cell */
  is_header: boolean = false;
  /** colspan: int32 — The number of columns the cell spans */
  colspan: number = 0;
  /** rowspan: int32 — The number of rows the cell spans */
  rowspan: number = 0;
  /** align: PageBlockHorizontalAlignment — Horizontal cell content alignment */
  align: TdPageBlockHorizontalAlignment | null = null;
  /** valign: PageBlockVerticalAlignment — Vertical cell content alignment */
  valign: TdPageBlockVerticalAlignment | null = null;
}

/** Decode a TDLib `pageBlockTableCell` object; null input -> null. */
export function decodePageBlockTableCell(json: TdJsonRaw): PageBlockTableCell | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockTableCell = new PageBlockTableCell();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  out.is_header = asBool(record['is_header']);
  out.colspan = asNumber(record['colspan']);
  out.rowspan = asNumber(record['rowspan']);
  out.align = decodeTdPageBlockHorizontalAlignment(record['align']);
  out.valign = decodeTdPageBlockVerticalAlignment(record['valign']);
  return out;
}

/** Encode a `pageBlockTableCell` into TDLib JSON wire shape. */
export function encodePageBlockTableCell(value: PageBlockTableCell): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockTableCell';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  out['is_header'] = value.is_header;
  out['colspan'] = value.colspan;
  out['rowspan'] = value.rowspan;
  out['align'] = value.align === null ? null : encodeTdPageBlockHorizontalAlignment(value.align);
  out['valign'] = value.valign === null ? null : encodeTdPageBlockVerticalAlignment(value.valign);
  return out;
}

/** pageBlockThinking = PageBlock — A "Thinking..." placeholder; for pending rich messages only */
export class PageBlockThinking {
  type: 'pageBlockThinking' = 'pageBlockThinking';
  extra?: string;
  /** text: RichText — Text of the placeholder */
  text: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockThinking` object; null input -> null. */
export function decodePageBlockThinking(json: TdJsonRaw): PageBlockThinking | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockThinking = new PageBlockThinking();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeTdRichText(record['text']);
  return out;
}

/** Encode a `pageBlockThinking` into TDLib JSON wire shape. */
export function encodePageBlockThinking(value: PageBlockThinking): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockThinking';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeTdRichText(value.text);
  return out;
}

/** pageBlockTitle = PageBlock — The title of a page; instant view only */
export class PageBlockTitle {
  type: 'pageBlockTitle' = 'pageBlockTitle';
  extra?: string;
  /** title: RichText — Title */
  title: TdRichText | null = null;
}

/** Decode a TDLib `pageBlockTitle` object; null input -> null. */
export function decodePageBlockTitle(json: TdJsonRaw): PageBlockTitle | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockTitle = new PageBlockTitle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = decodeTdRichText(record['title']);
  return out;
}

/** Encode a `pageBlockTitle` into TDLib JSON wire shape. */
export function encodePageBlockTitle(value: PageBlockTitle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockTitle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title === null ? null : encodeTdRichText(value.title);
  return out;
}

/** pageBlockUnsupported = PageBlock — Represents a block unsupported by the current application version */
export class PageBlockUnsupported {
  type: 'pageBlockUnsupported' = 'pageBlockUnsupported';
  extra?: string;
}

/** Decode a TDLib `pageBlockUnsupported` object; null input -> null. */
export function decodePageBlockUnsupported(json: TdJsonRaw): PageBlockUnsupported | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockUnsupported = new PageBlockUnsupported();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pageBlockUnsupported` into TDLib JSON wire shape. */
export function encodePageBlockUnsupported(value: PageBlockUnsupported): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockUnsupported';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pageBlockVerticalAlignmentBottom = PageBlockVerticalAlignment — The content must be bottom-aligned */
export class PageBlockVerticalAlignmentBottom {
  type: 'pageBlockVerticalAlignmentBottom' = 'pageBlockVerticalAlignmentBottom';
  extra?: string;
}

/** Decode a TDLib `pageBlockVerticalAlignmentBottom` object; null input -> null. */
export function decodePageBlockVerticalAlignmentBottom(json: TdJsonRaw): PageBlockVerticalAlignmentBottom | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockVerticalAlignmentBottom = new PageBlockVerticalAlignmentBottom();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pageBlockVerticalAlignmentBottom` into TDLib JSON wire shape. */
export function encodePageBlockVerticalAlignmentBottom(value: PageBlockVerticalAlignmentBottom): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockVerticalAlignmentBottom';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pageBlockVerticalAlignmentMiddle = PageBlockVerticalAlignment — The content must be middle-aligned */
export class PageBlockVerticalAlignmentMiddle {
  type: 'pageBlockVerticalAlignmentMiddle' = 'pageBlockVerticalAlignmentMiddle';
  extra?: string;
}

/** Decode a TDLib `pageBlockVerticalAlignmentMiddle` object; null input -> null. */
export function decodePageBlockVerticalAlignmentMiddle(json: TdJsonRaw): PageBlockVerticalAlignmentMiddle | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockVerticalAlignmentMiddle = new PageBlockVerticalAlignmentMiddle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pageBlockVerticalAlignmentMiddle` into TDLib JSON wire shape. */
export function encodePageBlockVerticalAlignmentMiddle(value: PageBlockVerticalAlignmentMiddle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockVerticalAlignmentMiddle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pageBlockVerticalAlignmentTop = PageBlockVerticalAlignment — The content must be top-aligned */
export class PageBlockVerticalAlignmentTop {
  type: 'pageBlockVerticalAlignmentTop' = 'pageBlockVerticalAlignmentTop';
  extra?: string;
}

/** Decode a TDLib `pageBlockVerticalAlignmentTop` object; null input -> null. */
export function decodePageBlockVerticalAlignmentTop(json: TdJsonRaw): PageBlockVerticalAlignmentTop | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockVerticalAlignmentTop = new PageBlockVerticalAlignmentTop();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pageBlockVerticalAlignmentTop` into TDLib JSON wire shape. */
export function encodePageBlockVerticalAlignmentTop(value: PageBlockVerticalAlignmentTop): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockVerticalAlignmentTop';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pageBlockVideo = PageBlock — A video */
export class PageBlockVideo {
  type: 'pageBlockVideo' = 'pageBlockVideo';
  extra?: string;
  /** video: video — Video file; may be null */
  video: Video | null = null;
  /** caption: pageBlockCaption — Video caption; may be null if none */
  caption: PageBlockCaption | null = null;
  /** need_autoplay: Bool — True, if the video must be played automatically */
  need_autoplay: boolean = false;
  /** is_looped: Bool — True, if the video must be looped */
  is_looped: boolean = false;
  /** has_spoiler: Bool — True, if the video preview must be covered by a spoiler animation */
  has_spoiler: boolean = false;
}

/** Decode a TDLib `pageBlockVideo` object; null input -> null. */
export function decodePageBlockVideo(json: TdJsonRaw): PageBlockVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockVideo = new PageBlockVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeVideo(record['video']);
  out.caption = decodePageBlockCaption(record['caption']);
  out.need_autoplay = asBool(record['need_autoplay']);
  out.is_looped = asBool(record['is_looped']);
  out.has_spoiler = asBool(record['has_spoiler']);
  return out;
}

/** Encode a `pageBlockVideo` into TDLib JSON wire shape. */
export function encodePageBlockVideo(value: PageBlockVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  out['need_autoplay'] = value.need_autoplay;
  out['is_looped'] = value.is_looped;
  out['has_spoiler'] = value.has_spoiler;
  return out;
}

/** pageBlockVoiceNote = PageBlock — A voice note */
export class PageBlockVoiceNote {
  type: 'pageBlockVoiceNote' = 'pageBlockVoiceNote';
  extra?: string;
  /** voice_note: voiceNote — Voice note */
  voice_note: VoiceNote | null = null;
  /** caption: pageBlockCaption — Voice note caption; may be null if none */
  caption: PageBlockCaption | null = null;
}

/** Decode a TDLib `pageBlockVoiceNote` object; null input -> null. */
export function decodePageBlockVoiceNote(json: TdJsonRaw): PageBlockVoiceNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PageBlockVoiceNote = new PageBlockVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.voice_note = decodeVoiceNote(record['voice_note']);
  out.caption = decodePageBlockCaption(record['caption']);
  return out;
}

/** Encode a `pageBlockVoiceNote` into TDLib JSON wire shape. */
export function encodePageBlockVoiceNote(value: PageBlockVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pageBlockVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['voice_note'] = value.voice_note === null ? null : encodeVoiceNote(value.voice_note);
  out['caption'] = value.caption === null ? null : encodePageBlockCaption(value.caption);
  return out;
}

/** paidMediaPhoto = PaidMedia — The media is a photo */
export class PaidMediaPhoto {
  type: 'paidMediaPhoto' = 'paidMediaPhoto';
  extra?: string;
  /** photo: photo — The photo */
  photo: Photo | null = null;
  /** video: video — The video representing the live photo; may be null if the photo is static */
  video: Video | null = null;
}

/** Decode a TDLib `paidMediaPhoto` object; null input -> null. */
export function decodePaidMediaPhoto(json: TdJsonRaw): PaidMediaPhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaidMediaPhoto = new PaidMediaPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  out.video = decodeVideo(record['video']);
  return out;
}

/** Encode a `paidMediaPhoto` into TDLib JSON wire shape. */
export function encodePaidMediaPhoto(value: PaidMediaPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paidMediaPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  return out;
}

/** paidMediaPreview = PaidMedia — The media is hidden until the invoice is paid */
export class PaidMediaPreview {
  type: 'paidMediaPreview' = 'paidMediaPreview';
  extra?: string;
  /** width: int32 — Media width; 0 if unknown */
  width: number = 0;
  /** height: int32 — Media height; 0 if unknown */
  height: number = 0;
  /** duration: int32 — Media duration, in seconds; 0 if unknown */
  duration: number = 0;
  /** minithumbnail: minithumbnail — Media minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
}

/** Decode a TDLib `paidMediaPreview` object; null input -> null. */
export function decodePaidMediaPreview(json: TdJsonRaw): PaidMediaPreview | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaidMediaPreview = new PaidMediaPreview();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.duration = asNumber(record['duration']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  return out;
}

/** Encode a `paidMediaPreview` into TDLib JSON wire shape. */
export function encodePaidMediaPreview(value: PaidMediaPreview): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paidMediaPreview';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['width'] = value.width;
  out['height'] = value.height;
  out['duration'] = value.duration;
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  return out;
}

/** paidMediaUnsupported = PaidMedia — The media is unsupported */
export class PaidMediaUnsupported {
  type: 'paidMediaUnsupported' = 'paidMediaUnsupported';
  extra?: string;
}

/** Decode a TDLib `paidMediaUnsupported` object; null input -> null. */
export function decodePaidMediaUnsupported(json: TdJsonRaw): PaidMediaUnsupported | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaidMediaUnsupported = new PaidMediaUnsupported();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `paidMediaUnsupported` into TDLib JSON wire shape. */
export function encodePaidMediaUnsupported(value: PaidMediaUnsupported): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paidMediaUnsupported';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** paidMediaVideo = PaidMedia — The media is a video */
export class PaidMediaVideo {
  type: 'paidMediaVideo' = 'paidMediaVideo';
  extra?: string;
  /** video: video — The video */
  video: Video | null = null;
  /** cover: photo — Cover of the video; may be null if none */
  cover: Photo | null = null;
  /** start_timestamp: int32 — Timestamp from which the video playing must start, in seconds */
  start_timestamp: number = 0;
}

/** Decode a TDLib `paidMediaVideo` object; null input -> null. */
export function decodePaidMediaVideo(json: TdJsonRaw): PaidMediaVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaidMediaVideo = new PaidMediaVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeVideo(record['video']);
  out.cover = decodePhoto(record['cover']);
  out.start_timestamp = asNumber(record['start_timestamp']);
  return out;
}

/** Encode a `paidMediaVideo` into TDLib JSON wire shape. */
export function encodePaidMediaVideo(value: PaidMediaVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paidMediaVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  out['cover'] = value.cover === null ? null : encodePhoto(value.cover);
  out['start_timestamp'] = value.start_timestamp;
  return out;
}

/** paidReactionTypeAnonymous = PaidReactionType — An anonymous paid reaction */
export class PaidReactionTypeAnonymous {
  type: 'paidReactionTypeAnonymous' = 'paidReactionTypeAnonymous';
  extra?: string;
}

/** Decode a TDLib `paidReactionTypeAnonymous` object; null input -> null. */
export function decodePaidReactionTypeAnonymous(json: TdJsonRaw): PaidReactionTypeAnonymous | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaidReactionTypeAnonymous = new PaidReactionTypeAnonymous();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `paidReactionTypeAnonymous` into TDLib JSON wire shape. */
export function encodePaidReactionTypeAnonymous(value: PaidReactionTypeAnonymous): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paidReactionTypeAnonymous';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** paidReactionTypeChat = PaidReactionType — A paid reaction on behalf of an owned chat */
export class PaidReactionTypeChat {
  type: 'paidReactionTypeChat' = 'paidReactionTypeChat';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
}

/** Decode a TDLib `paidReactionTypeChat` object; null input -> null. */
export function decodePaidReactionTypeChat(json: TdJsonRaw): PaidReactionTypeChat | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaidReactionTypeChat = new PaidReactionTypeChat();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `paidReactionTypeChat` into TDLib JSON wire shape. */
export function encodePaidReactionTypeChat(value: PaidReactionTypeChat): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paidReactionTypeChat';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** paidReactionTypeRegular = PaidReactionType — A paid reaction on behalf of the current user */
export class PaidReactionTypeRegular {
  type: 'paidReactionTypeRegular' = 'paidReactionTypeRegular';
  extra?: string;
}

/** Decode a TDLib `paidReactionTypeRegular` object; null input -> null. */
export function decodePaidReactionTypeRegular(json: TdJsonRaw): PaidReactionTypeRegular | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaidReactionTypeRegular = new PaidReactionTypeRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `paidReactionTypeRegular` into TDLib JSON wire shape. */
export function encodePaidReactionTypeRegular(value: PaidReactionTypeRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paidReactionTypeRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** paidReactor = PaidReactor — Contains information about a user who added paid reactions */
export class PaidReactor {
  type: 'paidReactor' = 'paidReactor';
  extra?: string;
  /** sender_id: MessageSender — Identifier of the user or chat that added the reactions; may be null for anonymous reactors that aren't the current user */
  sender_id: TdMessageSender | null = null;
  /** star_count: int53 — Number of Telegram Stars added */
  star_count: number = 0;
  /** is_top: Bool — True, if the reactor is one of the most active reactors; may be false if the reactor is the current user */
  is_top: boolean = false;
  /** is_me: Bool — True, if the paid reaction was added by the current user */
  is_me: boolean = false;
  /** is_anonymous: Bool — True, if the reactor is anonymous */
  is_anonymous: boolean = false;
}

/** Decode a TDLib `paidReactor` object; null input -> null. */
export function decodePaidReactor(json: TdJsonRaw): PaidReactor | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaidReactor = new PaidReactor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sender_id = decodeTdMessageSender(record['sender_id']);
  out.star_count = asNumber(record['star_count']);
  out.is_top = asBool(record['is_top']);
  out.is_me = asBool(record['is_me']);
  out.is_anonymous = asBool(record['is_anonymous']);
  return out;
}

/** Encode a `paidReactor` into TDLib JSON wire shape. */
export function encodePaidReactor(value: PaidReactor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paidReactor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sender_id'] = value.sender_id === null ? null : encodeTdMessageSender(value.sender_id);
  out['star_count'] = value.star_count;
  out['is_top'] = value.is_top;
  out['is_me'] = value.is_me;
  out['is_anonymous'] = value.is_anonymous;
  return out;
}

/** parseMarkdown = FormattedText — Parses Markdown entities in a human-friendly format, ignoring markup errors. Can be called synchronously */
export class ParseMarkdown {
  type: 'parseMarkdown' = 'parseMarkdown';
  extra?: string;
  /** text: formattedText — The text to parse. For example, "__italic__ ~~strikethrough~~ ||spoiler|| **bold** `code` ```pre``` __[italic__ text_url](telegram.org) __italic**bold italic__bold**" */
  text: FormattedText | null = null;
}

/** Decode a TDLib `parseMarkdown` object; null input -> null. */
export function decodeParseMarkdown(json: TdJsonRaw): ParseMarkdown | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ParseMarkdown = new ParseMarkdown();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = decodeFormattedText(record['text']);
  return out;
}

/** Encode a `parseMarkdown` into TDLib JSON wire shape. */
export function encodeParseMarkdown(value: ParseMarkdown): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'parseMarkdown';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  return out;
}

/** parseTextEntities = FormattedText — Parses Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, BlockQuote, ExpandableBlockQuote, Code, Pre, PreCode, TextUrl, MentionName, and DateTime entities from a marked-up text. Can be call... */
export class ParseTextEntities {
  type: 'parseTextEntities' = 'parseTextEntities';
  extra?: string;
  /** text: string — The text to parse */
  text: string = '';
  /** parse_mode: TextParseMode — Text parse mode */
  parse_mode: TdTextParseMode | null = null;
}

/** Decode a TDLib `parseTextEntities` object; null input -> null. */
export function decodeParseTextEntities(json: TdJsonRaw): ParseTextEntities | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ParseTextEntities = new ParseTextEntities();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.parse_mode = decodeTdTextParseMode(record['parse_mode']);
  return out;
}

/** Encode a `parseTextEntities` into TDLib JSON wire shape. */
export function encodeParseTextEntities(value: ParseTextEntities): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'parseTextEntities';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['parse_mode'] = value.parse_mode === null ? null : encodeTdTextParseMode(value.parse_mode);
  return out;
}

/** passkey = Passkey — Describes a passkey */
export class Passkey {
  type: 'passkey' = 'passkey';
  extra?: string;
  /** id: string — Unique identifier of the passkey */
  id: string = '';
  /** name: string — Name of the passkey */
  name: string = '';
  /** addition_date: int32 — Point in time (Unix timestamp) when the passkey was added */
  addition_date: number = 0;
  /** last_usage_date: int32 — Point in time (Unix timestamp) when the passkey was used last time; 0 if never */
  last_usage_date: number = 0;
  /** software_icon_custom_emoji_id: int64 — Identifier of the custom emoji that is used as the icon of the software that created the passkey; 0 if unknown */
  software_icon_custom_emoji_id: string = '';
}

/** Decode a TDLib `passkey` object; null input -> null. */
export function decodePasskey(json: TdJsonRaw): Passkey | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Passkey = new Passkey();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.name = asString(record['name']);
  out.addition_date = asNumber(record['addition_date']);
  out.last_usage_date = asNumber(record['last_usage_date']);
  out.software_icon_custom_emoji_id = asInt64(record['software_icon_custom_emoji_id']);
  return out;
}

/** Encode a `passkey` into TDLib JSON wire shape. */
export function encodePasskey(value: Passkey): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passkey';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['name'] = value.name;
  out['addition_date'] = value.addition_date;
  out['last_usage_date'] = value.last_usage_date;
  out['software_icon_custom_emoji_id'] = value.software_icon_custom_emoji_id;
  return out;
}

/** passkeys = Passkeys — Contains a list of passkeys */
export class Passkeys {
  type: 'passkeys' = 'passkeys';
  extra?: string;
  /** passkeys: vector<passkey> — List of passkeys */
  passkeys: Passkey[] = [];
}

/** Decode a TDLib `passkeys` object; null input -> null. */
export function decodePasskeys(json: TdJsonRaw): Passkeys | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Passkeys = new Passkeys();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.passkeys = decodeVector(record['passkeys'], (item: TdJsonRaw): Passkey => decodePasskey(item) as Passkey);
  return out;
}

/** Encode a `passkeys` into TDLib JSON wire shape. */
export function encodePasskeys(value: Passkeys): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passkeys';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['passkeys'] = encodeVector(value.passkeys, (item: Passkey | null): TdJsonRaw => item === null ? null : encodePasskey(item));
  return out;
}

/** passportAuthorizationForm = PassportAuthorizationForm — Contains information about a Telegram Passport authorization form that was requested */
export class PassportAuthorizationForm {
  type: 'passportAuthorizationForm' = 'passportAuthorizationForm';
  extra?: string;
  /** id: int32 — Unique identifier of the authorization form */
  id: number = 0;
  /** required_elements: vector<passportRequiredElement> — Telegram Passport elements that must be provided to complete the form */
  required_elements: PassportRequiredElement[] = [];
  /** privacy_policy_url: string — URL for the privacy policy of the service; may be empty */
  privacy_policy_url: string = '';
}

/** Decode a TDLib `passportAuthorizationForm` object; null input -> null. */
export function decodePassportAuthorizationForm(json: TdJsonRaw): PassportAuthorizationForm | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportAuthorizationForm = new PassportAuthorizationForm();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.required_elements = decodeVector(record['required_elements'], (item: TdJsonRaw): PassportRequiredElement => decodePassportRequiredElement(item) as PassportRequiredElement);
  out.privacy_policy_url = asString(record['privacy_policy_url']);
  return out;
}

/** Encode a `passportAuthorizationForm` into TDLib JSON wire shape. */
export function encodePassportAuthorizationForm(value: PassportAuthorizationForm): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportAuthorizationForm';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['required_elements'] = encodeVector(value.required_elements, (item: PassportRequiredElement | null): TdJsonRaw => item === null ? null : encodePassportRequiredElement(item));
  out['privacy_policy_url'] = value.privacy_policy_url;
  return out;
}

/** passportElementAddress = PassportElement — A Telegram Passport element containing the user's address */
export class PassportElementAddress {
  type: 'passportElementAddress' = 'passportElementAddress';
  extra?: string;
  /** address: address — Address */
  address: Address | null = null;
}

/** Decode a TDLib `passportElementAddress` object; null input -> null. */
export function decodePassportElementAddress(json: TdJsonRaw): PassportElementAddress | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementAddress = new PassportElementAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.address = decodeAddress(record['address']);
  return out;
}

/** Encode a `passportElementAddress` into TDLib JSON wire shape. */
export function encodePassportElementAddress(value: PassportElementAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['address'] = value.address === null ? null : encodeAddress(value.address);
  return out;
}

/** passportElementBankStatement = PassportElement — A Telegram Passport element containing the user's bank statement */
export class PassportElementBankStatement {
  type: 'passportElementBankStatement' = 'passportElementBankStatement';
  extra?: string;
  /** bank_statement: personalDocument — Bank statement */
  bank_statement: PersonalDocument | null = null;
}

/** Decode a TDLib `passportElementBankStatement` object; null input -> null. */
export function decodePassportElementBankStatement(json: TdJsonRaw): PassportElementBankStatement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementBankStatement = new PassportElementBankStatement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.bank_statement = decodePersonalDocument(record['bank_statement']);
  return out;
}

/** Encode a `passportElementBankStatement` into TDLib JSON wire shape. */
export function encodePassportElementBankStatement(value: PassportElementBankStatement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementBankStatement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['bank_statement'] = value.bank_statement === null ? null : encodePersonalDocument(value.bank_statement);
  return out;
}

/** passportElementDriverLicense = PassportElement — A Telegram Passport element containing the user's driver license */
export class PassportElementDriverLicense {
  type: 'passportElementDriverLicense' = 'passportElementDriverLicense';
  extra?: string;
  /** driver_license: identityDocument — Driver license */
  driver_license: IdentityDocument | null = null;
}

/** Decode a TDLib `passportElementDriverLicense` object; null input -> null. */
export function decodePassportElementDriverLicense(json: TdJsonRaw): PassportElementDriverLicense | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementDriverLicense = new PassportElementDriverLicense();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.driver_license = decodeIdentityDocument(record['driver_license']);
  return out;
}

/** Encode a `passportElementDriverLicense` into TDLib JSON wire shape. */
export function encodePassportElementDriverLicense(value: PassportElementDriverLicense): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementDriverLicense';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['driver_license'] = value.driver_license === null ? null : encodeIdentityDocument(value.driver_license);
  return out;
}

/** passportElementEmailAddress = PassportElement — A Telegram Passport element containing the user's email address */
export class PassportElementEmailAddress {
  type: 'passportElementEmailAddress' = 'passportElementEmailAddress';
  extra?: string;
  /** email_address: string — Email address */
  email_address: string = '';
}

/** Decode a TDLib `passportElementEmailAddress` object; null input -> null. */
export function decodePassportElementEmailAddress(json: TdJsonRaw): PassportElementEmailAddress | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementEmailAddress = new PassportElementEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.email_address = asString(record['email_address']);
  return out;
}

/** Encode a `passportElementEmailAddress` into TDLib JSON wire shape. */
export function encodePassportElementEmailAddress(value: PassportElementEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['email_address'] = value.email_address;
  return out;
}

/** passportElementError = PassportElementError — Contains the description of an error in a Telegram Passport element */
export class PassportElementError {
  type: 'passportElementError' = 'passportElementError';
  extra?: string;
  /** type: PassportElementType — Type of the Telegram Passport element which has the error */
  type_: TdPassportElementType | null = null;
  /** message: string — Error message */
  message: string = '';
  /** source: PassportElementErrorSource — Error source */
  source: TdPassportElementErrorSource | null = null;
}

/** Decode a TDLib `passportElementError` object; null input -> null. */
export function decodePassportElementError(json: TdJsonRaw): PassportElementError | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementError = new PassportElementError();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPassportElementType(record['type']);
  out.message = asString(record['message']);
  out.source = decodeTdPassportElementErrorSource(record['source']);
  return out;
}

/** Encode a `passportElementError` into TDLib JSON wire shape. */
export function encodePassportElementError(value: PassportElementError): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementError';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPassportElementType(value.type_);
  out['message'] = value.message;
  out['source'] = value.source === null ? null : encodeTdPassportElementErrorSource(value.source);
  return out;
}

/** passportElementErrorSourceDataField = PassportElementErrorSource — One of the data fields contains an error. The error will be considered resolved when the value of the field changes */
export class PassportElementErrorSourceDataField {
  type: 'passportElementErrorSourceDataField' = 'passportElementErrorSourceDataField';
  extra?: string;
  /** field_name: string — Field name */
  field_name: string = '';
}

/** Decode a TDLib `passportElementErrorSourceDataField` object; null input -> null. */
export function decodePassportElementErrorSourceDataField(json: TdJsonRaw): PassportElementErrorSourceDataField | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementErrorSourceDataField = new PassportElementErrorSourceDataField();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.field_name = asString(record['field_name']);
  return out;
}

/** Encode a `passportElementErrorSourceDataField` into TDLib JSON wire shape. */
export function encodePassportElementErrorSourceDataField(value: PassportElementErrorSourceDataField): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementErrorSourceDataField';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['field_name'] = value.field_name;
  return out;
}

/** passportElementErrorSourceFile = PassportElementErrorSource — The file contains an error. The error will be considered resolved when the file changes */
export class PassportElementErrorSourceFile {
  type: 'passportElementErrorSourceFile' = 'passportElementErrorSourceFile';
  extra?: string;
  /** file_index: int32 — Index of a file with the error */
  file_index: number = 0;
}

/** Decode a TDLib `passportElementErrorSourceFile` object; null input -> null. */
export function decodePassportElementErrorSourceFile(json: TdJsonRaw): PassportElementErrorSourceFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementErrorSourceFile = new PassportElementErrorSourceFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_index = asNumber(record['file_index']);
  return out;
}

/** Encode a `passportElementErrorSourceFile` into TDLib JSON wire shape. */
export function encodePassportElementErrorSourceFile(value: PassportElementErrorSourceFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementErrorSourceFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_index'] = value.file_index;
  return out;
}

/** passportElementErrorSourceFiles = PassportElementErrorSource — The list of attached files contains an error. The error will be considered resolved when the list of files changes */
export class PassportElementErrorSourceFiles {
  type: 'passportElementErrorSourceFiles' = 'passportElementErrorSourceFiles';
  extra?: string;
}

/** Decode a TDLib `passportElementErrorSourceFiles` object; null input -> null. */
export function decodePassportElementErrorSourceFiles(json: TdJsonRaw): PassportElementErrorSourceFiles | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementErrorSourceFiles = new PassportElementErrorSourceFiles();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementErrorSourceFiles` into TDLib JSON wire shape. */
export function encodePassportElementErrorSourceFiles(value: PassportElementErrorSourceFiles): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementErrorSourceFiles';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementErrorSourceFrontSide = PassportElementErrorSource — The front side of the document contains an error. The error will be considered resolved when the file with the front side changes */
export class PassportElementErrorSourceFrontSide {
  type: 'passportElementErrorSourceFrontSide' = 'passportElementErrorSourceFrontSide';
  extra?: string;
}

/** Decode a TDLib `passportElementErrorSourceFrontSide` object; null input -> null. */
export function decodePassportElementErrorSourceFrontSide(json: TdJsonRaw): PassportElementErrorSourceFrontSide | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementErrorSourceFrontSide = new PassportElementErrorSourceFrontSide();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementErrorSourceFrontSide` into TDLib JSON wire shape. */
export function encodePassportElementErrorSourceFrontSide(value: PassportElementErrorSourceFrontSide): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementErrorSourceFrontSide';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementErrorSourceReverseSide = PassportElementErrorSource — The reverse side of the document contains an error. The error will be considered resolved when the file with the reverse side changes */
export class PassportElementErrorSourceReverseSide {
  type: 'passportElementErrorSourceReverseSide' = 'passportElementErrorSourceReverseSide';
  extra?: string;
}

/** Decode a TDLib `passportElementErrorSourceReverseSide` object; null input -> null. */
export function decodePassportElementErrorSourceReverseSide(json: TdJsonRaw): PassportElementErrorSourceReverseSide | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementErrorSourceReverseSide = new PassportElementErrorSourceReverseSide();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementErrorSourceReverseSide` into TDLib JSON wire shape. */
export function encodePassportElementErrorSourceReverseSide(value: PassportElementErrorSourceReverseSide): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementErrorSourceReverseSide';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementErrorSourceSelfie = PassportElementErrorSource — The selfie with the document contains an error. The error will be considered resolved when the file with the selfie changes */
export class PassportElementErrorSourceSelfie {
  type: 'passportElementErrorSourceSelfie' = 'passportElementErrorSourceSelfie';
  extra?: string;
}

/** Decode a TDLib `passportElementErrorSourceSelfie` object; null input -> null. */
export function decodePassportElementErrorSourceSelfie(json: TdJsonRaw): PassportElementErrorSourceSelfie | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementErrorSourceSelfie = new PassportElementErrorSourceSelfie();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementErrorSourceSelfie` into TDLib JSON wire shape. */
export function encodePassportElementErrorSourceSelfie(value: PassportElementErrorSourceSelfie): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementErrorSourceSelfie';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementErrorSourceTranslationFile = PassportElementErrorSource — One of files with the translation of the document contains an error. The error will be considered resolved when the file changes */
export class PassportElementErrorSourceTranslationFile {
  type: 'passportElementErrorSourceTranslationFile' = 'passportElementErrorSourceTranslationFile';
  extra?: string;
  /** file_index: int32 — Index of a file with the error */
  file_index: number = 0;
}

/** Decode a TDLib `passportElementErrorSourceTranslationFile` object; null input -> null. */
export function decodePassportElementErrorSourceTranslationFile(json: TdJsonRaw): PassportElementErrorSourceTranslationFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementErrorSourceTranslationFile = new PassportElementErrorSourceTranslationFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file_index = asNumber(record['file_index']);
  return out;
}

/** Encode a `passportElementErrorSourceTranslationFile` into TDLib JSON wire shape. */
export function encodePassportElementErrorSourceTranslationFile(value: PassportElementErrorSourceTranslationFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementErrorSourceTranslationFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file_index'] = value.file_index;
  return out;
}

/** passportElementErrorSourceTranslationFiles = PassportElementErrorSource — The translation of the document contains an error. The error will be considered resolved when the list of translation files changes */
export class PassportElementErrorSourceTranslationFiles {
  type: 'passportElementErrorSourceTranslationFiles' = 'passportElementErrorSourceTranslationFiles';
  extra?: string;
}

/** Decode a TDLib `passportElementErrorSourceTranslationFiles` object; null input -> null. */
export function decodePassportElementErrorSourceTranslationFiles(json: TdJsonRaw): PassportElementErrorSourceTranslationFiles | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementErrorSourceTranslationFiles = new PassportElementErrorSourceTranslationFiles();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementErrorSourceTranslationFiles` into TDLib JSON wire shape. */
export function encodePassportElementErrorSourceTranslationFiles(value: PassportElementErrorSourceTranslationFiles): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementErrorSourceTranslationFiles';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementErrorSourceUnspecified = PassportElementErrorSource — The element contains an error in an unspecified place. The error will be considered resolved when new data is added */
export class PassportElementErrorSourceUnspecified {
  type: 'passportElementErrorSourceUnspecified' = 'passportElementErrorSourceUnspecified';
  extra?: string;
}

/** Decode a TDLib `passportElementErrorSourceUnspecified` object; null input -> null. */
export function decodePassportElementErrorSourceUnspecified(json: TdJsonRaw): PassportElementErrorSourceUnspecified | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementErrorSourceUnspecified = new PassportElementErrorSourceUnspecified();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementErrorSourceUnspecified` into TDLib JSON wire shape. */
export function encodePassportElementErrorSourceUnspecified(value: PassportElementErrorSourceUnspecified): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementErrorSourceUnspecified';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementIdentityCard = PassportElement — A Telegram Passport element containing the user's identity card */
export class PassportElementIdentityCard {
  type: 'passportElementIdentityCard' = 'passportElementIdentityCard';
  extra?: string;
  /** identity_card: identityDocument — Identity card */
  identity_card: IdentityDocument | null = null;
}

/** Decode a TDLib `passportElementIdentityCard` object; null input -> null. */
export function decodePassportElementIdentityCard(json: TdJsonRaw): PassportElementIdentityCard | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementIdentityCard = new PassportElementIdentityCard();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.identity_card = decodeIdentityDocument(record['identity_card']);
  return out;
}

/** Encode a `passportElementIdentityCard` into TDLib JSON wire shape. */
export function encodePassportElementIdentityCard(value: PassportElementIdentityCard): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementIdentityCard';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['identity_card'] = value.identity_card === null ? null : encodeIdentityDocument(value.identity_card);
  return out;
}

/** passportElementInternalPassport = PassportElement — A Telegram Passport element containing the user's internal passport */
export class PassportElementInternalPassport {
  type: 'passportElementInternalPassport' = 'passportElementInternalPassport';
  extra?: string;
  /** internal_passport: identityDocument — Internal passport */
  internal_passport: IdentityDocument | null = null;
}

/** Decode a TDLib `passportElementInternalPassport` object; null input -> null. */
export function decodePassportElementInternalPassport(json: TdJsonRaw): PassportElementInternalPassport | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementInternalPassport = new PassportElementInternalPassport();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.internal_passport = decodeIdentityDocument(record['internal_passport']);
  return out;
}

/** Encode a `passportElementInternalPassport` into TDLib JSON wire shape. */
export function encodePassportElementInternalPassport(value: PassportElementInternalPassport): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementInternalPassport';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['internal_passport'] = value.internal_passport === null ? null : encodeIdentityDocument(value.internal_passport);
  return out;
}

/** passportElementPassport = PassportElement — A Telegram Passport element containing the user's passport */
export class PassportElementPassport {
  type: 'passportElementPassport' = 'passportElementPassport';
  extra?: string;
  /** passport: identityDocument — Passport */
  passport: IdentityDocument | null = null;
}

/** Decode a TDLib `passportElementPassport` object; null input -> null. */
export function decodePassportElementPassport(json: TdJsonRaw): PassportElementPassport | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementPassport = new PassportElementPassport();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.passport = decodeIdentityDocument(record['passport']);
  return out;
}

/** Encode a `passportElementPassport` into TDLib JSON wire shape. */
export function encodePassportElementPassport(value: PassportElementPassport): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementPassport';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['passport'] = value.passport === null ? null : encodeIdentityDocument(value.passport);
  return out;
}

/** passportElementPassportRegistration = PassportElement — A Telegram Passport element containing the user's passport registration pages */
export class PassportElementPassportRegistration {
  type: 'passportElementPassportRegistration' = 'passportElementPassportRegistration';
  extra?: string;
  /** passport_registration: personalDocument — Passport registration pages */
  passport_registration: PersonalDocument | null = null;
}

/** Decode a TDLib `passportElementPassportRegistration` object; null input -> null. */
export function decodePassportElementPassportRegistration(json: TdJsonRaw): PassportElementPassportRegistration | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementPassportRegistration = new PassportElementPassportRegistration();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.passport_registration = decodePersonalDocument(record['passport_registration']);
  return out;
}

/** Encode a `passportElementPassportRegistration` into TDLib JSON wire shape. */
export function encodePassportElementPassportRegistration(value: PassportElementPassportRegistration): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementPassportRegistration';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['passport_registration'] = value.passport_registration === null ? null : encodePersonalDocument(value.passport_registration);
  return out;
}

/** passportElementPersonalDetails = PassportElement — A Telegram Passport element containing the user's personal details */
export class PassportElementPersonalDetails {
  type: 'passportElementPersonalDetails' = 'passportElementPersonalDetails';
  extra?: string;
  /** personal_details: personalDetails — Personal details of the user */
  personal_details: PersonalDetails | null = null;
}

/** Decode a TDLib `passportElementPersonalDetails` object; null input -> null. */
export function decodePassportElementPersonalDetails(json: TdJsonRaw): PassportElementPersonalDetails | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementPersonalDetails = new PassportElementPersonalDetails();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.personal_details = decodePersonalDetails(record['personal_details']);
  return out;
}

/** Encode a `passportElementPersonalDetails` into TDLib JSON wire shape. */
export function encodePassportElementPersonalDetails(value: PassportElementPersonalDetails): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementPersonalDetails';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['personal_details'] = value.personal_details === null ? null : encodePersonalDetails(value.personal_details);
  return out;
}

/** passportElementPhoneNumber = PassportElement — A Telegram Passport element containing the user's phone number */
export class PassportElementPhoneNumber {
  type: 'passportElementPhoneNumber' = 'passportElementPhoneNumber';
  extra?: string;
  /** phone_number: string — Phone number */
  phone_number: string = '';
}

/** Decode a TDLib `passportElementPhoneNumber` object; null input -> null. */
export function decodePassportElementPhoneNumber(json: TdJsonRaw): PassportElementPhoneNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementPhoneNumber = new PassportElementPhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.phone_number = asString(record['phone_number']);
  return out;
}

/** Encode a `passportElementPhoneNumber` into TDLib JSON wire shape. */
export function encodePassportElementPhoneNumber(value: PassportElementPhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementPhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['phone_number'] = value.phone_number;
  return out;
}

/** passportElementRentalAgreement = PassportElement — A Telegram Passport element containing the user's rental agreement */
export class PassportElementRentalAgreement {
  type: 'passportElementRentalAgreement' = 'passportElementRentalAgreement';
  extra?: string;
  /** rental_agreement: personalDocument — Rental agreement */
  rental_agreement: PersonalDocument | null = null;
}

/** Decode a TDLib `passportElementRentalAgreement` object; null input -> null. */
export function decodePassportElementRentalAgreement(json: TdJsonRaw): PassportElementRentalAgreement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementRentalAgreement = new PassportElementRentalAgreement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.rental_agreement = decodePersonalDocument(record['rental_agreement']);
  return out;
}

/** Encode a `passportElementRentalAgreement` into TDLib JSON wire shape. */
export function encodePassportElementRentalAgreement(value: PassportElementRentalAgreement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementRentalAgreement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['rental_agreement'] = value.rental_agreement === null ? null : encodePersonalDocument(value.rental_agreement);
  return out;
}

/** passportElementTemporaryRegistration = PassportElement — A Telegram Passport element containing the user's temporary registration */
export class PassportElementTemporaryRegistration {
  type: 'passportElementTemporaryRegistration' = 'passportElementTemporaryRegistration';
  extra?: string;
  /** temporary_registration: personalDocument — Temporary registration */
  temporary_registration: PersonalDocument | null = null;
}

/** Decode a TDLib `passportElementTemporaryRegistration` object; null input -> null. */
export function decodePassportElementTemporaryRegistration(json: TdJsonRaw): PassportElementTemporaryRegistration | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTemporaryRegistration = new PassportElementTemporaryRegistration();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.temporary_registration = decodePersonalDocument(record['temporary_registration']);
  return out;
}

/** Encode a `passportElementTemporaryRegistration` into TDLib JSON wire shape. */
export function encodePassportElementTemporaryRegistration(value: PassportElementTemporaryRegistration): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTemporaryRegistration';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['temporary_registration'] = value.temporary_registration === null ? null : encodePersonalDocument(value.temporary_registration);
  return out;
}

/** passportElementTypeAddress = PassportElementType — A Telegram Passport element containing the user's address */
export class PassportElementTypeAddress {
  type: 'passportElementTypeAddress' = 'passportElementTypeAddress';
  extra?: string;
}

/** Decode a TDLib `passportElementTypeAddress` object; null input -> null. */
export function decodePassportElementTypeAddress(json: TdJsonRaw): PassportElementTypeAddress | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypeAddress = new PassportElementTypeAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypeAddress` into TDLib JSON wire shape. */
export function encodePassportElementTypeAddress(value: PassportElementTypeAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypeAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypeBankStatement = PassportElementType — A Telegram Passport element containing the user's bank statement */
export class PassportElementTypeBankStatement {
  type: 'passportElementTypeBankStatement' = 'passportElementTypeBankStatement';
  extra?: string;
}

/** Decode a TDLib `passportElementTypeBankStatement` object; null input -> null. */
export function decodePassportElementTypeBankStatement(json: TdJsonRaw): PassportElementTypeBankStatement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypeBankStatement = new PassportElementTypeBankStatement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypeBankStatement` into TDLib JSON wire shape. */
export function encodePassportElementTypeBankStatement(value: PassportElementTypeBankStatement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypeBankStatement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypeDriverLicense = PassportElementType — A Telegram Passport element containing the user's driver license */
export class PassportElementTypeDriverLicense {
  type: 'passportElementTypeDriverLicense' = 'passportElementTypeDriverLicense';
  extra?: string;
}

/** Decode a TDLib `passportElementTypeDriverLicense` object; null input -> null. */
export function decodePassportElementTypeDriverLicense(json: TdJsonRaw): PassportElementTypeDriverLicense | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypeDriverLicense = new PassportElementTypeDriverLicense();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypeDriverLicense` into TDLib JSON wire shape. */
export function encodePassportElementTypeDriverLicense(value: PassportElementTypeDriverLicense): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypeDriverLicense';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypeEmailAddress = PassportElementType — A Telegram Passport element containing the user's email address */
export class PassportElementTypeEmailAddress {
  type: 'passportElementTypeEmailAddress' = 'passportElementTypeEmailAddress';
  extra?: string;
}

/** Decode a TDLib `passportElementTypeEmailAddress` object; null input -> null. */
export function decodePassportElementTypeEmailAddress(json: TdJsonRaw): PassportElementTypeEmailAddress | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypeEmailAddress = new PassportElementTypeEmailAddress();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypeEmailAddress` into TDLib JSON wire shape. */
export function encodePassportElementTypeEmailAddress(value: PassportElementTypeEmailAddress): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypeEmailAddress';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypeIdentityCard = PassportElementType — A Telegram Passport element containing the user's identity card */
export class PassportElementTypeIdentityCard {
  type: 'passportElementTypeIdentityCard' = 'passportElementTypeIdentityCard';
  extra?: string;
}

/** Decode a TDLib `passportElementTypeIdentityCard` object; null input -> null. */
export function decodePassportElementTypeIdentityCard(json: TdJsonRaw): PassportElementTypeIdentityCard | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypeIdentityCard = new PassportElementTypeIdentityCard();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypeIdentityCard` into TDLib JSON wire shape. */
export function encodePassportElementTypeIdentityCard(value: PassportElementTypeIdentityCard): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypeIdentityCard';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypeInternalPassport = PassportElementType — A Telegram Passport element containing the user's internal passport */
export class PassportElementTypeInternalPassport {
  type: 'passportElementTypeInternalPassport' = 'passportElementTypeInternalPassport';
  extra?: string;
}

/** Decode a TDLib `passportElementTypeInternalPassport` object; null input -> null. */
export function decodePassportElementTypeInternalPassport(json: TdJsonRaw): PassportElementTypeInternalPassport | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypeInternalPassport = new PassportElementTypeInternalPassport();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypeInternalPassport` into TDLib JSON wire shape. */
export function encodePassportElementTypeInternalPassport(value: PassportElementTypeInternalPassport): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypeInternalPassport';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypePassport = PassportElementType — A Telegram Passport element containing the user's passport */
export class PassportElementTypePassport {
  type: 'passportElementTypePassport' = 'passportElementTypePassport';
  extra?: string;
}

/** Decode a TDLib `passportElementTypePassport` object; null input -> null. */
export function decodePassportElementTypePassport(json: TdJsonRaw): PassportElementTypePassport | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypePassport = new PassportElementTypePassport();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypePassport` into TDLib JSON wire shape. */
export function encodePassportElementTypePassport(value: PassportElementTypePassport): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypePassport';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypePassportRegistration = PassportElementType — A Telegram Passport element containing the registration page of the user's passport */
export class PassportElementTypePassportRegistration {
  type: 'passportElementTypePassportRegistration' = 'passportElementTypePassportRegistration';
  extra?: string;
}

/** Decode a TDLib `passportElementTypePassportRegistration` object; null input -> null. */
export function decodePassportElementTypePassportRegistration(json: TdJsonRaw): PassportElementTypePassportRegistration | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypePassportRegistration = new PassportElementTypePassportRegistration();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypePassportRegistration` into TDLib JSON wire shape. */
export function encodePassportElementTypePassportRegistration(value: PassportElementTypePassportRegistration): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypePassportRegistration';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypePersonalDetails = PassportElementType — A Telegram Passport element containing the user's personal details */
export class PassportElementTypePersonalDetails {
  type: 'passportElementTypePersonalDetails' = 'passportElementTypePersonalDetails';
  extra?: string;
}

/** Decode a TDLib `passportElementTypePersonalDetails` object; null input -> null. */
export function decodePassportElementTypePersonalDetails(json: TdJsonRaw): PassportElementTypePersonalDetails | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypePersonalDetails = new PassportElementTypePersonalDetails();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypePersonalDetails` into TDLib JSON wire shape. */
export function encodePassportElementTypePersonalDetails(value: PassportElementTypePersonalDetails): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypePersonalDetails';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypePhoneNumber = PassportElementType — A Telegram Passport element containing the user's phone number */
export class PassportElementTypePhoneNumber {
  type: 'passportElementTypePhoneNumber' = 'passportElementTypePhoneNumber';
  extra?: string;
}

/** Decode a TDLib `passportElementTypePhoneNumber` object; null input -> null. */
export function decodePassportElementTypePhoneNumber(json: TdJsonRaw): PassportElementTypePhoneNumber | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypePhoneNumber = new PassportElementTypePhoneNumber();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypePhoneNumber` into TDLib JSON wire shape. */
export function encodePassportElementTypePhoneNumber(value: PassportElementTypePhoneNumber): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypePhoneNumber';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypeRentalAgreement = PassportElementType — A Telegram Passport element containing the user's rental agreement */
export class PassportElementTypeRentalAgreement {
  type: 'passportElementTypeRentalAgreement' = 'passportElementTypeRentalAgreement';
  extra?: string;
}

/** Decode a TDLib `passportElementTypeRentalAgreement` object; null input -> null. */
export function decodePassportElementTypeRentalAgreement(json: TdJsonRaw): PassportElementTypeRentalAgreement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypeRentalAgreement = new PassportElementTypeRentalAgreement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypeRentalAgreement` into TDLib JSON wire shape. */
export function encodePassportElementTypeRentalAgreement(value: PassportElementTypeRentalAgreement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypeRentalAgreement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypeTemporaryRegistration = PassportElementType — A Telegram Passport element containing the user's temporary registration */
export class PassportElementTypeTemporaryRegistration {
  type: 'passportElementTypeTemporaryRegistration' = 'passportElementTypeTemporaryRegistration';
  extra?: string;
}

/** Decode a TDLib `passportElementTypeTemporaryRegistration` object; null input -> null. */
export function decodePassportElementTypeTemporaryRegistration(json: TdJsonRaw): PassportElementTypeTemporaryRegistration | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypeTemporaryRegistration = new PassportElementTypeTemporaryRegistration();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypeTemporaryRegistration` into TDLib JSON wire shape. */
export function encodePassportElementTypeTemporaryRegistration(value: PassportElementTypeTemporaryRegistration): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypeTemporaryRegistration';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementTypeUtilityBill = PassportElementType — A Telegram Passport element containing the user's utility bill */
export class PassportElementTypeUtilityBill {
  type: 'passportElementTypeUtilityBill' = 'passportElementTypeUtilityBill';
  extra?: string;
}

/** Decode a TDLib `passportElementTypeUtilityBill` object; null input -> null. */
export function decodePassportElementTypeUtilityBill(json: TdJsonRaw): PassportElementTypeUtilityBill | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementTypeUtilityBill = new PassportElementTypeUtilityBill();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `passportElementTypeUtilityBill` into TDLib JSON wire shape. */
export function encodePassportElementTypeUtilityBill(value: PassportElementTypeUtilityBill): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementTypeUtilityBill';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** passportElementUtilityBill = PassportElement — A Telegram Passport element containing the user's utility bill */
export class PassportElementUtilityBill {
  type: 'passportElementUtilityBill' = 'passportElementUtilityBill';
  extra?: string;
  /** utility_bill: personalDocument — Utility bill */
  utility_bill: PersonalDocument | null = null;
}

/** Decode a TDLib `passportElementUtilityBill` object; null input -> null. */
export function decodePassportElementUtilityBill(json: TdJsonRaw): PassportElementUtilityBill | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementUtilityBill = new PassportElementUtilityBill();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.utility_bill = decodePersonalDocument(record['utility_bill']);
  return out;
}

/** Encode a `passportElementUtilityBill` into TDLib JSON wire shape. */
export function encodePassportElementUtilityBill(value: PassportElementUtilityBill): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementUtilityBill';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['utility_bill'] = value.utility_bill === null ? null : encodePersonalDocument(value.utility_bill);
  return out;
}

/** passportElements = PassportElements — Contains information about saved Telegram Passport elements */
export class PassportElements {
  type: 'passportElements' = 'passportElements';
  extra?: string;
  /** elements: vector<PassportElement> — Telegram Passport elements */
  elements: TdPassportElement[] = [];
}

/** Decode a TDLib `passportElements` object; null input -> null. */
export function decodePassportElements(json: TdJsonRaw): PassportElements | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElements = new PassportElements();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.elements = decodeVector(record['elements'], (item: TdJsonRaw): TdPassportElement => decodeTdPassportElement(item) as TdPassportElement);
  return out;
}

/** Encode a `passportElements` into TDLib JSON wire shape. */
export function encodePassportElements(value: PassportElements): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElements';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['elements'] = encodeVector(value.elements, (item: TdPassportElement | null): TdJsonRaw => item === null ? null : encodeTdPassportElement(item));
  return out;
}

/** passportElementsWithErrors = PassportElementsWithErrors — Contains information about a Telegram Passport elements and corresponding errors */
export class PassportElementsWithErrors {
  type: 'passportElementsWithErrors' = 'passportElementsWithErrors';
  extra?: string;
  /** elements: vector<PassportElement> — Telegram Passport elements */
  elements: TdPassportElement[] = [];
  /** errors: vector<passportElementError> — Errors in the elements that are already available */
  errors: PassportElementError[] = [];
}

/** Decode a TDLib `passportElementsWithErrors` object; null input -> null. */
export function decodePassportElementsWithErrors(json: TdJsonRaw): PassportElementsWithErrors | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportElementsWithErrors = new PassportElementsWithErrors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.elements = decodeVector(record['elements'], (item: TdJsonRaw): TdPassportElement => decodeTdPassportElement(item) as TdPassportElement);
  out.errors = decodeVector(record['errors'], (item: TdJsonRaw): PassportElementError => decodePassportElementError(item) as PassportElementError);
  return out;
}

/** Encode a `passportElementsWithErrors` into TDLib JSON wire shape. */
export function encodePassportElementsWithErrors(value: PassportElementsWithErrors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportElementsWithErrors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['elements'] = encodeVector(value.elements, (item: TdPassportElement | null): TdJsonRaw => item === null ? null : encodeTdPassportElement(item));
  out['errors'] = encodeVector(value.errors, (item: PassportElementError | null): TdJsonRaw => item === null ? null : encodePassportElementError(item));
  return out;
}

/** passportRequiredElement = PassportRequiredElement — Contains a description of the required Telegram Passport element that was requested by a service */
export class PassportRequiredElement {
  type: 'passportRequiredElement' = 'passportRequiredElement';
  extra?: string;
  /** suitable_elements: vector<passportSuitableElement> — List of Telegram Passport elements any of which is enough to provide */
  suitable_elements: PassportSuitableElement[] = [];
}

/** Decode a TDLib `passportRequiredElement` object; null input -> null. */
export function decodePassportRequiredElement(json: TdJsonRaw): PassportRequiredElement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportRequiredElement = new PassportRequiredElement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.suitable_elements = decodeVector(record['suitable_elements'], (item: TdJsonRaw): PassportSuitableElement => decodePassportSuitableElement(item) as PassportSuitableElement);
  return out;
}

/** Encode a `passportRequiredElement` into TDLib JSON wire shape. */
export function encodePassportRequiredElement(value: PassportRequiredElement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportRequiredElement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['suitable_elements'] = encodeVector(value.suitable_elements, (item: PassportSuitableElement | null): TdJsonRaw => item === null ? null : encodePassportSuitableElement(item));
  return out;
}

/** passportSuitableElement = PassportSuitableElement — Contains information about a Telegram Passport element that was requested by a service */
export class PassportSuitableElement {
  type: 'passportSuitableElement' = 'passportSuitableElement';
  extra?: string;
  /** type: PassportElementType — Type of the element */
  type_: TdPassportElementType | null = null;
  /** is_selfie_required: Bool — True, if a selfie is required with the identity document */
  is_selfie_required: boolean = false;
  /** is_translation_required: Bool — True, if a certified English translation is required with the document */
  is_translation_required: boolean = false;
  /** is_native_name_required: Bool — True, if personal details must include the user's name in the language of their country of residence */
  is_native_name_required: boolean = false;
}

/** Decode a TDLib `passportSuitableElement` object; null input -> null. */
export function decodePassportSuitableElement(json: TdJsonRaw): PassportSuitableElement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PassportSuitableElement = new PassportSuitableElement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPassportElementType(record['type']);
  out.is_selfie_required = asBool(record['is_selfie_required']);
  out.is_translation_required = asBool(record['is_translation_required']);
  out.is_native_name_required = asBool(record['is_native_name_required']);
  return out;
}

/** Encode a `passportSuitableElement` into TDLib JSON wire shape. */
export function encodePassportSuitableElement(value: PassportSuitableElement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passportSuitableElement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPassportElementType(value.type_);
  out['is_selfie_required'] = value.is_selfie_required;
  out['is_translation_required'] = value.is_translation_required;
  out['is_native_name_required'] = value.is_native_name_required;
  return out;
}

/** passwordState = PasswordState — Represents the current state of 2-step verification */
export class PasswordState {
  type: 'passwordState' = 'passwordState';
  extra?: string;
  /** has_password: Bool — True, if a 2-step verification password is set */
  has_password: boolean = false;
  /** password_hint: string — Hint for the password; may be empty */
  password_hint: string = '';
  /** has_recovery_email_address: Bool — True, if a recovery email is set */
  has_recovery_email_address: boolean = false;
  /** has_passport_data: Bool — True, if some Telegram Passport elements were saved */
  has_passport_data: boolean = false;
  /** recovery_email_address_code_info: emailAddressAuthenticationCodeInfo — Information about the recovery email address to which the confirmation email was sent; may be null */
  recovery_email_address_code_info: EmailAddressAuthenticationCodeInfo | null = null;
  /** login_email_address_pattern: string — Pattern of the email address set up for logging in */
  login_email_address_pattern: string = '';
  /** pending_reset_date: int32 — If not 0, point in time (Unix timestamp) after which the 2-step verification password can be reset immediately using resetPassword */
  pending_reset_date: number = 0;
}

/** Decode a TDLib `passwordState` object; null input -> null. */
export function decodePasswordState(json: TdJsonRaw): PasswordState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PasswordState = new PasswordState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.has_password = asBool(record['has_password']);
  out.password_hint = asString(record['password_hint']);
  out.has_recovery_email_address = asBool(record['has_recovery_email_address']);
  out.has_passport_data = asBool(record['has_passport_data']);
  out.recovery_email_address_code_info = decodeEmailAddressAuthenticationCodeInfo(record['recovery_email_address_code_info']);
  out.login_email_address_pattern = asString(record['login_email_address_pattern']);
  out.pending_reset_date = asNumber(record['pending_reset_date']);
  return out;
}

/** Encode a `passwordState` into TDLib JSON wire shape. */
export function encodePasswordState(value: PasswordState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'passwordState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['has_password'] = value.has_password;
  out['password_hint'] = value.password_hint;
  out['has_recovery_email_address'] = value.has_recovery_email_address;
  out['has_passport_data'] = value.has_passport_data;
  out['recovery_email_address_code_info'] = value.recovery_email_address_code_info === null ? null : encodeEmailAddressAuthenticationCodeInfo(value.recovery_email_address_code_info);
  out['login_email_address_pattern'] = value.login_email_address_pattern;
  out['pending_reset_date'] = value.pending_reset_date;
  return out;
}

/** paymentForm = PaymentForm — Contains information about an invoice payment form */
export class PaymentForm {
  type: 'paymentForm' = 'paymentForm';
  extra?: string;
  /** id: int64 — The payment form identifier */
  id: string = '';
  /** type: PaymentFormType — Type of the payment form */
  type_: TdPaymentFormType | null = null;
  /** seller_bot_user_id: int53 — User identifier of the seller bot */
  seller_bot_user_id: number = 0;
  /** product_info: productInfo — Information about the product */
  product_info: ProductInfo | null = null;
}

/** Decode a TDLib `paymentForm` object; null input -> null. */
export function decodePaymentForm(json: TdJsonRaw): PaymentForm | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentForm = new PaymentForm();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.type_ = decodeTdPaymentFormType(record['type']);
  out.seller_bot_user_id = asNumber(record['seller_bot_user_id']);
  out.product_info = decodeProductInfo(record['product_info']);
  return out;
}

/** Encode a `paymentForm` into TDLib JSON wire shape. */
export function encodePaymentForm(value: PaymentForm): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentForm';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['type'] = value.type_ === null ? null : encodeTdPaymentFormType(value.type_);
  out['seller_bot_user_id'] = value.seller_bot_user_id;
  out['product_info'] = value.product_info === null ? null : encodeProductInfo(value.product_info);
  return out;
}

/** paymentFormTypeRegular = PaymentFormType — The payment form is for a regular payment */
export class PaymentFormTypeRegular {
  type: 'paymentFormTypeRegular' = 'paymentFormTypeRegular';
  extra?: string;
  /** invoice: invoice — Full information about the invoice */
  invoice: Invoice | null = null;
  /** payment_provider_user_id: int53 — User identifier of the payment provider bot */
  payment_provider_user_id: number = 0;
  /** payment_provider: PaymentProvider — Information about the payment provider */
  payment_provider: TdPaymentProvider | null = null;
  /** additional_payment_options: vector<paymentOption> — The list of additional payment options */
  additional_payment_options: PaymentOption[] = [];
  /** saved_order_info: orderInfo — Saved server-side order information; may be null */
  saved_order_info: OrderInfo | null = null;
  /** saved_credentials: vector<savedCredentials> — The list of saved payment credentials */
  saved_credentials: SavedCredentials[] = [];
  /** can_save_credentials: Bool — True, if the user can choose to save credentials */
  can_save_credentials: boolean = false;
  /** need_password: Bool — True, if the user will be able to save credentials, if sets up a 2-step verification password */
  need_password: boolean = false;
}

/** Decode a TDLib `paymentFormTypeRegular` object; null input -> null. */
export function decodePaymentFormTypeRegular(json: TdJsonRaw): PaymentFormTypeRegular | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentFormTypeRegular = new PaymentFormTypeRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.invoice = decodeInvoice(record['invoice']);
  out.payment_provider_user_id = asNumber(record['payment_provider_user_id']);
  out.payment_provider = decodeTdPaymentProvider(record['payment_provider']);
  out.additional_payment_options = decodeVector(record['additional_payment_options'], (item: TdJsonRaw): PaymentOption => decodePaymentOption(item) as PaymentOption);
  out.saved_order_info = decodeOrderInfo(record['saved_order_info']);
  out.saved_credentials = decodeVector(record['saved_credentials'], (item: TdJsonRaw): SavedCredentials => decodeSavedCredentials(item) as SavedCredentials);
  out.can_save_credentials = asBool(record['can_save_credentials']);
  out.need_password = asBool(record['need_password']);
  return out;
}

/** Encode a `paymentFormTypeRegular` into TDLib JSON wire shape. */
export function encodePaymentFormTypeRegular(value: PaymentFormTypeRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentFormTypeRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['invoice'] = value.invoice === null ? null : encodeInvoice(value.invoice);
  out['payment_provider_user_id'] = value.payment_provider_user_id;
  out['payment_provider'] = value.payment_provider === null ? null : encodeTdPaymentProvider(value.payment_provider);
  out['additional_payment_options'] = encodeVector(value.additional_payment_options, (item: PaymentOption | null): TdJsonRaw => item === null ? null : encodePaymentOption(item));
  out['saved_order_info'] = value.saved_order_info === null ? null : encodeOrderInfo(value.saved_order_info);
  out['saved_credentials'] = encodeVector(value.saved_credentials, (item: SavedCredentials | null): TdJsonRaw => item === null ? null : encodeSavedCredentials(item));
  out['can_save_credentials'] = value.can_save_credentials;
  out['need_password'] = value.need_password;
  return out;
}

/** paymentFormTypeStarSubscription = PaymentFormType — The payment form is for a payment in Telegram Stars for subscription */
export class PaymentFormTypeStarSubscription {
  type: 'paymentFormTypeStarSubscription' = 'paymentFormTypeStarSubscription';
  extra?: string;
  /** pricing: starSubscriptionPricing — Information about subscription plan */
  pricing: StarSubscriptionPricing | null = null;
}

/** Decode a TDLib `paymentFormTypeStarSubscription` object; null input -> null. */
export function decodePaymentFormTypeStarSubscription(json: TdJsonRaw): PaymentFormTypeStarSubscription | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentFormTypeStarSubscription = new PaymentFormTypeStarSubscription();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.pricing = decodeStarSubscriptionPricing(record['pricing']);
  return out;
}

/** Encode a `paymentFormTypeStarSubscription` into TDLib JSON wire shape. */
export function encodePaymentFormTypeStarSubscription(value: PaymentFormTypeStarSubscription): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentFormTypeStarSubscription';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['pricing'] = value.pricing === null ? null : encodeStarSubscriptionPricing(value.pricing);
  return out;
}

/** paymentFormTypeStars = PaymentFormType — The payment form is for a payment in Telegram Stars */
export class PaymentFormTypeStars {
  type: 'paymentFormTypeStars' = 'paymentFormTypeStars';
  extra?: string;
  /** star_count: int53 — Number of Telegram Stars that will be paid */
  star_count: number = 0;
}

/** Decode a TDLib `paymentFormTypeStars` object; null input -> null. */
export function decodePaymentFormTypeStars(json: TdJsonRaw): PaymentFormTypeStars | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentFormTypeStars = new PaymentFormTypeStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  return out;
}

/** Encode a `paymentFormTypeStars` into TDLib JSON wire shape. */
export function encodePaymentFormTypeStars(value: PaymentFormTypeStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentFormTypeStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  return out;
}

/** paymentOption = PaymentOption — Describes an additional payment option */
export class PaymentOption {
  type: 'paymentOption' = 'paymentOption';
  extra?: string;
  /** title: string — Title for the payment option */
  title: string = '';
  /** url: string — Payment form URL to be opened in a web view */
  url: string = '';
}

/** Decode a TDLib `paymentOption` object; null input -> null. */
export function decodePaymentOption(json: TdJsonRaw): PaymentOption | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentOption = new PaymentOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.url = asString(record['url']);
  return out;
}

/** Encode a `paymentOption` into TDLib JSON wire shape. */
export function encodePaymentOption(value: PaymentOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['url'] = value.url;
  return out;
}

/** paymentProviderOther = PaymentProvider — Some other payment provider, for which a web payment form must be shown */
export class PaymentProviderOther {
  type: 'paymentProviderOther' = 'paymentProviderOther';
  extra?: string;
  /** url: string — Payment form URL */
  url: string = '';
}

/** Decode a TDLib `paymentProviderOther` object; null input -> null. */
export function decodePaymentProviderOther(json: TdJsonRaw): PaymentProviderOther | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentProviderOther = new PaymentProviderOther();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  return out;
}

/** Encode a `paymentProviderOther` into TDLib JSON wire shape. */
export function encodePaymentProviderOther(value: PaymentProviderOther): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentProviderOther';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  return out;
}

/** paymentProviderSmartGlocal = PaymentProvider — Smart Glocal payment provider */
export class PaymentProviderSmartGlocal {
  type: 'paymentProviderSmartGlocal' = 'paymentProviderSmartGlocal';
  extra?: string;
  /** public_token: string — Public payment token */
  public_token: string = '';
  /** tokenize_url: string — URL for sending card tokenization requests */
  tokenize_url: string = '';
}

/** Decode a TDLib `paymentProviderSmartGlocal` object; null input -> null. */
export function decodePaymentProviderSmartGlocal(json: TdJsonRaw): PaymentProviderSmartGlocal | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentProviderSmartGlocal = new PaymentProviderSmartGlocal();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.public_token = asString(record['public_token']);
  out.tokenize_url = asString(record['tokenize_url']);
  return out;
}

/** Encode a `paymentProviderSmartGlocal` into TDLib JSON wire shape. */
export function encodePaymentProviderSmartGlocal(value: PaymentProviderSmartGlocal): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentProviderSmartGlocal';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['public_token'] = value.public_token;
  out['tokenize_url'] = value.tokenize_url;
  return out;
}

/** paymentProviderStripe = PaymentProvider — Stripe payment provider */
export class PaymentProviderStripe {
  type: 'paymentProviderStripe' = 'paymentProviderStripe';
  extra?: string;
  /** publishable_key: string — Stripe API publishable key */
  publishable_key: string = '';
  /** need_country: Bool — True, if the user country must be provided */
  need_country: boolean = false;
  /** need_postal_code: Bool — True, if the user ZIP/postal code must be provided */
  need_postal_code: boolean = false;
  /** need_cardholder_name: Bool — True, if the cardholder name must be provided */
  need_cardholder_name: boolean = false;
}

/** Decode a TDLib `paymentProviderStripe` object; null input -> null. */
export function decodePaymentProviderStripe(json: TdJsonRaw): PaymentProviderStripe | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentProviderStripe = new PaymentProviderStripe();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.publishable_key = asString(record['publishable_key']);
  out.need_country = asBool(record['need_country']);
  out.need_postal_code = asBool(record['need_postal_code']);
  out.need_cardholder_name = asBool(record['need_cardholder_name']);
  return out;
}

/** Encode a `paymentProviderStripe` into TDLib JSON wire shape. */
export function encodePaymentProviderStripe(value: PaymentProviderStripe): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentProviderStripe';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['publishable_key'] = value.publishable_key;
  out['need_country'] = value.need_country;
  out['need_postal_code'] = value.need_postal_code;
  out['need_cardholder_name'] = value.need_cardholder_name;
  return out;
}

/** paymentReceipt = PaymentReceipt — Contains information about a successful payment */
export class PaymentReceipt {
  type: 'paymentReceipt' = 'paymentReceipt';
  extra?: string;
  /** product_info: productInfo — Information about the product */
  product_info: ProductInfo | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the payment was made */
  date: number = 0;
  /** seller_bot_user_id: int53 — User identifier of the seller bot */
  seller_bot_user_id: number = 0;
  /** type: PaymentReceiptType — Type of the payment receipt */
  type_: TdPaymentReceiptType | null = null;
}

/** Decode a TDLib `paymentReceipt` object; null input -> null. */
export function decodePaymentReceipt(json: TdJsonRaw): PaymentReceipt | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentReceipt = new PaymentReceipt();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.product_info = decodeProductInfo(record['product_info']);
  out.date = asNumber(record['date']);
  out.seller_bot_user_id = asNumber(record['seller_bot_user_id']);
  out.type_ = decodeTdPaymentReceiptType(record['type']);
  return out;
}

/** Encode a `paymentReceipt` into TDLib JSON wire shape. */
export function encodePaymentReceipt(value: PaymentReceipt): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentReceipt';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['product_info'] = value.product_info === null ? null : encodeProductInfo(value.product_info);
  out['date'] = value.date;
  out['seller_bot_user_id'] = value.seller_bot_user_id;
  out['type'] = value.type_ === null ? null : encodeTdPaymentReceiptType(value.type_);
  return out;
}

/** paymentReceiptTypeRegular = PaymentReceiptType — The payment was done using a third-party payment provider */
export class PaymentReceiptTypeRegular {
  type: 'paymentReceiptTypeRegular' = 'paymentReceiptTypeRegular';
  extra?: string;
  /** payment_provider_user_id: int53 — User identifier of the payment provider bot */
  payment_provider_user_id: number = 0;
  /** invoice: invoice — Information about the invoice */
  invoice: Invoice | null = null;
  /** order_info: orderInfo — Order information; may be null */
  order_info: OrderInfo | null = null;
  /** shipping_option: shippingOption — Chosen shipping option; may be null */
  shipping_option: ShippingOption | null = null;
  /** credentials_title: string — Title of the saved credentials chosen by the buyer */
  credentials_title: string = '';
  /** tip_amount: int53 — The amount of tip chosen by the buyer in the smallest units of the currency */
  tip_amount: number = 0;
}

/** Decode a TDLib `paymentReceiptTypeRegular` object; null input -> null. */
export function decodePaymentReceiptTypeRegular(json: TdJsonRaw): PaymentReceiptTypeRegular | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentReceiptTypeRegular = new PaymentReceiptTypeRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.payment_provider_user_id = asNumber(record['payment_provider_user_id']);
  out.invoice = decodeInvoice(record['invoice']);
  out.order_info = decodeOrderInfo(record['order_info']);
  out.shipping_option = decodeShippingOption(record['shipping_option']);
  out.credentials_title = asString(record['credentials_title']);
  out.tip_amount = asNumber(record['tip_amount']);
  return out;
}

/** Encode a `paymentReceiptTypeRegular` into TDLib JSON wire shape. */
export function encodePaymentReceiptTypeRegular(value: PaymentReceiptTypeRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentReceiptTypeRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['payment_provider_user_id'] = value.payment_provider_user_id;
  out['invoice'] = value.invoice === null ? null : encodeInvoice(value.invoice);
  out['order_info'] = value.order_info === null ? null : encodeOrderInfo(value.order_info);
  out['shipping_option'] = value.shipping_option === null ? null : encodeShippingOption(value.shipping_option);
  out['credentials_title'] = value.credentials_title;
  out['tip_amount'] = value.tip_amount;
  return out;
}

/** paymentReceiptTypeStars = PaymentReceiptType — The payment was done using Telegram Stars */
export class PaymentReceiptTypeStars {
  type: 'paymentReceiptTypeStars' = 'paymentReceiptTypeStars';
  extra?: string;
  /** star_count: int53 — Number of Telegram Stars that were paid */
  star_count: number = 0;
  /** transaction_id: string — Unique identifier of the transaction that can be used to dispute it */
  transaction_id: string = '';
}

/** Decode a TDLib `paymentReceiptTypeStars` object; null input -> null. */
export function decodePaymentReceiptTypeStars(json: TdJsonRaw): PaymentReceiptTypeStars | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentReceiptTypeStars = new PaymentReceiptTypeStars();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.transaction_id = asString(record['transaction_id']);
  return out;
}

/** Encode a `paymentReceiptTypeStars` into TDLib JSON wire shape. */
export function encodePaymentReceiptTypeStars(value: PaymentReceiptTypeStars): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentReceiptTypeStars';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['transaction_id'] = value.transaction_id;
  return out;
}

/** paymentResult = PaymentResult — Contains the result of a payment request */
export class PaymentResult {
  type: 'paymentResult' = 'paymentResult';
  extra?: string;
  /** success: Bool — True, if the payment request was successful; otherwise, the verification_url will be non-empty */
  success: boolean = false;
  /** verification_url: string — URL for additional payment credentials verification */
  verification_url: string = '';
}

/** Decode a TDLib `paymentResult` object; null input -> null. */
export function decodePaymentResult(json: TdJsonRaw): PaymentResult | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PaymentResult = new PaymentResult();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.success = asBool(record['success']);
  out.verification_url = asString(record['verification_url']);
  return out;
}

/** Encode a `paymentResult` into TDLib JSON wire shape. */
export function encodePaymentResult(value: PaymentResult): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'paymentResult';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['success'] = value.success;
  out['verification_url'] = value.verification_url;
  return out;
}

/** personalDetails = PersonalDetails — Contains the user's personal details */
export class PersonalDetails {
  type: 'personalDetails' = 'personalDetails';
  extra?: string;
  /** first_name: string — First name of the user written in English; 1-255 characters */
  first_name: string = '';
  /** middle_name: string — Middle name of the user written in English; 0-255 characters */
  middle_name: string = '';
  /** last_name: string — Last name of the user written in English; 1-255 characters */
  last_name: string = '';
  /** native_first_name: string — Native first name of the user; 1-255 characters */
  native_first_name: string = '';
  /** native_middle_name: string — Native middle name of the user; 0-255 characters */
  native_middle_name: string = '';
  /** native_last_name: string — Native last name of the user; 1-255 characters */
  native_last_name: string = '';
  /** birthdate: date — Birthdate of the user */
  birthdate: DateValue | null = null;
  /** gender: string — Gender of the user, "male" or "female" */
  gender: string = '';
  /** country_code: string — A two-letter ISO 3166-1 alpha-2 country code of the user's country */
  country_code: string = '';
  /** residence_country_code: string — A two-letter ISO 3166-1 alpha-2 country code of the user's residence country */
  residence_country_code: string = '';
}

/** Decode a TDLib `personalDetails` object; null input -> null. */
export function decodePersonalDetails(json: TdJsonRaw): PersonalDetails | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PersonalDetails = new PersonalDetails();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.first_name = asString(record['first_name']);
  out.middle_name = asString(record['middle_name']);
  out.last_name = asString(record['last_name']);
  out.native_first_name = asString(record['native_first_name']);
  out.native_middle_name = asString(record['native_middle_name']);
  out.native_last_name = asString(record['native_last_name']);
  out.birthdate = decodeDateValue(record['birthdate']);
  out.gender = asString(record['gender']);
  out.country_code = asString(record['country_code']);
  out.residence_country_code = asString(record['residence_country_code']);
  return out;
}

/** Encode a `personalDetails` into TDLib JSON wire shape. */
export function encodePersonalDetails(value: PersonalDetails): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'personalDetails';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['first_name'] = value.first_name;
  out['middle_name'] = value.middle_name;
  out['last_name'] = value.last_name;
  out['native_first_name'] = value.native_first_name;
  out['native_middle_name'] = value.native_middle_name;
  out['native_last_name'] = value.native_last_name;
  out['birthdate'] = value.birthdate === null ? null : encodeDateValue(value.birthdate);
  out['gender'] = value.gender;
  out['country_code'] = value.country_code;
  out['residence_country_code'] = value.residence_country_code;
  return out;
}

/** personalDocument = PersonalDocument — A personal document, containing some information about a user */
export class PersonalDocument {
  type: 'personalDocument' = 'personalDocument';
  extra?: string;
  /** files: vector<datedFile> — List of files containing the pages of the document */
  files: DatedFile[] = [];
  /** translation: vector<datedFile> — List of files containing a certified English translation of the document */
  translation: DatedFile[] = [];
}

/** Decode a TDLib `personalDocument` object; null input -> null. */
export function decodePersonalDocument(json: TdJsonRaw): PersonalDocument | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PersonalDocument = new PersonalDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.files = decodeVector(record['files'], (item: TdJsonRaw): DatedFile => decodeDatedFile(item) as DatedFile);
  out.translation = decodeVector(record['translation'], (item: TdJsonRaw): DatedFile => decodeDatedFile(item) as DatedFile);
  return out;
}

/** Encode a `personalDocument` into TDLib JSON wire shape. */
export function encodePersonalDocument(value: PersonalDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'personalDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['files'] = encodeVector(value.files, (item: DatedFile | null): TdJsonRaw => item === null ? null : encodeDatedFile(item));
  out['translation'] = encodeVector(value.translation, (item: DatedFile | null): TdJsonRaw => item === null ? null : encodeDatedFile(item));
  return out;
}

/** phoneNumberAuthenticationSettings = PhoneNumberAuthenticationSettings — Contains settings for the authentication of the user's phone number */
export class PhoneNumberAuthenticationSettings {
  type: 'phoneNumberAuthenticationSettings' = 'phoneNumberAuthenticationSettings';
  extra?: string;
  /** allow_flash_call: Bool — Pass true if the authentication code may be sent via a flash call to the specified phone number */
  allow_flash_call: boolean = false;
  /** allow_missed_call: Bool — Pass true if the authentication code may be sent via a missed call to the specified phone number */
  allow_missed_call: boolean = false;
  /** is_current_phone_number: Bool — Pass true if the authenticated phone number is used on the current device */
  is_current_phone_number: boolean = false;
  /** has_unknown_phone_number: Bool — Pass true if there is a SIM card in the current device, but it is not possible to check whether phone number matches */
  has_unknown_phone_number: boolean = false;
  /** allow_sms_retriever_api: Bool — For official applications only. True, if the application can use Android SMS Retriever API (requires Google Play Services >= 10.2) to automatically receive the authentication code from the SMS. See https:/... */
  allow_sms_retriever_api: boolean = false;
  /** firebase_authentication_settings: FirebaseAuthenticationSettings — For official Android and iOS applications only; pass null otherwise. Settings for Firebase Authentication */
  firebase_authentication_settings: TdFirebaseAuthenticationSettings | null = null;
  /** authentication_tokens: vector<string> — List of up to 20 authentication tokens, recently received in updateOption("authentication_token") in previously logged out sessions; for setAuthenticationPhoneNumber only */
  authentication_tokens: string[] = [];
}

/** Decode a TDLib `phoneNumberAuthenticationSettings` object; null input -> null. */
export function decodePhoneNumberAuthenticationSettings(json: TdJsonRaw): PhoneNumberAuthenticationSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PhoneNumberAuthenticationSettings = new PhoneNumberAuthenticationSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.allow_flash_call = asBool(record['allow_flash_call']);
  out.allow_missed_call = asBool(record['allow_missed_call']);
  out.is_current_phone_number = asBool(record['is_current_phone_number']);
  out.has_unknown_phone_number = asBool(record['has_unknown_phone_number']);
  out.allow_sms_retriever_api = asBool(record['allow_sms_retriever_api']);
  out.firebase_authentication_settings = decodeTdFirebaseAuthenticationSettings(record['firebase_authentication_settings']);
  out.authentication_tokens = decodeVector(record['authentication_tokens'], (item: TdJsonRaw): string => asString(item));
  return out;
}

/** Encode a `phoneNumberAuthenticationSettings` into TDLib JSON wire shape. */
export function encodePhoneNumberAuthenticationSettings(value: PhoneNumberAuthenticationSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'phoneNumberAuthenticationSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['allow_flash_call'] = value.allow_flash_call;
  out['allow_missed_call'] = value.allow_missed_call;
  out['is_current_phone_number'] = value.is_current_phone_number;
  out['has_unknown_phone_number'] = value.has_unknown_phone_number;
  out['allow_sms_retriever_api'] = value.allow_sms_retriever_api;
  out['firebase_authentication_settings'] = value.firebase_authentication_settings === null ? null : encodeTdFirebaseAuthenticationSettings(value.firebase_authentication_settings);
  out['authentication_tokens'] = encodeVector(value.authentication_tokens, (item: string): TdJsonRaw => item);
  return out;
}

/** phoneNumberCodeTypeChange = PhoneNumberCodeType — Checks ownership of a new phone number to change the user's authentication phone number; for official Android and iOS applications only */
export class PhoneNumberCodeTypeChange {
  type: 'phoneNumberCodeTypeChange' = 'phoneNumberCodeTypeChange';
  extra?: string;
}

/** Decode a TDLib `phoneNumberCodeTypeChange` object; null input -> null. */
export function decodePhoneNumberCodeTypeChange(json: TdJsonRaw): PhoneNumberCodeTypeChange | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PhoneNumberCodeTypeChange = new PhoneNumberCodeTypeChange();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `phoneNumberCodeTypeChange` into TDLib JSON wire shape. */
export function encodePhoneNumberCodeTypeChange(value: PhoneNumberCodeTypeChange): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'phoneNumberCodeTypeChange';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** phoneNumberCodeTypeConfirmOwnership = PhoneNumberCodeType — Confirms ownership of a phone number to prevent account deletion while handling links of the type internalLinkTypePhoneNumberConfirmation */
export class PhoneNumberCodeTypeConfirmOwnership {
  type: 'phoneNumberCodeTypeConfirmOwnership' = 'phoneNumberCodeTypeConfirmOwnership';
  extra?: string;
  /** hash: string — Hash value from the link */
  hash: string = '';
}

/** Decode a TDLib `phoneNumberCodeTypeConfirmOwnership` object; null input -> null. */
export function decodePhoneNumberCodeTypeConfirmOwnership(json: TdJsonRaw): PhoneNumberCodeTypeConfirmOwnership | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PhoneNumberCodeTypeConfirmOwnership = new PhoneNumberCodeTypeConfirmOwnership();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.hash = asString(record['hash']);
  return out;
}

/** Encode a `phoneNumberCodeTypeConfirmOwnership` into TDLib JSON wire shape. */
export function encodePhoneNumberCodeTypeConfirmOwnership(value: PhoneNumberCodeTypeConfirmOwnership): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'phoneNumberCodeTypeConfirmOwnership';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['hash'] = value.hash;
  return out;
}

/** phoneNumberCodeTypeVerify = PhoneNumberCodeType — Verifies ownership of a phone number to be added to the user's Telegram Passport */
export class PhoneNumberCodeTypeVerify {
  type: 'phoneNumberCodeTypeVerify' = 'phoneNumberCodeTypeVerify';
  extra?: string;
}

/** Decode a TDLib `phoneNumberCodeTypeVerify` object; null input -> null. */
export function decodePhoneNumberCodeTypeVerify(json: TdJsonRaw): PhoneNumberCodeTypeVerify | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PhoneNumberCodeTypeVerify = new PhoneNumberCodeTypeVerify();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `phoneNumberCodeTypeVerify` into TDLib JSON wire shape. */
export function encodePhoneNumberCodeTypeVerify(value: PhoneNumberCodeTypeVerify): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'phoneNumberCodeTypeVerify';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** phoneNumberInfo = PhoneNumberInfo — Contains information about a phone number */
export class PhoneNumberInfo {
  type: 'phoneNumberInfo' = 'phoneNumberInfo';
  extra?: string;
  /** country: countryInfo — Information about the country to which the phone number belongs; may be null */
  country: CountryInfo | null = null;
  /** country_calling_code: string — The part of the phone number denoting country calling code or its part */
  country_calling_code: string = '';
  /** formatted_phone_number: string — The phone number without country calling code formatted accordingly to local rules. Expected digits are returned as '-', but even more digits might be entered by the user */
  formatted_phone_number: string = '';
  /** is_anonymous: Bool — True, if the phone number was bought at https://fragment.com and isn't tied to a SIM card. Information about the phone number can be received using getCollectibleItemInfo */
  is_anonymous: boolean = false;
}

/** Decode a TDLib `phoneNumberInfo` object; null input -> null. */
export function decodePhoneNumberInfo(json: TdJsonRaw): PhoneNumberInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PhoneNumberInfo = new PhoneNumberInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.country = decodeCountryInfo(record['country']);
  out.country_calling_code = asString(record['country_calling_code']);
  out.formatted_phone_number = asString(record['formatted_phone_number']);
  out.is_anonymous = asBool(record['is_anonymous']);
  return out;
}

/** Encode a `phoneNumberInfo` into TDLib JSON wire shape. */
export function encodePhoneNumberInfo(value: PhoneNumberInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'phoneNumberInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['country'] = value.country === null ? null : encodeCountryInfo(value.country);
  out['country_calling_code'] = value.country_calling_code;
  out['formatted_phone_number'] = value.formatted_phone_number;
  out['is_anonymous'] = value.is_anonymous;
  return out;
}

/** photo = Photo — Describes a photo */
export class Photo {
  type: 'photo' = 'photo';
  extra?: string;
  /** has_stickers: Bool — True, if stickers were added to the photo. The list of corresponding sticker sets can be received using getAttachedStickerSets */
  has_stickers: boolean = false;
  /** minithumbnail: minithumbnail — Photo minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
  /** sizes: vector<photoSize> — Available variants of the photo, in different sizes */
  sizes: PhotoSize[] = [];
}

/** Decode a TDLib `photo` object; null input -> null. */
export function decodePhoto(json: TdJsonRaw): Photo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Photo = new Photo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.has_stickers = asBool(record['has_stickers']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  out.sizes = decodeVector(record['sizes'], (item: TdJsonRaw): PhotoSize => decodePhotoSize(item) as PhotoSize);
  return out;
}

/** Encode a `photo` into TDLib JSON wire shape. */
export function encodePhoto(value: Photo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'photo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['has_stickers'] = value.has_stickers;
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  out['sizes'] = encodeVector(value.sizes, (item: PhotoSize | null): TdJsonRaw => item === null ? null : encodePhotoSize(item));
  return out;
}

/** photoSize = PhotoSize — Describes an image in JPEG format */
export class PhotoSize {
  type: 'photoSize' = 'photoSize';
  extra?: string;
  /** type: string — Image type (see https://core.telegram.org/constructor/photoSize) */
  type_: string = '';
  /** photo: file — Information about the image file */
  photo: File | null = null;
  /** width: int32 — Image width */
  width: number = 0;
  /** height: int32 — Image height */
  height: number = 0;
  /** progressive_sizes: vector<int32> — Sizes of progressive JPEG file prefixes, which can be used to preliminarily show the image; in bytes */
  progressive_sizes: number[] = [];
}

/** Decode a TDLib `photoSize` object; null input -> null. */
export function decodePhotoSize(json: TdJsonRaw): PhotoSize | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PhotoSize = new PhotoSize();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = asString(record['type']);
  out.photo = decodeFile(record['photo']);
  out.width = asNumber(record['width']);
  out.height = asNumber(record['height']);
  out.progressive_sizes = decodeVector(record['progressive_sizes'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `photoSize` into TDLib JSON wire shape. */
export function encodePhotoSize(value: PhotoSize): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'photoSize';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_;
  out['photo'] = value.photo === null ? null : encodeFile(value.photo);
  out['width'] = value.width;
  out['height'] = value.height;
  out['progressive_sizes'] = encodeVector(value.progressive_sizes, (item: number): TdJsonRaw => item);
  return out;
}

/** pinChatMessage = Ok — Pins a message in a chat. A message can be pinned only if messageProperties.can_be_pinned */
export class PinChatMessage {
  type: 'pinChatMessage' = 'pinChatMessage';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat */
  chat_id: number = 0;
  /** message_id: int53 — Identifier of the new pinned message */
  message_id: number = 0;
  /** disable_notification: Bool — Pass true to disable notification about the pinned message. Notifications are always disabled in channels and private chats */
  disable_notification: boolean = false;
  /** only_for_self: Bool — Pass true to pin the message only for self; private chats only */
  only_for_self: boolean = false;
}

/** Decode a TDLib `pinChatMessage` object; null input -> null. */
export function decodePinChatMessage(json: TdJsonRaw): PinChatMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PinChatMessage = new PinChatMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.message_id = asNumber(record['message_id']);
  out.disable_notification = asBool(record['disable_notification']);
  out.only_for_self = asBool(record['only_for_self']);
  return out;
}

/** Encode a `pinChatMessage` into TDLib JSON wire shape. */
export function encodePinChatMessage(value: PinChatMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pinChatMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['message_id'] = value.message_id;
  out['disable_notification'] = value.disable_notification;
  out['only_for_self'] = value.only_for_self;
  return out;
}

/** pingProxy = Seconds — Computes time needed to receive a response from a Telegram server through a proxy. Can be called before authorization */
export class PingProxy {
  type: 'pingProxy' = 'pingProxy';
  extra?: string;
  /** proxy: proxy — The proxy to test; pass null to ping a Telegram server without a proxy */
  proxy: ProxyValue | null = null;
}

/** Decode a TDLib `pingProxy` object; null input -> null. */
export function decodePingProxy(json: TdJsonRaw): PingProxy | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PingProxy = new PingProxy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.proxy = decodeProxyValue(record['proxy']);
  return out;
}

/** Encode a `pingProxy` into TDLib JSON wire shape. */
export function encodePingProxy(value: PingProxy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pingProxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['proxy'] = value.proxy === null ? null : encodeProxyValue(value.proxy);
  return out;
}

/** placeGiftAuctionBid = Ok — Places a bid on an auction gift Must be empty if the receiver enabled paid messages */
export class PlaceGiftAuctionBid {
  type: 'placeGiftAuctionBid' = 'placeGiftAuctionBid';
  extra?: string;
  /** gift_id: int64 — Identifier of the gift to place the bid on */
  gift_id: string = '';
  /** star_count: int53 — The number of Telegram Stars to place in the bid */
  star_count: number = 0;
  /** user_id: int53 — Identifier of the user who will receive the gift */
  user_id: number = 0;
  /** text: formattedText — Text to show along with the gift; 0-getOption("gift_text_length_max") characters. Only Bold, Italic, Underline, Strikethrough, Spoiler, CustomEmoji, and DateTime entities are allowed. */
  text: FormattedText | null = null;
  /** is_private: Bool — Pass true to show gift text and sender only to the gift receiver; otherwise, everyone will be able to see them */
  is_private: boolean = false;
}

/** Decode a TDLib `placeGiftAuctionBid` object; null input -> null. */
export function decodePlaceGiftAuctionBid(json: TdJsonRaw): PlaceGiftAuctionBid | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PlaceGiftAuctionBid = new PlaceGiftAuctionBid();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.gift_id = asInt64(record['gift_id']);
  out.star_count = asNumber(record['star_count']);
  out.user_id = asNumber(record['user_id']);
  out.text = decodeFormattedText(record['text']);
  out.is_private = asBool(record['is_private']);
  return out;
}

/** Encode a `placeGiftAuctionBid` into TDLib JSON wire shape. */
export function encodePlaceGiftAuctionBid(value: PlaceGiftAuctionBid): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'placeGiftAuctionBid';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['gift_id'] = value.gift_id;
  out['star_count'] = value.star_count;
  out['user_id'] = value.user_id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['is_private'] = value.is_private;
  return out;
}

/** point = Point — A point on a Cartesian plane */
export class Point {
  type: 'point' = 'point';
  extra?: string;
  /** x: double — The point's first coordinate */
  x: number = 0;
  /** y: double — The point's second coordinate */
  y: number = 0;
}

/** Decode a TDLib `point` object; null input -> null. */
export function decodePoint(json: TdJsonRaw): Point | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Point = new Point();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.x = asNumber(record['x']);
  out.y = asNumber(record['y']);
  return out;
}

/** Encode a `point` into TDLib JSON wire shape. */
export function encodePoint(value: Point): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'point';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['x'] = value.x;
  out['y'] = value.y;
  return out;
}

/** poll = Poll — Describes a poll */
export class Poll {
  type: 'poll' = 'poll';
  extra?: string;
  /** id: int64 — Unique poll identifier */
  id: string = '';
  /** question: formattedText — Poll question; 1-300 characters; may contain only custom emoji entities */
  question: FormattedText | null = null;
  /** options: vector<pollOption> — List of poll answer options */
  options: PollOption[] = [];
  /** total_voter_count: int32 — Total number of voters, participating in the poll */
  total_voter_count: number = 0;
  /** recent_voter_ids: vector<MessageSender> — Identifiers of recent voters, if the poll is non-anonymous and poll results are available */
  recent_voter_ids: TdMessageSender[] = [];
  /** can_get_voters: Bool — True, if the current user can get voters in the poll using getPollVoters */
  can_get_voters: boolean = false;
  /** can_see_results: Bool — True, if the current user can see results of the poll */
  can_see_results: boolean = false;
  /** is_anonymous: Bool — True, if the poll is anonymous */
  is_anonymous: boolean = false;
  /** allows_multiple_answers: Bool — True, if multiple answer options can be chosen simultaneously */
  allows_multiple_answers: boolean = false;
  /** allows_revoting: Bool — True, if the poll can be answered multiple times */
  allows_revoting: boolean = false;
  /** members_only: Bool — True, if only the users that are members of the chat for more than a day will be able to vote */
  members_only: boolean = false;
  /** country_codes: vector<string> — The list of two-letter ISO 3166-1 alpha-2 codes of countries, users from which will be able to vote. If empty, then all users can participate in the poll */
  country_codes: string[] = [];
  /** option_order: vector<int32> — The list of 0-based poll identifiers in which the options of the poll must be shown; empty if the order of options must not be changed */
  option_order: number[] = [];
  /** type: PollType — Type of the poll */
  type_: TdPollType | null = null;
  /** open_period: int32 — Amount of time the poll will be active after creation, in seconds */
  open_period: number = 0;
  /** close_date: int32 — Point in time (Unix timestamp) when the poll will automatically be closed */
  close_date: number = 0;
  /** is_closed: Bool — True, if the poll is closed */
  is_closed: boolean = false;
  /** vote_restriction_reason: PollVoteRestrictionReason — The reason describing, why the current user can't vote in the poll; may be null if the user can vote in the poll */
  vote_restriction_reason: TdPollVoteRestrictionReason | null = null;
}

/** Decode a TDLib `poll` object; null input -> null. */
export function decodePoll(json: TdJsonRaw): Poll | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: Poll = new Poll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.question = decodeFormattedText(record['question']);
  out.options = decodeVector(record['options'], (item: TdJsonRaw): PollOption => decodePollOption(item) as PollOption);
  out.total_voter_count = asNumber(record['total_voter_count']);
  out.recent_voter_ids = decodeVector(record['recent_voter_ids'], (item: TdJsonRaw): TdMessageSender => decodeTdMessageSender(item) as TdMessageSender);
  out.can_get_voters = asBool(record['can_get_voters']);
  out.can_see_results = asBool(record['can_see_results']);
  out.is_anonymous = asBool(record['is_anonymous']);
  out.allows_multiple_answers = asBool(record['allows_multiple_answers']);
  out.allows_revoting = asBool(record['allows_revoting']);
  out.members_only = asBool(record['members_only']);
  out.country_codes = decodeVector(record['country_codes'], (item: TdJsonRaw): string => asString(item));
  out.option_order = decodeVector(record['option_order'], (item: TdJsonRaw): number => asNumber(item));
  out.type_ = decodeTdPollType(record['type']);
  out.open_period = asNumber(record['open_period']);
  out.close_date = asNumber(record['close_date']);
  out.is_closed = asBool(record['is_closed']);
  out.vote_restriction_reason = decodeTdPollVoteRestrictionReason(record['vote_restriction_reason']);
  return out;
}

/** Encode a `poll` into TDLib JSON wire shape. */
export function encodePoll(value: Poll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'poll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['question'] = value.question === null ? null : encodeFormattedText(value.question);
  out['options'] = encodeVector(value.options, (item: PollOption | null): TdJsonRaw => item === null ? null : encodePollOption(item));
  out['total_voter_count'] = value.total_voter_count;
  out['recent_voter_ids'] = encodeVector(value.recent_voter_ids, (item: TdMessageSender | null): TdJsonRaw => item === null ? null : encodeTdMessageSender(item));
  out['can_get_voters'] = value.can_get_voters;
  out['can_see_results'] = value.can_see_results;
  out['is_anonymous'] = value.is_anonymous;
  out['allows_multiple_answers'] = value.allows_multiple_answers;
  out['allows_revoting'] = value.allows_revoting;
  out['members_only'] = value.members_only;
  out['country_codes'] = encodeVector(value.country_codes, (item: string): TdJsonRaw => item);
  out['option_order'] = encodeVector(value.option_order, (item: number): TdJsonRaw => item);
  out['type'] = value.type_ === null ? null : encodeTdPollType(value.type_);
  out['open_period'] = value.open_period;
  out['close_date'] = value.close_date;
  out['is_closed'] = value.is_closed;
  out['vote_restriction_reason'] = value.vote_restriction_reason === null ? null : encodeTdPollVoteRestrictionReason(value.vote_restriction_reason);
  return out;
}

/** pollMediaAnimation = PollMedia — An animation */
export class PollMediaAnimation {
  type: 'pollMediaAnimation' = 'pollMediaAnimation';
  extra?: string;
  /** animation: animation — The animation */
  animation: Animation | null = null;
}

/** Decode a TDLib `pollMediaAnimation` object; null input -> null. */
export function decodePollMediaAnimation(json: TdJsonRaw): PollMediaAnimation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollMediaAnimation = new PollMediaAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeAnimation(record['animation']);
  return out;
}

/** Encode a `pollMediaAnimation` into TDLib JSON wire shape. */
export function encodePollMediaAnimation(value: PollMediaAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollMediaAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  return out;
}

/** pollMediaAudio = PollMedia — An audio */
export class PollMediaAudio {
  type: 'pollMediaAudio' = 'pollMediaAudio';
  extra?: string;
  /** audio: audio — The audio */
  audio: Audio | null = null;
}

/** Decode a TDLib `pollMediaAudio` object; null input -> null. */
export function decodePollMediaAudio(json: TdJsonRaw): PollMediaAudio | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollMediaAudio = new PollMediaAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeAudio(record['audio']);
  return out;
}

/** Encode a `pollMediaAudio` into TDLib JSON wire shape. */
export function encodePollMediaAudio(value: PollMediaAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollMediaAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeAudio(value.audio);
  return out;
}

/** pollMediaDocument = PollMedia — A document (general file) */
export class PollMediaDocument {
  type: 'pollMediaDocument' = 'pollMediaDocument';
  extra?: string;
  /** document: document — The document */
  document: Document | null = null;
}

/** Decode a TDLib `pollMediaDocument` object; null input -> null. */
export function decodePollMediaDocument(json: TdJsonRaw): PollMediaDocument | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollMediaDocument = new PollMediaDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeDocument(record['document']);
  return out;
}

/** Encode a `pollMediaDocument` into TDLib JSON wire shape. */
export function encodePollMediaDocument(value: PollMediaDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollMediaDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeDocument(value.document);
  return out;
}

/** pollMediaLink = PollMedia — A link */
export class PollMediaLink {
  type: 'pollMediaLink' = 'pollMediaLink';
  extra?: string;
  /** url: string — URL of the link */
  url: string = '';
  /** link_preview: linkPreview — Preview of the link; may be null if unknown */
  link_preview: LinkPreview | null = null;
}

/** Decode a TDLib `pollMediaLink` object; null input -> null. */
export function decodePollMediaLink(json: TdJsonRaw): PollMediaLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollMediaLink = new PollMediaLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.url = asString(record['url']);
  out.link_preview = decodeLinkPreview(record['link_preview']);
  return out;
}

/** Encode a `pollMediaLink` into TDLib JSON wire shape. */
export function encodePollMediaLink(value: PollMediaLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollMediaLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['url'] = value.url;
  out['link_preview'] = value.link_preview === null ? null : encodeLinkPreview(value.link_preview);
  return out;
}

/** pollMediaLocation = PollMedia — A location */
export class PollMediaLocation {
  type: 'pollMediaLocation' = 'pollMediaLocation';
  extra?: string;
  /** location: location — The location */
  location: Location | null = null;
}

/** Decode a TDLib `pollMediaLocation` object; null input -> null. */
export function decodePollMediaLocation(json: TdJsonRaw): PollMediaLocation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollMediaLocation = new PollMediaLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.location = decodeLocation(record['location']);
  return out;
}

/** Encode a `pollMediaLocation` into TDLib JSON wire shape. */
export function encodePollMediaLocation(value: PollMediaLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollMediaLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['location'] = value.location === null ? null : encodeLocation(value.location);
  return out;
}

/** pollMediaPhoto = PollMedia — A photo */
export class PollMediaPhoto {
  type: 'pollMediaPhoto' = 'pollMediaPhoto';
  extra?: string;
  /** photo: photo — The photo */
  photo: Photo | null = null;
  /** video: video — The video representing the live photo; may be null if the photo is static */
  video: Video | null = null;
}

/** Decode a TDLib `pollMediaPhoto` object; null input -> null. */
export function decodePollMediaPhoto(json: TdJsonRaw): PollMediaPhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollMediaPhoto = new PollMediaPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  out.video = decodeVideo(record['video']);
  return out;
}

/** Encode a `pollMediaPhoto` into TDLib JSON wire shape. */
export function encodePollMediaPhoto(value: PollMediaPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollMediaPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  return out;
}

/** pollMediaSticker = PollMedia — A sticker */
export class PollMediaSticker {
  type: 'pollMediaSticker' = 'pollMediaSticker';
  extra?: string;
  /** sticker: sticker — The sticker */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `pollMediaSticker` object; null input -> null. */
export function decodePollMediaSticker(json: TdJsonRaw): PollMediaSticker | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollMediaSticker = new PollMediaSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `pollMediaSticker` into TDLib JSON wire shape. */
export function encodePollMediaSticker(value: PollMediaSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollMediaSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** pollMediaVenue = PollMedia — A venue */
export class PollMediaVenue {
  type: 'pollMediaVenue' = 'pollMediaVenue';
  extra?: string;
  /** venue: venue — The venue */
  venue: Venue | null = null;
}

/** Decode a TDLib `pollMediaVenue` object; null input -> null. */
export function decodePollMediaVenue(json: TdJsonRaw): PollMediaVenue | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollMediaVenue = new PollMediaVenue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.venue = decodeVenue(record['venue']);
  return out;
}

/** Encode a `pollMediaVenue` into TDLib JSON wire shape. */
export function encodePollMediaVenue(value: PollMediaVenue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollMediaVenue';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['venue'] = value.venue === null ? null : encodeVenue(value.venue);
  return out;
}

/** pollMediaVideo = PollMedia — A video */
export class PollMediaVideo {
  type: 'pollMediaVideo' = 'pollMediaVideo';
  extra?: string;
  /** video: video — The video description */
  video: Video | null = null;
  /** alternative_videos: vector<alternativeVideo> — Alternative qualities of the video */
  alternative_videos: AlternativeVideo[] = [];
  /** storyboards: vector<videoStoryboard> — Available storyboards for the video */
  storyboards: VideoStoryboard[] = [];
  /** cover: photo — Cover of the video; may be null if none */
  cover: Photo | null = null;
  /** start_timestamp: int32 — Timestamp from which the video playing must start, in seconds */
  start_timestamp: number = 0;
}

/** Decode a TDLib `pollMediaVideo` object; null input -> null. */
export function decodePollMediaVideo(json: TdJsonRaw): PollMediaVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollMediaVideo = new PollMediaVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeVideo(record['video']);
  out.alternative_videos = decodeVector(record['alternative_videos'], (item: TdJsonRaw): AlternativeVideo => decodeAlternativeVideo(item) as AlternativeVideo);
  out.storyboards = decodeVector(record['storyboards'], (item: TdJsonRaw): VideoStoryboard => decodeVideoStoryboard(item) as VideoStoryboard);
  out.cover = decodePhoto(record['cover']);
  out.start_timestamp = asNumber(record['start_timestamp']);
  return out;
}

/** Encode a `pollMediaVideo` into TDLib JSON wire shape. */
export function encodePollMediaVideo(value: PollMediaVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollMediaVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  out['alternative_videos'] = encodeVector(value.alternative_videos, (item: AlternativeVideo | null): TdJsonRaw => item === null ? null : encodeAlternativeVideo(item));
  out['storyboards'] = encodeVector(value.storyboards, (item: VideoStoryboard | null): TdJsonRaw => item === null ? null : encodeVideoStoryboard(item));
  out['cover'] = value.cover === null ? null : encodePhoto(value.cover);
  out['start_timestamp'] = value.start_timestamp;
  return out;
}

/** pollOption = PollOption — Describes one answer option of a poll */
export class PollOption {
  type: 'pollOption' = 'pollOption';
  extra?: string;
  /** id: string — Unique identifier of the option in the poll; may be empty if yet unassigned */
  id: string = '';
  /** text: formattedText — Option text; 1-100 characters; may contain only custom emoji entities */
  text: FormattedText | null = null;
  /** media: PollMedia — Option media; may be null if none. If present, currently, can be only of the types pollMediaAnimation, pollMediaLink, pollMediaLocation, pollMediaPhoto, pollMediaSticker, pollMediaVenue, or pollMediaVideo */
  media: TdPollMedia | null = null;
  /** voter_count: int32 — Number of voters for this option, available only for closed or voted polls, or if the current user is the creator of the poll */
  voter_count: number = 0;
  /** vote_percentage: int32 — The percentage of votes for this option; 0-100 */
  vote_percentage: number = 0;
  /** recent_voter_ids: vector<MessageSender> — Identifiers of recent voters for the option, if the poll is non-anonymous and poll results are available */
  recent_voter_ids: TdMessageSender[] = [];
  /** is_chosen: Bool — True, if the option was chosen by the user */
  is_chosen: boolean = false;
  /** is_being_chosen: Bool — True, if the option is being chosen by a pending setPollAnswer request */
  is_being_chosen: boolean = false;
  /** author: MessageSender — Identifier of the user or chat who added the option; may be null if the option existed from creation of the poll */
  author: TdMessageSender | null = null;
  /** addition_date: int32 — Point in time (Unix timestamp) when the option was added; 0 if the option existed from creation of the poll */
  addition_date: number = 0;
}

/** Decode a TDLib `pollOption` object; null input -> null. */
export function decodePollOption(json: TdJsonRaw): PollOption | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollOption = new PollOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.text = decodeFormattedText(record['text']);
  out.media = decodeTdPollMedia(record['media']);
  out.voter_count = asNumber(record['voter_count']);
  out.vote_percentage = asNumber(record['vote_percentage']);
  out.recent_voter_ids = decodeVector(record['recent_voter_ids'], (item: TdJsonRaw): TdMessageSender => decodeTdMessageSender(item) as TdMessageSender);
  out.is_chosen = asBool(record['is_chosen']);
  out.is_being_chosen = asBool(record['is_being_chosen']);
  out.author = decodeTdMessageSender(record['author']);
  out.addition_date = asNumber(record['addition_date']);
  return out;
}

/** Encode a `pollOption` into TDLib JSON wire shape. */
export function encodePollOption(value: PollOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['text'] = value.text === null ? null : encodeFormattedText(value.text);
  out['media'] = value.media === null ? null : encodeTdPollMedia(value.media);
  out['voter_count'] = value.voter_count;
  out['vote_percentage'] = value.vote_percentage;
  out['recent_voter_ids'] = encodeVector(value.recent_voter_ids, (item: TdMessageSender | null): TdJsonRaw => item === null ? null : encodeTdMessageSender(item));
  out['is_chosen'] = value.is_chosen;
  out['is_being_chosen'] = value.is_being_chosen;
  out['author'] = value.author === null ? null : encodeTdMessageSender(value.author);
  out['addition_date'] = value.addition_date;
  return out;
}

/** pollOptionProperties = PollOptionProperties — Contains properties of a poll option and describes actions that can be done with the option right now */
export class PollOptionProperties {
  type: 'pollOptionProperties' = 'pollOptionProperties';
  extra?: string;
  /** can_be_deleted: Bool — True, if the option can be deleted using deletePollOption */
  can_be_deleted: boolean = false;
  /** can_be_replied: Bool — True, if the poll option can be replied in the same chat and forum topic using inputMessageReplyToMessage */
  can_be_replied: boolean = false;
  /** can_be_replied_in_another_chat: Bool — True, if the poll option can be replied in another chat or forum topic using inputMessageReplyToExternalMessage */
  can_be_replied_in_another_chat: boolean = false;
  /** can_get_link: Bool — True, if a link can be generated for the poll option using getMessageLink */
  can_get_link: boolean = false;
}

/** Decode a TDLib `pollOptionProperties` object; null input -> null. */
export function decodePollOptionProperties(json: TdJsonRaw): PollOptionProperties | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollOptionProperties = new PollOptionProperties();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.can_be_deleted = asBool(record['can_be_deleted']);
  out.can_be_replied = asBool(record['can_be_replied']);
  out.can_be_replied_in_another_chat = asBool(record['can_be_replied_in_another_chat']);
  out.can_get_link = asBool(record['can_get_link']);
  return out;
}

/** Encode a `pollOptionProperties` into TDLib JSON wire shape. */
export function encodePollOptionProperties(value: PollOptionProperties): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollOptionProperties';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['can_be_deleted'] = value.can_be_deleted;
  out['can_be_replied'] = value.can_be_replied;
  out['can_be_replied_in_another_chat'] = value.can_be_replied_in_another_chat;
  out['can_get_link'] = value.can_get_link;
  return out;
}

/** pollTypeQuiz = PollType — A poll in quiz mode, which has predefined correct answers If present, currently, can be only of the types pollMediaAnimation, pollMediaAudio, pollMediaDocument, pollMediaLocation, pollMediaPhoto, pollMediaVenue,... */
export class PollTypeQuiz {
  type: 'pollTypeQuiz' = 'pollTypeQuiz';
  extra?: string;
  /** correct_option_ids: vector<int32> — Increasing list of 0-based identifiers of the correct answer options; empty for a yet unanswered poll */
  correct_option_ids: number[] = [];
  /** explanation: formattedText — Text that is shown when the user chooses an incorrect answer or taps on the lamp icon; empty for a yet unanswered poll */
  explanation: FormattedText | null = null;
  /** explanation_media: PollMedia — Media that is shown when the user chooses an incorrect answer or taps on the lamp icon; may be null if none or the poll is unanswered yet. */
  explanation_media: TdPollMedia | null = null;
}

/** Decode a TDLib `pollTypeQuiz` object; null input -> null. */
export function decodePollTypeQuiz(json: TdJsonRaw): PollTypeQuiz | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollTypeQuiz = new PollTypeQuiz();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.correct_option_ids = decodeVector(record['correct_option_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.explanation = decodeFormattedText(record['explanation']);
  out.explanation_media = decodeTdPollMedia(record['explanation_media']);
  return out;
}

/** Encode a `pollTypeQuiz` into TDLib JSON wire shape. */
export function encodePollTypeQuiz(value: PollTypeQuiz): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollTypeQuiz';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['correct_option_ids'] = encodeVector(value.correct_option_ids, (item: number): TdJsonRaw => item);
  out['explanation'] = value.explanation === null ? null : encodeFormattedText(value.explanation);
  out['explanation_media'] = value.explanation_media === null ? null : encodeTdPollMedia(value.explanation_media);
  return out;
}

/** pollTypeRegular = PollType — A regular poll */
export class PollTypeRegular {
  type: 'pollTypeRegular' = 'pollTypeRegular';
  extra?: string;
}

/** Decode a TDLib `pollTypeRegular` object; null input -> null. */
export function decodePollTypeRegular(json: TdJsonRaw): PollTypeRegular | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollTypeRegular = new PollTypeRegular();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pollTypeRegular` into TDLib JSON wire shape. */
export function encodePollTypeRegular(value: PollTypeRegular): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollTypeRegular';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pollVoteRestrictionReasonClosed = PollVoteRestrictionReason — The poll is closed */
export class PollVoteRestrictionReasonClosed {
  type: 'pollVoteRestrictionReasonClosed' = 'pollVoteRestrictionReasonClosed';
  extra?: string;
}

/** Decode a TDLib `pollVoteRestrictionReasonClosed` object; null input -> null. */
export function decodePollVoteRestrictionReasonClosed(json: TdJsonRaw): PollVoteRestrictionReasonClosed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollVoteRestrictionReasonClosed = new PollVoteRestrictionReasonClosed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pollVoteRestrictionReasonClosed` into TDLib JSON wire shape. */
export function encodePollVoteRestrictionReasonClosed(value: PollVoteRestrictionReasonClosed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollVoteRestrictionReasonClosed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pollVoteRestrictionReasonCountryRestricted = PollVoteRestrictionReason — The user is from a country, users from which aren't allowed to vote */
export class PollVoteRestrictionReasonCountryRestricted {
  type: 'pollVoteRestrictionReasonCountryRestricted' = 'pollVoteRestrictionReasonCountryRestricted';
  extra?: string;
  /** country_code: string — Two-letter ISO 3166-1 alpha-2 code of the current user's country */
  country_code: string = '';
}

/** Decode a TDLib `pollVoteRestrictionReasonCountryRestricted` object; null input -> null. */
export function decodePollVoteRestrictionReasonCountryRestricted(json: TdJsonRaw): PollVoteRestrictionReasonCountryRestricted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollVoteRestrictionReasonCountryRestricted = new PollVoteRestrictionReasonCountryRestricted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.country_code = asString(record['country_code']);
  return out;
}

/** Encode a `pollVoteRestrictionReasonCountryRestricted` into TDLib JSON wire shape. */
export function encodePollVoteRestrictionReasonCountryRestricted(value: PollVoteRestrictionReasonCountryRestricted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollVoteRestrictionReasonCountryRestricted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['country_code'] = value.country_code;
  return out;
}

/** pollVoteRestrictionReasonMembershipRequired = PollVoteRestrictionReason — The user must be a member of the chat for at least a day to vote */
export class PollVoteRestrictionReasonMembershipRequired {
  type: 'pollVoteRestrictionReasonMembershipRequired' = 'pollVoteRestrictionReasonMembershipRequired';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat which must be joined for at least a day before the user can vote */
  chat_id: number = 0;
}

/** Decode a TDLib `pollVoteRestrictionReasonMembershipRequired` object; null input -> null. */
export function decodePollVoteRestrictionReasonMembershipRequired(json: TdJsonRaw): PollVoteRestrictionReasonMembershipRequired | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollVoteRestrictionReasonMembershipRequired = new PollVoteRestrictionReasonMembershipRequired();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  return out;
}

/** Encode a `pollVoteRestrictionReasonMembershipRequired` into TDLib JSON wire shape. */
export function encodePollVoteRestrictionReasonMembershipRequired(value: PollVoteRestrictionReasonMembershipRequired): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollVoteRestrictionReasonMembershipRequired';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  return out;
}

/** pollVoteRestrictionReasonOther = PollVoteRestrictionReason — The poll can't be voted by the user due to some other reason */
export class PollVoteRestrictionReasonOther {
  type: 'pollVoteRestrictionReasonOther' = 'pollVoteRestrictionReasonOther';
  extra?: string;
}

/** Decode a TDLib `pollVoteRestrictionReasonOther` object; null input -> null. */
export function decodePollVoteRestrictionReasonOther(json: TdJsonRaw): PollVoteRestrictionReasonOther | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollVoteRestrictionReasonOther = new PollVoteRestrictionReasonOther();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pollVoteRestrictionReasonOther` into TDLib JSON wire shape. */
export function encodePollVoteRestrictionReasonOther(value: PollVoteRestrictionReasonOther): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollVoteRestrictionReasonOther';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pollVoteRestrictionReasonScheduled = PollVoteRestrictionReason — The poll is from a scheduled message */
export class PollVoteRestrictionReasonScheduled {
  type: 'pollVoteRestrictionReasonScheduled' = 'pollVoteRestrictionReasonScheduled';
  extra?: string;
}

/** Decode a TDLib `pollVoteRestrictionReasonScheduled` object; null input -> null. */
export function decodePollVoteRestrictionReasonScheduled(json: TdJsonRaw): PollVoteRestrictionReasonScheduled | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollVoteRestrictionReasonScheduled = new PollVoteRestrictionReasonScheduled();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pollVoteRestrictionReasonScheduled` into TDLib JSON wire shape. */
export function encodePollVoteRestrictionReasonScheduled(value: PollVoteRestrictionReasonScheduled): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollVoteRestrictionReasonScheduled';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pollVoteRestrictionReasonYetUnsent = PollVoteRestrictionReason — The poll isn't sent yet */
export class PollVoteRestrictionReasonYetUnsent {
  type: 'pollVoteRestrictionReasonYetUnsent' = 'pollVoteRestrictionReasonYetUnsent';
  extra?: string;
}

/** Decode a TDLib `pollVoteRestrictionReasonYetUnsent` object; null input -> null. */
export function decodePollVoteRestrictionReasonYetUnsent(json: TdJsonRaw): PollVoteRestrictionReasonYetUnsent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollVoteRestrictionReasonYetUnsent = new PollVoteRestrictionReasonYetUnsent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pollVoteRestrictionReasonYetUnsent` into TDLib JSON wire shape. */
export function encodePollVoteRestrictionReasonYetUnsent(value: PollVoteRestrictionReasonYetUnsent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollVoteRestrictionReasonYetUnsent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pollVoteStatistics = PollVoteStatistics — A detailed statistics about poll votes */
export class PollVoteStatistics {
  type: 'pollVoteStatistics' = 'pollVoteStatistics';
  extra?: string;
  /** vote_graph: StatisticalGraph — A graph containing distribution of votes in the poll */
  vote_graph: TdStatisticalGraph | null = null;
}

/** Decode a TDLib `pollVoteStatistics` object; null input -> null. */
export function decodePollVoteStatistics(json: TdJsonRaw): PollVoteStatistics | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollVoteStatistics = new PollVoteStatistics();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.vote_graph = decodeTdStatisticalGraph(record['vote_graph']);
  return out;
}

/** Encode a `pollVoteStatistics` into TDLib JSON wire shape. */
export function encodePollVoteStatistics(value: PollVoteStatistics): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollVoteStatistics';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['vote_graph'] = value.vote_graph === null ? null : encodeTdStatisticalGraph(value.vote_graph);
  return out;
}

/** pollVoter = PollVoter — Represents a poll voter */
export class PollVoter {
  type: 'pollVoter' = 'pollVoter';
  extra?: string;
  /** voter_id: MessageSender — The voter identifier */
  voter_id: TdMessageSender | null = null;
  /** date: int32 — Point in time (Unix timestamp) when the vote was added */
  date: number = 0;
}

/** Decode a TDLib `pollVoter` object; null input -> null. */
export function decodePollVoter(json: TdJsonRaw): PollVoter | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollVoter = new PollVoter();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.voter_id = decodeTdMessageSender(record['voter_id']);
  out.date = asNumber(record['date']);
  return out;
}

/** Encode a `pollVoter` into TDLib JSON wire shape. */
export function encodePollVoter(value: PollVoter): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollVoter';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['voter_id'] = value.voter_id === null ? null : encodeTdMessageSender(value.voter_id);
  out['date'] = value.date;
  return out;
}

/** pollVoters = PollVoters — Represents a list of poll voters */
export class PollVoters {
  type: 'pollVoters' = 'pollVoters';
  extra?: string;
  /** total_count: int32 — Approximate total number of poll voters found */
  total_count: number = 0;
  /** voters: vector<pollVoter> — List of poll voters */
  voters: PollVoter[] = [];
}

/** Decode a TDLib `pollVoters` object; null input -> null. */
export function decodePollVoters(json: TdJsonRaw): PollVoters | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PollVoters = new PollVoters();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.voters = decodeVector(record['voters'], (item: TdJsonRaw): PollVoter => decodePollVoter(item) as PollVoter);
  return out;
}

/** Encode a `pollVoters` into TDLib JSON wire shape. */
export function encodePollVoters(value: PollVoters): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pollVoters';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['voters'] = encodeVector(value.voters, (item: PollVoter | null): TdJsonRaw => item === null ? null : encodePollVoter(item));
  return out;
}

/** postStory = Story — Posts a new story on behalf of a chat; requires can_post_stories administrator right for supergroup and channel chats. Returns a temporary story */
export class PostStory {
  type: 'postStory' = 'postStory';
  extra?: string;
  /** chat_id: int53 — Identifier of the chat that will post the story. Pass Saved Messages chat identifier when posting a story on behalf of the current user */
  chat_id: number = 0;
  /** content: InputStoryContent — Content of the story */
  content: TdInputStoryContent | null = null;
  /** areas: inputStoryAreas — Clickable rectangle areas to be shown on the story media; pass null if none */
  areas: InputStoryAreas | null = null;
  /** caption: formattedText — Story caption; pass null to use an empty caption; 0-getOption("story_caption_length_max") characters; can have entities only if getOption("can_use_text_entities_in_story_caption") */
  caption: FormattedText | null = null;
  /** privacy_settings: StoryPrivacySettings — The privacy settings for the story; ignored for stories posted on behalf of supergroup and channel chats */
  privacy_settings: TdStoryPrivacySettings | null = null;
  /** album_ids: vector<int32> — Identifiers of story albums to which the story will be added upon posting. An album can have up to getOption("story_album_size_max") stories */
  album_ids: number[] = [];
  /** active_period: int32 — Period after which the story is moved to archive, in seconds; must be one of 6 * 3600, 12 * 3600, 86400, or 2 * 86400 for Telegram Premium users, and 86400 otherwise */
  active_period: number = 0;
  /** from_story_full_id: storyFullId — Full identifier of the original story, which content was used to create the story; pass null if the story isn't repost of another story */
  from_story_full_id: StoryFullId | null = null;
  /** is_posted_to_chat_page: Bool — Pass true to keep the story accessible after expiration */
  is_posted_to_chat_page: boolean = false;
  /** protect_content: Bool — Pass true if the content of the story must be protected from forwarding and screenshotting */
  protect_content: boolean = false;
}

/** Decode a TDLib `postStory` object; null input -> null. */
export function decodePostStory(json: TdJsonRaw): PostStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PostStory = new PostStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.content = decodeTdInputStoryContent(record['content']);
  out.areas = decodeInputStoryAreas(record['areas']);
  out.caption = decodeFormattedText(record['caption']);
  out.privacy_settings = decodeTdStoryPrivacySettings(record['privacy_settings']);
  out.album_ids = decodeVector(record['album_ids'], (item: TdJsonRaw): number => asNumber(item));
  out.active_period = asNumber(record['active_period']);
  out.from_story_full_id = decodeStoryFullId(record['from_story_full_id']);
  out.is_posted_to_chat_page = asBool(record['is_posted_to_chat_page']);
  out.protect_content = asBool(record['protect_content']);
  return out;
}

/** Encode a `postStory` into TDLib JSON wire shape. */
export function encodePostStory(value: PostStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'postStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['content'] = value.content === null ? null : encodeTdInputStoryContent(value.content);
  out['areas'] = value.areas === null ? null : encodeInputStoryAreas(value.areas);
  out['caption'] = value.caption === null ? null : encodeFormattedText(value.caption);
  out['privacy_settings'] = value.privacy_settings === null ? null : encodeTdStoryPrivacySettings(value.privacy_settings);
  out['album_ids'] = encodeVector(value.album_ids, (item: number): TdJsonRaw => item);
  out['active_period'] = value.active_period;
  out['from_story_full_id'] = value.from_story_full_id === null ? null : encodeStoryFullId(value.from_story_full_id);
  out['is_posted_to_chat_page'] = value.is_posted_to_chat_page;
  out['protect_content'] = value.protect_content;
  return out;
}

/** preliminaryUploadFile = File — Preliminarily uploads a file to the cloud before sending it in a message, which can be useful for uploading of being recorded voice and video notes. In all other cases there is no need to preliminary upload... */
export class PreliminaryUploadFile {
  type: 'preliminaryUploadFile' = 'preliminaryUploadFile';
  extra?: string;
  /** file: InputFile — File to upload */
  file: TdInputFile | null = null;
  /** file_type: FileType — File type; pass null if unknown */
  file_type: TdFileType | null = null;
  /** priority: int32 — Priority of the upload (1-32). The higher the priority, the earlier the file will be uploaded. If the priorities of two files are equal, then the first one for which preliminaryUploadFile was called will be uploaded first */
  priority: number = 0;
}

/** Decode a TDLib `preliminaryUploadFile` object; null input -> null. */
export function decodePreliminaryUploadFile(json: TdJsonRaw): PreliminaryUploadFile | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PreliminaryUploadFile = new PreliminaryUploadFile();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.file = decodeTdInputFile(record['file']);
  out.file_type = decodeTdFileType(record['file_type']);
  out.priority = asNumber(record['priority']);
  return out;
}

/** Encode a `preliminaryUploadFile` into TDLib JSON wire shape. */
export function encodePreliminaryUploadFile(value: PreliminaryUploadFile): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'preliminaryUploadFile';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['file'] = value.file === null ? null : encodeTdInputFile(value.file);
  out['file_type'] = value.file_type === null ? null : encodeTdFileType(value.file_type);
  out['priority'] = value.priority;
  return out;
}

/** premiumFeatureAccentColor = PremiumFeature — The ability to choose accent color for replies and user profile */
export class PremiumFeatureAccentColor {
  type: 'premiumFeatureAccentColor' = 'premiumFeatureAccentColor';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureAccentColor` object; null input -> null. */
export function decodePremiumFeatureAccentColor(json: TdJsonRaw): PremiumFeatureAccentColor | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureAccentColor = new PremiumFeatureAccentColor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureAccentColor` into TDLib JSON wire shape. */
export function encodePremiumFeatureAccentColor(value: PremiumFeatureAccentColor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureAccentColor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureAdvancedChatManagement = PremiumFeature — Ability to change position of the main chat list, archive and mute all new chats from non-contacts, and completely disable notifications about the user's contacts joined Telegram */
export class PremiumFeatureAdvancedChatManagement {
  type: 'premiumFeatureAdvancedChatManagement' = 'premiumFeatureAdvancedChatManagement';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureAdvancedChatManagement` object; null input -> null. */
export function decodePremiumFeatureAdvancedChatManagement(json: TdJsonRaw): PremiumFeatureAdvancedChatManagement | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureAdvancedChatManagement = new PremiumFeatureAdvancedChatManagement();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureAdvancedChatManagement` into TDLib JSON wire shape. */
export function encodePremiumFeatureAdvancedChatManagement(value: PremiumFeatureAdvancedChatManagement): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureAdvancedChatManagement';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureAnimatedProfilePhoto = PremiumFeature — Profile photo animation on message and chat screens */
export class PremiumFeatureAnimatedProfilePhoto {
  type: 'premiumFeatureAnimatedProfilePhoto' = 'premiumFeatureAnimatedProfilePhoto';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureAnimatedProfilePhoto` object; null input -> null. */
export function decodePremiumFeatureAnimatedProfilePhoto(json: TdJsonRaw): PremiumFeatureAnimatedProfilePhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureAnimatedProfilePhoto = new PremiumFeatureAnimatedProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureAnimatedProfilePhoto` into TDLib JSON wire shape. */
export function encodePremiumFeatureAnimatedProfilePhoto(value: PremiumFeatureAnimatedProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureAnimatedProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureAppIcons = PremiumFeature — Allowed to set a premium application icons */
export class PremiumFeatureAppIcons {
  type: 'premiumFeatureAppIcons' = 'premiumFeatureAppIcons';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureAppIcons` object; null input -> null. */
export function decodePremiumFeatureAppIcons(json: TdJsonRaw): PremiumFeatureAppIcons | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureAppIcons = new PremiumFeatureAppIcons();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureAppIcons` into TDLib JSON wire shape. */
export function encodePremiumFeatureAppIcons(value: PremiumFeatureAppIcons): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureAppIcons';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureBackgroundForBoth = PremiumFeature — The ability to set private chat background for both users */
export class PremiumFeatureBackgroundForBoth {
  type: 'premiumFeatureBackgroundForBoth' = 'premiumFeatureBackgroundForBoth';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureBackgroundForBoth` object; null input -> null. */
export function decodePremiumFeatureBackgroundForBoth(json: TdJsonRaw): PremiumFeatureBackgroundForBoth | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureBackgroundForBoth = new PremiumFeatureBackgroundForBoth();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureBackgroundForBoth` into TDLib JSON wire shape. */
export function encodePremiumFeatureBackgroundForBoth(value: PremiumFeatureBackgroundForBoth): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureBackgroundForBoth';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureBusiness = PremiumFeature — The ability to use Business features */
export class PremiumFeatureBusiness {
  type: 'premiumFeatureBusiness' = 'premiumFeatureBusiness';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureBusiness` object; null input -> null. */
export function decodePremiumFeatureBusiness(json: TdJsonRaw): PremiumFeatureBusiness | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureBusiness = new PremiumFeatureBusiness();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureBusiness` into TDLib JSON wire shape. */
export function encodePremiumFeatureBusiness(value: PremiumFeatureBusiness): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureBusiness';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureChatBoost = PremiumFeature — The ability to boost chats */
export class PremiumFeatureChatBoost {
  type: 'premiumFeatureChatBoost' = 'premiumFeatureChatBoost';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureChatBoost` object; null input -> null. */
export function decodePremiumFeatureChatBoost(json: TdJsonRaw): PremiumFeatureChatBoost | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureChatBoost = new PremiumFeatureChatBoost();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureChatBoost` into TDLib JSON wire shape. */
export function encodePremiumFeatureChatBoost(value: PremiumFeatureChatBoost): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureChatBoost';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureChecklists = PremiumFeature — The ability to create and use checklist messages */
export class PremiumFeatureChecklists {
  type: 'premiumFeatureChecklists' = 'premiumFeatureChecklists';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureChecklists` object; null input -> null. */
export function decodePremiumFeatureChecklists(json: TdJsonRaw): PremiumFeatureChecklists | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureChecklists = new PremiumFeatureChecklists();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureChecklists` into TDLib JSON wire shape. */
export function encodePremiumFeatureChecklists(value: PremiumFeatureChecklists): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureChecklists';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureCustomEmoji = PremiumFeature — Allowed to use custom emoji stickers in message texts and captions */
export class PremiumFeatureCustomEmoji {
  type: 'premiumFeatureCustomEmoji' = 'premiumFeatureCustomEmoji';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureCustomEmoji` object; null input -> null. */
export function decodePremiumFeatureCustomEmoji(json: TdJsonRaw): PremiumFeatureCustomEmoji | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureCustomEmoji = new PremiumFeatureCustomEmoji();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureCustomEmoji` into TDLib JSON wire shape. */
export function encodePremiumFeatureCustomEmoji(value: PremiumFeatureCustomEmoji): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureCustomEmoji';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureDisabledAds = PremiumFeature — Disabled ads */
export class PremiumFeatureDisabledAds {
  type: 'premiumFeatureDisabledAds' = 'premiumFeatureDisabledAds';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureDisabledAds` object; null input -> null. */
export function decodePremiumFeatureDisabledAds(json: TdJsonRaw): PremiumFeatureDisabledAds | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureDisabledAds = new PremiumFeatureDisabledAds();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureDisabledAds` into TDLib JSON wire shape. */
export function encodePremiumFeatureDisabledAds(value: PremiumFeatureDisabledAds): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureDisabledAds';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureEmojiStatus = PremiumFeature — The ability to show an emoji status along with the user's name */
export class PremiumFeatureEmojiStatus {
  type: 'premiumFeatureEmojiStatus' = 'premiumFeatureEmojiStatus';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureEmojiStatus` object; null input -> null. */
export function decodePremiumFeatureEmojiStatus(json: TdJsonRaw): PremiumFeatureEmojiStatus | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureEmojiStatus = new PremiumFeatureEmojiStatus();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureEmojiStatus` into TDLib JSON wire shape. */
export function encodePremiumFeatureEmojiStatus(value: PremiumFeatureEmojiStatus): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureEmojiStatus';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureForumTopicIcon = PremiumFeature — The ability to set a custom emoji as a forum topic icon */
export class PremiumFeatureForumTopicIcon {
  type: 'premiumFeatureForumTopicIcon' = 'premiumFeatureForumTopicIcon';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureForumTopicIcon` object; null input -> null. */
export function decodePremiumFeatureForumTopicIcon(json: TdJsonRaw): PremiumFeatureForumTopicIcon | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureForumTopicIcon = new PremiumFeatureForumTopicIcon();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureForumTopicIcon` into TDLib JSON wire shape. */
export function encodePremiumFeatureForumTopicIcon(value: PremiumFeatureForumTopicIcon): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureForumTopicIcon';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureImprovedDownloadSpeed = PremiumFeature — Improved download speed */
export class PremiumFeatureImprovedDownloadSpeed {
  type: 'premiumFeatureImprovedDownloadSpeed' = 'premiumFeatureImprovedDownloadSpeed';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureImprovedDownloadSpeed` object; null input -> null. */
export function decodePremiumFeatureImprovedDownloadSpeed(json: TdJsonRaw): PremiumFeatureImprovedDownloadSpeed | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureImprovedDownloadSpeed = new PremiumFeatureImprovedDownloadSpeed();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureImprovedDownloadSpeed` into TDLib JSON wire shape. */
export function encodePremiumFeatureImprovedDownloadSpeed(value: PremiumFeatureImprovedDownloadSpeed): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureImprovedDownloadSpeed';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureIncreasedLimits = PremiumFeature — Increased limits */
export class PremiumFeatureIncreasedLimits {
  type: 'premiumFeatureIncreasedLimits' = 'premiumFeatureIncreasedLimits';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureIncreasedLimits` object; null input -> null. */
export function decodePremiumFeatureIncreasedLimits(json: TdJsonRaw): PremiumFeatureIncreasedLimits | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureIncreasedLimits = new PremiumFeatureIncreasedLimits();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureIncreasedLimits` into TDLib JSON wire shape. */
export function encodePremiumFeatureIncreasedLimits(value: PremiumFeatureIncreasedLimits): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureIncreasedLimits';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureIncreasedUploadFileSize = PremiumFeature — Increased maximum upload file size */
export class PremiumFeatureIncreasedUploadFileSize {
  type: 'premiumFeatureIncreasedUploadFileSize' = 'premiumFeatureIncreasedUploadFileSize';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureIncreasedUploadFileSize` object; null input -> null. */
export function decodePremiumFeatureIncreasedUploadFileSize(json: TdJsonRaw): PremiumFeatureIncreasedUploadFileSize | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureIncreasedUploadFileSize = new PremiumFeatureIncreasedUploadFileSize();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureIncreasedUploadFileSize` into TDLib JSON wire shape. */
export function encodePremiumFeatureIncreasedUploadFileSize(value: PremiumFeatureIncreasedUploadFileSize): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureIncreasedUploadFileSize';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureLastSeenTimes = PremiumFeature — The ability to view last seen and read times of other users even if they can't view last seen or read time for the current user */
export class PremiumFeatureLastSeenTimes {
  type: 'premiumFeatureLastSeenTimes' = 'premiumFeatureLastSeenTimes';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureLastSeenTimes` object; null input -> null. */
export function decodePremiumFeatureLastSeenTimes(json: TdJsonRaw): PremiumFeatureLastSeenTimes | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureLastSeenTimes = new PremiumFeatureLastSeenTimes();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureLastSeenTimes` into TDLib JSON wire shape. */
export function encodePremiumFeatureLastSeenTimes(value: PremiumFeatureLastSeenTimes): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureLastSeenTimes';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureMessageEffects = PremiumFeature — The ability to use all available message effects */
export class PremiumFeatureMessageEffects {
  type: 'premiumFeatureMessageEffects' = 'premiumFeatureMessageEffects';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureMessageEffects` object; null input -> null. */
export function decodePremiumFeatureMessageEffects(json: TdJsonRaw): PremiumFeatureMessageEffects | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureMessageEffects = new PremiumFeatureMessageEffects();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureMessageEffects` into TDLib JSON wire shape. */
export function encodePremiumFeatureMessageEffects(value: PremiumFeatureMessageEffects): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureMessageEffects';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureMessagePrivacy = PremiumFeature — The ability to disallow incoming voice and video note messages in private chats using setUserPrivacySettingRules with userPrivacySettingAllowPrivateVoiceAndVideoNoteMessages and to restrict... */
export class PremiumFeatureMessagePrivacy {
  type: 'premiumFeatureMessagePrivacy' = 'premiumFeatureMessagePrivacy';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureMessagePrivacy` object; null input -> null. */
export function decodePremiumFeatureMessagePrivacy(json: TdJsonRaw): PremiumFeatureMessagePrivacy | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureMessagePrivacy = new PremiumFeatureMessagePrivacy();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureMessagePrivacy` into TDLib JSON wire shape. */
export function encodePremiumFeatureMessagePrivacy(value: PremiumFeatureMessagePrivacy): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureMessagePrivacy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeaturePaidMessages = PremiumFeature — The ability to require a payment for incoming messages in new chats */
export class PremiumFeaturePaidMessages {
  type: 'premiumFeaturePaidMessages' = 'premiumFeaturePaidMessages';
  extra?: string;
}

/** Decode a TDLib `premiumFeaturePaidMessages` object; null input -> null. */
export function decodePremiumFeaturePaidMessages(json: TdJsonRaw): PremiumFeaturePaidMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeaturePaidMessages = new PremiumFeaturePaidMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeaturePaidMessages` into TDLib JSON wire shape. */
export function encodePremiumFeaturePaidMessages(value: PremiumFeaturePaidMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeaturePaidMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureProfileBadge = PremiumFeature — A badge in the user's profile */
export class PremiumFeatureProfileBadge {
  type: 'premiumFeatureProfileBadge' = 'premiumFeatureProfileBadge';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureProfileBadge` object; null input -> null. */
export function decodePremiumFeatureProfileBadge(json: TdJsonRaw): PremiumFeatureProfileBadge | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureProfileBadge = new PremiumFeatureProfileBadge();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureProfileBadge` into TDLib JSON wire shape. */
export function encodePremiumFeatureProfileBadge(value: PremiumFeatureProfileBadge): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureProfileBadge';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeaturePromotionAnimation = PremiumFeaturePromotionAnimation — Describes a promotion animation for a Premium feature */
export class PremiumFeaturePromotionAnimation {
  type: 'premiumFeaturePromotionAnimation' = 'premiumFeaturePromotionAnimation';
  extra?: string;
  /** feature: PremiumFeature — Premium feature */
  feature: TdPremiumFeature | null = null;
  /** animation: animation — Promotion animation for the feature */
  animation: Animation | null = null;
}

/** Decode a TDLib `premiumFeaturePromotionAnimation` object; null input -> null. */
export function decodePremiumFeaturePromotionAnimation(json: TdJsonRaw): PremiumFeaturePromotionAnimation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeaturePromotionAnimation = new PremiumFeaturePromotionAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.feature = decodeTdPremiumFeature(record['feature']);
  out.animation = decodeAnimation(record['animation']);
  return out;
}

/** Encode a `premiumFeaturePromotionAnimation` into TDLib JSON wire shape. */
export function encodePremiumFeaturePromotionAnimation(value: PremiumFeaturePromotionAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeaturePromotionAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['feature'] = value.feature === null ? null : encodeTdPremiumFeature(value.feature);
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  return out;
}

/** premiumFeatureProtectPrivateChatContent = PremiumFeature — The ability to enable content protection in private chats */
export class PremiumFeatureProtectPrivateChatContent {
  type: 'premiumFeatureProtectPrivateChatContent' = 'premiumFeatureProtectPrivateChatContent';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureProtectPrivateChatContent` object; null input -> null. */
export function decodePremiumFeatureProtectPrivateChatContent(json: TdJsonRaw): PremiumFeatureProtectPrivateChatContent | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureProtectPrivateChatContent = new PremiumFeatureProtectPrivateChatContent();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureProtectPrivateChatContent` into TDLib JSON wire shape. */
export function encodePremiumFeatureProtectPrivateChatContent(value: PremiumFeatureProtectPrivateChatContent): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureProtectPrivateChatContent';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureRealTimeChatTranslation = PremiumFeature — Allowed to translate chat messages real-time */
export class PremiumFeatureRealTimeChatTranslation {
  type: 'premiumFeatureRealTimeChatTranslation' = 'premiumFeatureRealTimeChatTranslation';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureRealTimeChatTranslation` object; null input -> null. */
export function decodePremiumFeatureRealTimeChatTranslation(json: TdJsonRaw): PremiumFeatureRealTimeChatTranslation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureRealTimeChatTranslation = new PremiumFeatureRealTimeChatTranslation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureRealTimeChatTranslation` into TDLib JSON wire shape. */
export function encodePremiumFeatureRealTimeChatTranslation(value: PremiumFeatureRealTimeChatTranslation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureRealTimeChatTranslation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureRichMessages = PremiumFeature — The ability to send rich messages */
export class PremiumFeatureRichMessages {
  type: 'premiumFeatureRichMessages' = 'premiumFeatureRichMessages';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureRichMessages` object; null input -> null. */
export function decodePremiumFeatureRichMessages(json: TdJsonRaw): PremiumFeatureRichMessages | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureRichMessages = new PremiumFeatureRichMessages();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureRichMessages` into TDLib JSON wire shape. */
export function encodePremiumFeatureRichMessages(value: PremiumFeatureRichMessages): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureRichMessages';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureSavedMessagesTags = PremiumFeature — The ability to use tags in Saved Messages */
export class PremiumFeatureSavedMessagesTags {
  type: 'premiumFeatureSavedMessagesTags' = 'premiumFeatureSavedMessagesTags';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureSavedMessagesTags` object; null input -> null. */
export function decodePremiumFeatureSavedMessagesTags(json: TdJsonRaw): PremiumFeatureSavedMessagesTags | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureSavedMessagesTags = new PremiumFeatureSavedMessagesTags();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureSavedMessagesTags` into TDLib JSON wire shape. */
export function encodePremiumFeatureSavedMessagesTags(value: PremiumFeatureSavedMessagesTags): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureSavedMessagesTags';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureTextComposition = PremiumFeature — The ability to compose text with AI */
export class PremiumFeatureTextComposition {
  type: 'premiumFeatureTextComposition' = 'premiumFeatureTextComposition';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureTextComposition` object; null input -> null. */
export function decodePremiumFeatureTextComposition(json: TdJsonRaw): PremiumFeatureTextComposition | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureTextComposition = new PremiumFeatureTextComposition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureTextComposition` into TDLib JSON wire shape. */
export function encodePremiumFeatureTextComposition(value: PremiumFeatureTextComposition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureTextComposition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureUniqueReactions = PremiumFeature — Allowed to use more reactions */
export class PremiumFeatureUniqueReactions {
  type: 'premiumFeatureUniqueReactions' = 'premiumFeatureUniqueReactions';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureUniqueReactions` object; null input -> null. */
export function decodePremiumFeatureUniqueReactions(json: TdJsonRaw): PremiumFeatureUniqueReactions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureUniqueReactions = new PremiumFeatureUniqueReactions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureUniqueReactions` into TDLib JSON wire shape. */
export function encodePremiumFeatureUniqueReactions(value: PremiumFeatureUniqueReactions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureUniqueReactions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureUniqueStickers = PremiumFeature — Allowed to use premium stickers with unique effects */
export class PremiumFeatureUniqueStickers {
  type: 'premiumFeatureUniqueStickers' = 'premiumFeatureUniqueStickers';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureUniqueStickers` object; null input -> null. */
export function decodePremiumFeatureUniqueStickers(json: TdJsonRaw): PremiumFeatureUniqueStickers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureUniqueStickers = new PremiumFeatureUniqueStickers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureUniqueStickers` into TDLib JSON wire shape. */
export function encodePremiumFeatureUniqueStickers(value: PremiumFeatureUniqueStickers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureUniqueStickers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureUpgradedStories = PremiumFeature — Allowed to use many additional features for stories */
export class PremiumFeatureUpgradedStories {
  type: 'premiumFeatureUpgradedStories' = 'premiumFeatureUpgradedStories';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureUpgradedStories` object; null input -> null. */
export function decodePremiumFeatureUpgradedStories(json: TdJsonRaw): PremiumFeatureUpgradedStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureUpgradedStories = new PremiumFeatureUpgradedStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureUpgradedStories` into TDLib JSON wire shape. */
export function encodePremiumFeatureUpgradedStories(value: PremiumFeatureUpgradedStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureUpgradedStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatureVoiceRecognition = PremiumFeature — The ability to convert voice notes to text */
export class PremiumFeatureVoiceRecognition {
  type: 'premiumFeatureVoiceRecognition' = 'premiumFeatureVoiceRecognition';
  extra?: string;
}

/** Decode a TDLib `premiumFeatureVoiceRecognition` object; null input -> null. */
export function decodePremiumFeatureVoiceRecognition(json: TdJsonRaw): PremiumFeatureVoiceRecognition | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatureVoiceRecognition = new PremiumFeatureVoiceRecognition();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumFeatureVoiceRecognition` into TDLib JSON wire shape. */
export function encodePremiumFeatureVoiceRecognition(value: PremiumFeatureVoiceRecognition): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatureVoiceRecognition';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumFeatures = PremiumFeatures — Contains information about features, available to Premium users */
export class PremiumFeatures {
  type: 'premiumFeatures' = 'premiumFeatures';
  extra?: string;
  /** features: vector<PremiumFeature> — The list of available features */
  features: TdPremiumFeature[] = [];
  /** limits: vector<premiumLimit> — The list of limits, increased for Premium users */
  limits: PremiumLimit[] = [];
  /** payment_link: InternalLinkType — An internal link to be opened to pay for Telegram Premium if store payment isn't possible; may be null if direct payment isn't available */
  payment_link: TdInternalLinkType | null = null;
}

/** Decode a TDLib `premiumFeatures` object; null input -> null. */
export function decodePremiumFeatures(json: TdJsonRaw): PremiumFeatures | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumFeatures = new PremiumFeatures();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.features = decodeVector(record['features'], (item: TdJsonRaw): TdPremiumFeature => decodeTdPremiumFeature(item) as TdPremiumFeature);
  out.limits = decodeVector(record['limits'], (item: TdJsonRaw): PremiumLimit => decodePremiumLimit(item) as PremiumLimit);
  out.payment_link = decodeTdInternalLinkType(record['payment_link']);
  return out;
}

/** Encode a `premiumFeatures` into TDLib JSON wire shape. */
export function encodePremiumFeatures(value: PremiumFeatures): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumFeatures';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['features'] = encodeVector(value.features, (item: TdPremiumFeature | null): TdJsonRaw => item === null ? null : encodeTdPremiumFeature(item));
  out['limits'] = encodeVector(value.limits, (item: PremiumLimit | null): TdJsonRaw => item === null ? null : encodePremiumLimit(item));
  out['payment_link'] = value.payment_link === null ? null : encodeTdInternalLinkType(value.payment_link);
  return out;
}

/** premiumGiftCodeInfo = PremiumGiftCodeInfo — Contains information about a Telegram Premium gift code */
export class PremiumGiftCodeInfo {
  type: 'premiumGiftCodeInfo' = 'premiumGiftCodeInfo';
  extra?: string;
  /** creator_id: MessageSender — Identifier of a chat or a user who created the gift code; may be null if unknown. If null and the code is from messagePremiumGiftCode message, then creator_id from the message can be used */
  creator_id: TdMessageSender | null = null;
  /** creation_date: int32 — Point in time (Unix timestamp) when the code was created */
  creation_date: number = 0;
  /** is_from_giveaway: Bool — True, if the gift code was created for a giveaway */
  is_from_giveaway: boolean = false;
  /** giveaway_message_id: int53 — Identifier of the corresponding giveaway message in the creator_id chat; may be 0 or an identifier of a deleted message */
  giveaway_message_id: number = 0;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active after code activation; 0 if the number of months isn't integer */
  month_count: number = 0;
  /** day_count: int32 — Number of days the Telegram Premium subscription will be active after code activation */
  day_count: number = 0;
  /** user_id: int53 — Identifier of a user for which the code was created; 0 if none */
  user_id: number = 0;
  /** use_date: int32 — Point in time (Unix timestamp) when the code was activated; 0 if none */
  use_date: number = 0;
}

/** Decode a TDLib `premiumGiftCodeInfo` object; null input -> null. */
export function decodePremiumGiftCodeInfo(json: TdJsonRaw): PremiumGiftCodeInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumGiftCodeInfo = new PremiumGiftCodeInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.creator_id = decodeTdMessageSender(record['creator_id']);
  out.creation_date = asNumber(record['creation_date']);
  out.is_from_giveaway = asBool(record['is_from_giveaway']);
  out.giveaway_message_id = asNumber(record['giveaway_message_id']);
  out.month_count = asNumber(record['month_count']);
  out.day_count = asNumber(record['day_count']);
  out.user_id = asNumber(record['user_id']);
  out.use_date = asNumber(record['use_date']);
  return out;
}

/** Encode a `premiumGiftCodeInfo` into TDLib JSON wire shape. */
export function encodePremiumGiftCodeInfo(value: PremiumGiftCodeInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumGiftCodeInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['creator_id'] = value.creator_id === null ? null : encodeTdMessageSender(value.creator_id);
  out['creation_date'] = value.creation_date;
  out['is_from_giveaway'] = value.is_from_giveaway;
  out['giveaway_message_id'] = value.giveaway_message_id;
  out['month_count'] = value.month_count;
  out['day_count'] = value.day_count;
  out['user_id'] = value.user_id;
  out['use_date'] = value.use_date;
  return out;
}

/** premiumGiftPaymentOption = PremiumGiftPaymentOption — Describes an option for gifting Telegram Premium to a user. Use telegramPaymentPurposePremiumGift for out-of-store payments or payments in Telegram Stars */
export class PremiumGiftPaymentOption {
  type: 'premiumGiftPaymentOption' = 'premiumGiftPaymentOption';
  extra?: string;
  /** currency: string — ISO 4217 currency code for the payment */
  currency: string = '';
  /** amount: int53 — The amount to pay, in the smallest units of the currency */
  amount: number = 0;
  /** star_count: int53 — The alternative Telegram Star amount to pay; 0 if payment in Telegram Stars is not possible */
  star_count: number = 0;
  /** discount_percentage: int32 — The discount associated with this option, as a percentage */
  discount_percentage: number = 0;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active */
  month_count: number = 0;
  /** store_product_id: string — Identifier of the store product associated with the option */
  store_product_id: string = '';
  /** sticker: sticker — A sticker to be shown along with the option; may be null if unknown */
  sticker: Sticker | null = null;
}

/** Decode a TDLib `premiumGiftPaymentOption` object; null input -> null. */
export function decodePremiumGiftPaymentOption(json: TdJsonRaw): PremiumGiftPaymentOption | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumGiftPaymentOption = new PremiumGiftPaymentOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.star_count = asNumber(record['star_count']);
  out.discount_percentage = asNumber(record['discount_percentage']);
  out.month_count = asNumber(record['month_count']);
  out.store_product_id = asString(record['store_product_id']);
  out.sticker = decodeSticker(record['sticker']);
  return out;
}

/** Encode a `premiumGiftPaymentOption` into TDLib JSON wire shape. */
export function encodePremiumGiftPaymentOption(value: PremiumGiftPaymentOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumGiftPaymentOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['star_count'] = value.star_count;
  out['discount_percentage'] = value.discount_percentage;
  out['month_count'] = value.month_count;
  out['store_product_id'] = value.store_product_id;
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  return out;
}

/** premiumGiftPaymentOptions = PremiumGiftPaymentOptions — Contains a list of options for gifting Telegram Premium to a user */
export class PremiumGiftPaymentOptions {
  type: 'premiumGiftPaymentOptions' = 'premiumGiftPaymentOptions';
  extra?: string;
  /** options: vector<premiumGiftPaymentOption> — The list of options sorted by Telegram Premium subscription duration */
  options: PremiumGiftPaymentOption[] = [];
}

/** Decode a TDLib `premiumGiftPaymentOptions` object; null input -> null. */
export function decodePremiumGiftPaymentOptions(json: TdJsonRaw): PremiumGiftPaymentOptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumGiftPaymentOptions = new PremiumGiftPaymentOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.options = decodeVector(record['options'], (item: TdJsonRaw): PremiumGiftPaymentOption => decodePremiumGiftPaymentOption(item) as PremiumGiftPaymentOption);
  return out;
}

/** Encode a `premiumGiftPaymentOptions` into TDLib JSON wire shape. */
export function encodePremiumGiftPaymentOptions(value: PremiumGiftPaymentOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumGiftPaymentOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['options'] = encodeVector(value.options, (item: PremiumGiftPaymentOption | null): TdJsonRaw => item === null ? null : encodePremiumGiftPaymentOption(item));
  return out;
}

/** premiumGiveawayPaymentOption = PremiumGiveawayPaymentOption — Describes an option for creating of Telegram Premium giveaway or manual distribution of Telegram Premium among chat members. Use telegramPaymentPurposePremiumGiftCodes or tele... */
export class PremiumGiveawayPaymentOption {
  type: 'premiumGiveawayPaymentOption' = 'premiumGiveawayPaymentOption';
  extra?: string;
  /** currency: string — ISO 4217 currency code for Telegram Premium gift code payment */
  currency: string = '';
  /** amount: int53 — The amount to pay, in the smallest units of the currency */
  amount: number = 0;
  /** winner_count: int32 — Number of users who will be able to activate the gift codes */
  winner_count: number = 0;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active */
  month_count: number = 0;
  /** store_product_id: string — Identifier of the store product associated with the option; may be empty if none */
  store_product_id: string = '';
  /** store_product_quantity: int32 — Number of times the store product must be paid */
  store_product_quantity: number = 0;
}

/** Decode a TDLib `premiumGiveawayPaymentOption` object; null input -> null. */
export function decodePremiumGiveawayPaymentOption(json: TdJsonRaw): PremiumGiveawayPaymentOption | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumGiveawayPaymentOption = new PremiumGiveawayPaymentOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.winner_count = asNumber(record['winner_count']);
  out.month_count = asNumber(record['month_count']);
  out.store_product_id = asString(record['store_product_id']);
  out.store_product_quantity = asNumber(record['store_product_quantity']);
  return out;
}

/** Encode a `premiumGiveawayPaymentOption` into TDLib JSON wire shape. */
export function encodePremiumGiveawayPaymentOption(value: PremiumGiveawayPaymentOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumGiveawayPaymentOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['winner_count'] = value.winner_count;
  out['month_count'] = value.month_count;
  out['store_product_id'] = value.store_product_id;
  out['store_product_quantity'] = value.store_product_quantity;
  return out;
}

/** premiumGiveawayPaymentOptions = PremiumGiveawayPaymentOptions — Contains a list of options for creating of Telegram Premium giveaway or manual distribution of Telegram Premium among chat members */
export class PremiumGiveawayPaymentOptions {
  type: 'premiumGiveawayPaymentOptions' = 'premiumGiveawayPaymentOptions';
  extra?: string;
  /** options: vector<premiumGiveawayPaymentOption> — The list of options */
  options: PremiumGiveawayPaymentOption[] = [];
}

/** Decode a TDLib `premiumGiveawayPaymentOptions` object; null input -> null. */
export function decodePremiumGiveawayPaymentOptions(json: TdJsonRaw): PremiumGiveawayPaymentOptions | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumGiveawayPaymentOptions = new PremiumGiveawayPaymentOptions();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.options = decodeVector(record['options'], (item: TdJsonRaw): PremiumGiveawayPaymentOption => decodePremiumGiveawayPaymentOption(item) as PremiumGiveawayPaymentOption);
  return out;
}

/** Encode a `premiumGiveawayPaymentOptions` into TDLib JSON wire shape. */
export function encodePremiumGiveawayPaymentOptions(value: PremiumGiveawayPaymentOptions): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumGiveawayPaymentOptions';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['options'] = encodeVector(value.options, (item: PremiumGiveawayPaymentOption | null): TdJsonRaw => item === null ? null : encodePremiumGiveawayPaymentOption(item));
  return out;
}

/** premiumLimit = PremiumLimit — Contains information about a limit, increased for Premium users */
export class PremiumLimit {
  type: 'premiumLimit' = 'premiumLimit';
  extra?: string;
  /** type: PremiumLimitType — The type of the limit */
  type_: TdPremiumLimitType | null = null;
  /** default_value: int32 — Default value of the limit */
  default_value: number = 0;
  /** premium_value: int32 — Value of the limit for Premium users */
  premium_value: number = 0;
}

/** Decode a TDLib `premiumLimit` object; null input -> null. */
export function decodePremiumLimit(json: TdJsonRaw): PremiumLimit | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimit = new PremiumLimit();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.type_ = decodeTdPremiumLimitType(record['type']);
  out.default_value = asNumber(record['default_value']);
  out.premium_value = asNumber(record['premium_value']);
  return out;
}

/** Encode a `premiumLimit` into TDLib JSON wire shape. */
export function encodePremiumLimit(value: PremiumLimit): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimit';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['type'] = value.type_ === null ? null : encodeTdPremiumLimitType(value.type_);
  out['default_value'] = value.default_value;
  out['premium_value'] = value.premium_value;
  return out;
}

/** premiumLimitTypeActiveStoryCount = PremiumLimitType — The maximum number of active stories */
export class PremiumLimitTypeActiveStoryCount {
  type: 'premiumLimitTypeActiveStoryCount' = 'premiumLimitTypeActiveStoryCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeActiveStoryCount` object; null input -> null. */
export function decodePremiumLimitTypeActiveStoryCount(json: TdJsonRaw): PremiumLimitTypeActiveStoryCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeActiveStoryCount = new PremiumLimitTypeActiveStoryCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeActiveStoryCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeActiveStoryCount(value: PremiumLimitTypeActiveStoryCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeActiveStoryCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeBioLength = PremiumLimitType — The maximum length of the user's bio */
export class PremiumLimitTypeBioLength {
  type: 'premiumLimitTypeBioLength' = 'premiumLimitTypeBioLength';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeBioLength` object; null input -> null. */
export function decodePremiumLimitTypeBioLength(json: TdJsonRaw): PremiumLimitTypeBioLength | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeBioLength = new PremiumLimitTypeBioLength();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeBioLength` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeBioLength(value: PremiumLimitTypeBioLength): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeBioLength';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeCaptionLength = PremiumLimitType — The maximum length of sent media caption */
export class PremiumLimitTypeCaptionLength {
  type: 'premiumLimitTypeCaptionLength' = 'premiumLimitTypeCaptionLength';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeCaptionLength` object; null input -> null. */
export function decodePremiumLimitTypeCaptionLength(json: TdJsonRaw): PremiumLimitTypeCaptionLength | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeCaptionLength = new PremiumLimitTypeCaptionLength();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeCaptionLength` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeCaptionLength(value: PremiumLimitTypeCaptionLength): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeCaptionLength';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeChatFolderChosenChatCount = PremiumLimitType — The maximum number of pinned and always included, or always excluded chats in a chat folder */
export class PremiumLimitTypeChatFolderChosenChatCount {
  type: 'premiumLimitTypeChatFolderChosenChatCount' = 'premiumLimitTypeChatFolderChosenChatCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeChatFolderChosenChatCount` object; null input -> null. */
export function decodePremiumLimitTypeChatFolderChosenChatCount(json: TdJsonRaw): PremiumLimitTypeChatFolderChosenChatCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeChatFolderChosenChatCount = new PremiumLimitTypeChatFolderChosenChatCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeChatFolderChosenChatCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeChatFolderChosenChatCount(value: PremiumLimitTypeChatFolderChosenChatCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeChatFolderChosenChatCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeChatFolderCount = PremiumLimitType — The maximum number of chat folders */
export class PremiumLimitTypeChatFolderCount {
  type: 'premiumLimitTypeChatFolderCount' = 'premiumLimitTypeChatFolderCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeChatFolderCount` object; null input -> null. */
export function decodePremiumLimitTypeChatFolderCount(json: TdJsonRaw): PremiumLimitTypeChatFolderCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeChatFolderCount = new PremiumLimitTypeChatFolderCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeChatFolderCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeChatFolderCount(value: PremiumLimitTypeChatFolderCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeChatFolderCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeChatFolderInviteLinkCount = PremiumLimitType — The maximum number of invite links for a chat folder */
export class PremiumLimitTypeChatFolderInviteLinkCount {
  type: 'premiumLimitTypeChatFolderInviteLinkCount' = 'premiumLimitTypeChatFolderInviteLinkCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeChatFolderInviteLinkCount` object; null input -> null. */
export function decodePremiumLimitTypeChatFolderInviteLinkCount(json: TdJsonRaw): PremiumLimitTypeChatFolderInviteLinkCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeChatFolderInviteLinkCount = new PremiumLimitTypeChatFolderInviteLinkCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeChatFolderInviteLinkCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeChatFolderInviteLinkCount(value: PremiumLimitTypeChatFolderInviteLinkCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeChatFolderInviteLinkCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeCreatedPublicChatCount = PremiumLimitType — The maximum number of created public chats */
export class PremiumLimitTypeCreatedPublicChatCount {
  type: 'premiumLimitTypeCreatedPublicChatCount' = 'premiumLimitTypeCreatedPublicChatCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeCreatedPublicChatCount` object; null input -> null. */
export function decodePremiumLimitTypeCreatedPublicChatCount(json: TdJsonRaw): PremiumLimitTypeCreatedPublicChatCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeCreatedPublicChatCount = new PremiumLimitTypeCreatedPublicChatCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeCreatedPublicChatCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeCreatedPublicChatCount(value: PremiumLimitTypeCreatedPublicChatCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeCreatedPublicChatCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeCustomTextCompositionStyleCount = PremiumLimitType — The maximum number of added text composition styles */
export class PremiumLimitTypeCustomTextCompositionStyleCount {
  type: 'premiumLimitTypeCustomTextCompositionStyleCount' = 'premiumLimitTypeCustomTextCompositionStyleCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeCustomTextCompositionStyleCount` object; null input -> null. */
export function decodePremiumLimitTypeCustomTextCompositionStyleCount(json: TdJsonRaw): PremiumLimitTypeCustomTextCompositionStyleCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeCustomTextCompositionStyleCount = new PremiumLimitTypeCustomTextCompositionStyleCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeCustomTextCompositionStyleCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeCustomTextCompositionStyleCount(value: PremiumLimitTypeCustomTextCompositionStyleCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeCustomTextCompositionStyleCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeFavoriteStickerCount = PremiumLimitType — The maximum number of favorite stickers */
export class PremiumLimitTypeFavoriteStickerCount {
  type: 'premiumLimitTypeFavoriteStickerCount' = 'premiumLimitTypeFavoriteStickerCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeFavoriteStickerCount` object; null input -> null. */
export function decodePremiumLimitTypeFavoriteStickerCount(json: TdJsonRaw): PremiumLimitTypeFavoriteStickerCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeFavoriteStickerCount = new PremiumLimitTypeFavoriteStickerCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeFavoriteStickerCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeFavoriteStickerCount(value: PremiumLimitTypeFavoriteStickerCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeFavoriteStickerCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeMessageTextLength = PremiumLimitType — The maximum length of text of sent messages */
export class PremiumLimitTypeMessageTextLength {
  type: 'premiumLimitTypeMessageTextLength' = 'premiumLimitTypeMessageTextLength';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeMessageTextLength` object; null input -> null. */
export function decodePremiumLimitTypeMessageTextLength(json: TdJsonRaw): PremiumLimitTypeMessageTextLength | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeMessageTextLength = new PremiumLimitTypeMessageTextLength();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeMessageTextLength` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeMessageTextLength(value: PremiumLimitTypeMessageTextLength): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeMessageTextLength';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeMonthlyPostedStoryCount = PremiumLimitType — The maximum number of stories posted per month */
export class PremiumLimitTypeMonthlyPostedStoryCount {
  type: 'premiumLimitTypeMonthlyPostedStoryCount' = 'premiumLimitTypeMonthlyPostedStoryCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeMonthlyPostedStoryCount` object; null input -> null. */
export function decodePremiumLimitTypeMonthlyPostedStoryCount(json: TdJsonRaw): PremiumLimitTypeMonthlyPostedStoryCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeMonthlyPostedStoryCount = new PremiumLimitTypeMonthlyPostedStoryCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeMonthlyPostedStoryCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeMonthlyPostedStoryCount(value: PremiumLimitTypeMonthlyPostedStoryCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeMonthlyPostedStoryCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeOwnedBotCount = PremiumLimitType — The maximum number of owned bots */
export class PremiumLimitTypeOwnedBotCount {
  type: 'premiumLimitTypeOwnedBotCount' = 'premiumLimitTypeOwnedBotCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeOwnedBotCount` object; null input -> null. */
export function decodePremiumLimitTypeOwnedBotCount(json: TdJsonRaw): PremiumLimitTypeOwnedBotCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeOwnedBotCount = new PremiumLimitTypeOwnedBotCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeOwnedBotCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeOwnedBotCount(value: PremiumLimitTypeOwnedBotCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeOwnedBotCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypePinnedArchivedChatCount = PremiumLimitType — The maximum number of pinned chats in the archive chat list */
export class PremiumLimitTypePinnedArchivedChatCount {
  type: 'premiumLimitTypePinnedArchivedChatCount' = 'premiumLimitTypePinnedArchivedChatCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypePinnedArchivedChatCount` object; null input -> null. */
export function decodePremiumLimitTypePinnedArchivedChatCount(json: TdJsonRaw): PremiumLimitTypePinnedArchivedChatCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypePinnedArchivedChatCount = new PremiumLimitTypePinnedArchivedChatCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypePinnedArchivedChatCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypePinnedArchivedChatCount(value: PremiumLimitTypePinnedArchivedChatCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypePinnedArchivedChatCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypePinnedChatCount = PremiumLimitType — The maximum number of pinned chats in the main chat list */
export class PremiumLimitTypePinnedChatCount {
  type: 'premiumLimitTypePinnedChatCount' = 'premiumLimitTypePinnedChatCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypePinnedChatCount` object; null input -> null. */
export function decodePremiumLimitTypePinnedChatCount(json: TdJsonRaw): PremiumLimitTypePinnedChatCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypePinnedChatCount = new PremiumLimitTypePinnedChatCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypePinnedChatCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypePinnedChatCount(value: PremiumLimitTypePinnedChatCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypePinnedChatCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypePinnedSavedMessagesTopicCount = PremiumLimitType — The maximum number of pinned Saved Messages topics */
export class PremiumLimitTypePinnedSavedMessagesTopicCount {
  type: 'premiumLimitTypePinnedSavedMessagesTopicCount' = 'premiumLimitTypePinnedSavedMessagesTopicCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypePinnedSavedMessagesTopicCount` object; null input -> null. */
export function decodePremiumLimitTypePinnedSavedMessagesTopicCount(json: TdJsonRaw): PremiumLimitTypePinnedSavedMessagesTopicCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypePinnedSavedMessagesTopicCount = new PremiumLimitTypePinnedSavedMessagesTopicCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypePinnedSavedMessagesTopicCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypePinnedSavedMessagesTopicCount(value: PremiumLimitTypePinnedSavedMessagesTopicCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypePinnedSavedMessagesTopicCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeSavedAnimationCount = PremiumLimitType — The maximum number of saved animations */
export class PremiumLimitTypeSavedAnimationCount {
  type: 'premiumLimitTypeSavedAnimationCount' = 'premiumLimitTypeSavedAnimationCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeSavedAnimationCount` object; null input -> null. */
export function decodePremiumLimitTypeSavedAnimationCount(json: TdJsonRaw): PremiumLimitTypeSavedAnimationCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeSavedAnimationCount = new PremiumLimitTypeSavedAnimationCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeSavedAnimationCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeSavedAnimationCount(value: PremiumLimitTypeSavedAnimationCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeSavedAnimationCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeShareableChatFolderCount = PremiumLimitType — The maximum number of added shareable chat folders */
export class PremiumLimitTypeShareableChatFolderCount {
  type: 'premiumLimitTypeShareableChatFolderCount' = 'premiumLimitTypeShareableChatFolderCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeShareableChatFolderCount` object; null input -> null. */
export function decodePremiumLimitTypeShareableChatFolderCount(json: TdJsonRaw): PremiumLimitTypeShareableChatFolderCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeShareableChatFolderCount = new PremiumLimitTypeShareableChatFolderCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeShareableChatFolderCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeShareableChatFolderCount(value: PremiumLimitTypeShareableChatFolderCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeShareableChatFolderCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeSimilarChatCount = PremiumLimitType — The maximum number of received similar chats */
export class PremiumLimitTypeSimilarChatCount {
  type: 'premiumLimitTypeSimilarChatCount' = 'premiumLimitTypeSimilarChatCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeSimilarChatCount` object; null input -> null. */
export function decodePremiumLimitTypeSimilarChatCount(json: TdJsonRaw): PremiumLimitTypeSimilarChatCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeSimilarChatCount = new PremiumLimitTypeSimilarChatCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeSimilarChatCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeSimilarChatCount(value: PremiumLimitTypeSimilarChatCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeSimilarChatCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeStoryCaptionLength = PremiumLimitType — The maximum length of captions of posted stories */
export class PremiumLimitTypeStoryCaptionLength {
  type: 'premiumLimitTypeStoryCaptionLength' = 'premiumLimitTypeStoryCaptionLength';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeStoryCaptionLength` object; null input -> null. */
export function decodePremiumLimitTypeStoryCaptionLength(json: TdJsonRaw): PremiumLimitTypeStoryCaptionLength | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeStoryCaptionLength = new PremiumLimitTypeStoryCaptionLength();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeStoryCaptionLength` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeStoryCaptionLength(value: PremiumLimitTypeStoryCaptionLength): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeStoryCaptionLength';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeStorySuggestedReactionAreaCount = PremiumLimitType — The maximum number of suggested reaction areas on a story */
export class PremiumLimitTypeStorySuggestedReactionAreaCount {
  type: 'premiumLimitTypeStorySuggestedReactionAreaCount' = 'premiumLimitTypeStorySuggestedReactionAreaCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeStorySuggestedReactionAreaCount` object; null input -> null. */
export function decodePremiumLimitTypeStorySuggestedReactionAreaCount(json: TdJsonRaw): PremiumLimitTypeStorySuggestedReactionAreaCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeStorySuggestedReactionAreaCount = new PremiumLimitTypeStorySuggestedReactionAreaCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeStorySuggestedReactionAreaCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeStorySuggestedReactionAreaCount(value: PremiumLimitTypeStorySuggestedReactionAreaCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeStorySuggestedReactionAreaCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeSupergroupCount = PremiumLimitType — The maximum number of joined supergroups and channels */
export class PremiumLimitTypeSupergroupCount {
  type: 'premiumLimitTypeSupergroupCount' = 'premiumLimitTypeSupergroupCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeSupergroupCount` object; null input -> null. */
export function decodePremiumLimitTypeSupergroupCount(json: TdJsonRaw): PremiumLimitTypeSupergroupCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeSupergroupCount = new PremiumLimitTypeSupergroupCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeSupergroupCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeSupergroupCount(value: PremiumLimitTypeSupergroupCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeSupergroupCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumLimitTypeWeeklyPostedStoryCount = PremiumLimitType — The maximum number of stories posted per week */
export class PremiumLimitTypeWeeklyPostedStoryCount {
  type: 'premiumLimitTypeWeeklyPostedStoryCount' = 'premiumLimitTypeWeeklyPostedStoryCount';
  extra?: string;
}

/** Decode a TDLib `premiumLimitTypeWeeklyPostedStoryCount` object; null input -> null. */
export function decodePremiumLimitTypeWeeklyPostedStoryCount(json: TdJsonRaw): PremiumLimitTypeWeeklyPostedStoryCount | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumLimitTypeWeeklyPostedStoryCount = new PremiumLimitTypeWeeklyPostedStoryCount();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumLimitTypeWeeklyPostedStoryCount` into TDLib JSON wire shape. */
export function encodePremiumLimitTypeWeeklyPostedStoryCount(value: PremiumLimitTypeWeeklyPostedStoryCount): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumLimitTypeWeeklyPostedStoryCount';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumPaymentOption = PremiumPaymentOption — Describes an option for buying Telegram Premium to a user */
export class PremiumPaymentOption {
  type: 'premiumPaymentOption' = 'premiumPaymentOption';
  extra?: string;
  /** currency: string — ISO 4217 currency code for Telegram Premium subscription payment */
  currency: string = '';
  /** amount: int53 — The amount to pay, in the smallest units of the currency */
  amount: number = 0;
  /** discount_percentage: int32 — The discount associated with this option, as a percentage */
  discount_percentage: number = 0;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active. Use getPremiumInfoSticker to get the sticker to be used as representation of the Telegram Premium subscription */
  month_count: number = 0;
  /** store_product_id: string — Identifier of the store product associated with the option */
  store_product_id: string = '';
  /** payment_link: InternalLinkType — An internal link to be opened for buying Telegram Premium to the user if store payment isn't possible; may be null if direct payment isn't available */
  payment_link: TdInternalLinkType | null = null;
}

/** Decode a TDLib `premiumPaymentOption` object; null input -> null. */
export function decodePremiumPaymentOption(json: TdJsonRaw): PremiumPaymentOption | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumPaymentOption = new PremiumPaymentOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.currency = asString(record['currency']);
  out.amount = asNumber(record['amount']);
  out.discount_percentage = asNumber(record['discount_percentage']);
  out.month_count = asNumber(record['month_count']);
  out.store_product_id = asString(record['store_product_id']);
  out.payment_link = decodeTdInternalLinkType(record['payment_link']);
  return out;
}

/** Encode a `premiumPaymentOption` into TDLib JSON wire shape. */
export function encodePremiumPaymentOption(value: PremiumPaymentOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumPaymentOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['currency'] = value.currency;
  out['amount'] = value.amount;
  out['discount_percentage'] = value.discount_percentage;
  out['month_count'] = value.month_count;
  out['store_product_id'] = value.store_product_id;
  out['payment_link'] = value.payment_link === null ? null : encodeTdInternalLinkType(value.payment_link);
  return out;
}

/** premiumSourceBusinessFeature = PremiumSource — A user tried to use a Business feature */
export class PremiumSourceBusinessFeature {
  type: 'premiumSourceBusinessFeature' = 'premiumSourceBusinessFeature';
  extra?: string;
  /** feature: BusinessFeature — The used feature; pass null if none specific feature was used */
  feature: TdBusinessFeature | null = null;
}

/** Decode a TDLib `premiumSourceBusinessFeature` object; null input -> null. */
export function decodePremiumSourceBusinessFeature(json: TdJsonRaw): PremiumSourceBusinessFeature | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumSourceBusinessFeature = new PremiumSourceBusinessFeature();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.feature = decodeTdBusinessFeature(record['feature']);
  return out;
}

/** Encode a `premiumSourceBusinessFeature` into TDLib JSON wire shape. */
export function encodePremiumSourceBusinessFeature(value: PremiumSourceBusinessFeature): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumSourceBusinessFeature';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['feature'] = value.feature === null ? null : encodeTdBusinessFeature(value.feature);
  return out;
}

/** premiumSourceFeature = PremiumSource — A user tried to use a Premium feature */
export class PremiumSourceFeature {
  type: 'premiumSourceFeature' = 'premiumSourceFeature';
  extra?: string;
  /** feature: PremiumFeature — The used feature */
  feature: TdPremiumFeature | null = null;
}

/** Decode a TDLib `premiumSourceFeature` object; null input -> null. */
export function decodePremiumSourceFeature(json: TdJsonRaw): PremiumSourceFeature | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumSourceFeature = new PremiumSourceFeature();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.feature = decodeTdPremiumFeature(record['feature']);
  return out;
}

/** Encode a `premiumSourceFeature` into TDLib JSON wire shape. */
export function encodePremiumSourceFeature(value: PremiumSourceFeature): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumSourceFeature';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['feature'] = value.feature === null ? null : encodeTdPremiumFeature(value.feature);
  return out;
}

/** premiumSourceLimitExceeded = PremiumSource — A limit was exceeded */
export class PremiumSourceLimitExceeded {
  type: 'premiumSourceLimitExceeded' = 'premiumSourceLimitExceeded';
  extra?: string;
  /** limit_type: PremiumLimitType — Type of the exceeded limit */
  limit_type: TdPremiumLimitType | null = null;
}

/** Decode a TDLib `premiumSourceLimitExceeded` object; null input -> null. */
export function decodePremiumSourceLimitExceeded(json: TdJsonRaw): PremiumSourceLimitExceeded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumSourceLimitExceeded = new PremiumSourceLimitExceeded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.limit_type = decodeTdPremiumLimitType(record['limit_type']);
  return out;
}

/** Encode a `premiumSourceLimitExceeded` into TDLib JSON wire shape. */
export function encodePremiumSourceLimitExceeded(value: PremiumSourceLimitExceeded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumSourceLimitExceeded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['limit_type'] = value.limit_type === null ? null : encodeTdPremiumLimitType(value.limit_type);
  return out;
}

/** premiumSourceLink = PremiumSource — A user opened an internal link of the type internalLinkTypePremiumFeaturesPage */
export class PremiumSourceLink {
  type: 'premiumSourceLink' = 'premiumSourceLink';
  extra?: string;
  /** referrer: string — The referrer from the link */
  referrer: string = '';
}

/** Decode a TDLib `premiumSourceLink` object; null input -> null. */
export function decodePremiumSourceLink(json: TdJsonRaw): PremiumSourceLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumSourceLink = new PremiumSourceLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.referrer = asString(record['referrer']);
  return out;
}

/** Encode a `premiumSourceLink` into TDLib JSON wire shape. */
export function encodePremiumSourceLink(value: PremiumSourceLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumSourceLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['referrer'] = value.referrer;
  return out;
}

/** premiumSourceSettings = PremiumSource — A user opened the Premium features screen from settings */
export class PremiumSourceSettings {
  type: 'premiumSourceSettings' = 'premiumSourceSettings';
  extra?: string;
}

/** Decode a TDLib `premiumSourceSettings` object; null input -> null. */
export function decodePremiumSourceSettings(json: TdJsonRaw): PremiumSourceSettings | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumSourceSettings = new PremiumSourceSettings();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumSourceSettings` into TDLib JSON wire shape. */
export function encodePremiumSourceSettings(value: PremiumSourceSettings): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumSourceSettings';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumSourceStoryFeature = PremiumSource — A user tried to use a Premium story feature */
export class PremiumSourceStoryFeature {
  type: 'premiumSourceStoryFeature' = 'premiumSourceStoryFeature';
  extra?: string;
  /** feature: PremiumStoryFeature — The used feature */
  feature: TdPremiumStoryFeature | null = null;
}

/** Decode a TDLib `premiumSourceStoryFeature` object; null input -> null. */
export function decodePremiumSourceStoryFeature(json: TdJsonRaw): PremiumSourceStoryFeature | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumSourceStoryFeature = new PremiumSourceStoryFeature();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.feature = decodeTdPremiumStoryFeature(record['feature']);
  return out;
}

/** Encode a `premiumSourceStoryFeature` into TDLib JSON wire shape. */
export function encodePremiumSourceStoryFeature(value: PremiumSourceStoryFeature): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumSourceStoryFeature';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['feature'] = value.feature === null ? null : encodeTdPremiumStoryFeature(value.feature);
  return out;
}

/** premiumState = PremiumState — Contains state of Telegram Premium subscription and promotion videos for Premium features */
export class PremiumState {
  type: 'premiumState' = 'premiumState';
  extra?: string;
  /** state: formattedText — Text description of the state of the current Premium subscription; may be empty if the current user has no Telegram Premium subscription */
  state: FormattedText | null = null;
  /** payment_options: vector<premiumStatePaymentOption> — The list of available options for buying Telegram Premium */
  payment_options: PremiumStatePaymentOption[] = [];
  /** animations: vector<premiumFeaturePromotionAnimation> — The list of available promotion animations for Premium features */
  animations: PremiumFeaturePromotionAnimation[] = [];
  /** business_animations: vector<businessFeaturePromotionAnimation> — The list of available promotion animations for Business features */
  business_animations: BusinessFeaturePromotionAnimation[] = [];
}

/** Decode a TDLib `premiumState` object; null input -> null. */
export function decodePremiumState(json: TdJsonRaw): PremiumState | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumState = new PremiumState();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.state = decodeFormattedText(record['state']);
  out.payment_options = decodeVector(record['payment_options'], (item: TdJsonRaw): PremiumStatePaymentOption => decodePremiumStatePaymentOption(item) as PremiumStatePaymentOption);
  out.animations = decodeVector(record['animations'], (item: TdJsonRaw): PremiumFeaturePromotionAnimation => decodePremiumFeaturePromotionAnimation(item) as PremiumFeaturePromotionAnimation);
  out.business_animations = decodeVector(record['business_animations'], (item: TdJsonRaw): BusinessFeaturePromotionAnimation => decodeBusinessFeaturePromotionAnimation(item) as BusinessFeaturePromotionAnimation);
  return out;
}

/** Encode a `premiumState` into TDLib JSON wire shape. */
export function encodePremiumState(value: PremiumState): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumState';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['state'] = value.state === null ? null : encodeFormattedText(value.state);
  out['payment_options'] = encodeVector(value.payment_options, (item: PremiumStatePaymentOption | null): TdJsonRaw => item === null ? null : encodePremiumStatePaymentOption(item));
  out['animations'] = encodeVector(value.animations, (item: PremiumFeaturePromotionAnimation | null): TdJsonRaw => item === null ? null : encodePremiumFeaturePromotionAnimation(item));
  out['business_animations'] = encodeVector(value.business_animations, (item: BusinessFeaturePromotionAnimation | null): TdJsonRaw => item === null ? null : encodeBusinessFeaturePromotionAnimation(item));
  return out;
}

/** premiumStatePaymentOption = PremiumStatePaymentOption — Describes an option for buying or upgrading Telegram Premium for self */
export class PremiumStatePaymentOption {
  type: 'premiumStatePaymentOption' = 'premiumStatePaymentOption';
  extra?: string;
  /** payment_option: premiumPaymentOption — Information about the payment option */
  payment_option: PremiumPaymentOption | null = null;
  /** is_current: Bool — True, if this is the currently used Telegram Premium subscription option */
  is_current: boolean = false;
  /** is_upgrade: Bool — True, if the payment option can be used to upgrade the existing Telegram Premium subscription */
  is_upgrade: boolean = false;
  /** last_transaction_id: string — Identifier of the last in-store transaction for the currently used option */
  last_transaction_id: string = '';
}

/** Decode a TDLib `premiumStatePaymentOption` object; null input -> null. */
export function decodePremiumStatePaymentOption(json: TdJsonRaw): PremiumStatePaymentOption | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumStatePaymentOption = new PremiumStatePaymentOption();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.payment_option = decodePremiumPaymentOption(record['payment_option']);
  out.is_current = asBool(record['is_current']);
  out.is_upgrade = asBool(record['is_upgrade']);
  out.last_transaction_id = asString(record['last_transaction_id']);
  return out;
}

/** Encode a `premiumStatePaymentOption` into TDLib JSON wire shape. */
export function encodePremiumStatePaymentOption(value: PremiumStatePaymentOption): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumStatePaymentOption';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['payment_option'] = value.payment_option === null ? null : encodePremiumPaymentOption(value.payment_option);
  out['is_current'] = value.is_current;
  out['is_upgrade'] = value.is_upgrade;
  out['last_transaction_id'] = value.last_transaction_id;
  return out;
}

/** premiumStoryFeatureCustomExpirationDuration = PremiumStoryFeature — The ability to set custom expiration duration for stories */
export class PremiumStoryFeatureCustomExpirationDuration {
  type: 'premiumStoryFeatureCustomExpirationDuration' = 'premiumStoryFeatureCustomExpirationDuration';
  extra?: string;
}

/** Decode a TDLib `premiumStoryFeatureCustomExpirationDuration` object; null input -> null. */
export function decodePremiumStoryFeatureCustomExpirationDuration(json: TdJsonRaw): PremiumStoryFeatureCustomExpirationDuration | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumStoryFeatureCustomExpirationDuration = new PremiumStoryFeatureCustomExpirationDuration();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumStoryFeatureCustomExpirationDuration` into TDLib JSON wire shape. */
export function encodePremiumStoryFeatureCustomExpirationDuration(value: PremiumStoryFeatureCustomExpirationDuration): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumStoryFeatureCustomExpirationDuration';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumStoryFeatureLinksAndFormatting = PremiumStoryFeature — The ability to use links and formatting in story caption, and use inputStoryAreaTypeLink areas */
export class PremiumStoryFeatureLinksAndFormatting {
  type: 'premiumStoryFeatureLinksAndFormatting' = 'premiumStoryFeatureLinksAndFormatting';
  extra?: string;
}

/** Decode a TDLib `premiumStoryFeatureLinksAndFormatting` object; null input -> null. */
export function decodePremiumStoryFeatureLinksAndFormatting(json: TdJsonRaw): PremiumStoryFeatureLinksAndFormatting | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumStoryFeatureLinksAndFormatting = new PremiumStoryFeatureLinksAndFormatting();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumStoryFeatureLinksAndFormatting` into TDLib JSON wire shape. */
export function encodePremiumStoryFeatureLinksAndFormatting(value: PremiumStoryFeatureLinksAndFormatting): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumStoryFeatureLinksAndFormatting';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumStoryFeaturePermanentViewsHistory = PremiumStoryFeature — The ability to check who opened the current user's stories after they expire */
export class PremiumStoryFeaturePermanentViewsHistory {
  type: 'premiumStoryFeaturePermanentViewsHistory' = 'premiumStoryFeaturePermanentViewsHistory';
  extra?: string;
}

/** Decode a TDLib `premiumStoryFeaturePermanentViewsHistory` object; null input -> null. */
export function decodePremiumStoryFeaturePermanentViewsHistory(json: TdJsonRaw): PremiumStoryFeaturePermanentViewsHistory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumStoryFeaturePermanentViewsHistory = new PremiumStoryFeaturePermanentViewsHistory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumStoryFeaturePermanentViewsHistory` into TDLib JSON wire shape. */
export function encodePremiumStoryFeaturePermanentViewsHistory(value: PremiumStoryFeaturePermanentViewsHistory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumStoryFeaturePermanentViewsHistory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumStoryFeaturePriorityOrder = PremiumStoryFeature — Stories of the current user are displayed before stories of non-Premium contacts, supergroups, and channels */
export class PremiumStoryFeaturePriorityOrder {
  type: 'premiumStoryFeaturePriorityOrder' = 'premiumStoryFeaturePriorityOrder';
  extra?: string;
}

/** Decode a TDLib `premiumStoryFeaturePriorityOrder` object; null input -> null. */
export function decodePremiumStoryFeaturePriorityOrder(json: TdJsonRaw): PremiumStoryFeaturePriorityOrder | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumStoryFeaturePriorityOrder = new PremiumStoryFeaturePriorityOrder();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumStoryFeaturePriorityOrder` into TDLib JSON wire shape. */
export function encodePremiumStoryFeaturePriorityOrder(value: PremiumStoryFeaturePriorityOrder): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumStoryFeaturePriorityOrder';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumStoryFeatureSaveStories = PremiumStoryFeature — The ability to save other's unprotected stories */
export class PremiumStoryFeatureSaveStories {
  type: 'premiumStoryFeatureSaveStories' = 'premiumStoryFeatureSaveStories';
  extra?: string;
}

/** Decode a TDLib `premiumStoryFeatureSaveStories` object; null input -> null. */
export function decodePremiumStoryFeatureSaveStories(json: TdJsonRaw): PremiumStoryFeatureSaveStories | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumStoryFeatureSaveStories = new PremiumStoryFeatureSaveStories();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumStoryFeatureSaveStories` into TDLib JSON wire shape. */
export function encodePremiumStoryFeatureSaveStories(value: PremiumStoryFeatureSaveStories): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumStoryFeatureSaveStories';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumStoryFeatureStealthMode = PremiumStoryFeature — The ability to hide the fact that the user viewed other's stories */
export class PremiumStoryFeatureStealthMode {
  type: 'premiumStoryFeatureStealthMode' = 'premiumStoryFeatureStealthMode';
  extra?: string;
}

/** Decode a TDLib `premiumStoryFeatureStealthMode` object; null input -> null. */
export function decodePremiumStoryFeatureStealthMode(json: TdJsonRaw): PremiumStoryFeatureStealthMode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumStoryFeatureStealthMode = new PremiumStoryFeatureStealthMode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumStoryFeatureStealthMode` into TDLib JSON wire shape. */
export function encodePremiumStoryFeatureStealthMode(value: PremiumStoryFeatureStealthMode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumStoryFeatureStealthMode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** premiumStoryFeatureVideoQuality = PremiumStoryFeature — The ability to choose better quality for viewed stories */
export class PremiumStoryFeatureVideoQuality {
  type: 'premiumStoryFeatureVideoQuality' = 'premiumStoryFeatureVideoQuality';
  extra?: string;
}

/** Decode a TDLib `premiumStoryFeatureVideoQuality` object; null input -> null. */
export function decodePremiumStoryFeatureVideoQuality(json: TdJsonRaw): PremiumStoryFeatureVideoQuality | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PremiumStoryFeatureVideoQuality = new PremiumStoryFeatureVideoQuality();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `premiumStoryFeatureVideoQuality` into TDLib JSON wire shape. */
export function encodePremiumStoryFeatureVideoQuality(value: PremiumStoryFeatureVideoQuality): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'premiumStoryFeatureVideoQuality';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** prepaidGiveaway = PrepaidGiveaway — Describes a prepaid giveaway */
export class PrepaidGiveaway {
  type: 'prepaidGiveaway' = 'prepaidGiveaway';
  extra?: string;
  /** id: int64 — Unique identifier of the prepaid giveaway */
  id: string = '';
  /** winner_count: int32 — Number of users who will receive giveaway prize */
  winner_count: number = 0;
  /** prize: GiveawayPrize — Prize of the giveaway */
  prize: TdGiveawayPrize | null = null;
  /** boost_count: int32 — The number of boosts received by the chat from the giveaway; for Telegram Star giveaways only */
  boost_count: number = 0;
  /** payment_date: int32 — Point in time (Unix timestamp) when the giveaway was paid */
  payment_date: number = 0;
}

/** Decode a TDLib `prepaidGiveaway` object; null input -> null. */
export function decodePrepaidGiveaway(json: TdJsonRaw): PrepaidGiveaway | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PrepaidGiveaway = new PrepaidGiveaway();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.winner_count = asNumber(record['winner_count']);
  out.prize = decodeTdGiveawayPrize(record['prize']);
  out.boost_count = asNumber(record['boost_count']);
  out.payment_date = asNumber(record['payment_date']);
  return out;
}

/** Encode a `prepaidGiveaway` into TDLib JSON wire shape. */
export function encodePrepaidGiveaway(value: PrepaidGiveaway): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'prepaidGiveaway';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['winner_count'] = value.winner_count;
  out['prize'] = value.prize === null ? null : encodeTdGiveawayPrize(value.prize);
  out['boost_count'] = value.boost_count;
  out['payment_date'] = value.payment_date;
  return out;
}

/** preparedInlineMessage = PreparedInlineMessage — Represents a ready to send inline message. Use sendInlineQueryResultMessage to send the message */
export class PreparedInlineMessage {
  type: 'preparedInlineMessage' = 'preparedInlineMessage';
  extra?: string;
  /** inline_query_id: int64 — Unique identifier of the inline query to pass to sendInlineQueryResultMessage */
  inline_query_id: string = '';
  /** result: InlineQueryResult — Resulted inline message of the query */
  result: TdInlineQueryResult | null = null;
  /** chat_types: targetChatTypes — Types of the chats to which the message can be sent */
  chat_types: TargetChatTypes | null = null;
}

/** Decode a TDLib `preparedInlineMessage` object; null input -> null. */
export function decodePreparedInlineMessage(json: TdJsonRaw): PreparedInlineMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PreparedInlineMessage = new PreparedInlineMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.inline_query_id = asInt64(record['inline_query_id']);
  out.result = decodeTdInlineQueryResult(record['result']);
  out.chat_types = decodeTargetChatTypes(record['chat_types']);
  return out;
}

/** Encode a `preparedInlineMessage` into TDLib JSON wire shape. */
export function encodePreparedInlineMessage(value: PreparedInlineMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'preparedInlineMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['inline_query_id'] = value.inline_query_id;
  out['result'] = value.result === null ? null : encodeTdInlineQueryResult(value.result);
  out['chat_types'] = value.chat_types === null ? null : encodeTargetChatTypes(value.chat_types);
  return out;
}

/** preparedInlineMessageId = PreparedInlineMessageId — Represents an inline message that can be sent via the bot */
export class PreparedInlineMessageId {
  type: 'preparedInlineMessageId' = 'preparedInlineMessageId';
  extra?: string;
  /** id: string — Unique identifier for the message */
  id: string = '';
  /** expiration_date: int32 — Point in time (Unix timestamp) when the message can't be used anymore */
  expiration_date: number = 0;
}

/** Decode a TDLib `preparedInlineMessageId` object; null input -> null. */
export function decodePreparedInlineMessageId(json: TdJsonRaw): PreparedInlineMessageId | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PreparedInlineMessageId = new PreparedInlineMessageId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asString(record['id']);
  out.expiration_date = asNumber(record['expiration_date']);
  return out;
}

/** Encode a `preparedInlineMessageId` into TDLib JSON wire shape. */
export function encodePreparedInlineMessageId(value: PreparedInlineMessageId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'preparedInlineMessageId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['expiration_date'] = value.expiration_date;
  return out;
}

/** processChatFolderNewChats = Ok — Process new chats added to a shareable chat folder by its owner */
export class ProcessChatFolderNewChats {
  type: 'processChatFolderNewChats' = 'processChatFolderNewChats';
  extra?: string;
  /** chat_folder_id: int32 — Chat folder identifier */
  chat_folder_id: number = 0;
  /** added_chat_ids: vector<int53> — Identifiers of the new chats, which are added to the chat folder. The chats are automatically joined if they aren't joined yet */
  added_chat_ids: number[] = [];
}

/** Decode a TDLib `processChatFolderNewChats` object; null input -> null. */
export function decodeProcessChatFolderNewChats(json: TdJsonRaw): ProcessChatFolderNewChats | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProcessChatFolderNewChats = new ProcessChatFolderNewChats();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_folder_id = asNumber(record['chat_folder_id']);
  out.added_chat_ids = decodeVector(record['added_chat_ids'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `processChatFolderNewChats` into TDLib JSON wire shape. */
export function encodeProcessChatFolderNewChats(value: ProcessChatFolderNewChats): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'processChatFolderNewChats';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_folder_id'] = value.chat_folder_id;
  out['added_chat_ids'] = encodeVector(value.added_chat_ids, (item: number): TdJsonRaw => item);
  return out;
}

/** processChatHasProtectedContentDisableRequest = Ok — Processes request to disable has_protected_content in a chat */
export class ProcessChatHasProtectedContentDisableRequest {
  type: 'processChatHasProtectedContentDisableRequest' = 'processChatHasProtectedContentDisableRequest';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** request_message_id: int53 — Identifier of the message with the request. The message must be incoming and has content of the type messageChatHasProtectedContentDisableRequested */
  request_message_id: number = 0;
  /** approve: Bool — Pass true to approve the request; pass false to reject the request */
  approve: boolean = false;
}

/** Decode a TDLib `processChatHasProtectedContentDisableRequest` object; null input -> null. */
export function decodeProcessChatHasProtectedContentDisableRequest(json: TdJsonRaw): ProcessChatHasProtectedContentDisableRequest | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProcessChatHasProtectedContentDisableRequest = new ProcessChatHasProtectedContentDisableRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.request_message_id = asNumber(record['request_message_id']);
  out.approve = asBool(record['approve']);
  return out;
}

/** Encode a `processChatHasProtectedContentDisableRequest` into TDLib JSON wire shape. */
export function encodeProcessChatHasProtectedContentDisableRequest(value: ProcessChatHasProtectedContentDisableRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'processChatHasProtectedContentDisableRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['request_message_id'] = value.request_message_id;
  out['approve'] = value.approve;
  return out;
}

/** processChatJoinRequest = Ok — Handles a pending join request in a chat */
export class ProcessChatJoinRequest {
  type: 'processChatJoinRequest' = 'processChatJoinRequest';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** user_id: int53 — Identifier of the user who sent the request */
  user_id: number = 0;
  /** approve: Bool — Pass true to approve the request; pass false to decline it */
  approve: boolean = false;
}

/** Decode a TDLib `processChatJoinRequest` object; null input -> null. */
export function decodeProcessChatJoinRequest(json: TdJsonRaw): ProcessChatJoinRequest | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProcessChatJoinRequest = new ProcessChatJoinRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.user_id = asNumber(record['user_id']);
  out.approve = asBool(record['approve']);
  return out;
}

/** Encode a `processChatJoinRequest` into TDLib JSON wire shape. */
export function encodeProcessChatJoinRequest(value: ProcessChatJoinRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'processChatJoinRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['user_id'] = value.user_id;
  out['approve'] = value.approve;
  return out;
}

/** processChatJoinRequests = Ok — Handles all pending join requests for a given link in a chat */
export class ProcessChatJoinRequests {
  type: 'processChatJoinRequests' = 'processChatJoinRequests';
  extra?: string;
  /** chat_id: int53 — Chat identifier */
  chat_id: number = 0;
  /** invite_link: string — Invite link for which to process join requests. If empty, all join requests will be processed. Requires administrator privileges and can_invite_users right in the chat for own links and owner privileges for other links */
  invite_link: string = '';
  /** approve: Bool — Pass true to approve all requests; pass false to decline them */
  approve: boolean = false;
}

/** Decode a TDLib `processChatJoinRequests` object; null input -> null. */
export function decodeProcessChatJoinRequests(json: TdJsonRaw): ProcessChatJoinRequests | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProcessChatJoinRequests = new ProcessChatJoinRequests();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.chat_id = asNumber(record['chat_id']);
  out.invite_link = asString(record['invite_link']);
  out.approve = asBool(record['approve']);
  return out;
}

/** Encode a `processChatJoinRequests` into TDLib JSON wire shape. */
export function encodeProcessChatJoinRequests(value: ProcessChatJoinRequests): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'processChatJoinRequests';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['chat_id'] = value.chat_id;
  out['invite_link'] = value.invite_link;
  out['approve'] = value.approve;
  return out;
}

/** processGiftPurchaseOffer = Ok — Handles a pending gift purchase offer */
export class ProcessGiftPurchaseOffer {
  type: 'processGiftPurchaseOffer' = 'processGiftPurchaseOffer';
  extra?: string;
  /** message_id: int53 — Identifier of the message with the gift purchase offer */
  message_id: number = 0;
  /** accept: Bool — Pass true to accept the request; pass false to reject it */
  accept: boolean = false;
}

/** Decode a TDLib `processGiftPurchaseOffer` object; null input -> null. */
export function decodeProcessGiftPurchaseOffer(json: TdJsonRaw): ProcessGiftPurchaseOffer | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProcessGiftPurchaseOffer = new ProcessGiftPurchaseOffer();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message_id = asNumber(record['message_id']);
  out.accept = asBool(record['accept']);
  return out;
}

/** Encode a `processGiftPurchaseOffer` into TDLib JSON wire shape. */
export function encodeProcessGiftPurchaseOffer(value: ProcessGiftPurchaseOffer): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'processGiftPurchaseOffer';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message_id'] = value.message_id;
  out['accept'] = value.accept;
  return out;
}

/** processPushNotification = Ok — Handles a push notification. Returns error with code 406 if the push notification is not supported and connection to the server is required to fetch new data. Can be called before authorization */
export class ProcessPushNotification {
  type: 'processPushNotification' = 'processPushNotification';
  extra?: string;
  /** payload: string — JSON-encoded push notification payload with all fields sent by the server, and "google.sent_time" and "google.notification.sound" fields added */
  payload: string = '';
}

/** Decode a TDLib `processPushNotification` object; null input -> null. */
export function decodeProcessPushNotification(json: TdJsonRaw): ProcessPushNotification | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProcessPushNotification = new ProcessPushNotification();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.payload = asString(record['payload']);
  return out;
}

/** Encode a `processPushNotification` into TDLib JSON wire shape. */
export function encodeProcessPushNotification(value: ProcessPushNotification): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'processPushNotification';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['payload'] = value.payload;
  return out;
}

/** productInfo = ProductInfo — Contains information about a product that can be paid with invoice */
export class ProductInfo {
  type: 'productInfo' = 'productInfo';
  extra?: string;
  /** title: string — Product title */
  title: string = '';
  /** description: formattedText */
  description: FormattedText | null = null;
  /** photo: photo — Product photo; may be null */
  photo: Photo | null = null;
}

/** Decode a TDLib `productInfo` object; null input -> null. */
export function decodeProductInfo(json: TdJsonRaw): ProductInfo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProductInfo = new ProductInfo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.description = decodeFormattedText(record['description']);
  out.photo = decodePhoto(record['photo']);
  return out;
}

/** Encode a `productInfo` into TDLib JSON wire shape. */
export function encodeProductInfo(value: ProductInfo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'productInfo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['description'] = value.description === null ? null : encodeFormattedText(value.description);
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  return out;
}

/** profileAccentColor = ProfileAccentColor — Contains information about supported accent color for user profile photo background */
export class ProfileAccentColor {
  type: 'profileAccentColor' = 'profileAccentColor';
  extra?: string;
  /** id: int32 — Profile accent color identifier */
  id: number = 0;
  /** light_theme_colors: profileAccentColors — Accent colors expected to be used in light themes */
  light_theme_colors: ProfileAccentColors | null = null;
  /** dark_theme_colors: profileAccentColors — Accent colors expected to be used in dark themes */
  dark_theme_colors: ProfileAccentColors | null = null;
  /** min_supergroup_chat_boost_level: int32 — The minimum chat boost level required to use the color in a supergroup chat */
  min_supergroup_chat_boost_level: number = 0;
  /** min_channel_chat_boost_level: int32 — The minimum chat boost level required to use the color in a channel chat */
  min_channel_chat_boost_level: number = 0;
}

/** Decode a TDLib `profileAccentColor` object; null input -> null. */
export function decodeProfileAccentColor(json: TdJsonRaw): ProfileAccentColor | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileAccentColor = new ProfileAccentColor();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asNumber(record['id']);
  out.light_theme_colors = decodeProfileAccentColors(record['light_theme_colors']);
  out.dark_theme_colors = decodeProfileAccentColors(record['dark_theme_colors']);
  out.min_supergroup_chat_boost_level = asNumber(record['min_supergroup_chat_boost_level']);
  out.min_channel_chat_boost_level = asNumber(record['min_channel_chat_boost_level']);
  return out;
}

/** Encode a `profileAccentColor` into TDLib JSON wire shape. */
export function encodeProfileAccentColor(value: ProfileAccentColor): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileAccentColor';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['light_theme_colors'] = value.light_theme_colors === null ? null : encodeProfileAccentColors(value.light_theme_colors);
  out['dark_theme_colors'] = value.dark_theme_colors === null ? null : encodeProfileAccentColors(value.dark_theme_colors);
  out['min_supergroup_chat_boost_level'] = value.min_supergroup_chat_boost_level;
  out['min_channel_chat_boost_level'] = value.min_channel_chat_boost_level;
  return out;
}

/** profileAccentColors = ProfileAccentColors — Contains information about supported accent colors for user profile photo background in RGB format */
export class ProfileAccentColors {
  type: 'profileAccentColors' = 'profileAccentColors';
  extra?: string;
  /** palette_colors: vector<int32> — The list of 1-2 colors in RGB format, describing the colors, as expected to be shown in the color palette settings */
  palette_colors: number[] = [];
  /** background_colors: vector<int32> — The list of 1-2 colors in RGB format, describing the colors, as expected to be used for the profile photo background */
  background_colors: number[] = [];
  /** story_colors: vector<int32> — The list of 2 colors in RGB format, describing the colors of the gradient to be used for the unread active story indicator around profile photo */
  story_colors: number[] = [];
}

/** Decode a TDLib `profileAccentColors` object; null input -> null. */
export function decodeProfileAccentColors(json: TdJsonRaw): ProfileAccentColors | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileAccentColors = new ProfileAccentColors();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.palette_colors = decodeVector(record['palette_colors'], (item: TdJsonRaw): number => asNumber(item));
  out.background_colors = decodeVector(record['background_colors'], (item: TdJsonRaw): number => asNumber(item));
  out.story_colors = decodeVector(record['story_colors'], (item: TdJsonRaw): number => asNumber(item));
  return out;
}

/** Encode a `profileAccentColors` into TDLib JSON wire shape. */
export function encodeProfileAccentColors(value: ProfileAccentColors): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileAccentColors';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['palette_colors'] = encodeVector(value.palette_colors, (item: number): TdJsonRaw => item);
  out['background_colors'] = encodeVector(value.background_colors, (item: number): TdJsonRaw => item);
  out['story_colors'] = encodeVector(value.story_colors, (item: number): TdJsonRaw => item);
  return out;
}

/** profilePhoto = ProfilePhoto — Describes a user profile photo */
export class ProfilePhoto {
  type: 'profilePhoto' = 'profilePhoto';
  extra?: string;
  /** id: int64 — Photo identifier; 0 for an empty photo. Can be used to find a photo in a list of user profile photos */
  id: string = '';
  /** small: file — A small (160x160) user profile photo. The file can be downloaded only before the photo is changed */
  small: File | null = null;
  /** big: file — A big (640x640) user profile photo. The file can be downloaded only before the photo is changed */
  big: File | null = null;
  /** minithumbnail: minithumbnail — User profile photo minithumbnail; may be null */
  minithumbnail: Minithumbnail | null = null;
  /** has_animation: Bool — True, if the photo has animated variant */
  has_animation: boolean = false;
  /** is_personal: Bool — True, if the photo is visible only for the current user */
  is_personal: boolean = false;
}

/** Decode a TDLib `profilePhoto` object; null input -> null. */
export function decodeProfilePhoto(json: TdJsonRaw): ProfilePhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfilePhoto = new ProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  out.small = decodeFile(record['small']);
  out.big = decodeFile(record['big']);
  out.minithumbnail = decodeMinithumbnail(record['minithumbnail']);
  out.has_animation = asBool(record['has_animation']);
  out.is_personal = asBool(record['is_personal']);
  return out;
}

/** Encode a `profilePhoto` into TDLib JSON wire shape. */
export function encodeProfilePhoto(value: ProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  out['small'] = value.small === null ? null : encodeFile(value.small);
  out['big'] = value.big === null ? null : encodeFile(value.big);
  out['minithumbnail'] = value.minithumbnail === null ? null : encodeMinithumbnail(value.minithumbnail);
  out['has_animation'] = value.has_animation;
  out['is_personal'] = value.is_personal;
  return out;
}

/** profileTabFiles = ProfileTab — A tab with documents posted by the channel */
export class ProfileTabFiles {
  type: 'profileTabFiles' = 'profileTabFiles';
  extra?: string;
}

/** Decode a TDLib `profileTabFiles` object; null input -> null. */
export function decodeProfileTabFiles(json: TdJsonRaw): ProfileTabFiles | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileTabFiles = new ProfileTabFiles();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `profileTabFiles` into TDLib JSON wire shape. */
export function encodeProfileTabFiles(value: ProfileTabFiles): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileTabFiles';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** profileTabGifs = ProfileTab — A tab with animations posted by the channel */
export class ProfileTabGifs {
  type: 'profileTabGifs' = 'profileTabGifs';
  extra?: string;
}

/** Decode a TDLib `profileTabGifs` object; null input -> null. */
export function decodeProfileTabGifs(json: TdJsonRaw): ProfileTabGifs | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileTabGifs = new ProfileTabGifs();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `profileTabGifs` into TDLib JSON wire shape. */
export function encodeProfileTabGifs(value: ProfileTabGifs): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileTabGifs';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** profileTabGifts = ProfileTab — A tab with gifts received by the user or the channel chat */
export class ProfileTabGifts {
  type: 'profileTabGifts' = 'profileTabGifts';
  extra?: string;
}

/** Decode a TDLib `profileTabGifts` object; null input -> null. */
export function decodeProfileTabGifts(json: TdJsonRaw): ProfileTabGifts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileTabGifts = new ProfileTabGifts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `profileTabGifts` into TDLib JSON wire shape. */
export function encodeProfileTabGifts(value: ProfileTabGifts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileTabGifts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** profileTabLinks = ProfileTab — A tab with messages posted by the channel and containing links */
export class ProfileTabLinks {
  type: 'profileTabLinks' = 'profileTabLinks';
  extra?: string;
}

/** Decode a TDLib `profileTabLinks` object; null input -> null. */
export function decodeProfileTabLinks(json: TdJsonRaw): ProfileTabLinks | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileTabLinks = new ProfileTabLinks();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `profileTabLinks` into TDLib JSON wire shape. */
export function encodeProfileTabLinks(value: ProfileTabLinks): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileTabLinks';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** profileTabMedia = ProfileTab — A tab with photos and videos posted by the channel */
export class ProfileTabMedia {
  type: 'profileTabMedia' = 'profileTabMedia';
  extra?: string;
}

/** Decode a TDLib `profileTabMedia` object; null input -> null. */
export function decodeProfileTabMedia(json: TdJsonRaw): ProfileTabMedia | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileTabMedia = new ProfileTabMedia();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `profileTabMedia` into TDLib JSON wire shape. */
export function encodeProfileTabMedia(value: ProfileTabMedia): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileTabMedia';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** profileTabMusic = ProfileTab — A tab with audio messages posted by the channel */
export class ProfileTabMusic {
  type: 'profileTabMusic' = 'profileTabMusic';
  extra?: string;
}

/** Decode a TDLib `profileTabMusic` object; null input -> null. */
export function decodeProfileTabMusic(json: TdJsonRaw): ProfileTabMusic | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileTabMusic = new ProfileTabMusic();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `profileTabMusic` into TDLib JSON wire shape. */
export function encodeProfileTabMusic(value: ProfileTabMusic): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileTabMusic';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** profileTabPosts = ProfileTab — A tab with stories posted by the user or the channel chat and saved to profile */
export class ProfileTabPosts {
  type: 'profileTabPosts' = 'profileTabPosts';
  extra?: string;
}

/** Decode a TDLib `profileTabPosts` object; null input -> null. */
export function decodeProfileTabPosts(json: TdJsonRaw): ProfileTabPosts | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileTabPosts = new ProfileTabPosts();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `profileTabPosts` into TDLib JSON wire shape. */
export function encodeProfileTabPosts(value: ProfileTabPosts): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileTabPosts';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** profileTabVoice = ProfileTab — A tab with voice notes posted by the channel */
export class ProfileTabVoice {
  type: 'profileTabVoice' = 'profileTabVoice';
  extra?: string;
}

/** Decode a TDLib `profileTabVoice` object; null input -> null. */
export function decodeProfileTabVoice(json: TdJsonRaw): ProfileTabVoice | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProfileTabVoice = new ProfileTabVoice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `profileTabVoice` into TDLib JSON wire shape. */
export function encodeProfileTabVoice(value: ProfileTabVoice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'profileTabVoice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** proxy = Proxy — Describes a proxy server */
export class ProxyValue {
  type: 'proxy' = 'proxy';
  extra?: string;
  /** server: string — Proxy server domain or IP address */
  server: string = '';
  /** port: int32 — Proxy server port */
  port: number = 0;
  /** type: ProxyType — Type of the proxy */
  type_: TdProxyType | null = null;
}

/** Decode a TDLib `proxy` object; null input -> null. */
export function decodeProxyValue(json: TdJsonRaw): ProxyValue | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProxyValue = new ProxyValue();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.server = asString(record['server']);
  out.port = asNumber(record['port']);
  out.type_ = decodeTdProxyType(record['type']);
  return out;
}

/** Encode a `proxy` into TDLib JSON wire shape. */
export function encodeProxyValue(value: ProxyValue): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'proxy';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['server'] = value.server;
  out['port'] = value.port;
  out['type'] = value.type_ === null ? null : encodeTdProxyType(value.type_);
  return out;
}

/** proxyTypeHttp = ProxyType — A HTTP transparent proxy server */
export class ProxyTypeHttp {
  type: 'proxyTypeHttp' = 'proxyTypeHttp';
  extra?: string;
  /** username: string — Username for logging in; may be empty */
  username: string = '';
  /** password: string — Password for logging in; may be empty */
  password: string = '';
  /** http_only: Bool — Pass true if the proxy supports only HTTP requests and doesn't support transparent TCP connections via HTTP CONNECT method */
  http_only: boolean = false;
}

/** Decode a TDLib `proxyTypeHttp` object; null input -> null. */
export function decodeProxyTypeHttp(json: TdJsonRaw): ProxyTypeHttp | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProxyTypeHttp = new ProxyTypeHttp();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  out.password = asString(record['password']);
  out.http_only = asBool(record['http_only']);
  return out;
}

/** Encode a `proxyTypeHttp` into TDLib JSON wire shape. */
export function encodeProxyTypeHttp(value: ProxyTypeHttp): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'proxyTypeHttp';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  out['password'] = value.password;
  out['http_only'] = value.http_only;
  return out;
}

/** proxyTypeMtproto = ProxyType — An MTProto proxy server */
export class ProxyTypeMtproto {
  type: 'proxyTypeMtproto' = 'proxyTypeMtproto';
  extra?: string;
  /** secret: string — The proxy's secret in hexadecimal encoding */
  secret: string = '';
}

/** Decode a TDLib `proxyTypeMtproto` object; null input -> null. */
export function decodeProxyTypeMtproto(json: TdJsonRaw): ProxyTypeMtproto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProxyTypeMtproto = new ProxyTypeMtproto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.secret = asString(record['secret']);
  return out;
}

/** Encode a `proxyTypeMtproto` into TDLib JSON wire shape. */
export function encodeProxyTypeMtproto(value: ProxyTypeMtproto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'proxyTypeMtproto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['secret'] = value.secret;
  return out;
}

/** proxyTypeSocks5 = ProxyType — A SOCKS5 proxy server */
export class ProxyTypeSocks5 {
  type: 'proxyTypeSocks5' = 'proxyTypeSocks5';
  extra?: string;
  /** username: string — Username for logging in; may be empty */
  username: string = '';
  /** password: string — Password for logging in; may be empty */
  password: string = '';
}

/** Decode a TDLib `proxyTypeSocks5` object; null input -> null. */
export function decodeProxyTypeSocks5(json: TdJsonRaw): ProxyTypeSocks5 | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: ProxyTypeSocks5 = new ProxyTypeSocks5();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.username = asString(record['username']);
  out.password = asString(record['password']);
  return out;
}

/** Encode a `proxyTypeSocks5` into TDLib JSON wire shape. */
export function encodeProxyTypeSocks5(value: ProxyTypeSocks5): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'proxyTypeSocks5';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['username'] = value.username;
  out['password'] = value.password;
  return out;
}

/** publicChatTypeHasUsername = PublicChatType — The chat is public, because it has an active username */
export class PublicChatTypeHasUsername {
  type: 'publicChatTypeHasUsername' = 'publicChatTypeHasUsername';
  extra?: string;
}

/** Decode a TDLib `publicChatTypeHasUsername` object; null input -> null. */
export function decodePublicChatTypeHasUsername(json: TdJsonRaw): PublicChatTypeHasUsername | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PublicChatTypeHasUsername = new PublicChatTypeHasUsername();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `publicChatTypeHasUsername` into TDLib JSON wire shape. */
export function encodePublicChatTypeHasUsername(value: PublicChatTypeHasUsername): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'publicChatTypeHasUsername';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** publicChatTypeIsLocationBased = PublicChatType — The chat is public, because it is a location-based supergroup */
export class PublicChatTypeIsLocationBased {
  type: 'publicChatTypeIsLocationBased' = 'publicChatTypeIsLocationBased';
  extra?: string;
}

/** Decode a TDLib `publicChatTypeIsLocationBased` object; null input -> null. */
export function decodePublicChatTypeIsLocationBased(json: TdJsonRaw): PublicChatTypeIsLocationBased | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PublicChatTypeIsLocationBased = new PublicChatTypeIsLocationBased();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `publicChatTypeIsLocationBased` into TDLib JSON wire shape. */
export function encodePublicChatTypeIsLocationBased(value: PublicChatTypeIsLocationBased): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'publicChatTypeIsLocationBased';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** publicForwardMessage = PublicForward — Contains a public forward as a message */
export class PublicForwardMessage {
  type: 'publicForwardMessage' = 'publicForwardMessage';
  extra?: string;
  /** message: message — Information about the message */
  message: Message | null = null;
}

/** Decode a TDLib `publicForwardMessage` object; null input -> null. */
export function decodePublicForwardMessage(json: TdJsonRaw): PublicForwardMessage | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PublicForwardMessage = new PublicForwardMessage();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.message = decodeMessage(record['message']);
  return out;
}

/** Encode a `publicForwardMessage` into TDLib JSON wire shape. */
export function encodePublicForwardMessage(value: PublicForwardMessage): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'publicForwardMessage';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['message'] = value.message === null ? null : encodeMessage(value.message);
  return out;
}

/** publicForwardStory = PublicForward — Contains a public repost to a story */
export class PublicForwardStory {
  type: 'publicForwardStory' = 'publicForwardStory';
  extra?: string;
  /** story: story — Information about the story */
  story: Story | null = null;
}

/** Decode a TDLib `publicForwardStory` object; null input -> null. */
export function decodePublicForwardStory(json: TdJsonRaw): PublicForwardStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PublicForwardStory = new PublicForwardStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.story = decodeStory(record['story']);
  return out;
}

/** Encode a `publicForwardStory` into TDLib JSON wire shape. */
export function encodePublicForwardStory(value: PublicForwardStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'publicForwardStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['story'] = value.story === null ? null : encodeStory(value.story);
  return out;
}

/** publicForwards = PublicForwards — Represents a list of public forwards and reposts as a story of a message or a story */
export class PublicForwards {
  type: 'publicForwards' = 'publicForwards';
  extra?: string;
  /** total_count: int32 — Approximate total number of messages and stories found */
  total_count: number = 0;
  /** forwards: vector<PublicForward> — List of found public forwards and reposts */
  forwards: TdPublicForward[] = [];
  /** next_offset: string — The offset for the next request. If empty, then there are no more results */
  next_offset: string = '';
}

/** Decode a TDLib `publicForwards` object; null input -> null. */
export function decodePublicForwards(json: TdJsonRaw): PublicForwards | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PublicForwards = new PublicForwards();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.forwards = decodeVector(record['forwards'], (item: TdJsonRaw): TdPublicForward => decodeTdPublicForward(item) as TdPublicForward);
  out.next_offset = asString(record['next_offset']);
  return out;
}

/** Encode a `publicForwards` into TDLib JSON wire shape. */
export function encodePublicForwards(value: PublicForwards): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'publicForwards';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['forwards'] = encodeVector(value.forwards, (item: TdPublicForward | null): TdJsonRaw => item === null ? null : encodeTdPublicForward(item));
  out['next_offset'] = value.next_offset;
  return out;
}

/** publicPostSearchLimits = PublicPostSearchLimits — Contains information about public post search limits */
export class PublicPostSearchLimits {
  type: 'publicPostSearchLimits' = 'publicPostSearchLimits';
  extra?: string;
  /** daily_free_query_count: int32 — Number of queries that can be sent daily for free */
  daily_free_query_count: number = 0;
  /** remaining_free_query_count: int32 — Number of remaining free queries today */
  remaining_free_query_count: number = 0;
  /** next_free_query_in: int32 — Amount of time till the next free query can be sent; 0 if it can be sent now */
  next_free_query_in: number = 0;
  /** star_count: int53 — Number of Telegram Stars that must be paid for each non-free query */
  star_count: number = 0;
  /** is_current_query_free: Bool — True, if the search for the specified query isn't charged */
  is_current_query_free: boolean = false;
}

/** Decode a TDLib `publicPostSearchLimits` object; null input -> null. */
export function decodePublicPostSearchLimits(json: TdJsonRaw): PublicPostSearchLimits | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PublicPostSearchLimits = new PublicPostSearchLimits();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.daily_free_query_count = asNumber(record['daily_free_query_count']);
  out.remaining_free_query_count = asNumber(record['remaining_free_query_count']);
  out.next_free_query_in = asNumber(record['next_free_query_in']);
  out.star_count = asNumber(record['star_count']);
  out.is_current_query_free = asBool(record['is_current_query_free']);
  return out;
}

/** Encode a `publicPostSearchLimits` into TDLib JSON wire shape. */
export function encodePublicPostSearchLimits(value: PublicPostSearchLimits): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'publicPostSearchLimits';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['daily_free_query_count'] = value.daily_free_query_count;
  out['remaining_free_query_count'] = value.remaining_free_query_count;
  out['next_free_query_in'] = value.next_free_query_in;
  out['star_count'] = value.star_count;
  out['is_current_query_free'] = value.is_current_query_free;
  return out;
}

/** pushMessageContentAnimation = PushMessageContent — An animation message (GIF-style). */
export class PushMessageContentAnimation {
  type: 'pushMessageContentAnimation' = 'pushMessageContentAnimation';
  extra?: string;
  /** animation: animation — Message content; may be null */
  animation: Animation | null = null;
  /** caption: string — Animation caption */
  caption: string = '';
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentAnimation` object; null input -> null. */
export function decodePushMessageContentAnimation(json: TdJsonRaw): PushMessageContentAnimation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentAnimation = new PushMessageContentAnimation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.animation = decodeAnimation(record['animation']);
  out.caption = asString(record['caption']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentAnimation` into TDLib JSON wire shape. */
export function encodePushMessageContentAnimation(value: PushMessageContentAnimation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentAnimation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['animation'] = value.animation === null ? null : encodeAnimation(value.animation);
  out['caption'] = value.caption;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentAudio = PushMessageContent — An audio message */
export class PushMessageContentAudio {
  type: 'pushMessageContentAudio' = 'pushMessageContentAudio';
  extra?: string;
  /** audio: audio — Message content; may be null */
  audio: Audio | null = null;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentAudio` object; null input -> null. */
export function decodePushMessageContentAudio(json: TdJsonRaw): PushMessageContentAudio | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentAudio = new PushMessageContentAudio();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.audio = decodeAudio(record['audio']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentAudio` into TDLib JSON wire shape. */
export function encodePushMessageContentAudio(value: PushMessageContentAudio): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentAudio';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['audio'] = value.audio === null ? null : encodeAudio(value.audio);
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentBasicGroupChatCreate = PushMessageContent — A newly created basic group */
export class PushMessageContentBasicGroupChatCreate {
  type: 'pushMessageContentBasicGroupChatCreate' = 'pushMessageContentBasicGroupChatCreate';
  extra?: string;
}

/** Decode a TDLib `pushMessageContentBasicGroupChatCreate` object; null input -> null. */
export function decodePushMessageContentBasicGroupChatCreate(json: TdJsonRaw): PushMessageContentBasicGroupChatCreate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentBasicGroupChatCreate = new PushMessageContentBasicGroupChatCreate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pushMessageContentBasicGroupChatCreate` into TDLib JSON wire shape. */
export function encodePushMessageContentBasicGroupChatCreate(value: PushMessageContentBasicGroupChatCreate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentBasicGroupChatCreate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pushMessageContentChatAddMembers = PushMessageContent — New chat members were invited to a group */
export class PushMessageContentChatAddMembers {
  type: 'pushMessageContentChatAddMembers' = 'pushMessageContentChatAddMembers';
  extra?: string;
  /** member_name: string — Name of the added member */
  member_name: string = '';
  /** is_current_user: Bool — True, if the current user was added to the group */
  is_current_user: boolean = false;
  /** is_returned: Bool — True, if the user has returned to the group themselves */
  is_returned: boolean = false;
}

/** Decode a TDLib `pushMessageContentChatAddMembers` object; null input -> null. */
export function decodePushMessageContentChatAddMembers(json: TdJsonRaw): PushMessageContentChatAddMembers | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChatAddMembers = new PushMessageContentChatAddMembers();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.member_name = asString(record['member_name']);
  out.is_current_user = asBool(record['is_current_user']);
  out.is_returned = asBool(record['is_returned']);
  return out;
}

/** Encode a `pushMessageContentChatAddMembers` into TDLib JSON wire shape. */
export function encodePushMessageContentChatAddMembers(value: PushMessageContentChatAddMembers): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChatAddMembers';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['member_name'] = value.member_name;
  out['is_current_user'] = value.is_current_user;
  out['is_returned'] = value.is_returned;
  return out;
}

/** pushMessageContentChatChangePhoto = PushMessageContent — A chat photo was edited */
export class PushMessageContentChatChangePhoto {
  type: 'pushMessageContentChatChangePhoto' = 'pushMessageContentChatChangePhoto';
  extra?: string;
}

/** Decode a TDLib `pushMessageContentChatChangePhoto` object; null input -> null. */
export function decodePushMessageContentChatChangePhoto(json: TdJsonRaw): PushMessageContentChatChangePhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChatChangePhoto = new PushMessageContentChatChangePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pushMessageContentChatChangePhoto` into TDLib JSON wire shape. */
export function encodePushMessageContentChatChangePhoto(value: PushMessageContentChatChangePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChatChangePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pushMessageContentChatChangeTitle = PushMessageContent — A chat title was edited */
export class PushMessageContentChatChangeTitle {
  type: 'pushMessageContentChatChangeTitle' = 'pushMessageContentChatChangeTitle';
  extra?: string;
  /** title: string — New chat title */
  title: string = '';
}

/** Decode a TDLib `pushMessageContentChatChangeTitle` object; null input -> null. */
export function decodePushMessageContentChatChangeTitle(json: TdJsonRaw): PushMessageContentChatChangeTitle | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChatChangeTitle = new PushMessageContentChatChangeTitle();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  return out;
}

/** Encode a `pushMessageContentChatChangeTitle` into TDLib JSON wire shape. */
export function encodePushMessageContentChatChangeTitle(value: PushMessageContentChatChangeTitle): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChatChangeTitle';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  return out;
}

/** pushMessageContentChatDeleteMember = PushMessageContent — A chat member was deleted */
export class PushMessageContentChatDeleteMember {
  type: 'pushMessageContentChatDeleteMember' = 'pushMessageContentChatDeleteMember';
  extra?: string;
  /** member_name: string — Name of the deleted member */
  member_name: string = '';
  /** is_current_user: Bool — True, if the current user was deleted from the group */
  is_current_user: boolean = false;
  /** is_left: Bool — True, if the user has left the group themselves */
  is_left: boolean = false;
}

/** Decode a TDLib `pushMessageContentChatDeleteMember` object; null input -> null. */
export function decodePushMessageContentChatDeleteMember(json: TdJsonRaw): PushMessageContentChatDeleteMember | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChatDeleteMember = new PushMessageContentChatDeleteMember();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.member_name = asString(record['member_name']);
  out.is_current_user = asBool(record['is_current_user']);
  out.is_left = asBool(record['is_left']);
  return out;
}

/** Encode a `pushMessageContentChatDeleteMember` into TDLib JSON wire shape. */
export function encodePushMessageContentChatDeleteMember(value: PushMessageContentChatDeleteMember): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChatDeleteMember';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['member_name'] = value.member_name;
  out['is_current_user'] = value.is_current_user;
  out['is_left'] = value.is_left;
  return out;
}

/** pushMessageContentChatJoinByLink = PushMessageContent — A new member joined the chat via an invite link */
export class PushMessageContentChatJoinByLink {
  type: 'pushMessageContentChatJoinByLink' = 'pushMessageContentChatJoinByLink';
  extra?: string;
}

/** Decode a TDLib `pushMessageContentChatJoinByLink` object; null input -> null. */
export function decodePushMessageContentChatJoinByLink(json: TdJsonRaw): PushMessageContentChatJoinByLink | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChatJoinByLink = new PushMessageContentChatJoinByLink();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pushMessageContentChatJoinByLink` into TDLib JSON wire shape. */
export function encodePushMessageContentChatJoinByLink(value: PushMessageContentChatJoinByLink): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChatJoinByLink';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pushMessageContentChatJoinByRequest = PushMessageContent — A new member was accepted to the chat by an administrator */
export class PushMessageContentChatJoinByRequest {
  type: 'pushMessageContentChatJoinByRequest' = 'pushMessageContentChatJoinByRequest';
  extra?: string;
}

/** Decode a TDLib `pushMessageContentChatJoinByRequest` object; null input -> null. */
export function decodePushMessageContentChatJoinByRequest(json: TdJsonRaw): PushMessageContentChatJoinByRequest | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChatJoinByRequest = new PushMessageContentChatJoinByRequest();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pushMessageContentChatJoinByRequest` into TDLib JSON wire shape. */
export function encodePushMessageContentChatJoinByRequest(value: PushMessageContentChatJoinByRequest): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChatJoinByRequest';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pushMessageContentChatSetBackground = PushMessageContent — A chat background was edited */
export class PushMessageContentChatSetBackground {
  type: 'pushMessageContentChatSetBackground' = 'pushMessageContentChatSetBackground';
  extra?: string;
  /** is_same: Bool — True, if the set background is the same as the background of the current user */
  is_same: boolean = false;
}

/** Decode a TDLib `pushMessageContentChatSetBackground` object; null input -> null. */
export function decodePushMessageContentChatSetBackground(json: TdJsonRaw): PushMessageContentChatSetBackground | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChatSetBackground = new PushMessageContentChatSetBackground();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_same = asBool(record['is_same']);
  return out;
}

/** Encode a `pushMessageContentChatSetBackground` into TDLib JSON wire shape. */
export function encodePushMessageContentChatSetBackground(value: PushMessageContentChatSetBackground): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChatSetBackground';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_same'] = value.is_same;
  return out;
}

/** pushMessageContentChatSetTheme = PushMessageContent — A chat theme was edited */
export class PushMessageContentChatSetTheme {
  type: 'pushMessageContentChatSetTheme' = 'pushMessageContentChatSetTheme';
  extra?: string;
  /** name: string — If non-empty, human-readable name of the new theme. Otherwise, the chat theme was reset to the default one */
  name: string = '';
}

/** Decode a TDLib `pushMessageContentChatSetTheme` object; null input -> null. */
export function decodePushMessageContentChatSetTheme(json: TdJsonRaw): PushMessageContentChatSetTheme | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChatSetTheme = new PushMessageContentChatSetTheme();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  return out;
}

/** Encode a `pushMessageContentChatSetTheme` into TDLib JSON wire shape. */
export function encodePushMessageContentChatSetTheme(value: PushMessageContentChatSetTheme): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChatSetTheme';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  return out;
}

/** pushMessageContentChecklist = PushMessageContent — A message with a checklist */
export class PushMessageContentChecklist {
  type: 'pushMessageContentChecklist' = 'pushMessageContentChecklist';
  extra?: string;
  /** title: string — Checklist title */
  title: string = '';
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentChecklist` object; null input -> null. */
export function decodePushMessageContentChecklist(json: TdJsonRaw): PushMessageContentChecklist | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChecklist = new PushMessageContentChecklist();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentChecklist` into TDLib JSON wire shape. */
export function encodePushMessageContentChecklist(value: PushMessageContentChecklist): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChecklist';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentChecklistTasksAdded = PushMessageContent — Some tasks were added to a checklist */
export class PushMessageContentChecklistTasksAdded {
  type: 'pushMessageContentChecklistTasksAdded' = 'pushMessageContentChecklistTasksAdded';
  extra?: string;
  /** task_count: int32 — Number of added tasks */
  task_count: number = 0;
}

/** Decode a TDLib `pushMessageContentChecklistTasksAdded` object; null input -> null. */
export function decodePushMessageContentChecklistTasksAdded(json: TdJsonRaw): PushMessageContentChecklistTasksAdded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChecklistTasksAdded = new PushMessageContentChecklistTasksAdded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.task_count = asNumber(record['task_count']);
  return out;
}

/** Encode a `pushMessageContentChecklistTasksAdded` into TDLib JSON wire shape. */
export function encodePushMessageContentChecklistTasksAdded(value: PushMessageContentChecklistTasksAdded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChecklistTasksAdded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['task_count'] = value.task_count;
  return out;
}

/** pushMessageContentChecklistTasksDone = PushMessageContent — Some tasks from a checklist were marked as done or not done */
export class PushMessageContentChecklistTasksDone {
  type: 'pushMessageContentChecklistTasksDone' = 'pushMessageContentChecklistTasksDone';
  extra?: string;
  /** task_count: int32 — Number of changed tasks */
  task_count: number = 0;
}

/** Decode a TDLib `pushMessageContentChecklistTasksDone` object; null input -> null. */
export function decodePushMessageContentChecklistTasksDone(json: TdJsonRaw): PushMessageContentChecklistTasksDone | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentChecklistTasksDone = new PushMessageContentChecklistTasksDone();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.task_count = asNumber(record['task_count']);
  return out;
}

/** Encode a `pushMessageContentChecklistTasksDone` into TDLib JSON wire shape. */
export function encodePushMessageContentChecklistTasksDone(value: PushMessageContentChecklistTasksDone): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentChecklistTasksDone';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['task_count'] = value.task_count;
  return out;
}

/** pushMessageContentContact = PushMessageContent — A message with a user contact */
export class PushMessageContentContact {
  type: 'pushMessageContentContact' = 'pushMessageContentContact';
  extra?: string;
  /** name: string — Contact's name */
  name: string = '';
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentContact` object; null input -> null. */
export function decodePushMessageContentContact(json: TdJsonRaw): PushMessageContentContact | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentContact = new PushMessageContentContact();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.name = asString(record['name']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentContact` into TDLib JSON wire shape. */
export function encodePushMessageContentContact(value: PushMessageContentContact): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentContact';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['name'] = value.name;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentContactRegistered = PushMessageContent — A contact has registered with Telegram */
export class PushMessageContentContactRegistered {
  type: 'pushMessageContentContactRegistered' = 'pushMessageContentContactRegistered';
  extra?: string;
  /** as_premium_account: Bool — True, if the user joined Telegram as a Telegram Premium account */
  as_premium_account: boolean = false;
}

/** Decode a TDLib `pushMessageContentContactRegistered` object; null input -> null. */
export function decodePushMessageContentContactRegistered(json: TdJsonRaw): PushMessageContentContactRegistered | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentContactRegistered = new PushMessageContentContactRegistered();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.as_premium_account = asBool(record['as_premium_account']);
  return out;
}

/** Encode a `pushMessageContentContactRegistered` into TDLib JSON wire shape. */
export function encodePushMessageContentContactRegistered(value: PushMessageContentContactRegistered): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentContactRegistered';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['as_premium_account'] = value.as_premium_account;
  return out;
}

/** pushMessageContentDocument = PushMessageContent — A document message (a general file) */
export class PushMessageContentDocument {
  type: 'pushMessageContentDocument' = 'pushMessageContentDocument';
  extra?: string;
  /** document: document — Message content; may be null */
  document: Document | null = null;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentDocument` object; null input -> null. */
export function decodePushMessageContentDocument(json: TdJsonRaw): PushMessageContentDocument | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentDocument = new PushMessageContentDocument();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.document = decodeDocument(record['document']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentDocument` into TDLib JSON wire shape. */
export function encodePushMessageContentDocument(value: PushMessageContentDocument): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentDocument';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['document'] = value.document === null ? null : encodeDocument(value.document);
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentGame = PushMessageContent — A message with a game */
export class PushMessageContentGame {
  type: 'pushMessageContentGame' = 'pushMessageContentGame';
  extra?: string;
  /** title: string — Game title, empty for pinned game message */
  title: string = '';
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentGame` object; null input -> null. */
export function decodePushMessageContentGame(json: TdJsonRaw): PushMessageContentGame | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentGame = new PushMessageContentGame();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentGame` into TDLib JSON wire shape. */
export function encodePushMessageContentGame(value: PushMessageContentGame): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentGame';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentGameScore = PushMessageContent — A new high score was achieved in a game */
export class PushMessageContentGameScore {
  type: 'pushMessageContentGameScore' = 'pushMessageContentGameScore';
  extra?: string;
  /** title: string — Game title, empty for pinned message */
  title: string = '';
  /** score: int32 — New score, 0 for pinned message */
  score: number = 0;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentGameScore` object; null input -> null. */
export function decodePushMessageContentGameScore(json: TdJsonRaw): PushMessageContentGameScore | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentGameScore = new PushMessageContentGameScore();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.title = asString(record['title']);
  out.score = asNumber(record['score']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentGameScore` into TDLib JSON wire shape. */
export function encodePushMessageContentGameScore(value: PushMessageContentGameScore): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentGameScore';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['title'] = value.title;
  out['score'] = value.score;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentGift = PushMessageContent — A message with a gift */
export class PushMessageContentGift {
  type: 'pushMessageContentGift' = 'pushMessageContentGift';
  extra?: string;
  /** star_count: int53 — Number of Telegram Stars that sender paid for the gift */
  star_count: number = 0;
  /** is_prepaid_upgrade: Bool — True, if the message is about prepaid upgrade of the gift by another user instead of actual receiving of a new gift */
  is_prepaid_upgrade: boolean = false;
}

/** Decode a TDLib `pushMessageContentGift` object; null input -> null. */
export function decodePushMessageContentGift(json: TdJsonRaw): PushMessageContentGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentGift = new PushMessageContentGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.is_prepaid_upgrade = asBool(record['is_prepaid_upgrade']);
  return out;
}

/** Encode a `pushMessageContentGift` into TDLib JSON wire shape. */
export function encodePushMessageContentGift(value: PushMessageContentGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['is_prepaid_upgrade'] = value.is_prepaid_upgrade;
  return out;
}

/** pushMessageContentGiveaway = PushMessageContent — A message with a giveaway */
export class PushMessageContentGiveaway {
  type: 'pushMessageContentGiveaway' = 'pushMessageContentGiveaway';
  extra?: string;
  /** winner_count: int32 — Number of users who will receive giveaway prizes; 0 for pinned message */
  winner_count: number = 0;
  /** prize: GiveawayPrize — Prize of the giveaway; may be null for pinned message */
  prize: TdGiveawayPrize | null = null;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentGiveaway` object; null input -> null. */
export function decodePushMessageContentGiveaway(json: TdJsonRaw): PushMessageContentGiveaway | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentGiveaway = new PushMessageContentGiveaway();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.winner_count = asNumber(record['winner_count']);
  out.prize = decodeTdGiveawayPrize(record['prize']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentGiveaway` into TDLib JSON wire shape. */
export function encodePushMessageContentGiveaway(value: PushMessageContentGiveaway): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentGiveaway';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['winner_count'] = value.winner_count;
  out['prize'] = value.prize === null ? null : encodeTdGiveawayPrize(value.prize);
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentHidden = PushMessageContent — A general message with hidden content */
export class PushMessageContentHidden {
  type: 'pushMessageContentHidden' = 'pushMessageContentHidden';
  extra?: string;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentHidden` object; null input -> null. */
export function decodePushMessageContentHidden(json: TdJsonRaw): PushMessageContentHidden | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentHidden = new PushMessageContentHidden();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentHidden` into TDLib JSON wire shape. */
export function encodePushMessageContentHidden(value: PushMessageContentHidden): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentHidden';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentInviteVideoChatParticipants = PushMessageContent — An invitation of participants to a video chat or live stream */
export class PushMessageContentInviteVideoChatParticipants {
  type: 'pushMessageContentInviteVideoChatParticipants' = 'pushMessageContentInviteVideoChatParticipants';
  extra?: string;
  /** is_current_user: Bool — True, if the current user was invited to the video chat or the live stream */
  is_current_user: boolean = false;
}

/** Decode a TDLib `pushMessageContentInviteVideoChatParticipants` object; null input -> null. */
export function decodePushMessageContentInviteVideoChatParticipants(json: TdJsonRaw): PushMessageContentInviteVideoChatParticipants | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentInviteVideoChatParticipants = new PushMessageContentInviteVideoChatParticipants();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_current_user = asBool(record['is_current_user']);
  return out;
}

/** Encode a `pushMessageContentInviteVideoChatParticipants` into TDLib JSON wire shape. */
export function encodePushMessageContentInviteVideoChatParticipants(value: PushMessageContentInviteVideoChatParticipants): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentInviteVideoChatParticipants';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_current_user'] = value.is_current_user;
  return out;
}

/** pushMessageContentInvoice = PushMessageContent — A message with an invoice from a bot */
export class PushMessageContentInvoice {
  type: 'pushMessageContentInvoice' = 'pushMessageContentInvoice';
  extra?: string;
  /** price: string — Product price */
  price: string = '';
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentInvoice` object; null input -> null. */
export function decodePushMessageContentInvoice(json: TdJsonRaw): PushMessageContentInvoice | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentInvoice = new PushMessageContentInvoice();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.price = asString(record['price']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentInvoice` into TDLib JSON wire shape. */
export function encodePushMessageContentInvoice(value: PushMessageContentInvoice): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentInvoice';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['price'] = value.price;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentLocation = PushMessageContent — A message with a location */
export class PushMessageContentLocation {
  type: 'pushMessageContentLocation' = 'pushMessageContentLocation';
  extra?: string;
  /** is_live: Bool — True, if the location is live */
  is_live: boolean = false;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentLocation` object; null input -> null. */
export function decodePushMessageContentLocation(json: TdJsonRaw): PushMessageContentLocation | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentLocation = new PushMessageContentLocation();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_live = asBool(record['is_live']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentLocation` into TDLib JSON wire shape. */
export function encodePushMessageContentLocation(value: PushMessageContentLocation): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentLocation';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_live'] = value.is_live;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentMediaAlbum = PushMessageContent — A media album */
export class PushMessageContentMediaAlbum {
  type: 'pushMessageContentMediaAlbum' = 'pushMessageContentMediaAlbum';
  extra?: string;
  /** total_count: int32 — Number of messages in the album */
  total_count: number = 0;
  /** has_photos: Bool — True, if the album has at least one photo */
  has_photos: boolean = false;
  /** has_videos: Bool — True, if the album has at least one video file */
  has_videos: boolean = false;
  /** has_audios: Bool — True, if the album has at least one audio file */
  has_audios: boolean = false;
  /** has_documents: Bool — True, if the album has at least one document */
  has_documents: boolean = false;
}

/** Decode a TDLib `pushMessageContentMediaAlbum` object; null input -> null. */
export function decodePushMessageContentMediaAlbum(json: TdJsonRaw): PushMessageContentMediaAlbum | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentMediaAlbum = new PushMessageContentMediaAlbum();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  out.has_photos = asBool(record['has_photos']);
  out.has_videos = asBool(record['has_videos']);
  out.has_audios = asBool(record['has_audios']);
  out.has_documents = asBool(record['has_documents']);
  return out;
}

/** Encode a `pushMessageContentMediaAlbum` into TDLib JSON wire shape. */
export function encodePushMessageContentMediaAlbum(value: PushMessageContentMediaAlbum): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentMediaAlbum';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  out['has_photos'] = value.has_photos;
  out['has_videos'] = value.has_videos;
  out['has_audios'] = value.has_audios;
  out['has_documents'] = value.has_documents;
  return out;
}

/** pushMessageContentMessageForwards = PushMessageContent — A forwarded messages */
export class PushMessageContentMessageForwards {
  type: 'pushMessageContentMessageForwards' = 'pushMessageContentMessageForwards';
  extra?: string;
  /** total_count: int32 — Number of forwarded messages */
  total_count: number = 0;
}

/** Decode a TDLib `pushMessageContentMessageForwards` object; null input -> null. */
export function decodePushMessageContentMessageForwards(json: TdJsonRaw): PushMessageContentMessageForwards | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentMessageForwards = new PushMessageContentMessageForwards();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.total_count = asNumber(record['total_count']);
  return out;
}

/** Encode a `pushMessageContentMessageForwards` into TDLib JSON wire shape. */
export function encodePushMessageContentMessageForwards(value: PushMessageContentMessageForwards): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentMessageForwards';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['total_count'] = value.total_count;
  return out;
}

/** pushMessageContentPaidMedia = PushMessageContent — A message with paid media */
export class PushMessageContentPaidMedia {
  type: 'pushMessageContentPaidMedia' = 'pushMessageContentPaidMedia';
  extra?: string;
  /** star_count: int53 — Number of Telegram Stars needed to buy access to the media in the message; 0 for pinned message */
  star_count: number = 0;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentPaidMedia` object; null input -> null. */
export function decodePushMessageContentPaidMedia(json: TdJsonRaw): PushMessageContentPaidMedia | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentPaidMedia = new PushMessageContentPaidMedia();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.star_count = asNumber(record['star_count']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentPaidMedia` into TDLib JSON wire shape. */
export function encodePushMessageContentPaidMedia(value: PushMessageContentPaidMedia): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentPaidMedia';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['star_count'] = value.star_count;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentPhoto = PushMessageContent — A photo message */
export class PushMessageContentPhoto {
  type: 'pushMessageContentPhoto' = 'pushMessageContentPhoto';
  extra?: string;
  /** photo: photo — Message content; may be null */
  photo: Photo | null = null;
  /** caption: string — Photo caption */
  caption: string = '';
  /** is_secret: Bool — True, if the photo is secret */
  is_secret: boolean = false;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentPhoto` object; null input -> null. */
export function decodePushMessageContentPhoto(json: TdJsonRaw): PushMessageContentPhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentPhoto = new PushMessageContentPhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.photo = decodePhoto(record['photo']);
  out.caption = asString(record['caption']);
  out.is_secret = asBool(record['is_secret']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentPhoto` into TDLib JSON wire shape. */
export function encodePushMessageContentPhoto(value: PushMessageContentPhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentPhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['photo'] = value.photo === null ? null : encodePhoto(value.photo);
  out['caption'] = value.caption;
  out['is_secret'] = value.is_secret;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentPoll = PushMessageContent — A message with a poll */
export class PushMessageContentPoll {
  type: 'pushMessageContentPoll' = 'pushMessageContentPoll';
  extra?: string;
  /** question: string — Poll question */
  question: string = '';
  /** is_regular: Bool — True, if the poll is regular and not in quiz mode */
  is_regular: boolean = false;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentPoll` object; null input -> null. */
export function decodePushMessageContentPoll(json: TdJsonRaw): PushMessageContentPoll | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentPoll = new PushMessageContentPoll();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.question = asString(record['question']);
  out.is_regular = asBool(record['is_regular']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentPoll` into TDLib JSON wire shape. */
export function encodePushMessageContentPoll(value: PushMessageContentPoll): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentPoll';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['question'] = value.question;
  out['is_regular'] = value.is_regular;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentPollOptionAdded = PushMessageContent — An option was added to a poll */
export class PushMessageContentPollOptionAdded {
  type: 'pushMessageContentPollOptionAdded' = 'pushMessageContentPollOptionAdded';
  extra?: string;
  /** text: string — Text of the option */
  text: string = '';
}

/** Decode a TDLib `pushMessageContentPollOptionAdded` object; null input -> null. */
export function decodePushMessageContentPollOptionAdded(json: TdJsonRaw): PushMessageContentPollOptionAdded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentPollOptionAdded = new PushMessageContentPollOptionAdded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  return out;
}

/** Encode a `pushMessageContentPollOptionAdded` into TDLib JSON wire shape. */
export function encodePushMessageContentPollOptionAdded(value: PushMessageContentPollOptionAdded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentPollOptionAdded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  return out;
}

/** pushMessageContentPremiumGiftCode = PushMessageContent — A message with a Telegram Premium gift code created for the user */
export class PushMessageContentPremiumGiftCode {
  type: 'pushMessageContentPremiumGiftCode' = 'pushMessageContentPremiumGiftCode';
  extra?: string;
  /** month_count: int32 — Number of months the Telegram Premium subscription will be active after code activation */
  month_count: number = 0;
}

/** Decode a TDLib `pushMessageContentPremiumGiftCode` object; null input -> null. */
export function decodePushMessageContentPremiumGiftCode(json: TdJsonRaw): PushMessageContentPremiumGiftCode | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentPremiumGiftCode = new PushMessageContentPremiumGiftCode();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.month_count = asNumber(record['month_count']);
  return out;
}

/** Encode a `pushMessageContentPremiumGiftCode` into TDLib JSON wire shape. */
export function encodePushMessageContentPremiumGiftCode(value: PushMessageContentPremiumGiftCode): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentPremiumGiftCode';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['month_count'] = value.month_count;
  return out;
}

/** pushMessageContentProximityAlertTriggered = PushMessageContent — A user in the chat came within proximity alert range from the current user */
export class PushMessageContentProximityAlertTriggered {
  type: 'pushMessageContentProximityAlertTriggered' = 'pushMessageContentProximityAlertTriggered';
  extra?: string;
  /** distance: int32 — The distance to the user */
  distance: number = 0;
}

/** Decode a TDLib `pushMessageContentProximityAlertTriggered` object; null input -> null. */
export function decodePushMessageContentProximityAlertTriggered(json: TdJsonRaw): PushMessageContentProximityAlertTriggered | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentProximityAlertTriggered = new PushMessageContentProximityAlertTriggered();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.distance = asNumber(record['distance']);
  return out;
}

/** Encode a `pushMessageContentProximityAlertTriggered` into TDLib JSON wire shape. */
export function encodePushMessageContentProximityAlertTriggered(value: PushMessageContentProximityAlertTriggered): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentProximityAlertTriggered';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['distance'] = value.distance;
  return out;
}

/** pushMessageContentRecurringPayment = PushMessageContent — A new recurring payment was made by the current user */
export class PushMessageContentRecurringPayment {
  type: 'pushMessageContentRecurringPayment' = 'pushMessageContentRecurringPayment';
  extra?: string;
  /** amount: string — The paid amount */
  amount: string = '';
}

/** Decode a TDLib `pushMessageContentRecurringPayment` object; null input -> null. */
export function decodePushMessageContentRecurringPayment(json: TdJsonRaw): PushMessageContentRecurringPayment | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentRecurringPayment = new PushMessageContentRecurringPayment();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.amount = asString(record['amount']);
  return out;
}

/** Encode a `pushMessageContentRecurringPayment` into TDLib JSON wire shape. */
export function encodePushMessageContentRecurringPayment(value: PushMessageContentRecurringPayment): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentRecurringPayment';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['amount'] = value.amount;
  return out;
}

/** pushMessageContentScreenshotTaken = PushMessageContent — A screenshot of a message in the chat has been taken */
export class PushMessageContentScreenshotTaken {
  type: 'pushMessageContentScreenshotTaken' = 'pushMessageContentScreenshotTaken';
  extra?: string;
}

/** Decode a TDLib `pushMessageContentScreenshotTaken` object; null input -> null. */
export function decodePushMessageContentScreenshotTaken(json: TdJsonRaw): PushMessageContentScreenshotTaken | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentScreenshotTaken = new PushMessageContentScreenshotTaken();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pushMessageContentScreenshotTaken` into TDLib JSON wire shape. */
export function encodePushMessageContentScreenshotTaken(value: PushMessageContentScreenshotTaken): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentScreenshotTaken';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pushMessageContentSticker = PushMessageContent — A message with a sticker */
export class PushMessageContentSticker {
  type: 'pushMessageContentSticker' = 'pushMessageContentSticker';
  extra?: string;
  /** sticker: sticker — Message content; may be null */
  sticker: Sticker | null = null;
  /** emoji: string — Emoji corresponding to the sticker; may be empty */
  emoji: string = '';
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentSticker` object; null input -> null. */
export function decodePushMessageContentSticker(json: TdJsonRaw): PushMessageContentSticker | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentSticker = new PushMessageContentSticker();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.sticker = decodeSticker(record['sticker']);
  out.emoji = asString(record['emoji']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentSticker` into TDLib JSON wire shape. */
export function encodePushMessageContentSticker(value: PushMessageContentSticker): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentSticker';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['sticker'] = value.sticker === null ? null : encodeSticker(value.sticker);
  out['emoji'] = value.emoji;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentStory = PushMessageContent — A message with a story */
export class PushMessageContentStory {
  type: 'pushMessageContentStory' = 'pushMessageContentStory';
  extra?: string;
  /** is_mention: Bool — True, if the user was mentioned in the story */
  is_mention: boolean = false;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentStory` object; null input -> null. */
export function decodePushMessageContentStory(json: TdJsonRaw): PushMessageContentStory | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentStory = new PushMessageContentStory();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_mention = asBool(record['is_mention']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentStory` into TDLib JSON wire shape. */
export function encodePushMessageContentStory(value: PushMessageContentStory): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentStory';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_mention'] = value.is_mention;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentSuggestBirthdate = PushMessageContent — A birthdate was suggested to be set */
export class PushMessageContentSuggestBirthdate {
  type: 'pushMessageContentSuggestBirthdate' = 'pushMessageContentSuggestBirthdate';
  extra?: string;
}

/** Decode a TDLib `pushMessageContentSuggestBirthdate` object; null input -> null. */
export function decodePushMessageContentSuggestBirthdate(json: TdJsonRaw): PushMessageContentSuggestBirthdate | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentSuggestBirthdate = new PushMessageContentSuggestBirthdate();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pushMessageContentSuggestBirthdate` into TDLib JSON wire shape. */
export function encodePushMessageContentSuggestBirthdate(value: PushMessageContentSuggestBirthdate): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentSuggestBirthdate';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pushMessageContentSuggestProfilePhoto = PushMessageContent — A profile photo was suggested to the user */
export class PushMessageContentSuggestProfilePhoto {
  type: 'pushMessageContentSuggestProfilePhoto' = 'pushMessageContentSuggestProfilePhoto';
  extra?: string;
}

/** Decode a TDLib `pushMessageContentSuggestProfilePhoto` object; null input -> null. */
export function decodePushMessageContentSuggestProfilePhoto(json: TdJsonRaw): PushMessageContentSuggestProfilePhoto | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentSuggestProfilePhoto = new PushMessageContentSuggestProfilePhoto();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pushMessageContentSuggestProfilePhoto` into TDLib JSON wire shape. */
export function encodePushMessageContentSuggestProfilePhoto(value: PushMessageContentSuggestProfilePhoto): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentSuggestProfilePhoto';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pushMessageContentText = PushMessageContent — A text message */
export class PushMessageContentText {
  type: 'pushMessageContentText' = 'pushMessageContentText';
  extra?: string;
  /** text: string — Message text */
  text: string = '';
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentText` object; null input -> null. */
export function decodePushMessageContentText(json: TdJsonRaw): PushMessageContentText | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentText = new PushMessageContentText();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.text = asString(record['text']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentText` into TDLib JSON wire shape. */
export function encodePushMessageContentText(value: PushMessageContentText): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentText';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['text'] = value.text;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentUpgradedGift = PushMessageContent — A message with an upgraded gift */
export class PushMessageContentUpgradedGift {
  type: 'pushMessageContentUpgradedGift' = 'pushMessageContentUpgradedGift';
  extra?: string;
  /** is_upgrade: Bool — True, if the gift was obtained by upgrading of a previously received gift; otherwise, if is_prepaid_upgrade == false, then this is a transferred or resold gift */
  is_upgrade: boolean = false;
  /** is_prepaid_upgrade: Bool — True, if the message is about completion of prepaid upgrade of the gift instead of actual receiving of a new gift */
  is_prepaid_upgrade: boolean = false;
}

/** Decode a TDLib `pushMessageContentUpgradedGift` object; null input -> null. */
export function decodePushMessageContentUpgradedGift(json: TdJsonRaw): PushMessageContentUpgradedGift | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentUpgradedGift = new PushMessageContentUpgradedGift();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.is_upgrade = asBool(record['is_upgrade']);
  out.is_prepaid_upgrade = asBool(record['is_prepaid_upgrade']);
  return out;
}

/** Encode a `pushMessageContentUpgradedGift` into TDLib JSON wire shape. */
export function encodePushMessageContentUpgradedGift(value: PushMessageContentUpgradedGift): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentUpgradedGift';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['is_upgrade'] = value.is_upgrade;
  out['is_prepaid_upgrade'] = value.is_prepaid_upgrade;
  return out;
}

/** pushMessageContentVideo = PushMessageContent — A video message */
export class PushMessageContentVideo {
  type: 'pushMessageContentVideo' = 'pushMessageContentVideo';
  extra?: string;
  /** video: video — Message content; may be null */
  video: Video | null = null;
  /** caption: string — Video caption */
  caption: string = '';
  /** is_secret: Bool — True, if the video is secret */
  is_secret: boolean = false;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentVideo` object; null input -> null. */
export function decodePushMessageContentVideo(json: TdJsonRaw): PushMessageContentVideo | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentVideo = new PushMessageContentVideo();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video = decodeVideo(record['video']);
  out.caption = asString(record['caption']);
  out.is_secret = asBool(record['is_secret']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentVideo` into TDLib JSON wire shape. */
export function encodePushMessageContentVideo(value: PushMessageContentVideo): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentVideo';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video'] = value.video === null ? null : encodeVideo(value.video);
  out['caption'] = value.caption;
  out['is_secret'] = value.is_secret;
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentVideoChatEnded = PushMessageContent — A video chat or live stream has ended */
export class PushMessageContentVideoChatEnded {
  type: 'pushMessageContentVideoChatEnded' = 'pushMessageContentVideoChatEnded';
  extra?: string;
}

/** Decode a TDLib `pushMessageContentVideoChatEnded` object; null input -> null. */
export function decodePushMessageContentVideoChatEnded(json: TdJsonRaw): PushMessageContentVideoChatEnded | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentVideoChatEnded = new PushMessageContentVideoChatEnded();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pushMessageContentVideoChatEnded` into TDLib JSON wire shape. */
export function encodePushMessageContentVideoChatEnded(value: PushMessageContentVideoChatEnded): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentVideoChatEnded';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pushMessageContentVideoChatStarted = PushMessageContent — A video chat or live stream was started */
export class PushMessageContentVideoChatStarted {
  type: 'pushMessageContentVideoChatStarted' = 'pushMessageContentVideoChatStarted';
  extra?: string;
}

/** Decode a TDLib `pushMessageContentVideoChatStarted` object; null input -> null. */
export function decodePushMessageContentVideoChatStarted(json: TdJsonRaw): PushMessageContentVideoChatStarted | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentVideoChatStarted = new PushMessageContentVideoChatStarted();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  return out;
}

/** Encode a `pushMessageContentVideoChatStarted` into TDLib JSON wire shape. */
export function encodePushMessageContentVideoChatStarted(value: PushMessageContentVideoChatStarted): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentVideoChatStarted';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  return out;
}

/** pushMessageContentVideoNote = PushMessageContent — A video note message */
export class PushMessageContentVideoNote {
  type: 'pushMessageContentVideoNote' = 'pushMessageContentVideoNote';
  extra?: string;
  /** video_note: videoNote — Message content; may be null */
  video_note: VideoNote | null = null;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentVideoNote` object; null input -> null. */
export function decodePushMessageContentVideoNote(json: TdJsonRaw): PushMessageContentVideoNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentVideoNote = new PushMessageContentVideoNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.video_note = decodeVideoNote(record['video_note']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentVideoNote` into TDLib JSON wire shape. */
export function encodePushMessageContentVideoNote(value: PushMessageContentVideoNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentVideoNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['video_note'] = value.video_note === null ? null : encodeVideoNote(value.video_note);
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushMessageContentVoiceNote = PushMessageContent — A voice note message */
export class PushMessageContentVoiceNote {
  type: 'pushMessageContentVoiceNote' = 'pushMessageContentVoiceNote';
  extra?: string;
  /** voice_note: voiceNote — Message content; may be null */
  voice_note: VoiceNote | null = null;
  /** is_pinned: Bool — True, if the message is a pinned message with the specified content */
  is_pinned: boolean = false;
}

/** Decode a TDLib `pushMessageContentVoiceNote` object; null input -> null. */
export function decodePushMessageContentVoiceNote(json: TdJsonRaw): PushMessageContentVoiceNote | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushMessageContentVoiceNote = new PushMessageContentVoiceNote();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.voice_note = decodeVoiceNote(record['voice_note']);
  out.is_pinned = asBool(record['is_pinned']);
  return out;
}

/** Encode a `pushMessageContentVoiceNote` into TDLib JSON wire shape. */
export function encodePushMessageContentVoiceNote(value: PushMessageContentVoiceNote): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushMessageContentVoiceNote';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['voice_note'] = value.voice_note === null ? null : encodeVoiceNote(value.voice_note);
  out['is_pinned'] = value.is_pinned;
  return out;
}

/** pushReceiverId = PushReceiverId — Contains a globally unique push receiver identifier, which can be used to identify which account has received a push notification */
export class PushReceiverId {
  type: 'pushReceiverId' = 'pushReceiverId';
  extra?: string;
  /** id: int64 — The globally unique identifier of push notification subscription */
  id: string = '';
}

/** Decode a TDLib `pushReceiverId` object; null input -> null. */
export function decodePushReceiverId(json: TdJsonRaw): PushReceiverId | null {
  if (json === null || json === undefined) {
    return null;
  }
  const record: Record<string, TdJsonRaw> = asRecord(json);
  const out: PushReceiverId = new PushReceiverId();
  if (typeof record['@extra'] === 'string') {
    out.extra = record['@extra'];
  }
  out.id = asInt64(record['id']);
  return out;
}

/** Encode a `pushReceiverId` into TDLib JSON wire shape. */
export function encodePushReceiverId(value: PushReceiverId): Record<string, TdJsonRaw> {
  const out: Record<string, TdJsonRaw> = {};
  out['@type'] = 'pushReceiverId';
  if (value.extra !== undefined) {
    out['@extra'] = value.extra;
  }
  out['id'] = value.id;
  return out;
}
